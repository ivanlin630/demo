# A 級族②（儀器）兩條先查（2026-09-02，只查不修）
#
# ═══════════════════════════════════════════════════════════
# ★★★#19 `reeval_attribution_bed` 死亡偵測 false-positive ⇒ 【已經好了，可銷案】
# ═══════════════════════════════════════════════════════════
# 條目描述的病：「單次 `state.teams` dict 查無即判死 → Team18 tick7239 瞬間 remove-readd 被誤判永久死亡」
#   舊樣式 `spec_death_tick == -1 and not spec_last.is_empty()` ⇒ ★窮盡 grep 該檔 = 0 次出現
# ★現行 code（`reeval_attribution_bed.gd:57-63`）：
# ```
# spec_gone_streak = 0   # 重現 → 歸零（濾 remove-readd 瞬態）
# …
# if spec_death_tick == -1 and spec_gone_streak >= _death_confirm_ticks:
#     spec_death_tick = spec_gone_start   # 連續 N tick 查無 → 確認死於起點
# ```
#   `_death_confirm_ticks = TimeScale.TICK_PER_DAY`（:41）⇒ ★要【連續一整個遊戲日】查無才判死
# ★修它的 commit：`a67e9682`（2026-07-15）
# ⇒ ★★條目寫於 2026-07-14，★★★【隔天就修好了】—— 而條目留到今天
#   （★與 #30 `_calc_reserve` 完全同形：問題早沒了，條目還在）
# ⇒ ★與族②同根？【不是】。#36 是 arm 順序；#19 是判死判準（連續性），兩者無關。
#
# ═══════════════════════════════════════════════════════════
# ★★#36 床的 arm 順序：真實剩餘【不是 272】
# ═══════════════════════════════════════════════════════════
# ★母體寫死：`docs/process/bed-arm-whitelist.txt` 的非註解行 ＝ 272（檔案全部存在，0 缺）
#
# ★★方法選擇（systems 要我判並寫理由）：**靜態先篩**，不抽樣、不全跑
# ```
# ①全跑 272 張：★貴（單張床動輒數十秒～數分鐘），而其中【137 張根本不走 GameSetup.setup】
#    ⇒ 對它們跑一輪只會得到「沒有 setup 所以沒有 setup 盲區」——花錢買一個已知答案
# ②抽樣：★★這題不適合。盲/不盲是【逐床性質】不是分布量；抽樣只能給比例，不能給名單
#    而 blueprint 要的是【母體縮不縮】＝要名單
# ③★★★靜態篩：能把 272 收斂到一個【可以逐張人讀】的小集合，然後才需要人判
# ⇒ 選③。★而它的限就是靜態的限（見文末）
# ```
#
# ★★★漏斗（★剝掉註解之後；見下「我自己的第一版是錯的」）
# ```
# 母體（白名單）                    = 272
# ①有 `GameSetup.setup()` 呼叫      = 135    （★其餘 137 張手工組世界 ⇒ 沒有 setup 可盲）
# ②其中有碰 Probe                   = 105    （★其餘 30 張完全不碰 ⇒ arm 順序對它們無意義）
# ③其中有 arm/reset                 = 102
#    ├ arm 在 setup 【前】＝正確順序 = ★90
#    ├ arm 在 setup 【後】           = 9
#    └ UNRESOLVED（arm 與 setup 不同函式，靜態判不出序）= 3
# ⑤arm 在後【且】讀 Probe 資料      = 9      ★「真盲候選」的【上界】
# ```
# ⇒ ★★★**272 → 9**（上界），而不是「272 張都是盲區」。
#
# ═══════════════════════════════════════════════════════════
# ★★★而那 9 張逐張讀完：**沒有一張是盲的**
# ═══════════════════════════════════════════════════════════
# ★關鍵區別（★這是判準的核心，不是細節）：
#   **「arm 在 setup 之後」有兩種完全相反的意思**——
#   ①★盲：床要量的東西【發生在 setup 期間】，而它被 reset 洗掉了（例：TERRAIN_WEIGHTS）
#   ②★★刻意：床要量的是【跑起來之後】的事，reset 是為了【排除 setup 噪音】
#   ⇒ ★★★而它們在 code 上長得【一模一樣】—— 只有讀「這張床在量什麼」才分得開
#
# 逐張（9/9）：
#   observability_path_test  :70 註解自己寫「★啟 Probe：驗 tracer re-query 不 bump 污染」⇒ ②刻意
#   qty_tap_bed              量 quantity tap ⇒ 跑起來之後的事 ⇒ ②
#   s3_perf_flatten_bed      量 burst tick dt 是否變平 ⇒ ②
#   s3_tier_interval_bed     量【觸發間隔】⇒ ②
#   s5_poll_unique_value     量輪詢獨特貢獻率 ⇒ ②
#   s5c_hunger_fatigue_bed   量飢餓/疲勞事件的時點分佈 ⇒ ②
#   s6_build_days_truth      量一座 farm 工地實花幾個遊戲日 ⇒ ②
#   seam3_sysreg_test        registry characterization（兩次 setup 對照）⇒ ②
#   specimen_bed             specimen 決策 tracer ⇒ ②
# ⇒ ★★★**已知真盲 = 0 張**（唯一那張 `s7_rootdiff_bed` 今天已被 measurer 修好，見下）
#
# ═══════════════════════════════════════════════════════════
# ★而我第一版是錯的 —— 陽性對照抓到它
# ═══════════════════════════════════════════════════════════
# ★對照物：`s7_rootdiff_bed`（★今天已被修好 ⇒ 它【不該】出現在候選名單裡）
# ★★第一版：它出現了 ⇒ 判準有問題
#   追因：它 :14 的【註解】寫著「arm Probe在GameSetup.setup()之前」
#   ⇒ ★★★我的樣式把【註解裡的 `GameSetup.setup()`】當成呼叫 ⇒ min(setup)=14 < min(arm)=17 ⇒ 判成「arm 在後」
# ⇒ 剝掉 `#` 之後的內容重跑 ⇒ 它消失，數字 10 → 9
# ★★這是我今天第五次踩「註解自成一欄」——而這次是【對照物】抓到的，不是我自己看出來的
#
# ═══════════════════════════════════════════════════════════
# ★★★而我要報一件我自己造的：**閘的那句話是錯的**
# ═══════════════════════════════════════════════════════════
# `bed_arm_gate.gd` 印：「★白名單 272 張 ⇒ 這就是【還沒治好的盲區規模】」
# ★而本輪量到：272 張裡 ★★【90 張已經是正確順序】、137 張根本沒有 setup、30 張不碰 Probe
# ⇒ ★★★「白名單張數」量的是【沒遷去用 helper】，不是【盲】—— 兩個是不同的母體
# ⇒ ★而我是那個閘的作者：這句話是我寫的，它把一個【遷移進度】說成【盲區規模】
# ★★建議（閘是我的，改法待 systems 點頭）：把那一行改成印【分解】而不是一個總數：
#   「白名單 272（其中：無 setup 137／不碰 Probe 30／arm 順序已正確 90／arm 在後 9／未解析 3）」
#   ⇒ ★★★遷移進度仍然看得見，而「盲區規模」不再被一個數字冒充
#
# ═══════════════════════════════════════════════════════════
# ★誠實限
# ═══════════════════════════════════════════════════════════
# ①★靜態序判只在【arm 與 setup 同一函式】時有效；不同函式的 3 張標 UNRESOLVED，★★沒有硬塞
#    （`headless_test` ／ `s4b_wake_coverage` ／ `unified_rank_calls_probe`）
# ②★★剝註解用的是「`#` 之後全丟」⇒ ★★★字串字面量裡的 `#` 會被誤剝（GDScript 無 block comment）
#    本輪 9+3 張我逐張看過，沒有這種情況，但它是這個方法的已知弱點
# ③★「②刻意」的判定是【讀出來的】：我讀的是床的檔頭與 reset 那行的註解，不是跑它
#    ⇒ ★★若某張床的檔頭寫錯了自己在量什麼，我會跟著錯
# ④★★★「真盲 = 0」的意思是【在這 272 張裡、用這個判準、我沒找到】——
#    不是「不存在」。★真正的判準（「這張床要量的東西發生在 setup 嗎」）不可機械化。
# ⑤只查不修：production diff = 0、床 diff = 0、known_issues 一字未改。
