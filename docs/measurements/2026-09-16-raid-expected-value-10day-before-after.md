# 掠奪走期望價值 ｜ 10 天窗 before／after（2026-09-16）

**before**：`docs/measurements/2026-09-16-raid-before-10day-raw.txt`，樹 **`225dd3960`**
  （＝票的 base `b6727627e` ＋ **只帶儀器**：`dispatch.掠奪.*` 那一行）
**after**：`docs/measurements/2026-09-16-raid-after-10day-raw.txt`，樹 **`0755f2365`**
  （＝base ＋ 掠奪期望值 ＋ **攻擊的 loot 項也乘 `effective_loot_rate`**）

**兩輪同條件**：`SC_TICKS=14400`（10 天）／`SC_EARLY_DAYS=7`／seed **1337**／`warring_states`／
`GODOT_TIMEOUT=3600`／兩輪皆 `status=completed`／`[FAIL] 數 ＝ 0`／`clean=yes`。
★**單 seed 單窗** ⇒ **方向可看，量級不可引用**。

---

## A. systems 的 merge 判準那一格

```
                     before        after
掠奪 真的被派出去      **47**         **5**      （no-op 398 → 268）
攻擊 真的被派出去        6            2         （no-op 659 → 381）
決策母體 `rank.winner_all.__total`  6562        6555   ★**幾乎相同** ⇒ 不是「決策變少了」
```

★**裁定的回報條件是「掉到 0 ⇒ `ref` 選錯了」** —— **它沒有掉到 0。**
★★**但我不會只報這一句**：**47 → 5 是 −89%**，而 **6 → 2 是 −67%** ——
⇒ ★★★**兩邊一起掉**，而那與本票的內容一致：**這一票對【兩個】即時收穫項都套了同一個折扣。**

## B. ★我預先登記過的另一個解釋，**被這組數字排除了**

我在開跑前寫過：
> 「若 after 掉到 0，②的 dump 會給出另一個候選解釋 —— 不是 `ref` 選錯，是**攻擊把掠奪整個吃掉了**。」

⇒ ★**那個解釋不成立**：**攻擊自己也掉了**（6 → 2）。
⇒ ★★所以這不是「一個 option 吃掉另一個」，是**兩個暴力 option 一起從 argmax 的前段退下來**。

## C. ★★★而【為什麼退下來】這一句，**本文件不下結論**

```
同一輪裡別的 option 也動了（逐 option 派出，before → after）：
   迎戰 645 → 803｜貿易 88 → 424｜囤貨 236 → 148｜建設 767 → 525｜徵收 1005 → 908
```
★**這些不是「本票改的東西」** —— 世界在 10 天裡**走了不同的路**（單 seed）。
★★**而「暴力少了、貿易多了」是一個【故事】，不是一個【量測】** ——
⇒ ★★★**下 behavior 因果結論要先有 specimen trace → QA 故事稽核**（用戶 2026-07-22 定的硬規則）。
**本文件只報【數】與【判準那一格】。**

## D. 對帳與已知限制

```
(4a)/(4b)：before 7678／28（母體 7706）  after 4914／61（母體 4975）★對帳兩輪皆成立
★母體本身掉了（7706 → 4975）⇒ **「優先序不足」這件事發生得少了** ——
   ★★而那同樣是【世界走了不同的路】的後果之一，不可單獨引用。
據點：before 20／after 19｜PRODUCE：before 3／after 1（★兩輪皆 day 4 首現 1）
```

★**誠實限**：
- **單 seed 單窗** ⇒ **存在性可引用**（掠奪 **仍然發生**：5 次，不是 0），**比率不可引用**。
- 本輪**無 specimen trace** ⇒ ★**不得用它下任何 behavior 因果結論**。
- `odds` 兩邊仍共用同一個**盲**的贏率（不讀對手）⇒ 那是 `odds-must-read-the-target` 那張票。
