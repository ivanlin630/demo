# S7 第一步：母體切分 —— ★而【母體定義本身要先訂正】
#
# ★★★票的前提：「80 顆 inline gate-ok ⇒ bare-tick-gate 的綠是那 80 個判斷撐的」
#   ⇒ ★實查：【兩個閘被混成一個】。
#
#   bare-tick-gate.sh   grep -c 'gate-ok' = ★0   （它完全不讀 gate-ok）
#   constitution_gate.gd grep -c 'gate-ok' = ★10 （gate-ok 是【憲法閘】的豁免標記）
#
#   ⇒ bare-tick 的「已結案」來自 scripts/debug/bare_tick_triage.gd 的【41 條規則】，
#     不是 inline gate-ok。★★兩個母體要分開盤，否則第二步會盤錯對象。

═══════════════════════════════════════════════════════════
母體 A｜inline `gate-ok`（★憲法閘的豁免，不是 bare-tick 的）
═══════════════════════════════════════════════════════════
總數 80（含 debug）＝ production 68 ＋ debug 12
  ★而 debug 那 12 顆裡有 10 顆在 constitution_gate.gd 自己的【規則說明文字】裡
    （:8 :12 :14 :39 …），★★不是被標記的 code ⇒ 真正的標記母體是 68 + 2(means_end_s3_test) = 70
  ⇒ ★★★「80」這個數包含了【閘自己的文件】。母體定義先修，再談內容。

## ★A 的時間語意切分（★這是票要的 N/M）
```
N（時間語意，tick 數當時長用）= ★3
M（非時間語意）              = 65
N + M = 68  ✓ 對帳（production 母體）
```

### N 的 3 顆逐一（★第④欄：有守衛嗎 / 會不會紅）
```
①faction_ai_system.gd:608
   if current_tick < team.residency_eval_next_tick: return   # gate-ok: guard early-return
   ★語意：cadence 排程比較 —— ★★兩邊都是 world tick ⇒ 換根【自動跟隨】，不漂
   ★★★第④欄 = (b) 安全（而它被標 gate-ok 的理由是「guard early-return」，與時間無關）

②faction_ai_system.gd:6160
   if current_tick - team.occupying_outpost_since >= OUTPOST_TAKEOVER_DAYS * TICKS_PER_DAY
   ★語意：天數 × 根 ⇒ ★★已導出，換根自動跟隨，不漂
   ★★★第④欄 = (b) 安全

③faction_ai_system.gd:6520
   if days_since > CONTACT_TIMEOUT_DAYS    （days_since 來自 _contact_elapsed_days）
   ★語意：單位是【天】而非 tick ⇒ ★★不隨根漂
   ★★★第④欄 = (b) 安全
```
⇒ ★★**母體 A 的時間語意只有 3 顆，而三顆【都已導出或本來就是天】⇒ 沒有 (a)、沒有 (c)。**
⇒ ★★★**票裡擔心的「80 顆判斷全部做於換根之前而沒被重判」——**
   **對【憲法閘】而言成立（它們判的是 god-view/rng/world-mechanic，與根無關），**
   **而對【時間漂移】而言，母體 A 幾乎是空的。**

═══════════════════════════════════════════════════════════
母體 B｜bare_tick_triage 的 41 條規則（★這才是 bare-tick 的「有人判過」）
═══════════════════════════════════════════════════════════
```
規則總數 = 41
含時間語意者 = ★27
非時間語意者 = 14
27 + 14 = 41 ✓ 對帳
```
### ★而「一顆都沒被重判」在母體 B 上【不成立】
```
:43-51 有【明確為 S2 換根而寫】的規則：
  _mk("const BASE_ACTION_TICKS[^=]*=\s*[0-9]+", "b_defer",
      "★意圖是 1/6 小時…舊根下 TICKS_PER_HOUR/6 = 10//6 = 1，整數除法把動作壓成 1 tick…
        ⇒ 必須與 S2 同時落地")
  :49 「它是為【S2 尚未落地的 main】寫的，S2 一 merge 它就該死」
⇒ ★★這些規則【知道有換根這件事】，且部分是換根【期間】寫的
⇒ ★★★所以正確的說法不是「一顆都沒重判」，是
   【41 條裡有多少條的判斷前提是相對於舊根說的】—— 而那是第二步要逐條查的。
```

═══════════════════════════════════════════════════════════
★給第二步的建議（★而決定權在 systems）
═══════════════════════════════════════════════════════════
```
★母體 A（68 顆 gate-ok）：時間語意只有 3 顆且都安全 ⇒ ★★不需要第二步
   —— 但它有【另一個】老化問題：那 68 顆判的是憲法語意（god-view/rng/dispatch），
      而那些判斷的前提是【當時的決策架構】。★★★那是另一條線，不是這一條。
★★母體 B（41 條規則 / 涵蓋 171 顆候選）：27 條時間語意 ⇒ ★★★第二步應該盤這裡
   建議切法：27 條裡先查【白名單類】(c_whitelist)——它們是「判過且放行」，
   ＝ 票裡說的 (c)「有守衛但已標，看起來被蓋到、實際上放行」那一格。
```
# ★窮盡：全域 grep，無 head 無 glob；每個數字先 `wc -l`／`grep -c` 再列項。
