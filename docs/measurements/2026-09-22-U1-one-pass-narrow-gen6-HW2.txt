# ① 一圈形態（★窄形狀：只合併走訪、不共用任何計算結果）—— 規模量測（★只量不判）
# 四要素 ＋ 硬體戳 ＋ 世代戳
#   窗   ：8 天（GS_DAYS=8；真天 TICKS_PER_DAY=1440）｜種子：1337 與 77｜config：warring_states
#   樹   ：634371248（origin/main）＋ 量測樁（拋棄樹 A:/GDS/_gt4，★樁不進 main）
#   硬體 ：HW-2 AMD Ryzen 7 5800X3D 8-Core｜世代：★世代 6（邊界 3fb2c3201）
#   ★★同一輪跑出來的數字與 U2 卷面同源（同一個 log），★沒有另外再跑一次

## 卷面
  seed  T_seg(s)   T_calc   T_catch    L(窄形狀)     S      U1
  1337    53.934   15.178    15.630      42.9%    0.587   9.45%
    77    49.845   13.934    14.413      43.1%    0.573   9.24%

L  ＝ (T_seg − T_calc − T_catch) / T_seg   ← ★窄形狀：catch 不共用 ⇒ 它算在【計算】側
S  ＝ 那幾段佔 gather.*
U1 ＝ S × [ L×25% ＋ (1−L)×9.4% ]（systems 預註冊的換算式，未改）

## ★母體衛生（systems 指定要印：塌了就作廢，不是答案）
四條走訪圈數：seed1337 各 206119 → 本輪【同一套樁、無 stub】⇒ 母體未塌
gather 呼叫：9237（1337）／8707（77）｜最後隊數 103／94
★上一輪 (乙) stub 對照法自己否掉自己的血證：stub 後圈數 206119 → 10422（塌 20 倍）
  ⇒ 本輪用 (甲) 計時法，圈數與無樁基線同量級

## ★★★誠實限（★這一格會改變怎麼讀那個 9.45%）
①L 是【上界】：迴圈體內我沒有具名包住的計算（has_belief／belief_pos／estimate_armed／
  評分算式）被算進【走訪】那一側 ⇒ 真 L 比 42.9%／43.1% 小。
②U1 對 L 【單調遞增】（係數 0.25 > 0.094）⇒ ★U1 也是上界
  ⇒ ★★★所以「落在門檻 10.0% 下方」這個結論站在【安全那一側】——
    與 systems 在 U2 用的同一條推理（真省 ≤ 上界）。
③窗敏感：同一套樁在【1 天窗】量到 L ＝ 56.1%，而 8 天窗是 42.9%
  ⇒ ★短窗數字不可引用（母體不同：隊數與 discovered 還沒長開）。
④L_calc 這一欄在世代 6 比世代 5 小（29%⇒28.2% 的 catch 佔比變化見 U2 卷面）：
  ★世代 6 拿掉了 observed_speed 的 randf ⇒ score 那一支變便宜 ⇒ ★★跨代不可引用。
⑤未量【寬形狀】（合併走訪＋共用計算結果）—— 照 systems 指示，它與另一張票重疊。
