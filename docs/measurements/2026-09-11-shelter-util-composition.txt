# 「收留」為什麼從不贏 —— ★組成（不是只印最終 util）
# 床：scripts/debug/registration_verbs_bed.gd｜worktree `.worktrees/converge2`｜seed 1337｜預設 config 未改
# ★★★兩份數字來自兩個窗（★母體不同不可混算）：
#   輸給誰／名次 ＝ **60 遊戲天**（候選 515 次）｜組成 ＝ **30 遊戲天**（母體 15 筆，只在 applicable 時刻）
# ★★Probe=ON；而「單幀裡有多少是儀器」目前**不知道**（OFF 那趟被 contention 污染，量測員重跑中）

=== ① 組成（30 天窗，母體 15 筆）===
drive **0.942** → ×weight → **0.481** → ×coeff → **0.347** → ×failure → 末端 **0.347**
逐筆（前 3，數字一致到小數三位）：
  {drive 1.000, weight 0.517, after_weight 0.517, coeff 0.677, after_coeff 0.350, fail_mult 1.000, final 0.350}
  {drive 1.000, weight 0.517, after_weight 0.517, coeff 0.691, after_coeff 0.357, fail_mult 1.000, final 0.357}
  {drive 1.000, weight 0.517, after_weight 0.517, coeff 0.705, after_coeff 0.364, fail_mult 1.000, final 0.364}

★掉最多的兩步：**×weight（0.942→0.481，×0.517）** 與 **×coeff（0.481→0.347，×0.69–0.71）**
★★而兩者都是**既有機制**，不是異常壓扁：
  `weight("shelter")` ＝ 0.3 ＋ 貪婪×0.5 − 慎重×0.2（★人格調變，本票新寫但落在既有形狀）
  `coeff` ＝ `NeedHierarchy.consistency_coeff`（★需求層級一致性，全 23 option 共用的同一把尺）
★★★`fail_mult` ＝ **1.000**（沒有失敗記憶在壓它）⇒ **沒有任何一步把它「壓扁」到不成比例**。

=== ② 它輸給誰（60 天窗，候選 515 次、贏 0 次）===
名次分布：第3名×5、第4名×9、**第7名×256、第8名×142、第9名×103**
輸給誰：**返家補給×302**、**maintain_tools:resource×180**、貿易×12、乞食×12、遷移找糧×5、徵收×2、外交×1、迎戰×1
並排（同 tick 同隊）：{tick 11280, team 32, winner 貿易 u=0.624 vs shelter 0.350, rank 3/8}

=== ③ ★★★落進哪一個預註冊結局 ===
★**(a) genuine** —— 而理由是三個數字並排：
  ①**沒有異常壓扁**：兩步各自是既有機制、`fail_mult`＝1
  ②**它的末端 util 0.35 與贏家 0.62 差的是【價值】不是【量級】**（同一個尺上的 1.8 倍，不是數量級）
  ③**它輸給的是「補給／維持」類**（返家補給 302／maintain_tools 180）⇒ ★領主在忙自己的供給
⇒ ★而**不是 (b) 度量衡病** —— ★★若是量級問題，末端會差一個數量級（像先前那顆假單價差 500 倍），而這裡沒有。
⇒ ★★★所以下一步**不是修接線，也不是 crank**：★要不要接受「亂世的領主沒空收人」**是 blueprint 的裁量**。

# 誠實限
# ①組成母體 **15 筆**（30 天窗）—— ★★不是小到不可判（≥10），但也**不足以談分布**，只談中央值
# ②本檔**沒有** >2 秒幀數（★那顆計數是在這兩趟之後才接上的）⇒ 下一份交件起一定帶
# ③`求居` 贏 82 次但**不產生登記**；6 支房客**全部 ←stub** ⇒ 驗收② 仍然是**紅**（照裁定保持）
