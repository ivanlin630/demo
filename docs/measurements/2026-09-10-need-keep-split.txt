# 「我夠不夠」再拆 —— ★★★答案：`effective_holding` 不是問題，**`need_keep` 才是**（24.95 ms／次）
# 床：scripts/debug/option_term_cost_bed.gd｜warring_states｜3000 tick｜seed 4242｜Probe=ON
# ★碼表走 `SimRunner.phase_timing`（界限 43）｜★★母體地板在算式之前（界限 44）｜★★★跑間噪音大 ⇒ 只談比例

段                                    次數        總計(s)        us/次
①-a `effective_holding`             3507         0.136         38.7      ← ★可以直接排除
①-b `need_keep`（＝下面三個加數）      3507        87.511      24953.2
　├ `_self_use`                     39186         0.659         16.8
　├ `_supply_chain`                 39186       289.875       7397.4     ← ★三個加數裡最大
　└ `_construction_facility_need`   39186       115.446       2946.1
守恆：①-a ＋ ①-b ＝ 87.647 s vs 前置滿檢查段 87.647 s ⇒ **差 0.000 s**

# ★★★而三個加數的和（405.98 s）**大於** `need_keep` 段（87.51 s）—— 這不是壞掉，是**兩件事**：
#   ①**母體不同**：加數的母體是 **39186 次**（全 sim 的 `need_keep`），
#     而 `need_keep` 那一欄的母體是 **3507 次**（只有 goal_resolver 這條路進來的）
#   ②★★**巢狀**：`_supply_chain`（`need_oracle.gd:183`）**會回頭呼叫 `need_keep`**
#     ⇒ 它的 7.4 ms 裡**含著下一層的時間** ⇒ ★★★**跨層不可相加**（同今天那條）

# === 扇出（★「每次很慢」與「一次查詢被展開很多次」是兩個結論）===
`need_keep`：頂層 **26078** 次／全部 **39186** 次 ⇒ **每次查詢展開 1.5 次**
深度分佈：深度1 ＝ 26078／深度2 ＝ 11594／深度3 ＝ 1514；**最深 3 層**
⇒ ★**不是指數爆炸**（1.5 倍、三層封頂）⇒ 貴的仍然是**單次**，不是展開次數。
⇒ ★★而頂層 26078 次遠多於 goal_resolver 的 3507 次
  ⇒ ★★★**`need_keep` 是全 sim 共用的 oracle**，這條決策路只是它的其中一個消費者。

# ⇒ 依 systems 先寫死的規則（一段 ≥60% ⇒ 下一步是那一段）：
#   三個加數的總時裡 `_supply_chain` 佔 **71%** ⇒ **下一段是 `_supply_chain`**
#   ★而它是**遞迴源**（:183 逐配方產出回呼 `need_keep`）⇒ ★★下一刀要量的是它的**自身時間**（扣掉巢狀）
