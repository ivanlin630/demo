# 階段④ 第一刀：known_issues 存量條目【對 code 對帳】（只盤不修，known_issues 一行未改）
#
# ★校準物（systems 派票前給的）：`known_issues:653` gather 條目 —— 已知 stale
#   ⇒ ★★判準必須抓到它；抓不到就是母體漏了一整類。
#   ⇒ ★★★而它在本輪【兩度救了我】：v4 與 v5 都把它判成 OK，那兩版看起來都更「乾淨」。

═══════════════════════════════════════════════════════════════════
★母體：systems 給的是【上下界】28..130，而我把它縮到 36
═══════════════════════════════════════════════════════════════════
```
known_issues.md 條目（`## ` 標題）        ＝ 133
其中含【反引號識別符】                    ＝ 93     ← ★systems 的 130 上界屬這一層
其中含【file:line 錨】（可機械對帳）      ＝ ★36
錨的總數（一條可有多個）                  ＝ 145
```
★**為什麼可機械對帳的是 36 不是 130**：識別符只證明「條目提到某個名字」，
  ★★而「名字還在」不代表「條目說的還對」——**能機械判的是【錨指到的那一行還是不是那回事】**。
⇒ ★★★所以我把母體收在【有 file:line 錨】的那 36 條／145 個錨；其餘 57 條需人讀，明標未收。

═══════════════════════════════════════════════════════════════════
★★★判準演進（★六版，而每一版都跑得動、都印得出數字）
═══════════════════════════════════════════════════════════════════
```
v1 條目切分＋識別符存在性                  ⇒ ★根本抓不到校準物
   因為 :653 的 stale 是【語意】不是【結構】：`gather` 這個名字還在
v2 file:line 錨 → 檔名直接對應             ⇒ 31 條有問題，★但大量 FILE_NOT_FOUND 假陽性
   `faction_ai` / `reaction` 是【簡稱】、`read_market_board` 是【函式名當錨】
v3 ＋名稱解析（簡稱→_system／函式名→所屬檔）⇒ OK 45 ／ DRIFT 95 ／ UNRESOLVED 5
   ★校準物命中，但手工抽驗發現假陽性：`order_system:3` 的 ORDER_LIFETIME 其實在第 4 行（窗內）
   ★★真因：識別符抽取要求【整個反引號區塊是單一識別符】，
     而 doc 寫的是 `ORDER_LIFETIME = 5 * TICKS_PER_DAY`（含空白）⇒ 抽不到
v4 反引號區塊【斷詞】                       ⇒ OK 95 ／ DRIFT 45 ／ UNRESOLVED 5
   ★★★假陽性大降，★而校準物【變成 OK】—— v4 太鬆，把它漏掉了
   ⇒ ★若沒有校準物，我會在這裡停下，交出一份「乾淨」而錯的報告
v5 識別符只取【錨所在那一行】               ⇒ OK 70 ／ DRIFT 70 ／ UNRESOLVED 5
   ★校準物仍是 OK —— 追因：`team` 這種 4 字母泛用 token 讓【任何】程式碼窗都命中
v6 ＋只採有辨識度的 token                   ⇒ ★★★OK 52 ／ DRIFT 88 ／ UNRESOLVED 5
   （長度 ≥6 或含底線，且排除 team/state/tile/world/target… 等泛用字）
   ★校準物 = DRIFT ✓；★★v3 抓到的那個已知假陽性（order_system:3）已修正為 OK
```
★**六版的教訓不是「最後那個數字」**，是：
  ★★**v4 的數字最好看（假陽性最少），而它是錯的** —— 分辨它們的唯一東西是校準物。

═══════════════════════════════════════════════════════════════════
★★三態（★systems 明令不壓成二態）
═══════════════════════════════════════════════════════════════════
```
①★DRIFT（錨漂移）        ＝ 88 個錨
   意思：條目引用 `<file>:<line>`，而那一行【不再包含條目描述的東西】
②★OK                     ＝ 52 個錨
③★★★UNRESOLVED           ＝ 5 個錨 —— ★不硬歸類，列出來給 systems 判
   known_issues.md:597   `known_issues:17`      ← ★自我引用（指的是本檔行號不是 code）
   known_issues.md:659   `_calc_reserve:667`    ← 函式名，而該函式現在查不到
   known_issues.md:799   `force_ge_target:1109` ← 同上
   known_issues.md:919   `player_query:232`     ← ★簡稱解析不到（實檔 player_query_api.gd）
   known_issues.md:1715  `distortion:114`       ← ★同上（實檔 distortion_engine.gd）
   ⇒ ★後兩個是【我的解析器缺 suffix】不是條目的錯 —— 誠實歸在 UNRESOLVED 而不是 DRIFT
```

═══════════════════════════════════════════════════════════════════
★★★而這裡有一件比數字更重要的事
═══════════════════════════════════════════════════════════════════
```
★DRIFT ≠ 條目 stale
  它只證明【行號指錯了】。條目說的病可能仍然存在，只是 code 搬過位置。
★★而校準物 :653 是【語意 stale】（三項副作用中的 EWMA 已修）——
  ★★★我抓到它，是因為【它的錨也漂了】，不是因為我讀懂了它 stale。
⇒ 也就是說：★我的判準對【錨漂移】是機械的，對【語意 stale】是【碰巧】。
⇒ ★★而反過來也成立：一條【錨還準、但描述已經過時】的條目，本刀【看不見它】。
   （★★★這正是 :653 若當初只寫「gather 會推進 EWMA」而不附行號時的樣子。）
⇒ 所以這 88 個 DRIFT 是【要人看的清單】，不是【88 條 stale】。
```

═══════════════════════════════════════════════════════════════════
★誠實限
═══════════════════════════════════════════════════════════════════
①★母體只收有 file:line 錨的 36 條；其餘 97 條（含 57 條有識別符無錨）★未收，不是「查過沒事」
②★★DRIFT 的判定用 ±5 行容差 + 有辨識度 token —— 容差是我拍的，換一個數字結果會變
   ⇒ ★而我沒有掃過容差敏感度（本輪未做）
③★★★UNRESOLVED 裡有 2 個是我的解析器缺 suffix（`_api` / `_engine`）
   ⇒ 補上會讓 UNRESOLVED 降到 3，而 DRIFT/OK 可能各動一格 —— 我沒補，因為那會讓
     「解析器改一次、數字動一次」而我要留下這一版的可複驗數字
④只盤不修：production 0 行，★known_issues.md 一行未改（判定與改寫是 systems）
