# 票乙交件：錢＝手段（前置鏈）—— ★**而這條路今天不 fire，那是世界的答案**（2026-09-15）

worktree `.worktrees/coinprereq`｜branch `feat/coin-as-prereq`｜commit `faed4ae1b`
床：`scripts/debug/coin_as_prereq_bed.gd`（acceptance）｜seed 1337／1440 tick

## ① 三條硬約束（★逐條有驗收格，不是宣稱）

```
①**取代不是附加**：買不起 ⇒ **同一個 `return` 點**回「先弄到錢」candidate
   ⇒ 兩條互斥分支共用一個 return ⇒ **一次呼叫只可能走一條**（控制流保證）
   ⇒ ★床**不讀 code**：在真世界上對帳 `entry 2480／satisfied 706／buy_wins 165／earn 0`
②**量自己算**：`qty_gap × BASE_PRICE[res] − 手上 coin`
   ⇒ ★床斷言 `estimated_payroll` **不在這條路上** ＋ **陽性對照**（違規字串會被同一判準抓到）
③**PV 只排內部序**：床斷言 `return _mk_candidate`、**回傳裡不含 `_pv`**
```

## ② ~~這條路 0 次，而那個 0 是乾淨的~~ ★★★**就地撤回（2026-09-15 晚）**

```
★**舊的結論（留著，因為我們曾經相信過它）**：
   「買路走到 165 次，而預算 > 手上的錢 0 次；預算只佔現金兩成
     ⇒ 『不 fire』是【世界的答案】，不是【機制的失敗】」
★★**它錯在哪裡**：**那份數字的窗是 1440 tick ＝ 一個遊戲天** ——
   **第一天每支隊的起始糧還在** ⇒ `maintain_food` 全 satisfied ⇒ **沒人需要買**。
   ⇒ ★★★**我把【窗的性質】寫成了【世界的性質】** —— 而這一句正是当時講得最硬的那句。
★**30 天窗（同一支床、同一個 seed 1337）的實測**：
   買路走到 **21086 次**｜**預算 > 手上的錢 4268 次（20%）**｜
   **「先弄到錢」取代買 3851 次**（對帳：4268 ＝ 3851 ＋ 417 湊不出手段）
   `maintain_food` 出現 **`active` 3 支**｜A 母體 **15/17 真的走進買路**
⇒ ★★**所以「票乙 dormant」這個結論【作廢】** —— 它每天 fire 一百多次。
★★★**而多 seed 的界線**（systems 裁）：
   ・**存在性不需要多 seed**：「這條路會 fire」—— 3851 次在任何 seed 都不會變成 0
   ・**而「20%」「3 支 active」是【比率】⇒ 要多 seed 才可引用**。
```

## ②-b 当時那份數字（★仍然有用：它是【第一天】的真實寫照）

```
                    改之前            改之後
   單價 ＝ 0 的     **95 / 165**      **0 / 165**
   預算／手上的錢   中位 0.000        **min 0.087｜中位 0.190｜max 0.226**
⇒ ★改之前的 0 **混著一個儀器錯**（我拿 `local_value`＝「我覺得它值多少」當「我要付多少」）
⇒ ★★改之後：**這些隊的採購預算只佔手上現金約兩成**
⇒ ★★★**所以「不 fire」是【世界的答案】，不是【機制的失敗】** ——
  **不准把它讀成「接線沒接好」然後去把 util 往上調：那是灌水。**
```

## ③ 兩件今天做不到的事（★寫出來，而不是留白）

```
★**劫掠那條手段沒接**：它的收益要用 belief 估的敵方資產，**而那不是 coin 當量**
  （正是攻擊票未解的那一格）⇒ 接它就得手填一個換算係數 ⇒ **「估算器禁手抄物理」禁止** ⇒ 不接。
  ⇒ ★★**所以硬約束③（PV 只排內部序）今天【結構在、一次都沒被行使】** —— 內部只有一個候選。
★**板上的真實要價**是取價的正解下一步，**而它撞感知鐵律**（未到場就讀得到要價 ＝ god-view）
  ⇒ **歸貿易軌**；★理由寫在 code 那一行旁邊，**不是只寫在 spec 裡**。
```

## ④ fp：★**差異侷限在 `teams` 域，而域內我證不了**

```
逐域（`domain_fp_probe.gd`，main vs branch，同 seed 同窗）：
   **相同 5 域**：`belief`／`factions`／`persons`／`tiles`／`world` —— **逐字相同**
   **不同 1 域**：`teams`（＋彙總 `__all__`）
goal 筆數：**main 302 → branch 250，差 52**；★**而隊數是 63** ⇒ **只有 52 支隊有過那個 goal**
⇒ ★★**「行為沒變」有兩條獨立支撐**：5 域逐字相同 ＋ **新分支 165 筆裡一次都沒走進去**
⇒ ★★★**而【`teams` 域內是否只有 `goal_state` 變】本次未證** ——
  **寫出來的未證，跟證過的一樣有用；沒寫的未證才會變成別人日後的假前提。**
  （★證它要在探針端濾掉那一筆重算 `teams` 域；systems 裁**不做** —— 成本／收穫不對。）
```

## ⑤ ★順帶一個**待驗**（★不是本票的結論）

```
`maintain_*` 是冪等補齊的 ⇒ 理論上 63 隊 × 5 ＝ **315**，**而實測 302**
⇒ ★**有隊從來沒跑過目標生成**（最可能是節流窗未到的新生隊，**也可能不是**）
⇒ ★★**這是推論不是觀測** ⇒ **標待驗、不開票**（systems 已寫進 known_issues）
⇒ ★★★**而它會咬的是日後任何「每支隊都有 X」的假設。**
```
