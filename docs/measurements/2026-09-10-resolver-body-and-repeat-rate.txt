# `_resolve_build_facility` 身體四段 ＋ 同 tick 重複率
# 床：scripts/debug/option_term_cost_bed.gd｜warring_states｜3000 tick ＝ 2.1 遊戲天｜seed 4242｜Probe=ON
# ★碼表走 `SimRunner.phase_timing`（界限 43）；★★跑間噪音大 ⇒ 只談比例；★★★母體地板在算式之前（界限 44）

段                                              次數      總計(s)        us/次      佔整支
①`derived_payoff`                              1779      22.205      12482.0      27.2%
②`FactionAISystem.new()._find_own_outpost`     1779       0.102         57.4       0.1%
③`material/tools` 遞迴 `_resolve_resource_prereq` 2189      59.014      26959.5      72.4%
④尾段（判斷 ＋ `_mk_delegate_candidate`）         118       0.084        715.2       0.1%
守恆⑤：四段和 81.406 s（母體 1779 次）vs **整支** 81.562 s（母體 1779 次）⇒ 差 **0.156 s ＝ 0.19%**

★★★而上一趟這一格守恆差 **−31.5 s**，成因值得記：
  我拿四段（母體 **1779** 次）去比 `rbf_*`（**只涵蓋從 path 迴圈進來的 1116 次**）——
  ★`_resolve_build_facility` **另外還被 goal 迴圈直接呼叫** ⇒ ★★**兩個母體不同的量不可以相減**。
  ⇒ 修法＝加一支【涵蓋全部呼叫端】的整支碼表（就是上表右欄）。

=== 同 tick 內【同一組輸入】重複算幾次（★systems 要的第二件）===
`_resolve_resource_prereq`（鍵＝隊＋res）：77 個 tick／呼叫 **1318** 次／相異 **1290** 個
  ⇒ 重複率 **2.1%**（同一組輸入平均算 **1.02** 次）
`_resolve_build_facility`（鍵＝隊＋facility）：呼叫 **1779** 次／相異 **1550** 個
  ⇒ 重複率 **12.9%**（同一組輸入平均算 **1.15** 次）
⇒ ★★★**memo／同 tick 快取在這裡沒有價值**：即使 100% 命中也只回收約 2%／13%。
  ⇒ ★而這一格是**在有人動手寫快取之前**就把它擋掉（今天已經有一張票是這樣被作廢的）。

=== ★遞迴造成的重疊（★引用本表前必讀）===
呼叫圖是**互相遞迴**的：
  `_resource_prereq_candidates` → `_resolve_build_facility` → `_resolve_resource_prereq` → …
⇒ ★③ 那 59.0 s **包含**巢狀的 `_resolve_resource_prereq` 時間，而後者在別的表裡**也被記過一次**
  ⇒ ★★**跨層的段不可相加**；本表的守恆只在**同一層**成立（而它成立到 0.19%）。
