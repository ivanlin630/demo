# ⑦群 43 條歸檔：對帳與驗收（2026-09-02，implementer 執行／systems 審定規則／blueprint 准）
#
# ═══ ★母體先寫死（systems 要求）═══
# 來源 ＝ `docs/measurements/2026-09-02-knownissues-triage-grouping.txt` 的【群⑦ 已結案／純紀錄】欄
# 該欄 ＝ 43 條，行號清單：
# 53 68 708 728 736 775 846 867 1069 1078 1122 1133 1461 1519 1531 1537 1544 1551 1558
# 1563 1569 1576 1582 1600 1605 1622 1721 1724 1727 1785 1815 1828 1833 1997 2020 2041
# 2069 2105 2168 2185 2259 2296 2339
#
# ═══ ★★驗收①：對帳（三個數字都印）═══
# known_issues `^## `      140 → 97      −43  ✓
# archive 歸檔標記行                43      每條一行 ＝43  ✓
# archive `^### `           43 → 88      +45  ★不是錯，見下
#   ★★+45 的兩個多出來的是【搬走的 body 自己帶的 `### ` 子標題】：[1833] 1 個、[1997] 1 個
#   ⇒ 43 + 2 = 45 對得上
#   ⇒ ★★★結論：「### 數」對這次搬家【不是可靠的對帳量】（它會被 body 內的子標題污染）
#     可靠的是【每條一行的歸檔標記】＝43。★我把兩個數字都印，因為只印一個會看起來像錯。
#
# ═══ ★★驗收②：內容不得截斷（fence 守恆）═══
# known_issues ``` 86 → 66（−20）
# archive      ``` 0  → 20（+20）
# ⇒ ★總數守恆 86 = 66 + 20，★★且兩邊皆為偶數（fence 平衡）
#
# ═══ ★★★驗收③：抽驗 → 我做成全驗 ═══
# systems 要求隨機抽 5 條逐字比對。★我做了 43/43。
# 比對對象 ＝ `git show HEAD:docs/known_issues.md`（★不是我手上的副本——副本比對證不了搬家）
# 結果：★★43 條【全部逐行完全相同】；★★★0 條標題殘留在 known_issues。
#
# ═══ ★驗收④：hash 指得到真 commit（★而我要先講它的性質）═══
# A 型（8 條）＝ 條目【自己引用】且 `git rev-parse` 解得開的 commit
# B 型（35 條）＝ 【此條目寫入 known_issues 的那顆 commit】（`git log -S` 找的）
#   ★★★B【不是結案證據】—— 它是「這段文字是誰寫進去的」。我在 archive 檔頭寫死了這個區別。
#   ⇒ ★而這本身是個發現：★★35/43 的【已結案條目，內文根本沒有附結案 commit】。
#     （所以 blueprint 騎士①「留結案證據 hash」對【舊條目】是回填不了的，只能回填「寫入 commit」。）
#
# ═══ ★驗收⑤：merge-gates runner ═══
# 7 支／137s／全綠（constitution / bare-tick / build-duration / bed-parse / bed-arm / ki-anchor / mailbox-integrity）
#
# ★★★而 ki-anchor 這一支綠得【不對】—— 我自己造的涵蓋率破口：
# ```
# 搬前 known_issues：L1 60（相異 51）  L2 82  L3 37  L4 1
# 搬後 known_issues：L1 52（相異 43）  L2 79  L3 36  L4 1
# 搬後 archive     ：L1  8（相異  8）  L2  4  L3  1  L4 43★
# ⇒ ★逐型守恆：60=52+8 ／ 82=79+4−1(archive 原有 1) ／ 37=36+1
# ⇒ ★★L4 的 +43 【全是我自己寫的索引行「原 known_issues:NNN」】，不是搬過去的錨
# ⇒ ★★★而閘只掃 known_issues ⇒ 相異錨從 51【靜默掉到 43】而閘照樣印 PASS
#     —— 那正是「母體縮小看起來像通過」。
# ```
# ★處置：`known_issues_anchor_gate.gd` 改【雙目標】（known_issues ＋ archive），
#   ★★並在輸出印出兩份的字元數 ⇒ 母體縮小【看得見】。
#   ★★★這與 systems 已經做的「檢索義務改雙目標」是同一件事的機器面：人改了，閘也要改。
#   （改在 branch；main 的閘要等 merge 才閉合這個洞 —— ★誠實記，現在 main 上這個洞是開的。）
#
# ═══ ★★★systems 問的第③件：那 43 條有沒有被別的條目引用 ═══
# ★我查了【檔內】與【全 repo】的 `known_issues:NNN` / `known_issues.md:NNN` 形式：
#
# ①★檔內自我引用只有兩處：
#    known_issues:930 → `:18`  ⇒ 屬 [15]（群①，★沒搬）
#    known_issues:963 → `:55`  ⇒ 屬 [53]（群⑦，★★搬了）
#    ★★★而這一條【本來就已經指錯】：引用它的句子是 2026-07-05 的 V4 envoy 診斷，
#      而今天的 :55 落在一條 2026-08 才寫的條目裡 ⇒ 錨早就漂走了。
#    ⇒ ★我【沒有】把它改指 archive —— 因為我不知道它原本要指誰，改了等於憑空發明一個引用。
#      ⇒ 列給 systems 判（它 own 這份檔）。
#
# ②★檔外指到【被搬走的條目】的（live 檔，非 handback 歷史）：
#    `docs/process/01_architect.md:189`            → `known_issues:728` ⇒ [728] ★已搬
#    `docs/process/detail/01_architect-cases.md:1734, :1767` → `known_issues:728` ⇒ [728] ★已搬
#    ⇒ ★★這三處我【沒有改】：`docs/process/*` 是 systems own。
#      建議改成 `archive/resolved_issues.md`「武器經濟 arc 正式收斂完畢」（按標題找，不用行號）。
#
# ③handback／specs/_archive 裡的舊引用我【不動】：它們是有日期的紀錄，改了等於改歷史。
#
# ═══ ★誠實限 ═══
# ①★我在 main working dir 直接改 `known_issues.md`（systems 授權執行他的檔）——
#   ★★只 add 這兩個檔，沒有 `git add -A`（今天剛查過「掃入→revert 回退」那條）。
# ②★★分群本身是我讀出來的判斷（見 triage 落地檔），★★★搬家的正確性可機械驗（上面①②③），
#   但【該不該搬這 43 條】仍然是判斷 —— 而那個判斷 blueprint 准了。
# ③★行號在搬家後【全部位移】：known_issues 現有 36 個 L3 ＋ 1 個 L4 錨是【檔內指 code】的，不受影響；
#   ★★但任何【外部指 known_issues 行號】的引用都因這次搬家而失效 —— 而它們本來就在漂。
