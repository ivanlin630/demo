# 遞迴：一次頂層呼叫【之內】的展開與重複（★這種 memo 語意天然中性 —— 而它也被關掉了）
# 床：scripts/debug/option_term_cost_bed.gd｜warring_states｜3000 tick ＝ 2.1 遊戲天｜seed 4242｜Probe=ON
# ★碼表／計數走 `SimRunner.phase_timing`（界限 43）；★★母體地板在算式之前（界限 44）

頂層呼叫（`_resource_prereq_candidates`）：**1318** 次
子問題總展開（`_resolve_resource_prereq`）：**2826** 次 ⇒ 每次頂層 **2.14** 次
其中相異子問題（鍵＝隊｜goal_type｜res）：**2826** 個 ⇒ 每次頂層 **2.14** 個
⇒ ★★★**重複率 0.0%** —— 一次呼叫之內**沒有任何一個子問題被算第二次**。
深度分佈：**深度1 ＝ 1318**（全部）⇒ ★不是「深」，是「寬」：同一層順序展開 2.14 個不同子問題。

# ★★所以 memo 這條路【整條關掉】（第七個被排除的假說）
#   ①跨 tick／同 tick：重複率 2.1%／12.9% ⇒ 收益封頂 13%、且要付語意風險 ⇒ 不做
#   ②一次呼叫之內：重複率 **0.0%** ⇒ ★即使是最安全的那一種 memo（語意天然中性）**也沒有東西可省**
#   ⇒ ★★★而這一格【只花一支計數】就把整條路線關掉，而它本來看起來是最順的一條。
# ★鍵的說明：鍵含 `goal_type`，但一次頂層呼叫只服務一個 goal ⇒ 去掉 `goal_type` 結論不變。

# 同趟其他守恆（都成立）：resolver 四段 72.840 vs 整支 72.995（0.21%）；
#   迴圈身體四段 37.800 vs 37.813（0.03%）；fp `16bb6924789c18ac031e61d34c2aeae7` 不變。
