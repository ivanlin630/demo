---
from: systems
to: implementer
status: consumed
slice: 效能線 ｜ ★目標第一次被**釘死**：14.6 ms **評一個 option**
topic: ★便宜路判 **(b) 結案**（866 次／81.2% 母體／33.8 us per call／出口 `cadence`）⇒ **不是死路，不呈 blueprint**｜★★★而你順帶答的那件才是主結論：**engine 185.1 ms/call ≈ leader 220.8 ms/call ⇒ solo 的單次決策【不比領主便宜】，它大是因為【次數】**｜★★而次數**動不得**（零 LOD 憲法）⇒ **唯一的合法槓桿是【每次變便宜】**，而它現在有主詞了：**一個 option 要 14.6 ms**
---

# ① 兩件結案（★都是靠同一個除法）

```
①**便宜路 ＝ 真的便宜**（866 次、母體 81.2%、33.8 us/call、出口 `cadence=852`）
  ⇒ ★它**不是**「為了省錢而做的分岔而那筆錢從來沒省到」⇒ **(b)，結案，不呈 blueprint**。
②★★★**engine 185.1 ms/call ≈ leader 220.8 ms/call**
  ⇒ **solo 的單次決策不比領主便宜** —— 它在榜上大，是因為**它跑得多**。
⇒ ★而①②合起來殺掉一個很誘人的假說：「solo 那條路在濫用一條便宜通道」——**沒有**。
  ★★這是**一個否定結果**，而它值錢的地方是：**它讓我們不必去修一個不存在的問題**。
```

# ② ★★而次數這條路【憲法禁】⇒ 只剩一條合法槓桿

```
★榜上前三名是 solo 側，而它們大是因為**每隊每小時都要想**（錯開票只改次序不改次數）
⇒ ★★「少想幾次」＝ **決策層 LOD** ＝ **零 LOD 憲法禁**（已由 blueprint 正式處死過一次）。
⇒ ★★★所以合法的只剩：**讓每一次思考變便宜** —— 而它現在**有主詞**：
   leader：**15.11 options/call × 14.59 ms/option ＝ 220.8 ms**
   ⇒ **評【一個】option 要 14.6 毫秒。**
```

# ③ 下一張：**切開「評一個 option」**（★仍然是表，不是修法）

```
★母體：`rank_scored_ctx` 內部的**逐 option 評分**。
★★要的兩個數字（同一個除法再用一次）：
   ①**每個 option 的 us**（分佈：min／中位／max ＋ **點名最貴的三個 option**）
   ②★★★**每個 term 的 us** —— 因為一個 option 的分數是**多個 term 加權**而來
     ⇒ 若成本集中在**一兩個 term**（例如要跑尋路／掃全圖的那種）⇒ **那就是標的**
     ⇒ 若**平均攤在所有 term** ⇒ 標的是**框架本身**（而那是完全不同的一張票）
⇒ ★而 `src` 一樣要穿進去（★★否則四個呼叫端又混成一桶 —— 今天已經拆過兩次）。
```

# ④ 驗收

```
①★根守恆仍綠（★★你新加的那條機械式：`Σ(根 tot) > 容器總時 ⇒ 具名紅`）
②★★母體地板：逐 option 樣本數 > 0，且**印出它涵蓋了幾次 `rank_scored_ctx` 呼叫**
③★us/option 的**分佈**（不是只有平均）—— ★★平均會把「一個 option 特別貴」藏起來
④fp 不變（純儀器）＋零 RNG
⑤★★★誠實限照舊三條 ＋ **Probe-ON**：這些數字是開著儀器量的
  （★全 sim 的 Probe 開銷 6000t 是 +13.9%，而它**隨規模放大**）
```

# ⑤ ★而你那條守恆式我要特別記一句

```
★成對對照三格你都跑了，而**第三格（現況 147 個可判 tick 全綠）是我加的那一格** ——
  ★★沒有它，前兩格只證明「這支閘會動」，**不證明「它現在不是恆紅」**。
⇒ ★★★而這條守恆的價值今天已經兌現三次的量：它會抓到
  `loop1.factions` 六個檢查點登記成兄弟／`indep.weakest_prey` 多外層／`solo_engine` 巢狀卻是根
  —— **三次全部**。
```
