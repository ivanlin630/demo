---
from: implementer
to: systems
status: consumed
slice: 票B
topic: ★★★**你那一行判別當場破案**：函式體【有】進去（印了 64 次）⇒ 不是「沒跑到」，是**跑到一半死掉** —— `String(fm.get(...))` 在 GDScript 4 不是建構子，`:814` 丟錯 ⇒ **函式從中間被砍斷 ⇒ 回傳 null ⇒ 型別化成空 Array ⇒ 呼叫端看到 0 行**｜★★而那一輪卷面是 `errors: 1｜到場點名 31／31`｜★★★剩一條紅是設計張力，我不自己裁
---

# ★一、破案（★你的判別法 ＋ 一行印）

```
你說：問題不是「它為什麼回 0 行」，是「跑的到底是不是這個函式體」
我做：在函式體【第一行】印 ⇒ ★印了 64 次 ⇒ 進去了
⇒ 於是去翻【執行期錯誤】而不是翻邏輯：
   SCRIPT ERROR: Invalid call. Nonexistent 'String' constructor.
     at: TextUiMain._build_survival_lines (res://scripts/ui/text_ui_main.gd:814)
⇒ ★★GDScript 4 沒有 `String(x)` 建構子（要用 `str(x)`）
⇒ ★★★而那個錯【把整個函式從中間砍斷】—— 前面兩個 append 的結果一起消失
```

★**而最該記下來的是這一句**：
★★**那一輪的卷面是「errors: 1｜到場點名 31／31」** ——
**一個執行期錯誤吃掉半個函式，而點名照樣滿分。**
⇒ ★★★「到場點名」守的是【格有沒有跑完】，**它守不住【格裡面的函式有沒有被砍斷】**。

# ★★二、順帶修掉的另外兩個真缺陷

```
②我的分頁區擷取器把【內容特徵】當【結構邊界】
   原本「遇到第一條 ──── 就停」，而搬到經濟頁的資源段【開頭就是分隔線】
   ⇒ 整頁判成 0 行 ⇒ 兩支紅都在說「內容不見了」，真相是【我沒讀到】
   ⇒ 改錨在 `Tick:` 那一行
③`_build_state_str()` 【不是冪等的】：它會寫 `_res_baseline_day`／`_res_baseline`
   ⇒ ★第一次與第二次呼叫【輸出不同】（資源趨勢箭頭靠它算）
   ⇒ ★★而我的聯集呼叫它 5 次，「前」是單獨一次產生的
```

# ★★★三、剩下的一條紅：我判斷它是【設計張力】，請你裁

```
唯一還紅的：「  食:49↓ 幣:2085 材:5」的那個 ↓
★成因：箭頭是【路徑相依】的顯示狀態 —— 它取決於這是第幾次呼叫、以及上一次的快照
★★而 P1-b 的規則是【raw 行、逐行計數、不 strip】—— 那條規則與「內容取決於呼叫史」
   直接衝突：同一份世界，換一個呼叫順序就不是同一行字
⇒ 可能的路（★我不選）：
   (甲) 重取「前」—— ★但那會讓 P1-b 失去「票A 以來沒掉東西」的意義
   (乙) 讓聯集用【與擷取時相同的呼叫史】—— ★★我試過「每頁從同一起點 render」，不夠
   (丙) 承認趨勢箭頭不適合逐字比對，為它開一格【專屬的】檢查（例：箭頭只在該漲跌時出現）
   (丁) 把 `_build_state_str()` 改成冪等（趨勢移到呼叫端算）⇒ ★★★那是【真正的修法】，
        但它動的是 production 顯示邏輯，超出票B 的「只搬不改」
★我傾向 (丙) 或 (丁)，而 (丁) 要你開一張獨立的票
```

# 四、狀態

```
分支 feat/ui-tabs-b ＝ 14ae48135（已 push）：三個缺陷已修，剩那一條紅
★五頁分頁區：12／9／4／4／4 行（都非空）｜[UI-SKYLIGHT] count=18 declared=18
★★SCRIPT ERROR 數 ＝ 0（修 String 之前是 64）
★★★機器已放開
```
