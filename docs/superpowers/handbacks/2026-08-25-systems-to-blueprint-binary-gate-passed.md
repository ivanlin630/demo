---
from: systems
to: blueprint
status: open
topic: ★★文明化二值閘【自己過了】:l0_to_l1 從 0 → 1 —— 不是誰讓步,是世界真的動了;但先別慶祝,量測是 stale 的,重量令已發
---

# ★★這個世界第一次做出了「文明化」這個動作

`camp-access @ b968f492` 的漏斗：
```
root.won_argmax = 5  →  l0_to_l1_start = 4  →  resume = 2  →  ★ l0_to_l1 = 1
```
⇒ ★**`outpost.l0_to_l1 > 0` ＝ 綠。**

**對照**：普查說 90 天 outpost **`11 → 9`、中途新增 ＝ 0**（只減不增、文明化從未發生）。
**現在中途新增 ＝ 1。**
從「**紮根從未進入候選**」→「**會贏、會開工、會完工**」。

## ★這正是二值閘的意義，我想把這一點記下來
**我拒絕放寬自己設的門檻，你也沒有替我放寬。**
中間有一段時間它就那樣紅著，只是**把 owner 掛到真兇身上**（`§F1`）。
⇒ ★**最後不是誰讓步，是世界真的動了。**
**「二值、無旋鈕可假造」的設計，撐住了整段拉鋸。**

## ⚠️ 但先別慶祝：**這個數字是 stale 的**
implementer 自己抓到：measurer 量的是 `b968f492`，
而**`遷移找糧` 的 delay（我准的行為改動）落在 `e927be2f`**。
⇒ ★**世界層三條必須在 `e927be2f` 同床重量，重量前這只是 `@b968f492` 的主張，不是定論。**
**重量令已發**（併報 outpost 普查，看「中途新增 ≥ 1」能不能獨立佐證）。

★**他還給了一條我立成通則的旁證**：**`det fp` 相同 ≠ 行為沒變** ——
fp 在兩個 commit 都是 `880d3adf…`，但 **a4 那 1000 tick 床根本沒跑到遷移找糧**
⇒ **fp 只覆蓋「那張床實際跑到的路徑」**（`03b §④g`）。

## 其餘現況
- **headless ＝ 8 ＝ 我方 baseline，0-new**（且**比 main 少一條** —— 他把 `T1:覓食 base 恆 1.0` 的過期編碼修對了）
- ★**新法條測綠**：`join_flow 好=12.52 → 投靠 ／ 擠=5.33 → 紮營` ——
  **兩個場景只差 host 品質、排序真的跟著翻** ⇒ **秤的是流量，不是人格**。這條測的是**規律**不是結果。
- **②③ 近乎持平**：**②本刀本來就不該指望它動**（根在 A1 票 §3 的 ③(d)）⇒ 不當改善證據，但仍需維持綠
- **cap de-patch 准繼續**（31.4% 非近 0），但**要問的問題換了**：
  不再是「cap 吃掉鑑別度」（那是量綱造成的），而是「**1.5 倍餬口這個上界憑什麼**」
- 又立兩條通則：`③④f` tap 語意標籤（`Probe.note` ＝ **peak** 不是「最後一次」）／`④g` fp 覆蓋範圍

**下一站**：`eta-single-model` 已 push → **A1 建設族**。
★**implementer 指出的交集值得注意**：A1 的 ③(d) 假說**可能就是 ② 棄置率不動的同一顆病** ——
若分佈證實，**兩條線併成一顆**。
