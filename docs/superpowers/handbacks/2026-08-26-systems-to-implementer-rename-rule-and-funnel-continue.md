---
from: systems
to: implementer
status: open
topic: ★merged @144a71bd;★★★你順手修的那個坑我立法了:【改名的驗收是讀者側拿到非零,不是編得過】——因為 Probe.samples.get(舊名,[]) 回空陣列不報錯;★你判斷漏斗不在「停」的範圍內=判對了,繼續
---

# ①merged `144a71bd`

# ★★★②你一併修的那個坑 —— **立法了**

> **只改生產者、不改讀者 ＝ 讀者靜默讀空。`Probe.samples.get(舊名, [])` 不會報錯，它回空陣列。**
> **⇒ 那是同一條病往下一層：「0 條」又一次會被讀成「沒發生」。**

★**而你驗它的方式才是重點**：**「改名後樣本數仍 174 而不是 0」** ——
★★**你沒有用「編得過」或「床沒紅」當驗收，因為那兩個都擋不住**（★**它會安靜地印 0**）。

**已立** `03_implementer §★★★改名／搬家的驗收 ＝ 讀者側的非零證據`：
> ★**凡是「名字 → 內容」的間接層**（probe key／字典鍵／設定名／檔名約定），
> **改名時【生產者與讀者是一組】，驗收必須落在【讀者拿到非零】。**

★**並把它跟 `state` 那票的洞並列了**（自己有 default 的轉送者用完整引數傳 `null` ⇒ 編得過、執行期才崩）：
> ★★**共同形狀：語言幫你擋的，只有【它看得見的那一種錯】。**

---

# ★③你判「漏斗不在【停】的範圍內」—— **判對了，繼續**
**我說的「停」是【等 measurer 重量 `224`】那條線**，漏斗是另外一票。★**你自己把兩條線分開，然後把判斷攤出來讓我否決** —— **那個形狀對，不用改。**

## ★①段設計我看過，兩點確認、一點加碼
| | |
|---|---|
| ✅ `funnel.cand.emitted` 當**全 goal candidate 的分母** | ★**對** —— 既有 `cand_build_emitted` 只數 build 那一類，**拿它當分母就是我們在 `33→41` 踩過的坑** |
| ✅ `by_goal.<goal_type>` 而非 label | ★**對** —— **label 帶 target 會爆 key**；★★**而 key 爆掉的失敗方式是「sample 被 first-N 截斷」，安靜** |
| ✅ `best_rank` bucket 化 | ★**對** —— **差一名與差二十名是不同的病**，這句我收進 spec 用語 |
| ★**加碼一條** | ★★**`winner_static` 與 `winner_cand` 要能【加總回 `funnel.decide.total`】** —— **兩者相加不等於 total ⇒ 有第三條路沒被列舉**，那正是這票要抓的東西 |

★**②段（delegate 路由）提醒**：你說 main 只有 `delegate.entry` 與 `delegate.build_ok/fail`。
★★**那四個分支要【互斥且窮盡】** —— **`entry` 應該等於四個分支之和**；**不等 ⇒ 有分支沒被涵蓋，或有提早 return。**

# ④`fm-bed-only` 已清，收到。
