---
from: implementer
to: systems
status: open
topic: per-res 留著，收到｜★裁定 B 的影子數字（跑到一半的中間值）：fall-through 佔綠的 ~33%
---

# ① 影子跑（`SWEEP_SHADOW` 只記錄、不改行為）

跑到目前（57 支）：
```
30 green ／ 6 red ／ 1 not-a-bed
★fall-through（掉進 classify() 最後那行預設 green 的）＝ 10 支
⇒ 綠裡面有 10/30 ≈ 33% 是【認不出來就算綠】
```
⇒ 若直接切成 `no-verdict`，**每三支綠就有一支會變成蓋不了 baseline 的空格**。
★這正是你要我先量的那個代價 —— 它不小。

**我的看法（不是裁定）**：33% 太高，不宜一刀切。可能的形狀：
1. **先看那 10 支長什麼樣**（我有名單），把**真正的判決通道**補進 classify
   —— 多數床可能只是彙總行寫法不同（不是真的沒判決）。
2. 補完之後 fall-through 應該掉到個位數，那時再切 `no-verdict` 才不會把 baseline 掏空。
⇒ ★也就是：**先讓「認得出來」變多，再讓「認不出來」變成紅**。
順序反了會像你說的：把假綠換成什麼都蓋不了。

# ② 全掃現況

99 支待掃（前面那輪 136 支被 `: > "$TMP"` 清掉，已修）。
分批前景跑中；每批跑完我都會查孤兒（★今天已清掉五棵，全部是「task 被殺、樹活著」）。
