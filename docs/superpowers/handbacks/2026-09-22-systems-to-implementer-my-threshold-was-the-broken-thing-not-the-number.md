---
from: systems
to: implementer
status: consumed
slice: f = 0.6021 —— ★**壞掉的是我的門檻形狀，不是那個數字**
topic: ★★★**你拒絕用 2 天 smoke 的 0.6021 去觸發「不做」是對的**，而錯的是我：**我把一個【連續的收益問題】壓成一條線，然後讓線去決定燒不燒一個世代邊界**｜★已改寫成**收益函數**（票 §12）：削掉的 pass 時間 ≈ (1−f) × 34.7%｜★★**而 f=0.60 ⇒ ≈14% ⇒ 1.15 秒降到 ≈0.99 秒 ⇒ 幾乎肯定跨不過 B3** ⇒ 這張票的意義**不是修好凍結**，是**拿掉一個不該存在的人工同步**｜★★★**你停掉那一輪 12 天量測 —— 那是本輪最值錢的一件事**
---

# 一、★門檻是我寫壞的

```
`f > 0.6 ⇒ 不做` —— ★**它把「值不值得燒一個世代」壓成一條線**
⇒ 而你量到 **0.6021**（只超 0.002）⇒ ★★**線兩邊的世界沒有任何實質差別**
⇒ ★★★**改寫成收益函數**（票 §12.1，**寫在 12 天數字回來之前**）：
      f=0.10 ⇒ ≈31%｜f=0.30 ⇒ ≈24%｜f=0.50 ⇒ ≈17%｜**f=0.60 ⇒ ≈14%**｜f=0.90 ⇒ ≈3.5%
⇒ ★**而真正要問的不是 f 多少，是「這些 % 能不能跨過玩家尺」**：
   1.15 秒削 14% ＝ **≈0.99 秒** ⇒ ★★**B3（>2s ≤1/日 且 p99<1s）幾乎肯定跨不過**
⇒ ★★★**所以這張票的意義不是「修好凍結」，是「拿掉一個不該存在的人工同步」** ——
   **而那是否值得燒一個世代邊界，是 WHAT 的欄，不是我的一條線。**
```

# 二、★★★你停掉那一輪，是本輪最值錢的一件事

```
「seed 1337 已開跑 → 我在同一棵樹 commit → seed 77 會載入新 code」
⇒ ★**兩顆種子跑不同的 code，而卷面上不會有任何一行告訴任何人**
⇒ ★★**而 `godot-busy.sh` 擋不到這一種** —— 它擋「別人在跑」，**擋不了「我改了它正在讀的檔」**
⇒ ★★★**已寫進量測協議**（`docs/process/reference-measurement-protocol.md` 檔尾）：
   **批次裡每一顆種子啟動時印下那棵樹的 HEAD sha；交件時比一次 —— 不同 ⇒ 判【不可判】。**
★而**正確處置是「停掉整批重排」不是「留著半批」** —— 你就是這樣做的，**而那一輪還沒有任何損失。**
```

# 三、★你撤回的那一句，我也要說我這邊的部分

```
你撤「`ThreatAssessment` 讀世界真值」——★而 R² 獨立讀完全函式，**又把我的措辭改精確了一次**：
  ★★**不是「從不碰 `other.tile_pos`」的構造保證**，是「**只在 visible-this-tick 時碰**」
  ⇒ 那是 **belief-等價**，與 `estimate_catch_up` 同一慣例 ⇒ **結論不變，措辭要改**
⇒ ★★★**而我要記的是**：我寫「構造保證」四個字，**會讓下一個人以為它永遠不碰真值** ——
   **實情是它碰，只是只在【碰了也等於沒碰】的時候碰。**
   ★若以後有人改了「visible-this-tick」的定義，**這條保證會跟著壞，而那四個字不會提醒任何人。**
```

# 四、介面硬規**補一句**（R² ②）

```
舊：允許取 handle 整個交給 `score()`；**禁止讀它任何欄位**
★新增：**也禁止在威脅判定路徑上呼叫 `other` 的【非 belief-gated 方法】**
⇒ ★★理由：**「禁讀欄位」有一個現成的繞道 —— `other.某個方法()`**，
   而它在 diff 裡**長得不像讀欄位**
⇒ ★★★**閘要掃兩種形狀，陽性對照也各跑一次**（讀欄位／呼叫方法）。
```

# 五、序

```
・12 天 × 兩顆種子（新判準、**同一份 code**）跑完 ⇒ ★**兩顆的 f 差距也要印**
  （★★差距比「離 0.6 多近」更能說明這個數字穩不穩）
・回來之後我把 **f ＋ 對應的預期 % ＋「跨不過 B3」** 一起呈 WHAT ⇒ **他裁值不值得**
・★實作仍未開工（等 WHAT 那一裁）
```
