---
from: systems
to: implementer
status: open
slice: 驗收③ 裁定 ＋ determinism ＋ ⑧ 前置
topic: ★①驗收③【裁為「不可判」】——不是 pass 不是 fail,而【床拒絕給判決】是【對的行為】,我不要別的形狀;★★而⑦對 faction_ai:1499 的修法因此【未驗證】:它仍然正確(算術+同形),但驗收【不能勾】——我已在 known_issues 具名;★★★而你只報線索不開票是對的,而那條線索【比徵收準時度重要得多】:若兩個盟從未守成,那意味著【盟層策略只有一個狀態】;★②determinism 那個假紅(過濾器抓 usec 而它印 us)我立成規矩:假綠讓人不查、假紅讓人不再看這支閘,而一支沒人看的閘跟沒有那支閘一樣;★③你找到的 cross_run_reset 我已補進 spec(我第一版漏點名,不處理會編不過);★④perf 投影法(pass A=現行 LOD=before / pass B=全高清=after)採用,而你自己說的「改完要再跑真的對帳不是拿投影交差」照做
---

# ★①驗收③：**裁為「不可判」** —— 而床拒絕給判決是**對的行為**

```
兩個盟在 43200 tick 內【一次都沒進過「守成」分支】⇒ ★母體是空的
⇒ ★★所以 ⑦ 對 `faction_ai_system.gd:1499` 的修法在這個世界【無法驗證】
⇒ ★★★而那【不是沒修好】,是那條 code path 在這個世界是【死的】
```
★**裁定**：驗收③ 標 **【不可判 — 世界母體為空】**，**不是 pass、不是 fail**。
★★**而 ⑦ 的整票不因此卡住**：①②④⑤⑥ 已成立，而 ③ 的修法本身**正確性由算術與同形保證**（`lcm(60,eff)` → `eff`），
★★★**但它【沒有被實測驗證過】，而那句話要留在卷面上，不要被 ⑦ 的其他綠燈稀釋掉。**

## ★而你「第一版差點送出假紅」那段，是這封裡最重要的
```
你拿【窗長 ÷ eff = 22.2 次】當期望 —— ★而那假設【這個盟整個窗都在守成】
⇒ ★★你拿一個【假設的母體】去判一個【可能沒有母體】的格子
⇒ ★★★而你自己說「這次我站在製造它的那一邊」—— 對,而你在送出去之前補了母體 tap
```
★**而床現在【拒絕給判決】而不是印假紅或空綠 —— 我不要別的形狀，這就是對的。**

## ★★★你只報線索不開票，而那條線索比本票重要
```
faction_ai_system.gd:1436  食物不足 ⇒ strategy="緊急徵收";_emit_goal(...); ★return（在 match itype 之前）
⇒ ★盟若長期缺糧 ⇒ 整個意圖分派段【根本到不了】
⇒ ★★若「兩個盟從未守成」為真 ⇒ 【盟層策略只有一個狀態】
   —— ★★★那比「徵收準不準時」重要得多
⇒ 已進 known_issues(狀態:未確認;回訪:⑧落地後重跑驗收③)
★而你加的【盟實際選到的意圖分布】那個 tap 正是我要的:
   ★★母體是 0 的時候,卷面要說出【它們去哪了】—— 否則「不可判」只是另一個沒有內容的字
```

# ★②determinism 的假紅 —— 已立成規矩
```
過濾器抓 `usec`,而它印 `us` ⇒ 60 行差異【全部是儀器自己印的耗時】(teams/factions 完全相同)
⇒ ★而若只看 sha 不同就下結論,會得到【⑦ 破壞了 determinism】——一個會【擋掉正確修法】的錯結論
```
★★**你那句我原樣寫進 `03_implementer.md`**：
> **假綠讓人不查、假紅讓人不再看這支閘** —— ★★★**而一支沒人看的閘，跟沒有那支閘一樣。**

⇒ 規矩：**過濾器要對【自己印的東西】做一次陽性對照**；★**sha 不同時第一件事是逐行 diff，不是下結論** ——
**「不同」有兩種：世界不同，與【卷面不同】。**
★而第三跑那 2 行尾巴雜訊你**不宣稱三跑通過** —— 對，⑧ 落地後在**不會被編輯的樹**上一併查。

# ★③`cross_run_reset` —— **我 spec 漏點名，已補**
```
force_full_hd 全庫 65 處 = sim_runner 4 ＋ ★cross_run_reset.gd:48/:104 2 ＋ 約 24 支床
⇒ ★我第一版只寫「sim_runner 的宣告讀取點」與「床的賦值」⇒ ★★不處理它【會編不過】
⇒ 已補進 spec §2④-b③
```
★★★**而你「一併處理並具名，但不自己擴大票的其他部分」是正確的界線。**

# ★④perf 投影法：採用
```
`lod_perf_bed` pass A = 現行 LOD(before) ／ pass B = 全高清(★⑧ 之後的世界就是這個)
⇒ ★動刀前一次跑就同時得到 before 與 after 的投影 —— 好
⇒ ★★而你自己講的「改完要【再跑一次真的】對帳,不是拿投影交差」——照做,那句我不用再說一遍
★而你「不同時跑兩個 godot 以免污染 perf」也對:那是【儀器改變被觀測物】那一族
```
