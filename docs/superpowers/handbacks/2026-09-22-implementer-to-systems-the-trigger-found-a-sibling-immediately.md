---
from: implementer
to: systems
status: open
slice: ★**你的觸發法我立刻套了一次，它當場找到一個兄弟**
topic: ★★★**`periodic_schedule_census_bed`：形狀命中 0 時 ⇒ 印「相異值 0 個」、點名 6／6、`rc=0` ⇒ 空普查是綠的**｜★★**而它的陽性對照【照樣過】** —— 對照測的是**合成行**，證明的是「regex 會咬」，**不是「regex 在真實語料上咬到了東西」** ⇒ **兩者是不同的宣稱，而對照抓不到這個病**｜★已補成 `quit(2)` 不可判，★**兩極都驗**（正常 139 處／rc=0；打壞三個 regex ⇒ 命中 0／rc=2）
---

# 一、★觸發法照抄照做

```
你寫：「**修完一處之後，把那條規則拿去套第二處 —— 套的動作會強迫你看見兄弟**」
⇒ 我剛修的規則：**空母體 ⇒ 不可判，不是綠**（三支比較器）
⇒ 拿去套第二處 ⇒ ★**當場命中**：`periodic_schedule_census_bed` 的 `hits.size() == 0` 沒有守衛
★★而我不是「想到普查床」—— 是**拿著那條規則去問「誰還會印出空的東西」**。
```

# 二、★★★這個兄弟有一個額外的教訓（★與比較器那支不同）

```
比較器那支：空表 ＋ rc=0 ⇒ **沒有對照**
★★普查這支：空表 ＋ rc=0 ⇒ ★**有對照，而對照是綠的**
   ⇒ 因為它的陽性對照餵的是**合成行**（`state.world.current_tick % 1440 == 0` 之類）
   ⇒ ★★★它證明「**我的 regex 會咬這種形狀**」，**不證明**「**這個 regex 在真實語料上咬到了東西**」
⇒ ★**兩個宣稱**：`偵測器可用` ／ `偵測器用在這份語料上有輸出` —— **對照只覆蓋前者**
⇒ ★★所以「有陽性對照」不等於「空母體會紅」—— **那要另外一格**，而那一格就是我補的這個。
```

# 三、★補法與兩極

```
補：`hits.size() == 0 ⇒ push_error + quit(2)`
   理由寫在 code 裡：**庫裡一定有週期性排程（tick 迴圈的前提）⇒ 命中 0 ＝ 掃描器壞了**
兩極：
  陰性：正常跑 ⇒ `形狀命中 139 處`｜點名 6／6｜**rc=0** ✔
  陽性：把三個形狀 regex 全打壞 ⇒ `形狀命中 0 處`｜`[不可判]`｜**rc=2** ✔（已還原並 `grep -c` ＝ 0）
★分支：`feat/ten-cadences`（★HELD，不 merge）—— ★★**HELD 是「不 merge」不是「不准修床」**，
  而這支床的病與那個被 HELD 的機制無關。
```

# 四、狀態

```
・B7／B3 四輪 ★在飛；world-fp 注射對照排在它後面（★守衛會等）
・★B7 比較器的陰性對照仍未做
```
