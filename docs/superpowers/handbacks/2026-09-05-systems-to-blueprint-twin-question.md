---
from: systems
to: blueprint
status: consumed
slice: ★「共位＝保證看見」的**孿生問題**：共位能不能【互動】
topic: ★★★implementer 用控制床挖出:互動【只在移動時觸發】—— _try_interact 的 production 入口只有 process_on_move 的 arrived/moved 兩條(我窮盡驗過)⇒ 雙方都不動的 pair 對 resolver 是【結構性隱形】;★★而它對你剛裁的那一刀是【逆風】:共位必見把 belief 修對後,同格 JOIN 隊的 move_target ＝ 自己腳下 ⇒ 它更不會動 ⇒ 更不會互動;★所以要你裁孿生的那一格:【共位是不是也該保證「能互動」】
---

# ①事實（★窮盡驗過，不是推論）
```
★`_resolve_join` production 呼叫點只有 interaction_system:293／296,都在 `_try_interact` 內
★★`_try_interact` 的 production 入口只有 :97(arrived)／:125(moved) —— 都在 `process_on_move` 內
   而它由 sim_runner:459 以 `moved_ids` 呼叫
⇒ ★★★雙方都沒動 ⇒ 不進 moved_ids ⇒ 不觸發互動【結構性,不是機率】
★控制床:48 tick 裡 moved 名單【每一 tick 都是 0 筆】
```

# ★★②而它對你剛裁的那一刀是【逆風】
```
★共位必見把 belief 修對 ⇒ 同格 JOIN 隊的 move_target ＝【自己腳下】⇒ ★★它更不會動
⇒ ★★★所以【感知修對了,反而讓互動更不會發生】
⇒ 兩個機制的假設互相矛盾:
   ①感知側假設「知道了就會靠近」 ②互動側假設「靠近的【動作】才觸發互動」
   ⇒ ★而當「已經在那裡」時,①沒有動作可做,②就永遠不觸發
```

# ★★★③要你裁的（★與你昨天那一裁同型）
```
問:【共位是不是也該保證「能互動」?】
★(a) 該 ⇒ 互動的觸發不能只掛在【移動事件】上,要有一條「同格即檢查」的路
   —— ★★而那是【接線】不是數值:現在的觸發點是 moved_ids,而同格是一個【狀態】不是【事件】
★(b) 不該 ⇒ ★★★那 JOIN／外交／徵收這些「必須同格才能做」的機制,
   需要一個【明確的到達動作】(例如抵達後仍發一次 arrive 事件),而不是靠「剛好移動過」
★而影響範圍不只 JOIN:diplomatic_ai_system.gd:138 明寫外交/徵收也對齊 process_on_move 同格外交
   ⇒ ★★任何「兩隊都待在原地」的情境,互動都不會發生 —— 而那正是【貧窮、走不動】的隊最常見的狀態
```

# ④★而我的處置（★講清楚我做了什麼、沒做什麼）
```
★共位必見那一刀:#1(同格未偵測 880→0)/#2/#5/#6/#7 全綠 ⇒ ★★它自己的因果範圍【達成了】
⇒ ★★★而 #3/#4(JOIN true<belief 下降、resolve>0)【很可能不會綠】—— 而那【不是它沒生效】
   ⇒ 我把 #3/#4 移到孿生那一票,而共位必見【照它自己的驗收】收
★而 implementer 【沒有動】JOIN 到達判定(spec §5 明寫不在範圍)—— 對:根修一次只動一條
```
