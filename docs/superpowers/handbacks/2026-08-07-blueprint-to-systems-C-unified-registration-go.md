---
from: blueprint
to: systems
status: open
topic: "[用戶拍C→F4統一註冊表GO(先驗行為不變性)·C=先做統一註冊表(便宜高值直解加東西大改痛)+收結構進度→回玩法→26決策func走B漸進Track②A backlog·★F4第一步=你評估:4張分散註冊表(options.gd REGISTRY+need_hierarchy:82 AFFINITY+terms.gd match+decision_engine/options OPTION_SET)併成單一註冊點=能byte-identical行為不變否?★關鍵風險(R²揭):AFFINITY漏列option靜默落_AFFINITY_UNIFORM(:113)=行為性差異——併表必須PRESERVE此semantic(未列者仍uniform)才byte-identical;若併表改到哪option拿到non-uniform affinity=行為變·分兩路:(a)可保semantic byte-identical→做F4=擴充性稽核(加mock行為域=machine驗只動一註冊處)=硬綠②operational示範→spec自檢→R²→build(fp byte-identical驗)→QA→merge(b)併表無法不改行為→flag回我裁(保uniform semantic維持byte-identical vs量測那變化)·★F4收後framework狀態=誠實非premature:①F1死常數人格化done+②F2/F3結構+F4註冊統一=擴充性operational(加東西=動一處的『註冊』部分解、但新行為若與5248決策核互動仍需碰faction_ai=full no-god-object未達=Track②A incremental backlog、記progress不宣稱done)·F4後回玩法待我新arc·序:你評估(a)/(b)→回報·地基KEEP·我FYI用戶C鎖+走便宜路"
---

# 用戶拍 C → F4 統一註冊表 GO（先驗行為不變性）

C = 統一註冊表（便宜高值、直解「加東西大改」痛）+ 收結構進度 → 回玩法 → 26 決策 func 走 B 漸進 Track②A backlog。

## ★F4 第一步 = 你評估行為不變性
4 張分散註冊表（options.gd REGISTRY + need_hierarchy:82 AFFINITY + terms.gd match + decision_engine/options OPTION_SET）併成單一註冊點 = **能 byte-identical 行為不變否**？
- **★關鍵風險（R² 揭）**：AFFINITY 漏列 option 靜默落 `_AFFINITY_UNIFORM`(:113) = 行為性差異 → **併表必 PRESERVE 此 semantic（未列者仍 uniform）才 byte-identical**;若併表改到哪 option 拿到 non-uniform affinity = 行為變。
- **(a) 可保 semantic byte-identical** → 做 F4：spec 自檢 → R² → build（fp byte-identical 驗）→ QA → merge + **擴充性稽核（加 mock 行為域 = machine 驗只動一註冊處）= 硬綠②operational 示範**。
- **(b) 併表無法不改行為** → **flag 回我裁**（保 uniform semantic 維 byte-identical vs 量測那變化）。

## ★F4 收後 framework 狀態（誠實非 premature）
①F1 死常數人格化 done + ②F2/F3 結構 + F4 註冊統一 = **擴充性 operational**（「加東西=動一處」的**註冊**部分解;但新行為若與 5248 決策核互動仍需碰 faction_ai = **full no-god-object 未達 = Track②A incremental backlog、記 progress 不宣稱 done**）。F4 後回玩法待我新 arc。

序：你評估 (a)/(b) → 回報。地基 KEEP。
