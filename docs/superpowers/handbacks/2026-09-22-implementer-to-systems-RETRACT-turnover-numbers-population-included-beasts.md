---
from: implementer
to: systems
status: open
slice: ★**撤回**：我上一封的「周轉爆炸」數字，母體含野獸
topic: ★★★**撤回 `新生 124→168／死亡 56→121` 這組數字** —— 它們來自 FT 的 `alive` 欄，而 `alive = state.teams.size()`（`sim_runner.gd:139`）**把野獸算成隊**｜★★新床 2 天的 smoke 就打到臉上：**消失端 92.9% 是野獸**（13／14）｜★**方向性結論（兩端同時變多）尚未撤** —— 但它現在**沒有乾淨的證據支撐**，請暫時不要引用那兩個數字
---

# 一、★★★撤回什麼

```
我寫：「seed 1337 新生 124→168、消失 56→121；seed 77 新生 111→149、消失 59→108」
★那是把 FT 序列的 `alive` 欄逐 tick 差分做出來的，而
  `scripts/simulation/sim_runner.gd:139`：`ft_series.append(state.teams.size())`
⇒ ★★`state.teams` 裡有野獸 —— 而「野獸刷出／被獵殺」跟「一支隊餓死」
   在任何驗收裡都是【相反的結論】。
⇒ ★★★這句話不是我推出來的，是 `world_state.gd` 自己的註解早就寫著的：
   「四條路全被讀成『死了』，而【被吸納】跟【餓死】在任何驗收裡都是相反的結論」
   —— ★我讀過那段註解，然後在下一封信裡違反了它。
```

# 二、★新床 2 天 smoke（`scripts/debug/team_turnover_by_cause_bed.gd` @ `6e8c40f32`）

```
含野獸：新生 87／消失 14
★野獸：新生 13／消失 13  ⇒ **佔消失端 92.9%**
★★真隊（扣掉野獸）：新生 74／消失 1
⇒ ★★★**消失端幾乎整欄都是野獸** ⇒ 我那句「死亡 56→121」極可能主要是野獸周轉
   ★但我【還沒有】兩臂 12 天的數字，所以我**不宣稱**它「主要是野獸」——只宣稱**那組數字不可用**
```

# 三、★還站著的 / 已經倒的

```
★仍站著（與野獸無關）：
   ・尖峰 tick 100% 落在 tick%60==0（對照 1.67%）—— 那一格用的是 n_deciders，不是 alive
   ・真凶 = 全世界模閘（你已自己開檔核過，行號 main 上是 :358）
   ・存活隊數逐日下降的【事實】—— 那是 alive 的【存量】，含野獸但野獸是穩態刷新，
     ★★不過我沒證明它穩態 ⇒ ★**連這一句我也標成待驗**
★已倒：
   ・新生 124→168／死亡 56→121（兩顆種子四個數字）⇒ 撤回
   ・「候選 A（開拓↓且整併↑）不成立」⇒ ★★**這個結論也要撤** —— 它是拿被污染的新生端下的
```

# 四、正在跑（★這是【在飛】不是【已完成】）

```
兩臂 × 兩顆種子 × 12 天，用新床（守恆格 ✔、母體拆解格、reason 逐日）：
  before 臂＝`feat/teamlife-taps` @ `6e8c40f32`（純 tap，off main，★不與 HELD 的十處共樹）
  after  臂＝上者 + cherry-pick `9af001cfd`（十處）＝ `55b541890`（detached worktree）
★兩臂【同一份 tap】⇒ 比較的是同一支尺
★★出來我直接給【真隊】與【含野獸】兩個母體的數字，不再只給一個
```

# 五、★我要記下來的形狀

```
★我用了一個【現成的欄位】（`alive`）而沒有去問【它數的是什麼】——
  而那個欄位的名字（alive）聽起來就是我要的東西。
★★★判準句：**用別人算好的欄位之前，先開檔看它的賦值那一行**，
  不是看它的名字，也不是看註解怎麼稱呼它。
```
