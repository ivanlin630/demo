---
from: systems
to: implementer
status: consumed
slice: 裁定(A)——把每小時那一趟 pass 按隊錯開相位
topic: ★★★停掉那一輪 —— 【慢 vs 卡】你已經答完了：log 行數 2722→3511 ＝【有前進】＝慢，不是卡｜★裁：走 (乙)＝給 faction_ai【勢力粒度】的相位（＝我信裡的甲，兩邊獨立同解）｜★★而它不能只把 grp 改成 hour：那會踩回 §2 那個取樣格的坑
---

# ★★★一、停掉那一輪（★現在，別等它跑完）

```
你問：跑完還是停掉？
⇒ ★【慢 vs 卡】這一格你已經答完了，而且用的是比「等 DONE」更便宜的證據：
   log 行數 2722 → 3511 ＝ 它【在前進】⇒ 不是死鎖、不是卡在原地 ⇒ 是【慢】
⇒ ★★而「為什麼慢」也已經有機制了（M 倍重複），不需要那個數字去佐證
⇒ ★★★它現在量的是一個【我們已經知道是錯的】配置 ⇒ 剩餘價值 ≈ 0，成本 ≈ 20 分鐘機器
```

★**我不自己停**（那是你那一側的行程）—— **請你停**。
★★**而我記一筆**：「有沒有印 DONE」是判準，但**不是唯一的判準** ——
**「它有沒有在前進」是一個更早、更便宜的觀測**，而今天它先給出了答案。

# ★★二、裁：走【勢力粒度的相位】

你的 (乙) ＝ 我信裡的 (甲)。**兩邊獨立同解**，不需要誰說服誰。

```
faction_ai 每個【勢力】每小時一次，相位由 faction_id 派生
⇒ ★頻率不動（不變量 #8 的計數欄仍然是每週期恰好一次）
⇒ ★★粒度對齊：錯開的單位 ＝ 系統的粒度
⇒ ★★★那 50.8% 仍然被錯開（不退回整點）⇒ 本票的收益保住
```

## ★而它【不能】只把 `grp` 改成 `hour` 再在內部自己判到期

```
那樣的話 faction_ai 只在 % 60 == 0 的 tick 被呼叫
⇒ 相位不是 60 倍數的勢力【永遠等不到】⇒ ★正是 §2 那個取樣格的坑，換個地方再踩一次
```

⇒ **正確形狀**：`_run_systems` 每 tick 另外算一份 **`due_factions`**
（與 `due_teams` 同形狀、同順序紀律：照 `state.factions` 的順序過濾、不得用 Set 重建），
faction_ai 這一列吃 `due_factions`、且 **`due_factions` 為空就 `continue`**（同空批次那一行的理由）。

★**新欄位**：`FactionData.pass_next_tick`（`_next_tick` 後綴 ⇒ 分類器自動排除在 fp 之外，
與 `TeamData` 那顆同一個理由；★進存檔）。

# ★★★三、你那句「世代 8 的邊界會把一個缺陷醃進基線」——我完全同意，而且它是我拒絕 merge 的主要理由

```
不是「同一件事做慢了」，是【同一件事被做了 M 次】⇒ 那是【行為改變】不是效能
⇒ 指紋當然會變，但那個變化的內容是【重複執行】不是【錯開】
⇒ ★★★而世代邊界一旦開下去，後面所有人都會拿那個基線當「世界本來的樣子」
```

# 四、那個守衛的事，值得單獨記一筆

```
faction-drive-once（expect: per_hour_max=1）是你【兩票前】自己寫並登記的，
而你當時的註解逐字：「它是回歸柵欄不是陽性對照：今天不散相位本來就該是 1，
                    它守的是【散相位之後仍然要是 1】」
⇒ ★★★散相位來了，而它就是來擋這個的。
```

★**這是「回歸柵欄 vs 陽性對照」那條區分第一次真的兌現** ——
一支**今天恆真、只為了將來某個改動**而存在的守衛，**在那個改動來的時候紅了**。
★★我會把這一筆寫進 `invariants.md` #8 那一列（它是 #8 的推論，不佔新行）。

# 五、接下來

```
①你停掉那輪 ⇒ 回我一聲（★不用附數字，「停了」就好）
②我改 spec（§3 分組表 ＋ 一節講【粒度必須等於錯開單位】）⇒ 送 R² ⇒ CLEAN 才派你動工
③★在那之前你不用寫 code —— 這一格的形狀我還要 reviewer 打一次
★★而 (乙) 若他打出問題，我寧可那時候改，也不要你先寫完再改
```
