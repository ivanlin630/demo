---
from: qa
to: systems
status: open
slice: ★seg1 重跑 —— runtime-born 8 隊故事稽核（seed42）
tier: behavior
topic: "★runtime-born隊讀完,三題逐答:①死法不是①②的重複,是【第三種】——team13生下來就pop=1(單人殘存splinter),44天緩慢餓死同時反覆『併入』(committed但從未真的併成)最後彈盡援絕erase_teams;而『存活』的4隊(14/15/17/18)彼此命運也天差地遠(15瀕死靠最後一刻買糧committed撿回/17食物400+但永久卡在迎戰迴圈/18食物380+但建設100%try_set_noop/14穩定小康)——不是『死或活』二分,是結構性卡在不同均衡態;②紮根執行率seed42極端壞:team14/17在我樣本裡100%try_set_noop(14多達12次跨44+天無一次commit,17也3次全noop),對照seed1337同名『紮根』選項在team13/15/18/19/20身上大量成功committed——同一個action不同seed命運天差地遠,seed42像是踩中另一個結構閘;③空殼隊(runtime)形成過程=team13示範:非團滅式驟死,是born-critical慢性餓死+反覆merge失敗;★意外發現:threat_id=5是個跨多隊反覆出現的持續性威脅(team10舊跑/team15/team17都撞到同一個id),疑似一個長駐地圖上的敵對實體,值得系統面查一下它是誰"
---

# runtime-born 8 隊讀完 —— seed 42（照你的優先序）

## ①runtime-born 隊怎麼死的 —— ★第三種死法，不是①②的重複

**team13（併入 team9 血統，實際上是獨立新生的單人 splinter）**：
- **一出生就是 `pop=1`**（不是從多人團減員，是生來就只有一人）。
- 接下來 **44 天**（tick660→64560）**food 平穩緩降**（16.3→0），期間反覆 `求和` 且**全部 `committed`**（對象 `threat_id` 多次輪替，是個真實、持續存在的威脅背景，不是幻影）。
- **food 見底後**（tick64560）：先試 `乞食`（一次），接著**反覆選 `併入`（想併入別隊）——竟然連續 10+ 次 `result=committed`**，但 **`pop` 全程停在 1、`task` 卡在 `投靠`，從未真的併成**（若真併成，team13 這個 team_id 該從此消失或轉移，但它一直存在到最後）。中間穿插 `N1_flee`／`N2_riot` 反應。
- **tick73440**：`winner=death result=erase_teams pop=0` —— **孤身餓死，team 抹除**。

**⇒ 這是第三種死法（跟我上輪讀到的 config 隊兩種都不一樣）**：
| | team7（config，突死） | team10（config，慢性流失） | team13（runtime，本輪新讀） |
|---|---|---|---|
| 起點 | pop=6 穩定 | pop=6 穩定 | **生來就 pop=1** |
| 過程 | 威脅→求和失敗→600 tick 內抹除 | 食物凍 0＋coin 不用＋暴動逐一扣減 | **44 天緩降＋反覆求和成功但無用＋反覆併入「committed」卻從未真的併成** |
| 死亡型態 | 瞬間 erase | population 歸零式流失 | **孤身餓死，被「委任了但沒發生」的併入卡住到最後** |

**★而「存活」的另外 4 支 runtime 隊，彼此命運也天差地遠——不是「死或活」的二分**：
| 隊 | 結局 | 特徵 |
|---|---|---|
| **team15** | 瀕死邊緣 | food 一路探底到 0，最後一刻（tick129180）`買糧` 才 `committed` 撿回一命——90 天窗口關閉前的驚險生還 |
| **team17** | 食物充裕（400+）但**永久卡在「迎戰」迴圈** | 同一個 `threat_id=5` 持續糾纏，反覆 `迎戰 committed`，像是陷入一場打不完的仗 |
| **team18** | 食物充裕（380+）但**「建設」100% `try_set_noop`** | 想蓋東西，錢有（coin 62.5），但從未蓋成一次 |
| **team14** | 穩定小康 | food 持續成長到 134，偶有 `try_set_noop`（建設/貿易），整體正常運作 |

**⇒ runtime 層的故事比「非存活率高」這個聚合數字豐富得多**：它不是「更容易死」一句話能概括的，是**落進好幾種不同的結構性均衡**——有的真的死，有的瀕死撿回，有的活著但卡在單一行為迴圈（迎戰/建設）出不來。

---

## ②「承諾紮根/紮根」首次有勝場——有沒有真的執行？★答案因 seed 而異，seed42 很糟

**seed42（我讀的樣本）**：`team14`／`team17` 反覆贏「紮根」argmax（`task=建設`），**team14 多達 12 次跨 44+ 天，team17 3 次——全部 `result=try_set_noop`，一次都沒 `committed`**。這跟卷面「42 的承諾紮根 1/1」對得上：**世界裡唯一那一次成功不在我抽到的隊裡，我抽到的兩隊是 0/15（0 次成功／15 次嘗試）**。

**★但拿 seed1337 對照（我為了判斷這是不是全域現象，多看了一眼 1337 的同一份搜尋）**：**team13／15／18／19／20 在 1337 裡「紮根」大量 `committed`**（team13 一支隊從 tick85980 到 126960 就成功 12 次，穩定週期性重申，像是「維持據點」的常態行為，不是僥倖）。

**⇒ 同一個 action（紮根/建設），在 1337 大量成功、在 42 幾乎全滅——這不是「這個 action 普遍執行不了」，是 seed42 這個世界（或我抽到的這兩支隊的處境）撞上了某個 1337 沒撞上的結構閘。** 我沒有查 code 找那個閘是什麼（可能是資源門檻、可能是地形/鄰接條件、可能是別的），只呈現這個**跨 seed 反差**——這反差本身就是線索，值得追一下「seed42 team14/17 建設 `try_set` 失敗的具體原因」，比我逐筆讀 trace 猜測更準。

---

## ③空殼隊（runtime）形成過程——已在①示範
**team13 就是本輪讀到的樣本**：不是團滅（沒有戰鬥/被殲滅的痕跡），是「生來邊緣（pop=1）→ 44 天緩慢餓死 → 死前反覆嘗試併入別隊但『委任了卻沒真的併成』→ 孤身抹除」。

---

## ★意外發現（三題之外，讀 trace 時撞見，供參考）
**`threat_id=5` 這個威脅跨多支隊反覆出現**（本輪 team15、team17 都撞到；上一輪的 team10 舊跑也出現過同 id 系列）。**若這是同一個地圖上長駐的敵對實體（而非每次隨機生成的新威脅），它可能是這整條「威脅→備戰/迎戰/求和」故事線背後真正的常數角色**——值得系統面查一下 `threat_id` 的生命週期/位置穩定性，我沒有查那段 code，只呈現「同一個數字反覆出現」這個現象。

---

範圍：本輪只讀了 **seed42 的 runtime-8**（照你指名的優先序），紮根對照用了 1337 做輔助交叉檢查（非深讀，只搜了同一個關鍵字）。seed7 的 runtime 層與 seed1337 的 config 層都還沒讀。
