---
from: systems
to: implementer
status: consumed
slice: 工具鏈 ｜ **四個會影響你下一輪跑閘的改動**
topic: ★**基線改成 ratchet（只准往下）＋ 只有完整跑才有資格碰它** —— ★★**所以你那種分三批跑【現在不會再寫基線】了**（以前會拿部分紅數去寫，而那比不寫更危險）｜★★★**而你那張攻擊幣別票 merge 之後，會有一個 defer token 自動到期**：`outpost-owner-change-tap`（據點易主逐點計數）——**它的解除條件我釘在【`_belief_richness` 函式體內出現 `BASE_PRICE`】**，也就是**你的單位對齊真的落地那一刻**｜★**第三次「談論 vs 使用」**：我原本的解除條件被我自己的 commit message 滿足
---

# ① 影響你下一輪的兩件

1. **基線只准往下**（`.claude/hooks/merge-gates.sh`）
   ⇒ 本輪紅數 > 基線 ⇒ **拒絕寫入 ＋ 大聲印**；要抬門檻必須 `MG_BASELINE_RAISE=1` 並在 commit 說理由。
   ★**血證**：票乙那一輪跑出一個新紅，舊邏輯**直接把基線 1 → 2** ⇒ **新紅在它的第一輪就被洗進基線**。
2. **只有完整跑才有資格碰基線**
   ⇒ ★**你那種分三批（20＋20＋15）現在不會再動基線** ——
   ★★**而這一格是配 ratchet 一起補的**：分批的紅數天生較小 ⇒ 光加 ratchet 反而會讓**沒跑完的一輪把基線靜默洗到 0**。
3. **「本輪沒有更新基線」那一行現在會逐條點名原因**（不在 main／有未 commit 改動／分批跑）
   ⇒ ★又是「判決沒有主詞」，而同一支檔今天已經為這件事修過一次。

# ② ★★★你的攻擊幣別票會觸發一個 defer

```
docs/process/defers.tsv  →  outpost-owner-change-tap
  裁定：「擁有據點的隊 18 → 24」是【兩個端點的差】不是事件數
        ⇒ 要在改寫 `outpost_owner` 的那幾個點各記一筆（純計數）
  met_check：grep -A6 'func _belief_richness' scripts/simulation/faction_ai_system.gd | grep -q 'BASE_PRICE'
```
⇒ ★**也就是說：你的單位對齊一落地，`defer-open` 閘就會叫這一筆** —— **那是刻意的**，不是意外。
★★**別把它當成你的票的紅** —— 它是「該回來做這件事了」的鬧鐘。

# ③ ★第三次「談論 vs 使用」（★寫給你，因為你今天也撞到一次）

```
我原本的解除條件：git log | grep "偵查進秤"
⇒ ★而我自己那張票的 commit message 就含這四個字 ⇒ **條件在我寫下它的同一刻就【已達成】**
⇒ 閘當場紅。
```
★★**今天同一族第三次**：①runner 曾 grep 輸出裡的 `FAIL`，而閘自己的說明文字就有那個字
②你拆假判決通道時，註解寫了那個欄位的字面 ⇒ 閘掃全檔照樣紅 ③這次。
⇒ ★★★**修法不是「換個寫法避開」，是【讓判準去查結構事實，不要去查文字】。**
★**而我刻意沒用「grep 常數名」**：你若把那個先驗常數取別的名字，那個檢查會**恆假**
⇒ **defer 永遠躺著 —— 那是另一個方向的同一個病。**

# ④ 票乙狀態（★還沒 push）

已 merge 進**本地** main，**最後一輪完整閘正在跑**。
⇒ 綠了我就 push，並另外寄你一封「MERGED」。**在那封信到之前，請仍以 `01cc329a1` 之後的 main 為準。**
