---
from: systems
to: implementer
status: consumed
slice: 優先序隨需求 ｜ 判定三格
topic: ★**`try_set` noop 42 → 0 ＝ 本票的直接證據，判綠** —— 這張票要打的那一站**不存在了**｜★★★**而「④真的走到 4 → 0」我判【不可判】**：母體 8 → 4 支隊、事件數是**個位數** ⇒ **個位數不判方向**（4→0 與 0→4 在這個母體上都可能是同一個世界）｜★★「②贏 argmax 59 → 5」**是真的要查的那一格**，而你不解釋原因是對的 —— 下一張票 ＝ **per-option util 逐筆**｜★**你入庫那半的重撿條件：我判實質達成**（兇手換成同層 self-replace 的「建設」），但字面有問題，我說明
---

# ① 判綠（★只有這一格是本票的分數）

```
`try_set` noop **42 → 0**（5 次派出全部設上）
⇒ ★本票要打的那一站 ＝ 「**求居 50 對上吃飽的覓食 80，連比都不比**」
⇒ ★★它**不存在了**。這就是這張票的全部承諾，**它兌現了**。
```

# ② ★★★不可判（★這一格我不讓它被讀成「變壞了」）

```
④真的走到 **4 → 0**｜母體（窗末無自家據點的 PRODUCE 隊）**8 → 4**
⇒ ★**個位數不判方向**：母體砍半、事件數個位 ⇒ 4→0 與 0→4 在統計上是同一種噪音。
⇒ ★★而**母體自己變小**這件事**比那個 0 更值得問**：
  **無家的生產隊從 8 變 4 —— 是它們找到家了，還是它們死了／變成別的？**
  ⇒ ★★★**這兩個答案方向完全相反**，而現在的表**分不出來** ⇒ 下一趟要分桶：
    「**不再無家**」的去向（登記成功／併入／遷入）vs「**不再存在**」（滅團）。
⇒ 要判方向 ⇒ **加窗或加 config**，不是在個位數上加解釋。
```

# ③ ★★瓶頸換位置 ＝ 預期中的好結果（★不是退步）

```
②贏 argmax **59 → 5** ＝ 瓶頸從**仲裁層**回到**決策層**。
⇒ ★機械擋拆掉之後，**才看得到真正的問題**：**求居的 util 夠不夠高**。
⇒ ★★而你不解釋原因**完全正確**（沒量就不說）——
  下一張票照我們的老規矩：**決策問題先 dump per-option util 逐筆**，不要靜態讀 code 開藥。
⇒ ★★★而**先別急著把 util 調高**：低 util 要先問**它是不是 genuine**
  （求居真的划算嗎？對一支靠貿易活得好好的隊，可能真的不划算）——
  ★**禁 crank** 那條在這裡是最容易破的。
```

# ④ ★入庫那半：我判**實質達成**，而字面有問題（我說清楚）

```
我寫的重撿條件是：「**這一刀落完後，若那 4 支真的到場的隊任務仍被置換**」
⇒ ★**字面上判不了** —— 因為 after 的到場數是 **0**，沒有「到場者」可看。
⇒ ★★但你給的證據**繞過了這個問題**：**抵達時任務被換掉**這件事**兩側都在**，
  只是**兇手換人**：before ＝ 覓食(@80)｜after ＝ **建設**（同為 50，走**同層 self-replace**）
⇒ ★★★**判：實質達成，撿起來** —— 而**形狀要更新**：
  它**不再是**「被高優先序插斷」，是「**被同層的東西就地替換**」
  ⇒ 那是 `try_set` 同層 self-replace 那條路，**不是優先序的事** ⇒ **新的一張票，不要塞回本票**。
★而我記這一筆：**我寫的重撿條件用了一個會歸零的量當主詞** ——
  ★★下次寫條件要用**不會消失的那個量**（「被置換的次數」而不是「到場者之中被置換的比例」）。
```

# ⑤ merge 還卡兩格（★不是這封信要你做的，只是別以為可以進了）

```
①**逃跑 +39 的下游**：戰鬥次數／滅團數／敗北方下場 before-after
②**幀數 321→518 的歸因**（`unified.rank.calls`／`try_set` 呼叫數 before-after）
```
