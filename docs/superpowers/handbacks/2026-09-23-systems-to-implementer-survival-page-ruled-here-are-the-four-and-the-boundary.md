---
from: systems
to: implementer
status: open
slice: 票B 第 2 批（生存頁）
topic: ★**blueprint 裁了，而且是「現在定，不等走查簽核」**：生存頁回答「**這一隊現在活得下去嗎**」的四題｜★★**而它跟常駐狀態列有重疊，那一格我用 HOW 裁掉**：狀態列＝一眼四件事（誰／在哪／在做什麼／幾時），生存頁＝**那一眼看不完的部分**（家在哪、離家多遠、為什麼）⇒ **不是兩個真相源，是同一件事的兩個解析度**｜★★★blueprint 的第②題直接吃掉你找到的那個 `focused_member`（「含被聚焦的那個人」）—— **你擺出可選項、他挑了一個**，那正是那封信該有的用法
---

# ★一、裁定原文（★四題，我不改字）

```
生存頁回答「這一隊現在活得下去嗎」：
  ①糧撐幾天（跑道與趨勢，★不是收支明細）
  ②人的狀態（飢餓／傷／士氣，★含被聚焦的那個人）
  ③身在何處、家在哪（位置、據點或營地、離家多遠）
  ④當下在做什麼、為什麼（現行任務與目標）
★★界線（他定）：生存＝「會不會死／幾天內」的急迫讀數；經濟＝「怎麼賺」的流量與價格
   ⇒ ★★★糧食出現在兩頁，而【問的問題不同】—— 這句話請寫進 code 註解，
     否則下一個人會把它當成重複而「順手統一」掉
```

# ★★二、宣告欄位（HOW，我定；★四個詞對四題）

```
_page_skylight_fields(0) ⇒ ["糧食跑道", "人的狀態", "位置與家", "任務與目標"]
★命名原則：★★用【問題】不用【資料表名】—— 天窗印出來的是給玩家看的
★★★而它們是【宣告】：接出一欄就從這裡拿掉，未接的照印天窗（票B 的既有規矩）
```

# ★★★三、與常駐狀態列的重疊 —— 這一格我用 HOW 裁掉，不回去問

```
狀態列（頁外常駐，text_ui_main.gd:689-694 那兩行）：
  Team# @ (q,r) [勢力]  ／  狀態: <task_summary>  疲勞: N%
blueprint 的 ③④ 看起來跟它重疊
★我的裁定：★★狀態列＝【一眼四件事】（誰／在哪／在做什麼／幾時）—— 它的價值是「永遠在」
  生存頁 ③④ ＝【那一眼看不完的部分】：家在哪、離家多遠、為什麼是這個任務
⇒ ★★★不是兩個真相源，是【同一件事的兩個解析度】
   —— 而判準是：狀態列那兩行【不動】（票A 的 P1-b 零損失還在守它）
```

★**而有一條我要你寫死在 code 註解裡**：

```
★狀態列與生存頁【不得各自去查一次】—— 同一份 _cached_snapshot，兩個 render 函式
⇒ ★★否則它會變成兩個來源然後 drift（票② §1 逐字警告過，我們今天已經在別處看過兩次）
```

# 四、第 2 批的範圍

```
①把 _page_skylight_fields(0) 填上那四個詞
②把「未分類」區塊裡屬於生存的行搬到生存頁（★搬動不改字，同第 1 批）
   —— ★★而【哪幾行算生存】照 blueprint 的界線判：急迫讀數 ⇒ 生存；流量價格 ⇒ 經濟
   ★★★判不掉的【留在未分類】，不要硬塞 —— 未分類 N 不歸零不是失敗，硬塞才是
③接得出來的就接（例：②的「被聚焦的那個人」⇒ focused_member 是現成的查詢面 key）
   ★接一欄就從宣告拿掉；沒接的照印天窗
```

# 五、驗收（★沿用，不新增）

```
Q1 讀點走查詢面｜Q2 成對對照（拿掉一欄 ⇒ 那一格變天窗，不是整頁壞掉）
Q3 同種子兩 tick 的 diff 非空且印出哪一行變了｜Q4 [UI-SKYLIGHT] count 變小且印出來
Q5 world-fp 不變
★★★而每一格照新規矩：先回答「什麼注射會讓它紅」
★機器還是我的（合併後電池快跑完了）⇒ 跑完我回你一行，你一次跑第 1＋2 批
```
