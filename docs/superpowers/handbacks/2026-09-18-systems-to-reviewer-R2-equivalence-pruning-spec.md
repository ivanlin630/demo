---
from: systems
to: reviewer
status: consumed
slice: `*multi` 成本 — 第二軸（等價剪枝）新 spec
topic: ★這次我**把判準寫在量之前**（量完照表走，不准挑一個好看的方向）｜★★請專門打三處：①「算了但沒人讀」那一欄會不會又是一份**手抄清單** ②2-b（選中結果逐字相同）的**母體**怎麼算才不會恆空 ③我把「沒有便宜的省法就結案」寫進判準表 —— 這一格**擋不擋得住我自己**
---

# 一、要審的

`docs/superpowers/specs/2026-09-18-equivalence-pruning-inside-one-gather-HOW.md`（新檔）。
背景：前一票量到「讓 gather 少跑」**整條路不可用**（純讀路徑仍有**合法的** RNG 抽取，拿不掉）
⇒ 換軸：**呼叫次數不動，同一次呼叫裡少做事**。

# 二、★請專門打這三處

```
(1) §1 我要求「算了但沒人讀」的欄位單獨列一欄。
    ★★而【怎麼知道沒人讀】？如果做法是「列出讀者清單再比對」——那又是【清單保證】，
      而今天我們已經被清單咬了三次（寬一格／窄一格／漏測試端）。
    ⇒ 請幫我想【構造式】的做法（我想到的是：讓那個欄位的 getter 記一次「被讀過」，
      跑完比對「算過的次數 vs 被讀過的次數」——但那會改變 production 形狀，我不確定值不值得）。

(2) 2-b「剪枝開關關 vs 開，選中結果逐字相同」——★母體是什麼？
    ⇒ 如果剪枝在那一輪【一次都沒有真的剪掉任何東西】，兩邊當然相同 ⇒ ★★這一格會【恆綠】。
    ⇒ 我目前只寫了「母體要印」，★★★但我沒有寫【母體為 0 時要紅】。這是不是一個洞？

(3) §1 的判準表最後一列我寫了「成本平均攤在很多小段 ⇒ 回報【這塊沒有便宜的省法】並結案」。
    ★★這是寫來擋我自己的（不許為了交差做一個省 3% 的剪枝）。
    ⇒ 請判：它是**會生效的判準**，還是一句**沒有機械觸發條件的自律**？
      如果是後者，該怎麼寫才會【到那一格自己紅】？
```

★**沒有 CLEAN 我不 dispatch**。而 §1 那個量測**不含設計選擇**（純取證），我會先請 implementer 去量 —— ★這一次我**先寫明**這是取證、不是動工，免得又變成「沒過 R² 就派工」。
