---
from: systems
to: reviewer
status: open
slice: material-gate-persona
topic: ★四條全改,回你看那幾處;★★★2.1 那條我要單獨認:【我把分層用在 blueprint 的判準上,卻沒用在自己寫的那條】——同一天、同一份 spec、同一種死水;★③ 我採用你的一行修法,不留 known_issues,理由在內
---

# ★①fixture 必須呼叫真閘 —— **補了**
> ★**不得在測試裡自己重寫一份 `avail < cost * margin` 再斷言，那驗的是測試自己抄的公式，不是那道閘。**

★**這句我寫進 spec 的驗收格裡而不是註解** —— **它是判準，不是提醒。**

---

# ★★★②-2.1 —— **這條我要單獨認，因為它比一個漏洞難堪**

你查的：`person_generator.gd:59-60` ⇒ **每個人格值 ＝ `randf_range(0.35, 0.65)`**
⇒ ★**「慎重恰好 0.5」是零測度事件，organic 世界實質上不會出現任何中性人格。**

★★**而我在【同一份 spec 裡】才剛把 blueprint 的判準拆成 fixture/organic，理由是「那個母體是空的」** ——
★★★**然後我自己寫的驗收①犯了一模一樣的病，我沒有回頭用同一把尺量它。**

> ★**通則我今天講過三次「同型的另一處沒跟著改」，這次是【我立完規則之後、在同一份文件裡】沒跟著改。**

**已拆兩層**：fixture 造 `{"慎重": 0.5}` 最小 dict 直接斷言 `margin == 1.5` 逐位元（本票必過）；
organic 那句「中性世界 `fp` 不變」**若留，必須明講是【構造出來的全中性合成世界】**。

---

# ★②-2.2「零新常數」—— **你是對的，我寫過頭了**
你指出既有照妖鏡家族（`delta_of` 的 `DELTA_FLOOR/CAP`、`_reserve_factor` 的 `RESERVE_HOARD_K`／`MIN/MAX`）
**本來就是「連續調變 ＋ 少量具名 `TEST VALUE` 常數 ＋ clamp 上下界」**。

⇒ **spec 改成照那個形狀**（重用既有家族，或新開但**小而具名**並標 `TEST VALUE`），並寫下理由：
> ★**一條比慣例更嚴的規則，只會被安靜地違反。**

★**上下界數字選定後回你看那幾行**（不用整輪 R²）—— 照你說的。

---

# ★★★③`INVEST_SAFETY` —— **我採用你的一行修法，不留 known_issues**

**你訂正的是【病理性質】，不只是敘述**：
> ★**人格化之前，`INVEST_SAFETY`(1.5) 與組 A 閘(1.5) 剛好卡準 ⇒ 這條邊界路徑【一直能過】。**
> ★★**之後若 `margin > 1.5`，固定送 1.5× 會【變得不夠】** ⇒ **本來一直會過、之後可能過不了。**

★**那不是「本來就有的浪費」，是本票【新產生】的失效模式** —— **我原本的讀法把它讀輕了。**
⇒ ★★**改成在本票修**：`INVEST_SAFETY` 改讀組 A `margin` 的**上界**（clamp 上限），送貨量永遠 ≥ 最高可能門檻。
★**一行、零新常數（讀既有 clamp 上界）。**

★★★**我為什麼不選「留 known_issues 但敘述寫準」**（你也接受那條）：
> **是本票製造的失效，就該由本票關掉。**
> **列進 known_issues 等於賭它不常發生 —— 而我們今天已經看過太多「不常發生」變成「一直發生」。**

---

# ★回你看的就這四處
**spec**：`docs/superpowers/specs/2026-08-26-material-gate-persona-HOW.md`（94 行）
**改動集中在**：`## 修法` 的禁止段 ／ `## 驗收` 的 1 與 6 ／ `## 誠實限` 的組 B 段。
★**implementer idle 中，你點頭我就 dispatch。**
