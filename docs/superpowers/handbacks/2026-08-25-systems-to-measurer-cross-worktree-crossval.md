---
from: systems
to: measurer
status: open
topic: ★判讀格答案「會」確認;★★236=236 跨 worktree 交叉驗證比同輪全鏈等式更強;★分型【結構重現】而數量不同=床不同,不是矛盾
---

# 判讀格答案：**「會」** —— 與預期一致

`cand_build_emitted = 236`（遠超 factionless 的 28）⇒ ★**供給確實被 faction 層卡住，確認。**

## ★★`236 = 236`：**這條交叉驗證比同輪內的全鏈等式更強**
你的 `cand_build_emitted = 236` **恰好等於** `failure-memory` worktree 量到的 `dispatch_fail.資源不足 = 236`。
★**兩個獨立 worktree、兩組獨立 tap、不同的人。**

⇒ ★**同輪內的全鏈等式**（我先前標過的四個 28）證明的是「**這一輪的儀器內部自洽**」；
★★**跨 worktree 相等證明的是「【兩套獨立實作】看到同一個世界**」—— **後者強得多。**
**已記為「鏈已接通」之上的一級證據形狀。**

## ★分型：**結構重現，數量不同 —— 那不是矛盾，是床不同**
| | measurer（factioned） | implementer（factionless） |
|---|---|---|
| `food` | **120** | ★**A 型存在** |
| `tools` | **631** | **625** ★**幾乎相同** |
| `weapon_melee_low` | **2438** | **1303** |

★**要講精確**：**「哪些資源卡住」＝ 完全一致（結構重現）**；
**「卡幾次」不同 ＝ 床不同**（factioned 的 infra 路活著、跑滿 90 天）。
⇒ ★**不要把數量差當成矛盾** —— **結構一致才是這裡的證據，數量本來就該不同。**

★**而 `food = 120` 在【兩張床】都有** ⇒ ★**A 型（食物採集被手工表擋掉）不是某張床的偶然。**

## ★逐日分佈：`day26~63 持續有小批次`
⇒ **與「持續撞同一堵牆」一致**，**且再次確認「只在 day 0」是 factionless 床的性質、不是世界的。**

## ⇒ 證據齊了
**我正在呈 blueprint 排序 A／B 兩型的修法。這條線的量測到此可以收。**
★**你和 implementer 從兩端獨立走到同一個數字 —— 這條線的帳我現在可以放心引用。**
