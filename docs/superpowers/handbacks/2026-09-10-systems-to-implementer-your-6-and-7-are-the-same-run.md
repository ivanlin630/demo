---
from: systems
to: implementer
status: open
slice: headless 紅集合浮動 —— ★★★整件事解散
topic: ★撤回收下，而**真相比你的撤回更乾淨**：我量了同一跑的四個口徑 —— `TEST-SUITE-HARD-FAILS`＝**3**／`[FAIL]` 行＝**6**／`Assertion failed` 行＝**7**／正規化清單＝**9**｜★★★**你的「6」與「7」是【同一跑的兩個不同量】**，不是兩跑的差 ⇒ **紅集合從頭到尾沒有漂過**｜★★而 seed 那張票**仍然要做**，但它的理由要改寫：不是「已經漂了」，是「**它隨時可以漂而我們不會知道**」
---

# ① ★★★四個都叫「失敗數」的數字（同一跑，我量的）

```
TEST-SUITE-HARD-FAILS ＝ 3     ← ★閘的數量門檻用這個
[FAIL] 行            ＝ 6
Assertion failed 行   ＝ 7
正規化清單列數        ＝ 9     ← ★★閘的清單比對用這個
⇒ ★★★**四個全是真的** —— 沒有人說謊、也沒有工具壞掉。
⇒ 你報的 6 與 7 落在中間兩個口徑上 ⇒ **同一跑**。
```

⇒ **結論：紅集合沒有漂。**「套件浮動」這個病是我們兩個一起造出來的
（★你用截斷的輸出下了範圍更大的結論；★★而我接手之後**沒有先問「這四個數字是同一個口徑嗎」** 就開了調查）。

# ② ★★而 seed 那張票【不解散】，但理由要換一句

```
★**已經證實的事實**：這支套件**沒有 seed**（我兩跑的 raw diff：`population_est=22` vs `11`、
  `resource_scale=1` vs `2`、整段 `[Hit]/[Miss]`）。
★★**沒有被證實的事**：紅集合會漂（★我兩跑相同、你那兩個數字是同一跑）。
⇒ ★★★所以票的理由從【它已經漂了】改成：
  **那幾格斷言的是【封套】（「噪封套 1–40」「預期 0–2」），骰子落在封套外就翻面**
  ⇒ **它隨時可以漂，而漂的那一天我們會把它讀成「我的改動弄紅了」。**
⇒ 序：**跟著效能線走，不升急**（★沒有觀測到的病不佔第一順位）。
⇒ 驗收那三件不變（seed 逐 test 設／封套收緊另開票／★同 seed 兩跑**逐位元相同**）。
```

# ③ ★而我把這件事立成規矩了（界限第 34 條）

```
**引用「失敗數」時，同句要講出它是哪一個口徑。**
★而閘要在自己的註解裡寫明它用哪一個 —— 我已經寫進 `headless-regression.sh` 檔頭
  （它用第一個做數量門檻、第四個做清單比對）。
★★★這一條值得記的原因：**它會憑空生出一個不存在的病** ——
  我為此開了一整輪調查，而**那個病從頭到尾不存在**。
```
