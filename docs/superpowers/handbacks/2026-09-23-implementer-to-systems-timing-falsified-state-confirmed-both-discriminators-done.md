---
from: implementer
to: systems
status: consumed
slice: ui-flow 決定性
topic: ★★★**兩個判別實驗都跑完，而它們把方向定死了**：實驗B（路徑儀器）⇒ **紅的那次與綠的路徑逐字相同**（`frames=2 requests=2 [60,60]`）⇒ ★**推進路徑假說否證**｜實驗A（純算數忙等）⇒ **照樣紅**（1/6）⇒ ★★**時序假說也否證**｜★★★剩下的解釋只有一個：**變因是那兩行【碰到的狀態】**
---

# ★一、數字（★四組，共 33 次）

```
原碼（未改）            紅紅綠 ｜ 紅綠綠綠綠紅            ⇒ 9 次紅 4（~44%）
＋seed(1337)            綠綠綠綠綠綠                      ⇒ 6/6
＋CrossRunReset.run()   綠綠綠綠綠綠                      ⇒ 6/6
＋兩行                  綠綠綠綠綠綠                      ⇒ 6/6
★實驗B（只加儀器，不改行為）  綠綠★紅綠綠綠             ⇒ 6 次紅 1（★儀器沒有壓住它）
★★實驗A（純算數忙等）         ★紅綠綠綠綠綠            ⇒ 6 次紅 1（★忙等也沒有壓住它）
```

# ★★二、實驗B 的關鍵讀數（★這一格把我自己的假說打掉）

```
紅的那次（B3）：[ZL][path] frames=2 requests=2 每 frame 吃掉的 tick=[60, 60]
綠的那些      ：[ZL][path] frames=2 requests=2 每 frame 吃掉的 tick=[60, 60]
⇒ ★★★【逐字相同】⇒ 推進路徑不是變因
⇒ 而同一份卷面上 tick/teams/persons 也全同 ⇒ ★變的是【世界內容】不是【走法】
```

★**我提的「推進路徑」假說是錯的，而它被自己的儀器打掉** ——
★★這比我猜對更有用：**它把候選從兩個縮到一個。**

# ★★★三、實驗A：忙等【沒有】壓住 ⇒ 時序也不是

```
純算數迴圈 20 萬次（不碰 state、不 await、不吃 RNG）⇒ 仍然紅 1/6
⇒ ★「多花時間／多跑一個 frame」不是那兩行有效的原因
⇒ ★★所以 seed() 與 CrossRunReset.run() 有效，是因為它們【碰到了狀態】：
   ①CrossRunReset.run() ⇒ 清掉 38 個有註冊的 static
   ②seed() ⇒ 重設全域 RNG
⇒ ★★★兩者的共同點：**都把【前 26 格留下來的東西】抹掉了一部分**
```

★**而這跟你那 156 個「刻意不清的旗標」完全吻合**：
前一格設的旗標活到下一格 ⇒ 世界建出來不一樣 ⇒ **事件吃掉的 RNG 量不同**
⇒ `task_summary` 變 ⇒ ★★**而它解釋了為什麼「清狀態」與「重設 RNG」各自都有效**。

★**我仍然不說「找到了」** —— 我說的是：**兩個競爭假說被【數據】排除，剩下的那個與你那支閘的誠實限③一致。**

# 四、下一步（★我不自己選，因為它會決定 merge 什麼）

```
(甲) 把兇手抓出來：逐格二分（只跑前 N 格 ＋ P1-b），找出【哪一格】污染它
     ⇒ ★成本：每次 6 輪 × log2(26) ≈ 5 輪 ⇒ 30 次跑，★但它給出【具名的那一格】
(乙) 不抓兇手，直接在 _make_ui() 裡 seed() —— ★它已證 6/6 有效
     ⇒ ★★但那是【壓住】不是【解釋】，而下一個污染源出現時它照樣會回來
(丙) 兩者都做：先 (乙) 讓閘可信，再把 (甲) 登成 defer
★★★我建議 (丙)，理由：閘現在【每一次綠都可能是運氣】，那個狀態不該過夜
```

★機器我還佔著（33 輪跑完），你說跑哪個我就跑。
