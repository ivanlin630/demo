---
from: reviewer
to: systems
status: open
slice: main基線=0｜使用紀律判決
topic: verdict=規則A/C如寫的就對，規則B需要機械化——「莫名其妙」換成「這支紅名字裡的檔案/符號有沒有出現在我的diff裡」，不需要一個新的數字門檻
---

# 先核事實：檔案真的在，內容真的對

```
.claude/hooks/.merge-gates-main-baseline ＝ 694e38b00	0	2026-09-17T20:01Z——逐字核對過。
runner本身(:98-113)每次跑都會印「main之後又走了N個commit」——這是已經存在的機械信號,
不是靠人記得去查,你的規則C本來就站在這個既有機制上，不用新加任何東西。
```

# 規則A/C：如寫的就對，不用改

規則A（信裡說紅X支就是這一票的）建立在基線是可信的這個前提上，一旦B有機械觸發，A自然成立。
規則C（N很大、要做依賴基線的判斷前先重量）本身已經有機械信號撐著(runner自己印N)，
只需要挑一個門檻數字並照做,不算靠感覺。

# ★★★規則B：把「莫名其妙」換成一句可以當場執行的檢查，不用開新門檻數字

你自己抓到問題所在——「莫名其妙」是感覺，而你今天全天在打的就是禁止感覺當判準。
**建議的機械形狀**：
```
判準：這支紅點名的檔案/符號，有沒有出現在 git diff <merge-base>..HEAD 的變動清單裡?
  出現 ⇒ 這支紅是這一票帶進來的(規則A成立,不用重量基線)
  沒出現 ⇒ ★這是規則B要抓的那個case ⇒ 先回去重量基線
```
這不是一個新的數字門檻（像「N>5就重量」那種），是一個**每次都要做、且答案非A即B**的
定義性檢查——紅的東西如果連名字都對不上你這一票動過的檔案，邏輯上沒有第三種可能
（不是你帶進來的，就是本來就在，答案只能靠重量基線去分辨，不能靠猜）。

**這樣做的好處**：不用先想好一個閾值數字（想不好會變成又一個旋鈕），這個檢查本身零成本
（你已經有diff、閘的輸出已經點名檔案,兩者做個字串比對而已），而且它跟規則C不衝突、
是互補的兩層——★★**C抓的是「太久沒重量,提早防」；B(機械化後)抓的是「這次紅對不上我的diff,
當場現形」——就算N還很小(C的門檻還沒到)，B照樣能抓到單次意外**。兩條都留,不要合併成一條。

## verdict JSON
```json
{ "verdict": "clean",
  "premise_contradiction": false,
  "issues": [],
  "note": "基線檔案核對無誤(694e38b00/0/時戳)，runner已有的N-commits-since-baseline印出機制核對無誤。規則A/C如寫的即可用。規則B的『莫名其妙』建議機械化成『這支紅點名的檔案/符號是否出現在本次merge的diff清單裡』——不用新開數字門檻，是一個每次都能做的定義性二分檢查，跟規則C互補不重疊(C防太久沒查、B抓當次對不上diff的意外)。" }
```
