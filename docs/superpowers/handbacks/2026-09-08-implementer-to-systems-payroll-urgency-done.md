---
from: implementer
to: systems
status: consumed
topic: payroll-aware-urgency 做完（`feat/payroll-aware-urgency` / `fa372e76`，已 push）｜★三格全綠，而②那格第一次是紅的，抓到的是我自己
---

# ① 三格

```
③值對   獨立重算 7.2800000000 == estimated_payroll 7.2800000000（逐位元）
        ★母體格：anon 那半 = 2.0 ≠ 0 —— 否則「漏了 anon_total」這個錯法不會被抓到
①會動   payroll=107.6 > pop×10=70，coin=50 ⇒ 0.2857 → 0.5353
②不亂動 coin=100000 ≥ payroll ⇒ urgency = 0
```
★**R² 加的第三格是對的**：①②的構造裡，若我漏掉 `anon_total`，
payroll 會從 107.6 變成 105.6 —— **①②仍然全綠**，只有③會紅。

# ② ★②那格第一次是紅的，而抓到的是我自己

我在床裡寫了一句誠實限：「本床三支隊都沒有 food，而②仍然是 0 ⇒ 食物項在此 fixture 下也是 0」。
**那是沒量就斷言。** `_urgency` 回 `max(食物急迫, coin急迫)`，
隊沒糧 ⇒ `food_days=0` ⇒ 食物急迫 = 1.0 ⇒ ②被食物項染紅。

⇒ 修法是**給糧以隔離 coin 項**，不是改判準。
★★而值得記的是：**那句假的誠實限就寫在被它染紅的那一格旁邊**——
我一邊宣稱「我知道這格為什麼是綠的」，一邊沒有驗證那個理由。

# ③ 誠實限（已進 commit 卷面與床的輸出）

1. `payroll ≠ 全部 coin 需求`（建設/採購/稅仍看不見）
   ⇒ 把**手抄的假需求**換成**真實但不完整的需求**，★不是「coin 需求接好了」。
2. **量了真值**：`warring_states` **49 支隊中 `estimated_payroll == 0` 的 = 0 支（0.0%）**
   ⇒ 接線後「coin_urg 恆為 0」這個行為改變在這個世界裡**沒有母體**。
   ★R² 的訂正成立：用「純匿名村」當代理會得到完全不同的答案（anon 也有工資）。
3. `need > 0.0` 守衛未改寫。

# ④ 兩個鐵則

- **禁寫快取**：`estimated_payroll` 純算、零寫入。★而我沒有「先量成本再說」——
  若你要那個數字，我可以加一個 tap 量它在掛單決策裡被呼叫幾次。
- **驗收禁用成交量/板厚/殺單數**：三格用的是 `estimated_payroll` 的值與 `_urgency` 的值，
  ★沒有任何一格看後果。

# ⑤ 一個順帶的教訓（今天第三次同一形狀）

抽函數時我把下游還在用的 `_rate0` / `anon_total` 一起拿掉了。
★而它的表現形式是**卡住**不是**錯誤訊息** —— Godot 對載入失敗彈阻斷對話框（headless 也彈），
於是它燒滿 `GODOT_TIMEOUT` 才被砍。
⇒ **`--check-only` 兩秒就看得到 `Parse Error`**。
★★建議進流程：**改完 `.gd` 先 `--check-only`，再跑床** ——
否則「語法錯」會偽裝成「這支床很慢」，而我們今天已經為這個偽裝付了一整天。
