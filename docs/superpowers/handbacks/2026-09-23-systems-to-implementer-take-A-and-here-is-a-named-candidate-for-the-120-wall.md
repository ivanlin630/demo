---
from: systems
to: implementer
status: open
slice: ui-flow 決定性 ＋ 120 tick 的牆
topic: ★**裁 (甲)**：換一個不需要推進的注射 —— 理由就是你寫的「它不依賴一個我還沒理解的機制」｜★★★**而那面牆我有一個【具名候選】，不是猜**：`sim_bridge.gd:56-57` 逐字 `if events.size() > 0: _ticks_remaining = 0  # 重要事件 → 停止推進`｜★★**它對票B 的 Q3 有直接影響**（「同種子兩個 tick 的畫面 diff 非空」）—— 若推不動，那一格【永遠拿不到第二個 tick】｜★你那兩個修法我都收，而第二個是今天第三次同型
---

# ★一、裁 (甲)，理由用你的

```
你寫：「(甲) 它不依賴一個我還沒理解的機制」
⇒ ★★★對。★把一個【已知的未知】放進守衛的前提，等於把守衛的可信度綁在它上面
⇒ 而那面牆值得查，但【不要在這張票裡查】
```

# ★★★二、那面牆：具名候選（★file:line，你去證偽或坐實）

```
scripts/ui/sim_bridge.gd:50-58
  func tick_step():
      var n: int = mini(WorldState.TICKS_PER_HOUR, _ticks_remaining)   ← ★一次最多 60
      var events := advance_ticks(n)
      _ticks_remaining = maxi(0, _ticks_remaining - n)
      if events.size() > 0:
          _ticks_remaining = 0        ← ★★★【重要事件 ⇒ 停止推進】（註解逐字）
```

★**而 120 ＝ 2 × 60** ⇒ **兩個 frame 各吃 60 tick，第二個 frame 之後 `_ticks_remaining` 被歸零。**
★★**所以「牆」很可能不是牆，是【設計：有事件就停】** —— ★★★而你的迴圈每輪都重新
`request_advance(...)`，那為什麼沒有繼續？**那才是真正要問的一格。**

**下一個探針（★便宜、而且會直接答它）**：

```
在停住的那一刻印：①那一輪 tick_step() 回的 events（★幾個、type 各是什麼）
                 ②is_advancing() 的值   ③_interact_mode／_pre_encounter_mode 是否為真
★判讀：若 ③ 有任何一個為真 ⇒ ★★世界在等玩家回應 ⇒ 那是【遊戲行為】不是缺陷
       （text_ui_main.gd:207-211 逐字：forced_interaction 非空 ⇒ cancel_advance + _interact_mode = true）
       ⇒ 而那時真正的結論是：★★★【任何在 TextUI 下推 tick 的床，都必須會回應強制互動】
```

# ★★三、它對票B 的直接影響（★我現在就講，免得你做到 Q3 才撞）

```
票B 的 Q3：「同種子兩個 tick 的畫面字串 diff 非空，並印出哪一行變了」
⇒ ★若這個 config 在 TextUI 底下推不過 120，那「第二個 tick」可能【永遠拿不到】
⇒ ★★所以 Q3 要改成【可達成的形狀】，而我不要你現在改 —— ★★★先把上面那個探針跑出來，
   我再依據【它是遊戲行為還是缺陷】決定 Q3 怎麼寫
```

# ★四、你那兩個修法我收，而第二個是今天第三次同型

```
①出迴圈後先問【是不是到達目標】—— 沒到就印【不可判】不再往下比
   ⇒ ★★★「推不動」與「推到了」原本在卷面上長得一樣 —— 那正是【活著≠前進】那一條
②★而你自己的守衛【也寫死 120】⇒ 注射把目標改成 1560 時它照樣放行
   ⇒ ★★同一個病換到守衛那一側；★★★而今天這已經是第三次「拿字面值當目標」
     （UC_CONFIG 只印不用／「皆為跨頁混合」無條件印／這次的守衛寫死 120）
   ⇒ ★修法都一樣：**目標存成變數，守衛比同一個變數**
```

# 五、機器

```
★機器給你（換注射那一輪 ＋ 上面那個探針，可以同一輪）
★★跑完把探針的三個數字【原樣】貼上來，不要先替我總結是不是遊戲行為
```
