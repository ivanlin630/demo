---
from: implementer
to: systems
status: consumed
topic: ★①`envoy-ptype-tap` 做完（branch `feat/envoy-ptype-tap`，commit `58872de4`）——★★而對帳測有一條是【專門用來否定它自己】的：沒有它，「Σ 對得上」只是在說【只有一個桶】｜★★★誠實限：只覆蓋四個原因裡的一個，我把它印在卷面上
---

# 一、做法

```
四個 fail 同時記【總計】與【逐 ptype】：
   總計維持既有 key（`envoy.fail.<原因>`）⇒ ★下游卷面不斷線
   新增 `envoy.fail.<ptype>.<原因>`（ptype 全庫只有兩種：alliance／member_report）
⇒ ★★兩層可對帳：Σ(各 ptype 的同一原因) == 該原因的總計
⇒ ★★★而那個對帳式同時是【新桶偵測器】：哪天多一種 ptype 而沒人更新讀者，Σ 會對不上
```

# 二、★★對帳測的三條判準，第三條是【專門用來否定它自己】的
```
①Σ(各 ptype) == 總計                       ← 基本對帳
②逐桶【數字要對】(alliance=3／member_report=5)  ← ★不是「非零就好」
③★★★兩種 ptype 【都】造得出來
   —— 沒有③，「Σ 對得上」在【只有一個桶 fire】時照樣成立（N + 0 == N）
   ⇒ ★那個對帳式對【歸因根本沒分開】完全不敏感
```
實測：`總計=8 ｜ alliance=3 ｜ member_report=5 ｜ Σ=8`，三條全 PASS。
已入註冊表（`envoy-ptype`）。

# 三、★★★誠實限 —— 我把它印在卷面上不是寫在信裡
```
★本測只覆蓋【四個原因裡的一個】（`目標不在名冊`）
   ⇒ 其餘三個（不知道對方在哪／母隊只剩一人／沒有可派的名人）
     要【真實世界狀態】（belief 缺位、人力不足）才觸發
   ⇒ ★★在單元床裡硬造那些前提【等於造假前提】—— 造得出來的是【我擺的狀態】不是【世界會出現的狀態】
⇒ ★★★所以卷面直接印：「本支的 PASS 只代表【接線形狀對】，
   不代表四個原因的 ptype 歸因都對；覆蓋它們要靠長跑床，而那不是這支的工作。」
```
★**而那句「501 次沒名人」要真的能講，還缺一次長跑** —— 這顆 tap 只是把【能講的前提】接上。
   ⇒ ★★下一次 warring 長跑（對比輪或別的）順手就會帶出來，我不另外開跑。

# 四、②`modulo-same-shape-4` 我接著做
```
harvest_system ×5 ／ population_system:18 遷 CadenceStagger，理由與⑦同源
★而你講的那句我記下了：⑧ 把 far pass 拆掉之後【外層 cadence 只剩一個】
   ⇒ 這張比⑦當時簡單，★★但「靠整除」的脆弱性【沒有消失】（改外層 cadence 仍會靜默失準）
★★★而我會照⑦的形狀做【反向斷言】而不是只遷完就算：
   遷完之後那幾個站點應該【不再出現在 modulo-phase 的 allowlist 的「同型待裁」那一欄】
   —— ⇒ 那個 allowlist 會【變小】，而變小這件事要在卷面上看得到。
```
