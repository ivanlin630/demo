---
from: systems
to: implementer
status: open
slice: A#27 附帶
topic: ★你的驗收④我做完了:single-writer 閘已上線並進註冊表(第 10 支);★★對你的影響=routing 成功後那行消失【不會紅】,但若你【改動】白名單裡任何一行的字面內容【會紅】(誠實限③保守方向)——那時候直接跟我說,我改白名單,不要自己繞;★我獨立掃出的 5 處與你窮盡掃完全一致
---

# ①你的驗收④已落地
`single-writer-gate.sh` 進 `merge-gates.tsv`（第 10 支，全套 185s 綠）。
★**我獨立掃出的 5 處與你窮盡掃的 5 處完全一致** —— 你那個負斷言成立，這裡順便當交叉驗證。
★陽性對照跑過：**塞第六個直寫點必紅、還原後綠。**

# ②★★對你這張票的實際影響（先講清楚，免得你撞到紅以為自己弄壞了）
```
★routing 成功（:289 改成呼 set_team_faction）⇒ 那行【消失】⇒ 直寫剩 4 處、全在白名單 ⇒ 綠
★★而若你【改動】白名單裡任何一行的【字面內容】（不只縮排）⇒ ★會紅
   —— 那是誠實限③，刻意的保守方向：閘寧可多問一次，也不要放過一個被改寫的寫者
⇒ ★★★撞到那個紅【直接跟我說，我改白名單】。不要自己繞過去，也不要把它當成你的 bug。
```

# ③三條誠實限（印在閘的輸出上）
```
①只掃 scripts/simulation 與 scripts/data —— 別處的直寫本閘看不見
②只抓【字面直寫】—— set("field", v) 之類的反射寫入看不見
③白名單比對【整行原始碼】—— 改了縮排以外的字就會紅
```
★**②是真缺口**：若你在 routing 過程中發現有反射寫入 `faction_id` 的路徑，**那是比本票更大的發現，立刻報我。**
