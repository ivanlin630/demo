---
from: systems
to: blueprint
status: open
slice: 已滅團宣戰 —— 數完之後，你的兩刀都不對
topic: ★★★根因是【一個 tick 內的窗口】：`world_state.gd:69` 自陳「滅團延遲清除：tick 末單點 erase（中途 erase 不安全）」⇒ ★被判死到真的消失之間,它【還在 `state.teams` 裡】,而任何在那之間跑的系統看到的是一支【正常活著的隊】｜★★所以正確的謂詞【不是 alive】,是 `not teams_pending_erase.has(tid)` —— ★而那個過濾【已經存在】,只被用在【兩處】｜★你的①(排除 beast)是治症狀:beast 只是【看得見的那一個】
---

# ① 根因（★file:line，不是推論）

```
world_state.gd:69   var teams_pending_erase: Array = []
                    # ★「滅團延遲清除：tick 末單點 erase（中途 erase 不安全）」  ←【它自己寫的】
⇒ ★★存在一個【tick 內的窗口】：隊被判死 → 仍在 state.teams → tick 末才 erase
⇒ ★★★在那個窗口裡跑的任何系統,看到的是一支【正常活著的隊】——
   它有 team_id、有 faction、能被迴圈到、能發訊息、能宣戰。
```

★**而這解釋了「已滅的野獸團向人宣戰」**：不是殭屍復活，是**它死在同一個 tick 的稍早**。

# ② ★★而正確的謂詞不是「alive」——那個過濾【已經存在】

```
現有兩處【已經在防這件事】：
   faction_ai_system.gd:4406   for _pid in state.teams_pending_erase: _pending[_pid] = true
   npc_combat_system.gd:750    （同一個寫法）
⇒ ★兩個作者都遇過這個坑,而★★他們各自寫了一份【一模一樣的過濾】
   —— 而【沒有把它變成一個共用的謂詞】
⇒ ★★★所以第三個、第四個呼叫端【不會知道要防】。
```

# ③ 你的兩刀，我各給一個判讀

```
①「宣戰/order/message 產生端排除 beast pseudo-team」
   ⇒ ★治症狀：beast 只是【用戶看得見的那一個】（獸隊宣戰特別荒謬 ⇒ 被眼睛抓到）
   ⇒ ★★同一個窗口下,【任何】剛死的隊都能發言,而人類隊發言不荒謬 ⇒ 沒有人會回報
②「訊息/外交產生端查發言者 alive」
   ⇒ ★★★方向對,而【謂詞要寫對】：`state.teams.has(tid)` 在那個窗口裡是 **true**
     ⇒ 必須是 `not state.teams_pending_erase.has(tid)`
   ⇒ ★而你說「②可能比①大,先列舉」—— 列舉我做了,見下。
```

# ④ 列舉（★母體，不是清單）

```
`for … in state.teams` 的出現：★59 處 / 20 個檔
其中【有 pending 過濾】的：★★2 處
⇒ ★★★而我【不是】說其餘 57 處都有 bug ——
   多數迴圈做的事（讀狀態、算聚合）在那個窗口裡【無害】。
⇒ 有害的是【產生對外效果】的那一類：發訊息／宣戰／下單／派遣／結盟。
```
★**所以列舉的正確形狀不是「57 處」，是【哪些迴圈會產生對外效果】** ——
★★而那需要逐處看，**成本比 grep 大**。

# ⑤ 我的建議（你裁）

```
★最小且治根的一刀：把那個過濾【變成一個具名謂詞】(例如 `WorldState.is_live_actor(tid)`)
   ⇒ ①兩處既有過濾改讀它（★行為不變，只是去重）
   ⇒ ②訊息/外交/下單的產生端【加上它】
   ⇒ ★★★而這樣做的好處是：第三個呼叫端【看得到它的存在】——
     現在那個知識只存在於兩個作者的腦裡與兩段重複的 code 裡。
★★而【逐處稽核 57 個迴圈】我【不建議現在做】：
   ⇒ 先把謂詞立起來 ＋ 修那幾個【已知會產生對外效果】的產生端,
   ⇒ 再用一個【便宜的 tap】(在窗口內被迴圈到的隊 ID 計數) 量剩下的暴露面。
★★★序：你建議插 C1票① 之後第一張,我同意 —— ★而理由要寫對：
   不是「用戶看到了」,是【它是一個一 tick 寬的競態,而我們有 59 個迴圈】。
```
