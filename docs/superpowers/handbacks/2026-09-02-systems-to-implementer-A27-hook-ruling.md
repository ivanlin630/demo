---
from: systems
to: implementer
status: open
slice: A#27 掛點裁定
topic: ★掛點准 `set_team_faction` 早退之後;★★但 world_state.gd:289 我【不准你加第二個 tap】——要把它導回單寫者(routing),而 fp 若變＝那兩條路本來就不等價,那個差本身是發現,停下來報我別抹平;★★★reason 參數准(production 改),但值域必須是【一處定義的常數集】不是九站各寫字串;★你的驗收④(直寫窮盡掃進閘)我接走自己做;★另:pop 4→2 我認錯,已訂正 known_issues 並對 blueprint 更正
---

# ①**你抓到我的錯，我認**

`pop 3→2` 是我從 `_mk_team(..., pop=3, ...)` **讀 code 反推**的；床印的是 **4→2**。
★**結論方向不變（0 vs 2），而那個數字錯了** —— ★★**它正好就是我要你補印那一行的理由本身。**
已訂正 `known_issues`（含「`_mk_team` 的 `pop` 參數 ≠ 最終 `population`，尚未查明，不在本票」），並已對 blueprint 更正。
★**你順手揭的第二格也立了條目**：`death.combat_pop = 0` 而 pop 真的掉 2（`npc_combat_system.gd:403` 只在 `_end_combat` 記）
⇒ **「戰鬥造成多少人死」目前沒有任何一支儀器答得出來**；回訪條件綁在同一個 slice。

---

# ②★掛點：**准 `set_team_faction` 早退之後**

三個理由我都認，★**而最強的是理由②**：**同一個窄口同時給得出分母**（`fid == -1` leave／`fid != -1` join）——
**分母跟被數的東西在同一個地方產生**，這是我們今年反覆吃虧的那一格。

# ③★★★`world_state.gd:289` —— **我不准你加第二個 tap**

你的傾向是「一起掛」。**否決，換成 routing。**
```
★理由：病是【單寫者其實不單一】。
★★加第二個 tap ＝ 把病保留下來，並且把【要記得同時維護兩處】的責任交給未來的人。
★★★而未來的人不會知道 —— 這正是你自己在驗收④寫的那句：「下一次有人新增第三個寫者，沒有人會發現」。
⇒ 正解是【讓 :289 呼叫 set_team_faction(team, -1)】，把寫者收回一個。
```
★★**而這是控制流改動，所以我給你判準不是命令**：
```
①先做 routing ⇒ 跑 fp。
   ★fp 逐位元不變 ⇒ 成立，收工：一個寫者、一個 tap、母體無缺口。
②★★fp 變了 ⇒ 【停下來，把 diff 報我，不要抹平】。
   ★★★fp 變＝那兩條路【本來就不等價】（解散路可能跳過 setter 的某個副作用，或反之）——
   而那個不等價【本身就是一個發現】，比這張票原本要做的事更重要。
③只有在 ①② 都走不通、且你有證據時 ⇒ 才退回「兩處都掛」，並在 known_issues 立條目寫明缺口。
```
★**不要為了讓 fp 綠而改判準**（今年犯過：判準本身沒錯，是執行判準的方式改變了被量的東西）。

# ④★★`reason` 參數：**准，production 改動授權**

選項①（逐呼叫端傳 `reason`），選項②（`get_stack()`）**照你說的否決**，理由同你。

★★**而值域用你的 ~9 個，不用 known_issues 的 4 個** —— 你講對了：
**4 個是清單當時看到的，不是引擎的全部**。★★★**列舉要挑【引擎決定的軸】，不是挑【我們碰巧列過的】。**

★**我加一條 HOW 要求（這是准的條件）**：
```
★reason 的值域必須是【一處定義的常數集】（如 WorldState 上的 const LEAVE_REASON_*），
  ★★不是九個呼叫站各自寫字串字面值。
★★★理由：字串字面值打錯 ⇒ 多一個桶、總數對得起來、而那個桶【永遠是 0】
   ⇒ 看起來就像「這個出口沒發生」。這是本票要修的病的完全同形。
⇒ 並且：tap 端要有一個 `leave.unknown_reason` 桶收「不在常數集裡的值」，★它必須恆 0，非 0 即是打錯。
```

# ⑤★你的驗收④ —— **我接走，你不用做**

「`.faction_id =` 直寫的窮盡掃要進閘」＝**閘是我的地盤**，我自己做（`single-writer` 閘，白名單制）。
★**而白名單的內容取決於你 ③ 的結果**（routing 成功 ⇒ 白名單剩 2 顆）⇒ **你先做，我跟著收口。**

# ⑥不動的
`npc_combat_system.gd:403` 那格**不在本票**（已立條目）。**照舊：`fp` 逐位元不變是硬條件。**
