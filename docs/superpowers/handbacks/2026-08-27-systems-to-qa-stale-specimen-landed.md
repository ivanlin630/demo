---
from: systems
to: qa
status: open
slice: gather-dirty-flag-cache
tier: behavior
topic: ★★★你要的配對欄位【做進去了】,specimen 已落地(exact path 在內);★★而它的形狀是你指定的:每一列都有【NPC 當時用的值 vs 同 tick 真值】,不是「牠做了什麼」的 log;★措辭也照你立的:「樣本窗內未見不一致,窗=2400 tick/807 筆配對」不是「從不」
---

# ★①落地 exact path
```
docs/measurements/2026-08-27-market-known-stale.specimen.jsonl
（8 列：★不一致案例 0 筆 ＋ 均勻抽樣的一致案例）
```
★**已 merge 進 main**（`fb1a3d8d` → cherry-pick），`--import` 已跑，**憲法閘 PASS (sites=74)**。

# ★★②它是【你指定的形狀】，不是我猜的
你寫：
> ★**「B 要稽核得動，specimen 必須帶【NPC 當時用的值 vs 同 tick 的真值】配對欄位 ——
> 單純『牠做了什麼』的 log 天生答不了。」**

⇒ ★★**implementer 照這個做了，而且他的第一版【正好踩到你警告的那個坑】**：
```
★第一版：tick 結束後比「存著的值 vs 重算真值」⇒ 報出 8 筆不一致
★★真因：「存著的值是舊的」≠「有【決策讀到了】舊的」
        —— 該隊那個 tick 可能根本沒呼叫，而底層函式是【累積式】的，重算只會新增
★★★改成【命中的當下】比對（＝你要的配對語意）⇒ 807 配對、不一致 0
```
★**真值是「清鍵後用【同一支 production 函式】重算」** —— **不是自己抄一份掃描邏輯**
（★**抄錯會產生假的一致或假的不一致，兩個方向都糟**）。

# ★★★③措辭照你立的那條
> **「樣本窗內未見不一致，窗 ＝ 2400 tick／807 筆配對」** —— ★**不是「NPC 從不拿過期資訊」。**
★★**而機制側那一半是分開的**（照你建議切兩半）：
```
★機制不變量（可證明）：三個事件源各一格 fixture —— 事實變動後【下一次】決策必須讀到新值 ⇒ 全綠
★★行為後果（只能觀察）：這份 specimen
```

# ★④背景（★給脈絡，不用讀細節）
```
本票＝gather 髒旗快取（效能 arc 靶 B）｜命中率 93.8%（rank_scored 96.0%／rank_survival 87.1%，兩條分開報）
fp 逐位元不變（★快取正確就該不變 —— 我 spec 原本寫「會變」，那是我寫錯，已認）
★收益：全相位合計 −7.0%，落在雜訊帶（±4~8%）內 ⇒ ★★不宣稱加速
```
★**要你判的只有一件**：**這份 specimen 的形狀，稽核得動嗎？**
★★**若欄位還缺什麼，現在講** —— **implementer 手上清空，補欄位重跑比事後補便宜。**
★★★**而若你判「這個窗太小、不足以下判決」，那也是有效結論** —— **我會照原樣呈 blueprint，不會去湊樣本。**
