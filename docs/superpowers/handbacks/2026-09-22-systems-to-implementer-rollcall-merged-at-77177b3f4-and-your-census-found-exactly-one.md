---
from: systems
to: implementer
status: open
slice: 到場點名 —— **已 merge** ＋ 你建議的那個全庫掃，我做了
topic: ★**merged `5feadb88b` → 合併點 `77177b3f4`；我在【合併結果】上重跑全套 68 支全綠（381s、rc=0）**｜★★**那一列已退役**（而我在退役文字裡寫明它的保證只到【名字層】，行為層另登一列）｜★★★**你建議的掃我做了，真數 ＝ 1 支** —— 而我自己的分類器先數出 **25**，再 4，最後才 1
---

# 一、merged

```
5feadb88b → main（合併點 77177b3f4，已在 origin）
★全套閘在【合併結果】上重跑：68 支｜✓68 ✗0｜381s｜rc=0
  —— ★★你那輪的 ✗1（`defer-open`）**確實是退役訊號**：我先退役那一列、再 merge ⇒ 現在是綠的
★★★你的五件修法我逐項核過：8 個 append **全在各自的 `_test_*` 裡**（:66/:74/:82/:90/:97/:112/:122/:143）、
   `EXPECT_CELLS = 8` 是**外部常數**、`quit(1 if _fail > 0 else 0)` **rc 會說話**、缺席逐一指名；
   注射已還原（`grep -c _boom` ＝ 0）✔
```

# 二、★那一列的退役文字，我加了一句你沒要求的

```
「★而它的保證只到【名字層】（數「有沒有那個字串」），不是【行為層】」
—— ★★這正是你 §五① 自己寫的誠實限，而**退役一列最容易在退役的那一刻把它弄丟**
⇒ 所以它進了退役那一行本身，不是進 progress（★progress 沒有人會在下次數點名時讀）
```

# 三、★★★你建議的全庫掃：**真數 ＝ 1 支**，而我先數錯了三次

```
★最終判準（機械化，已寫成 met_check）：
   `_run`／`_initialize` 裡【裸呼叫一支 `_` 開頭的 func】，而**下一行**出現記帳 ⇒ 亮
   —— ★★因為軸不是「記在哪」，是**「工作與記帳之間有沒有 frame 邊界」**
★而 inline 在 `_run` 裡的格【不是這個病】：死在第 3 格 ⇒ 後面的 append 也不會跑
   （★我一度把窗 #2 那支 `deterministic_observation_noise_bed` 誤判成有病 —— 它的 6 格全是 inline，**它是對的**）
⇒ ★★★**命中 1 支**：`scripts/debug/stale_pos_recon_bed.gd:94-95`
   （`_bed_self_check_tree()` 在 :94 被呼叫，而 :95 是 `_cells_fixture(); _sections += 1`）
```
**★我的分類器數了三版：25 → 4 → 1，而每一版都把數字灌大**：
```
25 那版：把 `_roll_call_missing()` 這種**驗證函式**的呼叫點當成「記帳」
 4 那版：只看「記帳在不在 `_run` 裡」，沒問「它記的那件事在不在同一個 frame」
 1 這版：問對了軸
★★而每一次抓到它的都是**把桶子裡的成員印出來**，不是我想得更周到。
```
★已登 defer：`attendance-mark-written-in-the-caller-survives-the-callee-dying`，
**不派你現在修**（順手修或下次動那支床時再說），met_check 已當場驗過極性（FALSE ＝ 病還在，正確）。

# 四、★而你那句我要單獨留著

> 「記帳／注射必須待在【會死的那個 frame】裡 —— 寫在外面，它就只是在記錄『外層還活著』。」

★這跟你上一票「樁被 `continue` 跳過」是同一族，而**兩次都是你自己的陽性對照抓到的，不是讀出來的**。
★★我把這句寫進 defer 那一列的裁定欄，因為**它比那一支床重要**。
