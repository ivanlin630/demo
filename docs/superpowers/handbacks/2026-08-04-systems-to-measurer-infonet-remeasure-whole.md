---
from: systems
to: measurer
status: consumed
topic: "[資訊網whole RE-measure(bootstrap fix後,Part2該活了):branch feat/info-network-whole d9550ad8,worktree .worktrees/info-network-whole(★godot --path對branch code跑禁原地checkout)·bootstrap fix=名冊fallback破herald/scout 0-fire死結·★★核心驗:Part2現該fire——help.herald_dispatched>0+scout.dispatched>0(前輪全0=死結,fix後該活)+help/scout.roster_fallback命中·症1:distribute.dispatch/food_delivered真>0(herald送居民need達領主team_known→distribute fire→convoy送糧)·Part1+3不退(trade.deal/order_fulfilled/board.relay仍≥前輪)·人格分化保留(per-option util dump傲少求務實早求,fallback不動util)·determinism byte-identical不凍雙seed·economy不爆keep-line·★canonical WarringHarness.run()掛specimen=中性(禁手寫loop,治前輪91vs86 bed artifact,observer-RNG方法re-run併此)·specimen trace逐motive→action→outcome(求援/賑濟/relocate/peer-trade死因)餵QA故事稽核·2 R²追蹤:hub效應(熱門市集belief freshness分佈→near-global-awareness?)+calibration genuine(util base真值人格modulate非crank)·誠實measured數字回systems別下accept結論·綠→我路QA故事稽核(回溯三因果+whole出verdict ref)→blueprint對用戶驗收·escaped_defects記"
---

# 資訊網 whole RE-measure（bootstrap fix 後、Part2 該活了）

bootstrap fix 交付（`d9550ad8`、名冊 fallback 破 herald/scout 0-fire 死結）。**RE-measure whole**（前輪 Part2 全 0=死結、fix 後該活）。
**branch**：`feat/info-network-whole d9550ad8`。**worktree** `.worktrees/info-network-whole`（★`godot --path` 對 branch code 跑、禁原地 checkout）。

## ★★核心驗（Part2 現該 fire）
- **`help.herald_dispatched >0` + `scout.dispatched >0`**（前輪全 0=bootstrap 死結；fix 後該活）+ `help/scout.roster_fallback` 命中（名冊 fallback 真觸發）。
- **症1：`distribute.dispatch / food_delivered >0`**（herald 送居民 need 達領主 team_known → distribute fire → convoy 送糧；前輪 distribute 仍 0）。

## 迴歸驗（Part1+3 不退）
- `trade.deal / order_fulfilled / board.relay_deposit` 仍 ≥ 前輪（bootstrap fix 不該退商業）。
- **人格分化保留**（per-option util dump 傲少求/務實早求——fallback 只加 target_pos、不動 util）。
- determinism byte-identical 不凍雙 seed、economy 不爆（keep-line）。

## ★canonical harness（治前輪 91 vs 86 bed artifact）
- **用 canonical `WarringHarness.run()` 掛 specimen**（`SpecimenDumpHelper.setup_from_env` 標準 hook、**禁手寫 loop**）→ 中性 specimen（observer-RNG 方法 re-run 併此、前輪岔開=你手寫 loop、非 released）。
- **specimen trace 逐 motive→action→outcome**（求援/賑濟/relocate/peer-trade + 死因餓死vs戰死vs合作存活）→ 餵 **QA 故事稽核**。

## 2 R² 追蹤
- **hub 效應**：熱門市集造訪頻率 + 隊 belief freshness 分佈 → 功能會否逼近 near-global-awareness（非只看 detector）。
- **calibration genuine**：util base=真值、人格 modulate 非 crank（per-option dump 確認）。

## 交付
- **誠實 measured 數字回 `to:systems`**（別下 accept 結論）。escaped_defects 記。
- 綠 → 我路 **QA 故事稽核（回溯三因果 jia/famine/anomaly + whole、出 verdict ref）→ blueprint 對用戶驗收**。灰/regression → 報 `to:systems`。
