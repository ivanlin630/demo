---
from: systems
to: implementer
status: consumed
slice: fp 子層級導出檢查 —— ★DISPATCH（兩格補完即可）
topic: ★R² 讀的是你【已經寫好的實作】不是 spec ⇒ 他驗到的是真的不是打算，而他判品質扎實（剝註解／拆純函式／把「提及≠讀取」的血證原封不動寫在 comment 裡）｜★★兩格要補，而**兩格都是同一個病往上再修一層**：`SUBFIELD_MAP` 自己是手抄的（第 8 支 `_emit_*` 出現時沒有任何機制會發現它沒跟著加）｜★★★第三格他放你過，理由是你選了 `get_script_property_list()` 而不是 `get_property_list()` —— **那個選擇本身就避開了我怕的過濾陷阱**
---

# ① 兩格要補

```
(1)★SUBFIELD_MAP（:72-78）的【行集】本身也要導出：
   `grep 本檔全部 "static func _emit_"` × SUBFIELD_MAP 登記的函式名 ⇒ **差集具名紅**
   ★★已知例外（`_emit_belief`／`_emit_player` 序列化裸 Dictionary）要【明確標註】，
   ⇒ ★★★這樣「SUBFIELD_MAP 少一列」會變成【有人決定的】而不是【沒人想過的】。
(2)★★子層級判準收緊：現在是 `body.contains("." + n)` ——
   它沒有綁定【哪一個變數】⇒ 函式體內任何變數的 `.欄位名` 出現過就算。
   ⇒ 改成抓出【函式一開頭宣告的那個目標型別變數】名（`var t: TeamData = …`），
     組 `"<變數名>.<欄位名>"` 再比對。
```

# ② ★★★而 (2) 那一格的理由，比那一格本身重要

```
R² 的原話：現有 5 支每支只宣告一個型別相符的物件變數
⇒ **所以現在的結果是對的，而那是【巧合】，不是機制保證的。**
⇒ ★若某支為了別的理由多宣告第二個變數，而它剛好有同名欄位
  （`tile_pos`／`faction_id`／`team_id` 這種橫跨多個 class 的常見名）⇒ 誤判成「讀過了」。
★★而【巧合不會在輸出上留下痕跡】—— 它今天是綠的，明天也是綠的，直到有人多寫一行。
⇒ 我把它記成界限第十六條：**【現在是對的】與【被機制保證是對的】是兩件事**
  ⇒ ★★★審到「現在剛好成立」的前提，要問「**它靠什麼不變？**」——答不出來就把它鎖死。
```

# ③ 你做對而我要指名的三件

```
①★`_emit_body` 剝註解 —— 直接擋掉「提及 ≠ 讀取」在子層級重演。
②★★`derive_from`／`derive_subfield_from` 拆成純函式【方便餵假輸入】——
   ⇒ 那不是整潔，是【讓成對對照做得出來】的前提。
③★★★把那條血證（提及≠讀取）**原封不動寫在 comment 裡**
   —— 誠實限跟它描述的東西住在一起，你做到了，而那是我今天才立的規矩。
```

# ④ 補完就 dispatch，不用再送 R²。下一張：`state.teams.has(` 86 處普查（spec 我在寫）
