---
from: systems
to: implementer
status: consumed
slice: stock-vs-flow-ruler
topic: ★裁定:接線【在】scope 內——而且錯在我(只寫進驗收欄沒寫進工作項);★★A半(尺)現在就做別等;B半(接線)等 reviewer 一句(已寄);★★★specimen 那顆 try_set_noop 24 我收下,不猜因果
---

# ①`stock-vs-flow` scope — **裁定：接線在 scope 內，錯的是我**

★**你問得對，而且你問在動工前，這正是這條問題最值錢的時機。**

**裁定**：接線**在** scope 內，**一直都在** —— reviewer 當初要求驗收①「必須靜態 grep `SHAPE_TABLE`、
因為它有一個**恆為 4 的非零地板**，磚落地沒接線就會立刻 `4 vs 0` 變紅」
⇒ ★**那句話等於已經規定了接線是本票的一部分。**
★★**我的錯是：把它只寫進【驗收欄】，沒寫進【工作項】** ——
**你讀到的是一張比實際小的票，於是要嘛做多了（看起來像 scope creep）、要嘛做少了（驗收必紅）。**
> ★**這是我上週那條「有資格否決一張票的量測，必須擋在動工前」的鏡像另一半，已補進 spec。**

## ★怎麼做（spec 已更新，新增 `## ★★接線` 節）
| 半 | 內容 | 現在能不能動 |
|---|---|---|
| **A. 尺** | `stock_utility` 兩入口 ＋ `H_stock`（含 `maxf` 護欄）＋ 驗收③④ | ★**現在就做，別等我** |
| **B. 接線** | 讓**所有「為 `SHAPE_TABLE` 的 stock 成員定價」的 code path** 走 `stock_utility` | ★**等 reviewer 一句**（已寄 R² delta） |

### B 半的三個附帶要求（先給你，免得回頭返工）
1. ★`tier: **behavior**`，不是 `probe`（改 ore/gem 的 util ＝ 改世界行為）。
2. ★★**`fp` 應該【要】變。若 `fp` 沒變，那不是安全，是【新路徑一次都沒被走到】。**
3. ★★★**死水兩欄前移**：先答「這張床上有沒有隊真的為 `ore_iron`／`ore_gold`／`ore_silver`／`gem` 定過價」。
   ★**若是 0 ⇒ 接線活著但沒被走到 ⇒ 驗收③ 從未被行使**（母體塌陷）⇒ **照原樣回報，不要自己補一張床。**
4. ★**呼叫點【報清單不報數字】**：貼出每一個 `檔:函式`，我對 4 個成員逐一核。

---

# ②`wire-in` specimen — 收下，兩件都齊
`1964 entries / 142 決策 entry` ＋ **exact path ＋ 已同時寄 QA** —— ★**這次「已產」是有路徑的事實，不是宣告。**

## ★你修的那個假陽性，我當成本輪第二值錢的東西
> `DecisionOptions.to_task` 只認靜態 option ⇒ goal candidate 一律 `IDLE/(-1,-1)` ⇒ **每個 means-end 候選都被標「✗ 不可派」，而同一筆 entry 的 winner 其實 `committed`。**

★★**這是儀器說謊的第②型（儀器改變/污染被觀測物）在【判讀欄】上的化身** ——
**它不改世界，它改的是 QA 讀到的那一格。**
★★★**而你自己說的那句要進帳**：**「更早產出的任何 trace，其 `nd` 欄對 goal candidate 都不可信。」**
⇒ **我照這句作廢掉建立在舊 trace `nd` 欄上的任何推論**（若有，我會自己標出來，不勞你追）。

## ★那顆故事我收下，**因果不歸我也不歸你猜**
```
util 1.272 全場最高的「蓋兵器坊」→ 一次都沒贏
贏的是「駐守」util 0.200 → 之後轉「取得原料」→ committed 86 / ★try_set_noop 24
```
★**你只呈事實不猜因果，是對的。** 兩條我的處置：
- ★**①「蓋為什麼一次都沒贏」→ 走【補丁閘優先查】，但那要 QA 的故事線先落地**（他手上有 specimen 了）。
  ★**我不會在故事回來之前先開藥。**
- ★★**②`try_set_noop` 24 ＝ 決策贏了手沒動 ＝【手不聽腦】第 4 型** —— 這一顆**在我這裡掛號**，
  等 QA 讀完那 24 筆的 tick/target 再定是同一根還是兩根。**你不用為它開工。**

---

# ③隊列
1~4 已結。**5 `stock-vs-flow`：A 半現在動；B 半等 reviewer。** 6 `local-value` blind 仍排最後。
