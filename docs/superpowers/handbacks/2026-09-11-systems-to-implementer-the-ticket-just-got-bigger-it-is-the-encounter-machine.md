---
from: systems
to: implementer
status: open
slice: 信使旅程切段 ⇒ ★**改名：相遇機制解剖**
topic: ★★★**blueprint 把三案併成一案，而它有 file:line**：**送信、見面、開打共用同一個「兩支隊真的碰到面」的判定** —— `interaction_system.gd` 的 pairwise 相遇裡同時住著 `_deliver_order`/`_deliver_envoy_proposal`（:390-393／:421-425）**和 `start_combat` 7 個呼叫點裡的 4 個**（:434/:436/:444/:453）｜★**不另開票**：刀已經架在它身上，**加一格就好**｜★★而那一格要**照既有的形狀**（`join.meet_target`/`meet_other` 那種**互斥且窮盡、可對帳**），不要發明新的
---

# ① 三案，一台機器（★這不是類比）

```
①信使送達：45 趟出發、**1 次送達**
②求居見領主：到了 4 次、**領主見到 0 次**
③迎戰接戰：736 次迎戰、**1 次真的開打**
⇒ ★三個症狀長得一樣，而它們**共用的不是「相似」，是【同一段 code】**。
```

# ② ★★要加的那一格（★★★三個數字，不是一個）

```
★現成範本就在同一支函式裡（**別發明**）：
  `atk.reached` / `atk.blocked_ct_197`｜`join.meet_target` / `join.meet_other`
  ⇒ 後者的紀律逐字是：**互斥且窮盡 ⇒ 兩格相加 ＝ dispatch ⇒ 可對帳**。
★★迎戰照這個形狀加**三個**（`TASK_ATTACK`/`TASK_LOOT` 那對**不涵蓋迎戰**）：
  ①**迎戰姿態起算**（736 那個）
  ②**同格相遇**數 ⇒ ★**這一格才是門鈴**：**有沒有真的碰到**
  ③**轉換成開打**數（1 那個）
⇒ ★★★**三個數字才分得出兩種世界**：
  **沒碰到**（相遇機制的病）vs **碰到了但沒打**（決策的病）
  —— 而它們在「736 → 1」這個比例上**長得一模一樣**。
```

# ③ 票的形狀沒變（★仍然只量不修）

```
★母體從【信使】擴到【相遇】，**做法不變**：結構列舉、每格 file:line、fp 逐字相同、
  個位數不判方向、窗末未完單獨一類。
★★blueprint 的原話我抄進 spec：**「若解剖坐實相遇 ＝ 世界互動的單一瓶頸，
  那將是比十倍加速更大的一刀 —— 先讓刀落，不預言。」**
⇒ ★★★所以**不要**在這一票裡動相遇機制的任何一行。
```

# ④ spec 已更新

```
`docs/superpowers/specs/2026-09-11-herald-journey-segments-HOW.md` §⑥（新增）
⇒ 含上面那三格、file:line、以及「不預言」那條紀律。
```
