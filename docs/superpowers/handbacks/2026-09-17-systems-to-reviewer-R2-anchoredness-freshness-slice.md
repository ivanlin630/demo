---
from: systems
to: reviewer
status: open
slice: 錨定性讓情報保鮮（後續票，spec §6）
topic: ★**R² 求審**：spec `2026-09-17-stale-position-goes-to-scout-pool-HOW.md` **§6 全新一節**（前面 §0-§5 你已審過、§5④ 是撤回段）｜★★**我最想被打的一點**：§6.1 我宣稱 `appearance()` 自己會在 `BELIEF_STALE_TICKS` 上把 activity 改寫成 `ACT_UNKNOWN`⇒錨定性在最需要它的那一刻不在 —— **這是我今天第二次拿一支函式當前提，上一次我只看簽章沒看內部就 spec 出了 god-view**｜★★★**請直接打三個前提**（下附行號），打不穿再談設計
---

# 一、請你打的三個前提（★都附 file:line，別客氣）

| # | 我的斷言 | 我的依據 | ★你該懷疑什麼 |
|---|---|---|---|
| P1 | `appearance()` 對 age > `BELIEF_STALE_TICKS` 的 claim **回 `ACT_UNKNOWN` + state="stale"**，原值取不回 | `belief_system.gd:405-407` | ★是不是有**別的** accessor 早就能拿到原值（那 §6.1 整節多餘）；★★`best_estimate` 回的 dict 裡 `activity` 在不在（若在，我的新 accessor 可能只是 wrapper） |
| P2 | `appearance()` 的三態契約**有現成消費者** ⇒ 改它的語義會波及 | `faction_ai_system.gd:947` | ★只有一個消費者的話，「新增姊妹函式」vs「加參數」哪個才不是多一份實作（〈禁第二份實作〉） |
| P3 | 若錨定性做成「不過期」且落在 `BeliefSystem` 那條線 ⇒ **攻擊/掠奪門會跟著鬆** | `_find_weakest_prey` 的 reachable 走 `belief_pos`（`belief_system.gd:123-141` 吃 `BELIEF_STALE_TICKS`） | ★★**我沒有逐一列舉 `belief_pos` 的所有呼叫點就下了「所有讀者」** —— 這是我這一輪被抓過的形狀（假窮盡）。**請逼我補全庫呼叫點清單，或自己掃一遍打我。** |

★**P3 我自承證據不足**：我寫的是「會波及所有讀者」，而我只實際查了掠奪那一條。
**若真實呼叫點只有一兩處，§6.2 的理由 2 要降級成「會波及 X、Y 兩處」而不是「全域」。**

# 二、設計層（前提站得住之後再看）

1. **§6.2 我否決了 blueprint 的「甚至不過期」** —— 只准降斜率。三個理由在文內（拔營⇒不可證偽／後門鬆攻擊門／(A)(B) 兩個落點不同義）。★**這是我推翻 WHAT 的一句話，請當敵意審**：我是不是把「不過期」讀得比他意思更死了？
2. **§6.3 我否決了他的 ③「誠實標被解掉」** —— 理由是時態：裁過 ≠ 接上。
3. **§6.4 格 6-c 是為 §6.1 那個閘造的守衛**；格 6-d 是「兩道門逐字未改」的 diff 格。★**請看格夠不夠能紅** —— 我這一輪吃過「格綠但驗的是函式不是世界」的虧（見 §5④ 末段）。

# 三、通過之後的路

R² CLEAN ⇒ 我寄給 implementer 開後續票。**本票（§0-§5）不動** —— 它五格綠、格4 在跑，不因為有後續票就回頭改。
