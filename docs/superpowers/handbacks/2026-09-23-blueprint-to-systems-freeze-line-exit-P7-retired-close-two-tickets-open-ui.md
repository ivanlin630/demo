---
from: blueprint
to: systems
status: consumed
slice: 凍結線出口 — 四件裁定
topic: ★**①P2 過 ⇒ 凍結線【可交玩】：世代 8 兩顆種子 >2s 天數 0/12、p99 257/267ms**｜★★**②P7 裁 (乙)：本票判準退役——「散相位不改總工作量」這個假設被【已知原因】證偽（本票順手拆掉勢力工作 M 次重複與兩圈全世界掃描），判準做對了它的事；不開 (丙) 隔離票（沒有任何決策需要那個歸因）；模板改 (甲)：日後散相位類票的吞吐格＝「不得【變慢】>5%」；世代 8 吞吐＝新基線**｜★③P4 不可判照規矩，世代 7 對照那輪跑完記「兩代母體皆 0」即可｜★★★④兩張 HALT 票永久關（世代 8 內）：結案謂詞＝「世代 8 上 [B3-FREEZE] 兩種子 PASS」，被世代內 merge 推翻 ⇒ 自動重開｜⑤下一站：UI 五分頁兩張票開工＋給用戶一行「怎麼開來玩」
---

# 一、交玩

```
判定：玩家尺（>2s ≤ 1/日 且 p99 < 1s）兩顆種子過，且對照世代 7 同床同儀器（7/12、5/12；1161/1052ms）⇒ 凍結線交付
對用戶：這是【玩起來不卡】的交付，不是【效能線結束】；渲染成本不在這把尺裡，玩了才知道（體驗回饋非驗收）
★請你回我一行【啟動指令】（GUI 版、main 世代 8），我原句轉給用戶；若需要 build 步驟也一併
```

# 二、P7

```
記法：P7＝「不適用（假設證偽）」＋一句原因＋量到的數（+79%/+48%，方向好）；不寫 PASS
模板（下張起）：吞吐格 ＝ 不得變慢 > 5%（單向）；世代 8 的 ticks/真秒 為新基線，附硬體戳與世代戳
不開隔離票：散相位 vs 前置票的歸因對任何後續決策都不承重；登 known_issues 一行「若日後要引用『散相位本身省多少』，那個數不存在」
```

# 三、兩張票關閉

```
frame-slicing-with-boundary-snapshot、interactive-frame-time-instrument ⇒ 永久關（世代 8）；理由：門票沒開，問題由 (A) 解決
結案謂詞（形狀）：[B3-FREEZE] gen=8 seed=1337/42 verdict=PASS 存在於卷面 ⇒ met_check 可 grep；被世代 8 內任何 merge 推翻（B3 變 FAIL）⇒ 自動重開
指令佇列化票：不關，獨立票（決定性），照走
```

# 四、下一站

```
UI 五分頁兩張票：你開 spec（走 R²），實作端接；驗收含截圖 harness（ObserverMain --obs-*）與「UI 讀路徑零直讀 live state」那格先不做（分片死了，那格的理由跟著消失——只留「畫面呈現上一顆完整 tick」語意，若現行 UI 已是幀末讀取就不動）
equip_mobilize 整體票、subteam-idle、failure-memory：序照舊，在 UI 兩張之後
```
