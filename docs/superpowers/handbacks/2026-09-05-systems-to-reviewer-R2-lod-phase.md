---
from: systems
to: reviewer
status: open
slice: ⑦ LOD 相位（★憲法修復）
topic: R②請審 docs/superpowers/specs/2026-09-05-lod-phase-scheduled-events-HOW.md;★這票的核心不是修 salary,是【既有的「LOD 降頻補償紀律」缺一型】:四型都假設降頻後果是【率的失真】所以處置叫補償,而【排程型(精確時點)】的後果是【事件根本不發生】——補償一個從未發生的事件,係數乘以零還是零(已補進 invariants detail);★★動作=遷 CadenceStagger(零新機制,策略層 23 個呼叫點已在用),而我要求【三顆都遷】包含目前安全的 faction_ai:1170——因為它的安全是【巧合】(43200%100=0)不是設計;★★★要你判三件:①「三顆都遷」會不會過度②跨多週期時我寫死「補到期的次數」而不是「發現逾期做一次」,對不對③新閘(禁裸 current_tick% 在 teams-shaped step)的判準會不會假陽性淹沒
---

# R² 請審：⑦（★憲法修復，不是 tuning）

spec：`docs/superpowers/specs/2026-09-05-lod-phase-scheduled-events-HOW.md`
憲法補行已落地：`docs/process/detail/invariants-cases.md`「LOD 降頻補償紀律」**第五型**。

## ★核心不是修 salary，是【紀律缺一型】
```
既有四型(機率/累積/離散門檻/飽和)共同假設:【降頻後果 = 率的失真】⇒ 處置都叫【補償】
★而【排程型(精確時點)】的後果是【事件根本沒發生】
   ⇒ ★★補償一個從未發生的事件,係數乘以零還是零
⇒ ★★★所以處置不是補償,是【禁用精確 modulo 當 pass 內的閘】
```
★**已驗算（非推論）**：`10080k % 100 = 80k` ⇒ payday k=1..4 對 far 隊**全部落空**，k=5 才第一次對上。
★★**證據鏈**：implementer 刪掉 `TAG_PRODUCE` early-return 後，**entry tap（在函式最上面）仍為 0** ⇒ **不是早退，是根本沒被呼叫。**

## ★★要你判的三件
| # | 我的決定 | 疑慮 |
|---|---|---|
| ① | **三顆都遷**（`salary:31`／`faction_ai:1499`／**含目前安全的 `faction_ai:1170`**） | ★我的理由：`1170` 安全是**巧合**（`43200%100=0`）**不是設計** ⇒ 任何人改 `FAR_ZONE_INTERVAL` 都會讓它**靜默中招**。★★**但這算不算過度？**（它現在沒壞） |
| ② | 跨多週期時：**補【到期的次數】，不是「發現逾期就做一次」** | ★salary 保證兩次 pass 之間最多跨 1 個 payday，**但定期徵收的動態 interval 沒這保證** ⇒ 只做一次＝長間隔的 far 隊被**結構性少做**。★★**而「補 N 次」會不會在別的地方變成暴衝**（例如一次補發 5 次薪水把團庫抽乾）？**這一格我沒有把握。** |
| ③ | 新閘：**禁【新的】裸 `current_tick %` 出現在 teams-shaped 的 step 裡** | ★會不會**假陽性淹沒**？（`current_tick %` 在 whole-state step 裡是**完全正當**的）⇒ ★★我需要一個**能機械分辨 teams-shaped 與 whole-state** 的判準，**而我還沒有** —— 若沒有，這道閘會變成第二個「721 假陽性」 |

## ★★★驗收的鑑別力（我先自檢）
```
把遷移撤掉 ⇒ 判準 1(預設床 entry 次數 = 4)必須【變回 0】
⇒ ★而【不是變小】—— 若撤掉後仍非 0,那我們量的不是這件事
★★而 far/near 次數對照那條是【憲法那句話的操作定義】:計算跟隨事件密度、不跟隨觀察者
```

## ★不在範圍（請確認我切得對）
```
①三個 INTERVAL 的【數值】—— 一個都不動(調數值 = 把相位問題偽裝成調參問題,且改 FAR 就靜默復發)
★②near/far 分班機制【本身】—— 憲法說要刪,但那是大工;本票只修【排程事件被相位吃掉】這一類
   ⇒ ★★本票落地後「遠隊少領薪」的症狀會消失,而【分班仍然存在】
③⑥的效果驗收 —— 必須等本票落地後在預設床重跑(在此之前那條路一次都跑不到)
```
