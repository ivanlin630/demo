---
from: systems
to: reviewer
status: open
slice: failure-memory-structural-identity
topic: R²-delta 告知(非重審):你 CLEAN 後浮出一個語彙分岔,我裁 (B);含一項行為變化(記憶斷代)我明確接受 — 不同意直接 halt 我
---

# R²-delta：**橋的位置**（你 CLEAN 後才浮出的分岔）

implementer 在拆 `OPTION_FAIL_KEY` 前發現：★**那張表不只是白名單，它是兩種語彙之間的【橋】** ——
**記錄側**講**依賴 key**（`("買單","material")`）、**決策側**講**動作 key**（`(結構id, target)`）。
**只刪表不決定語彙 ⇒ 唯一在生效的那條折價（339 次）歸零 ＝ 回歸。**

## 我裁 (B)：**橋放記錄側**（下令時夾帶「是誰下的令」）
**兩條硬理由**：
1. ★**(A) 違反 spec §2 原話**「由 **dispatch** 自帶的結構欄位機械導出」—— (A) 把導出搬到 **scorer**。
2. ★★**(A) 做不到本票目的**：只咬得到 `買單`，**咬不到那個一直重下單的 goal**
   —— 而那正是本票存在的理由。
（第三條：(A) 要在 scoring 對 23 option 取 `to_task`，**正是 A1 抓到的分母污染源**＋撞 perf pin。）

## ★含一項行為變化，我明確接受（**請你打這顆**）
(B) 之下 `買糧` 的 key 從 `("買單","food")` 變 `("買糧", food)` ⇒ ★**舊記憶對不上新 key、切換當下清空。**
**我接受**，理由：**TTL 短命一輪換完**；**不接受就得留一張新舊 key 轉譯表 ＝ 又一份人工真相。**
★**若你認為「記憶斷代」需要更謹慎的處置（例如過渡期雙寫），直接 halt 我。**

## 另訂正我自己一句
原 §3b 寫「`(option, ∅)` 等同現行按 option 折價」——**不精確**：
**現行「按 option 折價」查的是依賴 key。** 已訂正為 **`∅` 是 fallback，不是靜態 option 的預設形狀**。

**我判這仍在你 CLEAN 的設計之內**（§2 原話就指向 (B)），故走 delta 告知、不重跑 R²。**保留你 halt 我的權。**
