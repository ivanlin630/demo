---
from: systems
to: implementer
status: open
slice: 共位必見 ★派實作（R² CLEAN，零新常數）
tier: slice
topic: ★定案形狀:dist==0 分支確定看見／dist<=1 時 dist_f=1.0／dist>=2 現有公式【完全不變】—— ★★零新常數,而 R² 點破的關鍵是「dist<=1 的 1 不是手填參數,離散 hex 的 1 是【格點結構事實】,不像 vrange/3 的 3 是我自由選的比例」;★★★而真正的變化是 dist=0 那個【分類上的】改變(確定 vs 機率),不是數值上的 —— dist=1 只從約 0.92 升到 1.0,擾動最小化
---

# 派工（spec = `docs/superpowers/specs/2026-09-04-colocation-sight-HOW.md`）
```
dist == 0  ⇒ ★分支:【確定看見】,不進機率閘
             ⇒ ★★寫入【真 pos ＋ 外觀層欄位】(親見的定義);★★★不寫內部狀態(那才是 god-view)
dist <= 1  ⇒ dist_f = 1.0（★沿用公式自己在 dist=0 的天花板,不發明新數）
dist >= 2  ⇒ ★★★現有公式【完全不變】—— 遠場要【逐位元不動】
```

# ★驗收
| # | 判準 |
|---|---|
| 1 | ★**同格未偵測 ＝ 0**（★★現況 880/1710；★★★機械斷言不是論證） |
| 2 | 同格 pair 的 belief `tile_pos` ＝ 真 pos（逐筆貼一例） |
| 3 | ★JOIN 的 `true<belief` **大幅下降**（現況 39/55）；★★**不要求歸零**（belief 仍可過期） |
| 4 | `join.meet_target` **上升**（現況 1）／`join.resolve` **> 0**（現況 0） |
| 5 | ★★**遠場逐位元不動**：dist ≥ 2 的路徑 diff 只有【新增分支】，★★★沒有任何既有算式被改寫 |
| 6 | determinism 同 seed 三跑一致（★`fp` 會變 —— 這是根修） |
| 7 | 憲法閘 PASS ＋ 17 支全綠 |
| 8 | ★★★同格寫入的**欄位清單逐項列出** —— 證明只有真 pos ＋ 外觀層（★防 god-view 滲入） |

# ★★不在範圍
```
★①隱匿機制(未來有了再 MODULATE;現在【無隱匿】⇒ 共位必見)
★★②感知鐵律本體(決策吃 belief 不吃 god-view —— 不動)
★★★③JOIN 的到達判定:若修完 JOIN 仍失敗,那是【另一票】—— 不要順手一起改
```

# ③★而這是根修級（★做完先停 branch）
```
★遭遇／威脅／施主／JOIN 全吃這一條 ⇒ ★★做完【停在 branch】回報我
⇒ ★★★我 merge → 17 支 → 重建凍結 → peaceful×3 重跑（22 分）
★而重跑跟舊卷比時【只比不吃偵測的那些格】—— 這條我會寫進重跑派工單
```
