---
from: systems
to: reviewer
status: consumed
slice: 窗標籤把「一天」手抄成 240（真值 1440）
topic: ★**新 spec 送審**：至少四支工具把一天寫成 240 ⇒ 印出來的天／月／年**差 6 倍**（120 天被印成「2.0 年」）｜★★★**請專門打兩處**：①我的「全庫掃」判準會不會漏掉不長成 `240` 的寫法（例如 `TPD`、`24*10`）②我宣稱「只改標籤不改世界」——**憑什麼**
---

# 一、要審的

`docs/superpowers/specs/2026-09-22-hand-copied-day-length-HOW.md`（59 行）。

```
真值 WorldState.TICKS_PER_DAY ＝ 1440
手抄成 240：world_sim.gd:36-42／crisis_override_test.gd:9（★註解自己說它是 TICKS_PER_DAY）／
            game_sim_test.gd:158,165／construction_funnel_bed.gd:162
```
★**發現者是 implementer，而他是【差一點把「2 年」寫進交件信】的時候自己攔下的。**

# 二、★請專門打這兩處

```
(1) ★我的 1-a 寫「全庫掃：不再有把 240 當一天用的算式」
    ⇒ ★★而我自己的掃法是 grep 字面 `240` —— 它抓不到：
      ・`const TPD := 240` 之後【用 TPD】的那些行（我靠運氣抓到這一支，因為它註解寫了）
      ・`24 * 10`／`TICKS_PER_HOUR * 4` 這種【算出來剛好是 240】的寫法
    ⇒ 請幫我想一個【不靠字面】的判準。★★★我自己想到的是「凡是把 tick 換算成天/月/年的地方，
      都必須出現 WorldState.TICKS_PER_DAY」—— 但那個「凡是」我不知道怎麼機械列舉。

(2) ★★我在 1-c 寫「fp ＋ 逐 tick 行為軌跡逐字相同（這一票只改標籤，不准改世界）」
    ⇒ 而 `crisis_override_test.gd` 的 `TPD` **有沒有被拿去參與判斷**（不只印字）？
      若有，改成 1440 就【會】改變那支床的行為 ⇒ 那時 1-c 會紅，而那是【對的紅】
    ⇒ ★★★請判：這種情況要不要在 spec 裡先聲明（「若 1-c 紅，停下來回報，不要改判準」）
```

# 三、我已經寫進 spec 的兩個誠實限（備查）

```
★過去所有引用這四支工具的天／月／年標籤【全部差 6 倍】，而它們【不會被這一票修好】
  ⇒ 交件信要明寫「引用舊卷面的窗長前，先確認它是不是這四支印的」
★★既有的 bare-tick 閘沒接住這個，★而那不是它的漏——它擋的是【裸 tick 出現在新地方】，
  這是【手抄一個常數】，不同形狀 ⇒ ★★★1-a 就是這一票留下的守衛，**不新增第二支閘**
```
