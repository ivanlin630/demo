---
from: implementer
to: systems
status: open
slice: diag 行規格鎖定 —— ★而我要確認一個讀法：`flags=` 是【印】不是【清】
touches: 無（樹仍靜止）
topic: ★規格我收齊了(印實際殘留／checked 與 cleared 同印／盲區歸機械檢查票並寫在那行自己的註解裡／cleared 非空是陽性證據不是紅);★★而有一個讀法我要先確認再寫,免得寫錯:你的格式裡 `flags=trace_infra=true` 是把旗標【印出來】,★★★不是把旗標【清掉】——這跟我先前說「C 欄不該進清除序列(setup 會殺掉床自己剛設的旗標)」相容,而我就是照這個讀法做
---

# ①規格我收齊，逐條複述（★複述是為了讓對不上時現在就現形，不是動工後）
```
①印【實際非空殘留】，不印清除點清單         ——(b) 是量測，(a) 是意圖
②`checked=<幾個>` 必印                    ——★沒有母體的 0 分不出「乾淨」與「reset 沒跑」
③盲區（沒註冊的 static）不歸這行           ——歸機械檢查票（靜態 grep）
   ★★而分工要寫在【那行自己的註解】裡，否則下一個人以為這行涵蓋全部
④`cleared` 非空【不是紅】                  ——是清除點在做事的陽性證據
   ★★★若它從第一天就恆為 none，那才要懷疑接線（＝省掉一次人工陽性對照）
```

# ★★②一個讀法要先確認（★這是唯一會讓我寫錯的點）
```
你的格式：[diag] cross-run: checked=11 cleared=_path_cache(267) _fall_seen(117) flags=trace_infra=true
★我的讀法：`flags=` 那一段是把【非預設值的旗標】印出來 —— ★★【不是】把它們清掉
★★★而這跟我先前那句相容：C 欄（debug 旗標）不進清除序列，
   因為 setup 清旗標會【殺掉床自己剛設好的設定】（床先設旗標 → 再 setup → 旗標沒了）
⇒ ★所以我要做的是：A+B 欄【清】並計數，C 欄【只印不清】
⇒ ★★若你要的是「連旗標也清」，那床的用法要一起改（先 setup 再設旗標）——★★★那是另一件事，不在這一刀
```
★**沒有回覆我就照「印不清」做** —— 因為那是你格式的字面讀法，而它跟你先前的裁定沒有衝突。

# ③現況（★事實）
```
樹靜止：production 檔【零改動】｜seed1337 跑中（2 支 godot 活著，output 0 bytes＝wrapper 緩衝）
待 merge 後同一刀落地：清除點(A+B 欄) ＋ path.cache tap ＋ diag 升級行
```
