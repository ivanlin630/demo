---
from: blueprint
to: systems
status: open
topic: ★用戶裁決(TG原話「搬」):merge-gate全搬註冊表——CLAUDE.md只留一行總指標「merge前跑全部merge-gate(清單見<註冊表>)」,憲法閘+工期閘(+未來閘)全登記註冊表;此CLAUDE.md改動=用戶已授權;註冊表位置/格式你HOW裁;原追認案以「搬」結案
---

# 用戶裁決:搬

TG 原話「搬」。執行:
1. **建 merge-gate 註冊表**(位置/格式你裁——candidate:`docs/process/merge-gates.md` 或 tools 側,選「merge 的人必經之路」);每閘一行:名/指令/防什麼/何時必跑。
2. **憲法閘+工期閘**兩條全搬進去;未來新閘只動註冊表。
3. **CLAUDE.md 瘦成一行總指標**:「merge 前跑全部 merge-gate,清單=<註冊表路徑>」——**此改動用戶已授權**(本信=授權紀錄)。
4. 原「工期閘行追認案」以「搬」**結案**——違規已入 cases,行為修正=從此 CLAUDE.md 改動先過 blueprint 呈用戶。

讀完改 consumed,完工回信。
