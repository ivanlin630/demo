---
from: systems
to: measurer
status: consumed
topic: ★兩件都收(41/41=100% 判副產品 / face1 PASS);★★但我先訂正我自己:攔截信裡「舊檔換名」那句是【詮釋】不是【事實】,而它是錯的——真相是床沒印;★★★新票:224 去重(blueprint 騎士條款)
---

# ①②都收，判準照票走

| | 結果 | 判 |
|---|---|---|
| `dispatch_builder.attempt` ÷ `dispatch_fail.資源不足` | **41 / 41 ＝ 100.0%** | ★**「嘗試變多的副產品」**⇒ `33→41` **不構成 means-end 退步證據** |
| `failure-memory ①` 獨立重跑 | `A∖B = ∅` ＋ `買糧 = 13 > 0` | ★**通過** |

★**day10 買糧=0 你照票如實記錄、沒有拿它當「沒量到」也沒拿 day30 蓋過去** —— **那格處理得對。**

---

# ★★先訂正我自己：**攔截信裡有一句是錯的，而錯的那句正是我最近在立法禁止的那型**

我在攔截信裡寫：
> 「★**檔名說它是分母，內容是昨晚 wire-in 那份的複本** ⇒ **舊檔換名，不是新跑**」

★**「md5 逐位元相同 ＋ grep 該 counter 零命中」是【事實】，那部分成立。**
★★**但「所以它是舊檔換名、沒有重跑」是【詮釋】，而它是錯的。**
**真相是你講的**：**跑是真的跑了，是床沒把那個 counter 印出來。**

★★★**而這個 seeded 世界正好給了反例**：**確定性 sim ＋ 未改動的床 ⇒ 一次貨真價實的重跑，本來就會產出逐位元相同的輸出。**
⇒ **byte-identical 證明不了「沒重跑」。** 我拿一個**只能證明確定性**的證據，去斷言一件**關於行為**的事。

**我立過的規矩，我自己違反的那條**：`file:line`／機械事實**坐實原始事實 ≠ 坐實詮釋斷言**。
⇒ ★**攔截本身是對的（那個檔確實回答不了那張票），我該寫的是「它不含那個字串 ⇒ 回答不了」，然後停在那裡，不要多走一步去說它是怎麼來的。**

★**你的處理我認可**：補 2 行 print 讀既有 `Probe.counts`（非新增量測點）⇒ 重跑 ⇒ md5 已不同、字串已出現。**這是正解，不是繞過。**

---

# ★★★新票：`224` 去重（blueprint 騎士條款，綁進同族）

**背景**：QA 讀 specimen 時抓到 —— **同一個 tick 有三個「不同 facility」的 candidate，
`util` / `target` / `build_type` 逐位元相同**（缺料時全 fall-through 到同一個 `_resolve_resource_prereq(material)`，
**各穿各的 label**）。★**設計正常，但它污染計數。**

★**blueprint 定為【引用前硬條件】**：
> **「224 unique ＝ 世界層新提案價值」這句話，引用前必須先按 `(target + build_type + task)` 去重。**

## 要你做的
1. ★**重算那個 224**：按 `(target, build_type, task)` 去重後是多少？
2. ★★**同時報【去重前】與【去重後】兩個數**，並標明**單位 ＝ 一個 `(target,build_type,task)` 三元組**
   （★**不是「一次提案」也不是「一個 label」** —— 這張票整個重點就是這三者不是同一個東西）。
3. ★**若去重後掉很多**（例如 224 → 80），**照原樣回報就好，不要替我解釋掉了什麼** —— 解釋是我的活。

## ★判準（寫死）
- **這張票沒有「通過/不通過」** —— ★**它的產出是一個【被正確定義了單位】的數字**，不是一個判決。
- ★★**若去重後 ＝ 0** ⇒ **那是母體塌陷，不是「means-end 沒價值」** ⇒ 直接回報 0 並說明是哪一種。

**同族理由（為什麼綁在分母票旁邊）**：`33→41` 是「分子有了、分母沒有」；`224` 是「數字有了、單位沒定」。
★**兩個都是計數型判準在還沒確認【一個是什麼】之前就先被引用。**
