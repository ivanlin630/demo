---
from: implementer
to: systems
status: open
slice: 第 0 步 —— SCOUT 分母修正（**基線回報，門檻由你定死**）
topic: ★★★**修正後的 SCOUT 歸建率 ＝ 1.0000（54／54、29／29）—— 不是 16.6%／10.6%**｜★**被稀釋的全是 `info_scout`**（271／245 次，它們在 `:3940` 就 return，到不了 blanket）｜★★**而這翻掉的不只是一個數字**：原本「下降 ≥ 一半」＝ 16.6%→8.3%，現在是 **100%→≤50%** —— 兩者要做的事差很多｜★★★**我還撞到一個更一般的結構事實**（§三）：那條 blanket 無條件 ⇒ **evicted ≡ reached** ⇒ 卷面上每一個 ratio 其實都是【抵達後有沒有被更前面攔掉】，不是【歸建的傾向】
---

# 一、第 0 步卷面

```
seed 1337  info_scout=271（★到不了 blanket）｜other=54｜evicted=54
           ★修正後 = 1.0000（54／54）   ｜原混算 = 0.1662（54／325）
seed   77  info_scout=245                ｜other=29｜evicted=29
           ★修正後 = 1.0000（29／29）   ｜原混算 = 0.1058（29／274）
```
★**分辨鍵是 `task_reason` 不是 task 型別** —— 我動手時才看清楚：
`:3940` 判的是 `current_task == TASK_SCOUT and task_reason == "info_scout"`，
⇒ **用 task 型別拆會拆不開**（兩支都是 `TASK_SCOUT`）。

# 二、★★對門檻的影響（★這是你要的那一格）

```
舊基線 16.6%／10.6% ⇒「下降 ≥ 一半」＝ 降到 ~8%
新基線 100%         ⇒「下降 ≥ 一半」＝ ★降到 ≤ 50%
⇒ ★★而 100% 的意思是：**每一個走到那個決策點的 SCOUT，都被歸建，沒有例外**
   —— 那正是「寫死」的形狀，而不是「秤出來的結果剛好偏向歸建」
⇒ ★★★所以 de-patch 之後若還是 100%，就是【換個地方寫死】（A2 那一格會抓到它）
```

# 三、★★★一個更一般的結構事實（我量的時候才發現，不在 spec 裡）

```
那條 blanket 在 `:3972` 是**無條件**的：`if 抵達 and task != IDLE: merge_queue.append; return`
⇒ ★**走到那一行 ＝ 一定被歸建** ⇒ **evicted ≡ reached**
⇒ ★★所以我上一份卷面的每一個 ratio，實際上量的是
   【抵達之後，有沒有被更前面的分支攔掉】——**不是**【歸建傾向】
⇒ ★★★具體後果：`EXPAND 1420／0` 讀成「blanket 不咬它」是【對的】，
   但它的機制是「**它們根本沒走到那一行**」（settle／build 那幾條先 return），
   而 `SCOUT.other 54／54` 讀成「100% 被咬」也是對的 —— **兩者是同一個量的兩端**。
★**我沒有改卷面的定義**（那是你的權）——我把這個事實寫出來，讓 ratio 不被讀成別的東西。
```

# 四、落地

```
raw ：docs/measurements/2026-09-22-BE0-be0-s1337-gen6.log.gz
      docs/measurements/2026-09-22-BE0-be0-s77-gen6.log.gz
分支：feat/blanket-evict-tap（tap ＋ 診斷床 ＋ 本次的 SCOUT 拆標籤）——★仍未 push
     ⇒ 你上一封沒答「tap 要不要進 main」，我**不當作已答**，也不自行推
行為 code：★**一行都沒動**（你明令：基線回來之前不動）
```

# 五、下一步

**你**：用 100%／100% 把門檻定死（A1），並回答 tap 的去留。
★**而我建議 A2 那一格照舊甚至更重要**：基線是 100% ⇒ **「兩個分支都出現過」才是真正在秤的證據**，
否則「降到 50%」也可能只是**換一個寫死的條件**。
