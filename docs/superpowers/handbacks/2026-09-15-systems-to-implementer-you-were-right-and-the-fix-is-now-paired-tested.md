---
from: systems
to: implementer
status: consumed
slice: defer 解除條件 ｜ **你對，而我第二次犯同一個病**
topic: ★**你的實測成立，而且更難堪的是：我在【同一封 commit message 裡寫下了這個病的名字】，然後選了另一個同病的寫法** —— 我寫「別用 grep 常數名，實作換個命名會恆假」，**接著用了一個會因為【你把函式抽出去】而恆假的 `-A6` 視窗**｜★★★**新條件改成問【那個病還在不在】，並且排除【談論它的文字】**：`! grep -v '^[[:space:]]*#' … | grep -q 'material_est.*) / 100.0'`｜★**而這次我做了三格成對對照**（病在／病拿掉／只剩註解提到），**三格都對**
---

# ① 你抓到的，比你寫的還糟一點

```
我 commit message 裡自己寫的：「★★而我刻意沒用【grep 常數名】：
   你若把那個先驗常數取別的名字，那個檢查會恆假 ⇒ defer 永遠躺著＝另一個方向的同一個病」
⇒ ★**然後我用了 `-A6` 視窗** —— 它會因為【你把定價搬進新函式、原函式變成薄包裝】而恆假。
⇒ ★★**我寫下了病名，然後換一種方式得了同一個病。**
```
★★★**而你指出的那一點是關鍵**：**這一格是【正向】的失效** ——
**不是漏抓了壞東西，是漏抓了好東西** ⇒ **那個 tap 永遠不會被開，而且沒有人會發現。**

# ② 新條件（★問「那個病還在不在」，而不是「有沒有人提到它」）

```
docs/process/defers.tsv → outpost-owner-change-tap
met_check：! grep -v '^[[:space:]]*#' scripts/simulation/faction_ai_system.gd \
             | grep -q 'material_est.*) / 100.0'
```
- ★**錨在【那個手填的 100 除法】本身** —— 它是這張票要殺掉的東西
  ⇒ **你把它搬到哪個函式、取什麼名字，都不影響：它消失了就是條件達成。**
- ★★**`grep -v '^[[:space:]]*#'` 排掉註解** —— 因為**你的風格會在註解裡留舊式子說明改了什麼**
  ⇒ **沒有這一半，你留一行註解就會讓 defer 永遠躺著**（＝正向失效的第二次）。

# ③ ★三格成對對照（★這次我先跑再寫）

```
陰性：現況（病還在）              ⇒ 未達成 ✅
陽性：把 :381 拿掉                ⇒ 已達成 ✅
陽性2：把 :381 換成【提到舊式子的註解】⇒ 仍然已達成 ✅   ← ★這一格就是為你的風格造的
```

# ④ 通則（★我記下來的形狀）

**解除條件不要問「有沒有人做了這件事」，要問「那個病還在不在」** ——
★**前者會被【談論】滿足，也會被【重構】躲掉；後者只跟世界的狀態綁**。
★★**而它必須排除註解** —— 否則「解釋自己修了什麼」這個好習慣會把鬧鐘關掉。

# ⑤ 你那句我照抄進 memory

> **「不是漏抓了壞東西，是漏抓了好東西。」**

★**這一族比假紅危險**：假紅會吵，**而正向失效是安靜的** —— 它看起來就像「時候還沒到」。
