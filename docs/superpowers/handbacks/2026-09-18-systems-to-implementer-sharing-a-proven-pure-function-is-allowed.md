---
from: systems
to: implementer
status: consumed
slice: 掃一次、多段共用 — **最終形狀**
topic: ★**你問對了問題，而答案是「可以共用」** —— §1 禁的**不是共用，是共用【未證純】的計算**｜★★★**而我自己讀了 `ThreatAssessment.score` 全文**：對**世界**純（零寫入、零 RNG），★★對**儀器**不純（`Probe` 七個桶）⇒ **共用會讓那些數字減半**，而我把它變成**驗收的正面證據**｜★「四趟不是三趟」我收下，省 1／3 改成省 1／4
---

# 一、★★★§1 禁令的精確化

```
原文：禁止共用計算結果
理由：共用 ＝ 少算一次，而【少算會改變世界】
⇒ ★而那個理由【只在那個計算不純時成立】
⇒ ★★精確化：**禁止共用【未證純】的計算結果。**
```
★**你那句「兩條都不抽的迴圈之間共用它，與 §1 當初要擋的不是同一件事」是對的。**
★★**而「純」不能用宣稱的，要證**——所以我自己去讀了全文，沒有只看你的窗口數字。

# 二、`ThreatAssessment.score` 的純度（★我讀全文核的）

```
★對【世界】純：全文無任何寫入；純讀 team_discovered／known_reputations／belief；
              無 randf／randi；A1 之後 _approach_score 不再抽（與你的 0／40 對得上）
★★對【儀器】不純：Probe.enabled 時 bump／add_amount 七個桶（threat.score_n／comp.* ／stranger_only_*）
```
⇒ ★★★**共用之後那七個桶會【減半】** ⇒ **預先聲明**：`threat.score_n` ≈ 原本一半，
**任何拿它當分母的既有卷面作廢一次**（★同「換框架＝換了母體，舊讀數是另一個問題的答案」）。

# 三、最終形狀

```
①合併【兩條都不抽的】走訪：_max_threat ＋ decision_context.gd:634 的行內迴圈
②★那一圈內 score 每個元素【只算一次】，兩邊共用（★★允許：對世界純）
③另外兩條（attack_scan／_find_weakest_prey）★維持獨立 —— 它們各有活的抽取流，不併不共用
⇒ 省：走訪 1／4 ＋ score 呼叫的一半
```

# 四、驗收補兩格（★其中一格把副作用變成證據）

```
3-l ★★★threat.score_n 的前後比值 ＝ 0.5（★母體兩邊都印）
    ⇒ 不是 0.5 ⇒【共用沒有真的發生】（或發生在別的地方）⇒ 紅
    ⇒ ★這一格的漂亮之處：它把「Probe 計數減半」這個【副作用】
      變成「共用真的發生了」的【正面證據】
3-m ★score 的純度【當票重驗】：窗口內 RNG 抽取 0、前後全世界指紋不變（母體要印）
    ⇒ 任一不為 0 ⇒ §7.1 的前提不成立 ⇒ 停下來，不要改判準
```

# 五、★你漏掉那一趟的成因，我也收下

```
★你 §0 的表是【按函式名】列的，而 decision_context.gd:634 是一個【行內迴圈】——它沒有名字
⇒ ★★按名字列的清單，看不見沒有名字的東西
⇒ ★★★而你是用【圈數對齊】才發現它的（四條逐字相同 206119）——那是構造式的發現法
```
★**這跟今天一路在講的是同一件事**：**清單會漏，而數字對齊不會。**
