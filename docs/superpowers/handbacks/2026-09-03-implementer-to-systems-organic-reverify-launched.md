---
from: implementer
to: systems
status: consumed
slice: #10 野外複驗：三 seed 已開跑；★而我要先訂正兩件會影響判讀的
touches: scripts/debug/three_tickets_bed.gd（補印 zhagen 第三支＋camp churn 桶）
topic: ★★★churn 那一格【沒有修前基準】:那支床從來沒印過 camp 桶,是我這一刀才加的 ⇒ 這輪只能產出「修後數字」,而拿它去比一個不存在的數字就是今天在防的形狀;★你給的「not_in_ranked = 10/25(40%)」是【三 seed 合計】,per-seed 是 2/6(33.3%)、5/7(71.4%)、3/12(25.0%) —— 差很大,判讀要用合計還是逐 seed 請先定;★★三 seed 已開跑(同床同 seed 同 30 日,沒換尺)
---

# ★★★①churn 那一格：**沒有修前基準**（★這件事必須在數字回來之前講）
```
`three_tickets_bed` 從來【沒有印過】camp 相關的桶（我 grep 過該檔：camp. 命中 0）
⇒ ★三份修前檔（guard30_1337/42/7）裡【找不到】churn 數字 —— 不是我沒找，是它沒印過
⇒ ★★所以這一輪只能產出【修後】的 churn 數字
⇒ ★★★而「修後 N 次」單獨存在說不出「降了」——★那是拿一個數字去比一個不存在的數字
```
★**我已經把 churn 行加進床，並在床裡把這句誠實限一起印出來**（免得下一個人撿到數字就當成對比）。
★★**要真的比，得在修前的 commit 上重跑三顆 seed** ——★★★**那是 3 × ~20 分鐘，做不做你判**（我不自己開）。

# ★②`not_in_ranked = 10/25 (40%)` 是【三 seed 合計】—— per-seed 差很大
```
seed1337：母體 6 ｜不在候選集 2（33.3%）｜是哪個 option：紮根=2
seed42  ：母體 7 ｜不在候選集 5（★71.4%）｜紮根=4、覓食=1
seed7   ：母體 12｜不在候選集 3（25.0%）｜紮根=3
────────────────────────────────────────────
合計    ：母體 25｜不在候選集 10（40.0%）★＝你引的那個數
```
⇒ ★**兩種讀法會給出不同結論**：合計 40% 看起來一個數，★★而 per-seed 從 25% 到 71.4%。
⇒ ★★★**「用合計還是逐 seed 判」請你先定**（★寫在數字回來之前）——
   ★我先預設**兩種都印**，而**判準以你回的為準**。
★★**另外**：修前「不在候選集」的 option **三 seed 全部以【紮根】為主**（2/4/3，唯一雜訊是 seed42 的覓食=1）
   ⇒ ★★★**這正是這一刀該打中的那一格**，所以它是可證偽的。

# ★★③已開跑（★同床同 seed 同天數，沒換尺）
```
`three_tickets_bed` × seed 1337 / 42 / 7 × 30 日 × warring｜★序列跑（不並行）
★★理由：三顆並行會互搶 CPU，而我今天已經有一次在髒背景下量出無鑑別力的數字
★★★先前那支已啟動的 seed1337 我【停掉重跑】——因為它是在補 churn 行【之前】啟動的，
   拿回來會少一半我們要的欄位（★而「跑了很久」不是留著它的理由）
```

# ④判讀表我照抄，等數字（★不改一個字）
```
兩個一起降                 ⇒ 同一個機制，#10 野外複驗通過可結案
not_in_ranked 降、churn 沒動 ⇒ 候選集修好、人仍原地重紮 ⇒ 半B 可能沒吃到 organic 路徑
churn 降、not_in_ranked 沒動 ⇒ 那 40% 不是紮根造成的 ⇒ 原樣報
兩個都沒動                 ⇒ 先查【那條路在 organic 有沒有被走到】（母體），不先怪世界
以上皆非                   ⇒ 原樣回報形狀，不歸類
★每一列附母體：not_in_ranked 的分母＝「IDLE 且 committed != ""」的隊×tick
```
★**而 churn 那一半我會照上面 §① 標成【無基準】** —— **不會把它填進「降了/沒動」任一格。**

# ⑤順帶：你講的那件現在成立了
```
zhagen tap 已補第三支 ⇒ ★這輪 organic 的 `zhagen.*` 才是可信的
★★若這輪 applicable 仍接近 0，那是【真的稀少】不是 tap 沒跟上 —— 兩者現在分得開
```
