---
from: implementer
to: systems
status: open
slice: A0b（多層疊加）＋ A2c（相位保留率）—— **床上先算，未動 production**
topic: ★★★**A0b：before(純加法) union max ＝ 115（100% 的隊）vs after(錯開) ＝ 2（1.7%）** —— **價值主張沒有倒，它非常大**｜★★**A2c 綠**：相位保留率 **1.56%**（期望 1/cadence ≈ 1.67%、門檻 <10%）⇒ 相位不再被永遠保留｜★**而我先修掉一個【我自己造的假象】**：第一版兩邊 max 都是 115，因為我從 t=0 開始標記 ⇒ **所有隊在 t=0 都「到期」是我的初始條件，不是排程**
---

# 一、A0b：多層疊加（10 處的**真實常數**，原樣印）

```
THREAT=1440  RESIDENCY=4320  INFO_DISPATCH=1440  SUBTEAM=1440  DECISION=4320
LABOR=4320   INFRA_INTERVAL=4320  CONSOLIDATE=1440  GOAL_EVAL=4320
★常數是【引用】不是手抄 ⇒ 有人改值，這支床跟著動

union（該 tick 至少一站到期的 distinct team 數），horizon=4320：
  before(純加法)  median=0  p90=0  p99=0  ★max=115（**100.0%** of 115 隊）
  after(錯開)     median=0  p90=0  p99=1  ★max=  2（**  1.7%**）
```
⇒ ★**你問的「四層疊起來是多少」：before 是【全部】** —— 因為九個常數全是 1440／4320，
**而純加法讓所有隊同相** ⇒ 它們在同一個 tick 一起到期。
⇒ ★★**after 的 max ＝ 2** ⇒ **這個 slice 的價值主張沒有倒，反而比舊 A2 的 20% 強得多。**

# 二、★★★而我先修掉一個我自己造的假象（★它會讓這一格看不出差別）

```
第一版：我從 t=0 開始標記每一隊的到期點
⇒ ★**所有隊在 t=0 都「到期」** —— 那是【我的初始條件】，不是排程的性質
⇒ ★★兩邊的 max 都變成 115 ⇒ **before 與 after 看起來一樣** ⇒ 差別被我自己蓋掉
⇒ 修法：從【第一次真正到期】開始標（before 是 cd；after 是 cd + offset）
⇒ ★★★修完才看得到 115 vs 2
```
★**這與今天那幾次同一族**：**先問「這個數字是世界給的還是我造的」**。

# 三、A2c（相位保留率）—— 綠

```
同一 team 連續兩個 cycle 拿到同一 offset 的比率 ＝ **1.56%**（358／22885）
期望 ＝ 1/cadence ≈ 1.67%｜門檻 <10%（你寫的，我沒改）⇒ ★通過
★純加法的對照值 ＝ 100%（offset 恆定）⇒ **0% 與 100% 之間沒有刀鋒**（你的原話，實測相符）
```

# 四、★A0 維持 FAIL（★我沒有動它）

```
max 格 = 9 > 6 ⇒ 仍紅｜空格 13 ≤ 30 ⇒ 那半仍綠
★你說「維持 FAIL，不下修門檻」——我照做，**床裡那個 6 一個字都沒改**
★★而 §8.5 說註冊版要換統計量（守 median ≤7 且 p99 ≤10）——★**那是【換工作】，我還沒做**：
   我不在同一輪裡把「驗前提」和「守迴歸」混在同一支床的同一格裡，
   ⇒ **等你說要進註冊表，我再把它拆成兩格**（現在這支床的 A0 格仍是「驗前提」，所以它紅）
```

# 五、★誠實限（★必寫，你在票裡指定）

```
①★★本床**只模擬排程，不模擬 gating** ⇒ 它是**排程層的上界**
   ⇒ ★★★**只能 before vs after 互比，不可拿去跟世界量到的 61.5% 比**
②horizon=4320（＝最大 cadence 一個週期）｜teams=115（★與世界最後存活隊數同量級）
③union 只算「到期」，★一隊同 tick 有多站到期只算一次（distinct team）——與世界的 n_deciders 同定義
④★A0b 沒有涵蓋那兩處 **tile-scoped**（labor／idle_employ 的鍵是 tile 不是 team）
   ⇒ 它們的母體是 tile 數不是隊數 ⇒ **本輪未模擬**，而它們在 §2 的 10 處裡
```

# 六、下一步

**你**：①A0b 綠 ⇒ 要不要放行那 10 處的修改（★我還沒動任何 production）
②A0 註冊版的「換統計量」要不要現在做 ③tile-scoped 那兩處要不要也先在床上算。
★**我不自己往下做** —— 你上一封的規矩是「A0b 算完才決定要不要動那 10 處」。
