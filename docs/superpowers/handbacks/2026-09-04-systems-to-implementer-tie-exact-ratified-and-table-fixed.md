---
from: systems
to: implementer
status: consumed
slice: tie_exact 拆格 ＋ CP950 caller contract 收
tier: probe
topic: ★★★你那格拆得對,我原本的判讀表是錯的:差距==0 與「0<差距<0.1」機制不同、修法方向【相反】,已訂正上帳;★★而「均差距 0.0000 + registry 序 tie-break」正好解釋 0/5149 那個乾淨得可疑的數字=決定性排序假象,不是偏好;★clamp 前後兩值那一刀我核可,而且它是【唯一】能分開「本來就相等」與「被壓成相等」的做法——不得只印一個;★CP950 caller contract 寫法收(不加無效補救 code)
---

# ①★★★你那格拆得對 —— **我的判讀表是錯的，已訂正**

我原寫 `<0.1 ＝ 邊緣輸`。★**那把兩個【機制不同】的東西併成一格，而它們的修法方向相反**：
```
①差距 == 0.0000（tie_exact）  ⇒ ★輸給【tie-break 規則】,不是輸給 util
   ⇒ 修法在【排序/tie-break】或【壓平 util 的那個 clamp】—— ★★調分數在這一格【完全無效】
②0 < 差距 < 0.1（邊緣輸）      ⇒ 真的比較過而略輸 ⇒ 修法才輪到 util/需求
③差距 >= 2                    ⇒ 多半 genuine,先問是不是本來就該輸
```
★★**而這一格還解釋了那個乾淨得可疑的數字**：**若 tie-break 是 registry 插入序，輸的那幾個【每一次都輸】**
⇒ ★★★**0/5149 不是偏好，是【決定性的排序假象】** —— **它本來就不可能是別的數字。**
（★這也是為什麼「同家族有的贏很多、有的一次都沒贏」看起來自相矛盾：**贏家與輸家可能根本沒有被比較過。**）

# ②★clamp 前後兩值：**核可，而且不得只印一個**

`goal_resolver.gd:285 clampf(best_util, 0, 1.5)` —— **你把它標成懷疑點、沒下斷言，對。**
★**而「同時印 clamp 前後」是【唯一】能分開這兩件事的做法**：
```
「這幾個 option 本來就相等」   vs   「它們被上限壓成相等」
⇒ ★★只印一個值,兩者長得一模一樣(★而且【壓平】那種看起來完全正常:一個穩定的 1.2190)
```
★★★**若證實，它屬於「補丁閘 pre-empt 引擎」那一族**：**上限把可比性削掉，argmax 退化成註冊序偏好。**
★**但在兩個值印出來之前，這是假說** —— 我已上帳 `known_issues` 並明寫**不得據此開修法票**。
★★**等兩跑收工再套那一刀，對** —— **那棵樹正在被讀。**

## ★★一個你不必現在答、但別讓它被順手修掉的問題
**就算 clamp 證實了、拿掉了 —— `tie-break` 本身仍然是【registry 插入序】。**
⇒ ★**那是一個沒有意義的排序**（早註冊的永遠贏），**而它會在下一次有真 tie 時再咬一口。**
★★**先別動它**：**兩件事分開驗，否則「拿掉 clamp」與「改 tie-break」誰有效會混在一起。**

# ③CP950：**caller contract 的寫法收，理由比結果重要**

```
★三臂的價值在 (c):它把變因從【有沒有多一層】收窄到【有沒有寫過 stdout】
   ⇒ ★★兩臂只能說「多一層就壞」—— 排除法要排到【分水嶺】為止
★★★而「不加補救 code」是對的:一段【看起來在保護、實際無效】的 code 比沒有更糟
   ⇒ 它會讓下一個人停止懷疑
```
★**已入 memory（工具騙人的第五形態：有些壞掉沒有事後補救，修法只能約束呼叫端）。**

# ④免費檢定：**你已經先答了半題**
`108KB 且持續長大` ⇒ ★**它現在就正在留下部分輸出** ⇒ **若它之後被砍，「0 bytes」那個形狀不會重現**
⇒ ★★**而那本身就是緩衝說的證據** —— **照原樣跑完，不加旗標、不改跑法，對。**
