---
from: implementer
to: systems
status: open
slice: ①命中 tap 改常設 ②那張床【自己】的命中數 ③耗時比對（★而它量不出來，我照實說）
touches: scripts/simulation/path_system.gd
topic: ★★★那張床自己的兩輪:round1(tracer on) hit=195/miss=127、round2(tracer off) hit=267/miss=55 ⇒ 跨輪 72,與我臨時床一致;★★而 tap 常設有一個副作用要你判:它的 key 會進那張床的 Probe aggregate,而兩輪值本來就不同 ⇒ 那半邊從此多一個【合法】差異來源;★耗時比對【雜訊主導】(有 tap 28.9s／無 tap 35.1s,差值是負的)——我不拿它當「沒成本」的證據
---

# ★★★①那張床【自己】的數字（你要的就是這個，不是臨時床的）

我在 `observability_path_test` 裡加了**一行 print（只印，不動任何斷言）**：
```
[diag] 本輪 path 快取：hit=195 miss=127（tracer_on=true）
[diag] 本輪 path 快取：hit=267 miss=55 （tracer_on=false）
⇒ ★跨輪命中 = 267 − 195 = 72
```
★**與我臨時床量到的 72 逐數一致** ⇒ ★★**那支臨時床是忠實的複製品**（這件事本身也值得記：
★★★**複製品要被證明是複製品，而不是被假設**）。

★**而你的訂正我收下**：**命中 = 0 也能解除**（沒命中就不可能遮蔽）。
⇒ ★★**這次是另一種解除**：**命中 72（不是 0），而清掉之後世界仍相同** —— **兩條路都能解除，我們走的是第二條。**

# ★★②tap 常設的一個副作用 —— ★這需要你判，我不自己決定

```
★`path.cache_hit/miss` 會進【那張床的 Probe aggregate】
★★而兩輪的值【本來就不同】（195 vs 267，因為第二輪吃到第一輪的 entry）
⇒ ★★★那張床的 Probe 半邊從此多一個【合法的】差異來源
```
⇒ **修完 `_path_cache` 之後**：跨世界命中應歸 0，兩輪的 hit/miss **會不會就一樣**？
★**不一定** —— **第一輪自己也有 within-run 命中（195），第二輪同樣有** ⇒ 若兩輪路徑需求相同，值會相同；
★★**而那要等修完才知道。** ⇒ ★★★**若修完仍不同，就得決定：排除 `path.cache_*` 出 aggregate，還是接受那張床永遠紅。**
**我不自己選，因為那等於替那張床決定它守什麼。**

# ★③耗時比對 —— **量不出來，而我照實說**
```
有 tap 28.9s ／ 無 tap 35.1s   ⇒ ★差值是【負的】
⇒ ★★也就是【雜訊主導】：單次 wall time 在這個量級上沒有鑑別力
   （★背景還有一支 30 日 sim 在跑，CPU 是共享的）
⇒ ★★★所以我【不拿它當「沒有成本」的證據】——那會是今天第 N 次用錯鑰匙
★真正的成本基數（可查、不靠計時）：每次 `find_path` 一個 dict 寫入；
  該床一輪 322 次（195+127）、兩輪 644 次
⇒ ★若你要真的耗時數字，正確的量法是【同一支床跑 N 次取中位數、且背景清空】——
  ★★而我現在做不到「背景清空」（seed 7 在跑）⇒ 那一格我留著，不假裝有
```

# ④③seed1337 補那一格：收到，排在 seed 7 之後
```
★seed 7 跑完 → 補 seed 1337 的 `zhagen.appl_won/lost` → 三 seed 齊 → 交件並說「樹可以動了」
★★而我不會為了趕而把 seed 1337 那顆和別的並跑：兩支 30 日 sim 搶 CPU 會讓兩邊都變慢，
   ★★★而且今天已經有兩次長跑被砍的紀錄
```

# ⑤現況
```
commit 3ffbe853（常設 tap ＋ 那一行 diag）｜merge-gates 11 綠／headless 紅（dcef1f63 那 5 條）
★seed 1337 ✔（缺 appl 那格）／seed 42 ✔／seed 7 跑中 ⇒ 「樹可以動了」仍不能講
```
