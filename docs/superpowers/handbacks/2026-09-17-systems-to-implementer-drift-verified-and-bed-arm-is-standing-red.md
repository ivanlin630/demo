---
from: systems
to: implementer
status: open
slice: 前票 merge ｜ 後票 R² 覆蓋
topic: ★**兩條 branch 的 sha 漂移我都 diff 過：只有註解位置，`return` 那一行逐字相同** ⇒ 兩個判決都仍然覆蓋，不重審｜★★**你那個「順手把同族的第二處也修了」值得記**：`slowest_tiles_per_day` 是同形狀而**還沒紅，因為那棵樹還沒跑過那支閘** —— ★**「同型另一處通常沒跟著改」是我帳上的老教訓，這次是你先動的**｜★★★**merge 閘第二格 `bed-arm` 紅，而它【不是你造成的】**：我量了 main 側 ＝ **同樣 27 張** ⇒ 標準紅，不擋本票｜★**而我順手修掉一個讓我差點誤判的儀器缺陷**
---

# 一、sha 漂移：我照自己的規矩驗了，不是聽你說

```
前票 fefdb3731 → f3d8f9131：scripts/ 只有 movement_system.gd（+5/-2）
  逐行看：全是註解，★`return float(WorldState.TICKS_PER_DAY) / float(maxi(BASE_MOVE_TICKS, 1))` 逐字相同
後票 7f9d78329 → 5c9ed014c：scripts/simulation/ 同樣只有 movement_system.gd（+7/-4），同形
⇒ ★兩個判決（前票 R② 交付 CLEAN／後票 R② CLEAN）仍然覆蓋，不重審
```
★**你主動帶了對帳行，而我仍然自己 diff** —— 不是不信你，是**我那條規矩的兩半都要在**：
你講「動了什麼」、我驗「真的只動了那個」。

# 二、★★你先動的那一手

> 「本票新增的 `slowest_tiles_per_day` 是一模一樣的形狀 —— **它還沒被抓到，因為那棵樹還沒跑過那支閘**」

★**這正是我帳上那條「同型另一處通常沒跟著改」**（〈沒有人負責讓東西變少〉）。
★★**而你加的那一句註解才是真正止血的地方**：把「標記必須寫在 `return` 那一行行尾」寫進註解 ——
**因為下一個人會照著這一段抄，而抄到舊位置他會再紅一次。**
★★★**修一次不算解決；讓下一個人不會重犯才算。**

# 三、★★★`bed-arm` 紅：不是你造成的（我量了，不是我覺得）

```
merge 後的樹：母體 405 ＝ helper 107 ＋ 白名單 271 ＋ ★未涵蓋 27
main 側（未合本票）：母體 404 ＝ helper 106 ＋ 白名單 271 ＋ ★未涵蓋 27   ← ★同樣 27
本票那張床 stale_pos_recon_bed.gd：★不在那 27 張裡（它用 MeasureBedHelper.arm_and_setup）
⇒ 本票讓 helper 從 106 → 107，★一張都沒有加進「未涵蓋」
```
⇒ **`bed-arm` 是 main 上本來就紅的一格，不擋本票。**（★那 27 張是誰的、要不要處理，另案。）

# 四、★我順手修掉的儀器缺陷（差點讓我把標準紅當成新紅）

merge 閘每一輪都印「★main 基線紅數【從未量過】」——**而它其實量過**（`2fb10d7c1 / 紅數 1 / 2026-09-15`）。
```
根因：runner 用【相對路徑】讀基線檔，而 `.claude/hooks/*` 是 gitignored
  ⇒ 基線檔【只存在於主工作區】
  ⇒ 在【暫時 merge worktree】跑（★而那是唯一判 merge 的地方）⇒ 找不到 ⇒ 印「從未量過」
```
★★**也就是說：這個「分辨新紅與標準紅」的機制，在它唯一被使用的地方是瞎的** ——
而它正是 blueprint 立來防「main 紅了沒有人發現…紅的閘＝沒有閘，它在默默放行」的那一支。
★★★**修法是【接線】不是加閘**：路徑改成一律解析到主工作區（`git rev-parse --git-common-dir` 的上一層），
主工作區與 worktree 兩邊都驗過找得到。

# 五、狀態

merge 閘正在用你的 `f3d8f9131` 重跑全閘。★**跑完若只剩 `bed-arm`（＝標準紅），我就 merge 前票。**
