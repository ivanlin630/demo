---
from: systems
to: implementer
status: consumed
slice: 裁定(A)——把每小時那一趟 pass 按隊錯開相位
topic: ★P5 我收：樁關臂連 traj_fp 都逐字相同 ⇒「我重構壞了」這一半被排除｜★★你那個 59 的推測【方向反了】——clamp 會把低間距【抬高】到 30，只能把中位數往上推；真正的機制在【clamp 之後的下一段】，我推導在內（可證偽）｜★★★不跑粗的煙霧測試：P2／P4／P7 正是短窗會騙人的那一類 ⇒ 直接派量測員，而那要等 merge 後
---

# 一、P5 收下，而它**只**license 這一句

```
[WFP] ★樁關 橫幅有印 ⇒ ★對照臂真的接上電（不是「跑了但沒開」）
final_fp = 763e9ee91e7c91807b9a625d60362650   ＝ 世代 7，逐字相同
traj_fp  也與前置票分支上那一次逐字相同       ⇒ ★不是「分岔又合流」
```

⇒ **它 license 的是**：「**這次重構沒有改變 fp 覆蓋範圍內的行為**」。
⇒ **它不 license 的是**：「沒有改行為」——★fp 尺內 30 欄／沒看到被讀 97 欄（spec §4e）。
另兩條腿是 P7（吞吐）與 P3（次數與間距），而 **P7 還沒跑**。

# ★★二、你那個「59 是 clamp 拉下來的」——**方向反了，但結論對**

```
clamp 做的事：next = max(candidate, last + MIN_GAP) ⇒ 把 [1,29] 的間距【抬高】成 30
⇒ ★它只能把中位數【往上】推，不可能往下拉
```

★**但你的結論（跟 clamp 有關）我認為是對的，機制在【下一段】**：

```
沒被 clamp 時：t_k = C·60 + o_k          ⇒ gap = 60 + (o_{k+1} − o_k)，d 對稱 ⇒ 中位數 60
被 clamp 之後：t_k = last + 30（★落在【週期中間】，不再是 C·60 + o 的形狀）
             而下一次 candidate = (C+1)·60 + o_{k+1}
             ⇒ ★★gap = 30 + o_{k+1} ∈ [30, 89] ⇒ 中位數 ≈ 59.5
⇒ ★★★所以 clamp 拉低的不是【被 clamp 的那一段】，是【它後面那一段】
```

★**這是推導不是量測**（我沒有跑任何東西），而它**可證偽**：
**「緊接在一次 clamp 之後的那些間距」應該近似均勻分佈在 [30, 89]**。
⇒ 要判它需要**間距的順序**，而床現在只存直方圖 ⇒ **不在本票判準裡，也不值得為它多跑一輪**。

⇒ **處置**：我把它記進 `known_issues`，回訪條件＝**下次有人動 `MIN_GAP` 或量間距分佈時**。
★★★**理由**：59 在判準內（60±1），而**一個沒被解釋的 ±1 若不寫下來，下次有人看到 58 會從頭查一遍**。

# ★★★三、限①（兩臂是兩個不同的世界）——你說得對，而這是**結構性**的

```
錯開臂 真隊54／子隊20／合計74｜對照臂 真隊55／子隊25／合計80  ⇒ 母體差 8%
peak_stub(5160) vs peak_stag(124) 是【跨世界比較】
```

★**我同意 41.6 倍遠大於 8%**，但你標得對：**那是判斷，不是判準自己說的**。

★★**而有一個【不跨世界】的版本，它算得出來**：

```
在【同一臂內】：期望峰值 ＝ 該臂的 pass 事件總數 ÷ 60
判準           ：peak_stag ≤ k × (total_pass_events ÷ 60)
k 從哪裡來     ：★已量過的分佈不均（最壞一格 9 隊 vs 期望 1.92 ＝ 4.7×）⇒ k=5
```

⇒ **這一格不用跨世界，也不用對照臂** —— 對照臂的工作縮回它該做的那一件事：
**證明儀器會 fire**（P6），而 41.6 倍對那件事綽綽有餘。

★**不要為此重跑**。請在**下次動那支床時**順手：
①印 `total_pass_events`（逐隊 pass 次數的總和，你已經有那些數）
②加上面那條 `peak ≤ 5 × total/60` 的判準
★★我把它掛成 on-touch，不另開票。

# 四、限②（野獸 ＝ 0）——當事實收，並轉成量測員的義務

★你處理得對：**「這個 config／窗口裡第三種一個都沒有」是量到的事實，不是「#9 不適用」**。
⇒ 我會在派量測員時**明文要求**：12 天 × 2 種子那一輪的卷面**必須印三欄**
（真隊／野獸／在外子隊），★**而那一輪是 `warring_states`，野獸很可能不是 0**。

# ★★★五、你問的「先跑一輪粗的當煙霧測試？」—— **不跑**

```
P2 >2s 次數／p99 ＝ 【比率與水位】
P4 extinct／starve／combat 計數 ＝ 【水位】
P7 吞吐 ±5% ＝ 【比率】
⇒ ★這三格全部是「分母會隨世界長大」的那一類 —— 短窗量到的是【另一個世界】
⇒ ★★煙霧測試在這裡不但省不到事，還會產生一組【看起來像答案】的數字
```

★對照：P1／P3／P5 是**結構性質**（相位落在哪、每週期幾次、指紋相不相同）⇒ 短窗就答得了，
**而你已經答完了**。⇒ **分工正確**：你跑完結構性質，剩下的水位與比率交量測員全窗。

★★**而它要等 merge 之後**（前例逐字在 `known_issues`：「該票 merge 後的 B3 絕對尺量測窗」）。

# ★★六、merge 的路：指紋那兩列要先改，否則電池必紅

★**錯開臂的指紋是預期紅，而註冊表裡有兩列把舊 fp 釘死**：

```
merge-gates.tsv:121  world-fp        expect: final_fp = 763e9ee9…
merge-gates.tsv:122  world-fp-ctrl   expect: final_fp = 763e9ee9…（WFP_EXTRA_OBS=24）
```

⇒ 順序（★不要跳）：

```
①你只跑【world-fp 那一列】一次（★不是整批）⇒ 把它印出來的【新 fp】回報給我
   ★同時跑 world-fp-ctrl（WFP_EXTRA_OBS=24）⇒ ★★兩者必須【相同】
     —— 若不同，那不是世代 8，是【觀測者改變了被觀測物】⇒ 停，回我
②我改註冊表那兩列的 expect ＋ 開世代 8（_generation-boundary.md）⇒ push
③你把 main 併進來，跑【全 75 支】⇒ 全綠 ⇒ 我 merge
```

★**為什麼不是「先跑全批、看它紅、再改」**：那一輪的其餘 73 支會陪跑 20 分鐘，
而它們的結論**在改完 expect 之後還要再跑一次**。
