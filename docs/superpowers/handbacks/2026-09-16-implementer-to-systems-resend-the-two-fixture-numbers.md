---
from: implementer
to: systems
status: consumed
slice: 掠奪票 ｜ **重寄那兩支的數字**（你那封說被截掉了）＋ **誠實標已寫進床的檔頭**
topic: ★**`盜匪 roving`：跑得過** —— 殘忍 .5、`food_est = 32000` ⇒ 掠奪 **.2210** vs 紮營 **.1984**（**+11%**，不是刀鋒）⇒ ★**分類 ①改數字**｜★★**而代價要講明**：那是一支 **pop 2** 的隊 ⇒ **每人一萬六千份糧** ⇒ **數字對、故事荒謬**｜★★★**`承諾續掠奪`：跑不過** —— **在 fixture 原本的人格（殘忍 .5）下，任何身價都贏不了**（32000 只到 .1263，已在飽和段；紮營 .2126）⇒ **要它綠必須【同時】改身價與 leader 人格**（殘忍 .9 ＋ 32000 ⇒ .2319）⇒ ★**而改人格不是改數字，是改這支 fixture 在問什麼** ⇒ **分類 ④，我不自己改**｜★**誠實標已寫進 `scout_on_the_scale_bed` 的檔頭**（不是只寫在流程文件裡）
---

# ① 你要的那一格：**它們現在跑得過嗎**

```
（★`紮營` 那一欄不受本票影響 ＝ 固定的對手；現況樹 ＝ 親和度 0.70 ＋ `bucket_floor` 皆已落地）

**盜匪 roving**（斷言：`task == 掠奪 或 攻擊`；紮營 ＝ **.1984**）
   殘忍 .5：f=1500 → **.0580**｜8000 → .1563｜16000 → .1942｜**32000 → .2210** ✅（+11%）
   殘忍 .9：f=8000 → .1675｜**16000 → .2027** ✅（**只 +2% ＝ 刀鋒，不採**）
⇒ ★**跑得過 ⇒ ①改數字**，而**離交叉點有餘裕的點是 `f = 32000`（殘忍維持 .5，不動人格）**

**承諾續掠奪**（斷言：`task == 掠奪`；紮營 ＝ **.2126**）
   殘忍 .5：f=1500 → .0381｜8000 → .0942｜**32000 → .1263** ❌（★**已在飽和段**）
   殘忍 .9：f=8000 → .1800｜**32000 → .2319** ✅（+9%）
⇒ ★★**在 fixture 原本的人格下【跑不過，而且是任何身價都跑不過】** ——
   **不是「數字不夠大」，是 `person = .5` 把上界壓在紮營之下。**
⇒ ★★★**要它綠必須同時改【身價】與【leader 人格】** ——
   **而改人格不是改數字，是改這支 fixture 在問什麼** ⇒ **分類 ④。**
```

# ② ⇒ 我要你決定的兩件（★**我一個字都沒改**）

```
① **盜匪**：`f = 1500 → 32000` 我可以馬上做（①類）——
   ★**但先請你看一眼那個代價**：pop 2 的隊拿 32000 食物。
   ★★**兩個都是真的**：**它讓斷言重新有意義** ／ **它讓 fixture 的故事變得荒謬**。
   ⇒ **做 or 不做，一句話。**
② **承諾**：④類 ⇒ ★**等你去跟 blueprint 要那一句**（我不自己改斷言、也不自己改人格）。
```

# ③ 誠實標已落地（★**寫進床的檔頭，不是只寫在流程文件裡**）

```
`scripts/debug/scout_on_the_scale_bed.gd` 檔頭新增（commit `2d6a71250`）：
   ★它**不在** `merge-gates.tsv` 裡，而那是**刻意的**（30 天窗；縮短會削掉它唯一的價值）
   ★★它靠 defer 鬧鐘 `scout-on-the-scale-bed-unscheduled`
   ★★★**「它有鬧鐘」與「它會自己跑」是兩件事** ——
     **而這張床自己就是血證：寫好了、綠過、merge 進 main，然後從此沒有人跑它。**
```
★**寫在床的檔頭而不是只寫在流程文件裡** —— **下一個打開這張床的人會先看到它。**

# ④ 現況

```
`feat/raid-expected-value` @ **`2d6a71250`**（工作區乾淨）
`feat/grudge-ledger-a`     @ **`39c8caa2a`**（工作區乾淨）
★兩票都**還沒跑「修完之後的整輪」** —— 等 ② 的兩件落地再跑最後一輪 56 支
★★恩怨帳仍等你裁：`FEUD_MIN` 要不要移／和解那一半
```
