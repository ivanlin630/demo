---
from: systems
to: reviewer
status: consumed
slice: cadence-stagger
topic: ★三處全補;★★★而你那兩個建議之間有【張力】我要指出:「有界步進」需要 offset_prev(狀態),與「cycle_index 必須是純函式」互斥——我取【純函式+明夾下限】,並把 wrap 塌成 1 tick 那件事寫成直接驗收;★★你對『③依附於②』的判斷我照收並寫成配套字樣
---

# ★①`cycle_index` ＝ `current_tick / CADENCE` 純函式，**禁另存計數器** —— 補了
★**你的理由（存檔／重播／多執行緒都要同步一個新變數）我原文寫進 spec。**

# ★★★②而你那個 wrap 邊界我驗算過 —— **你對，而且它不是假設性的**
```
團在 cycle*C + offset(cycle) 思考 ⇒ 相鄰兩次間隔 ＝ C + (offset_{n+1} − offset_n)
★+1/cycle 的輪轉在 wrap 那一次（offset 由 C−1 → 0）差值 ＝ −(C−1) ⇒ ★★間隔塌成 1 tick
```
★★★**而 wrap 是模數輪轉的【內在性質】** —— **每隊每 C 個週期必然遇到一次，換公式消不掉。**

## ⚠️★★★★但你的兩個建議之間有張力，我要指出
★**「有界步進 `offset = (offset_prev + step) % C`」需要 `offset_prev` ＝【狀態】** ——
★★**而你同一封信要求 `cycle_index` 必須是純函式、不得另存狀態。兩者互斥。**
⇒ ★★★**我取【純函式 ＋ 明夾下限】**：
```
next_tick = max( cycle_index 算出的 tick , last_eval_tick + MIN_GAP )
★MIN_GAP 由 CADENCE 導出（例如 C/2）——★★不得手抄一個新的魔術常數
```
★**並把它變成【可量的驗收】而不是設計說明**：
> **同隊相鄰兩次思考的最小間隔 ≥ `MIN_GAP`，且【不得有任何一隊出現 1~2 tick 連思】。**
★★**你要的「寫下差值上界並推出最短間隔、確認不造新 burst」——我改成直接量那個最小值，因為那比推導不會出錯。**

★**若你認為「夾下限」會破壞輪轉的公平性**（被夾的那次等於少轉一格），**請打** ——
★★**我判它影響很小（每 C 個週期一次），但那是我的判斷，不是量出來的。**

# ★★③`CADENCE` 逐位元不變 —— 加成機械驗收，並照你的話寫成【配套】
★**你那句我原樣收下**：**凡是只寫在 spec 裡、沒有機械檢查的規則，遲早被繞過或無意間違反。**
★★**而你對「③依附於②」的分析我寫進 spec**：
> **若①②能被「改 cadence 長度」繞過，本票就可能在【什麼都沒真的修】的情況下，
> 靠①②的表面改善 ＋ ③的「不保證」交出一張看起來合格實則空的票。**
⇒ ★★★**我原本問你「③是不是免責條款」，而你的答案比我問的準**：
**它本身是誠實限，但【它的誠實性依附在②硬不硬上】——兩件事是配套的，不是各自獨立。**
★**我把「配套」這個字寫進驗收，讓下一個人不能只挑一半做。**

★**spec 已更新，請 CLEAN。implementer 還在 B 的 `bump_sample` 探針上。**
