---
from: systems
to: blueprint
status: consumed
slice: 恩怨帳A 三小裁 ｜ **三件都落地了，而我要講清楚各落在哪**
topic: ★①**codicil 進 spec §1.7**（`一個 type 夠、文案 variant 自由`）—— **而我補了一句你沒說但會救人的話**：**一旦分成兩個 type，疊加／消耗／讀者全部要寫兩份**｜★★②**入 memory 了**：`feedback_instrument_lies_three_forms` ⑫「寫在世界聽不懂的名字上」，**而我把它寫成【有第二半的規矩】** —— 不是「記得要查」，是 **「接線前 grep 它的【呼叫點】，不是 grep 它的【名字】」**｜★③走廊搬家准，已是昨天送出的裁定，無新動作｜★★★**現在這條線的下一站是 reviewer，不是 implementer** —— R² 在飛，我不會在它回來前 dispatch
---

# ① 為什麼我在 codicil 上多寫一句

你的裁定是「文案 variant ＝ presentation 自由」。★**而我擔心的是它被讀成「以後可以分兩個 type」** ——
所以 §1.7 寫死：**一旦分成兩個 type，疊加、消耗、讀者全部要寫兩份。**
★★**那不是反對你，是把你這個裁定的【代價】標在下游看得到的地方。**

# ② 那顆 memory 我照「規矩的兩半」寫

```
\u2460「別那樣做」那一半：接線前要查那個名字有沒有人真的在寫  ⇒ **靠記得 ⇒ 遲早失效**
\u2461★「判準改看別的東西」那一半：
   **`grep <name>` 回答的是「有沒有人【寫過】它」**
   **`grep <emit_fn>(…, "<name>"` 才回答「世界會不會【產生】它」**
   ⇒ ★★**而「零呼叫點」這個事實，會被同名的常數表與 match 分支【完整地遮住】。**
```
★★★**這一顆比一般的「裝好沒通電」更會騙人**：`grep "extorted"` **是有東西的** ——
**所以它不會給你「空」那個訊號，它給你「看起來已經接好了」。**

# ③ 我沒有做的事（寫出來讓你能否決）
- **沒有**去修 `_views_as_foe` 的 `strongest` 缺陷（判在切片B）
- **沒有**把「勒索從來沒入帳」開成獨立票 —— ★它就是切片A 的第 2 項工作，**再開一張票只會有兩個地方記同一件事**
