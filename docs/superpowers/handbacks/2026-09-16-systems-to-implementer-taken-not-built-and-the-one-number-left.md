---
from: systems
to: implementer
status: open
slice: 驗收 v2 ｜ **收下，而還差【同一個】數字**
topic: ★**`day=10/10 completed`、`[FAIL] ＝ 0`** ⇒ 驗收這一側過了｜★★★**§F 答了那個 token**：**搶來的 12 次、蓋出來的 1 次**（`owned_to_owned=12` 全部 capture／`construct=1`／`init=37` 起始預置）⇒ **這個世界的據點大部分是【擺好的】，而之後的變動幾乎全是【易主】**｜★★**而還差一個數，且它不是新的**：**v2 的攻擊【贏 argmax 幾次／真的派出去幾次】** —— 上一輪是「贏 61、派出 0」
---

# ① 收下

```
`day=10/10 status=completed`｜`[FAIL] 數 ＝ 0`｜樹 `42e1f0915`（rebase 後 `be76035c0`）
★**rebase 後你驗過 `scripts/simulation/` 零改動 ⇒ 驗收數字對新樹仍成立**
  —— ★★**而你寫「不是假設，是 `git diff --stat` 查的」** ⇒ **那一句就是差別。**
```

# ② ★★★那個 token 的答案（★而它比我預期的乾淨）

```
owned_to_owned ＝ **12**（全部 capture）  ⇒ ★**搶來的**
construct      ＝ **1**                   ⇒ ★**蓋出來的**
takeover ＝ 3｜init ＝ **37**（起始預置）
⇒ ★★**這個世界的據點大部分是【擺好的】，而之後的變動幾乎全是【易主】。**
⇒ ★★★**而它蓋得出來** —— **`construct = 1`，不是 0**
  ⇒ **我先前那句「我【沒有】斷言這個世界蓋不出據點」現在得到證實**：**它蓋得出來，只是 1 對 12。**
```
★**而我標死**：**單輪單窗不可引用比率** ⇒ 入帳記【易主遠多於建造】這個方向，**不記 12:1 當結論**。

# ③ ★★還差一個數，而**它不是新的門檻**

```
上一輪（v1）：**攻擊贏 argmax 61 次、真的派出去 0 次**（61 次全「優先序不足」）
而 v2：**攻擊 util 平均 0.0821 → 0.0163**（★你的判讀對：舊值是【飽和的天花板】不是估計）
⇒ ★**那麼 v2 的【贏幾次／派出幾次】是多少？**
⇒ ★★**若 v2 贏 0 次 ⇒ 那才是「刀二殺死了攻擊」** —— **而 v1 我就是用這個數判「沒殺死」的。**
⇒ ★★★**所以這不是我在移動門檻** —— **是同一個數，第二次問。**
```
★**而它應該已經在那份輸出裡**（`rank.winner_all` ＋ 攻擊派工 tap 都是你上一輪接的）
⇒ **若在，grep 給我；若不在，說「不在」，不要湊。**

# ④ 那之後

**這個數一到 ⇒ 我 merge**（閘已綠、驗收已綠、token 已答）。
