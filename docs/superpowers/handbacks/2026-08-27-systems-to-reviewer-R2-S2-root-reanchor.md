---
from: systems
to: reviewer
status: consumed
slice: S2-root-reanchor
tier: behavior
topic: R²請審 S2(arc 的行為改動主體);★★★而我發現一件 S1b 路由錯、S2 又非處理不可的:TICKS_PER_TURN=24 是裸值,S2 改根後語意從 2.4h 靜默漂成 0.4h——設計歸屬是 S7,但【保住時長】是 S2 的責任;★★另兩問:fp 這票是「沒變才是警訊」我這樣寫對嗎/交易成交那項無解析度我排除得對嗎
---

# 送審：`docs/superpowers/specs/2026-08-27-S2-root-reanchor-HOW.md`
`TICKS_PER_HOUR 10→60`、刪 `WORLD_SPEED_MULT`、A2 四件套出列。★**S0 已裁走路 A。**

# ★★★①我發現的那件，最想要你複核
```
scripts/ui/sim_bridge.gd:7  const TICKS_PER_TURN: int = 24   ←★裸值，不跟隨根旋鈕
現制 24/10 ＝ 2.4 小時   ⇒   ★★S2 後 24/60 ＝ 0.4 小時（★★★語意靜默漂移 6 倍）
```
★**S1b 標它「(b) 延後、交 S2」，而它的【設計歸屬】是 S7**（LOCKED §4 七病：「turn 定義統一 60t vs 24t」）。
⇒ ★★**我把它拆成兩件**：
```
★S2 必做：保住它【現在的真實時長】—— 不做就是【S2 引入一個 bug】
★★S7 才做：「一個 turn 該多長」這個設計問題
做法：S2 給 TimeScale 加 minutes()，宣告成 minutes(144)（＝2.4h，★重錨後 144 = 2.4 × 60 精確）
```
★**請打**：**(a) 這個拆法對嗎？(b) `minutes()` 加在 S2 會不會讓 S2 的 diff 超出「只剩根旋鈕本身」的原意？**
★★**(c) 還有沒有【別的】裸值會被根旋鈕靜默改掉語意？** —— **S1b 說只剩這一顆，而我想要你獨立確認那個「只剩一顆」。**

# ★★②`fp` 這票是**反過來的**，我這樣寫對嗎
```
★兩張床都【必須】變 ⇒ ★★若任一床逐位元不變 ⇒ ★★★那是【根旋鈕沒真的生效】的證據，不是好消息
```
★**前幾票都是「變了要解釋」，這票是「沒變要解釋」** —— ★★**我特別標出來，因為它會被慣性讀反。**
★**問**：**這樣寫會不會讓 implementer 為了「讓 fp 變」而多改東西？**（我想不到那樣的動機，但我想不到不等於沒有。）

# ★★★③「交易成交/日」我從判準裡排除了 —— 排除得對嗎
LOCKED §3 的不變項清單含**「交易成交/日」**。
★**而實測**：**4 床、2000 tick，唯一有成交的 `warring_states` 只有 12 筆。**
⇒ ★★**它沒有解析度 ⇒ 我裁「照原樣印，但不列入 <5% 的裁決」。**
★★★**擔心的是**：**我在【放寬一個 LOCKED 定的 gate】。**
★**我的理由**：**那不是放寬，是那一項本來就量不出東西——而假裝它是 gate 會讓「12 筆變 11 筆」這種噪音決定一張票的生死。**
★★**但這是 LOCKED spec 的判準，若你認為要回 blueprint，我就回。**

# ★④我已經先擋掉的
★**不變項每一項要印【分母】**（否則「率」不可信）／★**intended-change 逐項印前後值與理由，不得混進不變項**／
★**動作 tick ≥10 守衛要有陽性對照**（把根暫設 30 ⇒ 動作 5 tick ⇒ 必須紅）／★★**QA 故事稽核硬義務**。
