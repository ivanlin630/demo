---
from: systems
to: implementer
status: consumed
slice: 尾部人格在常態會搶 ｜ **DISPATCH（R² 已過，兩處訂正已吃進 spec）**
topic: ★**spec**：`docs/superpowers/specs/2026-09-16-peacetime-raid-tail-personality-HOW.md`｜★★**三個拉桿，而【每一個都要有自己的前後數】** —— 否則接完若仍差 3 倍，**我們分不出是哪一個沒力**（blueprint 要的 gap 分解表就是靠這個）｜★★★**`retaliation_risk` 直接呼 `ThreatAssessment._power_ratio(state, team, other)`**（`threat_assessment.gd:84`）—— **別另起一份**：那支裡面**已經修過「技能維不對稱」那個舊 bug**，另寫一份就是重演它｜★**`person` 的雙高加成形狀我裁了**（§6），**而我拒絕了直接用 `1−(1−a)(1−b)`：它讓連慫的人都變兇 ＝ 包裝成形狀的 crank**
---

# ① 三個拉桿（★**分開接、分開量**）
```
\u2460 **`w_wealth`**：`DiscountedFlow.flow_weight("wealth", values) = 0.5 + 貪婪` ⇒ **乘進 `take`**
   ★**掠奪與攻擊【一起】接**（上一張票剛把兩者合到同一把秤，別又拆開）
\u2061 **`subjective_cost`**：`util × (1 − retaliation_risk × (1 − 兇性容忍))`
   ★`retaliation_risk` ＝ **`ThreatAssessment._power_ratio(state, team, other)`**（★★**呼既有的，不要自己寫**）
   ★★`兇性容忍` ＝ 與 `person` 同一個 `d`（§6 那個），**不要另造一個人格組合**
\u2062 **`person` 的雙高加成**（§6）：`d = hi + (1−hi) × max(lo − 0.5, 0)`
   ★**單高逐字不變、雙高才更兇、慫的人不變** ⇒ ★★**這三句就是它的驗收**
```

# ② ★★每一個拉桿各一份前後（★這就是 gap 分解表）
```
基準：接線前
①後 ／ ①②後 ／ ①②③後   ⇒ **三個累積點**，每點都報：
  **尾部人格（好戰.9／貪婪.9）在常態富鄰居下的掠奪 util ＋ 當下贏家與其 util**
⇒ ★**若最後仍差 3 倍** ⇒ **這張表直接回答「缺項還是量級」** ——
  ★★**而那正是 blueprint 指定的下一步，不是我們自己加的功課。**
```

# ③ 驗收（§3 三格 ＋ §6 一格，★**四格同一組情境參數、只差人格**）
```
\u2460 **存在性**：尾部人格 ＋ 常態 ＋ 富鄰居 ⇒ **掠奪 fire**
\u2061 **中等人格仍不搶**（同情境）
\u2062 **絕境格**照前裁三條（非壓死／人格有方向／兇者會搶）
\u2063 ★**§6 雙高格**：雙高 ＞ 單高，**而單高的值與接線前逐字相同**
   ⇒ ★★**後半句是「不是 crank」的證明**；**沒有它，那一格只證明了「數字變大了」。**
```

# ④ 順帶兩件
```
・★**那兩支常態 fixture 的斷言先不要改** —— **這張票會讓「兇者常態會搶」變成【可能成立】**
  ⇒ **接完再回頭看它們該寫什麼**（★否則我們會寫一個下週就作廢的斷言）
・★★`殘忍 +15%` 留著（blueprint：**殘忍管【下手狠度】在結算層，不管想不想搶** ⇒ 語意本來就對）
```
