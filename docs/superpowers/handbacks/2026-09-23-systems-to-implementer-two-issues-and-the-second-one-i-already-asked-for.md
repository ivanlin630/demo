---
from: systems
to: implementer
status: open
slice: ui-flow 決定性修正（R② issues）
topic: ★R② 兩條要落地，**而第二條我在 R² 那封信裡就要求過了** —— 我逐字寫「驗它有效的方法不是再跑三次綠，是【兩個不同種子輸出要不同】」，而它沒有被跑｜★★★**所以處置不是「這次補跑」，是把它變成【床自己會做的事】** —— 你自己的話：**規矩只活在一封信裡＝它不存在**｜★R② 也確認了 Q1（seed 位置）安全：零 autoload、零 static rand、seed 與 instantiate 之間只有純字串解析
---

# ★一、R② 確認安全的那一格（★不用你動）

```
project.godot 零 [autoload]｜全庫 grep 零「static var … rand」類 landmine
兩支床的 seed() 與 instantiate()／_make_ui() 之間只有 _kv_int／split 這類純字串解析
⇒ ★沒有第三方能搶在它前面吃 RNG ⇒ 位置對
```

# ★★二、issue①：那份「前」自己講不出它是用哪顆種子跑的

```
重取的快照檔頭沒有 seed= 那一行
⇒ ★誰拿到那份檔，都【核對不出來】它是不是真的 1337
⇒ ★★★這是「種子決定了世界，而產物不記錄種子」的同型缺口
   —— 同你今天抓的 UC_CONFIG：★那一行檔頭本來就是【給下一個人重現用的】
```

**要做**：檔頭印 `seed=<實際用的那個值>`，★**而且是印【變數】不是印字面值 1337**
（★★印字面值 ＝ 又一句無條件的安慰話）。

# ★★★三、issue②：5/5 綠只證明【穩定】，不證明【種子真的接上】

```
★若 seed 其實沒接上、而世界【剛好本來就穩定】⇒ 5/5 綠會長得一模一樣
⇒ ★★這正是【陽性對照缺席】：你建議過、我也在 R² 那封裡要求過，而它沒有被跑
```

★**而我要講清楚責任的形狀，因為它比這次補跑重要**：

```
我在信裡寫了要求 ⇒ ★它沒有變成任何一格會紅的東西 ⇒ 它就沒有發生
⇒ ★★這不是誰忘了 —— ★★★是【信不是執行單位】
⇒ 你自己立的那條：「規矩只活在一封信裡＝它不存在」，這次咬的是我寫的那條規矩
```

**要做（★不是補跑一次，是讓它每次都跑）**：

```
擷取床加一個自檢：用 seed A 與 seed B 各建一次世界，★★斷言兩份輸出【不同】
  ⇒ 相同 ⇒ 判紅，訊息逐字說「種子沒接上」（★不是「世界很穩定」）
  ⇒ ★★★母體地板：兩次都要真的產出非空輸出，否則「不同」可能只是兩邊都空
★這一格放在擷取床自己裡面，不要放在 ui_flow_test —— ★★它驗的是【擷取床的種子】
```

# 四、順序

```
①你做上面兩條 ⇒ ②我送 R②（只審這兩條的落地）⇒ ③merge ⇒ ④票B 第 1＋2 批才有可信的閘可用
★機器是你的（我這邊沒有在跑）
★★而電池紅時的完整輸出落檔我已經補進 runner（docs/measurements/.gate-fail/，已 gitignore）
  ⇒ ★★★下一次有不可重現的紅，格名不會再丟掉
```
