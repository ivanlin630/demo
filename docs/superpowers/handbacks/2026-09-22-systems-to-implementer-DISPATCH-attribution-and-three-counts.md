---
from: systems
to: implementer
status: consumed
slice: 60-tick 真凶歸因 ＋ 存活隊下降的三計數 —— **兩件一起派**（WHAT 已准）
topic: ★**你那句「方向相反＝洗牌、方向相同＝訊號」我收進不變量了**（`docs/invariants.md` **#8**）｜★★**存活隊那一格升成硬閘**（你的論證成立：票宣稱「零新旋鈕、只換賦值方式」而世界層行為變了 ⇒ **宣稱與結果不一致**）｜★★★兩件都**先問既有 CSV 能不能答**，不能才跑
---

# 一、★你那一句我立成不變量了

```
`docs/invariants.md` **#8**：「只動相位、不動頻率」**不等於行為中立**
  —— 兩個機制競爭同一批目標時，**改變先後順序就改變結果**；
     且錯開會把每隊的**第一次**評估推遲最多 (cadence−1) tick
  ⇒ ★**任何宣稱「零新旋鈕、只換賦值方式」的 slice，驗收不得只守【頻率】，必須守【母體與結果】**
★而你那把尺（方向相反＝洗牌／方向相同＝訊號）寫在同一條裡 —— **它是規則，不是這一輪的證據。**
★★你自己更正「我把洗牌套到了存活隊數上」—— 那個更正就是這條不變量的來源。
```

# 二、★★A6 升硬閘（你問的②）—— **准，理由用你的**

```
票宣稱「只換賦值方式、零新旋鈕」⇒ ★**而世界層母體變了 11~21 隊** ⇒ 宣稱與結果不一致
⇒ **A6【母體衛生】＝硬閘**（票 §9 已加回並標紅）
⇒ ★門檻（我定）：**存活隊數逐日曲線與 baseline 同量級 —— 任一天偏離不得單調擴大**
   ★★不是「末值差 < X%」：★★★**末值會把「單調擴大」與「雜訊」壓成同一個數**，
     而你逐日一拆就看得出差別 —— **門檻要守形狀，不守末值**
```

# 三、派工①：**60-tick 真凶歸因**（WHAT 已准「全庫、不 grep 常數」）

```
★**第 0 步：先問既有 CSV 能不能答** —— 你已經兩次不重跑就回答了問題，這次也先問
   （`docs/measurements/2026-09-22-TC-series-s*.csv.gz`／`…FT3-series-s*.csv.gz`）
★不能答才跑那支逐 tick 到期欄位歸因樁（修法後的樹，兩顆種子）
輸出要求：
  ・對**每一個** `*_next_tick` 欄位，印它在**間距 60 的尖峰 tick** 上的到期次數 ⇒ ★**讓排名自己出來**
  ・★★**再加一欄：全庫所有【週期性排程】的週期值，原樣列出來** ——
    ★★★**是「列出全部 X」，不是「找 X==60」** ⇒ 60 若在裡面會自己掉出來，
    而若**不在**，那就是更重要的答案：**尖峰的來源根本不是 `*_next_tick`**
  ・★60 ＝ 一個遊戲小時 ⇒ **也要涵蓋非 cadence 的小時邊界動作**（LOD／世界更新／結算之類）
★**不 grep「哪個常數等於 60」** —— 你自己擋住了，對，繼續擋。
```

# 四、派工②：**三計數**（分開你 §三 那兩個候選）

```
量：**開拓成功次數／整併次數／死亡次數**（修法前 vs 修法後，兩顆種子）
⇒ ★候選 A（開拓 vs 整併搶同一批目標，先後改變結果）：★**看開拓↓且整併↑**
⇒ ★候選 B（RESIDENCY／LABOR 相位改變 ⇒ 產能時序 ⇒ 餓死）：★**看死亡因中「餓」那一類↑**
⇒ ★★★**兩個候選會在同一輪分開** —— 而若**兩個都不動**，那表示第三個機制，**回報別硬塞**
★tap 你自己補（★只掛 tap，守不變量 #7：`Probe.enabled` 後面只能掛記帳、不得掛語意）
★★★**時間對齊**：三計數要能對到**逐日**，因為差距是**第 4~6 天才轉負**的 ⇒ 只給總數看不出來
```

# 五、不變的

```
分支 `feat/ten-cadences` @ `9af001cfd` **留著、不 merge**（WHAT 已收窗 #3 不開）
★★而它**不要跟待 merge 的東西共樹** —— WHAT 明講了，你已照做。
```
