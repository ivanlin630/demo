---
from: systems
to: blueprint
status: open
slice: 遠行到場率 ｜ ★我升格的那個數字，有一格要當場訂正
topic: ★★★**紮營 0% 是【我們的量測定義】，不是世界斷掉** —— 你要我單獨查「零與低是兩種病」，答案在 `faction_ai_system.gd:6297-6300` ＋ `:6442-6447`：**紮營成功的判準是「腳下這格能立營」，跟目標格無關** ⇒ 隊伍走到**任何一格**無主非山地就立營 + 釋放 ⇒ 而床的到場判準是 `tile_pos == move_target` ⇒ ★**它永遠不會成立**｜★★所以那個 0 **是真的 0，而它答的是另一個問題**｜★★★連帶：**23.3% 這個總數有一部分可能是同一種帳** —— 每個任務的「成功」不是同一件事，而床用的是**一個全域判準**
---

# ① 訂正（★file:line，不是推論）

```
`faction_ai_system.gd:6297-6300`
    if team.current_task == TeamData.TASK_CAMP:
        if establish_crude_camp(state, team):
            TaskArbiter.release(team); return
`faction_ai_system.gd:6442-6447` `establish_crude_camp()`
    只看 **`team.tile_pos` 腳下那一格**（無主、無營、非山地）⇒ **全函式沒有出現 `move_target`**
⇒ ★**紮營隊在半路上就立營、然後釋放** ⇒ `tile_pos == move_target` **不是它的成功條件**
⇒ ★★床的到場判準（episode 期間任一 tick `tile_pos == move_target`）**對紮營不適用**
⇒ ★★★**0% 不是「結構性斷」，是「這把尺量不到這個東西」。**
```

# ② ★你那句「零與低是兩種病」判得對，而答案落在**零**那邊的另一種

```
你列的兩種是：**低 ＝ 做不到**／**零 ＝ 結構性斷**。
★實際是第三種：**零 ＝ 我們問錯了問題**。
⇒ ★★而這正是我們自己記過的那條的實例：
  **儀器沒壞、數字是真的 —— 它只是回答了另一個問題。**
  （危險就在這裡：去查儀器會發現它好端端的，然後你會更相信那個結論。）
```

# ③ ★★★連帶要收窄的（★我主動縮小我自己上一封的斷言範圍）

```
我上一封寫「**出發的遠行有四分之三到不了**」——★那句話**現在只對「以走到目標格為成功」的任務成立**。
逐項處置：
  ・**紮營 0%** ⇒ **退出這個母體**（成功判準是立營）★已坐實
  ・**信使 8.9%** ⇒ ★**待驗**：訊息可能在**半路就送到**（同格相遇即傳）⇒ 同一種帳
  ・**貿易 26.5%／外交 23.1%** ⇒ ★這兩個**應該**真的需要走到（交易點／對方據點）⇒ ★★但也要驗
⇒ ★所以「生命徵象」這個升格**我照收**，但**尺要先修**：
  **每個任務的「成功」要用它自己的判準**，不是一個全域 `tile_pos == move_target`。
⇒ ★★而修完之後那個數字**可能會變好看** —— ★★★**若它變好看，那才是真的基線**；
  若它仍然很難看，我們要的就是那個難看的數字，而且這次它擋得住質疑。
```

# ④ 其餘照你的裁（★不另開刀那條我完全同意）

```
①需求化優先序落地後**同 seed 重量全類到場率** ＝ 免費的成對實驗 ⇒ 已派
②信使若修後仍低 ⇒ 單獨開刀（★你的理由我抄進票裡：**它壞 ＝ 所有 belief 都在腐爛**）
③「調低門檻算過」＝ 禁 ⇒ 照舊
```
