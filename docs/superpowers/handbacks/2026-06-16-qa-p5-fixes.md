# Hand Back: QA P5 修復（B-1 守恆 / A-1 招募可達）

分支：`worktree-agent-a90c7e06a7f48b965`（兩 commit，TDD：先紅後綠）

## 實作摘要

- `scripts/simulation/player_command_system.gd`：`_accept_join_request` 改為「先預估可進人數（受 pop_cap 限）→ 撞滿回 `ok=false` 拒收 → merge 後量測實際併入 delta → 食物按 delta 扣、msg/payload 報實際併入人數」。（B-1）
- `scripts/ui/text_ui_main.gd`：team-target handler 在 `recruit` action 加子模式消費——偵測 recruit 回的 menu payload → 進 `_recruit_mode`，列 willing_named 候選 + 匿名選項；新增 `_handle_recruit_mode`/`_build_recruit_str`/`_exit_recruit_mode`、輸入 dispatch、panel router、mode-name、`_close_all_modes` reset。記名走 `recruit_named`(member-kind 經 `execute_action_with_target`)、匿名走 `recruit_anon`(team-kind)，reuse 既有 command/bridge 路徑，未複製招募邏輯。（A-1）
- `scripts/debug/headless_test.gd`：加 `_test_join_request_cap_capped`（撞滿拒收+食物不蒸發 / 部分容量只進部分+食物按 delta+msg=實際），註冊進 runner。
- `scripts/debug/ui_flow_test.gd`：加 `_test_recruit_named_reachable`（驅互動→recruit→進招募子模式→選記名→`recruit_named` 真執行：成員轉隊+coin 扣 150），註冊進 runner。

## 與根因的差異

- B-1：採 spec 建議的「撞滿 0 容量 → `ok=false` 拒收」語意（spec 給的兩選一中擇此），訊息「隊伍已滿，無法收留」。部分容量時食物按量測 delta 扣、msg 報實際併入，守恆與顯示一致。與根因分析一致，無偏離。
- A-1：與根因一致。對齊既有 trade preview / intel 子模式作法（Esc 退出、數字鍵選擇、退出時 `refresh_interaction_targets` 重掃同格）。匿名選項鍵位用 `[A]`（也接受 `[0]`），記名用 `[1~9]`。

## 連動風險

- `scripts/ui/main.gd`（停用圖形 UI）：其 `show_recruit_panel` 消費同一 recruit payload，未改動；本次只補 TextUI 主場景路徑，兩者並存無衝突。
- `respond_to_forced` → `join_request` 分派仍呼 `_accept_join_request`，現會在撞滿時回 `ok=false`（原必 ok）。既有 `_test_join_request_trigger_and_respond`（pop 2→5，未撞 cap）仍綠，無退化；但 forced join_request 在玩家隊已滿時現會「婉拒」而非靜默蒸發食物——這是正確行為。
- 招募後 team 的 pop/named 變動會觸發 `refresh_interaction_targets` 重掃，與既有互動目標刷新一致，無新破口。

## 待主 session 確認

- 視覺項：招募子選單文字 `── 招募 TeamN ── / [1] 名 忠誠X 技能（記名 150 coin）/ [A] 招募匿名人口（50 coin）/ [1~9]記名 [A]匿名 [Esc]取消`，鍵位/排版是否對齊既有子模式風格（trade/intel）由主 session 目視確認。
- 設計決策：B-1 撞滿採「拒收 ok=false」而非「靜默 joined=0 ok=true」。若希望保留 ok=true 但 joined=0 的語意，可調；目前實作守恆與 msg 都正確，差別僅在 UI 是否顯示「已滿」回饋。

## 測試證據

- headless：`=== DONE ===` 無 SCRIPT ERROR；B-1 新測 `撞滿 case: ok=false ... food_spent=0.00` / `部分 case: joined_reported=3 actual=3 food_spent=2.40`。既有 `投靠核心/投靠守恆整合/觸發+回應` 全綠。
- ui_flow：`=== UI Flow Test DONE === errors: 0`；A-1 新測 5 條全 PASS（進招募子模式 / 顯記名候選 / 顯匿名 / recruit_named 成員轉隊 / coin 扣 150）。
- ui_logic：`errors: 0`。
- survival_start multi sanity：`died=no`，`[CoinAudit] coin_eq init=815.0 final=815.0 delta=0.00`（守恆）。
