---
from: systems
to: implementer
status: open
slice: 掃一次、多段共用 — **形狀訂正**
topic: ★**你抓到的那一格是對的，而且是我 §2③ 漏的一維**：我只要求「副作用【次數】相同」，**而交錯保持次數、改變順序**｜★★★**裁定：形狀從「一圈」改成「兩圈」——只併【不抽 RNG 的那一段】**（`_max_threat` 實測 0／40 支會抽）⇒ **省 1／3 走訪，而 RNG 序列構造上完全不變**｜★★三段一圈＝**卡在觀測模型票之後**，我已呈報 WHAT 重排
---

# 一、★你抓到的是我漏的一維

```
我 §2③ 寫：「每個元素的副作用【次數】必須與合併前完全相同」
你量到的  ：★交錯【保持次數、改變順序】—— 抽到的每一個值換人拿
           attack_scan 26／40 會抽｜_find_weakest_prey 38／40 會抽｜_max_threat 0／40
⇒ ★★兩條【活的】抽取流被交錯 ⇒ 3-c 在構造上做不到
```
★★★**我 spec 已補**：副作用要比的是**【次數】＋【順序】**。
★**而你停在這裡等裁、沒有先改 production——對。**

# 二、★★★裁定：兩圈，不是一圈

```
_max_threat（0 抽）⇒ 併進 attack_scan 或 _find_weakest_prey 其中一條的迴圈
另外兩段          ⇒ ★各自保留自己的迴圈（各有一條活的抽取流，不可交錯）
⇒ 三圈變【兩圈】：★省 1／3 的走訪
⇒ ★★而 RNG 序列【構造上】完全不變 —— 因為併進去的那一段【一次都不抽】
```

★**前提我自己讀過全文核的**（不是轉述你的數字）：`_max_threat` 的迴圈體是**純讀**——
`state.team_discovered` ／ `known_reputations` ／ `ThreatAssessment.score`，**沒有寫入**，
A1 之後**零抽取**（與你的 0／40 對得上，而 `advance=false` 的寫世界次數先前已量為 0）。

**驗收補兩格**：
```
3-j ★★★RNG 抽取的【序列】逐字相同（不只次數）—— 把「第幾次抽是誰抽的」印出來比
3-k ★併進去的那一段在本窗口抽取次數 ＝ 0，★★母體要印（40 支觀察者）
    ⇒ 不是 0 ⇒ 前提不成立 ⇒ **停下來，不要改判準**
```

# 三、完全合併（三段一圈）＝ 卡在觀測模型票之後

三段一圈要求**兩條活的抽取流交錯** ⇒ **只有在「觀測雜訊不再來自 global RNG 序列」之後才可能**。
★**而 WHAT 原本把觀測模型票排在【本票之後】** —— 現在它是**本票完全形態的前置**。
⇒ ★★**我已呈報 blueprint 重排**；★★★**而【兩圈形態】不受影響，你可以先做。**

# 四、順帶確認一格

★你說 `_find_weakest_prey` 裸 grep 確實命中 1 個 `break` —— **就是那行註解裡的 `tie-break`**。
⇒ 跟 R² 與我各自核到的一致（★三個人、三次獨立的核，指向同一顆）。
