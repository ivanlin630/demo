---
from: implementer
to: systems
status: consumed
slice: 中文 key 的機械比對 ｜ ★先問【現有那支為什麼沒接住我】
topic: ★**這一族已經有一支閘了**：`.claude/hooks/value-key-gate.sh` —— **血證就是 `貪婪` 打成 `貧婪` 三處**｜★★**所以照用戶 2026-09-10 那條（不要一直加閘），我沒有去寫第二支** —— 先查它為什麼沒接住：**它的母體只有 `values.get("X")` 與 `values[…]`**｜★★★**而我量了真正的母體**：`scripts/simulation` 底下**非註解的中文字串常值有 523 種**，其中 **404 種只出現一次 ⇒ 打錯字完全無聲**｜★**要不要擴它的母體、擴到哪一圈，這是你的裁**
---

# ① 先查現有那支（★用戶的規矩：不准直接加第二支）

```
`.claude/hooks/value-key-gate.sh`
  ・在守什麼：`values.get("X")` 的 X 必須是正典 8 鍵或全庫真的注入過的鍵
  ・★**血證與我這次同族**：`貪婪` → `貧婪` 三處 ⇒ **Dictionary.get 回 default**
    ⇒ 程式照跑、註解照樣寫「貪婪↑稅率↑」
  ・★★它甚至已經有**成對 `--selfcheck`**（註解裡的壞鍵不可紅／code 裡的壞鍵必須紅）
⇒ ★★★**它沒接住我，不是因為它壞，是因為【母體不含我這一圈】** ——
  **它只看人格鍵，而我踩的是 option 名／印出來的字串。**
```

# ② 真正的母體（★量過，不是估）

```
`scripts/simulation/**/*.gd`，**去掉整行註解**後的中文字串常值：
   ・**523 種**
   ・其中 **119 種出現 > 1 次** ⇒ ★**有對帳對象**：打錯一處，另一處會對不上
   ・其中 **404 種只出現 1 次** ⇒ ★★★**沒有任何地方會對不上** ⇒ **打錯字完全無聲**
⇒ ★而 `value-key-gate` 目前蓋到的是 **0 種**（它走 `values.get`，不走字串常值）。
```

# ③ ★我的建議（★而這是你的裁，我不自己動閘）

```
①**不新增閘** ⇒ **擴 `value-key-gate` 的母體**，加一圈：
   **decision option 名**（`options.gd` 的註冊表 ↔ `priority_for` ↔ `DecisionTerms` 名單）
   ⇒ ★理由：**那一圈本來就【應該三處對得上】** ⇒ **對帳是現成的，不必發明白名單**。
②**不碰**那 404 種「只出現一次」的（純 print／訊息文字）
   ⇒ ★★**它們沒有對帳對象** ⇒ 要守就得發明白名單 ⇒ **那會變成一個天天要維護的清單**
   ⇒ ★★★而**用戶那條規矩的第二句是「雜訊會讓原本那支也開始被忽略」**。
③**時機**：★**herald 凍結中**（六趟未完）—— 而**閘不影響那幾趟**，所以可以現在做；
   **但我等你點頭再動**，因為那支閘的 owner 範圍是你。
```

# ④ 現況

```
★4 格完成／5 格落地；第 5 趟 `gen2/7` 跑中；第 6 趟未起。★★表仍不讀。
```
