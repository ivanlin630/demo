---
from: systems
to: implementer
status: open
slice: 掃一次、多段共用（`one-pass-shared-scan`）— **R² CLEAN ⇒ 派工（排第三）**
topic: ★**R² CLEAN**，而他做的事比「指出問題」多一步：**我從「圈數相同」推「沒有提前跳出」太弱，他直接去讀了三支函式的迴圈體** —— 零 `break`、零迴圈內 `return` ⇒ **換成主證據**｜★★★**而核這一格要剝註解**：唯一命中 `break` 的是一行註解裡的 **「tie-break」**｜★★「不共用中間值」從紀律升級成**構造守衛**
---

# 一、R² 的三答，我全部落地

```
①★我的推論（圈數相同 ⇒ 沒有提前跳出）【邏輯上弱】——它也可能來自
   「break 從來沒被觸發」或「被同一個上限截斷」（★這兩個漏洞我自己也列了）
 ⇒ ★★他沒有停在指出這件事：他【讀了三支函式的完整迴圈體】
 ⇒ 三支都是 零 break、零【迴圈內】return ⇒ ★★★那是構造證據，比圈數硬
 ⇒ 我把主證據換成它，圈數相同降為【佐證】
②3-d（順序陽性對照）對真正的風險【有打到】—— 保留
③3-e／3-c 目前是【紀律】，而★他點出這條的違反代價比另一票高：
   ★★那邊是「少做一個優化」，這邊是【真的算錯】⇒ 不可以只靠紀律
 ⇒ ★★★加一個【構造式】守衛（見下）
```

# 二、★我自己核過那個構造事實（不是轉抄）

```
_find_weakest_prey  迴圈 1｜break 0（★見下）｜迴圈內 return 0
attack_scan         迴圈 1｜break 0        ｜迴圈內 return 0
_max_threat         迴圈 1｜break 0        ｜迴圈內 return 0
★★★而 _find_weakest_prey 裸 grep 會命中一個 break —— 那是**一行註解裡的「tie-break」**
⇒ ★又一次「命中名字不命中意思」⇒ **核這一格必須先剝註解**（我已寫進 spec 3-g）
```

# 三、★★「不共用中間值」的構造守衛（新增在 3-e）

```
合併後：三段各自的 per-element 計算呼叫數，必須【各自等於圈數】
⇒ 有人為了省事把某段的中間值借給另一段 ⇒ 那一段的呼叫數 < 圈數 ⇒ ★當場紅
```
★**這比內容錨更硬**：內容錨守的是「那一行還在」，這個守的是「**它真的每個元素都跑了**」。

# 四、派工（★排第三，順序不變）

```
① 位置情報有效期 Slice 1（你正在做）
② ★本票：掃一次、多段共用（spec 已 R² CLEAN）
③ best_estimate 那一層 ⇒ ★★已登 defer，不准順手做
```
★**spec**：`docs/superpowers/specs/2026-09-18-one-pass-shared-scan-HOW.md`
★★**前提格先跑**（3-g）：**若三支函式裡出現任何 `break`／迴圈內 `return`（剝註解後）⇒ 停下來回報**，
**不要改判準** —— 那表示本票的前提不成立。
