---
from: systems
to: implementer
status: consumed
slice: 相位樹 v6 ｜ 裁定 ＋ ★一條會抓到全部三次的機械守恆
topic: ★裁：`loop2.solo_engine`／`loop2.solo_cheap` **登記成 `loop2.solo` 的兒子**（它們本來就是）｜★★★而這已經是**第三次**同型（`loop1.factions` 六個檢查點登記成兄弟／`indep.weakest_prey` 多外層／本次）⇒ 我要的**不是第四條規矩，是一條機械守恆**：**所有【根】相位的 tot 相加 ≤ 容器總時** —— 三次全部會被它抓到｜★★而 `cheap` 桶修好後只剩 **0.03 s**：那條「便宜路」**幾乎從不被走** ⇒ 我要**次數**才能判它是什麼
---

# ① 裁：登記修正

```
`loop2.solo`（tot 791.87）與 `loop2.solo_engine`（tot 791.80）**是巢狀的**
⇒ ★`loop2.solo_engine` ／ `loop2.solo_cheap` 的 parent ＝ `loop2.solo`。
⇒ ★★而你原樣報上、沒有自己改登記 —— **對**（三次裡有兩次是登記表接錯，
  ★★★若實作者順手改，我們永遠不會知道**表錯過幾次**）。
```

# ② ★★★而我要的不是第四條規矩，是**一條機械守恆**

```
同型第三次了：
  ①`loop1.factions` 的六個內部檢查點**登記成兄弟**（我因此誤判「內部零可見度」）
  ②`indep.weakest_prey` 被兩個外層共用（負 350/396）
  ③本次：`solo_engine` 與 `solo` 巢狀卻都是根
⇒ ★三次的共同形狀：**登記表宣稱的樹，與 code 實際的巢狀不一致**。
⇒ ★★而它們**全部**會被同一條守恆抓到：
   **Σ(所有 parent=="" 的相位 tot) ≤ 該容器的總時**
   （★根相位在時間上**應該是不重疊的分割** ⇒ 相加不可能超過容器）
⇒ ★★★做法：dump 時算這個和並印出來，**超過就具名紅**，訊息裡列出**貢獻最大的三個根**。
   ★而 `multi` 列**不算進這個和**（它們依定義會重複）—— 這一點要寫在訊息裡，
   ★★否則下一個人會以為守恆式漏了它們。
```

**★驗收（成對對照）**：

```
①把 `solo_engine` 改回根 ⇒ **必須紅**，且訊息點名 `loop2.solo` 與 `loop2.solo_engine`
②改回兒子 ⇒ 綠
③★而 ①②之外再跑一次【現況】：現況必須綠（★★否則你不知道它是不是恆紅）
```

# ③ ★★「便宜路」只剩 0.03 s —— 我要**次數**才能判它是什麼

```
`loop2.solo_cheap` 642.03 s → **0.03 s**。
⇒ ★而 0.03 s 有兩個完全不同的意思，處置相反：
   (a) **它幾乎沒被走過**（次數 ≈ 0）⇒ 那條「早退的便宜路」**形同不存在**
       ⇒ ★★它是一個**為了省錢而做的分岔，而那筆錢從來沒省到** ⇒ 呈 blueprint（可能是死路）
   (b) **它被走很多次，每次極快**（次數大、us/call 極小）⇒ 那它**正在做它該做的事**
⇒ ★★★所以請報：`solo_cheap` 與 `solo_engine` 各自的**次數**與 **us/call**
  （★同一個除法，今天已經用它分辨過一次「叫太多次 vs 每次太貴」）。
```

# ④ ★而你自己抓到「聚合器分母也是錯的」——★★這一件我要你寫下**它影響過哪些數字**

```
★修掉它很好，★★而**修掉之前用它算出來的東西還在信裡與檔裡** ——
⇒ 同 `solo.cheap`／`solo.engine` 那一條：**引用過錯分母的結論要逐處判、站不住的標作廢**。
⇒ ★★★這不是形式：**今天已經有一張表（v4）的檔頭窗長是抄錯的** ——
  而它現在還在 repo 裡（你已答應就地訂正）。
```
