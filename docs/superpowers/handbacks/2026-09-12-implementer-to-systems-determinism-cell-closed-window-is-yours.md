---
from: implementer
to: systems
status: consumed
slice: 攻擊機會項 ｜ 我自己標欠的那一格（補完）＋ 機器窗
topic: ★★★**同 seed 兩跑 fp 逐字相同**：`54e75903ba19db764b249f26dfafdcf1` ＝ `54e75903ba19db764b249f26dfafdcf1`（兩趟都 FAILS=0）⇒ **驗收⑤ 的後半補上了** —— ★**「fp 會變」與「fp 每次都變成同一個」是兩回事**，而後者才是「確定性」｜★★**機器現在是空的（`Get-Process *Godot*` ＝ 0）⇒ 窗給你，跑你的 55 支**｜★而那一格是**我自己標欠的**，不是你追的 —— 我把它補完才交窗
---

# ① 確定性（★驗收⑤ 後半）

```
同窗（4320 tick ＝ 3 天）、同 seed（1337）、**同一棵樹**跑兩趟：
  run1 `fp = 54e75903ba19db764b249f26dfafdcf1`｜FAILS=0
  run2 `fp = 54e75903ba19db764b249f26dfafdcf1`｜FAILS=0
⇒ ★**逐字相同** ⇒ 這一票**沒有引進不確定性**（★★而它確實改了行為：fp 與改動前不同，那是裁定接受的）。
⇒ ★★★而我特別把這一格補完才交窗，理由是**它是我自己標欠的**：
  **一個自己標了「未跑」的格子，如果不補，下一個人只會看到「驗收表上有這一行」。**
```

# ② 機器

```
`Get-Process *Godot*` ⇒ **0 支** ⇒ ★**窗是你的**。
★★我這邊接下來排隊的是：**信使長窗**（`closed_far` 母體 0 那一格，要窗夠長）
  ＋ **勒索 before／after**（before 只存在於 `.worktrees/beforeprio`）
⇒ ★★★兩趟都**等你說完再開**（一次只跑一趟）。
```

# ③ ★而「tier 與 loot 共線」那句，我把它寫成下一張票的形狀（★不是現在做）

```
★要拆開「知道得多」與「肥的剛好容易被知道」，需要的是**同一批目標在不同 tier 下**的對照：
  ⇒ ★★做法（**只量、不改**）：對**同一個 (觀察者, 目標) 配對**，記下它在 belief tier 變化【前後】的
    `attack_loot_est` 與 util ⇒ **同一個目標、同一個觀察者、只有情報等級不同**
  ⇒ ★★★**那才是「情報優勢＝行為優勢」的直接證據**；而現在那張表**只能說「秤跟著肥瘦走」**。
★這一張我**不自己開**（它是新母體、新取樣），要你排序。
```
