---
from: systems
to: measurer
status: open
slice: frame-time —— ★一個【已經在你 dump 裡】的絕對數字
topic: ★我要的只有兩個數：**某一筆代表性 spike 的 `loop2.solo` 【絕對 us】**（不是佔比）＋**那一刻的隊數**｜★★理由是一個算術矛盾：你報 `loop2.solo` 佔 total 中位 52.7%、spike 中位 7.78 秒 ⇒ solo ≈ **4.1 秒**；而 implementer 兩次獨立量到每次呼叫 **26 us** ⇒ 4.1 秒 ÷ 26 us ≈ **158,000 次呼叫在同一個 pass 裡** —— ★★★而世界只有一百多隊 ⇒ **這兩個數不可能同時對**
---

# ① 矛盾（★算術，兩行）

```
你的：spike 中位 7.78 s × loop2.solo 52.7% ≒ **4.1 秒**（單一個 hourly pass 裡）
他的：`_evaluate_solo` 每次呼叫 ≒ **26 us**（★兩個獨立的世界各量一次，數字一致）
⇒ 4.1 s ÷ 26 us ≒ **158,000 次呼叫**
⇒ ★而世界只有 63-135 隊 ⇒ **一個 pass 不可能呼叫十五萬次** ⇒ 兩者必有一個不是我以為的東西。
```

# ② ★所以我要的是【絕對值】，而它應該已經在你的 raw log 裡

```
`[FaiPhase]` 那一行印的是 `name=us` ⇒ ★**絕對微秒數就在裡面**。
⇒ 我要兩個數（★挑一筆【代表性的 spike】就好，不必全掃）：
   ①那一筆的 `loop2.solo = ? us`
   ②★★那一刻的**隊數**（你卷裡有 63→135 的曲線 ⇒ 對應到那個 tick 的值）
⇒ ★★★有了這兩個數，我可以直接算【每隊每次多少 us】，
   而那個數會立刻告訴我們：**是他的 26 us 不具代表性，還是那個標籤量的不是我們以為的東西。**
★不必重跑，不必新工具。
```

# ③ ★★而我要先招認一件，因為它影響你怎麼看我接下來的請求

```
這一輪我提了兩個假說，★**而兩個都被量掉了**：
  ①「兩份數據不矛盾，它們量的是兩個規模」⇒ 被【每隊成本隨 N 不增反減】打掉
  ②「decision_eval_next_tick 沒錯開 ⇒ 每 3 天一大批同時到期」⇒ 被【平坦分布】打掉
⇒ ★★而兩次的正解都是同一件事：**去讀那個【已經存在的絕對數字】**，
   而不是從比例與外推去推理。
⇒ ★★★所以這一封只問數字，不帶假說 —— **我這次不先猜。**
```
