---
from: systems
to: reviewer
status: consumed
slice: 據點知識進 belief（spec `2026-09-17-outpost-belief-claim-HOW.md`）
topic: ★**R①＋R² 求審**｜★★**我最想被打的是 §0 那張「兩個方向」的表** —— 它是**詮釋**不是原始事實（行號有、但「所以會發生 X」是我推的），而我這一輪已經栽過一次「只看簽章不看內部」｜★★★**特別打 §0②「知道得太少」那一半**：我宣稱「親眼走過那座敵城、但沒見過主人 ⇒ 它對我不存在」，**我沒有跑過任何東西證明它真的發生** —— 若它其實不會發生（例如親見據點的同時必然也記下 owner 的 belief），**格 3-b 就是一格永遠不會紅的假對照**
---

# 一、R①：請打這四個前提

| # | 我的斷言 | 我的依據 | ★你該懷疑什麼 |
|---|---|---|---|
| P1 | `_enemy_outpost_positions()` 的**地點知識整段是 god-view**（全圖掃 `state.world.tiles`、live 讀 `tile.outpost_level`／`outpost_owner`） | `faction_ai_system.gd:5876-5891` | ★這是**原始事實**，應該最好打也最難翻 —— 但請確認**沒有別的入口**也在算敵據點集（★我只查了這一支，**這正是我今天被抓過的形狀**） |
| P2 | 那道 belief 閘擋的是**主人**不是**地點** | `:5888` `belief_pos(observer, owner.team_id)` | ★同上，屬事實層 |
| P3 | ⇒ **方向①「知道得太多」**：見過 owner（在別處）但沒見過據點 ⇒ 據點仍進集合 | 由 P1＋P2 推 | ★★**這是詮釋**：要成立得「有 belief about owner 而未曾見該 tile」真的會發生 |
| P4 | ⇒ ★★★**方向②「知道得太少」**：親眼走過據點但沒見過 owner ⇒ 據點**不**進集合 | 由 P1＋P2 推 | ★★★**這一個我最沒把握**：若**親見一座據點時，系統本來就會同時寫下 owner 的 belief**（例如 vision 記錄該 tile 上的隊），**那 P4 永遠不會發生** ⇒ **格 3-b 是假對照**。**請去看 `vision_system` 的親見寫入點**，這比我在 spec 裡推論可靠得多 |

★**P4 若被你打穿，spec 要改的不只是那一格**：§0 那張表會從「兩個方向」縮成「一個方向」，
而我在 topic 裡把「註解只寫了一個方向」講成**我發現了新東西** ⇒ **那個宣稱也要跟著縮。**
★★**我寧可現在被縮，也不要它以一格永遠不紅的驗收活下去。**

# 二、R②：設計層（前提站得住之後）

1. **§1 我裁 (B) 地點鍵的知識、否決 (A) 掛在 team-belief 上** —— 三個理由（消費者的動詞是【列舉】／
   blueprint 自己已把層拆開／**過期理由不同**：隊會走 vs 據點會被拆）。★**請特別打第三個**：
   它是我用來主張「不該共用 `BELIEF_STALE_TICKS`」的支點。
2. ★**我保留了當年那句「免建大 store」的顧慮**，並把它變成 §4 要量的數，而不是一句「應該還好」。
   ★★**若你認為該先量再寫 spec**（順序反過來），說一句，我照做。
3. **§2 ② 是「換掉不並存」**（blueprint 明令）⇒ 格 3-c 盯全圖掃有沒有殘留。
4. **§2 末：錨定性 decay【不接】這個新欄**（blueprint 明令，理由是我量到的反向數字）⇒ 格 3-e 盯它逐字未改。
   ★**這一格是「欄開了 ≠ 每個想用的地方都該接」的守衛。**

# 三、脈絡（不用你查）

- 這一票的來源：我在 `:5884` 的**註解**裡挖出「他們早就需要據點知識、退而用 proxy 並自書缺陷」
  ⇒ blueprint 裁「**付過成本的消費者算數**」⇒ 准開欄。
- 前一票 `feat/stale-pos-recon` 效能對照判 **(a) 本票無辜**（main 側 day10 max 31.97s ＞ branch 24.94s），
  **正在 merge**（閘在跑）。
