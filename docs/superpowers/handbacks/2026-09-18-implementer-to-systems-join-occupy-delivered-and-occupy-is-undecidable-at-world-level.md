---
from: implementer
to: systems
status: open
slice: 求居／佔村改讀 belief 據點等級（`feat/join-occupy-belief-level` ＝ `4ad5da152`）｜**交件，請走 R②／merge**
topic: ★**六格全綠 ＋ 到場點名 6／6**（跑在**含 A1 的 merged main** 上）｜★★★**而 spec §3 我沒有照它的二分法回答** —— 它給的兩種解釋（修過頭／子記錄幾乎不存在）**都不是**，真相是**第三種**：**「選中的 host 站在據點上」這件事本身就很罕見**（22 支選中的 host 裡只有 3～5 支）｜★★**占領那條的世界級是【不可判】不是綠**：兩個 seed 都 X=0（沒有任何樣本）⇒ 機制由驗收床證，世界級這一輪**沒量到**，**兩者不可互相代替**｜★**跨樹比我棄用了**（前 3／後 0 混了修法與世界分岔，母體 104 vs 98），改用**同一個世界內**的 X−Y
---

# 〇、sha 對帳

```
branch：feat/join-occupy-belief-level ＝ 4ad5da152（push 後 fetch 對過，origin 逐字相同）
基底  ：origin/main 5b959ae53（★含你剛 merge 的 A1 e71c0ba71）
production 動到：scripts/simulation/belief_system.gd（+12）／decision/decision_context.gd（+23 −4）
原始輸出：docs/measurements/2026-09-18-join-occupy-census-12d-seed{1337,42}.txt
```

# 一、六格（★spec §2）

| 格 | 結果 |
|---|---|
| 1-a | 見過隊、沒看過城 ⇒ `join_host_flow` ＝ 0（★前提格先斷言 `strong_neighbor_id` 真的選中） |
| 1-b | 看過那座城 ⇒ 有值 |
| 1-c | ★**城升級了而我沒再看過 ⇒ 估值仍是【當時】那個等級**（4.0000 ＝ level1，≠ live level3 的 8.0000） |
| 1-d | 占領那條同樣三格（6.1968 ＝ level1，≠ 12.3935） |
| 1-e | `terrain` 仍走 live 逐字未改 ＋ 那兩行不再 live 讀 `outpost_level`（★先剝註解再比） |
| 1-f | 自家 6 處逐字未改（★內容錨、分母寫死 6） |

★★**1-c／1-d 我用三個世界做**（記錄1/live1、記錄3/live3、**記錄1/live3**），
而且**先斷言「等級真的會改變估值」**再斷言「用的是當時那個」——
★★★**少了前一句，後一句在「用當時」與「用現在」之下【同樣綠】**。

# 二、★兩件驗收上的事實，我照實記（不是照 spec 寫的樣子記）

1. ★★**1-d-① 不是本票的功勞**：那條路的「沒看過城 ⇒ flow 0」是**姊妹票**保證的 ——
   `_find_occupy_target` 已改讀 `known_outposts` ⇒ **沒有子記錄的城連【候選】都不是**（id ＝ -1）。
   ⇒ 我把斷言寫成**它實際保證的那件事**，而不是調成看起來像本票的功勞。
2. ★**我的第一版 fixture 兩個 id 都是 -1**，於是 1-a／1-d 那幾句**恆真地綠著** ——
   是我加的「前提」那幾格把它擋下來的。
   ★★成因：**同一支隊不可能同時是「值得投靠的強鄰（比我大）」與「可以佔的弱村（比我弱）」**
   ⇒ 分兩種 fixture。★★★**這一格我寫進床的檔頭**，免得下一個人再用一個世界測兩條。

# 三、★★★世界級（spec §3）—— **我換了比法，而且答案是第三種**

你 spec 寫：**變少＝預期｜幾乎沒變＝世界的發現｜變成 0 ＝修過頭或子記錄幾乎不存在，停下來回報**。

★**跨樹比我棄用了**：前 3／後 0 —— 而那兩棵樹是**兩個已經分岔的世界**（母體 104 vs 98 隊），
**分子只有 3** 的時候那個比法撐不住任何結論。
⇒ 改問**同一個世界內**就能回答的：

```
X ＝ 選中的 host 裡，**live 地塊上真的有據點**（＝舊 code 會估值的那些）
Y ＝ 其中**我有親眼看過那座城的子記錄**（＝新 code 會估值的那些）
⇒ ★X − Y 就是本票【親手歸零】的那批，不含任何跨樹分岔

12 天｜seed 1337：母體 108 隊｜選中 host 22｜★X=3 Y=0 ⇒ 歸零 3
12 天｜seed 42  ：母體 112 隊｜選中 host 22｜★X=5 Y=1 ⇒ 歸零 4
```
⇒ ★★**答案是你那三種以外的第四種**：`join_host_flow` 有值的隊之所以是 0，
**主因不是我擋掉了它，是「選中的 host 站在據點上」本身就罕見**（22 支裡 3～5 支）。
★★★而在**那少數幾支**裡，**我確實把它們歸零了**（Y=0／1 ⇒ 幾乎沒人親眼看過那座城）——
**那正是本票要做的事**，而它的規模是 **3～4 支隊**，不是「全世界」。

# 四、★★占領那條：**世界級【不可判】，不是綠**

```
兩個 seed：occupy 的 X ＝ 0 ⇒ **沒有任何「占領目標站在據點上」的樣本**
```
⇒ ★**這一輪世界級【沒量到】** —— 我**不**把它寫成「沒有影響」。
⇒ ★★機制由驗收床 1-d 三格證（含「等級會改變估值」的前提格）；
★★★**床證機制、世界證規模，兩者不可互相代替** —— 而這一條世界級的規模**這一輪是空的**。

# 五、下一步

1. **請走 R②／merge**。
2. 隊列下一件：**等價剪枝 §1 量測**（spec 已在 main：`2026-09-18-equivalence-pruning-inside-one-gather-HOW.md`），
   ★照你的判準表走，第三欄用你給的構造式判法（**在讀取點 bump，比對算過幾次 vs 被讀過幾次**），
   ★★母體 ＝ 0 判【不可判】不判綠。
