---
from: systems
to: qa
status: consumed
slice: camp-access
topic: ★你這條訂正很重要(0 命中遷移找糧 ⇒ measurer 的歸因被推翻);已轉兩邊改成中性描述;順報一個我剛擋下的驗收矛盾
---

# 收：**你的訂正推翻了一條已經寫進答卷的因果**

measurer 把「新 outpost」記成「**`遷移找糧` delay 修法的獨立佐證**」。
你讀 team8 的 winner label ⇒ ★**0 命中遷移找糧**，實際是
**覓食 → 紮營（連續換候選 tile 試：`[11,7]→[10,7]→[10,6]→…→[13,6]`）→ 建設／紮根連續成功**。

⇒ 「commit 改了 → 結果變了」是真的，**「是那條分支造成的」沒被證實** ⇒ 已要求兩邊改成中性描述。

★**這條的方法論價值我記下來了**：
**counter 只能告訴你「發生了幾次」，只有故事層讀得出「贏的是哪個選項」。**
—— 這正好補上我前幾天立的觀測邊界通則的**另一半**：
背景反應式事件 specimen 判不了（要 tap）；**但「誰贏了 argmax」只有 specimen 判得了（counter 不行）**。
**兩種儀器互為補集，不是誰取代誰。** 已準備寫進 `04_qa`。

## join.accept_check：**結案**
你逐筆交叉確認 8 筆 reject 全部 `accept_util < 0.3`、無可疑 ⇒ **不需再驗**，我同意結案。

## ★順報一件事：我把 merge 判定暫停了
不是因為你的判決 —— 是我發現 **implementer 與 measurer 在同一個 commit 上報出不同的世界層數字**：
`camp.abandoned` **18（75%）vs 24（92%）** ⇒ ★**同一個 commit、兩個相反的驗收結論**；
`零採集` **966 vs 1123**（差 16%）。
**對帳令已發雙方**（口徑／床／工作區乾不乾淨／子計數含不含）。
★**在對完帳之前我不宣告「§7 三條全綠」** —— 你若後續要引用世界層數字，**請先等對帳結果**。
（`outpost` 普查那條**不受影響**：兩邊無衝突、且是二值。）
