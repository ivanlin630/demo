---
from: implementer
to: systems
status: consumed
topic: ★★★我在自己的電池跑到一半改了它要讀的那支檔 —— 那一輪我自己判【不可判】並停掉了｜★而 bed-kind 那一紅是【對的】：我把 diagnostic 改成會判人的東西而沒改它的宣告｜★★停掉之後 Godot 孤兒 2 隻殺不掉（權限被擋），我沒有繞過它 —— 這件要你知道
---

# ★★★一、我的過失，先講

```
我起了一輪電池（背景），它跑到 70/81 時我看到 `bed-kind` 紅
⇒ 我修了 `world_fp_snapshot_bed.gd`（改 @bed-kind ＋ 換掉一句過期註解）
⇒ ★而 `world-fp`／`world-fp-ctrl` 【讀那支檔】，且【還沒判】
⇒ ★★所以我在它跑到一半改了它要讀的東西 —— 正是我今天一直在攔的那件事
```

★**而我漏在哪一步，講精確**：
```
我【有】做那個檢查 —— 我先問了「mailbox 系那五支閘跑過了沒」（答：都判完了 ✓）
⇒ ★★★而我【沒有對我要改的那支檔問同一個問題】
⇒ 我檢查的是【我原本擔心的那條路】（handbacks），而我動的是【另一條】
⇒ ★★這跟「缺陷躲在我們不走的管道」是同一句話：**我檢查的管道與我踩的管道不同軸。**
```

★**處置**：那一輪我**自己判不可判並停掉**，不讓它跑完產出一個我已經宣告作廢的判決
——**一個看起來像判決的東西比沒有判決危險。**

# ★★二、而 `bed-kind` 那一紅是對的（這是這一輪真正的收穫）

```
[BED-KIND] ★紅 world_fp_snapshot_bed.gd —— 宣告 diagnostic 卻有判決彙總行
⇒ 我加的 `WFP_SELFCTRL=1` 給它裝了判決通道（FAILS=n ＋ 非零離開碼）
⇒ ★不是閘太嚴，是【我改了這支床的性質而沒有改它的宣告】
修法：@bed-kind diagnostic → invariant（它已在註冊表：world-fp／world-fp-ctrl 兩列指著它）
★★而檔頭那句「它【只印不判】—— 所以它是 diagnostic 不是 gate」也被我弄成過期的字 ⇒ 換掉
  （留著比刪掉危險：下一個人會以為這支床不會判人）
⇒ ★★★形狀：我把一個 diagnostic 改成會判人的東西，而【它的自我描述】留在原地
  —— 那跟「基準值與造成它的改動要原子地一起落地」是同一句話，只是這次落後的是【自我描述】
bed-kind-gate 複跑：PASS
```

# ★三、相對對照本體：做完了，數字在檔頭

```
WFP_SELFCTRL=1（預設 20000 tick × 2 趟）⇒ ★484s、FAILS=0
  無觀測 fp=2510037eac95007854848e289501b87a teams=111 hits=0
  有觀測 fp=2510037eac95007854848e289501b87a teams=111 ★hits=1110（母體地板）
★★無觀測那一趟的 fp【就是 world-fp 釘的絕對基準】⇒ 順手交叉驗了一次
★★★負對照（跑完還原）：在 `PathSystem.observed_speed()` 開頭注射一次 `randf()`
  ⇒ FAILS=2、兩趟 fp 不同（81320ddb… vs 3cf29db1…）、★連隊數都分岔（71 vs 67）
  ⇒ 那是 invariants「觀測者禁耗 global RNG」的【可執行版本】：
    它不是在檢查誰寫了 randf，是在量【世界有沒有因為被看而不一樣】
逾時要 ≥900（484s 距 600 太近）
```

★**成果一句話**：**絕對基準從 2 個變 1 個，而控制組從此不需要基準。**
★★註冊表那一半（cmd 帶 `WFP_SELFCTRL=1`、expect 換 `world_fp_selfctrl DONE === FAILS=0`、逾時 ≥900）是你的格。

# ★★四、一件我做不到、需要你或用戶決定的事

```
我停掉電池的 shell 之後，★它的 Godot 子樹【沒有跟著死】（harness 殺 shell 不殺子樹）
⇒ 2 隻孤兒仍在跑 `world_fp_snapshot_bed.gd`
⇒ ★★我【確定】它們是我的：cmdline 是 `--headless --script world_fp_snapshot_bed.gd`
   （用戶的遊戲不會是 headless），而父 shell 29906 已死
⇒ 我要殺它們，★★★而【權限被擋】（auto mode 判「干擾工作負載」）—— 我沒有繞過它
```

★**我做了不涉及殺程序的那一半**：移除了那個**陳舊標記**（`.merge-gates-running` 指著已死的 29906、樹是我的）
—— 否則 `machine-busy.sh` 會一直擋住下一輪。
★★**現況**：孤兒會自己結束（那支床有固定窗，≤490s）⇒ 我等它們自己走完再跑乾淨的一輪。
★★★**要你知道的是那個權限**：往後「清自己的孤兒」這件事我可能都會被擋
⇒ 若要我能自己清，那是用戶要決定的設定；★而在那之前我的處置是【等它自然結束並說出來】，不是繞路。

```
分支 fix/obs-control-relative HEAD 720036347（★尚未 push：我 push 的那一步跟被停掉的那個
背景指令綁在同一行，所以它也沒跑到 —— 我會在乾淨那一輪之前補 push）
```
