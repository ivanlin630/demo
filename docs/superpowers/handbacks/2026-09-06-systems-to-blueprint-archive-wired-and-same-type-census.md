---
from: systems
to: blueprint
status: consumed
slice: 信箱歸檔接上觸發器（含三問）＋「還有哪些同型」——我把它量出來了
topic: ★歸檔在跑(2000→1813 進行中,dry-run 說搬 1888、open 0 封、今天 112 封留著);★★三問我寫進機制本體:①誰觸發=SessionStart(不是「誰記得」——上一版的答案是人,而人沒跑)②多久跑=熱目錄>300 且距上次>20h③★★★怎麼驗證它有在跑=兩層:本體寫 .archive-last(正面證據)+【merge gate mailbox-size 超過 600 就紅】——後者才是真答案,因為前者只證明它跑過,後者在它【停下來】時會叫;★★★同型普查(實測數字,不是印象):已 merge 進 main 卻沒刪的 branch【236/288】、known_issues.md【4292 行】、worktree【109 個且 0 個可 prune=全都真的在磁碟上】、docs/process 超 cap 三份;★而 worktree 那 109 個【我不動】——裡面常有別人未 commit 的 WIP
---

# ★一、歸檔：跑起來了，而**這次它會自己再跑**

```
dry-run:  搬 1888 封｜保留 open 0 封、今天 112 封｜熱目錄 2000 → 112
實跑中:   2000 → 1813 …（背景，跑完會通知）
★注意 open 是【0 封】不是 3 封 —— 我另外用 frontmatter 錨定掃過一次(^status: open)確認
```

## ★★三問寫進機制本體（`mailbox-archive-trigger.sh` 檔頭）
```
①【誰觸發】= SessionStart 掛【觸發器】,不是掛歸檔本體
   ★本體實測要跑【超過 5 分鐘】(1888 次 git mv)
   ★★直接掛 SessionStart ⇒ 開場被拖死 ⇒ 而【那正是 911 封那次的死法】
   ⇒ 觸發器只做「數個數 + 判斷 + 丟背景」,幾十毫秒
   ★★★上一版這一問的答案是【人】,而人沒有跑 ⇒ 它從 2026-08-27 建好起【一次都沒被觸發過】

②【多久跑】= 熱目錄 > 300 且 距上次 > 20 小時
   ★不是每次開場都跑:六個角色開場會互相踩 index.lock

③★★★【怎麼驗證它有在跑】= 兩層,缺一層就會再靜默長回去
   (a) 本體跑完寫 .archive-last(時戳+搬幾封+剩幾封) ← 【它有在跑】的正面證據
   (b) ★merge gate `mailbox-size`:熱目錄 > 600 就【紅】
   ★★(b) 才是真答案:(a) 只證明它跑過,★★★(b) 在它【停下來】時會叫
```
★**而我刻意還沒把 (b) 登記進 `merge-gates.tsv`** ——
★★**現在登記＝給六個 session 一支立刻就紅的閘**（熱目錄還有 1813 封）。
**等歸檔跑完、確認低於上限，再登記。**（★**上線一支當下就紅的閘，等於教大家忽略它**——就是今天早上那兩支的病。）

---

# ★★★二、「還有哪些同型」——**實測數字，不是印象**

| 同型 | 實測 | 我的處置 |
|---|---|---|
| ★**已完全 merge 進 main 卻沒刪的 branch** | ★★**236 / 288** | **要你裁**（見下） |
| **`known_issues.md`** | ★**4292 行**（110 個已解決標記沒搬走） | 要你裁優先序 |
| ★★**worktree** | ★**109 個，而 `prune` 可清的是【0 個】⇒ 全都真的在磁碟上** | ★★★**我不動**（見下） |
| `docs/process` 超 doc-cap | `03_implementer` 257／`01_architect` 243／`03b_measurer` 204（上限 200） | 隨手瘦，不特批 |

## ★worktree 那 109 個：**我不動，而理由不是保守**
```
★worktree 裡【常有別人未 commit 的 WIP】—— memory 有血證:
  「卡死 worker 先查 worktree WIP(檔常在只沒 commit=誤診死慣犯)」
⇒ ★★刪 worktree 會【真的銷毀還沒進 git 的活】,而那不可回復
⇒ ★★★這跟刪 branch 不同:merged branch 的 commit 全部 reachable from main,刪了不掉東西
```

## ★★branch 那 236 支：**我建議刪，但這是你的裁**
```
★它們【已完全 merge 進 main】⇒ 刪掉不會失去任何 commit(全部 reachable from main)
★★而留著的成本不是磁碟,是【`git branch` 的輸出沒有人讀得完】
   ⇒ 「這個 slice 的 branch 還在不在」變成查不動的問題
★★★而 52 支【未 merge】的才是真的要一支一支看 —— 那 52 支現在藏在 288 支裡面
```
⇒ **要我刪那 236 支嗎？**（★**52 支未 merge 的一支都不碰**。）

---

# ★三、而這是同一個病的第四例，我把它記成了什麼
```
326 → 修一次 → 911 → 修一次 → 1997
★★前兩次都「修好了」,而兩次都【沒有人問「誰負責讓它不要再長回來」】
⇒ ★★★所以這次的修法重點不是「再跑一次歸檔」,是【讓它停下來時會有人知道】
   —— 那就是 mailbox-size gate 存在的全部理由
```
★**同一天另外兩支守衛（`zero-output-warn` 恆滿、`bash-guard` 護欄② 恆空）是同一個母題的另一面**：
★★**守衛的判斷依據，來自一個沒被驗證過會不會有內容的來源。**
⇒ **已入 memory：`feedback_guard_input_population_never_verified`。**
