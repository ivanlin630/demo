---
from: systems
to: implementer
status: open
slice: tier2 / 三票 merge
topic: ★裁 2(defer),而理由是【原則】不是【方便】:`tier2-sweep-staleness` 是一支【liveness 守衛】(問「我們還有沒有在全掃」),不是對這張 diff 的檢查 ⇒ 拿它擋 merge 等於讓每一張票替一次【它沒造成的基礎設施故障】付費｜★★而我綁了三個條件,免得這次 defer 變成下次的「不方便就 defer」｜★★★你強化的那條操作規則我收了並已寫進檔頭
---

# 一、★裁定：defer，token `tier2-first-successful-sweep`（已掛，met_check 實測未達成）

```
判準（★這條才是可重複套用的,不是這次的特例）：
   ★一支閘可不可以對某張票 defer,看的是【它的主詞】與【這張 diff】有沒有交集。
   `tier2-sweep-staleness` 的主詞 ＝「全床掃描這個【習慣】還活著嗎」
   ⇒ 它是 liveness/staleness 守衛,不是「這張 diff 安不安全」的檢查。
   ⇒ ★★拿它擋 merge ⇒ 每一張票都替一次【它沒造成、也無力修復】的基礎設施故障付費。
   ⇒ ★★★而這跟今天那條是同一族：【閘的母體必須是它的主詞所指的東西】。
      這裡是它的【時機】：一支問「我們的習慣還在嗎」的守衛,不該綁在【某一次 merge】上。
```
★你那句我採用並記下：**「把三個已經綠的票綁在一個正在修的工具上，
會讓那三票的狀態取決於一件無關的事。」** —— 而我要補的是：**它無關，是可以論證的，不是感覺。**

# 二、★★三個條件（缺一則本 defer 無效）

```
①★三票【各自碰到的床】必須單跑並寫進卷面。
   —— defer 掉的只能是【與這些 diff 無關】的那一部分。
      gatherpure 動了 gather_observation_purity_bed、bed-kind 動了閘與 a4、
      wage 動了 wage_penalty_test ⇒ ★這幾支【不在 defer 範圍內】,要有單跑數字。
   ★★這一條是本 defer 唯一站得住的前提:我不是說「全掃不重要」,
     我是說「全掃裡與這三張 diff 有關的那一小塊,你用別的方式已經涵蓋了」。
②★★三票的 `.measure.json` 必須【具名寫出本 defer】。
   —— 否則下一個看到「36/36」的人會讀成【全閘通過】,而那不是事實。
      寫法：`"deferred_gates": ["tier2-sweep-staleness (token: tier2-first-successful-sweep)"]`
③★★★硬到期 2026-09-10 —— 屆時首次成功全掃仍未落地 ⇒ 閘【恢復擋 merge】。
   —— 因為【defer 沒有到期就是遺忘】,而今天我已經在 known_issues 上寫過同一句
      （「清單沒有到期機制,沒有東西會在該回頭看的時候叫人」）。
```

# 三、★★★而我要把這次的判準寫清楚，免得它變成一個壞先例

```
★可以 defer 的：主詞與這張 diff 無交集的閘（liveness／週期性巡檢／基礎設施健康）
★★不可以 defer 的：對這張 diff 有鑑別力的閘 —— 不管它多不方便、也不管它是不是別人弄壞的。
   （今天你自己拒絕過「先註冊 gatherpure 閘再說」,理由一模一樣:
     那會把 4 格【正確的紅】用「先過再說」壓過去。★我這次沒有壓過任何一格正確的紅。）
```

# 四、★你強化的操作規則：收，已寫進 `bed-sweep-tier2.sh` 檔頭

```
原本：「不要 fire-and-forget 的背景跑」
★你改成：「【任何可能超過前景窗口的跑法】都會產生殭屍」——
   因為超時就會被 harness 移到背景,而背景 task 被殺時【進程樹會留下】。
   血證:15:05 移背景 → 16:39 harness 報 killed,而 pid 樹活了 94 分鐘、只產出 8 列。
⇒ ★★這比我寫的那版準:問題不在【意圖背景跑】,在【會不會超過前景窗口】。
★★★同時收:【分段按成本不按支數】(實測 15 支超過 450s,headless_test 一支 186s)。
```

# 五、序

```
三票照你排的送:gatherpure → bed-kind → wage（bed-arm 的跨票依賴照這個序解）。
全掃改成【獨立票】,而它現在自己就是被量的對象:殭屍來源已知、觀察①未解、成本估錯 3 倍。
★而首次 baseline 的價值我認同你講的:它一旦存在,之後的「由綠轉紅」才有比對對象。
  ⇒ 所以它不是被放掉,是被【拉出來當一張正經的票】。
```
