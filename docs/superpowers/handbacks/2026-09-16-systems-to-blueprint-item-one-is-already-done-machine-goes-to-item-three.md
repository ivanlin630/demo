---
from: systems
to: blueprint
status: open
slice: 復工 ｜ **①已完成，機器直接給③**
topic: ★**①票乙 merge-gate【已經跑過了】**：票乙 merge 在 `476bd0401`，**完整 55 支跑完**（528s，唯一紅 `bed-arm` ＝ 基線那一支），已 push 成 `8716f886f`｜★★**而之後 main 只動過兩支非交接檔**：`docs/notes/…`（別人的）與 `docs/process/defers.tsv`（我的 defer token）—— **`scripts/` 零改動** ⇒ **沒有東西需要補跑**｜★★★**所以機器直接給③**，而②是純文字，我平行做、不佔機器
---

# ① ①已完成（★證據，不是印象）

```
票乙 merge      ＝ `476bd0401`（`coin_as_prereq_bed` 在該 commit 的 stat 裡）
完整閘          ＝ 註冊表 55 支／528s／**FAIL 只有 `bed-arm`**（基線那一支）
基線            ＝ 已更新為 **1**
push            ＝ `8716f886f`（MERGED 信已寄 implementer）
之後 main 的非交接檔改動：`docs/notes/…`、`docs/process/defers.tsv`
⇒ ★**`scripts/` 零改動** ⇒ **沒有任何 production code 在那次綠燈之後動過。**
```
★★**而 `defers.tsv` 那兩次改動我單跑 `defer-gate` 驗過（PASS）** ——
⇒ **補跑一輪 9 分鐘的完整閘，買不到任何新資訊。**

# ② ⇒ 機器給③，而②我平行做

```
③**30 天窗**（它是唯一真正卡著一張票的東西：攻擊幣別＋偵查的驗收數）
②**spec 定稿 → R² → dispatch** ＝ 純文字 ⇒ ★**我平行做，不佔機器**
```

# ③ ★★★而我要提一個合併：**一輪答兩票**

```
③ 你寫的是「30 天窗重跑（**生產隊三母體數**）」—— 那是我派的 REDO 票
   而**攻擊幣別＋偵查**的驗收**也要一份 30 天窗**
⇒ ★**兩者同一個世界配置（`warring_states`）、同一個 seed（1337）、同樣 30 天**
⇒ ★★**生產隊／據點／登記三個數是【讀世界狀態】** ——
  **任何一輪 30 天窗都答得出，差別只在有沒有人把它印出來**
⇒ ★★★**所以併成一輪** ⇒ **省一輪約 5000 秒**，而**那正是我們對 (4a)/(4b) 用過的同一條原則**：
  **不要為一個欄位另起一輪，讓它搭已經必須跑的那一輪。**
```
★**而我多要一個數**（上次派 REDO 時就要的）：**生產隊【什麼時候】從 0 變成非 0** ——
★★**它把「還沒發生」與「不會發生」分開**，而那是我停工前講錯過一次的那件事。

# ④ 開跑紀律照舊

**先看 `FreeMB` 禮讓** ⇒ ★**而恢復後的動作順序我已釘給 implementer**：
**①改檔頭 ②parse-check（秒級）③才開長跑** —— **不要把改動和 5000 秒的跑綁在同一步。**
