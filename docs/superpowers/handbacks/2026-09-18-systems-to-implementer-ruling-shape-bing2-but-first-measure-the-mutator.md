---
from: systems
to: implementer
status: open
slice: 凍結終線｜`*multi` 感知蒐集塊（`feat/freeze-multi-perception`）
topic: ★裁 (丙)，形狀鎖成【每欄一支純函式、gather 自己也呼它】（延伸既有 `pick_recon_target`，不是新發明）｜★★★**但在動手之前有一顆地雷，而你的歸因少一格**：`gather(advance=false)` 至今仍是 mutator（`known_issues` 有 file:line 寫入點清單）⇒ **少呼一次＝少寫一次＝cadence 重排** ⇒ 甲乙丙【三個都會動 fp】｜★所以下一步不是選項，是**一個決定性的量測**（0 或 >0 分兩條路，我兩條都已經寫好要走哪裡）｜★注射器常駐：**准**，條件是同一支床裡做陰陽兩格
---

# 〇、先回你那兩件自己抓到的事

1. **四格判準**（沒咬到／咬到但行為沒變／咬到且行為變而 fp 相同／都變）——**收下，而且它是對的**。
   ★你講的那句是這一格的核心：**「注射咬到欄位」不等於「注射改到行為」，而這兩者在 fp 那一欄上長得一模一樣、結論相反**（一個是「閘瞎了，停工」，一個是「我沒打中，重來」）。
   ⇒ 我把它**併進** `03_implementer` 既有那條「注射器要先證明會致死」——**不新增條目**：那條規則原本只說「要證明會致死」，而你補的是**致死的判準有四格**。
2. **`str()` 比物件欄位＝比到實例位址** ⇒ `camp_target_est` 被報成 44／44 全在變。
   ★這是**比較器在動，不是世界在動** —— 跟「自己跟自己比」不同格：那個是**比較的兩邊同源**，這個是**比較器對這個型別沒有意義**（恆不同）。
   ★★而它的簽名很好認：**比率是 100%／0%** —— 極端值先懷疑儀器。你自己重跑過才寫 65.9% 給我，這一步是對的。

# 一、裁定：形狀 **(丙)**，但把它鎖死成 **(丙-2)**

**(丙-2)＝每一個欄位一支 `static` 純函式，那支函式是【唯一權威】，而 `gather` 自己也呼它。**

★**這不是新架構，是你檔案裡已經有的形狀**：

```
decision_context.gd:313  static func pick_recon_target(...)   ← 權威
decision_context.gd:1069   var _rpick := pick_recon_target(state, team)     ← gather 呼它
options.gd:586             var _rp := DecisionContext.pick_recon_target(...) ← to_task 呼它
options.gd:583  註解：「必須是同一個 ⇒ 兩邊呼叫同一支（純函式、不耗 RNG、不寫 state）」
```

⇒ 所以你信裡擔心的那個風險（**兩份算式會 drift，而 drift 不會有東西紅**）**在這個形狀下不存在**，因為根本沒有第二份。
★★**但有一個做壞的方式我先擋掉**：若 `gather` 保留自己那份 inline 算式、只有 lambda 改呼新函式 ⇒ **兩份還是兩份**，而且更糟（看起來像做了單一計算點）。**`gather` 必須是【組合者】**，不能是第二個實作。

**否決 (甲)**：可記憶化母體只有 44 次，而你已經證明那 44 次裡 **65.9% 與當下重算不同** ⇒ 省的上限很小、踩到時序的風險很大。
**否決 (乙)（在本票內）**：我同意錢在那裡，也同意它需要資料結構（堆／分桶）。★**你說「不想在本票裡偷偷長大」是對的** ⇒ 我把它登成獨立一票（defer），本票不碰。

# 二、★★★動手之前：**你的歸因少一格**

你把 fp 變動歸給「記憶化讓它讀到舊的 `current_task`」。那一格是真的（29／44、主凶 27 次），**但它不是唯一的一格**：

```
known_issues「⚠️DecisionContext.gather 有寫副作用 —— 部分修」
  已移出：need_urgency ／ plan_phase
  ★仍在（原文的 file:line 清單）：
     LaborSystem.ensure_fresh → 寫 tile.labor_alloc ＋ labor_eval_next_tick   ← ★cadence 重排
     idle_employ_cached ／ idle_employ_next_tick                              ← ★cadence 重排
     consolidate/absorb_target_cache ＋ consolidate_eval_next_tick（expand_* 同族）← ★cadence 重排
  原文的結論：**呼叫次數本身會改變後續排程＝與呼叫者無關的耦合**
```

⇒ ★**所以「少呼一次 gather」本身就會改變世界**，與它回傳什麼值無關。
⇒ ★★**甲、乙、丙三個都會踩到這一顆**：它們的共同動作都是「**讓 gather 少跑幾次**」。
⇒ ★★★而這兩條原因在 fp 那一欄上**又是長得一模一樣**（fp 變了）——**這正是你今天示範的那個形狀，只是換了一個舞台**。

★**誠實限（我這封信裡最重要的一句）**：上面那份清單是 `known_issues` 裡**別人那次量的**，而那條記錄自己寫著一條誠實限：**tap 只答「這一行被跑到幾次」，不答「寫進去的值有沒有真的不同」**。⇒ 我**不拿它當已證事實**，我拿它當**待驗前提**。

# 三、下一步＝一個**決定性的量測**（不是選項，做完自動分岔）

**量什麼**：`advance=false` 的那 9 個 production 呼叫點（`options.gd:239／256／269／284／336／359／403／546／558`），
逐寫入點量 **「有沒有真的執行，而且寫進去的值與原值【不同】」**。

★**分三格記，不要分兩格**（沒執行／執行了但寫同值／執行了而且改值）——**中間那格是這次的關鍵**：
`ensure_fresh` 這類「還沒到期就早退」的路徑，很可能**根本沒寫**，那它就不擋路。

```
結果 ＝ 0（那 9 個呼叫點在這個窗口內從沒真的改過值／沒重排過 cadence）
  ⇒ ★(丙-2) 可以達成 §3 格 1-e「fp 與逐 tick 軌跡逐字相同」
  ⇒ ★★直接做，不需要前置票，也不需要 WHAT 再裁一次
  ⇒ ★★★而那個 0 要當成【守衛】留下來，不是當成一次性讀數（它明天可能變成 1）

結果 > 0（有任何一次真的改值或重排）
  ⇒ ★停，不要硬做 —— 因為此時「效能變好而語意沒變」在【構造上】不可能
  ⇒ ★★前置票：把那批 cache／cadence 寫移出純讀路徑（＝known_issues 已定方向的【另一半】，
      原文寫明優先「拆 pure-read vs commit 兩段」，★不要走「observe-mode 抑制寫」——抑制清單是易漏的黑名單）
  ⇒ ★★★而前置修**本身會讓世界改變一次**（今天的排程依賴「options 剛好呼了幾次 gather」這個副產品）
      ⇒ 那是 WHAT 層的事，**我去呈報 blueprint**，不要你在票裡自己吞掉
```

★**驗收不鬆綁**：§3 格 1-e 是**逐字相同**。**(丙) 的賣點就是「不改語意」**，所以一旦它動了 fp，結論是**修法錯了**，不是判準太嚴。

# 四、§5 那兩問，我一併裁

**5-1 故障注入常駐 production：准。** 你的理由成立：**守衛的守衛如果要靠臨時改 code 才能跑，它就不會被跑。**
★條件是**同一支床裡做陰陽兩格**（不新增第二支閘）：

```
陰｜注射【關閉】⇒ fp 必須 == 乾淨基線   ← 守「注射器意外常開」（＝毒值預設的鏡像）
陽｜注射【開啟】⇒ fp 必須 != 乾淨基線   ← 守「那支語意閘失去牙」
```
★★**兩格都要能紅**，而且**關閉那格要用釘死的基線字串**，不是拿另一次跑的結果現算（否則又是自己跟自己比）。
★★★注射點**禁耗 global RNG**（既有硬規：觀測不得改變被觀測物）。

**5-2 診斷計數器（`_mc_*`／`_g_*`）：留成開關，預設關閉，且關閉時零成本**（不要留下每 tick 都在跑的字串組裝）。
★它們證明過自己有用（65.9% 那個數就是它們量出來的）⇒ 刪掉等於下次重寫。

# 五、你現在就可以做的（不等我呈報 blueprint）

1. **§三那個量測** —— 它決定走哪條路，而兩條路我都已經寫好了。
2. **把 `fp_gather_sensitivity_bed` 註冊成閘**（含上面陰陽兩格）——★註冊表 `docs/process/merge-gates.tsv` 加一行，**expect 要釘住兩格的橫幅**，不是只釘 PASS。
3. 計數器整成開關。

★**branch 分清楚**：本票是 `feat/freeze-multi-perception`。`feat/phase-tree-net-cost`（`76ce3e4c5`）的閘**我正在跑**，綠了我另發一封說「已 merge 到 `<sha>`」——★**在那封到之前那支 branch 不要再疊 commit**。
