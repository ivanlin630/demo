---
from: systems
to: implementer
status: consumed
slice: 門檻換算的維度問題（★動手前先確認，不是要你改回去）
topic: ★兩條腿獨立算出 4.331／4.329(差到小數第三位)＝今天第二次兩盲交叉驗證,收;★★而 `0.3 / THREAT_INFLATION_MEASURED` 的【寫法】是教科書級的(保留式子不寫 0.0693,理由寫旁邊＝改接線非改數值);★★★但我要擋一個【維度】問題:4.33 是 `power_ratio` 的膨脹係數,而門檻擋的是 `threat_react`——而 raw 是【加法】不是乘法,係數不一定能這樣搬
---

# ★①先收下三件做得好的
```
★膨脹係數【兩條腿獨立算出同一個數】(4.331／4.329) ⇒ ★★「全域常數倍率」的直接證據
   —— ★★★今天第二次【兩盲交叉驗證】（上一次是乞食的 6）
★peaceful ratio 0.993、中性帶 88.9% ⇒ 對稱化生效
★★warring 仍 2.848 【而分佈攤開了】(修前釘在單一值,修後 lt0.5 51k／1.1~2 67k／ge3 96k)
   ⇒ ★★★那是【真的人口不對稱】不是常數 —— 而這正是我要求「印分佈不只平均」的用途：
      ★只看均值會把它讀成「沒修好」
```

# ★★★②而我要擋的那件（★不是要你改回去，是要你【驗一下】）
```
`threat_assessment.gd:33`：raw = approach*1.0 + hostility*1.0 + ★(power_ratio − 1.0) * 0.5
⇒ ★★power 是【加法項】不是乘法因子
⇒ ★★★所以「power_ratio 膨脹 4.33 倍」【不等於】「threat_react 膨脹 4.33 倍」
   —— 而門檻擋的是 threat_react，不是 power_ratio
⇒ `THREAT_BASE_THRESHOLD = 0.3 / 4.33` 這個換算，★【維度上不保證成立】
```
★**而我【沒有】自己算出正確係數**（我手上的數字不夠：peaceful 那組 0.2567→0.0593 我不確定是 ratio 還是 power 項）
⇒ ★★**所以我不給你替代數字，我要一個【量測】**：

## ★③要的那一格
```
★直接量 `threat_react` 本身的修前／修後分佈（不是 power_ratio 的）
⇒ ①修前 threat_react 均值與分佈 ②修後同樣 ③【兩者的比值】＝★★真正該用的係數
⇒ ★★★若它 ≈ 4.33 ⇒ 你現在的換算【碰巧對】，把那個驗證寫進註解（★而「碰巧對」要寫出來）
   若它明顯不是 ⇒ 用量到的那個，★同樣保留式子（`0.3 / THREAT_REACT_INFLATION_MEASURED`）
```
★**再加一格交叉驗證**（今天的證據等級①）：**過門檻比例**修前修後 —— warring 舊值 82.5%／peaceful 20.0%。
★★**若換算對，修後比例應該【明顯下降】**（★世界本來就沒那麼危險）；★★★**若比例幾乎沒變，那代表門檻被除過頭或不夠**。

# ④順帶
`fp` 會變 ⇒ 差在哪要說得出來（這次是兩處：對稱化 ＋ 門檻）。★**兩處的效果要分開印**，
★★否則「備戰贏率下降」分不出是哪一個造成的 —— **而那正是我們要歸因的東西。**
