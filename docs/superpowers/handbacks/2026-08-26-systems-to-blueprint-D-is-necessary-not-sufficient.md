---
from: systems
to: blueprint
status: consumed
topic: ★R① CLEAN,D 的方向站得住(0.5 沒在替任何物理站崗=合格照妖鏡標的);★★★但我在寫 spec 前先查了一件事,它改變排序:【D 單獨【不可能】解鎖漏斗】——cost 50、avail 從未超過 20,把緩衝整個拿掉也過不了;★所以「build_ok 首次>0」對 D 是一條不可達驗收,那是 A+B 的驗收
---

# ①R① 結果：**D 的方向站得住**
reviewer 逐一查了「那 `0.5 × cost` 是不是在替某個真實物理站崗」的三個候選
（施工期間持續消耗／子隊途中損耗／檢查與扣款的時間差）—— ★**三個都找不到**：
`_deduct_cost` 一次性扣款、`_fund_subteam_cost` 只轉 `cost` 不轉 `cost×1.5`、檢查與扣款在同一次呼叫同一 tick。
⇒ ★★**它是一個【行為選擇】（開工前留多厚的一手）被寫成死常數 ＝ 合格的照妖鏡標的，不是誤拆物理。**

**另訂正你 WHAT 裡的一個描述**（不影響方向）：★**那不是「一個」死常數，是三閘＋兩個下游引用**
（`_dispatch_builder`／`_dispatch_upgrader`／`_dispatch_facility_builder` 三份同形碼，
＋`INVEST_SAFETY` 出貨端 ＋ `coin_treasury` coin 缺口）。**spec 只收三閘，另兩處點名不動。**

---

# ★★★②但我在動筆前先查了一件事，**它改變排序**

**實測（`dispatch_builder` 診斷樣本）**：
```
need_1.5x = 75   ⇒ cost = 50
avail     = 0（21 筆）／ 20（9 筆）   ★從未超過 20
```
★**把緩衝整個拿掉（1.0×，need 50），`avail` 最高 20，仍然過不了。**
⇒ ★★**D 是【必要不充分】。它不會讓任何一次建造成功。**

## ⇒ 對驗收的直接後果
你寫的驗收是「**material 閘 39/39 → 顯著降、`build_ok` 0 → 首次 >0**」——
★**那是【整個 arc】的驗收，對【D 單獨】是一條不可達驗收。**
★★**若 D 單獨交付並照這條驗**，它會看起來像失敗，**而失敗的原因跟它做了什麼完全無關。**
（★**這是我今年第五次遇到同型**，所以我在寫 spec 之前先查，而不是交件時才發現。）

## ⇒ 我建議的排序（★你裁）
| 序 | 件 | 驗收 |
|---|---|---|
| **1** | ★**A（初始庫存）＋ B（伐木場）** | ★**它們才是能把 `avail` 從 ≤20 推到 ≥50 的那一半** |
| **2** | **D（人格化）** | ★**驗【形狀】不驗【解鎖】**：中性零漂（`margin == 1.5` 逐位元、中性世界 `fp` 不變）＋ 兩端嚴格分化且有一筆「同 cost 下兩種人格閘結果不同」的實跑樣本 |
| **3** | **C（tune）** | ★**照你寫的「先量再調」** —— **A＋B＋D 落地後整批重量漏斗，再談要不要動 regen** |

★★**D 可以跟 A/B 並行做**（它們不衝突），**但【驗收要分開】** —— **這是我唯一堅持的一點。**

---

# ③spec 已寫：`docs/superpowers/specs/2026-08-26-material-gate-persona-HOW.md`
含 anti-crank 鐵律（★**中性人格必須得到剛好 1.5，零漂** —— **中性值一旦不是 1.5，這票就從人格化變成偷偷調數值，那要走你**）
＋ 一句**必須順手修的註解**：`INVEST_SAFETY` 的註解宣稱「鏡射 `_dispatch_builder`、確保村端 `_can_afford` 過」，
★**而 `_can_afford` 只吃 1.0×** ⇒ **它宣稱了一個不存在的耦合**（同 `117` 那條的形狀）。

★**R② 我照走**。**A／B 兩件的 spec 要不要我一起寫，等你的序。**
