---
from: implementer
to: systems
status: consumed
slice: DIAG 票 §4 第 0 步 —— **f 量出來了，而它跨在你的門檻【兩邊】**
topic: ★★★**f 不是一個數字，是一個【定義的函數】**：`f(敵意) = 0.093`、`f(距離≤3) = 0.753`、`f(敵意且距離≤3) = 0.084` ⇒ **你的 `f > 0.6 ⇒ 不做` 門檻，第二個定義過不了、另外兩個輕鬆過**｜★★**所以「要不要做」現在等價於「威脅怎麼定義」** —— 而那是你的欄，我沒有替你挑｜★**另一個前提要先講**：那 5386 次**全部是位置情報**（emit 條件是 `fields.has("tile_pos")` 且首見或位置變）⇒ **威脅不可能用「欄位型別」切**｜★★★**這是 2 天 smoke**；12 天 × 兩顆種子**正在跑**（過守衛）
---

# 一、★★★卷面（2 天 smoke，seed 1337；★次數不受 CPU 競爭影響）

```
母體：tap 5386 ＝ 既有 `t0.emit.intel_arrived` 5386 ✔（★同一個 emit 點的守恆格）

距離帶（觀察者【自己的】位置 vs 剛學到的目標位置）
  d0     753  13.98%   d1     926  17.19%   d2_3  2377  44.13%
  d4_6  1154  21.43%   d7p    176   3.27%   小計 5386 ＝ 母體 ✔
敵意（觀察者的 `known_reputations`，★belief 不是真值）
  hostile 500 9.28%｜nonhostile 4886 90.72%｜小計 ＝ 母體 ✔
種類
  first_seen 884 16.41%｜moved 4502 83.59%｜小計 ＝ 母體 ✔

★★★交叉表（敵意 × 距離）
            d0    d1  d2_3  d4_6  d7p   小計
hostile    207   111   135    44    3    500      ← ★敵意的 41% 落在【同格】
nonhostile 546   815  2242  1110  173   4886
```

# 二、★★★f 的三個候選（★我全印，★★挑哪一個是你的欄）

```
f(敵意)             = **0.0928**（500／5386）   ⇒ ★遠低於 0.6 ⇒ 票買得到很多
f(距離≤3)           = **0.7531**（4056／5386）  ⇒ ★★**超過 0.6 ⇒ 照你的規則要【回報不做】**
f(敵意 且 距離≤3)   = **0.0841**（453／5386）   ⇒ ★遠低於 0.6
⇒ ★★★**同一個世界、同一份資料，三個定義給出【相反的裁定】**
⇒ 所以我把它交回來：**這一格不是量測問題，是定義問題。**
```

# 三、★我做了什麼來確保這個數字不是我選的

```
①**不自己定義威脅**：tap 記【定義無關的維度】（距離／敵意／首見-移動）的交叉表
   ⇒ ★你挑哪些格子加總，f 就是多少 —— ★★我先挑一組的話，f 會變成【我選的數字】
②**感知鐵律（你票的 §3）套回這個 tap 自己**：只讀
   `_ob.known_reputations`（這隊的信念）、`_ob.tile_pos`（自己的位置）、
   本次情報帶進來的 `fields["tile_pos"]`（★這隊【剛學到的】）
   ⇒ 判準句「如果這隊其實被騙了，這個分類會不會跟著錯？」⇒ **會** ✔
③★★★**沒有用既有的 `ThreatAssessment.score()`** —— 它吃 `other: TeamData` ＝ **god-view**
   （`decision_context.gd:1509` 直接 `state.teams.get(tid)`）
   ⇒ **它叫 belief-threat，但讀的是世界真值** ⇒ 拿它量 f，實作端做不出同一個數字。
   ★這一條順便是回報：**庫裡有一個名字與行為不一致的東西**，你要不要立案是你的欄。
④**守恆格**：tap 母體必須等於既有的 `t0.emit.intel_arrived` ⇒ 5386 ＝ 5386 ✔
   ⇒ ★沒有它，我可能量的是【另一個 emit 點】而數字看起來一樣合理。
⑤**熱路徑**：字串用【相接】不用 `%` —— ★同段註解寫著 `%` 的 Variant 裝箱
   曾讓 12 日窗第一次跑就撞 360s wrapper timeout。
```

# 四、★一個會改變你怎麼定義的前提

```
`belief_system.gd:285-293`：emit 的條件是 **`fields.has("tile_pos")` 且（首見該目標 or 已知位置改變）**
⇒ ★★**這 5386 次【全部】是位置情報** ——
   不存在「糧價情報」「外交情報」這種可以被歸為非威脅的別類。
⇒ ★★★所以「用內容切」在這裡**只能切【距離】與【敵意】**，切不了「情報種類」。
   ★而 WHAT 的原話是「緊迫由內容決定」——★★位置＋敵意就是這裡僅有的內容。
```

# 五、在飛

```
★12 天 × 兩顆種子**正在跑**（`godot-busy` 守衛在前）⇒ **這是【在飛】不是【已完成】**
床＋tap：`feat/pass-phase-bed`（★tap 動到 production `belief_system.gd`，純記帳、`Probe.enabled` 後）
★★而我**沒有開始實作** —— 你寫了「實作要等 WHAT 回 §8.3」，我停在第 0 步。
```
