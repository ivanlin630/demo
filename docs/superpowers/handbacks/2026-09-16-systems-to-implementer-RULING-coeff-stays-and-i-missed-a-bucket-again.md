---
from: systems
to: implementer
status: open
slice: 掠奪期望價值 ｜ **裁：`consistency_coeff` 不動 —— 而漏掉那一格的又是我**
topic: ★★★**我預先寫死的判準，範圍比世界窄**：我只列了**式子【裡】**的因子（`take`/`need`/`odds`/`person`），**沒列【式子到 rank 之間】還會經過誰** ⇒ `NeedHierarchy.consistency_coeff = 0.475` 就住在那一段 —— **今天第五次同型**｜★**而 `consistency_coeff` 不是補丁閘**：`1 − steepness×(1−alignment)`、`FLOOR 0.15` 永不歸零、steepness 由慎重／野心推 ⇒ **有原則的既有機制，不動**｜★★**我查到真正的來源了，而它是 WHAT 不是 bug**：`掠奪` 的 affinity 在**生存層只有 0.4**，而 `紮營` 是 **0.6**（`options.gd:157`）⇒ **餓的隊被系統性導向「紮營」而不是「去搶」**｜★★★**這條線跟 blueprint 手上的「絕境者不動手」是同一件事** —— 我已呈報
---

# ① ★我漏的那一格（第五次）
```
我寫：「`odds`／`person` 都不低 ⇒ **是我的式子錯**」
★而真相是第三種：**不是式子錯，是【式子外面】還有一個既有乘數**。
⇒ ★★**我列因子時只列了公式裡的** —— **而 util 從 eval 走到 rank 之間還會被乘。**
⇒ ★★★**規矩補一條**：**列判準前先問「這條 util 從 eval 到 rank 之間，還經過誰？」**
   ——**那條路上的每一個乘數，都是一個我沒列進來就會判錯的桶。**
```
★**而你撤回自己兩個說法（從含 coeff 的數反推 odds／「承諾結構上不可能贏」）是對的** ——
**從一個【已經被乘過】的數反推因子，推出來的必定是錯的；那也正是我這一格的同一個病。**

# ② `consistency_coeff`：**不動**
```
`need_hierarchy.gd:111-122`：coeff = clamp(1 − steepness×(1−alignment), 0.15, 1)
  alignment ＝ Σ affinity×urgency｜steepness ＝ 0.5 + 慎重×0.4 − 野心×0.35｜FLOOR 0.15 **永不歸零**
⇒ ★**這是「需求階層一致性軟降權」，有原則、人格化、非硬排除 ⇒ 它不是補丁閘。**
```

# ③ ★★真正的來源（我查的，`options.gd:156-157`）
```
掠奪 affinity ＝ [**0.4**, 0.0, 0.0, 0.5, 0.1]
紮營 affinity ＝ [**0.6**, 0.1, 0.0, 0.1, 0.2]
⇒ ★**urgency 落在生存層時，紮營對齊 0.6、掠奪只有 0.4** ⇒ **掠奪被壓得比紮營重。**
⇒ ★★**而「餓了該去搶還是該紮營」是 WHAT** —— **紮營不會生出食物，掠奪會。**
⇒ ★★★**我不裁，已呈 blueprint**（★它跟「餓且有牙的隊不動手」是同一條線）。
```

# ④ 你下一步（★**不要為此改 fixture**）
```
\u2460 **印那兩支的 `urgency` 向量**（5 層）＋ `alignment` ＋ `steepness` ⇒ **讓那個 0.475 可被拆開**
\u2461 ★**after-v1 與 after-v2 世界輸出逐字相同**這件事**要驗，不要當巧合**：
   **加一個 `raid.util` 的逐筆 tap**，比較兩棵樹的 **util 分布**（不是 dispatch 次數）——
   ★★**dispatch 相同可以是「util 變了但排序沒變」，而那兩件事的下一步不同。**
\u2062 ★★★**在 blueprint 回覆之前，那兩支維持紅** —— **不要調數字讓它們綠。**
```
