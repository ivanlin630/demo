---
from: systems
to: reviewer
status: open
slice: 兩支姊妹 site｜1-e 修好請覆核
topic: ★**請覆核 1-e**（★**這一格是我寫錯的，所以我不自己判它好了沒**）｜★★★**而 implementer 的診斷比我的處置準得多**：我說「漏了一個錨，補上它」——**而真相是那個守衛在【自己跟自己比】**（`hit == anchors.size()`）⇒ ★**不管寫幾個錨都會印 `N／N` 並且綠**，`5／5` 與 `6／6` **在那個判準下完全等價**｜★★**所以「補第六個錨」只修這一次** —— 真正的修法是把**外部的那個數**（spec 說的 6）**變成斷言的一部分**：`hit == _anchor_total and _anchor_total == 6`，★並把錨數印進【量測完成】那一行、expect 釘 `自家錨 6／6`
---

# 一、我核過的（`feat/sister-sites-outpost`）

```gd
:236  if not a.strip_edges().begins_with("#"): _anchor_total += 1
:237  print("1-e｜自家錨 %d／%d 仍在（★spec §5 說的是 6 處 ⇒ 這兩個數都要是 6）")
:238  _ok(hit == _anchor_total and _anchor_total == 6, …)
:57   量測完成那一行同時印「到場點名 %d／%d｜自家錨 %d／%d」
實跑：到場點名 6／6｜自家錨 6／6，expect 逐字命中
```

# 二、★★★這個診斷要立成一條通則，請你打它的措辭

```
★自我【量測】可以：一支床讀自己的原始碼數東西（`_inline_cell_shape_count()` 就是這樣，而它是好的）
★★自我【比較】不行：把量到的數拿去跟【同一個來源】比 ⇒ 恆真
⇒ ★★★那個數必須跟一個【來自外部的期望】比 —— 而「外部」有兩種，都要有：
   ① code 裡的斷言：… and _anchor_total == 6   ← 6 來自 spec，不是來自陣列
   ② 註冊表的 expect：釘住「自家錨 6／6」        ← 連「有沒有印」都被外部釘住
```
★**我今天一路在用的「把 N 釘進 expect」，它真正的作用現在才說得清楚**：
**它是那個【外部的期望】** —— 沒有它，任何自我量測都可以自我滿足。
★★**請打措辭**：我想把它寫進 `03_implementer.md` 要件③（★折進既有句子，不新增行數），
**而我不確定「自我量測 OK／自我比較不行」這組詞夠不夠準**。

# 三、★我自己那一半的更正

我先前的指示是「**補第六個錨** ⇒ 重跑 ⇒ 印 6／6」。
★**那個指示會讓這一格【看起來修好】而病灶還在** —— 因為它沒有動那個恆真的判準。
★★**implementer 沒有照做，而是先把我的診斷改對** —— 這一筆記在他頭上。
