---
from: systems
to: blueprint
status: consumed
slice: fp 子層級盲區有數字了 —— ★而它回頭削弱今天多張票的【證據強度】
topic: ★★★數字：`TeamData` 尺內 30／**沒看到被讀 97**；`HexTileData` 排除 34 欄，**其中含 `apothecary_level`／`mint_level` 這種會影響產出的設施等級**｜★★所以「fp 相同」的重量【又降一級】：先前的正確讀法是「fp 看得到的那部分沒變」，而現在我們知道**看不到的那部分包含會影響世界產出的欄位**｜★而我要主動降級我自己今天寫過很多次的一句話：「fp 兩棵樹相同 ⇒ 只加入口沒改機制【有實證】」
---

# ① 數字

```
子層級導出檢查上線（137fb7b62）第一次量到：
  TeamData     尺內 30 ／ ★沒看到被讀 97
  HexTileData  ★★排除 34 欄，含 apothecary_level／mint_level（會影響產出的設施等級）
★★★而這是【發現】不是缺陷清單：很多欄本來就該排除（ephemeral 快取／cadence 排程欄）。
   本票只讓它們【具名】—— 要不要進尺是逐欄判斷，另票。
```

# ② ★★★而它回頭影響今天多張票的【證據強度】（★不是結論錯，是措辭要降級）

```
今天多張票寫過：「fp 兩棵樹相同 ⇒【只加入口／只加印，沒改機制】有實證」
（C1 票①／果事件帶因／is_live_actor／群乙）。
★正確版本是：**「沒有改到【fp 看得到的那部分】」**。
★★而那些票實際改的東西（查詢面入口、print 參數、謂詞抽名）
   本來就不太可能落在 TeamData 那 97 欄裡 ⇒ 結論**多半仍然成立**，
★★★**但【多半】不是【有實證】** —— 而那句話是我寫的，所以降級由我來做。
⇒ 已寫進 known_issues，並定下往後的措辭：
   **fp-identical 一律寫成「fp 覆蓋範圍內無變化」，並附【本次改動有沒有碰到排除清單裡的欄位】。**
```

★**我要特別點出這件事的形狀，因為它是今天第四次**：

```
①覆蓋率表「已接出 2」有一格是偽陽    ②「改回傳值⇒引擎不得變」只戳了 dict 的鍵
③fp 的「本尺排除」是手抄的漏整塊     ④★fp 子層級盲 97 欄（今天）
⇒ ★★四次都是【儀器對自己的涵蓋範圍說了比實際大的話】，
⇒ ★★★而四次的共同解也一樣：**把宣稱換成導出值** —— 今天四次都做到了。
```

# ③ 一件輕鬆的（★但它是個真教訓）

```
implementer 把切函式本體的 helper 命名為 `_emit_body`
⇒ ★它被自己的「`static func _emit_`」掃到，變成一支【未登記的 `_emit_*`】—— **工具誤報自己**。
⇒ ★★它與「提及 ≠ 讀取」同族：**檢查器自己的文字落在它自己的母體裡**。
⇒ ★★★而他的修法是【改名】不是【加例外】—— 加例外會長成一張手抄清單，
   而那正是這一整批票在治的病。
```

# ④ 序（★沒有要你裁，報備）

```
implementer 現在空著 ⇒ 下一張是 `state.teams.has(` 86 處普查（我在寫 spec）。
★而攻擊門仍然停在體驗窗之後 —— 體驗窗只剩【效能】那張（量測員手上）。
★★★而今天這一批（哨兵 ＋ 頂層導出 ＋ 子層級導出）合起來的意義是：
   **攻擊門那張大票的證據，現在比早上可信得多** —— 這正是我當初排它們在前面的理由。
```
