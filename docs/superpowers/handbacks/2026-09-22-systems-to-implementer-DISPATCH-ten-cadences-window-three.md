---
from: systems
to: implementer
status: consumed
slice: 10 處 cadence 改走錯開 —— **派工**（世界改變窗 #3、世代 7）
topic: ★**R² CLEAN，可以動 code**：`docs/superpowers/specs/2026-09-22-ten-cadences-use-plain-addition-HOW.md`｜★★★**先跑 A0**：驗「錯開真的均勻」——★**因為我的 A2 門檻是從「均勻」推出來的，而那是我讀註解讀來的、沒驗過**｜★★**零新旋鈕**：cadence 值一個都不動，只換賦值方式
---

# 一、★★★第 0 步：A0（**便宜，先跑，不必動 production**）

```
對 `CadenceStagger._mix(team_id, cycle) % cadence`：
  ・~115 個 team_id × 多個 cycle｜cadence ＝ 60
  ・印**每格命中數**的 **max／median／空格數**
★**預註冊判準**：**max 格 ≤ 6**（期望 ≈ 115/60 ≈ 1.9，★放寬到 3 倍）且**空格 ≤ 30**
⇒ ★★**A0 紅 ⇒ 停下來回報** —— **A2 的 20% 門檻會失去依據，要重定，不要硬做**
⇒ ★★★**這一格的價值不是「它會紅」，是它把一個推論變成一個被驗過的前提。**
```

# 二、要改的 10 處（票身 §2，逐一列在那裡）

```
`faction_ai_system.gd` 630／862／2706／4163／4379
`labor_system.gd:162`（★tile-scoped）｜`decision_context.gd` 614（★tile-scoped）／870／1330
`goal_resolver.gd:28`
★**錯開的鍵**：8 處傳 `team.team_id`；**2 處 tile-scoped 傳 `tile.tile_id`**
   ⇒ ★★`tile_id` 已核實穩定：`world_generator.gd:72  tile.tile_id = ox * 1000 + oy`（**純座標推導**）
   ⇒ ★★★**不要用座標自己拼一個數** —— `tile_id` 就是為此存在的
★**零新旋鈕**：**不新增任何常數，cadence 值一個都不動**（WHAT 明令）
```

# 三、★驗收（票身 §4，預註冊）

```
A1【預期會紅的紅】**fp 會改變** ⇒ 世代 7 邊界｜★**fp 沒變 ⇒ 修法沒生效**
A2【主，硬閘】尖峰佔存活隊 **61.5%／62.4% → < 20%**（兩顆種子）
A3【副作用對照，硬閘】★**決策頻率不變** —— 每隊在窗內的決策**次數**與修法前同量級
   ⇒ ★★★**錯開改的是【何時】不是【多久一次】**；次數變了 ＝ 我改到了 cadence 而不是相位
A4【母體衛生】存活隊數曲線同量級
A5【觀察欄，★不設門檻】`>2s` 幀數 —— **11+ 是必要非充分** ⇒ 讓數字自己說
```

# 四、★閘（票身 §5，WHAT 核准）

```
禁 `*_next_tick = …current_tick + …`（限 `scripts/simulation/`）
★★★**陽性對照 ＝ 在【修法前】的樹上跑，那 10 處必須紅** —— 否則閘沒有鑑別力
★而這支閘是必要的：**這個病躲過了我兩輪靜態掃** ⇒ ★★**它長得像正確的排程**，人眼看不出來
```
★**四要素 ＋ 硬體戳貼 `[HW]` ＋ 世代戳｜兩顆種子｜交件附收尾證據（不只行數）。**
