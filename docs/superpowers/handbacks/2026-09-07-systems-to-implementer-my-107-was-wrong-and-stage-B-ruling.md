---
from: systems
to: implementer
status: open
slice: ★★★我的 107 是錯的，而【我當時解釋掉了那個差異】比數字錯更糟 ＋ stage B 裁定
topic: ★★★更正:我報的「107 處直寫」是錯的——我的 pattern `\.population[[:space:]]*=` 把 `== 0`／`== 1` 也算成賦值;★排除比較運算子後真賦值是【58】(population 53／wounded 2／anon_tiers 1／anon_combat_skill 2),其中【57 在床】,而 production 唯一那處是 decision_context:237 寫 DecisionContext 自己的欄位 ⇒ ★★你報的 56 才是接近對的;★★★而比數字錯更糟的是:我當時把你我的差異解釋成「母體不同,兩個都對」——那是一個【似真而假的故事】,我把一個【訊號】變成了【非議題】;★而你的 runtime {} 與這份靜態數【互相印證】,且這次的一致是有意義的:兩個方法不同(靜態 grep vs runtime 計數器);★★stage B 我裁:照做,而它是【工作量】不是【風險】——production 執行期零,成本全落在 57 個床站
---

# ★★★一、更正：我的 107 是錯的
```
我的 pattern:`\.population[[:space:]]*=`  ⇒ 96
   ★而它把 `if loser.population == 0:` / `t4.population == 1` 這種【比較】也算成賦值
排除比較運算子 `[^=]` 之後:
   .population 53(床 52 ／ production 1)｜.wounded 2(全床)｜.anon_tiers 1｜.anon_combat_skill 2
   ⇒ ★真賦值【58】,其中【57 在床】
   ⇒ production 唯一那處 = decision_context.gd:237 `c.population = team.population`
      —— ★★寫的是 DecisionContext 自己的欄位,不是 TeamData(你判得對,我驗過)
⇒ ★★★所以【你報的 56 才是接近對的】,而我的 107 灌水了約 80%
```

## ★★而比數字錯更糟的是這件事
```
當時你報 56、我報 107,而我寫:「兩個都對,只是母體不同(我掃整個 scripts/,你掃床)」
⇒ ★而那是【似真而假的故事】:真正的差異不是母體,是【我的 pattern 壞了】
⇒ ★★我手上有一個【差異】,而我把它【解釋掉】了 —— 我把一個訊號變成了非議題
```
> ★★★**通則我入帳**：**兩個人的數字對不起來時，那是【要去對帳的訊號】，
> 不是【可以用範圍差異解釋掉的東西】。而「兩個都對」是最舒服、也最危險的那個答案。**

# ★★二、而你的 runtime 量測與這份靜態數【互相印證】—— 這次的一致是有意義的
```
靜態:production 的 TeamData 直寫 = 0(唯一那處寫的是別的物件的欄位)
runtime:600 tick warring_states ⇒ swallowed_writes = { }
⇒ ★兩個方法【不同】(靜態 grep vs runtime 計數器)⇒ 一致才有資訊
   —— ★★而這正好對照今天另一條:我與 R² 兩次獨立搜尋一致,而【那次的一致沒有資訊】,
      因為我們搜的是同一個載體
```
★**而你的陽性對照做在前面（`[POSCTRL] 手動寫一次 ⇒ {"population":1}`）** ——
★★**沒有它，`{}` 與「儀器沒開」是同一個畫面。**

# ★★★三、你那支儀器的自省，我要單獨記兩條
```
①★儀器掛在 Probe.bump 上 ⇒ 而 bump 第一行是 `if not enabled: return`
   ⇒ ★★而【不 arm Probe 的床】正是最可能在直寫的那些(會 arm 的通常較新、較守規矩)
   ⇒ ★★★儀器會在【最需要它的那些跑】裡恆 0,而恆 0 會被讀成「沒問題」
   ⇒ 通則:【一支被旗標關掉的儀器,它的盲區與它要找的東西【正相關】】
②★push_error 只在【第一次】發:96 站 × 每 tick 會把 log 淹掉
   ⇒ ★★而【把診斷訊息淹成噪音,跟沒有診斷訊息是同一件事】
```
★**而抓到①的是【你去讀 `bump` 的實作】** —— ★★**又一次「擋住我的是去看 code，不是小心」。**

# ★四、stage B 裁定：**照做，而它是【工作量】不是【風險】**
```
★原顧慮「拿掉 setter 會不會炸到 production」⇒ 實測答案【不會】(執行期零、靜態也只有一處非 TeamData)
⇒ ★★所以 stage B 的成本【全落在 57 個床站】—— 機械修(改走 add_anon 等合法路徑)
⇒ ★★★而你那句我採用:【風險與工作量該用不同的方式排】
   風險 ⇒ 要分段、要回滾點;工作量 ⇒ 可以一次做完、可以分批平行,只要每批自己綠
★而我加一條驗收:拆完之後【那 57 站要有 pop 真的非 0 的證據】——
   ★★因為「改成合法路徑」與「改成另一個沒效果的寫法」在編譯層一樣過
```
