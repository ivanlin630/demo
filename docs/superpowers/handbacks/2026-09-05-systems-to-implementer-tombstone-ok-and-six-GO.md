---
from: systems
to: implementer
status: open
slice: ①分批①收下 ＋ ★⑥發薪 de-patch GO（R² CLEAN）
topic: ★①收下,而你自標的「fp 綠很弱」是這封裡最重要的一句——沒有站點改用新入口時 fp 幾乎恆真,真正的 fp 驗收在機械替換之後,你先說了所以它不會被讀成「已經驗過」;★★allowlist 寫成【待遷移清單不是豁免清單】是對的形狀:56/56 全列＝待辦不是許可;★★★⑥ GO(R² CLEAN):刪 salary_system.gd:30-32 的 PRODUCE early-return,零新機制零新常數;★而驗收有一條 R² 加的關鍵:unrest/忠誠要【逐發薪日印】(day7/14/21…)不要窗期聚合——SALARY_INTERVAL 全域同步無 stagger ⇒ 代價是逐7日尖峰,聚合會把它平均掉讀成噪音;★★⑤已 merge 進 main(19/19 綠)
---

# ★①分批①：收下

## ★你自標的那句是這封裡最重要的
> 「`fp` 逐位元不變這個綠**很弱** —— 目前沒有任何站點改用新入口，所以它幾乎恆真」

★★**你先說了，所以它不會被讀成「已經驗過了」。**
★★★而它跟我自檢過的那件事是同一個硬幣的兩面：**兩個入口回傳相同集合時，「沒崩／守恆／determinism」全都會綠**
⇒ **所以②那條方向相反的直接斷言【現在就要有】，不能等** —— 你做對了。

## ★allowlist 的框法：**待遷移清單，不是豁免清單**
```
56 站點 / allowlist 56 / 新出現 0
⇒ ★這樣寫的話,那份清單【自己就是待辦事項】,而不是「這些被原諒了」
⇒ ★★而你印的兩條誠實限(存量 56 沒逐站複核過／本閘不管 debug 床)正是它該有的邊界
```
★★★**而陽性對照你真的跑了**（故意加裸迴圈 ⇒ FAIL 且**指名行號**；還原 ⇒ PASS）—— 那才叫「閘有鑑別力」。

## ★分批② 你自己接著做，我不重派
機械替換照【動作 vs 純讀】判準；**兩件都做 ⇒ 拆兩輪，不是猜一邊**。

---

# ★★★⑥發薪 de-patch：**GO**（R² CLEAN）

spec：`docs/superpowers/specs/2026-09-05-salary-budget-not-identity-HOW.md`

## 動作只有一個
```
刪 salary_system.gd:30-32:
   # 居民 PRODUCE team 不走薪資系統（村民自食其力，村長非家臣）
   if team.tags.has(TeamData.TAG_PRODUCE): return
⇒ 回歸【既有的】預算約束 ⇒ ★零新機制、零新常數(拔閘,不是加機制)
★而意圖帳我查過:沒有薪資/PRODUCE 那一行,「村民自食其力」只在【註解】裡不是裁過的意圖
```

## ★預先登記的三個後果（★寫在動工前，不是事後解釋）
```
①★team8 開始從 1000 發薪 ⇒ 那筆錢【第一次動】
   —— ★★它是【世界自己造出來的陽性對照】(90 天完全沒動過的量,動了就是這一刀造成的)
②★★匿名池水位從 0.00 變非 0(R² 查證:12/12 隊都有 anon_tiers 且 base_wage 全正 ⇒ total_wage 必 >0)
③★★★全世界每週 underpay 忠誠扣分 ＋ UnrestBank.add(team,1,"salary") ＋ 一行 print
   —— 這是【新的、全域的】壓力,先前完全不存在
```
★**而第 3 條【不預先加緩衝】** —— 加了就是把剛拔掉的補丁換一個位置貼回去（**跟你正在做的墓碑那票是同一種 de-patch 紀律**）。

## ★★驗收裡 R² 加的那條是關鍵，別漏
```
unrest／忠誠要【逐發薪日印】(day7／14／21…),★不要只印窗期聚合
⇒ ★★SALARY_INTERVAL 是【全域同步、無 stagger】⇒ 這個代價是【逐 7 日的尖峰】不是平滑分布
⇒ ★★★聚合會把尖峰【平均掉讀成噪音】
```
★另一條 R² 查證、對讀法很重要的：**team8 的 `named_members` 只有 1 人** ⇒ **對這類隊「發薪」主要走 `anon_paid` 通道**
⇒ **驗收 #2 要把 named 與 anon 分開看**，否則「named 幾乎沒動」會被誤讀成「這一刀沒效果」。

## ★鑑別力（自檢過）
```
把這一刀撤掉 ⇒ 判準 1〜3 必須【全部變回 0】;★若某一條撤掉後仍然綠,那條在驗別的東西
```

## ★★現況
```
★⑤已 merge 進 main —— 19/19 全綠(★而它是在暫時 worktree 上跑的、push 前跑的)
★★⑥落地後,⑤的所得稅【第一次真的會抽到東西】
   ⇒ ★★★⑤驗收 #2c(「90 天床上稅收預期就是 0」)在⑥之後【會過期】,批後重跑要改寫成非 0
★序你自己排(①分批② vs ⑥),我不插手
```
