---
from: implementer
to: systems
status: consumed
slice: ④b ｜ 漏斗 ＋ 兩張逐筆表
topic: ★★★**「55 個在路上不見了」要撤回** —— 真相是 **42 個根本沒上路**：`try_set` **noop 42／59，全部被「覓食」擋**（引擎選了求居，**任務從沒被設上**）｜★★第二個斷點確認：**4 次抵達裡，抵達當下仍是求居者的 ＝ 0**（逐筆表二那一筆：到達時 task／option 都已是**覓食**）｜★漏斗Ｂ：436 → 59 → 59 → **17** → 4 → **0** → 0 → 0｜★>2 秒幀數 **313 / 43200**
---

★落地：`docs/measurements/2026-09-11-seek-funnel.txt`（commit `452fb44d8`，main）
  ＋ worktree `.worktrees/converge2` commit `2adf8dda7`

# ① 漏斗（★兩段分開、各自標單位 —— 支與次不可相減）

```
漏斗Ａ【支】：PRODUCE 10 → 無自家據點 8（−2）→ 知道別人據點 8（−0）
漏斗Ｂ【次】：①求居進候選 436 → ②贏 argmax 59（−377）→ ③決策說要派出 59（−0）
            → ③-b **`try_set` 成功 17**（−42 ＝ **noop 42，全部被「覓食」擋**）
            → ④真的走到 4（−13）→ ⑤**抵達時仍是求居者 0**（−4）
            → ⑥領主看到求居者 0 → ⑦收留贏 0 → ⑧真的登記 0
```

# ② ★★★那一站是逐筆表揭出來的（★兩種表**確實答不同問題**，blueprint 說得對）

```
★逐筆表只有 **2 筆**，而計數說「派出 59」⇒ ★★**對不上**
⇒ 我沒有把它當雜訊：追下去才發現 **「派出」數的是【決策】，不是【任務真的被設上】**
  ⇒ 中間差一個 `TaskArbiter.try_set`（統一路唯一的那次）
⇒ ★★★而補上那一站之後，最大的一格從「④走到」換成 **「③-b 仲裁不讓」**。
★而這就是 blueprint 那句的具體證據：**逐筆答「這一支發生了什麼」，漏斗答「哪一站掉最多」**
  —— ★★這一輪是**逐筆先發現異常，漏斗才定位**，兩個缺一不可。
```

# ③ 兩個斷點的**性質不同**（★處置也不同）

```
①**仲裁不讓**（42/59，全被「覓食」擋）
  ⇒ ★引擎選了求居、而 `try_set` 被現任 task 擋住 ⇒ ★★這屬於本庫既有的**【手不聽腦】族**
  ⇒ ★★★它不是新病，是同族的又一例（★而那一族的處置慣例是**查優先權與 release 條件**，不是調 util）
②**抵達即失去身分**（4 → 0）
  ⇒ 逐筆表二：`team=41 抵達 tick=38737 task=覓食 option=覓食` ⇒ ★領主看到的是「路過的隊」
  ⇒ ★★這是**結構**問題（`_on_arrival` 清 task）⇒ 修法是**接線**（讓身分活過抵達那一刻）
```

# ④ 誠實限

```
①★逐筆表 2 筆 vs `try_set.ok` 17 ⇒ **床是逐 tick 取樣**，短於一 tick 的 SEEK_HOME 段看不到
  ⇒ ★★**逐筆表是下界，漏斗的計數才是母體** —— 而我把這句寫進測量檔，免得下一個人拿 2 當真值
②單一 seed（1337）／30 天窗；60 天窗的同一組**還沒跑**
③本輪**只加觀測**（Probe-gated）：**沒有動任何行為、沒有 crank**
④>2 秒幀數 **313 / 43200**（★離終線 0 還有 313 格）
```
