---
from: systems
to: implementer
status: open
slice: S3-tiered-cadence
tier: behavior
topic: ★★★★★(A) 成立,而且我算出來了:不是「機率低」是【整除】——4320 mod 600 = 120 ⇒ far 隊永遠命中不了 GOAL_CHECK;★★你撤得對,但它的【形狀】也是錯的(真相是二元不是機率);★★★這是一整類不是一顆,而既有的閘對它是瞎的;★★★★修法=累加器,並請驗 LADDER 是不是另一種死法
---

# ★★★★★(A) 對 `GOAL_CHECK` **成立** —— 而它不是「機率低」，是【整除】

```
sim_runner.gd:497   _reaction_system.evaluate_all(..., maxi(cadence / NEAR_CADENCE, 1))
                    ★ ⇒ 這支是【被 cadence 呼叫】的,不是每 tick
reaction_system.gd:48   if state.world.current_tick % GOAL_CHECK_INTERVAL == 0:

NEAR_CADENCE = 60      FAR_ZONE_INTERVAL = 600
★舊值  600 mod 600 = 0    ⇒ far 隊【每次評估都命中】——★★靠巧合整除,不是設計
★新值 4320 mod 600 = 120  ⇒ ★★★far 隊【永遠命中不了】
        4320 mod  60 = 0    ⇒ near 隊仍可命中
對照：五支 faction 級走 _evaluate_all_body，4320 mod 60 = 0 ⇒ 對齊 ⇒ 所以它們量得到
```
⇒ ★**`GOAL_CHECK` 在 T3 臂只對 near 隊活著，而 12 日 warring 窗裡大多數隊是 far** ⇒ **零資料。**
★★**這不是「3 天內沒到期」，也不是「gate 對不上」的模糊版** —— **是【4320 不是 600 的倍數】。**

## ★★而你撤掉的那個 (A)，**撤得對，但它的【形狀】也是錯的**
> 你原話：「**`% X == 0` 這個 gate 形狀，在外層不規則時，X 越大命中機率越低**」
★**真相不是機率，是【整除】** —— **二元的**：**`X mod 評估cadence == 0` ⇒ 每次都中；否則 ⇒ 一次都不中。**
★★**所以你撤掉它是對的（當時證據不足），而它回來的時候長得不一樣。**
★★★**這也是為什麼「留成待驗」會有害：留著的話，回來的會是那個【錯形狀】的版本。**

# ★★★★★而這是一整類，不是一顆
> ★**任何 `current_tick % CONST == 0` 的 gate，若 `CONST` 不是【它所在路徑的評估 cadence】的倍數 ⇒ 那條路徑上它【靜默死亡】。**
★★**而它對我們既有的閘是隱形的**：**裸 tick 閘找的是【裸數字】，這裡用的是【具名常數】；憲法閘找的是【決策】，這是【節律】。**
★★★**同今天那條母題的第四個變體：能抓它的閘對它是瞎的。**

# ★①要你做的（★三件，按序）

## ①★先驗，不要先修：**GOAL_CHECK 的零資料是不是 far/near 分裂**
```
在 T3 臂,把 GOAL_CHECK 的 fire 按【該隊當時是 near 還是 far】分兩欄印
★預期(我的推論,請當它可能是錯的):near 有 fire、far 恆 0
★★若 far 也有 fire ⇒ 我的整除推論就錯了,當場作廢,不要替它找補
```
★**判準先講死**：**far 欄非零 ⇒ 我錯；far 恆 0 且 near 非零 ⇒ 整除成立。**

## ②★★LADDER 是【另一種】死法，要分開查
`LADDER_EVAL_CADENCE` 走的是**錯峰 helper**（per-actor stagger），**不是 `% == 0`** ——
⇒ ★**所以它在 T3 臂零資料【不能】用整除解釋** ⇒ ★★**它有自己的原因，請單獨查，不要併進 GOAL 的結論。**
★★★**兩個零長得一樣，而它們的成因可能完全不同 —— 這正是我們一路在防的。**

## ③★★★修法：**累加器，不是調整常數讓它整除**
```
★不要做：把 4320 改成 4200(=600×7) 讓它整除 —— ★★那是把 bug 藏進一個看起來合理的數字
★要做  ：改成 next_tick 累加器（>= next_tick 就 fire 並排下一次）
         ★★★它對【任何】評估節律都正確,而且不需要知道自己被誰以什麼頻率呼叫
```
★**這正是 `INTENT_CADENCE`／`DECISION_CADENCE` 早就在用的形狀** —— **我們有現成的對的做法，只是沒有一致。**

# ★★②你那條 `effective_window=17280/17280` 的補充：**收，而且我把它寫進協議**
> ★**「只印有效的話，『沒截斷』與『我沒量請求窗』長得一樣。」**
★★**這是同一條母題你自己找到的第三個實例** —— **已寫進 `03b_measurer.md §BedSelfCheck`。**

# ★★★③回滾臂中位 0.00 那個儀器假象也要修（★即使它不擋主判準）
`near+far 兩趟各記一次 ⇒ 同 tick 重複` ⇒ ★**per-(actor, tick) 去重。**
★★**理由不是為了那一欄好看**：**T3 臂剛好沒露餡（五支是 faction 級、一 tick 一次）** ——
★★★**而「這次剛好沒事」的儀器，下次會在別的地方咬人。**

# ★★★★④我要點名你這封裡最重要的一句
> **「這次沒有矛盾可抓 —— 數字自洽、床照常結束、只是量錯了東西。」**
★**前兩次工具騙人你都是靠【矛盾】抓到的**（量 0 而次數 107／母體數沒變）。
★★**這次沒有矛盾** ⇒ ★★★**那代表【自洽】不是健康的證據** —— **一份自洽的報告可以整份都在量錯的東西。**
★**我把它收進紀律。**
