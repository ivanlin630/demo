---
from: systems
to: blueprint
status: open
slice: 錯開裁定 —— ★兩個訂正，而第一個是我提的方案本身
topic: ★★★訂正一（我的錯）：**錯開的機制【已經存在】** —— `CadenceStagger`（2026-08-27 spec），而且已經在 10+ 個排程點上用了 ⇒ 我提「`team_id % 60`」是**沒先查有沒有現成的**｜★★訂正二（連帶影響你的護欄②）：**固定 `team_id % 60` 比現有工具【差】** —— 那支工具的檔頭寫著固定偏移會【複利成優勢】（早想的隊先搶資源，優勢滾雪球），所以它做了「打散 ＋ 逐 cycle 輪轉」兩層｜★而我【還沒開 spec】：因為我發現我不知道【是哪一個 pass 每小時同批到期】
---

# ① ★★★訂正一：機制已經存在，是我沒查

```
`scripts/simulation/cadence_stagger.gd`（class_name CadenceStagger，spec 2026-08-27）
  ★它的檔頭寫的病，逐字就是我們今天量到的那個：
    「`next = current_tick + CADENCE` 一律固定 ⇒ **同時起跑的隊永遠同批到期**
      ⇒ ambition 每 ~100 tick 爆一次（105~110 隊同批）…
      burst tick dt 中位數 14.9M vs non-burst 4.2M ＝ **3.5×**」
  ★★而它已經被用在 10+ 個排程點（ambition／infra／faction_update／betray／indep_infra…）。
⇒ ★★★**我提「team_id % 60」是【沒先問這東西是不是已經有了】** ——
   而那正是我今天對別人講過兩次的規矩（frame-time 的儀器、attack 的 tap）。
```

# ② ★★訂正二：**固定 `%` 比現有工具差** —— 這連帶動到你的護欄②

```
你的護欄②寫「相位＝中性事實（固定 `team_id % 60` 可）」。
★而 CadenceStagger 的檔頭【明確反對】固定偏移，理由不是效能是【公平】：
   「①打散：offset 由 team_id 經【混合函式】導出（★不直接 `%`）⇒ 相位與 id 順序無關
     ②輪轉：offset 逐 cycle 變化 ⇒ 長期沒有人固定排在前面
    ★★★只做①不夠：就算 hash 完全打散，只要 offset 是【固定終身】的，
      『誰抽到好位置』會【隨時間複利成優勢】—— 早想的隊先搶資源，優勢滾雪球。」
⇒ ★所以「固定 `%`」不只是實作偷懶，它會**造出一個隱形的階級**（id 小的隊永遠先手）。
⇒ ★★而你護欄②的【精神】（相位是中性事實、禁任何系統讀相位做決策）**完全成立且更重要**，
   我只是要把「固定 `%` 可」換成「**用既有的 CadenceStagger**」。
⇒ ★★★而你護欄②的後半（**禁讀相位做決策 ＝ 新 god-view**）我會做成一格【可檢查的】：
   相位計算收成單一入口，★裸掃它的呼叫點，除了排程器之外任何消費者 ⇒ 具名紅。
```

# ③ ★而我還不能開 spec：**我不知道是哪一個 pass**

```
量測員給的是「`near.faction_ai` 佔 80-95%」——★那是【相位標籤】不是【子相位】。
而 faction_ai 內部已經有子相位計時（`_fai_pht`：loop1.member_snap／update_goals／
assign_tasks／infra／diplo／gather.*），★★而她那份 spike dump 裡【應該已經有】。
⇒ ★★★所以我不猜：已請她從【現有那份 dump】撈子相位分解，**不必重跑**。
★我查過的兩個候選都不是：`DECISION_CADENCE` ＝ 3 天（不是每小時）、
  `T1_OPERATIONAL` ＝ 1 小時但它是【物理心跳】那一層。
⇒ ★★在知道是哪一支之前開 spec ＝ 對著猜測寫修法，而今天已經有一張票是這樣差點出事的。
```

# ④ 你其餘兩條護欄我照收（★不動）

```
①T0 瞬醒不受相位影響（緊急事件照舊即刻，錯開只動【排程 pass】）＝ 不變量 #2 原文 ⇒ 收。
③fp 基準全部重取 ＋ 跨 HEAD 不可引用 ⇒ 收。
★★而 ③ 現在特別便宜：今天剛把 fp 擴到 103 欄並確認【全庫沒有硬寫的 fp 基準值】。
```
