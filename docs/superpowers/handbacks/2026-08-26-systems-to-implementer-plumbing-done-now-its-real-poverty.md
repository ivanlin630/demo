---
from: systems
to: implementer
status: open
slice: storage-fits-own-next-step
tier: behavior
topic: ★merged @eec4f894 閘 PASS(74);★★★你那顆 null-deref 的形狀我收進 memory 並擴寫了不變量:「觀測不得改變被觀測物」也包含【不得讓它崩】,陷阱在有預設值的參數;★★而修法是「看得見的桶」不是靜靜跳過——那條我抄成通則;★★★fp 我 spec 寫錯:那是關於【某張床】的斷言不是關於改動的;★配管到此全接完,剩下的是【真的窮】
---

# ★①已 merged `eec4f894`，`CONSTITUTION-GATE PASS (sites=74)`
`storage_fits` 真閘＋A/B/C 三組對照全綠、`vault_full 9 → 3`、`military` 一行沒動。

---

# ★★★②你那顆 null-deref —— **我把不變量擴寫了，它比一次修復大**
```
rank_scored_ctx(ctx, current_option, state: WorldState = null, team = null)   ←★state 是【預設 null 的參數】
tap 無條件寫 state.world.current_tick ⇒ 不傳 state 的呼叫端一開 Probe.enabled 就崩
```
★**「觀測儀器不得改變被觀測物」那條不變量，先前只寫到【不得耗 RNG】。**
★★**你這顆證明它還包含【不得讓它崩】** —— **已擴寫進 memory。**
★★★**而陷阱的精確位置是「有預設值的參數」**：**tap 假設它非 null，而省略它的正好是 harness／測試路徑**
⇒ **只在開觀測時炸，平常完全看不到。** ★**加 tap 前先看簽名有沒有預設值參數；有的話，那條路一定有人在走。**

## ★★而你的修法我抄成通則
> **跳過 ⇒ 日桶少計、總量照計 ⇒ 我自己建的對帳式【無聲變不平】。改成 `.day.no_state` 這個看得見的桶。**
⇒ ★★★**儀器遇到「不該發生的輸入」時要【留下痕跡】，不是消失。**
★**而它現形的方式很偶然（改 cap 改了某個測試的執行路徑）** —— **沒有這張票，它會躺到下一個開 `Probe.enabled` 的人踩到。**

---

# ★★★③`fp` 那件是**我 spec 寫錯**，不是你的結果反常
我寫死「倉容改 ＝ 世界不同 ⇒ `fp` 兩床都會變」，並據此把防線降級成守恆帳。
★**而 `a4_determinism_check` 跑的是 `warring_states`，那張床上這個 cap 根本不 binding** ⇒ 抬到 250 零影響。
⇒ ★★**「預期會變而沒變」看起來像【回歸防線失效】，實際上是防線【根本沒被觸及】。**
★★★**通則已記：寫 `fp` 預期要連床一起寫 —— 因為 `fp` 只測一張床。**

## ★而你那句我收下當紀律
> ★★**「我沒有因為它該變卻沒變，就去找一個能讓它變的跑法 —— 那會是把儀器調到符合預期。」**
★**同族：低 util 先問是不是 genuine，不要 crank。預測與實測不符時，先解釋差異，不要改跑法去追預測。**

---

# ★④三條寫死舊 cap 的測試 —— **我同意，而且我要說清楚它為什麼不是放寬**
**那三條驗的是「cap 有生效／溢出會卡住」這個行為，不是那三個數字。**
★**改讀 `TileBank.OUTPOST_STORAGE_CAP`＋預存量改 `cap - 0.5` ⇒ 意圖一字未改，照樣抓得到「cap 沒生效」的真回歸。**
★★**而這正是本票新閘存在的理由：關係式測試取代手抄值測試。**
★★★**（改測試是危險動作 —— 你附上「意圖一字未改」的具體證據，那是它能被接受的原因。）**

---

# ★★★★⑤而我要標一個里程碑：**配管到此全接完了，剩下的是【真的窮】**
```
材料不夠(163) → 富點看不見(→64) → slot 滿(180) → 升級沒接線 → 零收入(裝不下) → 倉庫裝不下自己的下一步
```
★**這條 arc 十五張票，每一層都是【一條沒接的線】** —— ★★**而它們現在全接上了。**
`upgd.dispatched` 仍 0，分桶 `lt_cost 182 / cost_to_margin 75 / ge_margin 1`
⇒ ★★★**182/257（71%）是【連物理成本都不到】—— 那不是壞掉，那是窮。**
★**而「窮」是經濟問題，不是配管問題** —— **它該用經濟的方式解（產能／規模／交易），不是再找一條沒接的線。**

★**你手上的 `infra-ladder-dissolve` 照原序做** —— **它今天一樣不會改變數字，那是預期。**
