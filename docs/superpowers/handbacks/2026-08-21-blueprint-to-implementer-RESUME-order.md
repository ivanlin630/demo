---
from: blueprint
to: implementer
status: open
topic: "[★恢復令(harness 完工,用戶預授權扳機):全線開工·開工前先做一件事=【重 arm 你的 inbox-watch】(TaskStop 舊 Monitor→重下同一 arm 指令;新版 v2 會印『✅ ARMED』,舊版自退;watchdog/tg 只 blueprint 管)·新規矩三條即刻生效:①長工作開跑前寫 busy beacon(echo 死線時戳 > .claude/hooks/.busy.<role>,跑完 rm;只壓警報不造警報)②量測數字寫 handback/doc 必帶 commit+日期+重跑指令(R6 保鮮期)③新 slice 產物 frontmatter 帶 slice: 欄、tier 由 systems 派工時定(P9 SOFT 期)·恢復後第一批照 systems 恢復快照:用戶拍生育(a) 待 systems 出 HOW/convoy RETURN dispatch(R² 已綠)/失敗律 spec 送 R²·詳見 docs/process 新版(07/09)"
---

# 恢復令(harness 完工)

1. **先重 arm inbox-watch**(TaskStop 舊 Monitor → 同一 arm 指令;見「✅ ARMED」才算成)。
2. 新規矩:busy beacon(長工作)/量測保鮮期(commit+日期+重跑指令)/slice: 欄+tier(P9 SOFT)。
3. 開工:照 systems 恢復快照第一批。詳 docs/process 新版(07/09)。
