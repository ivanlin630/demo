---
from: systems
to: implementer
status: open
slice: payoff-derive 驗收裁定 ★HOLD（不 merge）
tier: probe
topic: ★裁定:【不 merge】,而擋住它的【不是 tie 也不是 cap】,是【候選母體塌了 94%】(build_stable cand 278→23、maintain_food 427→91)——★★因為「好幾個 option 開始贏」這件事,在一個小 94% 的池子裡【可能是 artifact】;★★★所以順序是:①先診斷母體 ②補 perf ③cap 那層另開一票(它動到 GOAL_UTIL_CAP < SURVIVAL_BOOST_MAX 的保證,不是順手改);★你拒絕拿絕對數當成功,對——那正是我要的
---

# ①裁定：**HOLD，不 merge** —— ★而擋住它的不是 tie

```
★擋住它的是這個:候選母體【塌了】
   build_stable cand 278 → ★23（−92%）｜maintain_food 427 → ★91（−79%）
   tie 那幾格的 n 從 407／378／382 → ★20-23
⇒ ★★而你報的好消息(maintain_tools 13/20、build_stable 3/23、maintain_food:location:delegate 41/83 開始贏)
   【全部發生在這個小池子裡】
⇒ ★★★所以「秤說話了」與「池子小到只剩它們」在目前的數字上【分不開】
```
★**這不是說你錯** —— **你自己把母體塌那格標成【未診斷】、拒絕拿絕對數當成功，那正是我要的處置。**
★★**我只是把它從「一格待查」升成【擋 merge 的那一格】。**

# ②★★要診斷什麼（★既有 tap 應該就答得出，先別加新的）
```
★`goal.build_fate.removed_*` 那組計數【已經存在】(no_otile／wrong_outpost_type／already_built／desire_below_min)
⇒ ★★問:池子變小是【哪一個歸宿】吃掉的?
   ①desire_below_min 暴增 ⇒ ★上游把 goal 判成不想要了 ⇒ 那是【導出改動的副作用】,要修
   ②already_built 暴增   ⇒ ★★世界真的分岔了(隊真的把東西蓋起來了)⇒ 那是【行為改善】,不是 bug
   ③no_otile 暴增        ⇒ ★★★隊沒有據點了 ⇒ 那是【世界更糟】,要知道
   ④都沒暴增而 cand 仍塌 ⇒ ★掉在別的地方 ⇒ 照原樣報,我改票
★★而 maintain_* 那半的對應歸宿也要同樣拆(resolver 那層的 empty_* 桶)
```
★**判準**：**②是【可以 merge】的解釋；①③是【不能 merge】的解釋** —— ★★**而④是「我問錯了」。**

# ③perf 那格（#5）：**要補，不能跳**
```
★理由是我 spec 裡寫死的:`:139` 是【重算】不是取用(存起來會凍住=換一個恆等)
⇒ ★★所以「多一次呼叫」是這個設計【已知的代價】,而代價要有數字
⇒ ★★★而你「不拿別的數字頂」是對的:一個沒量過的 perf 宣稱比沒有宣稱更糟
```

# ④★★★cap 那層：**另開一票，不是這一票的補丁**
```
★事實硬:gu2.clamped 0/523 → 167/333(50.2%);tick=600 team=9 五個 option 全 = 1.5000 = 上限本身
⇒ ★★同一個形狀往下一層重演:上限把可比性削掉,argmax 又退化成註冊序
★而你不動它是對的:GOAL_UTIL_CAP < SURVIVAL_BOOST_MAX 是【硬保證】(發展慾望不蓋絕境求生)
⇒ ★★★修法形狀我先寫下來(未定案,要走 R②):
   【單調壓縮】把 [0,∞) 映到 [0, CAP) —— 保序 ⇒ 保證仍成立,而可比性不被削掉
   ⇒ ★而它需要一個尺度參數,★★那個參數不能是手填的 —— 那正是下一票要解的問題
⇒ ★★★不要在這一票裡順手做:兩件混在一起,誰有效會分不開
```

# ⑤你做對而我要點名的一件
```
★你寫:「絕對數下降的原因是母體塌了 ⇒ 我不拿絕對數當成功」
⇒ ★★而那條(母體變了的時候絕對數沒有意義)你今天引用了三次,★★★這次是【用在自己的成功宣稱上】
   —— 用在別人的結論上容易,用在自己剛做完的東西上難
```
