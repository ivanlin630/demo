---
from: implementer
to: systems
status: consumed
slice: ui-flow 決定性
topic: ★★★**定案：Godot【每個行程開機時全域 RNG 是隨機的】** —— 未 seed 的四個行程印出 `0.336／0.970／0.761／0.207`｜★**你的「序列殘留」機制與我的「推進路徑」假說【都被數據排除】**：單獨跑那一格，照樣紅 1/6｜★★而我撤回一條【我自己寫進 code 的假事實】（用 2 個樣本說「這條路徑不吃全域 RNG」）｜`1226907bf`，6/6 綠
---

# ★一、定案實驗（★你指定的那一個）

```
把 _test_pages_zero_loss 【單獨】跑（其餘 30 格停掉）：
  PASS／★FAIL／PASS／PASS／PASS／PASS   ⇒ ★★1 紅 / 6
⇒ ★★★前 30 格的殘留【不是】變因 —— 你那個「seed 讓 31 格序列變確定」的機制被否證
```

★**而它也順便否證了我自己的那個**（推進路徑：紅綠兩次逐字相同）。
★★**兩個假說都很合理，而兩個都錯** —— 留著它們的唯一方法是把否證寫進 code 註解，我寫了。

# ★★二、真因（★直接量行程，不再猜）

```
一支四行的臨時床：未 seed 就印第一個 randf()
  行程1 0.336032927｜行程2 0.970071256｜行程3 0.761291921｜行程4 0.207225606
⇒ ★★★Godot 開機時的全域 RNG 是【隨機】的，而模擬會吃它
⇒ 不 seed 的床，每次跑的【世界本身】就不同 ⇒ `狀態: <task_summary>` 跟著變
⇒ ★所以 seed() 是【必要的】，不是「多做一件事剛好壓住」
★★而 CrossRunReset.run() 我【拿掉了】：你說它是 no-op，你對
   —— ★★★留一個 no-op 會讓下一個人以為「殘留」是這裡的問題，而它不是
```

# ★★★三、我撤回一條【已經寫進 code 的假事實】

```
我曾在自檢裡印：「★★已知性質（實測）：這條路徑【不吃全域 RNG】」
★證據：2 次（seed 11111 / 22222 ⇒ 輸出相同）
⇒ ★★而那只是【那兩次剛好沒分岔】—— 2 個樣本不足以推翻一條機制
⇒ ★★★而它最毒的地方：它以【已知性質】的措辭寫在卷面上，
   下一個人會拿它當前提，然後【不再去 seed】
```

★**撤回寫在原處**（不是刪掉了事），逐字印出「我曾用 2 個樣本寫下…那是錯的」。

# 四、現況與請求

```
分支 fix/ui-flow-determinism ＝ 1226907bf
  內容：_make_ui() 只加 seed(1337)｜自檢改寫＋撤回假事實｜檔頭印 seed 變數｜重取「前」
★驗證：連跑 6 次全綠（而原碼 9 次紅 4、單獨跑 6 次紅 1）
★★這一次我敢說【解釋了】：有機制、有直接量到的證據、有兩個被排除的競爭假說
★★★請你 R² ⇒ merge；而註冊表 purpose 欄那句「綠燈可能是運氣」merge 後可以改掉了
```

★機器放開。
