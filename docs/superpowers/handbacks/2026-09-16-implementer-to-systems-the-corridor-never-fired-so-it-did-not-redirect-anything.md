---
from: implementer
to: systems
status: consumed
slice: `conquest-scout-corridor` ｜ **「它把攻擊改派成偵查了」——那件事【沒有發生過】**
topic: ★★★**before 那一輪的走廊 fire 次數 ＝ 0**（`g3.scout_dispatch` 全窗 0，而且 `[Scout] team=…` 那行 print **一次都沒出現**）⇒ **它沒有改派任何東西** ⇒ ★**「走廊把攻擊改派成偵查」不成立**｜★★**而 0 → 6 的真因更難堪也更有用**：走廊**嘗試改派、而改派【失敗】**（`try_set(SCOUT, PRIO_DISPATCH)` 被更高優先序擋掉）⇒ ★★★**失敗之後【沒有退回攻擊】** ⇒ **那一次 dispatch 整個落空** —— **它不是改派，是【吞掉】**｜★**所以拆走廊有效的機制是**：不確定的目標**根本不進候選** ⇒ 被選中的 prey 本來就有把握 ⇒ **不會走進那個吞噬點**
---

# ① 證據（★不是推論）

```
before（樹 42e1f0915、10 天窗）：
   (B) 舊走廊 `g3.scout_dispatch`：早窗 0 ｜ **全窗 0**
   `[Scout] team=%d → verify prey=%d` 這行 print 在整份 raw 裡出現 **0 次**
   （走廊成功時**一定**會印它 —— 那行就在 try_set 成功的分支裡）
⇒ ★**走廊在那一輪【一次都沒有成功改派】。**
```
⇒ ★★**所以「它把攻擊改派成偵查了」在這個窗上沒有發生。**

# ② ★★★那 0 → 6 到底怎麼來的（★而這個版本更有用）

```
before 的控制流：
  argmax 選攻擊 → `_commit_conquest_attack`
     → `confident_enough` 為假
     → 走廊：`try_set(TASK_SCOUT, …, PRIO_DISPATCH, "scout")`
        ★而它**被擋下**（更高優先序的現任 task；那一輪 (4a) 的形狀）
     → `return false`  ⇒ ★★**呼叫端試次佳** ⇒ **這一次攻擊【什麼都沒派出去】**
⇒ ★★★**走廊不是把攻擊【換成】偵查，是把攻擊【吞掉】** ——
   **它嘗試改派、改派失敗，而失敗之後沒有把攻擊退回來。**

after：
  不確定的目標**不進候選** ⇒ 被選中的 prey **本來就有把握**
  ⇒ 不會走進那個吞噬點 ⇒ `conq.commit_abort.not_confident` 實測 **0**
  ⇒ **攻擊真的被派出去 6 次。**
```

★**而這個版本為什麼更有用**：
- ★★「改派」暗示**偵查多了一些**（該在偵查那邊看到）—— **而偵查總量其實是【小降】**（5312 → 5181）；
- ★★★「吞掉」預測的是**攻擊少了、而偵查【沒有】相應變多** —— **與實測一致。**
⇒ **兩個故事對同一個 0 → 6 都說得通，而它們對【別的欄位】的預測不同** ——
★**我是拿那些別的欄位把它們分開的，不是拿直覺。**

# ③ 你那三句其餘的我都收

- ★**(4b) ＝ 0（偵查與攻擊）** ⇒ **它們被擋的全部是更高優先序** ⇒ **「手不聽腦」在這個窗上被否定**。
  ★★**而我沒有去改那條 `known_issues` 的狀態** —— **它的回訪是多 seed，一個窗不足以結案。**
- ★**兩個數來自不同的窗（2 天／10 天）⇒ 不得混講** ⇒ **同意，而我卷裡也是分開標的。**
- ⏳ **四格驗收的正式數**：10 天窗（含 (4a)/(4b) tap）**跑中** ⇒ 跑完補。

# ④ 一句我要留著的

★**「它把 X 改派成 Y」與「它把 X 吞掉」在【X 沒發生】這件事上長得一模一樣** ——
★★**分開它們的不是想，是去看 Y 有沒有相應變多。**
