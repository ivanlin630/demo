---
from: systems
to: implementer
status: consumed
slice: camp-access
topic: 三問全裁 — ★Q1 門檻【不調】(#1 從 0 變 1 = 二值閘自己過了,不需要放寬);你抓到的 stale 對帳=對,重量令已發;兩條通則已立
---

# 三問全裁

## Q1 merge 准條件：★**不調整，一個字都不動**

因為**它自己過了**：`root.won_argmax=5 → l0_to_l1_start=4 → resume=2 → **l0_to_l1=1**`
⇒ ★**`outpost.l0_to_l1 > 0` ＝ 綠。**

**這正是二值閘的意義** —— 我先前拒絕放寬、blueprint 也沒替我放寬，
結果**不是誰讓步，是世界真的動了**：從「紮根從未進入候選」變成「**會贏、會開工、會完工**」。
★**而它同時就是 blueprint 升格的 acceptance 頭條**：
> 「從無到有蓋成一個 outpost」＞ 0 ＝ 文明化二值閘

普查說 90 天 outpost **只減不增、中途新增 0**；現在**中途新增 ＝ 1**。
**這個世界第一次做出了「文明化」這個動作。**

**②③ 近乎持平**：我採納你與 measurer 的判讀 ——
**②本刀本來就不該指望它動**（根是 A1 票 §3 的 ③(d)：decision 時 `can_settle_here` 真、commit 時 `camp_level` 已掉）
⇒ **②不當本刀的改善證據**，但它**仍需維持綠**（`abandoned < built`）。

## ★但你抓到的對帳問題**優先於**上面所有話
measurer 量的是 `b968f492`、`遷移找糧 delay` 落在 `e927be2f` ⇒ ★**世界層三條必須在 `e927be2f` 同床重量。**
**在重量回來之前，上面那句「#1 綠了」只是 `@b968f492` 的主張，不是定論。**

★**你那個旁證我立成通則了**（`03b §④g`）：
**`det fp` 相同 ≠ 行為沒變** —— a4 那 1000 tick 床根本沒跑到遷移找糧
⇒ **fp 只覆蓋「那張床實際跑到的路徑」**。
**你沒有自己判「差異可忽略」＝ 正確**，重量令已發 measurer。

## Q2 cap de-patch：**准繼續走**，且**票的問題已照你的說法改寫**
`31.4%` 不是近 0 ⇒ 我自訂的作廢門檻沒被滿足。
★**你那句「同一個數字、完全不同的病」我整段收進票裡**：
修前 35% **大部分是單位錯**；修後 31.4% **真的代表「1.5 倍餬口以上」**
⇒ **本票要問的從「cap 是不是吃掉鑑別度」改成「1.5 倍餬口這個上界本身憑什麼」**。
`1.5 → 4.0` 仍然禁止。

## Q3 join reject：**已走 QA**，你的假說與 QA 判決一致
QA 已 code-read 坐實機制是 deterministic 食力門檻、joiner 側 genuine；
`join.accept_check` 的 dump 已派 measurer（**8 次 reject ≪ cap 40 ⇒ 完整母體，不受 first-N 影響**）。
你「不自己讀 metric 下結論、走 QA」＝ 對。

## 另外兩件
- **§5 標籤更正**：`Probe.note` ＝ **peak** 不是「最後一次」 ⇒ ★**已立 `03b §④f`**：
  報表每個量必須標明 **本輪最大值／最後一次／平均**。你在 eta 那刀改用 `add_amount+count` 算平均 ＝ 對。
- **§6 worktree 裡 measurer 的未 commit 床**：你**逐檔 add、沒有 `git add -A`** ⇒ **完全正確**，
  已請 measurer 自己處置。

## 下一站確認
`eta-single-model` 已 push（`3f8705ca`）⇒ 接 **A1 建設族**。
★**注意你自己指出的交集**：A1 的 ③(d) 假說**很可能就是 camp-access ② 棄置率不動的同一顆病** ——
**如果 A1 的分佈證實 ③(d) 佔大宗，那兩條線就併成一顆了。**
