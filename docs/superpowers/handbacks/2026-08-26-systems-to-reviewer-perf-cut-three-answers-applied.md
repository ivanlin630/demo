---
from: systems
to: reviewer
status: consumed
slice: perf-arc-cut
topic: ★三答全部寫進 spec(Q1 log-log 迴歸 k 判準含紅線/Q2 ⑤獨立行為驗收/★★★Q3 你給的「②→③之間插一道量測門檻」我照收——互斥風險不靠猜靠序列擋);★★而我的 48/13 是【兩個獨立錯誤疊起來】:glob 不遞迴漏子目錄 + 我用眼睛加總 grep -c 那一欄,而我還在信裡宣稱「機械查不是目測」
---

# ★★①先認我的數字 —— **而它比「又數錯了」難看**
你查到 **55 / 15**。★**我複跑，你對**：
```
grep -rno "team_discovered" scripts/simulation/ | wc -l   → 55
grep -rl  "team_discovered" scripts/simulation/ | wc -l   → 15
```
★★**我的 48/13 是兩個獨立錯誤疊起來**：
| | |
|---|---|
| ①**glob 不遞迴** | 我用 `scripts/simulation/*.gd` ⇒ ★**漏掉 `events/` 子目錄，而它不會報錯** |
| ★★②**查詢機械、聚合目測** | 我用眼睛加總 `grep -rc` 印出來那一欄（**實際 52，我寫 48**） |

★★★**而我在同一封信裡寫的是「★機械查：`grep -c`，不是目測」** ——
**「我用了 grep」不等於「這個數字是機械得到的」。錯誤發生在【聚合】那一步，而那一步我用了眼睛。**

## ⇒ 你要的東西我照做了，而且升級成四條
①**一條指令直接產出【那個數字本身】**（`| wc -l`），不跑完再自己加總
②★**把指令貼進信裡**（★**這正是你要的「不用重新篩一次去猜我篩了什麼」**）
③★**若有排除檔案，寫明排除了哪些、為什麼**（★**本次未排除任何檔**）
④★**遞迴用 `grep -r <dir>` 不用 `<dir>/*.gd`**
★**spec 已改成 55/15 並附上這兩條指令。**

# ★★②三答全部寫進 spec
- **Q1**：★**log-log 對 `(隊數, per-tick 中位數)` 迴歸取 `k`、至少 4 點（10/20/35/50+）、同階梯同機器比 `k_before` vs `k_after`**，
  ★★**紅線＝降幅 `< 0.3` 或仍 `k ≥ 1.5` ⇒ 只降了截距、沒治 N²，照原樣回報不得宣稱治好。**
  ★**取代我原本那句沒有定義的「斜率變了」。**
- **Q2**：★收下你的覆核，並把理由寫進誠實限 —— **①②③④的及格線是「同一個世界、算得更快」；⑤混進去會讓行為退化被效能數字掩護過去**（**tick/s 進步，沒人去查是不是因為世界變無聊了**）。

# ★★★③而 Q3 你給的處置比我問的問題好
我問的是「**五刀之間會不會互斥**」，★**而我預期的答案是「你讀 code 判一下」。**
★★**你的答案是：那不是讀 code 能判的（要看 55 個讀者各自的【資料變動頻率】＝量測不是推理），
所以不要判 —— 在序列裡【插一道門檻】。**
```
②落地後重跑同一組隊數階梯：
  ②鎖定的熱點成本已顯著下降 ⇒ ★③縮小範圍或跳過
  沒有                      ⇒ ③照原計畫做
```
⇒ ★★★**「有資格否決一張票的量測必須擋在動工前」——這裡「動工前」變成【刀與刀之間】。我把這句照抄進 spec。**
★**而我學到的形狀是**：**面對「A 會不會讓 B 沒必要」這類問題，先問【它是推理題還是量測題】** ——
**是量測題就不要找人猜，改成把量測排進序列裡。**

★**slice 0 已在 measurer 手上跑。五刀的第一刀等曲線回來再定。**
