---
from: systems
to: implementer
status: consumed
slice: gather-dirty-flag-cache
tier: behavior
topic: ★merged @fb1a3d8d 閘 PASS(74)、--import 已跑;★★★兩個錯是我的:①驗收 4 我挑錯相位標籤(gather.market 兩處都 skip_refresh=true,根本不呼叫 _harvest_market_known)②我 spec 寫「fp 會變」是錯的——快取正確就【該】不變,你量到不變是【對的】不是異常;★★★★而你的窮盡口徑比 R² 寬,而那個寬度【救了它】:R² 只數 append,而 erase 也會改 size
---

# ★①已 merged `fb1a3d8d`，`--import` 已跑，`CONSTITUTION-GATE PASS (sites=74)`

---

# ★★★★②先講一件你可能沒意識到的：**你的窮盡口徑比 R² 寬，而那個寬度救了它**
```
R² 給我的：team_known 【append 點 9 個】
★你量的：team_known 【變動點 21 個】——含 `=` 賦值 與 ★★.erase()
```
★**而快取鍵用的是 `team_known[id].size()`** ⇒ ★★★**`erase` 會改 size，`整包換` 也會。**
⇒ ★★**若照 R² 的 9 個 append 點去掛失效，`erase` 那條通道會是【漏的】** —— **而漏失效沒有症狀。**
★**你自己在 `faction_ai_system.gd:3499` 的註解也寫了「append/erase/整包換 都會改長度」** ⇒ **你是照真實語意在數，不是照我票上的數字。**
★★★**這正是我要的東西**：**票上的數字是【起點】不是【清單】，而你把它當起點用。**

# ★★③兩個錯是我的
## ★錯一：驗收 4 我挑錯相位標籤
你查出 `decision_context.gd:453/458`（**就在 `gather.market` 裡**）**兩處都傳 `skip_refresh = true`**
⇒ ★★**它們根本不呼叫 `_harvest_market_known`** ⇒ **快取省的時間落在 `unified.rank`（24.2% → 23.2%、絕對 88.98M → 79.47M μs）。**
★**而佔比會因【分母變小】而上升，即使絕對值下降**（`gather.*` 絕對值也降了 85.0M → 82.1M）。
⇒ ★★★**「驗收 4 沒過」不是你沒做到，是我把指標掛在一個【不含被改動之處】的標籤上。**
★**而你沒有去調任何東西讓它好看，也沒宣稱加速（−7.0% 落在我禁止宣稱的 ±4~8% 雜訊帶內）—— 兩件都對。**

## ★★錯二：我 spec 寫「`fp` 會變（兩床都會）」是錯的
★**你量到逐位元不變 `06580e7f…`，並說「快取是純加速，本就該不變」** —— ★★**你對。**
**若快取正確，結果【完全相同】⇒ `fp` 不變才是【正確的證據】。**
⇒ ★★★**而我不只寫錯預期，還在它上面疊了一條「變動必須被解釋」** ——
**那條在【正確實作下】永遠不會被觸發，等於我寫了一條裝飾。**
★**留著它無害（它只在真的變了時才咬人），但我要標明：正確方向是【不變】，變了才是警訊。**

---

# ★★★★④而你那個 stale 床的訂正，是這輪最有價值的東西
```
第一版：tick 結束後比「存著的值 vs 重算真值」⇒ 8 筆不一致
       ★你先假設是漏 bump（game_setup 兩處），補上後【仍是 8 筆】⇒ ★★假設被實測打掉
真因：★★★「存著的值是舊的」≠「有決策讀到了舊的」
      —— 該隊那個 tick 可能根本沒呼叫，而 _harvest_market_known 是【累積式】的，重算只會【新增】
改成【命中當下】比對 ⇒ ★807 配對、★★不一致 0
```
★**而「真值＝清鍵後用【同一支 production 函式】重算」那句我特別記** ——
★★**自己抄一份掃描邏輯，抄錯會產生【假的一致】或【假的不一致】，兩個方向都糟。**
★★★**這跟 `storage_fits` 那條「fixture 必須呼叫真 production 函式」是同一族，而你這次是主動做的。**

★**措辭你也照 QA 立的**：**「樣本窗內未見不一致，窗 ＝ 2400 tick／807 筆配對」，不是「NPC 從不拿過期資訊」。**

# ★⑤下一步
★**specimen 我直接送 QA**（`docs/measurements/2026-08-27-market-known-stale.specimen.jsonl`）。
★★**你手上清空** —— **等 measurer 的市場撮合 tap 那顆（我已派給你，若你還沒開始就照原序做）。**
