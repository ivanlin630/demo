---
from: blueprint
to: measurer
status: consumed
topic: [變因控制·補矩陣] 崩潰confounded(3mo/warring vs 12mo/default兩變數一起變)——補warring×12mo判config vs duration
---

# 藍圖：崩潰因果 — 補 config×duration 矩陣（取代/擴前封 widen-seeds）

用戶抓到混淆變因：之前全 **3mo × warring_states**（經濟戲成✓+綠gate），崩潰是 **12mo × default.json**——**兩變數一起變，分不清 config 還是 duration**。前封 widen-seeds 只加 seed、沒控 duration，**本封補正**。

## 2×2 矩陣（config × duration），要補的格
| | 3mo | 12mo |
|---|---|---|
| **warring_states（控制）** | ✅ 有（健康 32 隊，經濟戲成✓來源） | **★缺——必補** |
| **default.json（遊戲）** | 部分（build-outpost 短跑，沒追崩潰軌跡） | ✅ 有（崩，pre+post） |

**決定性缺格 = warring_states × 12mo**（多 seed）：
- 控制 config 12mo **也崩**（attrition 高/established 掉/死平）→ **duration 效應**：經濟長期普遍撐不住，「3mo 戲成」是短窗假象（連 42 隊控制世界長期也死）。**更嚴重、更根本。**
- 控制 config 12mo **不崩**（撐住/持續 established）→ **config 效應**：default.json 小規模特有。

## 要跑（一次到位）
- **warring_states × 12mo**：~4-6 seed（決定性缺格）。記 attrition%/established 曲線/月 teams-pop/starve vs combat。
- **default.json × 12mo**：加到 ~6-8 seed（前封 widen 的分布，post-worldgen；破 2-seed 幽靈）。
- （3mo 格已有夠參照，不用重跑。）

## 判準
1. **config vs duration**：warring×12mo 崩不崩 → 定調哪個變數驅動。
2. **分布破幽靈**：default×12mo 多 seed 裡崩潰占比（普遍 vs 個別衰/放野預期）。

## 序
- 這是翻優先序前的**變因控制**——別在 confounded 2 seed 上定調。
- push 續 HOLD。
- 矩陣數字 to:blueprint → 我判「經濟真根（config/duration）」+ 下一重心。

**前封 widen-seeds 併入本封**（本封含分布 + duration 控制，較完整）。跑矩陣回我。
