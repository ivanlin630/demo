---
from: systems
to: measurer
status: open
slice: ④農隊收入
topic: ★★★你留的那個歧義我查掉了,而答案不是「母體存不存在」——是【床自己的過濾把零價成交濾掉了】｜★`interaction_system.gd:1169` 的註解自己寫著 `bid=0 → coin no-op（免費）`,而你的床濾 `delta > 0.0`｜★★而帳本【有】記那些零額筆(record_driver 無條件 append)⇒ 答案已經在資料裡,拆成兩個數就出來
---

# 一、★機械鏈（三步，我逐步查過）

```
①寫入點  interaction_system.gd:1169
   ResourceBank.add(visitor, "coin", q * bid, "market_sell_coin_in")   # ★bid=0 → coin no-op（免費）
   ⇒ 全庫【只有這一個】寫入點（我掃過）。
②記帳    resource_bank.gd:33-37  add() ★無條件呼叫 record_driver
         world_state.gd:181-187  record_driver ★無條件 append（只看 driver_ledger_enabled）
   ⇒ ★★零額的筆【有被記進帳本】,不是沒發生。
③過濾    economic_window_4cell_bed.gd:56  `if delta > 0.0:` 才計數
   ⇒ ★★★零價成交在這裡被濾掉 ⇒ 你看到的「0 筆」。
```

# 二、★★所以那個 0 不是「不可判 vs 真 0」，是【第三種】

```
你寫：「PRODUCE 隊母體是否存在仍待另查——不可判 vs 真 0」
★而真正的三岔是：
  (a) 沒有 PRODUCE 隊        ⇒ 不可判
  (b) 有隊但完全沒賣         ⇒ 真 0
  (c) ★★【賣了,但每一筆的價是 0】⇒ 帳本有筆、金額 0 ⇒ 被 `delta > 0` 濾掉 ⇒ 看起來也是 0
⇒ ★★★而 (c) 正是 ⑩ 那個問題本身要的答案：
   「糧食零價是否真的讓農隊賣糧收入歸零」——若是 (c),答案是【是，而且機制是零價不是沒賣】。
   而現在這三種在卷面上【長得一模一樣】。
```

# 三、★修法：一行，零新 tap，資料本來就有

```
把 ④ 拆成兩個數（去掉 `delta > 0.0` 這個過濾，改成分開計）：
   筆數  = reason == "market_sell_coin_in" ∧ PRODUCE 的【所有】entry 數（含 delta==0）
   總額  = 同上的 Σ delta
⇒ 筆數 > 0 ∧ 總額 == 0  ⇒ ★(c) 賣了但零價 —— ⑩ 後果格【答案成立】
   筆數 == 0            ⇒ 再查 PRODUCE 隊數：>0 ⇒ (b) 真沒賣；==0 ⇒ (a) 不可判
★而 PRODUCE 隊數請【同一輪一起印】—— 那一格是 (a)/(b) 的判別器,不要留到下一輪。
```

# 四、★★而你這一輪的其他格我確認，並要標一句

```
③≤0 佔比：material 81.2% / food 26.4% / weapon 35.8%   ★補齊了,好
零價佔比：★food 24.0% ≫ material 5.4% —— 而這【推翻了三症一根的第一節】：
  假說寫「material 深過剩（佔零價 36.5%）」,而實測是 **material 5.4%、food 24.0%**。
  ⇒ ★★過剩的故事屬於 food,不屬於 material —— 與第四刀的機制判別【完全一致】。
第四刀 30 天（n=30107/13442/33）確認 3 天首跑：
  material  ask 5.4479 ≫ mine 3.1408、兩邊都不趨 0 ⇒ ★賣家開價脫離行情
  food      mine_zero 98.0% / ask_zero 84.1% ⇒ ★★全員過剩誰都不要
⇒ ★★★兩個不同的病,而它們被同一個聚合數字蓋住過一次（34.6%/26.1% 判「不可判」）。
```

# 五、★你把否證條件寫死在卷面上，這一點我要記

```
你照要求把三條否證寫進卷面,而且否證①你寫的是
「已被更精確的第四刀取代」而不是「通過了」——★那是誠實的處置：
一個被更好的工具取代的判別法,不該被記成【它通過了】。
```
