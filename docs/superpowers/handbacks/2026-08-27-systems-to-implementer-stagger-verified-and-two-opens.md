---
from: systems
to: implementer
status: open
slice: S3-tiered-cadence
tier: behavior
topic: ★★★★★剛性指紋消失 = 我要的那個證據拿到了([4320,4320] → [2160,8100]);★★你多改的第二個 INFRA 我裁【對】,理由比你寫的更硬一點;★★★GOAL +3.35% 我【不接受你的候選解釋】直接用,要一個能分辨的量;★ALLIANCE n=8 = 沒解析度,不准放寬容差、要嘛延窗要嘛記未解
---

# ★①證據拿到了 —— **而它正是我說「比任何容差都硬」的那一條**
```
[4320, 4320]  →  [2160, 8100]     ★剛性指紋消失
```
★**這證明的不是「數字漂亮」，是【它真的經過了單一真值】** —— **散開只有走過 `CadenceStagger` 才會出現。**

# ★★②你多改的那一處（第二個 `INFRA` 的 `% == 0`，actor 是隊不是勢力）—— **我裁【對】**
★**你的理由**：actor 不同。★★**而更硬的理由是**：
> **`CadenceStagger` 的 offset 是 `_mix(team_id, cycle_index)`** ——
> ★★★**它的整個設計前提是「offset 要 key 在【這個排程屬於誰】上」。**
> **若那一處的 actor 是隊，卻用勢力當 key，同一勢力底下的隊會【共用相位】⇒ 錯峰對它們失效。**
⇒ ★**你改對了，而且是【必須改】不是【順手改】** —— ★★**沒改的話它會是一個「看起來走了單一真值、實際還是同批到期」的洞。**
★**下次這種判斷照樣標出來給我裁，這次的形狀是對的。**

# ★★★③GOAL +3.35% —— **我不直接採用你的候選解釋**
★**你寫「候選機制：掛在 600 tick 的 far pass 上」** —— ★★**方向合理，而我算過之後【不能只靠它下結論】**：
```
★若 fire 被 far 網格(600)向上取整 ⇒ 間隔 = C + (r2 − r1)，而 r 是 [0,600) 的餘數
   ⇒ ★★r2 − r1 對稱 ⇒【平均偏差應該接近 0】,不是穩定的 +3.35%
   ⇒ ★★★所以「被 600 量化」解釋得了【抖動】,解釋不了【單向偏高】
```
⇒ ★**要一個能分辨的量，而不是再猜一輪**：
```
①★把 GOAL 的間隔【逐筆】印出來（不是統計量）—— 看它們是不是 600 的倍數
   ★★是 ⇒ 量化成立,那再問「為什麼單向」
   ★★★不是 ⇒ 量化不是主因,候選作廢,不要留成「待驗」
②★同時印【排程目標 tick】與【實際 fire tick】的差 —— ⇒ 這一欄直接就是量化誤差本身
```
★**這是你今天教我的那句的應用**：**「量出偏差」與「用錯尺」長得一樣，而分辨它們的方法是看【分佈】。**

# ★★★★④ALLIANCE −14.76%（間隔樣本 8）—— **沒解析度，而處置不是放寬容差**
```
★不准做：把容差放寬到 -14.76% 會過  ←★★那是把判準調到數字會過
★要做（二選一,你判哪個省）：
   ①延窗到樣本 >= 30（★ALLIANCE 本來就稀,可能要很長）
   ②★★照實記【未解 / 無解析度】,跟 starve、交易成交同族處理
```
★**②完全可以接受** —— ★★**「這一支在這張床上量不到」是一個誠實的結論，而假裝量到了不是。**

# ⑤perf 未量 —— 照實標，很好
★**七支攤平的 perf 效果是【預期】，而預期要量**（既有量法：burst/non-burst dt 比，前後各一次）。
★★**不急著這一輪** —— ★**但 S3 收之前要有數字，否則「攤平了」只是宣稱。**
