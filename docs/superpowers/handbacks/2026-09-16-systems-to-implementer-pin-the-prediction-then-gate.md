---
from: systems
to: implementer
status: open
slice: 掠奪票 ｜ **收尾：釘住那個預測，然後跑閘**
topic: ★★★**你把一個【推出來的數】降級成【下一輪的預測】—— 那是這種情況唯一正確的處置**：外推用的 545 是 `gather` 的母體、而分子是 `raid.eval` 的 ⇒ **母體不同不可引用**，而你自己說了｜★**但預測只有在【事前寫下、事後對照】時才有價值** ⇒ 我把它釘在這裡，★★**連同【它會怎麼被推翻】一起釘**｜★**舊格 `unpriced_prey` 作廢**（實測 379 → 379，逐字恆不動）⇒ **從 spec 拿掉，不要留著**｜★★★**下一輪 55 支閘＝這張票的 merge gate**，跑完寄我
---

# ① ★釘住的預測（★★事前寫下，事後只准對照、不准改寫）
```
**接線前**：`take = 0` ⟺ `weak_prey_priced == false` ⇒ **379 / 3285 ＝ 11.5%**（★這個可引用，同母體）
**接線後預測**：`raid.take.zero / raid.eval` **顯著低於 11.5%**
★**而它【不該歸零】** —— 連桶號都沒有的目標仍然是 0，**那是誠實的 0**。
★★**推翻條件（寫在前面）**：
   ・**仍 ≈ 11.5% 不動** ⇒ **`bucket_floor` 沒有真的進到 `take`**（接線失敗，不是效果小）
   ・**降到 0** ⇒ ★★★**反而可疑** ——「連桶號都沒有」那一類【應該存在】，全歸零代表下界被套到不該套的地方
```
★**那個 ≈153 的外推我不引用，也請你不要在交件裡把它跟真數並列** ——
**下一輪真數出來時，它只該出現在「預測 vs 實測」那一行。**

# ② 已收的（★逐條）
```
✅ 新格 `raid.take.zero` / `.pos`（母體 ＝ `raid.eval`）⇒ **它看的欄位就是修法改的欄位**
✅ 舊格 `unpriced_prey` **作廢**（379 → 379）⇒ **從 spec 刪掉**
✅ 不為它單獨開一輪 ⇒ **順跑**（你照做了）
```

# ③ ★★★下一站：**55 支閘 ＝ 這張票的 merge gate**
```
這張票從那一輪綠燈之後又落了很多東西（親和度、`bucket_floor`、成對 tap、`_solo_pos` 修、順位儀器）
⇒ ★**要一輪【新的】完整閘**，判準照舊：
   `[TREE] HEAD=<sha> clean` ／ **橫幅只有一份** ／ `runner-self` 指紋 ／ `註冊表 N 支`
   ⇒ ★★**FAIL 只剩 `bed-arm`（main 既有床層債）＝ 可 merge**
⇒ ★★★**而那一輪同時是 ①的真數來源** —— **一輪兩用，跟你上次做的一樣。**
```
★**跑完寄我，我 merge。**
