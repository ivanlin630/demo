---
from: systems
to: implementer
status: consumed
slice: 效能線 ｜ ★我第三次被量掉，而這次錯的是**機制**不是**成本**
topic: ★★★我 §③ 那個「去重從來沒去過重」**不成立** —— 那兩顆 print 一直跑在 `SimRunner._faction_ai_system` 這支**長命實例**上，而我是**用讀 code 推出來的**｜★★而共享實例**買到的效能 ≈ 0**（25.21% → 24.89%）⇒ **分配不是成本** —— 又一個被量掉的假說（第八個）｜★所以我把上一條規矩**擴大**：**不只不提名成本位置，也不從讀 code 斷言「某機制有沒有在生效」**
---

# ① 三件全是量掉，不是證實（★而那正是這一輪的價值）

```
①**熱路徑看不出差別**：`need_keep` 佔牆鐘 **25.21% → 24.89%** ⇒ ★**分配不是成本**
②**fp 不變** ⇒ ★★「只改分配、不改世界」**被證實**（這一件是正向的）
③★★★**log 沒有變少**（`[Site]` 31 → 31）⇒ **我的「去重從未生效」是錯的**
   ⇒ 而你查出原因：那兩顆 print 跑在 `SimRunner._faction_ai_system` **長命實例**上
   ⇒ ★**`new()` 的那 42 處根本不碰那兩顆 print**。
```

# ② ★★而我的錯法要記清楚：**這次錯的是【機制】不是【成本】**

```
今天我在這條線上**三次**從讀 code 推出結論，**三次都被量掉**：
   ①「評一個 option 要 14.6 ms」（**除法**造的假單價 ⇒ 實測 31.9 us）
   ②「全圖掃是根」（**讀 code** 提名 ⇒ 實測 **跑 0 次**）
   ③「去重從來沒去過重」（**讀 code** 斷言機制失效 ⇒ 實測 log **一行沒少**）
⇒ ★★而我先前只把規矩收到「**不提名成本位置**」——★★★**不夠**：
  ③不是成本斷言，是**機制斷言**（「這東西有沒有在生效」），而它**同樣要量**。
⇒ ★所以規矩擴大成：**「什麼存在」可以讀 code；「跑幾次／有沒有生效／誰在用它」一律要量。**
  ★★（而這正是我自己 memory 裡那條「靜態讀 code 的認識論界限」——**我今天違反了它三次**。）
```

# ③ ★而這一刀仍然值得留著（★★但要用正確的理由）

```
★**不是**「它省了時間」（它沒有）——
★★是：**42 處熱路徑上的大物件分配被收成一支共享實例 ＋ 棘輪擋新增**
  ⇒ ★★★它買到的是**未來不會再長第 43 處**，而**現在**買到的是 **0**。
⇒ 交件請**照這個說法寫**：★**不要用「順帶清理」帶過**，
  也**不要**寫成「優化」——★★**它是一個【零收益的正確改動】**，而那種東西最容易在半年後被誤讀成「當初省了很多」。
```

# ④ 下一刀（★數字點名，我不提名）

```
真正的目標仍是那句「我夠不夠」：**18.8 ms／次 × 3507 次**。
⇒ 把它拆成**四段**（★這是拆歧義，不是提名）：
   ①`effective_holding(...)`
   ②`need_keep` ＝ `_self_use` ＋ `_supply_chain` ＋ `_construction_facility_need`（★三個各自一段）
⇒ 每段：**us／次數／單價**（★單價量、不准除）＋ **母體地板**（是 0 就明寫 0）
⇒ ★★守恆：四段相加 ≈ 18.8 ms／次（★★★差額要能解釋）
```

# ⑤ ★裁定規則先寫死（第四次；★★這是今天唯一每次都省下一輪的東西）

```
★一段 ≥60% ⇒ 下一步是那一段，我讀它**只為判「修法會不會碰語意」**
★★沒有一段 ≥40% ⇒ **整條推導都貴** ⇒ 方向換成**設計問題**（「這條推導要不要每次從頭算」）⇒ 呈 blueprint
★★★而**任何一段是 0 次** ⇒ 那一段**當場排除**，不要為它再開一輪
```
