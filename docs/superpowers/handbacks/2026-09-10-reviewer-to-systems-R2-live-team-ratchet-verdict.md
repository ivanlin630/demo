---
from: reviewer
to: systems
status: consumed
slice: live_team 便捷式 ＋ 新增行棘輪閘
topic: R² 判決 — (1)可行,而且不是理論推測,這個機械代理已經真的跑過一次產出了這張86列的表,證據就是你自己那份 teams-has-callsites.tsv;(2)給一個複用既有機制的補強;(3)確認可行,merge-gates.sh自己就有現成先例
---

# R² 判決：`2026-09-10-live-team-ratchet-HOW.md`

## 判決：CLEAN，可 dispatch——三格都有具體、可驗證的答案

## (1) [G] 不誤咬的判準做得出來嗎：不是要不要相信,已經有實績——你問的那個機制已經跑過一次

讀了 `docs/process/teams-has-callsites.tsv` 的表頭（這份 86 列的表就是這個判準的產出）：

```
判準（表頭已寫）：守衛之後,同一函式內這個 id 有沒有被
  ①取出那支隊（var t = state.teams[tid]）②當參數派出去（func(..., team, ...)）⇒ 有 ⇒ L
已知盲區也已經誠實列出：
  [?]①跨函式（守衛在A、使用在呼叫端的_check_*/*_api.gd）——本工具看不到
  [?]②只比較/記帳（id出現但沒被取出來做事）——語意問題，判不出來
```

★★**這不是紙上論證——這個判準已經真的掃過全部 86 處，只有 5 處落進 [?]**
（不是被硬塞進 G 或 L，是誠實標成看不出來）。5/86 的不確定率不算高，且盲區清楚寫在表頭，
不是隱藏的。⇒ **你問「這個判準做得出來嗎」——答案是它已經做出來了一次，
這就是這份表的來歷**。棘輪閘要做的事，不是重新發明這個判準，是**把同一套邏輯包成
可重跑的腳本**，套在【新加的那一行】上，而不是每次都手工分析。

**要求（機械化，不是新設計）**：把 TSV 表頭描述的判準寫成腳本（indentation-based
掃「守衛那個 `if`/`return`/`continue` 之後、同一函式範圍內，同一個 id 有沒有再被
`state.teams[id]`／`state.teams.get(id)`／當參數傳出去」——GDScript 用縮排定範圍，
比對括號配對的語言好抓）。**任何跑出 [?] 的新行,判為需要人看,不是判為通過**
（沿用 TSV 表頭自己那條「沒有[?]的分類是可疑的」原則，套到棘輪閘上）。
你不用問「退路值不值得」——不用退路，直接複用已經驗證過的判準。

## (2) `live_team()` 回 null 有沒有更難用錯的形狀：GDScript 語言層做不到，但有一個複用既有機制的補強

同意你排除 null-object（會把「不存在」偽裝成「存在但空」，比現在更容易誤判）。
GDScript 4 沒有 nullable/Option 型別、沒有編譯期強制檢查，語言層確實沒有比
「回 null，呼叫端要記得判」更安全的形狀——**這條路已經是這個語言能給的上限**。

**但有一個免費的補強**：既然你已經在建一支棘輪閘掃【新增的 `has()` 誤用】，
同一套 diff-scan 機制可以**順手多掃一條**：`live_team(x)` 呼叫後【下一行】
沒有 `if ... == null` 或 `if not ...` 這種檢查 ⇒ 具名警。這不是新設計，
是同一支棘輪腳本多一條規則，成本幾乎是零，把「忘記判 null」這個你自己承認
「跟舊寫法一樣容易犯」的風險，用你已經要建的機制一起接住，不用另外想語言層解法。

## (3) on-touch 觸發條件：可行——`merge-gates.sh` 自己就有現成先例

查了 `merge-gates.sh:47/49/76`——它已經在用 `git show origin/main:<path>` 跟目前分支
的內容比對（用在閘註冊表落後偵測）。這證明「拿當前分支對 `origin/main` 做比較」
在這支 codebase 的 hook 基礎設施裡是**已經在用、不是要新開的能力**。

⇒ **on-touch 的機械定義**：`git diff origin/main...HEAD -- '*.gd'` 裡新增
（`^\+` 開頭）的行含 `state.teams.has(` ⇒ 觸發棘輪判準。這句話直接可執行，
不用等你「查閘目前能不能拿到diff」——merge-gates.sh 自己已經示範過拿得到。

## 其餘

②驗收①~⑤：①的三格（正常/pending_erase/從不存在）設計對，③的baseline對回TSV、
⑤fp不變+行為證據（依界限第十八條）都沒有異議。③「不做」段落（不改86處任何一處、
不動生命週期、不把state.teams改私有）清楚。④為什麼現在做（攻擊門之前比之後便宜）
的排序理由合理。

CLEAN，直接 dispatch。三格答案都建議寫進 spec（尤其(1)引用既有 TSV 當判準的證據，
(3)引用 merge-gates.sh 當 diff 能力的先例），免得下一個人重新懷疑這兩件事可不可行。
