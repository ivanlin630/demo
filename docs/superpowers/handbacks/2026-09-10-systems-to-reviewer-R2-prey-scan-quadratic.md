---
from: systems
to: reviewer
status: consumed
slice: 獵物掃描的 O(N²)×尋路
topic: ★R² 送審：`specs/2026-09-10-prey-scan-quadratic-pathfinding-HOW.md`｜★★這是 26 秒凍結的【真正來源】：`find_prosperity_prey` 對【每一個 discovered 的隊】跑一次 `PathSystem.estimate_catch_up`（尋路）⇒ N=135 時 ~18,000 次尋路／pass ⇒ 量到 **33-50 ms／隊**｜★★★而本票【只做第一層：把不需要尋路的濾網移到尋路之前】—— 語意不變是它的地基，而我要你打的正是【它真的不變嗎】
---

# ★我要你優先打的三格

```
(1)★★★【語意真的不變嗎】：我主張把 `:262 weakness`（belief armed vs self armed）
   與 `:280 trip`（糧夠不夠）**移到 `:251 estimate_catch_up` 之前**，
   理由是那兩個【不需要尋路就算得出來】。
   ⇒ 請打：★**它們真的不依賴 `catch_result` 嗎？**
     ★★我看到 `:276 eta_days = catch_result.eta / TICKS_PER_DAY`，而 `:279 trip_need`
       用了 `eta_days` ⇒ **`trip` 那一格【可能依賴尋路結果】** ——
       ★★★**若是，我的第一層修法就少了一半，而我沒有查清楚就寫進 spec。**
   ⇒ 這一格請你直接判：哪些濾網真的可以前移，哪些不行。

(2)★per-tick 快取：我寫「命中率要量出來再決定值不值得」。
   ⇒ 請打：★★同一個 tick 內【真的會有多支隊問同一對 (team, tid)】嗎？
     （★★★我懷疑【不會】—— 因為 `estimate_catch_up(state, team, tid)` 的第一個參數是
       【發問的那支隊】⇒ 每支隊問的都是不同的 key ⇒ **快取命中率可能是 0**。
       ⇒ 若是，這一項應該從 spec 拿掉，而不是留著讓實作端去發現。）

(3)★★序：我寫「本票不是急件，因為錯開已經把單幀解掉了（4.5 秒 → 74 ms）」。
   ⇒ 請打：★★★這個「74 ms」是我**用平均值算的**（135 隊 ÷ 60 tick × 33 ms）——
     而**最壞的那一個 tick 不是平均**（錯開之後仍可能有 5-6 隊同時到期）。
     ⇒ 我可能低估了殘餘的 spike，而那會改變本票的序。
```

# ② 我已經處理的

```
①★數字是量出來的除法不是推論（4,458,883 ÷ 135 ＝ 33.0 ms／隊）。
②★★驗收①「回傳值逐隊逐 tick 相同」是本票地基 —— 第一層修法不准改變任何一個決策。
③★★★不做的事寫死：不改任何 `continue` 的條件（只改順序）／不碰第二三層／
   不順手改 `estimate_catch_up` 本身（**本票的論點是「少呼叫它」不是「讓它變快」**）。
```
