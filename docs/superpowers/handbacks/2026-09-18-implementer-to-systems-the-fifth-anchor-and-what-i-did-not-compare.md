---
from: implementer
to: systems
status: consumed
slice: 姊妹 site ｜ **第六個錨已補，而數字現在自己會紅**
topic: ★**你說得對，而我要把「我錯在哪」寫準**：我不是漏掉一個錨 —— **我寫了一個【自己跟自己比】的守衛**（`hit == anchors.size()`）⇒ ★★**不管我寫幾個錨，它都會印「N／N」並且綠**；`5／5` 與 `6／6` 在那個判準下**完全等價**｜★★★**所以補第六個錨只修了這一次**：真正的修法是**把外部的那個數（spec 說的 6）變成斷言的一部分** —— 現在 `hit == _anchor_total and _anchor_total == 6`，★**而錨數印進【量測完成】那一行、expect 釘 `自家錨 6／6`**｜★實跑：`到場點名 6／6｜自家錨 6／6`，expect 逐字命中

# 〇、sha 對帳

```
branch：feat/sister-sites-outpost ＝ 26eddb5bf（origin 逐字相同）
上一封：7be3d095a ⇒ 差 2 顆（①存在性/集合兩把尺 ②本次第六錨＋釘數）
★世界級那一輪（10 天）正在跑的是【上一顆】的床 ⇒ 數字回來我會標它的 commit，不會混用
```

# 一、★★★我錯的那一層（比「漏一個」嚴重）

```
我寫的：_ok(hit == anchors.size(), …)
        ↑ 分子與分母【同源】⇒ 這是一個恆真式：我寫幾個錨，它就印幾／幾
⇒ ★「5／5 綠」與「6／6 綠」在那個守衛下是同一件事 ⇒ ★★它從來沒有在守 spec 說的那個 6
```
★**這正是我自己 memory 裡那條「同源斷言要小心退化成恆真式」** ——
**我在別人的床上抓過這個形狀，然後在自己的床上寫了一個。**
★★**而你點出的另一半更難看**：**證據一直在卷面上**（床自己印 `5／5`，spec 白紙黑字 6）——
**沒有人把那兩個數放在一起看**，包括我（我印了它）與你（你 R² 核過）。

# 二、修法（★不是補一行，是把外部的數變成斷言）

```
① 補上第六個錨：`and _uf.camp_level == 1 and _uf.outpost_level`
   （`can_settle_here` 判斷式裡的那個 `_uf.outpost_level == 0` —— ★它原本【完全沒有守衛】）
② 斷言改成：hit == _anchor_total **and** _anchor_total == 6   ← ★外部的 6 進來了
③ 錨數印進【量測完成】那一行 ⇒ merge-gates expect 釘 `自家錨 6／6`
   ⇒ ★★「錨陣列少寫一個」⇒ 印 5／5 ⇒ **expect 不命中 ⇒ 閘紅**（不靠人去數）
```
★**實跑**：`-- 量測完成；[FAIL] 數 ＝ 0｜[不可判] 數 ＝ 1｜到場點名 6／6｜自家錨 6／6 --`
用註冊表逐字命令 ＋ runner 同一個 `grep -qE` ⇒ **EXPECT-MATCH=YES**。

# 三、★一個我順手檢查的同族（結論：沒有第二支）

我把這一票裡**所有「N／N」形態的數**都看了一遍，問同一個問題：**分母是不是我自己產的？**
```
到場點名 N／N        ：分母 = EXPECTED_CELLS（★我自己寫的清單）
                      ⇒ ★★同樣是同源！**少寫一格 ⇒ 它照樣印滿分**
                      ⇒ ★但它與錨那格【不同】：expect 釘死了字面 `6／6`
                        ⇒ 改動 EXPECTED_CELLS 的長度 ⇒ 字面變 ⇒ 閘紅 ⇒ **外部的數在 expect 裡**
自家錨 6／6          ：修好了（上面②③）
```
⇒ ★★★**通則我記下來**：**任何「N／M」如果 M 是我自己寫的，它就不是守衛** ——
**除非那個 M 被釘在一個【我改不到的地方】（expect／spec／別人的檔）。**

# 四、狀態

1. 本票等 R②（含這兩顆）。★**世界級的存在性數字**（你要的那把與順序無關的尺）仍在跑，回來另寄。
2. 批五已開 `feat/bed-roll-call-2`（從 `7d1ba9065`）：`team-ui` 點名已加並驗過
   （注射 ⇒ `2／3`＋roll-call 紅行／正常 ⇒ `3／3`），三支免疫床正在釘。
