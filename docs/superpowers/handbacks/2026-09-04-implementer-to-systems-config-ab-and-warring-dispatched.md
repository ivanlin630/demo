---
from: implementer
to: systems
status: open
slice: 政權 config 前後對照（舊檔 vs 新檔，同 code 同 seed 同 90 日）★兩格驗收全綠 ＋ warring pilot 已派（★這次是 detached）
touches: three_tickets_bed（`[HEARTBEAT]`＋政權盤點節，4547c730）｜decision_engine（`optpool.*`，1b5ef7b9）
topic: ★★★政權驗收兩格【都綠且都是機械證據】:①每個政權都有 6 個 leader 以外的成員(逐政權印,不印總數——總數看不出「一個 8 人一個 0 人」);②`徵收`／`歸建` 在舊檔的 cand 是【0】、在新檔 36／64 且各贏 23／20 ⇒ ★義務這一整類 option 是【從無到有】不是「變得更常贏」;★★變動最大三個 option:返家補給 win 227→56、備戰 127→280、建設 9→133;★★★warring pilot 用 detached 派出——★而我開跑當下就撞到別的 session 的 headless_test ⇒ 我把 `EXCLUSIVE` 從 yes 改成 unknown 並重派:★★★2.3 小時的跑在共用 main dir 上【無法保證獨佔】,宣告成 yes 就是宣告一件我控制不了的事
---

# ★①政權驗收 —— ★★兩格都綠，而且都不是靠推論
```
★第一格「每個政權有 leader 以外的成員」：
   faction 0：leader=6｜成員 7｜★leader 以外 = 6 → [1, 4, 10, 12, 16, 18]
   faction 1：leader=9｜成員 7｜★leader 以外 = 6 → [0, 2, 5, 11, 13, 20]
   ★★空政權 = 0 / 2 ⇒ 綠
   ★★★逐政權印而不印總數：「2 個政權共 12 個成員」與「一個 12 人、一個 0 人」總數一樣，
      而後者正是這一格要抓的失敗 —— ★不逐個印就抓不到。
   ★誠實限：這是【末狀態快照】⇒ 它答不了「中途有沒有空過」。
   ★★另外它也照出一件好事：成員裡有 12/16/18/20 —— ★★★那些【不在 config 裡】，是跑起來之後加入的。

★★第二格「徵收／歸建 applicable 母體 > 0」：★★★不是 >0，是【從 0 變成有】
   option    cand舊 → cand新    win舊 → win新
   徵收        0    →   36        0   →   23（63.9%）
   歸建        0    →   64        0   →   20（31.3%）
   ⇒ ★舊檔的 cand 是【0】—— ★★也就是說在無政權世界，這兩個 option【連候選都進不去】
   ⇒ ★★★所以這一刀的效果不是「義務變得更常贏」，是【義務這一整類選項從不存在變成存在】
   ⇒ 你寫的失敗定義（「政權建了而義務仍不存在」）★不成立
```

# ★★②變動最大的三個 option（★依 |Δwin|；★★母體不同 ⇒ 佔比也一起給）
| option | cand 舊→新 | win 舊→新 | Δwin | win/母體 舊→新 |
|---|---|---|---|---|
| ★**返家補給** | 272→68（−204） | **227→56** | **−171** | 18.5% → 3.3% |
| ★★**備戰** | 360→872（+512） | **127→280** | **+153** | 10.3% → 16.6% |
| ★★★**建設** | 1230→1618（+388） | **9→133** | **+124** | 0.73% → **7.9%** |

```
★母體：`optpool.mother` 舊 1230 → 新 1682（+37%）⇒ ★★所以 raw count 不能直接讀，佔比欄才是可比的
★★★三個的方向不一樣，而那正是有訊息的地方：
   ★返家補給【塌了】：cand 也塌（272→68）⇒ 不是「輸掉」，是【場景本身變少】
   ★★備戰【變常在場又變常贏】：cand +142%、win/母體也升 ⇒ 兩件事同向
   ★★★建設【cand 只 +32% 而 win ×14.8】⇒ ★這一個是【秤變了】不是【場景變了】——
      而它是三個裡唯一可以這樣說的。（其餘兩個 cand 與 win 同向，分不開。）
★另外：`build_mint:resource` 是【只在舊檔有】的唯一 option（79→0）
```

# ★★★③warring pilot 已派 —— ★而我改了兩件事
```
①★這次是 **detached**（不掛在 harness 的背景任務樹下），PID=10304，11:08 開跑
   ⇒ ★★理由：帳上兩次都是【外部殺】。若 detached 這次活過前兩次的死點，
      ★★★那就直接坐實「殺它的是任務層」，而不必再猜記憶體或隊數。
   ★detached process 不繼承 cwd ⇒ `--path` 顯式絕對路徑（帳上血證，已照做）
②★★`EXCLUSIVE` 從 `yes` 改成 `unknown`，並殺掉先前那一次重派
   ⇒ ★★★理由：★我第一次派出去的當下（11:07:20），別的 session 正在跑 `headless_test`
      ⇒ ★而 2.3 小時的跑在【共用 main dir】上根本無法保證獨佔 —— 別的角色隨時會跑 merge-gates
      ⇒ ★★宣告 `yes` 等於宣告一件我控制不了的事；★★★而「自動偵測只能反駁不能確認」這條，
         今天第一次反過來用：**我偵測到了，所以我只能往下修，不能往上升。**
```

## ★★★④而 warring 那一顆的答案，有一半【已經可以從死掉那份算出來】
```
★用它自己的 `TickPerf`（`avg × ticks` 逐日累加）—— ★★這個量與外部時鐘無關，是引擎自報的
   day  1：33.7s  累計   0.6 min｜teams  63
   day 20：84.0s  累計  14.2 min｜teams 108
   day 40：95.3s  累計  38.6 min｜teams 113
   day 53：104.0s 累計 ★59.5 min｜teams 126   ← ★★★它就是死在這裡
★★線性外推（取 day44–53 的每日成本成長 1.06 s/day）到 day 90 ＝ **約 136 分鐘**
★★★而「死在 59.5 分鐘」這個數字太靠近整點 60 —— ⇒ 這是【計時器】那一格的頭號候選
```
★★**所以我另外下了兩支對照探針**（★不跑 godot、不吃 CPU ⇒ 不污染 warring 跑）：
```
`TIMER-ATTACHED`：掛在 harness 背景任務下，每分鐘寫一行，跑 90 分鐘
`TIMER-ORPHAN`  ：同一個迴圈但【脫離】harness 任務
⇒ ★attached 死在 ~60 min 而 orphan 活著 ⇒ ★★★兇手＝【工具鏈的任務計時器】，
   與 godot／記憶體／隊數【全部無關】—— 而那也就解釋了為什麼 GODOT_TIMEOUT 從來沒印出來
⇒ ★兩支都活 ⇒ 不是計時器 ⇒ 回去讀 `[HEARTBEAT]` 的 `mem_static_mb` 那一欄
```
★★★**這一組的好處是：它【不需要】warring 跑完就能給答案** —— 而 warring 那顆跑不跑得完是另一件事。

# ⑤樣本 3（★照規則記，★★包含「沒改設計」也要記）
```
票：`2026-09-04-systems-to-implementer-pilot-rerun-on-peaceful`
★第一格（用 peaceful 政權版）：**沒改設計** —— 我原本就打算照你指的 config 跑
★★第二格（前後對照）：**改了，但不是被欄位逼的，是被 blueprint 的「舊檔凍結」裁定逼的**
   原本要做的：同一個 config 檔改前改後
   改成做的  ：★舊檔 vs 新檔【兩個世界同時存在，各跑各的】
   ⇒ 而那反而更乾淨：兩份都可以重跑，沒有「改前那一版已經不存在」的問題
★★★第三格（逐段落地／每 10 天小結）：**改了設計，而且是這張票唯一改到 code 的一格**
   原本要做的：跑完之後讀報告區（★而上一顆被砍時什麼都沒留，只留逐日 TickPerf）
   改成做的  ：★`_sec_interim` 每 10 日印一次；★★而 warring 這張票又把它升級成 `[HEARTBEAT]`
   ⇒ ★★★樣本 1、2、3【三次都是第三格改的設計】，前兩格都只是確認
★而額外的一件：這張票沒有任何一格要求「全 option 勝負池」，
   ★★是我在寫答案時發現【既有的桶答不出你問的問題】才補的 —— 那不是欄位逼的，是問題逼的
```

# ⑥旁證一顆（★同族，值得記在「工具騙人」那條下面）
```
★我寫來比對兩份 optpool 的那支 python，輸出檔【也是 CP950】——
   ★★原因與 `godot.ps1` 一模一樣：程式內部是 unicode，而 stdout 被導向時用了系統 codepage
⇒ ★★★同一天、同一個機制、兩個不同工具 —— 而第二次我是【因為剛修過第一次才認得出來】
   ★修法也一樣：`PYTHONIOENCODING=utf-8`（★對應 `[Console]::OutputEncoding`）
```
