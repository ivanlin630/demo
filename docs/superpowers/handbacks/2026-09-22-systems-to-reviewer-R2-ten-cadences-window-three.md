---
from: systems
to: reviewer
status: open
slice: 10 處 cadence 改走錯開 —— **R² 請審**（世界改變窗 #3、世代 7）
topic: ★**spec**：`docs/superpowers/specs/2026-09-22-ten-cadences-use-plain-addition-HOW.md`（75 行）｜★★★**根因是機制不是相關**：純加法 `next = now + C` 讓同相的隊**永遠同相**，而世界生成時它們**從一開始就同相**｜★★**我最不放心的三格在下**，請優先打｜★**一個我自己抓到的 file:line 錯已訂正**（`hex_tile_data.gd` → `tile_data.gd`）
---

# 一、一句話

```
33 處走 `CadenceStagger`、**10 處純加法** ⇒ 那 10 處把 **六成的隊**（61.5%／62.4%）鎖在同一個相位
⇒ 修法：**那 10 處改走 helper，零新旋鈕、cadence 值一個都不動**
⇒ ★**改變決策時刻 ＝ 改變世界** ⇒ 世界改變窗 #3、**世代 7**
```

# 二、★★我最不放心的三格

```
①**A2 的門檻（尖峰佔比 < 20%）** —— ★我的理由是「均勻錯開 ⇒ 期望每格 ~2%」⇒ 20% 寬鬆到不可能靠雜訊達成
   ⇒ ★★**請挑**：這個推導有沒有暗含「錯開真的會均勻」這個**未驗前提**？
     （★★★`_mix` 的分佈我**沒有驗過** —— 我只讀了它的註解說它打散）
②**tile-scoped 那兩處的鍵** —— 我指定 `tile.tile_id`（`tile_data.gd:3`，`class_name HexTileData`）
   ⇒ ★★**而我沒有驗它在世界重生成後是否穩定** ⇒ 若不穩定，錯開相位每次重生成會變
   ⇒ ★**那仍可接受，但我要知道** —— 請判這是不是一個必須先答的前提
③★★★**A5（>2s 幀數）我【不設門檻】** ——
   理由：**11+ 是必要非充分**（該桶 77.4%／82.6% 未凍結）⇒ 消掉尖峰**應該**讓凍結消失，**但那是推論**
   ⇒ ★**請挑**：不設門檻會不會讓這一票「做完了而沒有人能說它有沒有用」？
     （★★我的反面理由是：設一個我推出來的門檻，會把**推論**偽裝成**預註冊**）
```

# 三、★其餘

```
§5 的閘（禁 `*_next_tick = current_tick + …`）★**陽性對照 ＝ 修法前那 10 處必須紅**
  ⇒ ★★這一格特別重要，因為**這個病躲過了我兩輪靜態掃**（`% 60 == 0`／經過時間比較，都零命中）
  ⇒ ★★★**它長得像正確的排程** ⇒ 人眼看不出來 ⇒ 只能靠機械
§6 誠實限三條（A5 不保證凍結消失／22 處「其他」未逐處讀／`tile_id` 穩定性未驗）
```
★**R² CLEAN 之前我不 dispatch。**
