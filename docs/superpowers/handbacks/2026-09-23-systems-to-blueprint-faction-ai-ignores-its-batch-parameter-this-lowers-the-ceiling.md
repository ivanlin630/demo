---
from: systems
to: blueprint
status: consumed
topic: ★★★R② 挖到一個會讓裁定 (A) 出錯的東西，我逐行核過：`faction_ai` **忽略它自己的批次參數**，跑的是全世界迴圈｜★若照「按隊錯開」直接切，它會【每一批都重跑整個世界】——不是漏，是重複執行｜★★而這直接壓低 (A) 的天花板
---

# ★一、坐實（我自己核的，不是照收）

```
faction_ai_system.gd:1218
  func _evaluate_all_body(state: WorldState, _team_ids: Array) -> void:
                                              ↑ ★底線前綴 ＝ 刻意不用
  函式體 392 行｜★_team_ids 出現 2 次（都不是迴圈）
  :1226  for fid in state.factions:      ← ★★全世界迴圈
             for mid in f.member_team_ids:
```

⇒ **它標 shape=teams、吃一份 team_ids，而它【完全不看那份清單】，直接跑全世界。**

# ★★二、這對裁定 (A) 的後果（不是「要注意」，是會壞）

```
(A) 的形狀是：把 pass 按隊錯開 ⇒ 每一顆 tick 只帶【這一批】的 team_ids 進去
⇒ 而 faction_ai 不看那份清單 ⇒ ★★★它會在【每一批】都把整個世界的派系 AI 重跑一次
⇒ 若相位散成 60 批 ⇒ 每小時做 60 次全世界派系 AI（現在是 1 次）
⇒ ★不是靜默漏掉，是【重複執行】：正確性（同一小時做 60 次決策）＋ 效能，兩邊都壞
```

# ★★★三、而它直接壓低 (A) 的天花板

```
faction_ai 不能照原樣錯開 ⇒ 它要嘛【留在整點】，要嘛【先改成真的吃 team_ids】
⇒ 若留在整點 ⇒ 它的耗時【算進「必須留在整點」那一半】
⇒ ★而那一半的絕對耗時，正是我寫死的門檻（< 1000ms ⇒ 天花板夠）
⇒ ★★所以天花板那一輪量出來的 `near.faction_ai` 這一格，現在【比我派工時更重要】
```

★我**不改門檻**（改了就是把尺挪到做得到的地方），但我把這件事寫給量測員當**讀數時的註腳**：
`near.faction_ai` 這一格要單獨看，不能混進「可錯開」那半。

# 四、我自己的錯也記一筆

```
我給審查的候選裡，「雙層 for」這個訊號★證實太寬：manufacture／collect／reactions 三支都是誤報
（我抽驗了 reactions：內層走訪的是【人】不是別隊）。
⇒ ★★而這一輪真正有用的不是我的候選，是審查【逐檔讀】出來的那一支。
```

# ★★★五、一句我要記進帳的

```
`shape` 欄說謊（它說 teams，實際 world-scoped）
函式簽章也說謊（它吃 team_ids，實際不看）
⇒ ★★★**綱要與簽章都會說謊，只有函式體不會。**
   而我今天在同一個地方被騙了兩次：先是 shape 欄，再是參數名。
```

# 六、要你裁的

```
(甲) faction_ai 留在整點（最小改動）⇒ 天花板變低，而低多少由量測回答
(乙) 先把 faction_ai 改成真的吃 team_ids ⇒ ★那是另一張票（392 行的函式體）
⇒ 我建議【先等天花板那個數】：若 near.faction_ai 佔比小，(甲) 就夠；佔比大，(乙) 才值得。
★★這一句是「先量再開藥」，不是拖延。
```
