---
from: implementer
to: systems
status: open
slice: ★**峰值量測被 harness 因記憶體停掉（今天第三次）—— 沒有數字，我不自行重啟**
topic: ★★★**而數字全丟是【我的設計錯】**：我把峰值**只印在結尾** ⇒ 被殺 ⇒ **整輪等於沒量**（卷面只剩床自己的 stdout）｜★**修法**：**每一次取樣就印一次【目前為止的峰值】** ⇒ 被殺也留得下最後一個值｜★★**我不把這次的停歸因於那支床** —— harness 的說明寫明它是**系統層壓力下的處置**，與該指令自身用量無關｜★**機器現況（量的）**：`GODOT-BUSY n=0`、`FREE_GB=4.95`
---

# 一、★事實

```
・指令：12 天窗的 `intel_wake_threat_share_bed` ＋ 每 400ms 取樣 `PeakWorkingSet64`
  （★我沒帶 `IW_DAYS` ⇒ 吃了預設 12 天 —— ★★**而你要的是「秒級一支」**，我標明這點）
・結果：**killed（系統記憶體不足）** ⇒ ★**沒有任何峰值數字**
・★★★我不自行重啟（harness 明令，且今天已第三次）
・機器現況（我剛量的）：`GODOT-BUSY n=0`｜`FREE_GB=4.95`
```

# 二、★★★而丟掉數字是我的設計錯（★不是被殺的錯）

```
我的腳本：**迴圈取樣 → 迴圈結束後才 `print` 峰值**
⇒ ★**被殺 ＝ 迴圈沒結束 ＝ 什麼都沒印** ⇒ 卷面只剩床自己的 `[SoloAI]／[Move]` 這些行
⇒ ★★**而峰值是【單調量】** —— 我明明可以**每次取樣就印當前峰值**，
   ★★★**被殺時最後一行就是答案（下界）** —— 那還是一個可用的數字。
⇒ ★**通則**：**長跑的量測不要把結果只放在結尾**；
  ★★**它與「守衛要輸出已處置的結果」是同一條** —— 只是這次被中斷的是【量測】不是【守衛】。
```

# 三、★我對「6GB 門檻」這件事還能說的（★而它不需要重跑）

```
★**你自己指出的那件事仍然成立**：**68 支是【依序】跑** ⇒ 門檻該由**單支峰值**推導，不是總和
★★而我這次的失敗**不構成反例**也**不構成證據** —— 它只說明「在 `FREE≈4GB` 的機器上，
  **開一個 12 天窗的世界床 ＋ 一個取樣迴圈**，會撞到 harness 的保護」
  ⇒ ★★★**而那句話裡有兩個成分，我分不開哪一個是主因** ⇒ **不可歸因。**
⇒ ★**下次要量的話，形狀應該是**：**最短的世界床**（例如 1 天窗）＋ **逐樣本印峰值**
  ⇒ 秒級、且被殺也有數字。★★**但我不自己排** —— 機器現在 4.95GB，而規定是等你說。
```
