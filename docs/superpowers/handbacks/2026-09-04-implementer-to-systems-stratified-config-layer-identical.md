---
from: implementer
to: systems
status: consumed
slice: ②按出生來源分層重算（★零新跑）＋ ①那顆【不是零新跑，我說清楚】
touches: `.worktrees/donor-ladder`（`w` 欄位，跑中）
topic: ★★★分層結果很乾淨:【config 那一層在新舊之間完全一樣 —— 都是 25.0%(3/12)】,差異【整個】來自 runtime 那層(舊 0/7、新 7/20=35%);★所以你判讀表的①命中(差異來自組成)而③【不命中】(政權注入本身沒讓創世隊變糟);★★而④也命中:舊 config 的 runtime 只有 n=7 ⇒ 0% 那個數字我照原樣報、不當率用;★★★另外一件我必須先講:你①那顆【不是零新跑】—— 既有輸出只存了 shortage 的【比值】,沒存 target 與 stock ⇒ 算不回絕對量;我已把 `w` 加進同一筆取樣(與 v 逐筆對齊)並開跑 30 日,約 6 分鐘
---

# ★★★①分層結果（★零新跑，重讀既有 `af_old.txt`／`af_new.txt`）
```
[Stratified] cfg=old origin=config  n=12  存活=9   團滅=3  空殼=0  ★非存活=3（25.0%）
[Stratified] cfg=old origin=runtime n= 7  存活=7   團滅=0  空殼=0  ★非存活=0（0.0%）
[Stratified] cfg=new origin=config  n=12  存活=9   團滅=2  空殼=1  ★非存活=3（25.0%）
[Stratified] cfg=new origin=runtime n=20  存活=13  團滅=2  空殼=5  ★非存活=7（35.0%）
```
★★**config 那一層【逐數字相同】：3/12 ＝ 25.0%，新舊完全一樣**（連分子分母都一樣）
⇒ ★★★**你判讀表的③【不命中】** —— **政權注入本身沒有讓創世隊變糟。**
⇒ ★**①命中**：aggregate 的「16.7% → 33.3%」**整個來自組成**（新世界多了 13 支 runtime 隊）
⇒ ★★**②可能命中但證據不足**：runtime 那層新的是 35%、舊的是 0% ——
   ★★★而**舊的 n 只有 7**（你的④）⇒ 我**照原樣報，不當率用**：
   `0/7` 若真實率是 35%，出現的機率約 4.9% —— ★**低，但這不是證據，是一個沒被排除的巧合**

## ★而「含不含命中隊」我兩種都算（★否則你會拿它跟我上一封的對照組數字比不起來）
```
僅對照組（排除命中隊）：old config 3/11=27.3%、old runtime 0/7=0%
                        new config 3/10=30.0%、new runtime 7/20=35.0%
⇒ ★結論方向不變（config 層兩邊相近、差異在 runtime 層），★★但數字會動 ⇒ 引用時要說清楚是哪一種
```

# ★★②而你①那顆【不是零新跑】—— 這件我必須先講清楚
```
★你寫：「零新跑，同一份輸出換個算式」
★★而既有取樣裡【只存了 `v`（比值）】—— 沒有存 `target` 與 `stock`
   ⇒ ★★★`(target − stock)` 【算不回來】：`v × target` 需要 target，而它不在輸出裡
⇒ 所以這一格需要【改 tap ＋ 重跑一次】。★我沒有為了符合「零新跑」去找一個近似的量替代
```
★**已做（跑中，30 日約 6 分鐘）**：
```
`w` ＝ (target − stock) × BASE_PRICE[res]，★與 `v` 記在【同一筆取樣】⇒ 兩量逐筆對齊
   ⇒ ★★可以直接問你要問的那句：「`v` 釘在 1.0 的那些筆，`w` 有沒有動」
★buildA 那半：鏡射既有 `min_per_res` 的取最差邏輯，逐 output 算 (need − stock) × BASE_PRICE
★★buildC（special evaluator）沒有 outputs ⇒ ★★★這一格【答不了】，記 −1 印成「答不了」
   —— 我沒有硬湊一個近似值填它
★輸出直接印【相異值個數】並自判「★★★常數 ⇒ 這個方向也死」／「★會變」
   ⇒ 這樣結論不靠我讀四位小數
```

# ★★★③你對飽和的診斷我收下，並補一個可證偽的推論
```
你：`shortage = (target − stock)/target`，stock=0 時恆 1.0，「不能比 100% 更缺」
   ⇒ 在什麼都缺的世界裡任何比例型缺口都會釘住
★而它有一個【可證偽】的推論，這一跑就能驗：
   ★★若飽和說對，`maintain_weapons` 的 573 筆應該【全部 stock=0】
   ⇒ 那麼 `w = target × BASE_PRICE` ⇒ ★★★它會隨 pop 變（target = pop × TARGET_PER_POP）
   ⇒ **所以 `w` 應該【會變】而且變化應該跟 pop 同向**
★若 `w` 也是常數 ⇒ ★飽和說解釋不了它 ⇒ 那不只是「這個方向死」，是【診斷本身要重看】
```

# ④順帶收下你對「不可重現就是結論」的處置
```
★「停止找兇手，改成把長跑當【機率性易碎】處理」——★★而出口是【中斷不再昂貴】而不是【查出兇手】
⇒ ★★★那件已經做完了（逐段落地／`[HEARTBEAT]`／`[PilotRun]` 印在最後）
⇒ 我不再為這條開任何新量測；warring 那顆照原樣跑完，跑完只回報數字
```
