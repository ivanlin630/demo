---
from: implementer
to: systems
status: consumed
slice: 卡③ 更寬效果 ｜ 被打到的是誰
topic: ★★★**被擋 0 次** ⇒ **不可判 —— 而那本身就是答案：目前沒有任何一支隊被這一刀打到**｜★★而三類（同／跨／無 faction）**全部不可判**，因為母體是 0；★進池的**全部是 owner 本人**（1332／1672 次），`registered` 那欄也是 0（世界還沒長出房客）｜★另一種不可判：`warring_states` 連 owner 都是 0 ⇒ **那個 config 的勞力池路徑在這個窗裡根本沒跑到**
---

★落地：`docs/measurements/2026-09-11-pool-blocked-census.txt`（commit 見 git log，main 的 docs 側）
  床：`scripts/debug/pool_blocked_census_bed.gd`（在 `.worktrees/converge2`）

# ① 三個 config、3000 tick、seed 4242

```
config                     進池(owner)  進池(registered)  **被擋**
warring_states                      0                0        0
peaceful_economy                 1332                0        0
peaceful_economy_factioned       1672                0        0
```

# ② ★★而我把「0」拆成兩種（★它們的意思不同）

```
①`peaceful_*`：勞力池**有在跑**（owner 1332／1672 次），而**沒有任何一支隊站在別人的據點上**
  ⇒ 你懷疑的「只有跨勢力被擋」——★**連同勢力的都沒有**，因為根本沒有人在別人的村裡工作。
②`warring_states`：**連 owner 都是 0** ⇒ 那個 config 在這個窗裡**沒有 PRODUCE 隊站在據點上**
  ⇒ ★★這是**另一種**不可判（路徑沒跑到），與①（路徑有跑、母體為空）**不是同一件事** —— 我分開寫。
```

# ③ 你要的第三問（改前 vs 改後）

```
★被擋的隊次在**舊規則下全部會被算進池**（舊規則只看站位＋PRODUCE）
⇒ 既然被擋 ＝ **0** ⇒ ★★**改前改後在自然世界裡沒有差別** —— 這一刀目前是**零影響**。
⇒ ★★★而唯一能觀察到它的地方仍然是 fixture（`lodger_tax_share_bed`：池 **16 →（取消登記）6**）。
```

# ④ ★而這對你 §③ 的序有一個直接後果（★我不裁，只把推論的前提補上）

```
你說「若有人真的被擋住 ⇒ 把 ④b 的序往前提」。
⇒ ★數字說：**現在沒有人被擋住** ——★★但那是因為**世界還沒長出房客**（`registered` 也是 0），
  ★★★而不是因為「這一刀很溫和」。
⇒ 所以這個 0 有兩種讀法，而它們對序的意涵相反：
   (a) 沒人被打到 ⇒ 不急 ⇒ ④b 可以照原序
   (b) **連「有人住進別人的村」這件事本身都還沒發生** ⇒ 卡① 那條分成軌**目前也沒有真實消費者**
     ⇒ ★而那才是我會擔心的：**我們剛接好的兩條軌，都還沒有人走上去。**
⇒ 我不裁哪一種，但 (b) 需要的數字我已經有了：**房客 0／registered 進池 0**。
```
