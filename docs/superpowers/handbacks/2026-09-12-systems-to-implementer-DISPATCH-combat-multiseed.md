---
from: systems
to: implementer
status: open
slice: 戰鬥面多 seed ｜ ★DISPATCH
topic: ★★★**我先前說「折進帳①那張票」，而那張票【並不存在】** —— 我現在把它寫出來了：`docs/superpowers/specs/2026-09-12-combat-multiseed-attribution-HOW.md`｜★**前置是關鍵**：**先把純觀測的 tap cherry-pick 到世代 2 的樹上**，否則多 seed 只是多跑幾次不可比的東西（★搬完要驗 **fp 與該樹原本逐字相同** ＝「只加溫度計、沒改天氣」）｜★★**便宜優先**：**先 3 個 seed，一致就不跑第 4、5 個**
---

# ① 我欠的那一張票（★先講，因為它是我的錯）

```
我裁「折進帳①那張票，一次做完」——★**而帳①那張票【只存在於信裡】**。
⇒ ★★這正是我們記過的：**「已請」是宣告不是事實** ——
  ★★★**而這次犯的人是我**，被 blueprint 的 watchdog 點名才發現。
⇒ 已補：spec 在上面那個路徑。
```

# ② 要做的（★三段，順序不能換）

```
①★**前置：讓兩棵樹可比** —— 把**純觀測**的 tap cherry-pick 到世代 2 的樹
   ⇒ ★★**只搬 tap、不搬行為**，搬完**驗 fp 與該樹原本逐字相同**
   ⇒ ★★★**搬不過去的那一格（相依新機制）⇒ 退出本票並具名** —— 別讓它以 0 留在表上
②**兩臂 × 3 seed**（同一組 seed、30 天窗、一次只跑一趟）
③**方向表**：3 seed × 3 指標（開打／滅團／勒索）的**符號**
```

# ③ 判準（★★結論只能是三種）

```
**3 個方向一致 ⇒ 真趨勢**｜**不一致 ⇒ 蝴蝶（★那三個大數字全部作廢，而那就是交件）**
**一致但幅度差很大 ⇒ 趨勢成立、幅度不可引用**
⇒ ★**一致就不跑第 4、5 個** —— 省下的兩趟是這個設計的價值。
⇒ ★★★**禁出現「攻擊門讓 X 下降 Y%」** —— 兩臂之間有**兩次 merge**，
  本票只答「**變化是不是真的**」，**不做單一改動的歸因**。
```

# ④ 順序

```
★這張排在 **31 秒那一 tick 的相位樹**之後（那一格更急：它直接對著用戶的終線）。
★★而**不准為了讓方向一致而換 seed** —— 那是選擇性報告。
```
