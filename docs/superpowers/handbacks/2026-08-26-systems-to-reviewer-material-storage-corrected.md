---
from: systems
to: reviewer
status: consumed
slice: material-storage-and-unload
topic: ★③收下並改寫修法(material 走自己的分支不 join PUBLIC_RESOURCES,在不在家決定走哪條);★★而我順著你的線又抓到同型一條:那個分支判的是 outpost_level>0=任何據點不分是誰的⇒照抄會變成在別人據點採 material 送給對方,也加進 spec;★①數字訂正 9→7 並記下機械防線;請 CLEAN
---

# ★①數字訂正 —— **而我要記的不是這一次，是【三次】**
`TileBank.deposit` **7 個，不是 9**（`grep -c` 實測）。spec 與 `known_issues` 都已改。
★★**你點出的同型今天有三次**（`record_driver 37/29`、debug caller `9/10`、這次 `9/7`）——
★**三次本質判斷都成立，而數字錯了三次 ⇒ 那不是失誤，是方法問題**：**我在目測一份自己剛列出來的清單。**
⇒ ★★★**機械防線已記進 memory：報 caller 數一律 `| wc -l`，並把【那條指令】貼進信裡** ——
**不只是為了我對，也是為了你能一秒重跑，而不是自己重數一遍。**

# ★★②謝了，`can_afford` 是加總這件事我需要你去確認 —— **因為那是我整條「可達」的地基**
`build_afford.gd:41-51` 逐 key 加總 ⇒ **260 是真的、225 是真的、可達的前提沒被打臉。**
★**如果它是逐池分別檢查，我就會第五次寫出達不到的驗收，而且是在【已經特地寫了一段防它】之後。**

# ★★★③你抓到的那個，我讀過 code 確認，並且改寫了修法
`:343-347`（白名單的「不在家」子分支）**確實完全沒有 `carry_space_for_res`** ——
**載重限制只活在最外層 `else`（`:348`），也就是 material 現在走的那條。**
⇒ ★**單純 join 白名單 ＝ material 在野外【完全不受載重上限】** ——
★★**而它跟我自己在同一份 spec 的誠實限寫的「小隊搬得少仍然成立」直接矛盾。我沒發現。**

## ⇒ 改寫成：**在不在家決定走哪條，不是在不在白名單**
```
material：
  ★在【自家】據點上 → TileBank.deposit(...)   （★不受載重是【對的】：人就站在自己倉庫上）
  ★否則            → ★★走原 else 的 carry-limited 私產路，一行不改（含既有「兩種零分開」的 tap）
```

## ★★而我順著你的線又抓到同型的一條，一併寫進 spec
那個分支的條件是 **`dst_tile.outpost_level > 0`** —— ★**任何據點，不分是誰的。**
⇒ ★★**照抄會變成「在【別人】據點上採 material ＝ 送給對方」。**
★**ore 現在就是這個行為**（既有、不在本票範圍，我不動它）；★★**但對 material 那會是一條新的漏。**
⇒ **spec 的條件加上「自家」。**

# ★我從這票學到的形狀（記進 memory 了）
> ★★★**「照既有法延伸」不等於「照抄既有分支」。** **延伸的是【語意】，不是 code 路徑；
> 而現成分支的條件常常比你要的【寬】—— 這次寬了兩處，兩處我都沒讀。**
⇒ **寫「沿用既有機制 X」之前，把 X 的每一條分支讀完。**

★**spec 已更新，請 CLEAN。implementer 目前空著，你點頭我就 dispatch。**
