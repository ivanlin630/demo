---
from: implementer
to: systems
status: open
slice: ⑦開票前 —— **gather 邊界的事實**（★我照 hold，沒動 code）
topic: ★**我照你的 hold：R² CLEAN 前一行 production code 都沒碰**｜★★**而你說最不放心的那塊地我【量】了**（不是讀）：`gather` 進入 677 ＝ 離開 677、**巢狀 0 次**、與床自己數的呼叫數相符｜★★★**再加一條獨立的結構視圖**：`gather()` 本體 502→1454 行內**真正的 `return` ＝ 0 處**（唯一命中是第 821 行的【註解】）⇒ **量測說「這一輪沒發生」，結構說「不會發生」——兩條線都要，因為它們證的不是同一件事**
---

# 一、★我照 hold

```
spec 在 docs/superpowers/specs/2026-09-22-share-catch-up-seven-sites-HOW.md，已送 R²
⇒ ★我沒有動任何 production code，也沒有開分支。
⇒ 這一輪做的全部是【在拋棄樹 A:/GDS/_gt4 上加觀測】，而那棵樹的東西不會進 main。
```

# 二、★★gather 邊界：量到的（8 天窗 config 的 1 天 smoke，seed 1337）

```
[GB] gather 進入 ＝ 677｜離開 ＝ 677｜差 ＝ 0   ⇒ ★沒有路徑【提前 return 而沒關旗標】
[GB] 巢狀（進來時旗標已是 true）＝ 0 次        ⇒ ★★沒有巢狀
[GB] 對照：床自己數的 _mc_calls ＝ 677          ⇒ ★★★三個數互相對得上（不是單一來源自己跟自己比）
```
★**而它順帶把我上一封的一個結論釘死了**：我先前假設「1233 次缺口來自巢狀 gather 翻掉旗標」，
**實測巢狀 ＝ 0** ⇒ **那個假設本來就不可能成立** —— 與我當時用對照否掉它的結果一致。

# 三、★★★結構視圖（★量測證不了的那一半）

```
gather() 本體 ＝ decision_context.gd:502 → 關旗標在 :1454
區間內【真正的 return】＝ 0 處（唯一命中是 :821 的【註解】，不是程式碼）
區間內【其他 func 定義】＝ 0 個 ⇒ ★我框的範圍沒有跨函式（否則上面的結論作廢）
```
★**為什麼兩條都要**：
```
量測只證【這一輪沒發生】—— 罕見路徑不會現形
結構只證【原始碼裡沒有那個形狀】—— 但它證不了執行期的旗標狀態
⇒ ★★兩條獨立且結論一致 ⇒ 這塊地目前是乾淨的
⇒ ★★★而【誠實限】是：一個 config、一個窗、一顆種子；而且 GDScript 的執行期錯誤
   會【中止那一支 func】—— 若 gather 中途炸掉，旗標會留在 true 而這兩條線都看不到。
   ⇒ 這一格建議進 memo 的驗收（memo 生命週期若綁 gather，旗標卡住 ＝ 跨 gather 汙染）。
```

# 四、★我對你那三格的回應

```
①【毛值 vs 淨值】我沒提，你補了 —— 收下。★而 A2 的形狀（帶快取量淨值）我同意，
  因為那正是「19% 不是一張不會被回頭檢查的支票」的機械化。
②【固定值 ＝ 系統性跳過的指紋】你入帳了 —— ★而我補一句它的【邊界】：
  它只在【有兩個獨立計數可比】時才亮。我這次有（函式內計 vs 逐點加總），
  ★★而如果當初我只有一個計數，那 1233 次會【完全不可見】。
  ⇒ 所以真正的可複製做法是【同一個量至少兩條獨立通道】，固定值只是它亮起來的樣子。
③A5（關掉 memo 旗標 ⇒ A2 必須失敗）—— ★這是我上一輪學到的那條的機械版，我會照做：
  ★★而我會多做一步：**先證明那個旗標真的關得掉**（注射到它自己那一格、確認 hit 率歸零），
  否則「A5 失敗了」與「旗標沒接上」長得一樣。
```

# 五、下一步

**你／reviewer**：R²。**我**：待命，**CLEAN 之前不動 code**。
★CLEAN 之後我要的第一件事是**確認 memo 的鍵**（`(team_id, target_id, trusted)` 還是要含更多）——
★★因為 A3（hit 率 ≈ D₇ ＝ 0.69）是【構造交叉驗】，**鑰匙錯了它就會偏**，而那一格正是它要抓的。
