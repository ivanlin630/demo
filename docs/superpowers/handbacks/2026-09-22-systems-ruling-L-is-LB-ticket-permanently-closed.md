---
from: systems
to: all
status: consumed
topic: ★★★**裁 `L ＝ L_B`（44.6%／43.1%）⇒ 照 WHAT 預註冊的門檻，那張票【永久結案】** —— 而**裁定的根據不是偏好，是一個已驗的事實**：`estimate_catch_up` 會抽亂數 ⇒ 共用它就是改變世界｜★★**implementer 撞到的那 26.6% 才是真正的錢，而它卡在【同一顆前置】**｜★**我偏好的量法被它自己否掉了**，我照實記
---

# 一、裁定：**L ＝ L_B**

```
量到：L_A ＝ 71.2%／70.6%（把 estimate_catch_up 算成【走訪】）⇒ ★過門檻
      L_B ＝ 44.6%／43.1%（算成【計算】）              ⇒ ★★不過門檻
★★★而 L 要回答的問題是：【在「只合併走訪、不共用計算結果」的形狀下，哪些成本會消失？】
```

**決定性事實（我自己開檔核過，不是推的）**：
```
path_system.gd:258 estimate_catch_up() 內部呼 observed_speed(...)
path_system.gd:251 observed_speed() ⇒ ★ randf()
⇒ ★★共用 estimate_catch_up 的結果 ＝ 少抽一次亂數 ＝ 改變世界
⇒ ★★★§1 的禁令【直接適用】⇒ 它在本票形狀下【省不掉】⇒ 它屬於【計算】那一側
```
⇒ **L ＝ 44.6% ＜ 57.1%** ⇒ 照 WHAT 預註冊的門檻：**本票【永久結案】，不再重議。**

★**這個裁定不是「我覺得它比較像計算」** —— 是**「共用它會踩到亂數」這個事實**把它推到那一側的。
★★**而 implementer 不自己挑一邊是對的**：這一格本來就不是量測員能裁的 —— **定義屬於形狀，形狀是 HOW。**

# 二、★★那 26.6%：**它才是錢，而它卡在同一顆前置**

```
estimate_catch_up 佔 T_seg 的 26.6%，而它在【兩條走訪裡用同樣的引數各算一次】
⇒ ★看起來這才是真正的錢（比合併走訪大得多）
⇒ ★★而它省不掉的理由【跟一圈形態完全相同】：共用 ⇒ 少抽亂數 ⇒ 改變世界
⇒ ★★★它【不是本票的漏網之魚】，是【觀測雜訊決定性化那張票】落地後才會出現的東西
```
⇒ 已登進 defers 同一列，**並明寫「重看時要一起看兩塊：①四條全合 ②`estimate_catch_up` 只算一次」**。
★**兩塊卡的是同一顆前置** —— 而那顆前置 WHAT 已經排在「下一個世界改變窗」。

# 三、★我偏好的量法被它自己否掉（照實記）

```
我在派工信裡偏好 (乙) 對照法（把 per-element 計算換成常數，量走訪）
⇒ 實跑：stub 之後圈數 206119 → 10422 ★母體塌陷 20 倍（stub 改變了控制流）
⇒ ★★所以 (乙) 量到的「走訪」不是原本那個走訪 —— **它自己把自己否掉了**
★★★而我當初把 (甲) 的風險寫得比 (乙) 詳細
   ⇒ **我對兩條路的風險評估是【不對稱】的，而不對稱的那一側正好是【我偏好的那一側】。**
```

# 四、★預註冊門檻在這一票的實際價值

```
★它沒有「讓答案變好看」，它做的是【讓答案在被知道之前就有歸屬】
⇒ 量出 L_A 的那一刻，如果門檻是量完才訂的，我會很想把 estimate_catch_up 算成走訪
⇒ ★★而門檻已經在那裡 ⇒ 我能做的只剩【誠實地裁定義】，而定義有一個【與偏好無關的事實】可依
```
★★**所以這一票的產出是：一個用數字釘死的永久結案 ＋ 一個更大的候選被正確地排到前置之後。**
