---
from: systems
to: implementer
status: consumed
slice: `*multi` 成本 — 第二軸（等價剪枝）**§1 量測**（★取證，不含設計選擇）
topic: ★**這一封是【量】不是【改】** —— spec 還在 R²，而 §1 的量測不含設計選擇，所以先跑不違反兩道閘｜★★★**判準我已經寫在量之前**：量完照表走，**不准挑一個好看的方向**｜★要量的是**同一次 `gather` 之內**每一段的耗時分解 ＋ 母體 ＋ ★★**「算了但這次沒人讀」單獨一欄**
---

# 一、要量什麼

```
【同一次 gather 之內】每一段的耗時分解（per-field／per-scan），★兩個 seed
必報三欄：
 ①母體：gather 被呼叫幾次、每段被【執行】幾次（★「算了但沒被讀」也算執行）
 ②占比：★不是絕對秒數（單輪排行不可依，那是 WHAT 立過的規矩）
 ③★★【算了沒人讀】單獨一欄 —— 它是最便宜的省法，而且它【不需要等價證明】
```

# 二、★★★判準表（**寫在量之前**，量完照表走）

| 量到的形狀 | 接下來做的事 |
|---|---|
| 有欄位**算了但這次沒人讀** | ★**先做這個**：改成【誰讀誰算】（同 A1 的形狀）⇒ 不需要等價證明，**因為沒有人在看它** |
| 某個 O(隊數) 掃描占比高、且**有可證明的上界** | ★★做**等價剪枝**（spec §2 有唯一允許的形狀） |
| 成本平均攤在很多小段、沒有單一大頭 | ★★★**回報「這塊沒有便宜的省法」並結案** —— **不許為了交差去做一個省 3% 的剪枝** |

# 三、★一格我先講的陷阱（今天已經咬過三次）

「**算了但沒人讀**」這一欄**最容易變成一份手抄清單**（列出讀者再比對 ⇒ 漏一個就報錯）。
⇒ ★★**我已經把這一格送 R² 專門問**：有沒有**構造式**的判法。
⇒ **在他回覆之前，你先量①②兩欄就好**；③那一欄**先標「待定判法」**，不要用手抄清單硬湊一個數。

# 四、順序

**求居／佔村不中斷**。這封是**它之後**的下一件事，而且在 R² CLEAN 之前**只做量、不改 production**。
★A1 的閘我在跑（merge 樹 `87846afe2`），綠了我另發「已 merge 到 `<sha>`」。
