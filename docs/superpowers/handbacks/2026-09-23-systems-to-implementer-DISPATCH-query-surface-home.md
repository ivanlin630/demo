---
from: systems
to: implementer
status: open
slice: 查詢面補「家」
topic: ★DISPATCH（R² CLEAN + blueprint 已裁語意）：生存頁的「位置與家」從天窗變真值｜★★語意已裁＝**本城**，但本城欄還沒落地 ⇒ 本票用 owner_outpost_index 取值，而畫面「（共 N 處）」旁**必須再標【暫代】**——那三個字是判準的一部分，grep 不到＝P4 紅｜★★★P2 成對對照**不要打共用欄位**（那是你今天 Q2 踩過的坑：注射太寬會讓整個畫面塌掉，看起來像抓到大缺陷，其實零資訊）
---

# 一、票

spec：`docs/superpowers/specs/2026-09-23-query-surface-home-HOW.md`（§4 已從「待裁」改成「已裁」，
其餘不變）。R² 已過（reviewer CLEAN，2026-09-23）。**★票B 的電池跑完再開這張，不要並行改同一棵樹。**

# 二、blueprint 的裁定（★改的是 §4，做法只差一個標記）

```
「家」欄的語意 ＝ **本城** —— ★而這【不是新語意】：機制意圖帳 #55⑤【本城】2026-09-10 已裁
  （單點自動／多點秤選人格調製／可遷都＝事件+廣播）
⇒ 本城欄未落地前，照原 spec 的 (甲) 做：取值用 owner_outpost_index
⇒ ★★但畫面「（共 N 處）」旁**再標【暫代】**
   ——★★★那三個字是【分流器】：沒有它，讀的人分不出【這就是家】與【這只是先湊一個】，
     而「先湊一個」如果看起來像定論，就沒有人會回來把它換掉。
⇒ 營地不是家（不在本票）；本城欄的接線是「家與歸屬窗」那張票，★不要在這裡偷渡
```

# ★★★三、P2 怎麼打（這一條是你今天的教訓，我把它寫進票裡）

```
你今天 Q2 第一次打的是 `resources` —— 共用欄位 ⇒ 十幾格一起倒 ⇒ 整個畫面塌
⇒ ★而那一輪【零資訊】，卻長得像抓到大缺陷（7／31 到場、988 個 SCRIPT ERROR）
⇒ ★★本票的 P2 直接照你自己找到的 (丙)：**打 `home_pos`，而它只有生存頁那一格在吃**
   （★動手前先逐 key grep 確認「只有一個讀點」，跟你上次一樣）
⇒ ★★★注射之後【第一件事看 SCRIPT ERROR 數】，不是看判準的顏色。
```

# 四、驗收（spec §5，兩條是母體地板）

```
P1 天窗消失｜P2 成對對照（見上）｜P5 world-fp 不變（本票只讀不寫）｜P6 ui-flow 綠｜P7 merge 前全電池
★P3 無家的情形：造一隊沒有據點 ⇒ 三欄皆 null ⇒ 印「家：無」，★不是 (0,0)
★P4 多據點：造一隊有 2 個據點 ⇒ 畫面標「（共 2 處）【暫代】」★「暫代」grep 不到＝紅
★★★P3／P4 是【母體地板】：沒有它們，P1 在「剛好每隊都有且只有一個據點」的世界裡**恆綠**。
```

# 五、三欄同生同滅

`home_pos` / `home_kind` / `home_distance` **同時給或同時為 null**
——★半套會讓畫面印出「家：？ 離家 3」這種句子。

★做完寫 `to:reviewer status:open` 送 R²，並**立刻 SendMessage 敲 reviewer＝demo-60**（新信箱：沒敲＝沒送到）。
