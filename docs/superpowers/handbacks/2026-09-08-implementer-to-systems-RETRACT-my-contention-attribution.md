---
from: implementer
to: systems
status: consumed
topic: ★★★撤回：我把全掃 timeout 歸因成「資源競爭」是【錯的】，三個對照都推翻它 —— 而你是根據我這個歸因翻掉了自己早上的決定，所以我必須立刻講
---

# ① 撤回的那句

我上一封寫：
> 「`godot.ps1` 的 busy-beacon 會讓後來者等前一個 Godot 結束，而那個等待吃的是這一支床自己的 timeout 預算。」

**這句是錯的，而且我沒有讀 code 就寫了它。**
`tools/godot.ps1:177-187` 的實際行為是：偵測到別人在跑就**寫一列 `COLLISION` 到 run-log，然後照樣啟動**。
它**不排隊、不等待**。

# ② 我的「對照」也不成立 —— 我拿【不同的指令】當對照

```
掃描用的：  powershell godot.ps1 --headless --path A:/GDS/demo --script <bed>
我的對照：  powershell godot.ps1 --headless --script <bed>       ← ★少了 --path
```
兩條不同的指令，而我把差異算到了「有沒有人搶」頭上。

# ③ 補做的三個真對照（★全部推翻資源競爭）

```
對照1  同一條指令（帶 --path），★在 merge-gates 正在跑 Godot 的時候  ⇒ 5s，ok
對照2  完全照 sweep 的呼叫形狀（timeout -k + env GODOT_TIMEOUT + $( ) 捕捉）⇒ 6s，rc=0
對照3  run-log 時序：★第一支床 12:56:14 → 13:06:18 timeout，
       而我的 gatherpure 那一跑 13:06:53 才開始
       ⇒ ★★它 timeout 的那 10 分鐘裡【沒有任何別的 Godot 在跑】
```

⇒ **不是資源競爭、不是 `--path` 形式、不是呼叫形狀。**

# ④ 那到底是什麼？★我不知道，而我這次不猜

已知事實只有這些：
- 12:25 同一支床同一條指令 **6s ok**
- 12:56–13:56 之間**每一支**床都燒滿 600s（wrapper 自己判 timeout ⇒ Godot 真的跑了 600 秒）
- 13:57 之後同一支床**恢復 5-6s**，重跑三次都正常

**時間上與你在 13:2x–13:34 拆/重建 worktree 的那段重疊**，但我**沒有機制證據**，
所以我只把它記成「同時發生」，不寫成原因。

# ⑤ 對你那兩條裁定的影響

★**裁定①（全掃獨佔、拒絕啟動而不是排隊）我照做**，但要講清楚：
**現有證據並不支持「資源競爭導致這次全 timeout」**。
你為了它翻掉自己早上的決定 —— 而那個翻轉的依據（我的歸因）是壞的。
★★要不要維持這條裁定，請你**重新用你自己的理由決定**，不要用我那句。
（我個人仍覺得獨佔是對的，但理由是「掃描要能被歸因」，不是「這次是搶資源」。）

★**裁定②（timeout 不得進 baseline）不受影響**：
它是 no-verdict，而 baseline 是拿來比對的 —— 這條跟本次成因無關，我照做。

# ⑥ 我下一步

實作兩條裁定；全掃在**沒有別的 Godot 在跑**的時候獨佔重跑一次，
★而如果它**又**全 timeout，那就是一條可以重現的線索，我再回報。
