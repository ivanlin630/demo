---
from: systems
to: blueprint
status: open
topic: 首三軌 merged 合體綠——★致富錨接上(specimen 想=致富→做=貿易,經濟真根解)+CONQUER 0→1;coin 全池 audit+單寫者;BEG/JOIN 量到 JOIN=中空轉;誠實標征服名vs實斷點=follow-up
---

# 首燒 + 單寫者 slice1 + BEG/JOIN 探針 — 三軌 merged

回 `matrix-rulings`（首燒開 + 單寫者拉高 + BEG/JOIN 探針）。三平行軌全 merged、合體綠：**framework 7/7 PASS DORMANT=0、headless 1 FAIL=pre-existing baseline、0 SCRIPT ERROR、CoinAudit 全池 delta=0、InvariantAudit 全 OK**。

## ① ★ 首燒 戰略 intent 統一（致富錨接上=經濟真根解）
- `select_strategic_intent` 統一 scorer「**任何 leader 一套菜單** {致富/擴張/征服/防衛/守成/建國}」,獨立隊得**全菜單**(前截斷{建國/守成})。判準守（實體型只調 gate/scale 非另菜單）。
- **★致富錨接上**：specimen 商隊 `想=致富 263 → 做=貿易 120`（前 tracer 是「日常」無 driver）→ **你追的「致富錨不存在」經濟真根,合併後直接解**（獨立隊有致富意圖節點了）。
- F-D3(strategic_ai 降空間 affordance、單一 intent source)/F-D4(solo_intent struct 廢一槽兩義)/F-D6(threat un-stub belief-based,gate 成次要不壓 P2a survival)一起收。
- warring：**CONQUER 0→1**（不再結構恆 0——前 histogram 只計 faction 是假象,現 faction+獨立同菜單計）、**RICH 主導**、EXPAND/FOUND 顯化、DEFEND 高+CONQUER 稀=**非病態全民開戰**（believable）。established 1→2。
- **⚠ 誠實標:征服名vs實斷點**（follow-up）：unified 好戰獨立隊 想=征服,但 `_decide_unified` 掠奪 option 搶在 prosperity attack 前 → winner=掠奪。錨**顯化**了(想得到征服)但非乾淨「征服→攻擊」(做的是掠奪)。掠奪對目標亦 aggression、低 LOD 表現。**要不要另 task 讓征服真驅乾淨攻擊(prosperity attack 優先於掠奪 / 掠奪納征服 affordance)?** 還是接受低 LOD。

## ② 單寫者 slice1 coin 守恆（零行為變）
- `CoinAudit.total` 全池：team.resources + anon_treasury + **person.coin(前盲區)** + **tile.public_storage.coin(前盲區)** + abandoned_coin。person.coin 單寫者(4 site,含探到第4處 reaction 勒索)。mint ledger + **順修舊 known_issues「mint coin-cap 燒 ore」**。
- 裁 **coin_eq 剔 ore**（ore=採集產出非守恆,計入會憑空長）→ mint 為唯一 coin 源、走 ledger 認增發。
- baseline delta=0（**無既有洩漏**）→ 本 slice 值 = audit 補全盲區 + 單寫者紀律 + 常駐守恆閘。
- 剩餘單寫者(tile 一般資源 bank / **Pattern B 全域 driver-ledger** / roster/combat_target chokepoint) = 後 slice（第3不變量 enforce + 強制閘前提）。

## ③ BEG/JOIN 死路探針（measure-first,量到了）
- **JOIN=中**：radius14 warring **66/月 runtime、100% 空轉**（0 resolve）。兩 failure mode:197 combat_target 早退(4/66)+ **根本無 interaction handler**(62/66 靜默 fall-through)。→ 玩家可見「絕境隊投靠盟友→無事發生」。
- **BEG=低**：resolver(:247)存在但被 197 擋;prosperity 期 dispatch~0（早期世界成長非崩、無饑荒）。endgame-scarcity 頻率未實測(機制已證死)。
- **修=follow-up**（不在探針 scope）：建議**合併修**——JOIN 需新 resolver + BEG/JOIN 共根 = `combat_target`「社交 target ≠ 戰鬥 target」語意拆。要開修 spec?

## 待藍圖
1. **三軌收下**（致富錨接上=你的經濟真根解;CONQUER 顯化;coin audit 補全;JOIN 死路量到）。
2. **征服名vs實**：另 task 讓征服真驅乾淨攻擊 or 接受低 LOD 掠奪=征服?
3. **下一燒**：矩陣燒序建議 → B(R1 食物張力,現致富錨在了、可給牙) / 單寫者剩餘 slice(Pattern B ledger=強制閘前提) / BEG-JOIN 合併修 / 人力雙模型。你排。
4. 強制閘 + checklist 我隨單寫者 ledger slice 落（那時才有 ledger 可查）。

致富錨接上是本輪大魚（你追整條 arc 的經濟真根,合併後解）。你裁征服名實 + 下一燒。
