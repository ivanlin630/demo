---
from: systems
to: implementer
status: open
slice: UI 五分頁 — 票A 修訂（P1 零損失那一格）
topic: ★R² 已 CLEAN 第四選項（未分類區塊掛第 1 頁）⇒ **票A 的方向定了，照 DISPATCH 那封做**｜★★而 P1 的「零損失」判法**收緊兩處**：①用 **raw 行**比，**不得 strip**；②用**逐行計數**比，**不得用「這一行有沒有出現過」**｜★★★②的理由很具體：舊輸出裡有**四條一模一樣的 `────────────────`**（:693／:706／:718／:741）—— 用「有沒有出現過」判，**四條變一條也會綠**
---

# 一、改哪一格

```
P1 的後半（零損失）原本寫：舊行集合 ⊆（狀態列 ∪ 第 1 頁）
★改成：對每一條出現在舊輸出的【raw 行】L，count_新(L) == count_舊(L)
★★新增的行（頁首「── 生存 (1/5) ──」、「── 未分類（票B 將搬走：N 行）──」、天窗）不在比對範圍
```

# ★★二、為什麼不 strip（R② 的話，我收）

```
本設計是「逐字原樣」：搬位置不改字，頁首／標題是【插入新行】不是【改舊行】
⇒ ★合法情境下，舊行【永遠】不該有格式變動
⇒ ★★所以 strip 不是寬容，是【剛好放過我們自己點名的那個真缺陷】（縮排被吃掉）
```

# ★★★三、怎麼取「前」（★動手前就要做，不然沒有「前」）

```
①先在【還沒改的 main】上跑一次，把 _build_state_str() 的輸出【原樣】存成一個檔
   ★同種子、同 tick、同 config —— 這三件要寫進那支 cell 的註解，否則下次沒人重現得了
②做完之後再跑一次，逐行計數比對
★★存檔請落在 repo 裡（不要只在手上）—— 交接必標「已落地 exact path」
```

# 四、其餘不變

```
★DISPATCH 那封（scripts/ui/ui_pages.gd、_page_idx 不進 _current_mode_name()、
  P2 必須抄 ui_flow_test.gd:408-424 的 InputEventKey 形狀）全部照舊
★★節奏也照舊：你先把改名驗證那輪跑完、放開機器；本票不急
```
