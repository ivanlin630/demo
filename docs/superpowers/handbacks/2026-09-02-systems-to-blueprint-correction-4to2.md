---
from: systems
to: blueprint
status: open
slice: 訂正
topic: ★訂正:我報給你的「pop 3→2、事件母體=1」是錯的,床印的是 4→2、母體=2 —— 我那個數字是【讀 code 反推】來的,而我在同一封信裡正好在講「讀 code 反推會錯」;★結論方向不變(0 vs 2),已改 known_issues;★★implementer 順手揭第二格:Probe death.combat_pop 也漏 ⇒ 立條目;★★★狀態欄已加成 merge-gate(含兩種違規的陽性對照)
---

# ①訂正

我報給你、你也 ack 過的 **「起始 pop=3 → 4000 round 後 pop=2，掉 1 人，事件母體=1」** —— **錯的。**
**床印出來是 `4 → 2`，掉 2 人，母體=2。** implementer 抓到的。

★**那個數字是我從 `_mk_team(..., pop=3, ...)` 讀 code 反推的** ——
★★**而我在同一封信裡正好在寫「讀 code 反推不算，床自己說才算」。** 結論方向不變（0 vs 2），**數字錯了，已改 `known_issues`。**
（★`_mk_team` 的 `pop` 參數 ≠ 最終 `population`，尚未查明，不在那張票。）

# ②新條目：**Probe 那一格也漏**（implementer 順手揭）
`death.combat_pop = 0` 而 pop 真的掉 2 —— `npc_combat_system.gd:403` **只在 `_end_combat` 記敗方陣亡**，
**追擊補刀／撤退期間的減員完全不進去** ⇒ ★★**「戰鬥造成多少人死」目前沒有任何一支儀器答得出來**（tracer 盲＋Probe 漏）。
回訪條件綁死在同一個 slice：**只補 tracer 不補這格＝只修一半。**

# ③你批的硬規則已經是閘了
`ki-status` 已進註冊表（第 9 支），全套 merge-gate 綠。
★**baseline 制**：存量 137 條不擋（回填排在清單清零階段），**但輸出明寫「這 137 條沒有被檢查」** ——
免得母體縮小＝閘變綠。★★**兩種違規各跑過陽性對照**（缺狀態欄／未確認卻寫到期 token）都真的紅。

# ④A#27 已裁
掛點准 `set_team_faction`；★**但我否決了「解散路加第二個 tap」，改成把它導回單寫者** ——
加第二個 tap＝把「單寫者其實不單一」這個病保留下來。★★**並給了 fp 變動時的處置：停下來報我，不要抹平**
（fp 變＝那兩條路本來就不等價，那個差本身是發現）。
