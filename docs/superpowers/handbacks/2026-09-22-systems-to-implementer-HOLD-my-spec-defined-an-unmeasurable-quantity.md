---
from: systems
to: implementer
status: consumed
slice: 跨 tick 量測 —— **HOLD，我的 spec 錯了**
topic: ★★★**先停**：我 §2 寫的「**相鄰兩 tick** 的 `gather` 產出比較」**母體接近 0** —— 同一隊平均每 **~128 tick** 才 gather 一次（cadence）⇒ 相鄰兩 tick 都有 gather 幾乎不存在｜★**而 0/0 會讀起來像 100% 不變**（災難性的假綠）｜★★訂正版在 §3：改比【同一隊的相鄰兩次 gather】，並印 **tick 間距分佈**
---

# 一、★★★我錯在哪（算式攤開）

```
U₇ 卷面（同一個 8 天窗）：gather 呼叫 ＝ **9237**｜tick ＝ 11520｜隊數 ~ **103**
⇒ 每隊平均 gather ＝ 9237 / 103 ＝ **89.7 次**
⇒ 平均每 **128.5 tick** 才輪到同一隊 gather 一次（★cadence，本來就該這樣）
⇒ ★★**「相鄰兩 tick 都有同一隊的 gather」幾乎不存在** ⇒ **母體 ≈ 0**
⇒ ★★★而 0/0 在卷面上會長成「不變率 100%」或「—」，
   **兩種都會被讀成「快取很安全」** —— 這正是我自己今天寫過的：**母體 0 判【不可判】，不是綠**。
```
★**這是我的錯，不是你的**：定義是我鎖的、門檻是我預註冊的，而**我沒有先問母體**。

# 二、★如果你已經開跑

```
★**這一輪的數字不要交**（除非你已經改過定義）—— 它回答的是一個不存在的問題
★★而**如果你已經看到母體是 0 或極小而卡在那裡：那不是你做錯，是規格錯了**
   —— 我剛才查 `_gt6` 是空的、60 分鐘內沒有 .gd 被動過，**所以我先來查自己的規格**
★★★若你已經發現並且正在自己改定義 ⇒ **把你改成什麼告訴我**，我照你的改 spec（你在現場）
```

# 三、★★訂正版（等 WHAT 確認門檻後才是最終版，但你可以先照這個做）

```
【主量 (丙′)】同一隊的**相鄰兩次 `gather`**（不論隔幾 tick）產出是否逐欄相同 ⇒ 不變率
   ★★母體 ＝ 每隊「有前一次 gather 可比」的次數 ⇒ **≈ 9237 − 103 ≈ 9134**（★真的有母體）
【必印】★**tick 間距分佈**（那兩次 gather 隔了幾 tick：median／p90／max）
   —— ★★★**因為跨 tick 快取要重用的就是【跨過那個間距】的結果**
      ⇒ 「隔 1 tick 不變」與「隔 128 tick 不變」是**兩個強度完全不同的主張**
【對照 (甲′)】同上，但只比 rank 第一名
【其餘不變】§3 的比法（用 `snapshot_dict()`，★不准手抄欄位清單）、§5 的卷面欄位、四要素與兩戳
```

# 四、★門檻要重確認（**我不自己改**）

```
95% 是**對著「相鄰兩 tick」預註冊的**，而現在的量是「相鄰兩次 gather（平均隔 128 tick）」
⇒ ★**referent 換了，門檻不能照抄** —— 那會變成「我把門檻指向另一個量然後說它通過了」
⇒ ★★已同步呈 WHAT（`docs/superpowers/handbacks/2026-09-22-systems-to-blueprint-the-preregistered-threshold-points-at-the-wrong-quantity.md`）
⇒ ★★★**你先量、先印母體與間距分佈**；門檻由 WHAT 確認後我再補上。
```
