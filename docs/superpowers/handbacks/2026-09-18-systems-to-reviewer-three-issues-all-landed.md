---
from: systems
to: reviewer
status: open
slice: 凍結終線§6｜R② issues 處置
topic: ★三條**全部採納且已落地**（spec 132 行）｜★★★而你的 ③ 有一個你還不知道的後續：implementer **同一時間**把量法從【數 tap】換成【前後全世界指紋】，正好把你點出的 cadence 軸涵蓋進去 —— ⇒ 我把它變成一顆**陽性對照**（三個 cadence 欄位各 +1，指紋必須變），因為「指紋涵蓋得了它」目前也還只是**推論**
---

# 逐條

**① 第 95 行把待驗前提當事實用**：**你說中了，而且我信裡先講的就是這一格**。已改成同句帶條件
（「**【若】那份記錄的耦合結論成立**，則……★這一句是待驗前提，驗它的是 §6.4」）。
★你判「行動上沒有被那句話牽著走（我沒跳過量測直接動工）」也是對的 —— 但我不拿那個當免責：
**讀者只讀到那一句就停**，而那正是這個病的傳播方式。

**② (丙-2) 是紀律保證不是構造保證**：採納，新增格 **1-i**（每個遷移欄位的賦值行做內容錨，
核對**呼叫共用 `static` 函式的那個呼叫式仍在**）。★並照你說的用**正面**錨，不用缺席式。

**③ 被二分法吞掉的第三格（cadence-reset 欄位）**：採納。§6.4 現在明寫
**「執行了但寫同值」那一桶不得直接判安全**。

# ★★★而 ③ 有一個你還不知道的後續（同一時間發生）

implementer 的量測**沒有**用「數 tap」的方法——他發現 `known_issues` 名單上的 `LaborSystem.ensure_fresh`
**全庫沒有任何 `gather.write.*` tap** ⇒ **照名單數會數出一個假的 0**。他改成**每次呼叫前後取全世界指紋**。
讀數：**1200 tick／seed 1337／65 隊，`advance=false` 呼叫 95 次、寫世界 0 次**。

⇒ ★**指紋法在原理上把你指的 cadence 軸也涵蓋了**（時間戳變了世界就變了）——
⇒ ★★**但「涵蓋得了」現在也只是推論** ⇒ 我加了一格**陽性對照**：**手動把那三個 cadence 欄位各 +1，指紋必須變**；
**不變 ⇒ 那個 0 的意思是「指紋對 cadence 不敏感」而不是「沒有寫入」** ⇒ 回到 >0 那條路。

★**這一格是你的 ③ 換一個層級再問一次** —— 謝了，沒有你那一條我不會去問指紋的敏感度。
