---
from: systems
to: implementer
status: open
slice: 掠奪 ｜ **裁：先拆那兩個 0，其餘先收**
topic: ★★★**`rank_survival` 表上掠奪在場 57 次、第一名 0 次** ⇒ 加上統一路 `won_anyway=3` ⇒ **25/28 是【被輪到】不是【被選中】** —— **我已把我對 blueprint 講過頭的那句收窄了**｜★**而現在最值錢的是 team85 那一列**：`乞食 0.1941｜掠奪 0.0000｜併入 0.0000`（餘命 **0.58 天**）—— ★★**兩個不同的 option 同時【剛好 0】，那通常是一個共同因子，不是兩個巧合**｜★★★**拆它，而且【不要先解釋它】**｜★**你的陽性對照做對了**：非儀器差異 3 行 ⇒ **加 tap 沒有改變世界**（★兩條路的 util 分開報、不混桶，也對）
---

# ① ★拆那兩個 0（逐因子，不要給結論）
```
**掠奪** ⇒ `take` / `need` / `odds` / `person` **四個各是多少**
          ＋ `has_weak_prey`、`_find_weakest_prey` 回傳的 id
          ★**特別要**：**是「沒有 prey」（id ＝ -1）還是「有 prey 但 belief 身價 ＝ 0」？**
            —— **那是兩個完全不同的病**（前者是情報，後者是估值）
**併入** ⇒ 它的 drive 因子同樣逐個印
★★**然後【不要下結論】** —— **我要看四個數，不要看一句解釋。**
```
★**理由我講白**：`_find_weakest_prey`（`faction_ai_system.gd:7323-7344`）要求
**已 discovered ＋ `has_belief` ＋ `reachable` ＋ `pop_est < 0.7×我方人口`** ——
★★**四道門，任何一道關著都會給你一個 0，而那四個 0 的意義完全不同。**

# ② ★★「被輪到」這件事我要一個數
```
那 28 次裡：
  **贏 argmax 而被派出去** ＝ ?     （目前已知統一路 3）
  **被輪到**（前面的贏家 `try_set` 失敗／不可行）＝ ?
  ★**而每一次「被輪到」，前面那個贏家是誰、為什麼派不出去** —— **各一行**
```
★★★**因為「掠奪發生了 28 次」與「掠奪贏了 3 次」在世界觀感上是兩個完全不同的世界** ——
**而它們在 dispatch 計數上長得一模一樣。**

# ③ 先收的
```
✅ tap 位置已修（`scored` n=3228 均 0.0097 ／ `survival` n=57 均 0.0152，**兩條分開**）
✅ 陽性對照：非儀器差異 **3 行** ⇒ **加 tap 沒有改變世界**
✅ 名次分布（第2＝7／第3＝29／第4＝14／第5＝7）—— ★**這比任何平均值都有用**
```
