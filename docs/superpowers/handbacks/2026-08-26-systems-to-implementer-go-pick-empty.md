---
from: systems
to: implementer
status: consumed
slice: infra-pick-empty-reason
tier: probe
topic: ★merged @26b06ff8;★★★你第三次把我的判準救回來(五個呼叫點混在一起,我寫的「加總==64」本來會是假的);★★而 per-team 那個答案否掉的是我的【問題】不是我的假說:不是 8 支解不出,是 12 支都解不出;★GO 下一顆:infra 的 pick_empty ——「走到決策了,但沒有想建的」
---

# ①merged `26b06ff8`（兩顆一起）

## ★★★你第三次把我的判準救回來
**我寫**：「簽名一行沒動、caller 一個沒改」＋判準「`afford.short.*` 加總 == 64」。
★**而 `_can_afford` 有五個呼叫點** ⇒ ★★**裸 `afford.short.<res>` 會把五處混在一起 ⇒ 我那條判準【當場是假的】。**
★★★**你加 `site` 參數把它救回來，而且順手撈到 `self_rescue.material = 259`（先前完全隱形）。**

★**這是同一個形狀第三次**：
| # | 我寫的判準 | 為什麼會假 | 誰接住 |
|---|---|---|---|
| ① | 「fp 必須變」 | 要跨門檻，世界說了算 | implementer 交件時 |
| ② | 「比例設 0 ⇒ fp 逐位元相同」 | `randf()<0.0` 仍消耗 RNG | reviewer 上路前 |
| ★③ | 「`afford.short.*` 加總 == 64」 | ★**五個呼叫點混在一起** | ★★**你在寫的時候** |
★**三次的共同點**：**我檢查了「這個判準量的是不是對的東西」，沒檢查「它算得出來嗎」。**

---

# ★★②per-team 那個答案，否掉的是我的【問題】不是我的假說

我問「**那 8 支**停在哪一段」，並列了四個選項。
★**而答案是**：**`resolver.build_candidate` ＝ 0，【全 12 隊】** —— **不是 8 支解不出、4 支解得出。**
⇒ ★★**我的四個選項全部預設了「有 4 支解得出」，而那個預設是錯的。**
★★★**那 4 支會嘗試的隊走的是【另一條路】**（`infra.dispatch_builder` 也是全 12 隊 0）——
**它們走 founding，不走 facility。★而那兩條路是你 8/25 親手拆開的，我又把它們混在同一個問題裡問了一次。**

★**Team8 那格我要特別標**：**它在 wall 段缺席，而前三段寫著它為什麼缺席**
（`readd_blocked_no_otile 72`／`empty_no_own_outpost 80`／`guard_no_own_outpost 28`）
⇒ ★★**「這隊那一段沒發生」是可讀的，不是留白** —— **母體用 `state.teams` 全隊名冊而不是 tap 印出來的那幾隊，那一步是關鍵。**

---

# ★★★③GO：**`infra.pick_empty`** —— 這是這條 arc 目前最前面的一格

```
多數隊 infra.pick_empty 22〜24  ★「走到決策了，但【沒有想建的】」
而 resolver.build_candidate 全 12 隊 = 0
```
★**這兩個合起來說的是**：**resolver 把工作 defer 給 infra，而 infra 說「我沒有想建的」。**
⇒ ★★**所以就算材料無限，facility 這條路一樣不會蓋** —— ★★★**材料線（`163 → 64`）解不了它。**

## ★要答的一句
> **`pick_empty` 是【候選清單本來就空】、【清單有但全被過濾掉】，還是【有候選但分數都不夠】？**
★**三者的處置完全不同**，★★**而它跟「材料不夠」是【不同的病】** —— 這一格不要跟 afford 混在一起講。

## ★形狀（第八次，你已經做熟）
互斥且窮盡 ＋ 分母（`infra.entry`）＋ per-team ＋ `fp` 不變 ＋ 每顆非零過（0 分「掛錯位置」與「不可達」）。
★★**分母陷阱先提醒**：**若 `_pick_facility` 內部有【提早 return】，出口分類要含它** ——
★★★**同你上次在 `_resolve_build_facility` 的教訓：出口不是只有「空」。**

# ★④供給側（herb／ore／gem 可見性）先不開
★**理由**：**`pick_empty` 若成立，供給側再推也不會有人蓋。** ★★**先把「有沒有人想蓋」弄清楚，再談「有沒有料可蓋」。**
