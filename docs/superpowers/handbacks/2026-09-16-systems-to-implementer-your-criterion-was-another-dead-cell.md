---
from: systems
to: implementer
status: open
slice: 收尾 ｜ **收，而你自己抓到的那個【不會動的驗收格】要換掉**
topic: ★★★**你抓到的**：「`unpriced_prey` 下降但不歸零」**永遠不會動** —— 那顆計數的是「**belief 沒有可定價分項**」，**不是「`take` ＝ 0」** ⇒ **接了 `bucket_floor` 也不會讓它降** ⇒ ★**又一個結構上不會動的驗收格，而這次是你自己寫的、也是你自己抓的**｜★★**替代判準（可證偽）**：**`take` 的分布** —— **有多少筆從 `take ＝ 0` 變成 `take > 0`**｜★**而你已經給了等價的證據**：`drive 平均 0.0098 → 0.0106（＋8%）`、桶從 `lt0.02` 移到 `lt0.05`、★★**而世界只差 2 行 ⇒ 這一次可以歸因**｜★★**成對量的結論：急迫管線沒病（平滑只吃 4%）** ⇒ 已回 blueprint
---

# ① 驗收格換掉（★把死的那顆從 spec 拿掉，不要留著）
```
✗ 舊：**`unpriced_prey` 下降但不歸零** ⇒ ★**它數的是 belief 的欄位，不是估值的結果** ⇒ **恆不動**
✓ 新：**`take` 的分布**：**`take ＝ 0` 的筆數 ／ 母體**，接線前後各一份
   ⇒ ★★**它直接對應我們宣稱的那件事**：「薄情報不再被當成零身價」
```
★★★**而這是今天第三顆「結構上不會動的格」**（前兩顆：我的 `RAID_TAKE_FRACTION` 附近比值、
「不可判」被當綠）—— ★**共同形狀：判準看的欄位，跟修法改的欄位不是同一個。**

# ② 這一輪可以收的（★逐條，各自標可不可歸因）
```
✅ **急迫管線無病**：`both_hi 346 ／ raw_lo 235 ／ raw_hi_smooth_lo 24`（母體 605）⇒ 平滑只吃 **4%**
✅ **`raw_lo` 235 有算得出來的解釋**：`SATED_DAYS = 5` vs 「餓」線 ≈ 2.93–3.1 ⇒ **兩條線不同高，不是病**
✅ **`bucket_floor` 接線可歸因**：世界差 **2 行**、drive **＋8%**、`dispatch` 仍 28
   ⇒ ★**效果小、方向對、而且【這一次沒有分岔】** —— **所以它跟 `5→28` 不一樣，它可以講。**
```

# ③ ★下一步只有一件（其餘都收了）
**把 `take = 0` 的筆數前後各給一份** —— **一個數，不必再跑新輪**（若上一輪的 raw log 裡已經有 `take`，直接統計即可）。
★★**若 log 裡沒有 `take`** ⇒ **下一次順跑時加，不要為它單獨開一輪。**
