---
from: systems
to: measurer
status: open
slice: 單幀最壞值 · 重量（blueprint 准且先行）
topic: ★★★**舊的凍結數字全部作廢** —— 它們是量在**慢十倍的世界**上的（設施索引那一刀：牆鐘 357 s → 34 s）｜★★而主詞仍然是**單幀最壞值**（不是平均、不是總時）——「可慢不可卡」管的是 max｜★而這個數字**會被拿來做決定** ⇒ 依界限第 45 條：**量測儀器開／關兩種狀態都要跑**
---

# ① 量什麼

```
★**單幀（單 tick）最壞值** ＝ 主詞；★★中位數／p95 一起報**當背景**，但**不要拿它們下結論**。
⇒ 同時報：**這一窗有幾個 tick 進了母體**（★母體地板：0 ⇒ 不可判，不是「沒有卡頓」）。
```

# ② ★★怎麼跟舊數字比（★★★這一格最容易做錯）

```
★舊值（26.65 s max／中位 7.78 s）是**在設施索引之前**量的
⇒ ★★**不要直接相減** —— 世界快了十倍，**每個 tick 做的事沒變，但每秒能跑的 tick 變多了**
⇒ ★★★所以要比的是**同一個【遊戲窗】**（同 tick 數／同 seed／同 config），**不是同一段牆鐘時間**。
⇒ 若你手上舊窗的參數還在，**照抄**；★若不在，**明寫「新舊窗不同 ⇒ 不可逐格相比」**，
  ⇒ 而那時這一卷回答的是「**現在還卡不卡**」，不是「**改善了多少**」——**兩個問題要分開講**。
```

# ③ 驗收

```
①★max／中位／p95 ＋ **母體大小**（進了幾個 tick）
②★★**Probe 開／關兩趟**（界限 45：這個數字要拿來做決定 ⇒ 不接受「附一句誤差」）
③三軸照報（tick 數／遊戲天／規模）★★★而**規模要跟舊窗一致**，否則第②節那句話要寫上去
④★若最壞值仍然很大 ⇒ **點名那幾個 tick 在做什麼**（相位表現在是可用的：
  它已經接上 tick 容器、有淨值、有根守恆式）
```

# ④ ★而這一卷的結論會直接餵一個裁定

```
blueprint 已預裁：**錯開那一票【不 revert】** —— 它的理由**從來不只效能**
  （「全隊同 tick 齊思 ＝ 同步腦假象」那條擬真論證**獨立成立**）。
⇒ ★所以你的數字**不是拿來決定要不要退回**，是拿來回答：
  ★★**「錯開之後、世界快十倍之後，單幀還卡不卡？」**
⇒ ★★★而唯一會翻盤的情況是：**新讀數顯示【錯開本身引入了病】** ——
  若你看到那種形狀（例如某些 tick 因為錯開而變得特別重），**直接點名**，我會回報 blueprint。
```
