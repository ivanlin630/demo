---
from: systems
to: implementer
status: open
slice: `coin` 的價格 ｜ ★DISPATCH（blueprint 裁：排最前）
topic: ★spec：`docs/superpowers/specs/2026-09-15-coin-is-the-unit-HOW.md`｜★**先驗後修**：**若 `maintain_coin` 根本沒出現在 candidate 裡 ⇒ 停下來回報**（**那代表病在提名不在定價**）｜★★★**而「一行常數」的爆炸半徑我要你【窮盡列出】**：**全庫 32 處讀 `BASE_PRICE`** ⇒ **三分類、可對帳、不准有「其他」**｜★★**其中 `faction_ai_system.gd:4654` 是 `for res in BASE_PRICE:` 的逐鍵迴圈** ⇒ **加一個鍵會讓它多跑一圈** —— **那一處必須具名判**
---

# ① 為什麼一行常數要這麼小心

```
★**它看起來是「補一個漏掉的鍵」** —— ★★**而它實際上是【把一個一直是 0 的東西變成非 0】**
⇒ ★★★**所有「Σ 數量 × 價格」的地方，只要籃子裡有 coin，值都會變**
⇒ **而我們不知道那有幾處** —— **所以先列，再改。**
★**已知至少一處是我們要的**：`derived_payoff`（`:207`／`:220`）⇒ `maintain_coin` 不再恆 0
★★**已知至少一處要具名判**：`:4654` 的**逐鍵迴圈**（**加鍵 ⇒ 多一圈**）
```

# ② 驗收裡我要你特別看的那一格

```
★★★**`maintain_coin` 會不會【一上來就贏過一切】**
  ⇒ **它恆 0 了很久，而一旦有值，它的缺口可能很大**（`pop × 1.0 − stock`）
  ⇒ ★**若它突然佔據 argmax 的多數** ⇒ **回報，不要在本票裡壓它**
  ⇒ ★★**因為「壓它」會是 crank** —— **而正確的處置是問「為什麼缺口那麼大」。**
```

# ③ 三老症狀的讀數（★照 blueprint：只記方向）

```
**通縮／交易池／商人收益** —— **前後各一份**
⇒ ★**只記方向，不做歸因**（★★**單 seed 對照規矩** —— 而我們今天為它付過學費）
```
