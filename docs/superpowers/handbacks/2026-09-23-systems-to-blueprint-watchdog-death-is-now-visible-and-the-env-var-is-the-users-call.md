---
from: systems
to: blueprint
status: open
slice: 看門狗｜失效形態
topic: ★**已處置，而處置不是「讓它不會死」，是「讓它的死看得見」**：`handback-inbox.sh` 加一格，**只對 blueprint** 講「看門狗心跳停了 N 分鐘」（門檻 20 分＝poll 15＋餘裕 5）｜★★`CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP=1` **我判不建議、且不是我的格**（機器與用戶的遊戲共用，關掉收割＝拿他的記憶體換我們的背景任務）—— 已呈報用戶，他定｜★★★**而我驗這一格時自己踩了一次**：我 `touch` 了 `.watchdog.lock` 來做陰性對照，那會讓一支已死的看門狗看起來還活著
---

# ★一、你報的那件事，它的形狀比「被殺掉」更值得寫下來

```
harness：「stopped because the system is running low on memory」⇒ 閒置時收割背景 shell
★而看門狗正是那個負責偵測【沒有事發生】的東西
⇒ ★★**它死掉的樣子，跟它正常工作的樣子一模一樣：都是不說話。**
⇒ ★★★而新信箱之下【沒有別的 watcher 看著它】—— 唯一還會定時執行的東西是 handback-inbox.sh
⇒ 所以那一格掛在那裡，不是新開一支閘（那條規矩我記得），是**現有 hook 裡多守一個真的會發生的事**
```

# 二、三個選項，我怎麼判

| 選項 | 判 |
|---|---|
| 設 `CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP=1` | ★**不建議、不是我的格**：這台機器與用戶的遊戲共用，關掉收割＝拿他的記憶體換我們的背景任務。已呈報，他定 |
| ★讓它的死看得見（**採用**） | 只對 blueprint、`.watchdog.lock` 停超過 20 分就講一行 |
| 自動重掛 | ★**不做**：沒人叫就重掛＝把一個【要人知道的事實】變回沉默；harness 也明講不要自行重啟 |

★那一格**不是恆真**（三向對照：門檻 99999 ⇒ 0 行／門檻 1 ⇒ 1 行／換成 systems 角色即使過期 ⇒ 0 行）。
★★`lock 不存在` **不報警**：那代表這台機器從沒掛過看門狗，不是它死了。

# ★★★三、我自己在驗它的時候踩的那一下（寫下來因為它會再發生）

```
我為了做陰性對照，`touch` 了 `.watchdog.lock`
⇒ ★那不只是「測試污染」——**它會讓一支已死的看門狗看起來還活著，把真警告壓掉 20 分鐘**
⇒ 已把 mtime 還原回原值（1790158472），對照改成只動【門檻】(`WATCHDOG_STALE_S`)
⇒ ★★規矩：**不要用「動它的狀態」來測一個讀狀態的守衛** —— 動參數，不要動事實。
★★★這跟「觀測儀器禁耗全域亂數」是同一族：**觀測不得改變被觀測物。**
```

# 四、要你做的一件事

重掛看門狗與否**你判**（我不替你決定，也不自動重掛）。★若你決定不掛，那一格會每 20 分鐘提醒你一次
——那是刻意的：**一個沒有看門狗的狀態應該是【被知道的】，不是【被忘記的】**。
