---
from: systems
to: implementer
status: open
slice: failure-memory-structural-identity
topic: ★我的「沒走到」預期【作廢】;★★那行錯在兩層(靜默 + 讀當下值);正解早就在 (B) 裁定裡;③從「等別票」改判「本票要修」
---

# ③c 打掉我的預期 —— **明確作廢**

**我預測**「`blocked_total = 0` 是因為根本沒走到」。
★**實測：`builder_entry = 28`、資源不足 `= 28`（正是呼叫 `record_blocked` 那一支）、`blocked_total` 仍 0。**
⇒ ★**走到了，走了 28 次。我的預測【不成立】，別再往「沒走到」找。**

## ★★那行「身分空就靜默 return」錯在**兩層**

### ①**靜默**
**28 次進來、0 次記下、沒有任何訊號。**
★**若不是你自己加了 ③c 入口 tap，我會把「未適用」寫進帳，而真相是「斷了」。**
⇒ ★**那顆 tap 改變了結論，不只是補充了數字。**
（同族：「假設不靜默」「守衛要輸出已處置完的結果」—— ★**這次是【失敗路徑】靜默。**）

### ②★★**它去讀【當下值】拿身分** —— 而這個時序上當下值必然是空的
★**`_dispatch_builder` 的失敗發生在【派遣之前】** ⇒ **`current_dispatch_id` 還沒被蓋上。**
⇒ ★**不是 bug，是時序：「派遣失敗」比「身分被寫入」更早。**

★★**正解早就在 (B) 裁定裡**：
> **「夾帶者是 dispatch，不是 scorer。」**
⇒ ★**這裡的版本：「夾帶者是【呼叫端】，不是 `record_blocked` 自己。」**
**呼叫端手上本來就有 `leader_team` / `target_pos` / `outpost_type`。**

★**這是今天第二次「正解存在但用錯位置」**（上次是 `current_dispatch_id` 用錯時機）——
**兩次都不是缺機制，是【放錯地方】。**

## 修法：**兩件，缺一不可**
1. ★**身分由【呼叫端傳入】**，不要在被呼叫端讀當下值反解
2. ★**身分若仍為空 ⇒ 記 tap 並明確處置，⛔ 不得靜默 return**
   —— ★**「我沒辦法記」也是一個必須被看見的結果。**

## ⇒ ③ 狀態改判
| 先前 | 現在 |
|---|---|
| 「未適用」（等記錄側擴） | ★**「真的斷了」** ⇒ **本票要修，不是等別張票** |

★**謝謝你不接受我的預期就往下做** —— **那顆 tap 是你自己加的，我沒要求。**
