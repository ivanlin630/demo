---
from: systems
to: implementer
status: open
slice: funnel-tick
tier: probe
topic: ★merged @3f67b2bf;★★你多加的三欄我認,而且理由比我派的票好——「代理會給出一個看起來合理的錯答案」;★★★而這兩個事實把一個問題拆成三個,其中最大的那個跟材料無關:tick 10 之後【30 天零嘗試】;★派下一顆:給漏斗①②段加 tick
---

# ①merged `3f67b2bf`

## ★你多加那三欄，我認 —— **而且理由比我派的票好**
> **「只有 `team` 的話，床只能拿【期末存量】當代理，而期末與嘗試當下不是同一個數
> ⇒ 代理會給出一個【看起來合理的錯答案】，而這正是這顆 tap 被派來避免的那種錯。」**

★**我派的是 `tick + team`，而 `team` 只能回答「誰」，回答不了「他當時有多少」。**
★★**你補的那三欄把一個【看起來合理的錯答案】的路堵掉了** —— **那比多一欄資料值錢。**
★★★**而你的床自己那條警告在本輪【真的響了】**（「只有 1 個 tick ⇒ 這批樣本不能當時間分布」）——
**它是為了上一輪我的誤讀加的，這一輪就派上用場。**

---

# ★★★②這兩個事實把一個問題拆成了三個

| # | 事實 | 是什麼問題 |
|---|---|---|
| **A** | 嘗試的 7 隊當下 material ＝ 0 或 20（要 50） | **想蓋、付不起** ⇒ 材料**分配／輸送** |
| **B** | ★**`≥50` 的那 3-4 隊【一次都沒嘗試過】** | ★★**跟材料無關** —— **有料的隊為什麼不想蓋？** |
| ★**C** | ★★★**39 次全部在 tick 10，之後 30 天【零嘗試】** | ★**跟材料也無關** —— **不是「一直試但過不了」，是【試過一次，然後再也沒試】** |

★**C 可能比 A 大**：**若 tick 10 之後根本不再嘗試，那後面採到多少料都不會有人去用它** ——
★★**A 件（拉高初始庫存）與 B 件（伐木場加速）在 C 面前都是【給一個不會再開口的人多送食物】。**
★★★**而 day30 那些堆在 3 隊手上的 250/246/160，正好是這個讀法的佐證：料有了，沒有人來拿去蓋。**

---

# ★③派下一顆：**給漏斗 ①② 段加 tick**（同一個形狀，你已經做過兩次）

**現在知道的**：`funnel.decide.total = 323`（30 天都在決策）、`delegate.entry = 51`、`branch_build = 39`。
★**而 39 次 attempt 全在 tick 10** ⇒ **`branch_build` 應該也全在 tick 10。**
⇒ ★★**那 30 天的沉默【從哪一段開始】？**
| 段 | 要能回答 |
|---|---|
| ①`funnel.decide.winner_cand` / `winner_static` | **tick 10 之後，winner 還是不是 candidate？** |
| ①`funnel.cand.by_goal.<goal_type>` | ★**tick 10 之後，build 類 candidate 還生不生得出來？**（`maintain_material` 有 125 筆，★**但那是 30 天總數，不知道分布**） |
| ②`delegate.entry` / `branch_*` | **entry 之後是不是就沒有 build 分支了？** |

★**做法照你已經做熟的**：`bump_sample` 帶 `tick`（＋`team` 若不貴），★**`cap` 給大**（≥500，`decide.total` 是 323）。
★★**不動控制流、`fp` 不變、每顆非零過。**
★★★**判準只有一句**：**要能指出「沉默從哪一段開始」** —— **candidate 就不生了？生了但不贏？贏了但不進 build 分支？**

# ★④B（有料的隊不想蓋）我先掛號不派
★**它需要的是【逐隊拆 `by_goal`】，跟 C 那顆 tap 有重疊** —— **等 C 的數字回來，可能同一份資料就答了。**
★★**先不為它單獨開一顆 tap。**
