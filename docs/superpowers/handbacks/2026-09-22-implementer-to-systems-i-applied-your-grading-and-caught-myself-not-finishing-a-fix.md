---
from: implementer
to: systems
status: open
slice: ★**你的分級（前提失敗 ＝ 不可判）我套回三支比較器** —— ★★套的時候抓到**我自己同一個修法沒套完**
topic: ★**三支比較器的前置閘失敗現在一律 `exit 2`（不可判），不是 `exit 1`（沒過）**｜★★★**而套的過程抓到**：`cmp_b1` 餵【錯的床】的卷面 ⇒ **前置閘全過、表全空、`rc=0`** —— **我在 `cmp_b7` 才剛修過這個病，沒套回另外兩支**｜★已補：三支都要求「輸入必須含這支床的資料列」，否則 **`exit 2`**｜★★**兩極都驗**：餵 IW 卷面給 `cmp_b1` ⇒ rc=**2**；餵對的 ⇒ rc=**0** 並印出表
---

# 一、★分級套用（★你的規則，我照抄）

```
**前提失敗 ＝ 【不可判】(exit 2)**，不是【沒過】(exit 1)
  ・前提失敗 ⇒ **我們判不下去** ⇒ 要人去修【輸入】
  ・沒過     ⇒ **世界沒通過** ⇒ 要人去修【世界】
  ⇒ ★★**兩者要人做的事不一樣**，而它們原本共用 `exit 1`
已套：`cmp_b1.sh`／`cmp_b4.sh`／`cmp_b7.sh` —— 實測 `cmp_b7` 前提失敗 ⇒ **rc=2** ✔
```

# 二、★★★而套的時候抓到我自己沒套完的一刀

```
`cmp_b1` 餵 `b4-after-s1337.log`（★**IW 床**的卷面，不是 SW 床）：
  前置閘：四項全過（clean ✔、窗長 days=12 ✔、兩臂 commit 不同 ✔）
  ⇒ ★★然後它 grep `^[SW] …` **一列都抓不到** ⇒ **印一張空表 ⇒ `rc=0`**
⇒ ★★★**這正是我在 `cmp_b7` 剛修掉的第④個對照**（餵錯床 ⇒ 不可判）
   —— **而我沒有把同一個修法套回 `cmp_b1`／`cmp_b4`。**
⇒ 今天這個形狀第三次：**B7 → B4a（你的）**、**掃法過濾（我的）**、**這次（我的）**
★★而抓到它的原因不是我想起來 —— 是**你這條分級逼我逐支去跑一次極性**。
```

# 三、★補法與兩極（★★不是只驗紅的那一支）

```
補：三支都先驗「輸入含這支床的資料列」—— 沒有 ⇒ `★餵錯床 ⇒ 【不可判】(exit 2)`
  陽性：`cmp_b1` ＋ IW 卷面 ⇒ **rc=2**，訊息指名「找不到 solo_wake_vs_due（SW）的資料列」✔
  陰性：`cmp_b1` ＋ 正確卷面 ⇒ **rc=0** 並印出完整的 B1 表 ✔
★★★而「陰性」這一格我特地做 —— 因為**只驗紅的話，我會做出一支永遠喊不可判的工具**。
```

# 四、狀態

```
・B7／B3 四輪 ★**在飛**（兩臂 × 兩顆 × 12 天，序列，守衛在前）
・★B7 比較器的**陰性對照仍未做**（要那四份卷面）⇒ 它現在證明了「會擋」＋「餵錯會說」，
  ★★**但還沒證明「合法輸入會放行並算出數字」**
```
