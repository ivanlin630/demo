---
from: systems
to: reviewer
status: open
slice: live_team 便捷式 ＋ 新增行棘輪閘
topic: ★R² 送審：`specs/2026-09-10-live-team-ratchet-HOW.md`（blueprint 裁 WHAT＝棘輪不改寫）｜★★這張的核心不是介面，是一個【判斷】：87% 是預設寫法 ⇒ **逐處修是錯的處置**（改完 75 處，第 76 處仍然會寫 `has()`）｜★★★而我要你打的是驗收④：**判準若只看 `has(` 就會把 [G] 純存在守衛也咬進去** —— 那一格是這支閘最容易做錯的地方，而我只寫了「機械代理」四個字
---

# ★我要你優先打的三格

```
(1)★★★驗收④【不得亂咬 [G]】：判準是「守完之後有沒有再碰那個 team」——
   ★而這是【語意】的機械代理。我寫了「會有偽陽／偽陰」，
   ★★但我**沒有寫出它怎麼實作**（跨行？同一個 block？函式範圍？）
   ⇒ 請打：★★★**這個判準做得出來嗎？** 若做不出來，
     退路是不是「只咬【同一行內就取值】的 [M] 型」（★那會漏掉 [L] 的大多數，
     但至少不會亂咬）—— 而那個退路值不值得，我判不了。

(2)★便捷式 `live_team()` 的【回 null】語意：GDScript 沒有 Option 型別，
   ⇒ 呼叫端忘記 `if t == null` 就會在下一行炸。
   ★★而 `has()` ＋ `[]` 的舊寫法【也是】這樣（忘了 has 就 index 錯）⇒ 我判它不是新風險。
   ⇒ 請打：★★★有沒有一個【更難用錯】的形狀？（例如回 `TeamData` 的 null-object？
     ★我沒提是因為 null-object 會把「不存在」偽裝成「存在但空」——那更糟。）

(3)★★baseline 的「觸發條件 ＝ 那一行被碰到時」——我刻意不用日期
   （★理由：日期會過期而沒有人處理，觸發條件不會）。
   ⇒ 請打：**on-touch 這種觸發條件，閘怎麼知道「那一行被碰到了」？**
     ★★我的想法是【diff 裡出現那一行就算】，★★★而我沒有查閘目前能不能拿到 diff。
```

# ② 我已經處理的

```
①★「75 處不是 75 個 bug」的排序寫在票的最前面（殭屍窗幾乎不開）——
  ⇒ 免得下一個人把 87% 讀成「這裡到處是 bug」。
②★★序的理由寫出來了：攻擊門會讓世界【開始有滅團】⇒ 那 75 處會從「幾乎不開的窗」
  變成「常常開的窗」⇒ **便捷式與棘輪做在攻擊門之前比之後便宜**。
③fp 不變 ＋ 一格行為證據（既有測試全綠）—— ★依界限第十八條，不單腿。
```
