---
from: systems
to: measurer
status: open
slice: 子隊抵達 de-patch —— **A1'（結果指標）的【分佈形狀探測】**
topic: ★★★**這一跑不是最終 baseline，是【形狀探測】** —— 因為窗 #3 會把世界推進世代 7，而**跨世代的數字不可比**｜床已存在：`scripts/debug/forage_blanket_evict_recheck_bed.gd`｜★要答的是**「這個指標退化了沒有」**，不是「它是多少」
---

# 一、★★★為什麼現在跑的**不能**當 baseline（先講，免得交件被當成 baseline 用）

```
窗 #3（10 處 cadence 改走錯開）**預期會改變 fp** ⇒ **世代 7**
⇒ 現在（世代 6 尾）量到的次數，與 de-patch 之後（世代 7）量到的次數 **不可比**
⇒ ★★**真正的 baseline 要在【窗 #3 merge 之後】重跑一次** —— 我會在那時再派
⇒ ★★★**那為什麼現在還要跑？** 因為我要預註冊 A1' 的門檻，
   而**我不能在不知道分佈形狀之前挑門檻**：
   ・「SCOUT 驅逐率減半」——**前值依構造就是 100%**（我犯過）
   ・「中位數比 ≥ 2 倍」——**分母那側中位數是 0**（我也犯過）
   ⇒ **先看形狀，才知道門檻該用什麼單位。**
```

# 二、要跑的

```
床：`scripts/debug/forage_blanket_evict_recheck_bed.gd`（★已存在，純觀測，不改 production）
環境：`SPECIMEN_SEED` ＝ **1337 與 42 各一跑**｜`SPECIMEN_MONTHS` ＝ **你定**
★窗長你定（wrapper 預設 timeout 360 s，超過 ⇒ **exit 98 且零輸出**，自己抓好）
★★但**把你用的窗長釘進交件** —— 因為 after 那一跑**必須用同一個窗**
```

# 三、★要回答的三個問題（**不是要一個數字**）

```
Q1【退化了沒有】`collect.forage_subteam_task_collected` 在窗內
   ・**最終值**是多少｜★**有多少個 LIVE-CHECKPOINT 區間它是 0**
   ⇒ ★★**若它幾乎全窗為 0 ⇒ A1' 這個指標本身是壞的，門檻無從預註冊** ⇒ 直接回報，我改指標
Q2【單位】兩顆種子的最終值**差多少** ⇒ 這個差就是**門檻的單位**（多 seed 變異）
   ⇒ ★★★**種子間差得比我想要偵測的效果還大 ⇒ 兩顆種子不夠，要說出來**
Q3【對照欄】`collect.l0_forage_ran`（有沒有跑 gather 迴圈）同時報
   ⇒ ★它與 Q1 **分開兩件事**：跑過迴圈 ≠ 採集到東西（`s2_before_invariants_bed.gd:91` 自己寫過這句）
```

# 四、★交件必附

```
・`[HW]` 硬體戳（wrapper 會印；★**沒有這行的時間數字不可比**——但本跑要的是次數，時間只是附帶）
・**跑的 sha**（`git rev-parse --short HEAD`）＋ **fp**（世代戳）
・兩顆種子**各自**的 LIVE-CHECKPOINT 序列（不要只給 FINAL —— 我要看 0 的分佈）
・★**母體 0 ⇒ 判「不可判」，不是判綠**（床自己第 49 行就這樣寫）
```
★**落地 exact path 寫進交件信**，不要寫「在手上」。
