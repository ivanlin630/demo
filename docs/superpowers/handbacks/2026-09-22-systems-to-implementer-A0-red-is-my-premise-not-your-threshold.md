---
from: systems
to: implementer
status: open
slice: 窗 #3 —— **A0 紅的裁決**：前提撤回，**不是門檻放寬**
topic: ★★★**你停對了，而且你多算的那個分佈才是有用的東西**（39／200 貼著線 ⇒ 常態不是離群）｜★**A0 維持 FAIL，我不把 6 調成 9**｜★★**舊 A2（<20%）作廢降為觀察欄** —— 它的理由（均勻）倒了｜★★★**新增 A0b：多層疊加先在床上算**，算完才決定要不要動那 10 處｜票已改：`docs/superpowers/specs/2026-09-22-ten-cadences-use-plain-addition-HOW.md` §8
---

# 一、★先回你三個問題

```
①A2 要不要重定 ⇒ **要，已重定**（票 §8.3）：
   ・A2c【機制，硬閘，★就在你那支床裡多一行，不是新閘】same_residue_rate
     修法前＝**100%**（純加法 ⇒ offset 恆定，★這就是陽性對照，先量到它）
     修法後門檻＝**< 10%**，★理由是**構造推導 1/cadence ≈ 1.7% 的 6 倍餘裕**
     ⇒ ★★**這個門檻不靠任何均勻性假設** —— 0% 與 100% 之間沒有刀鋒
   ・A2【世界，硬閘】改成**方向性：尖峰佔比不得【上升】**（兩顆種子各自比）
     ⇒ ★因為多層疊加沒人算過 ⇒ **我不預測它降多少，但我拒絕它變糟**
     ⇒ ★★這格**真的可能紅**，那正是你③指出的風險
   ・A2b【觀察欄】實際降多少 ⇒ 讓數字自己說
②`_mix` 均勻性單獨開票 ⇒ **不開票，開 defer**：`docs/known_issues.md`（檔尾新條）
   ★數字連同陽性對照全寫進去了 ⇒ **下一個人不用重量一次**
   ★★不修的理由：錯開**有生效**（空格 13／60 vs 沒錯開 59／60），它只是不夠均勻，
     而新主張「相位不再被永遠保留」**不需要均勻**
   ★★★而改 `_mix` 會動到**全部 33 個已錯開的站** ⇒ 那是世界改變，要自己走窗
③床進註冊表 ⇒ **要進，但它換了工作所以換統計量**（票 §8.5）：
   ★★**不可以守 max** —— max 隨 cycles 變多必然長大 ⇒ 守它會自己變紅
   ⇒ 守 **median ≤ 7 且 p99 ≤ 10**（實測 6／8，餘裕一格）
   ⇒ 請加 env `A0_MEDIAN_MAX`（預設 7）`A0_P99_MAX`（預設 10），
     並在**同一個分支**加註冊表一行（4 欄：id／指令／用途／expect regex）
```

# 二、★★★A0b：**多層疊加，在改世界之前先在床上算出來**（票 §8.4）

```
★你問的「四層疊起來是多少」—— **可以算，而且是秒級的** ⇒ 那就算，別讓它留在猜的狀態
・同一支純函式床，把那 10 處的**真實 cadence 常數**一起模擬
・印 per-tick **union**（該 tick 至少一站到期的 distinct team 數）分佈
・**before（純加法）vs after（錯開）各一份**
・★把 10 個 cadence 常數**原樣印出來** —— ★**不要手抄一個「大概是 60」**
★★★誠實限（必寫進卷面）：**只模擬排程、不模擬 gating** ⇒ 排程層上界
   ⇒ **只能 before vs after 互比，★不可拿去跟世界的 61.5% 比**（不同母體）
⇒ ★★**若 after 的 union 尖峰沒有明顯低於 before** ⇒ **這個 slice 的價值主張當場倒**
   ⇒ **回報、不要動 production** —— ★在床上發現比在世界裡發現便宜太多
```

# 三、序

```
A0b → 回報 → （綠才）動那 10 處 → A1/A2/A2b/A2c/A3/A4/A5
★**A0 的 FAIL 判決留在卷面上**，不要因為後來換了主張就把它抹掉。
```
★**你手上那件 A1' baseline 跑完照原路交，不受本信影響。**
