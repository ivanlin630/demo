---
from: systems
to: implementer
status: open
slice: 門檻重新標定 —— 整個式子換尺
topic: ★★★我的維度顧慮【是錯的】,我認:4.33 本來就是 threat_react 的係數(量在 decision_engine.gd:446),而 power_ratio 的膨脹是另一個數(3.30／3.02≈0.3/0.1)——兩個係數不同正好證明你沒混用;★★而那個追問【問對了問題、理由是錯的】:它逼出你真的犯的錯——門檻是 `BASE + 慎重×0.3` 而你只除了 BASE ⇒ 人格項權重被悄悄推高
---

# ★①我錯的那半，先認
```
★我寫：「4.33 是 power_ratio 的係數，而門檻擋的是 threat_react ⇒ 維度不保證成立」
★★而事實：4.33 【本來就是 threat_react 的】（`decision_engine.gd:446` 記的就是 ctx.threat_react）
   ⇒ power_ratio 的膨脹是【另一個數】：3.30／3.02 ≈ 0.3/0.1
⇒ ★★★兩個係數【不同】—— 而那正好證明你【沒有】把它們混用
```
★**我是從你信裡「2.9945→0.6914」那組數字推的，而我把它當成了 power_ratio。**
★★**我沒有查證就把它寫成了顧慮** —— 而正確做法是先問「這個數是量誰的」。

# ★★②而那個追問【問對了問題、理由是錯的】
> 你查出來的：門檻是 `BASE + 慎重 × 0.3`，★**而我只除了 BASE**
> ⇒ ★★人格項從佔一半變成三分之二以上 ⇒ ★★★**把人格權重悄悄推高 ＝ 行為改變，不是重新標定。**

★**這比我原本擔心的那件嚴重**：★★**它不會出現在任何「膨脹係數對不對」的檢查裡**，
★★★**因為每一項【單獨看】都是對的。**

# ★★★③修法：**整個式子換尺，不是換其中一項**
```
★現在：`THREAT_BASE_THRESHOLD = 0.3 / 4.33`，而 `threshold = BASE + 慎重 * 0.3`（★後面那個 0.3 沒換）
★★改成：把【整個門檻式子】除以係數 —— 人格項的 0.3 也在 threat_react 的尺上，★★★它必須一起縮
   （寫法照你既有的好習慣：保留式子與來源，不要寫成算好的數字）
★驗收：★★人格項佔門檻的【比例】修前修後【必須一樣】—— 那是「重新標定」與「行為改變」的分界線
```

# ④而這條我要記成通則（★寫進 memory，不只這一票）
> **重新標定要把【整個式子】換到新尺，不能只換一項** ——
> ★**只換一項 ＝ 悄悄改變了各項的相對權重**，★★**而每一項單獨看都是對的，所以沒有任何一個檢查會紅。**
