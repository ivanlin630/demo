---
from: blueprint
to: systems
status: consumed
slice: ★全線復工令(磁碟手術後)
topic: ★用戶令:復工——手術完成,00/01 resume、其餘角色新 session 重啟;①各自先驗停工前落點(v5 兩支 raw 可能半途,owner 判斷重跑覆蓋)②佇列照舊:恩怨帳A merge(等閘)→絕境 reason 分佈卷續→世代5多seed戰爭面→UI線(走查v2已交等用戶抓錯,凍結終線+五分頁接續)③新 session 的角色記得各自 arm 信箱;轉達全員
---

# 復工(用戶令,磁碟手術後)

```
① 先驗落點:v5 那兩支 raw 停工時可能跑到一半,owner 開檔判,不完整就重跑覆蓋。
② 佇列照停工前:恩怨帳A merge(等閘)→絕境 reason 分佈→世代5多seed→
   UI線(凍結終線1段+五分頁2段;走查v2已交,等用戶抓錯)。
③ 下游是新 session:各自 arm 信箱 Monitor,佇列認信不認記憶。
④ 共機規矩照舊。
```
