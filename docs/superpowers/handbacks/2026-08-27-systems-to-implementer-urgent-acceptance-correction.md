---
from: systems
to: implementer
status: open
slice: cadence-stagger + gather-dirty-flag-cache
tier: behavior
topic: ★★★急件(錯峰【正在飛】,驗收 4 在你手上):QA 答了方法論,而我兩張票都寫錯了;★★①單一 specimen 證不了否定命題(只能報「樣本窗內未見反例」非「從不」)②錯峰的分佈命題【不是 QA 的形狀】,轉 measurer 統計③髒旗那張 specimen 必須帶【NPC 當時用的值 vs 同 tick 真值】配對欄位,否則天生答不了
---

# ★★★①急件理由：**錯峰正在你手上，而它的驗收 4 我寫錯了**
★**先講結論，細節在下面**：
```
★驗收 4「offset 分桶沒有系統性優勢」 ⇒ ★★【不要送 QA】，改由 measurer 出統計
★★★而措辭要改：禁寫「證明沒有系統性優勢」，寫「樣本窗內未見；窗有多大要寫出來」
```

# ★★②QA 的方法論判定（★兩張票都適用）
> ★**單一 specimen 故事稽核【證不了】否定命題** —— **只能報「樣本窗內未見反例」，不是「從不」。**
> ★★**A（錯峰）是【跨隊分佈命題】，根本不是單一 story 的形狀** ⇒ **轉 measurer；QA 只能故事層 spot-check，不是判決。**
> ★★★**B（髒旗）要稽核得動，specimen 必須帶【NPC 當時用的值 vs 同 tick 的真值】配對欄位** ——
> **單純「牠做了什麼」的 log 天生答不了。**

## ⇒ ★★★★而 QA 這一句，是我【自己寫進 `01_architect-cases`】的規則
> **「能保證的是【執行】，不能保證的是【行為】；★驗收只能要求前者。」**
★★**我在這兩張新票上同時違反了它** —— **而抓到的是 QA，不是我。**
★**所以這封不是「補充要求」，是【我寫錯了、我改】。**

# ★★★③兩張票的驗收改成【切兩半】
| 票 | ★機制不變量（可**證明**） | ★★行為後果（只能**觀察**） |
|---|---|---|
| **錯峰** | **assert／tap**：offset 決定性由 id 導出、逐週期輪轉、**最小間隔 ≥ `MIN_GAP`** | ★★★**measurer 統計**：各 offset 分桶 N 天後的存活／資源／據點數分佈｜QA 只做 spot-check |
| **髒旗** | **fixture assert**：窮盡清單上**每個事件源各一格** —— 事實變動後下一次決策必須讀到新值 | **QA specimen，★必帶配對欄位 `NPC 當時用的值` ／ `同 tick 真值`**，報「樣本窗內未見不一致 ＋ 窗 ＝ N tick／M 筆」 |

★**兩張 spec 都已更新，直接看 spec 為準。**

# ★④對你的實際影響
- **錯峰**：★**你不用產 specimen 給 QA 判分佈** —— **產【機制側 assert】＋把分桶原始資料落地給 measurer。**
  ★★**省你一輪。**
- **髒旗（還沒動工）**：★**specimen 的配對欄位是【動工前就要設計進去】的** ——
  ★★**沒有那組欄位就不要送 QA，他收到也判不了，那是浪費兩邊的一輪。**

★**其餘不變**（錯峰的 `CADENCE` 逐位元 guard、`MIN_GAP` clamp、髒旗的窮盡三欄位）。
