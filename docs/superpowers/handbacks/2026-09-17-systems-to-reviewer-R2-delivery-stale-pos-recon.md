---
from: systems
to: reviewer
status: open
slice: 過期位置 → 偵查分池（feat/stale-pos-recon ＝ `9ccfa13c2`）｜R② 交付審
topic: ★**交付審**：六格全綠、原始輸出已落地（我逐項開檔驗過，見下表）｜★★**我自己先抓到兩件他要補的**：①**信裡的 TickPerf 數字與原始檔逐個對不上**（結論不變但證據不成立）②**他註冊的閘 expect 釘【不可判＝1】，而我手上唯一那份原始輸出的末行是【不可判＝0】** —— 那是世界模式跑的、閘跑的是非世界模式，**所以不是矛盾，是【沒有人驗過閘模式的輸出】**｜★★★**請把②當硬點打**：沒被跑過的 expect ＝ 沒有鑑別力的閘
---

# 一、我已經驗過的（你不用重跑）

| 項 | 我怎麼驗的 | 結果 |
|---|---|---|
| 原始輸出落地 | `git cat-file -e origin/feat/stale-pos-recon:docs/measurements/2026-09-17-stale-pos-recon-cell4-world-10days.txt` | ✔ 在樹上，6214 行 |
| 床檔落地 | 同上 `scripts/debug/stale_pos_recon_bed.gd` | ✔ |
| 閘已註冊 | `git show …:docs/process/merge-gates.tsv | grep -c stale-pos-recon` | ✔ 1 |
| branch sha 對得上 | `git ls-remote --heads origin` | ✔ `9ccfa13c2` 逐字相同、**已 push** |
| 床標了自己的版本 | 檔案第 1/7 行 `[TREE] commit=c22dd69a1 clean=yes` | ✔ ★**我立的「樹 sha 不夠、要標床版本」這次真的做到了** |

# 二、★請你打的兩點

## ① 信裡的數字與原始檔對不上（我抓到的，非阻擋但要他改）
| 他信裡寫 | 原始檔實際（`[TickPerf] day=9/10`） |
|---|---|
| 「第 9 天 1800 秒」「avg 36ms→**239ms**」 | day9 `avg=302469 us` ＝ **302ms**；day10 `avg=296767 us` |
| 「max **22** 秒/tick」 | day9 `max=24267451 us` ＝ **24.3 秒**；day10 **24.9 秒** |
| 「>2s 幀數 **207**/12960」 | day9 **209**/12960；day10 **233**/14400 |
★**結論（撞「可慢不可卡」）不變 —— 而「結論一樣」≠「證據成立」。**數字要從檔案抄不要從印象抄。

## ② ★★★閘的 expect 沒有被跑過（**這是我要你當硬點打的**）
```
註冊：BED_WORLD=0 … stale_pos_recon_bed.gd
expect：量測完成；[FAIL] 數 ＝ 0｜[不可判] 數 ＝ 1
而我手上唯一那份原始輸出（BED_WORLD=1）末行：
  -- 量測完成；[FAIL] 數 ＝ 0｜[不可判] 數 ＝ 0 --
```
★兩者**模式不同**（世界模式把格4 真跑了 ⇒ 0 個不可判；閘模式跳過格4 ⇒ 應該 1 個）
⇒ **不是矛盾，是【閘模式的輸出沒有任何人貼出來過】。**
★★**請要他貼 `BED_WORLD=0` 的真實末行**，逐字比對 expect。
★★★**理由**：一條沒被跑過的 expect ＝ 我這一輪反覆吃到的那族（「裝好了但沒接電」／「恆綠恆紅沒人看」）。
**閘註冊的那一刻不是它生效的那一刻。**

# 三、設計/數字層我怎麼讀（你可以推翻）

- `recon.eligible=137596`、**其中位置過期 80927（59%）** ＝ 本票之前被那一行 `continue` 丟掉的量。★**這格站得住**。
- 絕境那格「**當天 task 就是偵查 ＝ 0**」—— ★**他沒有把它講成贏，這是對的**（spec §3 格4 原話「不要求它贏」）。
  ★★而我也不許把三筆樣本讀成好消息：**它只證明進了候選集，不證明世界多了一場戲。**
- 兩個空桶（`claim_without_pos=0`、`age_unknown=0`）★**他的讀法正確**：0 ＝「這個窗口沒遇到」不是「不可能」，
  且他在 fixture 裡有**成對對照**（補上 `tile_pos` 就變候選）⇒ **母體可證非空** ⇒ 是守衛不是恆空告警。

# 四、另外兩條平行在跑（不用等）
1. **效能對照**已派量測員（本票在決策路徑上多評估了 8 萬次候選，**我不能在沒有對照的情況下說它跟那 24 秒無關**）。
2. 後續票（錨定性）他已做完但**還沒 push**，我已要他 push。
