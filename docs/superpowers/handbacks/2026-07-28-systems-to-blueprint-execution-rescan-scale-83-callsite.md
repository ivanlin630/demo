---
from: systems
to: blueprint
status: consumed
topic: "[執行層重掃坐實規模·★83 TaskArbiter call site(你估~29 遠低估)+34 持守機制(27+7表外)+COMMITMENT_BONUS 零寫回確認(R①兩層無共讀坐實=執行層要建通路真build)·規模認知再上修但『真build如means-end』方向不變、用戶裁乙站·HOW大arc spec+slice post-compact(重工fresh context)] investigator 重掃補齊 27→83 call site/34 機制,確認執行層持守=真 build。proceed 中,HOW spec 完整化 post-compact。"
---

# 執行層重掃坐實規模（83 call site，真 build 確認）

investigator 重掃補齊（proceed 中，blueprint go）：

## ★規模再上修（但方向不變）
- **A) TaskArbiter 83 call site**（try_set/transition/release）——★你估 ~29 **遠低估**（faction_ai 39+interaction 15+outpost 8+player_cmd 6+其他）。核心真改點 = `task_arbiter:38 try_set`（整數 tier 比較）+ `:65 engine self-replace`；83 中多是 release（清 task，未必改）。
- **B) 34 持守機制**（27 已知 + 7 表外：decision cadence/consistency coeff/survival boost/threat boost/same-need fallthrough/idle-filler/beggar restore）。
- **C) COMMITMENT_BONUS 零欄位寫回確認**（R① 「兩層無共讀通路」坐實）→ 執行層要**建**寫回通路（真 build）。cadence 新鮮度落差 3 點。

## 意涵
- **「執行層持守=真 build 如 means-end 新子系統」確認**（83 call site 重寫仲裁 + 建跨層通路），用戶裁乙站得住、規模認知誠實（R① 該過的方向對）。
- 規模比你估大（83 vs 29），但**不改決策**（已裁真 build）；影響 **slice 粒度**（83 call site 要先分「真需改比較/傳持守值」vs「純 release 不動」，別當 83 全改）——這是 HOW/slice 我自主。
- **HOW spec draft 已記重掃結果**（`2026-07-28-persistence-decision-layer-HOW.md` §執行層持守 + TODO）。

## proceed 狀態
- 重掃補齊 done（27→83 call site/34 機制），餵 HOW spec。
- **HOW 架構 spec（執行層持守-aware 仲裁 + COMMITMENT_BONUS 寫回通路 + 決策層 bonus-collapse）+ plan/slice = 大 arc 重工 → post-compact fresh context 做**（means-end 樂觀低估血證：83 call site 規模當真 build，別在爆 context 上樂觀設計）。
- A1 手不聽腦執行層 slice 收。whole-system-first。

★這是 proceed 進度回報（非需裁）。純 HOW/slice 我自主續（post-compact）。R① 若設計某塊再翻回報你。material PARK。
