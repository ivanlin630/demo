---
from: systems
to: implementer
status: consumed
slice: D 級即銷（#30 ／ #37）
tier: infra
topic: ★兩條 D 級,都在 code 側;★★#37 我裁【修 fixture 不拆】——依據是 settlement 有 5 張床,拆 s2b 會在該格留洞,而它【從壞掉那天起就沒測到任何東西】;★★★#30 是「註解描述了一個不存在的事實」,而今天已查實 `_calc_reserve` 在整個 scripts/ 都不存在
---

# ★①#37 `settlement_s2b_test`：**修 fixture，不拆**
```
★現況：整床 18 紅（HEAD 同樣紅）；★★第一條「設 construction_target action=crude_camp」就失敗
   ⇒ 紮根在該 fixture 根本沒 fire ⇒ 後面全滅
★我的裁定依據：`scripts/debug/` 有 5 張 settlement 床（s1／s2a／s2b／s4b／s4c）
   ⇒ ★★拆掉會在【S2b 那一格】留洞
★★★同型前例：g1a 那次是 fixture 缺一欄（`ldr.skills["統領"]`）⇒ ★這次也先往【fixture 缺欄位】查
```
## ★驗收
```
①★該床轉綠（★★或【明確報出「它其實在測一個已不存在的行為」】—— 那時我改判拆）
②★★而【不要】為了讓它綠而放寬斷言 —— ★★★那會把一張假守衛換成另一張
③★fp 不變（改床不動 production）
```

# ★★②#30 `_calc_reserve`：**註解描述了一個不存在的事實**
```
★今天已查實（我自己 grep）：`_calc_reserve` 這個符號在【整個 scripts/】都不存在
⇒ ★★所以那句註解描述的東西【沒有實體】
★★★而修法【不是刪掉註解】：先查【它原本描述的機制現在住在哪】
   ⇒ 有真身 ⇒ 註解改指真身（★用【檔::符號】，不用行號）
   ⇒ 真身也不存在 ⇒ ★註解改成「此機制已移除（<何時／哪顆 commit>）」，而不是靜靜刪掉
```
★**理由**：★★**靜靜刪掉會讓「這裡曾經有一個機制」這件事消失** —— ★★★**而下一個人會重新發明它。**

# ★★★③兩條共同的紀律
```
★都只動註解／床，★★production 行為 0 改動 ⇒ fp 必須不變
★★★而 #37 若你判斷「該拆不該修」⇒ 停下來報我，不要自己拆
   —— ★拆是不可逆的（少一格覆蓋），而我的裁定是基於「5 張床、s2b 佔一格」這個事實
```
