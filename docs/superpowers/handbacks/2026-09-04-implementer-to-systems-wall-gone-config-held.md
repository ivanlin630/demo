---
from: implementer
to: systems
status: open
slice: 對齊跑回來了 —— ★config 切掉之後，「牆不見了」仍然成立
touches: 無 code（`scratchpad/funnel_match.txt`，30 日／`peaceful_economy`／seed 1337，與落地檔標頭逐字對齊）
topic: ★★★config 固定之後 `in_place_failed` 仍然是 **180 → 0** ⇒ ★「牆不見了」【不是 config 造成的】;★★而 `built_in_place` 反而【變多】:8 → 11,而母體從 336 掉到 128 ⇒ 成功率 2.4% → ★**8.6%（3.6 倍）**;★★★新的最大歸宿是 `pick_empty` 69/128 ＝ **54%**（＝選不出想建的設施）—— 卡點【往上游移了一格】,從「進門後被拒絕」變成「選不出要建什麼」;★而我仍然不歸因到任何【單一】改動:9 天的 code 差不只我今天兩刀
---

# ★★★①三份並排（★config 已固定，只剩 code 差）
| | entry | combat | noOp | underCon | ★pickEmpty | ★★built | ★★★inPlaceFail | dispatch |
|---|---|---|---|---|---|---|---|---|
| 2026-08-26 落地（`peaceful_economy`） | 336 | 0 | 28 | 92 | 26 | 8 | ★**180** | 2 |
| ★**2026-09-04 對齊（`peaceful_economy`）** | **128** | 1 | 12 | 35 | ★**69** | ★**11** | ★★**0** | 0 |
| 2026-09-04（`peaceful_economy_regime`） | 155 | 0 | 139 | 6 | 9 | 1 | ★★**0** | 0 |
```
★★★`in_place_failed` 在【兩個 config 上都是 0】⇒ config 不是解釋 ⇒ 「牆不見了」成立
```

# ★★②而不是「什麼都沒發生」—— 成功率是【升】的
```
`built_in_place`：8 / 336 ＝ 2.4%   →   ★11 / 128 ＝ **8.6%**（★★3.6 倍）
⇒ ★絕對數也升（8 → 11），★★而母體還掉了 62%（336 → 128）
⇒ ★★★所以這不是「路被關掉所以沒人被拒絕」，是【走到底的比例變高了】
```

# ★★★③但卡點只是【往上游移了一格】
```
新的最大歸宿：`pick_empty` = 69 / 128 ＝ ★**54%**（舊：26 / 336 ＝ 7.7%）
   ⇒ `pick_empty` ＝ ★★【選不出想建的設施】—— 而不是「選好了卻被擋」
⇒ ★★★所以這條鏈的敘事變了：
   舊：能選、選好了、去做了 ⇒ **被 `_subteam_upgrade_facility` 拒絕**（180）
   新：★過半數【根本選不出來】（69）⇒ 而選得出來的那些，★★不再被拒絕（0）
★而 `underCon`（該格正在施工）也從 92 掉到 35 ⇒ 排隊塞住的情況變少
```

# ④我仍然【不歸因到單一改動】（★而理由不是謹慎，是事實）
```
★兩份之間差【9 天的 code】—— 不只我今天那兩刀（payoff 導出／cap 壓縮）
⇒ ★★這中間別人也 merge 了東西，我沒有逐 commit 查
⇒ ★★★所以我能說的是：**「牆不見了」是現況，而【是誰拆的】我沒查**
★而若你要歸因，最省力的做法是【二分】：在 08-26 與今天之間挑中點各跑一次
   ⇒ 每次 ~2 分鐘，★★log₂(9 天的 commit 數) 次就能夾出來
```

# ★⑤對帳（★判準 F，讀數字之前先做）
```
`funnel_match.txt`：五個 section 全在（★31／31／31／31／31 列）｜無黏連｜DONE 在
★★而落地基準線那份是 14 列（它只印 entry>0 的天）⇒ ★★★兩者的「列數」不可直接比，
   我比的是【逐欄合計】而不是列數
```

# ⑥建議（★不是裁定，你的序）
```
★`known_issues` 那條「`defer_infra` 是一面牆」現在的地位：★★【當時為真，現況已變】
   ⇒ 我建議【就地標注現況】而不是刪 —— 那條的價值在於它記錄了一個被靜默吞掉的 return false
★★而真正該接手的新問題是：★★★`pick_empty` 54% ——【選不出想建的設施】是什麼意思
   ⇒ 那是我先前停在 `preqEmpty` 標「未拆」的同一條線的下游
```
