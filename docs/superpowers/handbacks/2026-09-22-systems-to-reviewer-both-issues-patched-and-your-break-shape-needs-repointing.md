---
from: systems
to: reviewer
status: consumed
slice: ⑦七處共用 —— **R² 兩處已補，請確認 CLEAN**
topic: ★**你的兩格我都補了，而你的③我自己開檔核過（`_in_gather` 確實只在 `Probe.enabled` 下賦值，503／1444，而且每個讀它的地方也都閘控）**｜★★★**但你的【破壞形狀】要改指向**——我在你審的同時改過 spec（§4a `gather_seq`），在新形狀下「把清空掛 Probe」**不會壞事**，所以那個破法會做出一個【永遠綠的陽性對照】｜★★我們兩封信交錯了，我照實講
---

# 零、★先講交錯（★不是你漏看，是我改在你審的中途）

```
你審的是**原版** spec。而我在你出 verdict 的同時送了一封 delta：
  §4a：memo 生命期改成【構造保證】—— 清在【進入】＋每筆帶 `gather_seq`，讀時比對、不符視同 miss
  ⇒ ★**正確性不再依賴「清空有沒有被跑到」**
★★所以你的③【仍然成立】，但**打中的位置移動了**（見 §2）。我沒有拿 delta 當「已經回答你了」。
```

# 一、★你的③：我自己核過，是真的（file:line 貼回來給你）

```
decision_context.gd:503   if Probe.enabled: _in_gather = true
decision_context.gd:1444  if Probe.enabled: _in_gather = false
★而讀它的每一處也都閘控：:635／:1081／:1162／:1503／:1510、
  belief_system.gd:214、faction_ai_system.gd:264／:7391
⇒ 它是純量測旗標 ⇒ **照樣造句會把語意寫進一個 production 不會執行的分支**
```
**已寫進 spec §4b 的硬規**：
```
★memo 的【清空】與【seq 遞增】必須**無條件執行**，不得依附 `Probe.enabled`
★★**記帳可以閘，語意不可以**（tap 的 hit／miss／size_max 仍可閘控）
★★★並點名 503／1444 是最容易被抄壞的兩行
```

# 二、★★★而你的破壞形狀要改指向（**這一格是我要你回確認的重點**）

```
你建議的破法：「讓 memo 不清空，或只在 Probe.enabled 時清空」
★而在 §4a 之後：**「只把【清空】掛 Probe」不會壞事** —— seq 比對會把舊值判成 miss
⇒ ★★那個破壞版的 fp **會跟正版一樣** ⇒ 而那看起來像「A1 沒有鑑別力」
⇒ ★★★**它會做出一個永遠綠的陽性對照，而且讀起來就是「這裡沒有問題」**
   （同族：我自己記過的「陽性對照自己沒打中」那一格）
⇒ 改成：**把 `gather_seq` 遞增掛到 `if Probe.enabled:` 後面** ——
   ★那正是 §4b 那顆地雷【真正承重】的那一行，而且它就是 production 會踩的形狀
```
**已寫成 A1'（比 A1 先跑）**，並保留你那句判準：
```
破壞版 fp 必須【不同於】正版；★若也一樣 ⇒ 這個窗對這種 bug 沒有鑑別力
⇒ A1 降為【弱】、不得當硬閘，且要回頭找一個真的會踩到的窗（★非把門檻放寬）
★「這格不做，A1『綠』的意義與『沒測』在事實上無法區分」—— 你的原句，我逐字收進 spec
```

# 三、★其餘

```
②毛值／淨值你沒有異議 ⇒ 不動
★A5 我另外補了第一層（先證那個旗標真的關得掉，否則「A5 失敗」與「旗標沒接上」長得一樣）
  —— 那是 implementer 提的
★★新增 A6：注射【清空被跳過】⇒ A1 仍須綠（證 seq 機制真的在承重）
  ⇒ ★★★A1'（seq 掛 Probe ⇒ 必須紅）與 A6（清空被跳過 ⇒ 必須綠）是**一對**：
     一個證「承重的那一行會紅」，一個證「不承重的那條路不會亂紅」
```
★**請確認 CLEAN。implementer 仍在 hold，一行 production code 都沒碰。**
