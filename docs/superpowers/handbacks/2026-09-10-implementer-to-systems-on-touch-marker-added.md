---
from: implementer
to: systems
status: open
slice: 雙算 on-touch 標記
topic: ★標記已加在算式上方（`decision_context.gd`，不是只在 known_issues）——照你的原文，含「刻意不修」的理由與「下次動這行是必帶格」｜★★而我要補一句它的**失效條件**：這種標記只在【有人真的編輯那一行】時起作用，而**改動可能發生在別的行卻讓它過期**｜★★★所以我在標記裡釘死了它依賴的兩個事實的 file:line
---

# ① 加好了（就一段註解）

位置：`decision_context.gd` 的 `c.settle_site_quality = clampf(_site.productivity * _farm_pot, …)` **上方**。
內容照你的原文，四點都在：**已知雙算／刻意不修的理由／下次動這行是必帶格／blueprint 那條規矩的第一個坐實實例**。
★**我沒有順手修那個雙算**（你明說 defer-with-record）。

# ② ★★而我補了一個你沒寫的：**它依賴的事實的 file:line**

我把 `world_generator.gd:90-91`（`productivity` 逐地形抽）寫進了標記裡。
★**理由**：這段註解的正確性**掛在那個事實上** ——
★★若哪天 `productivity` 改成與地形無關，**這段標記就從「提醒」變成「說謊」**，
而**沒有任何東西會提醒下一個人去更新它**。
⇒ ★★★**寫下它依賴的來源，是讓它至少【可以被查證】** ——
這跟今天早上「產線不可達」那條的處置是同一手：**斷言要能被證偽，否則它只會活得比事實久。**

# ③ ★★★而我要說清楚這種標記的失效條件（不誇大它）

```
✔ 有效：有人【編輯那一行】時會看見
✘ 無效：改動發生在【別的行】而讓它過期
   例：把 productivity 的生成改成地形無關（改的是 world_generator）
       ⇒ 標記還在 decision_context，而它已經錯了，★而沒有人會經過它
```
★**所以它不是守衛，是【給編輯者的便條】** ——
★★我不會把它記成「已防護」（同今天顯式化那次的結論：**買到可讀性，不是可偵測性**）。
★★★**真要可偵測**，形狀是「productivity 的生成端與 `_farm_pot` 的消費端之間有一道對帳」，
而那本身就是修雙算那張票的內容 ⇒ **在它被開之前，這裡就只有便條。**

# ④ 回歸

`bed-parse` PASS（393 張床全部載入）。★純註解，零行為。
