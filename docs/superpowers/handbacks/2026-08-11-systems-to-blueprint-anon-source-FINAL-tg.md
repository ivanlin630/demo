---
from: systems
to: blueprint
status: open
topic: "[★★anon drain 真源 FINAL(QA 讀既有數據定案、用戶指正『給 QA 讀』兌現)→你 TG 推用戶·★真源=unrest-split 兵變/分裂(event_unrest_split:102、QA 9-caller 核對唯一匹配、非 overflow 非 succession 非 dispatch-promotion=我+用戶假設皆非):Team0 unrest≥30+低忠誠(<0.35)異見 named 成員 goal 衝突→異見者帶 anon 追隨者(anon_split=統領×魅力×parent_anon×0.3、上限 pop/3)自立門戶=Team4/5/6·解釋非日邊界 tick(process_events 非日 gate)+Team4 人格異(新領袖=異見者本人非巧合)·★完整鏈:Team0 unrest 高→兵變分裂帶走 anon→池 5→0(day0-4)→下游 relief/care-scout/builder dispatch(綁 anon-specific 池 AnonTierSystem.total_pop、systems code-read 坐實)派不出·★41 天不回補:merge 回發生但 anon 未升=被併隊自己 merge 前已 0(QA 推論、需 merge-前 anon 快照 tap 100%坐實、可選補)·★★systems 認 over-claim(第3次、用戶當 measure-first 仲裁抓對):我先前『anon-overflow central 根 bug 擋 relief/manufacturing/builder』整條 framing 錯——真源是 genuine unrest-split(兵變)非 overflow 非 bug、是有動機的世界故事(不滿的人帶跟班分家)·★★中性給用戶判(池空≠bug 原則 feedback_resource_depletion_genuine_vs_blind):事實=genuine 兵變機制、非 causeless/mechanical;用戶裁平衡/設計:①團首 4 天就分裂太快/太頻繁?(unrest 為何早期就≥30?)②下游 relief/care/builder 因 anon 空 41 天派不出=想要的難度曲線 vs 該讓小領主更能派(dispatch 該能用 named 或 anon 該歸隊循環=用戶 target model『名帶匿+歸隊』)?·★用戶 target model vs code gap:用戶要 dispatch=名帶匿跟班+完成歸隊循環;code 是 dispatch_anon_messenger 建 leader_id=-1 anon-alone(非名帶匿)+此案 anon 被 unrest-split 帶走(非 dispatch 耗)=兩事·★給你 TG 素材:QA 真源(unrest 兵變)+完整鏈+over-claim 訂正+中性設計問·序:你 TG 推用戶→用戶裁 genuine-accept(兵變合理、難度如此)vs 調(unrest-split 太快/dispatch 該用 named/anon 歸隊)·地基 KEEP"
---

# ★★anon drain 真源 FINAL（QA 讀既有數據定案）→ 你 TG 推用戶

用戶指正「給 QA 讀」兌現：QA 讀既有 970-entry specimen+raw 定案、無重跑。

## ★真源 = unrest-split（兵變/分裂）
`event_unrest_split:102`（QA 9-caller 核對唯一匹配、**非 overflow 非 succession 非 dispatch-promotion=我+用戶假設皆非**）：Team0 unrest≥30 + 低忠誠(<0.35)異見 named 成員 goal 衝突 → 異見者帶 anon 追隨者（`anon_split=統領×魅力×parent_anon×0.3`、上限 pop/3）自立門戶 = Team4/5/6。解釋非日邊界 tick + Team4 人格異（新領袖=異見者本人）。

## ★完整鏈
Team0 unrest 高 → 兵變分裂帶走 anon → 池 5→0（day0-4）→ 下游 relief/care-scout/builder dispatch（綁 **anon-specific** 池 `AnonTierSystem.total_pop`、systems code-read 坐實）派不出。41 天不回補：merge 回發生但 anon 未升=被併隊 merge 前已 0（QA 推論、需 merge-前快照 tap 100%坐實、可選補）。

## ★★systems 認 over-claim（第 3 次、用戶當 measure-first 仲裁抓對）
我先前「anon-overflow central 根 **bug** 擋 relief/manufacturing/builder」整條 framing **錯**——真源是 **genuine unrest-split（兵變）**非 overflow 非 bug、是有動機的世界故事（不滿的人帶跟班分家）。

## ★★中性給用戶判（池空≠bug、[[feedback_resource_depletion_genuine_vs_blind]]）
事實 = **genuine 兵變機制、非 causeless/mechanical**。用戶裁平衡/設計：
1. 團首 4 天就分裂太快/太頻繁？（unrest 為何早期就 ≥30？）
2. 下游 relief/care/builder 因 anon 空 41 天派不出 = 想要的難度曲線 vs 該讓小領主更能派？
3. ★用戶 target model vs code gap：用戶要 dispatch=**名帶匿跟班+完成歸隊循環**；code 是 `dispatch_anon_messenger` 建 leader_id=-1 **anon-alone**（非名帶匿）+ 此案 anon 被 **unrest-split** 帶走（非 dispatch 耗）= 兩事。

序：你 TG 推用戶（QA 真源兵變 + 完整鏈 + over-claim 訂正 + 中性設計問）→ 用戶裁 genuine-accept（兵變合理、難度如此）vs 調（unrest-split 太快 / dispatch 該用 named / anon 歸隊循環）。地基 KEEP。
