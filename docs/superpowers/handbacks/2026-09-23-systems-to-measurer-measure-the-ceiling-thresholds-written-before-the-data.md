---
from: systems
to: measurer
status: open
topic: ★派量測：那 2.4 秒【怎麼分給 27 格】——這是裁定 (A) 的天花板｜★★門檻我【在看到數字之前】寫死在這裡｜★★★而正確的門檻不是百分比，是一個絕對值，理由在內
---

# 一、量什麼

```
床    scripts/debug/pass_tick_phase_breakdown_bed.gd（★今天已隨情報喚醒票落地 main）
樹    main（世代 7，≥ 22ac1b096）
種子  1337 與 42（★與 B3 同兩顆 —— 這樣兩份卷面可以互相對照）
★母體：**只取 dt > 2s 的那些 pass tick**（blueprint 令）——不是全部 tick
輸出  每一格相位的耗時，逐 tick 或彙總都可以，★但要能加總回那 2.4 秒
```

# ★★二、門檻（★寫在看到數字之前）

**先講為什麼不是百分比**：

```
裁定 (A) 是把「可錯開」那半按隊散到 60 顆 tick 上
⇒ 它們在【單一 pass tick】上的成本會攤成大約 1/60
⇒ ★★★新的最壞 tick ≈ **必須留在整點那半的耗時**（可錯開那半幾乎消失）
⇒ 所以要比的是【那一半的絕對耗時】與【1000ms】，不是它佔幾成
```

**三格，寫死**：

```
(甲) 必須留在整點那 7 格合計 < 1000ms
     ⇒ ★(A) 的天花板【夠】，p99 < 1s 摸得到 ⇒ 往下寫 spec
(乙) 合計 ≥ 1000ms
     ⇒ ★★(A) 也摸不到門檻 —— 跟分片票同一個結局，而我們【在設計之前就知道】
     ⇒ 回報 WHAT，不寫 spec
(丙) ★那 7 格【在卷面上找不到對應的相位標籤】
     ⇒ 不可判 —— 不是「很便宜」也不是「很貴」，是【我們沒有量到它】
```

★那 7 格是：`vision, move, propagate, intel, market, interactions, faction_snapshot`
（相位標籤可能不是一對一，例如 `near.messages` 可能蓋住 propagate／intel／market —— ★遇到就照實說，**不要幫它分**）

# ★★★三、兩條我先寫的誠實限

```
①★「攤成 1/60」是【理想錯開】的上界 —— 實際相位由 team_id 派生，分布不會完美均勻
  ⇒ 真實效果【比這個估計差】。所以 (甲) 應該讀成「天花板夠」，不是「一定達標」。
②★★母體是【>2s 的那些 tick】⇒ 它答的是「最壞情況怎麼組成」，
  ★★★不答「平均情況」。不要拿這份卷面去講平均。
```

# 四、跑法

```
★跑法細節你自己定（分幾批、背景與否），我不問。
機器現況我剛查：Godot 行程 0、FreeMB 約 14,000 ⇒ 空著。
★★卷面要帶 [HW] cpu= 戳與開跑前 FreeMB（同 B3 的規矩）。
```
