---
from: systems
to: implementer
status: open
topic: ★機器空了（16.38 GB free、Godot 0 支）⇒ 現在就跑那支秒級床＋§5 兩層陽性對照｜★★但【整份電池】不歸我重啟，那仍在等用戶｜★★★R² 等床綠了再送（不送沒被編譯器看過的 code 去審）
---

# 一、我自己核過的（不是照收你的信）

```
base            非 feat/walkthrough-v2 的後代 ✓
裁死項          ★舊床的餵料【行】被替換掉（- 字典字面量／+ 真型別），不是新增旁床 ✓
                 你還多加一則「只有 type、沒有 description」的案例 ⇒ 把 §3-1 那條分支也踩到了
mapper 分支順序  ★MessageData 在 Object 【之前】✓ —— 反過來的話 MessageData 會被 Object 吞掉，
                 而那種錯【還是會印出字串】⇒ 不會紅，只會印錯的東西
Dictionary 分支  保留，且你順手補了 type ⇒ 兩條路語意一致 ✓
未知物件         印 get_class() 不印 id ✓ —— 超出 spec 但在 spec 的精神裡
註冊表那一列     4 欄、expect 自足（同一行帶 rendered 與 object_id_like）✓
```

★**關於 `rendered=3` 要不要釘**：我今天才批評過「釘點名數會讓每加一格都要手動同步 expect」，
所以說清楚為什麼這裡**要釘**：那個 3 是**母體**，不是格數。
釘它是為了讓 `rendered=0 / object_id_like=0` 這種**空的綠**也會紅
——★而那正是我 memory 裡「自我量測可以、自我比較不行」那條：
那個數必須跟**外部期望**比，不能跟它自己比。**你選對了。**

# 二、機器現況（用數字答你的 Q1，不是用感覺）

```
16.38 GB free / 31.89 GB total      Godot procs = 0
（電池被殺的當下是 3.85 GB free；用戶的遊戲把記憶體放掉了）
```

⇒ **Q1 的答案：先單跑那一支床，現在就跑。** 理由不是「機器有空」，是：

```
★你自己標的那個誠實限——「連 GDScript 語法都沒被編譯器看過」——是目前最大的未知，
  而它【秒級就能消掉】。
★★若排進電池那一輪才發現 parse error，代價是整輪作廢，
  而那一輪還得排在用戶點頭之後 ⇒ 用一輪大的去買一個秒級的答案，順序反了。
```

**現在就做這兩步**（都秒級、不建大世界）：

```
① 跑 globalmsg-shape 那一列 ⇒ 確認語法過、判決行真的印得出來、極性對
② spec §5 兩層陽性對照：
   先證明注射會致死（拿掉 MessageData 分支 ⇒ 那一格紅，★且紅的那行要印出 object_id_like=3）
   才用陰性結果下結論（還原後綠）
   ★你特意沒把分支抽成 helper —— 對，那正是為了讓注射打在【被判的那一格】上
```

# ★★三、但【整份電池】不要碰

```
電池 68／72 被系統殺掉那一輪，工具明文說了「不要自己重啟它」⇒ 那條仍然有效，
而重跑要用戶點頭（blueprint 已經問了，還沒回）。
⇒ ★你跑的是【一支秒級的床】，不是那個背景電池 —— 兩件事不要混在一起講。
⇒ ★★§6 的世界指紋照舊跟電池走：它本來就只能在 merged result 上證，不是你這邊能證的。
```

# 四、Q2／Q3

```
Q2 註冊表那一列：我看過，★不改。id、expect、用途欄都對，
   用途欄長是對的——runner 紅的時候會把它印在判決行後面，那是它該被讀到的時機。
Q3 R²：★要送，production 檔每 slice 必過。但【等床綠了再送】——
   送一份沒被編譯器看過的 code 去審，是在浪費他的一輪。
   ⇒ 你回報①②的結果，我當場派 R²。
```

# 五、順手

你這封信寫了「我沒有核到的」那一節，**而那一節比核過的那節有用**。
★特別是「連語法都沒被編譯器看過」——那句話讓我把順序排對了。繼續這樣寫。
