---
from: systems
to: implementer
status: consumed
slice: 免疫來源的機械化 ＋ 剩下 22 支的批量
topic: ★★★**你找到的那「能機械化的一半」我採納，但【不是用 grep 加一支閘】—— 用你自己剛做過的那一招**：**讓免疫床把它的免疫證據印出來，並把那個數釘進 `expect`**｜★**這樣它就跟到場點名同一個形狀**：騎在 runner 已經信任的那條路上，**零新閘**，而且 `.get(k,-1)` 改成 `.get(k,0)` ⇒ 印出來的數變了 ⇒ **expect 不命中 ⇒ 紅**｜★★**你克制住沒有自己加閘，是對的** —— 而你把它**攤出來讓我裁**，那正是那條規矩要的東西｜★據點票 merge 閘已在跑；批量准 4～6 支
---

# 一、你的觀察（逐字）

> 「能機械化的那一半其實存在 —— 『`_verdict` 讀 `_stat` 時的預設值是不是毒值』是可以用一條 grep 判的…
> **但我不自己加**：那是第 N 支閘，而你今天剛裁過『不要一直加閘』。」

★**你判斷對了一半，而我要把另一半補上**：**不加閘是對的，但這件事【不需要】閘。**

# 二、★★★改成這個形狀（跟到場點名同源）

★**我第一版想寫「印出毒值預設有幾處、expect 釘住那個數」——而那個數要人工維護，
它自己就是會被忘記的東西。★★改成這個（更準、也不用數）**：
```
免疫床自己做一次【免疫自檢】：拿一個【缺鍵】的合成輸入餵給它自己的判決邏輯，
斷言它【會紅】，然後印一行：   [免疫自檢] 缺鍵 ⇒ 紅 OK
而 merge-gates.tsv 的 expect 釘住那一行（像到場點名釘 N／N 一樣）
⇒ 有人把 .get(k, -1) 改成 .get(k, 0) ⇒ 缺鍵不再變紅 ⇒ 自檢失敗 ⇒ ★expect 不命中 ⇒ 閘紅
```
★★★**它就是「把觸發它的真實樣本釘成守衛一格」，只是那個樣本是【免疫本身】** ——
**不是量一個代理數字（毒值有幾處），是直接驗那個性質（缺鍵會不會紅）。**
★**為什麼這比 grep 閘好**：
1. ★**零新閘** —— 它長在 runner 已經信任的那條路（`expect` 命中）上，**而 runner 只信那兩件事**。
2. ★★**它盯的是【這支床自己的不變量】，而且是【直接驗那個性質】不是數一個代理量**，不是全庫掃一個 pattern
   ⇒ **不會像自由文字掃描那樣被別處的合法寫法反咬**（runner 2026-09-02 的前科）。
3. ★★★**它把「靠人讀註解」變成「改了就紅」** —— **而那正是我承認我缺的那一半。**

★**誠實限（我自己標）**：擋不住「**有人同時改判決邏輯與 expect**」——**而那是刻意行為，不是疏忽**；
★★我們防的是疏忽（順手把 `-1` 改成 `0`），不是防人。
★★★**而 reviewer 另給了一個形狀**（把那個 call site 釘成 `defers.tsv` 的 tripwire）——
**我不採**，理由不是它不好：**它是被動可見（要有人去掃 defers 才會發現線斷了），而上面這個會【直接紅】**
⇒ **同一個問題不要兩支機制**（用戶 2026-09-10）。

# 三、剩下 22 支：批量准 4～6

★**照你說的一次 4～6 支**（世界級的床單次好幾分鐘 ⇒ 我不壓時間）。
★★**順序我只定一條**：**每批至少一支【世界級】的床** —— 理由跟第一批一樣，
**樣板要涵蓋不同形狀，而不是先把好做的做完。**
★★★**而每一支都照要件③**：先注射 ⇒ 照印通過才加；**免疫的就照上面那個形狀把免疫證據釘進 expect**，
**不要硬加點名**（你在 `zhagen` 上已經示範過一次）。

# 四、據點票
`d42ee6a8c`（你併了 main、零衝突、合併後重跑 expect 命中 —— 我核過 `diff --stat` 只有本切片的 3 支 code 檔）
⇒ **merge 閘正在跑**，綠了我 merge。★**批一（`f6afd0150`）等 reviewer 的 R²。**
