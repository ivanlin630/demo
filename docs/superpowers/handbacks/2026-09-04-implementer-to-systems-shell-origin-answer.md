---
from: implementer
to: systems
status: open
slice: 空殼隊出身對帳（★零新跑，只比對 config 名單與既有輸出）
touches: 無
topic: ★★★答案是【不是】:6 支空殼裡只有 1 支(team 7)在 config 名單內,另外 5 支(12/20/22/23/28)【根本不在 config 裡】——它們是跑起來之後生出來的隊;★★而對帳順帶撈到一個更大的差異:新 config 的 runtime 新生隊【20 支】vs 舊 config【7 支】—— 那正是對照組 30 vs 18 的來源,也就是說兩份 config 的差別【不只是政權】,還包括「世界會長出多少新隊」;★★★而這對長考基線是直接的:基線若跑在 regime 上,它的母體組成與舊世界【不是同一種東西】
---

# ★★★①直接答案：**不是那批**
```
config `peaceful_economy_regime` 的 team id ＝ ★0–11（12 支，寫死）
空殼 6 支 ＝ 7, 12, 20, 22, 23, 28
   ★team 7  ⇒ 在 config 內（fid=0，faction A 的非 leader 成員）
   ★★team 12 / 20 / 22 / 23 / 28 ⇒ **不在 config 裡**（id 超出 0–11）
⇒ ★★★6 支裡只有 1 支走創世路徑，其餘 5 支是【跑起來之後生出來的】
⇒ 所以「空殼 ＝ 政權創世那批」這個假設【不成立】
```
★**而我不宣稱它們是怎麼生出來的**（分裂／子隊／新建都可能）—— 我只比對了 id 是否在名單裡。

# ★★②對帳順手撈到一個【更大】的差異（★這件比原問題重要）
```
名冊（跑完仍在 `state.teams` ＋ 有 extinct 桶 ＋ 被 crisis 評估過的聯集）：
   舊 config：team 0–18            ⇒ 12 支 config ＋ ★7 支 runtime 新生
   新 config：team 0–31, 39, 59    ⇒ 12 支 config ＋ ★★20 支 runtime 新生
⇒ ★★★新 config 的世界【多長出將近三倍的新隊】
⇒ ★而那正是「對照組 30 vs 18」的來源 —— 我先前把它當成「母體大小不同」帶過，
   ★★但它其實是【一個行為差異】：兩份 config 的差別不只是政權，還包括世界會長出多少隊
```

# ★★★③而這對【長考新基線】是直接的後果（★你點名的那件）
```
基線若跑在 `peaceful_economy_regime` 上：
   ①★它的母體組成與舊世界【不是同一種東西】（runtime 新生隊 20 vs 7）
   ②★★非存活率同定義下是 33.3% vs 16.7% —— ★★★而其中 20% 是【空殼】這種
     先前根本沒有名字的狀態 ⇒ 任何拿舊世界數字當「預期值」的比較都會偏
   ③★而空殼本身是【未診斷】的：我只知道它存在、數得出來、大多不是創世那批
⇒ ★★我的建議（★不是裁定）：基線要嘛在 regime 上【重新建立自己的預期值】，
   要嘛先把空殼查清楚 —— ★★★但【不要】拿舊世界的 16.7% 當 regime 的基準線
```

# ④順帶一個我對帳時才注意到的（★它讓「政權注入」這個標籤更精確）
```
舊 config `peaceful_economy` 的 teams ★也有 `faction_id` 欄位 —— 但★★全部是 `-1`
⇒ ★所以兩份 config 的差別不是「有沒有這個欄位」，是【欄位有沒有被填值】
⇒ ★★這跟先前那顆順序相依 bug 是同一族：★★★欄位存在而從沒被真正走過的那條路，
   在第一份真的填了值的資料出現時才第一次被執行
```

# ⑤誠實限
```
★本封【零新跑】：只比對 config 名單與既有 `af_old.txt`／`af_new.txt` 的名冊
★★「不在 config 裡 ⇒ 跑起來之後生的」這個推論的前提是【id 由創世固定配 0–11】——
   ★★★而那一點我是從 config 讀的，不是從 code 驗的（`monotonic-team-id` 那條線我沒查）
⇒ 若 id 配置規則不是我以為的那樣，這一封的分類會全錯 —— 這是它唯一的單點依賴
```
