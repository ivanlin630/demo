---
from: systems
to: measurer
status: open
slice: S7-root-differential
tier: measure
topic: ★★★換根微分試驗:把「這顆常數會不會隨根漂」從讀 code(詮釋)換成量測——root 60 vs 120 同 seed,量【候選常數的套用次數 per person-day】,正確導出的應維持 1.00×、漂的會露出 2.00×;★★而統一的量是【套用次數】不是各自的語意量:常數本身不變,漂的是它每真實日被套用幾次;★兩端陽性對照必備(已知導出的 + 已知裸的),否則「全部 1.00×」既可能是好消息也可能是儀器沒開;★接在基線 before 腿【之後】
---

# ★①排序：**接在 S6 基線 before 腿之後**
★**兩個動作都動根，不要撞在一起。** ★★基線那輪的 verdict 先交，再開這一輪。

# ★★②量什麼 —— **套用次數 per person-day**（★不是各自的語意量）
★**候選有 92 顆，它們的語意量互不相同**（HP／飢餓／平滑窗／移動…）——**逐顆定義語意量不可行。**
★★**而它們共用一個機械量**：
```
★常數的【值】本身不會變 —— 漂的是【它每真實日被套用幾次】
⇒ 量：該常數的套用點,每 person-day 執行幾次
⇒ ★★換根 ×2 之後,正確導出的應【維持不變】(1.00×);漂的會直接顯示 2.00×
```
★★★**這個量對所有候選同一形狀，不需要懂任何一顆的語意。**

# ★★★③兩端陽性對照（★這是硬條款，不是加分）
```
★對照 A【已知導出、期望 1.00×】：任一 `const *_DAYS` 型（使用端自己乘 TICKS_PER_DAY）
★對照 B【已知裸值、期望 2.00×】：ui_logic_test.gd:77 TICKS_PER_DAY := 24.0 型（字面值不隨根走）
```
★★**為什麼兩端都要**：★★★**若結果「全部 1.00×」，那既可能是【真的都沒漂】，也可能是【儀器沒開】——
而 A 和 B 分別站在兩端，才把這兩種讀法分開。**
⇒ **A 若不是 1.00× 或 B 若不是 2.00× ⇒ 先修儀器，不要報數字。**

# ★④跑法
```
root : WorldState.TICKS_PER_HOUR  60 → 120
★下限驗過：遭遇動作 = TICKS_PER_HOUR/6 必須 >= 10 ⇒ 120/6 = 20 ✓（60/6=10 剛好在地板上）
seed : 1337   days: 30   beds: peaceful_economy + warring_states
★★段1 只做 ~17 顆（不是 92）：七病高優先 3 顆 ＋ 刀① `norm=NO` 的 14 顆
   （刀①那 14 顆是【命名軸完全看不見】的那些 ⇒ ★最值得先看）
★段2（刀② 那 71 顆）視段1 結果再決定 —— ★★我不先派,避免一票吃 92
候選清單：docs/measurements/2026-09-01-s7-step3-three-knives.txt
```

# ★★⑤誠實限（★必須跟數字一起印）
```
①★換根會讓 RNG 序列不同 ⇒ 世界軌跡分岔 ⇒ ★★比的必須是【率】,且兩床都看
②★解析度下限:這把尺答的是【有沒有整數倍漂移】,不是【有沒有任何偏差】
   ⇒ ★★2.00× 藏不住;1.05× 藏得住 —— ★★★把 1.05× 讀成「沒漂」是錯的,要讀成「這把尺看不到」
③★刀①是【高召回低精度】(靠 += *= 抓,會撈到對陣列/權重累加的)
   ⇒ ★★候選裡有明顯不是時間量的,照跑照報,★★★不要自己先剔除
      —— 剔除就是判定,而判定是這一輪量測要產的東西
```

# ★⑥交付
`docs/process/verdicts/S7-root-differential.measure.json`
★**每顆一行：常數名／file:line／套用次數 per person-day（root60）／（root120）／比值。**
★★**外加對照 A、B 兩行，明標期望值** —— **它們不是結果，是【這批數字能不能讀】的前提。**
