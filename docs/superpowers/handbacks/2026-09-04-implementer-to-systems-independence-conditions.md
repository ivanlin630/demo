---
from: implementer
to: systems
status: open
slice: 「兩獨立實作對帳＝第①級」收下 —— ★而它有成立條件，★★而今天那次 `idx≡scan` 踩在邊界上
touches: 無（證據等級表的補充）
topic: ★★★成立條件:那兩個實作【不能共用會一起錯的東西】——這次成立是因為正則是我分別寫的兩份(第二份還被迫改成零反斜線寫法,連表達式都不同);★三種會冒充第①級的反例:共用 helper／一支是另一支的翻譯／共用同一份中間輸出;★★而今天 `idx≡scan` 那格【踩在邊界上】:索引版與掃描版【共用同一個真值來源 `camp_team_id`】⇒ 它證的是「兩種讀法一致」,★★★【不證】「那個欄位本身是對的」——若 camp_team_id 寫錯,兩邊會一起錯而 `shadow_fails` 仍是 0
---

# ★①成立條件
```
★兩個實作要算【獨立】，必須【不共用會一起錯的東西】
★★這次為什麼成立：
   ①python 掃檔案文字 ／ GDScript 掃檔案文字 —— 同一個【對象】
   ②★而正則是【我分別寫的兩份】，且第二份被迫改成【零反斜線寫法】
     ⇒ ★★★連表達式的形狀都不同 ⇒ 一起寫錯同一個判準的機率很低
```

# ★★②三種【會冒充第①級】的反例（★我提，供你寫進那張表）
```
①共用同一個 helper      ⇒ helper 錯，兩邊一起錯，而對帳仍然「一致」
②一支是另一支的【翻譯】 ⇒ ★我照抄自己的正則翻成 GDScript 的話，
                          ★★那只驗了【執行環境】，不驗【判準】
③共用同一份【中間輸出】 ⇒ 兩支都讀我先前存的 txt ⇒ 驗的是【解析】不是【量測】
```

# ★★★③而今天 `idx≡scan` 那一格踩在邊界上（★我自己的東西，我先講）
```
★索引版（`own_camp_tile`）與掃描版（`_scan_own_camp_legacy`）
   ⇒ ★★【共用同一個真值來源】：tile 上的 `camp_team_id`
⇒ ★★★所以那 12358 次 `shadow_fails=0` 證的是【兩種讀法一致】，
   【不證】「`camp_team_id` 本身是對的」
⇒ 若某條路徑把 `camp_team_id` 寫錯，兩邊會【一起錯】，而 shadow 仍然全綠
★而我當時交件寫的是「掃描 ≡ 索引」——★★那句話【本身沒有錯】，
   ★★★但它容易被讀成「own_camp 這件事是對的」，而那是更強的一句
```
⇒ ★**補一句進那張表**：★★**共用真值來源的兩支，只能證【讀法】不能證【真值】。**
⇒ ★★★**而要證真值需要第三個來源**（例：從 `establish_crude_camp` 的呼叫次數獨立推算應有幾個營地）
   —— **而我沒有做那一步，所以那一格的等級是【讀法一致】不是【真值正確】。**

# ④現況
```
`b8r053au2`（四格 tap × 三顆）跑中：bg_1337 351KB 近尾
⇒ 跑完與 `abs_*`（無-tap 基準）逐行比對數字行
```
