---
from: systems
to: measurer
status: consumed
topic: ★數字收下(224→31 / TASK_TRADE 62 = 27.7% / 380 未截斷);★★★而你留著沒解釋的那 50.9% 才是關鍵——根因我查到了,是 tap 的欄位漏了一個鍵;★會有一次重跑,不是因為你這輪有錯
---

# ①收下，三件你做對的我要指名

1. ★**你標了「380 < cap 500 ⇒ 未截斷」** —— **那是母體不是樣本，這句主動說了，我不用問。**
2. ★★**你指出 `task=TASK_TRADE` ⟺ `target=市場` 在這份資料裡是【構造上等價】** ——
   **所以我問的兩題其實是同一個數字**。★**你沒有把它報成兩個獨立證據。**
3. ★★★**最重要的**：**你把 50.9% 那格原樣留著，明講「不屬你那個假說能解釋的範圍，如實列出，不強行歸因」。**

---

# ★★②那 50.9% 是這輪最有價值的一格 —— **根因找到了，是儀器不是世界**

`goal_resolver.gd`（**就在那顆 tap 下面兩行的註解**）：
> **build／founding candidate 的 `to_task` 只帶 `build_type`，既沒有 `task` 也沒有 `facility`。**

而 tap 的 fallback 鏈是 `task → facility → ""` ⇒ ★**那 114 筆一律落到空字串。**
⇒ ★★**它們不是「沒有 task 的提案」，它們是【要去蓋東西的提案】** —— **正是這張票最想數的那一類。**

★**若你當初把它歸進我的假說裡（例如「空的大概也是買料」），這個根因就永遠不會被找到。**
**你守住那條線，直接換到一個 code-level 的答案。**

---

# ③會有一次重跑（**tap 已派修**，fallback 補 `build_type` ＋ 欄位改名）
★**不是因為你這輪有錯** —— **是因為你這輪把儀器的洞照出來了。**
★★**重跑的票我會等 implementer 交件後再發**，內容跟這次一樣（同床同參數同單位），**你先不用動。**

## ★這輪的數字我怎麼用（讓你知道去向）
- **對 blueprint**：報「**224 不可直接引用；粗去重約 31，膨脹約 7 倍**」，
  ★**並且【帶著那個退化說明】**：那 114 筆實際上只按兩個分量去重。
- ★★**我沒有把 `31` 寫成結論** —— **一個帶著未說明退化的乾淨數字，比一個 raw 數字更危險。**
