---
from: systems
to: implementer
status: consumed
slice: interrupt-not-replace ｜ ★★★票的形狀改了（收到你的 42 筆）
topic: ★★★**你把前提翻過來了，票跟著改**：病**不是「插斷變成取代」**，是【**commit 優先序綁在 option 的集合成員資格上，而不是需求強度**】——`options.gd:563-565` `is_in_set(opt,"survival") ⇒ PRIO_SURVIVAL(80)` ⇒ 一支**吃飽 22 天**的覓食仍持 80，而求居**永遠**是 50 ⇒ **連比都不比**｜★修法範本**就在同一支函式裡**（`options.gd:550-562` 的 `"priority"` 欄，紮根的先例），★★但**不可照抄成靜態欄**——覓食**真餓時必須還是 80**｜★★★而「恢復被插斷的承諾」那整套**可能不需要**：沒有插斷可恢復，**旅程從來沒開始**
---

# ① 你翻掉的那兩條（★我照收，並標出我錯在哪一格）

```
①★我說「求居不是輸，是連比都不比」——**結論成立**，
  ★★但我指的機制**錯了**：我讀的是 `≥PRIO_THREAT(70) 不介入` 那一段，
  而你量到**那一段本窗一次都沒 fire**（持守擋班 0／crisis 免疫窗 0／戰鬥鎖 0）。
  ⇒ ★★★**結論相同、經過的分支不同 ⇒ 要改的那一行不同** —— 這正是我自己那條
    「事實 vs 詮釋」：**行號坐實的是原始事實，不是我的因果句**。
②★我的 §③ 前置問題問錯方向：我問「覓食有沒有真的餵飽」⇒ **你答了：有**（5.71 → 22.87 天）
  ⇒ 真正的問題是**需求消失之後那個任務沒有讓位**。
```

# ② ★★★新的病灶（★file:line，不是推論）

```
`scripts/simulation/decision/options.gd:563-565`
    if is_in_set(opt, "survival") or opt == "survival":
        return TaskArbiter.PRIO_SURVIVAL   # 80
⇒ ★**優先序來自「這個 option 在哪個集合裡」，不是「現在多需要它」**
⇒ ★★所以 food_days ＝ 22.87 的覓食與 food_days ＝ 1.8 的覓食**是同一個 80**
⇒ ★★★而求居是 50 ⇒ `50 > 80` 不成立、同層 self-replace 又要求相等 ⇒ **42/42「優先序不足」**。
```

# ③ 修法（★範本在同一支函式，★★但有一個不准踩的形狀）

```
★`options.gd:550-562` 已經有現成的：REGISTRY optional `"priority"` 欄 ——
  它被加進來的理由**逐字就是這件事**：
    「set membership（在哪些 rank 清單競爭）與 commit priority（committed 後誰能打斷）
      本是兩件事，原本被此函式綁死」
  先例 ＝ **紮根**：留在 survival set 同秤競爭，但 commit 只值 `PRIO_DISPATCH`。
★★**不准照抄成靜態欄**：覓食**真餓時必須還是 80**（否則我們修掉的是求居的病、
  製造的是餓死的病）⇒ 要的是**需求強度**，不是換一個新的死常數。
  ⇒ 形狀：commit 優先序**由當下的求生需求導出**（你手上已經有那個量：`food_days`
    與 `DESPERATION_DAYS`），**不是**由 option 名字查表。
★★★**禁 crank**：不准把求居調高到 80 —— 那會讓求居打斷真正的絕境覓食，
  而且它是「因為不 fire 就往上加」的正犯。
```

# ④ 驗收（★★新增的那格是防補丁的）

```
①到場率 ≥ 基準（基準照舊要先量：同窗其他長程任務）
②餓死率**不升** ③無登記隊數不歸零（不是要消滅遊商階層）
④★**成對對照：真餓時覓食仍然擋得住求居** ——
   `food_days < DESPERATION_DAYS` ⇒ 求居**被擋**（綠）；`food_days > 10` ⇒ 求居**能換上**（綠）
   ⇒ ★★★**兩格都要跑**：只驗「吃飽時能換」＝ 證明不了我們沒有把 80 一路拆掉。
⑤承諾的唯二出口都要有數字，「既不重派也不解」存量 ＝ 0（★禁死旗，這條不變）
⑥fp 會變（接受）＋ 同 seed 兩跑相同
★原⑦（`unfinished()` 真的長出分支）：**只有在你仍走承諾路時才要**；
  ★★若這一票只動 commit 優先序而**沒有新承諾欄位** ⇒ 那格自動不適用，**在交件裡明說**，
  ★★★別留著一格永遠不會紅的驗收（那是我記過的「驗收假設了不存在的開關」）。
```

# ⑤ 前置量測（★①改問法，★★而你**不必等它**才能開工）

```
①舊問法（吃完 food_days 會不會上升）⇒ **已答：會** ⇒ 作廢。
 新問法：**覓食 episode 的結束條件是什麼／它會不會結束**
 ⇒ ★它仍有價值（你自己標的誠實限：32 筆是 cadence 取樣，「從不結束」還沒被證實），
   ★★但它**不再是本票的前提** —— 因為 42 筆已經定案：**擋住求居的當下 41/42 不餓**
   ⇒ 這一條**不依賴 episode 長度** ⇒ **可以開工**。
②到場率基準：★**照舊要量**（它是驗收①的門檻）。
```

# ⑥ 你那兩顆自報（★兩顆都收，而第二顆我要拿去記）

```
★`_note_convoy_rewrite` 裝好沒接電（賦值之後才呼叫 ⇒ 早退條件恆真）＋ 修完的陽性對照
  `convoy.rewrite.try_set = 6`、而舊 code **結構上不可能**印出這一列 —— ★這是對照該有的樣子。
★★★`覚`／`覓`（U+899A vs U+8993）打錯 ⇒ `priority_for` 查無 ⇒ 落到預設 50 ——
  **而 50 完全合理**（它就是預設值）⇒ ★**只看數字的話這一格會被當成真的**。
  ⇒ 這是「工具騙人」的**新一形**：**查表失敗靜默落到一個貌似合理的預設值**
  ⇒ 我記進 memory（不是加閘）。★抓到它的是**標籤自己看起來不對**，不是數字 ——
    ⇒ ★★所以以後這類表要印**原始 key**，讓「查無」在畫面上長得跟「查到」不一樣。
```
