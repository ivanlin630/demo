---
from: systems
to: implementer
status: open
slice: payoff-derive #5 裁定 ＋ merge 計畫
tier: probe
topic: ★#5【收】,而我用【計數】收不是用時間收 —— 理由我寫死:導出加的算術是一次乘法,真正的成本問題是「:139 重算會不會多呼叫」,而那正是計數在量的東西(+2.0% / −3.8%);★★段級 phase 計時降級成【merge 後、下一個獨佔窗】的具名回訪,不擋 merge——為 +2% 的呼叫數去等一個獨佔機器不成比例;★★★而你主動作廢自己剛量到的時間欄(互相遞迴⇒巢狀重複計)已入帳:量時間前先問「這個計時區間會不會包含它自己」
---

# ①#5：**收，而且我講清楚為什麼【計數】足夠**
```
need_keep 每決策 123.31 → 125.79（+2.0%）｜_facility_deficit 56.00 → 53.90（−3.8%）
```
★**我 spec 要求 perf 的理由是明確的**：**`:139` 是【重算】不是取用（存起來會凍住＝換一個恆等）**
⇒ ★★**所以要防的風險是「重算會不會多呼叫」** —— **而那正是計數在量的東西。**
★★★**而導出本身加的算術是【一次乘法】** ⇒ **每次呼叫的成本沒有變** ⇒ **計數平 ⇒ 成本平。**
★**你先寫的誠實限也對**：**A 類 evaluator 本來就在大量呼叫 `need_keep`**
⇒ **「導出引入了這些呼叫」是錯的框架** —— **它們本來就在那裡。**

# ②段級 phase 計時：**降級成 merge 後的具名回訪，不擋 merge**
```
★理由:為 +2% 的呼叫數去等一個【獨佔機器】不成比例(warring 還在跑)
★★而它【不是取消】:綁在【下一個獨佔窗】上 —— warring 收工後你本來就會有一個
★★★若那時 phase 計時顯示【非平】⇒ 回報我,那會是一個【merge 之後才發現】的成本
   —— 我明寫這個風險,而不是假裝計數已經涵蓋時間
```

# ③★★★你主動作廢自己的數字那一步，已入帳成通則
```
★need_keep 與 _facility_deficit 【互相遞迴】⇒ 函式級 us_total 巢狀重複計 ⇒ 120 秒是假的
⇒ ★★而它【不是獨佔與否的問題】:就算獨佔跑,那個數字一樣是假的
⇒ ★★★通則:量時間之前先問【這個計時區間會不會包含它自己】
   ⇒ 而「呼叫次數」在這種結構下反而是乾淨的:★次數不會巢狀重複,時間會
```
★**這是今天你第三次主動作廢自己的數字**（哨兵、窗不同、這次的時間欄）——
★★**而三次都是【自己抓到的】，沒有等我查。**

# ④merge 計畫（★讓你不用等我）
```
★剩下的唯一 blocker = #4 determinism 三跑一致
⇒ ★★三跑綠了就【直接回報】,我這邊接:merge → 跑全 15 支閘 → push
⇒ ★★★而 goal_fate 拆解仍然想要,但它是【便宜確認】不擋 merge
   (若解釋是 already_built ⇒ 那是「秤說話之後世界真的不一樣」的直接證據,比 tie 降了更值錢)
★cap 那顆:merge 之後另開票,走 R②(單調壓縮保序 + 尺度參數不能手填)
```
