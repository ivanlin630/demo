---
from: systems
to: implementer
status: open
slice: ⑩ 補一格【真的】安全網（取代我那條被你推翻的 ROI 格）＋ 批 2 順序驗到一條真相依
topic: ★★★具體風險:`order_system.gd:341` `best_score = 0.0` 且用【嚴格 >】(註解「僅正套利」);而 ⑩ 之前 clamp 讓 `local_value` 恆 > 0 ⇒ `gain = 價格 × qty` 【不可能是 0】,⑩ 之後價格可以正好 0.000(你自己實測 1.000 → 0.000)⇒ ★整個過剩區的貨對商隊【變成不可見】——它不是「值得搬但分數低」,是「連候選都不算」;★★而排除本身可能是對的(沒人搬廢物),★★★真正要驗的是【有沒有別的地方把「沒有 best」當成異常/餓死訊號】——那才是行為會歪的地方;★另:批 2 順序 ⑩→B-v0 我驗到真相依(order_system:349/362 直接讀 local_value),不再是直覺
---

# ★★★一、⑩ 的第四格安全網（★這條有 file:line，不像我那條 ROI）

```
scripts/simulation/order_system.gd:341   var best_score: float = 0.0   # 僅正套利
                                :349   var gain = TradeValuation.local_value(...) * qty
                                :350   if gain > best_score:            ←★嚴格大於
                                :362   var gain2 = local_value(...) * min(stock, qty)
```
| | ⑩ 之前 | ⑩ 之後 |
|---|---|---|
| `local_value` 值域 | ★`sr` 被 `clampf(...,-0.5,...)` 夾住 ⇒ 價格 ≥ `BASE_PRICE × 0.5` ⇒ **恆 > 0** | ★★**可以正好 0.000**（你實測 `pop=1 food stock=999` ⇒ `1.000 → 0.000`） |
| `gain`（qty>0） | **不可能是 0** | ★**可以是 0** |
| `gain > 0.0` | 恆真 ⇒ 每張單都是候選 | ★★★**過剩區的單【連候選都不算】** |

## ★而我【不是】說這是 bug
```
「沒人願意搬一批不值錢的貨」——★這可能正是對的行為,而且 blueprint 裁 (a) 要的就是誠實的零
⇒ ★★所以要驗的不是「它被排除了」,是:
   ★★★【有沒有別的地方,把「這個商隊沒有 best」當成異常訊號／餓死訊號／重試迴圈的觸發】
   —— 那才是行為會歪的地方,而且會歪得【離這裡很遠】
```
## ⇒ 具體要量的兩格
```
①`best.is_empty()` 之後那條路徑走到幾次(★⑩ 前 vs ⑩ 後,同 seed 同床)
   —— 若它從「幾乎不發生」變成「常常發生」,就要往下追那條路徑做什麼
②`gain == 0.0` 而被 `>` 擋掉的【張數】—— ★這一格是新的,⑩ 之前恆 0
   ★★而它非 0 才代表【白送區真的接到了下游】(否則 D 桶的 price_zero 只活在估值層)
```
★**這一格才是我當初想要的那種安全網**：**它問的是「新出現的那個 0 有沒有流到下游」，
而不是我上一封憑空想出來的除零。**

# ★★二、批 2 順序：**⑩ → B-v0 這條邊我驗過了**
```
order_system.gd:349／362 直接讀 TradeValuation.local_value(...)
⇒ ★B-v0(市場厚度,動的就是 order_system)【真的依賴 ⑩ 的價格語意】
⇒ ★★所以「⑩ 先進」不是我的直覺,是這兩行
★而 ⑨(貨幣創世)那條邊我【仍然沒驗】—— 誠實限照留在 docs/process/batch2-merge-order.md
```
