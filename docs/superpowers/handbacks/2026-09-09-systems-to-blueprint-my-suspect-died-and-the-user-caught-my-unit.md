---
from: systems
to: blueprint
status: open
slice: exp 卷判讀 ＋ 用戶 reframe 收下
topic: ★★★我的頭號嫌犯【被我自己給的判別式殺掉】：99.1% 隊戰術>0 ⇒ `tact<=0` 早退不是根因;真根【更上游】——`TASK_TRAIN` 六次快照五次【0 隊被選中】⇒ ★不是執行端擋住,是決策端幾乎不選它｜★★塌陷候選①【正式死】:extort 拒絕 582 次 > 0｜★★★用戶戳的量綱我收,而它是今天同一條的【第四層】:我量了【存在】,而問題問的是【夠不夠】
---

# ① ★我的假說死了，而殺它的是我自己寫的那一格

```
我寫：「頭號嫌犯 training_system.gd:19 `if tact <= 0.0: continue`」
   ★並在票裡寫死①那一格是【判別式】：「若戰術普遍 > 0，我的頭號嫌犯就錯了，答案在④」
實測：★99.1% 的隊 戰術 > 0 ⇒ 假說【證偽】。
```
⇒ **真根在更上游**：`TASK_TRAIN` **六次快照有五次是 0 隊被選中**
⇒ ★★**不是執行端擋住，是決策端幾乎不選它。**

★★★**而這把整條線挪到另一個科**：原本我以為是「訓練這條供給路被一行早退掐死」，
實際是「**訓練這個選項在秤上贏不了**」⇒ **它屬於 util／決策層**，
而那正好是 `失敗反饋`／`統一決策框架` 那條線的地盤，不是 anon tier 的地盤。

# ② 塌陷候選①：**正式死**

```
勒索：extort 582 次、而【拒絕確實 fire】(>0)
⇒ ★★今天早上我判的「零拒絕是【觀測管道假象】——只有 accept 分支有 print」【坐實】。
⇒ 你那個「結局分布塌陷」候選①可以結案：★世界沒有塌，是日誌只印一邊。
```
★另兩題：`convoy` 5 次嘗試 4 次成交；`promote` 逐筆 64 樣本，
team17 平民 exp **0.5 → 26.4 緩爬**（門檻 50）⇒ ★**exp 有在累積，只是慢**
⇒ **「零來源」也死了** —— 而那正是我要那一格流量的原因。

# ③ ★而卷面有一欄要作廢（是我的錯，已修）

```
`exp.add.amount.*`（給出總量）不可信：anon_tier_system.gd:98 用 Probe.note()（＝peak）
而 sum 要用 add_amount()（probe_stats.gd:100）。★那一行是【我在票裡寫死的】。
⇒ 已派一行修法（`…-the-wrong-tap-api-was-mine.md`），
★★並明講【不用整輪重跑】：她的結論不依賴那一欄（那是 bump 次數不是 amount）。
```
★★★而我認的不只是「寫錯 API」：我在票裡**同時**寫了一個具體呼叫**和**一句模糊的
「沿用你慣用的形狀」⇒ **具體的被照做，模糊的沒有作用**
⇒ **一個具體的錯誤指令 ＋ 一句模糊的免責 ＝ 一個錯誤指令。**

# ④ ★★★用戶戳的量綱：收，而它是今天同一條的**第四層**

```
用戶：「都有不代表養得活」
⇒ 我的票量的是【存在性距離】(到最近 resource > 0 的格)
   而地理稀缺的真尺是【養活力距離】(到最近「撐得起我這隊」的地方)
```

★**今天同一條規矩的四層**：
```
層1 門檻【低於】世界的量        空家不返 40 vs 800
層2 門檻【高於】世界的量        SEEK 30 vs 地圖直徑 28
層3 容器 vs 內容                我算地圖直徑,而該算 d 的實際分布(2-3)
★層4 存在 vs 足夠（用戶戳的）   ★我量對了分布,但那是【另一個量】的分布
```
★★**層 4 最難自己發現**，因為**數字是對的、母體是對的、分布也是真的**
—— 錯的是**那個 predicate 回答的問題**。

★★★**而用戶給了旁證**：`GATE-A 離家 positional 餓 56-61%`（經濟 arc 舊讀數）
⇒ **位置性飢餓【已經被實測過】** ⇒ **地理稀缺可能早就存在，而我的卷說「沒有距離」**
—— 因為我量的 predicate 是 `resource > 0`，而世界的痛是 `regen < 我的 burn`。

⇒ **fork（山地食物→0 那刀）擱置** 我照收；**「養活力地圖」票我這輪派**（見下）。
★而 predicate **不用發明** —— code 裡已經有：
```
decision_context.gd:641-643（GATE-A home_food_productive）
   regen = REGEN_RATE[terrain][food] × tile.harvest_factor
   burn  = team.population × FOOD_PER_PERSON_PER_DAY
   productive = regen >= burn        ★★這就是「養得活」的既有定義,同源不手抄
```
