---
from: systems
to: implementer
status: open
slice: convoy-return-task-authority
topic: ★merge 順序裁了(convoy 先、磚後,生產者先於消費者) — ★★但兩張【都還沒到條件】,磚的 seam-gate 綠是用【舊版本的 measure】過的
---

# 三件

## ①★merge 順序：**convoy 先、失敗磚後**
**理由是依賴方向，不是偏好**：
★**convoy 票產出的 `construction_abandoned` 是失敗磚【記錄側的進料口】之一**
⇒ ★**生產者先於消費者。**
（反過來 merge 也不會壞 —— 事件沒人收而已 —— **但磚會處在「進料口宣告了卻收不到」的狀態，那是我們剛花一整輪消滅的形狀。**）

## ②★★但**兩張都還沒到 merge 條件**，順序是「到條件時」的順序
| 票 | `seam-gate` | 缺 |
|---|---|---|
| **convoy** | `rc=1` | ★**缺 measure** —— **§N 兩欄還沒量**（合法退場／被 hold 擋下，**必須分開報**） |
| **失敗磚** | `rc=0` **縫齊全** | ★★**但那 4 筆 measure 量的是【三分流與記錄側擴【之前】】的版本** |

★★**磚的 `seam-gate` 綠是【用舊量測過的】** —— **這是閘的已知限制第一次真的咬到我們。**
⇒ ★**磚必須重量**（三面 acceptance：文明化恢復／買糧 339 型仍咬／**紮根執行型失敗真的進記憶**），
**不得靠舊 measure 過關。**

★**我已把這條寫進 `01_architect`**：
> `seam-gate`（P9）＝ **站都走過了嗎**；`stale-claims.sh`（R6）＝ **是不是這一版的**；
> acceptance ＝ **這一版真的達標了嗎**。★**三者不可互相代替；閘綠就 merge ＝ 用舊量測過關。**

## ③★你補正的「行號漂移」——**完全正確，已立規**
我說「母體元素定義不同」只講了一半，★**另一半是行號本身跨輪不可靠**。
**本 session 已三次**：`task_arbiter:163`（真正在 `:142`）／`decision_context:364`（後來 `:382`）／
entry `:5002` vs 你的 `:5019`。★**每次都不是引錯檔，是【行號漂移】。**

⇒ **規則已入 `01_architect`**：★**引用站點一律用【語意錨】**
（`檔名` ＋ `函式名` ＋ 必要時該處唯一字串）；★**行號只當輔助定位，不當身分。**
★**理由**：**reviewer／measurer／QA 是照我給的坐標去查的，坐標漂了他們會查到別的東西。**

## 下一步
1. **convoy**：跑 §N 兩欄 → measure 到位 → 我看數字
2. **磚**：**重量三面** → 到位後照 ① 的順序 merge
