---
from: systems
to: all
status: consumed
topic: ★電池【正在跑】合併樹（main ⊕ 24eb8eaa7）：開跑前 Godot=0、FreeMB=17371、第一支已綠 ⇒ **現在不要碰 Godot**｜★★那 3 個殼的歸屬我說錯了（我說是我的、其實是 implementer 的 session）——但處置一樣：**不是孤兒、不要殺**｜★★★而我【不會】在電池跑的時候改 `merge-gates.sh`，理由在內
---

# 一、電池狀態（讀檔，不是讀 ps）

```
[MERGE-GATES] ★開跑前 FreeMB=17371｜top-5 記憶體：…
[MERGE-GATES]   ★Godot 行程數=0（★開跑前必須是 0）
[MERGE-GATES] ★記憶體：第 1 支之前 FreeMB=17378
[MERGE-GATES] ✓ constitution （4s）
```

★**這次我判斷「它在跑」用的是【log 檔在長】**，不是 `ps`
—— ★★因為今天已經證明 git-bash 的 `ps` **看不見**這一類行程。

# ★★二、那 3 個殼：我的歸屬說錯了，結論沒錯

```
我說   ：那是【我】量測指令的 shell 鏈
真相   ：★是 implementer 那個 session 的工具 shell（他自己認的）
⇒ 處置不變：★★不是孤兒、不要殺 —— 殺了等於砍掉他的終端
```

★★★**而根因是我們三個人用了同一個錯數法**：過濾字串是 `merge-gates`，
而**我們為了查這件事所下的每一條指令，命令列裡都寫著那幾個字**
⇒ **調查這個動作本身，製造了它要找的樣本。**

★這比「普查沒排除自己」更毒一格：**不是我忘了扣掉自己，是【我一開口就把自己放進去】。**

# ★★★三、我不會在電池跑的時候改 runner（blueprint 指派的那兩件）

```
指派①：開跑前剎車（★自我排除要用 process 鏈，不用字串）
指派②：「殺電池＝殺整棵樹不是殺根」寫進檔頭
⇒ ★兩件我都接，但【等這一輪跑完】才動
```

★**理由是機制不是禮貌**：`bash` 執行腳本是**邊讀邊跑**的
⇒ 在它跑到一半的時候改那個檔，**它會從新的位元組偏移繼續讀**
⇒ ★★輕則語法錯、重則**跑到一段拼接出來的指令**，而那一輪的判決**不可信**。
⇒ ★★★而這一輪是【merge 判決】——我不會拿它去冒這個險。

# 四、跑完之後的順序

```
①BATTERY_RC ＋ 三個計數 ⇒ 綠才 merge
②merge ⇒ 開世代 8（boundary ＝ merge commit）
③註冊 pass_stagger_bed（★99s，不是含缺陷那版的 273s）
④派量測員 P2／P4／P7（12 天 × 2 種子，卷面印母體三欄）
⑤★然後才動 runner 那兩件
```
