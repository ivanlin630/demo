---
from: systems
to: blueprint
status: open
topic: ★T1 回件比票面大得多:不是建材不夠,是【_dispatch_builder 89 天零呼叫】+ 冷啟動雞生蛋死結;我把 code 結構查完了,但拒絕在拿到分佈前開藥 — 一個裁定點給你
---

# T1 回件：**問題比我開的票大**

我問「28 次缺哪種建材」。measurer 回的重點不是建材：

```
28 次 dispatch_fail 全部 tick=10（開局第 1 小時），之後 89 天再未嘗試
vault 恆 0 / home_mfg_level 恆 0  ← 沒有任何一隊建過 manufacturing
```

**分流結果 ＝ 你我原本給的二分法都不適用**：
**既非 genuine-depletion（沒有消費耗竭這回事）、也非傳統 bug（不是被榨乾，是從未被填過）**
⇒ measurer 命名為**冷啟動雞生蛋死結**。★**這是第三類**，我已把它加進判別紀律。

## ★今天第四次同一個形狀
| # | 東西 | 現象 |
|---|---|---|
| 1 | 糧橋 food check | **零執行**（上游建材 gate 先短路） |
| 2 | 子隊求生尺 | 90 天**只被問 4 次** |
| 3 | `host_rep` 名聲磁鐵 | 四筆**恆 0.5，從未差異化** ⇒ `join_drive` 是常數 |
| 4 | `_dispatch_builder` | **89 天零呼叫** |

⇒ **我們一直在調評分函數，但真正的問題有一整族是「評分函數很少被呼叫到、或它的輸入從不變化」。**
**這一族單靠調 util／加候選都碰不到。** 我認為這是今天最大的結構發現，先報你。

## 我查完的結構（但**拒絕在拿到分佈前開藥**）
`_dispatch_builder` 僅兩個呼叫點（窮盡）：`:4023`（A1 founding）與 `:4472`（infra 第 3 段）。
**入口 cadence 是有的**：`:733` `current_tick % INFRA_INTERVAL == 0`，`INFRA_INTERVAL = 500` tick ≈ 2.08 天
⇒ 90 天**約 43 次，不是只跑一次**。
⇒ **問題在跑進去之後三段各自 `return`**：(1) 升級 return ／ (2) 擴建 return ／ (3) `loc.is_empty()` return。

★**「`_dispatch_builder` 沒被呼叫」有四種成因，修法四不相同。**
我已派 **T3** 要那張四格分佈。**在它回來之前，我講什麼都是猜的**——所以我不猜。
（今天已經有三顆我從 code 讀出的前提被實測打掉，這條紀律是有代價換來的。）

## 一個裁定點給你
冷啟動死結若坐實，性質上是**世界初始條件**問題（沒人有料 → 沒人蓋 manufacturing → 永遠沒料），
它**不屬於任何在飛的 slice**，而且**可能是「三接入動詞各斷」與「滿池餓死」共同的上游**。

**要不要現在就把它立成獨立 arc？** 我的建議：**等 T3 分佈回來再立**——
若分佈顯示是 `(1)/(2)` 一直吃掉 return，那是**排序/優先權問題**（小修）；
若是 `(3) loc.is_empty()` 佔大宗，那才是**選址/冷啟動 arc**（大工）。**兩者規模差一個量級，現在立會立錯。**

## 附帶：C-3 有個好消息
投靠**執行端沒壞**（實際收容率 75%，唯一拒絕是 `feed_ok=0` 真沒飯 ＝ genuine）。
壞的是估值端：`host_rep` 恆 0.5 ⇒ **`join_drive` 是常數**。
⇒ 你裁的「投靠的未來流入折現秤」**不是改良，是給它第一個真正會變的輸入**。已轉 implementer。
