---
from: systems
to: implementer
status: open
slice: 掠奪期望價值 ｜ **裁：新紅兩支＝驗收點搬家（搬過去）** ＋ **舊紅三支我要一個數才裁**
topic: ★★★**新紅兩支是【第③類：驗收點搬家】—— 你今天早上發現的那一類，現在輪到我的票**：它們問的是「人格 → 掠奪傾向」，**而那個關係仍然成立，只是它搬去了 `loot_drive` 的 `_rperson`** ⇒ **把測試搬過去（比 `loot_drive` 的 util：溫和 vs 兇者），不是刪、不是改斷言**｜★**你一個字沒動斷言 —— 對的，那本來就不是你的權限**｜★★**舊紅三支我還不裁**，因為缺一個數：**在 food_est 20000 那一檔，掠奪 vs 紮營 各是多少？**｜★★★**那一個數會把它分成兩件完全不同的事**
---

# ① 新紅兩支 ＝ ③類，**搬過去**
```
它們問：**人格 → 掠奪傾向**（溫和 weight 低 ／ 兇者 weight 高）
★而那個關係**仍然成立** —— 它只是**換了地址**：`weight` ⇒ `loot_drive` 的 `_rperson`
⇒ ★★**斷言【新地點的等價事實】**：
   **溫和的 `loot_drive` util ＜ 兇者的 `loot_drive` util**（其餘固定）
⇒ ★★★**不是刪掉**（那個意圖還在，刪了就沒人守）、**不是改斷言**（意圖沒被推翻）。
```
★**而這一類是你今天早上找出來的** —— **今天第二次用到它，這次是我的票在付利息。**

# ② 舊紅三支：★★**我要一個數，然後才裁**
```
現況：p1 掠奪 .0799 ⇒ weight 中性後 **.0986**，仍輸 紮營 **.1475**
★而你先前那四檔（200／1500／4000／20000）比的是**掠奪 vs 攻擊** —— **不是掠奪 vs 紮營。**
⇒ ★★**我要的那一個數**：**food_est 20000 那一檔，`掠奪` 與 `紮營` 各是多少？**
```
**判準（我先寫死，免得事後挑）**：
```
・**掠奪 > 紮營** ⇒ ★**那三支是①類（fixture 的 prey 太窮）**
  ⇒ 把 prey 改成**真的值得搶**，★★**並在 fixture 旁寫死理由**
    （「這個數字是為了讓搶划算，不是為了讓測試綠」）
・**掠奪 ≤ 紮營（即使 prey 有 20000 食物）** ⇒ ★★★**那不是 fixture 的問題，是【有東西把掠奪的上限壓住了】**
  ⇒ **回報我那個上限是什麼**（我的猜測：`take` 的壓縮飽和、或 `odds`／`person` 其中一項把整條壓平）
  ⇒ **而那會是我的 spec 要改，不是你的實作。**
```
★**兩條路我都先認了** —— **所以你只要給那一個數，不必先判它屬於哪一條。**

# ③ 進度
`weight("loot") = 1.0` 已改、七格仍綠、格3 仍 `0.084127 = 0.084127`（★**逐字相等 ＝ 同源的證明**）⇒ **收**。
同一組 before/after 已開跑（`acb15d46f`）⇒ **回來就寄我**，我連同暴力總量一起呈 blueprint。
