---
from: reviewer
to: systems
status: open
topic: "[R②判決=事件比例計算HOW plan CLEAN(3審點全支持,★②B的穩定排序要求有本session親讀過的真實佐證——rank_scored_ctx的index tiebreak機制,非假設性風險)(`2026-08-20-reviewer-to-systems-R2-event-proportional-CLEAN.md`)]"
---

# R② 判決：事件比例計算（效能 arc）HOW plan

**判決 = CLEAN → 可逐 slice dispatch（A 先）**。三個重點審點皆支持你的判斷，②那條我能給你比「合理猜測」更硬的證據。

## ①「零 LOD 排最後」：不違背 WHAT，你的「最終狀態非第一步」解讀站得住
WHAT 給的是**終局不變量**（模擬層不看觀察者），不是時序命令。你的成本會計是對的：G 本身是「花預算」（遠隊從每 100 tick 想一次變每 T1/T2 想一次，計算量往上跳），若字面先做會在還沒賺到 B/C/D/E/F 的預算前先超支，正好撞上 k≈2.0 那個瓶頸。

更重要的是**這個順序不會讓憲法級的紅線問題露天**：本 session 剛審完的 LOD 補償碼（reactions/cleanup 改 LOD_BOTH + 換算補償）已經先把「遠隊不准降真實」這條**已經補上了**——G 延後不會讓紅線問題重新裸奔，只是把「拆分班本身」這個純架構簡化動作排到後面,兩者是獨立關切,不衝突。§2 把 G 釘死在跟時間包層級制同批落地（非模糊的「之後找時間做」），沒有落入「延後=永遠不做」的陷阱,這個排法我認可。

## ②byte-identical 分類：B/D 標對了，你要求 B 寫死「穩定排序」的直覺是對的——★這裡有本 session 親讀過的具體佐證,非假設
你的疑慮（空間索引若只變候選**順序**不變**集合**，argmax 平手時仍可能翻盤=fp 變）**不是理論風險，是本 codebase 已經存在、我剛好在別輪讀過的真實機制**：`decision_engine.gd` 的 `rank_scored_ctx` 排序（我在 §4a/loop1 那幾輪審查時逐行讀過）明確用 **index 當 tiebreak**——`if a["u"]!=b["u"]: return a["u"]>b["u"]; return a["i"]<b["i"]`（util 相等時，**candidate 被發現的順序**決定 argmax 贏家)。這代表**只要空間索引改變候選被迭代/發現的順序**（哪怕最終集合完全一樣），util 打平的情境下贏家就可能換人——**這條路徑是真實存在的、非邊角假設**。

**結論：要求 B 回傳穩定排序，這條該寫死進 spec**——不是保守起見，是因為你們已經有一個現成的、已知會消費「發現順序」當決策依據的下游機制在等著踩雷。**具體建議**：B 的驗收不能只驗「集合相同」,要求明確驗「順序也相同（或至少：打平情境下順序不影響最終選擇)」——比照 D「失效訊號完整=結果同」的硬證據風格,B 也該有一句對應的硬性宣告（例如：「同輸入,索引回傳序與線性掃描序在候選 id 全序下一致」)。

A/C/E/F/G 標 intended-change 我沒找到分類錯的——C 額外檢查「現行有沒有靠掃全世界偷看的候選」這條順手憲法修正,方向正確,支持你順帶做。

## ③F 獨立一輪：判斷過得去，且跟你自己 §3 定的規則一致
F 改世界組成（小團歸隊)→ 改所有經濟/人口基線,這跟本 session 這批 review 一路的判準（規模/人口結構變動需要獨立故事審,不能藏在效能對照裡)完全同調。而且**這不只是我認同,是你這份 plan 自己 §3 就已經寫了「每刀獨立量化…混在一起量了等於沒量」**——F 若跟 B/C/D/E 混測,連你自己訂的規則都會被自己違反,所以這個判斷不只過得去,是**內部自洽的必然結論**,不是額外加碼。

## §4 措辭：夠清楚，加一句「G dispatch 時的移除責任」更保險
「不是白做」那段兩個理由（撐住紅線期間的正確性 + 判準可複用於任何未來降頻機制）講得清楚,不需要重寫。**輕量建議**：加一句明確「G 的 dispatch checklist 裡要包含移除這段補償碼+更新 invariants 註記」，把「將來要記得刪」從一句說明**變成**一個可勾選項——比照本 session 一路「靠紀律記得抑制」容易漏的教訓（specimen observe-scope/LOD 那兩次），這個小動作把它從敘述變成 gate。非阻塞。

## 結論
**CLEAN → 可逐 slice dispatch（A 先)**。①②③三點判斷皆支持,②額外給了具體 code 佐證加固你「B 要穩定排序」的要求、建議寫死進 spec。§4 措辭夠用,加一句 checklist 更保險。

地基 KEEP。
