---
from: implementer
to: systems
status: open
slice: 效能線 ｜ 設施存在索引（DISPATCH 交件）
topic: ★★★**牆鐘 184.7–357.4 s → 34.4 s（Probe=ON）／25.7 s（OFF）**；`_team_has_facility` **781.8–1081.1 → 5.7 us／次**；子樹佔比 **45% → 11.58%**｜★shadow **194190 次比對／0 不一致**；fp **三趟全同**｜★★而成對對照的結果**與 spec 預期相反**：關掉短路只從 5.7 → 4.5 us ⇒ **贏的是【索引】不是【短路】**｜★headless 回歸與 base **逐條相同**
---

★落地：commit `b56aa942a`（main）｜`docs/measurements/2026-09-10-facility-index-result.txt`
  ＋ `scripts/simulation/facility_existence_index.gd`

# ① 驗收六格

```
①★★★語意逐次相同：shadow **194190 次／0 不一致**（母體 > 0 ✅）
②成本：`_team_has_facility` **781.8／1081.1 → 5.7 us**；`_supply_chain` self **3898.5／7610.5 → 40.1 us**
   ★舊掃 tile 訪問**實測 122,533,890 次**（≈1.23 億，與先前估算吻合）
③★成對對照：**沒有回到現況量級**（見 §②）
④fp：`16bb6924789c18ac031e61d34c2aeae7` —— **新／關短路／Probe OFF 三趟全部相同**
⑤★牆鐘：**ON 34.4 s／OFF 25.7 s**（兩種狀態都跑，界限 45）；子樹佔比 45% → **11.58%**
⑥★母體：**有據點的隊 18／58（31.0%）** ⇒ 短路能吃掉的是另外 **69.0%** 的呼叫
＋ headless 回歸：斷言集合與 base **逐條相同**（7 條、HARD-FAILS 皆 3）
```

# ② ★★成對對照的結果與你 spec 的預期相反（★而它把功勞歸對了）

```
spec ③寫：「把短路關掉 ⇒ ②必須回到現況量級」
★實測：關掉短路 ⇒ gating **5.7 → 4.5 us／次**（★★不但沒回到 781.8，還更快一點點）
⇒ ★★★原因：**兩條路現在都不掃地圖了** —— 短路只是省掉「查一次聚合表」，
  而**真正的贏家是聚合索引本身**。
⇒ ★所以正確的對照組是 **shadow 那一趟**（它仍然跑舊全圖掃）：**1081.1 us／次**
  ⇒ 索引 vs 舊掃 ＝ **5.7 vs 1081.1 ＝ 190×**。
⇒ ★★而我把這件事寫進測量檔，因為它會改變**下一個人對這張票的理解**：
  ★★★**「短路」聽起來像主角，而數字說它是配角。**
```

# ③ 失效點（★裸掃列出 —— 你說列舉是這張票的一半）

```
掃法：對 8 個 `current_level_key` 做兩種形狀的裸掃
  ①`\.(key)\s*(=|\+=|-=)` 直接賦值　②`.set(` 帶【變數鍵】（★逐處看上下文，不靠正則猜）
production 命中 **3 處**，全部已呼 `FacilityExistenceIndex.invalidate()`：
  `outpost_system.gd:467`（設施完工 +1）／`:508`（拆除據點時各設施歸零）／`:764`（拆單一設施）
★同名但不是世界狀態：`village_estimate.gd:29`（`VillageEstimate.farming_level`）⇒ 不影響索引
★★debug／床**直寫** `tile.farming_level = N`（`headless_test.gd` 等 32 處）**不會 invalidate**
  ⇒ ★★★而那正是 shadow 的用途：它在床上把 stale 抓出來（本輪 **0 不一致**）
共用失效：`OwnerOutpostIndex.epoch`（owner 變／`outpost_level` 跨 0／`erase_teams` 死亡釋放）——照你裁的，不自己列一份
```

# ④ 誠實限

```
①牆鐘的改前值橫跨 184.7–357.4 s（機器負載）⇒ ★而改後 34.4 s **低於所有改前值**
  ⇒ ★★這次的差距**大到噪音吃不掉**（其餘幾輪我都只敢談比例）
②★子樹佔比 45% → 11.58%：**分母也變小了**（牆鐘 250 → 34 s）
  ⇒ ★★★所以這個比值下降的意思是「**這棵子樹不再是主角**」，不是「它變成 11% 的絕對量」
③`_supply_chain` 的呼叫次數**沒變**（39186）、gating 次數**沒變**（194190）⇒ 語意未動的旁證
④★而 `gap` 迴圈仍然 **0 次** —— 這條路上「確認我沒有那個設施」這件事**還是每次都在做**，
  ⇒ ★★只是現在**便宜了 190 倍**。★★★要不要讓它**根本不必問**（例如把 gating 上移），
    那是**設計問題**，我沒有碰。
```
