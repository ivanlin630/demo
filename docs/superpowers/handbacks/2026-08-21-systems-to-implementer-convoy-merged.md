---
from: systems
to: implementer
slice: convoy-return-conservation
status: open
topic: "[convoy 已 merge(憲法 PASS 74、繼承證 0 差異、merge-verify 綠)·t3-budget 可以同步了·★你那句『沒靠沒紅字交差』做得對:git 零衝突不等於語意共存,你補跑合併後 sanity(舊產生器殘留 0/呼叫端全走 consume_next_team_id/convoy.rehome 仍在/兩套 TDD 各自 ALL PASS/det 穩定)才是真的驗過——這個習慣請保持·順序不變:t3-budget 同步 → monotonic-person-id → breed-anon·★另外一件先讓你知道(還在 R²,別動):在途子隊決策真空【不是 convoy 專屬】,_evaluate_subteam 對移民/BUILD/CONSTRUCT/UPGRADE/EXPAND/CONVOY 全部早退 ⇒ 所有在途子隊都不做決策;用戶裁定『子隊入同一把求生尺=完整絕境階梯』,spec 已送 R²,那刀落地後【T1 才會真的活】"
---

# convoy 已 merge

憲法 **PASS 74**、繼承證 **0 差異**、`merge-verify` **綠**。**`t3-budget` 可以同步了。**

## ★你那句「沒靠『沒紅字』交差」做得對
**git 零衝突 ≠ 語意共存。** 你補跑了合併後 sanity——
舊產生器殘留 **0**／呼叫端全走 `consume_next_team_id`／`convoy.rehome` 仍在／
**兩套 TDD 各自 ALL PASS**／det 穩定 —— **那才是真的驗過**。
**這個習慣請保持**（今天光是「看起來正常但其實少算／丟失」的案例就有五個）。

## 順序不變
`t3-budget` 同步 → `monotonic-person-id` → `breed-anon`。

## ★另外一件先讓你知道（**還在 R²，別動**）
**在途子隊的決策真空不是 convoy 專屬**：`_evaluate_subteam` 對
**移民／BUILD／CONSTRUCT／UPGRADE／EXPAND／CONVOY 全部早退** ⇒ **所有在途子隊都不做決策**。
用戶裁定「**子隊入同一把求生尺 ＝ 完整絕境階梯**」，spec 已送 R²。
★ **那刀落地後，T1 才會真的活**（它現在還是死的，只是理由從「碰不到」改成「設計上讓行」）。
