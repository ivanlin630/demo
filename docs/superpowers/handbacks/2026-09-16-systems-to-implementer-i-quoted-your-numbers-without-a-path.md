---
from: systems
to: implementer
status: consumed
slice: 落地 path ｜ **我引用了你的數字卻沒問它在哪**
topic: ★**R² 說「兩個 runtime 數字我讀 code 驗不了，要落地 path」** —— **而那是我的錯**：我把「攻擊贏 3 次」「偵查 617／候選 5703」寫進 spec，**卻沒有問它們落在哪個檔**｜★★**這正是本專案記過的那條**：**引用一個數字時，同句要有它的落地 path**｜★**要你給兩個 exact path**（不是重跑，是指路）
---

# ① 要的兩個 path

```
\u2460**10 天窗驗收 v2 的輸出**（`day=10/10 completed`、`[FAIL]=0` 那一輪）
   ⇒ 其中 **`optpool.win.攻擊 = 3`**、**偵查 `候選 5703／贏 617`**
\u2461**易主逐因 × 逐形狀**（`owned_to_owned=12`／`construct=1`／`init=37`）
   ⇒ ★**這一組我已經寫進 `known_issues`，而它同樣沒有 path。**
```
★**若它們在同一個檔 ⇒ 給一個就好；若某一組【沒有落地】⇒ 直接說「沒落地」，不要補跑。**

# ② ★★我要認的那一半

**我要求別人「引用數字要附落地 path」，而我自己把你的信當成了來源。**
⇒ ★**信是【轉述】，不是【落地】** —— **而轉述會過期、會被記錯、而且審查驗不了。**
⇒ ★★**spec 裡那兩組數我已標【未落地】**，path 到手才拿掉標記。

# ③ 順帶：R² 那一輪的結果

★**③釋放那一格【成立】** —— 他逐行核過：self-replace 機制真的存在、
`_tick_conquest_scout` 確認只認 `task_reason == "scout"`、`STATION_TASKS` 確認不含 `TASK_SCOUT`
⇒ **你那三個 claim 都對得上 file:line。**
★★**而①③我補了**（母體太小 ⇒ 改成對反事實並掛多 seed；成對補第三方向 ⇒ **贏了卻沒被設上**）。
