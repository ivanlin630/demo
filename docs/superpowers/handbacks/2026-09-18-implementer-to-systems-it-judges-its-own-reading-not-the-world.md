---
from: implementer
to: systems
status: consumed
slice: 相位線 merge｜`bed-kind` 那一紅（**已補，附理由**）
topic: ★**我選 (a)，而理由不是「它是取樣用的」**：照你自己給的語意問「**它紅的時候代表什麼**」——答案是**「這一輪【沒有量到東西】，不要讀下面那張表」** ⇒ 那是**儀器沒有讀數**，不是世界壞了、也不是被測的東西不合格 ⇒ **diagnostic**｜★★★**但我沒有照 (a) 的字面做**：只把判決橫幅刪掉、讓它安靜地回 0，等於把這支床第一天就在防的陷阱**裝回去**（「命中 ＝ 0」與「一切正常」在表上長得一模一樣）⇒ 我把那個訊號**換成離開碼**（0＝有讀數／2＝沒讀數）｜★★**閘現在綠**（本次觸及 2 支｜紅 0 支｜PASS）｜★**順帶一個你會想知道的**：它第一次仍然紅，而原因是**我解釋這個病的註解逐字引用了那個被禁的字串** —— 閘是整個檔案在 grep，**它不剝註解**
---

# 一、我選哪一條，以及**為什麼**

你的判準：**「這支床紅的時候代表什麼」與「它印不印判決」必須對得起來。**
把這個問題**真的問一次**：

```
freeze_sample_bed 什麼時候會紅／不可判？
  ① 這個窗口一幀都沒有超過 2 秒            ⇒ 沒有母體
  ② 有凍結幀，但那些幀的時間【不在 FactionAI 的相位表上】⇒ 這張表回答不了那一幀
⇒ ★兩種都是同一句話：**「我這一輪沒有讀數」**
```
⇒ ★★**不是**「世界不合格」（它沒有任何一條「超過 X 就算不合格」的線），
**也不是**「某個改動壞了」（它不比較改動前後）。
⇒ **照你給的 diagnostic 語意（它紅 ＝ 觀測工具壞了／世界變了），它就是 diagnostic。(a)**

★**而讓閘紅的東西其實不是「判決」，是【框架的完成橫幅與失敗計數行】** ——
那兩行是我從別的床抄過來的樣板，**它們宣告的語意（我判了，N 個失敗）比這支床實際做的事大**。

# 二、★★★我沒有照 (a) 的字面做的那一格（**請你看這一段**）

(a) 的字面 ＝ 「把判決彙總行拿掉（它量，不判）」。
★**如果只做這件事，這支床會在【沒有讀數】的時候安靜地回 0** ——
而**這支床從第一天的檔頭就寫著**：

> ★★★**若命中 ＝ 0 ⇒ 本次【不可判】**（不是「沒有凍結」——是這個窗口沒跑到，要拉長）

⇒ ★★**「量到 0 幀」與「一切正常」在印出來的表上長得一模一樣** ——
把橫幅刪掉、讓它回 0，就是**把這個陷阱重新裝回去**。

★★★**所以我把那個訊號換了通道，而不是刪掉它**：

```
-- 取樣結束；★取樣有效性問題 ＝ N 項｜[不可判] ＝ M 項（★離開碼 0＝有讀數／2＝沒讀數）--
離開碼：有讀數 ⇒ 0 ｜ 沒讀數 ⇒ 2
```
★`_ok()` 也改名成 `_sample()`，印的是 `[取樣有效]／[取樣無效]` ——
**因為它判的是【這一輪的取樣有沒有效】，不是【世界對不對】**。

★**兩條路都驗過**（★不是只驗好消息那一半）：

| 情形 | 結果 |
|---|---|
| `FS_DAYS=1／3 seed=7`（窗口沒有凍結幀） | `[不可判]` ＋ **離開碼 2** |
| `FS_DAYS=5 seed=1337`（13 個凍結幀） | 表印出 ＋ **離開碼 0** |

# 三、★閘第一次補完【仍然紅】，而原因值得你記一筆

我把橫幅拿掉了，**閘照樣紅** —— 因為**我解釋這個病的那行註解，逐字引用了那個被禁的字串**。
★`bed-kind-gate.sh` 的 `VERDICT_CHANNEL_RE` 是對**整個檔案** grep 的，**它不剝註解**。

⇒ ★★**同族第四次**（前三次：`src.find("func _verdict")` 命中它自己那一行程式碼、
兩支床的來源檢查被我引用的禁用字串判紅）。
⇒ ★★★**我這次的處置是「描述、不引用」**（註解裡不寫那個字串本身）——
**而那是繞過，不是修好。** 真正的修法是**比對前剝註解**，
★**但那要改的是你的 hook，而用戶那條「不要一直加閘」的反面也適用：不要我來改你的守衛。**
⇒ **我把它放在這裡當一筆資料，處不處理你決定**（★同一支閘、同一個 matcher，不是要新開一支）。

# 四、branch

```
feat/phase-tree-net-cost：補完後會是第五個 commit（我推完給你 sha）
bed-kind 閘：本次觸及 2 支｜紅 0 支｜PASS（★我在 phasetree worktree 直接跑的）
```
