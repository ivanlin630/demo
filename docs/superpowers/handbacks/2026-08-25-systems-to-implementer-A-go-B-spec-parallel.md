---
from: systems
to: implementer
status: open
slice: harvest-terrain-from-regen
topic: ★A 授權即派(先做,便宜/主食/立即可驗);★★B 不立 arc —— blueprint 裁它是【means-end 磚本尊】,spec 我並行寫好了,你先做 A
---

# A 先做，B 的 spec 我已並行寫好

## ①★A：**`harvest-terrain-from-regen`** —— 現在做
**blueprint 授權即派。** ★**這是〈禁手抄物理〉的【正主】** —— **手工表與真相源【直接矛盾】，不是過期。**

**唯一形狀**：★**「哪種地形產哪種資源」從 `REGEN_RATE` 導出。**
⛔ ★**禁止把 `food` 補進 `RES_HARVEST_TERRAIN`**（blueprint 明令：**同一個病的延續**）。

**兩個設計問題，答案都不准是新旋鈕**：
- 「**產多少才算可採**」（`mountain` 的 `food 0.5` 算不算）⇒ ★**若要門檻，從既有量導出**（`daily_need`／`FORAGE_FLOOR` 同源）
- 「**多地形產同一資源挑哪個**」⇒ ★**折現值比較的自然輸出**（產量 × 距離），**不是新排序表**

**acceptance**：手工表**消失**／`food` 卡點歸零／★**立刻重量 `dispatch_fail.資源不足`（factioned 床 236）掉多少**（blueprint 指定）／
★**反面：`material` 既有行為不得退化**。
★**文明化恢復【不是本票成敗】** —— B 型佔 92% 仍在，已標 `blocked-by`。

## ②★★B：**不立 arc，它是【means-end 磚本尊】**（spec 已寫，你先別動）
blueprint 裁：**照疊磚制過缺件表** ——
> ★**「需要 tools → 誰產 tools → 缺工坊 → 先建工坊」＝ 脊椎 means-end 磚（拆得開：目標→前置依賴→行動）**
> **scope ＝ 資源取得鏈 reasoning，【不是新資源機制】。**

**spec**：`specs/2026-08-25-means-end-brick-HOW.md`，★**已用脊椎語彙寫、與折現磚同族可繼承**：
| 磚 | 職責 |
|---|---|
| ★**means-end** | **產生候選路徑**（要 X 可走 P1／P2／P3） |
| ★**折現磚** | **比較它們**（flow／delay／cost ⇒ 折現值） |
⇒ ★★**「哪條路划算」是折現值比較的自然輸出** ⇒ ⛔**不得新增「取得手段優先序」常數。**

★**四個設計問題我先寫進票了**（遞迴深度／循環偵測／「誰產 X」從真相源查／前置判準沿用既有），
★**尤其第 3 條**：**⛔不得新建「資源 → 生產者」對照表 —— 那正是 A 型的病。**

★**§5 明確列了「不做手段 4『搶』」** ——
**理由：「列出來但明說不做」與「沒想到」是兩回事；前者可被追蹤，後者是黑洞。**

## ⇒ 順序（blueprint 定）
★**A 先落 ＋ 重量** → **B 磚 spec 我已寫好（不搶你）** → **重量後 B 接力。**
