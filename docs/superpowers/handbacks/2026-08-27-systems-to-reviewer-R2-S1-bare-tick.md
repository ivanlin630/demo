---
from: systems
to: reviewer
status: open
slice: S1-bare-tick-and-guard
tier: refactor
topic: R²請審 S1;★★★我最想要你打的是【延後清單】那條:現制半小時＝5 tick 而 hours() 只吃整數小時,我裁「不能精確表達的 S1 原樣不動、逐顆列進延後清單交 S2」——★★因為在 S1 動它要嘛引入整數除法等價風險、要嘛就是改值,兩者都弄壞 S1 的隔離性;★另兩問在末段
---

# 送審：`docs/superpowers/specs/2026-08-27-S1-bare-tick-and-guard-HOW.md`
**scope 已由 blueprint 重框**（原 S1 把「已經是宣告式的 39 顆 const」算進工作量了）：
★**S1 ＝ ①裸 tick 字面量盤點＋改 ②守衛（找出來＋擋住新的＝同一件事兩面）。**

# ★★★①我最想要你打的：**延後清單那條**
```
現制 TICKS_PER_HOUR = 10 ⇒ ★「半小時」＝ 5 tick，而 hours() 只吃整數小時
改成 hours(1)/2  ⇒ 現制 5 ✓，★★但那是整數除法，換根旋鈕後未必還等價
四捨五入到 hours(1) ⇒ ★★★那是【改值】，把 S2 的行為改動偷渡進 S1
```
★**我的裁定**：**(b) 類（不能精確表達的）S1 【原樣不動】，逐顆列進延後清單交 S2。**
★★**理由**：**S1 的命門是 `fp` byte-identical —— 那條讓重錨的行為改動被隔離在 S2 一支。**
★★★**在 S1 動 (b) ＝ 弄壞整個 slice 分解賴以成立的隔離性。**

⇒ ★**請打**：**(a) 這個二分（能精確／不能精確）夠不夠？有沒有第三類我沒想到的？**
**(b) 「延後到 S2」會不會讓 S2 的 diff 變得太雜**（S2 本來只該剩根旋鈕本身，現在多了一批裸 tick）？
★★**若你認為 (b) 是真問題，我想聽替代**：**例如給 `TimeScale` 加 `minutes()`，讓 (b) 也能在 S1 精確表達** ——
★★★**但那要先確認現制下每一顆 (b) 都能被整數分鐘整除，否則只是把問題推到下一層。**

# ★★②另兩問
1. ★**盤點的可執行判準**：§4 的原定義是「**時間語意**的 int 字面量」——★★**「時間語意」機器判不出來。**
   我改成**操作版**：「與 `current_tick`／`*_next_tick`／`TICKS_PER_*`／`elapsed_ticks`／tick 參數同一運算式的 int 字面量」＝候選，**逐顆人工判定、判定結果留在 code 裡（白名單註記）不是留在 handback 裡**。
   ★★★**這個候選集會不會【漏】？**（例如某處先把 tick 存進一個沒有 `tick` 字樣的變數再比較）
2. ★**守衛的陽性對照我要求「三處各塞一次（`%`／比較／加減），三次都要紅」** ——
   ★★**這是照你今天教我的「對照要驗機制被排除，不是結果剛好是零」那條寫的。** ★**夠不夠？**

# ★③我已經先擋掉的
★**`fp` 兩張床都要逐位元不變**（★**S1 純標籤、機制不變 ⇒ 兩床都該不變；任一床變了就是偷偷改了值，直接回退不解釋**）
—— ★★**這是照今天學到的「`fp` 預期要連【床】＋【機制有沒有 fire】一起寫」。**
★**白名單逐條要理由**（LOCKED §4 明令，★**否則守衛退化成橡皮圖章**）。
★**空的延後清單要證明是真的空，不是沒找。**
