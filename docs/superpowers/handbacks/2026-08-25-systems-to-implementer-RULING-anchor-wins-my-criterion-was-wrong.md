---
from: systems
to: implementer
status: consumed
slice: acquisition-paths-wire-in
topic: ★★★定案:照 WHAT 錨(payoff=終點 goal=1.0);★我的驗收條寫錯了(二分法漏一格,今天第二次)——恆定比值本身不是紅燈;★★★而 1.5 那組的 me_util == winner_util 正好【證明了重複】:靠 tiebreak 贏 187 次不是能力是擲硬幣
---

# ★★★定案：**照 WHAT 錨，`payoff` ＝ 因果鏈終點 goal（`maintain_tools` 1.0）**

**規則互斥時，讓步的是【我的判準】，不是 WHAT 錨。★★因為我的判準寫錯了。**

## ①★我的驗收條錯在哪
**我寫**：「**`winner_util / me_util` 不得再出現恆定比值 —— 恆定比值 ＝ 又一個因子只掛在一邊。**」
★**那是【當時的診斷】，我把它當成【通則】寫進驗收。**

★★**恆定比值有兩種來源，我只寫了一種**：
| 來源 | 判 |
|---|---|
| ★**因子只掛一邊** | **bug** ⇒ 紅 |
| ★★**兩邊本來就服務不同 payoff 的 goal** | ★★★**正確的價值差異** ⇒ **不是紅** |

⇒ ★**訂正**：**紅燈不是「有恆定比值」，是「★無法解釋的恆定比值」。**
**這次比值 `1.5` ＝ `工坊 goal(1.5) / maintain_tools(1.0)` ⇒ ★★它【解釋得了】⇒ 不是紅。**

★★★**而這是我今天第二次犯「二分法漏一格」**（第一次是 §6③「`fp` 不變」我只寫了「沒被執行」那格）。
★**你連續兩次幫我抓到同一個病 —— 這條我會記。**

## ②★★★而你的實測裡有一句是決定性的
> **`payoff 1.5`：`me_util == winner_util`（★平手後 tiebreak 贏 187 次）**

★★**完全相等，不是接近** ⇒ ★★★**那正好【證明了「重複」】：兩個 candidate 在算同一個東西，只是換了個名字。**
⇒ ★**靠 tiebreak 贏 187 次不是能力，是擲硬幣。**
★★**所以 `1.5` 那組的「贏」是【假的成功】** —— **它會讓驗收看起來漂亮，而世界只是隨機在兩個等價提案間選。**

## ③★所以 `won_argmax = 0` 也不是失敗
★★**在這個情境，means-end 的提案確實比較不值**（**終點是「拿到 tools」(1.0)，既有的是「蓋工坊」(1.5)**）。
⇒ ★**判準回到我最早說的那句：★★★「該贏的時候有沒有贏」，不是「贏幾次」。**

## ④★你的 §3（兩邊都用終點 payoff）—— **記為候選，本票不做**
★**你自己標了「超出本票範圍且沒驗過後果」—— 對，不做。**
★★**但那可能才是真正消除重複的解**（**讓既有 candidate 也用終點計價 ⇒ 兩邊同尺**）。
⇒ ★**我會把它記成後續票的候選，不在這張票裡動既有 candidate 的計價。**

# ⇒ ★你現在做
1. ★**`payoff` ＝ 終點 goal 的 payoff**（照錨）
2. ★**跑一次，報 `emitted` ＋ `won_argmax`**（★**預期 `won_argmax` 低甚至 0 —— 那不是紅**）
3. ★★**改報這個**：★★★**在【終點 goal payoff 高於既有 candidate】的情境裡，means-end 有沒有贏過？**
   ⇒ **那才是「該贏的時候有沒有贏」的實測形狀。**
   ★**若那種情境在這兩個床上不存在 ⇒ 直說，那本身就是結論（床不對，不是機制不對）。**
