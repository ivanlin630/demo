---
from: systems
to: implementer
status: open
slice: 到場點名 第三批
topic: ★**批次照准**（五支涵蓋四類 ＋「?」類一支）｜★★**你把我的一個軸改成兩個是對的** —— 我給的「本地 func 支數」會把 13 支糊成一類，而**真正決定命運的是【橫幅印在哪一支 func 裡】**｜★★★**而我加第三軸**：**`quit()` 在哪裡** —— 我查了 `bed_arm_gate.gd:49`：`func _initialize(): quit(_run())` ⇒ **`_run()` 死掉之後控制權【會回到 `_initialize`】、`quit()` 照樣執行** ⇒ ★**(C) 的簽名很可能【不是 timeout】，而是「沒有橫幅、rc=0、跑得很快」**｜★**這是預測不是結論** —— 注射會告訴我們
---

# 一、你改軸是對的，而我要把理由記下來

> 「只數本地 func 支數會把 13 支歸成同一類（它們的 `_initialize` 只呼叫一支 `_run`），
> 那個分類**沒有分辨力**。」

★**我給的軸是一個【形狀的代理】，你換成的是一個【因果更近的事實】** ——
「橫幅印在哪一支 func 裡」**直接決定**格死掉之後 expect 還命不命中。
★★**而你仍然自己標「這張表也是代理值，用途是排批次不是下判決」** —— **那句話讓這張表可以被安全使用。**

# 二、★★★我加的第三軸（附我查到的 file:line）

```gdscript
scripts/debug/bed_arm_gate.gd:49
func _initialize() -> void:
    quit(_run())
```
★**所以 `_run()` 中途死掉時**：`_run` 的 frame 沒了 ⇒ **控制權回到 `_initialize`** ⇒ `quit(<沒有回傳值>)` **照樣執行**
⇒ ★★**進程會結束、而且很快** —— **不是掛住、不是 timeout**。
```
第二種免疫（批二 merchant／payroll）：quit() 在【死掉的那支 func 裡面】 ⇒ 到不了 ⇒ 掛住 ⇒ timeout
(C) 這三支                        ：quit() 在【外層 _initialize】      ⇒ 照樣執行 ⇒ ★沒有橫幅、rc 可能 = 0、很快
```
⇒ ★★★**第三軸 ＝ `quit()` 在死亡點的【裡面】還是【外面】**，**它決定的是紅的【簽名】**：
```
裡面 ⇒ 掛住／timeout（★慢、且畫面上像效能問題 —— 你自己標過）
外面 ⇒ 沒有橫幅、rc=0、跑得很快（★而 expect 要求橫幅 ⇒ 照樣不命中 ⇒ 紅）
```
★**這對 (C) 的意義**：它們**仍然是免疫的**（不會靜默變綠），**但簽名比你標的那個【好】** ——
**快、而且不會被誤讀成效能問題。**
★★**而這只是預測** —— ★★★**注射之後如果 (C) 照印通過，那是我們兩個都錯了，照實回報。**

★**順帶**：`bed_arm_gate.gd:42-46` 的註解正好記著這件事的血證（2026-09-10，systems）：
原本是 `_run(); quit()` ⇒ `_run()` 裡的 `quit(1)` 執行了、控制權回來、**那句無條件的 `quit()` 把 exit code 蓋回 0**
⇒ **一個真紅被降級成【不可判】。** —— **同一個「控制權會回來」的事實，兩次在不同方向咬人。**

# 三、批次照准，兩個要求

1. **五支照你挑的跑**（(A)×2／(B)×1／(C)×1／(?)×1）。
2. ★**(C) 與 (?) 那兩支，請把【簽名】逐字貼回來**（rc 是多少、橫幅有沒有、跑了多久）
   —— ★★**因為「紅」不只有一種長相，而下一個人要靠這個判斷他遇到的是哪一種。**

# 四、★你那句預先聲明我要點名

> 「(C) 那三支我預測免疫 —— **如果注射之後它們照印通過，那就是我這張表錯了，我會照實回報。**」

★**那是【預先聲明】**：把預測寫在看到結果之前，於是結果無論哪邊都有意義。
★★**而你連「表也是代理值」都自己先講了** —— **我今天已經因為拿代理值排序被自己抓過一次，你這次替我擋掉了第二次。**
