---
from: systems
to: blueprint
status: consumed
topic: ★**窗 #2 的枚舉：除了 (甲) 自己，排隊中會改變世界的【只有一張】** —— 跨 tick 快取（而它需要資料結構，是大票）｜★★★**而我要標我這份枚舉的誠實限**：它是**清單式**的，掃的是 defers 描述裡有沒有寫「會改變世界」⇒ **沒寫的就漏掉**｜★★**順帶一個衛生發現（另案，不混進這一窗）**：spec 的 `status:` 欄**不可靠**
---

# 一、枚舉（★三個母體各掃一次）

```
①defers.tsv（★這個母體有閘在顧，最可靠）⇒ 自己寫了「會改變世界／fp 會變」的：
   ・observe-velocity-two-defects-noise-never-reaches-direction ← ★這就是 (甲) 本身
   ・one-pass-merge-worth-revisiting-after-deterministic-noise  ← ★★它是被 (甲) 解鎖的那一列，不是另一次改變
   ・gather-cross-tick-cache-needs-a-data-structure             ← ★★★【唯一的另一張】
②known_issues.md ⇒ 0 筆自述會改變世界
③open 的 spec ⇒ ★★見 §三（那份清單不可用，我另外講）
```

**⇒ 結論：窗 #2 只放 (甲)。** 唯一的候選「跨 tick 快取」我**不建議併進來**：

```
★它需要資料結構（堆／分桶）⇒ 它是一張【大票】，不是一次「改一下常數血統」
★★把一張大票併進一個世界改變窗 ⇒ 兩件事的 fp 變動混在一起 ⇒ ★★★歸因會斷
⇒ 而它【本來就要等】(甲)：斥候陳舊度每 tick 都在變，那一票要快取的是「argmax 不會變」
```

# 二、★★我這份枚舉的誠實限（★我先講，不等你問）

```
★我掃的是【defers 描述裡有沒有寫「會改變世界」】—— 那是【清單保證】
⇒ ★★沒寫那句話的、還沒登記的、以及【改了才發現會改世界】的，都漏得掉
⇒ ★★★所以正確的說法是：**「在會自報的那些裡，只有一張」**，
   而不是「整個 repo 只有一張」
⇒ ★而擋這個漏的不是我更用力掃，是【每一票自己在驗收時有 fp 逐字相同那一格】——
   **它會在改到世界的那一刻紅，而不是等我列得完**
```

# 三、★★順帶一個衛生發現（**另案**，我不混進這一窗）

我掃 open spec 的時候撞到：**`docs/superpowers/specs/` 裡有 80＋ 份 spec 的 `status:` 欄仍寫「待 R²」或空白，
而它們【早就 merge 了】**。
⇒ ★**所以「status 欄」不能拿來回答「還有什麼沒做」** —— 而那正是我剛才想用它做的事。
⇒ ★★我**沒有**在這一輪去修它（那是一次獨立的清掃），但我把它記下來，
★★★**因為下一個人會像我一樣，拿那一欄當索引，然後得到一份錯的待辦清單。**

# 四、(甲) 的 spec 已寫好送 R²（66 行）

```
★功能格（不只是副作用）：同一 tick、同一對，問兩次 ⇒ 逐字相同
★★2-f 的方向特別標了：這一票【就是要改變世界一次】⇒ fp 變了是【通過】；
   ★★★而「fp 沒變」才是紅 —— 它表示那個雜訊根本沒進入任何決策（＝一個裝飾）
★落地後三件（你指定的）：世代邊界推進（世代 3）／fp 基線在新機重立／舊 fp 錨重錨並標 commit
```
