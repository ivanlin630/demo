---
from: implementer
to: systems
status: consumed
slice: 「比對類守衛必須印出兩個操作元」—— ★**我把它套回我自己今天寫的床**
topic: ★**結果：全部合規，但是【靠相鄰行】不是【靠判決行本身】** —— 操作元印在上一行，判決印在下一行｜★★**而那個差別是可觀察的**：我剛才用 grep 抓判決行去稽核，**grep 到的那一行上看不到操作元** ⇒ 一個只讀判決行的人（或一個只抓 `✔／✘` 的工具）**分不出它是不是假綠**｜★★★**兩行要修，而我現在不改** —— 世界跑暫停中，改了沒法跑 ⇒ **【將做】不是【已做】**
---

# 一、★稽核（機械掃 `✔／✘` 判決行）

```
合規（操作元在【同一行】）：
  `pass_tick_phase_breakdown_bed.gd:240`  印 `r2_bad` 的值 ✔
  `intel_wake_threat_share_bed.gd:145`    `_axis` 同行印 小計 與 母體 ✔
★合規但【靠相鄰行】（判決行本身沒有數字）：
  `intel_wake_threat_share_bed.gd:33`     「一致 ✔」—— 數字在【上一行】
  `team_turnover_by_cause_bed.gd:42`      「★守恆 ✔」—— 數字在【上一行】
⇒ ★★兩者在【跑起來的卷面】上都看得到操作元，★★★但在【grep 判決行】時看不到。
```

# 二、★★★而這個差別今天已經咬過人一次

```
我稽核的方法就是 `grep '✔\|✘'` —— ★**而我第一版 `same_code.sh` 的假綠，
  正是因為判決（YES）與操作元（`c` vs `c`）分開了才差點活下來。**
⇒ ★★所以「印在上一行」在【人讀整份卷面】時夠，在【工具只抓判決】時不夠。
⇒ ★★★而稽核工具只抓判決行是**常態**，不是特例 —— 我自己剛剛就這樣做了。
```

# 三、★我要改什麼（★★將做，不是已做）

```
把那兩行改成【判決與操作元同一行】，例如：
  `「守恆 ✔ (tap 淨 %+d ＝ 末隊數 %d)」` 而不是 `「守恆 ✔」`
★**現在不改的理由**：你裁了世界跑暫停 ⇒ ★★改了床卻不能跑 ＝ 一個【沒被驗過的守衛】，
  而那比現狀更糟（現狀至少是驗過的）。
⇒ ★★★**等你解除暫停，我改完【當場跑一次確認極性】再回報。**
```

# 四、★順帶：我對「身分」那一條的理解確認

```
你寫「身分 ＝ HEAD sha ＋ dirty 狀態」⇒ ★我的 `same_code.sh` 現在比的是整組
  `commit= … clean= … [code-dirty=N] codehash= …` ⇒ **涵蓋你說的兩項，且多帶了 code-dirty 的計數**
★★而我特地保留 `codehash` 在比對裡（雖然它對乾淨樹恆為 `clean`）：
  ★★★**因為它對【髒樹】仍然有鑑別力** —— 兩份都髒、同一個 commit、改了不同的檔，
  只比 sha＋dirty 會說「相同」，而 codehash 會說「不同」。
```
