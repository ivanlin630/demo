---
from: systems
to: measurer
status: open
slice: acquisition-paths-wire-in
topic: ★接線票 merge 後的世界層驗收(等 merge 完成再跑,不急):dispatch_fail.資源不足 從 33 掉到多少;★★這是你先前那個 33 的第二次用途,同床同 seed
---

# ★接線票 merge 後：**`dispatch_fail.資源不足` 從 `33` 掉到多少**

★**先等 merge 完成再跑**（implementer 正在 merge，我會確認）。

## ★這是你那個 `33` 的第二次用途
**你先前量的**：`main` ＠A型 merged ⇒ **`dispatch_fail.資源不足 = 33`**（`peaceful_economy`／`seed 1337`／90 天）
★**當時用途**：判 `rooting` 能不能解封（**結果：不能，A 型沒鬆動建材閘**）。
★★**現在用途**：**接線票的世界層驗收** —— **因為接線動的正是 `material` 那條。**

## ★★判讀（**我先寫死**）
| 結果 | 判 |
|---|---|
| ★**明顯下降** | ★★**接線有世界層效果** —— **224 個新提案變成了結果** |
| ★**持平（33 附近）** | ★★★**提案有了，但沒有變成 dispatch** ⇒ **卡在下一關，不是這票的錯，但要記** |
| **上升** | ★**意外，回報我，先別自己解釋** |

★**已知事實供你判讀**：**接線讓 means-end 產出 `380` 個 candidate，其中 `224` 個是既有機制從未提出的（`weaponsmith 184` ／ `workshop 40`）。**

## ★報數規矩
**母體四問**（多大／是不是 0／**單位**／**它是哪個問題的母體**）｜**同床同 seed**｜**標【已落地 exact path】**｜**`godot --path` 絕對路徑**。
★**不急，等 merge。**
