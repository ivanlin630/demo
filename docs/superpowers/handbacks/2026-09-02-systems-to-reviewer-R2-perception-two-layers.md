---
from: systems
to: reviewer
status: open
slice: perception-two-layers（外觀層 belief 欄位）
tier: R2
topic: ★R② 審 spec:把 tags/current_task 外觀投影/combat_target 三個【外觀層】欄位進 belief,parent_team_id 留情報層;★★寫入點只有一個(vision_system.gd:130 的 snap),我查過;★★★而我要你重點打兩件:(a)`current_task` 的【外觀投影】要投成什麼——這是最容易偷渡「意圖可見」的地方 (b)那個寫入點【已經在耗 RNG】(randi_range),新欄位若帶雜訊會改動 RNG 序列
---

# 背景（藍圖已裁，不用審）
**感知兩層**：①**外觀層**（親見可得）進 belief，觀察事件寫入；②**組織/內心層**只能靠情報；
③**`unknown` ＝ 誠實第三態，禁 default-pass、禁 fallback**。（★選項「讀 live ＋ 標 gate-ok」已被否決＝那是 baseline 第 76 行的形狀。）

# ★①欄位歸屬（藍圖定，我只列給你對照）
| 欄位 | 層 | 進不進 belief |
|---|---|---|
| `tags` | 外觀 | ★**進**（旗號／裝備／隊形看得出來） |
| `current_task` | 外觀（**只投影，見②**） | ★**進，但不是原值** |
| `combat_target` | 外觀 | ★**進**（★★而「看得到在打」與「看得出打誰」是兩件事，見②） |
| `parent_team_id` | ★組織 | ★★**不進** —— 靠情報（relay），沒有就是 `unknown` |

# ★★②要你重點打的第一件：**`current_task` 的外觀投影**
```
★藍圖原話：「當下活動【外觀】」可見，「打算做什麼」不可見
⇒ ★★所以【不能直接記 current_task 的列舉值】——那個列舉裡混著【意圖】(TASK_BUILD＝我要蓋)
   與【外觀】(行軍中／紮營中／耕作中 看得出來)
⇒ ★★★我 spec 的是【投影函式】：task → 外觀類別(移動中／駐紮／勞作／交戰／不明)
   而【不是】把 current_task 塞進 snap
★這是整份 spec 最容易偷渡「意圖可見」的一格 —— 請你逐一看那個投影表，
  ★★問我「這一格是外觀還是意圖」，我答不出來的就該歸不明
```
★**同理 `combat_target`**：我 spec 成**兩個欄位**——`in_combat`(外觀，看得到在打) 與 `combat_target_est`(★**要看得到對手才有**)。
★★**請打這個拆分**：若你認為「看得到在打就看得到打誰」，那就併回一個，我不堅持。

# ★★★③要你重點打的第二件：**寫入點已經在耗 RNG**
```
vision_system.gd:118-131（親見寫入,唯一 seam,我查過）
  ★:127 scale = clampi(scale + randi_range(-1, 1), 0, 3)   ←★★已經在耗 global RNG
⇒ ★★★新欄位若【帶雜訊】(例如 tags 有機率看錯) ⇒ 會【多耗 RNG】⇒ 全世界 fp 位移
⇒ 我的傾向：★外觀層欄位【不加雜訊】(旗號要嘛看到要嘛沒看到,不是看錯)
   ⇒ 零額外 RNG ⇒ ★★而這也讓「fp 只因為新欄位進 snap 而變」變成可解釋的單一原因
★請你判：不加雜訊會不會讓外觀層【太可靠】,以致 belief 與 god-view 實質等價?
  ★★（★若會，那雜訊就必須加，而 RNG 位移是必須付的代價，不是可以避開的）
```

# ④驗收（★含藍圖批的「寫入證據」條款）
```
①★★★每個新欄位【必須有非零的寫入證據】——不是只有讀取端
   ⇒ 否則會長出第三種病：欄位存在而恆空 ⇒ 決策永遠篩不到人，而看起來像「沒人符合條件」
②★unknown 三態要分得開：有值／過期／從未觀察到 —— ★★三者的計數要各自一個桶
③★★篩選端：unknown 一律不通過；★★★要有一個【unknown 被擋掉幾次】的計數
   —— 否則「沒人符合」與「大家都 unknown」長得一樣
④fp 會變（新欄位進 snap）⇒ 差在哪要說得出來；★若有額外 RNG 消耗，必須單獨標出來
```
