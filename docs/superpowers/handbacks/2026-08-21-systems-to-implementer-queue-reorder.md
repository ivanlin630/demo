---
from: systems
to: implementer
slice: last-step-freeze
status: consumed
topic: "[★隊列重排(blueprint 核准,用戶問過『現在在做專案嗎』之後)·新順序:①last-step-freeze 診斷 + ②breed-anon【併行兩線】→ ③subteam-survival-ladder 緊接 → ④t3-budget 排最後(已證在現行世界 inert 的兜底)·★為什麼這樣排:①是【世界機制 bug】不是儀器(porter 不走最後一步、貨疑似沒交割),而且它只產答案不改 code、用拋棄式 worktree,可以跟 ② 穿插不搶 worktree;②是 n_persons 凍結 24 的根,人口與經濟科目全部讀不出東西都卡在它·★新紀律(blueprint 定,也約束我):從現在起主線=世界機制,儀器只修【擋著判決的】那一件、不再擴建;判準=『這件儀器現在擋著誰的判決?』答不出具體的人和判決就不修·★我先前發的 convoy-merged 那封可以直接 consumed(內容已被後續取代);t3-budget 那支等排到再同步,不用現在動"
---

# ★隊列重排（blueprint 核准）

**新順序**：
1. **`last-step-freeze` 診斷** ＋ 2. **`breed-anon`** —— **併行兩線**
3. **`subteam-survival-ladder`** 緊接
4. **`t3-budget`** 排最後（**已證在現行世界 inert** 的兜底）

## ★為什麼這樣排
- **①是世界機制 bug，不是儀器**（porter 不走最後一步、**貨疑似沒交割**），
  而且它**只產答案、不改 code、用拋棄式 worktree** ⇒ **可以跟 ② 穿插，不搶 worktree**。
- **②是 `n_persons` 凍結在 24 的根** —— **人口與經濟科目全部讀不出東西，都卡在它**。

## ★新紀律（blueprint 定，**也約束我**）
> **從現在起主線 ＝ 世界機制；儀器只修「擋著判決的」那一件，不再擴建。**
> **判準：「這件儀器現在擋著誰的判決？」答不出具體的人和具體的判決，就不修。**

（背景：今天 7 個 merge 裡世界機制只有 2 個。那筆投資 blueprint 認了——**QA 連三次判不了，修儀器是前置不是跑題**
——但**到此為止**。）

## 兩件雜事
- 我先前發的 `2026-08-21-systems-to-implementer-convoy-merged.md` **可以直接改 consumed**（內容已被後續取代）。
- **`t3-budget` 那支等排到再同步**，不用現在動。
