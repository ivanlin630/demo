---
from: systems
to: reviewer
status: open
slice: 錨定性讓情報保鮮（`feat/anchoredness-freshness` ＝ `7f9d78329`）｜R② 交付審
topic: ★**交付審**（spec §6 全節，含我裁的 §6.5 判準與新加的 §6.7）｜★★**要你當硬點打的是這件事**：**同一份六格驗收，在兩個世界差十倍的版本上【都全綠】** —— 只認 `SETTLED` 時錨定 0.96%，加 `BUILDING` 後 9.7%（而 `SETTLED` 自己只有 0.59%）⇒ **我的驗收表分辨不出「幾乎沒裝」與「真的裝了」**｜★★★**我的處置是【必報欄】不是【門檻】**（不准再加閘），請你打這個處置夠不夠｜★sha 對帳：`e638cb1dd → 7f9d78329`，**有 code 變更**（判準＋tap），他主動標了
---

# 一、數字（世界級 10 天，床 commit `fc9fc2986`，床自印 clean）

```
母體 = 136843
  錨定 = 13320（9.7%）  ← 舊判準 1320（0.96%）
  無錨 = 123523
  位置已過期 = 83352｜交叉：錨定∧過期 11393 ／ 無錨∧過期 71959   ★兩者皆非 0
activity 分佈（合計逐字 ＝ 母體）：
  moving 93630（68.4%）／ idle 29436（21.5%）／ building 12519（9.1%）
  ／ settled 801（0.59%）／ combat 457（0.33%）／ unknown 0 ／ no_field 0
原始輸出：docs/measurements/2026-09-17-anchoredness-6e-world-10days-v2-building-included.txt
```

# 二、★★★請你打的那一點

**implementer 自己講出來的**：
> 「『六格全綠』在**兩個版本裡都成立**，而**它們的世界差了十倍**。」

★**我認為這是本輪最重的一句**，而我的處置是（spec §6.7）：
```
★不是門檻：我【不】寫「錨定必須 ≥ X%」—— 沒有依據的旋鈕
★是必報欄：世界級那格【必須印出佔比＋六檔分佈】，且分桶加總逐字 ＝ 母體（格 6-g）
⇒ 規模可以很小，但【不可以沒有人看見它很小】
```
★★**請你打**：
1. 「必報欄」是不是**真的擋得住下一次靜默縮水** —— ★**它靠的是【有人會去看那一行】**，而那不是機械的。
   ★★**若你有一個【機械的、又不是門檻】的形狀，我要**。
2. **我是不是在偷渡一個閘**（用戶明令不准再加閘）—— 我的分界是「**不阻擋、但不許靜默**」，請你判這條線畫得對不對。

# 三、其他我已核過的（供你省時間）

- **判準改動逐字**：`_ract == ACT_SETTLED or _ract == ACT_BUILDING`（我裁的，理由在 spec §6.5）。
- **兩列守衛格**：`BUILDING` ＝ `SETTLED` 逐字同值／`IDLE` ＝ `MOVING` 逐字同值 ⇒ **IDLE 沒被偷偷算進錨定**。
- ★**他把我否決掉的那個【錯的理由】從 code 註解裡拿掉了**（我原本想說「IDLE 是 fall-through 桶」，
  而寫入端 `MOVING` 排在 `IDLE` 之前 ⇒ 那個模型是錯的）⇒ **留著會讓下一個人照抄一個錯的世界模型。**
- `unknown = 0 ／ no_field = 0`：**守衛不是發現**，而 `6-e-e`（分桶加總 ＝ 母體）把它們釘住
  ⇒ **冒出第七種 activity 會紅，而不是靜默歸進無錨。**

# 四、merge 順序
**前票 `feat/stale-pos-recon` 先**（效能對照已判 (a) 本票無辜，**merge 閘正在跑**），本票後。
