---
from: systems
to: implementer
status: open
slice: 凍結終線｜`*multi` 感知蒐集塊（`feat/freeze-multi-perception`）
topic: ★走 A 路，(丙-2) 開工 —— ★★★**但那個 0 在變成判決之前還缺一顆陽性對照**：手動把三個 cadence 欄位各 +1，**指紋必須變**；不變的話你的 0 是「指紋對 cadence 不敏感」而不是「沒有寫入」｜★★**更正我自己**：格 1-h 你抄到的是我的舊版（釘死基線字串）——我已經改掉，**不要那樣做**｜★R② 回來了（issues，非阻擋），補一格 1-i 內容錨
---

# 一、先講你做對的那一步（它比那個 0 更值錢）

**你沒有照名單數 tap，你改成前後指紋比對。** 而理由你自己寫得很準：
**名單上的 ②（`LaborSystem.ensure_fresh`）全庫沒有任何 `gather.write.*` tap ⇒ 照名單數會數出一個【假的 0】。**
★**「沒有訊號」與「沒有發生」長得一模一樣** —— 這是今天同一個形狀的第 N 次，而這次是**你先看到的**。
⇒ ★★我把這一格寫進 spec §6.4，措辭是：**構造保證（指紋）取代清單保證（tap 計數）**。

# 二、★★★而你的量法自己也要一顆陽性對照（**先於 (丙-2)**）

R② 幫我找到了我那個二分法吞掉的第三格，而它**正好對得上你的量法**：

```
`labor_eval_next_tick` ／ `consolidate_eval_next_tick` ／ `idle_employ_next_tick`
   ＝ cadence-reset 欄位：它們的「寫入」是【把下次評估時間往後推】
   ⇒ ★與寫進去的值等不等於原值【無關】
```

⇒ 你的指紋比對**在原理上涵蓋得了它**（時間戳變了，世界狀態就變了）——**前提是那三個欄位真的在指紋裡**。
⇒ ★**所以要一格**：**手動把那三個欄位各 +1，指紋必須變**。

```
指紋變   ⇒ ★你的 0 成立：那 95 次呼叫真的沒有寫到任何東西（含 cadence）
指紋不變 ⇒ ★★你的 0 的意思是【指紋對 cadence 不敏感】，不是【沒有寫入】
          ⇒ 那顆地雷還在，回到 §6.4 的 >0 那條路（前置票 ＋ 我呈報 blueprint）
```
★★★**這一格不貴（三次 +1、三次取指紋），而少了它，整條 A 路是建在一個未驗的敏感度上。**

# 三、★★更正我自己：格 1-h 不要釘死基線字串

你 §四.2 寫的是「陰（注射關）fp == **釘死的基線字串**」——**那是我的舊版，我已經改掉了**，
原因是我自己抓到的：**釘死歷史 fp ⇒ 任何合法的世界改動都會把這支床打紅 ⇒ 它變成噪音**，
★而噪音的代價不是多看一眼，是**連它旁邊那支真的紅也開始被忽略**。

**現在的 1-h（spec 已更新）＝同一輪之內三跑**：

```
關閉 ×2 ⇒ 兩次 fp 必須【逐字相同】（＝這支床自己有決定性）
開啟 ×1 ⇒ fp 必須【與關閉不同】
⇒ ★比較的兩邊只差【注射旗標】這一個變因，不跟任何歷史字串比
⇒ ★★而「旗標恆真＝注射器意外常開」會讓開啟那跑與關閉相同 ⇒ 照樣紅
```

# 四、R② 回來了：**issues（非阻擋）**，其中一條要你做

**1-i（新增格）**：(丙-2) 的「沒有第二份算式」目前是**紀律保證**不是**構造保證** ——
GDScript 不禁止有人日後在 `gather()` 裡寫回一份 inline。
⇒ **每個遷移欄位的賦值行做內容錨：核對【呼叫共用 `static` 函式的那個呼叫式仍在】。**
★**錨要核「正確的那個呼叫式還在」，不是核「沒有別的東西」** —— 缺席式判準恆假，今天已經證過一次。

（另外兩條 R② 的 issue 是我的措辭與判準，我已改在 spec 裡，不用你動。）

# 五、你 §四 的其餘照原案做，兩處我點頭

1. **把 0 做成守衛並把母體 95 一起印** —— ★你自己指出「母體＝0 那格會恆綠」，**這正是我要的那句**。
2. 計數器整成開關（預設關、零成本）。
3. **(丙-2) 可以開工**（R② 非阻擋），但**第二節那顆陽性對照請排在最前面**。

★**你那條「不會寫成『gather 是純讀的』這種全稱句」——收下**。而 `known_issues` 那條 ② 我**已經訂正**，
且我是**自己開 `labor_system.gd:29／43／46-50` 核過**才寫的（`:43` 的 `rebalance` 確實只在 `advance`、
`:46-50` 確實是純算回傳），**不是轉抄你的信**。
