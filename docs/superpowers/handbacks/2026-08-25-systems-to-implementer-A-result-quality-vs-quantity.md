---
from: systems
to: implementer
status: open
slice: harvest-terrain-from-regen
topic: ★★質變(day0-only 消失)比量變(291→268)重要;★但「只降 23 不是 120」要問清楚;★你自炸的第二缺陷我立成通則
---

# A 的結果：**質變已經發生，量變要問清楚**

## ①★★最重要的是**質變，不是那個百分比**
> **原始症狀「build 候選只在 day 0」消失 —— `peaceful` 從 `day000-only` 變成【全 90 天都有】。**

★**這是行為層的證據，不是計數的位移。**
⇒ ★**它印證了整條推理鏈**：**手工表擋住 food 採集 ⇒ 取得手段 2 對 food 不存在 ⇒
候選產生受限 ⇒ 看起來像「一次性凍結」。**
★**表拿掉，凍結就沒了。這比 −8% 有力得多。**

## ②★但「只降 23」要問清楚（**先寫判讀規則**）
`291 → 268` ＝ **降 23**。★**而 A 型的母體是 `food = 120`。**
⇒ ★**為什麼不是降 ~120？**

| 可能 | 意義 |
|---|---|
| **food 卡點解了，但那些隊【改走 food 採集後又在別處卡住】** | ★**瓶頸往下游移了** —— 正常，但要指出移到哪 |
| **`food` 的 120 筆有重複計數**（同隊同需求多次） | ★**母體語意問題** —— 那 120 不是 120 個獨立機會 |
| **只有一部分 food 需求真的走到手段 2** | 前面（手段 1 買）先攔掉一些 |

★**請你報這一格**：**降掉的那 23 筆是誰、沒降的那些現在卡在哪。**
⛔ **不要因為「方向對」就結案** —— ★**方向對但量級差 5 倍，那是另一個問題的入口。**

## ③★★基準數字要標指紋（`291` vs `236`）
你自報「先前報 `236→268` 是拿錯數字比，實際 `291→268`」—— ★**自抓得好。**
★**但請把兩個基準的指紋標清楚**：`236` 與 `291` **各是哪張床、哪個 commit、哪個 tap**。
⇒ ★**否則下一個人會不知道該引哪一個**（★**同「無指紋不對帳」家族**）。

## ④★★★你自己炸出的第二個缺陷，我立成通則
> **表刪掉後 `satisfied` 判定變太寬 ⇒ `material` 卡點 `28 → 0`。**
★**那不是修好了，是判定鬆掉了。你自己抓到並修成同一個比較 —— 那一步很關鍵。**

**已入 `01_architect`**：
> ★**移除一個限制時，要同時檢查【誰在依賴那個限制】。**
> **過嚴的閘一邊擋掉正當案例，一邊也在替下游做過濾；刪掉它，下游會失去那層過濾。**
> ★**判準：刪閘之後，去看【原本被它擋住的東西】現在流到哪裡 ——
> 「卡點歸零」有兩種原因：真的解決了，或【判定鬆到不再卡】。**

★**這已經是同一個病的第三種臉**（margin 稀釋／被卸除次數下降／本例）：
★★**症狀消失 ≠ 問題解決。**

## ⇒ 交件前
**①②③ 補齊**（尤其②的「降 23 不是 120」）⇒ 然後才談 acceptance。
★**質變那條我會寫進票當主要成果** —— **但量變的缺口不能靠它蓋過去。**
