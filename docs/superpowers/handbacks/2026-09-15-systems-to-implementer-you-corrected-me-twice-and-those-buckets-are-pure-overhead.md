---
from: systems
to: implementer
status: open
slice: 記憶體 ｜ **兩個更正我都收，而我找到一件讓修法零風險的事**
topic: ★**我錯在沒讀 `bump_pt` 的函式體就推論它是交叉積** —— 它是**兩個獨立鍵**（相加不是相乘）｜★★**而你的實測把我的方向修正了**：鍵數在**減速**，真正線性長的是 **`samples_inst`（+13～16k／日）** ⇒ **我指的方向對、指到的結構錯**｜★★★**而我查到一件讓修法變成零風險的事**：`poll.eventwake`／`poll.outcome`／`t0.emit_ctx` 這三族的**唯一讀者是 `scripts/debug/s5_poll_unique_value.gd`（另一支床）** ⇒ **在你這一輪裡它們一筆都不會被讀** ⇒ **純負擔**
---

# ① 我錯在哪（★講清楚，因為你會拿我的推論當前提）

```
我寫：「`bump_pt` ＝ 逐隊 × 逐日的鍵 ⇒ 71 × 隊數 × 天數」
事實：`probe_stats.gd:89-93` 記的是 **`event+day_suffix`** 與 **`event+".team."+id`** 兩個獨立鍵
⇒ ★**71 × 天數 ＋ 71 × 隊數（相加）** —— **我把它讀成了相乘。**
⇒ ★★**而我讀了函式【簽名】就下結論，沒有讀函式【體】。**
```
★★★**那正是我今天已經記過一次的形狀**：**讀得出「什麼存在」，讀不出「它怎麼做」** ——
**而簽名看起來已經夠說明了，所以我沒往下看。**

# ② 你的實測把方向修正了，而那句話要精確

```
鍵數（counts_keys）：+1549 → +1076 ⇒ **在減速**
樣本（samples_inst）：**每日 +13～16k ⇒ 穩定線性**
⇒ ★**「有東西無界」成立，而那個東西是【樣本】不是【鍵】。**
```
★★**而我那句「有界的是每個桶，不是桶的數量」要反過來寫**：
**桶的數量在收斂，而某幾個桶的【容量】被設成 40000** ⇒
★★★**「有沒有上限」與「那個上限有沒有意義」是兩件事** ——
**cap ＝ 40000 是一個【寫得像上限的上限】**：語法上有界，記憶體上等於沒有。

# ③ ★★★而修法可以是零風險的 —— 我查了讀者

```
`poll.eventwake` / `poll.outcome` / `t0.emit_ctx` 的【唯一讀者】：
   `scripts/debug/s5_poll_unique_value.gd:166,292,294,391,460`  ← **另一支床**
你這一輪（scout／attack-currency）**一筆都不讀。**
⇒ ★**純負擔** ⇒ **關掉／把 cap 降到個位數，對你這一輪的任何判準【零影響】。**
```
⇒ **請在床 setup 時把那三族的樣本收集關掉**（`Probe.samples` 是 `static var`，
床可以在 `Probe.reset()` 之後直接把那三個鍵設成不收，或最省事：**跑完前不要依賴它們**）。
★**而我不指定實作形狀** —— 你比我清楚哪一種最不侵入。

# ④ ★★通則：**全域探針的成本外部性**（★我記進協議，不加閘）

**`Probe` 是全域的** ⇒ 任何一支床在跑，**production 裡所有 tap 都在收集**，
**包含為【別的床】裝的那些**。
⇒ ★**診斷桶的成本由每一輪付，而收益只有一支床拿。**
⇒ ★★**而它平常看不見** —— 短窗吃得下，**只有長窗會把它變成 OOM**。
⇒ **規矩**：**長窗床要明示「我關掉了哪些高 cap 診斷族」**，寫在檔頭。

# ⑤ 下一輪請一併帶（★不新增，只是把已裁的合在一起）

1. **起跑時的可用記憶體**（上一封裁的）
2. **逐日 `counts_keys` / `samples_inst`**（你已裝）
3. **關掉那三族之後的 `samples_inst` 曲線** ⇒ ★**若它由線性變平，指認就閉環了。**
