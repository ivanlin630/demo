---
from: systems
to: implementer
status: open
slice: food-tax-path ｜ worktree `.worktrees/foodtax` ｜ branch `feat/food-into-tax-path`
topic: ★DISPATCH（R² CLEAN，三格全過）：`specs/2026-09-11-food-into-tax-path-and-labor-pool-HOW.md`｜★★★**本票的驗收有兩格在自然世界【判不動】** —— 三個 config 在 t=0 房客都是 0，而「白捐 ＝ 0」在**沒有房客的世界裡自動成立** ⇒ **必須靠 fixture 造房客，而自然世界那格【明寫不可判】**｜★★而我自己裸掃驗過 R² 的兩個結論（不是只信信）
---

# ⓪ ★我先驗過的兩件（★★昨天我因為沒驗就轉述，散布了一條假的）

```
①`_apply_normal_tax` 的呼叫點：**production 只有一個**（`resource_system.gd:120`），
  ★而它已經是**泛用多資源**的路徑 ⇒ food 併進去**零額外風險**（R² 的結論，我裸掃證實）。
②`_convert_to_resident` **存在且是真實路徑**（`faction_ai_system.gd:2652`：
  被邀入 faction 後**同 faction outpost 落腳**）⇒ ★★**fixture 造的房客，是世界真的會產生的那一種**
  ⇒ ★★★這一格重要：**否則那三格驗的是一個不存在的情境**。
```

# ① 做什麼（★三件）

```
(a)★**換路**：採集所得的 food ⇒ 走 `_apply_normal_tax`（私產 → 按 `tax_rate` 撥 tile owner 公庫）
   ⇒ ★★**採集者 ＝ owner 時結果等價**（自己付自己）；**房客**才會變（100% 沒收 → 按稅率分成）
   ⇒ ★★★而 **(b)「房客吃得到」不必另外做** —— 消費端本來就吃私產。
(b)★`tax_rate` **接線**（現況零寫入點 ＝ 死常數 0.3）：
   **貪婪↑ ⇒ 稅↑／慎重↑ ⇒ 稅↓**（★R² 查實：這是「苛稅忍耐度改讀慎重」那次**同一個信任前例的對稱應用**，
   **不是借殼**）＋ 處境調變 ⇒ ★★**必須是一支函式**，不得變成新的死常數。
(c)★★勞力池（`labor_system.gd:72-78`）：從**零 owner／零 faction 檢查** ⇒ 改成
   **owner 隊本人 ＋ 登記在這格的居民**。
```

# ② ★★★驗收：**不可判那兩格是本票的重點，不是附註**

```
★fixture 床（用 `_convert_to_resident` 那條真實路徑造房客）：
   ①分成：採 100 糧 ⇒ **私產留 (1−tax)×100、公庫收 tax×100**（★兩個數字都印）
   ②吃得到：消費端**真的扣到它自己的私產**（★★看【扣】，不是看「有效糧變多」）
   ③白捐歸零：房客人力不再進地主勞力池；★★成對對照：關掉 (c) ⇒ **它要回到被算進去**
★★自然世界：**明寫「不可判」＋ 母體數字**（房客 N ＝ 0）
   ⇒ ★★★**不准**用「沒有不一致」冒充綠。
★④owner 自採**守恆**（私產＋公庫總和與改前相同）／⑤`tax_rate` **分布**（★不得全等於 0.3）
★⑥fp **會變**（裁定接受）＋ 同 seed 兩跑相同
```

# ③ 不做

```
①★不動礦／特殊物資的直入庫（**領主專營**＝卡①明文保留）
②不做「供養」那一邊（＝另一張）；★本票只把單向的**下半**接上：房客留得下自己那份
③不動登記錨的動詞；④不收緊借宿門禁
```

# ④ ★誠實限（★★寫進交件，不要只寫在這裡）

```
★本票**不宣稱世界變公平**：它只讓房客留得下 (1−tax)，★★而 tax 由**領主**決定 ⇒ **重稅仍然合法**
  —— 那正是卡① 要的張力。
★★★而「重稅會怎樣」有既有機制接著（慢性不滿 ⇒ 壓力／忠誠 ⇒ 起義）：
  **本票不動它**，但驗收⑤的分布會第一次讓那條路**有真的輸入**。
```
