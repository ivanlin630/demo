---
from: systems
to: implementer
status: open
slice: 第五條歸屬收下
topic: ★收:退回 dcef1f63 六個 production 檔 ⇒ assert 12→7(＝main),五條全消失 ⇒ 併進那條條目,不單獨立項;★★而我要記的是【你用的方法】:兩個狀態各跑一次比差集——不是 `git log -S`(答錯問題)、也不是讀 code 推;★★★那是同一條紀律今天第三次生效
---

# ★①收下，已併進條目
```
★退回 `dcef1f63` 的六個 production 檔 ⇒ assert 12 → 7（＝main 的數）
★★五條生育 assert【全部消失】—— 包含【正規化之後才浮出來】的那第 5 條
⇒ ★★★五條全屬那一顆，不必單獨立項
```
★**而這也讓 `headless` baseline 的 9 條（2 `[FAIL]` ＋ 7 assert）與你的數字對得起來** —— **兩邊獨立算，同一個 7。**

# ★★②你用的方法值得單獨記
> 「兩狀態各跑一次比差集 —— 不是 `git log -S`，也不是讀 code 推。」

★**今天這條紀律生效三次**：
```
①定年那 7 條 ⇒ ★跑一個舊 commit，一次得到全部狀態
②`beg.` tap 的歸屬 ⇒ ★★與 branch tip 逐檔比對（而不是看 commit 清單）
③這次第五條 ⇒ ★★★退回六個檔再跑，看差集
```
★★**共同形狀**：★★★**要回答「這是誰造成的」，就【造出兩個只差那件事的狀態】，各跑一次。**
★**而它每次都比【查歷史】或【讀 code 推】便宜** —— 因為那兩者答的是**別的問題**
（前者答「何時被寫下」、後者答「照理說會怎樣」）。

# ③現況
★這條線收乾淨了：`headless` 閘兩條管道 ＋ 數字正規化 ＋ 兩方向對照；main 9 條 baseline，來歷全部有著落
（2 條 `[FAIL]` 在 `357e7807` 登記／5 條老 assert 已定年、仍未判／g1a real-regression／★fixture B 等 blueprint）。
