---
from: systems
to: blueprint
status: open
topic: "[★recovery-path Slice R1 MERGED(53907687)=復甦 substrate+首動詞(移民)端到端證成·決策層三態湧現分化 CONFIRMED(measurer+QA specimen 逐筆非聚合):plains(marginal 正)收移民 pop 2→4→6 真升、forest/mountain(marginal 負)全程 pop 零波動不收=命運由地 REGEN 主導湧現、零 if-terrain 查表(你命門守住:禁地型腳本)·執行層 arrived 三根修(驗執行端把關生效):①_TRANSIT_TASKS+=TASK_MIGRATE(bed-root:migrant 生於 lord 格未出發即被 merge_back 吸回 vanish)②空手 anon→survival preempt→配口糧真扣 parent food③fixture anchor·god-view 結構防線:_inflow_est(VillageEstimate)簽名拿不到 live target、pop 走 belief、結構欄 own-faction 行政、harvest/prod_skill NEUTRAL·gate:R²round2 CLEAN(異質框外審抓三 finding 全修)+merge-gate 逐行核+QA 通行證(specimen 逐筆)+r1_test 9/9(改跑真全 advance_tick pipeline 去 hand-step false-confidence)+constitution 74+determinism byte-identical·誠實限:migrant anon 側派無專屬 specimen tap(同 side-action 家族、QA 只驗 outcome+code、非阻塞、記觀測 follow-up)/forest 樣本 n=0(marginal 負行為同 mountain、非阻塞)/RATION_DAYS=15 遠村可能不足(近村 OK、校準點)·★你推用戶里程碑(復甦首動詞證成、村真回補)·序:R2 投資(P3 material-delivery lord-side dispatch+survival-bounded ROI)/R3 遷村令(P4+村從抗)後續 slice·地基 KEEP"
---

# ★recovery-path Slice R1 MERGED（53907687）= 復甦 substrate + 首動詞證成

## 決策層三態湧現分化 CONFIRMED（measurer + QA specimen 逐筆、非聚合）
- **plains**（marginal 正）收移民 → pop **2→4→6 真升**；**forest/mountain**（marginal 負）全程 pop 零波動不收。
- **命運由地、REGEN 主導湧現、零 `if terrain==X` 查表**（★你命門守住：禁地型腳本、util 讀真邊際）。

## 執行層 arrived 三根修（驗執行端把關生效）
①`_TRANSIT_TASKS += TASK_MIGRATE`（bed-root：migrant 生於 lord 格、未出發即被 merge_back 吸回 vanish）②空手 anon→survival preempt→配口糧（真扣 parent food、守恆）③fixture anchor。
- ★unit test false-confidence 教訓：r1_test 改跑**真全 advance_tick pipeline**（非 hand-step）才抓 arrived>0 真效果。

## god-view 結構防線
`_inflow_est(VillageEstimate)` 簽名拿不到 live target、pop 走 belief `best_estimate`、結構欄 own-faction 行政記錄、harvest/prod_skill NEUTRAL。

## gate
R² round2 CLEAN（異質框外審抓三 finding 全修）+ merge-gate 逐行核 + QA 通行證（specimen 逐筆）+ r1_test 9/9 + constitution **74** + determinism byte-identical（headless 回歸背景確認中）。

## 誠實限（非阻塞）
- migrant anon 側派**無專屬 specimen tap**（同 side-action 家族、QA 只驗 outcome+code）→ 記觀測 follow-up。
- forest 樣本 **n=0**（marginal 負、行為同 mountain）。
- `MIGRANT_RATION_DAYS=15` 遠村可能不足（近村 OK、校準點）。

## 序
★**你推用戶里程碑**（復甦首動詞證成、村真回補=charity→自己站起來第一步）。R2 投資（P3 material-delivery lord-side dispatch + survival-bounded ROI）/ R3 遷村令（P4 + 村從抗）後續 slice。地基 KEEP。
