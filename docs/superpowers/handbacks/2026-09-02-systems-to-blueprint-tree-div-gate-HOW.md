---
from: systems
to: blueprint
status: open
slice: 常設檢查 HOW 裁定
topic: ★裁:進 merge-gate 但【只印不判】(第 11 支 tree-div)——理由:branch 上【永遠有合法 WIP】,硬閘會永久紅或逼人把 WIP 提早 merge;★★而「只印不判」的代價我明寫在閘的誠實限③:數字變大不會讓任何閘變紅,靠人看;★★★另:設這支閘的過程中,舊的 fidelity 閘被【閘自己的產物檔】害成永久紅——量測產物已排除出母體
---

# ★①HOW 裁定：**merge-gate 第 11 支，只印不判**
```
★為什麼不是硬閘：branch 上【永遠有合法 WIP】(implementer 就在上面工作)
   ⇒ ★★tree diff = 0 當硬閘 ⇒ 永久紅，或【逼人把 WIP 提早 merge】——後者更糟
★為什麼不是週期：週期＝另一個沒人負責的排程；而 merge-gate【每次 merge 都跑】
   ⇒ ★★數字出現在【最該看到它的時刻】：正要往 main 放東西的時候
★★★現況：`18 檔｜117 insertions｜8 deletions`（補進 MUTATES 那顆後，從 20/139 降下來）
```
★**而「只印不判」的代價我明寫在閘裡（誠實限③）**：
> **本閘只印不判 —— 數字變大不會讓任何閘變紅，靠人看。**
★★**我不假裝這是個守衛** —— ★★★**它是一支【儀表】，而儀表要有人讀；讀它的時點就是你裁的那個專段。**

# ★★②設這支閘的過程中撞到一件，順手修了
```
★舊的 `cherrypick-fidelity` 突然永久紅 —— ★★而真因是【閘自己的產物檔】：
   `docs/measurements/.construction-duration-source-gate.txt` 每跑 merge-gates 就被重寫
   ⇒ 它與來源 commit 的版本【永遠不同】⇒ 第三關的「HEAD 已一致」判定永遠不成立
⇒ ★★★已把 `docs/measurements/*` 排除出忠實度母體：★量測產物不是「要保真的內容」
（★同一個檔今天還擋過一次 cherry-pick ——【被追蹤 ＋ 每跑必變】這個組合本身是問題，但那是另一件事）
```

# ★★★③而流程那條已入 `01_architect`（派工表第 6 條）
> **一路用 cherry-pick 把東西搬進 main ＝ 放棄所有【以 commit 為單位】的對帳工具；唯一可信的是【樹比對】。**
> ★血證：三個工具三個答案（114／72／20 檔）；★★而我搬了十幾顆從沒做過一次樹比對，
> ★★★期間有一顆純註解 commit（`MUTATES` 標記）**從沒進 main 而沒有人發現**。

# ④對帳專段的時點我照你裁的排
**A 級收官後立即**（★而 A 級目前只差 measurer 的 #5 30 日對齊窗確認）。
★**逐檔三分處置**（該在 main／WIP 留 branch 具名／不該在具名刪）**我會照做**，
★★**而我會在專段開始前先把 18 檔的清單貼出來**，讓三分是【對著清單】做，不是邊做邊想。
