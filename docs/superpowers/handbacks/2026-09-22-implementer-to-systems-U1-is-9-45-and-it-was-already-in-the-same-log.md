---
from: implementer
to: systems
status: open
slice: ①一圈形態（窄形狀）—— **規模量測（只量不判）**
topic: ★**`U₁ ＝ 9.45%（seed 1337）／9.24%（seed 77）`**，`L(窄) ＝ 42.9%／43.1%｜S ＝ 0.587／0.573`｜★★★**我沒有再跑一輪** —— 這兩個數**與 U₂ 出自同一個 log**（同一套樁一次跑出三因子 ＋ L）⇒ 省下 12 分鐘，而且**兩張票的分母天生同源**｜★★**而 U₁ 也是【上界】**：L 是上界、且 U₁ 對 L 單調遞增 ⇒ 「落在門檻下方」站在安全那一側（與你在 U₂ 用的同一條推理）｜**門檻判定在你**
---

# 一、卷面

```
  seed  T_seg(s)   T_calc   T_catch    L(窄形狀)     S      U1
  1337    53.934   15.178    15.630      42.9%    0.587   9.45%
    77    49.845   13.934    14.413      43.1%    0.573   9.24%

L  ＝ (T_seg − T_calc − T_catch) / T_seg   ★窄形狀：catch【不共用】⇒ 算在計算側
U1 ＝ S × [ L×25% ＋ (1−L)×9.4% ]         ★你的預註冊換算式，未改
```
★**母體衛生（你指定要印）**：四條走訪圈數在本輪**沒有塌**（同一套樁、無 stub）；
`gather` 呼叫 9237／8707、最後隊數 103／94。
★★**上一輪 (乙) 自己否掉自己的血證我記著**：stub 後圈數 `206119 → 10422`（塌 20 倍）
⇒ **本輪用 (甲) 計時法**，所以那個坑不在這一輪。

# 二、★★★我沒有再跑一輪 —— 而這件事值得講

```
你派①的時候，①的數字【已經在 U₂ 那一輪的 log 裡】：
  同一套樁一次量出 T_seg／T_calc／T_catch ⇒ U₂ 用 S×E×D2、U₁ 用 (T_seg−T_calc−T_catch)/T_seg
⇒ ★我只是把同一組原始數字換一個算法讀 ⇒ 沒有新的跑、沒有新的世界
⇒ ★★好處不只是省 12 分鐘：**兩張票的 S 是【同一次量測】的同一個數**
   —— 不會出現「兩張票各自量、分母差一點、而那一點正好跨過門檻」
⇒ ★★★而壞處我也寫出來：**它們共用一切誤差** —— 若那套樁有系統性偏差，兩張票會【同方向】錯。
```

# 三、★★U₁ 也是上界（★這一格會改變怎麼讀 9.45%）

```
①L 是上界：迴圈體內我【沒有具名包住】的計算（has_belief／belief_pos／estimate_armed／評分算式）
  被算進【走訪】那一側 ⇒ 真 L < 42.9%／43.1%
②U₁ 對 L 單調遞增（係數 0.25 > 0.094）⇒ ★L 的上界 ⇒ U₁ 的上界
⇒ ★★★所以「9.45% < 10.0%」這個結論站在【安全那一側】——與你在 U₂ 用的同一條推理
   （真省 ≤ 上界 ⇒ 結案不會把一張其實值得做的票關掉）
```
★**而反方向我也講清楚**：**若你要的是「它是否【確實】低於門檻」而不是「上界是否低於門檻」**，
那我就得把剩下那幾支計算也具名包住再跑一輪（約 12 分鐘）——★★**但照上界推理，那一輪不會改變裁定方向**
（真 L 更小 ⇒ U₁ 更小 ⇒ 更低於門檻）。**所以我不主動跑；你要就說。**

# 四、★其餘誠實限

```
③窗敏感：同一套樁在【1 天窗】量到 L ＝ 56.1%，8 天窗是 42.9%
  ⇒ ★短窗數字不可引用（隊數與 discovered 還沒長開，母體不同）
④跨代不可引用：世代 6 拿掉了 observed_speed 的 randf ⇒ `score` 那一支變便宜
  ⇒ T_calc 的組成本身就跟世代 5 不同
⑤★未量【寬形狀】（合併走訪＋共用計算結果）—— 照你的指示，它與那張「7 處全共用」重疊
⑥計時樁成本：同套樁在世代 5 量過 ＋1.36%；★本輪未重量（★而它對【比例】的影響是一階抵消的，
  因為分子分母都含樁 —— 但我沒有量證，所以標成限而不是理由）
```

# 五、落地（exact path）

```
卷面：docs/measurements/2026-09-22-U1-one-pass-narrow-gen6-HW2.txt
raw ：docs/measurements/2026-09-22-U2-u2-s1337-gen6-HW2.log.gz   ★與 U₂ 同源（同一個 log）
      docs/measurements/2026-09-22-U2-u2-s77-gen6-HW2.log.gz
```

# 六、下一步

**你**：拿 `U₁ ＝ 9.45%／9.24%` 對預註冊的 `10.0%` 判 —— **我不判**。
★若判結案：那 `share-estimate-catch-up-across-all-seven-call-sites` 那一列就是下一個要量的東西
（你說排在①之後；★而它缺的正是【另外 5 處呼叫點的 T】—— 我這套樁只涵蓋 2 處，**要加樁才量得出來**）。
