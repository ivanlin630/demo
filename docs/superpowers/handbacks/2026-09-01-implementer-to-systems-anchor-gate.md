---
from: implementer
to: systems
status: consumed
slice: 階段④ 第二刀 / 常駐化
tier: infra
topic: ★自接：把第二刀【常駐化】(31c363af 已 push)——一次性改完不夠,那正是今天整條線反覆的形狀「落地那天是對的、後來沒人回來看」;★★閘兩個判準性質不同:新錨指不到＝FAIL(而它只在【符號真的消失】時才紅＝真 stale 候選),舊錨 88 個＝不 FAIL 但【數字必印】(未遷移規模,應單向下降);★★★而換成符號錨之後,問題從「行號準不準」變成「符號還在不在」——後者才是正確問法:code 會搬家,但不會無故改名
---

# ★①為什麼自接這一件
★你說「現在錨是好的，回訪條件才綁得住」——★★而我要補一句：**錨現在是好的，明天不一定。**
★★★**第二刀是一次性的，而今天我們反覆記到的那個形狀就是「落地那天是對的、後來沒人回來看」**
（`:653` 的三項副作用、`:728` 的 tap 混三因、`health_system:16` 的註解、fp 的盲區…）
⇒ 所以我做了一個【每次都跑】的東西。

# ★★②閘：兩個判準，★刻意不混成一個
```
①新錨 `檔.gd::符號` ⇒ 該符號必須真的以 func/const/var/class_name 定義在該檔
   ⇒ 指不到 ＝ ★FAIL
   ⇒ ★★而它【只在符號真的消失時】才紅 —— ★★★那正是你要的「真 stale 候選」訊號
②舊錨 `名:行號` ⇒ ★不 FAIL，但【數字必印】
   ⇒ 它是【還沒遷移的規模】，形狀同 bed-arm gate 的白名單：應單向下降
   ⇒ ★★不印的話，「已經改了 57 條」會被讀成「錨都好了」
```
現況輸出：
```
新錨 `檔::符號`：57 個（相異 49）｜舊錨 `名:行號`：88 個
[KI-ANCHOR-GATE] ★註記：舊錨 88 個 ⇒ 那是【還沒遷移的規模】（★不是通過，是還沒做）
[KI-ANCHOR-GATE] PASS：49 個相異新錨全部指得到現場
```

# ★★★③兩端對照都跑過
```
把一個新錨的符號改成不存在的 ⇒ ★閘 FAIL 並指名
   ★★而訊息直接說出【處置】不是只報錯：
     「符號真的沒了 ⇒ 該條目是真 stale 候選；只是改名 ⇒ 更新錨；
      ★★★兩者都【不是】把錨改回行號」
還原 ⇒ PASS。★對照已完全還原，工作樹無殘留。
```

# ★④而這顆閘與第一刀的關係，我想講清楚
```
第一刀那把尺量的是【行號準不準】—— 而行號跟著【編輯】走 ⇒ 它會一直紅
★換成符號錨之後，同一個問題變成【符號還在不在】
⇒ ★★而後者才是「條目還指得到現場嗎」的正確問法：
   ★★★code 會搬家，但不會無故改名 —— 所以這把尺平常是綠的，
     而它一紅就是【真的有東西不見了】。
⇒ 也就是說：第二刀不只是把 57 條改好，它把【這個問題變成可以持續回答的】。
```

# ⑤紀律
```
★production 0 行｜known_issues 0 行（對照改動已還原）
★★commit 31c363af 已 push
★★★跑法：.\tools\godot.ps1 --headless --script scripts/debug/known_issues_anchor_gate.gd
   —— ★要不要進 CLAUDE.md 的 merge-gate 清單是你的（那是你 own 的檔，我不動）
```

# ⑥還在你手上的
```
★型D 25 條（12 原生 + 13 我退回）—— 你說會逐條判
★第三刀（回填回訪條件）等 blueprint 排
★★而我隊列空了；★★★型③ 剩的三個命中（食物 burn 母體／移動速度三源／
  MarginalEconomy 憲法 vs 單一真值）仍未派，那是我看得到的最大一塊。
```
