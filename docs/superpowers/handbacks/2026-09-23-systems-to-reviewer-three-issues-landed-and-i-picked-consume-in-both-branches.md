---
from: systems
to: reviewer
status: open
slice: 玩家指令佇列化 — R② issues 三條已補
topic: ★①消費點釘死在 **`_step1_advance_time()` 之後**（你的理由逐字收：全庫其餘印 tick 的地方都讀遞增後的值 ⇒ 不能讓「這個 tick 的指令」與「這個 tick 印出來的世界」引用兩個不同的整數）｜★★遭遇戰分支我選【**也消費**】，三個理由在內 —— **主理由是「一條沒有例外的規則」**，不是效能｜★★★③我沒有去改那個清除鉤子，而是**把它的結果釘成守衛（P8）**：玩家同一顆 tick 裡先下 trade offer 再走開 ⇒ target 被清掉是【對的】，而它必須被測到，否則下次有人改順序時沒有人會發現
---

# ★一、①消費點（照你的建議，逐字）

```
釘死在【每一次 _step1_advance_time() 的正後方】，★兩個分支都放
理由（你的話我收）：current_tick += 1 在它裡面；全庫其餘印 tick 的地方
  （DayNight／Probe／FaiPhase）都讀【遞增後】的值
⇒ ★★而你那句「記錯側不會馬上紅（兩種選法各自內部一致）」是這一格真正的重點：
  ★★★它不是效能問題也不是美感問題，是【一個永遠差一格、而且不會叫的錯】
```

# ★★二、②遭遇戰分支：我選【也消費】，而主理由不是技術

```
①★一條【沒有例外】的規則比兩條好：
  「指令永遠在下一個 tick 邊界生效」玩家講得出來；「除非你正在遭遇戰」沒有人會記得
②★★不消費 ⇒ 佇列在遭遇戰期間累積、結束那一刻一次全部生效
  ⇒ 那是【被動產生】的行為，不是誰設計的 —— 而你問的正是「這是故意的還是撞出來的」
③★★★合法性不歸佇列管：handler 自己有前置檢查，不合法回 ok=false，
  而【那一條會被記進 command_log】⇒ 重播照樣重現
```

★**你那句「多半空不是保證空」我寫進 spec 了** ——
★★**規則要寫死，不是靠那個分支剛好是空的。**

# ★★★三、③那個第三寫入者：我不改鉤子，我釘結果

```
你找到的：sim_runner.gd:458 → clear_pending_targets → player_state.erase("pending_trade_target")
★我的判斷：那個清除是【對的】——你不能跟一個你剛走開的人交易
★★所以我不動它；★★★我動的是【它現在是一個由順序決定的結果，而沒有人在看它】
⇒ P8：同一顆 tick 內先 trade offer 再 move ⇒ 斷言 pending_trade_target 已被清除
   ★母體要求寫進判準：那條 move 必須【真的】讓玩家格子改變，否則鉤子不觸發 ⇒ 恆綠
```

★**這一格我特地標「不是在測 bug」** —— 它是把一個**今天正確、而且是順序決定的**行為
**釘成守衛**，理由逐字：**下次有人改順序時，沒有人會發現。**

# 四、②負對照：改成跨邊界，你的建議更強

```
舊：±1 tick ⇒ ★同一小時內位移一格很可能逐字相同 ⇒ 恆綠（我自己標了疑慮，你證實）
新：位移到【跨過一個小時或一天邊界】
★★你那句「母體選擇本身就帶保證」是關鍵差別：
  day_boundary 上 check_starvation_deaths／flush_forage_episodes／訊息剪枝【本來就在動東西】
  ⇒ ★★★不是「挑到敏感的那一條」，是【那個邊界保證有差】
```

# 五、順帶：你把 26 訂正成 24

```
★26 是【呼叫次數】，去重後 24 個 key —— 我在 spec §2-3 與 §3-3(a) 兩處都標了這個區別
★★而更值錢的是你逐個 key 去 grep 讀點：我那句「它不改世界」原本是【讀名字推的】，
  現在它是【逐個 key 核過的】
⇒ ★★★所以 §3-3(a) 的措辭我也改了：從「我推論」改成「R② 逐一核過，沒有系統直讀路徑」
```

# 六、狀態

```
三條都補完 ⇒ 依你「補完視為 CLEAN」，本票的 spec 我視為過閘
★而【不派工】：實作端在票A／票B 上，而本票排在它們之後（blueprint 的序）
★★spec：docs/superpowers/specs/2026-09-23-player-command-queue-HOW.md（239 行）
```
