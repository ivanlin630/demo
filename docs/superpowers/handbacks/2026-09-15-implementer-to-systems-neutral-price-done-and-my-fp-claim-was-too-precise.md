---
from: implementer
to: systems
status: consumed
slice: 票乙 ｜ 中性參考價已改 ＋ ★★★**逐域跑了，而我那句話要修正一個數**
topic: ★**改完之後那個 0 乾淨了**：單價 0 的 **0/165**，而預算／手上的錢 **min 0.087／中位 0.190／max 0.226** ⇒ **新路仍然 0 次，而現在那是【世界的事實】** —— **這些隊的採購預算只佔手上現金兩成**｜★★**逐域指紋**：**7 域裡 5 域逐字相同**（belief／factions／persons／tiles／world），**只有 `teams` 變**｜★★★**而我那句「每支隊少一筆」是錯的**：goal 筆數 **302 → 250，差 52，而隊數是 63** ⇒ **只有 52 支隊有過那個 goal**｜★**所以正確的結論比我原本寫的弱一格**，我照實寫
---

# ① 中性參考價（★裁定照做）

```
`_unit_price` ← **`BASE_PRICE.get(res, 0.0)`**（★而 res 不會是 coin ⇒ 那個陷阱在這裡不適用）
★理由我抄進 code 旁邊：**收益側 `derived_payoff` 已用 `BASE_PRICE` 換算成 coin
  ⇒ 成本側必須同幣別** —— ★★**我第一版是左手市價、右手心價。**
★★★**板上真實要價 ＝ 正解的下一步，而它撞感知鐵律**（未到場就讀得到要價 ＝ god-view）
  ⇒ **註明歸貿易軌**，寫在那一行旁邊 —— **免得下一個人偷偷改成直接讀板。**
```

# ② 改完之後的數字（★那個 0 現在是乾淨的）

```
買路走到 **165 次**｜**預算 > 手上的錢：0 次**
   單價 0 的：**0/165**（★改之前是 95/165）
   預算／手上的錢：**min 0.087｜中位 0.190｜max 0.226**
⇒ ★**儀器錯已經拿掉** ⇒ ★★**剩下的是世界的事實：這些隊相對於採購預算是現金充裕的。**
⇒ ★★★**所以「票乙今天 dormant」現在是一個【乾淨的結論】，不是一個混著儀器錯的 0。**
```

# ③ ★★★逐域指紋：**做了，而我的主張要收窄**

```
7 域 → **相同 5：`belief`／`factions`／`persons`／`tiles`／`world`（逐字相同）**
      **不同 1：`teams`**（＋彙總 `__all__`）
⇒ ★**與「行為沒變、狀態變了」一致** —— ★★**而它【不是證明】**：
  `teams` 域裡還有 resources／task／位置…… **這個測分不出域內是哪個欄位變的。**
★★★**而我原本寫的「每支隊少一筆」被自己的數字打掉**：
   goal 筆數 **main 302 → branch 250**，**差 52**；而**隊數是 63**
   ⇒ **只有 52 支隊有過 `maintain_coin`**（★11 支沒有 —— 我沒查為什麼，只報事實）
⇒ ★**正確的句子**：**差異侷限在 `teams` 域，且該域的 goal 筆數恰好少了 52 筆
  ＝ 曾有那個 goal 的隊數** —— **而「域內沒有別的欄位變」我證不了。**
```

# ④ 我沒有做、而你可能想要的下一格

```
★**要把它證死**，需要一個「`teams` 域排除 `goal_state` 之後的指紋」——
  ⇒ ★★而那要動 `state_fingerprint.gd`（加一個 debug-only 的變體）
  ⇒ ★★★**我沒有動它** —— **指紋是所有驗收的共同地基，為了一次診斷去改它，風險比收穫大。**
⇒ **若你要，我就做，而我建議的形狀是：不改既有函式，另加一支只給探針用的。**
```

# ⑤ 落地

```
worktree `.worktrees/coinprereq`｜branch `feat/coin-as-prereq`｜commit `faed4ae1b`
main：`scripts/debug/domain_fp_probe.gd`（★逐域探針，**它不下判決**）commit `8240c9e7e`
★另外你點名的兩件我都做了（commit `18df64e05`）：
  ①多 seed 床的假判決通道拆掉（★而它**真的**不下判決 ⇒ 宣告與事實一致）
  ②`TRADEABLE_RES` 補上「順序承載語意、新增附尾端、不得重排」
  ★★★而拆那條通道時**守衛又被我自己的註解咬到**：我在說明裡寫了那個欄位的字面
    ⇒ 閘掃全檔 ⇒ 照樣紅 —— **與 confusables 那次同形**（守衛與它要擋的東西不可以住在同一個字面上）。
```
