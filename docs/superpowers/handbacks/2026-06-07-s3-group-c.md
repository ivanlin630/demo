# Hand Back: S3 Group C — InteractionSystem 拆分

## 實作摘要

- `scripts/simulation/npc_combat_system.gd` — 新建，從 interaction_system 移出 21 個函式 + 12 個常數（計畫列出 10 個，另加 `ROUND_READINESS_DRAIN`、`LOOT_RATE` 因被移動函式引用）。含 `_init()` 自建 SimMessageSystem/SkillSystem/EquipmentSystem 實例。Public API：`process_ongoing_combat`, `tick_critical_npcs`, `start_combat`, `team_strength`, `calc_armed`, `subjugate_team`。
- `scripts/simulation/interaction_system.gd` — 移除戰鬥邏輯，加 `var _combat: NpcCombatSystem`，`process_on_arrival` / `_try_interact` / `subjugate_team` / `_should_pay_tribute` / `_should_attack` / `_try_diplomacy` / `_resolve_tribute` / `_write_tier2_intel` 改呼叫 `_combat.xxx()`。檔案從 1099 行縮減至 ~550 行。
- 外部 API 不變：`process_on_arrival`、`subjugate_team`、`resolve_trade_direct`、`resolve_extortion_direct` 全部保留在 InteractionSystem，呼叫方無需修改。

計畫與 spec 無差異。唯一補充：`ROUND_READINESS_DRAIN` 與 `LOOT_RATE` 雖未列入計畫常數移動清單，但被移動函式引用，故一併移至 NpcCombatSystem 並從 InteractionSystem 移除（否則編譯錯誤）。

## 連動風險

- `NpcCombatSystem` 建立自己的 `SimMessageSystem`/`SkillSystem`/`EquipmentSystem` 實例（與 InteractionSystem 各自獨立）。這些系統無狀態，多實例安全。
- `_kill_named_npc` 在 NpcCombatSystem 中動態 load `event_system.gd` — 同 interaction_system 原行為，無影響。
- `faction_ai_system.gd` 引用 `InteractionSystem.FOOD_RESERVE_TICKS` — 此 const 未移動，仍在 interaction_system，安全。

## 待主 session 確認

- 無
