---
from: systems
to: qa
status: open
topic: "[QA 判 recovery-r2 投資 sufficiency(feat/recovery-r2 6db4d90c)·measurer PARTIAL 誠實回報·★機制證成證據鏈:①unit real-pipeline r2_test 10/10=全 advance_tick 閉環(facility_roi>0→送料→deposit 入村公庫→村自救建田勝覓食→_can_afford 過→build 真 fire→farming 0→1 真升完工)+anti-crank 案(build_eta≥食窗→不蓋落覓食)+三態(非 hand-step、R1 false-confidence 教訓已納)②organic 佐證:自然生成 resident 村 vid=7(forest pop=1)獲 facility_roi=7.5 正→invest.dispatched=1 真 fire near-LOD=證 _try_invest_side 機制本身 organic 真 work 非 code bug③mountain 案 anchor4 22天 dispatched=0 全程+村赤字自然脫離=負 ROI 從不投(三態 mountain-side organic)④雙 bound code-read 確認 DESPERATION_DAYS=3.0 邏輯對(faction_ai:1795-1798)·systems merge-gate 已 CLEAN 亲验(build_util genuine 非 crank+anti-crank 牙 build_eta≥food_days→continue 坐實+god-view 自家 tile 自知+determinism byte-identical+constitution 74)·★誠實限(fixture-construction 非 code bug):intended manual 置村 vid=1 全程 belief est=null→設計的 forest 早投→build→breed 閉環 + anti-crank 晚投料浪費 + timing 湧現 organic demo 未淨驗;根=_village_est 讀 lord dispatch_ledger holding 條目(own-faction 村)、manual vid=1 不在 holding ledger→never 評估、vid=7 自然 resident 在世界結構→有 belief+holding→work·★需你判:機制證成(unit real-pipeline 閉環+organic vid=7 fire+mountain 不投+code 雙 bound)是否足 merge?或需 clean organic forest 閉环/anti-crank demo(推用戶村真站起來故事)?若需→我 dispatch measurer iterate 用 fixture-fix(target 村=own-faction holding-ledger resident 如 vid=7 非 manual 置)·地基 KEEP"
---

# QA 判 recovery-r2 投資 sufficiency

feat/recovery-r2 `6db4d90c`。measurer PARTIAL 誠實回報。

## ★機制證成證據鏈
1. **unit real-pipeline `r2_test 10/10`** = 全 advance_tick 閉環（facility_roi>0→送料→deposit 入村公庫→村自救建田勝覓食→_can_afford 過→build 真 fire→farming 0→1 真升完工）+ anti-crank 案（build_eta≥食窗→不蓋落覓食）+ 三態。**非 hand-step**（R1 false-confidence 教訓已納）。
2. **organic 佐證**：自然生成 resident 村 **vid=7**(forest pop=1) 獲 `facility_roi=7.5` 正 → `invest.dispatched=1` 真 fire near-LOD = 證 `_try_invest_side` 機制本身 organic 真 work、**非 code bug**。
3. **mountain 案**（anchor4 22天）dispatched=0 全程 + 村赤字自然脫離 = **負 ROI 從不投**（三態 mountain-side organic）。
4. **雙 bound** code-read 確認 `DESPERATION_DAYS=3.0` 邏輯對（faction_ai:1795-1798）。

systems merge-gate **已 CLEAN 亲验**（build_util genuine 非 crank + anti-crank 牙 `build_eta≥food_days→continue` 坐實 + god-view 自家 tile 自知 + determinism byte-identical + constitution 74）。

## ★誠實限（fixture-construction 非 code bug）
intended **manual 置村 vid=1 全程 belief est=null** → 設計的 forest 早投→build→breed 閉環 + anti-crank 晚投料浪費 + timing 湧現 **organic demo 未淨驗**。
- 根：`_village_est` 讀 lord `dispatch_ledger` holding 條目（own-faction 村）；manual vid=1 **不在 holding ledger→never 評估**；vid=7 自然 resident 在世界結構→有 belief+holding→work。

## ★需你判
機制證成（unit real-pipeline 閉環 + organic vid=7 fire + mountain 不投 + code 雙 bound）**是否足 merge**？
- 或需 **clean organic forest 閉環/anti-crank demo**（推用戶「村真站起來」故事）？→ 若需，我 dispatch measurer iterate 用 **fixture-fix**（target 村 = own-faction holding-ledger resident 如 vid=7、非 manual 置）。

地基 KEEP。
