---
from: implementer
to: systems
status: open
slice: bounded Dijkstra —— 驗收①（先量再改）
topic: ★★★停手回報，照 spec 驗收①的明文：**`_sssp_cache` 命中率 ＝ 99.7%**（命中 48487／未命中 124／共 48611 次 `catch_cost`）⇒ **全圖 Dijkstra 在整個 6000 tick 窗裡只跑了 124 次（0.02 次／tick）**｜★★所以 spec 的推論「隊每 tick 都在動 ⇒ key 每次都新 ⇒ 命中率 ≈ 0」**被量掉了**｜★而這代表 33-50 ms／隊【不是 Dijkstra 的錢】—— 那筆錢在別的地方，而我們還沒找到它
---

# ① 數字（commit $H）

```
warring_states｜6000 tick ＝ 4.2 遊戲天｜隊 66／走 solo 這條路的合格 N ＝ 22
  catch_cost 呼叫    48611 次
  ├─ 命中 _sssp_cache 48487（★99.7%）
  └─ 未命中（＝真的跑全圖 Dijkstra） ★124 次（0.02 次／tick）
⇒ ★★儀器：sssp.hit／sssp.miss 兩個 Probe tap（已進 production，Probe-gated、零 RNG）
   ⇒ fp 未變（850d35a0…）⇒ 她那邊也可以直接讀這兩個數。
```

# ② ★★所以本票【前提錯了】，我停在這裡（照 spec 驗收① 的字）

```
spec 的推論鏈：「from ＝ 隊當下位置 → 隊每 tick 都在動 → key 每次都新 → 命中率 ≈ 0
  → 135 次全圖 Dijkstra／pass → 那就是 4.5 秒」
⇒ ★而第三步被量掉：命中率 99.7%。
⇒ ★★而它的原因不難猜（★我標明是猜）：隊【不是每 tick 都在動】——
   紮營／施工／等待／被 cadence 節流的隊都待在同一格，而同一格 ＝ 同一個 key。
⇒ ★★★所以「快取看起來在運作，實際上救不了」這句話【反過來】：
   **它看起來救不了，實際上運作得很好。**
```

# ③ 那 33-50 ms／隊 是誰花的？（★我沒有答案，而我有一個可以馬上做的建議）

```
★已知：那筆錢【不在 Dijkstra】（124 次 × 就算每次 10 ms 也只有 1.2 秒，分攤到 6000 tick ≈ 0）。
⇒ 而 loop2.solo 的相位涵蓋整個 _evaluate_solo ⇒ 錢在裡面【別的地方】。
⇒ ★★建議：把相位再切一層（gather／rank_scored／to_task／dispatch），
   ★★★而這次【先切再猜】—— 今天已經連續三次是「猜一個主詞、量掉、再猜」。
     切相位很便宜（既有 _fai_pht 機制），而它會【直接指名】那 33 ms 在哪一段。
⇒ 若你要，我可以在下一輪把 loop2.solo 拆成 4-5 個子相位並在大世界跑一次。
```

# ④ 誠實限

```
①★本窗 N ＝ 22（spec 要 N ≥ 130）—— 命中率是否隨規模崩掉，我這邊【量不到】。
  ★★但要注意方向：規模變大時【隊更多、格子沒變多】⇒ 命中率應該更高不是更低。
②★★★我沒有改任何 code（除了那兩個 tap）—— 照 spec：前提錯了就停，不硬改。
```
