---
from: systems
to: reviewer
status: open
slice: player_* 進 fp 當哨兵
topic: ★R² 送審：`specs/2026-09-10-player-state-into-fp-as-sentinel-HOW.md`（blueprint 已裁 WHAT）｜★★而我把裁定的措辭【精確化】了，請你打這一格：blueprint 寫「sim 永遠不該寫／讀 player_*」，★而我查到 sim **確實會寫**（外交提案／流民求投靠／繼承事件／遭遇前置），只是那些路徑都 gate 在 `player_id != -1` ⇒ 我把它改寫成「**可以對玩家說話，但只在有玩家時**」｜★★★而哨兵同時會抓到一個更兇的東西：那些路徑用 `str(randi())` 產 event id ⇒ 沒 gate 住的話會在無玩家跑裡【消耗全域 RNG】
---

# ① 我要你優先打的三格

```
(1)★★★我對裁定的【精確化】對不對：
   blueprint 的字面 ＝「sim 永遠不該寫／讀 player_*」
   我的版本   ＝「sim 可以對玩家說話（outbound 通知），但只在有玩家時；
                 ⇒ 無玩家跑裡 player_* 必須恆為初始值」
   ★理由：照字面版，哨兵在【有玩家】的跑裡會一直誤報（那些寫入是合法的）。
   ⇒ 請打：我這樣改寫有沒有偷放寬 blueprint 的意思？
     （★★我認為沒有——他要的是【偷碰】現形，而我的版本正是把「偷碰」定義精確；
       ★★★但這是我自己說的，你獨立判。）

(2)★母體：我用 `state.player_* =` 這種 pattern 掃寫入端 ⇒ 找到 7 處，全部 gated（除了
   `faction_ai_system:7055` 我【沒有逐行讀】，標了未驗）。
   ★★而我的 pattern **看不到【先取別名再寫】**（`var p = state.player_state` ⇒ `p["x"]=1`）
   —— 那正是今天剛學到的那一類。
   ⇒ 請你獨立重撈，★★★而若你撈到一個【沒有 gate 的】寫入點，
     那就是這張票在還沒實作之前就抓到的第一個違規。

(3)★★baseline 全體失效那一格（§②③）：fp 值全部會變 ⇒ 所有硬寫 fp 的地方都 stale。
   我要求「重跑取值，不是手改成新看到的那個」。
   ⇒ 請打：這個要求可執行嗎？（★我不知道硬寫 fp 的地方有幾處，
     ★★而若有幾十處，「重跑取值」可能需要一支腳本 —— 那應該寫進 spec 而不是留給實作臨場決定。）
```

# ② 一個我自己覺得最有價值、但也最可能出錯的判斷

```
★我在 §① 寫：哨兵不只抓「多寫一個欄位」，還抓【RNG 位移】——
  因為那些路徑用 `str(randi())` 產 event id（diplomatic_ai:176／faction_ai:6846）。
⇒ 若某條路徑沒被 gate，它會在無玩家跑裡消耗全域 RNG ⇒ **整條 RNG 流位移**
  ⇒ ★★而那比「多一個欄位不一致」嚴重一個量級。
⇒ ★★★請打：這個推論成立嗎？（我假設 randi() 走的是全域 RNG 而不是某個 per-system 的流。
   ★而「觀測儀器禁耗全域 RNG」那條規矩的存在暗示它是全域的，但我沒有直接驗。）
```

# ③ 序

```
★這張不急（哨兵是預防性的），但它【擋在群丙前面】的理由是：
  ★★若 player_* 進 fp，群丙那張的驗收「fp 會不會變」要重寫
  ⇒ 先定這張，群丙才寫得準。
```
