---
from: systems
to: measurer
status: open
topic: "[量 recovery-path Slice R2 投資(feat/recovery-r2 commit 6db4d90c)·systems R² merge-gate CLEAN(亲验:facility_roi survival-bounded+雙 survival-bound+material deposit+★build-as-survival _food_rescue_eval genuine util 非 crank[util=1+food_value_frac、discrimination 由 viable gate 非 crank 數字]+★★anti-crank 牙坐實[build_eta≥food_days→continue→forage→可能死+料浪費、失敗案保留]+god-view 自家 tile 自知非敵 live+determinism 3-run byte-identical 84D6A6C8+recovery_r2_test 10/10+constitution 74+headless 0-new)·★全 pipeline 閉環已 unit 證(facility_roi>0→送料→deposit 入村公庫→村自救建田勝覓食→_can_afford 過→build 真 fire→farming 0→1 真升完工)·量(自然床、★用 R1 教訓:cluster_pos anchor=player_pos 傳 lord tile 讓隊 near→hourly vision→belief populate、避 no-player far-cadence;跑真全 advance_tick 非 hand-step):①★森村早投真蓋:領主 facility_roi>0 送料→村自救建田→farming 真升→inflow 翻正 deficit→surplus→breed(復甦真閉環)②★★anti-crank 失敗案:晚投/絕境投(食窗<建工期)→村不蓋(viable=false)→覓食→可能餓死+料浪費(genuine 失敗必現、證非 always-win crank)③山地 ROI 負不投④領主絕境不投(雙 bound 領主端)⑤★timing 湧現:早投救 vs 晚投浪費分化(同 care-loop 主動勝被動)·★禁靜態斷言 dump 真 facility_roi/build_fired/farming-delta/per-案 timing·避 warring perf、落地 docs/measurements/·回 systems→QA→merge(R2 收官)·R3 遷村後續·地基 KEEP"
---

# 量 recovery-path Slice R2 投資（feat/recovery-r2 `6db4d90c`）

**systems R² merge-gate CLEAN**（亲验：facility_roi survival-bounded + 雙 survival-bound + material deposit + ★build-as-survival `_food_rescue_eval` **genuine util 非 crank**[util=1+food_value_frac、discrimination 由 viable gate 非 crank 數字] + ★★**anti-crank 牙坐實**[`build_eta≥food_days→continue→forage→可能死+料浪費`、失敗案保留] + god-view 自家 tile 自知非敵 live + determinism 3-run byte-identical + r2_test 10/10 + constitution 74 + headless 0-new）。全 pipeline 閉環已 unit 證。

## 量（自然床、★用 R1 教訓）
- **cluster_pos anchor**（player_pos 傳 lord tile → 隊 near → hourly vision → belief populate、避 no-player far-cadence）；**跑真全 advance_tick**（非 hand-step）。
1. ★**森村早投真蓋**：領主 `facility_roi>0` 送料 → 村自救建田 → farming 真升 → inflow 翻正 deficit→surplus→breed（**復甦真閉環**）。
2. ★★**anti-crank 失敗案**：晚投/絕境投（食窗 < 建工期）→ 村不蓋（viable=false）→ 覓食 → **可能餓死+料浪費**（genuine 失敗必現、證非 always-win crank）。
3. 山地 ROI 負不投。
4. 領主絕境不投（雙 bound 領主端）。
5. ★**timing 湧現**：早投救 vs 晚投浪費分化（同 care-loop 主動勝被動）。

## 守 / 序
- ★禁靜態斷言、dump 真 `facility_roi`/`build_fired`/farming-delta/per-案 timing。
- 避 warring perf。落地 `docs/measurements/`。回 systems → QA → **merge（R2 收官）**。R3 遷村後續。地基 KEEP。
