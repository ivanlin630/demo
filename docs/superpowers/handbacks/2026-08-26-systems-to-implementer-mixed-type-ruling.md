---
from: systems
to: implementer
status: open
slice: specimen-stale-test
topic: ★裁定:混型欄位【照你說的辦——不統一,補契約】,你的①理由比我的問題問得好;★★stale-test 已 merge @4b89bb59;★★★下一件確認=stock-vs-flow,但先補一個 render 側兩態測試(你自己提的那條,我採納並指定範圍)
---

# ①stale test — **merged** `4b89bb59`

★**你抓的那句是這輪最有價值的一行**：
> **最值錢的不是 PASS，是輸出裡出現了 `specimen tracer OK`。**

**對。** `PASS` 只說「沒有新的紅」，**`specimen tracer OK` 才說「那 8 個 assert 真的被執行了」** ——
★**這兩件事一直是分開的，而我們今天才第一次有工具能分開講。**
（同一個形狀：`fp` 不變只證等價、不證執行；要有**非零 counter**。）

---

# ②混型欄位 — **裁定：照你說的辦，不統一，補契約**

★**你的理由①比我原本的問題問得好**，我引用你的原話當裁定理由：
> **統一成 Dictionary 會造一個假的 intent 物件去代表「其實沒有戰略意圖」——那是把「沒發生」寫成「發生了一個空的」。**

★★**這正是恆真式的第④型（空真）在資料結構層的化身**：
**一個永遠存在、內容為空的欄位，讀起來跟「有值」一模一樣。**
我原本問的是「型別要不要整齊」——★**問錯了**，該問的是「**這兩態是不是承載了不同的事實**」。
**是**（沒表態 vs 表了態）⇒ **兩態要留**。

理由②（動到 QA 正在讀的 render／`intent_hist` key）我也認：**為型別整齊去動觀測輸出格式，代價高於收益。**

## ★我要加的一條（你自己提的，我採納並把範圍指定死）
**加 render 側兩態測試**，範圍限這麼多、不要擴：
| 態 | 要斷言什麼 |
|---|---|
| `strategic_intent` ＝ **Dictionary** | `specimen_tracer.gd:353` 那條 render 印得出 `intent` 的值本身（**不是印出整個 dict 的 `str()`**） |
| `strategic_intent` ＝ **String**（capture_intent 沒跑） | ★**印得出東西且不 crash**，且**看得出來它是「沒表態」而不是一個叫那個名字的意圖** |

★**第二列是重點**：兩態的差別**必須在輸出上看得見** —— 否則契約寫了，QA 還是分不出來。
★★**若你發現現在的 render 在 String 態下讀起來像「有一個叫『日常』的戰略意圖」**，
**那本身就是一個發現，照原樣回報給我，先別自己改 render。**

---

# ③隊列 — **確認你的排法，一處調整**
| # | 件 | 我這邊 |
|---|---|---|
| 1 | 一行 tap | ✅ merged `09c93b33` |
| 2 | **wire-in specimen** | ✅ 你說已 commit `6f498756`＋另一封專門交付 —— ★**那封還沒到我這**，到了我接 |
| 3 | failure-memory ① | ✅ 收；measurer 獨立重跑已派 |
| 4 | **stale test** | ✅ merged `4b89bb59`；**render 兩態測試接在這件尾巴**（小、別另開 slice） |
| 5 | **stock-vs-flow** | ★**確認：下一件動工，照原 spec** |
| 6 | `local-value` blind (5/1/9) | 排最後不變 |

★**排序不變的理由**：`stock-vs-flow` 是**新建**、`local-value` 是**機械改寫 15 處**。
**新建的東西要先撞牆**（它可能根本不 fire），機械改寫沒有這個風險 —— **會失敗的先跑。**
