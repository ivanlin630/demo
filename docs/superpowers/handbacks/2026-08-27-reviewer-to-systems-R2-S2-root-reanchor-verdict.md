---
from: reviewer
to: systems
status: open
slice: S2-root-reanchor
topic: R②判決:clean(小建議)——①拆法對+獨立查triage確認只剩一顆(a_change=1/b_defer=1);②fp反向邏輯站得住;③交易成交排除認同,建議輕量知會blueprint非請示
---

# 判決：`clean`（一個輕量建議，不阻擋）

## ①TICKS_PER_TURN拆法——**對，而且我沒有只信你的轉述，自己查了 triage 檔**
`docs/measurements/2026-08-27-bare-tick-triage.txt` 對帳：`a_change=1、b_defer=1、c_whitelist=49、d_not_time=92`，合計143對上母體。★**獨立確認「只剩一顆延後」是真的**——`b_defer` 只有 `TICKS_PER_TURN` 這一條；`a_change` 也只有一條（`player_trade_system.gd:103` 的 `current_tick-1000`，記憶淡出窗口，語意上就是「100 小時」，1000/10=100 整數，分類正確）；`c_whitelist` 49 條清一色是曆法結構（`%24`小時制、`%30`日、`%4`季）——**這些從一開始就不是「tick 時長」候選，是行事曆索引常數，不在我今天擔心的「巧合整除」陷阱射程內。S1b 的分類乾淨，S2 這裡的拆法（保時長歸S2／該多長歸S7）站得住。**

**(b)** `minutes()` 加在 S2——單一新工具函式+單一呼叫點改寫，範圍跟 S1 對 39 顆常數做的事同類型，不算撐大 diff。

**(c)** 有沒有別的裸值被靜默改語意——上面對過 triage 全表，143 顆已全部結案（`NEEDS_HUMAN=0`），沒有第二顆遺漏。

## ②`fp`「沒變才是警訊」——**寫法對，被亂改的風險低**
S2 動的是全域根旋鈕，牽動幾乎所有計時相關計算（移動時長／決策 cadence／資源累積率……），要讓整個世界軌跡在改動後逐位元不變，除非某處根本沒吃到新值（快取/初始化順序/漏改），否則工程上幾乎不可能湊巧不變——**implementer 為了「讓 fp 變」刻意亂改東西的誘因很低，因為正常做完這票 fp 自然就會變；反而「不變」才是真的異常訊號**。這個反向寫法本身就是防呆，不是陷阱。

## ③交易成交排除——**認同，但建議輕量知會 blueprint，不用整個回頭請示**
12 筆／2000 tick 的母體，5% 相對差門檻在這個量級下沒有解析度（一筆之差就晃 8%+），拿它當生死判準只會讓噪音決定結果，這條排除是誠實處置不是放寬標準。★**但它是 LOCKED spec 明列的項目**——建議：**照你現在的做法送(印出來、不列入判準、記進known_issues)，同時發一則輕量通知給 blueprint（不是等回覆的請示，是讓他知道這個數據點存在、你排除的理由）**——這樣有記錄，不用因此卡住 S2 的 dispatch。

**premise_contradiction: false，三處都確認過，implementer 可以直接動工，不用等我再審。**
