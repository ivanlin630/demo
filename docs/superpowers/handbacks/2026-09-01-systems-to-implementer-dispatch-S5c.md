---
from: systems
to: implementer
status: open
slice: S5c-hunger-fatigue-halve
tier: behavior
topic: ★★★DISPATCH(R² CLEAN)——S5c 飢餓/疲勞減半,★四顆常數各 ×2;★★而三票的【順序】有理由:c → a → b,把【會重擲世界】的那一票放中間、把【依賴它】的放最後;★★★這票【不要】跟另外兩條一起交(spec 明令各自單獨落地)
---

# spec
`docs/superpowers/specs/2026-09-01-S5-background-laws-HOW.md` §S5c｜**R² CLEAN**

# ★①要改的（★四顆，全部 ×2）
```
resource_system.gd:38  HUNGER_GAIN_PER_DAY      0.05  → 0.10   (20 天滿 → 10 天)
resource_system.gd:39  HUNGER_RECOVER_PER_DAY   0.1   → 0.2    (10 天   →  5 天)
sim_runner.gd:9        FATIGUE_PER_DAY          0.048 → 0.096  (~20.8  → ~10.4)
sim_runner.gd:10       FATIGUE_RECOVERY_PER_DAY 0.24  → 0.48   (~4.2   → ~2.1)
```
★**WHAT 明寫要保的那件，我驗算過、reviewer 也獨立確認**：**「恢復比積累快」的比例【不變】**
（飢餓 1:2、疲勞 1:5 —— ★★同乘一個數，比例是數學上必然成立）。

# ★★②三票的順序：**c → a → b**（★理由不是難易，是【可比性】）
```
★S5c(本票):純數值,★不動 RNG ⇒ ★★同 seed 的前後比較【還可用】⇒ 先做,趁世界還可比
★★S5a(季節曲線):刪 randf_range ⇒ ★★★之後每顆骰子都換人擲 ⇒ 世界大幅分岔
   ⇒ 放中間,而它的驗收走【函數層】不走世界層前後比
★S5b(池再生×季節):它【依賴】a 的平滑曲線才有意義 ⇒ 放最後
```
★**⇒ 把【會摧毀可比性】的那一票放在【需要可比性】的那一票之後。**

# ★★★③驗收（★三條，而②比①硬）
```
①★算術:四顆的「到滿值天數」各自減半 —— ★★可直接算,不必跑（但要在交件裡寫出算式）
②★★★世界層:餓死／疲勞事件的【發生時點分佈】前移
   ★同 seed 前後比【本票可用】(不動 RNG),而★★仍要用【分佈】不是單點
   ⇒ 印:首次餓死 tick／首次疲勞滿 tick／兩者的中位與四分位
③★fp 必變(數值改了行為就變)——★★交件先聲明走的是哪一種,並附三跑 byte-identical
```

# ★★★★④誠實限（★交件不得違反）
- ★**不得宣稱「戲更好看」** —— ★★**那是新基線考憑質地判的，不是這一票的**（WHAT 自己標「試跑」）。
- ★**不得順手動別的常數** —— ★★**這四顆之外的任何數值變更，都會讓②的分佈比較失去歸因。**
- ★**若某一顆改完發現它【不是】那個語意**（例：`FATIGUE_PER_DAY` 其實不是每日累積）
  ⇒ ★★**停下來回報，不要調整到「看起來對」** —— **那是把判準調到數字會過。**

# ⑤閘
`憲法閘 / 裸 tick 閘 / 床解析閘 / headless Q1`；★**新 `class_name` 先 `--import`**。
