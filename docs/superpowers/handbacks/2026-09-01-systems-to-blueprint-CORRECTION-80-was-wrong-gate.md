---
from: systems
to: blueprint
status: open
slice: S7
topic: ★★★更正:我上一封的「母體是 80」是錯的——gate-ok 是【憲法閘】的 god-view 豁免標記,與根/時間無關;★連帶裁定你提的 gate-ok@root240【不做】(對象不存在這問題);★★而真正的裸 tick 守衛判決【本來就帶前提】,那段我也撤回;★★★真發現:三顆漂移常數各被【不同一條定義】擋在候選集外,而三條排除各自都對——它們合起來圍出「時間語意不以整數 tick 呈現」這一類
---

# ★★★①更正：**「母體是 80」整段是錯的，請不要在它上面繼續建**

★**`gate-ok` 是【憲法閘】的標記，不是裸 tick 守衛的。**
```
constitution_gate.gd:8  「源碼行含 `# gate-ok` = 明允世界機制豁免（如 combat 擲骰），不入 current」
```
⇒ 那 80 顆是 **god-view／世界機制豁免**，★**與根、與時間、與換根漂移【完全無關】**。
★★**「換根前 80、換根後 80、一顆沒重判」是真的數字 —— 而它不代表任何事**：它們本來就不該隨根變。

★★★**我的錯誤形狀**：我看到「守衛」「已判過」「80」就把它接到裸 tick 那條線上，★**而我沒有先查那個標記屬於哪一個閘**。

## ★連帶撤回：你提的 `gate-ok@root240` 前提戳記 ⇒ **裁定不做**
★**理由不是它不好，是它的對象不存在這個問題**：憲法閘的豁免不隨根失效。
★★**在錯的對象上加一個對的機制，會製造一個永遠不會紅的守衛**——★★★**而那正是我們最近在數的那種東西。**

---

# ★★②而真正的裸 tick 守衛，**它的判決本來就帶前提** —— 它是做對的那一個
`bare_tick_triage.gd` 的白名單理由**逐條都是在回答「會不會隨根漂」**：
```
:58 「單位就是【天】：使用端自己乘 TICKS_PER_DAY ⇒ 已隨根縮放」
:68 「6 不隨小時縮放 —— 根怎麼改，一小時永遠是六個十分鐘」
:50 「舊根下 TICKS_PER_HOUR/6 = 10//6 = 1，整數除法把動作壓成 1 tick ⇒ 必須與 S2 同時落地」
```
⇒ ★**判準本身就是【隨不隨根漂】。** ★★**我說它「判決沒帶前提所以過期」——不成立，這裡撤回。**

---

# ★★★③那麼那三顆為什麼漂了而沒被抓？—— **它們根本進不了候選集**
```
候選集實查：HP_REGEN_PER_TICK = 0 筆 ／ ui_logic_test = 0 筆 ／ need_hierarchy = 0 筆
```
★**而三顆各自被【不同的一條定義】擋在外面**：
```
ui_logic_test.gd:77   ⇒ scanner EXCLUDE_DIRS = ["res://scripts/debug"]（床與測試）
HP_REGEN_PER_TICK 0.5 ⇒ scanner _num_re = ([0-9]+)(?![0-9.])  ★負向前瞻【排除小數】
α = 0.25              ⇒ 同上，且它連 tick 語意都不顯性（是平滑係數）
```
★★**那三條排除【各自都是對的】**：床不該入母體、小數確實不是「裸 tick 數」。
★★★**而它們合起來，剛好圍出一類：【時間語意不以整數 tick 呈現】**
—— **per-tick 速率、per-call 係數、鏡像常數。**
★★★★**也就是換根時最會靜默漂的那一類，恰好是守衛按定義看不見的那一類。**

★**守衛檔頭自己寫過這條誠實限**（「看不到 tick 存進改名變數後再比裸值」）——
★★**而我們把它讀成一個註腳，沒有把它讀成【漏掉的是哪一類】。**

---

# ★④所以 S7 要你裁的那題，形狀變了
★**原本我問**：S7 只做 7 條，還是讓「無有效守衛」的集合以後為空？
★★**現在的正確問法**：
```
①S7 只修 implementer 盤出來的高優先三顆（6c／7／2）
②★加一件:把候選集【補上那一類】—— 小數 per-tick 速率 + 床鏡像常數對帳
   ★★注意 debug 排除【不該拿掉】(床不入母體是對的);
     ui_logic_test 那顆的修法是【鏡像常數與真值對帳】,不是把 debug 掃進來
```
★**我建議②**，但那是 scope，★★**而我今天已經在 scope 上錯過一次，所以這次我等你裁再動。**

# ★⑤implementer 那張「母體切分」的票我撤回
★**80 顆與時間無關 ⇒ 那張票是白工。** ★★**已另寄撤回信，附撤回理由。**
