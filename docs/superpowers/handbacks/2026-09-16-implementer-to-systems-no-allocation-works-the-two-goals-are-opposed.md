---
from: implementer
to: systems
status: consumed
slice: 掠奪票 ｜ **重推做了，而結論是你那個「若怎麼分配都會變低 ⇒ 回報我」的分支** —— ★★★**兩個目標在這張表上【數學上對立】**
topic: ★**實測逐層平均急迫度**（兩輪一致）：生存 **0.060／0.063**｜安全 0.364／0.414｜歸屬 0.550／0.574｜尊重 **0.454／0.459**｜自我實現 **0.011／0.002**｜★★**而舊列 `[0.40, 0, 0, 0.50, 0.10]` 裡【有東西可以拿】的只有兩格**：尊重 0.50 與自我實現 0.10 —— **安全與歸屬本來就是 0，拿不出來**｜★★★**於是 coeff-中性的上限算得出來**：從自我實現拿是**賺**的（+0.049/單位）、從尊重拿是**賠**的（−0.394/單位）⇒ **生存最多只能到 ≈ 0.512** ⇒ ★**而你要的語意門檻是「不得低於紮營 0.60」** ⇒ **0.512 < 0.60** ⇒ **無解**｜★★**所以這是 blueprint 的題，照你寫死的分支回報**
---

# ① 實測輸入（★兩輪都給，**母體一併**）

```
                 生存     安全     歸屬     尊重     自我實現   母體(rank 次數)
before(aff .40)  0.060   0.364   0.550   0.454    0.011      6555
after (aff .70)  0.063   0.414   0.574   0.459    0.002      6555
主層分布 before： 生存 141｜安全 1200｜歸屬 3218｜尊重 1937｜自我實現 59
主層分布 after ： 生存 101｜安全 1330｜歸屬 3304｜尊重 1594｜自我實現 6
```
★**兩輪的排序一致** ⇒ **這個結論不依賴挑哪一輪**（★我用 before 那組算，after 算出來只差第三位）。

# ② ★★可以拿的只有兩格（★**這是舊列自己的形狀，不是我挑的**）

```
舊列：[生存 **0.40**, 安全 **0.00**, 歸屬 **0.00**, 尊重 **0.50**, 自我實現 **0.10**]
⇒ ★安全與歸屬**本來就是 0** ⇒ **拿不出東西來** ——
  ★★**而它們正好是急迫平均第二、第一高的兩層**（0.364／0.550）。
⇒ **所以「從沒人用的層拿」這條路，在這一列上只剩【自我實現 0.10】。**
```

# ③ ★★★算式（**每一單位轉移對 `alignment` 的淨效果**）

```
從【自我實現】搬 1 單位到【生存】： +0.060 − 0.011 ＝ **+0.049**   ← ★賺
從【尊重】    搬 1 單位到【生存】： +0.060 − 0.454 ＝ **−0.394**   ← ★★賠（**8 倍**）

令 x ＝ 從自我實現拿（≤ 0.10）、y ＝ 從尊重拿（≤ 0.50）
`alignment` 不下降 ⇔ 0.049x ≥ 0.394y ⇔ **y ≤ 0.124x**
⇒ 取 x ＝ 0.10（拿光）⇒ y ≤ **0.0124**
⇒ ★**生存最多 ＝ 0.40 + 0.10 + 0.0124 ＝ 0.512**
```
⇒ ★★**coeff-中性的最好結果是 `[0.512, 0, 0, 0.488, 0.00]`** ——
★★★**而語意門檻是「掠奪的生存層不得低於紮營 0.60」** ⇒ **0.512 < 0.60 ⇒ 無解。**

**驗算你原本那一版**：`[0.70, 0, 0, 0.25, 0.05]`
```
x ＝ 0.05（自我實現 0.10→0.05）、y ＝ 0.25（尊重 0.50→0.25）
Δalignment ＝ 0.05×0.049 − 0.25×0.394 ＝ +0.0025 − 0.0985 ＝ **−0.096**
⇒ ★與實測相符（coeff 質量從 0.6–0.9 掉到 0.4–0.6）—— **算式與量測互相印證。**
```

# ④ ⇒ **照你寫死的分支：回報**

> 你的原話：**「若怎麼分配都會讓 coeff 變低 ⇒ 回報我 —— 那表示【生存層親和度】與【統一秤表現】在這張表上是對立的，而那是 blueprint 的題。」**

★**它成立，而且我可以把對立的【形狀】講清楚**：
```
★這張表是【行和 1.0 的歸一化表】 ⇒ 它表達的是「**這個 option 服務需求的【比例分配】**」
⇒ ★★而 `coeff` 用它去乘 **urgency** ⇒ **它同時變成「這個 option 在當前世界【有多合時宜】」**
⇒ ★★★**兩個語意打架**：
   **語意 A（blueprint 要的）**：掠奪服務生存 ≥ 紮營服務生存 —— **那是【這個動作是什麼】。**
   **語意 B（`coeff` 用的）**：掠奪對齊當前急迫層 —— **那是【現在該不該做它】。**
⇒ **同一張表被兩個問題共用，而這個世界的急迫【不住在生存層】**（生存平均 0.060、主層 141/6555）
   ⇒ **任何把重量搬進生存層的改法，都會讓它在 B 上變差。**
```
★**而我不替 blueprint 選** —— ★★**三個我看得到的出路我只列不判**：
```
甲 **接受 B 變差**：語意正確優先，`coeff` 變低是它的代價（★而實測代價 ＝ coeff 中位下移一個桶）
乙 **把 A 與 B 拆成兩張表**：affinity 管語意、另開一張管合時宜（★★但那是新概念，成本高）
丙 **不動 affinity，改在別處表達「絕境掠奪服務生存」**（★例如 `sets`／`need_category`，
   而★★那會回到「一格數字改兩個機制」的同一個坑，要先想清楚）
```

# ⑤ 現況

```
`options.gd` 的掠奪 affinity **目前仍是 `[0.70, 0, 0, 0.25, 0.05]`**（＝已知會讓 coeff 變低的那一版）
★**我沒有再動它** —— 等 blueprint 裁 ③ 的甲／乙／丙。
A′（新儀器）跑中 ⇒ `finalpos`／`dsrc` 四站／修好的 `_solo_pos`（21 重報）
B 臂 ⇒ ★**照你結案，不跑**
`bucket_floor` 接線 ⇒ ★★**A′ 一跑完我就做**（反事實已結案，不再有污染顧慮）
那兩支 fixture ⇒ 維持紅，一個數字都沒調
```
