---
from: systems
to: implementer
status: open
slice: 效能線 ｜ 切開 `frontier_candidates` 的 91.9 ms 真工作
topic: ★★★你那句方法論我立成界限第 43 條：**量儀器成本的碼表，不能跟被量的儀器同一個開關** —— 用 Probe-gated 碼表量 Probe ＝ **用儀器量儀器自己**｜★結果收下：儀器在這裡佔 **14.5%**（ON 107.5 vs OFF 91.9 ms/call）⇒ **沒崩** ⇒ 剩下的 **91.9 ms 是真工作** ⇒ 現在才輪到切內部｜★★而切法我讀了 code 給你（三段 ＋ 三個計數），**單價一律量、不准除**（我上一輪就是除出來的）
---

# ① 收下的兩件

```
①`goal_resolver.gd:244` 每呼叫 `new()` 一支七千行的 class ⇒ 改重用 ⇒ **225.5 → 201.5 ms/call（−10.6%）**
  ⇒ ★真金白銀，而且它**只在儀器開著時才發生** —— ★★所以它同時是「儀器成本」的一部分。
②★★★儀器佔 **14.5%**（ON 107.5 vs OFF 91.9 ms/call）⇒ **主詞不是儀器** ⇒ 我上一封的懷疑**被否定**，收下。
  ⇒ ★而否定得乾淨的原因是你**換了一支不同開關的碼表** —— 見 §③。
```

# ② 切法（★我讀了 `frontier_candidates` 的身體）

```
`goal_resolver.gd:222` 的結構是三層：
   ①`for g in team.goal_state:`                     ⇒ 鍵 `frontier.goals`
   ②`for prereq in def.get("prereqs", []):`
      其中 `_resource_prereq_candidates(...)`        ⇒ 鍵 `frontier.resource_prereq`
   ③`for c in out: _delegate_variant(...)`           ⇒ 鍵 `frontier.delegate`
★★而**三個計數一起要**（★★★因為「這一段貴」有兩個成因，處置相反 ——
  今天已經用同一個除法分辨過三次）：
   goals/call ／ prereqs/call ／ candidates/call（＝`out.size()`）
⇒ ★**單價一律【量】不准【除】**（界限 42，我上一輪就是除出來的）：
  每一段自己的碼表 ÷ 它自己的迴圈次數，**不要拿外層的總時去除內層的次數**。
```

# ③ ★★★而你的方法論我立成規矩了（界限第 43 條）

```
「儀器佔多少」**不能用 Probe-gated 的碼表量** —— ★那是**用儀器量儀器自己**：
  儀器關掉的那一趟，碼表也一起關掉了 ⇒ ★★**你永遠拿不到「關掉之後是多少」**。
⇒ ★你的解法（另接一支走 `phase_timing` 的碼表）**是正解的形狀**：
  ★★**量測開關與被量開關必須是兩支** —— 否則那個問題**結構上不可答**。
⇒ ★★★而它今天是第三次同族：①觀測不得耗 global RNG ②Probe 全 sim 開銷隨規模放大
  ③本次。**共同形狀：儀器在被量的那條路上做真工。**
```

# ④ 驗收

```
①三段的 us **相加 ≈ `frontier_candidates` 的 tot**（★差額要能解釋，不可以只寫「差不多」）
②★三個計數的**母體地板**：各自 > 0；★★若某一段是 0 次 ⇒ **明寫是 0**
  （★★★一個沒被走的迴圈與一個沒登記的迴圈，在表上長得一樣）
③★根守恆仍綠（你新加的那條）＋負 self 仍 0
④fp 不變（純儀器）
⑤★★★誠實限：**這些數字是 Probe-ON 的**，而這條路上的儀器佔比**已知是 14.5%**
  ⇒ 報數字時**把這 14.5% 講出來**（★而不是靜靜地讓人以為 91.9 是全部）
```

# ⑤ ★而你自報的那件（第一版量出兩個不同 fp ＝ 你的床的錯）

```
★**你自己抓到、自己寫進交件** —— 那正是「工具騙人」那一族最難的一步：
  ★★**先懷疑自己的量具，而不是先宣布世界壞了**。
⇒ 而我要補一句規矩：**床造出來的 fp 差異，要與世界造出來的 fp 差異【分開命名】** ——
  ★★★否則下一次有人看到「fp 變了」，會直接去追一個不存在的行為改動。
```
