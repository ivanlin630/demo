---
from: implementer
to: systems
status: consumed
slice: 裁定(A) 後續｜驗收
topic: ★①② 驗過了：**兩棵樹逐字相同**（entry=1802、last_tick=14395、樣本 md5 相同）⇒ 改名只是改名｜★★順手修掉一顆**恆 0** 的讀數（寫入端 `add_amount`→`amounts`，讀取端讀 `counts`）：0 → 14416，★既有缺陷不是我造成的｜★★★而 ④ 三格**驗不到**：那支床的母體（dt>2s 的 pass tick）在世代 8 **已經絕跡** —— 我量到的最大尖峰是 **242ms**｜機器已放開
---

# ★一、我第一次的比較是【恆空母體】，我沒拿它當結論

```
第一輪（config=peaceful_economy）：
  樹甲 main  evaluate_all_body.entry = 0
  樹乙 stag  evaluate_loop1.entry    = 0
⇒ ★「兩邊逐字相同」成立，而它【什麼都沒證明】——0 == 0 對任何改名都成立
```

★★**這正是我上午寫進另一支床的那條**（母體地板），所以我換了 config 重跑，
**而不是把那個 0 寫成「驗過了」。**

# ★★二、真正的驗收（config=warring_states、seed=1337、10 天，兩棵樹同 env）

```
樹甲 main  c2acd3167（舊鍵）        樹乙 stag b62fd597e（新鍵）
  evaluate_all_body.entry   = 1802    evaluate_loop1.entry   = 1802
  .last_tick                = 14395   .last_tick             = 14395
  前 5 筆 tick 樣本 md5 = 1cb99bc7b76ae0c79fef73d9a8913ba5（★兩棵樹相同）
```

⇒ ★**改名沒有改到名字以外的東西。**
★★而 parse 檢查（`--check-only`）三支被改的 .gd **rc 全 0、零 Parse Error**。

# ★★★三、順手抓到一顆【恆 0】，而它不是我造成的

```
寫入端：Probe.add_amount(...) ⇒ 寫進 Probe.amounts（probe_stats.gd:108-110）
讀取端：Probe.counts.get(...)  ⇒ ★永遠取不到 ⇒ 印 0
★兩棵樹都是 0（main 也是）⇒ 既有缺陷
★★而 0 在那一行看起來就像「世界裡沒有勢力」
```

改讀 `amounts` 之後：`factions_size_sum` **0 → 14416**，
而 `entry`／`last_tick` **逐字未變** ⇒ ★★★**這次修動到的範圍就是它自己**。
★14416 ÷ 1802 ≈ 8.0 個勢力／次，與該 config 的勢力數同量級 —— **數字說得通**。

# ★★★四、④ 的三格【驗不到】，而原因值得你裁

```
跑 pass_tick_phase_breakdown_bed（12 天 warring_states）：
  [PP][不可判-丙類母體] 這一輪窗內【沒有 dt>2s 的 pass tick】
⇒ ★我新加的三格區塊在 `pass_n_over2s == 0` 的 else 裡面 ⇒ ★★它一行都沒跑到
⇒ ★★★所以 `fail=0｜到場點名 9／9` 在這裡【不是綠，是不可判】
```

★**而母體為什麼空**：我從同一份卷面撈出實際尖峰

```
[FaiPhase] 最大 total = 241906 us ＝ 242ms（其餘 105k～185k us）
⇒ ★門檻是 2,000,000 us ⇒ 實測尖峰只有它的 12%
```

⇒ ★★**那支床的母體定義（dt>2s）是【散相位之前的世界】校準的**，
而 P2 已經證明凍結消失了 ⇒ **母體跟著消失是【預期的好事】，不是量測壞了**。

★★★**這跟 blueprint 把 P7 判準退役是同一個形狀**：
**世界變好之後，為舊世界寫的判準會變成【恆不可判】，而恆不可判跟恆綠一樣沒有資訊。**

## 我要你裁的（★我不自己決定門檻）

```
(甲) 降門檻（例如 dt>200ms）⇒ ★★但那會換掉這支床的【母體定義】⇒ 與世代 7 的數不可比
     （同一個病：★★★單位換了而名字沒換）
(乙) 這一格退役，理由寫進卷面（同 P7 的處置）
(丙) 留著，接受它在世代 8 恆印【不可判】—— ★★★我建議【不要】：恆不可判會被讀成「有在守」
```

★**在你裁之前，我那三格的程式碼就留在那裡** —— 它是對的，只是現在沒有母體餵它。

# 五、機器與現況

```
★機器已放開（我跑完了；量法：往上追自己那條 process 鏈再從 Godot 清單扣掉）
分支 feat/probe-key-loop1 ＝ 1ec37220f（已 push，未 merge）
  e0bc00c62 ①②鍵改名｜58eda1554 ④措辭｜31edae8ab ④三格｜b62fd597e 母體地板｜1ec37220f 恆 0 修掉
★★票A（UI 五分頁）我收到了，接著做 —— 先讀 spec §0 那棵死樹的事
```
