---
from: systems
to: implementer
status: open
slice: 票5｜打聽兩層
topic: ★blueprint 把「打聽」拆成兩層（**開選單＝查詢免費**／**真去問人＝指令可有成本**）⇒ 你做的正好對上（`_action_gather_intel` 走查詢、`confirm_*` 進佇列），★**不用改 code**｜★★★**但我加了一格對照 P13b**：把 `confirm_gather_intel` 一起放進 P13 跑 —— ★若它 5 跑下來 world-fp **也不變**，那代表 blueprint 那個「指令層」今天**在 code 裡還沒有落點**，那是要回報給他的事實，不是你要順手補的東西
---

# 一、blueprint 的裁定（你不用改 code）

```
①開選單（列已知／可選對象）＝ 查詢、免費、不入佇列        ← ★你做的就是這個
②真去問人拿新資訊 ＝ 互動 ＝ 指令、走佇列、可有成本        ← ★★`confirm_*` 就是這一層
⇒ ★★★所以票5 定案的**不是**「打聽永遠免費」，是「**開選單**免費」
  —— 哪天要收費，是把「問人」那一層做成有成本的指令，**禁在查詢上加成本**
```

# ★★★二、而我加了它的對照（spec §5 新增 P13b）

```
★我讀了 `InquirySystem.resolve_inquiry` 前 30 行，**沒有看到寫入**
⇒ ★★而「沒找到」不等於「沒有」——**那正是 P13 存在的理由**
  ⇒ ★★★我不拿我自己剛立的規矩的反面來下結論。
⇒ 所以改成一格會紅的東西：**把 `confirm_gather_intel` 一起放進 P13 的跑法**
   預期：它【應該】改變 world-fp（它是指令）
   ★若它 5 跑下來 world-fp 也不變 ⇒ 代表【今天打聽整條線都不寫世界】
     ⇒ ★★blueprint 的兩層拆分今天在 code 裡**還沒有落點**
     ⇒ ★★★**回報給他，不要順手補** —— 補它是新票（「問人」要產生什麼效果＝WHAT）
```

★而這一格順便是 P13 的**陽性對照**：P13 要證「查詢不改世界」，
★★而一組全部都不改世界的端點**分不出「工具壞了」與「它們真的都純讀」**
⇒ ★★★P13b 提供了那個【應該會變】的樣本。若它也不變，第一個要懷疑的是**跑法**，不是結論。

# 三、另一行意圖帳也記了

「玩家走進一格立刻按互動，第一次看到空清單」＝ (乙) 的後果，**本票禁補丁**；
★blueprint 說實玩太傷就開新票「進格時世界自更新可互動對象」⇒ 不是你這張票的事。
