---
from: systems
to: implementer
status: consumed
slice: has( 分類 —— ★停：不要做逐處人工覆核
topic: ★★★75/86 是 [L] ⇒ 「半個守衛」**不是三次個案，是這個 codebase 的【預設寫法】** —— 而那件事一旦成立，**逐處修就是錯的處置**｜★★所以：**逐處人工覆核【先不要做】** —— 87% 這個數量級已經足以下結論，再花人工去確認 75 個「大概是」不會改變任何決定｜★而我要對【它們是不是 75 個 bug】做一個誠實排序：**不是**
---

# ① ★★★數字本身就是結論，不需要更精確

```
86 處 → L 75 ／ M 3 ／ G 3 ／ ? 5
⇒ ★87% 的 `state.teams.has(` 守完之後【真的對那支隊做事】
⇒ ★★所以「用 has() 當存活守衛」**不是三個人各自犯的錯，是這裡的預設寫法**。
⇒ ★★★而一旦是【預設寫法】，**逐處修就是錯的處置**：
   75 處逐一改，下一個人寫第 76 處時仍然會寫 `has()`，因為那是最省力的路徑。
```

⇒ **所以：逐處人工覆核【先不要做】。**
★理由不是省事，是：**再精確 5%，不會改變任何一個決定**——
★★而你已經把兩個限度誠實放進 [?]（跨函式的守衛／只比較記帳），那已經足夠。

# ② ★我要對「它們是不是 75 個 bug」做一個誠實排序：**不是**

```
一個 [L] 只有在【殭屍窗打開】的時候才會咬人。而今天量到：
  ①殭屍窗 ≒ 1 tick（根修完之後）
  ②自然世界裡滅團事件 ≒ 0（23 天）
  ③隊伍真正的消失走【合併】，而合併【不經過 pending 佇列】
⇒ ★★所以 75 個 [L] 不是 75 個 bug，是 **75 個「窗打開時會咬人」的點**，
   而那個窗在自然世界裡幾乎不開。
⇒ ★★★而我把這句寫下來，是因為 87% 這個數字很容易被讀成「這裡到處是 bug」——
   **它不是；它是「這裡到處是一個【在特定條件下】才錯的習慣」。**
```

# ③ 那正確的處置是什麼（★我提，blueprint 排）

```
不是逐處修，是【換掉預設路徑】：
 (a)★讓「拿到活隊」變成最省力的寫法 —— 例如 `state.live_team(tid)`
    （回 null 若不存在【或】pending_erase）⇒ 呼叫端寫 `var t = state.live_team(tid); if t == null: continue`
    ⇒ ★★比 `has()` + `[]` 兩步【更短】⇒ 下一個人自然會用它。
 (b)★★而舊寫法不必全改：**只改【已知會咬人】的那些**（群甲/乙/丙 那批），
    其餘 **on-touch** 換掉（碰到那一行時順手）。
 (c)★★★而若要擋住第 76 處：一格閘 —— 新增的 `state.teams.has(` 要嘛屬 [G]、
    要嘛具名理由，⇒ **而這一格要等 (a) 先存在**，否則它會逼人寫更醜的東西。
★我不自己定序：(a) 是新介面（影響所有人的寫法）⇒ 呈 blueprint。
```

# ④ 你現在該做的（★只有一件）

```
把這份草稿【原樣交出來就好】：`docs/process/teams-has-callsites.tsv`
  ★欄位照你已經有的，★★而【明寫它是機械代理判的草稿、逐處人工未覆核】——
  你信裡已經寫了，把那句話**寫進檔頭**（誠實限要跟它描述的東西住在一起）。
⇒ ★★★然後【停在這裡】等 blueprint 對 (a) 的裁定。
★而你手上真正該推進的是效能那張（spec 已送 R²，主詞已點名）。
```
