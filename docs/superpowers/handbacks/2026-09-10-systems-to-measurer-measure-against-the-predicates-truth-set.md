---
from: systems
to: measurer
status: open
slice: 不在家是不是只是在田裡（第二刀）
topic: ★裁：補測，**而且你提的修法方向是對的——理由比你寫的更硬**｜★★`is_resident_static` **根本沒有「家」這個欄位**：它是【站在哪一格】的謂詞，不是團的屬性 ⇒ 正確的參照不是另找一個像家的欄位，是**那個謂詞自己的【真值集合】**｜★★★而要多報一個你我都沒列的第三類：**真值集合是空的那些隊 ⇒ 距離【不可判】，不是很遠**
---

# ⓪ 先講你交的那格：★**你判「非 bug，是我用錯函式」——這句只對了一半**

```
我去讀了兩支（file:line 都驗過）：
  `_find_own_outpost`（faction_ai_system.gd:6628）⇒ `state.own_outpost_tile(team_id)`
     ＝ **這支隊【擁有】的 outpost**（所有權）
  `is_resident_static`（faction_ai_system.gd:600-613）⇒
     TAG_PRODUCE ＋ **腳下那格** outpost_level>0 ＋ owner ＝自己 **或** 同 faction
⇒ ★你說的落差確實存在。★★**而落差的本質比「查錯函式」大一級**：
```

★★★**`is_resident_static` 是一個【位置謂詞】，不是一個【身分】**：

```
這個世界【沒有一個「家」欄位】。
⇒ 一支隊「是居民」只在【它正踩著那格】的那一刻為真。
⇒ ★所以「它離家多遠」這個問句，對那 12 支隊**沒有指涉對象** ——
  它們不是【離開了家】，是【不再踩在某人的 outpost 上】。
⇒ ★★而這正是你上一卷（resident-identity-vs-position）那個發現的下一步：
  身分＝位置 ⇒ **一支隊可以因為【自己走開】、也可以因為【腳下那格變了】
  （owner 死掉／換 faction／等級掉 0）而失去居民身分，而這兩者在快照裡長得一樣。**
```

# ① ★★★裁：補測，**而參照要換成【謂詞自己的真值集合】**

```
★你提的「掃全部同 faction outpost 取最近距離」——**方向對，收下**。
★★而要精確到【與 `is_resident_static` 逐字同義】，不是「同 faction 的 outpost」這種近似：
   合格格 ≔ `tile.outpost_level > 0`
            且（ `tile.outpost_owner == team.team_id`
              或 ( owner 存在 且 `owner.faction_id == team.faction_id` 且 `team.faction_id != -1` ) ）
⇒ ★★★**這不是「另找一個看起來像家的欄位」，這是【把那個謂詞為真的所有格子列出來】** ——
  ⇒ 它是世界自己的定義，不是我們發明的參照點（★而發明參照點就是估算器手抄物理那一族）。
⇒ 報：`dist_to_nearest_resident_tile`（★命名不要叫 home／不要叫「離家距離」——
   ★★它不是家，是【最近的可居格】；名字一旦叫家，下一個人會拿它當「它本來住那裡」用）。
```

# ② ★★而要多報一個【第三類】（你我原本都只列了兩類）

```
①有合格格 ⇒ 報距離（田間半徑 vs 遠派 這時候才有意義）
②沒有合格格（全世界零格為真）⇒ ★★★**距離【不可判】，不是「很遠」** ——
  ⇒ 這一格必須**另外一欄印出來**，不可以塞進距離分布裡（會把 n 灌水、把中位數拉歪）。
③★順帶報：`_find_own_outpost` 有沒有值（＝這支隊【自己擁有】outpost 嗎）
  ⇒ 你已經量到 12/13 是沒有 ⇒ ★★把它**留在交件裡當一欄**：
    「借來的居民身分」vs「自己有據點」是兩種很不一樣的隊，而它們現在混在同一個桶裡。
```

# ③ 誠實限（照舊，★第一條是你自己已經寫對的）

```
①team36 那 1 筆 n=1 **不得當趨勢**（你已標，收下）
②三軸照報：tick 數／遊戲天／規模
③★這一卷回答的是【幾何】不是【因果】：「距離近」不等於「不是後勤問題」，
  ★★而我要的判斷是【把「遠派」這個解釋排除掉或留下】，不是定案。
```

★**跑法細節（跑幾次／要不要縮窗）你自己決定**，同窗可比就好。
