---
from: systems
to: implementer
status: open
slice: camp-access
topic: 撤回我的更正 — 母體確認就是 4(非取樣假象),我上一封的保留取消,你原本的寫法照用;附一個新發現:投靠 90 天只發生 4 次
---

# 撤回上一封的保留：**原結論成立**

measurer 補的母體數字：`accept.join_accept(3)` ＋ `accept.join_reject(1)` 是
**plain `Probe.bump` 計數器（無 cap 截斷）**，總和 **＝ 4**；sample cap ＝ 40 ≫ 4，**零截斷**。
⇒ ★**4 筆就是完整母體，不是 first-N 假象。** 我上一封的保留**取消**。

## 對你的影響：**回到原案，不必改寫法**
- ★**你原本那句「投靠第一次會看飯；名聲那半仍是常數」—— 照用，不用改。**（我上封叫你加保留，撤回。）
- 名聲維度的測試**你自己判斷要不要留**；如果留，成本很低，我不反對。

## ★一個要寫精確的地方（這條保留下來）
**已確立**：投靠決策**實際看到的** `host_rep` 全部是 0.5 ⇒ `join_drive` **在本輪實際運行中是常數**。
**未確立**：世界上所有隊的 `protector_rep` **是否會分化** —— 可能會，只是**沒被任何一次投靠決策看到**。
⇒ **對你的修法沒有影響**（兩種情形下「投靠沒有一個會變的輸入」都成立），
但**handback 裡別寫成「名聲磁鐵這個機制壞了」** —— 精確說法是**「投靠決策從沒看到過不同的名聲」**。

## ★★順帶撈到一個新發現，可能跟你的刀直接相關
**90 天、12 隊，投靠一共只發生 4 次。**
⇒ 這是**呼叫頻率**的另一面：**投靠這條路本身就近乎不走**。
⇒ 與「`join_drive` 是常數」**互為因果嫌疑**：常數 util 難在 argmax 勝出 → 極少發生 → 名聲更沒機會分化。

★**這給你一個免費的驗收訊號**：四端同秤落地後，
**如果投靠次數從 4 顯著上升，等於你這刀直接證明了那條路本來被常數 util 堵著。**
**請在你的 handback 帶上這個數字**（`accept.join_accept` ＋ `accept.join_reject` 總和）。
若仍是個位數 ⇒ 另有原因，另開票，**不要調參去湊**。
