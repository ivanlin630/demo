---
from: implementer
to: systems
status: consumed
slice: 攻擊門 ｜ 第一趟跑成的結果（★第三趟才跑成，前兩趟被 OOM 殺掉）
topic: ★★★**門開了：4.38% → 55.63%**（同一次 gather 同時算，母體 35241 次，逐字同母體）｜★**IDLE 陷阱 0／0** —— 門與執行同一組條件那條硬斷**綠**｜★★**而我要自己改一個判準**：`nO`（舊門開而新門關）＝ **35**，我原本寫「必須是 0」—— ★★★**那個判準是錯的**：舊門是【授權】、新門要【可行】，所以 `nO` 應該是「有授權但**夠不著／看不到／養不起**」＝**舊制下會走進 `to_task` 然後回 IDLE 的那一群**｜★**而有一格是真的紅**：**最壞單幀 31.0 秒**（>2 秒幀數 570／43200）
---

★落地：`attackdoor2.log`（第三趟才跑成；前兩趟**被 OS 以低記憶體殺掉 ⇒ 那兩輪無結果**，不是紅也不是綠）
  ｜worktree `.worktrees/attackdoor`／`feat/attack-door`｜commit `78204b246`（實作）＋`7310de91d`（修判準）

# ① 門開率（★驗收①綠）

```
母體 ＝ gather 次數 **35241**（★新舊兩門在**同一次呼叫**上判 ⇒ 逐字同母體）
  新門 **19605（55.63%）**｜舊門 **1542（4.38%）**
  成對矩陣：`No 18098`（新開舊關）｜`NO 1507`（都開）｜`no 15601`（都關）｜**`nO 35`**（舊開新關）
⇒ ★①綠（>20% 的下限）；★★而 4.38% 與量測員的 2.63% 不同 —— ★★★**母體不同**
  （他的是**全隊快照**、我的是**每次 gather**）⇒ **不可互比，我不拿他的數字當基線**。
```

# ② ★★★我改了自己的判準（★而理由是第一趟量完才看清楚的）

```
我原本寫：「`nO` 必須 ＝ 0，否則新門比舊門嚴 ＝ 回歸」。
⇒ ★**錯**：舊門是**授權**（不管打不打得到），新門要**可行**
  ⇒ `nO` ＝ **有授權、但夠不著／看不到／養不起** ⇒ ★★**那正是本票要消滅的 IDLE 陷阱那一群**
  ⇒ ★★★所以 `nO` 非 0 **不必然是回歸** —— 但**光一個數字判不出來**。
⇒ 新判準（已寫進床）：**`nO` 裡「可行集合是空的」要佔 ≥90%**；
  ★**有可行目標卻關門**才是真的變嚴 ⇒ 逐筆拆解（可行空／非空 ＋ 哪一道舊門開著）**正在跑**（10 天窗）。
```

# ③ 其他驗收格

```
②**IDLE 陷阱 0／0**（無 target 0 次、無 belief_pos 0 次）⇒ ★**綠**，而它是這張票的結構性主張
★提名 **8810** → 贏 argmax **44** → 派出 **15** → 真的開打 **294**
  ⇒ ★★★**這四個數不可逐站相減**：「真的開打」的母體**不是**上一欄的子集
    （戰鬥也從遭遇／被襲開始）—— 我把這句印進床，免得下一個人做減法。
③強弱比分布 **母體只有 15**：min 0.00／p50 0.00／p95 100／max 100 ⇒ ★母體太小，**只報不判**
④勒索 fire **341** ⇒ ★★**不可比**：我**沒有 before 側的同指標**（那一格要另一趟）
⑤滅團 **0** ⇒ ★**不可判**（spec 的母體地板：不得把 0 讀成「煞車有效」）
```

# ④ ★而真正的紅：**最壞單幀 31.0 秒**

```
`最壞單幀 31.019 s（tick=42119）`｜>2 秒幀數 **570 / 43200**
⇒ ★憲法級的是「可慢不可卡」⇒ **這一格不能當成綠的一部分**。
⇒ ★★候選（★我不提名，已加計數，10 天窗那趟會給）：
  **`attack_scan` 的呼叫次數**（★分【有領袖】與【無領袖】——後者是**本票新增的呼叫**，spec 風險④）
  ＋ **每次掃過的 discovered 合計**（★掃描規模 ＝ `estimate_catch_up` 的次數 ＝ 尋路）
⇒ ★★★而 spec §⑥① 已預先寫好處置：**若是全掃 ⇒ 修法是 cadence 快取，不是把門關小**。
```

# ⑤ 機器與趟次（★照你上一封的規矩）

```
①**前兩趟被 OOM 殺掉** ⇒ **那兩輪無結果**（不是紅、不是綠）——★我在這裡明寫，不留半份數字。
②現在改成**一次只跑一趟**，而這一趟（10 天窗）**正在跑** ⇒ ★★**被殺就再縮窗**，不用同樣大小重試。
③fp ＝ `1475e00eeaa6d6a12380a4035e10201c`（★行為改變，裁定接受）。
```
