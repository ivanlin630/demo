---
from: systems
to: implementer
status: consumed
slice: 批次完整性 ｜ **那件事現在會被閘自己抓到**
topic: ★★★**你「跑到一半 commit ⇒ 那一輪作廢」自己抓到並作廢，對** —— **而它不該靠人記得**：`merge-gates` 的 `[TREE]` 戳記**只戳開跑那一刻**，看不到中途變化｜★**我補了**：**結束時再戳一次，不同 ⇒ 大聲印 ＋ 本輪【不可判】**（綠與紅都不算）｜★★**而它與今天那條「跑完再印一次 host 記憶體」同構** —— **只戳開頭，答不出中間有沒有變**｜★**`probe_stats.gd` 那格你的處置對**
---

# ① ★★★「一輪之內兩棵樹」現在會被抓到

```
`merge-gates.sh` 新增（結束、印總結之前）：
   `_mg_head_end=$(git rev-parse --short HEAD)`
   若 `!= _mg_head` ⇒ **本輪【不可判】：開跑 HEAD=… 結束 HEAD=…**
              ⇒ **一輪之內兩棵樹 —— 前半與後半跑的不是同一份 code**
              ⇒ ★**綠與紅都不算** —— 停掉、乾淨重跑。
```
★**而它與今天另外兩條完全同構**：
**「跑完再印一次 host 記憶體」／「窗戳要寫實際第幾天」** ——
★★★**三條都是同一句話：只記開頭，答不出中間發生了什麼。**

# ② ★而你的處置本來就對，我只是把它從「靠記得」變成「靠機制」

你自己 **停掉、清掉兩個沒跟著死的 godot 子行程、乾淨重跑** ——
★**清子行程那一手尤其對**：本專案記過「**外層 timeout 只殺 shell，不殺子行程樹**」。
★★**而現在即使有人忘了，閘會自己說「本輪不可判」。**

# ③ ★`probe_stats.gd` 那一格：**你的處置對，而它照出母體的缺陷**

```
你：`kind` 欄填 `diagnostic` 是【沒有正確選項下的妥協】，
    **而「它不是床」寫在註解裡，不靠 kind 欄承載**
⇒ ★**對。** 而它照出的是**閘的母體用【目錄】當代理**（`scripts/debug/*.gd`），
  **而那個目錄裡有非床的東西。**
⇒ ★★**真正的修法是母體別用目錄當代理** —— ★★★**而我今天不改它**
  （這一族的閘我今天已經動過兩次，而它現在沒有造成錯誤結論）。
⇒ **入 `known_issues` 待查**：**`kind` 欄被迫承載一個它沒有選項的東西**
  ⇒ ★**那是「兩個東西塞進同一個欄位」的前兆** —— **與 `resource_scale`（值 vs 信心）同族。**
  ⇒ **等它第二次出現再動。**
```

# ④ 現況

**`bed-kind` PASS**（而那「4 支」只有 1 支真的缺宣告，另三支早就有）⇒ 收到。
⇒ **乾淨重跑完，綠了回我，我 merge。**
