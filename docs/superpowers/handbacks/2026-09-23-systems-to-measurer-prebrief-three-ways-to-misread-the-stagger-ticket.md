---
from: systems
to: measurer
status: consumed
slice: 裁定(A)——把每小時那一趟 pass 按隊錯開相位
topic: ★預先告知（★還沒到你動手，code 還在寫）：這張票的驗收有三個【已知會被誤讀成紅】的地方，全部已經量過或已經裁定｜★★其中兩個是 backlog 裡預先寫下回訪條件、今天到期的｜★★★不要重量那兩組數字，它們在檔裡
---

# 〇、為什麼現在寄而不是等派工

★**免得你量到之後才被告知「那個是已知的」** —— 那等於浪費你一輪，
而且更糟：**你會先寫下一個歸因，然後我才來推翻它**。

```
現況：spec 已定稿（R² CLEAN，改了五處之後 reviewer 獨立重核仍 CLEAN）
      implementer 正在寫 code｜★你這一輪還沒開始，我只是先把讀數規則放到你手上
```

# ★一、第一個遊戲日每隊會【少一次】pass —— 預期，不是紅

```
機制：每隊第一次排程走 `if X == 0: X = CadenceStagger.next_tick(...)`
     ⇒ 第一次被推遲 [c/2, 2c) ＝ [30, 120) tick（★一生只發生一次）
⇒ P3「每隊每日 pass 次數 ＝ 24」在 day-1 會是 23–24
```

★**本票接受它**（改成「第一次當場跑」會把所有隊的第一次擠在同一顆 tick，
那正是這張票要消滅的形狀）。
★★**七格裡只有 P3 受影響**（reviewer 逐格核過）：其餘六格是聚合／單點／方向性判準，
30–120 tick 的一次延遲在 17280 tick 的窗裡淹沒。
★★★**誠實限**：那句「只有 P3」只涵蓋**本票自己的七格** ——
沒有人查過 repo 裡其他既有的床有沒有隱含「pass 從 tick 1 就開始」的假設。
⇒ 若有別的床在這票之後莫名其妙紅一格，**這裡是第一個該查的地方**。

# ★★二、相位分佈【不均勻】是已知的，而且已經量過 —— ★不要重量，也不要拿它判紅

```
床：scripts/debug/cadence_stagger_uniformity_bed.gd（秒級、純函式、自帶陽性對照）
teams=115 cadence=60 cycles=200 ⇒ 期望每格 1.92
  max 格：median=6  p90=7  p99=8  最壞=9   ｜ 空格：median=9  最壞=13
  ★超過「期望 3 倍(=6)」的 cycle ＝ 39／200（19.5%）⇒ 常態貼著線，不是離群
陽性對照：offset 恆 0 ⇒ max=115、空格=59
```

★★★**驗收表裡有一個「3 倍」，而它【不是】上面這個**：

```
已知不均（★不是紅）：每個 cycle 的【桶佔用】——最壞一格 9 隊 vs 期望 1.92
P3 的判準（★是紅）：逐隊的【clamp 觸發率】≤ 母體平均 × 3
                    問的是「有沒有系統性偏袒【同一批】隊」，不是「分佈均不均」
```

★**搞混的後果**：拿一個已知且已接受的事實去判本票紅。
★★本票的主張本來就不需要均勻：**最壞一格 9 隊 vs 今天 115 隊，峰值降一個數量級就夠**。

# ★★★三、指紋那一格有兩跑，**先跑樁關著那一跑**

```
樁關掉（pass_stagger_enabled=false）⇒ 指紋必須與世代 7【逐字相同】
樁開著                              ⇒ 指紋改變＝【預期紅】⇒ 世代 8
```

★**先跑樁關著那一跑的理由**：它一次就把
「重構寫壞了」跟「錯開改變了世界」**分成兩個可以各自判的問題**。
樁關著紅 ⇒ 去查重構，**不要去查錯開**；樁關著綠、開著紅 ⇒ 那是預期中的世代 8。

★★**而這一格的強度要標明**（別替我寫成「沒有改行為」）：

```
TeamData    尺內 30 欄 ／ ★沒看到被讀 97 欄
HexTileData ★排除 34 欄，含 apothecary_level／mint_level 這種會影響產出的設施等級
⇒ 正確措辭：「★fp 覆蓋範圍內無變化」
⇒ 它仍是【單腿】：另兩條腿是 P7（吞吐 ±5%）與 P3（每隊次數與間距）
```

# 四、權威在哪

```
spec  docs/superpowers/specs/2026-09-23-stagger-the-hourly-pass-HOW.md
      §6 是驗收表本體（P1–P7，含儀器欄與「假設為假時這格長什麼樣」欄）
★★驗收數字是 blueprint 在資料之前寫死的，我只補了「用什麼量」⇒ 有歧義回我，不要自己挑一個
```

★**這封不需要你現在做任何事**。等 implementer 落地、我 merge 完，才會有正式派工。
