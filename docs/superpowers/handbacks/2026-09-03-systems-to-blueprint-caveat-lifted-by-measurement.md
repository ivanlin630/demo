---
from: systems
to: blueprint
status: open
slice: 你下的那條降級處置 —— ★已解除，而且是【量掉的】不是說服掉的
topic: ★★★跨輪命中 = 72 ⇒ 母體不是空的,共享快取確實在作用;而【清了快取】world sig 仍逐位元相同 ⇒ 理由升級:不是「快取沒作用所以看不出差別」,是「快取作用了 72 次,拿掉之後世界仍逐位元相同」;★★方法漂亮到值得記:量跨輪共享【不必記鍵】——第二輪跑兩次(不清/先清)取差額;★而那個 72 順便成為 _path_cache 修法驗收②的修前基準
---

# ★①數字
```
第一輪（全新快取）  hit=195 miss=127
第二輪（不清快取）  hit=267 miss= 55   ←★跨輪命中 = 72
第二輪（先清快取）  hit=195 miss=127   ←★★與第一輪【逐數相同】⇒ 決定性沒問題
★★★而 B 組（清了快取）world sig 仍然逐位元相同
```
⇒ **我先寫死的兩條判讀都用上了**：**命中 >0 ⇒ 母體非空**；**清了仍相同 ⇒ 遮蔽假說死。**

# ★★②所以「折損」那句撤掉，而理由比原本強
> ★**不是**「共享快取沒作用，所以看不出差別」（那是【證不出來】）
> ★★**是**「共享快取**確實作用了 72 次**，而把它拿掉之後世界**仍然逐位元相同**」（這是【證出來了】）
★★★**兩者在文字上很像，在證據強度上差一整級** —— **而分開它們的就是那個 72。**
`known_issues` 已改，**殘留只剩一格排定的補證**（`observability_path_test` 本身在 `_path_cache` 修完後重跑，**那本來就要做**）。

# ★★★③方法值得記（我會寫進量測協議）
> **要量「跨輪共享了多少」，不必記鍵、不必逐筆比對** ——
> **把第二輪跑兩次（一次不清、一次先清），差額就是跨輪命中。**
★**而它同時給出決定性檢查**：**「先清的第二輪」與「第一輪」逐數相同** ⇒ 沒有別的東西在漂。

# ④副產物
**72 ＝ `_path_cache` 修法驗收②的修前基準**（同 seed／300 tick／warring）⇒ **修完該差額應為 0。**
★**這是今天第二次「診斷用的量測順手變成修法的驗收基準」** —— 便宜且不會 rot（它是數字不是敘述）。
