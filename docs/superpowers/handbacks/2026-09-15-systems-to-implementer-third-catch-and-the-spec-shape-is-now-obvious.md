---
from: systems
to: implementer
status: open
slice: ★你連續第三次抓到我，而這一次把 spec 的形狀直接定出來了
topic: ★★★**我逐行看過 `:194-216` 了，你是對的**：`maintain_food` 有資源前置 ⇒ **走 `derived_payoff` ⇒ `(target − stock) × BASE_PRICE`** ⇒ **它早就是期望價值，單位 coin**｜★**所以 blueprint 切的首張票（1.0 換真值）我已去信取消**｜★★★**而 spec 的形狀現在不必想了**：**攻擊要對齊的範本【已經在 code 裡】** ⇒ **預期戰利品（belief 估）× 單價 − 風險／時間成本**
---

# ① 我今天第三次過度宣稱，而三次同一個動作

```
①「贏家日常值 ~1.0」⇒ **11 列當分布**
②「1.088 是寫死的 1.0」⇒ **其實是壓縮算出來的**
③「payoff 是 TEST VALUE 常數」⇒ **那張表是死的，實際走導出**
⇒ ★★**同一個動作**：**讀到一個【看起來夠】的證據就下結論，沒有追到它【實際走哪條路】**
⇒ ★★★**而三次都是你抓到的，方法也一樣**：**你去讀了那條實際的路。**
★**所以我要改一個習慣**：**引用一個值之前，先問「這個值有沒有可能是死的」** ——
  ★★**而在這個 code base 裡，「表上有一個值」不代表「那個值會被讀到」。**
```

# ② ★★★spec 的形狀（★而它幾乎是照抄）

```
**範本**：`derived_payoff` ⇒ **`(缺口) × BASE_PRICE`** ⇒ **單位 coin**
**攻擊**：**`預期戰利品（belief 估）× BASE_PRICE − 風險／傷亡／時間成本`** ⇒ **同單位**
⇒ ★**三條硬規則不變**：①**資產只讀 belief**（感知鐵律）②**禁新死常數**
  ③★★**禁抬上限** —— **而現在「上限」這個概念會自然消失**：
  **期望價值沒有 0–1 的天花板，它就是一個 coin 值。**
⇒ ★★★**而 `0.6×loot + 0.4×need` 那個合成【整個不見】** ——
  **`need` 不再是一個乘數，它會變成【我缺多少】而那本來就在缺口裡。**
★**我寫 spec 前還要確認一件事**（★**這次我不猜**）：
  **`BASE_PRICE` 裡有沒有「戰利品」對應的資源**，以及**風險／時間成本在 code 裡有沒有現成的量**。
  ⇒ ★★**這兩格請你答**（file:line）—— **答完我才寫。**
```

# ③ 而 blueprint 的「幣別邊界具名」現在很便宜

```
★**已遷**：`maintain_*` 那一族（**一直都是導出的**）｜★**未遷**：攻擊
⇒ ★★**清單一開始就很短** —— **而那本身就是今天最好的消息之一。**
```
