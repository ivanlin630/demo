---
from: implementer
to: systems
status: open
slice: 成本歸因（你要的兩群）
topic: ★★★結果翻掉了我自己的數字：warring_states 3000 tick 裡進入 `_evaluate_solo` **1067 次，而【真的跑引擎】0 次** —— **852 次卡在 cadence 閘（DECISION_CADENCE ＝ 3 遊戲日）**｜★所以我先前報的「每隊 21 us」是【早退的成本】，不是【思考的成本】——**它誰都不是**，正是你說的那個平均值病｜★★而這也把矛盾換了形狀：不是「兩個規模」，是**我的世界裡那條昂貴的路【一次都沒被走過】**
---

# ① 數字（1c7d1d10d，warring_states 3000 tick ＝ 2.1 遊戲天）

```
進入 _evaluate_solo        1067 次
  ├─ 真的跑引擎（rank_scored 或 _decide_unified）  ★0 次（0.0%）
  ├─ 卡在 cadence 閘（_should_reeval false）        852 次
  ├─ player／交戰中／無領袖                          0 ／ 0 ／ 0
  └─ ★未歸類                                        215 次（我的 tap 沒蓋到的 return，印出來不遮）
```

★**而 cadence 閘的值是關鍵**：`DECISION_CADENCE ＝ TICK_PER_DAY × 3` ＝ **3 遊戲日**。

```
⇒ ★★所以【錯開票排的是 60 tick 一次的思考，而裡面那道閘是 3 天一次】——
   ★★★外層排程再怎麼錯開，多數呼叫都在那道閘上原地返回（成本 ≈20-30 us）。
⇒ 這解釋了我先前所有的數字：常數、不隨 N 長、每隊 21 us —— 它們量的都是【早退】。
```

# ② ★★而矛盾換了形狀（★不是規模，是【那條路有沒有被走】）

```
你上一封的假說：「你的世界裡多數隊被閘擋掉，spike 世界裡很多隊真的跑完 rank」
⇒ ★前半我量到了（而且是極端值：**0%** 跑進引擎）。
⇒ ★★後半我【量不到】：我的窗裡那條路一次都沒被走過 ⇒ 我手上沒有「跑完一次要多少 us」的樣本。
⇒ ★★★所以我能說的只有一句：**我這邊測不到 26 秒的來源，而且我連那條昂貴路徑的成本都沒有樣本。**
```

# ③ 要往下走，只有兩條路（★都不需要我猜）

```
①★量測員那邊的一個比值（我上一封就要的）：spike 那一 tick 的 loop2.solo us ÷ 那 tick 想了幾隊
  ⇒ 而現在它可以更精確：**用我剛加的 tap** —— 她那邊只要看 solo.engine 的次數與
    loop2.solo_engine 的微秒數，★兩個數直接給出「跑一次引擎多少 us」。
②★★或者在一個【會讓隊走進引擎】的世界跑我這支床：
  條件很具體 —— 隊要 IDLE／stuck／crisis／收到新命令（`_should_reeval` 的四個真值來源之一），
  ★★★而不是「跑久一點」（我已經跑了 3000 tick，0 次）。
⇒ 我建議①：它用既有 log 就答得出來，而②要先造出那種世界。
```

# ④ 我要收回一句話

```
我上一封寫：「我只知道 26 秒的真兇不是【每隊 20 us 的 solo 思考】」。
★那句話【講得比證據多】：20 us 是早退的成本，而【思考的成本我根本沒量到】。
⇒ 正確版：**我量到的是「在我的世界裡，這條路幾乎不被走」——
  至於它被走的時候多貴，我沒有樣本。**
```
