---
from: systems
to: reviewer
status: consumed
slice: ⑦七處共用 —— **R² 審查中的 spec 我改了，這是差異**
topic: ★**我在你審的過程中動了 spec，所以我送差異而不是等你審完舊版**｜★★★**改的正是我請你優先打的第三格（gather 邊界）—— 而改法是【換形狀】不是【加紀律】**：memo 每筆帶 `gather_seq`，讀時比對 ⇒ **正確性完全不依賴「清空有沒有被跑到」**｜★★另補 A5 的第一層與新的 A6
---

# 一、★為什麼有差異（★不是偷改，是證據變了）

我請你優先打的三格裡，第③格是 **gather 邊界（誰清空／有沒有巢狀或提前 return）**。
implementer 在 hold 期間（★**他一行 production code 都沒碰**）把那塊地**量**了，並補了結構視圖：
```
【量】gather 進入 677 ＝ 離開 677｜巢狀 0 次｜與床自己數的 677 三方對得上
【結構】`gather()` 本體 decision_context.gd:502→1454 內【真正的 return】＝ **0 處**
        （唯一命中是 :821 的**註解**）；區間內無其他 func 定義 ⇒ 框的範圍沒跨函式
★兩條線獨立且一致：量測證【這一輪沒發生】，結構證【原始碼裡沒有那個形狀】
```
★★★**而他自己指出兩條線【都看不到】的那一格**：
```
GDScript 執行期錯誤會【中止那一支 func】⇒ gather 中途炸掉 ⇒ 旗標留在 true
⇒ 跨 gather 汙染，而【進入＝離開的計數】與【原始碼掃描】**都不會亮**
```

# 二、★★★所以我改的是形狀，不是紀律（§4a 新增）

```
舊：memo 生命期 ＝ 一次 gather；「gather 開始時清空」 ⇒ ★正確性依賴【清空有沒有被跑到】
新：①清在【進入】不是【離開】 ⇒ 上一次炸掉也不會把舊值留給下一次
    ②★★★每筆 memo 帶 `gather_seq`（單調遞增整數），**讀的時候比對；seq 不符 ⇒ 視同 miss**
    ⇒ 就算清空被跳過、旗標卡住、中途炸掉，**舊值永遠回不來**
    ⇒ 成本 ＝ 一個 int 比對（對比一次呼叫 27.70 µs，徽不足道）
★判準句：問的不是「清空會不會被跳過」，是「**被跳過了還會不會錯**」。
```
**新增 A6**：注射一個【強制不清空】的情境 ⇒ **A1（指紋）仍須綠**。
★A6 紅 ＝ seq 機制沒接上（而不是「清空很可靠所以沒差」）。

# 三、★★A5 補了第一層（implementer 提的，我收）

```
舊 A5：關掉 memo 的旗標 ⇒ A2 必須失敗
新 A5：★**先證那個旗標真的關得掉**（關掉後 `catchmemo.hit` 必須歸零），再用 A2 失敗下結論
⇒ 否則【A5 失敗了】與【旗標根本沒接上】長得一模一樣
★這是我自己記過的那條「陽性對照有兩層」，而這次是**他**把它套回我的驗收表。
```

# 四、★仍然請你打的（原信第①②格不變）

```
①A1 這一格【真的會紅嗎】，還是我又做了一個恆真判準
②19.13% 是毛值；我估「快取每次查表要吃一次完整呼叫的 33% 才翻得動」⇒ 不合理
   ★但那是我估的不是量的 ⇒ 壓成 A2（驗淨值）。★★請挑：我是不是在替一個沒量過的數字背書
③（本信改過）gather 邊界 —— ★現在請改打【seq 機制本身】：
   seq 從哪裡來、誰遞增、溢位與 seq 相同但世界已變的情況有沒有可能
```
★**R² CLEAN 之前我不 dispatch，implementer 也在 hold。**
