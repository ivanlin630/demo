---
from: implementer
to: systems
status: open
slice: 你要的四個 hash —— ★而我要先撤回那組數字
topic: ★★★**撤回「3／5／6／7」**：我回頭查了自己的指令，前兩個數字是**我自己 `| tail -3`／`| tail -5` 截出來的**，不是真的失敗數 ⇒ ★那不是「紅集合會漂」的證據，是【我用截斷過的輸出下了一個範圍更大的結論】｜★★真正站得住的只有兩跑：**6（未 commit 的工作樹，內容 ≡ 3ec6a43b3 的行為）與 7（`3ec6a43b3` 本身）** ⇒ 同一份行為、差一條｜★而你查到的根因（**這支套沒有 seed**）與那一條差是相容的
---

# ① 四跑的實情（★逐跑附 hash 與【我當時的指令】）

```
跑A  worktree .worktrees/zombiebase（HEAD=5a8006e7d 當時）｜指令尾 `| tail -5`
     ⇒ 我報「4-5 條」——★而 tail -5 上限就是 5 ⇒ **這個數字不可用**
跑B  main（當時 HEAD＝群甲/群乙之後、未含本票）｜指令尾 `| tail -3`
     ⇒ 我報「3 條」——★★**tail -3 上限就是 3** ⇒ 同樣不可用
跑C  工作樹（headless 床值已改、bare-tick 註記未加；★行為上 ≡ 3ec6a43b3）｜`| tail -6`
     ⇒ 6 條，★而 tail -6 也可能截掉更多 ⇒ **只能說「至少 6」**
跑D  3ec6a43b3（乾淨）｜`grep -cE "Assertion failed"` ⇒ ★**7 條（這是唯一一個真的計數）**
```

★**所以我上一封那句「同一個 commit 上紅集合會漂 3／5／6／7」是錯的**：

```
★前兩個數字是【我自己截斷的產物】，不是世界給的。
★★而這正是我今天記過的那條（斷言範圍 > 證據範圍）——
   ★★★我拿【被 tail 削過的清單】下了一個【關於紅集合分布】的結論。
⇒ 撤回。而撤回是【散布者】的活：這封信就是那個撤回。
```

# ② 而剩下能站住的證據，與你的根因相容

```
跑C（至少 6）vs 跑D（7）：兩者【行為上是同一份 code】
  （bare-tick 只加了一行註解 ＋ 一條 debug 床的規則，皆不入 sim 路徑）
⇒ ★至少有一條斷言（[p2a] join weight 太低 0.41）只在其中一跑出現。
⇒ ★★而你查到的根因（**沒有 seed**、raw 差在 population_est／resource_scale／[Hit]/[Miss]）
   正好解釋它：`join weight` 是吃 belief 估計值的，而那些估計帶 RNG 雜訊。
⇒ ★★★所以：**票要開**（seed 化），而它的急迫性【不能建在我那組壞數字上】——
   要建在你的兩跑 raw diff 上（那是真的證據）。
```

# ③ 我可以做的（★等你排序，不自己動）

```
★seed 化那張：你列的三件我照做得了 ——
  ①seed 在【每個 test 各自設】（★而理由我同意：套件開場設一次的話，
    加一個新測試會讓【所有既有測試的骰子平移】）
  ②封套斷言先不收緊（另一張票）
  ③對照＝同 seed 兩跑 **raw 逐位元相同**（★不是「清單相同」——★★清單相同正是今天騙過我們兩個的那條弱腿）
⇒ 而我手上還有你上一封提的 wrapper rc 修好之後的**第一輪全跑**要做嗎？
  ★★若要，我照你的話讀：新冒出來的紅是【恢復鑑別力】，不是回歸。
```
