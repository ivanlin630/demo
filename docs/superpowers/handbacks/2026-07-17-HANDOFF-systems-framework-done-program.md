---
from: systems
to: systems
status: consumed
topic: "[★交接·systems session 續] 框架做好程式(真統一+零殘留+可擴充三位一體驗收,defer behavior)。本session merged:生產框架/Arc1 need oracle/constitution_gate v2+de-patch軌2。現態:baseline 91(37 gate-ok/54待),seam#1控制流收斂spec ready-R²(建議異質),fast-follow multi-seed在飛。下步:seam#1 R²→impl/54待triage/stream②seam#2-3/stream③情緒接線。無卡點(GO自主驅動)"
---

# ★交接：systems session（框架做好程式）

## 現行主線：框架「做好」= 真統一+零殘留+可擴充 三位一體驗收（用戶定，defer behavior 到框架驗收過）
- 用戶標準：**真統一**（每決策真只走一條:零手派路由/散落入口/繞道/近似重複）+**零殘留閘**（一個殘留閘→垃圾結果,constitution_gate 綠證）+**可擴充**（加東西動幾處）。**兩健全不變量**（下游零決策/零干擾）機器證組合。
- 主線非「統一散亂 oracle」（R① 多次證稽核 over-count、憲法溶解 arc 早統一大半）——**是殲滅殘留閘+真統一控制流**。

## 本 session 已交付（全 merged main）
1. **生產框架**（供給牆破:has_facility 恆1→31%/成品 18x）。
2. **Arc1 統一 need oracle**（need-quantity 單一源:need_keep+demand 兩量,生產/商業/facility 共讀;merge e483f85c）。
3. **constitution_gate v2 + de-patch 軌2 值閘**（merge 08d3a39d）。

## 現態（框架做好 stream①）
- **baseline**（`.worktrees/constitution-gate-strengthen`→已 merged main `scripts/debug/constitution_baseline_v2.txt`）：**91 閘,37 gate-ok legit 標,54 未標待**。綠=54→0。
- **軌2 值閘 DONE**（閘1 _threat_recent/閘5 tribute FLEE/閘7 孤兒刪/try_proactive 陡化;閘2a/3/4/6 gate-ok）。
- **fast-follow multi-seed 在飛**（measurer,軌2 分化 confirm:try_proactive 低慎重/tribute 修測法/militancy 綁軍事設施 thin）。

## 在飛 + 下步（無卡點,GO 自主驅動）
1. **★seam#1 控制流收斂 spec ready→R²**（`specs/2026-07-17-seam1-control-flow-convergence.md`,建議升異質框外審）：收斂 rank_survival/threat/ambient 非統一 filtered 路+手派路由→全隊走 rank_scored 一條路 + applicable/to_task 折 REGISTRY（真統一+擴充）。**語意合併 survival軟/threat硬 = measure 風險**。→ 我用戶「等下交接」暫停 R² dispatch,**續:dispatch R² seam#1**。
2. **fast-follow measure** 回→判分化收 or 陡化 tune。
3. **54 待 triage/de-patch**（含軌1 route/dispatch_entry 控制流 = seam#1 收;剩 legit 標 + threshold/early_return 逐 code triage）。
4. **stream② seam#2/#3**（`_facility_deficit` 資料驅動 / `sim_runner` 系統 registry）。**stream③ 情緒接線**（person.goals/memory dormant→決策輸入維度）。

## ★可複用原則（全入 invariants,續 de-patch 照用）
- **兩健全不變量**（下游零決策/零干擾+機器證組合）。**RNG 判準 3 案**（純骰 de-patch/世界 outcome legit/人格加權機率 legit-IF-陡）。**單一源 oracle 判準**（oracle 外各算=硬遷/oracle 內 flat 值=軟債 deferred）。**源統一硬-值推導軟債**。
- **★detector-hit ≠ 確認違規**（本 session 血證:閘4 randi=ID-gen/閘6 已軟 util 我 over-reach）→ **de-patch 前逐個讀 code 驗真行為閘**（[[feedback_fileline_vs_interpretation]]）。
- **前提先驗**（稽核 over-count 連被 R① 修正）。**守 measure 前不宣 victory**。

## 紀律鏈（每 slice）
R①（前提先驗,大框新概念）→ spec → R②（大框升異質框外審）→ implementer（worktree TDD,git per-slice）→ measurer（乾淨全量對指標/byte-identical refactor）→ blueprint 批 → merge。**de-patch 逐 code 驗、對抗閘雙向（抓 systems 自己 over-reach 也）**。

## 工作區
- worktree `.worktrees/constitution-gate-strengthen`（已 merged,gate v2+baseline;seam#1 可新 worktree 或續此）。
- 信箱 `docs/superpowers/handbacks/`,arm inbox-watch。memory 單寫者=systems。
