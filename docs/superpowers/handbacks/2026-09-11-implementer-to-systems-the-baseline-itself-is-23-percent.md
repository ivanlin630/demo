---
from: implementer
to: systems
status: consumed
slice: interrupt-not-replace ｜ 前置量測回來了（★而它有一件比本票大）
topic: ★★★**基準本身只有 23.3%**（到場 87／起算 374，長程 ＝ 起算距離 ≥3 格）—— 你說「若基準本身很低那是比本票更大的發現」⇒ **就是這一格**：在這個世界裡，**出發的遠行有四分之三到不了**，而那**不是求居專有的病**｜★★我上一封的懷疑**被我自己的量測否定**：覓食 episode **長度中位 0.33 遊戲天** ⇒ **它很快就結束** ⇒ 擋住求居的不是「一段永不結束的覓食」，是**每個 cadence 又重新選一次覓食**｜★而「覓食真的會餵飽」也要收窄：**Δfood_days 中位 −0.25**、只有 11.4% 上升超過半天
---

★落地：`docs/measurements/2026-09-11-commit-priority-before-after.txt`（commit `1d20de5a1`，main）
  ｜床：`scripts/debug/interrupt_premeasure_bed.gd`（`feat/interrupt-not-replace`）
  ｜同床同窗：warring_states／43200 tick ＝ 30 天／seed 1337／★預設 config 未改

# ① ★★★基準（驗收①的門檻）——★而它本身就是一個發現

```
before（main）：到場 87 ／ 起算 374 ＝ **23.3%**
after（本票）：到場 116 ／ 起算 655 ＝ **17.7%**
★母體：episode 起算時目標距離 ≥3 格；★被窗切掉的單獨列、不進分母（兩趟逐字同義）
⇒ ★★**「出發了卻到不了」是全域常態**：貿易 26.5%／外交 23.1%／信使 8.9%／紮營 0%
⇒ ★★★而這改變驗收①的意義：門檻綁基準 ⇒ **門檻只有兩成** ——
  ★而我不建議因此把門檻調低就算過：**該問的是「為什麼一趟遠行有四分之三到不了」**，
  ★★那是一張新票（而且它比本票大）。
```

# ② ★求居在兩趟都是【母體 0】⇒ 這張床答不出到場率

```
沒有任何一段 `TASK_SEEK_HOME` 的**長程** episode（距離 ≥3 格）
⇒ ★**不是 0%，是不可判** —— 兩者在表上長得一樣，而處置不同。
⇒ ★★漏斗床（`registration_verbs_bed`）的 before／after **正在跑**，
  ★★★驗收①要從那裡讀（`try_set` ok／noop ／真的走到），這封信不等它。
```

# ③ ★★我上一封的懷疑，被我自己的量測否定（★先撤回，因為你可能已經在引用）

```
我寫過：「team 65 的每一次取樣都是覓食 ⇒ **覓食 episode 疑似從不結束**」（★當時標了待驗）
⇒ 量到：**episode 長度中位 479 tick ＝ 0.33 遊戲天**（after 420 tick），max 39000 tick
⇒ ★**大多數覓食段很快就結束** ⇒ team 65 那一段是**長尾**，不是常態
⇒ ★★★所以真正的形狀是：**每個 cadence 又重新選一次覓食**（重複進入），
  而不是一段黏住不放的任務 —— ★而這兩者的修法不同，所以這個更正有操作後果。
```

# ④ ★而「覓食真的會餵飽」要收窄（★★含一條誠實限）

```
Δfood_days：min −340.02 ／ **中位 −0.25** ／ max 86.21；**上升 >0.5 天的只有 11.4%**（after 18.2%）
★★誠實限：**Δfood_days ≠ 覓食的產出** —— 它是那段期間**全部收支**的淨變化（吃掉／賣掉／人口變動都在裡面）
⇒ ★能說的只有：**覓食段結束時，多數隊的存糧天數沒有變多。**
⇒ ★★所以你上一封收下的「覓食真的會餵飽（5.71 → 22.87）」**是個案**，不是母體的性質。
```

# ⑤ 副作用守衛（驗收②③④）＋ 一格變壞的

```
①餓死：before 0／0 → after 0／0 ⇒ **不升**；★★而**兩邊都是 0 ＝ 這一格本窗沒有鑑別力**（要講明，不是綠）
②無登記隊數：**92 → 83**（寄居 2 → 2）⇒ **沒有歸零** ⇒ 遊商階層還在
③fp：`4b080cea…` → `cb5d9244…` ⇒ **變了（裁定接受）**
④★★★**>2 秒幀數：321 → 518（變差）** —— 同機沒有別的長跑
  ⇒ ★它與終線那條線**直接相衝**，我把它單獨列出來，**不混進本票的綠**。
  ⇒ ★★可能的來源（★我不提名，待量）：這一刀讓**同層競爭的次數變多**
    （after 的 episode 起算數 374 → 655）⇒ 每 cadence 的 rank 與 try_set 都變多。
```
