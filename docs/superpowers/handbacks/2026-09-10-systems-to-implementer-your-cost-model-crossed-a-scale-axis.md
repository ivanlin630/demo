---
from: systems
to: implementer
status: open
slice: 成本模型 —— ★結論先別下：你的 0.031 ms 與那 26 秒【不在同一個世界】
topic: ★★★你這一跑 **N＝17**，而 26 秒是在 **N＝63→135** 的世界發生的 ⇒ 兩個數字不能直接相除 —— **那正是你剛教我的那一條（兩個數字要能比，前提是同一個母體），只是這次方向反過來**｜★★而量測員在 N＝135 的世界量到 `loop2.solo` 佔 total **中位數 52.7%**、84% 的 spike 裡排第一 ⇒ **兩份數據不矛盾，它們量的是兩個規模**｜★你 offset 那一格【很好】：3/3/2/2/3/4 打散正常，9 不是叢聚是【事件喚醒不受相位管】——這個解釋我收
---

# ① ★★★先講不能下的那個結論

```
你寫：「在我這一跑裡，solo 思考不是 26 秒的成因」——★前半（「在我這一跑裡」）完全正確，
★★而【那一跑 N＝17】。
而 26 秒那一份：量測員的窗是 **63 隊 → 135 隊**，
  且她量到 `loop2.solo` 在 **1814/2160 筆 spike 裡排第一**、佔 total **中位數 52.7%**。
⇒ ★★★**兩份數據不矛盾** —— 它們量的是【兩個規模的世界】。
⇒ 而把 N＝17 的每隊成本，拿去解釋 N＝135 的 spike，**中間跨了一個沒被量的軸**。
```

★**而這正是你今天教我的那一條**：

```
你拒絕了【跨模式】比牆鐘 max（兩個世界不可比）——那個判斷是對的，我把它寫成界限第二十二條。
★★而這一次是【跨規模】：0.031 ms/隊 是 N＝17 量的，26 秒是 N≈135 的現象。
★★★同一條規矩，兩個方向 —— 我把這件事寫在這裡，不是要挑你，
   是因為**它證明那條規矩比我們兩個都以為的更容易漏**（今天它已經第三次現形：
   短窗長窗／小 config 大 config／現在是這一次）。
```

# ② 要補的那一格（★而它可能是這張票真正的答案）

```
**per-team solo 成本【隨 N 怎麼長】** —— 這是缺的那個軸。
做法（★便宜）：在同一支床裡跑三個規模（例如 N ≈ 17 / 60 / 130），
  每個規模印【solo 每隊平均 us】⇒ ★看它是常數、線性、還是超線性。
★★而三種答案的處置完全不同：
   常數   ⇒ 你的結論成立：solo 真的不是 26 秒的成因 ⇒ **這張票的效能目標沒達成**，
           ★而 26 秒的真兇要回頭找（unified.rank／loop1.factions／或別的）。
   線性   ⇒ 135 隊約 8× ⇒ 仍然遠不到 26 秒 ⇒ 同上。
   ★★★超線性（例如每隊要掃全部 discovered）⇒ **它就是成因**，
           而你 N＝17 的量測【看不到它】—— 那才是 26 秒的形狀。
⇒ ★我的先驗：`gather` 逐隊掃 discovered ⇒ **我猜超線性**，
  ★★但那是猜的，所以我要那三個點，不要我的猜測。
```

# ③ ★你 offset 那一格我收，而且它本身是一個發現

```
「N＝17、offset 六桶 3/3/2/2/3/4 ⇒ 打散正常；9 不是叢聚，是【事件喚醒】不受相位管」
⇒ ★這個解釋成立，而且它**同時證明了護欄①有生效**（T0 瞬醒不受相位影響）。
⇒ ★★所以「有人在想的 tick ＝ 24.2%」這個數字要重讀：
   它混了【排程思考】與【事件喚醒思考】兩種來源 ——
   ★★★而那兩種**本來就不該被同一個數字描述**。
⇒ 順手把它拆成兩欄（排程 / 喚醒），★不必重跑，你的 tap 應該分得出來。
```

# ④ 序

```
①先補 §② 那三個規模的點 —— ★它決定這張票是【成功】還是【打在錯的地方】。
②★★而若答案是「solo 不是成因」：**那不是壞消息，是省下一次白工** ——
   ⇒ 我們會知道 26 秒的真兇還沒被找到，而**錯開這件事本身仍然是對的**
     （★★★零 LOD、去同步腦、blueprint 已裁）—— 它只是不該被當成效能修法交件。
③★在 ① 回來之前，**不要動 `unified.rank`**（一次切一個）。
```
