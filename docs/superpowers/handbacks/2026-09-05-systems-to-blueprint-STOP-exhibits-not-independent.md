---
from: systems
to: blueprint
status: open
slice: ★★★B 開場包 —— 推給用戶【之前】要先看這封
topic: ★★★三件展品【可能不是三個獨立證據,而是一個原因的三個症狀】——measurer 自己標了這條誠實限,我把它推到底:config/peaceful_economy.json 是【手寫 12 隊】,11 生產 + 1 商隊(id=8,★全床唯一帶 coin 1000 的隊);★而 measurer 的 day30 快照說 12/12 全帶 TAG_PRODUCE ⇒ 與 config 對不上(她也寫了「已知18隊、現存14隊」⇒ 母體早就變了);★★我查了 runtime 唯一的 TAG_PRODUCE 寫入點是 outpost_system.gd:525(建造子隊完工安頓),商隊走不到 ⇒ 缺口未解;★★★已派 measurer 補 team8 的下場(活著嗎/tags/named_members/那 1000 coin 去哪),而【那一格回來之前,B 開場包的證據份量不該被當成四件】
---

# ★★★推給用戶之前，先看這一封

## 匿名池的結果收到：**比「水位低」更極端**
```
母體 treasury_rows = 1(90 天只有 1 筆,而且是 delta=0 的 no-op)
入金/出金 = 完全沒有｜水位全程最高 0.00 中位 0.00｜期末 per-team 全數 0.00
consider_extraction fire = 0 次
⇒ ★用戶那句「匿名抽積蓄＝現制即是」在這張床上是【真正的空轉】:不是抽得少,是【沒有可抽的東西】
```

## ★★但 measurer 自己標了一條誠實限，而我把它推到底了
> 「這批三個展品**可能是同一個 world composition（100% PRODUCE 隊型）下的同一組症狀，非三個獨立證據**」

```
★我查了 config:config/peaceful_economy.json 的 teams[] 是【手寫 12 隊】(不是 generator 隨機)
   11 隊 tags=["統領","生產"]
   ★★1 隊 tags=["商隊"] —— id=8「③商隊_中介」,pop 5,anon{平民:4}
      ★★★而它是【全床唯一帶 coin 1000 的隊】
⇒ ★所以 config 是 11/12,而 measurer 的 day30 快照是 12/12 —— 【對不上】
   (她同一封也寫了「已知 18 隊、現存 14 隊」⇒ 母體早就不是 config 那 12 隊了)
★★而 runtime 唯一的 TAG_PRODUCE 寫入點 = outpost_system.gd:525 `_auto_settle_builder`
   (建造【子隊】完工後就地安頓) ⇒ 商隊【走不到那條】⇒ 缺口未解
```

## ★★★所以我要求的是：**這一格回來之前，不要把它當成四件互相印證的證據**
```
①若 team8 早死/轉型 ⇒ 三個症狀【共用一個原因】:這張床上【沒有雇主,也沒有錢的來源】
   ⇒ ★那句話仍然重要,但它是【一件事】不是【四件】—— ★★寫成四件是把證據份量灌水
②若 team8 活著且有錢卻沒發薪 ⇒ 那是【另一個獨立的病】,而且比目前包裡任何一件都急
⇒ ★★★兩種結果我都接受,而【差別大到會改寫開場包的主張】
```
★已派 measurer：**team8 的下場**（活著嗎／tags／`named_members` 幾個／**那 1000 coin 去哪了**／day30 那「12 隊」的 team_id 到底是哪 12 隊）。

## ★另外一件與此直接相關的，要一併進包裡
```
★peaceful_economy 是【手寫的一張「全是村莊」的床】
⇒ ★★所以「薪資系統從未執行過一次」這句,可能不是【經濟壞了】,而是
   【這張床按設計就沒有雇主】—— 兩者的修法完全不同
⇒ ★★★而要分辨它,需要 warring_states 的同一組數字(measurer 已標 scenario-general 未驗)
```
★**我不擋你推** —— **但若現在就推，請把「三症狀可能同源」與「這張床按設計沒有雇主」兩句寫在包裡**，
★★否則用戶看到的會是「四個獨立證據指向經濟崩潰」，**而我們手上還沒有那個強度的證據。**
