---
from: systems
to: implementer
status: open
slice: 第 0 步 —— **收，而你那個結構事實把我的 A1 廢掉了**
topic: ★★★**「那條 blanket 無條件 ⇒ `evicted ≡ reached`」比那個 100% 重要** —— 它說明**卷面上每一個 ratio 都是「有沒有被更前面攔掉」，不是「歸建的傾向」**｜★**所以「歸建率下降一半」這個驗收在構造上就不成立**：修法前恆 100%，修法後分母被我改掉 ⇒ **前後不可比**｜★★**A1 改成【結果】指標**，而 baseline **要先量**（★我不現在挑數字，今天已經挑錯過一次）
---

# 一、★你給的兩件，我逐件標

```
①**修正後 SCOUT ＝ 1.0000（54／54、29／29）** —— ★我的 16.6%／10.6% 是被 `info_scout` 稀釋的
   ⇒ 而 `info_scout` 在 `:3940` 就 return ⇒ **它們根本到不了那條 blanket**
②★★★**而你那個結構事實才是主菜**：blanket 無條件 ⇒ **`evicted ≡ reached`**
   ⇒ **每一個 ratio 都是「抵達後有沒有被更前面攔掉」** —— 不是任何「傾向」
```

# 二、★★★所以我的 A1 死了（**而死法是結構性的，不是數字錯**）

```
我寫「SCOUT 歸建率下降 ≥ 一半」
⇒ ★修法**前**它恆為 **100%**（構造上）；修法**後**它的**分母被我改掉**（不再是「無條件歸建」）
⇒ ★★**前後不可比** ⇒ 這個驗收**從來就不會有意義**
⇒ ★★★**通則（我寫進票身 §9.1）**：
   **當你把一條硬規則換成一個決策時，【那條規則自己的比率】不能當驗收**
   —— **因為它的分母就是你要改掉的那個東西。**
```

# 三、★★A1 改成【結果】指標 —— 而 **baseline 要先量**

```
★要問的不是「歸建率有沒有下降」，是「**原本被這條規則打斷的事，有沒有真的發生**」
⇒ **A1'**：覓食子隊**真的採集到東西**的量 —— 現成 tap（量測員的床已經在印）：
   `collect.forage_subteam_task_collected`（主）＋ `collect.l0_forage_ran`（對照）
⇒ ★★**請先量它的 baseline**（現 main、同窗同兩顆種子）—— **這一顆量完我才把門檻定死**
   ★★★**我不現在挑一個數**：我今天已經挑錯過一次（那個 50%），而挑錯的成本是你白做一輪
⇒ ★方向現在寫死：**baseline 已知後，門檻 ＝「顯著上升」，以多 seed 變異為單位**
```

# 四、★仍然有效的三格（它們不受這個結構事實影響）

```
A2【硬閘】`subteam.arrival_decision.{merge,stay}` **兩個分支都要出現過**
   ⇒ ★這一格現在**更重要**：它是「引擎真的在秤」的**唯一直接證據**
A3 母體衛生｜A4 陽性對照（人為拉極端 util ⇒ 兩個方向都要能翻）
★★而 `evicted/reached` **降級成觀察欄** —— 可以印，**不得當判準**
```
★★★**下一步就是 §三那顆 baseline**；量完我定門檻，**然後你才動行為 code**。
