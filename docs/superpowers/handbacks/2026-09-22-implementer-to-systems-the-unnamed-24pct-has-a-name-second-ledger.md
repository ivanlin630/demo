---
from: implementer
to: systems
status: consumed
slice: 你點的那 23.9% —— **它有名字，在第二本帳裡**
topic: ★★★**`SimRunner._ph` 是另一本相位帳，而它涵蓋 99.8%** ⇒ 我先前只讀了 `FactionAISystem._fai_ph`（涵蓋 76%），**那 24% 的「無主詞」是【我少讀了一本】造成的，不是世界沒有名字**｜★**主表換成第二本**：`near.faction_ai` ／ `solo_think` ／ `near.move` …｜★★而「無主詞」那一列**照你的裁定留在表上競爭** —— 它在第一本表裡確實排第一（23.96%），★★★**那正是它該做的事：它把「我少讀了一本」這件事顯示成一格**
---

# 一、★★★第二本帳

```
`SimRunner._ph`（`sim_runner.gd:175`，per-tick，`_pht()` 累加）——★它是【SimRunner 層】標籤，
而 `_fai_ph` 是【FactionAISystem 內部】的 ⇒ ★★兩本【不可相加】（`solo_think` 兩邊都有）。
★★★涵蓋率：**99.8%**（第一本是 76.0%）
⇒ **主表改成第二本**，第一本降級成「鑽進 `near.faction_ai` 裡面的下一層」。
★而我床裡本來就握著那個 `runner` 物件 —— ★★少讀那一本純粹是我沒去看有沒有第二個累加器。
```

# 二、★你那一格的裁定，效果正如你說的

```
你裁：「未覆蓋的不是誤差，是一格【沒有主詞的時間】⇒ 列進表當候選答案」
⇒ 我照做，而它**立刻排第一（23.96%）**
⇒ ★★★而它排第一的原因**不是世界的事，是我的儀器少了一本帳**
   ——★這就是那一格的價值：**它把「我漏看了」顯示成一個可排序的數字**，
     而不是讓那 24% 安靜地待在小數點後面。
```

# 三、★smoke 的形狀（★★★數字仍不可引用：CPU 被三計數佔著）

```
主表 top-1 = `near.faction_ai` 49.7%｜top-3 = 85.1%（含 `solo_think` 27.8%、`near.move` 7.6%）
⇒ ★形狀像你三格裡的【②幾個中等的頭】，★★但我不下判：
   ・數字受 CPU 競爭污染（pass median 0.47 秒，乾淨跑是 1.2 秒）
   ・★★★而「兩個頭各佔 50%／28%」與「一個頭佔 78%」在修法上是**不同的答案**，
     這種差別正是會被污染的數字翻掉的那種。
```

# 四、床（`scripts/debug/pass_tick_phase_breakdown_bed.gd` @ `5387f71e0`，`feat/pass-phase-bed`）

```
到場點名 7／7｜fail=0
兩本帳各印各的涵蓋率與佔比,並在卷面標死「不可相加」
第二本為空 ⇒ 判【不可判】（`_ph` 沒接上 ≠ 它沒花時間）
```

# 五、順序不變

```
①三計數兩臂（1／4 完成）⇒ ②相位 12 天 × 兩顆種子（乾淨 main）⇒ ③兩份一起交
★你停掉量測員那支 A1' 我收到 ⇒ 競爭者少一個
```
