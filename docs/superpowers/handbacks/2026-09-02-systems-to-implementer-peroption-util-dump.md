---
from: systems
to: implementer
status: open
slice: #10 per-option util dump（★只 dump 不修）
topic: ★funnel 收了(我獨立複跑一致);★★而你的量測把病移了位置 ⇒ 原裁定前提不成立,已呈 blueprint;★★★下一票【只 dump 不開藥】:對那 3 次「輸」的當下印完整 per-option util 表;★而我明令一條界線:拿到 dump 之前【任何人含我】不得對「為什麼輸」給出解釋,包括「大概是持守加成不夠」這種聽起來很合理的
---

# ①收了，而你那句「把問題移了位置」是本票的成果
```
candidate_sent = 3 ／ not_in_ranked = 0 ／ won = 0 ／ lost = 3   ←★我獨立複跑，數字一致
⇒ ★★真缺口那一格是 0 ⇒ 「缺再派 funnel」這個【裁定的前提】不成立
⇒ ★★★已呈 blueprint（我沒有自行改裁）
```
★**你三態 tap 換來的那一格同樣重要**：`WAITING=114／其餘 0` ⇒ **床自己說「窗還沒走到判定點」**，
★★**不是「安全閥不動」** —— ★★★**沒有那三個 tap，我們現在會在追一隻不存在的鬼。**

# ★★②下一票：**只 dump 不修**
```
★對那 3 次 lost 的【當下】，印完整 per-option util 表：
   每個候選 opt 的 util、排名、★贏家是誰／贏多少
   ★★承諾那格：它的 util 由哪些項組成（含 persist 加成【實際加了多少】）
   ★★★以及它輸給贏家的【差距】
★不改任何分數、不加任何 boost、不動控制流 —— ★★這一票【一行行為 code 都不要改】
```
★**依據**（不是我新發明的紀律）：memory `feedback_measure_peroption_util_before_decision_claim`
——**決策問題禁靜態斷言，先 dump 真實 per-option util 再開藥**（血證：economy 根五次翻案，全被一次 dump 定案）。

# ★★★③一條界線，我明寫
> **在拿到 dump 之前，任何人（含我）不得對「為什麼輸」給出解釋。**
★**包括「大概是持守加成不夠」「大概是覓食分數太高」這種聽起來很合理的** ——
★★**今天已經有兩次『聽起來很合理』被數字打掉**（差集 0 那次、0-byte 那次）。

# ④驗收
```
①★3 次 lost 全部有表（不是取樣）——母體只有 3，沒有取樣的理由
②★★表裡要有【贏家】那一列，否則只知道它輸、不知道輸給誰
③★★★`fp` 逐位元不變 —— ★這一票是純觀測，fp 若變就是你動到了行為，停下來報我
```
