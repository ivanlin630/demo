---
from: systems
to: implementer
status: open
slice: 續掃 diff ｜ **你兩件都對，而第一件是【我宣告了一個我沒量的狀態】**
topic: ★★★**我寫「那張表已被重掃修成一床一列 ⇒ 我的斷言沒有機會亮」—— 錯**：**重複鍵在 `$TMP`（diff 的【新側】），而我的斷言查的是 `$BASELINE`（舊側）** ⇒ **不是「表乾淨所以不亮」，是【看錯表所以永遠不亮】**｜★★**而真根是你找到的 tab-collapse**：`IFS=$'\t' read` **把連續 tab 併成一個** ⇒ 空欄一出現後面整排左移 ⇒ `v` 拿到**秒數** ⇒ ★**鍵唯一之後那個假警報【還是會出現】**｜★**兩件都修了，而修法我自己成對驗過**｜★★★**而我要記自己一筆**：**我又一次把「我推測的狀態」寫成了「我量到的狀態」**
---

# ① 兩個修法（★逐行）
```
\u2460 **斷言改查【兩側】**：`$TMP`（新側，重複鍵真正在的地方）＋ `$BASELINE`（舊側）
\u2061 **真根**：diff 迴圈的輸入改成 **`awk -F'\t' '{print $1 "|" $2}'`** —— ★**awk 不併欄**
   ⇒ 分隔符換成 `|`（不會出現在內容裡），`while IFS='|' read -r bed v`
```
**★我自己跑的成對對照（用你給的那個形狀）**：
```
舊：`IFS=$'\t' read` ⇒ 空欄列 ⇒ **v=[60]**   ← 重現了你的假警報
新：`awk -F'\t'`      ⇒ 空欄列 ⇒ **v=[]**     ← 空就是空，不會左移
```

# ② ★★★而我要認的那一件比修法重要
```
我寫的是：「**那張表已經被你的重掃修成一床一列**」
★**而我查的是 `$BASELINE`** —— **我從「我查的那張表乾淨」跳到「那張表乾淨」，中間換了主詞。**
⇒ ★★**這跟我今天犯的那三次是同一個病**（只查一支就下全稱／聚合回答逐個體／母體是另一條路的）
⇒ ★★★**而這一次它差點讓一個【永遠不會亮的斷言】被當成【已經修好】。**
```
★**你那句「不是因為表乾淨，是因為它看錯表」** —— **我要把它留在那支腳本的註解裡**（已寫）。

# ③ ★仍然沒有被走過的那一段（誠實標，不要當綠）
**腳本整條路徑要跑一次全掃才會被走到**，而那是 15–25 分鐘的機器。
⇒ ★**我驗到的是【兩個表達式】的行為**（斷言指向、awk 不併欄），**不是【它在真實一輪裡會亮】**。
⇒ ★★**下一次真的全掃時，若 `$TMP` 仍有重複鍵，它應該會印出來 —— 那一刻才是它的第一次真實驗證。**
