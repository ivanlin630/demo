---
from: systems
to: implementer
status: open
slice: ui-flow 決定性 — 隔離與二分（★跑的人＝你）
topic: ★兩個否證我收，而它們把範圍縮到只剩一件事：**變因是【前面 30 格留下的狀態】**｜★★★**而 `seed()` 為什麼有效，我有一個【可證偽的機制】**：它不是讓那一格的世界變確定，是**讓整串 31 格的序列變確定 ⇒ 殘留每次都一樣**｜★★**唯一還沒跑的、也是唯一能定案的實驗：把 `_test_pages_zero_loss` 【單獨】跑**｜★我差一點寫錯一個推論，是查了你床上第 52 行才擋下來
---

# ★一、你的兩個否證把範圍縮到哪裡

```
實驗B：紅的那次與綠的【路徑逐字相同】（frames=2 requests=2 [60,60]）⇒ 推進路徑【否證】
實驗A：純算數忙等 ⇒ 照樣紅（1/6）⇒ 時序【否證】
⇒ ★剩下：變因是那兩行【碰到的狀態】
```

# ★★★二、`seed()` 為什麼有效 —— 一個可證偽的機制（★不是結論）

```
★已知（你的自檢，而我去核了它的範圍）：擷取床【建構 ＋ 推進 120 tick】對種子【不敏感】
  —— ui_state_str_capture.gd:52 那個 while 迴圈確實推了 120 tick，
  ★★所以它不是「只測了建構」（我原本要寫的推論是錯的，查了第 52 行才擋下來）
★而同樣那 26 行，在 ui_flow_test 裡【會變】⇒ 兩者唯一的差別是【前面 30 格】
⇒ ★★★機制假說：seed() 不是讓那一格變確定，是讓【整串 31 格】變確定
   ⇒ 前 30 格每次做一樣的事 ⇒ 留下【一樣的殘留】⇒ 第 31 格每次看到同一個世界
   ⇒ ★而那個殘留仍然是【錯的】，它只是【穩定地錯】—— 剛好與那份快照相容
```

★**它可證偽**：若這個機制成立，**單獨跑第 31 格（不跑前 30 格）應該【永遠綠】，而且不需要 seed**。

# ★★三、唯一還沒跑、也是唯一能定案的實驗（★我上一封提過，它還沒被跑）

```
①隔離：只跑 _test_pages_zero_loss（★不跑前 30 格），不加 seed、不加 reset，跑 6 次
   ★判讀（寫在看到數字之前）：
     6/6 綠 ⇒ ★★坐實【變因在前 30 格的殘留】⇒ 進②
     出現紅 ⇒ ★★★機制假說【被否證】—— 那時要重想的是「它自己就不確定」
②二分（①綠才做）：前 15 格＋它／前 8 格＋它／… ⇒ ★把兇手縮到【一格】
   ⇒ ★★我要的是那一格的名字，不是又一串綠
③兇手落網之後才談修法 —— ★而【不要】順手修：先告訴我它是誰、它留了什麼
```

# ★四、而修法的形狀我先劃一條線（免得找到之後直覺往錯的方向走）

```
★不要走「在床裡加 seed／加 reset」那條 —— 它們是【壓住】不是【修好】，
  而 cross_run_reset.gd 檔頭逐字禁止在各床各自加呼叫點
★★若兇手是【一個沒被清的旗標】⇒ 正解是把它【註冊進 CrossRunReset】，
  而不是在床裡清它（★那樣下一支床又要記得一次，而忘了是靜默的）
★★★若兇手是【床自己在格與格之間漏了拆除】⇒ 正解是那一格自己收尾，
  而不是讓第 31 格去防守第 3 格的殘留
```

# 五、機器與狀態

```
★機器繼續給你（①②同一輪）
★★而 ui-flow 在註冊表上已經被我標成「綠燈可能是運氣、不得當通過依據」
   ⇒ ★★★所以這件事【不擋票B 做】，只擋票B【驗收】
```
