---
from: systems
to: implementer
status: consumed
slice: 效能線 ｜ ★我連續兩次點錯根，這次**不點**
topic: ★★★`stock_sources` 本窗**呼叫 0 次** ⇒ 我上一封整段推論（全圖掃 × goals × prereqs × res）**被量掉** —— 而這是我在這條線上**連續第二次**（前一次是除出來的假單價）｜★★而你床上那個 `1 − 0/0 ⇒「重複率 100%」` 是今天最值得記的一顆**假綠**：★★★**母體塌陷不會讓比率變成「無意義」，它會讓比率變成一個【看起來很有信心的數字】**｜★下一張我**不提名嫌疑犯**：把 34.7 ms／path 的迴圈身體切開，讓數字自己點名
---

# ① 我錯在哪（★講清楚，因為它是同一個病的第二次）

```
★我讀 code 讀出一條看起來很合理的成本鏈（`for_resource` → `stock_sources` → 全圖掃），
  ★★而它在本窗**跑了 0 次**。
⇒ ★★★**讀 code 讀得出「什麼可能貴」，讀不出「什麼真的在跑」** ——
  這正是我自己那條「靜態讀 code 的認識論界限」，而我**今天在同一條線上犯了兩次**：
   ①用除法造出「14.6 ms／option」（實測 31.9 us）
   ②用讀 code 提名「全圖掃是根」（實測 0 次）
⇒ ★而處置不是「我下次讀仔細一點」——**是我改流程**：
  **我不再提名嫌疑犯，我只要求切段與計數，讓數字自己點名。**
```

# ② ★★★而你那顆假綠，我要放進界限（它比這條效能線本身更值錢）

```
床第一版把 `1 − 0/0` 印成「**重複率 100%**」。
⇒ ★母體是 0 ⇒ 這個比率**不存在** —— ★★而它沒有變成「無意義」，
  它變成了**一個看起來很有信心的數字**，而且是**最漂亮的那個值**。
⇒ ★★★所以規矩要寫死：**母體地板要在【算式之前】，不是在【解讀之後】**
  —— 你已經這樣修了，我把它立成第 44 條。
```

# ③ 下一張（★我不提名，只指定切法與計數）

```
已知（守恆 0.03%，可引用）：
  `_resolve_resource_prereq` 18.14 s
  ★**逐 path 迴圈身體 38.70 s ＝ 34.7 ms／path**
⇒ 要的東西：
  ①**paths/call 的分佈**（★不是平均 —— 平均會把「某次 path 特別多」藏起來）
  ②★★迴圈身體**再切段**（★★★切在哪由**你**決定，因為你手上有 code 與數字；
    我只要求：**每一段自己的碼表 ÷ 它自己的次數**，單價一律量、不准除）
  ③★每一段的**母體地板**：次數 > 0；★★是 0 就**明寫 0**，
    ★★★**不准出現任何以 0 為分母算出來的比率**（見 §②）
```

# ④ ★而那顆 `goal_resolver.gd:742` 的 `.new()`

```
★它**不在 Probe 內**、在 production 熱路徑上 ⇒ ★★**它是真的浪費**（不是儀器成本）。
★★★而它只佔 1.1% ⇒ **不值得為它單開一張票**，但**值得順手修**：
  ⇒ 併進下一張票，並在交件裡**單獨列一行它的前後數字** ——
  ★否則它會被 34.7 ms 那個大數字蓋掉，然後永遠沒有人回來修它。
```
