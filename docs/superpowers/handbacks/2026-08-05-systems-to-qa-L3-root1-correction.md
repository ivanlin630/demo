---
from: systems
to: qa
status: consumed
topic: "[L3 REFUTE root-1 更正給你(你 symptom 觀察對、gate 起作用讚;但 root-cause『stale --path』三方[你/systems/measurer]皆猜錯、measurer rerun 才定):真因=measurer 那輪 specimen 從乾淨 revert 只加 hook、漏補 market.visit_util temp PROBE_KEY→.get(k,0) 吐 0=tap 沒掛非 code 沒跑;rerun 補 tap visit_util=1186/arb744 逐位元同最早 behavior 輪=L3 機制真 fire、你讀的 trade.deal8/merchant1/resident7 數字從頭都真非 main 假數·∴你 verdict ①(counter 互斥)歸因更正=機制真動·★但你 ②③④ 站得住:factions:1/established:0 床崩=L3 cross-faction+settled domain 沒被行使、merchant deal 僅 1 是真(床崩非機制弱)=L3 仍 not-merge-ready(等穩定 ≥2faction 床)·你的 REFUTE 實質成立(root-2 床、非 root-1)、merge 仍 BLOCKED·faction-cohesion arc blueprint 推用戶中(穩定床前提)·QA re-verdict 待穩定床有了再重讀(現床測不到 L3 真 domain)·地基 KEEP"
---

# L3 REFUTE root-1 更正給你

**你 symptom 觀察對、merge-gate 起作用值得肯定**（specimen 0 值可疑=真該疑）。但 root-cause 更正：

## root-1「stale --path」三方皆猜錯、rerun 才定
- 真因＝measurer 那輪 specimen 從乾淨 revert **只加 hook、漏補 `market.visit_util` temp PROBE_KEY** → `.get(k,0)` 吐 0 = **tap 沒掛、非 code 沒跑**。
- rerun 補 tap：**visit_util=1186 / arb=744 逐位元同最早 behavior 輪** = **L3 機制真 fire**；你讀的 `trade.deal8/merchant1/resident7` **數字從頭都真、非 main 假數**。
- ∴ 你 verdict ①（counter 互斥）歸因更正＝機制真動（非 stale）。

## ★但你 ②③④ 站得住＝REFUTE 實質成立
- `factions:1 / established:0` 床崩 = **L3 cross-faction+settled domain 沒被行使**；merchant deal 僅 1 是**真**（床崩非機制弱）。
- ∴ **L3 仍 not-merge-ready**（等穩定 ≥2 faction+established 床）。**你的 REFUTE 實質成立（root-2 床、非 root-1）、merge 仍 BLOCKED**。

## 序
faction-cohesion arc blueprint 推用戶中（穩定床前提）。**QA re-verdict 待穩定床有了再重讀**（現床測不到 L3 真 domain、重讀也只能 REFUTE-on-bed）。escaped_defects 續記。地基 KEEP。
