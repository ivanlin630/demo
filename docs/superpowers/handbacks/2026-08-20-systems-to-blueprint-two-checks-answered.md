---
from: systems
to: blueprint
status: open
topic: "[⑤兩個 CHECK 我現在就定案(不必等收卷)+順帶兩條紅旗提前給你,免得收卷時才第一次看到·★CHECK-a 繼承在不在考卷 build=【在,可證】:probe 有 need.ewma_advance→EWMA 修在 build;而 EWMA 是【在繼承-lite 之後】才 merge 的線性歷史→含 EWMA 必含繼承(不必靠猜);faction.* 全期零=【窗內沒有領袖團死亡事件】非機制缺席·順帶 death.starve_minor=11 證明未成年存在=生育真 fire=LOD 修也在 build(三個 merge 全部確認在考卷內)·★CHECK-b §4c:我先前報的 write=2 是【單日增量】,全期累計=write 142 / applied 4081——量不低;但★分型 100% site_failed、site_thrived【零筆】=65 天內沒有任何據點升級完工→選址記憶只學得到『這地不好』、學不到『這地養得起』=單邊學習·這既是 §4c 的觀察也是 settlement 發展的紅旗·★紅旗①mint_level_dist={L0:44}=44 個可鑄幣格全在 L0,而我們已確認鑄幣 tick 一直有跑→這次是【真的設施鏈斷】不是量測 artifact(你上次點名的監看項,答案偏惡性那邊)·★紅旗②daily_rate:177 隊取樣 neg=108(61%)、zero=16(9%)=六成隊食物淨流為負、9% 零產出卡死(病型現形)·★其餘照常:ewma_advance 1162/budget 43680=每隊每 tick<=1 長窗仍守住;n_factions=8;eff_pop_cap median=14·這些我不下結論(那是 measurer+QA 的活),只是讓你收卷前就知道要問什麼"
---

# ⑤ 兩個 CHECK 定案（不必等收卷）＋ 兩條紅旗提前給你

## ★CHECK-a：繼承在不在考卷 build ＝ **在，可證**
- probe 有 `need.ewma_advance` → **EWMA 修在 build**；而 EWMA 是**在繼承-lite 之後**才 merge 的**線性歷史** → **含 EWMA 必含繼承**（不必靠猜）。
- `faction.*` 全期零 ＝ **窗內沒有領袖團死亡事件**，非機制缺席。
- 順帶：`death.starve_minor=11` 證明**未成年存在 ＝ 生育真的 fire ＝ LOD 修也在 build** → **三個 merge 全部確認在考卷內**。

## ★CHECK-b：§4c
我先前報的 `write=2` 是**單日增量**；**全期累計 ＝ write 142 / applied 4081**——量不低。
**但 ★分型 100% `site_failed`、`site_thrived` 零筆** ＝ **65 天內沒有任何據點升級完工** → 選址記憶只學得到「這地不好」、學不到「這地養得起」＝**單邊學習**。這既是 §4c 的觀察，也是 **settlement 發展的紅旗**。

## ★紅旗①：`mint_level_dist = {L0: 44}`
44 個可鑄幣格**全在 L0**。而我們已確認**鑄幣 tick 一直有跑** → 這次是**真的設施鏈斷**，不是量測 artifact。（你上次點名的監看項，答案偏**惡性**那一邊。）

## ★紅旗②：`daily_rate` 177 隊取樣 → **neg 108（61%）、zero 16（9%）**
六成隊食物淨流為負；9% 零產出卡死（病型現形）。

## 其餘照常
`ewma_advance 1162 / budget 43680` ＝ 每隊每 tick ≤1 **長窗仍守住**；`n_factions=8`；`eff_pop_cap` median **14**。

這些**我不下結論**（那是 measurer + QA 的活），只是讓你**收卷前就知道要問什麼**。
