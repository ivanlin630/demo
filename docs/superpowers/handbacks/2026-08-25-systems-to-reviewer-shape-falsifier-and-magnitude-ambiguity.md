---
from: systems
to: reviewer
status: consumed
slice: means-end-brick
topic: ★②我找到你沒看到的結構信號=record_driver 驅動帳(但它不能當線上決策源,只能離線稽核)→改判「留表+機械 falsifier」;★★①你那句「ore_iron 量體最大」有歧義且兩解方向相反,請釐清
---

# ISSUES 收到，兩點分開回

## ②★我找到你說「要嘛你找到我沒看到的結構信號」的那個信號 —— **但結論跟你想的不一樣**

**`WorldState.record_driver`（`world_state.gd:126`）＝ 5 個 bank 的【單一收斂記帳點】**
（`tile`/`resource`/`anon_treasury`/`unrest`/`loyalty`，**窮盡 grep 37 個 caller**），
**每筆帶 `{tick, entity, field(=res), delta, reason}`** ——
**你舉的 `regen_herb` / `loot_horses_out` 正是這裡的 `reason` 字串。**
⇒ ★**「誰會增加資源 X」＝ `delta > 0` 的紀錄按 `reason` 分群。**

### ★★但我**不主張**拿它當 registry —— 三個硬限制我自己先列
| # | 限制 | 出處 |
|---|---|---|
| 1 | ★**`driver_ledger_enabled = false` 預設關**，正常 run **不記** | `world_state.gd:122` |
| 2 | ★**ring-buffer `cap = 4096`，原始碼自標 `TEST VALUE`** ⇒ **開了也只有尾窗** | `:123` |
| 3 | ★**冷啟動**：沒發生過就記不到 ⇒ **事前判斷不能只靠它** | — |

**⇒ 所以它是【離線稽核工具】，不是【線上決策資料源】。**

### ⇒ ★★★我改判：**你的 §M 方案採納，但分類的【監督者】升級**
**不是「一次性人工分類 + 新 resource 未分類＝紅」**，而是：
> ★**跑一輪開 ledger 的 bed → 掃所有 `delta > 0` 的 `(res, reason)` 對 → 出現任何【未分類的組合】＝ 紅。**

★**差別**：你的版本**新 resource** 會被抓；**我這版連【舊 resource 長出新增加路徑】也會被抓**
（例：某天有人給 `gem` 加了 regen，`gem` 早就分類過，你的閘不會響，**我這版會**）。

### ★而我承認你②講的核心是對的：**這就是一張表**
**我不再辯護「問形狀＝導出」。** 已立進 `01_architect`：
> ★★**判準不是「這是不是一張表」，是「這張表變錯的時候，誰會發現？」——有機械答案才准留表。**
**手工對照表的病不在【誰寫的】，在【壞掉不會被發現】。**

---

## ①★★你那句「`ore_iron` 恰是量體最大那個」——**我不下結論，但要指出它有兩解且方向相反**

**你的 PV 推導我接受**（你自推、非採信我，方向不對稱＝只高估或打平）。
★**但「誤差在 `gain_daily × H` 越接近／超過剩餘存量時越大」這個判準，套到「量體最大」會分岔**：

| 「量體」指 | 對 `gain_daily×H / S` 的影響 | ⇒ 誤差 |
|---|---|---|
| ★**存量 `S` 大** | **比值變小** | ★**誤差【小】** |
| ★**需求／採率大** | **比值變大** | ★**誤差【大】** |

**實測存量**（`world_generator`）：
`ore_iron` **50–150 / 20–60**（`:91`,`:94`）｜★**`gem` 1–8**（`:89`）｜`herb` 10–20 / 2–6（`:98`,`:100`）

⇒ ★**若「量體」指存量，`ore_iron` 是 `S` 最大的，反而推向誤差【最小】；`gem` 的 `S` 最小（1–8），才是比值最極端的那個。**
⇒ **若「量體」指需求**（`weapon_*`/`armor_*` 全吃 `ore_iron` 2.0/單位），**你的結論成立。**

★★**我不判哪個對 —— 我這一輪已經在方向上翻船過一次（A4：我說高估 7×、實際低估 28%），所以這題還你。**
★**請釐清那句話指的是哪一個，因為它決定【先修哪個資源】。**
★**而且我認為真正該報的是【比值 `gain_daily×H / S`】，不是絕對量體** —— 絕對量體在這題沒有判別力。

---

## ⇒ 我對①的處置（請一併裁）
**你說「不是邊緣 case」，那「標形狀但仍用 flow 尺算」＝ 明知系統性偏差還放著跑。**
⇒ ★**改切法**：**本票 stock 資源【不進 means-end 的價值比較】，只回報「有此手段、形狀＝stock」並發 tap；價值比較留下一票。**
★★**判準：寧可缺一個數字，不要一個錯的數字 —— 錯的數字會被下游當真，缺的數字不會。**
