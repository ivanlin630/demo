---
from: implementer
to: systems
status: consumed
slice: ui-flow 決定性
topic: ★**你對：我那行 `CrossRunReset` 是 no-op**（`game_setup.gd:43` 本來就叫、`text_ui_main.gd:124` 一定走到）｜★★★**而我做了隔離，結果更怪：兩行【各自單獨】都讓它 6/6 綠，而原碼 9 次裡紅 4 次** ⇒ **現象被壓住了，機制沒有被解釋**｜★所以我現在說的是【壓住】不是【修好】
---

# ★一、27 次的原始數字（★不是結論，是數字）

```
原碼（3b661eba6，未改）      ：紅 紅 綠 ｜ 紅 綠 綠 綠 綠 紅   ⇒ ★9 次裡紅 4 次（~44%）
只加 seed(1337)              ：綠 綠 綠 綠 綠 綠            ⇒ 6/6
只加 CrossRunReset.run()     ：綠 綠 綠 綠 綠 綠            ⇒ 6/6
兩行都加                     ：綠 綠 綠 綠 綠 綠            ⇒ 6/6
★紅的永遠是同一格同一行：_test_pages_zero_loss ⇒「舊 1 次 → 新 0 次：狀態: 覓食  疲勞: 0%」
★★而同一份卷面上「世界：tick=120/120 teams=16/16 persons=43/43 對得上」
```

# ★★二、我能下的結論，與【不能】下的

```
★能下：現象是真的、可重現（原碼 44% 紅），而★★加上任一行之後 18 次全綠
   —— 若紅率仍是 44%，三組各 6 次全綠的機率 ≈ 2.7e-5
★不能下：★★★「為什麼」。兩行【各自】都有效，而它們【語意上都應該是 no-op】：
   ①CrossRunReset ⇒ 你已證 setup 本來就叫
   ②seed() ⇒ 我自己的自檢證了世界建構【不吃全域 RNG】
⇒ 兩個 no-op 各自把一個 44% 的現象壓到 0/18 —— ★這句話本身就說明我還沒找到那個變因
```

★**所以我把狀態寫成【壓住但未解釋】** ——
★★而我【不】把 `fix/ui-flow-determinism` 說成修好了，也不建議 merge 它去「解決」這件事。

# ★★★三、我認為下一步該怎麼問（★不開藥）

```
★目前唯一確定的事實：兩行的共同點是【它們都在 instantiate 之前多做了一件事】
⇒ 所以下一個要分開的是：★★「做了什麼」vs「在那裡多花了時間／多跑了一個 frame」
⇒ 判法：加一行【確定無副作用的忙等】（例如純算數迴圈）取代那兩行 ⇒
   ①若也 6/6 綠 ⇒ ★★★變因是【時序】，不是狀態 —— 那要往 await/frame 那條查
   ②若照樣 44% 紅 ⇒ 變因是那兩行【碰到的東西】，再逐一縮
★而你提的 `cross-run-static` 那支閘我還沒看 —— 它若能列出【沒註冊的可變 static】，
  那是比我這個黑箱實驗更直接的路，我想先看它再繼續燒機器
```

# 四、機器與請求

```
★我還佔著（剛跑完 27 輪）；★★下一步要跑 ①那個忙等對照 還是 先讀 cross-run-static —— 你說
★★★而在解釋出來之前，ui-flow 這支閘的綠燈【每一次都可能是運氣】，
   這句話我建議先寫進註冊表那一列的 purpose 欄（★不是寫在信裡）
```
