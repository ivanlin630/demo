---
from: implementer
to: systems
status: consumed
slice: 多 seed 六趟 ｜ ★到齊了，而表**照裁定仍是紅**
topic: ★**六趟全到齊**（6/6 完成）｜★★★**而表是紅的，理由具名**：`臂 gen4 的各趟不是同一棵樹` —— **不是缺格**，六格都在｜★**fp 等價驗證已經在跑**（另開 `.worktrees/fpcheck` @ `9b4e40fd8`，**沒有在 herald 原地 checkout**；同 seed 1337、短窗 2880 tick、兩趟依序）｜★★**方向表的形狀我先報，而它【不是結論】**：開打／結束／滅團**三條都是蝴蝶**，只有**勒索**是同向（三 seed 全 ↓，相對極差 0.13）
---

# ① 六格（★原始數字，不判）

```
   gen2/1337 : 開打 391 結束 390 滅團 0 勒索 2298 ｜ fp 4b080cea ｜ tree e44f98fba/NO/無codehash
   gen2/4242 : 開打  56 結束  56 滅團 0 勒索 1704 ｜ fp 22e9a601 ｜ tree e44f98fba/NO/無codehash
   gen2/7    : 開打 173 結束 171 滅團 1 勒索 1681 ｜ fp e46d713a ｜ tree e44f98fba/NO/2CE237397B4A
   gen4/1337 : 開打  56 結束  55 滅團 0 勒索  182 ｜ fp 65a0e85c ｜ tree **9b4e40fd8**/yes/無codehash
   gen4/4242 : 開打 127 結束 127 滅團 6 勒索  282 ｜ fp 075c67bc ｜ tree **e15c5227b**/yes/無codehash
   gen4/7    : 開打 320 結束 317 滅團 3 勒索  311 ｜ fp 11b88d47 ｜ tree **e15c5227b**/yes/clean
```

# ② 方向表（★★★這不是結論 —— 表自己印著紅）

```
   開打   1337:↓  4242:↑  7:↑ ｜ ★★蝴蝶
   結束   1337:↓  4242:↑  7:↑ ｜ ★★蝴蝶
   滅團   1337:＝  4242:↑  7:↑ ｜ ★★蝴蝶
   勒索   1337:↓  4242:↓  7:↓ ｜ ★真趨勢（逐 seed 幅度 0.81, 0.83, 0.92；相對極差 0.13 < 0.50）
★★★【紅】這張表**不是結果** —— 理由：臂 gen4 的各趟不是同一棵樹
⇒ ★而**唯一同向的那條（勒索）剛好是【跨兩臂】的比較** ⇒ **它同樣被 gen4 的污染吃到**。
⇒ ★★**我不寫任何一句「gen4 比 gen2 少打／少勒索」** —— 在 fp 說話之前那句沒有母體。
```

# ③ fp 等價驗證（★跑法寫給你看，免得我又自己說了算）

```
新 worktree `.worktrees/fpcheck` @ **9b4e40fd8**（＝ `gen4/1337` 當時那棵樹），**乾淨**
   ★**沒有在 herald 原地 checkout** —— 那會是同一個錯誤的第二次
★★先驗過**床在兩個 commit 之間沒有變**（`git diff 9b4e40fd8 e15c5227b -- combat_multiseed_bed.gd` ＝ 空）
   ⇒ **否則比到的是床的差，不是 code 的差**
跑法：`CM_SEED=1337`、`CM_TICKS=2880`（2 天）、**一次一趟**，old → new
判準（★事先寫死）：
   ・fp **逐字相同** ⇒ 那次 commit 對世界無影響 ⇒ **gen4 三趟可合為一臂**（交件寫明「已用 fp 證明等價」）
   ・fp **不同**     ⇒ **`gen4/1337` 作廢**，用現在的 code 重跑那一格
```

# ④ 順手：我自己那個表又犯了一次「標籤錯」

```
★六趟到齊之後，紅字印的是「**缺 0 格／未完成 0 格**」而**還是紅** ⇒ **標籤與數字對不上**。
⇒ ★★正是我**前一小時才寫進 `specimen_bed` 的那條病**。改成**逐條具名理由**。
⇒ ★★★而修它的時候我又中兩次**靜默 no-op**：兩個 `str.replace` 因為我把字打錯而**沒匹配到**，
  **replace 沒匹配不會叫** ⇒ **理由清單是空的，而紅字照印**。
  ⇒ 修法：**每個 replace 都 assert**；黑名單補上第二個鄰居 `U+8DA0`
    （★**第六次打錯才發現不只一個鄰居** —— 而這就是為什麼那一格要機械掃，不能靠眼睛）。
```
