---
from: systems
to: implementer
status: open
slice: S2-propagate-cadence-evidence
tier: probe
topic: ★★★★送達那條線接完了,而它是【兩層疊加】:TTL 早死(已修)＋★propagate 掛在【每 tick】(從未分層) ⇒ 新根一小時 60 tick 而非 10 ⇒ 傳播機會 ×6;★★而這【不是常數】是【機制的節律】——守衛找數字,抓不到「每 tick 做一次」這個結構;★★★要一顆 tap 把假說變證據,而它 S3 也會用到
---

# ★★★★①送達那條線接完了 —— **是兩層疊加，不是一個原因**
```
★measurer 的比值：★★發出幾乎沒動（修前 −4.3%／修後 +4.6%，都在 ±5% 內）
   而送達/發出 比值 before 7.209 → 修前 1.780（−75.3%）→ 修後 10.708（+48.5%）
⇒ ★★★是【傳遞效率】變了，不是【上游變多】
```
★**而他查到 `propagate` 掛在【每 tick 的 arrival 事件】上。**
⇒ ★★**兩層疊加就解釋了全部**：
```
①TTL 被壓成 5 天 ⇒ 訊息早死 ⇒ 送達 ↓（−76%）      ←★已修
★②propagate 每 tick 一次，而新根一小時 60 tick 而非 10 ⇒ ★★傳播機會 ×6 ⇒ 送達 ↑
⇒ ★★★修好 ① 之後，② 這半【從來沒被碰過】⇒ 淨效果衝過 before（+55%）
```

# ★★②而它是【新的一類】—— 守衛結構性抓不到
```
★第一型：引用處裸數字        ⇒ 掃描器抓得到
★★第二型：定義處裸語意        ⇒ 名字啟發式抓得到（第二軸）
★★★第三型：★機制的【節律】掛在 tick 上 —— 「每 tick 做一次」
   ⇒ ★★★★它【沒有數字】。守衛找的是字面量，而這裡一個字面量都沒有
```
★**所以它不是「S1 又漏了一顆」** —— ★★**它在守衛的設計範圍之外。**

## ★★★而它的正確歸屬是 S3，不是 S2
★**LOCKED spec §3 的 T1 層 ＝「1 小時心跳」** ⇒ ★★**`propagate` 若該是 T1，它現在跑【6 倍頻】。**
★★★**而「哪些機制掛在哪一層」正是 S3（層級制骨架）的工作** —— **S2 只改根。**
⇒ ★**我會把這條路由呈 blueprint；本票只要【證據】，不要你修它。**

# ★★★③要的（★一顆 tap，純觀測）
```
★每遊戲【小時】實際 propagate 的次數（★不是每 tick，要能和「1 小時心跳」對照）
★★母體：該小時的 arrival 事件次數（★沒有分母的次數不可信）
★★★而 S3 也會用到它 —— 分層之後要驗「頻率真的降下來了」
```
★**位置紀律照舊**：**不得在任何 `Time.get_ticks_usec()` 區間內新增呼叫**；★**陽性對照用「key 不存在」**；★★`fp` 兩床不變（純觀測）。

# ★④而我要先講死：**這顆不是要你證明假說對**
★**若量出來 propagate 每小時的次數【沒有】隨根變 6 倍** ⇒ ★★**假說錯，照原樣回報。**
★★★**我今天已經有兩個假說被實測打掉**（隊數母體／RNG 流位置）—— **這顆也可能是第三個。**
