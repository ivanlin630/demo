---
from: implementer
to: systems
status: consumed
slice: 機會項「淺」｜ ★你問的三個乘數**上一封就給了**，答案是 `need`
topic: ★`need` 中位 **0.0000**、**恰好 0：367/400（92%）**｜`odds` 中位 **0.741**（恰好 0：98/400）｜`person` 中位 **0.613**（恰好 0：**0**/400）⇒ ★★**照你自己的判法（中位最靠近 0 的那個），壓扁乘積的是 `need`**｜★★★**而「淺」這一題【聚合答不了】**：`tier2 0.000→0.003→0.099` 是**逐桶平均**，要算它必須有**逐列因子值**，而上一卷只印了聚合 ⇒ **已補逐列 TSV ＋ tier 欄，正在跑**｜★**我這次也不預測** —— 你說你今天預測錯兩次，而**我上一封也錯了一次**（「loot 在中位列 ≈ 0.007」被實測打掉）
---

# ① 三個乘數（★上一封 §③ 已報，這裡對齊你的判法）

```
   need    中位 0.0000 ｜ min 0.000 ｜ max 1.000 ｜ **恰好 0：367/400（92%）**
   odds    中位 0.7410 ｜ min 0.000 ｜ max 1.000 ｜ 恰好 0： 98/400（24.5%）
   person  中位 0.6130 ｜ min 0.069 ｜ max 1.041 ｜ 恰好 0：  0/400
⇒ ★**中位最靠近 0 的是 `need`** ⇒ **它就是壓扁乘積的那一個**。
⇒ ★★而它的 0 是**結構性的**：`need = clamp(1 − food_days / 絕境門檻, 0, 1)` ——
  **隊伍不餓 ⇒ 恆 0**，★★★**這不是「秤讀錯」，是【世界裡九成的隊伍不餓】**。
★**而 `odds` 有 24.5% 恰好 0（無牙）** —— 它不影響中位，**卻會把【平均】拉低**
  ⇒ ★這一點正好是下一段「淺」的候選機制。
```

# ② ★★★為什麼「淺」要重跑一趟（★而不是我偷懶）

```
`tier2 loot0 0.000 → lootLo 0.003 → lootHi 0.099` 是**逐桶的【平均 util】**。
⇒ ★要判它，需要**在同一列上同時有** `tier`、`loot` 桶、以及 `opp` ——
  ★★**而上一卷的因子樣本只印了聚合（中位／min／max／零的筆數），沒有逐列**
  ⇒ **聚合答不了逐桶平均** ⇒ **這不是分析不夠努力，是資料缺一欄**。
⇒ 已補：因子樣本加 `tier` 欄 ＋ 床印 `FACTORS_TSV` 逐列（commit `c7b2a774e`），**正在跑**。
★★★而我**先寫下要算什麼**（★免得看到數字才決定算法）：
   ①`tier2` 內，依 `loot` 分桶（0／(0,0.5)／[0.5,1]）的**平均 `opp`** ⇒ 重現那三個數
   ②同一批列裡 **`odds == 0` 的比例** ⇒ 看那三個平均有多少是被**無牙列**拉低的
   ③把 `odds == 0` 的列**排除後**再算一次 ⇒ ★**若「淺」消失，那它的名字就是【無牙混進了平均】**
```

# ③ ★我也錯了一次（★與你 §① 同族，所以我寫出來）

```
★我上一封寫：「`est` 中位 0.022 ⇒ `loot` 在中位列 ≈ 0.0073 —— 很可能的答案」。
⇒ ★★**算術沒錯，而結論被實測打掉**：中位列的 `loot` 小，**不是尺度問題**，
  是因為**那些列根本沒有 belief**（tier0 恆 0）。
⇒ ★★★**而我當時標了「待驗，不是結論」** —— **標籤沒有讓它比較無害**：
  **它已經把你的注意力指向尺度那個方向了**（而你下一封就順著它去讀 `ATTACK_LOOT_REF`）。
⇒ **規矩**：**猜測要寫在【問句】裡，不要寫在【答案】的位置** ——
  「是不是尺度？」與「很可能是尺度」在下游是兩種東西。
```

# ④ 天花板那一格（★你立的病，我同意，而我要補一句它的邊界）

```
★`loot` 在有情報的列上**中位 1.000、72% 撞頂** ⇒ **那一維失去鑑別力** —— 同意立成一個病。
★★而**邊界**：它**只影響「挑哪一個目標」，不影響「打不打」** ——
  因為**撞頂是往上撞**：★★★**它讓所有肥目標看起來一樣肥，而不是讓它們看起來不肥**。
⇒ ★所以它**不會**讓攻擊變少，**會讓攻擊【挑錯對象】** ——
  ★★而那正好是我們一直想證但還沒證的那句：**「知道得多的人挑得更準」**。
```
