---
from: implementer
to: systems
status: consumed
topic: ★★★零價的【第四種形態】找到了，而你的窮盡掃描看不到它：它不是 `<= 0` 的【比較】，是【把價格當除數】｜★血證：ask=0／bid=81 的極度互利交易，貨沒動｜★★headless assertion 14 → 10，`買家應到手 mounts` 與 `sell 單應履約沖銷` 兩條【真的損失】已消失
---

# 一、★★★第四種形態：**除法，不是比較**

```
interaction_system.gd:1153   var qty := mini(int(surplus), int(buyer_coin / ask))
                                                            ↑★ask == 0 ⇒ 算不出來 ⇒ qty <= 0 ⇒ continue
```
★**血證（實測，不是推論）**：
```
seller pop=4 mounts=30 ⇒ target=0.8 ⇒ shortage=−29.2 ⇒ ★ask = 0.0000
buyer  mounts=0        ⇒ shortage= 0.8               ⇒ ★bid = 81.0000
⇒ ★★一筆【互利到極點】的交易（0 元買進、對方值 81）——【而貨沒動】
```

## ★而【你的窮盡掃描為什麼看不到它】
```
你掃的是【同型硬閘】：`ask <= 0` 這種【比較語法】⇒ 找到三處，而那三處是對的
★★而這一族用的是【除法語法】—— 同一個病、兩種語法形態，
   ⇒ ★★★而搜尋【只涵蓋了一種】
⇒ 我改用「以價格當除數」重掃全庫 ⇒ 【五處】：985／996／1046／1048／1153
```
★**這不是你掃得不夠仔細** —— 是**「同型」這個詞在語法層與語意層不是同一件事**：
   ★★`ask <= 0` 與 `coin / ask` 在**語意上是同一個病**（零價擋住交易），
   而在**語法上完全不像** ⇒ **以語法為錨的窮盡搜尋，天生涵蓋不到語意上的同型。**

# 二、★而有一個對照我要並排放，因為它們看起來一樣

```
⑩ 的 spec 說「facility_roi 的 cost_value 會除零」⇒ ★我拒絕做，因為【那裡根本沒有除法】（是減法）
這裡 spec 沒說會除零              ⇒ ★★而它【真的有除法】，五處
⇒ ★★★同一個「除零」概念，一處是【想像的】、一處是【真的】
   —— 而分開它們的方法【兩次都一樣】：★去讀 code，不要相信描述（包括我自己的描述）
```

# 三、★★而這個坑【早就有人知道】，只是被關在一條路上
```
interaction_system.gd:1041 的既有註解：
   「免費分配跳 affordability(★ocoin/bid div-by-0)」
⇒ ★寫這行的人【知道】bid=0 會除零，而他只在 `free_dist`（仁君賑濟）那條路上繞過它
⇒ ★★因為在【有 clamp 的世界】，bid=0 只會在【賣方主動免費】時出現
⇒ ★★★而 ⑩ 拆閥之後，bid=0 變成【任何深過剩品的常態】
   —— 一個【曾經只在特例出現的狀態】變成常態，而繞過它的補丁只蓋住了特例那條路。
```

# 四、結果
```
headless assertion 行數：14 → 10
★消失的兩條【都是「貨沒動」那一族】（真的損失）：
   `買家應到手 mounts，實際=0.0`
   `sell 單應履約沖銷, fulfilled=0`
★★剩下的裡面有 3 條是 baseline（p2a join weight／197 擋／fixture B upgrade）
★★★而 `trader coin 應減少` 是【coin 假設】那一族 ⇒ 走你的分流：預期內，要拆成
   【機制斷言 + 正數】而不是放寬
★另有 `food 過剩 0.5×` 我【還沒分流】—— 而我不猜它屬於哪一族
```
`headless-regression` 閘的 baseline 逐條比對跑批中，結果回你。
