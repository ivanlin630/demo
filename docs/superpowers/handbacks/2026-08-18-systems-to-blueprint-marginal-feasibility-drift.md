---
from: systems
to: blueprint
status: consumed
topic: "[邊際分配可行性評估(你要並行評)+★★發現農業a遺留drift·①可行性=YES:邊際分配可行、per-labor yield皆可算(gather=productivity×COLLECT_RATE resource:295、farm=farming_level×FARM_UNIT_YIELD×harvest 農業a);同資源內食物兩工位改按per-labor邊際產出分配(勞力流向報酬高者)、跨資源類留need-weight=你講的概念乾淨;computed live own-tile(self-knowledge非god-view、非MarginalEconomy的est-based那套belief primitive、但同邊際思路);fp intended-change(勞力分配變、farm發展好自然贏);moderate rebalance重構(labor_system食物工位分配段)·②★★發現農業a遺留drift(grounded file:line、影響你的決策):food_flow._sustainable_inflow:46-47仍算farming_bonus=1+farming_level×0.5並乘入inflow(MarginalEconomy._inflow_est鏡射它)=OLD gather-boost模型=正是農業a從物理移除的:289同公式→信念(估算器)-物理(farm_yield獨立線labor-starved 21%)DRIFT;估算器仍以為farming boost gather、物理已改獨立線;影響camp_marginal/facility_roi/runway過估farming價值+decision層farming投資ROI用舊模型;spec §3『ROI估算器不改物理跟上信念』意圖=但農業a讓物理DIVERGE非match(獨立線≠估算器gather-boost);★follow-up候選:更新_sustainable_inflow/farming_bonus建模新farm_yield物理or接受directional-consistency;且邊際方向落地=allocation用新物理、decision-estimator用舊=coherence須一併理·per-team measure照跑、數據+此drift一起餵你三分流決策·地基KEEP"
---
# 邊際分配可行性 + ★★農業a 遺留 drift
## ①可行性=YES
邊際分配**可行**：per-labor yield 皆可算（gather=`productivity×COLLECT_RATE` resource:295、farm=`farming_level×FARM_UNIT_YIELD×harvest` 農業a）。同資源內食物兩工位改按 per-labor 邊際產出分配（勞力流向報酬高者）、跨資源類留 need-weight=你講的概念乾淨。computed **live own-tile**（self-knowledge 非 god-view、非 MarginalEconomy est-based belief primitive、但同邊際思路）。fp intended-change。moderate rebalance 重構（labor_system 食物工位分配段）。
## ②★★發現農業a遺留 drift（grounded、影響你決策）
`food_flow._sustainable_inflow:46-47` **仍算 `farming_bonus=1+farming_level×0.5` 乘入 inflow**（MarginalEconomy._inflow_est 鏡射它）=**OLD gather-boost 模型**=正是農業a 從物理移除的 :289 同公式 → **信念(估算器)-物理(farm_yield 獨立線 labor-starved 21%)DRIFT**。估算器仍以為 farming boost gather、物理已改獨立線。
- **影響**：camp_marginal/facility_roi/runway **過估 farming 價值**（用舊 boost 模型）+ decision 層 farming 投資 ROI 用舊模型。
- spec §3「ROI 估算器不改物理跟上信念」意圖=但**農業a 讓物理 DIVERGE 非 match**（獨立線≠估算器 gather-boost）。
- **★follow-up 候選**：更新 `_sustainable_inflow`/farming_bonus 建模新 farm_yield 物理 or 接受 directional-consistency。且**邊際方向落地=allocation 用新物理、decision-estimator 用舊=coherence 須一併理**。
per-team measure 照跑、數據+此 drift 一起餵你三分流決策。地基 KEEP。
