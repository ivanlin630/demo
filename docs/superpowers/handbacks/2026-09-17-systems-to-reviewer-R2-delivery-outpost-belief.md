---
from: systems
to: reviewer
status: open
slice: 據點知識進 belief（`feat/outpost-belief` ＝ `b412e1417`）｜R② 交付審
topic: ★**交付審**：八格全綠，**兩個方向在真世界都量到了** —— 只有新版有 297（我走過那座城、沒見過主人）／只有舊 proxy 有 213（我沒走過、只見過主人）／兩者都有 100｜★★**你當初要我正面回答的「免建大 store」有數字了**：99 個觀察者、條目總和 438、峰值 13、均 4.42｜★★★**我最想被打的是一件他自己報上來的事**：他的床曾經「3-h 整格沒跑，而末行照樣印 `[FAIL] ＝ 0`」（GDScript 執行期錯誤**只中止那一支 func**）—— 他加了【到場點名】修掉；★**而我要問的是：這個洞【其他 229 張床全都有】嗎？**
---

# 一、數字（我認為這一組比「綠了」有價值得多）

```
迴避集不是變大或變小，是【換了一批】：
  只有新版有 = 297   ← 我走過那座城、而我對主人沒有位置情報（★舊版丟掉了）
  只有 proxy 有 = 213 ← 我沒走過、只是見過主人（★舊版憑空塞給我）
  兩者都有   = 100
⇒ ★★舊版 313 筆裡有 213 筆（68%）是它【不該知道】的
規模：觀察者 99｜條目 總和 438／峰值 13／均 4.42 ⇒ 當年「免建 team_outpost_known 大 store」的顧慮，答案是【不是問題】
```
★**3-b（你核過是活對照的那一格）在真世界有 297 筆** —— **那個「沒有人寫下來過的方向」不只存在，還是多數。**

# 二、★★★請你打的那一點（比本票大）

他自報：
> 第一次跑 3-h 時我寫 `m.payload = {...}` —— `MessageData` 沒有 `payload` 欄（是 `params`）。
> GDScript：`Invalid set index` ⇒ ★**執行期錯誤只中止那一支 func**，不中止整支床
> ⇒ 3-h 的三個斷言**一個都沒跑**，而末行照樣印 `-- 量測完成；[FAIL] 數 ＝ 0 --`

★**「沒有失敗」與「沒有執行」在畫面上一模一樣。** 他的修法是【到場點名】（每格自報到場，末尾對名單，少一格＝FAIL，現在印 `6／6`）。

★★**我要你判的是範圍，不是他的修法**：
```
Q1：這個形狀是不是【全庫 229 張床都有】？（★一支 func 內的執行期錯誤靜默中止該 func，而末行照印 0）
Q2：若是 ⇒ 我們現在的「[FAIL] 數 ＝ 0」這個 expect，★對【沒跑到的格】完全沒有鑑別力
Q3：★★★而處置【不可以是加一支新閘】（用戶 2026-09-10 明令）——
     我傾向：①把「到場點名」寫成新床的預設寫法（process doc 一行）
             ②舊床不回頭改，但**任何人動到一張床就順手加**
     ⇒ 請你打這個處置：它是不是又一個「靠人記得」的東西？
```
★**我自己的懷疑**：①②聽起來就像我今天批評過的「必報欄靠有人會去看」。
**若你有一個機械又不加閘的形狀，我要。**

# 三、其他（供你省時間）

- 3-c：全圖掃已刪、不再 live 讀 `tile.outpost_owner`／`outpost_level`、改讀**具名**的 `BeliefSystem.known_outposts(`
  ⇒ ★**那個名字正是 `defers.tsv` 裡三支同病灶 site 的機械判準**（我定名的用途在這裡兌現）。
- 3-d③：**城被拆、再看一眼 ⇒ 子記錄被擦掉**（1 → 0）★這格是我沒要求的，他自己補的。
- 3-e：`decision_context` **沒有**去碰新欄（blueprint 明令「欄開了 ≠ 每個想用的地方都該接」）。
- 3-g：既有四個讀者只走 key，37／37 走得到。
- `headless-regression`：`HARD-FAILS ＝ 3 ＝ baseline`，**失敗清單與 baseline 逐條相同**。
- 閘已註冊並用 runner 同一個 `grep -qE` 比對 ⇒ EXPECT-MATCH=YES。

# 四、排程
前票已 merge 進 main（`46b6e950d`）；後票（錨定性）merge 閘跑中；本票在你手上。
