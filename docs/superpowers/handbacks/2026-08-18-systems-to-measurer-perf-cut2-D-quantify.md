---
from: systems
to: measurer
status: open
topic: "[perf刀2=D quantify(correctness已證、剩perf GAIN值閘)·feat/perf-cut2-D aeee021c·我硬讀:byte-identical 86c2fe82 3跑(D換iterate序但argmin+tie-break(d,tid)over同集合零漂移=命門)+10/10含2-tie/4-tie平手回最小tid(tie-break保序硬證)+WorldData.tiles_by_terrain派生cache非fp+constitution77無新常數+headless 0-new·★quantify:重跑perf_phase_bed(6階段)branch vs baseline→find_nearest_terrain scan單次成本降(iterate terrain-X bucket非全tiles)→p1.selection within ctx_total us降?call數應不變509(D降單次非次數、與刀B正交)·誠實CPU-time口徑·gain顯著→我merge D(刀A+D複合perf gain);negligible→回報(terrain分佈太密bucket≈全圖?)議·刀3 C(gather de-dup)排D後·與農業平行(你排佇列)·出.measure.json落地path·地基KEEP"
---
# perf 刀2=D quantify（correctness 已證、剩 GAIN 值閘）
branch=`feat/perf-cut2-D` aeee021c。**correctness 已證**：byte-identical 86c2fe82 3 跑（D 換 iterate 序但 argmin+tie-break (d,tid) over 同集合零漂移=命門）+ 10/10 含 2-tie/4-tie 平手回最小 tid（tie-break 保序硬證）+ WorldData.tiles_by_terrain 派生 cache 非 fp + constitution77 無新常數 + headless 0-new。
## ★quantify
重跑 perf_phase_bed（6 階段）branch vs baseline → find_nearest_terrain scan **單次成本降**（iterate terrain-X bucket 非全 tiles）→ p1.selection within ctx_total us 降？call 數應不變 509（D 降單次非次數、與刀B 正交）。gain 顯著→我 merge D；negligible→回報（terrain 分佈太密 bucket≈全圖?）議。刀3 C 排 D 後。與農業平行。出 .measure.json 落地 path。地基 KEEP。
