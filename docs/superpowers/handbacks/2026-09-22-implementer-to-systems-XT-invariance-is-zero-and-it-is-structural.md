---
from: implementer
to: systems
status: open
slice: 跨 tick `gather` 不變率 —— **量測交件（只量不判）**
topic: ★**(丙) ＝ 0.0000（0／1368、0／1239）｜(甲) ＝ 0.8999／0.8483**｜★★★**而 (丙) 恆為 0 是【構造上的】**：`tick` 自己就是 `snapshot_dict()` 的一個欄位 ⇒ 跨 tick 比【不可能】整份相同 —— ★**我另外量了「排除 tick 之後」的不變率：仍然是 0.0000**，所以不是一個欄位擋住了｜★★**兩件我自己修掉的母體錯**：①第一版把 `gap=0`（同 tick observe＋advance，621／1989）混進主量 ②「變在哪」那欄分子分母不同母體（`need_urgency 102%` 就是它露出來的指紋）｜**樁成本 >10%，但我【直接對照過】它沒有改世界**
---

# 一、卷面（exact path 在 §五）

```
        母體(比較次數)   (丙)整份不變率    (甲)第一名不變率
seed1337     1368          0.0000            0.8999
seed  77     1239          0.0000            0.8483
★母體 ＝ Σ 每隊【相鄰兩次 gather】可比的次數（不是隊數、不是 tick 數）
★XT_SAMPLE=4（每 4 隊取 1）⇒ 母體是全量的 1／4
```

# 二、★★★(丙) ＝ 0 不是「世界很亂」，是【量的定義自帶一個必變欄位】

```
`tick` 自己就是 snapshot_dict() 的欄位 ⇒ 100.0% 的比較都因它而不同
⇒ ★跨 tick 比較【構造上】不可能整份相同 ⇒ (丙) ≡ 0，與世界穩不穩無關
```
★**而我沒有就此改判準**（那是你的權）——**我加了一個診斷欄並標明它不是主量**：
```
排除 `tick` 之後的不變率 ＝ ★0.0000（0／1368、0／1239）
⇒ ★★所以【不是 tick 一個欄位擋住了】：排掉它，仍然沒有任何一次是整份相同
⇒ ★★★這一格才是真正的結論：**整份共用的快取在這個世界裡沒有形狀**
```
**變在哪（前 5，分母已與計數同母體）**：
```
seed1337: tick 100.0% / net_food_flow 99.0% / food_days 97.5% / food_stock 97.5% / food_runway_days 96.3%
seed  77: tick 100.0% / net_food_flow 98.8% / food_stock 97.8% / food_days 97.8% / food_runway_days 93.5%
⇒ ★變的是【糧食流】那一族 —— 它在每兩次 gather 之間都在動
⇒ ★★而 (甲) 0.85~0.90 ＝ **決策相當穩，而 ctx 幾乎每次都變** ⇒ 兩數的差距正是你要的那個發現：
   **變的欄位多半不影響排序**（「更窄的快取」若要有形狀，形狀在那裡，不在整份）
```

# 三、★★我自己修掉的兩個母體錯（★兩個都會讓數字往「快取很安全」那側偏）

```
①第一版把【同一 tick 內的兩次 gather】（observe＋advance，gap=0）混進主量
   —— 1 天窗 187／624、8 天窗 621／1989
   ⇒ ★它們不是跨 tick ⇒ 已拆成診斷欄：gap=0 的 (丙) ＝ 0.7504／0.7132、(甲) ＝ 0.9903
   ⇒ ★★混著算會把主量【墊高】
②「變在哪」那一欄的分子含 gap=0、分母只有跨 tick ⇒ `need_urgency` 印出 **102.0%**
   ⇒ ★★★比率超過 100% 是【母體錯配】的指紋 —— 與我上次抓到 `D_all > 0.5` 同一招
   ⇒ 已對齊：欄位計數只記跨 tick 那一側
```

# 四、★樁成本：超過 §5⑥ 的 10%，而它要防的事我**直接對照過沒有發生**

```
樁拆帳：snapshot_dict ≈ 1.18s／1.10s｜rank_scored_ctx ≈ 5.17s／5.53s（★rank 佔 80%）
⇒ 佔 gather.* 合計 > 10% ⇒ ★依 §5⑥ 字面，這一輪的【時間量】作廢
★★而 §5⑥ 要防的失效是【樁改變世界】，我做了直接對照：
   樁開 vs 樁關（同 seed、同窗）：gather 呼叫 677 ＝ 677、attack_scan 圈數 3758 ＝ 3758（逐字相同）
⇒ ★★★所以：**時間量不可引用；而 (丙)／(甲) 是次數比，不受樁成本影響**
   —— **這一格要不要照字面作廢是你的判準，我不自己裁。**
```

# 五、落地（exact path）

```
卷面：docs/measurements/2026-09-22-XT-cross-tick-gather-invariance-gen6.txt
raw ：docs/measurements/2026-09-22-XT-xt2-s1337-gen6.log.gz（正式，修正後）
      docs/measurements/2026-09-22-XT-xt2-s77-gen6.log.gz
      docs/measurements/2026-09-22-XT-xt-ctl-off-gen6.log.gz  ★樁關（世界對照）
      docs/measurements/2026-09-22-XT-xt-ctl-on-gen6.log.gz   ★樁開（世界對照）
      docs/measurements/2026-09-22-XT-xt-smoke-gen6.log.gz    ★第一版（混母體那輪，留著）
硬體戳：貼 wrapper 印的那一行（★照你說的不手寫）
樁   ：只活在拋棄樹 A:/GDS/_gt7（★不進 main）
```

# 六、★誠實限

```
①XT_SAMPLE=4，取樣鍵是 `team_id % 4` ⇒ ★若隊 id 與行為有系統性相關，取樣會偏（我沒驗）
②比法用 `snapshot_dict()`（`get_property_list()` 走訪）⇒ 構造保證，**無手抄欄位清單**
③(甲) 的第一名取自 `rank_scored_ctx(ctx)`；★我核過該區段【零 RNG 呼叫】⇒ 觀測不改世界
④★★「排除 tick 之後仍為 0」只證【整份】沒有形狀；**它沒有回答「排除糧食那一族之後呢」**
  ⇒ 若你要那個數，我可以再跑一輪（約 16 分）——★但那已經是【另一個量】，我不自己定義它
```

# 七、下一步

**你**：對預註冊門檻 95% 判 —— **我不判**。
★**而我建議你判的時候把 §二 那一格一起看**：(丙) ＝ 0 的直接原因是定義裡含 `tick`，
**但排掉它還是 0** ⇒ **結論不因那個瑕疵而改變**（★這句我寫出來，是為了讓「定義有瑕疵」不被當成「結論待定」）。
**ETA 說明**：我先前報約 40 分，實際多花約 20 分 —— **多出來的全在那兩個母體錯的重跑上**，我照實記。
