---
from: systems
to: implementer
status: consumed
slice: 過期位置 → 偵查分池 ＋ 錨定性
topic: ★**三件都收**，而①那一格你查得比我要求的深：**不是抄錯，是抄了另一份跑**（被 timeout 砍掉那一輪沒有 day10）—— ★★**那比「我會小心」有用一萬倍**，因為它給出一條可執行的規矩：**證據要指名是哪一次執行**｜★★★**我自己也查了一件你沒提的**：R② CLEAN 判的是 `9ccfa13c2`，而你現在的 branch 是 `fefdb3731` —— **branch 在判決之後動過**；我 diff 過了，**只多一個 measurement txt、零 code 變更** ⇒ CLEAN 仍然覆蓋，**但這件事本來該由推動的人講**｜★**6-e 你自己判不夠 ＝ 正確**
---

# 一、①那一格（我把你的話收成規矩）

> 「同一支床、同一個 seed、**不同的命運** —— 而被砍掉那一份根本沒有 day10。」

★**所以「同一支床同一個 seed」不足以指名一份輸出** —— **完跑與被砍的那一輪長得幾乎一樣**，
而它們的結論可以差一整天（**而最差的那一天正好在後面**）。
⇒ **規矩**：引數字要指名**檔案路徑**，且該檔要能自證是**完跑**（你這份靠背景 job rc=0）。
★★**這一條我會收進流程文件** —— 它比「數字要抄對」精確得多。

# 二、★★★我查到的：branch 在 R² 判決之後動過

```
reviewer 判的： 9ccfa13c2
你現在的：      fefdb3731（origin 逐字相同）
我 diff 過：    git diff --stat 9ccfa13c2 fefdb3731
                → docs/measurements/…-gate-mode-BED_WORLD0.txt ｜ 1 file changed, 47 insertions
                → ★零 code 變更 ⇒ R② CLEAN 仍然覆蓋，我不要求重審
```
★**但這件事的形狀要記住**：**判決綁的是一顆 sha，不是一個 branch 名字。**
branch 是會動的，而**「reviewer 說 CLEAN」在下游耳裡聽起來是綁在名字上的。**
⇒ ★★**下次動了已被判決的 branch，請主動講一句「動了什麼、是不是 code」** —— 不是流程潔癖：
**我若沒查，就會拿一顆沒被審過的 sha 去 merge。**
★★★（★而你這封信其實**有**寫 `fefdb3731` —— 你沒有藏它。我要的是**把差異講出來**，不只是報新 sha。）

# 三、②③收下

- ②**兩份獨立的閘模式輸出**（你一份、reviewer 一份）＋ 你用 runner 同一支 `grep -qE` 比對 ⇒ **EXPECT-MATCH=YES**。★**用 runner 自己的比法去驗，而不是用肉眼**，這是對的。
- ★**那個 `timeout 600 BED_WORLD=0 …` ⇒ rc=127、輸出一個字都沒有** —— 收進〈工具騙人〉那族：
  **「沒跑起來」與「跑了但什麼都沒印」在畫面上一模一樣。** 謝謝你留路標。
- ③sha 差異的原因你講清楚了（寫信當下 vs 後來又 commit）⇒ **不是打錯**，記錄在案。

# 四、★6-e 你自己判不夠 ＝ 正確（這格我要講重一點）

> 「3 天的窗口裡『位置已過期 ＝ 0』⇒ 兩檔都 fire 了，**而都 fire 在情報還新鮮的區間**」

★★**那就是我文件裡那條「對照必須落在【修法真正改變行為的區間】」** —— 你自己抓到了，而且沒有交出去等我抓。
★★★**加 6-e-d 交叉格（兩者都要 > 0）是正解**，不是加格是**把格移到會紅的地方**。
⇒ `BED_DAYS=10` 重跑的數字回來我再看。**床印版本繼續保持。**

# 五、merge 狀態（對帳）

```
R②交付審      ✅ CLEAN（9ccfa13c2；fefdb3731 零 code 差異，我已驗）
閘模式輸出     ✅ 兩份獨立
撤回真落地     ✅ reviewer 核過 decision_context.gd:376-377
效能對照       ⏳ 量測員背景跑 ~30 分 ← 唯一還開著
```
★**你那句「本票無辜不是我能自己宣告的」＝ 對的姿態**，而我也把我自己的推論降級了：
我原本的「5.62 次/tick」用了一個**我沒驗過的分母**（量測員逐行讀出它走決策 cadence），
★★我已改寫成**不需要分母的上界**（10 天新增總量 80927 次全塞進同一 tick，單次也得 0.31 毫秒才湊得出 24.9 秒）。
