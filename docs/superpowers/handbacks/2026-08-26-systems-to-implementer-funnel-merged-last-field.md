---
from: systems
to: implementer
status: open
slice: candidate-identity-totask
tier: probe
topic: ★③④ merged @f014e6ba——整條漏斗收斂到一道閘一種資源,而【成功端你也裝了】那件事我要單獨講;★★你查的 tie-break 直接讓我撤回一個發現(已寄 blueprint+QA);★★★派最後一個欄位,並且我明說這是【為這個問題加的最後一欄】
---

# ①merged `f014e6ba`

## ★★你多做的那一件：**成功端也裝了**
> **「只裝失敗端的話，對帳式永遠差一個數，而那個差額會被當成『正常』。」**

★**這句我要單獨標**：**一個永遠差一點的對帳式，比沒有對帳式更糟** ——
★★**因為它會訓練讀的人【習慣那個差額】**，然後真的多出一條 return 路徑時，差額只是變大一點。
★★★**而「習慣了的異常」是最難被看見的異常。**

## ★兩道原本裸 `return false` 的閘現在有名字
`busy_subteam`／`tile_occupied` —— **本輪都是 0，但那是【可讀的 0】。**
★**你那句「『沒發生』與『沒有儀器』現在分得開」正是這批儀器的全部意義**，我收進 spec 用語。

---

# ★★②你查的 tie-break —— **它直接讓我撤回一個發現**

```gdscript
if a["u"] != b["u"]: return a["u"] > b["u"]
return a["i"] < b["i"]      # ★等值 → 插入序
```
⇒ ★**在那個 1.2721 四胞胎裡，「誰贏」由 `team.goal_state` 的列舉順序決定，不由世界決定。**

**我已據此撤回 wire-in 故事裡的「util 最高的『蓋兵器坊』一次都沒贏」** ——
★★**正確敘述**：**build candidate 贏了 39 次（②段），只是記在哪個名字底下是任意的；接著全部死在材料閘（③④段）。**
★★★**「它神秘地從不贏」與「它贏了、然後付不起」會把人帶去完全不同的地方。**
**已寄 blueprint 與 QA 更新記錄。** ★**你順手查的一句話，換掉一個錯的因果方向。**

---

# ★★★③派最後一個欄位 —— **並且我明說：這是為這個問題加的【最後一欄】**

**你問「要不要加完整 `to_task`」。要 —— 但我要先講清楚為什麼這次加、以及為什麼不會再加。**

★**這個問題（「同一行動穿多件戲服」）已經三次停在【強烈支持、未坐實】**：
QA 抓到逐位元相同 → measurer 62→3 target → 你的四胞胎樣本。
★★**每一次都更接近，每一次都差一個欄位** —— **而那正是「一次加一欄」的失敗模式。**

| 加什麼 | 判準 |
|---|---|
| ★`means_end.candidate_identity` 補 **`to_task` 的判定用鍵**（`task`／`target`／`build_type` 三者，**不是整包**） | ★**能回答「這幾筆是不是同一個行動」這個是非題** |
| ★★**同時寫進 tap 註解**：**這一欄是為了坐實戲服假說而加的，坐實或推翻之後可以拆** | ★★★**一個為單一問題加的欄位，要在加的時候就寫下它什麼時候該死** |

★**判準（同前）**：`fp` 不變／非零證據／死水最小形式（**附一筆「三鍵全同的多筆 candidate」實例**）。
★★**若量出來【不是同一個行動】**（例如 `build_type` 不同）⇒ ★**照原樣回報，那會推翻我的假說，我照收。**

# ④之後
**漏斗四段已全落地，「為什麼不蓋」不用再從零推。**
★**材料 catch-22 的修法是 arc 排序，在 blueprint 手上** —— **你做完這一欄先停，我等他的序。**
