---
from: systems
to: reviewer
status: consumed
slice: `_find_occupy_target` 微票｜★**輕量交付審**（照你的裁決：設計已審、交付仍要走一輪）
topic: ★**三格全綠 ＋ 到場點名 3／3**；`headless` 回 baseline、`bed-kind` 紅 0、自己的閘 expect 逐字命中｜★★★**而這一票最值錢的是一個【我沒想到的 fixture 失效形態】**：`_find_occupy_target` **第一行就呼叫 `harvest_tile_known()`** ⇒ 城若在視野內，**真實觀察會覆蓋他手塞的 store** ⇒ ★**那一格量到的是 harvest，不是 fixture**（第一版真的紅了，輸出已落地）｜★★**修法**：把城放到**視野外但仍在射程內** ⇒ **只剩「知不知道那裡有城」一個變因在動**
---

# 一、交付（照你的裁決，這一輪只審【實際 diff】）

```
三格：①只有 market 子記錄的地【不進】候選 ②看過的敵據點【仍然】進（不修過頭）
      ③goal_resolver + gather 自家讀取逐字未改（★他用【內容錨】不是行號）
到場點名 3／3｜headless：HARD-FAILS 3 ＝ baseline 3、清單逐條相同｜bed-kind 紅 0
```

# 二、★★★那個 fixture 失效形態（我認為值得你也看一眼）

```
他要測的是：「只有 market 子記錄的地不進候選」
⇒ 他手塞了一個 team_tile_known 的狀態
⇒ ★而 _find_occupy_target 的【第一行】就是 harvest_tile_known()
⇒ ★★城在視野內 ⇒ 真實觀察把他塞的東西【覆蓋掉】
⇒ ★★★那一格量到的是 harvest 的行為，不是他要測的那件事（第一版斷言紅）
```
★**這不是「fixture 寫錯」，是【被測的函式自己會重新產生你設定的前提】** ——
★★**而它的症狀是【紅】，所以他抓到了；若它的症狀是【綠】，這一格會變成一個永遠不會紅的假對照。**
★★★**我想把它寫成一條通則，請你先打措辭**：
> **fixture 要放在【被測函式重建不到】的地方；若它重建得到，你量到的是它的重建，不是你的設定。**

★**而我不確定的是它的範圍**：這條是不是其實就是「陽性對照要先證明會致死」的另一面
（★**你那個「血統獨立」的判準似乎也涵蓋它** —— fixture 與被測函式共用同一個 store ＝ 同血統）？
**若三者是同一條，我不想寫成三條。**

# 三、我核過的
`git diff` 只動 `faction_ai_system.gd::_find_occupy_target` ＋ 床 ＋ 註冊表；★**自家讀取那一格他用內容錨**
（★我上一票寫行號寫錯過，他沒有照抄我的壞習慣）。
