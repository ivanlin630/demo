---
from: systems
to: implementer
status: consumed
slice: 效能線 ｜ 32.6 ms 的**純計算**
topic: ★收：**是計算不是查表**（查表 37.8 us ＝ 0.12%／身體 32565.8 us ＝ 99.88%，守恆差 **0.000 s**）⇒ 那條「換字典、語意天然中性」的路**回收上限 0.12%，不做** —— **第五個被排除的假說**｜★★下一輪兩件：①**把身體切開**（切點由你定）②★★★**量【同一 tick 內、同一組輸入】重複算幾次** —— 而 §③ 有一條硬要求：**比率前面先擋母體**（上一輪的 `1−0/0` 就是這樣變成「100%」的）
---

# ① 先確認一件事：這一輪的排除**很乾淨**

```
★守恆 ①＋② vs facility 段：**差 0.000 s** ⇒ ★★那不是「差不多」，是**沒有第三塊**
  ⇒ ★★★所以「查表 0.12%」這個結論**不留餘地**：不必再回頭懷疑它。
⇒ 而它是這條線上**第五個被排除的假說**（叫太多次／term 太貴／儀器是主因／path 太多／查表）。
```

# ② ★而下一輪我要的第二件，是**冗餘**（★不是提名，是結構問題）

```
`_resolve_build_facility` 被呼叫 **1116 次**（本窗），而它是**純計算**。
⇒ ★問：**同一個 tick 內，同一支隊、同一組輸入，它被算了幾次？**
  ★★（例如：不同 goal ／ 不同 prereq 走進來，而最後問的是**同一件事**）
⇒ ★★★而這是**結構問題不是提名**：我沒有說「一定有冗餘」——
  ★若沒有冗餘，這個數字會直接告訴我們，而那又是一個**便宜的排除**。
```

# ③ ★★★硬要求（★上一輪的血就在這）

```
**任何比率的算式前面先擋母體** —— 母體 0 ⇒ 印「不可判」，**不准印數字**。
⇒ ★上一輪 `1 − 0/0` 印出「重複率 **100%**」，而 100% 正好是
  ★★**會讓人立刻跑去做那個快取**的那個值。
⇒ 本輪的「重複率」若母體不足，**就寫不可判** —— ★★★而那**不是失敗**，
  它與「量到 0% 重複」一樣，都會**排除掉一條路**。
```

# ④ 驗收

```
①身體切段：各段 us／次數／單價（★單價量、不准除）；★★母體地板逐段
②★重複率：**相異輸入數 ／ 總呼叫數**，兩個數字**都要印**（★★不要只印比率 ——
  ★★★有原始的兩個數，下一個人才驗得動你的比率）
③守恆：切段相加 ≈ 32565.8 us／次（★差額要能解釋）
④fp 不變＋零 RNG；★誠實限帶著「這一段儀器 14.7 us／次」
```

# ⑤ ★而我要先講清楚【拿到數字之後我會怎麼裁】（★★免得數字回來我臨場決定）

```
★若**有大量重複** ⇒ 方向是**同 tick memo**，★★而它**不是自動語意中性的**：
  要先回答「同一 tick 內，這個計算依賴的東西會不會變」——★★★而那正是我上一輪
  寫過的那個陷阱（資源在同 tick 內會被採空），**這次要先答，再做**。
★若**幾乎不重複** ⇒ 方向是**讓單次變便宜** ⇒ 那時才需要知道它**在算什麼**
  ⇒ ★而那一刻我會讀 code —— **但只用來理解，不用來提名成本位置**（我今天在這件事上錯過兩次）。
```
