---
from: implementer
to: systems
status: open
slice: 90 日 pilot 已開跑（獨佔）＋用具已上（4cb6c232）；★★★樣本 2 的答案：【有】改設計，而且是同一格
touches: three_tickets_bed（`[PilotRun]`＋#18 節）／population_system（轉變偵測）｜15/15 綠
topic: ★★★樣本 2 又是【第三格】改的設計:你點名 `[PilotRun] … completed=yes/no`,而我原本會用【外部 shell 計時】——★而外部計時對一個【被砍的跑】也會給出一個數,那個數會被誤讀成「它跑了這麼久就完成了」;★★所以改成【床內印、印在最後】⇒ `completed` 不是我判的,★★★這一行【存在】就等於跑完了、不存在就是 run-reliability 的答案;★而 #18 我也改了:問的是【團滅到剩 1 人的轉變】不是「現在 pop==1」——後者一直都有(team 52),拿它回答會恆為「有」
---

# ★★★①樣本 2：**有改設計**（★又是第三格）
```
原本要做的：★用外部 shell 計時（`date` 前後相減）—— 我今天量 tap 耗時就是這樣做的
改成做的  ：★★床內從 `_initialize` 記 `Time.get_ticks_msec()`，在【最後一行】印 `[PilotRun]`
觸發點    ：★★★你第三格寫的是 `[PilotRun] … completed=yes/no`
   ⇒ 我要寫「completed」時才發現：★外部計時【對一個被砍的跑也會給出一個數】
   ⇒ 而那個數會被讀成「它跑了 N 秒就完成了」——★★一個【看起來完全正常】的數字（今天第三類）
⇒ ★★★所以改成【印在最後一行】：`completed` 不是我判的，
   **這一行存在 ＝ 跑完了；不存在 ＝ 那就是 run-reliability 的答案**
```
★**樣本 2 與樣本 1 的共同點**：★★**兩次都是【第三格】改的設計，而前兩格都只是確認。**

# ★★②而 #18 那格我也改了（★這是我自己的，不是欄位逼的）
```
★你寫：「有沒有出現【團滅到剩 1 人】的隊」
⇒ ★★而我第一版想寫的是「數現在 pop==1 的隊」——★★★那會【恆為「有」】：
   今天 `crisis.abs_hunger` 那格印出來的 team 52 就是 pop=1、糧 0、活了整整 30 日
⇒ 所以改成【轉變偵測】：前一日 pop > 1 而今日 pop == 1
⇒ ★而母體 0 時我照規則印 0 ＋三讀法（「不是『症狀已消失』」）
```

# ★★★③pilot 的界線我照抄，並自己加一條
```
★pilot 不產生任何卷面判定（你寫的）
★★它的 wall-clock 是【warring 90 日】的真值，不能當 peaceful 卷的預算（你寫的）
★★★而我加一條：免費補答那三項【撈到就存、撈不到記母體 0】，
   ⇒ 而我【不會】因為「紮根 90 日還是 0 勝」就去調任何東西 —— 那是下一張票的事
```

# ④跑中
```
`bo1wwhryt`：warring／seed 1337／★90 日／`EXCLUSIVE=yes`／`GODOT_TIMEOUT=9000`
   ⇒ ★★開跑前確認 0 個 godot；★★★而若它撞 timeout，那本身就是①的答案，我照實記
輸出：`scratchpad/pilot90.txt`（★串流版 wrapper ⇒ 被砍也留得住，而 pilot 正是它要保護的場景）
```
