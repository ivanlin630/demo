---
from: implementer
to: systems
status: open
slice: JOIN 第二步【有數字了】—— ★而最刺眼的不是判別量，是【距離分布只有兩個值】
touches: `feat/old-growth-forest`（`join.meet_target`／`meet_other` ＋ 移動層逐日距離）
topic: ★★★判別量:`meet_target`=**1**｜`meet_other`=**6**｜對帳 1+6=7 == `dispatch` ✅ ⇒ ★七次相遇裡【只有一次遇到目標】;★★而移動層更刺眼:母體 55（每日×JOIN 隊）,`same`=**32**（58%）,而【距離分布只有兩個值】—— **00=16｜14=39**,★★★中間 1–13 格【一個樣本都沒有】;★所以它不是「慢慢走但走不到」,是【要嘛已經在目的地、要嘛離 14 格不動】—— 我照原樣報,不歸類
---

# ★①判別量（★對帳過）
```
`join.meet_target` = **1** ｜ `join.meet_other` = **6**
★對帳：1 + 6 = 7 == `join.dispatch` = 7 ✅（互斥且窮盡）
⇒ ★★七次「JOIN 隊出現在接觸對」裡，★★★【只有一次】對方真的是它的 `social_target`
⇒ 其餘六次是【遇到不相干的隊】—— 而那六次不觸發 handler 是【正確行為】
```
★**而那一次 `meet_target` 之後 `join.resolve` 仍然 = 0**
⇒ ★★所以你的第三分支（`meet_target > 0` 而 `resolve = 0` ⇒ 病在 resolver 條件）★★★**有 1 個樣本**
   ⇒ 而 **n=1 不足以判**，我標成【指向，不是結論】

# ★★★②而移動層更刺眼：**距離分布只有兩個值**
```
母體（每日 × JOIN 隊）= **55**
first=4 ｜ closer=**9** ｜ same=**32** ｜ farther=**10**  ★對帳 4+9+32+10=55 ✅
★★距離分布（離自己的 `move_target` 幾格）：**00 = 16 ｜ 14 = 39**
   ⇒ ★★★中間 **1–13 格一個樣本都沒有**
```
⇒ ★**所以它不是「慢慢走但走不到」**（那會看到 13、12、11… 的中間值）
⇒ ★★形狀是【要嘛已經站在目的地（16 次），要嘛離 14 格（39 次）】
⇒ ★★★而 `same`=32／55 ＝ **58%** 的 JOIN 隊-日【距離完全沒變】
★**我不歸類**，但我把兩個可能寫出來讓你選下一刀（★而它們的下一步不同）：
```
①`move_target` 每次被【重設】⇒ 距離在 0 與 14 之間跳，而隊其實沒走
②隊真的【走不動】而 14 是初始距離 ⇒ 那 16 次 dist=0 是【另一批隊】（已在目的地）
⇒ ★而分辨它們只要一個量：★★【同一隊】的距離序列 —— 我目前是【聚合】不是【逐隊】
```

# ★③而 `dist=0` 那 16 次是本封最該追的一格
```
★有 16 個 JOIN 隊-日【站在自己的 `move_target` 上】
★★而同期 `meet_target` 只有 **1** 次
⇒ ★★★「站在目的地」與「遇到目標」之間差了 15 次 —— 而那段【沒有任何 counter】
⇒ ★可能的意思：★★到了目的地而【目標不在那裡】（belief 過期／目標移動了）
   ⇒ 而那正是 `join.abort_ghost`（撲空）該抓的，★★★但它 = **0**
   ⇒ 所以「到了而撲空」目前【既沒被 abort_ghost 抓到，也沒被任何站記下】
```

# ④我做了什麼／沒做什麼
```
★做了：兩組判別量都【互斥且窮盡＋印對帳行】（1+6=7 ✅／4+9+32+10=55 ✅）
   ⇒ 在 `feat/old-growth-forest`，★★不動考程樹；`constitution_gate` PASS；`SCRIPT ERROR` 0
★★沒做：①沒加【逐隊距離序列】（那是分辨上面兩個可能的量，而母體怎麼切是你的設計）
   ②沒查 `dist=0` 那 16 次到底發生什麼（★需要逐隊 trace，而我不自己擴大範圍）
★★★而移動層我用隊自己的 `move_target` 不是目標隊的真實位置：
   ★後者是 god-view，而它會把【目標自己跑掉】與【我走不動】混成同一個數字
```
