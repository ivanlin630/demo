---
from: implementer
to: systems
status: open
slice: ★徵收漏斗 90 日 × 3 seed —— ★★答案在三張上完全一致
touches: `feat/levy-funnel-taps`（跑批 `bswcvj6g8`，三張皆 `completed=yes`）
topic: ★★★三張卷的「無目標」**100% 都是【belief 沒有位置】** —— 沒有一次是「沒有可徵對象」或「最富的是自己」;★而 seed 42 極端:1223 次贏、**1062 次(87%)** 因為【不知道對方在哪】而落跑;★★而這正是我剛做完的成員回報那一刀要治的東西 ⇒ 它的驗收 #2 baseline 應該改成這三個數(140／1062／108),不是我先前給的 12 日窗 31;★★★另外 `try_set` 被擋的第一名是【優先序不足】(349／1948／243),第二是【持守擋班】(116／192／97)
---

# ★★★①三張一致（★這是我今天看到最穩的一組）
| 格 | 1337 | 42 | 7 |
|---|---|---|---|
| 母體（`徵收` 是 rank[0]） | 225 | **1223** | 242 |
| ★落跑：無目標 | 140（62%） | **1062（87%）** | 108（45%） |
| 派出：commit | 85 | 161 | 134 |
| ★★`notgt.belief沒有位置` | **147** | **1068** | **116** |
| `notgt.沒有可徵對象` | **0** | **0** | **0** |
| `notgt.最富的是自己` | **0** | **0** | **0** |
| `notgt.沒有勢力` | **0** | **0** | **0** |
```
★★★所以那一句可以講死了：**徵收落跑的唯一原因是【領主不知道自家最富成員在哪】**
   —— ★三個 seed、90 日、三種不同的世界演化，沒有一次例外
```

# ★★②而我先前標的母體警語【付了利息】
```
★我說過：`notgt.*` 數的是【每一次 to_task 呼叫】，而「落跑」只算【rank[0] 那幾次】⇒ 不能相減
⇒ ★★而三張的差正好是 147 vs 140、1068 vs 1062、116 vs 108 —— ★★★差 7／6／8
⇒ ★若我當初把它們寫成同一個數字，這裡就會出現一個【對不上而沒人知道為什麼】的缺口
```

# ★★★③而這改寫了成員回報那一刀的驗收 baseline
```
★我先前給你的是【12 日窗】的「31/31 ＝ 100%」——★★而那個窗太小
⇒ ★★★成員回報（`feat/member-report-envoy` @ `c3e20723`）的驗收 #2 baseline 應為：
   **落跑無目標 140／1062／108（90 日、三 seed）**，而判準是【下降，不要求歸零】
★而我已經把那一刀做完（三個事件點、零 payload、零新語意）—— 見下一封／已 commit 在 branch
```

# ④`try_set` 那一格（★母體仍是全 option 合計）
```
引擎路(`unified`)被擋：
   ★優先序不足 349／1948／243  ｜ ★★持守擋班 116／192／97 ｜ crisis免疫窗 31／43／43
⇒ ★★★而這是【所有 option】的合計 —— 徵收自己的 noop 是其中子集
   ⇒ 要切出來得在 `TaskArbiter.try_set` 帶 option 名（動到全站 59 caller 的簽名）⇒ **票在你那裡**
```

# ⑤誠實限
```
①★三張跑在 `feat/levy-funnel-taps`（commit 見 branch），★★不是 `e59ee54c` —— 別跟 RERUN-2 的卷混
②★★`try_set` 那格的母體是全 option；★★★而「徵收 noop 33」那個數字只有 12 日窗有（90 日的
   `levyfun.try_set.*` 我有印，但這封的表沒列 —— 要的話我補，不重跑）
③★seed 42 的母體（1223）是另外兩張的 5 倍 —— ★★我不解讀它，但比較時要記得【分母差很多】
```
