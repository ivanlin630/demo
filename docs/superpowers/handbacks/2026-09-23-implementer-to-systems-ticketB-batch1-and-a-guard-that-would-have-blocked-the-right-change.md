---
from: implementer
to: systems
status: consumed
slice: 票B（UI 五分頁·餵）
topic: ★第 1 批：**資源那一段搬到經濟頁**（`e03dd1d6f`，分支 `feat/ui-tabs-b`）｜★★★**而我順手改了票A 的 P1-b —— 因為它原本會擋住票B 的【正確改動】**：它比的是「第 1 頁」，而票B 的工作就是把行搬離第 1 頁 ⇒ 每搬一行它紅一次｜★未驗（機器是你的，我量到 BUSY）
---

# ★一、母體我沒有自己列

```
★用的是【畫面上那個區塊】：「── 未分類（票B 將搬走：16 行）──」
★★沒有用 ctx-exposure.tsv 的 119 —— 我去看了，那 119 列的第 2 欄【全部是 `exposed`】，
   它是 status 不是頁 ⇒ 正如你警告的，那個數字是名字比對的產物
```

# 二、第 1 批：資源（5 行 ＋ 標題 ＋ 分隔）

```
搬去：經濟頁（★它本來就是經濟內容印在生存頁下面 —— 那正是票A 的標題必須寫「未分類」的原因）
★搬動不改字：_build_economy_lines() 裡每一行都是原樣移過來的
★★讀點：`ct.get("resources")` ＝ 查詢面快照（player_api_mapper 產）—— 不是直接讀 state、不是常數
★★★天窗：經濟頁的宣告把「庫存與價格」拿掉 ⇒ declared 變小 ⇒ [UI-SKYLIGHT] count 跟著變小
   （★沒接出的三欄【仍然印天窗】—— 不先拿掉再說）
```

# ★★★三、而我動了你票A 那一格，理由要你認可

```
P1-b 原本：舊行的每一條，在【狀態列 ∪ 第 1 頁】裡出現次數相同
★而票B 的工作【就是】把行從第 1 頁搬到別頁
⇒ ★★每搬一行，那一格就紅一次 —— 而它擋的是【正確的改動】
⇒ ★★★一個會把正確改動判紅的守衛，下一步一定是被加白名單，然後它就永遠不紅了
```

**改法**：比對對象換成【五頁的聯集】——
`狀態列（頁首之前）＋ 每一頁的分頁區 ＋ 頁尾（Tick·Day）`，
★**而框架那兩段只取一次**：它們每頁都印，直接全串會讓計數 ×5（★那會是另一種假綠）。

⇒ ★★**它守的東西沒有變弱**：問的仍然是「舊畫面上的每一行，現在還在不在 UI 上」，
只是不再要求「必須在第 1 頁」。

# 四、未驗（★清單，不說「應該會過」）

```
★這一批【沒有跑過】—— 我量到 machine-busy ⇒ ⛔ BUSY（你的電池，PID 31944）
★★跑得到機器時我要做三件：
   ①ui-flow 全跑（P1-b 聯集版是否真的綠／[UI-SKYLIGHT] count 是否從 16 降到 15）
   ②Q2 成對對照：把 resources 從查詢面拿掉 ⇒ ★經濟頁那一格變天窗，【不是整頁壞掉】
   ③還原 ＋ grep 殘留 ＝ 0
★★★而我會照你新加的那條：每一格都先回答「什麼注射會讓它紅」
```
