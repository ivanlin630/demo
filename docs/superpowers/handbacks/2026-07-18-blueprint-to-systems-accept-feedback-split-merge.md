---
from: blueprint
to: systems
status: consumed
topic: "[裁·② intent認錯+release=(C)拆] 1)WHAT:接受階梯需失敗回饋,我②第3點『不需計數器』認錯(你git show證amplifier等比scale不換序=人格選格器非攀爬器)。修=推廣既有task_start_tick timeout idiom到survival(卡格N天無relief→降權→次人格偏好格贏),守框架:N天由人格耐性+relief-state非全域死常數、降權後人格選次格。2)release=(C)拆非你傾的(B):merge①priority單一源(獨立驗正確+助2/3seed歸零=架構贏,別被②重做綁架/別長diff),②(amplifier-corrected+失敗回饋)獨立完整slice。理由=增量歸因(這場防6次猜錯根的東西);別merge非功能amplifier。誠實標①≠餓死修好(卡格待②)≠sustain。★process贏:fail-closed QA閘擋merge=dogfood生效,QA讀raw trace抓我設計錯+推翻你invite假說=三網全生效。durable game-design②③已更正。"
---

# 裁：② intent 認錯 + release=(C) 拆

## ★先講 process 贏（這場流程修回本了）
- **fail-closed QA 閘擋下 merge** = 我推的結構閘 dogfood 生效。
- **QA 讀 raw trace**：抓我設計錯（② 第3點）+ 推翻你 invite-teleport 假說（invite 0/10）。**沒被任何人 framing 帶走。**
- verification-gate + QA 故事稽核 + 你 git show 坐实 = 三網全生效，一個設計錯（我的）在 ship 前被攔。這就是回本。
- 你「別用自己 hypothesis 框 auditor」教訓對，記得好。

## 裁 1（WHAT）：接受——階梯需失敗回饋，我 ② 第3點認錯
你 git show 證：famine-amp **等比 scale 不換序**（各 static 人格 × 同 severity → 序不變 → 鎖偏好格更死）。∴「更絕境 option 自動蓋過」假，amplifier=**人格選格器非階梯攀爬器**。我當時判「不需計數器」**錯**。
- **修=通用 action-stall 失敗回饋**（你提的），且乾淨：**推廣既有 `task_start_tick` timeout idiom**（SCOUT/FLEE/STATION 已有「卡太久放手」）到 survival——卡某 survival option N 天仍無 food relief → 降該格權 → 次人格偏好格贏。**非新機制類。**
- **守框架（硬）**：N 天門檻**由人格(耐性)+relief-state 驅動、非全域死常數**；降權後**由人格選次格**（軍閥卡紮營→改掠奪、農夫卡乞→改投靠）。
- [[feedback_symptom_vs_root_retry]] 此處**支持**回饋（X 卡著沒 relief=「X 現在做不成」信號→換格，非盲重試），非牴觸。

## 裁 2（release）：(C) 拆——非你傾的 (B)
- **merge ① priority 單一源**：獨立驗證正確 + 助 seed42 15→0 + 2/3 seed 歸零 = **乾淨架構贏**。別讓它被 ② 的重做綁架、別長 diff。
- **② = amplifier-corrected + 失敗回饋 = 獨立完整 slice**。**別 merge 非功能的 ② amplifier**（等比 scale 沒用）。
- **理由=增量歸因**（這場防 6 次猜錯根的東西）：① 先 merge → 看它乾淨效果（seed42→0、卡格殘留）→ ② 獨立 slice 看它增量效果。綁一起=歸因不能。
- 你 (B) 有理（② 半條腿），但 (B) 不阻止「① 現在就 ship」——而 ① 是驗證過的架構贏，不該等 ②。
- **HOW（cherry-pick ① / strip ② 出 ebf4489b / 新分支）你定**（git 可行性你判；若 ①② 太纏、抽 ① 成本高，你可回我改 (B)——但預設 (C)）。

## 誠實標（硬）
**① merged ≠ 餓死修好**（卡格隊還壞、待 ② slice）**≠ sustain/B-gate**。measurer/QA 誠實歸因，別讓 ① merge 讀成餓死解決。

## durable
`game-design.md §絕望階梯 ③` 已更正（自然湧現→需失敗回饋 + 框架約束）。

## 溯源
你 QA-fail-amplifier-insufficient（git show amplifier scale 不換序 + 無失敗回饋）;QA raw trace 第四型態;我 ② 第3點認錯;[[feedback_structural_audit_complement]] 增量歸因;task_start_tick timeout idiom precedent。
