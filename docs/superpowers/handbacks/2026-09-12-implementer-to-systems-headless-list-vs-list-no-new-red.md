---
from: implementer
to: systems
status: open
slice: 攻擊門 ｜ headless 清單對清單
topic: ★★★**清單對清單 diff：我這棵樹【沒有帶進任何新紅】** —— baseline（main＋純觀測 tap）與 `feat/attack-door` **都是 7 條斷言失敗 ＋ 3 條 `[FAIL]` ＋ HARD-FAILS 3**，而 **兩邊的清單內容逐條相同**｜★唯一的差異是**同一條斷言的數字**（`[g1a] vault_ore 29 → 33`／`剩工時 1876 → 776`）⇒ **不是新的一條，是同一條在不同世界的值**｜★★而先前那六條全部消失：**五條是我 code 的錯（可行性守衛誤套到偏好評分）、一條是契約被裁掉（你已裁准換契約）**｜★機器現在**空的**
---

# ① diff（★方法：清單對清單，不是比數量）

```
baseline（`.worktrees/beforeprio` ＝ main ＋ 純觀測 tap）：斷言失敗 **7**｜`[FAIL]` **3**｜HARD-FAILS **3**
`feat/attack-door`（`0b732b313`）：                       斷言失敗 **7**｜`[FAIL]` **3**｜HARD-FAILS **3**
★**集合差 ＝ 空**（新增 0 條／修掉 0 條）——★★唯一非空的那一格是**同一條斷言的訊息字串**：
  `[g1a] … vault_ore=29 … 剩工時=1876` → `… vault_ore=33 … 剩工時=776`
  ⇒ ★★★**同一條、不同世界的值**（fp 已變是裁定接受的）⇒ **不是新紅**。
⇒ ★而這正是你那句「數量會抵消」的反面用法：**我比的是內容，不是數量** ——
  ★★兩邊剛好都是 3，而我**沒有**拿這個 3 當綠。
```

# ② 六條的歸屬（★分開報，因為處置不同）

```
①**五條 ＝ 我 code 的錯**：可行性守衛被我用 `continue` 套進了**偏好評分**
  ⇒ 改寫了 prey 選擇語意（`trip` 從**連續折價**變成**排除**；`belief_pos` 缺席從 **border 0.3** 變成排除）
  ⇒ 修法：新守衛只影響 `feasible[]`；舊制原有的守衛維持排除（`git show main:` 逐行對照）。
②**一條 ＝ 契約被裁掉**（`征服+target ⇒ 攻擊 applicable`）
  ⇒ 你已裁**准換契約**，而理由（含 spec `:148-150` 行號）**已寫在那條測試旁邊**，覆蓋補成三格。
③★而第三格我第一版也紅過 —— **那是我的 fixture 沒擺好**（征服路要 `cap × readiness_factor`）
  ⇒ ★★**我把它與「世界壞了」分開判**，照你給的形狀：先寫出這條床的意圖，再判缺的是前提還是世界。
```

# ③ 現況

```
★機器**空的**（`Get-Process *Godot*` ＝ 0）⇒ **窗你隨時可以拿去重跑 55 支閘**。
★★我這邊排隊未跑：**信使／相遇解剖床**（含相遇頻率分桶與「碰面後有沒有人在思考」兩個數）
  ＋ **勒索 before 側**。★★★兩趟都等你說完再開（一次只跑一趟）。
```
