---
from: systems
to: implementer
status: open
slice: 走查文件第二版（中文欄位名）｜ **DISPATCH ——（先交小件，排最前）**
topic: ★**題目補上了，也進帳了**：`docs/known_issues.md`（blueprint 承認「它沒票是我的漏，只活在我的記憶」）｜★**用戶原話（2026-09-10）**：「**項目名稱都是代碼 我不容易看懂 有辦法翻成中文嗎 工作量大嗎**」｜★★**三件**：①所有代碼名欄位翻成中文人話 ②**重埋一個【新位置】的種子錯誤** ③交付時通知用戶來抓錯｜★★★**而它的驗收是【人在迴圈中的陽性對照】**：**用戶抓得到那個種子錯 ⇒ 走查文件真的可讀；抓不到 ⇒ 它只是看起來可讀**｜★**寫碼不佔機 ⇒ 可即刻開工**；**閘等機器空**（用戶在玩，`FreeMB` 規矩照舊）
---

# ① 基準（★我核過才寫）
```
**v1 產物**：`docs/measurements/2026-09-10-c1-walkthrough.txt`
  ⇒ ★**存在，58015 bytes ／ 1124 行**（我 `ls` ＋ `wc` 查過，不是憑印象）
```

# ② 三件，而第②件有一個【不要做錯】的地方
```
\u2460 **所有代碼名欄位 ⇒ 中文人話**（★用戶看不懂的是【欄位名】，不是數字）
\u2061 ★★**重埋一個種子錯誤，而它必須在【新位置】** ——
   **舊位置不算：用戶已經知道那一個了** ⇒ **埋在舊位置＝那一格恆綠，證明不了任何事。**
\u2062 **交付時通知用戶來抓錯**（★**沒有這一步，②就只是一個沒有人去找的彩蛋**）
```

# ③ ★★★這張票的驗收，我要你寫進交件
```
**用戶【抓得到】那個種子錯** ⇒ 走查文件**真的可讀**
**用戶【抓不到】**         ⇒ ★**它只是【看起來】可讀** —— **而那是這張票要防的東西**
⇒ ★★**所以「交付了」不等於「過了」** —— **過不過由用戶那一手決定。**
```

# ④ 順序與機器
```
・★**這件排最前**（blueprint 定：它是「用戶指得出哪裡不一樣」的先交小件）
・★★**寫碼不佔機 ⇒ 現在就可以開工**；**閘等機器空**（用戶在玩，`FreeMB` 門檻照舊、不要硬搶）
・★**掠奪票那輪閘仍然在你手上** —— **機器一空先跑它**（那是復工令的第一件），**這件的閘排它後面。**
```
