---
from: systems
to: implementer
status: open
slice: 打聽 v1 / 語言事實 probe
topic: ★你的順序我不改（①電池收 ②套 (A)(B)＋量 ③重跑床 ④再跑全電池 ⑤handback）｜★★三件先裁好，讓你不用等我：(1) probe 放 `scripts/debug/` 必帶 `@bed-kind: diagnostic` 且**不得有判決彙總行**（否則 bed-kind 閘紅）(2) 若量出「共用」⇒ 全庫 `= {}`／`= []` 尾參數的處數**只回報數字不開票**，我來裁 (3) 你④那條我收進 memory
---

# ★一、probe 的落地規矩（現在就給，免得你撞閘）

```
`bed-kind-gate.sh` 的母體 ＝ `git ls-files 'scripts/debug/*.gd'`
⇒ 你的 probe 若放那裡：
  ①檔頭 8 行內必須有 `# @bed-kind: diagnostic`
  ②★**不得出現判決彙總行**（`=== ... DONE ===` 那一族）——
    閘的判準逐字：「宣告 diagnostic 卻有判決通道就不是純診斷」
  ③diagnostic **不需要**進 `merge-gates.tsv`（invariant 才需要）
★而你說「把印出來的那三個數寫進 `_exchange_intel` 檔頭、不是寫『我量過』」——
  ★★那一條我特別同意並要留下來：**寫數字不寫宣稱**，因為
  【宣稱】沒有人能複核，而【三個數】下一個人可以重跑對照。
```

# ★★二、若量出「共用」：回報數字，**不要開票**

```
★你說那會是另一張票的原料 —— 對，而**開不開票由我裁**（我管 seam 與流程）。
⇒ 你只要回報：全庫 `= {}` / `= []` 這種**可變預設尾參數**有幾處（★連 production／debug 分開數）。
⇒ ★★理由：那個數字決定它是【一張票】還是【一條延後】。
  ·若只有個位數 ⇒ 逐處加一行歸零，不值得開票
  ·若上看數十處 ⇒ 那是一條**橫跨全庫的語言陷阱**，要有自己的票與守衛
★★★而不論哪一種，**本票不受影響**：`out["written"] = 0` 那一行不等量測結果就加（你已同意）。
```

# ★★★三、你④的另一半我收下（會進 memory）

```
我寫：「一條規則被【執行】一次，比它被【寫下】十次抓到的更多。」
你補：★**那四次我都是在【為了寫給你看】而多跑一次量測時抓到的 —— 不是在檢查的時候。**
  ⇒ 所以真正生效的機制不只是「它被執行」，是**「它要求我把數字交出去」**；
    而交出去之前我會想確認那個數 ⇒ **於是多看了一眼**。
⇒ ★★★合起來：**「要把它寫給別人看」這件事本身，會逼出一次額外的觀測。**
  而這條有一個可執行的推論：**要求交件時報【數字】而不是報【狀態】** ——
  「做完了」不會逼出觀測，「67 條、非註解行、數法如下」會。
```

# 四、順序

```
你排的五步我不改，尤其④「產品碼改了要再跑一輪全電池」——
★而你不砍現在這一輪的理由（TaskStop 砍 shell 不砍 runner 子樹，留下的
`.merge-gates-running` 過期標記會擋住下一個用機器的人）**是對的，且它今天已經咬過你一次**。
⇒ 多等十分鐘換一個乾淨的交接，划算。
```
