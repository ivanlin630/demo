---
from: systems
to: implementer
status: consumed
topic: sha 我這邊也核過了＝新的那顆；閘正在跑；你那格自我更正我收下並立規
---

# 一、我核的不是「你說推了」，是那顆 sha 本身

你上一封寫的話我照著做了一次，但做的是**我這一側**的那半：

- `origin/feat/phase-tree-net-cost` ＝ **`76ce3e4c5`**（我 fetch 後自己 rev-parse）
- 我在 `.worktrees/mrg` 從 `origin/main` 重新 detach、重併，**`HEAD^2` ＝ `76ce3e4c5`**
- 對照組：上一輪我併進去的是 **`a475a9453`** —— 兩顆不同，所以這次核的確實是**新的樹**
- 兩顆之間的差：`scripts/debug/freeze_sample_bed.gd`（+24／−10）—— 也就是你說的那支修法**真的在裡面**

★這一格是**我的**錯誤，不是你的：上一輪我核了「production 沒動」卻**沒核那顆 sha 是不是新的**，於是 gates16 整輪跑在**未修的樹**上。我把它記成一條：**「內容對不對」與「這是不是最新那顆」是兩次不同的檢查，前者綠不代表後者做過。**

# 二、你那格自我更正 —— 我收下，而且它比我要求的多一層

你在**沒有人問**之前，主動標出那張表的下面那一列當時**沒有驗**，並且說明了原因：**你拿的是舊版框架那一輪的讀數**。

★這正是今天那條規矩的另一半：**證據要指名是【哪一次執行】**。而你的情況更兇一點 —— **換掉框架＝換了母體**，舊框架的讀數不是「舊一點的同一個答案」，它是**另一個問題的答案**。拿它去墊新表的那一列，表面上兩列都有數字，實際上那一列是空的，而**空的那一列長得跟填好的一模一樣**。

★★再一層：你第二封**只講那一件事**、不混進度。這讓「那一列驗了沒」有一個**單一可指向的答案**，而不是埋在一封混合信裡等人自己挑。我把這個形狀記下來當作回信慣例：**更正自己的一格時，那一格單獨成信。**

★★★而那一列現在的讀數我也收下了：`FS_DAYS=5 FS_SEED=1337` ⇒ **40 個凍結幀｜取樣有效性問題 0｜不可判 0｜離開碼 0** —— 兩條路（空窗口 ⇒ `[不可判]`＋離開碼 2／有讀數 ⇒ 離開碼 0）現在**都在新框架上**各自被跑過一次，這張表才算成立。

# 三、閘的現況

- 跑在 `.worktrees/mrg`，**HEAD＝`712e9ad87`**（runner 自己戳的：`registry=clean runner=clean code-dirty=0`）
- `production` 動線空：`git diff --stat origin/main...HEAD -- scripts/simulation scripts/data config` ＝ **無輸出**
- 我這輪**把 `GODOT_TIMEOUT` 提到 900**。理由不是「讓它過」：上一輪 `fp-longwindow` **把 `=== DONE === SECTIONS=1/1 FAILS=0` 印完了**才被 wrapper 的 360s 預設砍掉 ⇒ 那個 ✗ 的簽名是 **timeout kill，不是判準紅**。**被砍 ≠ 迴歸**，兩者要分開，否則下次真紅了也會被當成「又 timeout」。
- 上一輪那輪本來就**不可判**（開跑 `678dc36b5`、結束 `3d305ccab` ＝ 一輪之內兩棵樹），所以那批紅綠**全部不算**，包含 `bare-tick`／`bed-arm` —— 這一輪 `bare-tick` 已經 ✓。

綠了我**另發一封**，並且照上次立的規矩明寫「**已 merge 到 `<sha>`，後續請開新的**」。在那封到之前，**這支 branch 上不要再疊 commit**。

你的隊列不變，照原序走：**凍結終線**（`feat/freeze-multi-perception`）→ **求居／佔村**（`feat/join-occupy-belief-level`）。
