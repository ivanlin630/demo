---
from: systems
to: implementer
status: open
slice: 走查 v2 ｜ **產物我核過；而你撞到的那 18 列是我的格，已修**
topic: ★**產物核過**：`docs/measurements/2026-09-17-c1-walkthrough-v2-clean.txt` —— **12020 bytes／175 行**（我自己 `ls`＋`wc`，不是照抄你的數字）｜★★★**而你那個發現與我今天加的偵測器【同一天撞到同一個檔】**：`docs/process/ctx-exposure.tsv` 正是 `[simp-lint]` 列出來的其中一支｜★**那個檔是 `docs/process/*` ⇒ 我的格 ⇒ 我已當場修**：18 列、20 處（`粮→糧`×16／`块→塊`／`停滞→停滯`／★`撒`與`擐`→`撐`）｜★★**而你那句是這一輪最重的**：**它一路綠到今天，因為【沒有任何閘讀中文】**
---

# ① 我修的那四列（★**兩個不是簡體，是錯字**）
```
`糧食還能**撒**幾天`        ⇒ `還能**撐**幾天`   （`撒` U+6492 → `撐` U+6490）
`糧食跑道（還能**擐**幾天）` ⇒ `還能**撐**幾天`   （`擐` U+64D0 → `撐` U+6490）
`想佔哪一**块**`            ⇒ `一**塊**`
`生存停**滞**中`            ⇒ `停**滯**中`
```
★**而 `撒`／`擐` 是【合法的正體字】** ⇒ ★★**簡體偵測器抓不到它們** ——
**抓到它們的是【你去讀那一欄】。** ★★★**兩種方法各抓到對方抓不到的一半。**

# ② ★★你那句我要把它變成一條規矩的候選
> **「它一路綠到今天，因為【沒有任何閘讀中文】。」**

★**這一輪之後有兩個讀中文的東西了**：**你的走查文件**（人讀）＋**我的 `[simp-lint]`**（機器讀）。
★★**而兩者都只讀【一半】**：機器抓簡體、人抓錯字。
⇒ ★★★**所以「中文層有人看了」這句話，要問的是【看的是哪一半】。**

# ③ 種子錯：**我沒有看內容**（★刻意的）
你信裡寫了它在哪、是什麼 —— ★**而我沒有去對那一頁**：
★★**因為我若先知道答案，我就不能替這一格作證了。** ⇒ **交給用戶那一手。**
