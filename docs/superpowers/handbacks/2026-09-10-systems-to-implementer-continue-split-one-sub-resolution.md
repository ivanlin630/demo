---
from: systems
to: implementer
status: consumed
slice: 效能線 ｜ ★停止規則判【繼續】
topic: ★★★**45.61%（ON）／41.20%（OFF）** ⇒ 依先寫死的規則（≥20% ⇒ 繼續）**繼續**｜★★而你多跑一趟 OFF 那一手我立成界限第 45 條：**當一個結論可能被儀器污染時，最強的處置不是【附上誤差】，是【在兩種狀態下都算一次】** —— 兩邊都成立，儀器這個問題就**關掉**，而不是被註解｜★下一刀：**切開「一次子問題展開」**（切點由你定），而我讀了 code —— **只講它在算什麼，不講錢在哪**
---

# ① 判定：繼續

```
子樹 ÷ 牆鐘：**ON 45.61%（124.906 ÷ 273.847 s）／OFF 41.20%（97.513 ÷ 236.694 s）**
⇒ ★兩種狀態都遠高於 20% ⇒ **繼續**（規則是我在拿到數字之前寫死的，所以這裡沒有臨場判斷）。
⇒ ★★而 45% 這個數字本身是一件**要讓 blueprint 知道的事** ——
  ★★★**這不是 bug，這是這個設計的價錢**：手段-目的規劃**每次決策、每支隊、從頭算一遍**。
  ⇒ 我會在下一刀的數字回來之後一起呈報（★現在呈報會變成「一個沒有下文的壞消息」）。
```

# ② ★★而你那一手我立成規矩了（界限第 45 條）

```
「只報 ON 再附一句『儀器 +13.9%』」vs「ON 與 OFF **都算一次**」
⇒ ★前者把儀器問題**變成一個註解**（而註解會被跳過）；
  ★★後者把它**關掉**（兩邊都成立 ⇒ 這個結論**不依賴儀器**）。
⇒ ★★★而成本只是**多跑一趟** —— 對一個要拿來做「繼續或停止」決定的數字，**那便宜得離譜**。
```

# ③ 下一刀（★我讀了 `_resolve_build_facility`，只講它在算什麼）

```
它回答的是：**「蓋這個設施」這個目標，現在是不是一個可行的候選？**
  ⇒ 讀 `FACILITY_DEF` ／ 算 `derived_payoff` ／ 找自家據點 ／ 檢查是否已經蓋好 ／
    然後展開**材料與工具的前置**（＝那 72.4%、2826 個**各不相同**的子問題）
★而我**不說錢在哪** —— 我只指出**它的形狀**：
  ★★**它是一次完整的「要達成這件事，我還缺什麼」的推導**，
  ★★★而它**每次決策、每支隊、從頭算一遍**（0% 重複已經證明了這一點）。
⇒ 要的：**把一次子問題展開切開**（切點你定）＋ 每段次數 ＋ 單價（★量、不准除）
  ＋ 母體地板（★★是 0 就明寫 0，不准以 0 為分母算比率）。
```

# ④ ★而我先寫下【下一輪的裁定規則】（同前兩輪，先寫後拿）

```
★若切出來**有一段獨大（≥60%）** ⇒ 下一步是**那一段**，而我會讀它在算什麼並判**修法會不會碰語意**。
★★若**攤平（沒有一段 ≥40%）** ⇒ 那代表**沒有一個熱點，是整條推導都貴**
  ⇒ ★★★那時**方向會換**：不是「優化某一段」，而是**「這條推導要不要每次都從頭算」** ——
    而那是**設計問題**（呈 blueprint），不是效能問題。
```
