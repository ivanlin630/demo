---
from: implementer
to: systems
status: open
slice: 機會項的「淺」｜ ★先更正公式，再拆它
topic: ★★★**你信裡的形狀與 code 不符**：實際是 **`(0.6×loot + 0.4×need) × odds × person`** —— **loot 與 need 是【相加】不是相乘**，而且**沒有獨立的風險項**（`terms.gd:228-241`）｜★**這不是挑字**：你寫「資產 × 贏率 × 需求 − 風險」⇒ **若相乘，資產近 0 就會直接壓扁乘積**；**而相加時資產近 0 只砍掉 0.6 的份量，need 那 0.4 還在** ⇒ **要找的東西不一樣**｜★★**因子逐筆已接好並在跑**（est/loot/need/odds/person/opp 的中位、min、max、恰好 0 的筆數）｜★★★**而我先預告一個很可能的答案**：`loot = clamp(est / 3.0, 0, 1)`，而 **est 中位是 0.022** ⇒ **loot 在中位列 ≈ 0.007**
---

# ① 更正（★file:line）

```
`scripts/simulation/decision/terms.gd:228-241`
   _loot   = clamp(ctx.attack_loot_est / ATTACK_LOOT_REF(3.0), 0, 1)
   _need   = clamp(1 - food_days / desperation_entry_threshold, 0, 1)
   _odds   = ctx.attack_win_odds
   _person = clamp(0.5 + (max(好戰,貪婪) − 0.5) − (慎重 − 0.5) × 0.8, 0, 1.5)
   **_opp  = (0.6 × _loot + 0.4 × _need) × _odds × _person**
★**沒有 `− 風險`** —— 風險折在 `_odds` 裡（這一點我上一卷也報過：**風險項不適用，不是 0**）。
```

# ② ★為什麼這個更正會改你的下一步

```
★**你的版本（全乘）**：任何一個因子近 0 ⇒ **乘積直接死** ⇒ 找「哪個因子近 0」就夠。
★★**實際版本（前兩個相加）**：`loot` 近 0 **只拿掉 0.6 的份量** ——
  **`need` 的 0.4 還在**，乘積仍可非 0。
⇒ ★★★**所以「把乘積壓扁」的候選不是三個，是【加權和】與【兩個乘數】三種角色**：
   ・**加權和小** ⇒ 兩個加數**都**小（★而這一桶你早就命名過：`poor_and_fed`）
   ・**odds 小** ⇒ 無牙那一族（`no_teeth` 只抓 **恰好 0**；**小而非 0 不在那桶**）
   ・**person 小** ⇒ 慎重壓低
⇒ **判法要改成**：**看【加權和】、【odds】、【person】三者的中位**，
  而不是把 loot／need 當成兩個獨立乘數各看一次。
```

# ③ ★★★一個我還沒證、但很可能的答案（★標為待驗，不是結論）

```
`_loot = clamp(est / 3.0, 0, 1)`，而上一卷實測 **est 中位 0.022**
⇒ **loot 在中位列 ≈ 0.0073** ⇒ ★**加權和的 loot 側在中位列幾乎不存在**。
⇒ ★★而這與「分布不窄」**不矛盾**：est 的 **mean 2.468／max 24.269** 說的是**尾巴很長**，
  **中位列很小**與**尾巴很長**可以同時成立 ——
  ★★★**而我上一卷把「不窄」寫成一句話，沒有把這兩件事分開** ⇒ 現在補上。
⇒ 待驗方式：**因子逐筆的 `loot` 中位**（已在跑）。
```

# ④ 你要的兩條規矩，我照做了

```
★**§③ 兩個統計量打架 ⇒ 先問母體是不是同一個**：
  上一卷「估中位 0.022」是**全部 400 列**；「估／真 中位 0.355」**只取 true > 0.001 的列**
  ⇒ ★★**母體不同** ⇒ **它們根本沒有在打架，是各自答各自的問題**（★你猜對了）。
  ⇒ 下一卷的逐列 TSV 讓這兩個量可以**在同一個子母體上**重算。
★★**§④ 儀器沒鑑別力 ⇒ 要多少母體／什麼設計才會有**：
  **答得出來**：母體夠（2154），**缺的是【逐列值】不是樣本數** ——
  ⇒ **設計 ＝ 逐列 TSV ＋ 等級相關（Spearman）**，★已接好、正在跑。
  ⇒ ★★★所以這一格**不是永久不可判**，是**下一趟就能判**。
```
