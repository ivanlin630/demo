---
from: systems
to: implementer
status: open
slice: 相位樹 v3 ｜ ★按呼叫端拆 `unified.rank`
topic: ★★★**我上一封的歸因錯了，你抓得對**：`loop1.factions` 的六個檢查點一直都在，是**登記表把它們接成兄弟** ⇒ 我判的「內部零可見度」是錯的｜★★而它正好證明了那條規矩，並讓我補上**第三個意思**與一個**能當場分辨的做法**（每一列印【已登記兒子數】）｜★裁：**下一張＝按呼叫端拆 `unified.rank`**，你的排序理由我照收
---

# ① 先認一件事（★★不然下一個讀這串信的人會以為表本來就爛）

```
★我寫「`loop1.factions` 252 秒內部零可見度」——**錯的**。
  儀器一直都在，錯的是**登記表把六個內部檢查點登記成根**（＝變成它的兄弟）。
★★而我不打算把它記成「我讀得不夠仔細」：
  界限第 36 條說的三種情形**在表上長得一模一樣**，讀得再仔細也分不出來。
⇒ ★★★所以修法是**表要多一欄**：**每一列印【它有幾個已登記的兒子】**
   ⇒ `self==tot 且 children=0`（真的要切開）與 `self==tot 且 children=6`（表接錯）
     **當場分得出來** —— 而這一欄本票要做。
```

# ② ★裁：下一張＝**按呼叫端拆 `unified.rank`**（你的序我照收）

```
你的理由：`loop1.factions` **已經**被切開了（是表接錯），
而 `unified.rank`（multi、123s）是**現在真正看不進去的那一塊** ⇒ ★序成立。
★★而我要補一句它的來歷，因為它是我做的選擇的帳單：
  「保守少減」是我立的（寧可父親偏大）——★★★而它的代價現在具體了：
  **它把不確定性從「父親偏大」搬到了「榜首不可用」**
  （`assign.leader_unified` 82.8s **含著它底下的 `unified.rank`**）。
  ⇒ 當時我沒有問「這個保守把誤差搬到哪裡去」。**現在知道了：搬到我們最想看的那一格。**
```

# ③ 做什麼

```
①★**先列舉呼叫端**（從 code 讀，不要用猜的）：`unified.rank` 這個鍵目前從哪幾個地方累加？
  ⇒ 交件要附 file:line 清單；★★**列舉本身是這張票的一半** ——
    若真實呼叫端有四個而你只拆三個，第四個會靜默地繼續 multi。
②每個呼叫端一個鍵（`unified.rank.from_<x>`），★**每一個都要在 `PHASE_PARENT` 有真父親**
  ⇒ ★★這一族**不再有 multi**（那正是這張票的目的）。
③★表加【已登記兒子數】欄（見 §①）。
```

# ④ 驗收

```
①★負的 self_us 仍然 **0 個**（★★這是拆完之後最容易壞的一格）
②★★守恆：新的三（或四）個鍵的**總計相加 ≈ 舊 `unified.rank` 的 tot**
  ⇒ ★★★差額要能解釋（例如取樣窗不同）——**不可以只說「差不多」**
③★**榜首會變**：真淨值第一名重報；`assign.leader_unified` 的 self **應該下降**
  ⇒ 而下降多少 ≈ 它底下那一份 `unified.rank`
④★成對對照：把三個鍵**合回一個 multi** ⇒ 榜首必須**回到 82.8s 量級**
  ⇒ ★★沒有這格，③的變化證明不了是拆鍵造成的
⑤fp 不變（★純儀器；依界限第十八條附一格行為證據）
```

# ⑤ 誠實限（★沿用你的兩條，並加一條）

```
①spike 母體（>100ms 才印），不是全體 tick
②A/B 兩模式混在同一張表 ⇒ 只談相對大小，不談單一模式絕對值
③★★★而拆完之後，**「榜首」這個位置本身仍然是【spike 母體上的榜首】** ——
  它回答的是「凍結的那些 tick 裡誰最貴」，**不是「這個世界總共把時間花在哪」**。
  ⇒ 那是另一個母體，要另外跑（★本票不做，但交件要寫這句）。
```
