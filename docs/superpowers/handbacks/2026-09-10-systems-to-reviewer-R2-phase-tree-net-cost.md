---
from: systems
to: reviewer
status: consumed
slice: 相位樹（淨值）
topic: ★R² 送審 `specs/2026-09-10-phase-tree-net-cost-HOW.md`｜★★論點：**在有父子關係之前，「下一個要修誰」這個問題無法回答** —— 用巢狀表的「總計」排序＝把父親和兒子放進同一個排行榜｜★★★而我選了【手抄父子表 ＋ 未登記就紅】而不是 enter/exit 包裹式，理由是「哪個相位包在哪個相位裡」是**寫 code 的人知道的靜態事實**，不需要 runtime 去發現 —— 這一個選擇請你打
---

# ① 我要你打的三格

```
(1)★★★**做法選擇**：手抄 `PHASE_PARENT` 表 ＋ 閘（未登記的名字 ⇒ 具名紅）
   vs enter/exit 包裹式（真堆疊）。
   ⇒ 我選前者的理由：`_fai_pht` 是【檢查點式】不是包裹式（faction_ai_system.gd:822-831），
     改成包裹要動每一個呼點＋動熱路徑；★而父子關係是靜態事實。
   ⇒ 請打：★★**手抄表的代價我是不是低估了**（它會不會變成第二份「會過期的清單」）。
     ★★★我的處置是「未登記 ⇒ 紅」＋驗收②的【父的 self_us 不得為負】，
       而我知道**它擋不住「登記成錯誤的父親」** —— 這一格我明寫在誠實限裡。
(2)★驗收②我寫「父的 self_us 為負 ⇒ 判紅，不判四捨五入」
   ⇒ 請打：★★負值還有沒有**別的**成因（例如同一個相位名被兩處累加、
     或相位區間真的重疊而不是包含）——★★★若有，那「判紅」是對的但**理由會誤導下一個人**。
(3)★序：本票**不優化任何東西**，它只讓表可以排序。
   ⇒ 請打：在效能線上先做這個、還是先挑一個看起來最大的去優化？
     ★★我的立場是前者（界限第 30 條就是從這裡立的），而我知道它看起來像繞路。
```

# ② 背景數字（★implementer 交件，我沒有重算）

```
印全之後（650 筆 spike）：loop1.factions 404.9s／unified.rank 369.8s／loop1.assign_tasks 361.7s
★而 assign.leader_unified 206s／assign.members 155.7s 幾乎確定是 assign_tasks 的子相位
⇒ ★★總計欄是巢狀的，加起來超過 100%。
★★★另外：`loop2.solo` **已經不在這份表裡** —— 錯開票把 `_evaluate_solo` 移出 `evaluate_all`
  ⇒ 這份表是【派系側】的帳，solo 的帳在另一個容器，**兩份不可相加**。
```
