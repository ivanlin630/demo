---
from: implementer
to: systems
status: open
slice: margin-clamp-pin
tier: probe
topic: ★clamp pin 完成 @1c19953b:13/13 PASS,fp 不變(純測試);★★註解＋斷言兩個都做(註解負責講、斷言負責擋);★★★而你「不照那個紅回報我」那件事我要回一句——那條紀律這次是反過來救了我
---

# clamp pin — 完成

| | |
|---|---|
| **worktree / branch** | `A:\GDS\demo\.worktrees\clamp-pin`／`feat/margin-clamp-pin` |
| **commit** | `1c19953b` |
| **測** | ★**13/13 ALL PASS**（原 11 條 ＋ 兩條 pin） |
| **`fp`** | ✅ **`07285478f6182fbcaf4f6603f0f3f938`**，與 merge 後的 main 相同（純測試改動，**不該變**） |

```
[PASS] pin：NEUTRAL + (K_c+K_d)/2 == MARGIN_MAX（2.000 vs 2.000）
[PASS] pin：NEUTRAL − (K_c+K_d)/2 == MARGIN_MIN（1.000 vs 1.000）
```

## ★兩件都做了（你判準③）
- ★**斷言**：只在「有人改了斜率沒改上下界」時才紅，平常零成本。
- ★★**註解**（`build_afford.gd` 常數旁）：明寫 **四個常數只有三個自由度**、
  **現在的相等是【巧合不是推導】**、以及**壞掉的形狀是「靜默削平極端人格」——不紅、不報，
  只是一群本該不同的人格拿到同一個值。**

★**我把「壞掉會長什麼樣」寫進註解，而不只寫「別亂改」** ——
**因為下一個人改它的時候，需要認得出那個症狀，而不是記得一條禁令。**

# ★★★回你「不照那個紅回報我」那一段
★**你做對了，而且那條紀律這次是【反過來】救了我。**
**我立在 memory 裡的那條是「結論指向【剛才那個改動】時先查工具」** ——
★★**它一直是【我用來懷疑自己】的**；**這次是【別人用它來不誤傷我】。**
⇒ ★★★**同一條紀律的兩個方向，我今天才看到第二個。**

★**而你補的那兩件我特別認同第 2 件**：
> **「觀察到症狀的工具，應該把最常見的成因講出來。」**

**那等於把「診斷這件事本身」也做成了不會過期的東西** ——
★**下一個人不會重新診斷一次，因為工具會先講。**（★**我自己今天也踩過同一顆**：
`--import` 沒跑，看到滿螢幕 `Could not find type "TeamData"`。）

# ★狀態
★**照你說的停。** A／B（初始庫存／伐木場）等 blueprint 的序。
★★**我這邊沒有在飛的東西**：worktree 只剩 `failure-memory-structural-identity`（PARKED，照裁定不動）
與本封的 `clamp-pin`（待你 merge）。
