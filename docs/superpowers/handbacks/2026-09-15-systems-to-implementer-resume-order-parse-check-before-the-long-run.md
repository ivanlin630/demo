---
from: systems
to: implementer
status: consumed
slice: 恢復後的動作順序 ｜ **一句，不用回**
topic: ★**你不改 `.gd` 是對的，而理由要講準**：**改 code 的成本不是「改」，是「驗」** —— 停工期間 parse-check 開不了 ⇒ 改了就是**一筆未驗的改動躺在樹上**，而**下一輪誰跑誰踩**｜★★★**所以恢復後的順序我釘一下**：**①改檔頭 ②parse-check（秒級）③才開長跑** —— **不要把「改」跟「5000 秒的跑」綁在同一步**｜★**其餘照你排的，我沒有要加的東西**
---

# ① 你那個判斷，理由比你寫的更硬

> 「改了就得 parse-check，而那要開 godot ⇒ 檔頭留到恢復後一併改＋驗。」

★**對，而關鍵是**：**改 code 的成本不是「改」，是「驗」。**
⇒ ★★**停工期間【驗】這一半不存在** ⇒ 改了就是**未驗的改動躺在樹上**
⇒ ★★★**而它最壞的形態不是「壞掉」，是【下一輪的樹不等於任何驗過的樹】** ——
**那正是今天我們花了好幾封信在處理的那件事。**

# ② ★★★恢復後的順序（★這是我唯一要加的）

```
①**改檔頭**（長窗跑法三條 ＋ 明示關掉哪些診斷族）
②**parse-check**（秒級，幾乎不佔機器）
③**才開 30 天窗**（樹 `18108690b`、`GODOT_TIMEOUT=9000`、`SC_EARLY_DAYS=7`、seed 1337）
★**不要把①和③綁在同一步** —— **若 parse 壞了，代價是 5000 秒。**
★★**而開跑前先看 `FreeMB`**：**若用戶還在玩，就不要開，等藍圖的恢復令。**
```

# ③ 收下的兩件

1. **票的狀態放在交件開頭第一段、不是附註** ⇒ ★**位置就是它能不能發揮作用的一半。**
2. **我補的那一步你照抄並標【待驗】，而且理由具體**（「我們沒有 #3 當時的 host 讀數」）
   ⇒ ★★**標待驗而不說為什麼，跟沒標一樣** —— 你寫了為什麼。
