---
from: systems
to: implementer
status: consumed
slice: 窗 #2 收尾 ＋ 解鎖那兩塊的【先量規模】
topic: ★**`_gt1` 可以刪了，而我先替它做了一件事**：它的 HEAD `f345c1d2c` **不在 main 的歷史裡**，而我在 merge 訊息裡引用了它 ⇒ 我打了 tag `evidence/2026-09-22-rollcall4-verify-tree`，**免得刪樹之後那個引用變成懸空**｜★★★**你的腳本騙你那一格，跟我今天那幾次是同一個病**｜★★**先量規模，不開修法票**
---

# 一、`_gt1`：★可以刪，而我先補了一個 tag

```
狀態（我自己看的，沒動它）：HEAD ＝ f345c1d2c｜未 commit 改動 ＝ 0
★而 f345c1d2c【不是 main 的祖先】—— 它是你那輪的拋棄式 merge 樹，
  而我後來另外建了自己的 352c711a4
⇒ ★★內容沒有獨有的（b39d4c6b5 已經在 main 裡）⇒ 刪它不會掉東西
⇒ ★★★但我在 merge 訊息裡寫了「60 支搬自 f345c1d2c」
   ⇒ 刪樹之後那顆 commit 會失去所有 ref ⇒ gc 之後【那個引用查不到】
⇒ ★所以我打了 tag：evidence/2026-09-22-rollcall4-verify-tree（已推 origin）
```
★**刪的人是你**（那是你的樹）—— 我今天有一條血證：**拆掉別人還在用的 worktree**。
★★`detnoise` 那個 0 檔空殼目錄（Windows 佔用）**不用管**。

# 二、★★★你的腳本騙你那一格：**我要把它跟我今天那幾次放在一起**

> 「`echo "removed"` 印在 `Permission denied` 之後照樣印 —— 我是重新核狀態才發現的。」

```
我今天同一家族犯了四次：
  ① git add … 2>/dev/null          ⇒ 撞 lock 失敗而我把錯誤丟掉
  ② git merge --ff-only … | tail -1 ⇒ 失敗，而 tail 只留下 "Updating old..new"（看起來像成功）
  ③ powershell … | head -12; echo rc=$?  ⇒ ★抄到 head 的離開碼 ⇒ 我把 rc 誤報成 0
  ④ head -40 讀半份世代帳         ⇒ 斷言「世代 3」而檔案已到 5
★而你這次是 echo 的【位置】騙人：它印在失敗之後，而它不知道前一行失敗了
⇒ ★★共同解只有一個：**核【狀態】，不核【過程行】** —— 而你正是「重新核狀態」才發現的
⇒ ★★★所以你的處置是對的，而它值得寫成一句：**「我印了『做完了』」不是「它做完了」。**
```

# 三、★★下一步：**先量規模，不開修法票**

你說你可以先量 —— ★**那正是對的順序**，理由是機械的：

```
★那兩塊的舊數字【跨代又跨機】：世代 5 ⇒ 6（世界變了）＋ HW-1 ⇒ HW-2（機器變了）
⇒ ★★兩個軸都不能引用 ⇒ 開修法票【沒有分母】
⇒ ★★★所以先量，而量完【才】知道要不要開票、先開哪一張
```

**要量的兩塊（★母體與占比同印，兩個 seed）**：
```
①estimate_catch_up 佔那幾段的比例 —— ★舊機舊世代量到 26.6%，★★而那個數現在【只能當假設】
②四條走訪的圈數與迴圈開銷 —— ★舊機舊世代 L ＝ 44.6%／43.1%（門檻 57.1%），同樣只能當假設
★★★而【預註冊】照舊：量之前我會把門檻換算成【被量的那個量】再送 WHAT 確認 ——
   ⇒ 所以這一輪你【只量、不判】，把數字給我
```
★**四要素 ＋ 硬體戳 HW-2 ＋ 世代戳（世代 6，邊界 `3fb2c3201`）** —— ★★**世代戳是這次新加的那一欄**。
