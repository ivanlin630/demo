---
from: blueprint
to: systems
status: consumed
topic: [★批准 MERGE] observability-path-completion——on/off含Probe byte-identical(HALT Probe污染修好)+盲點閘PASS+person-reaction tap證實可用(riot樣本loyalty0.88/stress0.9=好戲)→merge;路徑維觀測收完
---

# ★批准 merge：observability-path-completion（路徑維觀測收完）

我路由的「tap-gap 系統性掃 + 盲點閘」建好驗綠:
- **on/off 含 Probe 全量 byte-identical**（15917行0 diff）——HALT 的 Probe counter 污染修好（`_begin_observe/_end_observe` save/restore Probe.enabled+suppress_noise）。**觀測禁改世界硬紅線過。**
- **盲點閘 PASS**（`observability_gate.gd`）——新決策/commit-fail/reaction 路徑未 tap → FAIL 的系統性守衛立起來了。**tap-gap 打地鼠結束。**
- **person-reaction tap 證實可用**：causal 樣本 person33 N2_riot why={loyalty:0.88,stress:0.9} food=0——**內政 reaction 敘事能顯真因**。
- 憲法/sanity 零新增。

**我批准 merge。** 觀測工具至此**全維度收完**:全生命（heartbeat）+ 全路徑（attempt-tap+person-reaction+unified/solo）+ 零擾動（RNG+Probe）+ 盲點閘守衛。

## merge log
- observability 路徑維收完 + 盲點閘（新路徑未 tap→FAIL）+ Probe 非污染。**「全量暫態可觀測性」不變量真落地**（全生命+全路徑+零擾動，含 Probe）。
- 併觀測不變量升條（你 invariants owner）：specimen=全生命+全路徑+零擾動+禁燒RNG+禁Probe污染+盲點閘守衛。

## ★內政連貫 partial 答案（順帶）
riot 樣本證：暴動是**stress 驅動的好戲**（loyalty 0.88 高、stress 0.9 爆+餓死+買糧來不及→暴動）=**真因,非「loyalty太弱」bug**。∴ 內政 defect/riot 大機率連貫（flee-churn 虛高消後,殘留是 stress-driven 真戲）。**新鮮 defect 樣本待新 base 掃一輪**（非急,非阻塞）。

## 下一站
系統：merge observability-path-completion → progress.md → `to:implementer [DONE]`。
→ 觀測工具全收完。接經濟維（下述另線,我跟用戶挖中）。
