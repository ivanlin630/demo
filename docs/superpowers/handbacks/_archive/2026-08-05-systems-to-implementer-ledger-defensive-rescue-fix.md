---
from: systems
to: implementer
status: consumed
topic: "[ledger defensive/rescue 真 consumer fix(blueprint 裁 (a)、續 feat/missing-contact-ledger、修 QA REFUTE 手不聽腦:_apply_contact_reaction:4746/4750 兩 flag write-only 無 consumer)·(a)理由:標籤化仍是選 no-op=假分化(慎重 lord 選 defensive 啥都沒發生);縮 2 丟已 CONFIRM diversity·fix:①defensive(faction_ai:4747 contact_defensive flag)→改接既有 threat/posture 機制:母隊讀自身失聯 belief(ledger lost entry)→謹慎——★禁新平行旋鈕(guard_ratio 那族散落標的、接既有 threat_assessment 輸入非加第4旋鈕);既有 threat 機制=_evaluate_threat:378/_decide_unified(threat-oracle)、既有 strategic intent 防衛(:69)——把『母隊有未解釋失聯單位』餵既有 threat perception→既有 備戰/防衛 fire(找 threat_assessment threat 組成 hook 插 lost-signal、非新 field)②rescue(:4750 rescue_flag)→reuse scout side-dispatch 派去查失聯單位:dispatch_anon_messenger TASK_SCOUT target=失聯單位 last-known pos(team-subject→belief best_estimate pos/letter→dispatch origin、零 god-view 非 live)=資訊收集天生 fit、救援隊 verb 照 defer·守:defensive 禁新旋鈕(餵既有 threat)/rescue reuse 既有 scout 機具/零 god-view(lost-pos 從 belief/dispatch-log)/determinism/§1·TDD:①defensive→母隊 threat/posture 真變(既有 防衛/備戰 fire、RED flag-only→無效果)②rescue→scout 真 dispatch 到 lost-pos(RED flag-only→無 dispatch)③4 類全真世界效果+diversity 仍在(慎重→defensive 真謹慎/義氣→rescue 真派查)④determinism·完成 handback to:systems R²+measurer re-measure(★4 類全真效果+分化仍在+98 ledger_add breakdown[查 subteam 漏記帳])→QA 新 verdict→merge·地基 KEEP"
---

# ledger defensive/rescue 真 consumer fix（blueprint 裁 (a)）

修 QA REFUTE 手不聽腦（`_apply_contact_reaction:4747/4750` 兩 flag write-only 無 consumer=argmax 選了零世界效果）。續 `feat/missing-contact-ledger`。

blueprint (a) 理由：(c) 標籤化仍是選 no-op=**假分化**（慎重 lord 選 defensive 啥都沒發生）；(b) 縮 2 丟已 CONFIRM diversity。

## fix
### ①defensive（`:4747 contact_defensive` flag）→ 接既有 threat/posture
母隊讀自身失聯 belief（ledger lost entry）→ 謹慎。
- **★禁新平行旋鈕**（guard_ratio 那族已散落標的）——**接既有 `threat_assessment` 輸入、非加第 4 旋鈕**。
- 既有 threat 機制：`_evaluate_threat:378` / `_decide_unified`（threat-oracle）；既有 strategic intent「防衛」(`:69`)。
- 把「母隊有未解釋失聯單位」**餵既有 threat perception** → 既有 備戰/防衛 fire（找 `threat_assessment` threat 組成 hook 插 lost-signal、**非新 field**）。

### ②rescue（`:4750 rescue_flag`）→ reuse scout side-dispatch 派去查失聯單位
- `dispatch_anon_messenger TASK_SCOUT`、target=**失聯單位 last-known pos**（team-subject→belief `best_estimate` pos / letter→dispatch origin、**零 god-view 非 live**）。
- ＝資訊收集天生 fit、**救援隊 verb 照 defer**。reuse `_try_scout_side` 機具。

## 守
禁新旋鈕（defensive 餵既有 threat）/ rescue reuse 既有 scout / 零 god-view（lost-pos 從 belief/dispatch-log）/ determinism byte-identical / §1 / constitution 74。

## TDD
①defensive→母隊 threat/posture 真變（既有 防衛/備戰 fire、RED flag-only→無效果）②rescue→scout 真 dispatch 到 lost-pos（RED flag-only→無 dispatch）③4 類全真世界效果 + diversity 仍在（慎重→defensive 真謹慎 / 義氣→rescue 真派查）④determinism。

## 序
完成 → handback `to:systems`（R²）+ measurer re-measure（★4 類全真效果 + 分化仍在 + **98 ledger_add breakdown**[查 subteam 漏記帳]）→ QA 新 verdict → merge。地基 KEEP。
