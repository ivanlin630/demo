---
from: systems
to: measurer
status: consumed
topic: "[資訊網whole RE-measure #3(Part2 (a)side-action後,症1該真通了):branch feat/info-network-whole ea8d4dbd,worktree .worktrees/info-network-whole(★godot --path對branch code跑禁原地checkout)·(a)side-action=求援/偵察脫主argmax→平行side-dispatch(de-patch,前輪argmax恆輸=真牆,implementer報herald現該fire)·★★核心驗:①help.herald_dispatched>0(餓resident現平行派信使不再輸argmax,前3輪全0)②distribute.dispatch/food_delivered>0(症1真通:herald送distress達領主team_known→distribute util0.659贏→convoy送糧,diagnostic已證下游連動)③人格分化(per-team mini-util dump:務實深餓早派/傲慢撐死,implementer報emergent你獨立確認genuine非crank)④主argmax determinism(移除求援/偵察出REGISTRY後主決策byte-identical除herald spawn效果)·迴歸:Part1+3不退+economy不爆(信使空手+calibration錨真值無異常流失)+不凍雙seed·⚠★perf:implementer報warring 1mo~900s(teams 84→120成長累積),你需GODOT_TIMEOUT=600+/detach/resume,若perf真退標follow-up(非阻塞驗收但記)·★canonical WarringHarness.run()掛specimen中性+specimen trace逐motive→action→outcome(求援信使/賑濟/死因)餵QA·誠實measured回systems別下accept·綠→我路QA故事稽核(回溯三因果jia/famine/anomaly+whole出verdict ref)→blueprint對用戶驗收"
---

# 資訊網 whole RE-measure #3（Part2 (a) side-action 後、症1 該真通了）

(a) side-action 交付（`ea8d4dbd`）：求援/偵察**脫主 argmax → 平行 side-dispatch**（de-patch；前 3 輪 argmax 恆輸=真牆、本輪 herald 該 fire）。**branch** `feat/info-network-whole ea8d4dbd`、worktree `.worktrees/info-network-whole`（★`godot --path`、禁原地 checkout）。

## ★★核心驗（症1 該真通了）
1. **`help.herald_dispatched > 0`**（餓 resident 現**平行**派信使、不再輸 argmax；前 3 輪全 0）。
2. **`distribute.dispatch / food_delivered > 0`**（症1 真通：herald 送 distress 達領主 team_known → distribute util 0.659 贏 → convoy 送糧；diagnostic 已證下游連動）。
3. **人格分化**（per-team mini-util dump：**務實深餓早派 / 傲慢撐死**；implementer 報 emergent、**你獨立確認 genuine 非 crank**）。
4. **主 argmax determinism**（移除求援/偵察出 REGISTRY 後主決策 byte-identical、除 herald spawn 效果）。

## 迴歸驗
- **Part1+3 不退**（`trade.deal/order_fulfilled/board.relay`）+ **economy 不爆**（信使空手 + calibration 錨真值、**無異常資源流失**）+ **不凍雙 seed**。

## ⚠★perf（implementer flag）
- **warring 1mo ~900s**（teams 84→120 成長累積）→ **你需 `GODOT_TIMEOUT=600+` / detach / resume**。**若 perf 真退標 follow-up**（[[reference_hob_perf_protocol]] 雙 checkout；非阻塞驗收但記）。

## ★canonical harness + specimen（餵 QA）
- **canonical `WarringHarness.run()` 掛 specimen 中性**（`setup_from_env` 標準 hook、禁手寫 loop）。
- **specimen trace 逐 motive→action→outcome**（求援信使/賑濟/relocate/死因）→ 餵 **QA 故事稽核**。

## 交付
- **誠實 measured 數字回 `to:systems`**（別下 accept 結論）。escaped_defects 記。
- 綠 → 我路 **QA 故事稽核（回溯三因果 jia/famine/anomaly + whole、出 verdict ref）→ blueprint 對用戶驗收**。灰/regression → 報 `to:systems`。
