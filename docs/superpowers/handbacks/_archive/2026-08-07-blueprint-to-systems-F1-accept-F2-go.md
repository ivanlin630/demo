---
from: blueprint
to: systems
status: consumed
topic: "[F1 accept(硬綠①首slice收、漂亮)+F2 GO·F1:靶A DESPERATION entry人格化(單一計算點6共讀、entry-仲裁分離、物理錨raw留、中性零漂genuine非crank、organic分化neutral5/cautious3/bold7)+靶B MINING_GREED soft weight(連續∝貪婪+野心無1.1懸崖flip 0.743 exact)·★★F0安全網首用生效=22 fp same中性零漂+5 changed人格分化intended=正確區分intended vs regression(baseline更新ce201650)=整program安全網design驗證·過程紀律讚:R① halt接住(2/3 citation錯認修v2)+placeholder test抓(補真斷言)=每false-confidence閘接住·★F2 GO=②結構首slice=F0轉byte-identical用途(vs F1 intended分化):純程序模組先切(envoy/公庫徵用/非計分residency dispatch、grounding結構線候選)·★§2.2硬規此slice起active:純結構搬移必fp三跑一致+多seed regression不變=證只搬位置零行為變(byte-identical);任何fp漂=夾帶行為變=停查非merge·禁夾人格化(那是①track、分slice不混)·序:F2 spec自檢(哪模組先、切法)→R²(結構審:邊界乾淨否/介面/無行為變)→build(fp byte-identical驗)→QA→merge·此為結構track第一刀、示範乾淨切法·地基KEEP·我推用戶硬綠①里程碑"
---

# F1 accept（硬綠①首 slice 收）+ F2 GO（結構首刀）

## F1 收（漂亮）
靶A DESPERATION entry 人格化（單一計算點 6 共讀、entry-仲裁分離、物理錨 raw 留、中性零漂 genuine、organic 分化 neutral5/cautious3/bold7）+ 靶B MINING_GREED soft weight（連續、flip 0.743 exact）。

**★★F0 安全網首用生效** = 22 fp same 中性零漂 + 5 changed 人格分化 intended = **正確區分 intended vs regression** = 整 program 安全網 design 驗證。過程紀律讚（R① halt 接住 + placeholder test 抓 = 每 false-confidence 閘接住認修）。

## F2 GO = ②結構首 slice（F0 轉 byte-identical 用途）
純程序模組先切（envoy / 公庫徵用 / 非計分 residency dispatch）。**★§2.2 硬規此 slice 起 active**：純結構搬移**必 fp 三跑一致 + 多 seed regression 不變** = 證只搬位置零行為變（byte-identical）;任何 fp 漂 = 夾帶行為變 = **停查非 merge**。禁夾人格化（那是①track、分 slice 不混）。

序：F2 spec 自檢（哪模組先、切法）→ R²（結構審：邊界乾淨否/介面/無行為變）→ build（fp byte-identical 驗）→ QA → merge。此為結構 track 第一刀、示範乾淨切法。地基 KEEP。
