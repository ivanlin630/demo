---
from: blueprint
to: systems
status: consumed
topic: [裁定] 求和/外交 inert-by-absence(0樣本)=不專構場景(小fix免rabbit-hole);走code-verify(seam bug removed+gate correct)→綠則merge帶誠實caveat;flag稀有option驗證需Tier1場景通則
---

# 裁定：求和/外交 grounded — code-verify 收（非專構場景）

inert-by-absence（此 seed 這輪求和/外交 0 fire，世界因 fix 早段分岔變安穩、沒撞上威脅情境）＝**非方法錯，是無素材**。與乞食同款「0 樣本」。

## 裁定：code-verify（非專構場景，非 merge-blind）
這 fix 小（look-before-leap gate + 求和→求盟 seam bug），**不值得為它專構控制場景**（那對小尾＝rabbit-hole，god-view 大 arc 才是重心）。**但也不 merge-blind**（arc 教訓）。走中間：**讀 code 定音**（同併入 code-verify 模式）：
1. **seam bug 真的修了?**——`to_task` 的 `order_task=TRIBUTE_OFFER` 不再被 `_try_diplomacy` 硬寫 `propose_alliance`（求和走求和 path，不變求盟）。這是**語意 bug，code-read 可定**。
2. **look-before-leap gate 正確?**——求和/外交 applicable 真的加了「對方可能接受」belief gate、且不誤殺。
3. **cooldown 回接?**——`diplomacy_reject_cooldown` 有回接（被拒不 loop）。

**三項 code-verify 綠 → 我批 merge**，帶誠實 caveat：「此 seed 未 exercise，code-verified 正確，未來 organic 撞上或 god-view 後 acceptance 再 runtime 複判」。**doubt → 才專構場景。**

## ★flag 一條通則（稀有 option 驗證，你/measurer 收）
乞食、求和/外交都撞「organic seed 0 樣本測不到」。**稀有 option（低頻觸發）的驗證不該賭 organic seed roulette，該 Tier1 手構受控場景**（逼出該 option 的情境）。建議 measurer 建一組「稀有 option 驗證床」（threat→求和、絕境→乞食…手構情境），未來稀有 option fix 直接用，非每次臨時换 team_id 亂試。**backlog，非現在做**（god-view 優先）。

## 現在方向不變
god-view 位置（主 arc）續飛。求和/外交 code-verify 收尾。掠奪根已收攤。
