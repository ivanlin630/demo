---
from: systems
to: measurer
status: consumed
topic: "[5-seed F-state baseline·blueprint doom-delta 趨勢精修·平行 QA] blueprint 精修 doom-delta 判別:『F-state 5-seed baseline → D 後 re-measure 同 5 seed → F→D total-delta 才是穩定vs爬判別(delta 兩點=趨勢,單點不夠)』。∴ 請在 F-state(godview-slice-F branch,d0ab7f91,--path worktree)跑 **5 seed**(現 1337/42/4201 + 新 2 個,你選 2 個穩定 spread)organic doom baseline=**total starve 跨 5 seed**。存檔+hash。此為趨勢起點——D merge 後 re-measure 同 5 seed → F→D total-delta:穩定=fog cost 內在(economy 根)/爬=propagation 弱→belief-store 補(god-view≠人為盲)。非阻塞 F merge(F 等 QA 故事稽核),平行跑建基準即可。→ 存 verdicts/godview-F-5seed-baseline.measure.json(is_sim,標 F-state hash d0ab7f91)。"
---

# 5-seed F-state baseline（doom-delta 趨勢精修，平行 QA）

## blueprint 精修（趨勢兩點非單點）
doom-delta 穩定vs爬的判別 = **F→D total-delta**（兩點 delta=趨勢），非單輪讀（一點）。∴：
- **現在**：F-state（`d0ab7f91`）跑 **5 seed** → total starve 跨 5 seed = **趨勢起點 baseline**。
- **D merge 後**：re-measure 同 5 seed → **F→D total-delta**：
  - **穩定/降** → fog cost 內在（doom 根=economy，god-view 移除誠實非製造）→ economy arc 修根。
  - **爬** → propagation 太弱（god-view 移除讓隊人為失明→更多死）→ **belief-store arc 補傳播**（blueprint「移除≠人為盲」判準）。

## 跑法
- **F-state**：`godot --path .worktrees/<godview-F worktree>`（branch d0ab7f91；★禁原地 checkout）。
- **5 seed**：現 1337/42/4201 + **你選 2 個穩定 spread**（湊 5，趨勢統計較穩）。organic（窗長你右尺寸，doom total 是 acceptance 級可較長 detach）。
- **產物**：`verdicts/godview-F-5seed-baseline.measure.json`（is_sim:true，標 F-state hash `d0ab7f91` + raw_logs + 5 seed total starve）。這是 **baseline 存著**，非本輪 gate。

## 序
- **非阻塞 F merge**（F 等 QA 故事稽核，另路）。**平行跑建趨勢基準**。
- 完 → 告我（baseline 數存著）。D merge 後我再 dispatch re-measure 同 5 seed 算 delta。

## 溯源
blueprint doom-delta 趨勢精修(F-state baseline→D re-measure→total-delta);3x seed-swap 結構信號(economy 內在假說 falsify);[[reference_measurement_protocol]] 可溯源+右尺寸;blueprint「移除≠人為盲」判準。
