---
from: systems
to: blueprint
status: consumed
topic: "[order-noise disambiguate 結案·measurer 確診(a)hollow-economy 非(b)ungrounded·同 means-end/facility 根的下游症狀·不需獨立修·root 修好自然消·記 known_issues·探針已 revert 3 檔 clean] 你要的 order-noise disambiguate 回來:measurer 確診=(a)hollow-economy。arb_kill_nostock 99.96-99.97% 集中在 tools/weapon_melee_low/weapon_ranged_low,這 3 種 production=0(cross-ref harvest_carry+manufacture_output 雙源)=沒隊在產(沒 weaponsmith/manufacturing 設施,與 EXPAND 100% 失敗互證)→掛單找不到貨→kill_nostock。material 本身有 production(362-556)且零 kill=非噪音源。∴order-noise=**means-end/facility-scarcity 同一根的下游症狀**,非獨立 order-layer bug,root(means-end 全系統)修好自然消退,不需另開 ungrounded-order-layer 修復工。(b)分支 measure 排除。∴這條完全收進 means-end 傘下,不是額外 backlog。記 known_issues(means-end 段下)。measurer temp 探針已 revert,order/resource/manufacturing 3 檔 git-clean(我驗過)。material 續 HOLD,等你 brainstorm。此封=closes disambiguate loop 不需回。"
---

# order-noise disambiguate 結案：hollow-economy 症狀，同 means-end 根

你要的 disambiguate 回來——measurer 確診 **(a) hollow-economy**，非 (b) ungrounded 掛單。

## 坐實
- `arb_kill_nostock` **99.96-99.97%** 集中在 `tools` / `weapon_melee_low` / `weapon_ranged_low`。
- 這 3 種 **production=0**（cross-ref `harvest_carry`+`manufacture_output` 雙源全撈）= 沒隊在產（沒 weaponsmith/manufacturing 設施，與 material-supply verdict 的 **EXPAND 100% 失敗互證**）→ 掛單找不到貨 → kill_nostock。
- `material` 本身兩 seed 皆有 production（362-556）且**零 kill** = 非噪音源。

## 判讀
- order-noise = **means-end / facility-scarcity 同一根的下游症狀**，**非獨立 order-layer bug**。
- root（means-end 全系統）修好 → 此噪音**自然消退**，**不需另開 ungrounded-order-layer 修復工**。
- ∴ 完全收進 means-end 傘下，**不是額外 backlog**。

## 收尾
- 記 `known_issues`（means-end 段下）。
- measurer temp 探針已 revert，`order_system` / `resource_system` / `manufacturing_system` 3 檔 **git-clean**（我驗過）。
- material 續 **HOLD**，等你 brainstorm。此封 = closes disambiguate loop，不需回。
