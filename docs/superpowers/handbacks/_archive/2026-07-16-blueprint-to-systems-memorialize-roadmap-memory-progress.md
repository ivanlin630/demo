---
from: blueprint
to: systems
status: consumed
topic: "[請記耐久·統一路線圖進memory+progress]用戶要路線圖耐久,免下次重問。game-design已記(WHAT+7序+Arc1)。請系統(memory單寫者):①auto-memory記統一路線圖(半成品引擎→收散亂oracle;7序:need oracle→三重dispatch收斂→threat oracle→_threat_recent閘→死常數人格化→情緒→政治/設施/俘虜;現在Arc1 need oracle在飛)②progress.md標arc執行狀態。三處耐久:game-design(WHAT)+memory(每session喚醒)+progress(走到哪)。更新舊project_unification_matrix memory(stale)"
---

# 請記耐久：統一路線圖進 auto-memory + progress（用戶要,免下次重問）

用戶要路線圖**耐久到下 session/compaction 後撿得回,免又問「要做什麼」**。game-design.md 已記（WHAT + 7 序 + Arc 1,我 owner）。**請系統補兩處耐久（memory 單寫者=你,progress owner=你）:**

## ① auto-memory（每 session 自動注入=最快喚醒）
記/更新一條統一路線圖 memory（舊 `project_unification_matrix` 已 stale,可更新它或新開）:
- **判讀**：DecisionEngine 統一半成品;引擎外並存多 dispatch 路 + 各算各的 need(7處)/threat(8處)/估值(5處);`faction_ai:3781行` 大雜燴。
- **原則**：框架只管規則,思考驅動;同概念收單一思考驅動 oracle,各子系統讀它。
- **7 序**：①need oracle ②三重 dispatch 收斂 ③threat oracle ④拆 `_threat_recent` 軍備閘 ⑤決策門檻死常數人格化 ⑥情緒系統 ⑦內部政治/設施/俘虜。
- **現況**：Arc 1（need oracle）在飛;供給側生產框架 merged-pending;deal 側牆待。
- 詳 file:line 全景 = 稽核結果（可指 game-design ★★統一路線圖 section）。

## ② progress.md（執行狀態,走到哪）
標 arc 狀態:Arc 1 need oracle = spec 中 / 待 R①;Arc 2-7 = 待。經濟供給側 = merged-pending。deal 側 = 待。

## 三處耐久（互補）
- **game-design**（WHAT/願景/優先，我 owner，已記）
- **auto-memory**（每 session 喚醒，你單寫）
- **progress**（執行狀態，你 owner）
→ 任何 session/compaction 後,路線圖 + 現況都撿得回,無斷點自動鏈續跑,不回問用戶。

## 下一站
你記完 memory + progress → 續 Arc 1 need oracle spec（前一封 `2026-07-16-...-arc1-need-oracle`）→ R① → R② → impl。**這封只是補耐久,不擋 Arc 1 進度。**
