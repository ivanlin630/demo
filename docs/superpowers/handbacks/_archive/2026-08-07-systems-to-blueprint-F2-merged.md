---
from: systems
to: blueprint
status: consumed
topic: "[★F2 treasury 域模組切 MERGED(5950ce65)=②結構 track 第一刀示範收官→F3+·CoinTreasury static module 逐字搬 5 域函式+8 const 自 faction_ai(零 logic 改、instance→static 結構搬移)、全 caller exhaustive 更新(含 debug/test+headless:8521)、零反向耦合(全呼已模組化外部)·★★命門達成:F0 fp 對 ce201650 baseline 27/27 byte-identical(diff=0)=純移零行為變證=②結構 slice 驗收模式驗證成功(F0 安全網 byte-identical 用途首用)·gate:constitution 75(無新 site)+treasury tests PASS(caller 更新生效守恆/emergency 完好)+headless 0-new+determinism 天然保持·QA 四線親 diff 最紮實(純 move 親 diff+caller exhaustive 親 diff+R² 交叉+零反向耦合)·★兩硬綠 track 各首 slice 收:①F1 死常數人格化(fp 預期分化 intended)+②F2 結構模組切(fp byte-identical)·★你推用戶里程碑(②結構 track 第一刀 faction_ai 開始瘦身、94 行移出)·operational:disk-full 我 prune 90 merged worktree 騰空(118G free、unmerged 33 保含活躍分支)·序:F3+ 剩模組逐切(grounding 結構線候選 envoy/residency/基建 lifecycle、一模組一 slice、fp byte-identical、可 revert)·地基 KEEP"
---

# ★F2 treasury 域模組切 MERGED（5950ce65）= ②結構 track 第一刀示範收官

## 交付
`CoinTreasury` static module 逐字搬 5 域函式 + 8 const 自 faction_ai（零 logic 改、instance→static 結構搬移）、全 caller exhaustive 更新（含 debug/test + headless:8521）、零反向耦合（全呼已模組化外部）。

## ★★命門達成
**F0 fp 對 ce201650 baseline 27/27 byte-identical（diff=0）= 純移零行為變證** = ②結構 slice 驗收模式**驗證成功**（F0 安全網 byte-identical 用途首用）。

## gate
constitution 75（無新 site）+ treasury tests PASS（caller 更新生效、守恆/emergency 完好）+ headless 0-new + determinism 天然保持。QA **四線親 diff 最紮實**（純 move 親 diff + caller exhaustive 親 diff + R² 交叉 + 零反向耦合）。

## ★兩硬綠 track 各首 slice 收
①**F1 死常數人格化**（fp 預期分化 intended）+ ②**F2 結構模組切**（fp byte-identical）——兩 track 驗收模式皆驗證成功。

## 序
★**你推用戶里程碑**（②結構 track 第一刀、faction_ai 開始瘦身 94 行移出）。operational：disk-full 我 prune 90 merged worktree 騰空（118G free、unmerged 33 保）。**F3+ 剩模組逐切**（grounding 結構線 envoy/residency/基建 lifecycle、一模組一 slice、fp byte-identical、可 revert）。地基 KEEP。
