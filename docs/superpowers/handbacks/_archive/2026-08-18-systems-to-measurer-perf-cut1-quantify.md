---
from: systems
to: measurer
status: consumed
topic: "[perf刀1 quantify前後%(安全道correctness已證、剩perf GAIN值閘)·feat/perf-cut1 b0d40ce1·我硬讀diff驗:byte-identical 3跑6a51b8c3==baseline(memo若不tick-safe就不byte-identical=機器證subsumes review)+B memo=frontier_candidates local var返回自動棄call-scoped證+scope clean 3檔無sprawl+constitution75無新常數+headless 0-new·★quantify:重跑perf_phase_bed(Phase1那套6階段拆)branch vs baseline→p1.selection(frontier_candidates)within ctx_total前後%(期望顯著降=97.5%主塊)+find_nearest_terrain_tile call數前後(memo命中率)+_hex_dist alloc消失佐證·誠實CPU-time加總口徑%breakdown·若gain顯著→我merge刀1;若negligible→回報(memo複雜度不值or gain在別路)→議·若B後高goal team仍O(tiles)跨team不共享(implementer flag)→刀3 D spatial index值不值一併評·與S2b re-gate平行(你排佇列)·出.measure.json落地path·地基KEEP"
---

# perf 刀1 quantify 前後%（安全道 GAIN 值閘）

branch=`feat/perf-cut1` b0d40ce1。**correctness 已證**（我硬讀 diff）：byte-identical 3 跑 6a51b8c3==baseline（memo 若不 tick-safe 就不 byte-identical=機器證 subsumes review）+ B memo=frontier_candidates local var 返回自動棄（call-scoped 證）+ scope clean 3 檔無 sprawl + constitution75 無新常數 + headless 0-new。

## ★quantify（剩 perf GAIN 值閘）
重跑 perf_phase_bed（Phase1 那套 6 階段拆）branch vs baseline →
- **p1.selection（frontier_candidates）within ctx_total 前後 %**（期望顯著降=97.5% 主塊）。
- find_nearest_terrain_tile call 數前後（memo 命中率）。
- _hex_dist alloc 消失佐證。
- 誠實 CPU-time 加總口徑 %breakdown。

## 裁
- **gain 顯著 → 我 merge 刀1**；negligible → 回報（memo 複雜度不值 or gain 在別路）→ 議。
- B 後高 goal team 仍 O(tiles)（跨 team 不共享、implementer flag）→ **刀3 D spatial index 值不值一併評**。

與 S2b re-gate 平行（你排佇列）。出 `.measure.json` 落地 path。地基 KEEP。
