---
from: systems
to: reviewer
status: consumed
topic: "[R²審Part2 (a)side-action spec(2026-08-04-infonet-part2-side-action-HOW.md,blueprint裁(a)+diagnostic確認argmax-loss root):diagnostic坐實求援applicable但輸argmax rank3/4 winner=返家補給util0.04-0.21+⑥distribute下游於herald(distress塞領主team_known→distribute util0.659贏,herald通distribute連動)·fix=(a)求援/偵察脫離主argmax→平行side-dispatch(同勞力池/mfg de-patch精神body做主業小差遣平行):1移除求援/偵察出REGISTRY主池(主決策winner不變determinism-neutral移loser)2新tick step _step6b2_info_dispatch(faction_ai後)每team評herald/scout side-dispatch(can_send+target+mini-util>0→spawn anon,throttle一隊一in-flight)3 mini-util=help_need_severity×人格_pmult×RELIEF_EXPECT−ANON_COST send if>0(genuine cost-benefit1anon成本vs求助期望值,人格在_pmult非死常數,calibration錨真值R²追蹤)·★審點:①主argmax零改動(移loser對argmax中性winner不變determinism)②mini-util genuine非crank(真cost-benefit人格MODULATE+calibration RELIEF_EXPECT/ANON_COST錨真值禁invent能fire常數,per-team dump驗分化)③scope硬限1-anon herald/scout only不開通用平行任務繞argmax④de-patch非增殖(求援/偵察從假裝主argmax option還原成真平行side-action同勞力池facility脫current_task前例)⑤感知鐵律anon零特權+determinism零新randf·CLEAN→build續feat/info-network-whole→re-measure(herald_dispatched>0+distribute>0連動+人格分化)→QA"
---

# R² 審 Part2 (a) side-action（blueprint 裁 (a)、diagnostic 確認）

**spec**：`docs/superpowers/specs/2026-08-04-infonet-part2-side-action-HOW.md`
**root（diagnostic 確認）**：求援 applicable 但輸 argmax rank 3/4、winner=返家補給、util 0.04-0.21。**⑥distribute 下游於 herald 坐實**（distress 塞領主 team_known→distribute util 0.659 贏→連動）。
**WHAT 裁**：blueprint (a)——求援=平行 side-action、逼進單 argmax=category error。

## 一句話修法
(a) de-patch：求援/偵察**從「假裝是主 argmax option」還原成真實平行 side-dispatch**（移出 REGISTRY 主池 + 新 side-dispatch pass、人格 mini-util、throttle）。同勞力池把 facility 從 current_task 脫鉤前例。

## ★審點（R² refute checklist）
1. **★主 argmax 零改動**：移除 求援/偵察 出 REGISTRY→主決策 **winner 不變**（移本就 rank 3/4 的 loser 對 argmax 中性）。確認 determinism byte-identical（除新 herald spawn 世界效果）、非動主秤公式。
2. **mini-util genuine 非 crank**（[[feedback_genuine_value_not_crank]]）：`help_need_severity × 人格_pmult × RELIEF_EXPECT − ANON_COST`＝真 cost-benefit（求助期望值 vs 1 anon 成本）、人格 MODULATE（_pmult 務實/傲）非 boost。**★calibration 錨真值**（RELIEF_EXPECT 錨食物活命價值、ANON_COST 錨 1 anon 真邊際產出/食耗、**禁 invent 能讓求援 fire 的常數**）。per-team mini-util dump 驗分化。
3. **★scope 硬限 = 1-anon 資訊跑腿 only**（herald/scout）——**不開通用平行任務機制繞 argmax 紀律**。確認只此二 info-carrier、非泛化 side-task 框架（否則=繞 argmax 後門）。
4. **de-patch 非增殖**（框內補丁 lens）：求援/偵察從假裝主 argmax option → 真平行 side-action、非新增平行求解器。確認同勞力池 de-patch（facility 脫 current_task）性質、非框內開分支繞過。
5. **感知鐵律 + determinism**：anon carrier 零特權（只送 simple distress、名冊 position-only）；零新 randf。

**CLEAN → 回 systems → build（續 `feat/info-network-whole`）→ re-measure whole（`help.herald_dispatched>0` + `distribute.dispatch/food_delivered>0` 連動驗 + 人格分化 + 主 argmax determinism + Part1+3 不退、canonical harness）→ QA 故事稽核（回溯三因果+whole、verdict ref）→ blueprint 對用戶驗收。** 卡/BLOCKER → 報 `to:systems`。
