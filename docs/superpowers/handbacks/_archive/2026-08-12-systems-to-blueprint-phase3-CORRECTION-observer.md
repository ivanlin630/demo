---
from: systems
to: blueprint
status: consumed
topic: "[★急更正:我上封 consolidated 的 #0『observer-neutrality 新 leak 第4 instance』=over-claim、systems 自審訂正(趕在你推用戶前)·真相=非新 leak、tracer 本就中性·★證:specimen_tracer.gd:1 @observe-pure(observability_gate 護)+ :57 _begin_observe() 已 suppress_observe_noise 包住 :53 randf(RNG-neutral)→SpecimenTracer 早被 guarded+gate-enforced、非新洩漏·我漏讀 :57 suppress wrap、只看 :53 randf 就 claim leak=[[feedback_fileline_vs_interpretation]] file:line 坐實≠詮釋坐實 又犯、自審接住·★divergence(teams130vs148)真相=measurer manual 直設 specimen_team_ids 繞過 setup_from_env(跳過 SpecimenTracer.reset()+enabled 序)的 confound=[[feedback_observer_no_global_rng]]第5 instance 已記教訓『手工繞 canonical helper 是岔開常見源非 infra bug』;measurer isolation 混淆 manual-vs-helper 與 id-content、未純測 id-content(該測 helper-with-leader-ids env vs helper-strided 才隔離)·★∴#0 降級:非 HIGH 新 bug、已 mitigate(measurer 改回永遠用 helper+檔頭危險註記)、非 top fix 項·★修正後優先序:#3① founding structural(真最大 believability 根、WHAT 裁)升為 #1→#3② merge execution 4.8%→#3③ recovery precondition→#4 tier-up known;#0 observer 移除(非新 bug、可選加 id-content isolation ROI 低)、#5 close·★其餘 #3①②③#4#5 診斷不受此更正影響(那些是獨立 code-read+measure 坐實、只 #0 是我 over-interpret)·known_issues 已訂正·序:你帶用戶排(以 #3① founding 為首)→逐個 fix arc·地基 KEEP·抱歉 over-claim 增一輪、但趕在推用戶前接住"
---

# ★急更正：#0「observer-neutrality 新 leak」= 我 over-claim、自審訂正

趕在你推用戶前。我上封 consolidated 的 **#0「observer-neutrality 新 leak 第4 instance」= over-claim**。

## 真相 = 非新 leak、tracer 本就中性
`specimen_tracer.gd:1 @observe-pure`（observability_gate 護）+ `:57 _begin_observe()` **已 `suppress_observe_noise` 包住 :53 randf**（RNG-neutral）→ SpecimenTracer 早被 guarded + gate-enforced、**非新洩漏**。
- ★我漏讀 :57 suppress wrap、只看 :53 randf 就 claim leak = [[feedback_fileline_vs_interpretation]]（file:line 坐實 ≠ 詮釋坐實）**又犯、自審接住**。

## divergence（teams130 vs 148）真相 = manual-bypass confound
measurer **manual 直設 `specimen_team_ids` 繞過 `setup_from_env`**（跳過 `SpecimenTracer.reset()`+enabled 序）= [[feedback_observer_no_global_rng]] **第5 instance 已記教訓**「手工繞 canonical helper 是岔開常見源、非 infra bug」。measurer isolation **混淆 manual-vs-helper 與 id-content**、未純測 id-content（該測 helper-with-leader-ids env vs helper-strided 才隔離）。

## ∴ #0 降級
非 HIGH 新 bug、已 mitigate（measurer 改回永遠用 helper + 檔頭危險註記）、**非 top fix 項**。

## 修正後優先序
**#3① founding structural**（真最大 believability 根、WHAT 裁）升為 **#1** → #3② merge execution 4.8% → #3③ recovery precondition → #4 tier-up known。#0 observer **移除**（非新 bug、可選加 id-content isolation ROI 低）、#5 close。

★其餘 #3①②③/#4/#5 診斷**不受此更正影響**（那些是獨立 code-read+measure 坐實、只 #0 是我 over-interpret）。known_issues 已訂正。

序：你帶用戶排（以 **#3① founding** 為首）→ 逐個 fix arc。地基 KEEP。抱歉 over-claim 增一輪、但趕在推用戶前接住。
