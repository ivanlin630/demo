---
from: systems
to: implementer
status: consumed
topic: "[dispatch build 統一派遣模型(用戶拍 B 名帶匿+歸隊真架構修、行為變 slice、spec docs/superpowers/specs/2026-08-11-unified-dispatch-model-design.md LOCKED blueprint WHAT+R①硬數據+R² CLEAN)·新 slice feat/unified-dispatch off 更新後 main(199dd40b)·★fix scope=3 真 drain 點(scout _try_scout_side:2062 / care-scout _dispatch_care_scout:5137 / rescue :5231——現 dispatch_anon_messenger 孤匿名 leader_id=-1→succession 安全網 faction_ai:784 誤升 named→不歸隊 monotonic drain)·herald 移出(letter-carrier 不 spawn subteam、R² 訂正)·★★§HOW-binding:①§2 組成 named-led(依重要性 util 秤:關鍵事親信多記名/routine 單人一次要記名跑腿[單獨=記名非孤匿名]/規模事記名領隊+匿名跟班;重要性×可動用記名數×信任×人格 modulate、記名稀缺 genuine 戰略約束非 crank)②§3 全員歸隊(記名歸 roster、匿名跟班歸池、無 monotonic drain)③§4 匿名→named 只湧現(領隊死接班/跟班不爽脫隊;機械升格除=3 點修後從不誕生 leaderless subteam→784 從不誤觸;主動升格 parked)④§1 匿名不落單單獨必記名·★機制:改 3 點 dispatch_anon_messenger(leaderless)→dispatch()(named sub_leader 如既有 envoy/builder/convoy)、借 spare 記名領隊→非 leaderless→succession 不誤觸;★不動 faction_ai:784 本身(真 leaderless 團 succession 照走)·★★驗收§6:anon 不再 drain+組成分化+湧現升格照 fire+機械升格 0+★re-measure 下游(relief/care/builder anon 漏光疑真根、修後派得出否、團數/O(N²)幽靈團硬數字非預設 4×over-claim 教訓)+determinism 3-run byte-identical+無 regression(recovery/cohesion/info-net/iii)+constitution 綠·★★行為變 slice=F0 fp 預期分化 intended·完成 handback to:systems R²→measurer 量→QA specimen→merge→re-measure→blueprint 推用戶·地基 KEEP"
---

# dispatch build 統一派遣模型（用戶拍 B、行為變 slice）

spec：`docs/superpowers/specs/2026-08-11-unified-dispatch-model-design.md`（LOCKED、blueprint WHAT + R①硬數據 + R² CLEAN）。新 slice `feat/unified-dispatch` off 更新後 main（`199dd40b`）。

## ★fix scope = 3 真 drain 點
scout `_try_scout_side:2062` / care-scout `_dispatch_care_scout:5137` / rescue `:5231`——現 `dispatch_anon_messenger` 孤匿名（leader_id=-1）→ succession 安全網 `faction_ai:784` 誤升 named → 不歸隊 monotonic drain。herald 移出（letter-carrier 不 spawn subteam、R² 訂正）。

## ★★§HOW-binding
1. **§2 組成 = named-led**（依重要性 util 秤：關鍵事親信多記名 / routine 單人一次要記名跑腿[單獨=記名非孤匿名] / 規模事記名領隊+匿名跟班；重要性 × 可動用記名數 × 信任 × 人格 modulate、**記名稀缺 genuine 戰略約束**非 crank）。
2. **§3 全員歸隊**（記名歸 roster、匿名跟班歸池、無 monotonic drain）。
3. **§4 匿名→named 只湧現**（領隊死接班真 succession / 跟班不爽脫隊真動機；機械升格除=3 點修後從不誕生 leaderless subteam→784 從不誤觸；主動升格 parked）。
4. **§1 匿名不落單、單獨必記名**。

## ★機制關鍵
改 3 點從 `dispatch_anon_messenger`（leaderless）→ `dispatch()`（named sub_leader、如既有 envoy/builder/convoy）、借 spare 記名當領隊 → 非 leaderless → succession 不誤觸。★**不動 faction_ai:784 本身**（真 leaderless 團 succession 照走）。

## ★★驗收（§6）
anon 不再 drain（指標團池穩）+ 組成分化 + 湧現升格照 fire + **機械升格=0** + ★re-measure 下游（relief/care/builder anon 漏光疑真根、修後派得出否、團數/O(N²)幽靈團**硬數字非預設**[4×over-claim 教訓]）+ determinism 3-run byte-identical + 無 regression（recovery/cohesion/info-net/iii）+ constitution 綠。★★**行為變 slice=F0 fp 預期分化 intended**（記錄方向）。

## 序
handback `to:systems`（R²：named-led 3 點 + 歸隊 + 機械升格 0 + §2 util genuine 非 crank + fp 分化 intended）→ measurer 量（anon 穩+組成分化+湧現升格+下游 re-measure 硬數字）→ QA specimen → merge → re-measure → blueprint 推用戶。地基 KEEP。
