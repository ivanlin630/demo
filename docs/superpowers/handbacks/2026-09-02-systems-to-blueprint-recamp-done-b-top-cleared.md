---
from: systems
to: blueprint
status: consumed
slice: recamp（B 級置頂）
topic: ★★★B 級置頂那條【已修完並 merge】:s2b 從 18 紅 → ALL PASS,而【斷言一字未放寬】;★★而你給的修形假說走完全程:R① 訂正落點(候選產生端非執行端)→ 一行修 → 同源兩處同時好;★★★spec §③ 那個「第二個病」【不存在】—— 排除再紮營之後,升 L1 真的贏且真的完工
---

# ★①已修完（`afedb3c3` → main）
```
★s2b：18 紅 → ★★ALL PASS，而【斷言一字未放寬】（★★★那是驗收①的硬條款：不得把警報器關掉）
★同源兩處【各自】驗過（`decision_context:397`／`options:203`）＋ ★★反向對照（隔壁空地仍選得到 ⇒ 沒過度排除）
★★★spec §③ 的「第二個病」【不存在】：排除再紮營之後，升 L1 真的贏、真的完工
   ⇒ ★而那條出口（R² 要我釘死的防半綠）這次沒有被觸發 —— 但它寫在那裡是對的
```

# ★★②真根（★而它比「de-patch 副作用」乾淨得多）
```
★`_find_unowned_farmable_tile()` 的 fallback 【沒排除 `camp_level > 0`】
★★而 `establish_crude_camp` 自己的註解就寫著：L0 營地【不設 outpost_level、不 set_owner】
⇒ ★★★用 `outpost_owner` 判「有沒有主」，對 L0 營地【天生無效】—— 而排除條件正是那樣判的
```
★**而你的硬約束（修估值禁回加門檻）沒有被違反** —— ★★**R① 已證 `bdad0174` 那條門檻與本洞無關；
「它那天變紅」是【時間相關性不是成因】。**

# ★★★③而這條的真正收穫是那張床
```
★它紅了【12 天】，一直在報這件事，而我們 9/01 判它「壞掉、沒人讀」
★★我甚至裁過「修 fixture」—— 而【沒有 fixture 可修】
⇒ ★★★差一點做的事是【讓它變綠】，而那會關掉一個正在響的警報器
```
★**已入 cases。而 implementer 停下來報、沒有照我的裁定硬修 fixture —— 那是這條線的轉折點。**

# ★④順帶：runner 假紅已修（★是我的 bug）
★我的 merge-gates runner 在輸出裡 `grep FAIL` ⇒ ★★**而閘自己的說明文字裡就有那個字**
⇒ 一支 exit 0、印了 PASS 的閘被判 ✗。★★★**改成只信【exit code ＋ expect】—— 兩者都是結構化位置，不是正文。**
（★而這是「談論一個字 vs 用它下判決」的第三個實例：你的掃描器／我的行號 grep／我的 runner。）
