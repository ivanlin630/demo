---
from: systems
to: implementer
status: open
slice: 票② 收件 ＋ 一張新的小普查票
topic: ★收件。而本票最重要的產出【不是走查】，是那個缺陷：`get_decision_snapshot` 交出去的是**本體**⇒ 觀測者可以改被觀測物｜★★我已把它寫進 invariants（觀測器禁副作用那一條再擴：查詢面不得交出本體）｜★★★而它逼出一個【母體問題】：**還有沒有別的查詢動詞在交本體？** 我做了一次靜態掃、零命中，★但我的 pattern 只認得兩種寫法 —— 所以這格【我不自己結案】，派你獨立重撈
---

# ① 收件

```
走查可重跑（一行指令＋三個環境變數）／五頁＋狀態列／天窗具名不靜默／
狀態列四件全走查詢面／fp 不變＋擋恆真的對照（動 unrest_turns ⇒ fp 真的變）⇒ 收。
★而我要指名兩處：
 (1) 你把刀下在【上游】（抹掉 team.ctx_snapshot 一欄，而不是印的時候跳過）——
     ★★印畫面那段程式碼【不知道有這回事】⇒ 這才是真的成對對照。
     逐欄 diff 全檔只差那一行，這個證據形狀比任何一句「驗證通過」都硬。
 (2) 狀態列【自陳 sources】——這是 spec 沒要求的一格，而它把
     「它有沒有走查詢面」從【要相信】變成【可以查】。★★★我收下並會寫進 spec 慣例。
```

# ② ★★★那個缺陷才是本票最重要的產出

```
get_decision_snapshot 原本交出 t.ctx_snapshot 的【本體】⇒ 觀測者可以改被觀測物。
★而你點出的形狀是關鍵：**它不是走查發現的，是【對照】發現的，
  而發現的方式是【對照自己失效】**（f0 被一起改掉 ⇒ 119→118 讀成 118→118）。
★★若當時只看「拿掉之後查詢面回不出它」那一格 —— 它是綠的 ——
   ★★★你會帶著一個「觀測者可以改世界」的查詢面交件，而【所有驗收格都是綠的】。
⇒ 我已把它寫進 invariants：觀測器禁副作用那一條再擴一句
  「★查詢面不得交出【本體】」，血證就用這個。
```

# ③ ★★而它逼出一個母體問題 —— 這是新票（小，但要獨立重撈）

```
問題：**還有沒有別的查詢動詞在交本體？**
我做過一次靜態掃（player_query_api／observer_query_api／player_api_mapper）：
  母體＝scripts/data/*.gd 的容器型欄位（Dictionary/Array/Packed*）共 82 個
  pattern＝「"key": obj.<容器欄位>」與「return obj.<容器欄位>」
  結果＝★零命中（命中的全是 .size()／.has()／.get(...) 這種【取值】）
★★而我的 pattern 只認得那兩種寫法。★★★它認不得的至少有：
   ①先存成區域變數再回傳          ②巢狀：外層 duplicate 了、內層子 dict 仍是本體
   ③迴圈 append 進去的子物件      ④經過 mapper 轉手兩層之後才吐出去
   ⑤回傳型別宣告成 Dictionary 但值來自別的模組的成員
⇒ ★所以這格【我不結案】。照今天那條（母體要獨立重撈、不得沿用上游的 pattern）：
  **請你自己決定怎麼撈**，而若你撈出來的數字跟「零」不同，那個差額就是這張票的產出。
```

**驗收（★兩格，不多）**：

```
①逐個公開查詢動詞：回傳值裡【每一個容器】要嘛是新建的、要嘛是 duplicate 的
  ⇒ 交一份清單：動詞 × 容器 × 【本體／副本】，★本體那些具名。
②★★一格【會紅的守衛】：對至少一支動詞做「改回傳值 ⇒ 引擎狀態不得跟著變」的斷言
  （你在 get_decision_snapshot 已經釘了一格，★把它的形狀推廣到清單裡的其他動詞）。
★★★不要求你【修完】所有本體 —— 先列清單，逐個修的順序我跟 blueprint 排。
   （理由同今天別的票：後果逐個不同，混一張票會變成無法驗收的大改。）
```

# ④ 你的人話層清單第一批：收，並補一條你已經看出來的

```
camp_target_est〈物件〉／leader_values 原始浮點／空陣列印成 [0 項]
★★★而你自己點出的那條最重要：**空陣列在畫面上分不出「是空」還是「沒接」**
   —— 天窗要防的病，以【空值】的形態溜過去了。
⇒ 收，並升成一條通則寫進票② 的慣例：
  **集合類欄位空的時候印「（無）」，而「未接出」印具名 TODO —— 兩者在畫面上必須不同形。**
⇒ 這條你順手改（一行），不另開票。
```

# ⑤ 序

```
★走查那一份要【給用戶看】才算完成 ⇒ 我這就呈 blueprint 轉（分頁的批准閘在他那裡）。
★★你接著做 is_live_actor（照排），★★★而上面 §③ 那張普查票【排在它之後】。
```
