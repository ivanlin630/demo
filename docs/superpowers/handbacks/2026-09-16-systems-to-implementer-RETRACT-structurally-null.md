---
from: systems
to: implementer
status: open
slice: 掠奪 ｜ **撤回：「結構上必為 null」是我錯的，而且我講得很用力**
topic: ★★★**我的錯逐字**：我從「**現在**那些掠奪從哪條路出來」推論「改 coeff 碰不到它」—— **而改動可以把掠奪【搬到另一條路上贏】** ⇒ **我把一個【會被改動影響的變數】當成了常數**｜★**標籤改回 A**，而且對照**不是 null**｜★★**你那條誠實限畫得正確**：兩輪世界整個分岔 ⇒ **28 不可直接歸因**；而 **`won_anyway` 0 → 3 是同一條路上的同義比較 ⇒ 那一格可引用** —— ★★★**這兩句的分法，就是今天「跨樹不可比」那條判準的正確用法**｜★**而最硬的證據是來源整條換掉**：before 全 `[Survival]`／after 全 `[SoloAI]` 21 筆、零筆 Survival
---

# ① 我錯在哪（★這一顆我要留下形狀）
```
我做的推論：
  \u2460 查「**現在**掠奪從哪條 rank 出來」 ⇒ `rank_survival`
  \u2461 查「`rank_survival` 讀不讀 coeff」 ⇒ 不讀
  \u2062 ⇒ 結論「**改 coeff 對掠奪結構上無效**」
★**②是對的、①也是對的，而③錯了** —— 因為 ★★**①的答案【本身就會被這個改動改變】。**
⇒ ★★★**通則**：**用「現況的路徑」推論「改動後的效果」，等於假設路徑不變** ——
  **而路徑正是決策系統裡最容易被改動搬動的東西。**
⇒ **要問的那一句是**：**「這個改動會不會讓它【換一條路贏】？」** —— **我沒問。**
```

# ② ★★你那條誠實限我要指名（它是今天判準的正確用法）
```
・**28 這個數**：兩輪世界整個分岔（非 perf 差異 11197 行）⇒ ★**不可直接歸因**
・**`won_anyway` 0 → 3**：同一條路、同一個定義 ⇒ ★★**可引用**
⇒ ★★★**同一份跑，兩個數，一個能用一個不能用** —— **而你把它們分開了。**
   **這正是「跨樹不可比」那條判準該有的樣子**：**不是整份作廢，是【逐格判可不可比】。**
```

# ③ 標籤：**改回 A**
```
・「無對照可驗」那句 ⇒ **刪掉**
・★**改寫成**：**「對照有效；而 `28` 因世界分岔不可直接歸因，可引用的是 `won_anyway 0 → 3`
  與【來源整條換掉】（before 全 `[Survival]`／after 全 `[SoloAI]` 21 筆）。」**
```

# ④ ★★而這件事比標籤大 —— 我要呈 blueprint
**掠奪從「絕境階梯的產物」變成「引擎在正常決策裡選的」** ——
★**這不是這張票的副作用，這可能就是它真正做到的事**：
**以前只有快餓死的隊會去搶（`[Survival]`），現在一支【算過划算】的隊會去搶（`[SoloAI]`）。**
