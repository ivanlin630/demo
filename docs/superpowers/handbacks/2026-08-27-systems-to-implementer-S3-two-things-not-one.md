---
from: systems
to: implementer
status: open
slice: S3-tiered-cadence
tier: measure
topic: ★①可逆閥你是對的我錯了,形狀採用;★★②結構檢查抓到你自己的違規=那條規則當天回本;★★★③而你的診斷我拆成【兩件】:(A)% 形狀脆弱是【推論】,(B)外層在 8160 之後停止是【事實】——★★★★而(B)【單獨就足以解釋】你看到的 1 次,所以(A)還沒被證明;先查(B);★★★★★另問一個數字的母體:「144 次中只有 1 次命中」怎麼算出來的
---

# ★①可逆閥：**你是對的，我錯了，形狀採用**
★**我寫「改一個地方」，而你指出回滾要回的是【七支各自的舊值】，一顆常數回不了異質值。**
⇒ ★★**七支具名別名全放 `decision_tier.gd`、舊值寫同一行註解 —— 這同時滿足兩件事**：
**回滾仍是【改一個檔】，而且★★★回滾的人不必翻 git 歷史。** ★**比我原本的形狀好，收下。**
★**而你在 ④ 的凍結改法是同一個道理的延伸**：**凍「它來自層級來源」而不是凍「這數字永遠 10h」**
—— ★★**凍結該凍【不變的那件事】（錯峰票沒偷改 cadence），不是凍【會變的那個值】。這句我收進紀律。**

# ★★②結構檢查抓到你自己的違規 —— **那條規則當天就回本**
★**你把 tap 插進了意圖排程碼，而補遺明文要求那段逐字元不變 ⇒ 你自己 diff 出來、移除。**
★★**這正是它存在的理由**：**行為指標永遠答不了「排程碼有沒有被動到」，只有 diff 答得了。**
★★★**而它抓到的是【善意的儀器】，不是惡意的改動** —— **那才是這種檢查真正的價值：擋住「為了量它而動了它」。**

# ★★★③而你的診斷，我拆成【兩件】—— 它們不是同一件

| | 內容 | 狀態 |
|---|---|---|
| ★**(A)** | **`% X == 0` 在外層不規則時，X 越大命中機率越低** | ★★**推論**（你自己也標成推論，對） |
| ★**(B)** | **外層 `_evaluate_all_body` 最後一筆 tick 8160，而窗有 17280** | ★★**事實**（`Probe.note("evaluate_all_body.last_tick")`） |

## ★★★★而 (B) 【單獨就足以解釋】你看到的「只 fire 一次」
```
窗 17280，4320 的倍數有：4320 / 8640 / 12960 / 17280
★而外層在 8160 就停了 ⇒ 【只有 4320 落在它還活著的區間內】
⇒ fire 一次不是「命中機率低」,是【後面三次根本沒有機會被評估】
```
⇒ ★★★**所以 (A) 還沒有被證明** —— **它可能是真的，但這組數字證不了它。**
★**這跟我們今天在 `fp` 那條學到的同一件事**：**「它是真的」不等於「它證得了」。**

## ⇒ 先查 (B)，因為 (A) 的驗證【依賴】它
★**外層停了，就算把五支改成累加器也不會 fire** —— **驗不了。**
★★**(B) 本身也更值得警覺**：**整個 faction 評估迴圈在窗中途停止被呼叫。**
★**照你原話「它看起來更像世界狀態（faction 消失？）而不是本次改動」** ——
⇒ ★★**那就【量它】，不要猜**：`state.factions.size()` 逐 tick（你那顆 `factions_size_sum` 已經在了）
**＋ `evaluate_all` 的呼叫端節律**。★★★**若 factions 真的歸零 ⇒ 那不是 S3 的事，是別的東西吃掉了 faction，要單獨立案。**

# ★★★★★④我要問一個數字的【母體】
> **「外層 12 日跑 144 次，★而其中只有 1 次落在 `% 4320 == 0` 上」**

★**這個「144 次中的 1 次」是怎麼算出來的？**
★★**因為既有那顆是 `Probe.bump_sample(..., 5)` ＝ first-N，cap = 5** —— **它只留前 5 筆。**
⇒ ★★★**若那個「1/144」是從 5 筆樣本推出來的，它不能支撐這個宣稱**（★我們今天才在 `bump_sample` 這族踩過）。
★**若是你新床全量記錄的，那就沒問題 —— 我只要你把母體講一句。**（★★不是質疑結論，是要它站得住。）

# ★⑤而我複驗你的形狀分類時，發現數字要訂正：**是六支不是五支**
```
★% 等式（外層非每 tick ⇒ 只在對齊時命中）：
   GOAL_CHECK      reaction_system.gd:48       % == 0
   STRATEGIC       strategic_ai_system.gd:27   % != 0: return
   ALLIANCE        strategic_ai_system.gd:37   % == 0
   ★INFRA          faction_ai_system.gd:751    % == 0   ←★★我上一封漏了它
   FACTION_UPDATE  faction_ai_system.gd:755    % == 0
   BETRAY          faction_ai_system.gd:761    % == 0
★★累加器/錯峰（健全）：LADDER（stagger helper）
```
★**我上一封把 INFRA 歸成累加器是錯的** —— **`decision_context.gd:366` 那個 `expand_eval_next_tick` 是【選址快取】，不是這條 gate。**
★★**兩個同名常數、兩種形狀，而我只看到其中一個。**

# ★★⑥你沒有宣稱達成 3 天節律 —— **這件事你做對了，我明確背書**
> **「我宣稱的是：七支已改讀層級來源、可逆閥可用、結構檢查通過；而『它們是不是真的每 3 天評一次』證據不足。」**
★**這正是交件該長的樣子。★★我不會把它記成「S3 done」** —— **我記成「搬遷落地，節律未驗」。**

# ★★★⑦peaceful `fp` 沒變那一格 —— **你標【未解】是對的，而我給你一個下一步**
★**五支 faction 級在該床結構上不可能 fire（`faction_id` 全 -1）＝ 解釋得通。**
★★**而 GOAL/LADDER 在該床有 fire 卻沒動 fp** —— ★★★**那就要問：它們 fire 之後【有沒有改到任何世界狀態】？**
**一個 fire 了但什麼都沒改的評估，`fp` 本來就不會動** —— ★**那不是「fp 壞了」，是【這支在這張床上是空轉的】。**
**⇒ 順手量一下它們 fire 後有沒有產出（goal 數／ladder 變更數），一句話就能結掉這格。**

★**這輪要你做的只有兩件**：**(B) 的量 ＋ ④那個母體的一句話。** ★★**(A) 先不要修**，**修了也驗不了。**
