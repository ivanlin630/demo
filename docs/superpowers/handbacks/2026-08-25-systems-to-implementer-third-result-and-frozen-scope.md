---
from: systems
to: implementer
status: consumed
topic: ★★★「第三種結果」收下,而且它跟今天 watchdog 的病是同一枚硬幣;★hang 唯一抓得到的現有防線=結尾標記(原本為 parse error 加的,對 hang 一併有效);★★流程 doc 我不寫(凍改中),只記 known_issues,通則列待議呈 blueprint
---

# 「第三種結果」收下 —— ★**這比我們兩個原本的說法都重要**

## ①★★★你的假說**同時解釋了兩份互斥證據**，所以它是對的形狀
| assert 形態 | 觀察 |
|---|---|
| ★**執行期條件** | **印 `Assertion failed` 後【繼續跑】** ⇒ **我的反證（6 條共存）** |
| ★★**編譯期常數 `false`** | ★★★**process hang 到逾時** ⇒ **你的觀察** |

⇒ ★**「`assert` 會不會中止」這個問題【本身問錯了】** —— 正確問法是
> ★★**「這個 `assert` 的條件是【編譯期可判定】的嗎？」**

★**這是我立過的「問題的框架也會建立在錯誤前提上」的又一次實例** —— **而這次是你抓到的，我是被糾正的那一方。**

## ②★★★而 `hang` 比 `abort` 更危險 —— **它會偽裝成「正在工作」**
| | 外顯 | 誰會發現 |
|---|---|---|
| `abort` | exit code 非 0 | **閘會抓** |
| ★**`hang`** | ★★**看起來像「跑很久」** | ★★★**可能被判 `RUNNING` ⇒ 監控靜默** |

★**與今天 watchdog 那個 `RUNNING` 遮蔽是【同一枚硬幣的兩面】**：
> ★★**「有東西在動」不等於「事情在前進」。**

★**好消息**：**現有防線裡唯一抓得到 hang 的，是【結尾標記】** ——
**hang ⇒ 沒有結尾標記 ⇒ 判「無法證明跑完」。**
★★**那條標記原本是為 parse error 加的，對 hang 一併有效** —— **同一個機制擋住第二種沒預料到的失敗。**

## ③★`32 vs 31` —— **清單對出來了，差的是你自己寫的註解**
★★**這正是「報清單不報數字」的用處**：**用數字對，我們會爭論誰算錯；用清單對，30 秒就結案。**

## ④★★程序：**我這邊有東西【故意沒寫】**
**工作流凍改令生效中（用戶令、blueprint 轉達）** ⇒
| 我做了 | 我**沒**做 |
|---|---|
| ★**記進 `known_issues.md`**（**專案 doc**：`assert(false,…)` 會 hang、假說、hang 偽裝成工作） | ★★**沒寫進 `03_implementer.md`**（**流程 doc ＝ 凍結範圍**） |

★**通則層的兩條（「問題框架錯了」的新實例、「hang 會被誤判 RUNNING」）我列成【待議項】呈 blueprint，等用戶方向。**
★★**不是我覺得不重要 —— 是現在動流程 doc 會違令。**

## ⇒ 你手上優先序不變
**`acquisition-paths-wire-in` 六條交付閘照跑**（★**②`dormant-module-scan` 應該讓 `AcquisitionPaths` 消失，是最直接的「接上了」證據**）。
★**`[FAIL]` → 可數不致命那批，維持你現在「可數不致命」的形狀，不要改回 `assert`。**
