# Hand Back: Text UI + 座標正數化

## 實作摘要
- `world_generator.gd`：tile_pos 和 tile_id 加 (radius, radius) offset，所有座標 ≥ 0
- `headless_test.gd`：更新所有在地圖內的 tile_pos/tile_id 引用（超出地圖的測試位置保持不動）
- `text_map_renderer.gd`（新建）：static render()，可在 headless 驗證
- `text_ui_main.gd`（新建）：Node 控制器，keyboard input + tick advance + UI refresh
- `scenes/TextUI.tscn`（新建）：TextUI 主場景
- `project.godot`：主場景改為 TextUI.tscn

## 與 spec 的差異
- `WorldState.TICKS_PER_DAY` 不存在為 class constant；使用 `_state.ticks_per_day`（instance var = 24）
- MapLabel 改為 `bbcode_enabled = false`（spec 為 true，但 true 會吃掉游標括號 `[P]`/`[@]`）
- HintLabel 移除 `[P]成員`（KEY_P 未實作）

## 連動風險
- 座標正數化後，所有舊的負數座標硬碼（如其他系統中的 tile_pos 計算）可能失效；已確認 simulation systems 都用 tile_pos delta 計算，不依賴絕對座標符號
- `text_ui_main.gd` 的 `_ready()` 只初始化玩家 team，實際遊玩需要更完整的世界初始化
- `WorldState.TICKS_PER_DAY` 在 text_ui_main 中用 `_state.ticks_per_day` 代替；time-constants-refactor 合併後需確認欄位名稱一致
- `SimBridge.advance_ticks(1)` 遇到事件時提前停止 loop，Space 鍵可能推進少於 TICKS_PER_DAY ticks；行為上是每次 Space 推進「至少一個 tick，遇事件停止」而非整天

## 待主 session 確認
- Space 鍵提前停止（遇事件）是否符合預期？或需改用 `_runner.advance_tick` 直接迴圈？
- 初始 team 設定是否需改成載入完整 headless_test 等效的多 team 世界？
- 目測確認（需開 Godot 啟動 TextUI）是否正常顯示地圖和狀態區？
- `TextMapRenderer` 的 explored state 目前 = in_vision（TODO comment），是否需在此 branch 補完？
