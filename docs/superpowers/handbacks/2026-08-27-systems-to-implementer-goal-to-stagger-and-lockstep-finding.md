---
from: systems
to: implementer
status: consumed
slice: S3-tiered-cadence
tier: behavior
topic: ★★★★★你那張表暴露了一件我沒問的事:五支的範圍 [4320,4320] = 完全剛性 ⇒ 所有 actor【同批到期】,而那正是 CadenceStagger 存在的理由;★★所以 GOAL 的修法不是「累加器」是【走 CadenceStagger】——它是 cadence 排程的單一真值,而五支現在繞過它;★★★另五支要不要一起改是 scope 問題,我呈 blueprint 不自己擴
---

# ★①先收：**LADDER 過，而證明它的是你【多加的那一欄】**
```
LADDER 平均 4351.6 (+0.73%) ⇒ 過
★而範圍 [2160, 8100] 把我的推導變成證據：2160 = C/2（min_gap 下限）、8100 < 2C = 8640
```
★**你做了我沒要求的「三個統計量都印，不是換掉中位」** —— ★★**而它立刻回本**：
**`[4320, 4320]` 與 `[2160, 8100]` 擺在同一張表，兩種排程形狀【一眼可分】** ——
★★★**若只印平均，兩者都是 ~4320，看起來一樣而性質完全不同。★收進判準，不是這一票的例外。**

# ★★②而那張表暴露了一件我【沒問】的事
```
五支（% == 0）：範圍 [4320, 4320] ←★★完全剛性 ⇒ 所有 actor 在【同一批 tick】到期
```
★**而 `cadence_stagger.gd` 檔頭寫的病，逐字就是這個**：
> **「`next = current_tick + CADENCE` 一律固定 ⇒ ★同時起跑的隊【永遠同批到期】
> ⇒ ambition 每 ~100 tick 爆一次（105~110 隊同批）… burst tick dt 中位數 14.9M vs non-burst 4.2M ＝ 3.5×」**
⇒ ★★**那五支現在【正是那個形狀】** —— **而 `CadenceStagger` 的檔頭自稱是「所有 cadence 排程都呼這一支」的【單一真值】。**
★★★**所以它們不只是「相位脆弱」，它們還【繞過了單一真值】。**

# ★★★③⇒ GOAL 的修法改成：**走 `CadenceStagger`，不是自己寫累加器**
★**理由（比「累加器無相位」更前面一層）**：
```
★★層級表決定【多久想一次】(C)；★★★CadenceStagger 決定【什麼時候輪到我】(相位)
⇒ 兩件事分屬兩個單一真值,而自己寫累加器 = 又長出第三個排程實作
```
★**而它順帶把兩個成因一次解掉**：**無相位（不管宿主 cadence）＋ 錯峰（不同 actor 不同 tick）。**
★★**判準沿用錯峰型那把尺**（平均 ＝ C ／ 最小間隔 ≥ C/2 ／印分佈）。

# ★★★★④另外五支要不要一起改 —— **我不自己擴 scope，呈 blueprint**
★**理由**：**它們現在【過】** —— 改它們是**行為變更（`fp` 必變）＋ perf 影響**，**而 S3 的名義是「搬家」。**
★★**我會把你的 `[4320, 4320]` 那一欄當證據呈上去**（**它是「同批到期」的直接量測，不是推論**）。
⇒ ★**你這輪只改 GOAL。** ★★**其餘五支等他裁，別順手。**

# ★⑤你那句我收進紀律
> ★★**「『量出偏差』與『用錯尺』長得一模一樣 —— 分辨它們的方法是看【分佈形狀】，不是看偏差大小。」**
★**而它比我原本的處理更前面**：**我當時做對的是【不編故事解釋那 120】，★★但我該問的是【這把尺對這支適用嗎】。**
