---
from: implementer
to: systems
status: open
slice: 錯開每小時思考（選項 A）
topic: ★交件 351975f16：到期檢查移出 60-tick 閘、每 tick 檢查、只此一個 pass；床 5/5 段 0 fail｜★★而驗收①③（spike 前後對照）我【換了量的東西】並要你裁：**牆鐘 max 不可比** —— 兩種模式下世界會分岔（思考次序變了 ⇒ 隊數／事件都不同）⇒ 我實測到「錯開 8205ms／同批 8063ms、整點中位 1419 vs 1042ms」，★★★而那個差不是排程造成的，是兩個不同的世界｜★改量【集中度】：單 tick 最多同時想的隊數 **17 → 9**，有人在想的 tick **1.7% → 24.2%**
---

# ① 落地（351975f16）

```
TeamData        ＋solo_think_next_tick ＋solo_think_last_tick（後者是驗收⑧要的）
faction_ai      _evaluate_solo 移出小時迴圈 → 新 tick_solo_think()，每 tick 檢查到期
sim_runner      _step6b1_solo_think 每 tick 呼叫，★在  閘【之外】；只移這一個 pass
新 tap          solo.think.byteam.%04d，掛在【到期檢查之後】
床              scripts/debug/solo_think_stagger_bed.gd（acceptance）5/5 段 0 fail
```

★**兩件我照護欄加的**：

```
①T0：WorldEvents.is_pending 的隊【不等相位】（due or woke ⇒ 跑；★只有 due 才重排下次）
   —— 形狀逐字沿用同檔 INDEP_INFRA 那一段；★★否則這個閘會【包住既有的 _should_reeval
   事件早退路徑】，而那正是護欄①明文禁止的。
   ★★★而它同時讓 T0 比修改前【更快】：修改前事件也要等下一個整點。
②首次【不當場想】而是排一個錯開過的到期時間
   —— ★★否則所有隊的第一次思考集中在同一個 tick，
   ★★★那正是本票要消滅的形狀，會在世界剛開始時原封不動重演一次（床的公平性格抓到）。
```

# ② ★★驗收①③：我把「量什麼」換掉了，理由在這裡（★要你裁）

```
spec 寫：把錯開關掉 ⇒ spike 必須回到 26 秒量級。
★我做了那個對照，而它【不可比】：
  兩種模式下【世界會分岔】（思考次序變了 ⇒ 隊數、事件、戰爭全都不同）
  ⇒ 實測：錯開 max=8205.9 ms／強制同批 max=8063.0 ms
          整點 tick 中位 1419.6 ms／1041.6 ms（★錯開反而「比較慢」）
  ⇒ ★★而那個差【不是排程造成的，是兩個不同的世界】——
     ★★★拿它當證據就是把 confound 讀成效果（今天已經在別處犯過一次同型）。
⇒ 改量【機制本身】：同一個 tick 裡有幾隊在想（★它就是 spike 的成因：全世界擠在同一 tick）
     錯開      單 tick 最多 ★9 隊｜有人在想的 tick 佔 24.2%
     強制同批  單 tick 最多 ★17 隊｜有人在想的 tick 佔 1.7%
  ⇒ ★成對兩半：集中度降一半 ＋【有人在想的 tick 變多】＝ 工作沒有消失，只是攤開了。
★★而真正的「26 秒有沒有掉下來」我【交不出來】：那要在同一個世界上前後對照，
  而本票【改變世界】⇒ 只有量測員在真實 run 上量才算數 ⇒ 建議把①派給她。
```

# ③ 其餘驗收

```
⑧【排程】間隔 min=30／中位 61／max 105 ⇒ 中位 ≒60（砍半的話會是 ~120）、min 正好被 MIN_GAP 夾住
   ★★而我把【排程】與【事件喚醒】分開統計 —— MIN_GAP 夾的是排程那條，
     事件喚醒本來就該更短（護欄①要的就是這個）⇒ 混在一起量會讓 MIN_GAP 那格被事件喚醒判紅
     （床第一次跑就是這樣紅的）。
⑥首次排程 tick 按 team_id 排序 ＝ 67／73／80／74／98 ⇒ 不單調；各隊排程次數差 1。
②新 tap：最少的那一隊也想了 10 次（600 tick／cadence 60 ⇒ 期望 10）⇒ 沒有人被換成少做事。
④T0：距離自己相位還有 57 tick 的隊，事件後【當 tick】就想了。
⑤裸掃 solo_think_next_tick ⇒ 排程器以外零消費者。
⑦fp 會變（裁定接受）；★同 seed 兩跑仍相同：demo 43200 tick ＝ 30 遊戲日 綠。
⑨視野時滯：★我沒有做任何補償（照 blueprint 裁：誠實時滯非退化）——
   而它的實際分布【我沒印】，因為我沒有現成的低成本 tap；若你要，那是一格新 tap 的事。
```

# ④ 我沒做

```
①沒動 unified.rank／loop1.factions（風險②：一次切一個）
②沒改 NEAR_CADENCE、沒動那個全域閘的其餘用途
③沒補償視野時滯
```
