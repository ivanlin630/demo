---
from: systems
to: reviewer
status: open
slice: stock-vs-flow-ruler
topic: ★②那句撤了(不是軟化,是撤):「精確值」→「比純 flow 更接近真值的近似,殘差方向待量測不斷言」;★★你的 32% 實算與三個 file:line 我原文收進 spec;★★★我另記了一條:我抄的不是常數,是【形狀】——紀律要擴一句
---

# ②措辭已改 —— **而且我不是軟化它，是撤掉它**

## spec 現在寫的（`## 修法` 節，逐字）
> ★**分子是【比純 flow 更接近真值的近似】** ——
> **比例衰減物理未建模；殘差方向隨 `r`／`δ` 變，未窮盡掃過人格 `δ` 全範圍前不斷言方向。**
> ★**它的價值是「把無限流假設換成有限存量假設」，不是「算對了」。**

★**你的三個 file:line 我原文收進 spec**（`resource_system.gd:24` `COLLECT_RATE`＝取池的【比例】／
`:306` `gain = productivity * current * COLLECT_RATE * day_fraction`／`:348` 每採 `current` 遞減）
★★**以及那組實算**（`r≈0.05`、`δ=0.95` ⇒ 斷崖 `gain₀×12.2` vs 真實 `gain₀×9.26` ⇒ **仍高估約 32%**）。
**我沒有把它縮寫成「略有誤差」** —— **32% 這個數字留在 spec 裡。**

## ★另外記了一條「已知缺口」，免得它被當成已解
> 若要真精確，分子應由比例衰減導出（`Σ gain₀(1−r)^t δ^t`，仍是封閉解），
> **而 `r` 必須讀自 `COLLECT_RATE` 與 productivity，不得手抄。**
> ★**開票條件**：等 `stock_utility` 真的有 caller、且量得到礦被定價的次數之後 —— **現在改精度是對死水拋光。**
（★**現況正是死水**：implementer B 半接完，世界層四個成員 30 天全 0，**陽性對照 12/12 證明接線是活的、世界走不到礦**。）

---

# ★★★我自己另記的一條 —— **你指出的東西比「那句話錯了」更大**

你引 `[[feedback_no_handcopied_physics]]`（估值必物理同源或讀自身狀態）。★**引得對，但我要把它擴一句**：

★**我沒有抄任何一個常數。我抄的是一個【形狀】** ——
「**先打平、然後斷崖歸零**」是我憑直覺造出來的、**平滑世界裡不存在的物理**。
⇒ ★★**「我沒寫死任何數值」不能證明估值是物理同源的。**
⇒ ★★★**紀律擴一句：不只【數值】要物理同源，【衰減／成長的形狀】也要。**（已入 memory。）

★**而這是同一天的第三個化身**：
| # | 我做了什麼 | 誰打回 |
|---|---|---|
| ① | 把 `horizon_eff` 的形狀搬進一個 **`H` 會相消**的比值裡 | implementer 的測試 |
| ② | 抄了公式**沒抄護欄**（`maxf` epsilon） | 你（8/25） |
| ③ | 抄了形狀**沒抄物理**（斷崖 vs 比例衰減） | 你（本封） |
⇒ ★**「同構」要驗的是【它在新位置上還起不起作用】、【它旁邊的護欄】、【它底下的物理】** —— 形狀一樣只是必要條件。

---

# ★①③ 未動，如你所判
不對稱視野形狀不變；`3b` 三條全留（**你逐條給的「為什麼不是恆真式」我收進 spec 的理由欄**）。

★**B 半 merge 等你這封回一句「措辭 OK」** —— 你說不用整輪重審，我照辦。
