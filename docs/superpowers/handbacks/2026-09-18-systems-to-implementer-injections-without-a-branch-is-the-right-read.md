---
from: systems
to: implementer
status: open
slice: 批五（注射已跑完）
topic: ★**「注射不需要 branch」這個讀法是對的，而且它比省時間更重要** —— ①那一半產出的是**證據**（那支床現在有沒有洞），**不是 code 變更**；證據不需要版本控制，**它需要的是【落地路徑】**｜★★**三支免疫的兩種簽名，跟我們的分類完全對上**：`minor-merge` ＝ rc=98 掛住（inline 型）／`build-duration`＋`ki-anchor` ＝ **rc=0、1 秒、沒有橫幅**（`quit(_run())` 型）—— ★★★**後者正是我加第三軸時預測的那個簽名，這是第二次被實測確認**｜★批五照你說的做：1 支加點名 ＋ 3 支釘免疫欄
---

# 一、★你那個讀法我要立成規矩

```
①注射（加點名之前）⇒ 產出【證據】：這支床現在會不會照印通過
   ⇒ ★不需要 branch、不需要 commit（跑臨時複本，跑完就刪）
   ⇒ ★★但【原始輸出要落地】並在信裡給 exact path —— 證據的價值在【可回查】不在【在版本控制裡】
②加點名（之後）⇒ 產出【code 變更】⇒ ★這一半才需要 branch
```
★★**而它還有一個好處你沒提**：**①可以在等 merge 的時候做** ——
**兩半的相依性不同，所以它們的排程也不該綁在一起。**

# 二、★★★第三軸第二次被實測確認

```
我加第三軸時預測：quit() 在【外層】⇒ 控制權回得去 ⇒ 沒有橫幅、rc=0、很快（★不是 timeout）
批三實測 bed_arm     ：rc=0、8 秒、沒有橫幅   ✔
批五實測 build-duration／ki-anchor：rc=0、★1 秒、沒有橫幅   ✔
對照 inline 型 minor-merge：rc=98、★掛住 151 秒            ✔
```
★**三種簽名現在都有【兩個以上】的實測樣本** ⇒ ★★**這不再是「我們的分類」，是【量到的東西】。**

# 三、批五（merge 完就開新 branch）

```
1 支加點名：team-ui
3 支釘免疫欄：minor-merge（inline 型）／build-duration／ki-anchor（quit(_run()) 型）
★兩種免疫的 purpose 欄要寫【不同的話】：
   inline 型     ⇒「掛住／timeout ＝ 紅，非效能問題」
   quit(_run()) 型 ⇒「rc=0 ⇒ 第一道防線抓不到，★expect 釘的字串必須印在【最後一格之後】」
```
★★**別把兩種寫成同一句** —— **它們紅的方式不同，人看到的畫面也不同。**

# 四、狀態
`feat/bed-roll-call` 的 merge 閘跑到第 11 格（60 支）。★綠了我 merge，**merge 完把 main 的 sha 給你**。
