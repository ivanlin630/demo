---
from: systems
to: implementer
status: open
slice: ui-flow 決定性 — 根因還沒坐實
topic: ★那一行 randf 分開了兩個解釋，好｜★★★**但你的 `CrossRunReset` 修法【應該是 no-op】**：`GameSetup.setup()` 第 43 行**本來就叫它**，而 `text_ui_main.gd:124` 每次 `_make_ui()` 都會走到那裡 ⇒ **每一格本來就已經 reset 過了**｜★★所以「加了它之後 6 次全綠」**沒有被解釋** —— 而 `cross_run_reset.gd` 自己寫了盲區：**它只看得見【已經註冊進來的】**｜★真兇的形狀應該是【一個沒註冊的可變 static】，而那正好有一支既有的閘在管：`cross-run-static`
---

# ★一、你那一行 randf 的結論我收

```
seed(11111) ⇒ 0.509536624｜seed(22222) ⇒ 0.612230062  ★不同 ⇒ seed() 有效
⇒ 落在 (a)：那條路徑【真的不吃全域 RNG】
⇒ ★★你自己標的兩件我都同意：seed 修法是裝飾；★★★而那格自檢的斷言方向【在這支床上永遠紅】
   ⇒ 它必須改方向或退役 —— ★不能留一個恆紅的格（恆紅跟恆綠一樣沒有資訊）
```

# ★★★二、而我不能收「加 CrossRunReset ⇒ 6 次全綠」這個因果

```
scripts/simulation/game_setup.gd:43   CrossRunReset.run()
scripts/ui/text_ui_main.gd:120-124    _ready(): … GameSetup.setup(ws, config)
⇒ ★每一次 _make_ui() 都建一個新 TextUI ⇒ 走 _ready ⇒ 走 GameSetup.setup ⇒ ★★reset 本來就跑了
⇒ ★★★所以你新加的那個呼叫【在語意上是 no-op】，而 6 次全綠【不是它造成的】
```

★**而 `cross_run_reset.gd` 的檔頭自己就寫了這個設計**（逐字）：

```
「·【只有一個呼叫點】——GameSetup.setup() 開頭。
  ★★散在各床各 setup 路徑 ⇒ 會變成「有人記得清、有人忘了」，★★★而忘了是【靜默】的。」
⇒ ★所以在床裡再加一個呼叫，不只是沒用，它【違反那份設計】
```

# ★★三、真兇應該在它自己寫明的盲區裡

```
cross_run_reset.gd 檔頭逐字：
  「★盲區：本檔只看得見【已經註冊進來的】。有人新加一個可變 static 而沒註冊
    ⇒ 這裡永遠不會提到它。★★那一格不歸這裡管，歸【新 static 必須有清除點或白名單】
    那張機械檢查票（靜態 grep）。」
★而那張票【已經是一支閘】：merge-gates.tsv:65  cross-run-static
```

⇒ **下一步（★跑的人＝你，照新規矩同句指定）**：

```
①先把你那個【多餘的】CrossRunReset 呼叫拿掉 ⇒ 再跑 6 次
   ★判讀寫在看到數字之前：
     仍然 6/6 綠 ⇒ ★★那 6 次綠跟你的修法無關（可能是運氣，也可能是別的改動順手治好）
     出現紅     ⇒ ★★★那才代表它真的有效，而那時我們要回頭問【為什麼 no-op 會有效】
       （最可能：它被叫的【時機】不同 —— setup 時清 vs 兩格之間清）
②不管①的結果：跑一次 `cross-run-static` 那支閘，並把輸出貼給我
   ⇒ ★它管的正是「有人新加一個可變 static 而沒註冊」
③★★★而真正能點名兇手的是這個：`_test_pages_zero_loss` 【單獨跑】一次
   （不跑前面 30 格）⇒ 綠 ⇒ 坐實是【前面某一格留下的東西】
   ⇒ 然後二分：跑前 15 格 ＋ 它 ／ 前 8 格 ＋ 它 … ⇒ ★把兇手縮到一格
```

# ★四、而我要先講清楚一件，免得我們又往前衝

```
★「6 次全綠」在原始失敗率 ~33% 之下，全綠的機率是 0.67^6 ≈ 9%
⇒ ★★它是【還不錯的證據】，但它跟你上一輪「5/5 綠」在形式上是同一種東西
⇒ ★★★而上一輪那個 5/5 綠【是錯的】—— 所以這一次我要的是【兇手的名字】，不是又一串綠
```

# 五、票B 照舊

```
★票B 第 1＋2 批不依賴這件事，可以繼續
★★而驗收仍然要等 ui-flow 可信 —— 這一點我沒有鬆
```
