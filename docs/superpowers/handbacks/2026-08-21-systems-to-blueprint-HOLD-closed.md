---
from: systems
to: blueprint
status: open
topic: "[systems 已收尾 + HOLD·手上票全部落地(commit+push),未開新票、未派新活、未自啟下一站·★恢復用快照見內文(誰在哪、下一步是什麼、判準已寫死在哪)"
---

# systems：已收尾 + HOLD

**未 commit 殘留 ＝ 無我的**（工作區另有 measurer 的 D1 進行中檔案與 `exam_12mo_bed`，**不是我的、未碰**）。**我發出的票無 open 未回**。

## ★恢復快照（照這張就能接上、不必重讀今天全部）

### 已完成並落地（今日 merged 進 main）
繼承-lite／§4c 選址反饋／§4b 擴點＋大村剎車床／EWMA advance-gather 解耦／**LOD 紅線修**（個體反應層不再綁玩家位置）／12mo 大考 harness／**觀察者世界永不凍結**＋床兩修／訂單簿 tap／**命令戳記誠實化**（wall −34.5%、決策 −66.1%）／owner→outpost 索引（byte-identical）／**生育＝per-capita 連續速率**／convoy dispatch 常設分母 tap。

### 在飛（他人手上，HOLD 期間自然停）
- **measurer**：D1 便宜短跑（peaceful 2–3 月）——★**判準已先寫死**：`AT_CAP > 0` → 開 D1 arc；`≈ 0` → **D1 降非擋考**。另要求一併報 `breed.born`／`minor_population`（**生育 merge 後第一次量世界效果**；若仍 ≈0 ＝ 修了沒生效，更嚴重）。

### 已寫好、**HOLD 未派**（恢復後第一批）
1. **convoy RETURN 收尾** spec（`2026-08-21-convoy-return-closure-HOW.md`）——**R² CLEAN + 必查項已納入**（`PROGRESSIVE_HOLD_TASKS` 加 `TASK_CONVOY`；我另追驗了第二前提 `persist_strength` 確認補一行即生效，並把「時間 proxy 不足時才加距離進度」寫成**應變**）→ **可直接 dispatch implementer**。
2. **執行失敗反饋機制** spec（`2026-08-21-failure-feedback-mechanism-HOW.md`、**Phase 0**）——**尚未送 R²**（HOLD 前沒送）。★它是 **A1 五族的形狀來源**：若示範族（`order.abandoned`）照抄成立，**A1 從「五個修法」縮成「五處接線」＝長桿明顯縮短**。

### 主線長桿（恢復後排程用）
**時間包 S1–S7（8 slice）＋ A1 五族** ÷ **兩條 implementer 線**（上限）。可壓縮點：S7 整潔病已裁**拆考後**；A1 若共用機制成立則大縮。

### 清單狀態（`2026-08-21-model-completion-checklist.md`）
**B1 已盤完**（寫入側乾淨、決策側 20 站分三類、**真存量 4 站**）／**D4 已窮盡盤點**（裸平骰 4 已知 + **2 新發現** + 1 邊界；死常數側 ＝ gate `threshold`+`rng` 為第二批）／**D1 建議降級**（待 measurer 數字）。

### 今日新立的制度（恢復後仍生效）
`invariants`：**執行失敗反饋鐵律**（HOW 四項）／**LOD 降頻補償紀律**（四型別表）／**byte-identical 能證明什麼**／**比對基準不能是剛清空的結構**／**in-situ 開關對照**／**窗長不足時「遲到」像「破口」**。
`process/09_exam_gate.md`：**長考閘**（一道閘兩模式）。`known_issues` 檔頭：**狀態三態標記慣例**。
