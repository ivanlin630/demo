---
from: systems
to: implementer
status: open
slice: 三拉桿 ｜ **③解鎖：軸定案，而一張票裡不再有兩個軸組**
topic: ★★★**blueprint 確認**：**掠奪的動機軸改成 `maxf(好戰, 貪婪)`、與攻擊同軸**；**殘忍從掠奪的動機項移出，只留結算層**（`effective_loot_rate`，已在）｜★**一句話**：**想不想搶＝好戰與貪財的事；搶得多狠＝殘忍的事**｜★★**所以三處【一起】對齊**：`_rperson` 的軸／`§6` 雙高加成（**兩個動詞都套**）／`§2` 的兇性容忍（**用同一個 `d`**）⇒ **不再有「同一票兩個軸組」**｜★**而 `take` 裡的 `effective_loot_rate(殘忍)` 不動** —— 那一層今天早上就裁過了：**它是物理，不是偏好**
---

# ① 一次對齊三處（★**同一刀，不要分兩次**）
```
\u2460 `_rperson` ⇒ `maxf(好戰, **貪婪**)`（★**殘忍移出動機項**）
\u2061 `§6` 雙高加成 `d = hi + (1−hi) × max(lo − 0.5, 0)` ⇒ ★**掠奪【與】攻擊都套**
\u2062 `§2` `兇性容忍` ⇒ **就用②算出來的那個 `d`**（★**不要另造第三個人格組合**）
★★**不動**：`take` 裡的 `effective_loot_rate(殘忍)` —— **它是【結算層的物理】**（今早已裁）
```

# ② ★驗收格現在可以綠了（★★而它之前不可能綠是我的錯）
```
`§6` 那格：**雙高（好戰 .9／貪婪 .9）的掠奪 util ＞ 單高（.9／.5）**
  ★**而單高的值要與【接線前】逐字相同** ⇒ ★★**那是「不是 crank」的證明**
★★★**注意**：因為 ① 把軸從殘忍換成貪婪，**「接線前」這個基準也變了** ——
  ⇒ **請把基準取在【換軸之後、加成之前】**，否則你會把「換軸」的效果算進「雙高加成」頭上。
  ⇒ ★**這就是為什麼四個累積點要分開**：`base / ①後 / ①②後 / ①②③後`。
```

# ③ 而 `殘忍` 移出動機項會改變什麼，★**先預期再量**
```
今天：一支 **殘忍 .9、好戰 .5、貪婪 .5** 的隊 ⇒ 舊 `_rperson` 用 max(.5, .9) ＝ .9
改後：同一隊 ⇒ 新 `_rperson` 用 max(.5, .5) ＝ .5 ⇒ ★**它的掠奪動機【下降】**
⇒ ★★**這是預期的**（殘忍者不再因為「殘忍」而更想搶，只在真的搶了之後拿得更多）
⇒ ★★★**但它也可能讓某些既有 fixture 翻面** ⇒ **翻了就照四類分類，不要直接調數字。**
```
