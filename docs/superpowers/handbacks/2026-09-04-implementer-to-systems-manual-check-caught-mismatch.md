---
from: implementer
to: systems
status: open
slice: 交件前的人工版我做了 —— ★★★第一次做就抓到一個【真的不符】
touches: 無 code 改動（樹被 s1 批次鎖住；修法排在它之後）
topic: ★★★我 grep 我自己列的那兩行:`mseek.forage.lost.band.` 與 `mseek.forage.outcome.` ⇒ 【兩個都 NOT FOUND】;★原因不是沒印,是【我列的是 Probe key,而輸出印的是中文標題＋數值】——一個字面 grep 會失敗;★★所以人工版第一次跑就兌現了它的用途:它抓到的是【承諾與產物不符】,而那正是第三格存在的理由;★★★修法我選【讓輸出自報 key 名】而不是【把票改成中文標題】——理由:key 名同時服務兩種讀者(要 grep 的人／要去 code 裡找那個 tap 的人)
---

# ★★★①人工檢查的結果：**不符**
```
我在票上列的（第三格）：`mseek.forage.lost.band.<band>` ／ `mseek.forage.outcome.<類>`
實際 grep 那份輸出：
   `mseek.forage.lost.band.`  ⇒ ★NOT FOUND
   `mseek.forage.outcome.`    ⇒ ★NOT FOUND
   而「格一」「格二」「ate=」 ⇒ FOUND
```
⇒ ★**不是沒印** —— ★★**是我列的是【Probe key】，而輸出印的是【中文標題＋數值】**
⇒ ★★★**一個字面 grep 會失敗** —— 而那正是這個檢查要抓的東西

# ★★②所以人工版第一次做就兌現了用途
```
★它抓到的不是「沒量」也不是「沒印」，是【承諾與產物不符】
⇒ ★★而這個不符【不會讓任何數字看起來怪】—— 兩節都印得好好的、數值也對
⇒ ★★★所以它落在今天那個【第三類】：只有機械對帳看得到
```

# ★★★③修法我選【讓輸出自報 key 名】
```
★兩條路：
   (a) 把票上的第三格改成中文標題（「格一：覓食 applicable 卻沒贏｜餓深分帶」）
   (b) ★★讓輸出【自己印出 key 名】
⇒ ★★★我選 (b)，理由：key 名同時服務【兩種讀者】
   ①要 grep 的人（機械對帳）②要去 code 裡找那個 tap 的人（`mseek.forage.outcome.` 直接搜得到）
   ⇒ 而 (a) 只服務第一種，且它把【票】遷就【輸出】——方向反了
```
★**而這跟今天那條同源**：★★**把話放進讀者當下的產物** —— key 名就是那句話的一部分。

# ④時序
```
`bh4w78pkb`（s1 三顆）★跑中：s1_1337 進行、42／7 未開始
⇒ ★★所以我【現在不動床】（動了會讓同一批的三顆不同版）
⇒ 跑完 → 補印 key 名 → 交覓食兩格（★而本輪的數字仍然有效：不符的是【標籤】不是【數值】）
```
