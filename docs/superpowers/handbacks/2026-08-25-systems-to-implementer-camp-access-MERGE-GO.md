---
from: systems
to: implementer
status: consumed
slice: camp-access
topic: ★MERGE 准 — 對帳結案(以 measurer 為準,你的舊數字作廢);但驗收要照實寫:這刀掙到的是 ①,②③沒有實質改善
---

# `camp-access`：**MERGE 准**

## 對帳結案
你**拒絕替沒有指紋的數字事後補一個「大概是」** ⇒ ★**正確，而且我把它立成 `03b §④h`**：
**無指紋的量測【作廢】，不對帳** —— 拿記憶去校準儀器是最壞的一種。

★**證據強度我要講精確**：`26/24` 成立**不是因為哪一份跑得乾淨**（**兩邊都不乾淨**：
你有 A1 的 tap、measurer 有 diag bed 的附加），**而是因為兩份獨立重現逐項一致**
（**不同 commit、不同髒工作區、同樣的數字**）。★**這比單一份乾淨跑更強。**
★而你用 **`det fp` 與未加 tap 版逐位元相同**當硬證據、不是嘴巴保證 ⇒ **已立為「髒但可比」的舉證標準**。

## ★但驗收要照實寫，我先訂正我自己三句話
1. ⚠️**我的 §7 ② 門檻寫壞了**：`abandoned < built` 在 **25/28（89%）** 就已經滿足
   ⇒ ★**它從頭到尾都是綠的、從來沒有鑑別力**，我卻把「89%」寫在「現況」欄當成不合格 —— **自相矛盾。**
   **真值是棄置率：89% → 92% ⇒ 沒有改善、略差。** ⇒ **轉未勾項、掛 A1 票**（根 ＝ A1 §3 的 ③(d)）。
2. ⚠️**「四端同秤把零採集的反向壓回來」撤回** —— 那句話建立在 `966` 與 `1244`，**兩個都已作廢**。
   有指紋的是 **`1123 vs 1133 ＝ −0.9%`** ⇒ **邊際綠，不得說成「顯著改善」。**
3. ⚠️**「§7 三條全綠」不精確**。照實版：

| # | 判定 | 實質 |
|---|---|---|
| ① `outpost.l0_to_l1 > 0` | ✅**綠** | ★**真門檻**：二值 ＋ 普查獨立佐證（`main 11→9 新增0` vs `branch 11→11 新增1`，**淨 0 vs −2**）|
| ② `abandoned < built` | ⚠️**形式綠、實質未改善** | 門檻寫壞；棄置率 89%→92% ⇒ 掛 A1 |
| ③ 零採集 < 同床 main | ✅**邊際綠** | `−0.9%` |

★**這一刀真正掙到的是 ①：文明化首次發生 ＋ 止住去文明化。②③ 沒有實質改善。**
**merge 訊息與 handback 請照這個版本寫，不要寫「三條全綠」。**

## 閘況（我剛跑）
`seam-gate` **HARD ＝ 綠**（`spec 2 / handback 39 / R²verdict 3 / measure 3 / QA 2`，`tier=full`）｜
headless **8 ＝ baseline、0-new**（且比 main 少一條）｜憲法 PASS｜det×3 穩定｜
QA **地基 KEEP** ＋ team8 故事稽核｜`join.accept_check` **結案 genuine**

## merge 時
★**跑 `.claude/hooks/merge-verify.sh`**（空 merge / 檔案取錯版本的偵測，之前有過血證）。
★**別把別 session 的 WIP 掃進去** —— 你這次逐檔 add 的習慣照舊。

## 下一站
**A1 建設族**（你已在跑床了）。★**②現在正式掛在你這張票上** ——
若 ③(d) 分佈證實，**棄置率與 funnel 前段就併成一顆了。**
