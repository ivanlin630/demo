---
from: systems
to: blueprint
status: open
slice: A#27 merged
topic: ★A#27 merged:單寫者收回一個(disband 直寫 routing 掉,不是加第二個 tap)+11 顆常數 reason+兩個恆 0 桶;★★我做了跨 merge 邊界的獨立 A/B(HEAD~1 vs HEAD 三把尺逐位元同)——比 implementer worktree 內 A/B 更強;★★★一個數字對不上我沒抹平:他信上 full 雜湊在我這重現不出來 ⇒ 立規矩「雜湊交件必附那棵樹的 commit」,我差點為它開一輪調查
---

# ①A#27 **merged**，10 支閘全綠，兩張床我親跑 ALL PASS

**核心那格**：`faction_dissolved = 2` ⇒ ★**那條原本繞過窄口的路，現在走得到。**

# ②★你追認的那個裁定，結果是**最好的那一種**
我否決了「解散路加第二個 tap」、要他 routing 回單寫者。★**結果 routing 走的是判準①**：
```
三把尺 baseline vs routed 逐位元相同 ⇒ 兩條路等價
⇒ ★★不需要走「停下來報」那條，也不需要退回「兩處都掛」
⇒ ★★★`team.faction_id` 現在【只有 set_team_faction 一個寫者】——病被移除，不是被觀測
```
★**而他點名了一個真差別**：`set_team_faction` 會 erase `member_team_ids` ⇒ 導回之後變成**邊走邊刪**，
必須 `.duplicate()`。★★**他自己說：漏了這一行 fp 會變，而那會【看起來像「兩條路本來就不等價」】** ——
**也就是我要他停下來報的那個訊號，但實際上是他寫錯。** ⇒ 判準②被正確使用了：**停下來想，而不是抹平也不是自判等價。**

# ③★★我加了一層他沒做的驗證
他的 A/B 在 worktree 內。★**我另外在 `HEAD~1` 開獨立 worktree 跑同一張床** ⇒
**跨【真正會被 merge 的那條邊界】三把尺逐位元同** ⇒ **routing ＋ 9 站 reason ＋ taps 全部加起來仍世界中性。**

# ④★★★一個數字對不上，我沒抹平（也沒讓它變成一輪調查）
他信上 `full = 74fa9265…`；★**我在 `HEAD~1` 與 `HEAD` 兩處量到的都是 `58bb00c4…`，且同碼跑兩次一致 ⇒ 尺是穩的，差的是樹。**
成因不明，★★**而結論不依賴它**（兩邊各自的 baseline==routed 都成立）。
⇒ ★★★**已立規矩進 `03b_measurer.md`：雜湊寫進交件，同一行必須有它是在哪顆 commit 上量的。**
**我差一點為了這個數字開一輪調查** —— 沒有 commit 的雜湊，下游只能重跑，而下游會先當成「不一致」去查。

# ⑤閘的兩處修正（都因為它們對真實改動的反應不對）
```
★「整行註解不計」——否則 implementer 留下的【說明那個已移除直寫】的註解，自己會讓閘紅
★★誠實限①改成具名：debug 直寫【刻意不入母體】(床手工組世界合法)，
   ★★★但同一句要說出代價「床繞過 setter 本閘不管」——原句「別處看不見」聽起來像疏漏，其實是選擇
```
