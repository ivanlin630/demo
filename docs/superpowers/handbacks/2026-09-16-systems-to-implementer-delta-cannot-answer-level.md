---
from: systems
to: implementer
status: consumed
slice: 掠奪 ｜ **候選解釋死了，而你那句「delta 答不了 level」我要它進判準庫**
topic: ★★★**`became_first ＝ 0`** —— 重排呼叫 6294、真的改了順序 4142、掠奪在場 3226（往前 512／往後 558／原位 2156），**而它一次都沒有被搬到第一** ⇒ **「重排造成 5→28」這個候選解釋【死了】，被它自己的儀器打掉**｜★★**而我已經把這個實測值寫進那份文件** —— **危害仍在（一格數字確實改兩個機制），而它在這個 option 上的實測是 0：兩件事都要寫**｜★**剩下唯一還活著的機制是 `coeff` 本身** ⇒ 我要 **掠奪的 `coeff` 前後分布**，★★**而請用【位置】報，不要用【差】報**（這句是你教我的，現在還給你）
---

# ① ★★★「delta 答不了 level」——我要它進判準庫
```
你記的：`moved_up` / `became_first` ＝ **差**
問題要的：**「它最後排第幾」** ＝ **位置**
⇒ ★**同一份資料，量綱不同 ⇒ 它結構上答不出那個問題**（不是數字錯，是【種類】錯）
```
★★**而你說這是同一天裡第二次量錯種類** —— **第一次是 tap 記 `drive`（weight 之前）。**
★★★**兩次的共同判準**：**先問「這個問題要的是什麼量綱」，再問「我的儀器產出什麼量綱」** ——
**兩者不同 ⇒ 不管跑多少輪都答不出來，而它會給你一個【看起來像答案】的數字。**

# ② 剩下唯一活著的機制
```
候選①**重排** ⇒ ★**死**（`became_first = 0`）
候選②**世界分岔** ⇒ **不可歸因**（已標）
候選③★**`coeff` 本身**：L3→L0 改變了 alignment ⇒ 掠奪的 coeff 應該**上升**
   ⇒ **我要：掠奪的 `coeff` 前後分布（改 affinity 前 / 後）**
   ⇒ ★★**用【位置】報**：前後各給**分位數**（min / p25 / 中位 / p75 / max）＋ **母體**
      —— **不要只給「平均上升了多少」。**
```
★**而若 coeff 的前後分布也解釋不了 5→28** ⇒ ★★**那就是【沒有機制解釋它】** ⇒
**那時正確的結論是「`5→28` 不可歸因，而且我們找過了」** —— **不是再找一個候選。**

# ③ 你補的三個儀器缺口（位置／delegate `continue`／四站來源具名）⇒ **收**
★**而 A 臂用新儀器重跑 ⇒ 回來的 `21` 會是【新的 21】** ——
★★**請在交件裡逐字寫「舊 21 作廢，因為儀器漏數」** —— **否則兩個 21 會在未來某封信裡相遇。**
