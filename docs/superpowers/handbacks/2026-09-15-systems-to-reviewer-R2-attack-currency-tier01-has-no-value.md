---
from: systems
to: reviewer
status: open
slice: 攻擊幣別票 ｜ **一個設計選擇要你打**
topic: ★**根因已定**：`faction_ai_system.gd:379-384 _belief_richness()` **兩條回傳路徑、兩種單位** —— 一條是 `(coin + food + material) / 100.0`（★**不同單位直接相加、再除一個手填的 100**），另一條**直接回那個 0..3 的桶號**｜★★**而 72% 撞頂的真因是【桶號剛好等於除數】**（`clampf(3/3,0,1) = 1.000`，實測 tier1 p10/p50/max ＝ 2.0/3.0/3.0 逐字吻合）｜★★★**要你打的是第二步**：tier0/1 只有桶號、**估不出價值** ⇒ **(a) 那一層的 loot 項不參與** ／ **(b) 桶號 × 一個具名代表值**
---

spec：`docs/superpowers/specs/2026-09-15-attack-loot-monotone-compression-HOW.md` §⑦

# ① 第一步（★我認為無爭議，仍請你確認沒有第三條路徑）

**讓 `_belief_richness` 回同一種單位 ＝ coin 當量**：
`Σ (資源估 × BASE_PRICE[資源])`，**拿掉那個手填的 100**。
★**單位的來源不是我發明的**：`derived_payoff` 用的就是 `BASE_PRICE` ⇒ **兩側同幣別**。
★★**請驗**：`_belief_richness` **確實只有那兩條回傳路徑**（我讀到的是 `:379-384`），
**沒有第三條 fallback** —— ★★★**而我今天已經在「窮盡」這件事上錯過兩次**，所以這一格我不自己判。

# ② ★★★要你打的設計選擇

```
tier0/1 的 belief 裡【只有桶號】(`resource_scale` 0..3)，沒有分項估值
⇒ (a) **承認估不出** ⇒ 那一層的 `loot` 項**不參與**（★不是給一個假值）
⇒ (b) **桶號 × 一個具名代表值** ⇒ ★★**而那個代表值必須導出，不准手填**
```
**我傾向 (a)**，理由：**(b) 會再造一個「看起來很合理」的數字** ——
而本專案今天已經為「看起來合理的數字」付過兩次學費
（`(coin+food+material)/100`、以及把桶號當價值）。

★**請你打的點**：
1. **(a) 會不會讓 tier0/1 的攻擊【結構上永遠不划算】** —— 少一個正項 ⇒ 它的 util 恆低
   ⇒ ★★**那就不是「承認估不出」，是「偷偷把它判死」** ⇒ **若是，(a) 就錯了。**
2. **(b) 的代表值有沒有一條真的導得出來的路** —— 例如**同 tier 已知目標的實際資產中位數**
   ⇒ ★但那是 god-view（我們沒看過那些目標的真值）⇒ **請確認它是不是死路。**
3. ★★★**第三種可能我沒想到的**：例如**讓 tier 本身變成 confidence 而不是 value**
   （桶號決定「我對這個估計多有把握」，而不是「它值多少」）⇒ **若這條可行，它可能比 (a)(b) 都對。**

# ③ 範圍

- **本票不動壓縮**：§② 那個「用哪個 CAP」的留白格**已取消**——
  ★**單位對齊之後很可能不需要壓縮**（coin 值沒有 0–1 天花板，而撞頂的原因是桶號不是真的很肥）。
- **§⑧ 標著【舊尺上的幾何】的那些數字**（0.900／1.232／72%／典型值 0.27）
  ⇒ **單位對齊後全部要重算，現在不得引用。**
