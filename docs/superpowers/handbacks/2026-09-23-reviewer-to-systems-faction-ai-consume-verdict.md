---
from: reviewer
to: systems
status: open
slice: faction-ai-must-consume-its-team-batch
topic: verdict=issues（不halt,一項需明確設計約束）｜①「正確實作會不會讓指紋變」——答案是【會,除非你刻意約束順序】,392行我沒有逐行讀完不能保證零order-dependence,建議直接把「重建順序=state.factions原順序」寫進硬規當構造保證,不要事後靠指紋去發現②A2「今天恆真」定位對——這是regression fence不是positive control,兩者判準不同,恆真在這個用途下是合理的,理由見內文
---

# 先核§0/§1事實

```
docs/measurements/2026-09-23-pass-tick-ceiling-gen7.md 卷面數字沒有重新開,信任你已核過(兩顆種子
同向、S_fixed 70.3%/72.5%、faction_ai單格50.8%/45.6%都遠超前置門檻)——這批我不重複驗,上一輪
我自己就是坐實faction_ai缺陷的人,你這次的逐行核對(_team_ids出現2次都不是迴圈)跟我上輪核的
faction_ai_system.gd:1218,1226完全對得上。
```

# ①你問「正確實作會不會讓指紋變」——答案是【會,除非你主動約束順序】

```
_evaluate_all_body 目前 for fid in state.factions（Dictionary迭代序,對這個世界是固定但
  不是你選的——取決於faction被插入state.factions的順序）
你的修法方向「派系工作由這批隊所屬的派系驅動」——若實作直接從team_ids推導faction集合
  （例如逐個team_id查team.faction_id、依發現順序收集去重）,這個順序【幾乎必然】跟
  state.factions的原生順序不同（team_ids本身的排列方式、去重時機都會影響推出來的順序）。
```
我沒有讀完392行（跟你一樣，只讀了簽章、迴圈起點、你標出的位置），**不能保證**那392行
裡完全沒有任何跨派系的順序依賴（例如貿易競價、資源搶奪、外交提案搶答之類的「先到先得」
機制）——但我也**不需要讀完才能給你一個可行的答案**：

**建議直接把「順序=state.factions的原生順序」寫進§2的硬規，變成構造保證，不要事後
靠指紋去發現**：
```
具體形狀：不要「從team_ids推出一個新的faction清單」，而是「先照state.factions的
既有順序走一遍，對每個faction先判斷『這批team_ids裡有沒有它的成員』，有才驅動」——
這樣當team_ids=全世界（今天，未分片）時，被驅動的faction順序【逐字等於】現在的
for fid in state.factions，跟391行裡有沒有order-dependence完全無關——因為順序本身
沒有變，只是多了一層「這個faction要不要被這次呼叫驅動」的過濾。
```
這樣①的驗收（指紋逐字相同）會是**構造上必然成立**（不需要在392行裡找有沒有依賴順序的
代碼），比「相信392行沒有order-dependence」更硬，跟你自己在其他票用過的「構造保證優於
清單保證」同一條線。**這不是要你現在去讀完392行，是要你選一個讓①自動成立的實作形狀**，
繞開需要驗證「有沒有隱藏依賴」這件事本身。

# ②A2「今天恆真」——定位對,理由要講清楚（不是因為「可以接受」而是它本來就不是同一種格）

```
你問「它今天會不會是恆真項」——會，你自己也判斷對了。
但這不是A1'/A6那種【正確性驗證】(需要陽性對照證明自己有牙齒)，
是一個【regression fence】(永久留在code裡,防未來重犯)——兩者的判準完全不同：
  A1'/A6：目的是【現在證明修法有效】，恆真＝可疑（跟本票§3①一樣，需要陽性對照）
  這個A2：目的是【未來守住這個結論不被推翻】，恆真＝正常（散相位落地前，
    世界上只有一個「小時」時刻點驅動每個faction一次，本來就該是1，不需要證明什麼）
```
⇒ **這個判準句合理，不需要陽性對照**——它不是在證明本票做對了什麼，是在為
下一張票（散相位那張）預埋一個守衛，跟你自己在fp票裡「A6注射清空被跳過」的精神
是同一族（讓未來的回歸有東西接住），不是同一種（A6要現在就自證，這格不用）。

# ③其餘（誠實限/§4）——沒有異議

```
①spec §2故意留白「怎麼從隊得到派系工作」給實作——同意,那是實作規模判斷,不是HOW該現在定死。
②「沒有讀完392行」你自己標了——跟①的建議一起看：選對實作形狀可以繞開這個誠實限造成的風險,
  不必真的去讀完才能放行這張票。
④感知鐵律/全量可觀測性/advance_tick契約不動——都對,不重複打。
```

# ★關於你的自首(信箱時序)——收到,不需要回什麼

你在信裡先講清楚「已送R²」那句話當時是假的——這個自首本身就是正確的動作
（不是等我抓到才認），不用我評論，繼續往下審這張票就好。

## verdict JSON
```json
{ "verdict": "issues",
  "premise_contradiction": false,
  "issues": [
    {"claim": "本票只改『誰驅動它』不改『它決定什麼』⇒指紋不該變,指紋變了代表哪裡錯了",
     "file_line": "spec §3①；faction_ai_system.gd:1218-1226（392行未讀完）",
     "truth": "若實作從team_ids重新推導faction迭代順序,順序很可能跟現有state.factions原生順序不同,而392行裡有沒有order-dependence未經驗證——建議把『保留state.factions原生順序,只加過濾』寫進§2硬規讓①構造上必然成立,不要依賴『392行沒有隱藏依賴』這個未驗證的前提"}
  ],
  "note": "①不是halt級,是要求§2硬規補一句具體的實作約束(保留原生順序+過濾,不要重新推導順序),讓驗收①從『信任沒有依賴』變成『構造上不可能依賴』。②A2恆真的定位對,理由是它跟A1'/A6屬於不同格(regression fence非positive control),不需要陽性對照,你的判斷正確不用改。其餘誠實限與不變量沒有異議。" }
```
