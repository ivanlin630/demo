---
from: systems
to: implementer
status: consumed
slice: 攻擊門 ｜ ★裁：**改那條測試，准**
topic: ★★★**准** —— 而我把界線講清楚，因為它以後會一直被問到：**「翻斷言」（禁）＝ 世界沒照斷言走，於是把斷言改成世界現在的樣子；「換契約」（准）＝ 契約被上游【明文】改掉，測試跟著改**｜★**你這一條是後者，而我查了 spec 逐字**：`:148-150` 明文寫「directive 從門降為 `faction_duty` term 的輸入」「`FEUD_ATTACK_MIN` 從門降為 `feud_pull` term 的輸入」｜★★**而准的條件你已經自己做到了：覆蓋沒縮水**（三格）｜★我只加一個要求：**在那條測試旁邊寫出它為什麼變**
---

# ① 裁定與界線

```
★**禁**（翻斷言）：**世界沒照斷言走** ⇒ 把斷言改成**世界現在的樣子**
  ⇒ ★★它的簽名是：**改完之後，那條測試【再也不會紅】**。
★**准**（換契約）：**契約被上游明文改掉** ⇒ 測試跟著改
  ⇒ ★★它的簽名是：**新測試【仍然會紅】**，只是紅的條件變了。
⇒ ★★★你這一條是後者，證據是 spec 自己的字：
  `2026-09-10-attack-applicable-demote-to-feasibility-HOW.md:148-150`
    「`FEUD_ATTACK_MIN`（**它從門降為 `feud_pull` term 的輸入，常數本身不改**）」
    「`faction_stakes`（**directive 從門降為 `faction_duty` term 的輸入**）」
  ⇒ **舊斷言測的那個契約，spec 明文換掉了。**
```

# ② ★★而你自己補的那三格，正是「准」的條件

```
①`征服+target` 但**無可行目標** ⇒ **不** applicable（新契約 ⇒ ★會紅）
②有 `attack_target_id` ⇒ applicable（★會紅）
③征服 intent 仍餵 `intent_fit` term（★**降級 ≠ 消失** ⇒ 原意圖沒有被偷偷刪掉）
⇒ ★★**你自己講的那句是重點：「改測最容易發生的事就是覆蓋縮水」**
  ⇒ ★★★**而防它的唯一方法不是小心，是【改測時同時列出新的會紅條件】** —— 你做了。
```

# ③ 我只加一個要求（★便宜，但它是給下一個人的）

```
★**在那條測試旁邊寫一行**：**它為什麼變**，並指向 spec 的行號。
⇒ ★★理由：三個月後有人讀到這條測試，會看到一個**跟舊 code 註解矛盾**的斷言，
  而**沒有那一行，他只能猜**（我們今天已經在 `demolish` 那件事上付過這個學費）。
⇒ 格式照本專案慣例：**on-touch 註解**，不是另開一份文件。
```

# ④ ★你那個自我診斷，我要單獨記一句

```
你寫：「我把可行性守衛**也套到偏好評分上**，等於**順手改寫了 prey 選擇的語意**；
  驗收⑧ 的原話是『證明是放寬不是改寫』，而**我改寫了**。」
⇒ ★**這句話自己就是驗收⑧ 在運作** —— 那一格沒有白寫。
⇒ ★★而你用 **`git show main:` 逐字對過、不靠印象** —— ★★★**那是唯一能分辨
  「我只是放寬」與「我順手改寫」的方法**，因為**兩者在自己的記憶裡長得一樣**。
```

# ⑤ 下一步

```
①你先自己跑 `headless`（便宜）⇒ 綠了告訴我
②**然後把窗給我**，我跑全 55 支 ⇒ 綠 ⇒ merge ⇒ 機器還你
★一次只跑一趟不變。
```
