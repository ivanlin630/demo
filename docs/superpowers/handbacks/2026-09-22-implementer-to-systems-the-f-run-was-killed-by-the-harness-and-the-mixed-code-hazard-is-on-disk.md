---
from: implementer
to: systems
status: consumed
slice: ★**更正狀態**：f 那一輪**已不在飛** ＋ ★★★混碼危害**不是假設，它在硬碟上**
topic: ★**harness 因系統記憶體不足停掉了那個背景 shell**（環境，不是量測失敗）⇒ **我照規定不自行重啟**｜★★**我先前說「在飛」現在過期了** —— 這一句我主動更正，不等你來問｜★★★**而我先前預測的「兩顆種子跑不同 code」——實測是真的**：`iw-s1337.log` **沒有**權威 f 那一節、`iw-s77.log` **有** ⇒ **同一個 job、同一份指令、兩份不同的 code**
---

# 一、★狀態更正（★★這一欄最容易過期，所以我主動報）

```
・背景 shell `bzeyfueso` ＝ **killed（系統記憶體不足）** —— ★harness 的處置，非指令失敗
  ⇒ ★★**我不自行重啟**（規則明寫：記憶體可能仍然吃緊，要等被要求）
・我先前寫的「12 天 × 兩顆種子 f **在飛**」⇒ ★**現在只剩半件**：
   seed 1337 的 **Godot 子進程仍活著**（pid 8604／23476，16:59:22 起，`godot-busy` 看得到）
   ⇒ ★★殺 shell 不會殺子進程樹（老教訓，這次是它救了半份卷面）
   ⇒ 它會自己寫完 `iw2-s1337.log`；★★★**seed 77 不會跑** —— 迴圈在 shell 裡，shell 死了
・★所以：**f 的 12 天卷面，最多會有【一顆種子】** —— 而一顆種子不足以取代 2 天 smoke 的判斷
```

# 二、★★★混碼危害：**實測證據，不是推論**

```
我先前停掉前一輪的理由是「seed 1337 載舊 code、seed 77 會載新 code」。
★而那一輪的兩份 log 都留在硬碟上，我用【權威 f 那一節在不在】當探針：
    iw-s1337.log   權威 f ＝ **0 節**（舊 code）   完成 ✔
    iw-s77.log     權威 f ＝ **1 節**（新 code）   完成 ✔
⇒ ★★**同一個 job、同一行指令、兩顆種子跑了兩份不同的 code**，而兩份卷面都印「DONE」。
⇒ ★★★**若我沒停它、又剛好只看總表**，我會拿這兩顆種子去平均一個 f ——
   **而那個平均數不對應任何一份 code。**
★★通則（我要記住的）：**Godot 在啟動時載入腳本 ＋ 多顆種子依序啟動
  ⇒「跑到一半改樹」＝ 同一批樣本混兩份 code，且【每一份卷面自己看都是綠的】。**
★★★而現成的機械檢查很便宜：**卷面上印 code 的指紋**（`[TREE] codehash=…` wrapper 已經在印）
  ⇒ 我下次比對多顆種子之前，**先比 codehash** —— 不同就不可合併。
  ★這是【將做】不是【已做】：我還沒把它寫進任何床。
```

# 三、★現有可用的東西（★★不要被上面嚇到，有效的部分還在）

```
・**2 天 smoke 的 f ＝ 0.6021**（新判準、單一 code、守恆格過）—— ★仍然有效
・逐消費者 woke 份額（11 支，K 清單現場掃）—— ★仍然有效
・相位拆解 12 天 × 兩顆種子（乾淨）—— ★★**不受影響**（那是更早、獨立的一輪）
・跨種子差幅 6.98%（n=2）—— 不受影響
```

# 四、★我不做的事

```
✘ 不自行重啟那個 job（harness 明令）
✘ 不拿 `iw-s1337` ＋ `iw-s77` 去平均 f（混碼）
✘ 不拿單一顆種子的 12 天 f 去蓋掉 2 天 smoke 的結論
⇒ ★**要不要重跑、什麼時候重跑，我等你說**（★★這是機器資源的事，不只是量測的事）
```
