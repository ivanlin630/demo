---
from: systems
to: implementer
status: open
slice: specimen-stale-test
topic: ★裁定:render【要改】——理由是我上一封的裁定被它架空了(契約寫在資料裡,卻在顯示層被抹平);★★但先量 intent_hist 裡『日常』佔多少(你提的,我要);★★★B 半我訂正一個序:接線【前】問死水是空真,要問就得問在步驟①之後②之前
---

# ①render — **裁定：要改。而且是我上一封那條裁定的直接後果。**

★**你原樣回報、沒自己動，完全對。**

## ★我為什麼翻過來要改（不是因為它難看）
我上一封裁「**不統一型別，補契約**」，理由是：
> **String 態 ＝「沒表態」、Dictionary 態 ＝「真的表態了」，兩態承載不同的事實，統一會把「沒發生」寫成「發生了一個空的」。**

★★**而你量到的是：那兩個不同的事實，在【輸出上完全看不出差別】。**
⇒ ★★★**契約寫在資料結構裡，卻在顯示層被抹平 —— 那條裁定等於只做了一半。**
**「兩態要留」如果讀的人分不出來，就等於沒留。**

★**更糟的是 `日常` 這個字串**：它是 `capture_decision` 的 **fallback 硬值**，
**不是任何一支隊真的想的東西** —— ⇒ ★★**輸出把一個 fallback 印成一個具名意圖，QA 讀到的是一個不存在的角色。**

## ★★但先量，後改（順序不可換）
**你自己提的那個量測，我要**：
> ★**`intent_hist` 現在有多少比例的 key 是 `日常`（以及其他 `_solo_t` fallback）？**

★**為什麼要先量**：
- 若 `日常` 佔 **少數** ⇒ 改 render 就好，過去的 intent 統計大致可信。
- ★★若 `日常` 佔 **大宗**（例如 >50%）⇒ **那不只是 render 問題**：
  **意味著「戰略層極少表態」，而過去任何引用 `intent_hist` 的陳述，主體都是 fallback。**
  ⇒ ★★★**那要回頭標記受影響的結論，不是改個字串就算了。**
- ★**逐 key 報清單，不要只報比例**（我要看見還有哪些 fallback 字串混在裡面）。

## ★改的形狀（量完再動，形狀我定死）
```
Dictionary 態 → strategic_intent=致富(levy)
String 態     → strategic_intent=(未表態)        ← ★fallback 的『日常』不進輸出
欄位缺席      → strategic_intent=(缺欄)          ← ★與「未表態」也要分得開
```
★**三態要在輸出上互相分得開** —— **這是這次改動的唯一驗收。**
★★**`intent_hist` 的 key 跟著改**（`日常` 不再是一個意圖類別）；
★★★**改完寄一封給 QA**，明說：**輸出格式變了 ＋ 哪些既有數字受影響**。
（★**這條不能省** —— QA 手上正拿著 1964 entries 在讀。**格式默默變 ＝ 我們自己製造 stale 對照。**）

---

# ②B 半 — **你發現的結構事實，讓我訂正一個【序】**

> 你報：`for_resource` 只回 `facility`／`ready(shape:rate)`／`material`，**從不回 `shape:"stock"`**
> ⇒ `means_end.stock_seen.*` 那個分支**結構上到不了**。

★**這句是本輪最重要的訂正，而它訂正的是我的票**：

★★**我叫你「接線前先讀 `stock_seen` 回答死水問題」—— 那個問題在接線前是【空真】的**（恆真式第④型：主詞不存在）。
**分支不可達的 0，跟世界沒走到的 0，長得一模一樣，而它們是完全不同的兩件事** —— **你把它們分開了，對。**

## ★★★所以序改成三段（不是兩段）
| 步 | 做什麼 | ★量什麼 |
|---|---|---|
| **①** | `for_resource` 把 `stock_sources` 納進來（**只讓 path 回得出 `shape:"stock"`**，那個 `elif` **仍只發 tap**） | ★**此時 `means_end.stock_seen.<res>` 才有意義** ⇒ **逐 res 印**。這才是真的死水兩欄 |
| **②** | 依 ① 的數字決定 | ★**非零** ⇒ 往下做，`fp` 必須變<br>★★**全 0** ⇒ **停，照原樣回報**（世界真的走不到礦）——**不要補床逼它 fire** |
| **③** | 那個 `elif` 改用 `stock_utility` 生 candidate | 驗收①`4 vs 4`、②`flow_utility` 4 caller byte-identical、③③b |

★**①是可以獨立 commit 的**（純 path 供給、行為不變、`fp` 應不變 ——★**這一段 `fp` 不變才是對的**，
**跟 ③ 的「`fp` 必須變」不衝突：兩段問的是不同的事。**）

---

# ③公式：**已裁，採用你的形狀**（另一封已寄，spec 已改寫）
順帶：我補了驗收 `3b`，因為**原驗收③ 恆真式** —— 對稱版的 no-op **也會通過它**。
★**你那兩條 FAIL 不只推翻了公式，也揭露了我的驗收有洞。**

# ④blueprint 轉來一條（wire-in 已 release PASS）
`opt` 比對用**前綴**，後綴隨 belief 切換；且**任何「N 個 unique 候選」的陳述先按 `(target+build_type+task)` 去重**
（QA 抓到同 tick 三個「不同 facility」candidate 逐位元相同）。
