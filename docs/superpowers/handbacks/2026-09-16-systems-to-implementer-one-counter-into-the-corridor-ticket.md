---
from: systems
to: implementer
status: open
slice: corridor 票 ｜ **併一顆計數進去（★不是新票）**
topic: ★**measurer 回報：(4a)/(4b) 靠聚合只能定 163/621，450 卡在 survival-class 的 priority 動態衰減**｜★★★**裁：在 deny 那一刻分類，不要存逐筆樣本** —— `arbiter.deny.優先序不足.4a` ／ `.4b`（**擋它的 priority ＞ 它自己 ⇒ 4a**）｜★**理由**：`bump_sample` 是 first-N（偏差）／樣本會長（今天才追過 OOM）／**而我們要的只是一個二分，在那一刻可算、事後不可算**
---

# ① 一顆計數，兩個 key

```
在 `arbiter.deny.優先序不足` 那個點（兩個 priority 都在手上）直接比：
   **擋它的 task priority ＞ 它自己** ⇒ `…優先序不足.4a`   ★**正確的擋，不是病**
   **同級或更低、或無人擋**         ⇒ `…優先序不足.4b`   ★★**那才是手不聽腦**
★**而 `.opt.<選項>` 那一維保留** ⇒ **要能答「偵查的 4b 幾次／攻擊的 4b 幾次」。**
```

# ② ★★為什麼不是逐筆樣本（★三條，都有血證）

```
\u2460`bump_sample` cap 預設 8 ⇒ **first-N** ⇒ ★**只蓋到最早那一段**
   （★血證：床印「過 1 的 0 筆」而全窗計數 4268）
\u2461**樣本會長** ⇒ ★★**今天才因為 40000-cap 的桶追過一次 OOM**
\u2462★★★**分類在那一刻是可算的**（兩個 priority 都在），**事後從聚合反推才不可算**
   —— **而那正是 measurer 卡在 450/621 的原因。**
```

# ③ 併進 corridor 票，**不是新票**

★**同一塊派工路徑**，而**那張票的驗收本來就要「設上／no-op」那組數** ⇒ **順路。**
★★**而它不改任何行為** —— **純計數** ⇒ **fp 不該變**（★若變了，那是一個發現，回報）。

# ④ 順帶提醒

★**這一顆是 measurer 在等的** —— **裝好請直接寄他一封**（我已先告訴他你在裝）。
