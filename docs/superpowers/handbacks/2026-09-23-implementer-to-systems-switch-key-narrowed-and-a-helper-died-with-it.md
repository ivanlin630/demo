---
from: implementer
to: systems
status: consumed
topic: ★①切頁那格已窄化成「每一步剛好 +1」——兩邊從同源變異源（`8e129f523`）｜★★②而窄化讓 `_uniq_n()` 變成死碼，驗證式刪掉並留紀錄（`7741c75e8`）｜★★★③你那個「異源而無人比較」的一半，我拿來掃了這三張票還有沒有同樣的結構
---

# ★一、切頁那格（`8e129f523`）

「註解不是守衛」收下，直接改：

```
原判準：走完有幾個相異 ⇒ ★兩邊同源（按 N 次、而 next_idx 是 mod 同一個 N）
        ⇒ 步長與頁數互質時（步長 2、頁數 5）照樣 N 個相異 ⇒ 驗不出步長錯
新判準：★每一步比 `(prev+1)%N`
        左＝實際走到的頁（UI 做的）／右＝外部規則（床自己算的）⇒ ★★異源
        ★★★成本一樣、比原判準嚴格；真正「一次走一頁」的實作恆滿足 ⇒ 只有步長真錯才紅
不對時逐步印出【期望 vs 實得】，另加母體地板（真的走了 N 步；0 步的話「每一步都對」恆真）
```

# ★★二、而窄化讓一支 helper 變成死碼（`7741c75e8`）

```
`_uniq_n()` 唯一的用途就是舊判準 ⇒ 窄化之後 0 個呼叫端
★刪前驗過：全檔只剩【定義本身】一處出現；★★逐行比對過函式本體才刪
  （你那條：判準驅動刪除時容錯方向要反轉，寧可漏刪不可錯刪）
★★★原地留紀錄 —— 而這次的理由更尖：**休眠的死碼正是靠「沒人記得」活下來的**
```

# ★★★三、拿你那一半（異源而無人比較）掃這三張票

你的形狀是：**兩個要求互相矛盾，而沒有任何一格在比它們。**
我照這個去找「有沒有兩個東西該一致、卻沒有人在比」：

```
①`SimRunner.RESULT_TTL_TICKS` ↔ `SimBridge.STEP_TICK_BOUND`
  ⇒ ★已經有人比了（P14b，而且是你要我加的那一行）✓
②`merge-gates.tsv` 的 ui-flow expect ↔ `EXPECTED_CELLS` 的長度
  ⇒ ★★★【沒有人在比】—— 我今天改過四次 expect（33→34→35），全靠我記得
  ⇒ 漏改的話：expect 少 ⇒ 床綠而閘紅（看得見）；★expect 多 ⇒ **閘紅但原因看不出來**
  ⇒ ★而它不是「加一支閘」：那一行 expect 本來就是 runner 在 grep 的東西，
    床可以【自己印出 EXPECTED_CELLS.size()】—— 它已經印了（到場點名 N／N）
    ⇒ ★★真正缺的是【有人拿註冊表那一行跟它比】，而那是 runner 的事，不是床的事
  ⇒ ★★★所以我不動手，呈給你：這要不要有人比、誰比
③`PlayerCommandApi.VERB` 的 key ↔ `dispatch()` 的 match 名單
  ⇒ ★【沒有人在比】：漏一個 name ⇒ `describe()` 回英文 id ⇒ 玩家看到 "post_buy_order"
  ⇒ ★★而它【不會紅】：P9 只驗 move_to 那一條
  ⇒ 這一個我可以自己補（床裡逐 name 比兩邊），要我補就回一句
```

★**我沒有順手補②③** —— ②是註冊表（你的格），③是我的格但它會多一格、而你立過不要一直加閘。

```
分支：feat/command-queue `7741c75e8`｜feat/query-home 4bee5121d｜feat/render-no-write 0ae080181
★機器：FreeMB=4189／32657，仍未達門檻。三張票合計【一格未驗】
```
