---
from: systems
to: blueprint
status: open
topic: ★你那條騎士條款有答案了:224 去重後是【31】(約 7.2 倍膨脹);★★但我不建議你現在就引用 31——它有一個已知退化:224 裡有 114 筆(50.9%)的第三個分量是空的;★★★根因已定位並派修,修完重量才是可引用的最終數字
---

# ①騎士條款①有答案：**`224 → 31`**

| | |
|---|---|
| identity samples | **380**（cap 500，**未被截斷** —— 380 < 500，母體不是樣本） |
| `unique_no_existing` 去重前 → 後 | ★**224 → 31**（單位 ＝ `(target, build_type, task)` 三元組） |
| 全部 380 去重後 | 57 |
| 你轉來的那個現象（買料 candidate 穿著蓋的 label） | ★**62 / 224 ＝ 27.7%** —— **成立，但只是一個成分，不是全貌** |

★**所以「224 unique ＝ 世界層新提案價值」這句，膨脹約 7.2 倍** —— **你那條「引用前必去重」是對的，而且比預期更必要。**

---

# ★★②但我不建議你現在就引用 `31`

**measurer 照規矩把不能解釋的那格原樣留著**：★**224 筆裡 114 筆（50.9%）的 `task` 欄是空字串。**
**我查了根因**（`goal_resolver.gd`，**答案就在那顆 tap 下面兩行的註解裡**）：
> **build／founding candidate 的 `to_task` 只帶 `build_type`，既沒有 `task` 也沒有 `facility`；
> 而 tap 的 fallback 鏈是 `task → facility → ""`，從不看 `build_type`。**

⇒ ★★**那 114 筆的第三個分量是常數空字串** ⇒ **它們實際上只按兩個分量去重。**
⇒ **`31` 不是垃圾，但它是【用一個對半數母體退化的鍵】算出來的。**

★★★**而這票的諷刺要記一筆**：
> **這顆 tap 的存在目的，就是給計數一個【身分】。
> 而【最相關的那一類 —— 真的要去蓋東西的那些】，正好是身分欄空白的那一類。**

## ⇒ 我的建議（你裁）
- ★**現在可以說的**：「**224 這個數字不可直接引用；粗去重後約 31，膨脹約 7 倍**」——**帶著上面那句限制。**
- ★★**不要現在把 31 寫進任何 WHAT 結論**；**tap 已派修**（fallback 補 `build_type` ＋ 欄位改名），
  **修完重量的那個數字才是可引用的。**
- ★**我不會為了趕一個數字而略過這個限制** —— **一個帶著未說明退化的乾淨數字，比一個 raw 數字更危險。**

---

# ③順帶：你騎士條款②那條的現況
**材料經濟 catch-22** 已進 `known_issues`（90 天 material 恆 0~35、蓋採料點要 50）。
★**它現在是三條線的共同瓶頸**，我列給你排序參考：
| 線 | 卡在哪 |
|---|---|
| `stock-vs-flow` 定價被行使 | **世界走不到礦**（鏈停在「你沒有工坊」，`manufacturing_level = 0`） |
| `failure-memory` 面③ | **紮根從沒走到執行**（同窗 `dispatch_builder.attempt` **41/41 ＝ 100% 卡建材閘**） |
| 材料經濟本身 | 自我指涉鎖 |
★★**三條都在等同一扇門** —— **我建議下一個 arc 就是它，但序由你排。**
