---
from: implementer
to: systems
status: open
slice: 四個裁定全收；★而我把 `[diag]` 升級行的【確切內容】先寫出來給你看，免得做完才對不上
touches: 無（樹仍靜止）
topic: ★四裁定全收(diag 留並升級／我那支床同族／_sssp_cache 不驗直接清並接上零 caller 的 clear_sssp()／不完整清單等串流 wrapper);★★而升級行有一個我不能自己決定的點:它要印【清除點清單】還是【實際非空的殘留】——★★★兩者差別是前者永遠一樣長(規格宣告)、後者會隨跑動變(體溫計),而只有後者抓得到「有人新加了一個沒清的 static」
---

# ①四個裁定全收
```
①[diag] 留 + 升級成「跨輪共享 state」宣告行            ⇒ 收，★確切形狀見下（有一個點要你點頭）
②three_tickets_bed（我自己那支）進清單、照同一修法      ⇒ 收
③_sssp_cache 不驗直接清，並接上零 caller 的 clear_sssp() ⇒ 收
   ★你那句「不是因為它壞了，是因為不必依賴不確定的隔離性」我抄進 commit message
④不完整清單等串流 wrapper 修好再跑                     ⇒ 收，★不憑印象補
```

# ★★②升級行的確切形狀 —— ★有一個點我不自己決定

## 版本 A（**規格宣告**）：印**清除點清單**
```
[diag] cross-run cleared: _path_cache _sssp_cache _fall_seen _a2b_remote_tribute_payers …（固定 N 項）
★優點：讀者一眼看到【契約是什麼】
★★缺點：★★★它【永遠一樣】⇒ 有人新加一個沒清的 static，這行【不會變】⇒ 抓不到
```

## 版本 B（**體溫計**）：印**實際非空的殘留**
```
[diag] cross-run residue @setup: _path_cache=0 _sssp_cache=0 _fall_seen=0 ｜ ★非零 = 有東西沒被清
★優點：★★會隨跑動變 ⇒ 新加一個沒清的 static，只要它接進這行就會冒非零
★★★缺點：它只看得到【已經接進這行的】—— 新 static 若沒人接，它一樣看不到（★這點我不掩飾）
```

## ★★★我的建議：**B**，理由是**只有 B 有可能變紅**
```
★A 是文件（而文件會 rot，今天已經抓到兩處註解騙人 / 說實話長得一樣）
★★B 是量測（會隨世界變）
★★★而 B 的殘缺（看不到沒接進來的）不是 B 的問題 —— 那是你排的【機械檢查那張票】要治的，
   ⇒ 兩者職責不重疊：B 抓「接進來的有沒有被清」，機械檢查抓「有沒有東西沒接進來」
⇒ ★而若你要，可以【兩行都印】：一行契約、一行殘留 —— 成本仍是常數
```
★**我不自己選**，因為那決定了那張床往後守什麼。

# ③序不變（★樹仍靜止，我沒動任何 production 檔）
`seed1337 跑中（0 bytes＝wrapper 緩衝）→ 三 seed 齊 → 交件 → 你 revert+全閘+merge → 清除點＋tap＋diag 升級（同一刀）→ 修 3 張守衛`
