---
from: systems
to: implementer
status: open
slice: 窗標籤把「一天」手抄成 240（真值 1440）— **R² CLEAN ⇒ 派工**
topic: ★**母體要分三類，而第三類最容易被你歸成「不是問題」**：算式對、**而顯示字串在教讀者一個錯的換算**｜★★★**已知例外要逐支列出並附前後判決**：`crisis_override_test.gd` 的 `TPD` 是**判斷輸入** —— 而我實驗過：改對之後 **6 FAIL → ALL PASS**｜★★**不要裸掃 `240`**（它在這個 repo 裡有合法身分）
---

# 一、母體（★我重掃過，不是「至少四處」）

```
裸掃 \b240\b：65 行／39 檔 —— ★大部分不是缺陷（MOVE_TICKS_PER_HEX ＝ 240 tick ＝ 4 小時，合法）
★★收窄成【把 240 當週期用】（`/ 240`／`% 240`／`240 * 30`）⇒ **25 行／15 檔** ← 這是你的母體
★★★而收窄之後【仍然抓不到】：`const TPD := 240` 之後【用 TPD】的那些行
   ⇒ 這個洞我寫在 spec 裡，沒有假裝補上 —— ★你動手時請順手記下你撞到的其它形狀
```

# 二、★★★三類（**分錯類比漏掉更糟**）

| 類 | 判法 | 修法 |
|---|---|---|
| (a) 算式手抄 | 真的用 240 去除／模／乘 | ★改讀 `WorldState.TICKS_PER_DAY`（**不是把 240 改成 1440**） |
| (b) 註解／歷史說明 | 只在註解裡提到 240 的由來 | ★**不要動**（`time_scale.gd` 自己寫「240 tick ＝ 4 小時」是對的） |
| (c) ★★★顯示字串說錯算法 | **算式對、字串錯** | ★改成由常數組出來：`"…(day=tick/%d)…" % WorldState.TICKS_PER_DAY` |

★**(c) 的血證**：`construction_funnel_bed.gd:162` 印 `day=tick/240`，而 `:164` 算的是 `TICKS_PER_DAY`
⇒ ★★**算式是對的，而卷面在教讀者一個錯的換算** ⇒ **讀的人拿它反推 tick 就錯 6 倍**
⇒ ★★★**它最容易被歸成「不是問題」，而它是唯一會【讓讀者自己算錯】的一類。**

# 三、★已知例外（必須逐支列出並附前後判決，不得靜默）

```
crisis_override_test.gd:9 `const TPD := 240   # WorldState.TICKS_PER_DAY`
  ⇒ ★TPD 是【判斷輸入】不是標籤
  ⇒ ★★我做過實驗：換成 WorldState.TICKS_PER_DAY ⇒ **6 FAIL → ALL PASS**
  ⇒ ★★★所以那 6 支紅【不是行為 bug】，是那支床自己的常數在說謊
  ⇒ 你交件時要把這一支【單獨列一段】：前 6 FAIL／後 ALL PASS，並寫明它為什麼不算「越界」
★而 1-c（fp 逐字相同）只對【真的只是標籤】的那些處；例外處一律逐支列，不准混進總數
```

# 四、驗收（spec 有全表，這裡挑三格）

```
1-a ★判準收窄成【把 240 當週期用】＋ ★★母體要印（今天 25 行／15 檔）
1-c ★fp ＋ 逐 tick 軌跡逐字相同（★★例外逐支列出並附前後判決）
1-d 到場點名 ＋ expect 釘 N／N
```

# 五、★順帶（不是你的活，但你會撞到）

那支 `crisis_override_test` **沒有註冊成閘** ⇒ **它紅了很久而沒有人看到**。
★**你這一票不要順手去註冊它**（那是另一件事），★★但請在交件信裡提一句你看到的狀態 ——
**我要把它記進 `beds-that-can-red-but-nobody-runs-them` 當第一個具體受害者。**
