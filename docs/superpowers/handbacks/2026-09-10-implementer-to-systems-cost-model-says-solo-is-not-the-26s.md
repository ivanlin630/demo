---
from: implementer
to: systems
status: consumed
slice: 成本模型那一格（你要的）
topic: ★數字：**solo 思考每隊只要 0.031 ms** ⇒ 代入 x=17（同批）＝0.60 ms、x=9（錯開後）＝0.35 ms｜★★所以【集中度 17→9】翻譯成秒數是 **0.25 ms** —— 而用戶感受到的是 26 秒 ⇒ ★★★**在我這一跑裡，solo 思考不是 26 秒的成因**：整點 tick 中位 944 ms 由【其餘每小時工作】扛著｜★而你 §③ 的懷疑（offset 不夠均勻）我量了：**N＝17、offset 六桶 3/3/2/2/3/4 ⇒ 打散是正常的**，9 不是叢聚，是【事件喚醒】不受相位管
---

# ① 成本模型（同一跑之內，不跨模式 —— 照你要的形狀）

```
x ＝ 這個 tick 真的想了幾隊｜y ＝ 這個 tick 的 wall-time（★只收非整點 tick）
  x=0 ⇒ y 中位 0.072 ms（n=453）
  x=1 ⇒ 0.128 ms（n=123）
  x=2 ⇒ 0.134 ms（n=13）
  x=3 ⇒ 137.971 ms（n=1）★★單樣本
⇒ 只用 n≥10 的點：**y ≒ 0.072 ＋ 0.031·x ms**
⇒ 代入：x=9（錯開後 max）＝0.35 ms｜x=17（強制同批 max）＝0.60 ms
```

★**而床第一次跑時我差點交出一個假模型**：

```
x=3 那一筆（137 ms、n=1）會把斜率帶成 **46 ms/隊** ⇒ 外推出「x=17 ＝ 790 ms」。
★那個數字看起來剛好符合期待（「同批很貴」）—— ★★而它來自一個樣本。
⇒ 改成【只用 n≥10 的點】，並在床裡寫死這條；★★★樣本不足時印
  「算不出可信的斜率，不外推」並判【不可判】（不是綠）。
```

# ② ★★★所以用戶的問題（會不會還卡 26 秒）：**這一跑答不了，而它指出了下一個主詞**

```
整點 tick 中位 944 ms／max 8.2 秒 —— ★而 solo 思考在其中只佔 0.35 ms。
⇒ ★★整點那一坨是【其餘每小時工作】：loop1.factions／unified.rank／member_strategy／
   merge／ladder…（本票照風險②沒有動它們）。
⇒ ★★★誠實結論：**本票把 solo 思考攤開了（機制上成立、可量），
   但它【不是】26 秒的主要成因 —— 至少在我這個 600 tick／62→? 隊的窗裡不是。**
⇒ 建議兩件：
  ①量測員在【真 run】上重量 spike（同一世界前後對照只有她做得到）
  ②★而下一個切的對象，數字指向【整點那一坨的其餘部分】，不是 unified.rank 的 8.2% 而已
     —— ★★但那要等①的數字，不要我猜（一次切一個）。
```

# ③ 你 §③ 的懷疑：我量了，**不是叢聚**

```
★真正走這條路的隊 **N ＝ 17**（★不是全部 62 隊：_evaluate_solo 只跑
  faction_id == -1 且非子隊非野獸的隊）⇒ 理論期望 x ≒ 17/60 ＝ 0.28。
★★offset 直方圖（tick 300 當下，17 隊，六桶）：0-9:3 10-19:3 20-29:2 30-39:2 40-49:3 50-59:4
  ⇒ **打散是正常的**（每桶 2-4，均勻）。
★★★那 max x=9 從哪來？—— **事件喚醒**（護欄①：T0 不受相位管）。
   事件常常一次打到很多隊（同格勞力危機之類）⇒ 那 9 隊是【同一個事件叫醒的】，
   而它們【本來就不該被相位擋住】。
⇒ 所以「9 ≫ N/60」不是 hash 沒打散，是【那個量把排程與事件混在一起數】。
   ★而這與驗收⑧那一格是同一個教訓：排程與事件要分開統計。
```
