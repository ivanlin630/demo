# Hand Back: S1 tile-bank（TileBank 單寫者 + coin 守恆 connect）

branch：`feat/tile-bank`（commits `c5ffc40` → `22629b1` → `0531f28`，base `d4c8b75`）
spec：`docs/superpowers/specs/2026-07-03-tile-bank-design.md`
plan：`docs/superpowers/plans/2026-07-03-tile-bank.md`

## 實作摘要（3 commit）

**Commit 1 `c5ffc40` — TileBank chokepoint + 站點收編（純 refactor，pointwise CLEAN）**
- `scripts/simulation/tile_bank.gd`（新）：`class_name TileBank`，static，mirror ResourceBank/AnonTreasuryBank。
  - 公庫 `public_storage`（capped）：`set_amt` / `deposit`（capped add，回實際入庫）/ `withdraw`（clamp remove）/ `get_stored`。
  - 自然池 `tile.resources`（uncapped，cap 由呼叫端 resource_cap/WILD_* 各自套）：`pool_set` / `pool_add` / `pool_get`。
  - `cap(tile,res)`：`_get_storage_cap` 邏輯 + 3 常數（OUTPOST/MOUNT/FOOD_STORAGE_CAP）搬入此=**單點**。
  - 每操作帶 `reason` → `WorldState.record_driver`（driver-ledger，預設 off 零成本）。
- `scripts/simulation/outpost_system.gd`：`_get_storage_cap` / `storage_cap` 委派 `TileBank.cap`，移除 3 常數（既有呼叫端 `os._get_storage_cap` 不動）。
- 收編站點 6 檔：`ambush_system` `harvest_system` `hunt_system` `resource_system` `manufacturing_system` `interaction_system` `faction_ai_system`（+ `outpost_system` breed/train/deduct）。

**Commit 2 `22629b1` — mint 走 TileBank + mint-cap 守恆測**
- `outpost_system.gd`：`_tick_mint` coin/ore 寫入收編 `TileBank.set_amt`（reason=mint / mint_consume_gold/silver），minted 軌（Probe `mint_coin`）保留。
- 滿 coin-cap 不燒 ore 的守恆修**既在**（room-cap 前置限量，本 branch 前已落地）→ 本 commit 補測固化。
- `headless_test.gd`：`_test_mint_cap_no_ore_burn`（滿 cap 不鑄不燒 ore / 有餘裕 coin delta==minted + ore 按比例耗）。

**Commit 3 `0531f28` — off-map 滅團 coin 顯性 ledger sink**
- `world_state.gd`：新 `offmap_extinct_coin: float`（全域 sink）。
- `coin_audit.gd`：`total()` 全池納 `state.offmap_extinct_coin`。
- `faction_ai_system.gd`：`_route_extinct_assets` no-tile 分支改記 sink（reason=`extinct_no_tile` + record_driver）→ 守恆閉合，不再靜默丟。
- `headless_test.gd`：`_test_extinct_offmap_coin_ledger`。
- `docs/known_issues.md`：兩舊項（off-map leak / mint-cap 燒 ore）標 ✅，單寫者 slice roadmap 更新。

## 站點數前後
- 收編 tile 直寫 **~40 站點**（Task1 ~36 + mint 4）。grep `public_storage[…]=` / `tile.resources[…]=`（tile-ref 賦值）殘量 = 0（除豁免）。
- 豁免（明示，非漏）：
  - `world_generator.gd`（~17 initial-gen）+ `game_setup.gd:467`：bootstrap 初始化（建隊構造同類豁免）。
  - `player_command_system.gd:176/259/274`：player 路徑（F-P 傘，spec §3 不做）。
- **另型欄位殘量（非資源量，未納 TileBank）**：facility levels（outpost/mint/stable/farming_level）、`stable_progress`、`construction_team_id`、`abandoned_coin`（scalar，已 CoinAudit 全池）、`resource_cap`（靜態上限）。

## pointwise 證（純 refactor 無行為變）
- **Task1 commit**：`seeded_warring_bed` 3 seed（1337/42/7）×3月 → **total_diffs=0 全綠**（逐點相同，零行為變）。
  - baseline JSON 在 branch base（d4c8b75）擷取，refactor 後逐點 diff。
- Task2 mint routing = 無損（RHS 逐位保留）；Task3 off-map 分支正常地圖不觸發 → **full HEAD（Task1+2+3）月線 sanity：seed 1337 ×3月 total_diffs=0**（零行為變證，涵蓋全 3 commit）。

## mint / off-map 修影響量
- **mint**：routing 無行為變（cap-fix 既在）。測證滿 cap 不燒 ore、鑄入 coin delta==minted。CoinAudit 200-tick delta=0 minted=0（現 config 無 mint fire，同 baseline）。
- **off-map sink**：只在 degenerate off-map 滅團（`_nearest_valid_tile` radius-12 全無）觸發，正常世界不觸發 → 主線 CoinAudit 不變。修的是 latent leak（coin 憑空丟失 → 顯性 sink 守恆）。

## CI-scan pattern（強制閘地基）
`tile_bank.gd` 頂註解：禁新直寫 `tile.public_storage[<res>] =` / `tile.resources[<res>] =`（tile-ref 賦值）；豁免 = 本檔 + world_generator/game_setup（bootstrap）+ player 路徑。可掃（同 B 波 banker 慣例）。

## 驗收（Task4）
- headless：**25 OK / 1 FAIL**（`弱目標未加入攻擊 goal` = pre-existing，plan 容忍）、**0 SCRIPT ERROR**、`=== DONE ===`。
- CoinAudit 200-tick：before=after=86.19，**delta=0 minted=0**。
- InvariantAudit：population / faction / subteam / roster 雙向 + 反向 **全 OK**。
- framework_validation：**PASS=7 DORMANT=0**（S5 g1.mint=1 = mint 魂 routing 後仍活）。
- 新測：`mint cap/ledger OK (minted=10)`、`extinct off-map ledger OK (sink=40)`。

## 連動風險
- `CoinAudit.total` 全池定義**擴充**（+`offmap_extinct_coin`）：任何讀 coin_eq 的 audit/測皆自動含此池（0 常態）；既有測未破。
- `record_driver` 首次接受 `HexTileData` entity（原僅 Team/Person）：entity 為 Variant 存 dict，無型別假設 → 安全。driver-ledger 預設 off，正常 run 零影響。
- `OutpostSystem` 移除 3 cap 常數 → 若外部有直讀 `OutpostSystem.OUTPOST_STORAGE_CAP` 者會斷（grep 確認**僅** `_get_storage_cap` 內用，已隨搬移；無外部引用）。

## 待主 session 確認（系統-owned 文件，未自行改）
1. **`invariants.md` coin_eq 定義擴充**：財產/守恆段 coin_eq 全池現含 `+ state.offmap_extinct_coin`（第3不變量 slice 收尾）。建議補一行。
2. **`progress.md`**：單寫者 slice「tile-granary-bank / tile.resources bank」標 done（TileBank 落地），mint 守恆 connect done。
3. **auto-memory**：`project_framework_seams` / `project_economy_arc` / `project_unified_decision_framework` 的「tile.resources bank 剩餘 slice」可更新為 done（系統單寫者）。
4. 建議後續：TileBank 的 CI-scan 接進強制閘（現僅註解）；driver-ledger Pattern B 全域落地（TileBank 已備 reason）；另型欄位（facility levels/stable_progress）若要單寫者化為下 slice。
