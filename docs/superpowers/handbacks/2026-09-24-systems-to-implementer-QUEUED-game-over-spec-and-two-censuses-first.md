---
from: systems
to: implementer
status: open
slice: 故事結束｜排隊中
topic: ★**這是排隊，不是派工**（先做真機 bug #1）｜spec 已寫：`2026-09-24-game-over-is-a-story-end-not-world-physics-HOW.md`｜★★**修法的形狀是【移除】不是【新增】**：`sim_runner` 不該認識 `game_over`（意圖帳 #43／#44 早裁，code 沒服從）｜★★★**動手前兩份普查，我不預測它們的大小** —— ①誰假設玩家還活著 ②誰假設世界會在 game_over 停下來
---

# ★一、為什麼是 de-patch

```
意圖帳 #43：game_over ＝ UI 層的故事結束、可續觀、**非世界物理**｜#44：**世界的存在不綁玩家**
★而 `sim_runner.gd:114-116` 讀它並 return ⇒ **把一個 UI 旗標接成了世界物理**
⇒ ★★所以 P2 是【結構格】：`sim_runner.gd` 裡 `game_over` 出現次數 ＝ 0
  —— 它不會到期，而它擋的是【下一次有人把 UI 旗標接回世界物理】
★★★`awaiting_heir` 那一支【不動】：它是【等玩家輸入的模態】，不是故事結束，兩者不同類（P16 仍守它）
```

# ★★二、動手前兩份普查（★我不預測它們的大小）

```
①誰假設玩家還活著：grep `state.player_id` 的消費者，逐處判「玩家已死而世界照跑時它會怎樣」
  ⇒ ★這是本票真正的體積 —— **你數完回報，我再決定要不要拆票**
②誰假設世界會在 game_over 停下來：grep `scripts/debug/**` 的 `game_over`
  ⇒ ★★有床靠「跑到 game_over 就結束」的話，本票會讓它跑不完或永遠跑
  ⇒ ★★★而那不是本票改壞了它，是**那支床的終止條件一直接在一個 UI 旗標上**
```

# ★★★三、三個選項每一個都要【現在就能走通】

```
續觀 ⇒ 玩家無隊時畫面不能炸（P4 直接守它）
附身另一隊 ⇒ ★先 grep 確認那個機制現在就能被呼叫；★★**若不能，這個選項不要放上去**，回報 blueprint
離開 ⇒ 直接退出
⇒ ★★★**一個按了沒反應的選項，比沒有那個選項更糟** —— 同「拒絕禁靜默」那一族。
```

★P5 提醒：world-fp **會變**（世界多跑了）⇒ **基準重設要與造成它的改動原子落地**（今天踩過一次）。
