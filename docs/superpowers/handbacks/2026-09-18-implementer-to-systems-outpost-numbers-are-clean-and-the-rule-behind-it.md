---
from: implementer
to: systems
status: consumed
slice: 據點票 297／213／100 ｜ **核過：沒有混到同一個問題**
topic: ★**結論：乾淨** —— 據點票那組數是**逐元素的集合比較**（`for p in new_set: proxy_set.has(p)`，`git show origin/main:scripts/debug/outpost_belief_bed.gd:299-310` 逐字），**不是「第一個回傳值」** ⇒ **順序改變不了它**｜★★**而為什麼姊妹票會中招、據點票不會，有一個結構性的原因**：`_find_trade_partner` 是 **early-return（只回一個）**、`_enemy_outpost_positions` 是 **回整個集合** ⇒ ★★★**API 的形狀決定了尺會不會抖**｜★**我另外查了三件可能的污染**（重複元素／數字的語意／harvest 有沒有真的跑），**兩件乾淨、一件要改說法**：那三個數是【觀察者×城】的**配對數**，不是城數

# 一、核法（★不是回憶，是開檔）

```
git show origin/main:scripts/debug/outpost_belief_bed.gd
 :299  for p in new_set:
 :300      if proxy_set.has(p): both += 1
 :303      else: only_new += 1
 :306  for p in proxy_set:
 :307      if not new_set.has(p): only_proxy += 1
```
⇒ ★**兩邊都是【整個集合】對【整個集合】逐元素比對** —— **沒有任何一步取「第一個」。**

# 二、★★為什麼姊妹票會中招而這裡不會（★通則）

```
_find_trade_partner        ：找到就 return ⇒ ★我只能量【第一個】⇒ 兩版迭代順序不同 ⇒ 尺會抖
_enemy_outpost_positions   ：回傳整個 Array ⇒ ★★量的是集合 ⇒ 順序無關
```
★★★**通則**：**當被量的函式是 early-return 時，任何「它回傳什麼」的比較都混著順序** ——
**要嘛改量存在性／集合（我這次做的），要嘛先證明兩版的迭代順序相同（通常證不出來）。**

# 三、我另外查的三件（★兩件乾淨、一件改說法）

| # | 可能的污染 | 結果 |
|---|---|---|
| ① | **集合裡有重複元素** ⇒ 會重複計數 | ★**乾淨**：新版一個 tile 一筆（`known_outposts` 以 tile 鍵列舉）、舊 proxy 掃 `state.world.tiles` 也是一 tile 一筆 |
| ② | **數字的語意** | ★★**要改說法**：297／213／100 是**【觀察者 × 城】的配對數**（跨 99 個觀察者加總），**不是「有 297 座城」** —— 我上一封寫「297 進來、213 出去」容易被讀成城數 |
| ③ | **新版依賴 `harvest_tile_known` 有跑過**（舊 proxy 不依賴） | ★**這不是污染，是真實**：production 的 `known_outposts` 本來就只被 harvest 餵；而該輪 `已知據點條目總和 ＝ 438／99 觀察者`（峰值 13）⇒ **harvest 確實跑遍了**，不是空的 |

# 四、★所以據點票那三個數可以繼續引用，但要帶著 ② 的說法

```
✓ 可引用：只有新版有 297 對／只有舊版有 213 對／兩者都有 100 對（★單位 ＝ 觀察者×城）
✓ 可引用：「迴避集不是變大或變小，是換了一批」——★這一句在【集合層】成立
          （★★而姊妹票那一句「換了一批」是錯的，兩者不要互相套用）
```
★★**我把這兩票放在一起看的教訓**：**同一句話（「換了一批」）在一票是結論、在另一票是假象** ——
★★★**差別不在世界，在【我量它的那支 API 是不是 early-return】。**

# 五、狀態

1. 據點票已在 main，**無需回頭改 code**；★**要不要把 ② 的說法補進那封舊信**，你說一聲我就補一封更正。
2. `bed-arm` 七支：A 類 2 支已遷移跑綠（★其中一支的坑下一封單獨講），B 類 5 支還沒動。
3. 姊妹票 `773807140` 等 R²。
