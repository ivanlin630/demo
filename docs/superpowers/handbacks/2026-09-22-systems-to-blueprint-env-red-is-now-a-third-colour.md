---
from: systems
to: blueprint
status: open
topic: ★**做完了：環境紅現在是【第三種顏色】**，不再被算成測試紅｜★★★**而它讓整輪【不可判】、離開碼 2**（0＝過／1＝判準紅／**2＝引擎沒啟動**）——★綠與紅都不算，**也不進任何統計**｜★★**成對驗過**：真環境失敗 ⇒ ⚡ENV；**只是在說明裡提到那些字而自己通過的閘 ⇒ 照樣 ✓**；真的判準紅 ⇒ 照樣 ✗
---

# 一、修法（改既有 runner，不加新閘）

```
判斷順序（★順序本身就是防誤判的機制）：
 ①先判【真的通過】：rc＝0 且 expect 命中 ⇒ ✓
   ⇒ ★★這一步擋掉「一支【談論】UnauthorizedAccess 的閘被誤判成環境紅」
     （★2026-09-02 那次假紅就是「談論一個字」與「用它下判決」在文字上不可分）
 ②再判【環境簽名】⇒ ⚡ENV，★不進 FAILED、不進基線、不計入任何統計
 ③才是 rc≠0 ⇒ ✗／expect 沒命中 ⇒ ✗(no-verdict)
```

**環境簽名（兩條，都是今天的真樣本）**：
```
・UnauthorizedAccess／已停用指令碼執行／running scripts is disabled／無法載入 *.ps1／cannot be loaded
・★「這一輪沒有真的重跑」—— bare-tick 自己印的那句
  ⇒ ★★它其實是【更好的】簽名：床自己說「我沒有跑」，而不是我們去猜
```

# 二、★★★離開碼現在有三個值（這一格是重點）

```
0 ＝ 全部通過
1 ＝ 判準紅（測試failure）
2 ＝ ★★★本輪【不可判】：引擎在那幾支上一次都沒被啟動 ⇒ 綠與紅都不算
```
★**為什麼要第三個值**：舊制下「環境壞掉」與「測試壞掉」**都是 rc≠0**，
⇒ 而它們要人做的事**完全相反**（一個是去修機器，一個是去修 code）。
★★**而剩下那幾支的綠也不能拿來充數**：**部分樣本的綠不是全部通過**。

# 三、成對驗過（★會紅 ＋ 不會亂紅）

```
①真環境失敗（今天的機器就是壞的）⇒ ⚡ENV constitution／bare-tick／build-duration
   ＋「本輪不可判」＋ 離開碼 2                                   ✔
②合成：一支【在說明裡提到 UnauthorizedAccess】而自己通過的閘 ⇒ ✓（沒有誤判成 ENV）   ✔
③合成：一支真的判準紅、輸出裡沒有環境簽名 ⇒ ✗ 並進 FAILED                        ✔
```
★**誠實限（我先講）**：**陰性方向今天驗不了** —— 環境還壞著，所以
「健康環境下 ENV ＝ 0」這一格**還沒有被證明**。⇒ **政策修好後的第一輪，要確認 ENV＝0**，
★★在那之前我不說「這支偵測器不會亂報」，我只說**它在今天這三個樣本上分得開**。

# 四、你交代的另外兩件

- **40 支紅先標「環境」不進統計** ⇒ ★已是機制，不是紀律：它們現在**進不了 `FAILED`**，
  基線也**不更新**（那一行本來就會印原因）。
- **硬體邊界兩軸分開（秒 vs 次）** ⇒ 已落地，**HW-1 的 CPU 型號我留空待用戶補**（我不編）。
