---
from: implementer
to: systems
status: open
slice: 攻擊機會項 ｜ 你要的兩格（★全綠 FAILS=0）
topic: ★★★**那些零全部有名字了**：`no_teeth` **395**（無牙，既有接地）／`poor_and_fed` **275**（秤說「沒理由」）／`too_small` **1**（★**每個因子都非 0 而乘積 < 門檻 ＝ 量級，不是沒讀到**）／`unexplained` **0**｜★★**而 `too_small` 這一桶是【硬斷紅了一次】逼出來的**（第一趟 `unexplained=1`）—— **紅的內容告訴我要補哪一格**｜★★★**驗收⑦：tier2 是唯一三桶都有資料的一列，而它【單調遞增】**（loot0 **0.000** → lootLo **0.003** → lootHi **0.099**）⇒ **util 確實跟著目標的肥瘦走**｜★**但「知道得多挑得更準」我標【未證】**：**tier 與 loot 在這個世界幾乎共線**
---

★落地：`docs/measurements/2026-09-12-attack-zero-buckets-and-tier-loot.txt`（commit `bc2f2dbea`，main）
  ｜實作 `88ae8bb28`｜fp `54e75903ba19db764b249f26dfafdcf1`（3 天窗／seed 1337）

# ① 那 662 個零（★你說「一半的母體沒有解釋我不收」——現在有了）

```
`no_teeth` **395**（贏率 0）｜`poor_and_fed` **275**（目標不肥 ＋ 自己不餓）
`too_small` **1**｜`personality` 0｜**`unexplained` 0**
⇒ ★★**五桶的語意不同**：前兩桶是**結構**（秤說沒理由）／`personality` 是**人格**／
  ★★★`too_small` 是**量級**（每個因子都非 0、乘積 < 門檻）—— **它不是「沒讀到」，是「讀到了但很小」**。
⇒ ★母體注意：這裡分類的是**機會項自己的零**（1279 次裡的 671）；
  而「零 final 662」是**五項相加之後**的零 ⇒ ★★**兩個數不可互相相減**。
★而 `too_small` 是**第一趟那個硬斷紅出來的**：`unexplained = 1` ⇒ 我補了第五種
  ⇒ ★★★**一個會紅的判準，紅的時候會告訴你缺哪一格** —— 這就是它的價值。
```

# ② 驗收⑦（★你說這是本票最有意思的那一格）

```
              loot0            lootLo           lootHi
tier0      **0.012**(n=297)   不可判(n=0)      不可判(n=0)
tier1      不可判(n=0)        不可判(n=0)      **0.201**(n=240)
tier2      **0.000**(n=15)    **0.003**(n=64)  **0.099**(n=663)
⇒ ★★★**tier2 單調遞增** ⇒ **秤跟著目標的肥瘦走**（這一格**成立**）。
⇒ ★而 tier0／tier1 **各自只有一個 loot 桶有資料** ⇒ 它們的「落差」**不可判**（不是 0，是沒有資料）。
⇒ ★★★**而我要主動標一條你沒問的限**：**tier 與 loot 在這個世界幾乎【共線】**
  （tier0 的 belief 幾乎都算出 loot0；tier1 幾乎都算出 lootHi）
  ⇒ **不能**從這張表說「**知道得多的隊挑得更準**」——
  ⇒ ★★要證它需要**同一批目標在不同 tier 下的對照**（同一個獵物、不同情報等級）
    ⇒ **本卷沒做** ⇒ **標【未證】**，不是標綠。
```

# ③ 本票還欠的一格（★我先講，免得它被當成已經做了）

```
★**同 seed 兩跑 fp 相同**（spec 驗收⑤的後半）—— **本卷沒跑**（機器吃緊，一次只跑一趟）
⇒ ★★而它**不該省**：這一票**改了行為**（fp 變了是預期），
  ★★★而「fp 會變」與「fp 每次都變成同一個」是兩回事 —— **後者才證明它是確定性的**。
⇒ 機器有空我補一趟（同窗同 seed 跑兩次比 fp）。
```
