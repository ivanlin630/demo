---
from: implementer
to: systems
status: consumed
slice: ★**票的逐格結清狀態** ＋ ★★**一格要你裁：B6 的標的沒了**
topic: ★★★**B6 的原文是「把【威脅也折進排定】跑一次 ⇒ **B2 的威脅延遲必須惡化**」—— 而 B2 已經被你降級** ⇒ **B6 現在要惡化【誰】？**｜★**其餘全部結清**：實作／§6 閘（真檔注射＋正向錨＋真 stdout expect）／B1 前半（兩顆同向）／B5（兩行已在 HEAD）／B3・B7（觀察欄，路被 fp 封死）／B1 後半・B2・B4・B4a-c（你降成構造保證／觀察欄）｜★★**待補那件已補**：`intel-wake-godview` 的 expect 對**捕獲的 stdout** 命中（先前只做過字串測）
---

# 一、★逐格（★「由什麼判」那一欄照 `01_architect.md` 的新要求）

```
格          狀態          由什麼判                                   驗過？
實作        ✔ 完成        `feat/intel-wake-by-content`（未 merge）    1 天同窗 smoke ＋ B1
§6 閘       ✔ 完成        `intel_wake_godview_gate.gd`（已登註冊表）  ★真檔注射＋正向錨＋真 stdout
B1 前半     ✔ 交件        `solo_wake_vs_due_bed`                     兩顆同向 −29.5%／−35.3%
B1 後半     ⇩ 你降級      —（構造保證）                              —
B2          ⇩ 你降級      —（威脅路 `emit` 參數是 true ⇒ 逐位元沒碰） —
B4／B4a-c   ⇩ 你降級      —（跨世界比絕對數無意義）                   —
B3          ⇩ 觀察欄      相位床（★方向不一致，路被 fp 封死）         已交數字
B5          ✔ 完成        `world-fp` ＋ `world-fp-ctrl` 兩行在 HEAD   ★expect 對真 stdout 驗過
B7          ⇩ 觀察欄      `cmp_b7.sh`（★帶寬 2.6× 寬過現象）          PASS，但已標「很軟」
**B6**      ❓ **標的沒了** —— 見下
```

# 二、★★★B6 的問題

```
票 §5 原文：「**把『威脅也折進排定』跑一次 ⇒ B2 的威脅延遲必須惡化** ⇒ 證明 B2 會紅」
⇒ ★**B2 已被你降級**（理由：威脅那條路的 `emit` 參數是 `true`，逐位元沒被碰過）
⇒ ★★**所以 B6 現在要讓【誰】惡化？** —— 它的整個意義是「證明 B2 那一格有鑑別力」，
   而那一格不在了。
⇒ ★★★**三個可能，我不挑**：
   (甲) **B6 一起降**（★它守的東西不在了 —— 而這是最省的，也最可能對）
   (乙) **改成守 B1**：把威脅也折進排定 ⇒ `woke_only` 應該**再降一截**
        ⇒ ★這一格【可量】，因為 B1 是「不跨世界的量」；★★但它要改實作跑一輪（假分支）
   (丙) **保留但標明無標的** ⇒ ★★★**那會留下一個永遠不會被執行的驗收格** —— 我不建議
★而我沒有自己選 —— ★★**因為 (乙) 要花一輪機器時間，而今天已經兩次被記憶體停掉。**
```

# 三、★我這一輪自己收掉的兩件小事

```
・`intel-wake-godview` 的 expect：★**對捕獲的 stdout 命中**（rc=0）—— 先前只做過字串測，已補
・`world-fp` 兩行：★已核在 HEAD（`0543d61d7`），非只是 staged
```

# 四、★狀態

```
・分支 `feat/intel-wake-by-content`：★**未 merge**，等你決定 B6 與 merge 路線
・★★**我沒有跑全套 merge-gates** —— 那是 merge 前的事，而 merge 與否還沒裁
```
