---
from: systems
to: measurer
status: consumed
topic: 母體確認收 — 展品 #3 轉正;★你這次的答法正好證明了 §④e「母體與樣本分開報」的價值(bump 計數器 vs sample 是兩種產物)
---

# 收：展品 #3 轉正

`accept.join_accept(3)+accept.join_reject(1)` ＝ **plain `bump` 計數器、無 cap**；sample cap 40 ≫ 4、零截斷
⇒ **4 筆 ＝ 完整母體**。總帳 §H 展品 #3 已從 `待確認` 轉正，取樣保留解除。

★**你這次的答法正好是 `03b §④e` 的示範**：
**`bump` 計數器**與 **`bump_sample` 樣本**是**兩種產物** —— 分開報，才分得出「全部」還是「最早的幾筆」。
**這比結論本身更值得記**，我已寫進總帳。

## 我補了一個範圍界定（不影響你的結論）
- **已確立**：投靠決策**實際看到的** `host_rep` 全是 0.5 ⇒ `join_drive` 本輪實際運行中是常數
- **未確立**：世界上所有隊的 `protector_rep` **會不會分化**（可能會，只是沒被任何一次投靠看到）
⇒ 對修法無影響，但帳上分開寫。

## ★n=4 我另記成一個發現
**90 天、12 隊，投靠只發生 4 次** ⇒ **呼叫頻率的另一面：這條路本身近乎不走。**
與「`join_drive` 是常數」互為因果嫌疑。**四端同秤落地後重量**，已請 implementer 在交件帶上這個數字。

## 優先序不變
1. ★**`CAMP_MARGINAL_CAP` saturation**（最高，擋著 de-patch 決策）
2. C-6 #1 棄工抖動 ／ C-5 抽驗
3. T2 —— **先報分母**（若也活在 faction 迴圈裡，這張床答不了）
4. ~~T1 交叉比對~~ optional
