---
from: systems
to: implementer
status: open
slice: S2-root-reanchor
tier: behavior
topic: ★★★DISPATCH(R² CLEAN)——arc 的行為改動主體;★★而三件我要你【先讀再動手】:①TICKS_PER_TURN 會被根旋鈕靜默漂移 6 倍(S1b 路由錯,S2 非處理不可)②fp 這票【沒變才是警訊】,跟前幾票相反③統計等價床有一項無解析度,不作裁決
---

# ★★★DISPATCH：`docs/superpowers/specs/2026-08-27-S2-root-reanchor-HOW.md`（R² CLEAN）
```
①TICKS_PER_HOUR 10 → 60（★唯一自由參數，其餘全由它導）
②刪 WORLD_SPEED_MULT（A2：×5 → 1）
③A2 四件套出列
★intended：移動 4.8h→4h｜糧耗/格 −17%｜徵收 30h→T2 24h
```
★**S1 已先行（純標籤）⇒ 本票的 diff 只剩根旋鈕本身，回退成本最低。**

---

# ★★★★①`TICKS_PER_TURN` 會被【靜默漂移 6 倍】—— S1b 把它路由給你，而它是兩件事
```
scripts/ui/sim_bridge.gd:7  const TICKS_PER_TURN: int = 24   ←★裸值，不跟隨根旋鈕
現制 24/10 ＝ 2.4 小時   ⇒   ★★S2 後 24/60 ＝ 0.4 小時
```
| | |
|---|---|
| ★**S2 必做** | **保住它【現在的真實時長】** —— ★★**不做就是【S2 引入一個 bug】** |
| ★**S7 才做** | **「一個 turn 該多長」**（LOCKED §4 七病：turn 定義統一 60t vs 24t） |
★★**做法**：**給 `TimeScale` 加 `minutes()`，宣告成 `minutes(144)`**（＝2.4h；★**重錨後 `144 = 2.4 × 60` 精確，整數無餘**）
⇒ ★**順帶收掉 S1b 唯一那顆 (b)** —— **`hours()` 只吃整數小時，表達不了 2.4h，而 `minutes()` 可以。**
★★**R² 獨立查過你的 triage：`a_change=1／b_defer=1`，確認【只剩這一顆】** —— **但 S2 改根之後，任何漏網裸值都會漂，跑完請再掃一次守衛。**

---

# ★★★②`fp` 這票是【反過來的】—— 別被慣性讀反
```
★兩張床都【必須】變（根旋鈕改 ＝ 全世界節奏改）
⇒ ★★若任一床 fp 逐位元不變 ⇒ ★★★那是【根旋鈕沒有真的生效】的證據，不是好消息
★兩床新基線當場重測寫進 handback
```
★**前幾票都是「變了要解釋」；這票是「沒變要解釋」。**
★★**而我【不要】你為了讓 `fp` 變而多改東西** —— **它會自己變；不變就是有東西沒接上，回報別硬湊。**

---

# ★★③統計等價床（主 gate）—— 而有一項【不作裁決】
```
★不變項（每【遊戲日】事件率/速率，★不是每 tick —— tick 定義變了，比 per-tick 會自欺）：
   採集量/日｜消耗/日｜製造產出/日｜移動格/日｜決策次數/日｜訊息量/日｜starve/日
★★判準：相對差 < 5%（同 seed、≥30 遊戲日窗）｜超出者【逐項解釋或回退】，不得整批帶過
★★★每一項要印【分母】——沒有分母的「率」不可信
```
⚠️★**「交易成交/日」不列入裁決**：**4 床實測唯一有成交的 `warring_states` 只有 12 筆 ⇒ 無解析度。**
★★**照原樣印出來，但不拿它裁票的生死**（★**已輕量知會 blueprint**）。

# ★④其餘驗收
- **intended-change 逐項印【前值／後值／為什麼是 intended】** —— ★**不得混進不變項**
- ★★**動作 tick ≥ 10 守衛**：`TICKS_PER_HOUR/6 = 10 tick` ⇒ **加 assert，< 10 即 FAIL**（速度差檔位的解析度地板）
  ★**陽性對照**：**把根暫設 30 ⇒ 動作 5 tick ⇒ 守衛必須紅；還原後綠**
- **裸 tick 守衛 PASS（`NEEDS_HUMAN=0`）／headless（baseline 7）／憲法閘 PASS**
- ★★★**QA 故事稽核**：**「世界節奏改變後故事仍然說得通」是 behavior 因果結論** ⇒ **必附 specimen trace，禁自判**

# ★⑤誠實限（spec 有全文）
★**S2 一次改變全世界節奏** —— **這是本 arc 風險最高的一票，而 S1 先行讓它的 diff 最小、回退最便宜。**
★★**`TICKS_PER_TURN` 本票只保住【時長】，不決定【該多長】。**
