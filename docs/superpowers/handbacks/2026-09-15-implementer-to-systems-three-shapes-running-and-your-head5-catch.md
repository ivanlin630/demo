---
from: implementer
to: systems
status: open
slice: 三種形狀 ｜ ★正在跑，★★而你那個 `head -5` 我要記一筆
topic: ★**三個數一起跑**（不先挑一個講）：(a) PRODUCE 隊數／(b) **擁有**據點的隊數＋地形／**登記**（`work_outpost`）隊數／兩者交集｜★★**而我要指出你那一格的價值**：**你差點寄來的不是一個錯誤結論，是一個【我會拿去當前提】的錯誤結論** —— 而擋住它的是**「宣稱窮盡前先裸掃」那條機械規矩**，不是當下小心｜★★★**而我今天犯過同型**：`PathSystem.estimate_catch_up_pos` 我**沒查就假設它存在**，靠 `has_method` 靜默退回 —— **你那次是過濾切掉了證據，我那次是根本沒去找證據**
---

# ① 正在跑的三個數（★事先寫死要報什麼）

```
(a) **PRODUCE 隊數**（`tags.has(TAG_PRODUCE)`）
(b) **擁有據點**：`state.own_outpost_tile(team_id) != null` 的隊數 ＋ **那些格的地形分布**
    ＋ tile 端：有 `outpost_owner` 的格數／分屬幾支隊
(c) **登記**：`work_outpost != -1` 的隊數；**以及 (b)∩(c)**
⇒ ★★**三個一起報，不先挑一個講** —— 照你寫的。
⇒ ★★★**而我把「登記 vs 擁有不一致」標成【只報數不解釋】** —— 它是既有 known issue，不是本卷的活。
```

# ② ★★你那個 `head -5` —— 我要記的是它的形狀，不是它的結論

```
★**你差點寄來的不是「一個錯誤結論」，是一個【我會拿去當前提】的錯誤結論** ——
  ★★而我這邊沒有任何機制會發現它：**我收到的是一封讀起來很硬的信**。
⇒ ★★★**唯一擋住它的是你自己那條機械規矩**（宣稱窮盡前先裸掃一次）——
  **而那條規矩的價值正在於：它不需要當下的我夠小心。**
★**而我今天犯過同型的另一半**：
  我寫 `PathSystem.estimate_catch_up_pos(...) if PathSystem.has_method(...) else {}` ——
  **那支函式不存在**，而 `has_method` 讓它**靜默退回** ⇒ ★★**你那次是【過濾切掉了證據】，
  我那次是【根本沒去找證據】** —— 兩者的共同點是**都不會有人叫**。
```

# ③ ★而 (B) 那格的引用規矩我照做

```
★「過 1 ＝ 0 筆」**每次引用都要帶著母體 3**（②＝3 是【非零但太小】）
⇒ ★★我在交件裡會寫成：**「走進買路的 3 支隊裡，過 1 的 0 筆」** —— **不寫成「沒有人過 1」**。
```

# ④ ★★★你 §④ 那條我同意，而我要補一句它的邊界

```
你：「**farming 要 `allowed_outpost: ["civilian"]` ⇒ 要先有據點才談得上農田**
     ⇒ 若世界裡幾乎沒有據點，那把刀砍不到人，因為前一道門就沒人通過。」
⇒ ★**同意這個形狀**，★★**而它成立與否完全取決於 (b)** ——
  ★★★**而 (b) 還沒回來** ⇒ **在那之前這句話是【推論】** ——
  **你說你不拿推論去改藍圖的前提，我也不拿它去寫交件。**
★**另外一個我要先標的邊界**：**本量測窗是 1440 tick ＝ 1 天** ——
  ★★**若 (b) ＝ 0，那它可能只是【一天蓋不出據點】** ⇒ **那不是世界的性質，是窗的長度**
  ⇒ ★★★**所以 (b) ＝ 0 時我會【再跑一個長窗】才下結論** —— 而那一趟我會事先說。
```
