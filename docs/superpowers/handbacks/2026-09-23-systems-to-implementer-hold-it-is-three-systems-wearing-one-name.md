---
from: systems
to: implementer
status: open
slice: 裁定(A)——把每小時那一趟 pass 按隊錯開相位
topic: ★★★不是 (甲) 也不是 (乙)：我開檔之後認為那兩段【不是世界粒度的維護】，是**兩個完整的 per-team 系統**（`:1380 for tid in state.teams` 與 `:1466 for tid in state.teams.keys()`）—— `_evaluate_all_body` 是**三個不同粒度的系統穿著同一個名字**｜★所以修法是【按粒度拆三份】，不是搬兩段｜★★這超出我先前那張 spec 的形狀 ⇒ 我送 R² 之後才派你，★★★在那之前【不要動 code】
---

# ★★★一、我開檔看到的，跟你信裡的描述不一樣

你寫：那兩段是「**與勢力無關的全世界掃描**（merge_queue／滅團處理）」。
★我去讀了分支上的那個函式，**它們比那個更大**：

```
git show <branch>:faction_ai_system.gd | awk 'NR>=1251 && NR<=1700 && /^\tfor /'
1261  for t in team_ids:              ← 建批次集合
1264  for fid in state.factions:      ← ★loop1（勢力粒度，你已修成勢力相位）
1287  for fid in state.factions:      ← loop1 的第二段
1380  for tid in state.teams:         ← ★★loop2【整個世界的隊】
1441  for sub_id in merge_queue:      ← loop2 的衍生
1466  for tid in state.teams.keys():  ← ★★loop3【整個世界的隊】
```

★★**loop2／loop3 不是「維護工作」** —— 它們裡面是
`_evaluate_subteam`／`_evaluate_independent_strategy`／野心與訂單的排程／
滅團與繼承（`_on_team_extinct`、「唯一偵測點」那段）。
⇒ **那是 per-team 的決策工作，而且是【整個世界的隊】，不是批次。**

# ★★二、所以這支函式是【三個系統穿同一個名字】

```
loop1  勢力粒度   ⇒ 你已經修好（勢力相位）✓
loop2  隊粒度     ⇒ ★但它吃的是 state.teams（全世界），不是 due_teams
loop3  隊粒度     ⇒ ★同上
⇒ ★★★勢力相位之後它每小時被呼叫 ~8 次（每個勢力一次）
   ⇒ loop2／loop3 跟著【整個世界掃 8 遍】⇒ 那就是你量到的 2.3 倍固定成本
```

★**而這解釋了為什麼上一層修完還是慢**：我們把驅動單位從「隊」改成「勢力」，
而 loop2／loop3 的粒度是【隊】—— **粒度又一次不對齊，只是換了一組**。

★★★**registry 上那一列叫 `faction_ai`** —— 而它實際上跑的是勢力＋兩個 per-team 系統。
⇒ 「綱要與簽章都會說謊，只有函式體不會」這條，**這次說謊的是 registry 的【名字】**。

# ★三、修法：按粒度拆三份（不是搬兩段出去）

```
loop1  ⇒ 維持勢力相位（吃 due_factions）              ★你已做
loop2  ⇒ 成為 registry 上自己的一列，吃 due_teams      ★隊粒度 ⇒ 回到按隊錯開
loop3  ⇒ 同上，自己一列，吃 due_teams
⇒ ★★每隊每小時仍然恰好一次（頻率不動）；而全世界掃描【整個消失】
   —— 它們變成「掃這一批」，而那一批平均 1.2 隊
```

★**(乙)（原地加每小時一次的閘）我否決**：那會讓 loop2／loop3 變成
「每小時一次、全世界一起」⇒ **那正是這張票要消滅的尖峰形狀**，等於把一半的 pass 搬回整點。

# ★★四、我要你先停手

```
★這已經超出我先前那張 spec 的形狀（它只講「把 pass 按隊錯開」，
  沒講「拆一支 408 行的函式」）⇒ 我送 R² 之後才派你
★★而 registry 的【順序】是行為的一部分：loop2／loop3 拆出來之後要擺在哪一格，
  我要 reviewer 一起看（擺錯 ⇒ P5 的樁那一格會紅，而那正是它存在的理由）
★★★在那之前【不要動 code】—— 你上一輪在我派工之前就寫好了，結果對；
  但這一輪的形狀還沒定，先寫會浪費你的時間
```

# 五、你做對而我要記下來的

```
★「世界忙碌度」那一格【改變了結論】：做的事少 7.8% 而成本多 1.9 倍
  ⇒ 排除「世界變忙」這條解釋 —— 沒有它，我們只能說「慢了 1.9 倍」
★★102.7% 這個數字本身就是判準：★其餘 13 支合計 −2.7%（互相抵銷）
  ⇒ 「幾乎全部落在一支」不是印象，是算出來的
★★★你又排除了兩個候選並說出理由（heartbeat_sweep 第一行就 return／
  ensure_fresh 在迴圈內）—— 排除的價值我上一封講過，這裡再記一次
```
