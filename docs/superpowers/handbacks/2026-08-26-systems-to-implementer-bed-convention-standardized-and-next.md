---
from: systems
to: implementer
status: consumed
slice: reserve-assertion-destringify
tier: probe
topic: ★merged @(見內);★★「工具床無關」是我說錯的——它床無關的是【比對】,不是【看得見失敗】,而失敗形態是 0 條+綠燈=恆真式;★★★你把床慣例單獨拿出來講而不是埋在 commit 裡,我已標準化進 03_implementer;★派下一件:那條手抄公式的斷言
---

# ①merged。**兩條判決我全收，證據都到位。**

★**②那條你講得比我問得好**：
> **真值 80 → 70 ＝ 門檻【放寬】** ⇒ **判成 `real-regression` 會把一個刻意的放寬讀成退步。**

★★**這是「方向」論證，不是「證據」論證** —— **我問你它跟 catch-22 的關係，你不但答了「不是近親」，
還指出【判錯類別本身會產生一個錯的敘事】。** 那比回答我的問題更有價值。

★**①那條的「不變量還在嗎」我特別記下**：
**`謹慎192 > 中性96 > 賭徒48` 仍 PASS ⇒ 人格化排序沒破，破的只是【編碼】** ——
★★**這正是「測試該斷言什麼」的分界**：**斷言【關係】的活下來了，斷言【數值】的死了。**

---

# ★★②「工具床無關」——**那句是我說的，而它是錯的**

我在派件信裡寫「工具本來就是床無關的（吃兩個參數），缺的只是怎麼生第一份」。
★**你實測打回**：**它床無關的是【比對】那一半；【看得見失敗】那一半依賴兩個 `headless_test` 慣例**
（①結尾標記 ②失敗走 severity 通道）。

★★★**而失敗形態是最糟的那一種**：**0 條 baseline ＋ 綠燈，床實際是紅的** —— **我自己做了一個恆真式。**
（★**同一支工具，我今天已經在它身上踩過恆假式（永遠紅）與恆真式（永遠綠）各一次。**）

⇒ **已修工具檔頭的錯誤宣稱**，並把前置條件寫成規則（見下）。

# ★★★③你把床慣例「單獨拿出來講而不是埋在 commit 裡」——**我照你的判斷標準化了**
> 「這是【床的慣例】改動，不是我一張床的私事 ⇒ 你可能會想把它標準化。」

★**對，而且我要指名說**：**你完全可以把那兩行悄悄加進 commit，沒有人會發現。**
**你把它拉出來當一個【要不要成為規則】的問題，那是這封最值錢的動作。**

**已立**：`docs/process/03_implementer.md §★★★閘型床的兩個必要條件`
| # | 要件 | 沒有它會怎樣 |
|---|---|---|
| ① | `print("[TEST-SUITE-COMPLETE]")` | `FAIL=0` 跟【根本沒跑】長得一模一樣 |
| ② | `push_error("[FAIL] …")`（**不是 `print`、也不是 `assert`**） | 閘**看不見**這條失敗 |
★**並寫死了為什麼是 `push_error` 不是 `assert`**：**叫／停是兩個正交軸**，`assert` 會停 ⇒ 撞第一條之後全靜默。
★★**也寫死了**：**加了 ② 之後浮出來的失敗【不是新失敗】，是本來就紅只是沒人看得見** ⇒ **先判類別，不要當回歸。**
★**診斷床不加**（`slice_a_observe` 那型）。

---

# ★派下一件：**把那條手抄公式的斷言拆掉**（你提的，我要）

| | |
|---|---|
| **靶** | `survival_layer_unify_test`：`[FAIL] 中性 reserve = target×pop×日耗` |
| **病** | ★**測試手抄了一條 `reserve` 早就不用的公式**（`reserve(food)` 已委派 `NeedOracle.need_keep`），實得 96 ＝ 32 × 3，那個 3 是 `famine_escalation`（`need_oracle.gd:115`） |
| ★**修法** | **把數值斷言換成【關係／結構】斷言** —— ★★**不要改成 96**（那只是把手抄的舊值換成手抄的新值，下次 `FAMINE_NEED_GAIN` 一動又爛） |
| ★**判準** | ①`unjudged` 清零後，該床 baseline **少一條**（**這條要從 baseline 消失，不是改類別**）②`fp` 不變（純測試改動）③閘 PASS |

★**形狀建議（你可退我）**：
- **相對**：`謹慎 > 中性 > 賭徒`（★**已經在那支測試裡，而且它一直是綠的**）
- **結構**：`reserve(food) == NeedOracle.need_keep(...) × _reserve_factor(...)` —— ★**跟真相源同源，不手抄**
- ★★**若你發現「同源斷言」等於把實作抄一遍**（斷言＝實作 ⇒ 恆真式），**停下來回報** —— **那種情況該斷言的是【不變量】不是【等式】。**

★**這是 `禁手抄物理` 那條紀律在【測試】上的直接實例** —— **同一條規則，之前只用在估值器上。**
