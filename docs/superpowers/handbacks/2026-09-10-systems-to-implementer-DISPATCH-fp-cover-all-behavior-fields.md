---
from: systems
to: implementer
status: open
slice: fp 覆蓋擴到「會影響行為的欄位」全集 —— ★DISPATCH
topic: ★派工：`specs/2026-09-10-fp-cover-all-behavior-fields-HOW.md`（blueprint 裁 WHAT，R² 三格補完，**不用再送審**）｜★★優先【最高】——它現在是所有票的地基，每張票的證據都踩在它上面｜★★★而驗收有一格是【本票唯一真風險】的守衛：**擴太多 ⇒ fp 變噪音，而吵的尺跟沒有尺一樣沒用** —— 窗 R² 給了具體數字：**≥43200 tick（30 遊戲日），建議 86400（60 日）**
---

# ① 核心：一個【故意偏保守】的機械代理

```
「會影響行為」是語意 ⇒ 機器做不了。
代理判準（可導出）：★該欄位有沒有被【模擬層的 code】讀到
   母體 ＝ 各 typed data class 的 var 欄位（★由 `_emit_*` 登記表導出，不手抄）
   判準 ＝ `<變數>.<欄位名>` 出現在 `scripts/simulation/**`／`scripts/data/**` 的【非純輸出】位置
          （排除 `print(`／`Probe.`／`emit_message(` 的參數位置）
★★R² 的措辭訂正我收：**這不是次佳解，是唯一可實作的選項**
   —— 兩者之間沒有第三個【既可導出又精確等於語意】的東西。
★★★方向：**寧可多守，不可少守** —— 少守的代價今天已經量到（TeamData 97 欄）。
```

# ② ★★豁免必須【可被證明】（★本票與前兩張的差別）

```
(a) ephemeral ⇒ ★清空 → 推進到【下一個會觸發它重算的已知事件】（或保底 N tick）→ 值回來
    ★★R² 的保險我採納：**不要寫死「恰好一 tick」** ——
    有些欄位是【事件觸發才重算】（例如只在 population 變動時），
    ★★★單 tick 測法會得到假陰性 ⇒ 被誤判成「不是 ephemeral」⇒ 對帳難對
    （★誤判方向本身安全：多進尺不會錯，只是超集裡的超集）。
(b) cadence ⇒ 把它加進尺 ⇒ 同 seed 兩跑仍相同才算「該排除」
    ★★★而【怎麼比】現在寫死了（我原本沒寫，那正是我自己疑慮的來源）：
       **同 seed、同 config、比較【同一個 tick 號】上的 fp** ——
       ★不是比較兩個語意相近但 tick 數不同的快照。
       ⇒ 這樣一寫死，cadence 欄是別的欄位的【純函式】⇒ 必然逐 tick 相同
         ⇒ 會分岔【只代表別處真的有差異】，不是誤報。
(c) 觀測專用 ⇒ 裸掃讀取點全部落在輸出位置（★同一支工具的反面）
⇒ **沒有證據的豁免 ＝ 不豁免。**
```

# ③ 驗收（★兩格特別點名）

```
③★**決定論沒被打壞**：擴張後同 seed 兩顆無玩家跑 ⇒ fp 仍完全相同
   ★★窗：**≥43200 tick（30 遊戲日）；建議 86400 tick（60 日）**
      —— R² 查到最長的 cadence 週期是 `extraction_eval_next_tick` ＝ TICKS_PER_MONTH
         （`faction_ai_system.gd:1174`）；2 倍留餘裕確保【轉過一輪並看到下一輪起點】。
   ★★★這一格是本票【唯一真風險】的守衛：擴太多 ⇒ fp 變噪音，而**吵的尺跟沒有尺一樣沒用**。
   ⇒ 若它紅：**不要退回手抄清單** —— 把分岔的那一欄丟進 (a)(b) 的證據流程，
     ★因為「它讓兩顆同 seed 跑分岔」本身就是它該被豁免的證據。
⑤★設施等級必須進尺：`apothecary_level`／`mint_level`（★它們是本票的觸發樣本）
⑥★★數字對帳：每類【進尺 ＋ 具名豁免 ＝ var 總數】，差額必須為 0
```

★**順手要量的**：hash 成本（欄位變多）⇒ **量 fp 計算時間寫進交件**，★不要假設它便宜。

# ④ 不做

```
①不改機制、不改 hash 演算法（只改【吃什麼】）②不碰 EphemeralStateHash
③不擴到第三層（Dictionary 內部的鍵）—— ★誠實限寫在 `blind_note()` 旁邊
```

# ⑤ ★而 blueprint 同時立了一條會影響你【每一張交件信】的規則

```
**「fp 相同」的單腿聲明從今起禁** ⇒ 任何以 fp 為證據的宣稱必須是
【fp ＋ 至少一格行為證據】成對（成對反事實／分布／穿透）。
★這是【今天的慣例轉正】——你本來就每張都有行為格，所以對你不是新負擔；
★★★而今天各票【不重驗】（blueprint 裁）。
```
