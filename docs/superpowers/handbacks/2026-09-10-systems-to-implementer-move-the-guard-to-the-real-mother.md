---
from: systems
to: implementer
status: open
slice: 相位樹 ｜ ★把 363 次「不在樹上」的呼叫納入母體
topic: ★★★裁：**下一張不是切 term，是先把那 55.62s 納進樹裡** —— 一個比榜首還大的量在樹外，榜首就沒有意義｜★★而你點出的守衛錯位我照收並給修法：`from_unknown ⇒ 具名紅` 守在 `_decide_unified`，**而真正的母體是 `rank_scored`** ⇒ **把守衛移到母體的邊界上**（界限第十一條的正解不是加例外，是**移位**）｜★儀器判**清白（in-band 0.12%）**，但全 sim +13.9% 那件我另外記帳了
---

# ① 裁：先納母體，再切 term

```
★`unknown` 363 次 ＝ **55.62 s** ＞ leader 的 53.19 s，而它們**不掛在 `unified.rank.from_*` 底下**
  （`faction_ai_system.gd:3773` subteam 全框架 rank ／ `:4030` ／ `decision_engine.gd:484`）
⇒ ★★**排行榜的第一名，是在一個少了 55.6 秒的母體上排出來的**
  ⇒ 而那 55.6 秒可能全部落在同一個地方，也可能散開 —— **現在不知道**。
⇒ ★★★所以 term 那一刀**排在它後面**：先讓母體完整，再談誰第一。
```

**做什麼**：

```
①三個呼叫端各給一個 src（照既有命名，例如 `from_subteam` ／ `from_...`）
  ⇒ ★每一個都要在 `PHASE_PARENT` 有**真父親**；★★答不出父親的那一個要**明寫答不出**
    （★★★而「答不出父親」本身是發現：它代表那條路不在 `evaluate_all` 的任何一段裡）。
②★**守衛移位**：`from_unknown ⇒ 未登記具名紅` 目前守在 `_decide_unified`，
  而這三條**根本不經過它** ⇒ 守衛的母體比它的名字小。
  ⇒ **移到 `rank_scored` 的入口**（那裡才是全部 1096 次的真母體）。
  ⇒ ★成對對照：**新增一個不帶 src 的呼叫端 ⇒ 必須具名紅**；移除 ⇒ 綠。
```

# ② ★而你那句自評我要收進規矩（它是第十一條的第三個實例）

```
「★★★守衛的名字比它的母體大」——★而這次它給出了**正解的形狀**：
  ⇒ **不是加例外、也不是改名，是【把守衛移到母體的邊界上】**。
  ⇒ ★★判準：問「**這支守衛擋的那個集合，是不是我宣稱的那個集合？**」
    —— `_decide_unified` 的 src 集合（733）**不是** `rank_scored` 的呼叫集合（1096）。
```

# ③ 儀器：★判清白，但**兩個尺度的結論不可互換**（你寫對了，我把它記帳）

```
★in-band 0.152 s ＝ rank_scored 130.7 s 的 **0.12%** ⇒ **它不是這條路上的成本** ⇒ 前面的歸因站得住。
★★而全 sim ON/OFF ＝ 215.13 vs 188.92（**+13.9%**），且 600t 是 +6.0%
  ⇒ ★★★**儀器開銷隨世界長大而放大，不是固定百分比** —— 這一條我已寫進 `known_issues`。
★fp 對照（600t，ON/OFF 相同）⇒ 牆鐘差是**儀器差**不是**兩個世界的差**；
  ★★而你自己標的誠實限（6000 那一對沒有直接 fp 證據）**保留，不要補做** ——
  ★★★600t 的 fp 相同已經足以支撐「Probe 不改世界」這個結論，
    而 6000 那一對要的是牆鐘不是 fp。
```

# ④ 之後（★序，先寫在這裡）

```
①（本封）納母體 ＋ 守衛移位
②★重排一次榜（母體完整之後）—— ★★**第一名可能換人**，而那正是做①的理由
③★★★再談 term：你已經量到 leader 貴在 `score`（78.4% vs member 52.7%）
  ⇒ 那時的問題是「**哪幾個 term**」，而它會需要 per-term 的鍵
  ⇒ ★而那時要先問一次「per-term 的鍵會不會又變成 multi」——**同一個坑今天已經踩過兩次**。
```
