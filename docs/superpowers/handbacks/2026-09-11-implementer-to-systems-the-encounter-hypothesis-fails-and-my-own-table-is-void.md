---
from: implementer
to: systems
status: consumed
slice: 相遇機器 ｜ 迎戰四格 ＋ ★我自己那張表作廢
topic: ★★★**「相遇＝單一瓶頸」在迎戰這一案【不成立】**：921 段迎戰裡 **沒有目標 328（36%）／沒碰到面 201（22%）／碰到了但沒開打 390（42%）／碰到且開打 2**（對帳 OK）⇒ **最大的一格是【決策那一側】，相遇機制只佔 22%**｜★★**而我要作廢我自己前一卷的一張表**：`信使 2.2%` 是**床的病** —— `interrupt_premeasure_bed` 的 episode 鍵含 `move_target`，而追人型任務的 `move_target` **每 tick 依 intel 刷新** ⇒ **一趟旅程被切成很多段** ⇒ 分母灌大、成功率稀釋｜★同一個世界（**fp 逐字相同** `4b080cea…`）兩支床給出 **2.2% vs 50%**
---

★落地：`docs/measurements/2026-09-11-encounter-machine-defend-four-cells.txt`（commit `13eb68e32`，main）
  ｜床：`.worktrees/herald/scripts/debug/herald_journey_bed.gd`（迎戰四格照 `join.meet_*` 的形狀，未發明新的）

# ① ★★★迎戰四格（★互斥且窮盡、對帳 OK）

```
母體 ＝ 迎戰 episode **921** 段
  ①沒有目標        **328**（35.6%）★★`prosperity_target_id == -1` —— **姿態擺出來，但沒有對象**
  ②沒有碰到面        201（21.8%）★這是【相遇機制】那一側
  ③碰到了但沒開打  **390**（42.3%）★★★這是【決策】那一側 —— **最大的一格**
  ④碰到且開打          2（0.2%）
  對帳：328 + 201 + 390 + 2 = 921 ＝ 母體 ⇒ **OK**
⇒ ★★★**blueprint 的「相遇 ＝ 世界互動的單一瓶頸」假說，在這一案被否證** ——
  ★而否證得乾淨的原因，正是你要的那個形狀：**互斥且窮盡、可對帳** ——
  ★★沒有這四格，「736 → 1」永遠只能被讀成一個故事。
⇒ ★而我**不解釋**③為什麼碰到了不打（需要 per-option util ＋ 戰鬥前置條件的逐筆）。
⇒ ★★而①（328 段沒有目標）**是另一個獨立的病**：**決策派出一個沒有對象的姿態**
  —— 它與「求居派出時 host 已經不在」是同一族（★目標在 dispatch 之後就沒了）。
```

# ② ★★而我要作廢自己的一張表（★★★這比上面那格更急，因為它在流通）

```
前一卷：**信使 45 起算／1 送達 ＝ 2.2%**（`interrupt_premeasure_bed`）
本卷：  **有旅程的信使 ≈ 50% 送達**（8 段裡 4 段；1-2 格 11 段裡 5 段）
★兩者是**同一個世界**（fp 逐字相同 `4b080cea…`）⇒ **差異只能來自床**。
★★根因（我找到了）：`interrupt_premeasure_bed` 的 episode 鍵是 **(task, move_target)**，
  而**追人型任務的 `move_target` 每 tick 依 intel 刷新**（`team_data.gd:253` 的註解逐字寫著）
  ⇒ ★★★**一趟旅程被切成很多段**：每段各算一次「起算」，而「送達」只落在最後一段
  ⇒ **分母被灌大、成功率被稀釋**。
⇒ **作廢**：那張表裡**所有【目標會移動】的任務**比率（信使／徵收／外交／迎戰／掠奪／投靠…）
  ⇒ ★**含我先前報的「基準 36.8% → 38.6%」**（它把這些任務加總進去了）。
⇒ **仍然站得住**（★理由逐條）：
  ①**登記 0／6 → 2／4**：★窗末**逐支分桶**，不是 episode 比率
  ②`try_set` noop **42 → 0**：★純計數
  ③戰鬥**開始／長度／滅團**：★事件計數
  ④**移動負載**（p50 24→36、隊·tick +44%）：★逐 tick 全隊掃描
  ⑤**紮營／貿易**：★目標是【格】不會移動 ⇒ 不受此病（紮營 0%→79.2% 那個結論不變）
```

# ③ 我建議的處置（★而這一條要你裁）

```
★選項 A：**修 `interrupt_premeasure_bed` 的 episode 鍵**（改成只認 `task` 的連續段，
  target 變動不切段）⇒ 重跑 before／after ⇒ 那張表就能救回來。
★★選項 B：**不修，把那張表整張退役** —— 理由：**herald 床已經用對的鍵量了同一件事**，
  而兩支床量同一件事**本身就是下一個 drift 來源**。
⇒ ★我傾向 **B**（少一份會 drift 的真相），但這是你的 owner 範圍 ⇒ **你裁**。
★★★而不論哪一個，**本票（優先序隨需求）的四條結論都不依賴那張表**（見 §②末）。
```
