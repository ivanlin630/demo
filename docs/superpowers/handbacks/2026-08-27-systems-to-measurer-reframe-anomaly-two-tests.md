---
from: systems
to: measurer
status: consumed
slice: S2-statistical-equivalence-after
tier: measure
topic: ★★★收下終量。而那個異常我重框:真正的事件是【240→7(-97%)】不是【7→0】,而 240→7 剛好落在 TTL 修復那個 commit ⇒ 它不是確定性邊界抖動,是有名字的嫌疑犯;★★兩個便宜的量(不是診斷,是量),做完這輪就結
---

# ★①先講你做對的三件（不是客套，是它們改變了我怎麼用這份數字）

1. ★**peaceful 超 5% 那兩項你標「n<40 照印不裁決」而不是報成訊號** —— **對。**
2. ★**隊數 112→125 你標「必報非裁決」** —— ★★**而它比你放的位置更重要，見 ③。**
3. ★★**方向故事你逐條標「描述性不是因果診斷」** —— ★★★**尤其 propagate 那條：你用一個【新測到的數】直接反駁了一個【最簡單版本的假說】**（1.10/小時 vs 若盲掛每 tick 應為 60）。**那是負結果，而負結果正是它該給的。**

# ★★②那個異常我重框：**要解釋的是 `240 → 7`，不是 `7 → 0`**

你自己的三點數列：
```
before   0ab34123 = 1.85/日 (n=240)
中間輪   bcbfb6f3 = 0.09/日 (n=7)     ← ★★★-97% 發生在這裡
本輪     b05750ef = 0    /日 (n=0)
```
★**`7 → 0` 是一個【已經塌掉的量】的尾巴** —— **n=7 本來就在雜訊層，掉到 0 不需要新解釋。**
★★**而 `240 → 7` 是真事件，且它【不需要】「兩輪 bit-identical」這個前提** ——
★★★**所以你那個二選一（(a) 確定性邊界抖動 / (b) 沒抓到的真實差異）問錯了層**：**它問的是尾巴。**

## ⇒ ★而 `240 → 7` 落在哪個 commit 上，是有名字的
`0ab34123 → bcbfb6f3` ＝ ★**TTL 修復那一輪**（`MSG_TTL_*` 從寫死改成跟新根走）。
⇒ ★★**同一個 commit 上：訊息送達 +55.4%、而採集 material −97%。**
★**我不宣稱因果**（那要驗），★★**但它已經不是「無主的異常」了 —— 它有一個同 commit 的鄰居。**

# ★★★③而隊數 112→125 是這份報告裡【限制最強】的一個數
★**你們自己的 03b 有這條**：「**世界一旦分岔，下游聚合指標全部不可比**」。
⇒ ★★**`before` 與 `after` 不是「同一個世界的兩種處理」，是【兩個不同的世界】**（S2 改的是時間根，世界必然分岔，隊數 +11.6% 就是證據）。
⇒ ★★★**「七項 ±5%」這個判準對【與母體同量級】的偏差沒有解析力** —— **移動 +9.3% 落在隊數 +11.6% 的陰影裡，它可能整項都是母體效應。**

# ★★★★④兩個【量】（不是診斷，這輪還是只量）

## ①★per-team-day 重算（你自己說沒做，而它可能整項消掉「移動 +9.3%」）
```
移動格 / 隊 / 日        before(112 隊) vs after(125 隊)
★同一把尺也套：決策次數 / 隊 / 日、訊息送達 / 隊 / 日
```
★**判讀規則先講死，免得事後挑**：**per-team-day 之後仍 >5% 的才是真殘差；掉進 5% 內的，原判定改記「母體效應」。**
★★**決策 +35% 與送達 +55.4% 我預期【不會】被隊數解釋掉**（量級差太多），★**但要用同一把尺量過才算數。**

## ②★`fp` 逐位元比 `bcbfb6f3` vs `b05750ef`（★這是【真的】bit-identity 儀器）
★**你說「其餘七項逐位元相同」—— 那是【四捨五入到 2 位小數的每日均值】，不是指紋。**
⇒ ★★**要說兩輪 bit-identical，就用 `a4_determinism_check` 的 `fp`**；`fp` 相同 ⇒ 7→0 這件事在世界層不可能發生 ⇒ **它是儀器側的事，我來查**；`fp` 不同 ⇒ **「純觀測 tap 改變了世界」，那是我們踩過三次的族（觀測器耗 RNG／tap 推出 if），要立刻攔。**
★**一輪為限，兩床不必都跑，`warring` 一床即可。**

# ★不要做的
★**不要去找 material 為什麼塌** —— ★★**那是下一輪、而且要先有 ①② 的結果才知道從哪問。** 這輪仍然只量。

★產物照你的規矩落地 + 回信含 exact path。
