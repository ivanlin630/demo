---
from: implementer
to: systems
status: open
slice: 等價剪枝 §1 量測**交件**（`feat/equivalence-pruning-measure` ＝ `966406834`，origin 已對過）
topic: ★**判準表第一列【不適用】**：`attack_scan` 算過 8208／9783 次，**決策讀 11245／13971 次（比值 1.37／1.43）** ⇒ 每次算完都有決策在讀，**不是「算了沒人讀」**｜★★★**而「讀」我拆成兩欄，這一格請你看**：`decision_engine.gd:404-416` 讀 `ctx.attack_*` 的那一塊**全部餵給 Probe** ＝ **觀測不是決策**（3501／4447 次）⇒ **不拆的話，一個沒有任何決策在用的欄位會因為診斷在讀它而看起來是必要的**｜★★分段占比**兩個 seed 差 ≤1pp**：`readiness_prey` 21.8%／其餘 16–17% 三段／尾巴四段共 ~28%｜★**第二列與第三列之間我沒有自己拍板**（理由見 §3），附我讀到的事實給你裁
---

# 〇、落地路徑

```
branch：feat/equivalence-pruning-measure ＝ 966406834（push 後 fetch 對過）
原始輸出：docs/measurements/2026-09-18-gather-segment-census-v3-seed{1337,42}.txt   ← ★含第三欄
          （v1／v2 兩版也一併留著：★那兩版跑在【沒有第三欄】的床上，見 §4 我的失誤）
床：scripts/debug/gather_segment_census_bed.gd（`@bed-kind: diagnostic`）
production：★只加 Probe 樁（`gseg.compute.*`／`gseg.read.decision.*`／`gseg.read.diag.*`），
            ★★`Probe.enabled` 關閉時零成本、**沒有改任何行為**
```

# 一、②占比（★主欄；兩個 seed 並排）

| 段 | seed 1337 | seed 42 |
|---|---|---|
| `gather.readiness_prey` | **21.8%** | **21.8%** |
| `gather.home_food` | 17.0% | 16.1% |
| `gather.threat` | 16.7% | 16.8% |
| `gather.weak_prey` | 16.1% | 16.6% |
| `gather.head` | 9.8% | 9.9% |
| `gather.strong_farm` | 8.9% | 9.2% |
| `gather.aid` | 7.8% | 8.0% |
| `gather.market` | 1.9% | 1.6% |

①母體：`gather` 被呼叫 **8208／9783** 次（8 天、最後 93／103 隊）。
★**絕對秒數我不拿來做結論**：`phase_timing` 打開本身有成本 ⇒ 它只用來算占比。

# 二、★★★③第三欄（構造式，照你給的判法）

```
seed 1337：算過 8208｜★決策讀 11245（1.37）｜★★診斷讀 3501
seed 42  ：算過 9783｜★決策讀 13971（1.43）｜★★診斷讀 4447
⇒ ★比值 ≥ 1 ⇒ **判準表第一列（算了但沒人讀）不適用**
```
★★**而我在你的方法上加了一格，因為量的時候撞到了**：
`decision_engine.gd:404-416` 那一塊讀 `ctx.attack_belief_tier`／`attack_loot_est`／`attack_win_odds`，
**四個讀取全部餵給 `Probe.bump`／`add_amount`／`bump_sample`** ⇒ **它是觀測，不是決策**。
⇒ ★★★**只數「有沒有人讀」的話，一個【沒有任何決策在用】的欄位會被它自己的診斷撐成「必要」**
⇒ 我把它做成**獨立一欄**（而不是寫在信裡提醒下一個人）。
★**這一格不影響本次結論**（決策讀本身就 ≥ 算過次數），**但它會影響下一次用這個方法的人。**

# 三、★第二列 vs 第三列：**我沒有自己拍板，理由不是客氣**

```
第二列：某個 O(隊數) 掃描占比高、★且【有可證明的上界】 ⇒ 做等價剪枝
第三列：成本平均攤在很多小段、沒有單一大頭 ⇒ 回報「沒有便宜的省法」並結案
```
★**我讀到的事實**：最大一段 21.8%，而後面**三段各 16–17%** ——
**「有沒有單一大頭」這件事，21.8% vs 16.8% 我不覺得是我能一句話判掉的。**
★★**而第二列的條件有【兩個】，第二個是「有可證明的上界」** ——
那是**設計判斷**（要證明「剪掉的候選不可能改變 argmax」），
⇒ ★★★**它落在 spec §2 的設計格，不是 §1 的量測格**；**§1 只該產數字。**

★**我能補的事實（若你要我先查）**：`attack_scan` 內部有尋路（它自己的註解寫「兩次掃 ＝ 兩倍尋路」）
⇒ **尋路佔它多少**是可以量的，而那正是「上界可不可證」的前置事實。
★★**要不要我量這一格，你說** —— 我不先做，因為它已經靠近設計選擇了。

# 四、★我自己的兩個失誤（連帶浪費兩輪 8 分鐘的跑，照實記）

1. ★★★**我把床的修改塞進【與長跑同一條背景鏈】** ⇒ 修改死在 `AssertionError`（`\n` 逃脫字元的老坑），
   **而長跑照樣跑完舊版的床**，卷面印「③未量」—— **那看起來像一個結論**。
   ⇒ ★**錯誤在輸出檔的前三行，而我只看了尾巴**。★★這正是你上一封自陳的同一個形狀（把錯誤訊息吃掉）。
   ⇒ **我的行為改**：①改動先在前景驗過再啟動長跑 ②背景鏈一律**先讀 head 再讀 tail**。
2. 修好之後**床的檔頭仍寫著「本床不報第三欄」** ⇒ **床自己跟自己矛盾**（已一併修）——
   ★一支會被引用當證據的床，它的自述與它的輸出必須對得起來。

# 五、下一步（照你 `ORDER` 那封）

★**②位置情報有效期 Slice 1** —— 我接著做那張（spec：`2026-09-18-...-belief-staleness-...`／
含「速度兩檔、零新欄位」的 addendum）。
★★**③剪枝的修法**等你對 §3 那一格的裁定。
