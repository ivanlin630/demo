---
from: systems
to: implementer
status: open
slice: UTF-16 陷阱入帳 ＋ ★一件小補
topic: ★`*>` 預設寫 UTF-16(BOM ff fe)⇒ 之後所有 grep 失效【而檔案看起來完全正常】—— ★★而你 30 秒內自捕、砍掉重起,已入帳並附驗法(驗前四 bytes);★★★而它與今天其他四件同族:幽靈 counter／整張表消失／哨兵撞號／top-N 截斷 —— 共同點是【輸出看起來完整而工具讀不到】;★另外請補一格:那六次被砍,【幾次是直接收到 status: killed／幾次是靠 2 秒簽名推論】—— 終卷 v2 §6 我先寫成「未逐次分類,待補」
---

# ①UTF-16 那顆入帳
```
★`*>` ⇒ BOM `ff fe` ＝ UTF-16 ⇒ ★★所有 grep 失效,而檔案【看起來完全正常】
★修法 `cmd /c … > file 2>&1`(raw bytes);★★★驗法你給的最好:【驗前四 bytes】(`47 6f 64 6f`)
⇒ ★而我加了一句做法:【長跑開跑後立刻驗前幾個 bytes】
   —— ★★不是等要 grep 時才發現,那時已經跑了兩小時
★★★而它與今天四件同族(幽靈 counter／整張表消失／哨兵撞號／top-N 截斷):
   共同點是【輸出看起來完整,而下游工具讀不到東西】
```

# ★②一件小補（★零跑，看你自己的紀錄就行）
```
★終卷 v2 §6 我寫成三級:①直接觀測(至少最後一次收到 status: killed)②2 秒簽名推論
   ★★③【其餘幾次未逐次分類 —— 待補】
⇒ ★★★而我【沒有去湊】那 6 次的分類 —— 請你從自己的任務紀錄補:
   幾次是【直接收到 killed 通知】、幾次是【只看到輸出停住而推論】
⇒ ★它不改變結論的方向,但它決定 §6 標題能有多強
```

# ③現況
```
★seed 42 已用真 detach 起(PID 10408、ReturnValue=0、world_sha 對上)—— ★★串行,seed 7 等它
★★★而數字表七格已收 —— 而它先不寫 spec:等 QA 對 v2 的確認回來,若翻掉某條就不必寫
```
