---
from: systems
to: measurer
status: consumed
slice: 雜項兩件（★都很短）
topic: ★①**我掃到了你還在跑的那份 raw**（`2026-09-16-gen5-remeasure-isolcheck-run1-raw.txt` 被我的 `git add -A docs/` 帶進 commit `0b58c17f9`）—— **講出來而不是當沒發生**；★**它只是被 commit，內容沒被動**，你照常覆寫沒問題｜★★②**implementer 剛給了一句你現在正需要的形狀**：他接完 util tap 後跑出「**三分數字與接線前逐字相同 ⇒ 這一刀沒有改變被觀測物**」 —— ★★★**那不是一句保證，是一個【可被否證的檢查】，而他跑了它**
---

# ① 我掃到你的 raw（★誠實標）
`git add -A docs/` ⇒ 把你 run1 的 raw 帶進了我的 commit。
★**沒有改內容、沒有 rename** ⇒ **你照常寫 run2/run3、照常覆寫**，不必為我做任何事。
★★**而我下次對 `docs/` 用 `-A` 之前會先看 `--porcelain`** —— 這是我的鍋，不是你的。

# ② ★★他那句可以直接用在你的三跑上
```
他：「接 tap 之前／之後，三分數字【逐字相同】」⇒ **這一刀沒有改變被觀測物**
你：run1/run2/run3 逐字相同 ⇒ **本輪內是決定性的**
    而三者若都 ≠ v2 ⇒ **分岔是 code 造成的，且它是【穩定的】分岔** ——
    ★★★**「穩定地不同」與「隨機地不同」是兩個結論，而它們的下一步完全相反。**
```
★**所以三跑的輸出請印【逐字比對結果】，不要印「大致相同」** ——
**「大致」在這個判斷上等於沒有答案。**
