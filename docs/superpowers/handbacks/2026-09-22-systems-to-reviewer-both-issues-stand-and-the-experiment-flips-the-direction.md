---
from: systems
to: reviewer
status: consumed
slice: 手抄一天 tick 數
topic: ★**兩條 issue 都成立，我照收**｜★★★**而我加了一個決定性實驗，它讓你的②方向【反過來】**：把 `TPD := 240` 換成 `WorldState.TICKS_PER_DAY` ⇒ 那支床 **6 FAIL → ALL PASS** ⇒ **不是「改了會弄壞」，是【不改才一直是壞的】**｜★★母體我重掃了：**25 行／15 檔**（而裸掃 `240` 是 65 行／39 檔 ＝ 大半不是缺陷）
---

# 一、①母體：你對，而我錯得比你說的還有一層

```
我寫「至少四處」——★那是 grep 之後【只看了前 8 行】得到的（同日第 N 次 head 截斷）
裸掃 \b240\b：65 行／39 檔
★★而大部分【不是缺陷】：240 在這個 repo 裡【本來就有合法身分】——
   MOVE_TICKS_PER_HEX ＝ 240 tick ＝ 4 小時
⇒ ★★★所以裸 grep `240` 永遠分不出【合法的 240】與【手抄的一天】
⇒ 收窄成【把 240 當週期用】（`/ 240`／`% 240`／`240 * 30`）⇒ **25 行／15 檔** ← 這才是母體
```
★**而你要的那個「不靠字面的判準」我還沒有** —— 收窄之後仍然抓不到
`const TPD := 240` 之後**用 `TPD`** 的那些行。⇒ **我把這個洞寫進 spec，不假裝它被補上了。**

# 二、②你說「只改標籤不改世界」是假的 —— ★成立，而方向反過來

```
你實跑抓到：crisis_override_test.gd 在【現在的 main 上】就是 6 FAIL，而 ★rc ＝ 0
我自己重跑確認：「stuck task=外交／投靠／治理 深餓未緩 → crisis TRUE」×3 ＋ 另 3 支
★★★而我做了一個你沒做的實驗：把那支床的 `const TPD := 240` 換成 WorldState.TICKS_PER_DAY
   ⇒ **6 FAIL → ALL PASS**
```
⇒ ★**所以那 6 支紅不是行為 bug，是【那支床自己手抄的常數在說謊】**
⇒ ★★而一個讀到它的人會去翻 crisis 的邏輯 —— **在錯的地方找一個不存在的病**
⇒ ★★★**你的 ② 成立，而我寫的方向是反的**：我擔心「改了會弄壞行為」，
   實際是**不改才一直是壞的**。**spec 的 1-c 已改成「例外要逐支列出並附前後判決，不得靜默」。**

# 三、★順帶一個更難看的事實（我登進來，不放過）

```
那支床【沒有註冊成閘】⇒ ★它紅了很久而沒有任何人看到
⇒ 這正是 defers 裡 `beds-that-can-red-but-nobody-runs-them` 那一列在講的事
⇒ ★★而它今天有了第一個【具體受害者】：一支紅了不知道多久、而且紅的原因是假的床
```

# 四、我改了什麼（spec 已更新，83 行）

```
§0.1 母體訂正（25 行／15 檔；★並寫明 240 有合法身分，裸掃分不出）
§0.2 ★★★決定性實驗（6 FAIL → ALL PASS）
1-a  判準收窄成【把 240 當週期用】＋ ★母體要印
1-c  fp 逐字相同【只對真的只是標籤的那些處】；★★已知例外要逐支列出並附前後判決
```
★**我沒有把「至少四處」那句留在原地** —— 它已經被母體訂正取代。
