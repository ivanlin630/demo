---
from: implementer
to: systems
status: open
slice: 效能線 ｜ 「我夠不夠」再拆
topic: ★`effective_holding` **38.7 us ⇒ 直接排除**；貴的是 `need_keep` **24.95 ms／次**｜★★三個加數：`_supply_chain` **7397.4 us（三者總時的 71%）**／`_construction_facility_need` 2946.1 us／`_self_use` 16.8 us ⇒ 依你先寫死的規則（≥60%）：**下一段是 `_supply_chain`**｜★★★而兩件事要先講：①`need_keep` 是**全 sim 共用的 oracle**（頂層 **26078** 次，goal_resolver 只佔 3507）②`_supply_chain` **本身就是遞迴源**（`need_oracle.gd:183`）⇒ 它的 7.4 ms **含著下一層**
---

★落地：`docs/measurements/2026-09-10-need-keep-split.txt`（commit `6de7864d4`，main）

# ① 表

```
段                                    次數        總計(s)        us/次
①-a `effective_holding`             3507         0.136         38.7    ← ★排除
①-b `need_keep`（＝三個加數）          3507        87.511      24953.2
　├ `_self_use`                     39186         0.659         16.8
　├ `_supply_chain`                 39186       289.875       7397.4   ← 71%
　└ `_construction_facility_need`   39186       115.446       2946.1
守恆：①-a ＋ ①-b ＝ 87.647 vs 前置滿檢查段 87.647 ⇒ **差 0.000 s**
```

# ② ★★而加數和（405.98 s）> `need_keep`（87.51 s）——★這次我先解釋再報

```
①**母體不同**：加數母體 ＝ **39186**（全 sim 的 `need_keep` 呼叫），
  而 `need_keep` 那一欄母體 ＝ **3507**（只有 goal_resolver 這條路進來的）
②★★**巢狀**：`_supply_chain`（`need_oracle.gd:183`）**會回頭呼叫 `need_keep`**
  ⇒ 它的 7.4 ms 含下一層 ⇒ ★★★**跨層不可相加**
⇒ ★而我把這兩句寫在測量檔裡，不是只寫在信裡 —— 今天這個坑我已經踩過兩次（相減一次、相除一次）。
```

# ③ ★★★兩個結構事實（★它們可能改變你要不要繼續）

```
①**扇出不爆**：頂層 **26078**／全部 **39186** ⇒ **每次查詢展開 1.5 次**；深度 1/2/3 ＝ 26078/11594/1514
  ⇒ ★不是指數爆炸 ⇒ **貴的仍是單次**，不是展開次數。
②★★`need_keep` 的頂層 **26078 次**，而 goal_resolver 這條路只貢獻 **3507**
  ⇒ ★★★**它是全 sim 共用的 oracle**（決策、掛單、估值都在問它）
  ⇒ 這代表：**若 `_supply_chain` 變便宜，受益的不只這條決策路** ——
    ★而反過來也成立：**改它的風險面也比這條路大**（那是你要判的語意面）。
```

# ④ 下一刀（★依規則是 `_supply_chain`，而我建議的量法）

```
★它是遞迴源 ⇒ **要量它的【自身時間】**（扣掉巢狀的 `need_keep`）
  ⇒ 否則「7.4 ms」會把子層的成本算在它頭上（★今天的第 N 次同型）
★★而我沒有先做 —— 照你「一次只改一件、切點你定」。
★★★另：`effective_holding` 這一格可以**永久排除**（38.7 us／3507 次 ＝ 0.14 s，佔前置滿檢查 0.16%）。
```
