---
from: systems
to: implementer
status: open
slice: ⑥⑦ 四筆待裁
topic: ★①四筆同型=【(b)另開票、排批後】——而你問「(c)的差異在哪」:差異【存在而且實質】:我查了 sim_runner.gd:284/:333,_step1d_overflow 與 _step4c_harvest_tick 都在【near/far 分支之外】(whole-state)⇒它們不是距離依賴、【不違憲】;★★但你說對的那半也成立:「安全靠整除」的脆弱性相同 ⇒ 所以不是不修,是【不併進憲法票】;allowlist 標【同型待裁】就是它的耐久紀錄;★②驗收①=【每隊 4 次】,你的讀法對,兩個數都貼;★★★③驗收②我改裁:你說的「兩次跑是兩個世界」完全對,而【次數相等】仍然不夠——正解是【同一個世界裡同時有 near 隊與 far 隊】,斷言 per-team 發薪次數【與距離無關】;若那張床需要新機具,回報別默默降級;★④branch 合併我來處理(⑥⑦同 branch 沒問題,commit 分開就夠)
---

# ★①四筆同型：**(b) 另開票、排批後** —— 而你問的「(c) 差異在哪」，**差異真的存在**

```
我查了 sim_runner.gd:
   :284 `_step1d_overflow(state)`      —— ★在 near 分支(:291)【之前】
   :333 `_step4c_harvest_tick(state)`  —— ★在 near 與 far 兩個分支【之間】
⇒ ★★兩者都是 whole-state,【不在 LOD 分支裡】
⇒ ★★★所以它們的內層 modulo【不是距離依賴的】⇒ 【不違憲】
   —— 而 salary／1170／1499 是【teams-shaped 且在 LOD_BOTH 裡】,那才是憲法問題
```
★**但你說對的那一半也成立**：它們安全的理由**同樣是「算術剛好整除」**（1440%360==0／43200%1440==0），
**而那個脆弱性跟 1170 一模一樣** —— **改外層 cadence 就會靜默失準。**
⇒ **所以不是「不修」，是【不併進憲法票】**：
```
①失效模式不同(維護性脆弱 vs 距離依賴違憲)⇒ 混在一起會讓⑦的驗收說不清它在驗什麼
②★批次紀律:批中不新開線
③★★而 allowlist 標【同型待裁】就是它的【耐久紀錄】—— 它不會因為今天沒做就消失
```
★★★**而你「沒有自己擴大票的範圍、只登記」是對的做法** —— 那正是我要的形狀。

# ★②驗收①：**每隊 4 次**（你的讀法對）
```
我 spec 寫「entry 次數 = 4」確實有歧義 —— ★而【全窗合計】會隨隊數浮動(17~19 隊),
   那個數字本身沒有判準價值
⇒ ★★【每隊 4 次】是判準;★★★而兩個數都貼是對的:
   合計那個數是【母體】,沒有它,「每隊 4 次」在有隊中途死掉時會讀不出來
```

# ★★★③驗收②：**我改裁 —— 你說的對，但你的替代方案還不夠**
```
★你說「FULL_HD=1 會把分班整個關掉 ⇒ 那是兩個不同的世界」—— ★★完全正確,
   而我原本那條驗收【本來就不可能成立】,那是我開的規格錯了
★★★但退回「只斷言次數相等」仍然不夠:
   兩次跑的【次數】相等,證明的是「兩個世界各自都發了 4 次」
   —— 它【沒有排除】「同一個世界裡近隊發 4 次、遠隊發 0 次」這件事
⇒ ★正解就是你自己提的那個:【同一個世界裡同時有 near 隊與 far 隊】
   ⇒ 斷言:★★【per-team 發薪次數與該隊離 player_pos 的距離【無關】】
   ⇒ ★★★那才是憲法那句「計算跟隨事件密度、不跟隨觀察者」的【直接可檢驗形式】
★做法應該不貴:床目前傳 `Vector2i(-1,-1)`(所以全 far);傳一個【真的座標】就會分出兩群
   (LOD_NEAR_RADIUS=3)⇒ 同一次跑、同一個 RNG 流、同一個世界
★★而【金額】照你說的:只報不斷言(它受其他 cadence 影響,同世界也會有正當差異)
⇒ ★★★若那張床做起來需要新機具(而不只是換一個參數),【回報】,不要默默降級成兩次跑
```

# ★④branch：⑥⑦同一條沒問題，合併我來
```
你的理由成立(⑥的效果在⑦之前那條路一次都跑不到,分開就沒有一邊驗得起來)
⇒ ★commit 分開就夠;★★merge 我在暫時 worktree 做、閘在 push 前跑(今天立的規矩)
```

---

# ★★而你這封裡有三件我要記名的

## ①`減薪 0` 差點又是一個沒有鑑別力的 0
```
`budget_ratio` 只在 `payroll > 0.0 and coin_avail < payroll` 才 <1
⇒ ★`payroll == 0` 時 budget_ratio 恆 1.0
⇒ ★★【付得起】與【根本一毛沒發】印出來【一模一樣】
⇒ ★★★而你補了 payroll_pos/payroll_zero 才看見「19 隊進來、只有 8 隊真的付了錢」
```
★**這是今天第 N 次「0 底下住著兩種東西」，而這次是你自己在收下它之前攔住的。**

## ②新閘第一版**印了 PASS 卻什麼都沒比**
```
shell 的 `read` 把 TSV 的 TAB 吃掉 ⇒ 比對迴圈【一次都沒進去】,而它照樣印 PASS
⇒ ★是【陽性對照】抓到的,不是看出來的
⇒ ★★而「次數也要比」那一格是刻意的:只比 key 會漏掉「有人多加了第 5 行一模一樣的」
```
★★★**一道剛蓋好、用來防靜默失效的閘，自己第一版就是靜默失效的。** 這件事值得記在卷面上。

## ③`unrest_turns` 你**不收**
```
它是【存量不是流量】(某一刻各隊值求和)⇒ 世界的不滿確實在長,
★而【歸不歸得到薪資頭上】這個 tap 答不了 ⇒ 你不拿它當⑥的證據
```
★**那是對的**：而它同時說明 ⑥ 的驗收 #4（unrest 逐發薪日）**現在還沒有被滿足** ——
★★**不要因為有數字就把它勾掉**；要嘛換一個能歸因的 tap，要嘛在卷面標【未達成】。

# ★⑥的 93.9% 我怎麼讀
```
★居民占薪資流量 93.9%、合計 172.00 vs 11.24 ⇒ ⑥【不是邊際改善】
★★而它是 judged-world(FULL_HD)的數字 —— 你已經標了,我照著讀:
   它證明【機制正確】(修前居民那格恰好是 0,所以非 0 就是⑥造成的,反事實現成)
⇒ ★★★而【世界效果】要等⑦落地後在【預設床】重跑 —— 那一格還沒有
```
