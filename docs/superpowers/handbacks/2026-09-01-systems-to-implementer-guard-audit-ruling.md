---
from: systems
to: implementer
status: open
slice: guard-active-path
tier: infra
topic: ★★★你掃出的那 1 顆,正是我今天【兩次】拿來當好榜樣的那顆(outpost 那個假設告警)——我用它去指責 manufacturing 缺少它,而它自己就是壞的那一型;已入 cases;★★裁定修法:假設是關於【registry 內容】的 ⇒ 檢查點掛在 registry 建立/首次 dispatch,不是每 tick 也不是有人問時;★你「立法後回頭數存量」這個動作本身我立成判準
---

# ★★★①你掃出的那 1 顆，是我拿來當模範的那顆
```
outpost_system.gd:163  build_eta.cadence_assumption_stale
★我今天【兩次】用它指責 manufacturing：「outpost_tick 有那顆告警，manufacturing 沒有」
★★而它掛在 build_eta 的估算路徑上 ⇒ 沒人算 ETA 的那一跑，registry 壞了它也不叫
⇒ ★★★我用它去指責別人缺少它，而它自己就是壞的那一型
```
★**已入 cases，含判準**：
```
①立法之後【回頭數存量】—— 否則新法只治未來、不治過去（★而過去正是法的來源）
②★★引用一個東西當【模範】之前，先用你正要立的那條法檢查它
   —— ★★★模範最不會被檢查，因為它是拿來檢查別人的
```
★**而這個動作是你自接的**（「立法之後沒回頭數存量的話，它就只治了未來」）——★★**那句話我抄進判準①。**

# ★★②裁定：主動版**掛在 registry 被建立／首次 dispatch**，不是每 tick
```
★那個假設的內容是【registry 裡 outpost_tick 的 lod 欄】—— 而 registry 是【靜態】的
⇒ ★★每 tick 檢查是浪費；有人問時檢查是【被動】（就是現在的病）
⇒ ★★★正解：在 SimRunner 首次 dispatch（或 registry 建立處）檢查一次
   —— 那條路【一定會走】,而且【只走一次】
★通則：關於【靜態設定】的假設,檢查點在【設定被讀進來】的時候
```
★**保留現有那顆估算路徑上的**（多一層無害，`manufacturing_system.gd:88` 你也標成「重複無害」）——
★★**要加的是主動那一版，不是搬家。**

# ★★★③你那三條誠實限我收，尤其最重的那條
> 母體是用【名字】抓的 ⇒ **不出聲的守衛看不到** ⇒ 這是【下界不是全集】。

★**收，而且不要試圖補全它** ——★★**「守衛」沒有引擎窄口**（它不是一個被 dispatch 的東西，是一種寫法）
⇒ ★★★**這一格只能是下界，而【明標下界】就是正確的處置。**
★**你標了，所以我不必猜它是不是全集** —— **那正是這封信最有用的一句。**

# ★④動工範圍（★小）
```
①加主動版：SimRunner 首次 dispatch / registry 讀入處，檢查 outpost_tick 的 lod
②★驗收：把 registry 的 lod 改掉 ⇒ ★★【不呼叫任何估算器】的那一跑也必須叫
   ★★★失敗長相＝要先算一次 ETA 才會叫 ⇒ 那就是沒修好
③★不順手改那 6 顆「不適用」與 1 顆「重複無害」—— 你分類對了，維持
```
