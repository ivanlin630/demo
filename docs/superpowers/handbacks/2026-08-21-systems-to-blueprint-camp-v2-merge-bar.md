---
from: systems
to: blueprint
status: open
topic: ★四端同秤結果 2 綠 1 紅 — 一個我【不願自己裁】的問題:§7 門檻是我寫的,現在該不該放寬?附完整數字與我的建議
---

# 四端同秤：世界回話了，但我卡在一個我不該自己裁的點

## 數字（同床 peaceful 90d seed 1337，baseline ＝ current main）
| # | §7 驗收 | 結果 |
|---|---|---|
| 1 | `outpost.l0_to_l1 > 0`（二值） | **0 ⇒ 紅** |
| 2 | `camp.abandoned < camp.built` | **18 < 24 ⇒ 綠**（折現磚單獨版 25/28＝89%） |
| 3 | 零採集 < 同床 main | **966 < 1133 ⇒ 綠**（★折現磚單獨版 1244 ＝ 反向，**四端同秤把它壓回來了**） |

**附帶**：`camp.won_argmax` 12 → **69**；母隊人口 35 → **49**；
`camp.lost_to` 從「覓食一家獨大」變成 **備戰 137／workshop 72／survival 67／併入 52**
＝ **四端真的在同一把尺上競爭了**（這正是你要的形狀）。

## ★#1 那個 0 現在有完整斷點鏈
```
紮根贏 argmax        = 8      ← ★估值層(這刀的活)是好的:applicable 且贏了 8 次
→ 真的開工           = 1      ← 7 個掉在 commit/仲裁端
→ 完工               = 0      ← 那 1 個也沒完成(工期端,與 75% 棄置同源)
```
⇒ **不是「紮根秤輸」也不是「不 applicable」**，是**贏了之後在下游斷兩次**。
**兩個斷點都在這刀範圍外**（commit/仲裁端 ＋ 工期端）。

## ★我卡在哪，以及為什麼不自己裁
**§7 那句「三條同時綠才算世界層過」是我自己寫的。**
我寫它的時候**假設 #1 的阻塞在這刀之內** —— implementer 的數字顯示**不是**。

**放寬自己設的門檻，正是我這兩天一直禁止 implementer 做的事**
（禁調 cap／禁調 δ／禁改測試讓紅變綠）。**我不能自己開這個例外。**
而且 acceptance 的原話（「滿池餓死清零 ＋ 蓋了就丟率崩」）是**你**給的 ⇒ 這是你的尺。

## 我的建議（供你裁，非決定）
**建議 merge**，理由三條：
1. **#2/#3 是你要的兩件，都真的動了**，且 #3 把折現磚單獨版的**反向**壓回正向 ——
   **這正是「磚只鋪一端會製造假象」的反證**。
2. **#1 的價值已經兌現了**：它把一個**無解釋的 0** 變成 **`8 → 1 → 0` 有座標的斷點鏈**。
   二值門檻的用途是「不准用旋鈕假造」，**它做到了**；它擋不住的是「斷點在別的 slice」。
3. **繼續押著不 merge，下游那兩個斷點反而更難修**（要在一條長期不 merge 的 branch 上疊）。

**若你裁不 merge**，我照辦，並把 funnel 前段票升成本刀的一部分。

**但有一條我不建議動**：`headless` 仍紅（9→15）**必須先清乾淨**才談 merge ——
其中三條我已裁定「合法改測」（測試綁著舊編碼，新斷言必須**更強且零魔數**），
四條投靠族**先量 `join_host_flow` 再說**（很可能是測試場景在「join 還是常數」的年代寫的、
根本沒給 host 放食物 ⇒ 修 fixture 非改斷言），一條 `承諾(掠奪)` 標 `known-blocked-by`。
**這一段我沒有放寬，也不打算放寬。**
