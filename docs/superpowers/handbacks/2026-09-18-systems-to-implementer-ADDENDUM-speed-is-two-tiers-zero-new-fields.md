---
from: systems
to: implementer
status: consumed
slice: 位置 belief 過期線物理化 — Slice 1（**補件**，不改範圍）
topic: ★**WHAT 裁下來了，而它讓這一票變【小】不是變大**：「目標可能速度」做**兩檔**、**零新欄位** —— 錨定（讀**已 merged 的錨定性**）⇒ 最慢那一端；無錨 ⇒ 基準速度｜★★**所以 spec §3 那兩行從「我的沿用」升級成「WHAT 已裁」**，你照原案做就對｜★★★誠實標同時改寫：不是「在函式裡成立、在世界裡不成立」，而是**「兩檔分化【在】世界裡，連續分化【不在】」**
---

# 一、改了什麼（spec 已更新，你不用改做法）

```
drift_tiles ＝ age_ticks × 【目標可能速度】÷ TICKS_PER_DAY
★【目標可能速度】＝兩檔（WHAT 裁 2026-09-18）：
   錨定（believed 駐紮／建設中 —— ★讀【已 merged 的錨定性】，不是新欄位）⇒ 最慢那一端（2 格/日）
   無錨                                                                  ⇒ 基準速度（6 格/日）
 ★降斜率、★★不歸零（取 0 ⇒ 那則 belief 變成【不可證偽】，永遠掉不到會被重新偵查的價值）
 ★★★錨定性自身同線過期（它不是永久標籤）
```
★**這正是你手上已經有的東西**：錨定性那一票已經 merge，`appearance()` 自己會過期的閘也在。
⇒ **這一票不需要開任何 belief 欄位。**

# 二、為什麼值得記住這個裁法

WHAT 沒有給我「連續的速度分化」，理由是**兩檔就已經把世界要的東西給出來了**：
**駐村的三天後仍然準、行軍的三小時就不準。**
⇒ ★而**連續分化要新開一個【移動能力】belief 欄位** ⇒ 那是**第二個付費消費者**才付的錢。
⇒ ★★**他連付費證據的形狀都指定了**：這把尺日後若量到**受害者集中在【同一檔內的快慢差】**
（輕騎兵 vs 牛車都算「無錨」，而那個誤差真的咬人）⇒ **帶數字回來議開欄**。
★★★**我登進追蹤表了**，所以下一個人不用重挖。

# 三、其餘不變

Slice 1 仍然是：**只造尺、不改任何讀者**（格 1-e：fp ＋ 逐 tick 行為軌跡逐字相同）；
`tolerance_tiles` **必填、無預設值**；先拆 `AID_REFUSED_TTL_TICKS`；
`anchoredness_freshness_bed.gd` 那個內容錨**同票更新、不是刪掉**。

★**排序不變**：凍結終線（含把影子雜湊換成既有的 `EphemeralStateHash`）→ 求居／佔村 → 本票。
