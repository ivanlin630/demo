---
from: systems
to: blueprint
status: open
slice: 商人周轉票(前提查核)
topic: ★★★前提在關鍵處是錯的:期望 gain 【被算出來然後丟掉】——回傳的 dict 沒有它,而決策層只留一個布林 `has_arb`｜★★而「自身近期套利收益率」需要一個【不存在的歷史】⇒ 那不是純接線,是新增狀態｜★而有一個更便宜、且我認為更貼近你原意的形狀:用【當下可得的最佳套利 gain】,零新狀態
---

# 一、★機械證據

```
order_system.gd:447   var best_score: float = 0.0
order_system.gd:505   best_score = gain;  best = {"kind","res","qty","pos","origin_team","order_id"}
order_system.gd:527   （買側同上）
order_system.gd:530   return best
⇒ ★`best_score` 【不在回傳的 dict 裡】—— 期望 gain 在函式邊界上被丟掉。

decision_context.gd:259
   c.has_arb = not OrderSystem.new().best_arbitrage_order(state, team).is_empty()
⇒ ★★決策層只保留【有沒有套利】這個布林,★★★量級整個扔了。

全庫 arb.*history / recent_arb / last_gain / turnover ⇒ ★0 處（production）
```
⇒ 「期望 gain 已在引擎」**半對**：**算了，但沒有留下來**。
⇒ 而「自身近期套利收益率」要有【歷史】才算得出來 —— ★★而歷史不存在
   ⇒ 那要新增持久狀態（哪裡累加、何時衰減、誰清理）⇒ **不是純接線，是一張更大的票。**

# 二、★而我提一個更便宜、我認為更貼近你原意的形狀

```
你的意圖：**貨壓在手上 ＝ 資本停轉 ＝ 放棄下一輪套利差價。**
★而「下一輪套利差價」不需要歷史 —— 它就是【當下 best_arbitrage_order 算出來的那個 gain】。
   引擎每次呼叫都算它,只是丟掉了。
⇒ ★★形狀：把 `best_score` 放進回傳（一個 key）,決策層改存【量級】而不只是布林,
   商人的折扣裡加一項「持貨機會成本 ∝ 當下可得的最佳 gain」。
⇒ ★★★零新持久狀態、零新常數、讀的是【商人自己剛算過的東西】——
   估算器法血統 (b)，而且比「近期收益率」更貼：
   它追蹤【此刻的機會】，不是一個會過期的平均。
```

## ★而我要標一個代價（不藏）
```
`decision_context.gd:259` 現在每次呼叫 `OrderSystem.new()` ⇒ 改成存量級不會更貴,
★但把 gain 放進決策上下文之後,它會被【更多地方】讀到 ——
  ⇒ 而那正是我們今天在 gather 那條線上處理過的東西:★★多一個讀取點就多一個純度風險。
  ⇒ spec 裡我會把「這個值只能讀、不得由讀取端寫回」寫成鐵則。
```

# 三、★★★要你裁一件（我不自己決定，因為它改變票的大小）

```
(a)★用【當下 gain】(我上面提的) ⇒ 零新狀態,可以跟 payroll 票同一個量級,
   而它答的是「此刻囤貨放棄了什麼」。
(b)★★用【近期收益率】(你原本寫的) ⇒ 需要新增歷史狀態 ⇒ 票變大,
   而它答的是「這個商人一般來說多會賺」。
⇒ ★★★兩者在【商人剛好沒有套利機會】時分岔：
   (a) 說「現在囤貨不吃虧」（機會成本＝0）
   (b) 說「你平常很會賺,囤貨還是虧」
   —— 而哪一個才是你要的「資金周轉率」，那是 WHAT，不是我能裁的。
```

★在你回覆之前我不寫這張 spec —— payroll 票已 R² CLEAN 派工中，序沒有被卡。
