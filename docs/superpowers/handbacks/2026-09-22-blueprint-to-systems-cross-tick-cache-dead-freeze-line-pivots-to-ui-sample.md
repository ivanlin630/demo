---
from: blueprint
to: systems
status: open
slice: 跨 tick 快取前置量測 — WHAT 判讀 ＋ 凍結線下一格
topic: ★**(丙)=0／0 且排除 tick 仍 0 ⇒ 依預註冊「無條件沿用」形狀永久結案（世代 6）；gap=0 桶 0.75／0.71 也 <95% ⇒ 帶 TTL 的更窄票【也不開】**｜★★樁成本 >10%：時間量作廢、次數比可引用（直接對照 677=677／3758=3758 證樁沒改世界，§5⑥ 要防的正是那件事）｜★★★「欄位幾乎每次變、第一名 85–90% 不變」**不是**快取執照：沿用舊第一名＝10–15% 的決策是錯的＝util 不是真值＝改變世界｜凍結線下一格＝在 HW-2／世代 6 重跑【UI 路徑】的凍結樣本（9/18 那套 176/17280 的床），不再往 gather 內部打
---

# 一、依預註冊執行（你不必問我，這裡只是把我的判讀寫下來）

```
(丙) 整份不變率 ＝ 0.0000（0／1368、0／1239）；排除 tick 後仍 0.0000 ⇒ < 95% ⇒ 「跨 gather 無條件沿用整份結果」永久結案（世代 6 內；規模謂詞，不編 met_check；世代邊界重量）
gap=0（同 tick 兩次 gather）桶：(丙) 0.7504／0.7132 ⇒ 也 < 95% ⇒ 我預註冊的「短間距桶過 95% 且 n≥500 ⇒ 另開 TTL 票」條件不成立 ⇒ TTL 票不開
★母體印了（1368／1239，取樣 1/4），尾巴不是問題；兩顆種子同側 ⇒ 非邊界案
defers `gather-cross-tick-cache-needs-a-data-structure` 那列：結案理由寫【規模謂詞：整份不變率 0】＋ 觸發器＝世代邊界重量
```

# 二、樁成本那格（我的判準，我裁）

```
§5⑥「樁 >10% 作廢」要防的是【樁改變被觀測物】；implementer 做了直接對照：gather 677＝677、attack_scan 圈數 3758＝3758 逐字相同
⇒ 次數比（丙／甲／變在哪）可引用；時間量（snapshot 1.18s／rank 5.17s）不可引用、不進任何卷面
⇒ ★規矩本文補一句（你改協議檔）：「樁 >10% ⇒ 時間量作廢；次數量在【樁開/樁關計數逐字相同】的直接對照下可引用」—— 不然下次又要人裁一次
```

# 三、★★★那個「發現」不是執照

```
「欄位幾乎每次變（糧流族 97–99%）而第一名 85–90% 不變」⇒ 有人會讀成「快取第一名就好，只錯 10–15%」
⇒ 不行：util 必須是真值；沿用舊 argmax ＝ 每 7–10 次決策有 1 次是拿過期資訊做的 ＝ 引擎被腳本化了一成 ＝ 改變世界
⇒ 唯一合法的快取形狀是【純函式 memo 鍵在全部輸入上】—— 而輸入每次都變 ⇒ 永遠不命中 ⇒ 沒有形狀
⇒ 這條寫進 defers 那列的結案理由第二段，免得下一代重挖
```

# 四、凍結線下一格（WHAT）

```
gather 內部三張票（兩圈／catch_up 一次／一圈）結案、七處共用已 merge（整場 6.78%）、跨 tick 快取死 ⇒ ★gather 內部這條路到此為止，不再開第五張
凍結線的交付物是【玩家眼睛看到的卡頓】：舊機樣本 176／17280 幀 >2s（9/18，UI 路徑），新機 headless 8 天窗只剩 2／0 幀
⇒ 下一格 ＝ 在 HW-2／世代 6 重跑 9/18 那套【UI 路徑】凍結樣本床（同窗同 config），印：>2s 幀數、p99、median、母體
   預註冊（數字前）：>2s 幀數 ≤ 1／每遊戲日 且 p99 < 1s ⇒ 凍結線【可交玩】，UI 五分頁兩張票開工，交用戶玩＝體驗回饋非驗收
                     否則 ⇒ 用新樣本的 top-5 區塊重排（profile 在 HW-2 重跑），下一張效能票照模板四欄預註冊
   ★這兩個門檻是我定的玩家尺，不是量出來的；若你認為 UI 路徑的 p99 天然高於 1s（渲染），回我一封改門檻，數字前
派量測員或實作端你定；用戶玩遊戲時讓路照舊
```
