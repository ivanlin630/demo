---
from: systems
to: implementer
status: open
slice: 凍結終線｜`*multi` 感知蒐集塊（`feat/freeze-multi-perception`）
topic: ★**派工**：R² CLEAN｜spec `docs/superpowers/specs/2026-09-18-freeze-multi-perception-block-HOW.md`｜★★★**第一件事不是修效能，是【先證明語意零改變閘會紅】** —— 故意弄髒快取 ⇒ **fp 指紋必須不同**；★**若 fp 仍相同 ⇒ 停下來回報，那支閘對這一票沒有鑑別力**｜★★**reviewer 逐字**：「**靜態讀 code 是更弱的替代，不是更省的等價**」—— 所以那一輪不能用「我讀過 fp 涵蓋什麼」代替｜★**branch 從 merge 完的 main 開**（相位線那條我正在 merge，綠了給你 sha）
---

# 一、順序（★這一票的順序本身就是驗收的一部分）

```
① ★先跑陽性對照：故意讓快取回一個【過期的蒐集結果】⇒ fp 指紋必須【不同】
   ⇒ ★★若 fp 相同 ⇒ **停下來回報**（別修了先，那支閘擋不住這一票）
② 才開始修：快取／髒旗／事件驅動 —— ★計算跟隨【事件密度】不是跟隨 tick
③ 前後對照：★同 seed 同窗、★★兩個 seed 都報、★★★176 幀掉多少【如實報】（沒掉也要報）
④ fp 等價：修法前後同 seed 同窗【逐字相同】
```

# 二、★★兩條硬線（WHAT 明令）

```
★禁降真實：不准少看幾格／少掃幾隊
★禁凍結：不准整段不算
⇒ ★★可操作版（我補的）：**同樣的輸入必須得到同樣的輸出，只是不必每次重算**
```
★★★**髒旗的失效清單**（哪些寫入點會讓快取失效）**要用【裸符號全庫掃】得到** ——
**不是憑印象列**（★假窮盡最兇的形態是「我自己加的過濾條件」）；**那份清單要寫進 code 註解。**

# 三、★單輪排行不可依（blueprint 逐字）
```
*multi 兩輪都是第一（371.8s／186.7s ＝ 第二名的 2.73×／2.16×）⇒ ★target 穩
而排行第 4／5 兩輪換位 ⇒ ★★不要順手去動別的區塊：要動先拿兩輪的數
```

# 四、順帶那一顆計數（★reviewer 判「範圍克制得對」）
`unified.prosp` 兩輪零出現 ⇒ **在外層條件加一顆計數**（`opt=="攻擊"` 且 `征服` 的次數）：
```
外層 > 0 而 prosp ＝ 0 ⇒ 卡在 _is_prosperity_candidate
外層 ＝ 0              ⇒ 母體稀
```
★**只加計數、不 root-cause** —— ★★**那是另一票的活。**
