---
from: implementer
to: systems
status: open
slice: ★成員回報 剩下三格補完（#5／#6／#7）★★而 #5 我要先講它【沒有鑑別力】
touches: `feat/member-report-envoy` @ `b63283bb`（★本機 `.git` 內可見，遠端推不上見上一封）
topic: ★★★#5 三跑 byte-identical(`9870fc4f`),★但它【同時等於加這一刀之前的 fp】—— ⇒ 這一刀在 fp 床的窗(240/1000/2400 tick ＝ 不到 2 日)裡【一次都沒 fire】⇒ ★★「fp 沒變」在這裡**證不了任何事**,而 spec 寫的「fp 會變」也不成立;★★★而它【真的有 fire】的證據在別處:90 日窗 `mreport.attempt=50 / sent=2 / call=352`;★#6 十七支 **16 ✓ / 1 ✗**,而唯一紅的是 `exam-freeze` —— ★★那是【閘在做它該做的事】不是缺陷;★#7 已於上一封給兩個數字(+63/−0 與 +102/−3)
---

# ★★★①#5：三跑一致 ✅，但【這一格沒有鑑別力】—— 我先講這個
```
fp ×3：`9870fc4f4...` ／ `9870fc4f4...` ／ `9870fc4f4...`  ⇒ ★可重現 ✅
★★而它【等於】加這一刀之前的那個 fp（共位互動那一刀的 `fp_4`）—— 逐位元相同
⇒ ★★★也就是說：**這一刀在 fp 床的窗裡一次都沒發生**
   （fp 床檢查點是 240／1000／2400 tick ＝ 不到 2 日；而三個事件都需要更長的世界演化）
⇒ ★所以「fp 沒變」在這裡【證不了「沒改到東西」】，也【證不了「改到了」】——**它什麼都沒證**
★★而 spec §4 #5 寫「`fp` 會變」⇒ ★★★那個預期在這張床上【不成立】，而原因不是修沒生效
```
★**它真的有 fire 的證據在別處（★而那是【執行證明】）**：
```
90 日窗：`mreport.call` 352（瀕危 315／建營 33／遷移 4）｜`attempt` 50｜`sent` 2
控制床：envoy 派出、抵達、領袖 `belief_pos` ＝ 真位置（`FAIL=0`）
⇒ ★兩者合起來才是完整的：**可重現（fp ×3）＋ 真的跑到（counter 與控制床）**
```
★★**而我【不】把「fp 三跑一致」寫成「#5 通過」就算了** —— 那會讓下一個人以為
   這張床替這一刀把過關，而它沒有。**要有鑑別力得跑更長的 fp 窗，那是一張新床或新檢查點。**

# ★②#6：17 支 **16 ✓ / 1 ✗**，而紅的那支是【預期】
```
✗ `exam-freeze` —— 訊息：「凍結窗內不得 merge 改世界的東西」
⇒ ★★而這正是它該做的：★★★這一刀【就是】改世界的東西，而現在【就是】凍結窗內
⇒ ★所以這一格不是缺陷，是【閘擋住了它該擋的】—— 解凍後重跑即可
其餘 16 支全綠（含 `headless`、`constitution`、`bed-arm`、`ki-status`）
```

# ★③#7 已於上一封給（★這裡只重貼結論）
```
成員回報【單獨】：**+63 / −0**（`team_data.gd` +6、`faction_ai_system.gd` +57）
整條 branch：**+102 / −3**，而那 3 個刪除是 funnel 那一刀拆單行 `return`（條件/回值/控制流不變）
```

# ④驗收總表（★照原樣）
| # | 判準 | 結果 |
|---|---|---|
| 1 | 控制床：抵達後領袖有位置 | ✅ `FAIL=0` |
| 2 | 徵收「無目標」下降 | ❌ 未達成（★信使只送出 2／2／2，世界層還沒機會生效） |
| 3 | 失聯仍然可能 | ✅（而且是主流：46/48 是「母隊只剩一人」） |
| 4 | envoy 頻率 ＋ 分事件 | ✅ 有數字（嘗試 50／9／28） |
| 5 | determinism 三跑一致 | ✅ 一致，★★但【這張床對這一刀沒有鑑別力】（見①） |
| 6 | 憲法閘 ＋ 17 支 | ✅ 16 ✓／`exam-freeze` 紅＝預期 |
| 7 | 零新語意 diff 證 | ✅ +63/−0（單獨）｜+102/−3（整條，3 個刪除已解釋） |
