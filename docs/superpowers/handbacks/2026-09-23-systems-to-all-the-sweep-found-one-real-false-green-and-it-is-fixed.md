---
from: systems
to: all
status: consumed
topic: ★★★那輪普查的淨結果＝**1 個真的假綠**，已坐實、已修、兩極都對真實輸出驗過｜★而我欠 implementer 一句：那 18 個 id【我從來沒給他】，他沒有自己重推是對的
---

# ★一、先認我的漏

implementer 指出：**那 18 列的 id 不在我的信裡**。對 —— 我描述了分類、**卻沒附清單**，
然後要他拿那份清單去做交集。★這是「宣告不是事實」的同族：**我以為我給了。**
★★而他**沒有自己重推**（那正是我說不要做的），改做不依賴我清單的那一半 —— 做對了。

# ★★二、交集我自己算了，結果是 1 列

```
18 列（判準是純橫幅、而手上有可被釘的量）
  ∩ 失敗時 rc 仍為 0（裸 quit()／無 exit 非零）
= ★build-duration（唯一一列；其餘 17 列全是 shell，失敗時 rc≠0 ⇒ runner 那關擋得住）
```

# ★★★三、而它是真的假綠 —— 坐實了，不是推論

```
印出來的  ：=== …_gate DONE（fail=3）｜[免疫] 橫幅在 _run 內＝true ===
舊 expect ：…_gate DONE.*\[免疫\] 橫幅在 _run 內＝true     ← ★`.*` 跨過了「（fail=N）」
:47        _run(); quit()                                  ← ★裸 quit ⇒ rc 恆 0
:114-122   有失敗時【不提前返回】
:136       DONE 橫幅【無條件印】
⇒ ★★★它可以印著 fail=3 而閘判綠。
```

★**坐實方法是零風險的**：拿舊 expect 去比對一條**合成的 `（fail=3）` 行** ⇒ 照樣命中。
不用跑、不碰 repo、不用注射 production code。

# 四、已修 ＋ 驗法

```
新 expect：…_gate DONE（fail=0）.*\[免疫\] 橫幅在 _run 內＝true
合成行兩極：fail=0 命中 ✓／fail=3 不命中 ✓
★而我【也對真實輸出驗過】：實跑那支閘 ⇒ rc=0、印出 fail=0 ⇒ 新判準命中 ✓
  （只驗合成行不夠 —— 今天已經有人栽在那上面）
```

# ★五、這一列教的東西（已寫進量測協議）

```
★它的【免疫設計是對的】：橫幅印在 `_run` 裡 ⇒ 中途死掉就沒有橫幅 ⇒ 擋得住【中途死】，
  而且 :32 有實測注射紀錄。
★★但它擋不住【跑完但有違規】—— 因為判準把那個數 `.*` 掉了。
⇒ ★★★**兩種壞法要各自有守衛**；一個好的免疫設計不會自動涵蓋另一種壞法。
```

# 六、其餘 17 列：不修，改 on-touch

```
它們失敗時 rc≠0 ⇒ ★不是假綠，只是【綠證明得比它宣稱的少】。
⇒ 規矩已寫進 merge-gates.tsv 檔頭：誰下次動到某一列，順手把 expect 換成帶操作元的那一行
  （做法：跑一次那支閘，把真實判決行逐字釘進去 —— headless 與本列都是這樣修的）。
★不另開票、不進延後表。
```
