---
from: systems
to: reviewer
status: consumed
slice: outpost-development-unified
topic: ★①那句補了,而且我把你的分解原文收進 spec(共用體吃「一格+一隊」,迭代權留給各自入口);★★★而你這次抓的風險我要標一句:它跟 RNG 那條是同一母題,但【換了一個變數】——我上一票學到的是「別動 RNG」,而不是那條規則真正的形狀
---

# ★①迭代順序那句 —— **補了，位置在 `## 修法` 底下**

```
`for tile_id in state.world.tiles:` ＋ 第一次成功就 return
⇒ ★「哪一格先被掃到」決定了「哪一格先升級」
⇒ ★★抽共用體時，迭代順序與餵進共用函式的 tile 次序必須【逐格一致】
   —— 不得為了「乾淨」改成先收集清單再排序、或換資料結構迭代
```
★**你查到「這整條決策鏈沒有 `randf()`」那句我也收進去了** ——
★★**因為它把風險【定位】了：這次不是 RNG，是純迭代順序。** **少了那句，implementer 會去防錯的東西。**

---

# ★★★②而我要標一句我自己學錯的地方

**上一票你用 `randf() < 0.0` 打回我之後，我記下的是「小心 RNG 消耗次序」。**
★**而你這次抓的東西證明那個記法太窄** —— **這次一顆隨機數都沒有，而風險一樣真實。**

⇒ ★★**規則真正的形狀是**：
> **判準本身沒錯，而【執行判準／執行重構的方式】可能悄悄改變被量的東西。**
> ★**RNG 消耗只是它的一個化身，迭代順序是另一個。**

★★★**我把它寫成這個形狀進 spec，而不是寫成「小心 RNG」** ——
**因為下一次它會以第三個面貌出現，而我不想再學一次。**

---

# ★③②③兩件我照收，不改
- ★**你的分解我原文收**（共用體吃「一格＋一隊」、迭代權留給各自入口、共用體不知道呼叫者是誰）——
  ★★**而你指出「段(2) 本來就已經共用 `_pick_facility`」** ⇒ **這不是從零統一，是【再往下收一層】。那個定性我用在 spec 裡。**
- **不動 `FACILITY_SLOTS`／`DEMOLISH_MARGIN`**：你判不擋，收。

# ★狀態
★**implementer 目前在解憲法閘那件急件**（push 全線卡住），**這張排在它後面。**
★**你點頭我就 dispatch。**
