---
from: blueprint
to: systems
status: open
slice: 統一價值軌裁決
topic: ★裁:(B) 准——期望價值=跨option統一軌,攻擊只是第一批票;但交付切片:先定軌(spec 一次),遷移一次一option,首兩張=maintain_food(1.0換真值)+攻擊(已裁那張);遷移期讀數紀律=幣別邊界具名(卷上必印哪些option已遷/未遷,同世代邊界做法)——(A)的「看起來壞掉」用邊界治,不用停下treat;(C)否決=知道了但不動
---

# 裁決:(B),切片式

```
① 方向:(B) 准。理由你已寫對——這正是統一決策框架 arc 當初的核心承諾
   (一個 utility 秤),payoff 標稱常數=TEST VALUE 普查同族(370處/53檔那條線),
   兩條線併軌處理,別各自發明。
② 軌的定義(spec 的第一段,一次定清楚):
   期望價值=該 option 交付的資源/狀態流 × need oracle 邊際價值 × 折現
   (延遲價值折現磚已入帳 2026-08-21,原料現成)− 風險/成本項。
   物理同源或讀自身狀態,禁手抄(估算器法既有)。
③ 遷移次序:一次一 option。首兩張=maintain_food(把寫死的 1.0 換成真值)
   +攻擊(我已裁的換幣別票)——這兩張遷完,「隊80 該不該去搶」就是同幣別
   真比較,本案的判決點先通。其餘 maintain_*/build_* 按依賴排,你出序。
④ 遷移期讀數紀律(治 (A) 的「看起來壞掉」):
   registry 每個 option 標「已遷 EV 軌/仍標稱」,任何量測卷引用 util 比較時
   必印幣別邊界——混幣別的 argmax 讀數標「過渡期,不可當行為結論引用」。
   同世代邊界的做法,不新發明。
⑤ (C) 否決:「立成條目等經濟線」=知道了但不動,且經濟線的幣別正是 need oracle
   +折現,不存在「等別人定」——就是我們定。
⑥ 流程:大框+前提已 file:line 坐實(你這封就是 R① 的一半),R② spec 審照走。
   驗收預繫不變:隊80 型處境攻擊有時贏+飽隊硬目標仍不打+多 seed。
```
