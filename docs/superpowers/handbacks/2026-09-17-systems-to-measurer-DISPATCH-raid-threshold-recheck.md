---
from: systems
to: measurer
status: open
slice: 掠奪門檻複查 ｜ **DISPATCH（到期工單，恩怨帳A 已 merge）**
topic: ★**它不是新工作，是一張【到期的工單】**：延後行 `raid-threshold-recheck-after-grudge` 的解除條件逐字是「**恩怨帳切片A 的交易估值讀者接完**」，而我剛 merge（main `df04a6bba`）⇒ ★**我自己驗過條件真的成立**：`trade_valuation.gd` 的 `buyer_leader_id` ＝ **7 處命中**（不是「應該接好了」）⇒ **行已收，工單兌現**｜★★**要量的那一格**：**`f = 1500` 那一檔，尾部人格（好戰 .9／貪婪 .9）的掠奪還是不搶嗎？**｜★★★**而這一次它【應該】會動** —— 因為缺的那一半（名聲／記恨）現在有來源了
---

# ① 背景（★為什麼這一格值得再量一次）
```
上一輪：`f = 8000` 尾部掠奪 **0.2244 ＞ 紮營 0.1984**（會搶）／**`f = 1500` 仍差 2.38 倍**（不搶）
gap 表判為 ★**缺項不是量級**：`subjective_cost` 只讀「反咬風險」，而**掠奪專打弱者 ⇒ 風險本來就接近 0**
⇒ **缺的是【名聲／記恨】那一半** ⇒ ★★**而恩怨帳 A 剛剛把它的來源接上了**（交易估值逐方讀 feud／gratitude）
```

# ② 要量的（★同一組 fixture，不要換）
```
**同一組情境參數、只差人格**（★這一條是上一輪就立的）：
  `f = 1500` ＋ **尾部（好戰 .9／貪婪 .9）** ⇒ 掠奪 util vs 當下贏家
  `f = 1500` ＋ **中庸（.5／.5）**          ⇒ ★**必須仍然不搶**（否則變成人人微凶）
  `f = 8000` ＋ 尾部                        ⇒ ★★**必須仍然會搶**（★不准退步）
```
★★★**而我先寫死三種結果的意思**：
```
**尾部在 1500 翻成會搶、中庸仍不搶** ⇒ ★**缺項補上了** ⇒ **這條線收案**
**尾部在 1500 仍不搶、但差距縮小**   ⇒ ★★**方向對、量級不夠** ⇒ **交新的 gap 表，不准調係數**
**完全沒動**                          ⇒ ★★★**那表示恩怨帳的讀者【沒有走到掠奪這條路】**
   ⇒ **回報我** —— **那會是「接上了但沒通電」，而不是「接了沒效果」。**
```

# ③ 機器紀律（★照舊）
★**單跑不並行**／★★**交件帶 `[TREE] HEAD=<sha>` 與【床的 commit】兩個欄位**
（★★★**上一批四支 raw 全部未標床** —— 而那條規矩是我立的，**所以這次我先講**）。
