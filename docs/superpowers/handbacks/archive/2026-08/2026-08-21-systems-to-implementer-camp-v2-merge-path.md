---
from: systems
to: implementer
status: consumed
slice: camp-access
topic: ★merge 准(headless 先清淨,我的條件一條沒鬆) — 但這不是放寬,是【分帳】兩道閘;#1 不豁免只是改掛真兇;你下一張票 = A1 建設族(你自己量出來的 8→1)
---

# blueprint 裁定：**merge 准**，但先把 headless 清乾淨

★**先講清楚它不是「門檻被放寬了」** —— 我沒有替你放寬，blueprint 也沒有。
它是**分帳**：**slice 交付閘** 與 **世界驗收尺** 是**兩道閘**（已寫進 `05_acceptance.md`）。

| 閘 | 判什麼 | 你這刀 |
|---|---|---|
| **slice 交付閘** | **這刀的本分做到沒**（估值層四端同秤） | ✅ **驗實** ⇒ 交付准 |
| **世界驗收尺** | 世界指標到沒 | ❌ #1 仍 0 ⇒ ★**不豁免、不消失**，轉記 completion checklist **未勾項，紅著** |

**世界尺原話一字未改**（滿池餓死清零 ＋ 蓋丟率崩 ＋ `L0→L1 > 0`，**三綠才算世界層過**）。
變的只有 **owner** —— 掛到真兇身上，不再要你這刀為別片的斷點負責。

## merge 路徑（**headless 必須先清淨，這條我沒鬆**）
1. **覓食三條** ⇒ **合法改測**，照裁定：新斷言 **(a)** `food_days` 1.0 vs 30.0 兩次 eval **必須相等**（零魔數、比 `==1.0` 強）
   **(b)** 新增「自家營地 tile vs 荒地 tile 必須不同」。
2. **投靠四條** ⇒ **先跑那輪 tap 量 `join_host_flow`**。若 ≈0 ⇒ **修 fixture（給 host 真的放食物）**，
   ★**不准動斷言想證的那件事**。若 host 有飯而紮營仍贏 ⇒ **那是真結論，照實報，不准調參**。
3. **`承諾(掠奪)`** ⇒ 標 `known-blocked-by: 承諾泛化磚`，不動。
4. 清完 ⇒ det×3 ＋ 憲法 ＋ ★**`seam-gate` 現在是 HARD 了**（`camp-access` 目前缺 QA-verdict ⇒ 會擋，
   走完 QA 才過；逃生門 `SEAM_MODE=soft` 只給診斷用，**不准拿來繞 merge**）。

## ★你的下一張票：**A1 建設族**（blueprint 直接指定）
**`8 → 1` 就是「手不聽腦第 4 型」本尊**：**建設 argmax 贏了、`try_set` noop**。
它**早就列在清單 A1／B6**，但**一直沒有硬數字** —— ★**你這輪量出來的 `8 → 1` 就是那個數字**。

做法照家族既有方法：★**結構列舉 drop 點，不要逐隻抓**
（列出「argmax 贏 → 真正 dispatch」之間**所有**可能的 drop 點，看**分佈**；
逐隻追會漏掉沒想到的那一種 —— 這族已經栽過三次）。

`1 → 0`（工期端，與 75% 棄置同源）**另開 camp-access 工期票**，**不併進 A1 票**。

## 記一筆你這輪的價值
`camp.won_argmax` 12 → 69、`camp.lost_to` 四分天下、零採集把折現磚單獨版的**反向 1244 壓回 966** ——
**這是「磚只鋪一端會製造假象」最直接的反證**。
而 #1 那個 0：**二值門檻的使命（禁旋鈕假造）已經達成** ——
它沒被任何調參糊過去，**而是把一個無解釋的 0 逼成有座標的斷點鏈**。**那是好結果，不是失敗。**
