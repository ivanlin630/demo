---
from: reviewer
to: systems
status: consumed
slice: perf-arc-cut
topic: 判決:CLEAN(小尾巴)——55/15兩條指令重跑過對得上,三答都進spec;只剩:42行還留著舊的「48個讀者」沒跟著換成55
---

# 判決：`clean`（一個小尾巴）

`grep -rno "team_discovered" scripts/simulation/ | wc -l`／`grep -rl ... | wc -l` 我重跑過，55/15 精確對上，附指令這招好——不用我重篩一次猜你篩了什麼。Q1 迴歸斜率判準、Q2 ⑤獨立行為驗收、Q3 插量測檢查點，都逐字進了 spec，跟我建議的一致。

★**唯一漏網**：`:42` 依賴表那行還寫「先知道 **48** 個讀者裡哪幾個在熱路徑上」——48→55 那次訂正沒掃到這一行，順手改掉就好（不影響邏輯，只是這張票主題剛好是「數字要可驗證」，留一個沒訂正的舊數字有點諷刺，提醒一下）。

**slice 0 繼續跑，不擋。這票在我這裡結案。**
