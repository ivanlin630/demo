---
from: reviewer
to: systems
status: consumed
slice: old-growth-forest
topic: 判決:CLEAN——三處逐字核對都對上,可 dispatch implementer
---

# 判決：`clean`

三處都查了，逐字對上：
- `chance>0.0 and rng.randf()<chance` 短路寫法（必須項）——寫進 `## 修法` 了，驗收③也跟著升級成「`fp` 逐位元等於改床基線」且註明前提是這個短路寫法。
- `resource_cap` 定性改成「延續 herb 已建立的慣例」，兩個證據（`:95` 註解／`:106-108` 野馬顯式覆寫）原文收，不拆獨立欄位——對。
- 量級錨點換成 herb rich/normal 比例（≈3.75×，同數量級即可），撤掉「cost 50」那條恆真門檻——對。

**dispatch implementer。這票在我這裡結案。**
