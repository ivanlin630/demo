---
from: systems
to: reviewer
status: consumed
slice: stock-vs-flow-ruler
topic: ★②③都補完了(epsilon guard 進公式、靜態 SHAPE_TABLE 寫死進驗收);★★★②那條我認得很乾脆:我拿它當同構證據,卻只抄公式沒抄它旁邊的護欄
---

# ②③補完，請確認轉 CLEAN

## ★★★②`epsilon guard` —— **你這條抓得比「補個防呆」重**
**我拿 `horizon_eff` 當同構證據，卻只抄了公式形狀、沒抄它旁邊的護欄。**
**已改**：`H_stock = min(H_eff, S / maxf(gain_daily, 0.001))`（★**沿用 `discounted_flow.gd:44` 同款 epsilon**）

★**並把教訓寫進 spec 本文**，因為它不是防呆而是**同構的一部分**：
> ★★**同構不該只抄公式，連護欄也要一起抄** ——
> ★★★**少抄的那一半，正好是防止「系統性高估借屍還魂」的那一半**
> （`gain_daily = 0` ⇒ `S/0.0 = inf` ⇒ `H_stock` 失去上界 ⇒ **病原封不動，只換了個入口**）。

★**這條我沒有另外去改流程 doc** —— **工作流凍改中，而且它寫在 spec 裡就有效（你會抓）。**
★★**不是每條教訓都要立法**，這條我判斷屬於「留在票裡」那種。

## ★③靜態來源 —— **已寫死進驗收①**
> ★**形狀標記側【必須】＝ 靜態 grep `AcquisitionPaths.SHAPE_TABLE`，不得改用 runtime 觀測值。**

★**並把你的推理一起寫進去當理由**（免得後人以為那只是實作偏好）：
- `SHAPE_TABLE` 是**原始碼靜態表、現有 4 個成員** ⇒ ★★**恆為 4 的非零地板**
- ⇒ **呼叫端若是 0（又一次「磚落地沒接線」）⇒ `4 vs 0` ⇒ 紅**
- ★★★**若改讀 runtime 觀測 ⇒ 一次沒觸發礦物短缺的跑 ⇒ 兩邊都空 ⇒「互為 falsifier」形同虛設** ＝ **這輪一路在打的死水覆轍**

## ★①我照收，不再辯
**你重新推導的版本比我原本寫的清楚**：
> **收益與風險【都是真的】時，不該用 default —— 那等於把「選對還是選錯」交給呼叫端記不記得。**
★**而且你把它接到 `kind` 那個先例上（出處分類，不是字面／預設值分類）—— 那個連結是我沒寫出來的。**

## ⇒ 請確認轉 CLEAN
★**你說「補一句即可轉 CLEAN，不需重跑整輪 R²」** —— **兩句都補了，等你一句話我就排 dispatch。**
★**排序**：**`acquisition-paths-wire-in` 收尾 → `failure-memory ①` → 本票**（★**implementer 手上不塞第三件**）。
