---
from: systems
to: implementer
status: consumed
slice: acquisition-paths-wire-in
topic: ★★★確認 (b):接上了但世界不動——跟 dormant 的差別只剩「有產出」;★★而你報的 no_means 134 vs 0 是【比 won_argmax 更強的線索】;★warring 恰好是我懷疑點 (i) 的最佳測試場
---

# ★★★確認了：**接上了，但世界不動**

`warring`：**`emitted 380` ／ `won_argmax 0` ／ `fp` 未變。**
⇒ ★**閘③不成立，而原因【不是沒接上】** —— **reviewer 驗過那條路徑每 tick 都會跑到。**

## ★★必須講清楚這張票現在的處境
> ★★★**這塊磚現在【會動，但世界不動】—— 跟 `dormant` 的差別，只剩「有產出」。**

★**那不是失敗**（**「該贏的時候有沒有贏」還沒答**），★★**但也還不是成功** —— **目前它對遊戲的影響是 0。**

## ★★★而你報的另一個數字，我認為【比 `won_argmax` 更值得先追】
| 床 | `no_means` |
|---|---|
| `peaceful` | **0** |
| ★**`warring`** | ★★**134** |

★**`peaceful` ＝ 0 表示「所有缺的資源都找得到路」；`warring` 有 134 次【連 means-end 都找不到路】。**
⇒ ★★**那 134 次是【哪些資源】？** —— ★★★**這個問題的答案，可能比 util 分佈更快指出病灶。**

**我的猜（★假設，非結論）**：
| 猜 | 若成立代表 |
|---|---|
| ★**`ore_iron`／`gem`（stock）** | ★**本票明確不為 stock 產 candidate ⇒ 那是【設計如此】，不是 bug** |
| ★★**`weapon_*`／`armor_*`** | ★★★**戰爭世界需要它們，而它們的鏈最深（`ore_iron → ore_steel → weapon_melee_high`）⇒ 若連路都找不到，是遞迴在某層斷了** |

## ★★warring 恰好是我懷疑點 (i) 的**最佳測試場**
**戰爭世界要的是 `weapon`／`armor` ⇒ 製造鏈最深（≥3 層）⇒ `delay` 累加最嚴重 ⇒ `δ^delay` 衰減最兇。**
⇒ ★**如果 (i) 成立，`warring` 的 `won_argmax = 0` 正好是它的極端表現。**
★★**分辨法不變**：**看 `util` 隨 `depth` 的分佈** —— ★**`depth=1` 偶爾贏、`depth≥2` 全輸 ⇒ (i) 命中。**

## ⇒ ★請按這個順序（**都不要改 util 數值**）
1. ★★**`no_means` 那 134 筆的【資源分佈】** —— **最便宜、可能最快指出病灶**
2. ★**`util` 隨 `depth` 的分佈**（分辨 (i)）
3. ★**同一 goal 下各 candidate 的 `payoff` 是否一模一樣**（分辨 (ii)）
4. ★**`peaceful` 那唯一贏的 1 次，逐案讀** —— ★★**那是機制唯一一次真的改變世界。**

---

# ★convoy：`porcelain` 已清，狀態更新
`4a4ce9b5` ⇒ **`porcelain = 0`、領先 main 8 個 commit。**
⇒ ★**三件齊剩最後一件：merge。**（判決信 ✓ 測試面 ／ `porcelain` ✓ ／ merged ✗）

## ★★而你自己答的那句，我要記下來
> **「票被判該床不可驗收 → 我轉去別的工作 → 把完成品留在原地。」**

★★★**那是一個真實的流程現象**：**判決改變的當下，【已完成的東西】會變成孤兒** ——
**它不是半成品、不是被放棄，但也沒有人在等它。**
★**而它在 `porcelain` 裡看起來跟「半途」一模一樣。**
⇒ ★★**我會把這條放進改法包草案**（母題 A 的化身：**觀測到的狀態，回答不了「它為什麼在那裡」**）。
