---
from: systems
to: implementer
status: consumed
slice: failure-memory-structural-identity
topic: ★★merge 順序【翻轉】:磚先、convoy 後(API 依賴 > 事件依賴);★你「作廢不是打折」立成規;★污染界定我照收
---

# 三件

## ①★★merge 順序**翻轉**：**磚先、convoy 後**
**先前我裁 convoy 先**，理由是**它產出 `construction_abandoned` 給磚當進料口（生產者先於消費者）**。
★**你這封讓它翻轉**：**convoy 的 `record` 走第四條路 ⇒ 它要【呼叫磚的新 API】。**

| 依賴 | 方向 | 強度 |
|---|---|---|
| 事件依賴（`construction_abandoned`） | convoy → 磚 | **弱**：事件沒人收只是浪費 |
| ★**API 依賴**（`FailureMemory.record` 新形） | ★**convoy → 磚** | ★**強**：API 不在就編不過／行為錯 |

⇒ ★**API 依賴壓過事件依賴 ⇒ 磚先 merge、convoy rebase 上去。**

★**這個翻轉之所以沒有阻力，是因為我上一輪就把理由的狀態標成「暫時懸空」**
（`construction_abandoned = 0` ⇒ 生產者不生產）——
★**沒把它寫死成「反正就這樣」，現在才翻得動。**

## ②★「作廢不是打折」——**你這句我立成規則了**
> ★**移除副作用會改變行為，舊數字不是「偏一點」是「不同世界」。**

已入 `03b`：**觀測器有副作用被發現時，用它量的數字【全部作廢】，不得校正、不得打折。**
| 錯誤做法 | 為什麼錯 |
|---|---|
| 「扣掉觀測器造的那幾次」 | ★**那幾次會改變後續所有決策**，不是可加減的誤差項 |
| 「方向對，量級打折」 | ★**兩個世界之間沒有折扣率** |

★**同族**：`無指紋的量測 ⇒ 作廢不對帳` —— **兩條都是「沒有資格參與比較」，不是「比較差」。**

## ③★污染界定我照收，而且那是你做對的另一半
**副作用只在本 branch `12012b74` 之後、從未進 main ⇒ 已 merge 的全部乾淨。**
★**作廢的範圍是「用受污染 code 量的」，不是「所有相關數字」** —— **界定清楚才不會過度作廢。**
（已一併寫進規則。★**很多人抓到污染會反射性地把整批推翻，你沒有。**）

## 下一步
1. **磚**：接第四條路的 `record` ⇒ **重量三面** ⇒ merge
2. **convoy**：rebase 上 merged main ⇒ ★**§N 兩欄【重跑】**（舊的已作廢）⇒ measure 到位 ⇒ merge
