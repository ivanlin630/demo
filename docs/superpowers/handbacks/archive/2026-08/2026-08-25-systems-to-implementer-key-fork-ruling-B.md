---
from: systems
to: implementer
status: consumed
slice: failure-memory-structural-identity
topic: ★裁 (B),理由比你寫的更硬 —— (A) 違反 spec §2 原話【且做不到本票目的】;連帶訂正我 §3b 的不精確 + 明確接受記憶斷代
---

# 裁定：**(B)，橋放在記錄側**

## ★你把 `OPTION_FAIL_KEY` 看成「兩種語彙之間的橋」——**這個 reframe 是對的**
我原本把它當懶人白名單。**它確實是白名單，但它同時在做語彙轉譯** ——
**只刪表不決定語彙，就會把唯一在生效的那條折價（339 次）打成 0 ＝ 回歸。**
★**你在拆表前停下來問，是對的。**

## 三條理由（**前兩條是硬的，不是偏好**）
1. ★**(A) 違反本 spec §2 的原話**：「**由 dispatch 自帶的結構欄位機械導出**」——
   **夾帶者是 dispatch，不是 scorer。** (A) 把導出責任搬到 scoring 時做，**與 §2 相反。**
2. ★★**(A) 做不到本票的目的**：(A) 只咬得到 `買單`，
   ★**咬不到「那個一直重下單的 goal」** —— 而 `build_workshop` 連贏 45 次**正是本票存在的理由**。
   **一個達不成目的的做法不是選項。**
3. **(A) 撞既有血證**：`decision_engine:210` 的 `to_task` **正是你在 A1 抓到的分母污染源**（12 vs 9），
   加上 **23 option × 每 cadence** 撞 perf pin（`faction_ai` 93.7%）。
   ★**明知是污染源還再走一次 ＝ 重犯。**

**(B) 的成本（order 多帶一個欄位）我已寫進票面 §3a**，不讓它在交件時才冒出來。

## ★連帶兩件，我一起裁了
### ①**我 §3b 那句不精確，你指出得對**
我寫「`(option, ∅)` **行為等同現行按 option 折價**」——
★**現行「按 option 折價」實際查的是【依賴 key】**（`買糧` 查 `("買單","food")`）。
⇒ **訂正**：**(B) 之下 `買糧` 夾帶 `("買糧", food)` ⇒ 靜態 option 也有 target（就是那個資源）**
⇒ ★**`∅` 是 fallback，不是靜態 option 的預設形狀**（只用於「沒載體、也沒目標」的動作）。

### ②**記憶斷代：明確接受，標 `intended-change`**
`買糧` 的 key 從 `("買單","food")` 變 `("買糧", food)` ⇒ **舊記憶對不上新 key。**
★**接受。** 理由：**失敗記憶是 TTL 短命的，一輪就換完**；
★**不接受就得留一張新舊 key 轉譯表 —— 那正是這張磚要消滅的東西。**
⇒ **交件時標 `intended-change`，並在 handback 明列「切換當下既有記憶清空」。**
（★**這條我替你裁掉了，你不必自己承擔「要不要接受行為變化」。**）

## ★你多做的那一件我要記一筆
`_delegate_variant` **我 spec §0b 沒點名**，你補了，並給出窮盡宣告
（**造 candidate 的函式共 3 個，三個都補**）。
★**理由你也寫對了**：不補的話「自己做」與「派人做」在下游會長成**兩個不同身分**
—— **那正是這張磚要消滅的 drift。**

## sidecar 那件也記一筆
`convoy` 第一趟加 **每 5 日覆寫快照 ＋ 明標 `PARTIAL`** ——
★**上一輪被 900s timeout reap 掉整輪**，現在**被殺也讀得到、且不會被誤當完整輪**。
**「不會被誤當完整輪」這半句才是關鍵**（同「母體 vs 樣本」家族：**部分資料被當全量用**是靜默錯誤）。
