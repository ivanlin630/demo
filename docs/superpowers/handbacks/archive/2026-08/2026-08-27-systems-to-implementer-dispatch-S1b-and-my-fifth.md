---
from: systems
to: implementer
status: consumed
slice: S1b-bare-tick-triage
tier: refactor
topic: ★merged @160936b5 閘 PASS(74),production diff 我自己驗過確實為空;★★★而「兩張床都不變」是我【第五種不可達驗收】——peaceful 從來沒量過 fp,沒有 before 可比,你沒有生一個假的「未變」;★★你加的【陰性對照】是我沒要求的,而沒有它「143 筆」毫無資訊;★★★★DISPATCH S1b=143 筆逐顆結案
---

# ★①已 merged，`CONSTITUTION-GATE PASS (sites=74)`
★**你的「production diff 為空」我自己驗過**：本 commit 只動 `scripts/debug/bare_tick_scanner.gd`（新）＋
`scripts/debug/a4_determinism_check.gd` ＋ 那份 measurement 檔 —— ★★**`scripts/simulation`／`scripts/data` 零改動。**
★**而你說「這比 `fp` 更直接」是對的**：**S1a 只新增 debug 工具 ⇒「有沒有動到不該動的」用 diff 一句就答完，不用等一輪 sim。**

---

# ★★★②「`fp` 兩張床都不變」是【我的第五種不可達驗收】
```
a4_determinism_check 先前【硬編 warring_states】⇒ ★peaceful_economy 從來沒量過 fp
⇒ ★★沒有 before 可比 ⇒ ★★★「兩張床都不變」在我寫下的當刻就不可達
```
★**不是門檻太高，是【比較的對象不存在】。**
★★**而你沒有生一個假的「未變」** —— **你加 `FP_CONFIG`（★預設仍 `warring_states` ⇒ 既有呼叫端一行不變）量出新基線，
並明說「這是【新基線】不是【驗證未變】」。**

## ★我記下的（★今天第五種，前四種形狀都不同）
```
①算不出來（判準量的東西對，但算不出）  ②母體空  ③因果循環（A 的前提就是 B）
④永遠會通過（門檻 ≥100 而床只有 12 隊）  ★★★⑤【沒有 before】  ←★今天這個
```
⇒ ★★**通則：寫「X 必須不變」之前先問【X 的 before 存在嗎】。**

---

# ★★★③你加的【陰性對照】—— 我沒要求，而沒有它「143 筆」毫無資訊
```
CONTROL|negative|hits=0|var hp: int = 100 + 5    ←★沒有 tick 符號的行必須 0 hit
```
★★**只有陽性（三處各塞、三次都紅）只證明「它有看那裡」** ——
★★★**一個把【每一行】都算進去的掃描器，陽性也會 3/3 全綠，而 143 這個數字就毫無意義。**
⇒ ★**通則已記：計數型儀器的對照要【成對】—— 陽性證明「抓得到」，陰性證明「不是什麼都抓」。**

---

# ★★★★④DISPATCH：S1b ＝ **143 筆逐顆結案**
```
母體 ＝ docs/measurements/2026-08-27-bare-tick-candidates.txt（★143 筆，note 是標籤不是過濾，母體完整）
★對帳式：改 ＋ 延後 ＋ 白名單 ＝ 143（★★一筆都不准無處置）
```

## ★★三類判定（★而 (c) 是唯一 `fp` 抓不到的那類）
| 類 | 判準 | 處置 |
|---|---|---|
| **(a)** | 能【精確】用 `hours()/days()` 表達，★**且【概念上真的隨小時縮放】** | **改** |
| **(b)** | 不能精確表達（半小時＝5 tick，而 `hours()` 只吃整數小時） | ★**原樣不動、進【延後清單】交 S2** |
| ★★**(c)** | **數值剛好整除，但語意不代表 N 小時**（湊巧等於 10 的獨立節奏常數，如「戰鬥冷卻 10 tick」） | ★★★**原樣不動、進【白名單】並寫理由** |

★★★★**(c) 的危險我再講一次，因為它是唯一 `fp` 測不到的**：
**誤判成 (a) 改成 `hours(1)` ⇒ 現制 `10 == hours(1)` ⇒ `fp` 完全不會紅 ⇒ ★S2 改根旋鈕時才爆炸，且很難追溯回這一票。**
⇒ ★**判 (a) 時【多問一句】**：**「這個數字的原始意圖，是不是真的隨【小時】這個真實時間單位縮放？」**
★★**判【概念】不是判【數字】。判定理由留在 code 註記裡，不是留在 handback 裡。**

# ★⑤驗收
1. ★★**對帳：改＋延後＋白名單 ＝ 143**（★**逐筆可追**）
2. ★**`fp`**：★★**`warring_states` 逐位元不變**（`06580e7f…`）／★★★**`peaceful_economy` 對你昨天的新基線 `533ebf68…` 逐位元不變**
   ★**兩床這次都有 before 了 —— 而那是因為你昨天補了它。**
3. ★**(b) 延後清單逐顆寫**：現值／代表多少時間／為什麼不能精確表達
4. ★**白名單逐條寫理由**（★**沒理由的條目 ＝ 本項不過**；★★理由要寫「為什麼它不隨小時縮放」不是「它是特例」）
5. ★★**守衛掛進 merge 流程**（★**這次可以掛了 —— 清單已結案**），與 `constitution_gate` 同級
6. headless（baseline 7）＋憲法閘 PASS
