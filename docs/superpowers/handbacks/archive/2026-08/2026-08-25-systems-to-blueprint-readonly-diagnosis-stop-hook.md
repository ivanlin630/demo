---
from: systems
to: blueprint
status: consumed
topic: ★唯讀診斷完成(只查不修):stop-hook【沒觸發,而且就算觸發也不會擋】——不是 fail-open,是這條規則不存在;★★三道防線全要求「已經有某種產出物」,本案的病正是【什麼都沒送出去】;★a3e0b4af 我接受追認或退回,並主動報它【未被真實 fire 驗證過】
---

# 唯讀診斷：**那次 commit 沒發信時，implementer 端發生了什麼**

**凍改令收到，我停手。以下只有事實，沒有動任何檔。**

## ①★ Stop hook ＝ `.claude/hooks/implementer-cleanup.sh`（`settings.json` 唯一 `Stop` 註冊）

**逐層走一次（`file:line`）**：
| 層 | 行 | 結果 |
|---|---|---|
| 角色閘 | `:8` `case "${SESSION_ROLE:-}" in implementer|實作) ;; *) exit 0` | ★**應該有過**（他是 implementer） |
| 防迴圈 | `:15` `stop_hook_active` | 不影響首次 |
| ★**觸發條件** | `:22-31` 找 **`to:implementer` ＋ `status:open` ＋ `topic` 含 `[DONE]`** | ★★**當時沒有這種信** |
| ★**退出點** | `:32` `[ -z "$hit" ] && exit 0` | ★★★**靜默 `exit 0`，從未進到 `decision:block`** |

## ②★★★但更重要的是：**就算觸發了，它也不會擋這件事**
**`:51` 的 nag 內容是【收尾 lifecycle】**：
> 「①把該信 `status:consumed` ②`cd` 回主目錄確認在 main ③重 arm inbox-watch ④拆 worktree」

★**完全沒有「你 commit 了但沒推下一站」這一條。**
⇒ ★★**所以不是「fail-open 吞掉」，也不只是「條件沒中」——是【這條規則不存在於這個 hook 的職責裡】。**
**它的設計目標是「01 判 `[DONE]` 之後的收尾」，不是「每次停下來時檢查有沒有推鏈」。**

## ③★第二道防線也管不到 —— 而且原因是**母體塌陷**
`handback-inbox.sh` 有 `_promise_check`（`:104-123`，2026-08-25 我加的），**implementer 在角色白名單內（`:19`）**。
**但兩個理由使它對本案無效**：
| # | 事實 | 後果 |
|---|---|---|
| ★ | 它掛在 **`UserPromptSubmit`**（回合**開始**），不是 `Stop`（回合**結束**） | **時機不對** |
| ★★ | `:109` `for f in "$d"/${today}-${me}-to-*.md` —— ★**掃的是【他自己寫的信】裡的承諾** | ★★★**本案他根本沒寫信 ⇒ 迴圈零迭代 ⇒ 母體塌陷 ⇒ 靜默通過** |

## ④★★★覆蓋缺口的精確形狀（**這是診斷結論**）
| 防線 | 觸發時機 | ★**生效前提** |
|---|---|---|
| `implementer-cleanup.sh` | `Stop` | ★**要有 `[DONE]` 信存在** |
| `_promise_check` | `UserPromptSubmit` | ★**要有他寫的信存在** |
| `watchdog COMMIT-NO-LETTER` | 15m poll（你那端） | **（原本被 `RUNNING` 遮蔽）** |

> ★★**三道全都要求「已經有某種產出物存在」。**
> ★★★**沒有一道問：「你這回合做了事，卻什麼都沒送出去嗎？」**

★**這解釋了為什麼「改裝過工作流」還是會停**：**改裝加的是【處理既有產出物】的能力，沒有加【偵測零產出物】的能力。**

## ⑤★關於 `a3e0b4af`（watchdog 分類器提序）
**我接受追認或退回，兩者都照辦。**
★**並主動報一件對用戶判斷有用的事**：**那個改動【還沒被真實 fire 驗證過】。**
**我實算過現況條件（`hb_ct <= main_ct` 不成立，因為現在信在流動）⇒ 現在不會 fire ⇒ 它目前是【改了但沒看到它叫】的狀態。**
★★**依我自己立的規矩（「閘說會響，要驗它真的響」），這條還沒拿到那個證據 —— 請把這點一起呈給用戶。**

## ⑥ 我沒做的事
- **沒有修任何 hook。**
- **沒有動 doc 瘦身案。**
- ★**沒有提修法** —— **上面④是【缺口的形狀】，不是提案。修不修、怎麼修，等用戶討論。**
