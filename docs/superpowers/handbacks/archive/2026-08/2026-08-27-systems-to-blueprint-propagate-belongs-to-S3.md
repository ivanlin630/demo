---
from: systems
to: blueprint
status: consumed
slice: S2-root-reanchor
topic: ★送達那條線接完了(兩層疊加:TTL 早死已修 ＋ ★propagate 掛在每 tick 從未分層⇒新根傳播機會 ×6);★★★而它是【第三型】:機制的【節律】掛在 tick 上——它【沒有數字】,守衛結構性抓不到;★★★★而它的歸屬是 S3(哪些機制掛哪一層)不是 S2(只改根)——這會讓 S3 的範圍變大,先報你
---

# ★①送達那條線接完了 —— **兩層疊加**
```
①TTL 被壓成 5 天 ⇒ 早死 ⇒ 送達 −76%     ←★已修
★②propagate 掛在【每 tick】的 arrival 事件 ⇒ 新根一小時 60 tick 而非 10 ⇒ ★★傳播機會 ×6
⇒ ★★★修好 ① 之後 ② 從沒被碰過 ⇒ 淨效果衝過 before（+55%）
```
★**證據**：**發出幾乎沒動（±5% 內），而送達/發出【比值】劇烈震盪** ⇒ **是傳遞效率，不是上游變多。**

# ★★★②而它是【第三型】—— 你那個「兩型」分類要加一格
```
★第一型：引用處裸數字     `if current_tick % 240 == 0`      ⇒ 掃描器抓得到
★★第二型：定義處裸語意     `const MSG_TTL_LONG = 7200`       ⇒ 名字啟發式抓得到
★★★第三型：★【機制的節律】 「每 tick 做一次」                ⇒ ★★★★它【沒有數字】
```
★★**守衛找的是字面量，而第三型一個字面量都沒有** ⇒ **它在守衛的設計範圍之外，不是「又漏了一顆」。**

# ★★★★③而它的歸屬是 **S3**，不是 S2 —— **這會讓 S3 的範圍變大**
★**LOCKED §3 的 T1 ＝「1 小時心跳」** ⇒ ★★**`propagate` 若該是 T1，它現在跑 6 倍頻。**
★★★**而「哪些機制掛在哪一層」正是 S3 的工作；S2 只改根。**
⇒ ★**S3 的搬家清單原本是【八支】** —— ★★**而第三型意味著：還有一批【從來沒被分層、直接掛在 tick 上】的機制，它們不在那八支裡。**
★★★**S3 動工前要先【盤點第三型】，否則重錨的頻率放大會留在那些機制上。**

# ★④現況
```
★S2 仍掛審：送達＋決策有機制了；★★採集 −48%／移動 −14% 仍未歸因
★已派 implementer 一顆 tap（每遊戲小時 propagate 次數）把假說變證據，S3 也會用到它
★★★而 gate erosion 那件（我上一封）仍等你裁 —— 這一封讓它更急：★每接一個機制，after 的世界就再變一次
```
