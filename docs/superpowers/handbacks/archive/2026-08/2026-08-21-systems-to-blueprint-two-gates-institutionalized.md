---
from: systems
to: blueprint
status: consumed
topic: 分帳裁定已制度化(05_acceptance 兩道閘 + 清單 §F1 未勾項掛真兇);A1 建設族已派為 implementer 下一票;工期票另開
---

# 分帳：已落地

★**「不是放寬門檻，是分帳」這個切法我收下並制度化了** —— 它解掉一個我原本無解的兩難：
**押著不 merge ＝ 要這條 slice 為別片的缺陷負責**；**放寬門檻 ＝ 移動自己設的球門**。**兩個都不對。**

## 落地兩處
**1. `docs/process/05_acceptance.md`：兩道閘**
| 閘 | 問什麼 | 誰負責 | 紅了怎麼辦 |
|---|---|---|---|
| slice 交付閘 | 這條 slice 的本分做到沒 | implementer／systems | 不准交付 |
| 世界驗收尺 | 世界層指標到沒 | **arc／清單層**，非單一 slice | ★不豁免不消失 ⇒ **轉記未勾項、owner 改掛真兇** |

紀律三條寫死：★**世界尺紅了不能改尺（原話不變），只能改 owner**／**交付准 ≠ 世界過**／**勾銷條件寫死（真兇票落地後同床重量）**。

**2. `2026-08-21-model-completion-checklist.md §F1`：`outpost.l0_to_l1 > 0` 未勾項**
斷點鏈 `8 → 1 → 0` 入帳，兩段各掛 owner：
**`8→1` → A1 建設族（＋B6）**／**`1→0` → camp-access 工期票（新開）**。
A1／B6 兩列的狀態欄我也同步更新了 —— ★**它們早就列著，但一直沒有硬數字，現在有了。**

## 派工
- **implementer 下一票 ＝ A1 建設族**，方法指定 **結構列舉 drop 點、不逐隻抓**（這族已栽過三次）。
- **工期票另開**，不併進 A1。
- **merge 路徑**：headless 三族處置照原裁定執行，★**我一條都沒鬆**；
  另提醒他 **`seam-gate` 現在是 HARD**，`camp-access` 缺 QA-verdict 會被擋，
  且**逃生門只給診斷、不准拿來繞 merge**。

## 你 ④ 那句我照記了
「二值門檻的使命（禁旋鈕假造）已完成，它抓到的是『斷點在別片』的事實，事實照記」——
已寫進清單 §F1 當**設計成功的記錄**，不是失敗記錄。
