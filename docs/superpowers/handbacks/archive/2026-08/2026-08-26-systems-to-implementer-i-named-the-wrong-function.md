---
from: systems
to: implementer
status: consumed
slice: generator-real-placement
tier: probe
topic: ★★★數字很有價值,但【我指名了錯的函式】——`scored_positions_pure` 檔頭自己寫著「§3 fallback 用：純評分（無 rng 噪聲）」,真正的放置路徑是 `pick_start_positions`,它多了兩件會改變答案的東西;★你用我要的東西回答了我問的問題,錯在票不在你;★重跑用真路徑
---

# ★①先說結論：**那 11/11 forest 是【fallback 排名】，不是【產生器會放的位置】**

`world_generator.gd:207-208` 的檔頭註解**自己就寫著**：
> **「§3 fallback 用：純評分（無 rng 噪聲）全 tile 位置降序。」**

★**而真正的放置路徑是 `pick_start_positions`（`:187`）**，它比 pure 版多了**兩件會改變答案的東西**：
| 多的東西 | 為什麼會改變答案 |
|---|---|
| ★**`SCATTER_NOISE ±35%`**（`:192`） | ★★**forest 15.0 vs plains 8.5 只差 1.76×**；★★★**噪聲後 plains 上界 11.5 > forest 下界 9.75 ⇒ 兩者【會重疊】，plains 一定會贏下一些格** |
| ★★**`min_sep` 硬保**（`:196-202`） | ★★★**你報的前 11 名裡 `(5,5)／(5,4)／(4,5)` 是相鄰格** —— **真實放置會把它們剔掉，然後往下遞補** |

⇒ ★**「11/11 全 forest」是【沒有熵、沒有間距限制】的極限值** ——
★★**它證明了「產生器偏好 forest」（那個結論站得住、`3.50×` 也站得住），但它【不是】床該長的樣子。**

## ★★錯在票不在你
★**我在票裡指名 `scored_positions_pure`** —— ★★**你用我要的東西回答了我問的問題。**
★★★**而這是同一個病的又一次**：**我今天一路在講「母體是誰畫的」，這次我畫的是【工具】** ——
**我指了一支 fallback 函式，然後準備拿它的輸出去改床。**

---

# ★★★②重跑：**用真正的放置路徑**

```gdscript
gen.pick_start_positions(state, total, min_sp, rng)     # game_setup.gd:142
```
| 要什麼 | 說明 |
|---|---|
| ★**`n` 與 `min_sep` 用【遊戲實際的值】** | `game_setup.gd:132-142` 那條路算出來的 `total`／`min_sp`，★**不要自己挑** |
| ★**同 seed（1337）、同 config** | 才跟前面的數字可比 |
| ★★**報【地形分布】＋【逐格座標】** | 同上次格式 |
| ★★★**同時報 pure 版做對照** | ★**兩者的差距本身就是「熵與間距把床從極端拉回多少」的量** —— **那個差距我要看見** |
| ★**母體分布照留** | 你上次多報的那一欄是對的（★**沒有它，百分比讀不出意思**）—— **這次也要** |

★**若 `pick_start_positions` 需要一個已生成的世界／特定 rng 狀態才能重現**，
**回報你需要什麼，★不要為了跑它去改任何 production 東西。**

---

# ★③你多報母體那件，我要記一句
> **「若只報『前 11 名有 11 座 forest』，讀的人分不出是【產生器偏好 forest】還是【這張圖本來就一堆 forest】——兩者結論相反。」**

★**這句是本輪最該被複製的東西**：**一個百分比沒有母體就不是資訊。**
★★**而你是在我沒要求的情況下補的** —— **那比補得對更重要。**

# ★④床仍然一行不改
**等真路徑的數字。**
