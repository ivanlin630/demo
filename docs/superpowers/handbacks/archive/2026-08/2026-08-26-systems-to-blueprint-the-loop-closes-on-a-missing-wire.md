---
from: systems
to: blueprint
status: consumed
topic: ★★★這條 arc 追到底了,而終點是一個【接線缺口】不是一個決策:據點升級只掛在 faction 路徑上,而這張床 12 隊全是獨立隊(faction_id=-1)⇒ 那個迴圈跑零次 ⇒ NPC 沒有任何一條路 L1→L2;★要你裁一件 WHAT:獨立隊該不該有升級路徑
---

# ★★★①閉環完整了

```
蓋不了設施
  ← slot 滿（★L1 civilian 只有 2 格，FACILITY_SLOTS = [2,3,5]）
    ← 據點永遠不升級（★30 天後仍 L1×11）
      ← ★★★升級只掛在 `for f in factions` 迴圈裡，而這張床 12 隊全是 `faction_id = -1`、config 無 `factions` 欄
        ⇒ 迴圈跑零次 ⇒ `_evaluate_infrastructure` entry = 0
```
★**窮盡搜索坐實四條升級路徑各自唯一 caller**（`_dispatch_upgrader` / `_evaluate_infrastructure` /
`start_upgrade_level`＝**玩家路徑** / `_subteam_upgrade_level`＝只由前者派）
⇒ ★★**NPC 在這張床上【沒有任何一條路】把據點從 L1 升到 L2。**

★★★**而這跟「NPC 不想升級」是完全不同的一件事** —— **前者連秤都沒上，後者是秤了以後輸。**

---

# ★★②所以要你裁的 WHAT 只有一句

> ★**獨立隊（`faction_id = -1`）該不該有據點升級路徑？**

★**我的判斷（供你參考，不是裁決）**：**應該有** ——
**這張床 12 隊全是獨立隊，而整條經濟鏈（設施→產出→材料→建設）都壓在「據點會長大」這個假設上。**
★★**而它跟【統一矩陣】那條老線是同一個形狀**：**一個決策只長在某一條路徑上，其他路徑的人根本不在秤上。**
★★★**若你同意，那就不是「加一個功能」，是【把既有的決策接到所有該有它的人身上】** —— **那是 de-patch，不是新機制。**

## ★若你要更保守的選項
| | |
|---|---|
| (a) ★**接線**：讓獨立隊也走 `_evaluate_infrastructure`（★**決策邏輯一行不改**） | **我建議這個** |
| (b) 改床：給這張床加 faction | ★**那是繞過問題** —— **獨立隊在真實世界裡也存在，而它們一樣需要長大** |
| (c) 判定「獨立隊本來就不該升級」 | ★★**若是這樣，那 `FACILITY_SLOTS` 的 L2／L3 對獨立隊就是死規格**，**而整條材料經濟要重新想** |

---

# ★★★③而我要報告一件關於【這條 arc 怎麼走的】的事

**這條 arc 的可見牆，每一層都是真的，而每一層都不是根**：
```
材料不夠（163）→ 富點看不見（→64）→ slot 滿（180）→ 據點不升級 → ★升級沒接線
```
★**前三層每一層都能量到、都能修、修了數字都真的動** —— ★★**而它們沒有一層是根。**
★★★**根在第四層，而它是一個【零】：一個從來沒有被呼叫的函式。**

⇒ ★**這正是 `feedback_structural_audit_complement` 那條**：**measure-first 只抓得到近端。**
★★**我們是靠【每一格都問「它的分母是什麼」】才一路走到零那一格的** ——
**而最後那一格的分母（「這個函式被走到幾次」）是 implementer 自己加的，不是我要求的。**
★★★**沒有它，那八個歸宿會印成一片 0，而「從沒被呼叫」與「呼叫了但都在第一道閘掉頭」長得一模一樣。**

# ④現況
**所有儀器已 merge 進 `main`**（`df7d364c`）。**implementer 手上乾淨，等你這一裁。**
★**material-bound 那條（Team6／9、`afford.short.wall.material = 64`）我壓著沒開** ——
**它是另一條線，而且它只影響 2 支隊；★slot／升級那條影響 8 支。**
