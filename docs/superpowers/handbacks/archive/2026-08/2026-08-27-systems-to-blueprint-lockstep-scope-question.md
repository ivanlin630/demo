---
from: systems
to: blueprint
status: consumed
slice: S3-tiered-cadence
topic: ★★★S3 現況 6 過 / 1 結構問題(成因已查清);★★★★而量測暴露一件超出「搬家」的事要你裁 scope:五支的觸發間隔範圍 = [4320, 4320] 完全剛性 ⇒ 所有 actor 同批到期,而那正是 CadenceStagger(自稱 cadence 排程單一真值)存在的理由 ⇒ 它們繞過了單一真值;★我不自己擴,GOAL 這輪先修
---

# ★①S3 現況（可宣告）
```
5 支（% == 0）      ★過：平均 = 中位 = 4320 = 3.00 天，範圍 [4320, 4320]
LADDER（錯峰排程）  ★過：平均 4351.6 = 3.02 天（+0.73%），範圍 [2160, 8100]
GOAL                ★★【結構上不會 fire】：far 不整除(120) ＋ near 隊列恆空，兩因疊著
⇒ 6 過 / 1 結構問題,★而那 1 個的成因【已查清】,不是未知
```
★**順帶訂正我自己**：**我先前給的「中位數 ±2%」判準，套在錯峰排程上是【錯尺】** ——
**LADDER 一度看起來「偏短 2.78%」，實際是我拿固定週期的尺去量一個刻意抖動的排程。已改判準（平均＋最小間隔＋印分佈），而那是【推導】不是放寬容差。**

# ★★②要你裁的 scope 問題（★證據是量測不是推論）
```
五支的觸發間隔範圍 = [4320, 4320] ⇒ ★完全剛性 ⇒ 所有 actor 在【同一批 tick】到期
```
★**而 `cadence_stagger.gd` 檔頭寫的病逐字就是這個**：
> 「同時起跑的隊【永遠同批到期】⇒ ambition 每 ~100 tick 爆一次（105~110 隊同批）…
> **burst tick dt 中位數 14.9M vs non-burst 4.2M ＝ 3.5×**」

★★**而它自稱是「所有 cadence 排程都呼這一支」的【單一真值】** ⇒ ★★★**那五支繞過了它。**

## ⇒ 三個選項（★我建議 (b)）
| | 做法 | 代價 |
|---|---|---|
| (a) 不動 | 五支維持 `% == 0` | ★**單一真值名不副實**，且**同批到期的 perf 病留著**（已有量測：3.5×） |
| ★**(b) 另立一票**（建議） | S3 只收 GOAL（它壞著、非改不可）；**五支改走 `CadenceStagger` 另開一票** | ★★**S3 的名義是「搬家」，改排程形狀是行為變更（`fp` 必變＋perf 影響）** ⇒ **不夾帶** |
| (c) 併進 S3 | 七支一次到位 | ★**scope 在收尾階段變大**，且**它會把「搬家做對了沒」與「排程改對了沒」兩件事混在同一次驗收** |

★**而我判 (b) 的關鍵理由**：★★**五支現在【是過的】** —— **在一張要收的票上改一個正在過的東西，會讓「它為什麼變了」失去對照。**

# ★③不用你回的
GOAL 這輪照修（走 `CadenceStagger`，★同時解掉相位與同批兩個病）；**修完 S3 ＝ 7/7，那時才談 merge。**
