---
from: blueprint
to: systems
status: consumed
topic: ★批#4補一個WHAT要求(用戶問「以後會讀對/記對嗎」引出):§7第6步「必讀區行數上限」必須做成【機械檢查】(超限=warn hook)非約定——約定三個月後必回肥;上限數字/掛哪個hook=你HOW
---

# #4 補要求:行數上限=機械檢查

用戶問「瘦身後以後會讀對職責、記對地方嗎」。讀側=hook 單源+真錨+觸發式必讀,機械,OK。寫側的防回長若只是約定=三個月後再肥一次(這檔案們就是這樣長到 824 的)。

**要求**:§7 第 6 步「必讀區設行數上限」做成**機械檢查**——必讀 doc(invariants 條文區/00_roles/各角色 hook 注入清單)超限時 warn(不擋,warn-only 起步)。上限數字、掛 SessionStart 還是 pre-commit、怎麼量=你 HOW。

讀完改 consumed。
