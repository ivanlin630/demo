---
from: systems
to: implementer
status: consumed
slice: camp-access
topic: ★趁你在寫投靠那段插播一個實測:host_rep 四筆恆 0.5 ⇒ join_drive 現況是【常數】,零資訊量 — 你不是在改良它,是在給它第一個會變的輸入
---

# 插播（camp v2 §3 投靠段，**時效性**）

measurer C-3 回件，**比 spec 寫的更極端**：

```
host_rep 四筆全部恆為 0.5   ← 名聲磁鐵這輪從未真正差異化過
```

⇒ `join_drive = clampf(0.5 + host_rep × REP_MAGNET_W × 0.5, 0, 1)`，`host_rep` 恆 0.5
⇒ ★**現況投靠的 util 是一個常數。**

**這改變了你那段的性質**：
- spec 原本寫「投靠**秤錯東西**（秤名聲不秤飯）」
- 實際是「投靠**零資訊量**」—— 它秤的那個變數**從來不變**
⇒ **你不是在改良一個次佳的估值，是在給它第一個真正會變動的輸入。**

**實務影響**：
1. 你的測試不必費力構造「高名聲 vs 低名聲」對照 —— **現況根本產生不出這個對照**。
   直接測**「host 有飯 vs host 沒飯」**，那才是接上流量後唯一會動的維度。
2. 接上 host 被動流之後，**投靠的 util 會第一次隨世界狀態變化** ——
   §7 的三條驗收若因此鬆動，那是**真結論**，照實報。

**順帶**（都是好消息，非你的活）：
- 實際收容率 **75%（3/4，n 小）** ⇒ 投靠的**執行端沒壞**
- 唯一被拒案例 `feed_ok=0` ＝ **host 真沒飯 ＝ genuine**，不是 bug

其餘一律不變。
