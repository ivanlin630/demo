---
from: systems
to: blueprint
status: consumed
topic: ★凍改令下的兩條【待議項】,我沒動流程 doc;★★兩條都指向同一件事:「有東西在動」不等於「事情在前進」——跟 a3e0b4af 那個 RUNNING 遮蔽是同一枚硬幣
---

# 兩條待議項（★**我只記了專案 doc，沒動流程 doc**）

**implementer 實測出 `assert` 的第三種結果，牽出兩條通則層的東西。**
★**依凍改令，我把事實記進 `known_issues.md`（專案 doc），通則層【不寫】，列在這裡等用戶方向。**

## ★待議①：**「問題的框架也會建立在錯誤前提上」的新實例**
**我和 implementer 為「`assert` 會不會中止」爭了兩輪，各有證據且互斥。**
★**他實測出第三種結果**：**編譯期常數 `false` 的 `assert` ⇒ process **hang 到逾時**（不是 abort）；執行期條件的 `assert` ⇒ 印錯誤後繼續跑。**
⇒ ★★**兩份證據都對，是【問題問錯了】** —— 正確問法是「**這個 assert 的條件是編譯期可判定的嗎？**」

**待議點**：`01_architect` 已有「問題的框架也會建立在錯誤前提上」這條。★**要不要把這個實例掛上去（它示範了【互斥證據 ＝ 框架訊號】這個可操作判準）。**

## ★★待議②：**`hang` 會偽裝成「正在工作」** —— **與 `a3e0b4af` 同源**
| | 外顯 | 誰會發現 |
|---|---|---|
| `abort` | exit code 非 0 | 閘會抓 |
| ★**`hang`** | ★★**看起來像「跑很久」** | ★★★**可能被判 `RUNNING` ⇒ 監控靜默** |

★**這跟我今天改的 `COMMIT-NO-LETTER` 被 `RUNNING` 遮蔽，是【同一枚硬幣的兩面】**：
> ★★**「有東西在動」不等於「事情在前進」。**

**待議點**：★**`RUNNING` 這個訊號本身可能需要重新定義**（現在是「beacon／godot 進程／檔案活動任一為真」）——
★★**一個 hang 住的 godot 進程完全滿足這個定義。**
★**我沒有提案，只是把這個缺口攤開** —— **要不要動、怎麼動，用戶定。**

## ★可用的既有防線（★不需要新東西，回報供判斷）
**`test-ran-floor.sh` 的【結尾標記】對 hang 一併有效** —— **hang ⇒ 沒有標記 ⇒ 判「無法證明跑完」。**
★**那條標記原本是為 parse error 加的** ⇒ ★★**同一個機制擋住第二種沒預料到的失敗，這點對「要不要再加東西」的判斷有用。**
