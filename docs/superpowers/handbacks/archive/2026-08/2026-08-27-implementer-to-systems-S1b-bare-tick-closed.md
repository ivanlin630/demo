---
from: implementer
to: systems
status: consumed
slice: S1b-bare-tick-closure
tier: infra
topic: ★★★S1b 結案：143 筆逐顆有處置、NEEDS_HUMAN=0，★而【(a) 需要改 ＝ 0】——沒有一顆字面量真的要轉 hours()；★★守衛已掛(判準是「沒人判過的形狀」不是總數，陽性對照跑過)，但 CLAUDE.md 是你的檔 ⇒ 指令附在下面請你貼；★★★我加了票上沒有的第四類 (d) 不是時間量 134 筆,理由與風險寫在②,請你裁要不要保留這個分類軸
---

# ①結論（★先給對帳式）

```
母體 143 ＝ (a) 改 0 ＋ (b) 延後 1 ＋ (c) 白名單 8 ＋ (d) 不是時間量 134
NEEDS_HUMAN = 0                    ★一筆都沒有無處置
```

★★**`(a) = 0`** —— 這是本票最該被看見的一行：**清單裡沒有任何一顆字面量真的需要改成 `hours()`。**
★**所以 S1b 的產出不是「改了 N 顆」，是「證明了不用改，而且證明可以重跑」。**

落地：
- 分類器 `scripts/debug/bare_tick_triage.gd`（★可重跑、可對帳，不是我的判斷快照）
- 清單 `docs/measurements/2026-08-27-bare-tick-triage.txt`
- commit `bd250e50`（已 push）

# ★★②我加了票上沒有的第四類 —— **(d) 不是時間量 = 134**（★這條要你裁）

**票只定義了 (a)/(b)/(c)。而 S1a 掃描器的判準是【同一行有 tick 符號就報】**，
⇒ **同一行的整數常常根本不是 tick**：`bump_sample(..., 200)` 的取樣上限、`% X == 0` 的那個 0、
`get(k, -1)` 的哨兵、`maxi(1, …)` 的下限、`"speed": 1` 的倍率…

★**把它們硬塞進 (a)/(b)/(c) 的後果是【白名單灌水】** ——
**而白名單的全部價值就在「每一條都有人真的想過」。一份 142 條的白名單沒有人會讀。**

★★**風險我自己講**：**(d) 是一個【我發明的逃生口】** ——
**分類器把某顆判成「不是時間量」，那顆就永遠不再被人看到。**
⇒ 我用兩個機械防線壓它，但**要不要保留這個軸是你的裁決**：
1. **形狀認不出來一律 `NEEDS_HUMAN`**（不是 fallback 進 (d)）⇒ ★**漏判表現成「要人看的變多」，不會表現成「悄悄歸類掉」。**
2. **每條 (d) 規則都要寫【為什麼它不是時間量】**，寫在規則表裡，跟清單一起輸出。

★★★**而我第一版跑出 11 筆 `NEEDS_HUMAN`** —— **我逐顆手判，然後把判斷【編回規則表】而不是改成我的印象**，
⇒ **下一個人重跑會得到同一份清單。**

# ★③(b) 延後，逐顆（★票要求：現值／代表多少時間／為什麼不能精確表達）

| 站 | 現值 | 代表多少時間 | ★為什麼不能精確表達 |
|---|---|---|---|
| `scripts/ui/sim_bridge.gd:7` `TICKS_PER_TURN` | `24` | **2.4 小時**（現制 10 tick/小時） | ★`TimeScale.hours()` **只吃整數小時** ⇒ `hours(2)=20`、`hours(3)=30`，**都不是 24** ⇒ ★★**不是忘了改，是【改了就會偏移】** |

⇒ **原樣不動，交 S2**（S2 若給 `hours()` 小數或 `minutes()`，這顆就能收）。

# ★④(c) 白名單 8 筆 —— **理由一律寫「為什麼它不隨小時縮放」，且【寫在 code 註記裡】**

| 站 | 值 | 理由（★不是「它是特例」） |
|---|---|---|
| `world_state.gd:6` `TICKS_PER_DAY` | 240 | ★**根常數本身** —— `hours()/days()` 由它導出，**轉換是循環定義** |
| `world_state.gd:9` `/ 24` | 24 | 曆法結構：**一天幾小時**。★根從 240 改掉時，**這個 24 必須維持 24**，否則「小時」不再是小時 |
| `text_ui_main.gd:756` `% 24` | 24 | 同上（顯示端） |
| `text_ui_main.gd:757` `% 30 + 1` | 30 | 曆法結構：一月幾天 |
| `text_ui_main.gd:759` `% 12` | 12 | 曆法結構：一年幾月 |
| `harvest_system.gd:10` `% 4` | 4 | 曆法結構：一年幾季。★分子已由 `SEASON_LENGTH` 導出（會隨根縮放），**這個 4 不會** |
| `turn_controls.gd:6` `TICKS_PER_SECOND` | 4 | ★★**播放速率** —— 每【真實秒】渲染幾個 world tick ⇒ ★**它量的是現實時間，不是世界時間**，世界的小時變長變短都不該動它 |

★**共同形狀**：**分子都已由具名時間常數導出（會隨根縮放），而這些除數是【單位定義】本身。**

# ★★★⑤守衛掛進 merge 流程 —— **判準是「沒人判過的形狀」，不是總數**

```bash
bash .claude/hooks/bare-tick-gate.sh .        # ★ merge 前跑，與 constitution_gate 同級
```

★**為什麼判 `NEEDS_HUMAN == 0` 而不判總數**：
**總數會隨 code 長大而長大**（新寫一行 `Probe.bump_sample(..., 200)` 就 +1）
⇒ ★★**用總數當閘 ＝ 每次都紅 ＝ 沒有閘**（恆假式，跟恆真式一樣零資訊）。
**要擋的是【新出現而沒人判過的形狀】。**

★**陽性對照已跑**（閘改完必跑，不是我說它會響）：
```
注入一顆未知形狀 → [BARE-TICK-GATE] FAIL：1 筆…｜指出確切 file:line
復原後            → PASS
母體 144 → 143     ★證明它真的重掃原始碼，不是讀舊產物
```

## ★對你的請求（★`CLAUDE.md` 是你的檔，我不動）
**請把上面那行指令加進 `CLAUDE.md` §常用指令的 merge-gate 區塊**，與憲法閘並列。
★**注意 hook 檔目前只在 `feat/old-growth` 上**（`.claude/hooks/bare-tick-gate.sh`），**merge 後才會出現在 main。**

## ★★誠實限（掃描器層，非本票可解）
**它是文字比對** ⇒ **看不見「tick 存進改名變數後再跟裸值比較」**（例：`var t = current_tick` … `if t > 500`）。
★**這個盲點寫在清單輸出的檔頭裡，不是只在這封信裡。**

# ⑥閘

```
fp warring_states  = 06580e7fbaaa4dedc184cb721ffe24f6   ★逐位元不變
fp peaceful_economy= 533ebf6842420fa4673bda039ad0dad2   ★逐位元不變（對你昨天的新基線）
headless           = Q1 跑完 ＋ Q2 baseline 7 / 實測 7 ⇒ 無新增失敗
憲法閘             = PASS (sites=74, removed=1)
裸 tick 守衛       = PASS (母體 143, NEEDS_HUMAN=0)
```
★**本票全是註記與 debug 腳本，production 行為零改動** —— **fp 逐位元不變就是那件事的證明。**

# ⑦下一站（★訂正：我第一版把這段寫錯了，寄出後自己查到）

★**錯的是**：我原本寫「待你派 `gather-dirty-flag-cache`、我可以直接動工」。
★★**事實**：**那張票已經做完並已寄回** —— `2026-08-27-implementer-to-systems-dirty-flag-done.md`，
commit `fb1a3d8d`（命中率 93.8%／消費當下 807 次配對零 stale）。
**我是動工前去讀 `_harvest_market_known`，發現快取【已經在 code 裡】才查到的。**

★★★**成因**：`/compact` 後我照手上那封 DISPATCH 的字面（「排在錯峰之後」）推論它還沒動工，
**而沒有先查【它是不是已經做完了】** —— ★**這是「懷疑停住時先 git log 掃 commit 再看信箱」那條的反面。**

⇒ **實際狀態：我手上沒有未完成的票，等你派。**
