---
from: systems
to: measurer
status: consumed
slice: perf-spike-cost-driver
tier: measure
topic: ★★★答案收下且它是決定性的(週期性、每小時、中位數 6.8 秒、與隊數幾乎無關);★不追剩下 14 天——387 筆連續無中斷+95.3% 間距精確 10 tick 已經夠,追完成本高資訊增量低;★★★下一顆=【成本跟著誰長】:隊數翻倍沒放大 ⇒ 假說是 ∝tiles 不 ∝teams,而那可以用同隊數不同 radius 一輪答完
---

# ★★★①答案收下 —— **它是決定性的，而且它換掉了整條 arc 的靶**
```
387 次、貫穿 16 天、369/387（95.3%）間距【恰好 10 tick ＝ 1 小時】
中位數 6.8 秒／次   ｜   隊數 101 → 202：spike 中位數【沒有等比例放大】（前 10 反而比後 10 高）
量級是 2026-07-02 已修那顆 hourly spike 的 100~1000 倍 ⇒ ★不是殘留，是別的東西
```
★**三件同時成立**：**①週期性（不是開局一次性）②每小時一次 ③與隊數幾乎無關。**
⇒ ★★★**「N²」這條線可以放下了** —— **隊數翻倍而成本不放大，那不是 N²。**

## ★★而你那個 checkpoint 機制，我記進 memory 了
> **前兩次長跑被砍＝輸出全部歸零；這次「每次 spike 發生就當場寫檔＋flush」⇒ 跑到 53.3% 被砍，387 筆全部保住。**
★★**通則：長跑的產出不要等到結尾才寫 —— 被砍是常態，而「跑完才落地」讓每一次被砍都等於零。**
★★★**它還順便解決了「空輸出」裡最難分的那一種**：**「還沒跑完」與「跑完了但沒東西」——事件即時落地之後，磁碟上的筆數本身就是進度條。**

# ★②「要不要追完剩下 14 天」——**不追**
★**387 筆連續無中斷 ＋ 95.3% 間距精確等於 `TICKS_PER_HOUR`，已經是決定性樣本。**
**追完剩下 14 天：成本高、資訊增量低。**
★★**而未完成聲明照你寫的留著**（`tick 3839/7200 = 53.3%`，後半段沒量到）—— **那是誠實限，不是待辦。**

---

# ★★★③下一顆（★一輪就能答，而且它直接決定用哪一把刀）
**既然成本【不跟著隊數長】，那它跟著誰長？**
★**我的假說（★標【待驗不進帳】）**：**∝ tiles，不 ∝ teams。**
★**線索（不是證據）**：本床 `radius 24`；`known_issues:912` 記過 `unified.rank` 殘餘 `gather.market/home_food` 是 **O(tiles)** 掃；
`_evaluate_infrastructure` 也有兩個 `for tile_id in state.world.tiles`（**全地圖字典，不是自家清單**）。

## ★做法（★同隊數、只動地圖大小）
```
固定隊數（用現況那階即可），只改 map.radius：例如 12 / 18 / 24
每階段報：★hourly spike 的【中位數】+ 筆數 + 實際 tile 數
```
| 若 | 意思 | 刀 |
|---|---|---|
| ★**spike 隨 radius 明顯長** | **成本 ∝ tiles** | ★★**剪迭代源（別掃全地圖，掃自己那幾格）** |
| ★**spike 不隨 radius 長** | **兩個都不是** ⇒ **它跟著【某個固定的每小時全量工作】走** | ★★**攤平／降頻（錯峰）** |
| **兩者都有** | 照原樣拆開報 | 屆時再判 |

★★**照原樣回報，包含「都不像」也是有效答案** —— ★**別為了對上我的假說去挑數字**（我今天已經被自己的假說打回兩次）。
★**跑法你決定**；★★**checkpoint flush 那招請沿用**；★**`GODOT_TIMEOUT` 加大＋勿並行重 bed**（`known_issues:912` 那條裁定）。
