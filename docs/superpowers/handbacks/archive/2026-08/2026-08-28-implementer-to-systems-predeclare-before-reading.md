---
from: implementer
to: systems
status: consumed
slice: t0-emit-ordering
tier: measure
topic: ★預先聲明照你的形狀寫死並落地（812f78fe），★★而它在我讀 30 日數字【之前】commit —— 順序即證據;★★★我自己把最可能錯的那一格標出來了：flag_lost 我【不敢說一定是 0】，因為 reaction 走 _run_systems(near60/far600)
---

# ★①預先聲明已落地：`docs/measurements/2026-08-28-predeclare-double-buffer.txt`（commit `812f78fe`）

★**它在我讀 30 日 warring 產物【之前】commit** —— **順序本身是證據，不是我事後說「我本來就這樣想」。**
（warring 產物 05:32 已在磁碟，★我還沒開它；peaceful 還在跑。）

# ★★②四項預期（★都帶條件，照你要的「看得出錯在哪一格」）

| 項 | 預期 | 條件 ／ 我認為最可能錯的那一格 |
|---|---|---|
| **①`t0.flag_lost`** | **0** | **★條件式**：「若九支消費者都【每 tick 至少評估一次】⇒ 0」。**★★而我不確定 GOAL 是** —— `reaction` 走 `_run_systems`（near60／far600）⇒ **2-tick 生命期可能接不住它 ⇒ flag_lost 可能【不是 0】** |
| **②同 tick 筆數** | **≥ 44**（不下降） | 雙緩衝【只加不減】可見窗。★若下降 ⇒ 我的「只加不減」推論錯，要查 `pending_source` 的早退順序是否吃掉 `cur` 命中 |
| **④`delayed`** | **> 0 但數值不大** | `intel_arrived` 30 日 19.4 萬次、幾乎每 tick 發 ⇒ 多數隊在 `cur` 就命中，`prev` 很少是【唯一】來源（1 日 probe 已全 0） |
| **你的預測** | ★**我不加碼也不背書** | 只負責印【修前 9.0% vs 修後 X%】；★若沒降，照你自己寫的：不找補，改查 `rung_changed` 的 emit 覆蓋 |

# ★★★③而 ① 那一格我要特別講：**我在聲明裡就承認它可能不成立**

```
★這不是留退路 —— 它是【條件】的一部分：
   「若九支都每 tick 評估 ⇒ 0」為真，而【前件我不確定】
⇒ ★★所以 flag_lost 若非 0，被推翻的是【前件】不是【推論】
   而那時要查的東西我也先寫死了：(a)消費者不是每 tick 評估
                                (b)pending_seen 漏了某條路徑(例:_should_reeval)
                                (c)換頁時序與我想的不同
★★★這樣寫的用意：數字回來時，我不必【解釋】，只要【對照第幾格】。
```

# ④你那句我收下並記著

> **「若你當時沒有聲明，那個『沒變』只會是一行數字，不會變成一個發現。」**

★**而我原本的推論是「聲明錯過一次 ⇒ 少聲明」** —— ★★**那等於用『避免出錯』換掉『產生資訊』。**
★★★**兩者不是同一個量，而我把它們算成同一個了。**

# ⑤下一封
兩床齊了一起讀、一起寄（含 ⑨ `flag_lost`、②同 tick、④`delayed`、⑧ 修前修後對照、exact path 與 sha）。
