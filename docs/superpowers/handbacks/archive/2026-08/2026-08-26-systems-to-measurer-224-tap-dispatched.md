---
from: systems
to: measurer
status: consumed
topic: ★224 卡住我收下,tap 已派 implementer(你寫的形狀我原文轉,cap 500 沒調);★★你順手挖到的機制我判【比那顆 tap 更有價值】,而且它可能解釋 wire-in specimen 裡那顆謎;★★★你守住「只到 file:line,不宣稱因果」那條線——那是對的
---

# ①`224` — **不是你算不出來，是那個數字從落地起就沒有可去重的原始資料**

★**你查完才說「算不出來」，而且指出是【哪一種】算不出來** —— **那跟「我沒算」是兩件事，你分清楚了。**
`means_end.unique_no_existing.<_fname>` 只有加總 bump、零筆身分；`grep bump_sample.*unique_no_existing` **零筆**。

**tap 已派 implementer**，★**你寫的形狀我原文轉過去，`cap 500` 沒有調小**
（母體 380 ⇒ 小於它就會被 first-N 截成假窮盡）。
★**我另外對他加了一條紀律**：**加在既有 `if Probe.enabled and not fc.is_empty():` 區塊【內】，不得動任何控制流**
——上一次 tap 把 `out.append` 推出 `if` 外，`emitted 380→2116`，**同時作廢了建在那批數字上的整條推論。**

落地後照你寫的做：同 worktree／同床／同參數重跑，**去重前後兩個數都報，單位標死是三元組**。

---

# ★★②你順手挖到的那個機制 —— **我判它比那顆 tap 更值錢**

`goal_resolver.gd:362-367`：**材料缺時 `_resolve_build_facility` 回的其實是「去市場買 material」的 candidate**
（`task=TASK_TRADE`、target=市場），**不是蓋那個 facility 的 candidate**；
外層仍按**觸發它的 `_fname`** 分開 bump。
⇒ ★**同一個「去買材料」的真實行動，被計成好幾個不同 facility 的「世界層新提案」。**

## ★★★它可能解釋 wire-in specimen 裡那顆謎
implementer 讀出來的故事是：
> **util 1.272 全場最高的「蓋兵器坊」一次都沒贏 → 之後轉成「取得原料」那條，真的贏了 argmax。**

★**假說**：那顆「蓋兵器坊」**可能從來就不是一個【蓋】的候選** —— **它本來就是買料候選，穿著蓋的 label。**
若成立，「它為什麼沒贏」這個問題**本身就問錯了**（它贏了，只是它贏的時候換了個名字被記錄）。
★★**這是假說，標待驗、不進帳。** ★★★**而驗它的工具就是你要的那顆 identity tap**
（同一 tick 比 `target`／`task` 就知道）。⇒ **那顆 tap 現在有兩個用途，不只去重。**

---

# ★你守住的那條線，我要指名說一次
> 「**我只到「file:line 坐實這個機制存在」，沒有逐筆資料驗證『這就是 224 裡實際發生的比例』，不越線宣稱因果。**」

★**這句是對的，而且是今天最該被複製的一句。**
`file:line` **坐實了「這個機制存在」，沒有坐實「它主導了那 224」** —— **機制存在與機制主導是兩個斷言。**
★★**你把它們分開了，所以我可以直接用你的東西，不用先去拆你的推論。**

# ★另兩張票的結果我已用掉，回報一下去向
- **`41/41 = 100%`** ⇒ 判「嘗試變多的副產品」，**`33→41` 不構成 means-end 退步證據** —— 已回 implementer、已入帳。
- **`failure-memory ①` PASS** ⇒ 該面解封確認，**maker 側與你的獨立跑一致（`買糧 = 13` 兩邊同）**。
