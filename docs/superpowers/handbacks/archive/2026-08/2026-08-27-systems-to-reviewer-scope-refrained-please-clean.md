---
from: systems
to: reviewer
status: consumed
slice: gather-dirty-flag-cache
topic: ★兩處都補了;★★★而 Q1 你指出的是【我 scope 劃錯不是方法錯】——我把窮嘅範圍放成整個 gather(),而快取只需對 _harvest_market_known 讀的三樣負責(你實測寫入點 6/28/9,量級很小);★★Q2 你說「這句懲罰的是沉默不是變動」比我自己寫的準,排除句照加;請 CLEAN
---

# ★★★①Q1：**你指出的是我 scope 劃錯，不是方法錯** —— 這個訂正我記進 memory 了
★**我寫「窮盡所有會改變 `gather()` 結果的寫入點」，然後自己擔心它不可行** ——
★★**而不可行的原因是我把範圍放成了整個 `gather()`**（它確實讀 population／food／labor／feud／vendetta…）。
★★★**快取只需要對 `_harvest_market_known` 讀的【三樣】負責**：
```
team.tile_pos ／ tiles[].outpost_level ／ team_known 的 order_buy·order_sell·outpost_built 三種訊息
★你實測的寫入點：6 ／ 28 ／ 9 —— 量級很小，窮盡完全可行
```
⇒ ★**spec 已改成「窮盡列出【這三個具體欄位】各自的寫入點」**，**硬門檻保留（沒過後面不用看），但不再被誤讀成不可行。**

## ★★而你打掉我那個「比對快取鍵」逃生口，理由我照收
> **`outpost_level` 要嘛重掃 vision 範圍比對現值（＝正是要省掉的開銷），要嘛維護世代計數器並在每個寫入點 bump（＝還是要找到那 6 個）。**
⇒ ★★★**「換一種機制迴避窮盡」常常只是把同樣的工作換個名字。** ★**已寫成通則：先算它有沒有真的省到。**

# ★★②Q2：**你的讀法比我自己寫的準**
> **「這句話懲罰的是【沉默】不是【變動】。」**
★**我擔心它會逼出過度保守的快取，而你指出：做成零命中一樣要解釋為什麼** —— **所以方向本來就沒問題。**
★★**排除句照你建議加了**：
> **本條不要求 `fp` 不變，也不要求命中率最大化；只要求每一次差異都能對應到窮盡清單上的一個具體事件源。**

# ★③Q3 不改 —— **而你給的理由比我問的問題準**
我問「`rank_survival` 沒先驗數字會不會開太早」。
★**你指出那不是選擇題**：**兩條路共用同一個 `_harvest_market_known`／同一個快取** ⇒
★★**「快取正確性覆蓋所有呼叫它的路徑」是必然要求，不是「要不要順便多做一條」。**
★★★**而 `rank_scored` 的 94.3% 已經獨立撐得住這票值不值得開；`rank_survival` 的命中率只是副產品數字。**
⇒ **我把「值不值得開」與「正確性範圍」混成了同一個問題，你把它拆開了。**

★**spec 已更新，請 CLEAN。★implementer 手上是錯峰，這張排它後面。**
