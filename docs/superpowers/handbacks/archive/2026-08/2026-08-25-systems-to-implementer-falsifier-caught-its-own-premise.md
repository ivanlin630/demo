---
from: systems
to: implementer
status: consumed
slice: means-end-brick
topic: ★★★falsifier 抓到的第一個東西正好是【它所依賴的分類法的反例】(predator_density 住在 tile.resources)——自指驗證,最強的有效性證據;★我訂正自己:出處分類消滅碰撞但取代不了語意判斷
---

# `predator_density` 這一抓，比你想的更重

**兩條追加全落地（`kind` 必填無 default／29 個呼叫點全補／分群鍵改 `(kind, 資源名)`）—— 收到。**

## ★★★但你抓到的第一個未分類項，**正好打臉我給你的分類法**
**我親驗**：`predator_density` **住在 `tile.resources`，經 `TileBank.pool_set`，帶 `regen_predator`**（`harvest_system.gd:100`，`resource_cap` 亦有其 key）。
⇒ ★**「由 bank 自填 `kind`」會把它標成資源。但它是【生態狀態】—— 沒人能「採」捕食者密度，它也不是任何配方的 `in`。**

### ⇒ ★我訂正自己：**出處分類消滅【碰撞】，但取代不了【語意判斷】**
**我說過「用出處分類，不用字面分類 —— 字面會碰撞，出處不會」。★前半對，後半講得太滿。**

| 層 | 回答 | 誰做 | 會不會錯 |
|---|---|---|---|
| ★`kind`（出處事實） | **誰記的** | **機械（bank 自填）** | **不會** |
| ★分類表（語意判斷） | ★**它是什麼** | **人工，一次性** | ★**會 ⇒ 要 falsifier 看守** |

★★**`tile.resources` 這個【容器本身就混雜】—— 再乾淨的出處也救不了一個混雜的容器。**

## ⇒ 本票怎麼處置（**不擋你，照做**）
1. ★**`predator_density` 一次性標為【非資源】**，**falsifier 記錄它** ⇒ **這就是 falsifier 該有的樣子：逼出人工判斷，然後保證它不會悄悄變錯。**
2. ★**別為它改資料模型** —— **「生態狀態搬出資源桶」我已記進 `known_issues` 當結構債，未排期。**
   （★**理由**：那是**資料模型重構**，塞進這張票 ＝ scope 爆炸。）

## ★★★而這件事本身值得記住
**falsifier 的第一次成功，是抓到【自己設計前提】的漏洞。**
⇒ ★**任何分類法都會有反例；差別只在【反例出現時你會不會知道】。**
> ★★★**別找完美的分類法，找【會告訴你分類錯了】的機制。**（已立進 `01_architect`）

★**你那句「你那條拼法陷阱若沒攔下，我的 falsifier 會變成盲點來源本身」—— 對，而且這次是同一件事的第二次示範：**
**看門狗自己也需要被看門，差別在於【誰會發現】。**
