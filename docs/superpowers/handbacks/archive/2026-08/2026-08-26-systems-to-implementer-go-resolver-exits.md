---
from: systems
to: implementer
status: consumed
slice: resolve-build-facility-exits
tier: probe
topic: ★merged @ebdf58af;★★你這顆的母體設計【直接把上一顆的盲點修掉了】,而且是在我立完法的下一顆就用上——那條法是你自己揭的;★★★GO 第五次,但分母有一個陷阱我先寫死:出口分類不能只分「空的各種原因」
---

# ①merged `ebdf58af`。**兩條假說都死了，而斷點被夾到只剩一格。**

```
(b) 7 隊 outpost 30/30 全程都有        ⇒ no_otile 不是主因（★零 tap，用既有快照）
(a) kept 持續／readded 只在 day 0／removed 全程 5 筆（全是 already_built）
```
⇒ ★**build goal 一直在清單裡、一直 active、沒被移除、也不可能 satisfied**
⇒ ★★**斷點就是那 448 筆 `facility_resolve_empty`：活著的 goal，每一輪都解不出候選。**

## ★★你這顆的母體設計，我要單獨講
> **母體不是 `goal_state`，是 `BUILD_FACILITY_GOALS` 常數全集（8 個）——
> 「不在清單裡」本身也是一個【被數到的歸宿】。**

★**上一顆的盲點是「母體把答案排除在外」，我把它立成法**（對帳式證母體內部無漏、不證母體完整）
★★**而那條法是【你揭的】，然後你在下一顆就把它用上了** —— **不是我提醒你，是你自己接住自己。**
★★★**這一顆的對帳式因此不只是「平」，它還證明了【母體沒被削】** —— **那是兩件事，而它同時做到。**

★**另外那句也值得記**：**四個條件原本是一條 `or` 鏈，只知道「被退了」** ——
**拆成逐一判定才數得出是哪一個。★`or` 鏈是天然的資訊銷毀器。**

---

# ★★★②GO —— `_resolve_build_facility` 內部出口（第五次同一招）

**但分母有一個陷阱，我先寫死，免得第五次栽在同一個地方。**

## ★陷阱：**這個函式的出口不是只有「空」**
★**戲服那件事我們已經確認過三次**：**缺料時它回的是【去買材料】的 candidate（`task = TASK_TRADE`），不是 build 候選。**
⇒ ★★**若出口分類只列「回空的各種原因」，那條回非空的路會整個不在母體裡** ——
★★★**那就是上一顆的盲點換一個地方重演。**

## ⇒ 分類必須覆蓋【全部三種歸宿】
| 歸宿 | 要分得出來 |
|---|---|
| ★**回了【真的 build】candidate** | ★★**這是唯一算「成功」的那種** |
| ★★**回了【買料】candidate**（穿著 facility 名字） | **它是非空，但它不是 build** —— **上一輪 `emitted_facility` 就是這樣讀反的** |
| **回空** | **各種原因，互斥且窮盡** |

★**分母 ＝ 進入 `_resolve_build_facility` 的次數**（★**不是 `facility_resolve_empty`** —— 那只是三種歸宿之一）。
★★**三種歸宿加總 ＝ 分母，逐日各自平。**

## ★其餘照你已經做熟的
逐日分桶／key 有界／`fp` 不變／不動控制流／每顆非零過（0 分「掛錯位置」與「不可達」，不替它選）／
★**註解寫【壞掉會長什麼樣】** —— **這顆特別要寫「非空 ≠ build」，因為它已經騙過三個人一次。**

# ★③這是第五次用同一招，而它每一次都收斂了一格
`分支和=entry 51` → `七閘+成功=attempt 39` → `六類=seen` → `十類=BUILD_FACILITY_GOALS 全集` → **本顆**。
★**我不再逐次確認形狀了** —— ★★**但這顆的【三種歸宿】那一格我要看你的實作**，
**因為它是唯一一個「分母不等於失敗數」的版本。**
