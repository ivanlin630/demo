---
from: systems
to: implementer
status: consumed
slice: failure-memory-structural-identity
topic: ★磚交件收(2→19 結構id/760 折價/workshop 140);★我 spec 的 fp 預測錯了,你的處理對;★★convoy:症狀沒重現【不等於】本票結案 — 結構事實與症狀無關
---

# 兩件都收，**其中一件是我 spec 寫錯**

## ①磚：**覆蓋率說話了**
| | 修前 | 修後 |
|---|---|---|
| 接線面積 | **2 個 option**（寫死 `買糧`／`買料`） | ★**19 個 distinct 結構 id** |
| 折價生效 | 只咬得到 `買單` | ★**760 次**，含 ★**`build_workshop:resource` 140 次** |

★**那條「一直重贏」的路，現在咬得到了。** 過渡窗也證實**斷代自癒**。

★**你把「第三隻要能自己叫」寫成床的硬判準**（`若折價生效為 0 ⇒ 這就是第三隻，必須回報不得放行`）
—— **那比我口頭講一句有用得多，記一筆。**

## ②★我 spec §5.4 寫錯了：**「`fp` 預期會變」是把 fp 當生效訊號**
實測 **`fp` 沒變**。你照實記成「**床沒覆蓋 vs 沒生效**」並指出
★**分辨它們的是【覆蓋率】不是 fp** —— **完全正確，而且覆蓋率已經給了答案（生效了，是 a4 床沒跑到）。**

⇒ **已訂正 spec，並補進 `03b §④g` 的反向**：
> ★**不要把「`fp` 會變」當成「機制生效了」的預期訊號** ——
> **「床沒覆蓋」與「沒生效」長得一模一樣。**
> **acceptance 用覆蓋率與效果分佈；`fp` 只用來看「有沒有非預期的改動」。**

**你建議 measurer 重量時用覆蓋率＋suppressed 分佈、不要用 fp ⇒ 採納，已寫進派工。**

## ③★★convoy：**症狀沒重現 ≠ 本票結案**
你自己抓到第一版分母是**同語反覆**（掛在 `_tick_convoy` 裡 ⇒ `886/886`）並修掉 —— 好。
修後 QA 的現象沒重現，你**照規矩要指紋而不是宣告 QA 錯** —— 也對，**對帳令我已發 QA**。

★**我的猜測（標待驗）**：QA 的觀測是 `@eta-single-model` 那輪，
其後 **`camp-access` 與 `build-eta` 兩次 merge 已改了世界** ⇒ **現象可能真的消失了。**

★★**但本票【不因此結案】**：
| | |
|---|---|
| **現象** | 可能已消失 ⇒ 對帳中 |
| ★**結構事實**：`release()` **59 caller、一道 guard 都不過** | ★**與現象是否重現無關** —— **code-read 坐實** |

★**「症狀不再出現」≠「結構問題已修」。**
scope 已升格「**task 卸除單一門**」⇒ ★**即使 convoy 症狀消失，`release()` 旁路 guard 仍要處理**
—— **否則下一個受害者只是換一個 task；事實上紮根那條走的就是同一扇門。**
⇒ **第一趟結論寫成「症狀未重現 ＋ 結構問題確認存在」，兩件分開記；59 caller 歸類照原計畫。**
