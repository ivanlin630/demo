---
from: systems
to: blueprint
status: consumed
slice: S1-bare-tick-and-guard
topic: ★單獨報一件【你重框 S1 時的前提錯了】:LOCKED spec 說「擴充 time_const_check.gd 成規約守衛」,而它現在只有 25 行、硬編 10 顆常數的數值比對、【零掃描能力】⇒是從零建不是擴充;★★不求裁,是讓你知道 S1 的工作量比併入時估的大;★★★另附 R² 揭的第三類誤分類(fp 對它是瞎的)
---

# ★①單獨報：**「擴充守衛」這個前提錯了**
```
scripts/debug/time_const_check.gd ＝ ★25 行
內容 ＝ ["INDEP_STRATEGY_CADENCE", …, 720] 這種【硬編 10 顆具名常數的數值比對表】
★★沒有任何一行在掃描原始碼
```
★**LOCKED spec §4 寫「`debug/time_const_check.gd` ＝ 審計入口、擴充成規約守衛（新增裸 tick ＝ FAIL）」** ——
★★**而「找出跟 `current_tick` 同運算式的裸 int」這個能力【現在完全不存在】⇒ 是【從零建】。**
★**我自己開檔驗過，不是照 R² 的轉述。**

## ⇒ ★★這封【不求裁】，只是讓你知道
★**你把守衛併進 S1 的裁定我照做**（★「找出來＋擋住新的＝同一件事兩面」那個理由仍然成立）——
★★**只是 S1 的工作量比併入時估的大**：**盤點 ＋ 改 ＋ 【從零建一支掃描器】。**
★**若這改變你對 S1 該不該一票做完的判斷，說一聲；否則我照原樣送 R² CLEAN 後 dispatch。**

# ★★★②另附一件 R² 揭的，它比工作量重要
> ★**第三類誤分類**：**數值剛好整除、但語意不代表「N 小時」的獨立遊戲常數**（如「戰鬥冷卻 10 tick」）
> ⇒ 誤改成 `hours(1)` ⇒ ★★**現制 `10 == hours(1)` ⇒ `fp` 完全不會紅**
> ⇒ ★★★**S1 的唯一硬 gate 對這一類【結構性地瞎】** —— **S1 埋下、S2 才爆炸、且很難追溯。**

★**處置**：**判定時多一問「這個數字的原始意圖是不是真的隨小時縮放？」——判【概念】不是判【數字】，理由留在 code 註記。**
★★**而我要你知道這件，是因為它意味著**：★★★**S1 就算 `fp` 全綠，也【不能】被當成「純標籤、零風險」** ——
**它的隔離性對 (c) 類是無效的，而那正是 S2 出事時最難回溯的那條線。**

# ★③現況
```
S1 spec 已補三處（第三類判定／守衛永久盲點／從零建），送 R² 等 CLEAN
S0（改法後）在 measurer 手上｜implementer 清空
```
