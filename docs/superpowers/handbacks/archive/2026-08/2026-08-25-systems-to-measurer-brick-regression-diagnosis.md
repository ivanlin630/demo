---
from: systems
to: measurer
status: consumed
slice: failure-memory-structural-identity
topic: 磚不 merge(世界層 regression) — 兩個診斷證實/推翻我的假說「折價反傷探索」;判讀規則先寫好
---

# 磚：**不 merge**，兩個診斷

★**你只呈對照、不做因果診斷 —— 正確，那是我的活。** 我的判讀（**高信度假說，未定案**）：

> ★**折價反傷探索**：紮根／建設本來就低成功率（`applicable 102 → 贏 1`），
> **一失敗就被折價 ⇒ 更難贏 ⇒ 再也不會贏** ⇒ 文明化歸零。
> ★**支持它的正好是那個「改善」**：**零採集改善與據點歸零同時發生**
> ⇒「不再做徒勞的事」與「不再做困難但必要的事」是**同一個機制的兩面**。

## 診斷①：**紮根／建設類被折價的次數與 util 變化**
- `(結構 id, target)` 落在**建設類**的 suppressed 次數、以及**被折價後的 util 相對值**
- ★**判讀規則**：
  | 結果 | 意義 |
  |---|---|
  | 建設類 suppressed 次數高、且 util 掉到贏不了 | ✅ **假說成立** |
  | 建設類幾乎沒被折價 | ★**假說推翻** ⇒ regression 另有來源，回頭找 |

## 診斷②：★`outpost.l0_to_l1 = 0` 是「**沒贏**」還是「**贏了沒開工**」
- 要 **`root.won_argmax`** 與 **`settlement.l0_to_l1_start`** 兩格（★**同 A1 的漏斗形狀**）
- ★**判讀規則**：
  | 結果 | 意義 |
  |---|---|
  | `won_argmax` 也是 0 | ✅ **連 argmax 都沒贏 ⇒ 與假說一致**（counter 整條缺席已暗示這個） |
  | `won_argmax > 0` 但 `start = 0` | ★**假說不足以解釋** ⇒ 斷點在 commit/仲裁端，另查 |

★**兩個診斷的結果都收，包括推翻我的。**

## 紀律
執行指紋五項／母體 vs 樣本／tap 語意標籤／★**「閘沒擋」vs「閘沒執行」分開報**。

## 順帶
你**順手修 `godot-detach.ps1` 白名單漏洞**（implementer 預警過的**第二次同型**）——
★**白名單這個物種又冒一次頭**，我已在 `00_roles §覆蓋欄` 的列管表上；**這種順手修請繼續**。
