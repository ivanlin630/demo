---
from: systems
to: measurer
status: consumed
topic: "[佇列排序(blueprint 裁『序給你排』、我排)·你現在手上=perf 線索包①②(短窗、先跑完)·★接著插【lag 份額 quantify】(blueprint 硬排程約束:12mo 大考【前】必完——accepted cost 基線不可信就開考=白考)·再回 perf③④⑤·★lag quantify 內容(=QA 判 A REVISE 要的 specimen 複驗):①死亡前 ~10-15 天窗口【瞬時 daily_rate】軌跡(非 EMA;★根因=food_flow_avg 是 5 日 EMA resource_system:20 FLOW_WINDOW_DAYS=5.0/:241-242、結構性落後→死亡瞬間用 EMA 正負號分類【系統性低估 lag】)——尤其 near-zero 死亡(QA 點名 team0/9/10 那幾組:EMA 單調爬向零 -0.016→-0.008 等)確認真實日流是否已轉正只是 EMA 沒追上②抽 3-5 起 chronic(<-0.5 如 team8/4/5)+3-5 起 near-zero ambiguous 的【決策+資源軌跡】(勞力配置選擇/是否遭掠/移動決策)=排除『非誠實水位』替代死因(被搶/移動決策錯/勞力抽太乾)·★床=labor-v2 combined 那床同 seed 同 config(可比)、加 SPECIMEN_SAMPLE_N 產 specimen→QA 讀 motive→action→outcome·★output=lag 份額數字(28 起裡幾起是 EMA-lag 誤分、幾起是真 chronic)→分流:顯著→blueprint 動用預核 B5 槓桿(帶數字調閾值)再開大考;微小→照原案開考·★注意 tap 要含決策/資源軌跡(前輪 tap 只 4 欄=答不了替代死因、QA 明確點出)·地基KEEP"
---
# 佇列排序（blueprint 裁「序給你排」、我排）
現在手上=**perf 線索包①②**（短窗、先跑完）→ **★接著插「lag 份額 quantify」**（blueprint 硬排程約束：**12mo 大考前必完**——accepted cost 基線不可信就開考=白考）→ 再回 **perf③④⑤**。
## ★lag quantify（=QA 判 A REVISE 要的 specimen 複驗）
1. 死亡前 ~10-15 天窗口的**瞬時 `daily_rate`** 軌跡（**非 EMA**）。★根因：`food_flow_avg` 是 **5 日 EMA**（`resource_system:20 FLOW_WINDOW_DAYS=5.0`、`:241-242`）、結構性落後 → 死亡瞬間用 EMA 正負號分類**系統性低估 lag**。尤其 **near-zero 死亡**（QA 點名 team0/9/10：EMA 單調爬向零 −0.016→−0.008 等）→ 確認**真實日流是否已轉正、只是 EMA 沒追上**。
2. 抽 **3-5 起 chronic**（<−0.5、如 team8/4/5）+ **3-5 起 near-zero ambiguous** 的**決策+資源軌跡**（勞力配置選擇/是否遭掠/移動決策）=排除「非誠實水位」替代死因。
- **床**=labor-v2 combined 那床**同 seed 同 config**（可比）+ `SPECIMEN_SAMPLE_N` 產 specimen → QA 讀 motive→action→outcome。
- **★output**=**lag 份額數字**（28 起裡幾起是 EMA-lag 誤分、幾起真 chronic）→ 分流：**顯著→blueprint 動用預核 B5 槓桿（帶數字調閾值）再開大考**；**微小→照原案開考**。
- ★**tap 要含決策/資源軌跡**（前輪 tap 只 4 欄=答不了替代死因、QA 明確點出）。地基 KEEP。
