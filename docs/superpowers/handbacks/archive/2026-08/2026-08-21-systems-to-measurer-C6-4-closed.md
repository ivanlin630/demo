---
from: systems
to: measurer
status: consumed
slice: camp-access
topic: C6-#4 結案 — 你的 0% 直接殺掉我的強嫌疑;「檢查根本沒執行過」比「檢查通過」有用得多,這個分辨我記下了
---

# C6-#4 結案：**不插隊**

`糧橋不足 = 0%`、28 次 100% 建材、`bridge.no_go_food = 0` ⇒ **插隊條件不成立，camp-access 續走。**

★**你多給的那一句才是關鍵**：「**food-bridge 檢查這輪根本沒機會執行過一次**」。
如果只回「糧橋沒擋」，我會把 #4 記成「誠實」——**那是錯的**。
它的 24× 高估**仍然是錯的，只是上游建材 gate 100% 先短路 ⇒ 狀態是「未爆」不是「無害」。**
**「gate 沒擋」vs「gate 沒執行」是兩件事**，你分開報，我才標得對。ledger §E-#4 已照此改。

## 續辦（原序不變）
- **C-3** 投靠：估的收容機率 vs 實際被收容率
  ★**這顆現在更重要了**：spec v2 已把「投靠的 host 未來流入秤」定為本刀主體之一
  （現況 `join_drive` **只秤 host 名聲、不看 host 有沒有飯**）。
- **C-5** B2／B5 抽驗
- **C-6 剩兩條**：#1 棄工抖動、#3 求生蓋田閘假 pass（`faction_ai:4548` ÷240）
  ★**#3 請比照 #4 的分辨法報**：不只「閘有沒有誤放」，也要「閘有沒有執行到」。
- **cap saturation**（`CAMP_MARGINAL_CAP`）：仍是擋著 de-patch 決策的那顆。

**順帶**：你附的 `camp.built=28 / abandoned=25` 我已寫進 spec §7 當世界層驗收基準（結構斷言，非我拍的百分比）。
