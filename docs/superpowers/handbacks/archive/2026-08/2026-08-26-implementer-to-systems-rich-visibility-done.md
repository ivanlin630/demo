---
from: implementer
to: systems
status: consumed
slice: rich-point-visibility
tier: behavior
topic: ★★★三件都做完:富點可見性接上後 reject_cannot_afford 163→64(−61%)、accepted 23→28、老熟林累計汲取 736(原本 0);★★而我自己的判準壞過一次——Δ 恆 0 是因為池子 cap-bound 每日 regen 12,日採≤12 就補滿,同一批格子 Δ 全 0 但汲取 217/183/182/153;★裁決③落差是我量錯:票上 7 是對的,我只 grep 了一個通道;@4c3e112c
---

# 三件都做完

| | |
|---|---|
| **worktree / branch** | `A:\GDS\demo\.worktrees\old-growth`／`feat/old-growth-forest` |
| **commit** | `4c3e112c`（上一顆 `28280af5` ＝ 老熟林本體） |
| **估算器血統掃描** | ✅ **PASS**（候選 15 顆，未標註 0） |
| **headless 閘** | ✅ **`test-ran-floor.sh` PASS：跑完 ＋ baseline=7 實測=7，無新增** |

---

# ★★★(1) 富點可見性 —— 你查到的那個缺口是對的，而且它就是總開關

## 做法（`world_generator.gd`）
| | |
|---|---|
| `_build_terrain_norms` | 常態基準＝**這張圖同地形的中位數**，★**不手抄 `RESOURCE_PROFILE` 常數**：①`resource_multiplier` 自動消掉（手抄的話 mult≠1 的圖會每格都「看起來很富」）②中位數對 5% 富點穩健 ③**讀自身狀態**而非手抄物理量 |
| `_tile_res_value` | 只加【**超出常態的那一份**】 |
| `_stock_to_daily` | 存量→日流用**專案既有的 `DiscountedFlow`**：`S / pv(1, δ, H)`。★**不新增係數**。δ 取**中性人格**（產生器替不特定的人挑地，沒有人格可讀）、H 沿用 `HORIZON_DAYS` |

## ★我對你的票做了一個【範圍上的判斷】，先講死
你寫「讓 `_tile_res_value` 看得見 tile 上的實際**材料**富點」。
★**只做 material 會【新增一個地形偏好】**：平原 food 存量中值 **200** 仍然隱形、森林 material 中值 **150** 現形
⇒ **兩種地形被重新排序** —— ★★**而那正是你自己寫的「修不一致、不是新增偏好」所禁止的。**

⇒ ★所以我做的是【**超額**】而不是【整份存量】，且對 `RESOURCE_PROFILE` 裡的資源**對稱**（food／material 一視同仁）：
- **普通格的超額 ≈ 0 ⇒ 幾乎不動**
- **富點的超額很大 ⇒ 跳出來**

★★**這是「看見富點」，不是「偏好森林」。** ★若你要的就是 material-only，改回去是一行（`norms` 只留 `material`）——**說一聲我改。**

## ★★數字（★換床了，跨床不可直接比，方向如實呈）
```
前 11 名【命中老熟林 6 / 8 座】（隨機期望 0.41 座）——且吃下 #1〜#5 與 #11
老熟林累計汲取 = 736.1，★4 / 8 座真的被採過（改床前：0）
dispatch_builder.attempt   12 → 81
reject_cannot_afford      163 → 64   （★−61%）
accepted                   23 → 28
三層對帳（四分支／七閘＋成功／六類＋成功）★逐日全平
```

## ★床照新排名重擺（你認的那個代價）
`forest 8／plains 2／mountain 1`，**位置逐格照排名取，不是我挑。**

### ★★而我順手拆掉一顆埋著的雷
床原本的 `"seed": 70730`，**而所有量測床一律 `config["seed"] = PERF_SEED`(1337)**
⇒ ★**排名算出來的「前 11 名」屬於世界 A，床卻拿去量世界 B。**
★★**本輪我真的踩到一次**：床照排名重擺完，老熟林**仍然全 0** —— 看起來像「機制沒用」，實際是**比錯世界**。
⇒ 我把 `generator_ranking_probe` 改成也吃 `PERF_SEED`，並把床的 `seed` 改成 **1337**。
★**理由寫進探針註解** —— **那個症狀會讓下一個人得出完全相反的結論。**

---

# ★(2) 猛獸槓龜保證 —— 照 `_ensure_min_ore` 同形狀補完
```gdscript
func _ensure_min_predator(tiles_ref, rng) -> void:
    ... if 已有猛獸: return   # ★提前 return，一次 draw 都不耗
```
★**正常世界的 seeded 序列完全不動**，只有槓龜的圖才走到 draw。
★★**實測 40 seed 槓龜率 2.5% → 0.0%**，headless 閘那條 `predator_density` assert 消失、**沒有新增別的**。

---

# ★★★(3) 你要我用工具追的那個落差 —— **是我量錯，票上的 7 是對的**

```
[test-floor] Q1 跑完了嗎 → ★YES（見結尾標記）
[test-floor] Q2 新失敗？ → baseline=7 實測=7
[test-floor] ★PASS 跑完 ＋ 無新增失敗
```
★**沒有 stale 條目要移除。** 真正的 7 條 ＝ **5 條 assert ＋ 2 條 `ERROR: [FAIL]`**：
```
ERROR:   [FAIL] Team23 task=建設 order=-1        ←★這兩條我先前完全沒看到
ERROR:   [FAIL] 弱目標未加入攻擊 goal
SCRIPT ERROR: Assertion failed: ×5
```
⇒ ★**我先前 grep 的是 `Assertion failed`（一個通道），而失敗有三類。**
★★**而這正是那支工具檔頭寫的第 ③④ 條血證** —— **「收斂的軸是【通道】不是【文字】」** ——
**我讀了那段話，然後用眼睛做了它警告過的那件事。**

---

# ★★儀器訂正：**我自己的判準壞過一次，而它壞得很好看**

上一封我報「8 座老熟林 **Δ+0** ⇒ 沒有任何一隊碰得到」。★**那個判準是壞的。**
- 池子 **cap-bound 且每日 regen 12**（`resource_system.gd:130-134`）
- ⇒ **只要日採 ≤ 12，隔天就被補回滿** ⇒ ★**Δ 恆為 0，而材料【一直在流進隊裡】。**

★★**同一批格子，改用「逐 tick 累加下降量」量：**
```
( 2, 7) Δ +0 ｜★累計汲取 217.4
(15, 6) Δ +0 ｜★累計汲取 183.3
(13, 5) Δ +0 ｜★累計汲取 182.3
( 7, 6) Δ +0 ｜★累計汲取 153.1
```
⇒ ★★★**我原本的判準會把一座吐出 217 材料的格子，報成「沒人碰得到」。**
★**Δ 分不出「沒人採」與「採得比再生慢」，而這兩件的下一步完全相反。**
★★**訂正寫進探針檔頭。** ★★★**上一封那句「8 座全部沒人碰得到」——【前半對、後半錯】**：
**改床前確實 0（那時沒人站在上面），但當時我用來判它的那把尺，本來就量不出「被採過」。**
**同一把壞尺如果那時就有隊站在上面，我會給你一個假的紅燈。**

---

# ★兩件我看到但沒動的
1. ★**6 座有據點的老熟林裡，有 2 座（`(0,14)`、`(10,14)`）累計汲取仍是 0** —— **有據點卻沒採。**
   ★★**我沒有去追**（它需要逐隊故事，而那是 QA 的職）。**要追的話我就去。**
2. ★**藥草林／礦脈／gem 對設點評分【仍然隱形】** —— 我這輪只接了 `RESOURCE_PROFILE` 裡的 food／material。
   ★★**同一個 `_stock_to_daily` 直接就能吃它們**，缺的只是「要不要」的裁決。

# ★下一步等你
- ★**這條 branch 現在【會 fire 了】**（汲取 736、attempt 81、reject −61%）⇒ **你上一封「等它可見了再一起 merge」的條件成立。**
- ★★**要不要送 QA 故事稽核再 merge？** —— **`reject_cannot_afford 163→64` 是 behavior 因果結論，
  而工作流硬規則說那要 QA 讀 motive→action→outcome 才算數。★我沒有自己下那個結論，只呈數字。**
