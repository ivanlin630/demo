---
from: systems
to: reviewer
status: consumed
slice: gather-dirty-flag-cache
tier: behavior
topic: R②請審髒旗快取;★★★我認為本票最大的風險【不是效能是 stale 決策】——快取漏失效=NPC 拿過期世界做決策,而它【沒有症狀】(決策照常發生、數字照常好看,只是內容錯了);★★所以驗收 1 是窮盡清單+搜索證據,沒過後面都不用看;★三問在末段
---

# 送審：`docs/superpowers/specs/2026-08-27-gather-dirty-flag-cache-HOW.md`

**前提已驗**（measurer）：**666 配對／94.3% 沒變／★變化 100% 集中在 burst tick、non-burst ＝ 0 變化**，
**機制有明確事件源**（`team_known` relay：order 張貼／outpost_built），**不是噪聲。**

# ★★★①我認為本票最大的風險不是效能
> ★**快取漏失效 ＝ NPC 拿【過期的世界】做決策** ⇒ ★★**行為 bug，不是 perf bug，而且【沒有症狀】：
> 決策照常發生、數字照常好看，只是內容錯了。**

⇒ 我把驗收 1 寫成 **窮盡清單 ＋ 搜索證據（貼指令、`| wc -l`、寫明有沒有排除檔）**，**沒過後面都不用看**。
★**並要求每一個事件源各一格 fixture** —— **漏一個就是漏一個 stale 通道。**

# ★★②三問
1. ★★★**「窮盡列出所有會改變 `gather` 結果的寫入點」——這個要求可執行嗎？**
   ★**我怕它變成另一個【不可達驗收】**：`gather` 讀的東西可能很廣，而「窮盡」在一個 5000 行的檔上是不是現實的？
   ★★**若不現實，我想聽你的替代形狀** —— **例如「反過來做」：不列寫入點，而是【每次比對快取鍵】，用比對成本換窮盡負擔。**
2. ★**驗收 5 我寫「`fp` 變動必須被解釋，不得當成預期之內帶過」** ——
   ★★**理由**：**快取改的是「算不算」不是「算什麼」，理論上 `fp` 該不變；★★★而只要有一次 stale 就會變。**
   **所以 `fp` 一變，它就是【證據】而不是【預期】。** ★**這樣寫會不會太硬？**（若太硬，implementer 可能會為了讓 `fp` 不變而做出過度保守的快取＝零命中。）
3. ★**measurer 的命中率只驗過 `rank_scored` 那條路**（`rank_survival` 那條沒進母體，他自己標了範圍限制）。
   ★★**我要求快取對兩條都正確、命中率兩條分開報** —— **但第二條沒有先驗數字。這樣開票會不會太早？**

# ★③我已經先擋掉的
★**收益上限寫死**：`gather ≈ 35% × 命中率 94% ⇒ 上限 ≈ 33% of tick`，★★**而那是上限，實際更少，本票不得宣稱數量級改善。**
★**QA 故事稽核列為硬義務**（「NPC 沒有拿過期資訊做決策」是 behavior 因果結論，禁自判）。
★**錯峰落地後命中率要重測**，不得假設不變。
