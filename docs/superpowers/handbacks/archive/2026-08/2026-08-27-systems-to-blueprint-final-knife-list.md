---
from: systems
to: blueprint
status: consumed
slice: perf-arc-cut
topic: ★★★鑽到底了,刀單完整版;★★而最重要的一句是:【這是常數因子問題,不是複雜度問題】——不 ∝teams、不 ∝tiles、gather 底層是 O(vision_radius²)=O(3²) 有界,所以不會有「一刀砍掉 90%」的東西;★刀單=③⑤撤、④在飛(靶A)、②髒旗快取是靶B主刀、①排②之後;★★★★而②要先驗一個前提再開票,理由在內
---

# ★★★①鑽到底了 —— 完整圖像
```
spike ＝ 每小時一次｜中位數 ~6.8 秒｜★不 ∝teams（101→202）｜★不 ∝tiles（3.84× 不單調）
┌ 靶 A：loop3.orders_ambition ── cadence 對齊 burst（~8/45 個 spike tick，burst/non-burst ＝ 3.5×）
└ 靶 B：整條決策路徑【穩定地貴】
    unified.rank ≈ 43.7% of tick｜單次 100~150ms｜★均攤（top-1 只佔 1.8%）
    gather.*     ≈ 35% of tick（★跨頂層：`rank_scored` ＋ `rank_survival` 兩條路都呼叫）｜持續性大宗、非單點
```

## ★★★★而最重要的一句：**這是【常數因子】問題，不是【複雜度】問題**
```
不 ∝teams ／ 不 ∝tiles ／ ★gather.market 底層是 O(vision_radius²) ＝ O(3²) 有界掃描（VISION_RADIUS=3）
```
⇒ ★★**不會有「一刀砍掉 90%」的東西。** ★★★**要靠【每次決策少做一點事】逐項省。**
★**這句我要你先聽到，因為它決定「什麼叫做完」** —— **若期待是數量級改善，那期待本身要先調。**

# ★★②刀單（完整版）
| 刀 | 判定 | 依據 |
|---|---|---|
| ★**④ 錯峰** | ★**在飛**（已 dispatch，R² CLEAN） | **靶 A；★只解 ~8/45 個 burst tick，中位數不會歸零（已寫進誠實限）** |
| ★★**② 髒旗快取** | ★★★**靶 B 的主刀** | **`gather.*` 是持續性 35%，而它掃的是【變化慢】的東西（vision 窗、市集已知快取）** |
| ① T0 事件驅動 | ★**排 ② 之後** | **②的更徹底版；先看 ② 之後還剩多少，再決定值不值得** |
| ★③ 掃描剪枝（治 N²） | ★**撤** | **不 ∝teams、不 ∝tiles；且 `gather` 底層已是 bounded vision 掃描，沒有全圖掃可剪** |
| ★★⑤ 隊數收斂 | ★★**撤，且有害** | **成本不隨隊數長 ⇒ 減隊數不會變快，而它犧牲世界內容** |

# ★★★★③而 ② 要先驗一個前提再開票 —— **這不違背我「不再往下鑽」的承諾**
★**我承諾的是不再往下【分解成本】**（再下去要逐行 profile、成本跳一級）。
★★**而這一步是【驗證刀的前提】，不是分解成本** —— **兩件事不同，我先講清楚免得看起來像食言。**

**要驗的前提**：
> ★★★**`gather` 每次算出來的東西，到底變不變？**
> **若每次輸入都真的變了 ⇒ 快取無效，②就是白工。**

★**做法便宜**：**量 `gather` 的【輸入變動頻率】**（同一隊連續兩次 gather 之間，市集已知／vision 窗內容有沒有變）。
★★**這是「補丁閘優先查／measure-first」那條紀律的直接應用** —— **不先驗就寫快取，等於賭。**
★★★**而它有明確的失敗出口**：**若變動頻率高 ⇒ ② 撤，回到 ①（事件驅動）或宣布「靶 B 沒有便宜的刀」。**

# ★④順帶：一條舊機制宣稱正式死亡（★引用紀律的第三個實例）
`known_issues:912` ③：**「`gather.market`／`home_food` ★O(tiles) 掃」**
★**實測 file:line 打掉**：`faction_ai_system.gd:3463-3475` 迭代的是 `VisionSystem.VISION_RADIUS`（＝3）的固定窗，
`:3440-3458` 迭代的是 `team_market_known` 每隊自己的快取 —— ★★**都不是 `state.world.tiles` 全圖。**
⇒ ★★★**位置活（`gather.market`／`home_food` 確實是大宗），機制宣稱死（不是 O(tiles)）。**
★**我會把它改寫成不可引用形式並標出推翻它的證據**（同 `k≈2.1` 那條的處置）。
