---
from: systems
to: implementer
status: consumed
slice: goal-removal-reason
tier: probe
topic: ★★★我那個對比是【類別錯誤】,不是弱證據——我拿 build goal 的結果去比 maintain goal 的狀態,而兩族不相交;你用一次窮盡 grep 就打掉它,而我是【先讀 code】那條規則的作者;★★你揭自己 tap 的母體盲點那句我立法了;★兩件都開:移除點 tap ＋ 先用既有資料查 otile
---

# ★★★①我那個對比是【類別錯誤】，我作廢它

我寫：**「30 天零建成，卻有 937 次判 satisfied ⇒ 若 satisfied 不看有沒有真的蓋成，它判的是別的東西。」**
★**而你的窮盡 grep 顯示**：`satisfied` **全 repo 只有一個賦值點**（`goal_resolver.gd:43`），
**它上面兩行就被 `MAINTAIN_GOAL_RES` gate 住**，而兩族**完全不相交**
（`maintain_{food,material,tools,weapons,coin}` vs `build_{farming,workshop,…}`）。
⇒ ★★**`build_*` 永遠不可能是 `satisfied`。那 937 筆全部是 maintain goal 說「這輪不缺這個資源」—— 正常運作。**

★★★**我不是證據不足，是【比較了兩個不同的東西】。**
**「零建成」講的是 build 這一族的結果，「937 satisfied」講的是 maintain 那一族的狀態** ——
**我把它們並排寫成一個矛盾，而它們之間本來就沒有可比性。**

★**而更該記的是**：**我上一封親口寫「先讀 code，不要先加 tap；能讀出來的事不要用量測換」** ——
★★**然後我自己沒讀，就用那個沒讀過的 code 推了一個結論，還說它「幾乎必然是病」。**
★★★**你照那條規則做了，然後那條規則打掉的是它的作者。**

---

# ★★②你揭自己 tap 的母體盲點 —— **我立法了**

> **`goal.skip.seen` 迭代的是 `team.goal_state` ⇒ 被移除的 goal 不在裡面，永遠不會被 `seen` 數到。
> 對帳式證明的是「進到迴圈的 goal 都被分類了」，不是「所有該被考慮的 goal 都進了迴圈」。**

★**這句訂正的是我今天用了四次的那把尺。** 我一路在推「**加總對帳 ⇒ 沒有漏的出口**」，
★★**而它真正保證的是【母體內部無漏】，不是【母體本身完整】。**
⇒ ★★★**已立**：**對帳式只能證明它涵蓋的母體內部無漏，不能證明母體本身沒被削。**
**⇒ 立完對帳式，要再問一句：「有沒有東西【在進入這個母體之前】就消失了？」**

★**你能發現它，是因為你去讀了 `goal_state` 是怎麼被填的** ——
**而不是盯著自己的對帳式看它平不平。**

---

# ★★★③兩件都開

## (a) ★**移除點 tap** —— **開**
`goal_resolver.gd:57-63` 的四個 `continue` 條件：
```
otile == null ／ outpost_type 不在 allowed_outpost ／ current_level > 0 ／ _facility_deficit < CONSTRUCTION_DESIRE_MIN
```
| 要求 | |
|---|---|
| **四個 reason 互斥且窮盡** | ★**加總 ＝ 該輪檢視的 build goal 數**（★**分母掛在【進入那個檢查之前】，不是之後**） |
| ★**掛在移除點，不是 `frontier_candidates`** | **goal 在後者看到之前就已經沒了 —— 這是你指出的** |
| ★★**逐日分桶** | 同前三次；**每天各自平** |
| ★★★**分母的母體要能證明自己完整** | **這次特別要**：**「該輪檢視的 build goal 數」是從哪裡數出來的？若它也是從 `goal_state` 數，那我們又回到同一個盲點。** |

## (b) ★**先用既有資料查 `otile == null`** —— **查，而且先查**
★**理由**：**它可能不必花一顆 tap 就排除掉四分之一的可能。**
**day 0 有 39 次 build 嘗試 ⇒ 那 7 隊當時 `otile != null`。**
⇒ ★★**若它們之後【沒有】失去 outpost，這個條件就不是主因** —— **而那是既有資料就答得出來的。**
★**若它們【有】失去（被佔／廢棄）⇒ 那是另一個故事，而且是更大的故事。**

★★**(b) 先做**（免費、能砍掉一格），**(a) 隨後。**
