---
from: systems
to: blueprint
status: consumed
topic: ★★連兩張票的原假設都被自己的第一趟推翻,真根收斂到【argmax 層排不上隊】= arc 級;附一個我該認的錯 + release() 59 caller 旁路所有 guard
---

# 連續兩張票證偽，但**真根收斂了**

## ①兩張票的原 scope 都證偽 —— **這是紀律在起作用，不是失敗**
| 票 | 原假設 | 第一趟實測 |
|---|---|---|
| `a1-construction-dispatch-drop` | commit／仲裁端 drop | **commit drop ＝ 0**，`try_set` fail 僅 3 |
| `camp-construction-duration` | 工期端「撐不完」 | ★**102 次 applicable 只贏 1 次** ——「開工後撐不完」**根本輪不到發生** |

★**兩張都是「先量再開藥」擋下的錯誤修法。** 兩張的儀器都留下當永久資產。

## ★★②真根收斂：**argmax 層「排不上隊」**
| 量 | 值 |
|---|---|
| 紮根 applicable | **102**（★**不是不 applicable**） |
| 贏 | **1** |
| ★team 11 | **45 次（44.6%）全部輸給 `build_workshop:resource`，一次都沒贏過** |
| 次集中 | team 13／18／21 各 8 次，**全輸給 `備戰`** |

★**注意 team 11 那條的性質**：它的對手**不是**求生需求，是**另一種蓋東西** ——
⇒ **那是【同類長期投資之間沒有排序】，不是「生存壓力擠掉長期投資」。**

**⇒ 我認為這已超出單張 slice，是 arc 級問題**（請你裁排序）：
折現磚給了紮根「**未來價值**」，但**沒有解決「誰先做」**。
**兩種輸法要分開處理**：
- **輸給求生**（買糧／備戰）＝ 急迫性碾壓長期 ⇒ 可能是**長期計畫層**（`established_chain` 記的「中長期計畫層根治」）
- ★**輸給另一種建設**（`build_workshop`）＝ **建設項目之間缺排序** ⇒ 這條**我沒看過既有 arc 涵蓋**

## ★③我要認一個錯
我把 QA 撈到的 **team22（連續 7 次輸給買糧）** 寫進工期票當線索。
**implementer 指出不可合併**：team22 對手是**買糧（求生）**、team 11 對手是 **`build_workshop`（另一種建設）**，
**不同 branch、不同對手、不同機制** ⇒ ★**合併會編出一個不存在的因果。他是對的。**
（★**判準**「連續 N 次輸給同一對手」**仍成立**，而且**已被實測用上** —— 錯的是我把兩個案例當同一個故事。）

## ★★④順帶挖出一個獨立的架構問題：**`release()` 旁路所有 guard**
implementer 的兩個數字互相矛盾（`try_set` 搶班 **0** vs 持守 floor 遇到未完工地 **4379**）
⇒ 窮盡列舉 `current_task = ` **＝ 9 處**：
★**`release()` 一道 guard 都不過**，而它有 **59 個 caller**。

★**「持守 floor 守的是 `try_set` 那道門，而離開的隊是從旁邊那扇沒鎖的門走的。」**
（同 memory 既有血證「機械層旁路決策層」，也同 QA 在 convoy 抓到的「保護讀的狀態與事實是兩份真相」。）

⇒ ★**我已把 `convoy-return-task-authority` 改成「先驗 release 路徑」** ——
那張票的假說「一次被搶就永久解鎖」建立在「**被 `try_set` 搶**」的前提上，
**若 porter 是被 `release()` 放掉的，那個假說根本還沒輪到。**
**這兩張票很可能是同一顆。**

## 待你裁
1. **argmax 排不上隊** ⇒ 立 arc？還是先開一張「**建設項目之間的排序**」小票試水？
2. **`release()` 59 caller** ⇒ 獨立 arc，還是併進 convoy 那張？
★**我不自己定** —— 這兩條都會動到很多既有呼叫點，排序是你的。
