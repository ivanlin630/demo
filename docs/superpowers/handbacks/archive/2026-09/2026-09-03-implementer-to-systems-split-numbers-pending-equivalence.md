---
from: implementer
to: systems
status: consumed
slice: 分桶數字到手 —— ★而它們【還不能用】，等價證明跑中
touches: scripts/data/world_state.gd／faction_ai_system.gd／three_tickets_bed.gd（shadow 接線，尚未 commit）
topic: ★★★數字:`has_home` 修前 24／9／17 → 修後【0／0／0】(三 seed 全歸零);★而 `no_home` 64→75／64→68／69→66 ⇒ ★★方向【不一致】(兩顆上升一顆微降) —— 所以不是你表上第一列的「no_home 幾乎不動」,我不硬塞;★★★總量的下降【完全由 has_home 歸零解釋】:1337 −24+11=−13(88→75)、42 −9+4=−5(73→68)、7 −17−3=−20(86→66),三顆逐數對得起來;★而這些數字【暫時不算數】——等價證明(idx≡scan)跑中,你的規矩是對不上就作廢
---

# ★★★①數字（★但見 §③：等價未證完之前這些【不算數】）
```
             has_home（有家再蓋）        no_home（無家初次）        camp.built 總量
seed1337     24 → ★0                     64 → 75 (+11)             88 → 75 (−13)
seed42        9 → ★0                     64 → 68 (+4)              73 → 68 (−5)
seed7        17 → ★0                     69 → 66 (−3)              86 → 66 (−20)
```
## ★對帳（★三顆逐數對得起來 ⇒ 分桶沒有漏算）
```
1337：−24 (has_home) ＋11 (no_home) ＝ −13 ✓（88→75）
42  ：−9  ＋4  ＝ −5  ✓（73→68）
7   ：−17 −3  ＝ −20 ✓（86→66）
⇒ ★★總量的下降【完全由 has_home 歸零解釋】
```

# ★★②照你先寫死的表 —— ★而我不硬塞
```
第一列「has_home 大幅降、no_home 幾乎不動」
   ★has_home：24／9／17 → 0／0／0 ＝ ★★大幅降，而且是【三 seed 全歸零】，不是「大幅」是「全清」
   ★no_home：+11／+4／−3 ⇒ ★★★兩顆【上升】、一顆微降 —— 這【不是】「幾乎不動」
第二列「no_home 也跟著降」⇒ ★不成立（只有 seed7 −3，−4.3%，且方向不一致）
⇒ ★★所以嚴格講落【第四列：以上皆非】—— ★★★而我照原樣報，不把 +11 讀成「幾乎不動」
```
★**可以說的**：**「抑制了重複紮營」有三 seed 一致的證據（24/9/17 → 0）**；
★★**「傷到初次紮營」沒有三 seed 一致的證據**（+11／+4／−3）。
★★★**而 no_home 上升是什麼，我不解釋** —— 那需要另一個問句（誰在初次紮營、為什麼變多）。

# ★★★③這些數字【暫時不算數】：等價證明跑中
```
你的規矩：兩支不同儀器不得直接比 ⇒ 先證【掃描 ≡ 索引】
★已接線（尚未 commit）：
   `WorldState.own_camp_tile()` ⇒ `OwnerCampIndex.shadow` 開時同時跑全圖掃並 `shadow_check`
      —— ★★★這同時補掉我那個【複製了機制卻沒接線】的洞（`zero-caller-guard` 抓到的那一格）
   `establish_crude_camp` ⇒ shadow 開時並排 bump `camp.built.scan_has_home/scan_no_home` ＋ `scan_mismatch`
   床 ⇒ `CAMP_SHADOW=1` 才開（★預設關：開了每次查詢多掃一次全圖）
★三條驗收（寫在數字之前，你訂的）：
   `shadow_fails = 0` 且 `shadow_checks > 0` 且 索引版分桶 ≡ 掃描版分桶
⇒ ★★三條全過 ⇒ §① 那些數字才算數；★★★任一不過 ⇒ 修前那三顆【作廢】，我照說的停
跑中：`equiv_1337`（30 日、CAMP_SHADOW=1）
```

# ④順帶一個乾淨的佐證
```
★修前這批的 `camp.built` 總量 88／73／86 —— ★★與前一批 churn 基準【逐數相同】
⇒ ★★★而前一批【沒有】回植掃描 tap ⇒ 那段回植的掃描【沒有改變被觀測物】（如宣稱：Probe-gated 唯讀）
```
路徑：`scratchpad/split_pre_{1337,42,7}.txt`（修前）／`m2_{1337,42,7}.txt`（修後）
