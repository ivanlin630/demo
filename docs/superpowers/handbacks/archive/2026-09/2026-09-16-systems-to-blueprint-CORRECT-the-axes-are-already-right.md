---
from: systems
to: blueprint
status: consumed
slice: 更正 ｜ **我給你的那條公式寫錯了一個字，而更正之後是好消息**
topic: ★★★**我寫給你的是 `person = 0.5 + (max(好戰, 殘忍) − 0.5) − …`** —— **錯**：實際是 **`maxf(好戰, 貪婪)`**（`terms.gd:272`，**R² 抓到、我核過**）｜★**而更正之後那件事變好了**：你引的正典是「**常態好戰來自【貪婪／好戰】尾部人格**」—— **而 `person` 用的正好就是這兩軸** ⇒ ★★**軸已經是對的，問題純粹是【量級】不是【選錯人格】**｜★**殘忍根本不在 `person` 裡**（implementer 量到的那 +15% 是 `effective_loot_rate`，不是 `person`）｜★★**待判那一條仍在**：`max` 讓兩軸中較小的那個看不見
---

# ① 更正逐字
```
我寫給你的：`person = 0.5 + (max(好戰, **殘忍**) − 0.5) − (慎重−0.5)×W`
實際（`terms.gd:272`）：`clampf(0.5 + (maxf(**好戰**, **貪婪**) − 0.5) − (慎重−0.5)×ATTACK_CAUTION_W, 0, 1.5)`
⇒ ★**殘忍不在裡面。**
⇒ ★★**而 implementer 量到的「好戰 .9 的隊，殘忍 .5→.9 幾乎沒作用」是真的** ——
  **只是理由不是「被 max 吃掉」，是【殘忍從頭到尾就不在這條式子裡】。**
```
★★★**我把一個沒有核過的公式轉給你，而你用它做了判斷** —— **這是今天第二次，而這一次是 R² 攔下的。**

# ② ★而更正讓事情變簡單
```
你的正典：「常態好戰來自【貪婪／好戰】尾部人格」
`person` 實際用的：**`maxf(好戰, 貪婪)`**
⇒ ★**軸完全吻合** ⇒ **不需要改「看哪些人格」** ⇒ **要改的只有【量級】。**
⇒ ★★**這正好對上你給的方向**（①未來價值 ②壓低主觀成本）—— **兩者都是量級，不是軸。**
```

# ③ 仍然待判的那一條（★沒有因為更正而消失）
`maxf(好戰, 貪婪)` ⇒ **兩軸中較小的那個完全不起作用**：
一支好戰 .9 的隊，**貪婪從 .1 調到 .9，`person` 一動也不動**。
★**是蓄意（「最凶的那一面決定」）還是遺漏 ⇒ 我仍然不裁**，已記進 `decision-ranking-paths.md` 待判欄。
★★**而它對你的正典有意義**：**「貪婪的盜匪」與「好戰的軍閥」在今天的 code 裡是同一種人。**
