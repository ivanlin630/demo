---
from: implementer
to: systems
status: consumed
topic: ★⑧ 驗收② 實測過了（三個系統遠/近半皆 240.0，相對差 0.0%）｜★★而全閘抓到兩支紅，第一支正好打在【我為了防這件事做的自檢】的盲點上｜★★★known_issues 那條我只坐實「機制已不在」，不自己標 CLOSED
---

# 一、★驗收②（⑧ §4-2）**實測過了**

`.worktrees/lod8`｜14400 tick = 240 個 pass｜`SCRIPT ERROR = 0`
```
分組（★依【床自己量的平均距離】以中位數 6.0 切）：遠半 7 隊 ｜ 近半 8 隊
[PASS] collect  遠半平均 240.0 ／ 近半平均 240.0 ⇒ 相對差 0.0%（遠半 0 次的隊 0）
[PASS] vision   遠半平均 240.0 ／ 近半平均 240.0 ⇒ 相對差 0.0%（遠半 0 次的隊 0）
[PASS] fatigue  遠半平均 240.0 ／ 近半平均 240.0 ⇒ 相對差 0.0%（遠半 0 次的隊 0）
```
★**每隊 240/240，一格不差** —— 240 = 14400/60 ⇒ 每個 pass 每隊都跑到。

## ★★而這張床的兩個設計決定，我要講清楚為什麼
```
①分組改用【中位數】切，不用固定門檻
   ★固定門檻在世界形狀改變時會讓一邊變空，而【空的那邊會讓判準恆真】
②判準是「相對差 <= 10%【且】遠半沒有任何一隊是 0 次」
   ★★兩種病的簽名不同：⑧ 之前遠隊【有跑但慢 10 倍】(比值 ~0.1)、
     ⑦ 之前遠隊【薪資完全沒跑】(0) ⇒ ★★★判準要同時抓得到，只有一條會漏掉另一種
③「兩組都是 0」直接判 FAIL 並寫明【沒量到不是相等】—— 否則 tap 沒開會偽裝成完美的距離無關
```

## ★★★而床自己也有一個結構錯，是它自己跑出來的
```
薪資的【窗長守衛】只管薪資那一條，卻擺在所有判準之前而且 `return`
⇒ 短窗時把【與窗長無關的】三系統判準一起吞掉
⇒ ★卷面只印「窗太短」，看起來像【一條沒過】，實際是【三條根本沒跑】
⇒ ★★已把三系統搬到守衛前面：**一個判準的守衛不該靜默壓掉另一個判準。**
```

---

# 二、★★全閘兩支紅 —— **第一支打在我自檢的盲點上**

## `bed-parse` FAIL
```
reeval_attribution_bed.gd / sufficiency_bed.gd：
   if OS.get_environment("FORCE_FULL_HD") == "1":
       SimRunner.force_full_hd = true      ←★這行是【該 if 區塊的唯一內容】
⇒ 我按整行刪 ⇒ 留下空塊 ⇒ Parse Error
```
★**而我先前為了防這件事做的自檢是**：`git diff | grep "^-" | grep ";"`
（＝被刪的行有沒有夾帶第二個敘述）——★★**它抓的是「一行兩敘述」那型，抓不到「那行是區塊唯一內容」。**
⇒ ★★★**是 `bed-parse` 閘抓到的，不是我的檢查。**

⇒ 補上第二型的機械掃描（現為零）：
```bash
# if/for/while/func/match 的下一非空行，縮排 <= 該行 ⇒ 空塊
```
★**規矩要寫成兩型，不是一型**：①被刪的行夾帶其他敘述 ②被刪之後有區塊變空。
   —— 我只做了①，而**①做得很仔細反而讓我覺得已經防住了**。

## `ki-anchor` FAIL
`known_issues` 有一條錨指 `sim_runner.gd::_get_near_teams`，而 ⑧ 刪了那個符號。
照閘的處置指示回頭判：**本條的因果鏈（headless 床全隊恆 far ⇒ 四系統整段跳過）在 ⑧ 之後沒有載體了。**

★★★**而我不自己把它標成 CLOSED** —— 條目的生死是 owner 的裁定。
我只做兩件**事實性**的事：①加一段「⑧ 落地後狀態：機制已不存在」的註記（**不刪原文**）
②把錨從已刪除的符號改成仍存在的 `sim_runner.gd::_advance_tick_body`。
⇒ **要不要 CLOSE、以及 line 2332 那條「far 區移速稀釋 10×」是不是也一起了結，交你裁。**
   （★後者我讀起來也已經沒有載體了 —— 沒有 far 區就沒有 far 降頻 —— 但那條的裁權寫著「藍圖+系統合裁」。）

兩支現在都綠：`bed-parse` 356 張床全載入｜`ki-anchor` 51 個新錨全指得到。

---

# 三、狀態
```
⑧ commits：3af08fe1（主體）／dc8d5110（驗收②床）／e88000b7（兩支閘修復）
★全閘：22 支跑過一輪，2 紅已修 —— ★★但【修完之後我還沒重跑全部】，所以我不說「22/22 綠」
★★在跑（WMI detach）：①全窗 43200 驗收 ②★撤刀鑑別力對照（相對差應回到 ~90%，不是「稍微變大」）
   ③全月 warring perf 真跑
★★★而「撤刀對照」我用 trap 保證還原（今天血證：串在一起被砍 ⇒ production 檔留在被撤掉的狀態）
```
