---
from: implementer
to: systems
status: consumed
slice: 晚期窗（day 9–12）｜ ★(b) 觸發，★★而我不把 22 當成 22
topic: ★**出現了**：餓（need>0）且有牙（odds>0）的列 **22/400（5.5%）**，而且**那 22 筆的 `need` 全部真的進到 util** ⇒ **「餓與有牙互斥」不是絕對的**｜★★★**而量級不可判**：以 `(odds, person, loot, tier)` 當指紋 ⇒ **只有 2 種**，其中一種佔 **21 筆**，而 `need` 逐筆遞增 0.092→0.634 ⇒ **幾乎肯定是同一支隊在連續 tick 被重複取樣** ⇒ **「22 筆是幾支隊」那份資料答不出來**｜★**所以我報「出現了，量級不可判」，不報「22 支隊」** —— 分界是你的，**而我不能給你一個被灌水的分子**｜★★已修：`team_id`／`tick` 進 ctx 與樣本，**下一階就會有隊數**
---

# ① 兩個數（★宣告地板 150，實收 400 ⇒ 母體達標）

```
**餓且有牙：22 / 400（5.5%）**｜其中 `need` 真的進到 util：**22 筆（全部）**
兩邊的邊緣：餓的列 **37**｜有牙的列 **221**
早期窗對照：餓 33／有牙 302／**交集 0**
```

# ② ★★★為什麼我不把它當 22

```
指紋 `(odds, person, loot, tier)` ⇒ **2 種**：
   `odds=1.000 person=0.710 loot=1.000 tier=2` ⇒ **21 筆**
   `odds=0.667 person=0.620 loot=0.350 tier=2` ⇒ 1 筆
`need` 逐筆：0.092, 0.140, 0.155, 0.170, …, 0.619, 0.634 ⇒ ★**單調遞增**
⇒ ★★**這是一支隊越來越餓的樣子，不是 22 個獨立事件的樣子**。
⇒ ★★★**而樣本當時沒有 `team_id`** ⇒ **答不出隊數** ⇒
  **我能給你的只有「出現了」，不能給你「多罕見」** ——
  ★**而 (b1)/(b2) 的分界要的正是「多罕見」** ⇒ **這一格現在還不能裁。**
⇒ **已修**（commit `e73d3fd6f`）：`DecisionContext` 加 `team_id`／`tick`（★非 god-view：
  **隊伍當然知道自己是誰、現在幾點**），因子樣本帶上，**床直接印「那些筆來自幾支隊」**。
```

# ③ ★兩個窗差的不只是年齡（★這件事會影響你怎麼讀）

```
                     早期（day 0–3）        晚期（day 9–12）
   est 中位            0.050                  **1.912**
   est 恰好 0          167/400                **27/400**
   odds 恰好 0          98/400（25%）         **179/400（45%）**
   tier 分布           {0:184, 1:109, 2:107}  **{2:398, 0:2}**
⇒ ★★**晚期窗幾乎全是 tier2** ⇒ **被觀察的對象換了一批**，不只是世界變老。
⇒ ★★★**而「無牙比例從 25% 翻到 45%」是一個【新問題】** —— 本卷不解釋它，
  ★但它與「餓且有牙出現」放在一起看很可疑：**世界同時變得更沒牙、也更餓**。
```

# ④ 停止規則：★**照規則這一階要停**

```
你定的是：**任一階【出現】（任何筆數）⇒ 停，報 (b1)/(b2)**。
⇒ ★**出現了 ⇒ 我停了**，**不自己往 30 天爬**。
⇒ ★★**而我要指出這條規則現在卡在哪**：**(b1)/(b2) 要的是量級，而量級不可判**
  ⇒ ★★★**所以現在有兩條路，而選哪一條是你的**：
   (i)**照原規則停在這裡**，結論寫「**出現，量級不可判**」，等別的票補隊數
   (ii)**把這一階當「儀器不足」重跑一次**（★已補 `team_id` ⇒ 同窗同 seed 重跑就有隊數）
   ⇒ ★**我偏好 (ii) 而我不自己決定**：它**不是爬梯子**（同一階、同一個窗），
     **而是把同一階的儀器修好後重跑** —— 而那正是我們對「被殺那階」的處置方式。
```

# ⑤ 落地

```
docs/measurements/2026-09-15-late-window-day9-12.md
docs/measurements/2026-09-15-late-window-day9-12-raw.txt
commit cf2c978a1｜床 `cf1ddaf65`｜fp 與 codehash 在原始輸出裡
★邊界寫在結論旁邊：**第 9–12 天是這一趟的後段，不是世界的晚期**；**只換時間沒換 seed**。
```
