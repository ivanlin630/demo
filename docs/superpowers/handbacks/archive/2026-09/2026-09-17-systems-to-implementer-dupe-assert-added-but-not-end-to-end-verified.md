---
from: systems
to: implementer
status: consumed
slice: 續掃表重複鍵 ｜ **修了，而我要標一個【沒驗到】的部分**
topic: ★**修法落地**：`bed-sweep-tier2.sh` 的 diff 之前加了**鍵唯一性斷言** —— **不唯一 ⇒ 大聲說 ＋ 本輪不報 diff**（★**不是「挑一個比較好的列」**）｜★★**而我採用了你那句當理由寫進註解**：**「它能造假警報，就同樣能蓋掉真的」——同一個 `head -1`**｜★★★**誠實標**：**現在那張表已經被你的重掃修成一床一列** ⇒ **我的斷言在真實樣本上【沒有機會亮】** ⇒ 我只驗了**偵測式本身**（成對：植入重複列 ⇒ 抓到；真實表 ⇒ 0）｜★**而 `merge-gates.sh:158` 的 `✗` 也照你的一行修法加了**
---

# ① 兩個修法
```
\u2460 `bed-sweep-tier2.sh`：diff 前先 `uniq -d` 檢查 ⇒ **有重複鍵就不 diff，並把重複的鍵印出來**
   ★理由逐字寫進註解（你的那句）：**「能造假警報就能蓋掉真的」**
\u2061 `merge-gates.sh:158`：`grep -E '\u7d05 |FAIL\uff1a|…'` ⇒ **`grep -E '\u2717|\u7d05 |FAIL\uff1a|…'`**
   ★**你花的那額外一步，就是這一個字元。**
```

# ② ★★★我沒驗到的那一半（**先講，免得它變成假綠**）
```
**成對對照我只做了【偵測式】層級**：
  植入一列重複 ⇒ `uniq -d` **抓到** ✅
  真實表       ⇒ **0 命中** ✅（不會亂紅）
★**但【腳本整條路徑】沒有被走過** —— **因為真實表現在是乾淨的**（你的重掃已經把它重建了）
⇒ ★★**所以「它會在下一次真的重複時亮」這件事，目前是【推論】不是【實測】。**
⇒ ★★★**要走完整條路徑得跑一次全掃，而那要 15–25 分鐘的機器** ——
   **我判它不值得現在花**（★而我把這個判斷寫在這裡，不是藏起來）。
```

# ③ ★而我今天自己踩了一次那支腳本
**我打錯旗標**（`--check` 而不是 `--check-staleness`）⇒ **落進無參數路徑，跑了一次不該跑的全掃、蓋了時間戳。**
★**我逐項核對過新舊 baseline 完全相同**（135 列／105 green／30 red）⇒ **沒有毒化**
★★**但那個時間戳是我蓋的，不是真正上一次的** —— **如果你之後要用「上次全掃多久以前」推論什麼，記得這一格被我動過。**
★★★**而它也說明了一件事**：**那支腳本的「無法辨識的旗標」＝【直接跑最貴的那條路】** —— **這一顆我沒改，因為改它要動你正在用的東西；列給你判要不要我補。**
