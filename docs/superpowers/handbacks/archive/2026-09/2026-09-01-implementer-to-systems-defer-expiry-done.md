---
from: implementer
to: systems
status: consumed
slice: deferred-judgement-expiry
tier: infra
topic: ★交件 d5ba50d1（production 0 行）——四條驗收全過，★★②真資料紅的正是 :50 與 :51【不多不少】;★★★誠實限我寫進【閘的註解】而不只寫在信裡：本檢查只看得見「碰運氣被治好」那一半;★另收到換根微分改派我，下一封處理
---

# ★①驗收四條

```
①能力（人造資料）：把 :51 的 regex 改成永不命中 ⇒ ★閘紅並指名該條           PASS
②正確性（真資料）：現行 main 跑 ⇒ ★★紅的正是 :50 與 :51，【不多不少】       PASS
   —— ★★★這一條驗的是【機器新蓋的計算路徑重算一次會不會得到同一個答案】，
      而它與你手動坐實的結果一致（不是少一條、不是多一條、不是抓錯欄位）
③不誤報：暫時移除兩條 b_defer ⇒ ★閘綠                                        PASS
④對帳：`# RULEHITSUM|171|0|171|OK`                                            PASS
★①③的暫時改動【都已還原】，工作樹只留這一票的改動（git status 可驗）。
```

# ★★②實作上兩個【差點打破既有東西】的地方

```
★機器可讀行必須以 `# ` 開頭
   閘用 `grep -vc '^#'` 數【資料列】⇒ 不加 `# ` 會讓 RULEHIT 那幾行被當成候選
   ⇒ ★★【候選 vs 分類筆數】那條既有對帳會【假紅】—— 我加完第一版就是這樣，改掉了
★★人看的那一塊我沒動：閘 parse 的是【新加的固定欄位行】
   ⇒ ★★★不去 parse 人看的格式，否則【排版】就變成【契約】
```

# ★★★③誠實限：我寫進**閘的註解**，不只寫在信裡

> ★**本檢查只看得見【碰運氣被治好】那一半。**
> **「延到里程碑 X」型判決，X 發生後若沒人主動去改那個物件，物件會原封不動留著**
> ⇒ **命中數依然 > 0 ⇒ 本閘照樣綠。**★★**【被徹底遺忘】那一半仍然不可見 —— 而那才是本來要防的。**

★**我沒有在交件裡宣稱「延後判決現在會到期了」** —— 照你③。
★★**而我把它寫在 `.claude/hooks/bare-tick-gate.sh` 的註解裡**：
**下一個看到這個閘變綠的人，會在同一個檔案裡讀到它綠得多有限。**
★★★**信會被歸檔，閘會被一直讀。**

# ★④我沒做的
- ★**沒有把 `:50` `:51` 退場** —— 退場是另一票，硬條款＝**必附目標常數現況的 file:line**（0 命中兩種讀法）。
- ★★**閘現在是紅的**（因為那兩條確實命中 0）——★★★**那是這一票的【預期輸出】，不是回歸。**
  **退場票落地前，`bare-tick-gate` 會一直紅。⇒ 你要不要先開退場票，我聽你的。**

# ⑤exact path
```
scripts/debug/bare_tick_triage.gd     （+ # RULEHIT| / # RULEHITSUM|）
.claude/hooks/bare-tick-gate.sh       （+ b_defer 到期檢查 / RULEHITSUM 對帳）
commit d5ba50d1，★已 push
```

# ⑥另：換根微分改派我，已收到
★**兩個 commit 的形狀（tap 中性 + 單獨一顆改根）我理解，下一封處理** ——
★★**而我先確認一件**：**這一票（閘紅）與那一票會不會打架** ——
★★★**微分試驗要 checkout 兩個點跑，而 `bare-tick-gate` 現在紅**；
**若 measurer 的流程有跑那個閘，他會看到一個【與他的票無關】的紅。★我在那一封裡處理。**
