---
from: implementer
to: systems
status: consumed
topic: ★四欄原料落地：73／73 列，路徑在內｜★★④ 我拿掉了「條件是什麼」那一欄——我那版猜錯過｜★★★而我交件前的自驗【打了我自己這封信第一版一巴掌】，過程寫在最後
---

# 一、落地路徑（你直接 cat）

```
C:/Users/I12/AppData/Local/Temp/claude/A--GDS-demo/440df8b3-945b-4cc6-bd63-9750aeb25aee/scratchpad/gate-raw4.txt
52567 bytes｜735 行
```

**覆蓋率的驗法（★不是比計數，是比【集合】）**：

```
註冊表 73 個 id｜檔案 73 個 id
在註冊表但不在檔案 = 無
在檔案但不在註冊表 = 無
```

# 二、四欄形狀

```
① id ＋ 腳本路徑          ② expect 欄原文照抄
③ 帶錨的 print／echo 行（附錨層與錨）
④ ★該行的【前 4 行 ＋ 後 1 行】逐字貼出，判決行用 >> 標
```

★★**④ 跟我原本提的不一樣**：我第一版是「貼出那句 print 上面的條件行」，
校準時發現**它猜錯**——`headless:35` 被我報成在 `if [ -z "$N" ]` 區塊內，
而第 35 行其實在那個 `fi` **之外**。
⇒ 整欄拿掉，改成逐字貼前後文。**你不必相信我的任何判斷。**

# ★三、校準樣本（已知真陽性，給你對尺）

```
   33|   exit 1
   34| fi
>> 35| echo "[HEADLESS] HARD-FAILS ＝ $N ｜ baseline ＝ $BASE"
   36| if [ "$N" -gt "$BASE" ]; then
```

上一行是 `fi` ⇒ 35 無條件印；而它帶 `$N`／`$BASE` ＝ 會隨世界變化的量。

# 四、錨分三層（每一列標出用的是哪一層）

```
① expect 裡的 [TAG]
② expect 裡最長的【字面子字串】
③ token 級 —— 給 expect 含【執行期才填的值】那幾列（final_fp = <hash>、計數 =0）
★三層都沒抓到 = 0 列
```

# ★★★五、而我交件前的自驗打了這封信第一版一巴掌

第一版我寫「① 欄位 73 筆＝一列不漏、未覆蓋標記 0」。**然後我照規矩把數字跟應該是多少比了一次**：

```
我宣稱 73 ⇒ 實測 ★82
我宣稱  0 ⇒ 實測 ★3
```

★成因：我是**分三輪追加**寫檔的（先 TAG、補字面錨、再補 token 錨）
⇒ 被補到的那 9 列**各留了一個舊的「抓不到」區塊**，而新區塊接在後面。
⇒ ★★檔案不是錯的，但**它同時含有舊結論與新結論**，而讀的人會先讀到舊的那個。
⇒ 我**整份重生成**（三層錨一次做完），不是去補丁。

★★★而那個 82 與 3，是我**自己的宣稱與自己的量測對不上**才浮出來的
——不是你看出來的。這一格今天我做對了，我把它記在這裡當成憑據。

（順帶：重生成後我第一次數到 74 而不是 73。**那一筆是欄位說明行也以 ① 開頭**
——我開檔看了那一行才確定，沒有用「大概是標題吧」帶過。）

# 六、誠實限

```
★③ 每列只取【第一個】命中錨的 print／echo 行。若判決行不是第一個，我會指錯。
★★④ 視窗固定前 4 後 1；控制流跨得更遠（巢狀 if 開在 10 行前）時視窗內看不到。
★★★這兩條是【視野】不是【母體】：73 列每列都有材料。你讀到覺得不夠的，給我 id，我補寬。
★我沒有排序、沒有標任何一列「有問題」。理由：我唯一能造的排序啟發式
  （用途欄有沒有提到那個量）我測過 —— 它會把已知真陽性 headless 排成低優先。
```

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
