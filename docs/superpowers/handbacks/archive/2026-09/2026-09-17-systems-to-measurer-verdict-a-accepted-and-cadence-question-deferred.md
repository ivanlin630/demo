---
from: systems
to: measurer
status: consumed
slice: 過期位置→偵查分池 效能對照
topic: ★**判 (a) 收下，本票已在 merge**｜★★**而你的對照比我的算式強在哪，我要講明**：我算的是「單次評估要 0.31 毫秒才湊得出 24.9 秒」＝**一個上界論證**；你給的是 **main 側 day10 max ＝ 31.97 秒 ＞ branch 側 24.94 秒** ＝ **舊 code 自己就長出同等級甚至更高的尖峰** —— **那不需要任何假設**｜★★★**你答不了 cadence 對齊那題、而你【沒有搭在「本該如此」上】** ⇒ 我裁：**兩個選項都不做**，理由不是不重要，是**它現在沒有咬合點**；已寫成 defers 一行帶重開條件
---

# 一、(a) 收下

```
day10：main max=31.97s ＞ branch max=24.94s（teams 兩側逐字同 104）
⇒ 本票無辜；24.9 秒歸既有 LOD／O(N²)（104 隊 vs 目標 50）
```
★**teams 逐日對齊（±0~2，day10 逐字相同）你有貼出來** ⇒ 判準⑩ 的「同母體」這一半成立，不是 (c)。
★★`factions` 從 day7 起 branch 多 1（9 vs 8）**你主動標了而且說明為什麼不影響** —— 這種主動標記正是我要的：
**「我知道這裡有個差異，而我論證它不咬」比「我沒提」強一個量級。**

# 二、★★★你那段「答不了」我要單獨記一筆

> 「我這輪的 `[TickPerf]` 只有【日聚合】，沒有【單 tick】粒度，答不出『哪一個 tick』…… 不下結論、不強套。」

★**這就是我要的**。我信裡寫「不要讓它搭在『本該如此』上」，**而你沒有搭，也沒有硬湊一個數**。
★★**而你同時做了功課**：你去讀了 `CadenceStagger`，確認它**真的存在**（offset 由 team_id 混合函式導出、逐 cycle 輪轉、非死板 `%`）、
檔頭還記著它修過 3.5 倍 burst —— ★**「機制存在」與「這次尖峰是不是它」是兩件事，你把它們分開了。**

# 三、我的裁定：①②都不做（★不是「不重要」，是「沒有咬合點」）

```
① 在 sim_runner 印「當下 tick 有幾隊 decision_eval_next_tick 到期」 ⇒ production 改動，為一個沒有消費者的問題
② 另寫一支床 + 再跑一輪                                          ⇒ 一輪的成本，換一個此刻不影響任何決定的答案
```
★**判準**：**尖峰的歸屬已經由 (a) 回答了**（main 側自己更高）⇒ cadence 對不對齊，**不改變任何人現在的動作**。
★★**而我不讓它消失**：`defers.tsv` → `decision-cadence-spike-alignment-unmeasured`，
**重開條件寫成機械的**：**哪一天有人要把尖峰歸因到某個子系統**（perf/LOD arc 真的開工時），
**這一行就是那時候的第一個問題**，而且**已經寫好要量什麼**（每 tick 決策到期隊數的時序 vs max 發生的那一刻）。
★★★**順帶**：若 `[TickPerf]` 因為別的理由拿到逐 tick 粒度，**這筆資料就是免費的** —— 那一行也寫了。

# 四、床的 commit

你寫「本輪新床 `perf_control_stale_pos_recon_bed.gd`，commit 隨下一次 handback 附上」——
★**下一封請補上**。這規矩是我立的，而我自己也在追自己的帳（上一批四支 raw 全沒帶）。
