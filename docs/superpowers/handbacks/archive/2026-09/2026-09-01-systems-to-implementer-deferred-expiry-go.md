---
from: systems
to: implementer
status: consumed
slice: deferred-judgement-expiry
tier: infra
topic: ★R² CLEAN(issues 小,已補誠實限)⇒ 動工;★★工具層,production 0 行:triage 輸出逐規則命中數 + 閘新增「b_defer 命中數==0 ⇒ FAIL」;★★★reviewer 找到我想不出的反例並已寫進誠實限——本閘只看得見「碰運氣被治好」那一半,「被徹底遺忘」那一半仍不可見,★別在交件時宣稱「延後判決現在會到期了」
---

# ★①動工範圍（★spec: `docs/superpowers/specs/2026-09-01-deferred-judgement-expiry-HOW.md`）
```
1. scripts/debug/bare_tick_triage.gd  輸出【逐規則命中數】（目前只有逐桶）
2. .claude/hooks/bare-tick-gate.sh    新增：★任何 b_defer 規則命中數 == 0 ⇒ FAIL
   訊息要說出【處置】：「人回來看它是【病好了】還是【regex 靜默失效】」
3. ★對帳沿用既有形狀：逐規則命中數合計 == 候選總數（不平 ＝ 有東西被靜默吐掉）
★production 0 行
```

# ★★②驗收四條（★reviewer 判定②不是同義反覆，是三角的第二個角）
```
①人造資料驗【能力】：把一條 b_defer 的 regex 改成永不命中 ⇒ 閘必須紅
②★真資料驗【正確性】：現行 main 跑 ⇒ 必須紅,且紅的正是 :50 與 :51
   ★★這一條驗的不是「:50/:51 是 0 命中」(那我已手動坐實),
     ★★★是【機器新蓋的那條計算路徑重算一次會不會得到同一個答案】(少數一條/多數一條/抓錯欄位)
③退場驗【不誤報】：把 :50 :51 退場後再跑 ⇒ 閘必須綠
④逐規則命中數合計 == 171 ⇒ 不平則 FAIL
```

# ★★★③交件時【不要】宣稱「延後判決現在會到期了」
reviewer 找到了我想不出的反例，已寫進誠實限：
> ★**「對象還在」≠「延後理由仍然有效」。**「延到里程碑 X」型的判決，X 發生後若沒人主動去改那個物件，
> **物件會原封不動留著 ⇒ 命中數依然 > 0 ⇒ 本閘照樣綠。**

★★**這次那兩條沒害人，是【剛好有人因為別的理由動了它】。**
★★★**所以本閘讓「碰運氣被治好」那一半可見了，「被徹底遺忘」那一半仍然不可見 —— 而後者才是本來要防的。**
⇒ **交件的話術照這個範圍寫。**★**我寧可它被準確地小看，也不要它被高估然後有人靠它。**

# ★④退場那票仍是分開的
★**不要順手把 :50 :51 退場**（驗收③只是【暫時改一下驗給我看】，不是 merge 掉它們）。
★★退場票的硬條款＝**必附目標常數現況的 file:line** —— **0 命中有兩種讀法。**
