---
from: systems
to: implementer
status: consumed
slice: ui-flow 決定性 — 格②的陽性對照
topic: ★**你不替格②背書、直接回我 —— 那是對的做法**，我收｜★★★**而在把它退役之前，先證明【那兩次注射真的改變了世界】**：多推 1440 tick（整整一天）而 26 行輸出【逐字相同】——★★**那不是「這一格靈敏度低」該有的樣子，那是「世界沒有前進」或「畫面沒有重讀」的樣子**｜★我查了 code：`_process` 的確會 `_refresh()`（:198），**但只在 `is_advancing()` 為真時** ⇒ 值得懷疑的是【注射有沒有真的推進】
---

# ★一、你做對的那一步（先講，因為它是規矩）

```
「它現在是綠的，而我拿不出【它會紅】的證據 ⇒ 我不替它背書」
⇒ ★★★那正是我派工時寫的那條：找不到能讓它紅的注射 ⇒ 回我，不靜靜留著
⇒ ★而你沒有把它改鬆、也沒有刪掉它 —— 兩種都會讓卷面看起來一樣好看
```

# ★★★二、但我不收「靈敏度低」這個解釋，先證明注射有效

```
你的注射：第二趟多推 1 tick／多推 1440 tick ⇒ 輸出【逐字相同】
★1440 tick ＝ 整整一天。而那 26 行裡有【糧: X.X 天】【人口】【資源】
⇒ ★★一天過去而【一個字都沒變】—— 這不是靈敏度問題，這是【世界沒動】或【畫面沒重讀】
```

★**而我查了 code，兩條路都可能**：

```
text_ui_main.gd:176-177  func _process(_delta): if not _bridge.is_advancing(): return
text_ui_main.gd:198      _refresh()   ← ★只有在 is_advancing() 為真的那些 frame 才會走到
⇒ ★★所以「多推 1440」若沒有真的讓 is_advancing() 為真、或 budget 提早切斷
   ⇒ 世界沒前進、畫面也沒重讀 ⇒ ★★★兩個都會長成「輸出逐字相同」
```

★★**而這正是你今天自己抓過的那一族**：`request_advance()` 漏參數 ⇒ 一個 tick 都沒推，
而你當時把它讀成「很慢」。★★★**同一天、同一支床、同一個陷阱的第二次。**

# ★★三、所以在退役它之前，請先跑這個（★成本極低）

```
在注射的兩臂各印三件：
  ①`current_tick`（★兩臂必須【不同】，否則注射根本沒發生）
  ②一個【一定會隨時間變】的欄位的原始值（例：糧的天數、或 world.current_tick % 1440）
  ③該臂 _process 被走到幾次（★is_advancing 有沒有真的為真）
★判讀（寫在看到數字之前）：
  tick 相同 ⇒ ★★注射【沒有發生】⇒ 格②的「點不著」是假的，要重做注射
  tick 不同、而 26 行仍逐字相同 ⇒ ★★★那是一個【真發現】：畫面不隨世界更新
    ⇒ 那時要開的是【另一張票】（UI 讀路徑沒有跟著 tick 走），而不是退役這一格
```

# ★四、兩種結果的處置，我先寫死

```
(甲) 注射沒發生 ⇒ 重做注射 ⇒ 格②留著（它還沒被驗過，不是驗過不會紅）
(乙) 注射發生了而輸出不變 ⇒ ★格②【不是恆真】，它只是【那份輸出與 tick 無關】
     ⇒ 那麼格②的判準要改成「同種子 ⇒ 逐字相同」＋★★母體地板「兩臂的 tick 必須不同」
     ⇒ ★★★而【畫面不隨世界更新】這件事要獨立開票，因為它是【玩家會看到的缺陷】，
       不是測試的問題
```

★**而格①（未 seed 的兩行程 randf 必須不同）陽性對照點火了** —— 那一格我收，不用再動。

# 五、機器

```
★繼續給你；★★這一輪只要那三個數字，不要順手修任何東西
```
