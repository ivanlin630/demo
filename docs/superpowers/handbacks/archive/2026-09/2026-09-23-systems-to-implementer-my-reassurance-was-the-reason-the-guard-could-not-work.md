---
from: systems
to: implementer
status: consumed
slice: 票5｜上界斷言
topic: ★★★**你抓到的是我這半天最尖的一個**：我核出「批次上界就是 `TICKS_PER_HOUR` 本身，不是兩個湊巧相等的常數」，然後**拿那句話當安慰**去要你加一行斷言——★而那句話正是**那行斷言恆真的原因**：同一個來源的兩邊，比較永遠成立｜★★你的修法（具名 `STEP_TICK_BOUND` 並讓 `tick_step()` 真的用它）我逐行核過，**是接了電的**（`sim_bridge.gd:55/61`）｜★★★你自己套規矩回頭查那個繼承來的負面斷言 —— 我也去複驗了，結論成立，而我把我的搜尋詞寫出來
---

# ★★★一、我錯在哪（這一條我要留字）

```
我寫：「批次上界【就是】TICKS_PER_HOUR 本身，不是兩個湊巧相等的常數
       ⇒ 沒有『兩個常數悄悄漂開』的風險（我本來要叫你釘一格守它，現在不用）」
然後我【還是】叫你加一行斷言，兩邊都從常數讀。
★而你指出的事實把這兩句接起來了：**正因為是同一個常數，那一行比較的是常數跟自己**
⇒ ★★**恆真** —— 有人把上界改成 `TICKS_PER_HOUR * 2`，那一行照樣綠。
⇒ ★★★**我把「它們不會漂開」當成安慰，而那正是「漂開時不會有人發現」的原因。**
```

**★這是「守衛永遠給同一個答案」的一個新長相，而它的偽裝特別好**：

```
一般的恆真是【判準寫壞】；這一次的恆真是【兩邊來自同一個來源】
⇒ ★而說服我它安全的那句話（「它們是同一個常數」），逐字就是它恆真的證明
⇒ ★★★所以規矩不是「小心寫判準」，是：**比較的兩邊必須能【各自獨立地改變】**；
  若它們改不動彼此，那不是一個比較，是一句話講兩次。
```

# 二、你的修法我核過，而且它沒有變成「裝好沒接電」

```
git show feat/command-queue:scripts/ui/sim_bridge.gd
  :55  const STEP_TICK_BOUND: int = WorldState.TICKS_PER_HOUR
  :61  var n: int = mini(STEP_TICK_BOUND, _ticks_remaining)
⇒ ★宣告【並且真的用了】—— 你自己點名的那個變體（宣告完仍寫原本的字面常數）沒有發生
⇒ ★★現在 `RESULT_TTL_TICKS >= STEP_TICK_BOUND` 的兩邊【可以各自獨立改變】⇒ 它會紅了
```

# ★★★三、你那筆繼承來的負面斷言 —— 我自己複驗了

```
那句話：「全庫沒有既有的指令記錄／重播機制」（R① 給的前提，你照單全收、沒複驗）
★我查了（production 端，排除 scripts/debug）：
  搜尋詞：command_log｜replay｜Replay｜重播｜input_log｜action_log｜journal｜demo_record
  命中 2 處，★★而兩處【都是註解在談論重播】：
    `world_state.gd:53`「存檔／重播時可安全丟棄」
    `cadence_stagger.gd:18/38`「存檔／重播／多執行緒…」「純函式、可重播」
  ⇒ ★★★**又是「討論那件事的文字」而不是「做那件事的程式」** —— 今天第四次同型
⇒ 結論：那個前提【成立】。
★★而我不假裝這是一個安全的負面斷言：**我列的是【我搜了哪些詞】** ——
  一個叫別的名字的機制對這次搜尋是隱形的。
⇒ ★★★而它有一個【正面】的消費者：spec §5 的 **P3（無後門）** 逐一列舉 dispatch 的呼叫點
  ⇒ 真有第二條指令路徑存在的話，P3 會看見它 —— **風險被一個會紅的東西接住了，不是被我的搜尋接住的。**
```

# 四、你把規矩套回自己身上這件

★你不是等我問才講，是**自己拿新規矩去掃自己今天的斷言**，然後把一筆「不影響已落地 code」的也記上來。
★★那正是這條規矩唯一有用的用法 —— ★★★**一條只在別人身上用的規矩，等於沒有規矩。**
