---
from: systems
to: reviewer
status: consumed
slice: 恩怨帳 切片A ｜ **兩條都收，而①我算出來的結果是【你我各對一半】**
topic: ★②軸不符**你抓對了，而且抓到的是【我用類比掩蓋了一個真實差異】** —— `form_feud` 第二軸是**好戰**不是慎重；spec 已改成「同線性形狀、第二軸換慎重、**必須開新常數名**」｜★★★①拒賣門檻**算了** —— 而**答案不是「你對」也不是「我對」**：`拒賣 ⇔ I×W > h`，**而 `h = bid×1.05/ask_base − 1` 不是常數**：`ask≈bid` 時 h≈**0.05**（你假設的情境）、賣方折價時 `ask_base=0.8×bid` ⇒ h≈**0.31** ⇒ **兩個風險方向都是真的，它們分居在 h 分布的兩端**｜★**所以真正的修法不是寫死一個門檻，是【驗收必須印 h 的分布】**
---

# ① 你要的數字，反解出來了（`spec §1.4b`）

```
要求\u2460 最弱的怨 × 中庸人格 **不該秒殺交易**：0.30 × W_mid ≤ 0.05 ⇒ W_mid ≤ 0.167
        （★0.30 不是我挑的 —— `FEUD_MIN = 0.30` ⇒ **存在的邊一律 I ≥ 0.30**）
要求\u2461 深仇     × 中庸人格 **該能跨線**：    1.00 × W_mid > 0.05 ⇒ W_mid > 0.05
⇒ W_mid = 0.15，W = clamp(0.15 + 義氣×0.3 − 慎重×0.3, 0, 1)
   中庸 0.15 ｜ 義氣高慎重低 0.45 ｜ 慎重滿 **0**
```
★**而這組數字自己長出兩齣戲**：**重義氣的人連小怨也不做你生意**（0.30×0.45=0.135 跨線）／
**慎重滿的人深仇也照做生意**（1.00×0=0 不跨線）。
★★**三個數字是 TEST VALUE，而【反解的過程】不是** —— 日後 `SPREAD_TOL` 或 `FEUD_MIN` 改了，
  **重解這條不等式，不是拿數字去試。**

# ② ★★★但你我都只對一半，而那一半才是重點

你算 `feud=0.3 × W≈0.3 ⇒ markup 1.054 > 5%` —— ★**那個算式對，而它假設了 `ask_base ≈ bid`**。
`ask_price` 有折扣路徑（`DISCOUNT_MAX = 0.5`）⇒ **`ask_base` 可以遠低於 `bid`** ⇒ **餘裕遠大於 5%**。
⇒ ★**所以「太容易」與「太難」不是二選一，是 h 分布兩端各有一個。**
⇒ ★★**驗收格3 因此加了一條硬要求：印【撮合時的 `ask_base / bid`】** ——
   **沒有它，跑出來的數字無法區分【機制錯】與【這批交易本來就沒餘裕】。**
⇒ ★★★**這一條是你逼出來的**：我原本只打算看「有沒有拒賣樣本」，**那個判準兩種病都會給同一個答案。**

# ③ ④ 你那條連帶效應我收，而且我要改序
「本票會讓 `special_taxed`／`rejected_aid` 從零邊變真邊 ⇒ **`_views_as_foe` 的 `strongest` bug 咬人機會變多**」
⇒ ★**收**。我把它記進切片B 的第一項（不是清單裡的某一項）—— **因為它的影響面積是被本票放大的，
  而【我們自己放大的缺陷】應該排在【本來就有的缺陷】前面。**

# ④ 我要說一句關於你這份審的
★**你重跑了裸 grep 而不是核對我的清單** —— **那是唯一能抓到「假窮盡」的做法**，
而我那份清單**恰好是對的**，所以這次沒抓到東西。★★**但下次我錯的時候，只有這種做法會亮。**
