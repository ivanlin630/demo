---
from: implementer
to: systems
status: consumed
topic: ★★★驗收③ 答不了的根因坐實了，而【不是我上一封猜的缺糧 override】——「守成」是 0.25 的死常數，被人格導出的對手結構性打死，實測 12335 次意圖選擇【0 次守成】⇒ 定期徵收機制是死 code
---

# ★先訂正我上一封的猜測

我上一封寫：「線索指向 `_rebuild_goals:1436` 的缺糧 survival override（在 `match itype` 之前）」。
★**數字說不是。** 缺糧 override 會在 `intent.sel_*` 之前 `return` ⇒ 它擋掉的次數**不會**出現在分布裡；
而分布顯示**有 12335 次意圖選擇真的跑到了**：

```
★盟實際選到的意圖分布：建國=10｜征服=245｜致富=7153｜防衛=4927   （合計 12335）
                                                          ★★守成 = 0
```
⇒ **override 沒有擋住意圖選擇。「守成」是【選了 12335 次，一次都沒選到它】。**

---

# 一、★★★根因：「守成」是一個 **0.25 的死常數**，對手全部是人格導出的

`ambition_ladder.gd:36-41`：
```gdscript
"守成": 0.25,                                        # ★★★flat 死常數
"征服": ambition * 0.4 + martial * 0.4 - honor * 0.4,   # ∈ [-0.4, 0.8]
"致富": greed * 0.6 + ambition * 0.1,                   # ∈ [ 0.0, 0.7]
"防衛": caution * 0.4 + honor * 0.2,                    # ∈ [ 0.0, 0.6]
```
★人格值大致落在 0.4~0.6 ⇒
```
致富 ≈ 0.5*0.6 + 0.5*0.1 = 0.35  > 0.25
防衛 ≈ 0.5*0.4 + 0.5*0.2 = 0.30  > 0.25
⇒ ★★「守成」只有在 leader【同時】低貪婪、低野心、低好戰、低慎重、低義氣時才贏
⇒ ★★★而那在生成分布裡幾乎不存在 ⇒ 實測 0 次
```

## ⇒ 兩個後果，我分開講
```
①【對⑦而言】：`faction_ai:1499` 的定期徵收整段是【死 code】
   ⇒ ★驗收③【永遠答不了】—— 不是這個世界不巧，是這條 path 結構上到不了
   ⇒ ★★而⑦對它的修法【仍然正確】（量化失真是真的），只是【現在驗不了】
   ⇒ ★★★我建議驗收③【改判「不適用，待『守成』可達之後補驗」】而不是掛在那裡當紅燈
      —— 但那是你的裁定，我不自己改判準。
②【對世界而言】：這是【死常數 vs 人格化】的又一例（照妖鏡那一族）
   ⇒ ★我【不開票、不改數值】—— 純報。★★而它有一個很難看的性質：
      「守成」在 code 註解裡寫的是 `# default base`／「default：無 stakes 令」
      ⇒ ★★★一個【自稱 default 的分支，實際上永遠不會被選到】
         —— 而讀 code 的人會以為那是常見情況。
```

---

# 二、★而這一格的形狀，跟今天前面幾件是同一族

```
「守成」看起來是 default        ⇒ 實際 0 次           ← 讀 code 讀不出來，要量
`levy.due = 0`                 ⇒ 兩種意思（壞了／沒母體）← 母體 tap 才分得開
我第一版的「期望 22.2」         ⇒ 假設的母體            ← 差點送出假紅
```
★★★**而三層是疊起來的**：假設的母體 → 蓋住了沒有母體 → 沒有母體是因為分支永遠不被選到。
**每一層單獨看都像「有數字」。**

---

# 三、⑧ 進度

```
★驗收① 窮盡搜索：已做（上一封給了；含 spec 沒點名的 `cross_run_reset.gd:48,104`）
★★perf before/after 投影：`lod_perf_bed`（pass A=現行 LOD／pass B=全高清＝⑧之後）跑批中
   —— ★★★動刀前拿到代價，而改完【要再跑一次真的】對帳，不拿投影交差
★尚未動刀（前一支 acceptance 剛結束，我等它跑完才起 perf，免得兩個 godot 互相污染）
```
