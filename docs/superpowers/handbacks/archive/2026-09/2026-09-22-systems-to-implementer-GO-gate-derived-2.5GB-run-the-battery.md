---
from: systems
to: implementer
status: consumed
slice: ★★★**門檻推導出來了：`MEM free ≥ 2.5GB` —— 而現在是 4.95GB ⇒ 跑電池**
topic: ★**推導**：`233MB（實測，1 天窗）× 4（外推到 12 天世界，★未量、明寫）× 2（OS／其他行程波動）≈ 1.9GB ⇒ 取 2.5GB`｜★★**我的 6GB 被降掉 2.4 倍，而降它的是你那 233MB** —— ★**一個便宜的量測，解開了整條鏈**｜★★★**序**：**電池 ⇒（綠）閘陽性對照 ⇒ merge ⇒ 世代 7 上跑 B3**
---

# 一、★門檻的血統（★寫在這裡，讓下一個人不用重猜）

```
**233.2 MB** ＝ **實測**（1 天窗世界床，兩個 Godot 行程加總；單行程 229.7MB）
**× 4** ＝ ★**外推**到 12 天窗（世界長到約 100 隊）—— ★★**這一步【沒有量】，我明寫**
**× 2** ＝ 留給 OS／其他行程波動（★我原本那條倍率規則）
⇒ **≈ 1.9GB ⇒ 取 `2.5GB`**（★取整，且再留一點）
⇒ ★★★**而我的 6GB 是它的 2.4 倍** —— **降它的不是我的判斷，是你那個 233MB。**
```

# 二、★★★而下一個人要怎麼把那個「×4」換掉

```
★**電池跑的時候，讓最重的那支床【自己印峰值】**（★增量輸出，照今天那條）
⇒ ★★**下一輪就能用【量到的 12 天峰值】取代這個外推** ⇒ **門檻再收一次**
⇒ ★★★**而這正是我要的形狀**：**外推寫在卷面上，等著被一個實測取代** ——
   **而不是藏在一個「看起來很安全」的數字裡。**
```

# 三、★★序（★逐步，每步印狀態）

```
①**跑 merge 電池**（68 支）⇒ ★跑前印 `godot-busy` 與 `MEM free`
②**綠 ⇒ 在 main 上跑一次閘的陽性對照**（★branch-local 的閘跑不算 merge gate）
③**merge**（★而 merge 前用 `git diff main...` 看【刪除那一半】—— 我今天漏過一次）
④**世代 7 上跑 B3**（`freeze_sample_bed` 12 天 × 兩顆）⇒ `>2s ≤ 1／日 且 p99 < 1s`
   ⇒ **不過 ⇒ (乙) 分片 defer 門票觸發**
★**任何一步被 harness 砍 ⇒ 判【不可判】不是【紅】**，且**卷面要留得下部分結果**。
```
