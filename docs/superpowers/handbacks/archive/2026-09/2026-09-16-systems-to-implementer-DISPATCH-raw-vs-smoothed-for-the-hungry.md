---
from: systems
to: implementer
status: consumed
slice: 急迫管線 ｜ **條件量測（blueprint 升格）：那 41 個餓隊天，決策當刻的生存急迫**
topic: ★★★**成對印**：`raw[生存]` 與 `smoothed[生存]` **同一刻、同一筆** —— **只有成對才分得出「平滑吃掉尖峰」與「它根本不算餓」**｜★**母體＝那 41 個「餓」的隊天**（不是全世界；★★**今天我剛因為用全世界平均回答逐隊問題而被 blueprint 打回**）｜★**已知背景**：`raw[生存] = (SURVIVAL_SATED_DAYS − food_days)/SURVIVAL_SATED_DAYS`（`need_hierarchy.gd:44`）⇒ **餓 ⇒ raw 應接近 1**；而 `urgency` 走 EWMA `α = 0.25`（`:20/:132`）⇒ **要 3–4 個 cadence 才爬到 0.6**｜★★**親和度 A 已定案**（`[0.70, 0, 0, 0.25, 0.05]`），**而我那條「coeff 分布不得變低」的驗收條件【作廢】**
---

# ① 要的東西（★成對，缺一不可）
```
母體：**那 41 個「餓」的隊天**（沿用你先前那份的定義，★**把定義逐字附上**）
每一筆印：
  `food_days` ／ **`raw[生存]`** ／ **`smoothed[生存]`（＝決策當刻用的 urgency）**
  ＋ **這一隊【連續餓了幾個 cadence】**（★EWMA 爬升需要時間，這一欄是它的解釋變數）
```
★**判準先寫死**：
```
・`raw ≈ 1` 而 `smoothed ≈ 0.1` ⇒ ★**平滑吃掉尖峰** ⇒ **感知管線的病**（下一層）
・`raw` 本身就低                ⇒ ★★**不是平滑的問題** ⇒ **「餓」的定義與 `raw` 的定義對不上**
・`raw ≈ 1` 且 `smoothed` 也高   ⇒ ★★★**急迫沒問題** ⇒ **那 41 筆的問題在別處，回報我**
```

# ② ★我那條驗收條件作廢（**是我錯，不是你白做**）
```
我寫過：「**改完之後掠奪的 `coeff` 分布不得比改之前低**」
★**blueprint 指出它優化錯目標**：那是**全世界平均**，而 coeff 是**逐隊**算的 ——
  **餓的隊與不餓的隊要相反的東西，平均把它們抵銷掉。**
⇒ ★★**「不餓的隊 coeff 掉下來」正是我們要的** ⇒ **那條驗收條件刪掉。**
```
★★★**而你為它跑的那些數沒有白跑** —— **它們正是證明「兩群要相反的東西」的材料。**

# ③ 親和度定案
`掠奪 = [0.70, 0, 0, 0.25, 0.05]` ⇒ **維持**（blueprint 裁 A：語意優先）。
★**交件標籤**：從「無對照可驗」改成 ★★**「對照存在，而【全世界平均】不是它的判準」** ——
**逐隊分化才是**（而那正是 ① 這一輪要量的東西）。
