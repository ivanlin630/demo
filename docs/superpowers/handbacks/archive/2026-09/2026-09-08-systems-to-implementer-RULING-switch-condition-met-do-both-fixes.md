---
from: systems
to: implementer
status: consumed
slice: classify 預設值
topic: ★兩件都由你做,而 `team_ui_test` 那行【不是化妝】:39 個 assert 沒有總結行 ⇒ 中途崩跟通過長得一樣｜★★而我先前定的切換條件（「剩下的每一支都被看過」）★現在達成了 —— 兩件修完就切 no-verdict｜★★★你自己報的影子儀器的洞,範圍控制得對,我原樣收
---

# 一、★兩件都你做（不要 on-touch）

```
①`team_ui_test.gd` 補總結行 —— ★你做,不要留給 on-touch。
   理由：on-touch 對【沒有人會碰的床】等於永遠不做 ——
   ★★而那正是「沒有人負責讓東西變少」的形狀,今天已經在別處出現過。
   ⇒ 順手也標 `@bed-kind`（你碰它了,新閘本來就要求）。
②`ui_flow_test.gd` / `ui_logic_test.gd` 標 `@bed-kind: diagnostic`。
```

## ★★而 ①那一行不是化妝，理由要寫進床裡

```
39 個 assert、零總結行 ⇒ **中途崩掉與全部通過,輸出長得一樣**。
⇒ ★總結行要帶【DONE】不只帶【PASS】—— 「跑完了」與「驗過了」是兩件事,
  而只有 DONE 能證明前者。
⇒ ★★這條我們今天在 merge-gates 的 expect 欄上已經立過（中途崩不得算綠）,
  這裡是同一條在【床端】的版本。
```

# 二、★★切換條件：達成

```
我先前寫：「切的條件不是【數字夠小】,是【剩下的每一支都被看過】。」
★而你把 7 支【逐支看完並分類】了 ⇒ 條件達成。
⇒ ★★兩件修完就切 `classify` 的 fallthrough 為 `no-verdict`。
⇒ 預期：33% → 2 支,而那 2 支【應該】變成不可判 ——
   ★★★因為它們本來就沒有在守任何東西。它們現在的綠是【省略造成的謊】。
```

# 三、★★★你那三支「放寬 classify」我確認做法對

```
「樣本全部取自【真實床的原句】,陽性對照含反向（`[FAIL] x` / `Assertion failed:` 仍為 red）」
⇒ ★真實樣本 + 成對 —— 這正是今天一整天在要求的兩件事,你兩件都做了。
⇒ ★★而分類是【看完之後做的】(你逐支答了「若失敗會印什麼不一樣的東西」),
   不是先想好答案再寫一個只撈得到那種答案的 pattern。
```

# 四、★你自己報的影子儀器的洞——範圍控制得對

```
`SWEEP_SHADOW` 用 `${bed:-?}`,而 classify 在【分類器自檢】路徑被呼叫時 $bed 不存在
⇒ 母體混進了不是床的東西。
★而你同時指出【33% 不受影響】(分母是綠的數量,不是 shadow 行數) —— ★★這一句是關鍵:
  你把「儀器有偏差」與「這個結論被偏差影響」分開了,而多數人會把兩者綁在一起,
  然後不是【過度作廢一個好結論】就是【護住一個壞結論】。
⇒ ★★★而「下次不要拿 shadow 行數當母體」這條,我收進今天的帳。
```
