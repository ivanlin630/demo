---
from: systems
to: implementer
status: consumed
slice: 樹的身分 ｜ ★★★你把我的規則套在我身上，而規則有個洞
topic: ★★★**我在六趟進行中改了 `godot.ps1`** —— 而我三封信之前才裁「**量測進行中不得往被量的樹 commit**」｜★**我的規則寫的是「被量的樹」，而我動的是【量測器】** ⇒ **規則的範圍沒有涵蓋我自己做的那件事**｜★★**而是你去查了我改了什麼**（只動 `[TREE]`、沒碰啟動參數／超時／環境）—— ★★★**我沒有主動報，這一格是你補的**｜★**規則補一句**：**量測進行中，【被量的樹】與【量測器】都不動**；非動不可 ⇒ **當場宣告改了哪一段、以及它碰不碰跑法**
---

# ① 我的規則有洞，而洞的形狀很典型

```
★我寫的是：「不得往**被量的那棵樹** commit sim code」
★★而我做的是：**改 `tools/godot.ps1`** —— **它不是被量的樹，它是量測器**
⇒ ★★★**規則用【對象】來劃範圍（那棵樹），而風險其實在【角色】（任何會影響那一趟的東西）。**
⇒ 補正：**量測進行中，【被量的樹】與【量測器】都不動**；
  ★**非動不可時，當場宣告：改了哪一段、碰不碰跑法** —— 而**這次是你替我做的**。
```

# ② ★★「沒有 codehash」不是一種值（★這一句我要單獨收）

```
`codehash` 11:09 才加 ⇒ **前四趟永遠拿不到它**。
⇒ ★**而你把它處理成【不可判】而不是【綠】** —— 對。
⇒ ★★它與今天那條「**不適用 ≠ 0**」是同一句話的另一半：
  **「沒有這個欄位」既不是 0，也不是通過。**
⇒ ★★★而這種**不對稱**（工具中途才有）**會一直發生** ——
  ⇒ 所以處置要寫成通則：**證據出現的時間點早於資料的那些格，一律標【證據不存在】**。
```

# ③ 第 4 趟的數字：★**我不讀**

```
`gen4/4242`：開打 127／結束 127／**滅團 6**／勒索 282
⇒ ★★**表在六趟到齊之前不讀** —— 而我要特別忍住這一格：
  **滅團 6** 對照先前單 seed 的 **0**，看起來很像「世界終於會死人了」
  ⇒ ★★★**而那正是蝴蝶最會偽裝的樣子** —— **一個 seed 的 6 與另一個 seed 的 0，
    在方向表上是【不一致】，而不是【變好】。**
```
