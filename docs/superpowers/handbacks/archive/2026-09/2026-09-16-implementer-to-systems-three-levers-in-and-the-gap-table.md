---
from: implementer
to: systems
status: consumed
slice: 常態掠奪票 ｜ **三拉桿全接（含軸對齊）**＋★★★**gap 逐項分解表**（照 §5：接完仍不夠就交表，不 crank）
topic: ★**驗收床八格全綠**；★★**存在性格在 `f = 8000` 成立**：尾部（好戰.9／貪婪.9）掠奪 **0.2244 ＞ 紮營 0.1984 ⇒ 首選＝掠奪**，而**單高（.9/.5）0.1516 與中庸（.5/.5）0.0738 仍不搶** ⇒ **§3 的格1＋格2 同時成立**｜★★★**但在「合理富裕」那一檔（`f = 1500`）仍差 2.38 倍** ⇒ **交 gap 表**：①`w_wealth` **＋40%**／③雙高 `d` 0.90→0.94 **＋4.4%**／②主觀成本 **−1.8%** ⇒ **合計 ×1.436（0.0580 → 0.0833）**｜★**而最值得看的一格**：**接線前，貪婪對【掠奪】完全沒有作用**（舊軸是 好戰／殘忍）⇒ 尾部與單高**接線前逐字同分（0.0580）**｜★換軸當場弄紅了我自己的絕境格（它的「兇者」寫的是舊軸）—— **已改**
---

# ① 三拉桿（commit `5bec468f2`）

```
①**戰利品的未來價值**：`take × DiscountedFlow.flow_weight("wealth", values)` ＝ `0.5 + 貪婪`
   ★**權重不是加成**：不改變「搶得到多少」，只改變**這個人有多在意那筆錢**（中性 .5 ⇒ ×1.0 逐字不變）
   ★★**兩邊一起接**（掠奪＋攻擊）
②**主觀成本**：`× (1 − retaliation_risk × (1 − 兇性容忍))`
   ★`retaliation_risk` ＝ **直接呼 `ThreatAssessment._power_ratio`**（不另起一份 —— 它已修過「技能維不對稱」）
   ★★在 `gather` 算並存進 ctx（`eval` 拿不到 `state`／`team`）
   ★★★**「道德折價」那一半沒有接**，寫在 code 裡讓「沒做」可被看見
③**雙高加成**：`d = hi + (1 − hi) × max(lo − 0.5, 0)`；**三處用同一個 `d`**（`person`／兇性容忍／兩個動詞）
★**軸對齊**（blueprint 裁）：掠奪 `person` 由 `max(好戰,殘忍)` ⇒ **`max(好戰,貪婪)`**；
  殘忍**只留結算層** `effective_loot_rate` ⇒ ★★**「殘忍出現兩次不是雙計」現在更強：它只出現一次。**
```

# ② ★★★gap 逐項分解表（**常態、鄰居 pop 3、`f = 1500`、尾部人格 .9/.9**）

```
基準（接線前，同一組人格）      ＝ **0.0580**
  ★**而這個基準本身就是一個發現**：接線前**尾部（.9/.9）與單高（.9/.5）逐字同分** ——
    ★★**因為舊軸是 `max(好戰, 殘忍)`** ⇒ **貪婪對【掠奪】完全沒有作用。**

① `w_wealth` ＝ 0.5 + 貪婪(.9) ＝ **1.40**            ⇒ ×1.400   **＋40.0%**
③ 雙高 `d`：0.900 → **0.940**（person 逐字等於 d）    ⇒ ×1.044   **＋4.4%**
② 主觀成本：`pr 0.30 × (1 − d 0.94)` ＝ **0.018**      ⇒ ×0.982   **−1.8%**
────────────────────────────────────────────
合計 ×**1.436** ⇒ **0.0580 → 0.0833**（實測值逐字相符）
對手 紮營 ＝ **0.1984** ⇒ ★**仍差 2.38 倍。**
```
★**照你 §5：我交表，不調係數。**

# ③ 而三檔身價一起看，**它其實會 fire —— 在更肥的鄰居上**

```
f = 240（TARGET×8）  ：尾部 0.0165｜單高 0.0111｜中庸 0.0054 ⇒ 全部紮營
f = 1500（×50）      ：尾部 0.0833｜單高 0.0563｜中庸 0.0274 ⇒ 全部紮營
f = 8000（×267）     ：**尾部 0.2244 ⇒ 首選＝掠奪** ✅｜單高 0.1516 ❌｜中庸 0.0738 ❌
```
⇒ ★★**`f = 8000` 那一檔【同時滿足】spec §3 的格1（尾部會搶）與格2（中等仍不搶）** ——
★★★**而它的代價是「合理富裕」那條線要放到 `TARGET × 267`。**
⇒ **那條線在哪裡是 WHAT** ——
★**我不自己挑**（今天已經有一次「數字對、故事荒謬」被你跟 blueprint 一起否決）。
⇒ **若你要那兩格進床，告訴我用哪一檔；若 8000 太肥，那就是「接完仍不夠」⇒ ②的表就是答案。**

# ④ ★換軸當場弄紅了我自己的驗收格（**我把它記在 code 裡**）

```
絕境格的「兇者」原本寫 **殘忍 .9** ＝ **舊軸** ⇒ 換軸後首選變回乞食 ⇒ **格8-c 紅**
⇒ 已改成 **好戰／貪婪 .9**（動機軸）⇒ 綠（掠奪 4.3445 vs 乞食）
★★**而這正是我今天一直在講的那個病，這次落在我自己的格上**：
  **軸換了之後，寫著舊軸的那一格【當場失去鑑別力】** —— 而它是【紅】的那種，所以我看得見；
  ★★★**如果它剛好還是綠的，我就不會發現。**
```

# ⑤ 現況

```
`feat/raid-expected-value` @ **`5bec468f2`**（工作區乾淨）｜驗收床 **八格全綠**
★掠奪票的 56 支**最後一輪已跑過**（`FAIL：bed-arm defer-open` ＝ 基線＋鬧鐘，`headless` 已轉綠）
  ⇒ ★★**但那是三拉桿【接線前】的那一輪** ⇒ **要再跑一輪**（接完之後的）
`feat/grudge-ledger-a` @ `20e57dccd`：`取 max intensity` 已改 ⇒ ★**它的 56 支也要重跑**
⏳ 等你：**③那條「合理富裕」的線**（f = 1500 還是 8000）
```
