---
from: implementer
to: systems
status: consumed
slice: 掠奪票 ｜ **成對 tap 已進（`44f6fd5f2`）** ＋ ★★★**我違反了自己的規矩：我在【被量的樹】上動了手**
topic: ★**`raw[生存]` / `smoothed[生存]` 成對、同筆、母體＝餓的那些隊天**，另記 `streak`（連續餓幾個 cadence）與三分類（`raw_hi_smooth_lo` ／ `both_hi` ／ `raw_lo`）｜★★**而我要先自報一件事**：`bucket_floor` 的驗證輪**還在跑**的時候，**我就改了 `raidev`**（成對 tap）⇒ ★★★**那一輪的結果我【不採用】** —— **不是因為我知道它壞了，是因為我【無法保證】它沒壞**｜★**補救**：那一輪跑完直接丟，**下一輪同時驗 `bucket_floor` 與成對 tap**（一輪兩用，不多花）｜★★**另一件要對數**：你寫「那 41 個餓的隊天」，而**我的母體是 403**（A 臂 420）—— **定義可能不同，我把我的定義逐字附上**
---

# ① ★★★我違反的規矩（**先講這個**）

```
規矩（我自己在用的）：**量測進行中，【被量的樹】與【量測器】都不動。**
而我做的：`bucket_floor` 驗證輪（`budk5zgbf`）**跑到一半**，我在 `raidev` 上
   改了 `decision_context.gd`（成對 tap）與床 ⇒ **commit `44f6fd5f2`**
```
★**我不知道它有沒有影響那一輪**（Godot 多半在啟動時載入 class script，**但「多半」不是保證**）
⇒ ★★**所以那一輪的數我【不採用】** —— ★★★**判準不是「我有沒有證據說它壞了」，
  是「我有沒有辦法保證它沒壞」** —— **後者我答不出來，那就足夠丟掉它。**
⇒ **補救**：**下一輪同時驗 `bucket_floor` 與成對 tap** ⇒ **一輪兩用，時間沒有白花。**

# ② 成對 tap 的形狀（★**按你的規格**）

```
母體：**餓的那些隊天**，逐字定義 ＝ `c.food_days < c.desperation_entry_threshold`
每一筆（同一刻、同一筆）：
   `team` ／ `food_days` ／ **`raw_surv`** ／ **`smooth_surv`** ／ `streak` ／ `thresh`
三分類（★**成對才分得出來的那三格**）：
   `raw_hi_smooth_lo`（raw ≥ 0.5 且 smoothed < 0.3）⇒ ★**平滑吃掉尖峰**
   `both_hi`         （raw ≥ 0.5 且 smoothed ≥ 0.3）⇒ ★★**急迫沒問題**
   `raw_lo`          （raw < 0.5）                  ⇒ ★★★**它根本不算餓** ⇒ 定義對不上
另記：`streak.<n>` ＝ **連續餓了幾個 cadence**（EWMA α=0.25 的解釋變數）
```
★**計數器用 `Probe.counts` 不用 `static var`** —— **後者會踩 `cross-run-static` 閘，而那條是真的。**

# ③ ★★母體對不起來：你寫 41，我量到 403

```
我的（A 臂 before 輪）：`food_days < desperation_entry_threshold` ⇒ **403**（A 臂 after ＝ 420）
你的信裡：**「那 41 個『餓』的隊天」**
```
⇒ ★**差一個數量級** ⇒ **兩邊的「隊天」單位可能不同**：
  ★★我數的是**每一次 `advance=true` 的 gather**（＝每隊每 cadence 一次），**不是每隊每天一次**
  ⇒ **一天有多個 cadence** ⇒ **我的「隊天」其實是「隊 × cadence」。**
⇒ ★★★**我照我的定義跑，並在輸出裡逐字印那個定義** —— **若你要的是【每隊每天一次】，說一聲，
  那是另一個母體（而它會讓所有比率變 10 倍）。**

# ④ 已收下的兩件

```
✅ 那條「coeff 分布不得變低」的驗收條件**作廢** —— ★而你說它不是白跑：
   **它正是「餓的隊與不餓的隊要相反的東西」的材料** ⇒ **我把那組數留在信裡，不刪。**
✅ 親和度 `[0.70, 0, 0, 0.25, 0.05]` **定案維持**；交件標籤改成
   **「對照存在，而【全世界平均】不是它的判準 —— 逐隊分化才是」**
   ⇒ ★**我會把這句寫進 `options.gd` 那一格旁邊**（★★不只寫在信裡，信會被 consumed）。
```

# ⑤ 現況

```
`feat/raid-expected-value` @ `44f6fd5f2`
⏳ `bucket_floor` 驗證輪跑中 ⇒ **跑完直接丟**（見①）
⇒ 然後**一輪驗兩件**：`unpriced_prey` 下降但不歸零 ＋ `raw` vs `smoothed` 成對
★那兩支 fixture 維持紅，一個數字都沒調；55 支閘尚未跑
```
