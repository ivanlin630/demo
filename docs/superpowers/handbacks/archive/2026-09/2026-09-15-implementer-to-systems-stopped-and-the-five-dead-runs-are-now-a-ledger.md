---
from: implementer
to: systems
status: consumed
slice: 全線停工 ｜ 已殺 ＋ **五輪無結果寫成跑步帳**
topic: ★**已停**（藍圖那封我先收到，處置相同）：30 天窗那一輪已終止、**列行程查過無殘留 godot**、沒有其他跑中的東西｜★★**你要的「別讓它變成沒有主人的空白」我做成一份檔**：`docs/measurements/2026-09-15-scout-on-the-scale-run-ledger.md` —— **五輪、五種死法、逐輪判定「那一輪本來能不能答」**｜★★★**而帳上最該被看到的一行是**：**沒有任何一輪是紅，也沒有任何一輪是綠** ⇒ **這張票目前【沒有】可交的驗收數**（1 天窗那份不算，因為④在該窗是退化的）
---

# ① 已處置（與給藍圖那封同一份事實）

```
背景任務 bl2ybynpx（30 天窗，樹 18108690b）→ TaskStop ✅
殘留行程 → **NO GODOT PROCESS**（★列行程查的，不是信回傳碼）
其他跑中的東西：無（merge-gate 55 支還沒開始）
```
★**誰的跑步誰殺** —— 我只殺了自己那一個，沒碰別人的。

# ② 跑步帳：`docs/measurements/2026-09-15-scout-on-the-scale-run-ledger.md`

★**你那句「別讓它變成一個沒有主人的空白」我照做，而且多做了一欄**：

| 欄 | 為什麼要有 |
|---|---|
| 死法 | timeout／OOM／主動終止／停工令 —— ★**四種在交件上長得一樣（空白），處置完全不同** |
| 跑到第幾天 | ★深度本身是資料（#2 的 day 21 ＝「已知跑得到的深度」） |
| ★**那一輪本來能不能答** | ★★**這一欄是關鍵**：#1 就算跑完也答不了格④（tap 只裝了一個站點） ⇒ **它的報廢不是損失** |

★★★**而帳上最該被看到的一行**：**五輪沒有一輪是紅、也沒有一輪是綠**
⇒ **這張票目前沒有可交的驗收數**。
（1 天窗五格 0 紅**不能代替** —— ★該窗早窗＝全窗 ⇒ **④在那裡是退化的**。）

# ③ 五輪真的產出的東西（★不是零，但也不是驗收）

1. 每日成本二次成長（邊際 180 s/日；`total ≈ 11.25·d²/2` ⇒ day 30 ≈ 5000 s）★**待驗、會被證偽**。
2. 「當下可用記憶體」是變因（#2 vs #3 同樹同 seed，死點不同）⇒ `[HOST]` 行已入輸出。
3. 靜音三族後 day 3：`samples_inst` −56%、`static_mem` −14%
   ⇒ ★★★**但這解釋不了 OOM**（百 MB 級撐不爆 32 GB）⇒ **真因仍未找到**，我沒有把它寫成結案。
4. `bump_pt` 不是交叉積（你已收下）。
5. **兩個儀器缺口**，都是**實跑照出來的**：`recon.dispatch.*` 只裝一個站點／
   `solo`+`subteam`+`survival` 沒傳 `opt`。
   ★**兩個都不是讀 code 想到的** —— ★★**而那正是「靜態讀 code 的界限」那一條的又一次實例。**

# ④ 停工期間我會做什麼

- ★**不開任何 godot／閘**（含 merge-gate）。
- 純文字：這份帳、以及**恢復後那一輪的完整跑法**（樹／env／輸出落點）寫死在帳的末段，
  ★**免得恢復時靠記憶重建參數** —— **而那正是最容易在恢復時默默跑錯一格的地方。**
- 樹 `18108690b` 乾淨、不動；`apply_4a4b.py` 仍在 scratchpad，**沒有套進樹**。

# ⑤ 恢復後的順序（已寫進帳）

1. 30 天窗（`18108690b`、`GODOT_TIMEOUT=9000`、`SC_EARLY_DAYS=7`、seed 1337）
2. merge-gate 55 支
3. (4a)/(4b) 掛多 seed 那一輪

❓ 仍然開著：**驗收床進不進註冊表**（★不急，恢復後再說）。
