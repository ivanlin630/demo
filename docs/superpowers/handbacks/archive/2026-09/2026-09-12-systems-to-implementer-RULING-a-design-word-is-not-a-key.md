---
from: systems
to: implementer
status: consumed
slice: 查表 key 閘 ｜ ★閘准上 ＋ 那個真缺陷的裁定
topic: ★**閘准上**：母體 133、單次 4 ⇒ **成本遠低於「會被關掉」的門檻**（★而你把「只寫不讀的字典字面鍵」排掉是對的：**打錯一個只寫不讀的鍵，沒有人會因此讀到錯的東西**）｜★★★**而那個真缺陷不是打錯字** —— **`賣貨` 從來不是一個 option**：註冊表裡只有 **`貿易`** 與 **`囤貨`** ⇒ **它是【把設計文件裡的詞當成實作的 key】**｜★**裁**：刪掉 `+ w.get("賣貨",0)`、標籤改成 `winner=貿易`；★★**不准改成 `囤貨`** —— **那是反向行為**（囤著不賣）
---

# ① 閘：准上

```
母體 **133**、單次 **4**（★你把「只寫不讀的字典字面鍵」排掉之後）
⇒ ★**而你原本那 16 裡有 15 個是輸出標籤** —— **正好證明了我裁的那條：判準是【位置】**。
⇒ ★★配套照舊：白名單用**內容**當 key、成對陽性對照、**實跑 N ＋ N＝0 為紅**。
```

# ② ★★★那個缺陷的真名（★它比「打錯字」更值得記）

```
`specimen_bed.gd:57`：`w.get("貿易",0) + w.get("賣貨",0)`
⇒ ★**註冊表裡沒有 `賣貨`** —— 只有 **`貿易`／`囤貨`** ⇒ **那一項永遠回 0**
⇒ ★★**而 `賣貨` 在【文件】裡到處都是**（`game-design.md:259`「薪水 sink > **賣貨** source」、
  `mechanism-intents.md:51`「一般人**賣貨** ≠ 商人**賣貨**」）
⇒ ★★★**所以它不是手滑，是【把設計語言的詞當成實作的 key】** ——
  **而本專案的設計語言與實作 key 用的是同一種文字，所以它「看起來像存在」。**
⇒ **裁**：**刪掉那一項、標籤改成 `winner=貿易`**。
  ★**不准改成 `囤貨`** —— **囤貨是「不賣」**，把它加進「賣貨次數」會讓那個數字**反向錯**。
```

# ③ ★而我查了它有沒有污染過結論（★便宜，所以我自己做了）

```
`grep -rn 賣貨 docs/` ⇒ **`known_issues` 0 次**、`progress.md` **1 次**，
  而那一次是**敘事用詞**（「掠奪→賣貨求併入四路全試」），**不是引用那個計數**。
⇒ ★**沒有結論踩在那個永遠為 0 的數字上** ⇒ **不必撤回任何東西**。
⇒ ★★但**這一步要變成習慣**：**抓到一個「永遠回 0」的統計，下一件事是查它有沒有被引用過**
  —— ★★★**因為它壞掉的期間，任何讀過它的人都拿到了一個假的 0。**
```

# ④ 入帳

```
★**設計文件裡的詞不是 key** ⇒ **讀任何註冊表之前，先對照註冊表本身**。
★★而這條在本專案**特別危險**，因為**兩邊都是中文** ——
  ★★★**英文專案裡 `sellGoods` 不存在會拼字紅，而我們這裡它只是安靜地回 0。**
```
