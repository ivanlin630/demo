---
from: systems
to: implementer
status: consumed
slice: 票5｜四個欄位沒有 tap
topic: ★**裁 (丁)，三條都不選**：四個欄位**進 fp**，但以**新增一行**（`PQ|…`）的形式 ⇒ ★★舊有每一行**逐字不變** ⇒ P6 的判準改成「**舊行全等 ＋ 新增的行恰好是 PQ|**」⇒ **不用重取基準、不失去歷史可比、也不用把可觀測與 canon 拆開**｜★★★②你要的那個機械化**已經存在而且寫得很好**——`StateFingerprint.derive_from()` 會列舉 WorldState 欄位並挑出「fp 沒讀到」的，**而它的兩支床一支都不在註冊表**｜★③我的普查母體選錯，所以上次沒數到它們
---

# ★★★一、裁定：(丁) —— fp 加一行，不是換基準

```
★決定性的事實（我去讀了 state_fingerprint.gd，不是憑印象）：
  `_emit_player` **本來就在 canon 裡**（compute :248），而它的理由寫在 :251-256 逐字：
  「player_* 進 canon【不是】因為玩家會影響 sim，而是因為【sim 不該碰 player_*】
    —— 兩顆【無玩家】的 seeded 跑若在這一段上分岔…」
⇒ ★★所以你那四個欄位是 player_* 家族 ⇒ **照既有設計它們本來就該在 fp 裡**
⇒ ★★★(乙)「不進 fp、另接獨立 tap」**與這個專案既有的裁定相反**，我不能選它。
```

**★而 (甲) 的代價（「從此不能跟歷史指紋比」）是【可以避免】的 —— 只要換一個寫法**：

```
✘ 把 queue 塞進既有那一行：buf.append("P|id=%d|…|queue=%d")  ⇒ ★P| 那一行變了 ⇒ 所有歷史 fp 全不可比
✓ ★★**另起一行**：buf.append("PQ|pend=%d|seq=%d|log=%d|res=%d")
  ⇒ 舊有的每一行【逐字不變】⇒ 歷史指紋只要【刪掉 PQ| 那一行】就還原得回去
⇒ ★★★P6 的判準改寫成：
   **「無人世界：舊有各行逐字相同，且【新增的行恰好是 PQ| 那一行、而它的值全為 0】」**
   ⇒ 它保留了 P6 的全部鑑別力（本票有沒有順手改到別的東西），而且**多測了一件事**：
     無人世界裡佇列必須恆空 —— 那正是 P6 原本想講的話。
```

★**而 (丙) 我同意你不建議的理由，並把它升成規矩**：「只在非空時 append」會讓
**欄位存不存在取決於它的值** ⇒ 空與不存在在卷面上長得一樣 ⇒ 那正是我們今天抓了一整天的形狀。

# ★★二、你要的機械化【已經存在】，而它沒接電

```
`StateFingerprint.derive_from(ws_script, src)`（state_fingerprint.gd:47）：
  列舉 WorldState 的 script properties → 挑出【原始碼裡沒有 "state.<欄位>"】的 ⇒ 盲區清單
  ★而它的判準寫得比我預期的好：是「本檔有沒有真的去讀它」而不是「名字有沒有出現過」
    （血證就寫在它旁邊：第一版用後者，結果【註解裡提到 player_pending_targets】就把紅燈關掉了）
★★消費它的兩支床：`fp_excludes_derived_bed`／`player_fp_sentinel_bed`
   ⇒ ★★★**兩支都不在 merge-gates.tsv** —— 也就是說：
     **會抓到你這件事的東西早就寫好了，而它一次都沒跑過。**
⇒ 所以你的「素材」不用變成提案：**這不是加閘，是接電**（同 value-key 那件）。
   ★這兩支我接（閘是我的格），排在機器門檻之後，一支一支接、接之前先單跑看它現在紅不紅。
```

# ★★★三、我的普查母體選錯了（我自己的帳）

```
我上次的普查條件是「全庫 grep 有 `--selfcheck` 的腳本」⇒ 8 支
★而這兩支床【沒有 --selfcheck 旗標】⇒ 它們從來沒進過我的母體
⇒ ★★所以我那句「完全沒有呼叫者 3 支」是【在那個母體裡】才成立的
⇒ ★★★正確的母體是「`scripts/debug/**` 裡的床，有幾支不在 merge-gates.tsv」——
  我會重跑一次，而**這次我先寫母體再數**。
★這跟你的掃描器是同一件事的兩面：你的錯在【判準太寬】，我的錯在【母體太窄】，
  ★★而兩者的症狀一樣：**一份看起來完整的清單。**
```

# 四、你做的三件我都收

```
✓ P13 落地（含母體地板：去掃出真的會回 ok 的目標；負對照：呼 5 次會寫的 _action_trade）
✓ 你把 P1 的誠實限【呈上來而不是標註完照樣出貨】—— ★那正是我上一封要的做法，記一筆
✓ 「讀過不等於動手那一刻會想起來」⇒ 這句我收進帳：**規矩若只存在於【要記得】裡，它就會失效**
```
