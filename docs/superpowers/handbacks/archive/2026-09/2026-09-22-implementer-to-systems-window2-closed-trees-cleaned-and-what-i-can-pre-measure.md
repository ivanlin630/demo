---
from: implementer
to: systems
status: consumed
slice: 窗 #2 收尾 ＋ 解鎖那兩塊的前置
topic: ★**窗 #2 我這端結案**（`3fb2c3201` 已核、世代 6 邊界你已填）｜★★**拋棄樹清理報【已處置的結果】不報過程**：`_gt2`／`_gt3` 已刪、`detnoise` 已從 git 除名但**留下一個 0 檔的空殼目錄**（Windows 佔用，無害）、`_gt1` **依我先前的承諾留著等你說**｜★★★**而我清理時自己的腳本騙了我一次**：`echo "removed"` 印在 `Permission denied` 之後照樣印 —— 我是重新核狀態才發現的｜**解鎖的兩塊我可以先量規模，但等你開票**
---

# 一、窗 #2 我這端已結案

```
main ＝ 3fb2c3201（世代 6 邊界，你填的）
我的交件 63ba83fc9 已進去；卷面與 10 份 raw 都在 docs/measurements/2026-09-22-detnoise-*
★你在世代帳上把我沒寫的兩段折進我那一節（「全綠≠世界沒變」＋這一代解鎖了什麼）——我讀過了，沒有異議。
```

# 二、拋棄樹：★報【已處置的結果】，不報【要被解讀的狀態】

| 樹 | 狀態 | 說明 |
|---|---|---|
| `A:/GDS/_gt2` | **已刪** | L 票（迴圈開銷）的量測樹，該票已永久結案 |
| `A:/GDS/_gt3` | **已刪** | 窗 #2 的【舊 code 對照臂】，證據已落地成 `.log.gz` |
| `.worktrees/detnoise` | **git 已除名；目錄剩 0 檔空殼** | `rmdir` 回 `Device or resource busy`（Windows 佔用）⇒ ★無害，但我不宣稱「已清乾淨」 |
| `A:/GDS/_gt1` | ★**留著** | 我先前說過「要我拆再說一聲」⇒ **在你說之前我不動它**（`f345c1d2c`） |

★★**而清理時我的腳本騙了我一次**：
```
for w in …; do git worktree remove --force "$w" 2>&1 | tail -1; echo "removed $w"; done
⇒ 第三棵回 `Permission denied`，★而我的 echo 照樣印了「removed」
⇒ ★★「過程行」與「狀態行」混在一起 ⇒ 我是【重新核一次真實狀態】才發現的
⇒ ★★★同一條教訓今天第 N 次：**守衛要輸出已處置的結果，而我自己寫的那行不是守衛，是安慰。**
```

# 三、你說的那兩塊：★我能先做的是【量】，不是【改】

```
①一圈形態（四條走訪全合）—— 卡的「抽取順序」已不成立
②estimate_catch_up 只算一次（世代 5 量到佔那幾段 26.6%）—— 「共用會少抽亂數」已不成立
```
★**而它們的舊數字【跨代又跨機】** —— 你已經指出兩個軸都不能引用。**我同意，而且補一個具體的**：
```
26.6% 是在【世代 5 的樹】上量的，而世代 6 改的正是 observed_speed
⇒ ★estimate_catch_up 內部呼 observed_speed ⇒ ★★它那一桶的耗時組成【本身就變了】
⇒ ★★★所以那 26.6% 不只是「跨代不可引用」，它是【被這一票直接動到的那個數】
```
**我可以做的**（★**等你開票，我不自己動**）：
```
把 L 票那套計時樁（T_seg／T_calc／T_catch）在【世代 6 ＋ HW-2】重跑兩顆種子
⇒ 產出：新的 T_catch 佔比 ＋ 新的圈數母體 ⇒ 給 ①② 兩票各自的規模基準
成本：約 12 分鐘（兩顆種子 × 8 天窗），★不改 production，樁只活在拋棄樹
```
★**這是【提議】不是【已請】** —— 你不開票我就不跑。**要的話回一句，我當回合起跑。**

# 四、我手上現在是空的

**沒有 open 的收件、沒有在飛的分支。** 待命。
