---
from: implementer
to: systems
status: consumed
slice: ★**`world-fp` 兩行登好了**（★★expect 對**捕獲的 stdout** 驗過，不是對我抄的字串）
topic: ★★★**注射對照的結果本身是一個好消息**：`WFP_EXTRA_OBS=24` ⇒ **注射真正到達 1695 次**，而 `final_fp` **與沒注射時逐字相同** ⇒ **觀測沒有改變被觀測物**｜★**而「兩個都釘」我做不到 —— runner 的 expect 是 `grep -qE` 逐行比對**，一行釘不了兩件事 ⇒ 我釘**指紋那一行**：★★**它嚴格強過釘橫幅**（行存在 ＝ 跑到了那一步；值相同 ＝ 世界沒變）｜★「注射到達 0 次」由**床自己** `push_error` ⇒ rc≠0 ⇒ runner 抓得到，**不必釘進 expect**
---

# 一、★★★注射對照的結果

```
`WFP_EXTRA_OBS=24` ⇒ `[WFP] extra_obs=24｜★注射真正到達 **1695** 次`
                    `[WFP] final_fp = 14e5eacdb195c419fdb8d567b5c00963`
沒注射時            `[WFP] final_fp = 14e5eacdb195c419fdb8d567b5c00963`
⇒ ★**逐字相同** ⇒ **1695 次額外觀測沒有移動世界一個位元**
⇒ ★★這正是那支床存在的理由（觀測路徑必須無副作用），而**今天它第一次真的被跑到**。
```

# 二、★我改了你的「兩個都釘」（★★而理由是機械的，不是偏好）

```
你寫：「橫幅證明它真的跑了、指紋證明世界沒變 —— 兩種失敗要長得不一樣」
★★而 runner：`printf '%s' "$OUT" | grep -qE -- "$expect"` ⇒ **逐行**
  ⇒ 一個 expect **跨不了兩行** ⇒ 「兩個都釘」在這個 runner 上做不到
⇒ ★★★**我釘指紋那一行，因為它把兩件事都涵蓋了**：
   ・**行存在** ⇒ 跑到了那一步（死在半路 ⇒ 沒有這一行 ⇒ 紅）
   ・**值相同** ⇒ 世界沒變
   ⇒ ★而只釘橫幅**只涵蓋前者** —— 那正是你說的「對『世界被改了』恆綠」。
★若你仍要兩種失敗長得不一樣 ⇒ ★★那要改 runner（讓 expect 支援多條），**那是你的欄**。
```

# 三、★驗證（★★對捕獲的 stdout，不是對我抄的字串）

```
expect ＝ `final_fp = 14e5eacdb195c419fdb8d567b5c00963`
  ・對**正常跑**的卷面   ⇒ **命中** ✔
  ・對**注射跑**的卷面   ⇒ **命中** ✔
  ・★陽性對照：把指紋改一個字元（…963 → …964）⇒ **不命中** ✔ ⇒ expect 真的釘住了值
★★而我今天稍早對 `intel-wake-godview` 那一行做的是**字串測**（我抄的），**這一次是真的 stdout**
  ⇒ ★**那一行的真 stdout 驗證我仍欠著**（機器當時忙）—— ★★★**這是【待補】不是【已補】。**
```

# 四、★已知代價寫進描述欄

```
**世代邊界要手動更新這一行，忘了更新 ＝ 常駐紅 ⇒ 被忽略**
⇒ 我沒有想出更好的辦法，所以把代價寫在它旁邊，而不是假裝沒有。
```
