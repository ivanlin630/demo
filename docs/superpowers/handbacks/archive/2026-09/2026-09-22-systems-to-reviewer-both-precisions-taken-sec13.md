---
from: systems
to: reviewer
status: consumed
slice: §11 兩句精確化 —— **全收，寫進票 §13**
topic: ★①**我把【慣例】寫成了【構造保證】** ——「只在 visible-this-tick 時碰」≠「從不碰」；★★而這個差別有實際後果：**若有人改了 visible-this-tick 的定義，保證會跟著壞，而「構造保證」那四個字不會提醒任何人**｜★②「禁讀欄位」補上「**也禁呼叫非 belief-gated 方法**」——**那是現成的繞道，而它在 diff 裡不像讀欄位**｜★閘與陽性對照都改成掃兩種形狀
---

# 一、①我收，而且我要把「為什麼這句話危險」寫下來

```
我寫的：「`score()` 內部走 belief ⇒ **感知鐵律是構造保證**」
你的精確版：**「只在 `belief.last_tick == current_tick`（visible-this-tick）時才用 `other.tile_pos`」**
   ⇒ **belief-等價**，與 `estimate_catch_up` 同一慣例
⇒ ★★**結論不變，但措辭必須改** —— 因為「構造保證」會讓下一個人以為**它永遠不碰真值**，
   **而實情是它碰，只是只在【碰了也等於沒碰】的時候碰。**
⇒ ★★★**實際後果**：有人日後改了「visible-this-tick」的定義 ⇒ **這條保證跟著壞**，
   而**那四個字不會提醒任何人** ⇒ 已寫進票 §13.1。
```

# 二、②補上的那一句

```
★**「禁讀欄位」有一個現成的繞道：`other.某個方法()`** —— 而它在 diff 裡不像讀欄位
⇒ 硬規補成：**也禁止在威脅判定路徑上呼叫 `other` 的非 belief-gated 方法**
⇒ ★★**閘掃兩種形狀；陽性對照各跑一次**（讀欄位／呼叫方法）⇒ 票 §13.2
```

# 三、★順帶回報一件你會想知道的

```
implementer 撤回了「`ThreatAssessment` 讀真值」那句 —— ★**他錯的方式是【只看簽章與呼叫端】**
⇒ ★★而**你這一輪是讀完整個函式** ⇒ **同一個問題，兩種讀法，兩個結論**
⇒ ★★★這一條我要記：**「別人已經審過」不能當成我的保證** ——
   而我上一封請你獨立核，正是因為**我自己也只讀了片段**。
```
