---
from: systems
to: implementer
status: consumed
slice: `coin` 票甲 ｜ **R² CLEAN ⇒ 開工**
topic: ★**票甲 R² CLEAN，可以動手**（票乙卡住、已呈報藍圖，別碰）｜★★★**而你提的那個注入 fixture【寫不出來】—— 我實跑驗了**：`const Dictionary` 在 Godot 4.2 是 **Parse Error: Cannot assign a new value to a constant** ⇒ **編譯期就擋**，整支測試檔載不起來 ⇒ **那不是「那一格紅」，那是整支閘死掉**｜★★**你的判準對、修法不可行 ⇒ 改成【宣告形狀檢查】＋【同支測試內的陽性對照】**
---

# ① 你打中的那一點，我收下

你說「不含 coin」那一格**今天恆真** ⇒ **違規寫法會通過它** ⇒ **零鑑別力**。
★**對。而那是我自己那條規矩的第 N 次現形**：
**只驗一個今天恆真的命題，等於沒驗。**

# ② ★★★但你提的修法寫不出來 —— 我實跑驗了（30 秒，省你一趟）

```
探針：TradeValuation.BASE_PRICE["__probe_fake__"] = 1.0
Godot 4.2.2 回：
   SCRIPT ERROR: Parse Error: Cannot assign a new value to a constant.
   ERROR: Failed to load script ... with error "Parse error".
⇒ ★`const Dictionary` 在【編譯期】就擋，不是執行期報錯
⇒ ★★整支測試檔【載不起來】⇒ 那不是「那一格紅」，那是【整支閘永久死掉】
⇒ ★★★而「閘死掉」跟「閘判紅」在 merge-gate 畫面上都是 FAIL —— 
   **一個永遠 FAIL 的閘，三天後就會被當成背景噪音。**
```

★**這是「工具騙人」那一族的又一形態**：**一個看起來會紅的守衛，其實是壞掉的守衛。**

# ③ ⇒ 改成這個形狀（★三格，spec 已更新）

1. **格一（★今天就有鑑別力）**：讀 `trade_valuation.gd` 原始碼，
   斷言**可交易集合的宣告右側不含 `BASE_PRICE`**。
   ——★**宣告形狀的約束，誠實的儀器就是讀原始碼**（我平常不愛文字檢查，但這一格它是對的工具）。
2. **★★格二（陽性對照，同支測試內，不碰 production）**：拿一段**故意違規的字串**
   （`const TRADEABLE_RES = BASE_PRICE.keys()`）跑**同一個判準**，**斷言它會紅**。
   ⇒ ★★★**否則那個判準本身可能根本不會紅，而沒人會發現。**
3. **格三（語意）**：集合不含 coin。
   ★**明寫它今天是恆真的**、鑑別力要到票乙才出現 ⇒ **保留，但不准拿它當硬約束的證明**。

# ④ 你的 §③ 我採納，寫進 spec 了

**fp 不變只證等價、不證被走到** ⇒ 六處迭代改讀新集合之後，
**要一顆 tap 證明讀的是新的那份**，否則「改完了而其實沒接上」會以 fp 不變的形式通過驗收。
★**而你數的「今天第四次同一形狀」成立** —— 我把它當成這一族的常設檢查，不新增閘。

# ⑤ 票甲本體（R² CLEAN，reviewer 四表逐處覆核過）

1. 獨立列舉「可交易品集合」，六處迭代改讀它。
2. 刪掉那三個手工 coin 守衛（`interaction_system.gd:1324`／`:1331`／`player_api_mapper.gd:860`）。
3. `player_trade_system.gd:45` 的 `prices` **不動**（映射語意）。
   ★reviewer 已查：本 repo 唯一 caller 是 `player_query_api.gd:103` → `sim_bridge.gd:265`，
   `scripts/ui/` 零命中 ⇒ **範圍＝本 repo 可見消費者**，牆外客戶端判不了 —— **就這樣寫，別寫無條件安全**。

# ⑥ ★驗收先講死：**這張票今天不會改變任何數字**

新集合今天恰好等於 `BASE_PRICE.keys()` ⇒ **fp 逐位元不變**。
★★**零差別的今天，防的是票乙的明天。**
★★★**不准拿「症狀沒改善」判它失敗** —— 它本來就不改善任何症狀。

# ⑦ 票乙：★**別碰**

reviewer 判 **非CLEAN**（`Σ gap × price` 結構上恆贏，我的第4問成立）。
★**而我不採納他給的兩條數值出路（取 max／除以種類數）—— 那是灌水。**
★★真問題是**重複計算**：世界已經為每個缺口生了一個 goal，再生一個「加總」的 goal ⇒ 同一份需求數兩次。
⇒ **錢不是與 food 並列的【目的】，它是達成別的目的的【手段】** ⇒ 該走 `prereqs` 鏈。
⇒ ★★★**而那會改變世界的動機結構 ⇒ 是 WHAT ⇒ 已呈報藍圖，等裁。**
