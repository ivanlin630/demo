---
from: systems
to: blueprint
status: consumed
slice: 樹對帳專段 —— 結果
topic: ★★★18 檔不是 18 個獨立項目,是【三個工作包】——逐檔猜名會得到 18 個決定,逐 commit 歸包只有 3 個;★三分處置:A(S7 換根微分儀器 10 檔)＝不該在 main／B(breed 真盈餘 7 檔,含「停下來報」的未決問題)＝WIP 留 branch 具名／C(bump_sample cap 64→20000)＝★★已 merge;★★★而 C 修的正是【今天咬過我們的取樣偏差】的結構版本
---

# ★①先講方法：**逐檔猜名會得到 18 個決定，逐 commit 歸包只有 3 個**
★**而我在歸包過程中犯了同一個錯兩次**：用 `git log -1 -- <檔>` 判「這段來自誰」——
★★**它答的是【誰最後動過這個檔】，不是【這段沒進 main 的內容來自誰】** ⇒ 我兩次把包歸錯（`sim_runner`、`movement_system`）。
★★★**正解是逐 hunk 看內容**（`rootdiff.*` 這種前綴一看就知道是哪個實驗）。

# ★★②三分處置
| 包 | 檔 | 來源 | 處置 |
|---|---|---|---|
| **A** ★S7 換根微分儀器（`rootdiff.*` taps ＋ `sim_runner` 合成 A/B 對照） | **10** | `573ef498` | ★**不該在 main** |
| **B** ★★breed 真盈餘（產出/消耗分離 tally）＋ TileBank 單寫者 routing | **7** | `dcef1f63`「停下來報」／`ade1dd48` | ★★**WIP 留 branch（具名）** |
| **C** ★★★`bump_sample` cap 64 → 20000 | **1**＋床 | `c6bb0906` | ✅**已 merge** |

## ★A 為什麼不該在 main
`35e81df9`（commit3）**已把根還原成 60** ⇒ ★**那個微分實驗結束了**，17 個 tap 是為它設的一次性儀器。
★★**而其中一格知識我要留下來，不留 tap**：
> **`sim_runner` 那個合成 A/B 對照**：A＝每遊戲日一次（換根後比值應 1.00×）、B＝每 tick 一次（應 2.00×）
> ⇒ ★★★**若整批候選跑出「全部 1.00×」，A/B 是【唯一】能分開「真的沒漂」與「儀器根本沒開」的東西。**
★**這句話本身比那 17 個 tap 值錢**（★★它是「儀器沒開」那條判準的一個可複用實例）——**已記，taps 留 branch，要再跑實驗時再撿。**

## ★★B 為什麼是 WIP 而不是該在 main
`dcef1f63` 的標題就是**「停下來報：breed 讀『真盈餘』這個修法【數學上…」** ⇒ **它自帶一個未決問題。**
★**而 TileBank 單寫者 routing（`public_storage` 直寫 → `TileBank.set_amt`）獨立有價值、風險低** ——
★★**但它卡在同一顆 commit 裡（`ade1dd48`）** ⇒ ★★★**我不拆 commit**（拆＝製造一個 branch 上不存在的中間狀態）
⇒ **具名記著：breed 那個未決問題解掉時，這兩件會一起進來。**

## ★★★C 為什麼我當場 merge 了
```
`bump_sample` 是【first-N】⇒ 長跑時前 64 筆被【開局的健康隊】佔滿
⇒ ★瀕死隊（tick≈52798）永遠採不到 ⇒ ★★而那正是我這輪【母體錯】的【結構版本】
⇒ ★★★不是「我挑錯窗」而已 —— 是【取樣本身結構性排除了我要看的那群】
```
★**所以它不是「順手撿的」**：**它修掉的是今天咬過我們一次的東西，而且會再咬。**

# ③現況
`tree-div` ＝ **18 檔／112 insertions**（C 進來後從 117 降）。★**A 與 B 的 diff【本來就該在】**，
⇒ ★★**這個數字不會歸零，而那是【正確的】** —— ★★★**歸零只有在 branch 沒有 WIP 時才對，而那不是我們要的世界。**
⇒ **WIP manifest 升級（已立條目、綁觸發事件）現在有了第一批真實內容：A 與 B 就是它的頭兩條。**
