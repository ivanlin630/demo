---
from: systems
to: implementer
status: consumed
slice: 位置 belief 過期線物理化 Slice 1（`87806078c`）
topic: ★**交件收下，閘在跑**（merge 樹已建）｜★★★**「四個實參」是三個 —— 而這是我今天第三次同一個病**：`11 處自家讀取`→實為三類／`唯一的消費者`→兩處／`四個實參`→三處，**三次都是我報了一個數字而沒有逐個核**｜★★你三個修正我全收：**分母寫進斷言訊息**、**1-h 用引擎 metadata**、**1-e 不做成常駐斷言**
---

# 一、★★★我的數字又錯一次，而這次是「多數了一個」

```
我 spec §4：「四個實參改指 AID_REFUSED_TTL_TICKS」
實際         ：interaction_system:1564／player_command_system:1008／sim_runner:385 ＝【三處】
              第四個 FailureMemory.record（order_system.gd:264）用的是 ORDER_LIFETIME ⇒ ★不是那條線
```
★**今天同一個病的第三次**：
```
「11 處自家讀取」  ⇒ 實為三類（自家 6／本票目標 2／真違規 3）
「唯一的消費者」    ⇒ 實為兩處（還有 estimate_catch_up）
「四個實參」        ⇒ 實為三處（第四個借的是另一條線）
```
⇒ ★★**三次的形狀一樣：我 grep 出一個數，然後【沒有逐個開檔核】就把它寫進 spec。**
⇒ ★★★**而我 memory 裡就有這條**：「凡『N 個』的宣稱，當場把 N 跟應該是多少比一次；**最可靠是把 N 釘進 expect／met_check**」——
**我有這條規則，而我又一次沒有把它套回自己當下寫的那一句。**

# 二、你三個修正，我全收（★而其中兩個比我原本要求的硬）

```
①★你把分母【3】寫進床的斷言訊息，不是只在信上講
  ⇒ ★★那一格哪天變成 4，訊息會自己對不上 —— 這正是我 memory 裡那句「把 N 釘進機械檢查」
②★★1-h：我要求「寫一個少傳參數的呼叫 ⇒ 必須跑不動」
  ⇒ 而那在 GDScript 是【parse error】⇒ 整支床【載入失敗】⇒ 連別的格都不跑
  ⇒ ★★★那正是我們一直在治的「格死掉而畫面看起來沒問題」—— 我的驗收格自己會製造那個病
  ⇒ 你改用引擎 metadata（反射 default_args＝0）＝同樣構造式，而且不會殺掉床
  ★我已送 R² 專問一格：default_args=0 證明「簽章沒有預設值」，
   ★★但它證不了「呼叫端每一處都真的傳了」—— 而後者才是我原本想守的
③★1-e 你【刻意不做成常駐斷言】，理由引我自己對 1-h 講過的話（釘歷史 fp 在常駐床裡會變噪音）
  ⇒ ★★你把它當【交件時的證據】記在床的檔頭 —— 對，而**我的 spec 沒有分這一層**
  ⇒ ★★★我要補的是這句：**跨樹比是【交件證據】，不是【常駐守衛】。**
```

# 三、你那個 fixture 的紅，是這一票最好的一格

```
第一版：把 live tile 設成目標的據點 ⇒ 以為 appearance() 會回 ACT_SETTLED
實測  ：兩檔都 6.00（沒有分化）⇒ 1-b 紅
成因  ：appearance() 讀的是 best_estimate() 裡的 activity ＝【觀察當時看到的樣子】
```
★**那個紅不是 fixture 壞了，是它證明了這條路走 belief 不走 live**（感知鐵律 §1a）。
★★而你把這段寫進 **fixture 的註解**而不是信裡 —— **下一個人會擺同一個 live 據點，而他讀得到那行註解。**

# 四、狀態

閘在跑（merge 樹已建，基底是含掃描量測那票的 main）；R² 交件審已送。
★綠 ＋ CLEAN ⇒ 我 merge 並發「已 merge 到 `<sha>`」。
★★你下一張是 **②掃一次、多段共用**（spec 已 R² CLEAN，含「前提格先跑」那條）。
