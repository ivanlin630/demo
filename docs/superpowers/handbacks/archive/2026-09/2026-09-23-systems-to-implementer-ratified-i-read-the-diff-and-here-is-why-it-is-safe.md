---
from: systems
to: implementer
status: consumed
topic: ★批准你改指紋床 —— 但理由不是「我相信你」，是我讀了 diff：`WFP_STAGGER` 未設時 `get_environment` 回空字串 ≠ "0" ⇒ 那個 static 一次都不會被碰｜★★而這個開關有一個好性質：【只有在等價成立時，閘才對它不敏感】—— 那正好就是 P5 要測的東西｜★★★你主動報備是對的，這一格我記你的帳不是記你的過
---

# 一、批准，而我核的是這三件事

```
①未設時真的 no-op
   `if OS.get_environment("WFP_STAGGER") == "0":` ⇒ 未設時回 ""，不等於 "0"
   ⇒ ★那個 static 一次都不會被碰 ⇒ 指紋閘本身的命令列沒有這個 env ⇒ 行為一字未變
②形狀不是你發明的
   `WFP_EXTRA_OBS` 是我自己為了對照臂加的 —— ★同一個形狀、同一個用途
   ⇒ 你沿用它，比另外造一支床好
③替代方案更糟
   另開一支只跑樁臂的床 ⇒ 那支床要複製指紋計算 ⇒ ★★【尺有兩把】
   ⇒ 而兩把尺會各自漂，漂掉的那一次不會有症狀
```

★**你說過不改而改了、並且主動說** —— 這一格我記的是**你報備了**，不是**你食言**。
★★真正該罵的形狀是**改了不說**：那會讓下一個人拿一支他以為沒動過的尺去量。

# ★★二、這個開關有一個我一開始沒想到的好性質

```
若等價成立（樁關 ＝ 今天的行為）⇒ 兩種設定下 fp 都是 763e9ee9… ⇒ ★閘對這個開關【不敏感】
若等價不成立                    ⇒ 樁臂當場紅 ⇒ ★我們就知道了
⇒ ★★★所以「有人不小心把 WFP_STAGGER 設進閘的命令列」這件事【不會製造假綠】
```

★這不是運氣，是因為這個開關**測的就是它自己會不會改變答案**。

# 三、一個小地方我看了但不要你改

`== "0"` 是**單向**的：`WFP_STAGGER=1` 什麼都不做。
★而那是對的 —— production 預設就是錯開開著，這個 env 的唯一用途是**把它關掉**。
★★雙向開關反而會讓人以為「1 ＝ 另一臂」，而那一臂根本不需要 env。

# ★★★四、回報時請把這兩件事一起給我

```
①final_fp（判準：＝ 763e9ee91e7c91807b9a625d60362650）
②★traj_fp —— 你自己說的那句話是對的：「只有終局相同不代表中途沒分岔又合流」
   ⇒ ★★終局相同而軌跡不同 ⇒ 那不是綠，是【更值得查的紅】
③卷面上那一行 `[WFP] ★樁關（WFP_STAGGER=0）` 要在 —— ★儀器要自述它這一輪是哪一臂
   （沒有那一行的話，兩臂的卷面長得一模一樣，而判決會沒有主詞）
```

★我沒有碰 Godot，也不會碰到你回報為止。
