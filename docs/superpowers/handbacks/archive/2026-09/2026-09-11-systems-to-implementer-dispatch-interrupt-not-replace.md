---
from: systems
to: implementer
status: consumed
slice: interrupt-not-replace ｜ worktree `.worktrees/interrupt` ｜ branch `feat/interrupt-not-replace`
topic: ★DISPATCH（R² 三格已吸收，§⑦ 是最終版）：`specs/2026-09-11-interrupt-not-replace-HOW.md`｜★★★**先做前置量測，再動 code**：①覓食 episode 的起訖 `food_days`（★插斷會不會結束）②同窗**其他長程任務的到場率**（★★求居的門檻**綁它**，不綁一個我拍的 30%）｜★★而 R² 抓到一個覆蓋邊界：**掃描閘只逼欄位分類，不逼 `unfinished()` 真的讀它** ⇒ 驗收⑦ 是成對對照
---

# ① 順序（★前置量測在前，★★它可能讓這張票停下來）

```
①★**覓食 episode 的起訖 `food_days` ＋ 長度** ⇒ ★★若吃完**沒有明顯上升**
  ⇒ **停下來回報** —— 「插斷非取代」在那種世界裡**也會無限迴圈**（吃→還是餓→再吃）
  ⇒ ★★★（R² 查到機制**真的會產生食物**、且有**設計好的飽足 buffer** ⇒ 機制面成立，
    **而夠不夠只有這一格答得出來**）
②★**同窗其他長程任務的到場率**（基準）⇒ ★★①的門檻 ＝ **≥ 這個基準**
  ⇒ ★★★而**若基準本身很低**（例如 20%）⇒ **單獨回報** —— **那是比本票更大的發現**。
```

# ② 做什麼（★三件，全在既有語彙裡）

```
①求居成為**承諾**：加欄位並**在 `commitment_fields.gd` 分類**
②**插斷不改**：覓食照舊贏、照舊插斷（★真餓先吃是對的）
③**恢復**：survival episode 結束 → `release()` → **re-rank**，而承諾**活過那次 release**
  ⇒ ★**不加特權、不加常數**（禁走廊是 blueprint 明文）
★★唯二出口要落地：**重派成任務**／**引擎解承諾**（真放棄）
  ⇒ ★★★**既不重派也不解 ＝ 死旗 ＝ 本票自己製造一個新的手不聽腦**（驗收⑤ 擋它）
```

# ③ 驗收（★★★⑦ 是 R² 抓到的，最容易漏）

```
①到場率 **≥ §①② 的基準**（★不再是我拍的 30%）｜②恢復率 **≥ 90%**
③餓死率**不升**｜④**無登記的隊數不歸零**（★★不是要消滅遊商階層）
⑤★承諾兩個出口都要有數字，而**「既不重派也不解」的存量 ＝ 0**
⑥fp 會變（裁定接受）＋ 同 seed 兩跑相同
⑦★★★**`unfinished()` 真的長出分支**：**成對對照** —— 設下 ⇒ 回報；清掉 ⇒ 不回報
   ⇒ ★**兩邊都要跑**：只驗一邊 ＝ 驗到「它存在」而**不是**「它被讀」
   ⇒ ★★而這一格的理由要寫進 code 旁：**掃描閘只逼分類，不逼被讀** ——
     ★★★**欄位被分類（閘綠）而 `unfinished()` 從不回報它 ＝ 承諾在紙上存在、在執行上不存在**
```

# ④ 目標校準（★寫進交件）

```
★**不是要消滅無家階層**：52.6% 靠貿易、壽命中位 29 天 ＝ **遊商階層雛形**
⇒ ★★**要修的只有一件：選了求居的隊，要能走到。**
```
