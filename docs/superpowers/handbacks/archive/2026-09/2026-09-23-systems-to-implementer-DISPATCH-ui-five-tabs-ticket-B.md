---
from: systems
to: implementer
status: consumed
slice: UI 五分頁 票B（把欄位餵進去）
topic: ★DISPATCH 票B（票A 與三格都已 merge：`01b5b4128`／`ed128a1e7`）｜★★**母體不是我列的清單，是【畫面上那個「未分類（票B 將搬走：N 行）」區塊】＋走查今天印得出來的欄位** —— ★★★你每搬走一欄，那個 N 就變小，而 `[UI-SKYLIGHT] count=` 那一行就是進度本身｜★合併後全電池在我這邊跑，**機器是我的**，你先寫 code
---

# 〇、spec 與前置

```
spec：docs/superpowers/specs/2026-09-23-ui-five-tabs-HOW.md §3（票B）＋ §5 的 Q1–Q5
已落地：票A 的框（五分頁／狀態列／具名天窗／未分類區塊）＋ 三格恆真項已改成會紅的
★頁名：UiPages.PAGE_ORDER（生存／經濟／威脅／社交／記憶）—— ★★不要在別處再寫一份
```

# ★★一、母體：不要自己列清單

```
①畫面上的「── 未分類（票B 將搬走：N 行）──」區塊 ＝ 待分類量
②走查（c1_walkthrough.gd）今天印得出來的欄位 ＝ 可接出的上限
⇒ ★★★這兩個都是【長在系統裡的】—— 你不需要、也不應該另外維護一份「票B 待辦」
⇒ ★而「119 欄」那個數字【不要用】：它是名字比對的產物（progress.md 2026-09-10 逐字警告）
```

# ★★★二、每一欄的硬規（spec §3，逐字）

```
①讀點必須走【公開查詢面】（PlayerQueryApi／SimBridge 快照）
   ★不得直接讀 state；★★不得是常數／占位字串（blueprint 驗收方向，逐字）
②接上一欄 ⇒ 該欄的天窗消失；★沒接上的【仍然印天窗】（不許先拿掉天窗再說）
③★★★搬動不改字：從「未分類」區塊搬到對應頁時，那一行的文字【逐字不變】
   ⇒ 要改措辭是【另一件事】，混進來會讓 Q3 的 diff 講不清楚
```

# ★三、我要你加的一行（★票A 那邊已經講過，這裡是正式要求）

```
[UI-SKYLIGHT] count=N declared=M       ← ★機器可讀、固定格式
用途三個：
  ①Q4「天窗遞減」有一個【數字】可以追，不是靠人看清單變短
  ②defers `skylight-count-zero-guard-expires` 的 met_check 錨在 count=0
    ⇒ ★★你把最後一欄接出來的那一刻，它會叫醒人去【退役 P4】（不是讓 P4 紅著）
  ③★★★declared==0 時要印出兩種可能（票B 接完／宣告函式壞了）並明說【這一格不判】
```

# 四、驗收（spec §5 Q1–Q5，★我把你會問的兩件先講）

```
Q1 [來源] 逐欄 grep 讀點 ⇒ 指向查詢面；常數／占位＝紅
Q2 [成對對照] 拿掉一欄的查詢面 ⇒ 【那一格】變天窗（★不是整頁壞掉）
Q3 [會動] 同種子兩個 tick 的畫面字串 diff 非空，★並印出【哪一行】變了
Q4 [天窗遞減] 每批之後 count 變小且印出來
Q5 [fp] world-fp 不變（★本票只讀不寫）
★★你會問的①：要不要一次接完？——【不要】，分批，每批自己跑 ui-flow
★★★你會問的②：Q2 的「拿掉查詢面」要不要還原？——要，照你前兩次的做法：還原＋grep 殘留 ＝ 0
```

# ★★五、一條我從今天學到、現在寫成要求的

```
★每加一格新 cell，請同時回答「什麼注射會讓它紅」——
  ★★今天三支恆真格的教訓是：【寫得出斷言】跟【那個斷言能紅】是兩件事
★★★而特別當心你剛學到的那個角落：把「字串在不在」換成「值對不對」【不會自動】解決恆真
   —— 值比較在【兩邊都失敗】那一格仍然恆真，需要母體地板站在旁邊
```

# 六、節奏

```
★機器現在是我的（合併後電池，HEAD=ed128a1e7）⇒ 你先寫 code，別起 Godot
★★我跑完回你一行；★★★而票B 之後就是【玩家指令佇列化】
  （spec 已 R② CLEAN，defers token: command-queue-dispatch-after-ui）
```
