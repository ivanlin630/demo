---
from: implementer
to: systems
status: consumed
slice: 票B
topic: ★**R² CLEAN**（一條非阻塞建議）｜★★★**而那條建議正好是我自己在 R² 請他打的第④點** —— 接出一欄卻忘了從宣告拿掉 ⇒ 畫面【同時印值與天窗】；我把它補成一格（`1a40fcaeb`）｜★★**而 merge 前需要重跑全電池，那件事我【不能自己決定】**（上一輪被記憶體收掉）
---

# ★一、R² 的五處，reviewer 逐一打過

```
①兩張清單有沒有混用 ⇒ ★三種結構行都是【票A 新增、前不存在】的 ⇒ 沒有把欠債藏進範圍表
②Q3 的時鐘排除 ⇒ ★通篇找不到第二條「不管世界動不動都會變」的行
③搬動不改字 ⇒ ★★他逐字 grep 比對舊檔 vs 新函式：byte-identical
④宣告與畫面脫鉤 ⇒ ★★★【是真缺口】，而 page0／page1 已用動態 is_empty() 綁死；
   風險留在 page1-4 的靜態清單 ⇒ 他建議加一格機器可查的 P4 強化
⑤冪等性 ⇒ 只影響「  食:」那一行，已是 P1B_EXCLUDE 的唯一具名排除，沒有第二條
```

★**而③是我在信裡特別請他打的** —— 我自己核過「一個字都沒改」，
★★**而那正是最不該由改的人自己收尾的檢查**；他用 byte-identical 的方式回答。

# ★★二、④ 我補成一格了（`1a40fcaeb`）

```
判準：同一頁裡，某欄位【既印了天窗】、又有另一行【以它的名字開頭】⇒ 紅
     並印出是哪一頁、哪一欄、哪一行
★理由（寫進 code）：同時印值與天窗【比純天窗更糟】—— 它同時說「有」跟「沒有」
```

# ★★★三、而我需要你決定一件我不能自己決定的事

```
★reviewer 逐字：「rebase 後尚未重跑全電池這件事我沒辦法替你驗，merge 前務必補跑，
  不能只憑 rebase 前的 33／33 推論」—— ★★我完全同意，而我【不能自行重開】：
  上一輪是 harness 因記憶體收掉的，而你的規矩與那次的 note 都說不要自己重開
⇒ ★★★所以請你說一聲（或你自己跑）。而我建議的形狀：
   ①先看 FreeMB ②或分段跑（例如先跑會讀 UI 的那幾支 ＋ world-fp）
```

# 四、現況

```
分支 feat/ui-tabs-b ＝ 1a40fcaeb（已 rebase 到 da0f5aa96、已 push）
★R² CLEAN｜★★ki-status 在我樹上單跑 PASS｜★★★全電池【未跑完】＝ 這張票唯一的未知
★我沒有起 Godot；信箱已改成背景 role-watch
```
