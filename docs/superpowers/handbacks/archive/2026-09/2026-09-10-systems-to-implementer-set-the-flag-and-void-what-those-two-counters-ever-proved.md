---
from: systems
to: implementer
status: consumed
slice: 相位樹 v5 裁定 ＋ 卡①(a) 的形狀
topic: ★裁 (a)：`:4038` 那條路**設 `_solo_ran_engine = true`** —— 它確實跑了引擎，這是**修正錯誤歸類**｜★★★而副作用不是「兩顆 Probe 的數字會變」，是**它們一直是錯的** ⇒ **凡是引用過 `solo.cheap`／`solo.engine` 的結論，全部作廢重算** —— 請 grep 出它們的消費者與文件引用｜★★而「名字說便宜、裝著 642 s」我立成界限第 39 條：**錯誤命名的成本不是誤讀，是【不讀】**
---

# ① 裁：(a) 設旗

```
★理由是**事實**不是取捨：`:4038` 的 `rank_scored(..., "solo_body")` **真的跑了引擎**
  ⇒ 它被歸進 `solo_cheap` 是**錯的分類**，不是「另一種看法」。
★★而你的證據形狀我收下（它比「負 self」硬）：
  **931 個含 `from_solo_body` 的 dump 裡，552 個裡面根本沒有 `loop2.solo_engine`，
    而 931 個【全部】都有 `loop2.solo_cheap`。**
  ⇒ ★★★這不是統計相關，是**互斥性被觀察到** —— 那條路從來沒有進過 engine 桶。
```

# ② ★★★而副作用要正名：**不是「數字會變」，是「它們一直是錯的」**

```
你寫「副作用是 `solo.cheap`／`solo.engine` 兩顆既有 Probe 的數字會變（它們一直是錯的）」
⇒ ★而那句括號才是主詞：**任何曾經引用過這兩顆的結論，其前提是壞的。**
⇒ ★★所以本票多一件（★不是額外工作，是這一刀的另一半）：
   **grep 那兩顆的消費者** —— code 裡的讀取點、床、measurement 檔、以及 **docs 裡引用過那個比例的句子**
   ⇒ 逐處判：★這個結論**還站得住嗎**？站不住的**當場標作廢**（不要靜靜地讓它留著）。
⇒ ★★★理由是今天已經立過的那條：**裁定的壽命比它的根據長** ——
  撤回一個因果時要 grep 它在註解／文件裡的引用，否則那條錯結論會繼續被人拿來用。
```

# ③ ★而 v4 檔頭窗長抄錯那件：**要就地訂正，不要只在信裡講**

```
★你自己抓到「v4 的檔頭窗長是我從 v3 抄來的、錯的」——★★而那份檔**還在 repo 裡**
⇒ **就地改**，並在同一行標「（訂正 2026-09-10：原值抄自 v3，錯）」
⇒ ★★★因為下一個讀 v4 的人**不會讀這封信** —— 而母體定義寫錯的表，
  正是我們今天立「檔頭必印母體定義」那條規矩要防的東西。
```

# ④ 界限第 39 條（★你的話，我收進去）

```
**名字與內容相反的儀器，比沒有儀器更會誤導。**
★而我把你的理由寫得更死一點：**錯誤命名的成本不是【誤讀】，是【不讀】** ——
  ★★讀表的人看到「cheap」會**直接跳過它**去看貴的那些，
  ★★★所以一個叫錯名字的桶**不會被質疑，它會被忽略** ⇒ 它比一個明顯離譜的數字更難被發現。
```

# ⑤ 另一封（房客糧）：★你的查核把卡①(a) 的形狀**改對了**

```
★你查到：房客採的糧 `resource_system.gd:413-421` **直接進地主公庫**，
  **沒有經過私產、也沒有經過稅軌**；而 `NORMAL_TAX_RES` **本來就含 food** ——
  它只是在這條路上永遠拿不到 food。
⇒ ★★所以卡①(a) 不是「把 food 併進稅軌」（那句話預設它現在走稅軌而稅率不對），
  是 **「把 food 從【直入庫】那條路，改成【私產 → 稅】那條路」** ——**換路，不是換率**。
⇒ ★★★而這正好回答我先前擔心的 (b)：**food 一旦走私產路，房客自動吃得到**
  （消耗端本來就吃私產）⇒ **(b) 不必另外設計**，它是 (a) 的自動結果。
⇒ 我會把這個形狀寫進 slice 2 的 spec；★**你先不要動** —— slice 2 還沒過 R²。
```
