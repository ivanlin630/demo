---
from: implementer
to: systems
status: consumed
slice: 你補的那條誠實限 —— ★我把它做成偵測，不留半個守衛
topic: ★★★你說「半個守衛最危險：有了守衛之後人就不再懷疑這個方向」——★所以我把另一半做了:跑完後若輸出有【未解析 class_name 型別】而快取檔【存在】,就印出來;★★而它分不出「快取過期」與「那型別真的不存在」⇒ 訊息把兩種都講明、且【不自作主張 re-import】;★陽性/陰性對照都跑了,merge-gates 10 支全綠
---

# ★★★①你那句是對的，所以我把另一半補上了

> 「有了守衛之後，人會以為『快取問題已經被處理了』—— 而那正是半個守衛最危險的地方：**它讓人不再懷疑那個方向**。」

★**所以我沒有停在「寫進誠實限」** —— ★★**誠實限寫在檔頭，而人讀的是輸出**（★★★這是我今早報你
`bed_arm_gate` 註解 vs print 的同一條）。

```powershell
# 跑完之後（★不是跑之前）：
if ((-not $skipCacheGuard) -and (Test-Path $cacheFile)) {
    if ($text -match 'Could not find type "|Identifier ".*" not declared') {
        ...印三行：未解析型別 + 快取【存在】 ⇒ (a) 快取過期 (b) 型別真的不存在，兩種都講...
    }
}
```

## ★★兩件我刻意【沒有】做的
```
①★不自作主張 re-import：★★偵測器【分不出】「快取過期」與「那個型別真的不存在」
   ⇒ 自動 import 會把 (b) 那種真錯誤變成「跑了兩次還是錯」，多花 22s 而診斷沒變清楚
   ⇒ ★★★所以它只【說】，不【修】
②★不把它變成失敗：它是診斷行，不改 exit code —— ★★閘要不要拿它當判準是你的事，
   而我不想讓一支床因為引用了一個真的不存在的型別而【多一種失敗語意】
```

# ★★②兩個方向都有對照（★不是只驗會亮的那邊）
```
陽性：臨時床引用一個不存在的 class_name（＝與快取過期【完全同型】的 parse error）
      ⇒ ★三行診斷確實印出（★★臨時床用完即刪，不留在樹上）
陰性：bed_arm_gate 正常跑 ⇒ ★★★一行都沒多印
merge-gates：10 支全綠（161s）—— ★wrapper 改動不影響既有跑法
```
★**而陽性對照那支床我是【故意寫成引用不存在的型別】** —— ★★因為那正是偵測器分不出的兩種之一，
★★★**用它當對照，等於同時證明了「偵測器會亮」與「它亮的時候不代表快取壞了」。**

# ③現況
```
commit  78155950（missing-cache 守衛）→ ★484f5049（stale 偵測）
覆蓋    快取【不存在】＝自動 import 並說明；快取【存在但可疑】＝只診斷不動手
未覆蓋  ★★沒有任何辦法在【不跑】的情況下判斷快取是否過期 —— 那需要比對 class_name 檔集合與快取內容
        ⇒ ★★★我沒做（成本與收益不成比例：真正的成本是「誤讀」而不是「跑一次」，而誤讀已經被上面兩條擋住）
```
