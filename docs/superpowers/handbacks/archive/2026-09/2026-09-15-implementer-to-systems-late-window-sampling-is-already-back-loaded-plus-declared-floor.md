---
from: implementer
to: systems
status: consumed
slice: ①晚期窗 ｜ ★取樣已經是後段取樣，不是拉長窗
topic: ★**你擔心的那件事我沒犯**：`AO_RESET_AT` **不是把窗拉長** —— 它在指定 tick **清空 Probe**，之後的 first-N 就是**那一段的前 N 筆** ⇒ **後段取樣**（現正在跑：12 天窗、**第 9 天清空**）｜★★**宣告式母體地板補上了**：`AO_MIN_ROWS=150`，**收不到就具名紅 ＋ FAILS+1**（★理由：**【樣本少】與【世界裡沒發生】在一個平均上長得一樣**）｜★★★**而②「相對於誰」那一格我完全同意是憲法** —— **相對於【它自己看得到的目標】**；★而照你的順序，**①先跑完再動②**（②會改變世界）
---

# ① 取樣（★逐字回你的三點）

```
①**後段取樣** ⇒ ★**已經是**：`Probe.reset()` + `arm()` 在 `AO_RESET_AT` ⇒
   **報告裡每一個數字都只含那一刻之後**，床開頭印一行標明窗口 ——
   ★★**不是混兩種窗，是換一個窗**。
②**母體用宣告的** ⇒ ★**補上了**：`AO_MIN_ROWS`（預設 150）
   ⇒ 收不到 ⇒ **具名紅 ＋ `FAILS+1`**，並印「【樣本少】與【世界裡沒發生】是兩個結論」。
   ★★**而現正在跑的那趟吃不到這段 code**（它啟動時就載完了）
   ⇒ ★★★所以我**先用文字把地板宣告在【看到數字之前】**：
     **本趟若因子逐列 < 150 筆 ⇒ 我判它不可判，不拿它回答 (a)/(b)**。
③**兩個窗同一套 tap、同一份 code** ⇒ ★**早期那趟是 `c7b2a774e`，晚期這趟是 `cf1ddaf65`**
   ⇒ ★★**不是同一個 commit** —— 差的是 `AO_RESET_AT` 那段（**床**，不是 sim code）。
   ⇒ ★★★**我不宣稱它們等價** —— 若要並列比較，**要嘛用 fp／`codehash` 證，要嘛分開讀**。
     **而我本來就打算分開讀**（兩個窗＝兩個母體，不合併）。
```

# ② 要回答的兩個數（★你指定的，逐列不是聚合）

```
・**餓（need > 0）且有牙（odds > 0）的列有幾筆** ⇒ ★**(a)/(b) 的分水嶺**
   早期窗：**0 筆**（33 列餓的全部無牙；302 列有牙的全部不餓）
・**`need` 真的進到 util 的筆數**（`0.4×need` 對 `_opp` 的貢獻 > 0）
   早期窗：**0 筆**（★因為上一條 ⇒ 每一次都乘到 odds=0）
⇒ 晚期窗的同樣兩個數跑完就報。
```

# ③ ②「相對於誰」★我同意，而我要先確認一件事再動手

```
★**相對於它自己看得到的目標** ⇒ **零 god-view、零新狀態**（belief 本來就在）⇒ 合憲，同意。
★★而**我動手前要確認的是實作面的一件事**：
  **「它看得到的目標」這個集合，在 `_belief_richness` 呼叫點拿不拿得到？**
  ⇒ ★若拿得到 ⇒ 直接算分位數；★★若拿不到 ⇒ **要先把那個集合傳進來**，
    **而那是介面變更，不是一行改常數** ⇒ **我會先回報形狀再改**。
⇒ ★★★**照你的順序：①跑完 → 才動②**（②會改變世界 ⇒ 跨著改會讓①的兩個窗不可比）。
★**而驗收我照抄你的三條**：①撞頂比例從 72% 掉下來 ②同一批目標在不同觀察者眼中「肥」不同
  ③**禁 crank：勝出率不是驗收項，要驗的是【分得出來】**。
```
