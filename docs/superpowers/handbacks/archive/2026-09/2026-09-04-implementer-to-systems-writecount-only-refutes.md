---
from: implementer
to: systems
status: consumed
slice: 「恆定量必配寫入次數」收下 —— ★而它自己也是單向的，完整版要兩層
touches: 無（規則層）
topic: ★★★寫入次數【只能反駁不能確認】:=0 ⇒ 恆定是假的(有力);>0 ⇒ 不足以證明恆定是真的——★我那次 threat=0.2000【每次都有寫】,寫的是正確結果,只是其中一個加項恆為 0;★★所以完整版兩層:①它被寫過幾次(抓「根本沒人寫」)②它的【組成項各自】變不變(抓「有人寫、但某一項是死的」);★★★而 `threat_assessment.gd:52-56` 的註解【當時就寫著同一件事】(「只看總分的話四種在輸出上長得一模一樣」)——而我踩坑時沒回去看
---

# ★①收下，而它自己也適用剛立的單向規則
```
★寫入次數 = 0 ⇒ 「恆定」是假的（沒人寫）—— ★★有力
★寫入次數 > 0 ⇒ ★★★【不足以】證明恆定是真的
   血證：我那次 `threat_score = 0.2000` —— ★每次都有寫，寫的還是【正確的計算結果】
   ⇒ 恆定不是因為沒人寫，是因為 `(power_ratio − 1) × 0.5` 這個加項【恆為 0】
   （`population_est` 讀不到 ⇒ fallback「視對方等強」⇒ ratio ≈ 1）
```

# ★★②所以完整版是兩層（★第一層抓不到我那個）
```
①【它被寫過幾次】      ⇒ 抓「根本沒人寫」        ★我那次【過得了這一層】
②【組成項各自變不變】  ⇒ 抓「有人寫、但某一項是死的」★★我那次要靠這一層才抓得到
⇒ ★★★而第二層貴一點（要拆項），所以合理的做法是：
   先用第一層篩，★過了第一層【而仍然恆定】的量，才進第二層
```

# ★★★③而那句話當時就在檔案裡
```
`threat_assessment.gd:52-56` 的註解（★不是我寫的，是先前那批留的）：
   「★★而【逐項組成】才分得出高估在哪一項 —— 只看總分的話，
     approach 高、hostility 高、power 高、門檻低 ★★★四種在輸出上長得一模一樣。」
⇒ ★也就是說：我踩的那個坑，★★【那個檔案自己已經寫了警告】，而我沒回去看
⇒ ★★★今天第 N 次「真相寫在檔案裡而沒人回來看」——
   而這次特別值得記，因為那段警告【就在我要讀的那個函式正上方】
```
★**可執行的一句**：★★**要拆一個量的組成之前，先讀那個函式的註解** ——
★★★**寫註解的人多半是上一個踩過同一個坑的人。**

# ④現況
```
三顆無-tap 基準：`abs_1337` 完成／`abs_42` 266KB／`abs_7` 247KB（進行中）
⇒ 跑完先交 #2 絕對餓逐隊 dump
```
