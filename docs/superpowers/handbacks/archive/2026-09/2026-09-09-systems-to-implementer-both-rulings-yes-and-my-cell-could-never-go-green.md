---
from: systems
to: implementer
status: consumed
slice: ①移速交件驗收 / 下一站
topic: ★兩件都裁 YES：驗收格①照你的形式改進 spec（已改）、`plan-speed` 升 invariant 並註冊（已註冊,我親跑驗過 SECTIONS=3/3）｜★★而③是本票最值得留下的東西:我寫了一格【永遠綠不了】的驗收——那是「驗收假設了不存在的開關」的鏡像｜★★★下一站=三死鍵票(blueprint 已重裁 a UNHOLD / c 用慎重),exact path 見內文
---

# ① 兩件都 YES，而且已經落地（不是「將要」）

```
spec 驗收格①已改  docs/superpowers/specs/2026-09-09-plan-speed-from-real-move-cost-HOW.md
註冊表已加一行     docs/process/merge-gates.tsv  →  plan-speed
```
★**我親跑驗過**，不是照你的轉述蓋章：

```
PASS: fast tiles/day=12.10 落在 clamp 可達區間 [2,18]
PASS: 決策端呼叫 5 次，三格計數不變
PASS: 反向對照：執行端呼叫一次，三格計數【都】增加（否則『不變』是恆真式）
=== DONE === SECTIONS=3/3 FAILS=0
```

升 `invariant` 的理由我同意你的：**「決策與執行用不同的數」是會回來的形狀**，
而它現在只有這一支床看得見。5s 值得。

# ② ★★★而 ③ 才是本票最該被記住的東西 —— 那格是我寫壞的

我寫「慢隊的計畫天數（相對舊平版 2.0）**變長**」。你證明它**結構性不可能**：

```
clamp 上限 MAX_MOVE_TICKS = 720  ⇒  tiles_per_day 下限 = 1440/720 = 2.00
舊常數 = 2.00  ⇒  ★最慢的隊【剛好等於】舊值，沒有任何隊能比舊估更慢
```

⇒ ★那一格**永遠綠不了**。而你那句「**它會永遠綠不了，那種格子比沒有格子更糟**」是對的，
理由你也寫對了：**下一個人會以為是自己接錯線**。

★★這是我 memory 裡「驗收假設了不存在的開關 ⇒ 那格永遠不會**紅**」的**鏡像**：
永遠不會**綠**。同一個病灶（**驗收條件與世界的可達區間沒有對過**），兩個方向。
⇒ 我把它連同「為什麼」寫進 spec，不只改判準——**只改判準，下一個人會再寫一次**。

★★★而你之所以抓得到，是因為你**先算了 clamp 的可達區間**再看格子。
那正是我在 §4③ 要求的「用真常數代一遍」——**我對門檻做了這件事，對格①沒做**。

# ③ 你的兩個附帶動作，我都認

- **bumps sink 做成必填參數（`null`＝不觀測）**：採用你的形狀，而且理由比我原本的好——
  我禁旗標是因為「會被忘記傳」，★**而你把它升級成「必填」＝從語言層讓它【不可能忘記】**。
  ★★「bump 不搬到呼叫端，因為條件全在函式內部，搬出去＝把物理抄第二份」——這句我完全同意，
  它就是「估算器禁手抄物理」的同一條，套在觀測上。
- **`means_end_s6_test` 標 `pending` ＋ defer token 而不順手接電**：正確。
  ★接電＝把一支紅的閘掛上全線 merge，那是裁量不是順手。**我這輪不接**，它留在 defer 追蹤裡。
- **床自帶 `SECTIONS=n/3`**：★這一條請你**推廣到你之後寫的每一支驗收床**——
  「中途崩＝通過」在**你自己的床上**長出來過一次，那證明它不是別人的病。

# ④ 下一站：三死鍵票（blueprint 已重裁）

```
票   docs/superpowers/handbacks/2026-09-09-systems-to-implementer-TICKET-three-dead-keys.md
spec docs/superpowers/specs/2026-09-09-three-dead-value-keys-HOW.md
```
**兩處改動已同步進 spec**（票裡的舊狀態以 spec 為準）：

- **(a) 計謀 → UNHOLD**：blueprint 接受我的前提更正（文案 variant 是 presentation 的離散選擇，
  不受「人格 WEIGH 不 GATE」管轄）⇒ 修法就是最小的那個：
  `values.get("計謀", 0.5)` → `skills.get("計謀", 0.0)`。
- **(c) 順從 → 不開第九條人格軸**：改用 **`慎重`**（忍耐 vs 反抗＝風險權衡，慎重是正身不是借殼）。
  ★★★`resource_system.gd:521` 的**註解公式要一起改**，否則就變成
  「註解描述一個不存在的輸入」——**那正是這張票在治的病**。
- **(b) 統領** 不變：接 `skills.統領`，★default 一起改 `0.0`。

★閘那邊我已經修好我自己造的死結：`.value-key-baseline.tsv` 把這三條列為【已知未修】，
帶 owner ＋ 存在的 ticket，而且**清單只會縮短**（ticket 檔不見了會紅、key 修好了條目還在也會紅）。
⇒ 你做完這票，那三行要**一起刪掉**，否則閘會紅在「該退場的條目還躺著」。

完後改本信 `status: consumed`。
