---
from: implementer
to: systems
status: consumed
topic: ★清掃完成：9 支、21 字（18 在字表內 ＋ 3 在字表外）｜★★而我差一點用自己造的工具違反用戶那條硬規 —— 過程寫在內｜★★★lint 剩的 6 檔全是【討論這些字的信】
---

# 一、落地

```
branch   feat/simp-clean-9（base = origin/main 5985ccd0c）
commit   a71fd0f00（已 push，git ls-remote 問伺服器同 sha，worktree clean）
```

```
现→現 ×10   滞→滯 ×4   应→應 ×1   过→過 ×1
两→兩 ×1    卖→賣 ×1   ★邻→鄰 ×2  ★缮→繕 ×1     合計 21 字 / 19 行 / 9 檔
```

★後兩個字（`邻`／`缮`）**不在字表裡**——我是在 `faction_ai_system.gd:6731-6733`
**那幾行上直接看到的**，就在我要改的 `现` 旁邊。你那條誠實限（字表 54 字不是全集）
在這一票上**當場兌現了一次**。

# ★★二、而我差一點用自己造的工具違反用戶那條硬規

我本來想「既然字表不全，我自己列一份更大的」。列了、掃了，然後看輸出：

```
world_state.gd:5  字=只     # 世界以【時間】思考…要換 tick 粒度只改這一顆
world_state.gd:249 字=游    # …而下游從 ledger 讀出來的三個「0 筆」
```

★`只`、`游`、`里` **在正體中文裡本來就是這樣寫**。我那份清單是從簡體字集抄來的，
它包含**兩邊共用的字** ⇒ **照它改會把正確的正體字改掉**，
而那正是用戶立「禁盲目全替換」那條規矩要擋的事。

★★**我丟掉了那份清單**，只保留「我逐字看過、確認正體無此寫法」的 `邻`／`缮`。
★★★而這是我今天**同一族的第四次**（`name=N` 漏全形／hash 漏中文條件句／`exit 數字` 漏 `exit $fail`／
這次是**清單含了不該含的**）——前三次方向是「看起來更嚴重」，**這一次的方向是會真的改壞東西**。
⇒ 差別在於前三次我只是報錯數字，這一次我手上拿著 sed。

# ★三、風險核查（你說改字會改變 grep 命中——我逐條查過）

```
註冊表 expect 欄含這 6 字的列        = 0
這 9 支被註冊表 cmd 直接叫到的        = 0（它們是被間接載入的 production／床）
hook 裡 grep 這些字的                = 只有 doc-line-cap.sh 自己的字表（設計上排除自己）
4 處字串字面量（print／assert 訊息）   = 沒有任何閘或 hook 釘它們
Probe 鍵名                           = 21 處無一落在 Probe 鍵上
```

# 四、驗證

```
diff              19 行改動｜★每行套上映射後【逐字等於】新行（機械驗，不是目視）
字表殘留（9 支）    0
godot --import    ★零 SCRIPT ERROR／Parse Error
                  ★★rc=1 —— 而我在【乾淨的另一棵樹】跑同一指令也是 rc=1
                  ⇒ 基線行為，不是本改動；附帶結論：--import 不能當判準閘用
```

★**誠實限**：我沒有跑 headless 回歸（機器上有 2 支 Godot 在跑 B3，我不跟量測搶）。
本票是註解／字串字面量的字元替換、且 diff 已機械驗為純換字 ⇒ 我判斷風險極低，
**但「極低」不是「驗過」**。要我等機器空了補跑就說。

# ★★★五、lint 現在剩 6 檔，而那 6 檔不該改

```
全部是 handback 信 —— 它們含那些字是因為信在【討論】那些字，
包括你派工給我那封（裡面列了 两/颗/种）。
⇒ 跟 hook 排除 doc-line-cap.sh 自己是同一個形狀：偵測器與【談論偵測器的文件】都會自我命中。
★我沒有動它們，也【不建議加第二支閘】去排除信箱 —— 雜訊會讓原本那支開始被忽略。
★★但它意味著 [simp-lint] 的計數從今天起會長期停在一個非 0 的數，
  而那個數的內容是【信】不是【code】。讀的人分不出來 —— 這一點你可能想在別處記一句。
```

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
