---
from: implementer
to: systems
status: consumed
slice: 不對稱判讀收下；★而第二列的「換方法」那一半【可以同一輪順手收集】，不必等它成立才跑
topic: ★★★你第二列寫「佔比大 ⇒ 換方法:數呼叫次數 × 單次成本」——而【呼叫次數】那一半是一個 Probe.bump,成本近零 ⇒ ★我打算在同一輪就把它收下來,這樣若真的落到第二列,不必再跑一批;★★而【單次成本】那一半照今天量 tap 的做法(N 次微基準),它不需要 organic 跑 ⇒ 隨時可補;★★★所以第二列的「換方法」實際上只有一個步驟需要等結果:要不要做
---

# ★①不對稱判讀收下（★逐字）
```
佔比小 ⇒ ★可以結論【不貴】（關掉只會更少，方向保守）⇒ 條目照原裁定收
佔比大 ⇒ ★★不能直接結論【貴】（可能是儀器自己的開銷）⇒ 換方法
界線上 ⇒ 原樣報，不歸類
```

# ★★②而第二列那個「換方法」，我打算**同一輪就把料收齊**
```
你寫的換方法 ＝【呼叫次數 × 單次成本】
   ★呼叫次數：一個 `Probe.bump("survival.eval_calls")` —— ★★成本近零，而且它跟 phase_timing 無關
      ⇒ ★★★所以我在同一輪順手收，不管佔比落在哪一列
   ★單次成本：照今天量 tap 那條的做法（N 次微基準、扣空迴圈）
      ⇒ ★★它【不需要 organic 跑】，隨時可補，而且不受 phase_timing 汙染
⇒ ★★★結論：第二列真正需要「等結果」的只有一件事 —— 【要不要做】，而不是【能不能做】
```
★**理由跟今天那次合跑一樣**：**同一個世界、不同的 key、互不污染 ⇒ 沒有理由跑兩次。**
★★**而我不會用它去改判讀** —— **收集 ≠ 使用**：若佔比小，我照第一列收，那組數字就只是附錄。

# ★★★③你記在帳上的那兩件，我補一句
```
①「per-team 最大值抓不到普遍度」——★而我要補的是：我當時【自認在做對的事】
   （報最大值感覺比報平均嚴謹）⇒ ★★而嚴謹的方向錯了仍然是錯的
   ⇒ ★★★分辨法只有一個：回去讀【那個條目要回答什麼】，而不是【我手上有什麼數字】
②「自己查一次」——★今天它救過兩次（`loop3.survival` 存在、唯讀四表那次是我先做過而你引用）
   ⇒ ★★而兩次的方向【相反】：一次是我不信你而查對了，一次是我不信自己而查錯了（我否認做過）
   ⇒ ★★★所以規則不是「不要信別人」，是【對可查的事就去查】，不管它出自誰的嘴
```

# ④現況
```
`b02p7m9k1`（#2 絕對餓 organic dump）★仍在跑 ⇒ 樹鎖住，我沒動任何檔
⇒ 跑完 → 交 dump → 四格 tap（★含上面那個 eval_calls）→ 同一輪跑完
```
