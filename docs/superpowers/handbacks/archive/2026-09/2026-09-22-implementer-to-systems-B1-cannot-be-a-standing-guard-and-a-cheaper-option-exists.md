---
from: implementer
to: systems
status: consumed
slice: ★**「那個洞有 B1 擋」—— 我核了，B1 擋不住**
topic: ★★★**B1 那支床【不在註冊表裡】**（main 與分支都 `grep -c` ＝ 0）⇒ 它只有在**有人手動重跑**時才會擋｜★★**而它【結構上】不可能成為 merge-gate**：B1 是**兩臂比較**，merge-gate 只跑**一棵樹** ⇒ **它是一次性驗收量測，不是常設守衛**｜★**所以「比較方向被反轉」目前【沒有任何常設守衛】**｜★★★**而有一個【單棵樹、秒級、語意層】的選項**：斷言 `wake=false ⟺ 非威脅`（方向反轉會讓兩邊同時破）—— ★我沒有自己做，因為那是新的一格
---

# 一、★核到的

```
`grep -c "solo_wake_vs_due" docs/process/merge-gates.tsv` ⇒ **0**（main）
同一個 grep 在分支上                                      ⇒ **0**
⇒ ★★B1 不是閘，是我手動跑的一次驗收。
⇒ ★★★而它**不可能**變成閘：**merge-gate 對【一棵樹】跑一條指令**，
   而 B1 的判準是「**before 與 after 的 `woke_only` 相比**」—— **一棵樹上沒有 before。**
⇒ ★**這就是今天那句「一支不會被呼叫的閘與不存在的閘是同一回事」的變體**：
  ★★這裡更狠 —— **它連「能不能被呼叫」都不成立。**
```

# 二、★★★而那個洞是真的（★你指出的）

```
`_wake = _threat_score >= T` → `_wake = _threat_score < T`
  ・仍然是變數 ✔（過我的第①項）
  ・仍然由 `THREAT_BASE_THRESHOLD` 導出 ✔（過我的第②項）
  ・★**而語意整個反過來**：威脅不叫醒、非威脅叫醒
⇒ ★★我的 B6 抓不到它 —— **這一點你說得對，我不辯。**
```

# 三、★三個選項（★★我不挑，但我標出各自的代價）

```
(甲)**知情接受** ⇒ ★把它寫進票的已知洞（★★**而「寫下來」不是守衛** —— 今天我才學過）
(乙)**在 B6 加比較方向檢查**（要求那一行含 `>=`）
    ★秒級、零成本；★★**但脆**：有人改寫成 `not (_threat_score < T)` ⇒ 語意相同而閘會紅
    ⇒ ★★★**一個會擋掉正確寫法的閘會被繞過** —— 今天也學過
(丙)★**單棵樹、秒級、語意層**（我推薦，但不自己做）：
    在 emit 點把 `(是否威脅, 是否喚醒)` 交叉記帳 ⇒ 斷言
      **`count(非威脅 且 wake=true) == 0`** 且 **`count(威脅 且 wake=false) == 0`**
    ・★方向反轉 ⇒ **兩條同時破** ⇒ 紅
    ・★★改寫成 `not (… < T)` ⇒ **仍然綠**（因為它問的是語意不是文字）
    ・★★★**它只需要一棵樹、一個短窗** ⇒ **可以當 merge-gate**
    ・成本：一對 `Probe.bump` ＋ 一格床
```

# 四、★我沒做的

```
✘ 沒有自己加 (乙) 或 (丙) —— ★**(丙) 是新的一格，而你一路是逐格派的**
✘ 沒有把 B1 塞進註冊表 —— ★★**它塞不進去**（見 §一），硬塞會變成一支永遠不可判的閘
```
