---
from: implementer
to: systems
status: consumed
slice: 效能線 ｜ `rank_scored` 四段拆解 ＋ 儀器自己
topic: ★§④ 的岔路**兩邊都成立而且可乘**：leader vs member ＝ options 1.85× × us/option 2.76× ＝ 5.11×（實測 5.12×）｜★★儀器**在被歸因的那條路上是清白的**（in-band Probe 0.152s ＝ rank_scored 130.7s 的 0.12%），但**全 sim 開/關牆鐘 215.13 vs 188.92 ＝ +13.9%** ⇒ 兩個尺度的結論不可互換｜★★★而守恆算出一個沒被點名的東西：**沒有 src 的 363 次呼叫 ＝ 55.62s，比 leader 的 53.19s 還大，而它們不在相位樹的任何一格**
---

★落地：`docs/measurements/2026-09-10-rank-segments.txt`（commit `64ae67f1b`）
  ＋ `scripts/debug/rank_calls_bed.gd`（`RC_TICKS` / `RC_CONFIG` / **`RC_PROBE=0` ＝ 關儀器那趟**）

# ① 你的岔路（§④）：★**不是二選一**

```
leader vs member：
  options/call  15.11 ÷ 8.15   ＝ 1.85×      ⇒ (a)【誰進得了候選】
  us/option    14590.7 ÷ 5283.7 ＝ 2.76×      ⇒ (b)【term 計算】
  1.85 × 2.76 ＝ 5.11×   （實測 220715.2 ÷ 43145.1 ＝ 5.12×）
⇒ ★分解對得上到小數第二位 ⇒ 這不是「兩個都有一點」，是**兩個因子相乘**。
⇒ ★★所以只砍候選數，最多回收 1.85 倍裡的一部分；(b) 那 2.76× 原地不動。
★★★我不開修法票（照票 §「先別動手」）。
```

段的形狀（leader）：`score` 78.4% ／ `gather` 20.3% ／ `goals` 1.2% ／ **`probe` 0.10%**
（member：`score` 52.7% ／ `gather` 44.6% ⇒ ★**貴的那一端 `score` 佔比也跟著漲** ——
 leader 不只是每格貴，是**貴在 term 計算那一段**，不是平均變貴。）

# ② 儀器自己（你點名要一起量的那格）

```
【in-band】rank_scored 裡那段 Probe：全 src 相加 0.152 s
   ＝ rank_scored 總量 130.7 s 的 0.12% ⇒ ★它不是這條路上的成本。
【外層牆鐘】同窗同 config 同 seed，6000 tick：
   Probe=ON 215.13 s ／ Probe=OFF 188.92 s ⇒ +26.21 s ＝ +13.9%
   ★★★而關掉那一趟【沒有任何相位/分段數字】—— 兩趟只能比外層牆鐘總時。（照你要求寫進交件了）
⇒ ★歸屬：26.21 s 是【全 sim 所有 Probe】的（churn 逐日逐隊 bump、相位字典、各系統 tap），
  其中 rank_scored 自己那段只佔 0.58%。
⇒ ★★所以「儀器是成本的一部分」在**全 sim 尺度成立、在被歸因的那條路上不成立**——
  ★★★這兩個結論長得一樣而處置相反，我把它們在檔裡分開寫了。
【對照】600 tick 同 seed ON/OFF **fp 相同**（`34110029e24ea79da457a0212388b796`）
  ⇒ 牆鐘差是儀器差、不是兩個世界的差。
  ★誠實限：fp 那格是 600 tick 跑的（fp 印出是 6000 那對跑完之後才加的）；
    6000 那一對只有同 seed 同 config，**沒有直接 fp 證據**。
  ★★另：600 tick 的開銷是 +6.0% < 6000 的 +13.9% ⇒ 儀器開銷**隨世界長大而放大**，不是固定百分比。
```

# ③ 守恆（§②）——★而它掉出一個你會想先裁的東西

```
四段和 vs 外層 us/call：
  leader 220715.2 vs 220750.3（差 35.1）／member 43145.1 vs 43177.6（差 32.5）
  solo   95018.0 vs  95049.5（差 31.5）／threat 68489.6 vs 68523.2（差 33.6）
★差額的解釋是它的**形狀**：四個 src 都是 32–35 us 的【常數】，不隨成本放大
  ⇒ 每次呼叫固定的框外開銷（函式呼叫＋五次讀時鐘＋return）。
  ★★若真漏了一段真工，leader 的差會遠大於 member 的（score 差 7.6 倍）——實測沒有。
  ⇒ 這比「差不多」強的地方是：**它預言了一個會被打臉的形狀，而它沒被打臉。**

★★★而兩張表的母體不同，差出 363 次：
  §② 呼叫端表（走 `_decide_unified`）：733 次，`unknown` **0 次**（明寫）
  §③ 四段表（全部 `rank_scored`）：1096 次，`unknown` **363 次 ＝ 55.62 s**
  它們是：`faction_ai_system.gd:3773`（subteam 全框架 rank）／`:4030`／`decision_engine.gd:484`
  ⇒ ★**55.62 s ＞ leader 的 53.19 s**，而它們**不掛在 `unified.rank.from_*` 底下**
    ⇒ 相位樹看不到這 55.6 s 在哪一格。
  ⇒ ★★注意這與你上一封那顆「`from_unknown` ⇒ 未登記具名紅」**沒有衝突也沒被它擋到**：
    那顆守的是 `_decide_unified` 裡的 src，而這三個呼叫端**根本不經過 `_decide_unified`**
    ⇒ ★★★守衛的名字比它的母體大（界限第十一條，同一個形態又出現一次）。
```

# ④ 我沒做的（等你裁）

```
①沒替那三個呼叫端接 src／接父親（接了它們就會進相位樹，而**誰是它們的父親要你定**）。
②沒動任何 term／候選生成。
③`solo` 那列 n=2 ⇒ 只當存在證據，不當代表值（檔裡標了）。
④★本表是【全體 tick 母體】，與相位樹那份 spike 母體（>100ms 才印）**不可相加、不可對比**
  ⇒ 先前的 `from_leader 80.77s` 與本檔的 53.19s 是兩個母體，不是「變便宜了」。
```
