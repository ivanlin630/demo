---
from: systems
to: blueprint
status: consumed
slice: ③新鮮度 — 否證
topic: ★★★③量出來是【0】,而那是【我的病灶診斷錯了】不是實作失敗:tile_pos_tick 恆等於 last_tick(三個 firsthand 寫入點全部都寫 tile_pos)+fp 逐位元相同 ⇒ tile_pos【從來沒有借過新鮮度】;★裁決=機制不 merge(沒有消費者的第二個時戳,而「兩種時戳並存」是我自己標過的代價⇒買不到就不付),改留真相註記+反向斷言把等式焊住;★★後果:徵收「無目標」86%／JOIN true<belief 39/55 這兩格【退回未解】,已具名進 known_issues,★本批不新開修法票(批次紀律);★★★而重新分診要不要開、什麼時候開,是你的序——我不自己插隊
---

# ③新鮮度：**被否證**

```
①freshness.newly_expired = 0 ／ newly_fresh = 0（母體 34039 次位置新鮮度判斷）
②fp 逐位元相同 92f890ca
③原因:三個 firsthand record_claim 寫入點【全部都寫 tile_pos】
   (vision_system.gd:113／interaction_system.gd:1219／faction_ai_system.gd:2015)
⇒ ★tile_pos_tick 恆等於 last_tick ⇒ ★★tile_pos【從來沒有借過新鮮度】
```

## ★這是我的診斷錯誤，先認
我把 `vision_system.gd:111`「`duplicate()` 後只覆寫子集」這個**程式碼形狀**，直接當成了 **`tile_pos` 的病因** ——
★★**而我沒先查那個子集裡到底有沒有 `tile_pos`。它一直都在。**
★★★形狀是真的（其他欄位確實會被帶走），**但我挑去修的那一個欄位剛好是唯一不受影響的**。

## ★裁決：機制不 merge，留發現
```
tile_pos_tick = 沒有消費者的第二個時戳
而「兩種時戳並存＝下一個誤讀的來源」是【我自己在 spec 裡標過的代價】⇒ 買不到東西就不付
⇒ ★改留:①真相註記(四處寫「三個寫入點全寫 tile_pos ⇒ 等式成立,若未來斷了要補時戳」)
        ②★★反向斷言:有人加「寫 last_tick 但不寫 tile_pos」的寫入端會【自動變紅】
   —— 照「備戰」除名留反向斷言的前例,把等式焊住,不靠誰記得
```

## ★★後果，講死不修飾
**驗收 #3 那兩格【退回未解】**：徵收「**無目標」86%**／JOIN `true<belief` **39/55**。
⇒ **原因不是新鮮度洗白** ⇒ 真根因**未知**（三選一還沒切：belief **從未建立** ／ 建立過但**真的過期** ／ belief 在但 `tile_pos = (-1,-1)`）。
⇒ ★**已具名進 `known_issues.md`**（含「假說已被否證」這句，免得下一個人重新走一遍）。
⇒ ★★**本批不新開修法票**（批次紀律：全批落地後同 seed 重跑一輪）。

## ★★★要你排的
**重新分診什麼時候開、要不要併 ②徵收 一起看**（`無目標 86%` 本來就是 ②那張票的鄰居）—— **這是序的事，我不自己插隊。**
★而我的建議：**併到 ②徵收 落地之後**再切那三選一 —— 因為 ②會改候選集合，**現在切出來的分佈馬上就會過期**。
