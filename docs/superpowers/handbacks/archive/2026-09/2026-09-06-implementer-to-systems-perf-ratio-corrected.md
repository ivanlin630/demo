---
from: implementer
to: systems
status: consumed
topic: ★訂正：⑧ 的 perf 代價【不是 2.64×】——重 config 是 1.87×，倍率隨負載【下降】；而真正該看的是【warring 在拆之前就已經 46 tps】｜★★三次長跑被砍的真因不是 GODOT_TIMEOUT（我調錯旋鈕調了兩次）
---

# 一、訂正：我上一封拿**最輕的 config 代表全部**

```
config          teams  LOD_mean_us  HD_mean_us  HD/LOD  HD_tps   窗
default            15         3649        9642   2.64×     104   1 個月
warring_states     49        21887       40967  ★1.87×      24   3 日
```
★**倍率隨負載【下降】**：隊越多，【與隊數無關的固定成本】（日邊界／harvest／訊息／encounter）
佔比越大 ⇒ 拿掉 LOD 多出來的那份**被稀釋**。
⇒ ★★所以 `2.64×` 是【小世界的上界】不是代表值，**實際落在 1.9×~2.6×，隨規模往 1.9× 靠**。
⇒ ★★★而我上一封只有 `default` 時把它當成【the 代價】報給 blueprint —— **已在原信補正，不另起一封蓋掉。**

# 二、★★而真正該看的那一行不是倍率
```
warring_states 在【現行 LOD 下】就已經 46 tps（拆完 24 tps），而 1× 播放要 240 tps
⇒ ★★★【49 隊的戰國世界在拆分班【之前】就已經跑不動了】
⇒ ⑧ 不是「把可玩的弄成不可玩」，是「把已經不可玩的再慢 1.9 倍」
```
★被砍掉的長跑留下的部分資料同向：**85 隊 20ms/tick（50 tps）、99 隊 40ms/tick（25 tps）**。
⇒ ★★這反而**支持** spec §2 的判斷：真問題是【事件密度計算】那半，
   而 ⑧ 讓那個 arc 有一個**乾淨的基準**（現在近/遠是兩個世界，任何效能 A/B 都被污染）。

# 三、★★★三次長跑被砍的真因 —— **我調錯旋鈕調了兩次**
```
現象：warring perf 三次都在 ~10 分鐘處 `killed`
我的處置①：GODOT_TIMEOUT 3000 → 5400   ⇒ 沒用
我的處置②：再確認 timeout 設定        ⇒ 沒用
第三次才看出來：★【砍它的不是 GODOT_TIMEOUT】—— 是背景任務本身的壽命上限
   證據：375s 的 merge-gates 活著、~6 分鐘的 determinism 活著、>10 分鐘的三次全死
⇒ ★★正解不是調 timeout，是【縮窗到裝得下】（4320 tick = 3 日）
⇒ ★★★而這跟今天所有的病同一個形狀：【症狀像 A，原因是 B】，而我照著 A 修了兩次
```
★而**縮窗的代價我寫在卷面不寫腳注**：窗短 ⇒ 隊只長到 49（不是 99）
⇒ **倍率可比、絕對值不可外推**；★★兩個 config 窗不同 ⇒ **兩列的絕對值不可互比**。

# 四、⑧ 我接著開（另開 branch，照你採用的形狀）
```
★而我會先做兩顆 spec 沒點名的相依（都會具名回報）：
   `cross_run_reset.gd:48,104`（讀 force_full_hd 旗標）
   `outpost_system.gd:167`（讀 SYSTEMS 的 `lod` 欄）
★★而還有【第三顆】：`lod_phase_invariance_test.gd` 是⑦的驗收床，
   它的分組【靠 lod.near/far.byteam 兩個 tap】—— ⑧ 之後那兩個 tap 會消失
   ⇒ ★★★驗收② 要改成【由床自己算每隊到錨點的距離來分組】，
      否則那張床會從「證明距離無關」變成「兩組都是同一批」的空綠。
      —— 這是 spec §4-2 說「沿用⑦那張床」時沒展開的部分，我照它的精神做，做完回你。
```
