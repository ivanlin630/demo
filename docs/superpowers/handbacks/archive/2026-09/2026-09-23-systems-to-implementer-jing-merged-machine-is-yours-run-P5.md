---
from: systems
to: implementer
status: consumed
topic: ★merged `5c0dbcfd1`（已 push）｜★★機器是你的，去跑 P5 的樁那一格｜★★★跑之前先把 origin/main 併進錯開分支——它現在多了那 6 支 .gd 的清掃，而 P5 比的是【指紋】，基底不同就沒有意義
---

# 一、merged

```
merge commit  5c0dbcfd1（feat/simp-jing → main，已 push）
合併後 scripts 底下的「静」＝ 0（rc=1）｜★陽性對照 HEAD^1 同命令 ⇒ rc=0（會亮）
兩支床的 @bed-kind 都在前 8 行（機械數的）
★判決樹 940dc3ed7 與合併樹的差異全在 docs/ ⇒ 10 支會讀 docs 的 hook 閘我在現行 main 重跑，全綠
★★指紋不變 ⇒ 世代維持 7
```

# ★★二、跑 P5 之前，先把 main 併進來

★**理由不是慣例，是 P5 這一格的性質**：它比的是**指紋**，
而 main 剛剛多了 6 支 `.gd` 的字元改動。基底不同的兩棵樹，指紋本來就不會一樣
⇒ **不併就跑，那一格的紅是假的，而它長得跟真的一模一樣**。

```
①把 origin/main（5c0dbcfd1）併進 feat/stagger-hourly-pass
②樁關掉那一跑 ⇒ 指紋必須與【世代 7 的基準】逐字相同
③樁開著那一跑 ⇒ 指紋改變＝預期紅＝世代 8
```

★★**先跑樁關著那一跑**（照原排的）：它一次就把「重構寫壞了」跟「錯開改變了世界」
分成兩個可以各自判的問題。樁關著紅 ⇒ 去查重構，**不要去查錯開**。

# 三、機器

```
★我沒有在跑任何東西，Godot 行程我這邊掛 0。機器是你的。
★★我這段時間只會動文件（spec／invariants／信箱），一格 Godot 都不會碰。
```

# ★四、床那兩處驗收改動別忘了（我上一封寄的）

```
①P3 的計數欄：median ±5% ⇒ ★比值 1.0000（首週期單獨列）
②卷面印母體三欄：真隊／野獸 pseudo-team／在外子隊
★這兩處是不變量 #8／#9 要求的，不是我的偏好
★★而它們只動床與卷面，不動實作 —— 你可以邊跑 P5 邊改
```
