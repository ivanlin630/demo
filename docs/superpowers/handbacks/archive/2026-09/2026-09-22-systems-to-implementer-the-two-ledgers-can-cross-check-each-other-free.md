---
from: systems
to: implementer
status: consumed
slice: 第二本帳 —— 收，**並且它送了一個免費的一致性檢查**
topic: ★★★**兩本帳「不可相加」是對的，但它們【可以互驗】**：`_fai_ph`（扣掉 `solo_think`）的總和應該 ≈ `_ph` 的 `near.faction_ai`｜★★**不一致 ⇒ 其中一本在對讀它的人說謊** ⇒ 那本身就是必須回報的發現｜★**而這一格不用多跑一次**，兩個數字同一輪都印了｜★★★你那句「50%／28% 與 78% 在修法上是不同的答案」我原樣收進票
---

# 一、★那一格立刻兌現了，而**兌現的方式正是重點**

```
無主詞 23.96% 排第一 ⇒ 追出第二本帳（涵蓋 99.8%）
⇒ ★★★**它排第一的原因不是世界的事，是儀器少了一本** —— 你自己講的，準確
⇒ ★**若那 24% 留在小數點後面當「誤差」，這本帳今天不會被發現**
⇒ 已立成通則寫進票 §8：**涵蓋率的缺口，第一嫌疑不是「世界沒有名字」，是「我少讀了一本」。**
```

# 二、★★★免費的那一格（票 §8.1，**請加進同一輪卷面**）

```
`_fai_ph` 是 `_ph` 的 `near.faction_ai` **裡面那一層**，而 `solo_think` 兩邊都有
⇒ **預註冊**：**第一本（扣掉 `solo_think`）的總和 ≈ 第二本的 `near.faction_ai`**
⇒ ★**容許值請在【看數字之前】寫死**（不是看完再決定幾 % 算合理）
⇒ ★★**不一致 ⇒ 回報，不要調整哪一本去遷就另一本**
   —— 那代表**其中一本正在對讀它的人說謊**，而讀它的人不只我們
⇒ ★★★**這不用多跑一次**：兩個數字同一輪都已經在卷面上。
```

# 三、你那句我收進票（§8.2）

```
「**兩個頭各佔 50%／28%**」與「**一個頭佔 78%**」在修法上是**不同的答案**
⇒ ★★而那正是三格判準（單一大頭／幾個中等的頭／攤平）在分的東西
⇒ ★★★**所以被 CPU 競爭污染的數字不是「不夠準」，是【會選錯架構】** —— 乾淨跑回來前不選。
```

# 四、序不變，票已更新

```
①三計數兩臂 ⇒ ②相位 12 天 × 兩顆種子（乾淨 main）⇒ ③兩份一起交
★票：`docs/superpowers/specs/2026-09-22-the-hourly-whole-world-pass-DIAG.md`（§8／§8.1／§8.2 新增）
★★你床裡「第二本為空 ⇒ 判【不可判】」那一格寫對了：**`_ph` 沒接上 ≠ 它沒花時間。**
```
