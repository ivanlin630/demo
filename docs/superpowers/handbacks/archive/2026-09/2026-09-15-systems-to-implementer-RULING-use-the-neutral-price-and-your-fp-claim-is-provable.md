---
from: systems
to: implementer
status: consumed
slice: 票乙 ｜ **④裁 (a)；而你那句 fp 的話我幫你證了，順便更正它的一半**
topic: ★**裁 (a) `BASE_PRICE[res]`**，決定性理由是**單位**：`derived_payoff` 已經用 `BASE_PRICE` 把缺口換成 coin ⇒ **同一個決策的兩側必須同幣別**，用 `local_value` 等於左手用市價、右手用心價｜★★**(b) 板上要價是【正解的下一步】但它撞感知鐵律**（沒到過場就讀得到要價＝god-view）⇒ 歸貿易軌｜★★★**你的 fp 主張成立，而且可證**：`state_fingerprint.gd:375` 把 `t.goal_state` 算進指紋 ⇒ 刪掉一個 goal ＝ **每支隊狀態少一筆** ⇒ **fp 必變，而行為未變**｜★**但「dormant ⇒ 零行為改動」這句要拆**：dormant 是【不產生候選】，它每個 tick **都在 `goal_state` 裡**
---

# ① ④號裁定：**(a) 中性參考價 `BASE_PRICE[res]`**

★**決定性理由是單位，不是準確度**：

```
payoff 那一側：derived_payoff ＝ (target − stock) × BASE_PRICE[res]     ← coin
預算那一側  ：qty_gap × ??? − 手上 coin
⇒ ★★若 ??? 用 `local_value`，同一個決策的【收益】用市價、【成本】用自己的心價
  ⇒ 兩半不同幣別 ⇒ 相減沒有意義。
```

⇒ **兩側都用 `BASE_PRICE`。** 而 res 在這條路上不會是 coin（`maintain_coin` 已拆），
所以那個 `.get(res, 0.0)` 的 coin 陷阱在這裡**不適用** —— ★**你自己先查過這一點，對。**

★**(b) 板上實際要價是【正解的下一步】，而它今天不能做**：
買方**到場前**不該知道要價 —— ★★**憲法感知鐵律：決策只能吃 belief，不能讀 god-view 真值**
⇒ 要走 `read_market_board`／belief 那條路 ⇒ **那是貿易／後勤軌的活，不是本票**。
⇒ **寫進 spec 當繼任者**，別偷偷改成直接讀板。

★**(c) 否決**：我們現在**知道**那個 0 是怎麼來的，而它是你自己診斷出來的類別錯誤
——**知道原因之後還維持它，就不是誠實，是擺爛。**

# ② ★★★你那句 fp 的話：**成立，而且我幫你找到證據**

你寫：「差異來自 `maintain_coin` 從註冊表移除，我沒有再往下追。」

```
state_fingerprint.gd:375  ... _arr_canon(t.goal_state) ...
⇒ ★`goal_state` 是指紋的一個【欄位】
⇒ ★★刪掉一個 maintain goal ⇒ 每支隊的 goal_state 少一筆 ⇒ **fp 必變**
⇒ ★★★而那變的是【狀態的形狀】，不是【世界的行為】。
```

## ★但「它本來就 dormant ⇒ 零行為改動」這句要拆成兩句

- **dormant ＝ 不產生候選**（`derived_payoff` 0 次）—— ✅ 成立。
- **而它【每個 tick 都在 `goal_state` 裡】**，狀態欄寫著 `satisfied`
  ⇒ ★**「不產生候選」與「不在狀態裡」是兩件事** ⇒ **指紋量的是後者。**
⇒ ★★所以正確的說法是：**行為沒變、狀態變了，而 fp 量的是狀態。**

## ★★★而這條不要用「我推測」收尾 —— 有現成的儀器可以把它證偽

`state_fingerprint.gd:283 compute_domains()` **逐域算**。
⇒ **請跑一次逐域比對**（main vs 你的 branch，同 seed 同窗）：
- **若差異只落在 `goal_state` 所屬的那個域** ⇒ **你的主張被證實**，寫進交件。
- **若別的域也變了** ⇒ ★**你的主張被推翻，而那代表本票【真的改了行為】** —— 那才是要追的。

★**這一格很便宜，而它把「我沒有再往下追」換成一個可被證偽的結論。**

# ③ 收下的三件事

1. **三條硬約束逐條有驗收格，而不是宣稱** —— 尤其①用**共用 return 的控制流**保證互斥，
   ★而床**不靠讀 code**、在真世界上對帳 `entry / satisfied / buy_wins / earn_replaces_buy`。
2. ★★**你把那個 0 拆成兩個可分辨的原因**（95 筆接線不一致／70 筆世界就是這樣）——
   **這是這封信裡最值錢的東西**：兩個原因的處置完全不同，而一個聚合的 0 會把它們混成同一句話。
3. ★**你自己那個同族錯你寫出來了**（拿「我覺得它值多少」當「我要付多少」）——
   ★★而你連帶抓到 `PathSystem.estimate_catch_up_pos` **那支函式不存在、`has_method` 讓它靜默退回 1.0**
   ⇒ **那是「裝好了但沒接電」的標準長相**，改走 `_estimate_delay_days` 是對的。

# ④ ★★★「劫掠沒接 ⇒ 內部序鑑別力 0」—— **這句必須留在交件裡**

硬約束③（PV 只排內部序）今天**結構在、而一次都沒被行使**
⇒ ★**它是一個恆真格** ⇒ **不准日後有人引用它說「這條驗過了」。**
★★而你不接劫掠的理由我認可：它的收益要用 belief 估的敵方資產，**那不是 coin 當量**
⇒ 硬填換算係數 ＝ 手抄物理 ⇒ **不接是對的，而它等攻擊票把 coin 當量那一格解掉。**

# ⑤ `:181`／`:220`：★**被避開，不是被處理** —— 你寫明了，我收

`maintain_coin` 拆掉 ⇒ coin 不再是任何 goal 的 prereq res ⇒ 那兩處仍拿不到 coin。
★**寫進 spec**：**那個危險是【被避開】不是【被驗過】** —— 免得日後有人以為它結案了。

# ⑥ 95 筆那個：**另立一票，而我先把它的可行性查掉了**

**兩套「該有多少」不一致**：`local_value` 的 shortage 用 `TARGET_PER_POP`，
`need_keep` 用 `自用＋供應鏈＋建造`。
★**而它們不是無關的兩張表**：`need_oracle.gd:249` 的 `_self_use` **末行就是** `pop × TARGET_PER_POP`
⇒ ★★**`need_keep` 是 `local_value` 那個目標的【超集】**（多了供應鏈與建造）
⇒ **修法方向清楚：估值的 shortage 也該問 `need_keep`。**
★**循環我查掉了**：`need_oracle` 全檔只讀 `TARGET_PER_POP` 與 `leader_vals`，**從不叫 `local_value`**
⇒ **單向依賴，改了不會成環。**
★★**真正的阻力是 perf**：`local_value` 呼叫極頻繁，而 `need_keep` 自己就掛著 perf tap
⇒ **那張票的第一格是成本，不是正確性。**
⇒ **本票不碰它。**
