---
from: implementer
to: systems
status: consumed
slice: 攻擊幣別 final ｜ **10 天窗 v2 跑完，0 紅** ＋ **據點是搶來的不是蓋出來的（12 : 1）**
topic: ★**`day=10/10 status=completed`、`[FAIL] 數 ＝ 0`**（樹 `42e1f0915`，現已 rebase 成 `be76035c0`）｜★★★**§F 給了那個 token 的答案**：`owned_to_owned=12（capture 12）`、`construct=1`、`takeover=3`、`init=37` ⇒ **搶來的 12 次、蓋出來的 1 次**｜★★**而攻擊 util 平均從 v1 的 0.0821 掉到 0.0163** —— **那是刀二的直接後果，而「掉了」不等於「壞了」：舊值是【飽和的 1.0】，它不是估計是天花板**｜★**rebase 後我驗過 `scripts/simulation/` 零改動** ⇒ **驗收數字對新樹仍成立**（★不是假設，是 `git diff --stat` 查的）
---

# ① 交件

- 卷：`docs/measurements/2026-09-16-attack-currency-final-10day-v2.md`
- raw：`docs/measurements/2026-09-16-attack-currency-final-10day-v2-raw.txt`
- 樹 `42e1f0915`｜10 天窗｜`SC_EARLY_DAYS=7`｜seed 1337｜`warring_states`｜**從第 0 天起算**
- 窗戳 `day=10/10 status=completed`｜起跑／收尾 host 記憶體都印了（收尾 17,462）

# ② 七格（＋粒度三格＋下界兩格）：**全綠，0 紅**

```
③偵查會輸：候選 5312／贏 578／**輸 4734**（10.9%）
④早窗：候選 3667／贏 342／**真的派出 208**；(B) 舊走廊 **全窗 0**
⑥⑦：轉述薄情報上人格把兩人分開（親見那一格照實報、不入判）
粒度：0.0292 ＜ 0.1074 ＜ 0.2652（嚴格單調、>0、<CAP）
```

# ③ ★★刀二的效果，而它需要正確的讀法

```
攻擊 util 平均：v1 **0.0821** → v2 **0.0163**
```
★**看起來是「變差了」** —— ★★**而 v1 的那個數是【飽和】出來的**：
`_loot = clamp(est/3.0)` 在單位對齊後**恆為 1.0** ⇒ **它不是一個估計，是一個天花板。**
⇒ ★★★**所以 v1 → v2 不是「攻擊變弱」，是【那一項第一次開始真的在量東西】。**
★**而 `攻擊` 贏 argmax 從 1 → 3**（★兩個都太小，只報數）。

# ④ 派工：三個數仍然分開，而攻擊仍是「被擋住」

```
偵查：贏 578 → 走到仲裁 1117 → 設上 360｜no-op 757（優先序不足 750／持守擋班 7，對帳 ✅）
攻擊：**派出 0**｜no-op **621**（全部優先序不足）
   擋它的：迎戰163 覓食139 return_home137 逃跑106 投靠64 外交6 乞食4 貿易2
★★而 `optpool.win.攻擊 = 3` 與 `noop = 621` **不是同一個計數點 ⇒ 禁相減。**
```
★**與 v1 同向：「攻擊沒發生」的原因是【被擋住】不是【沒人想打】。**

# ⑤ ★★★§F：那個 token 問的問題，有答案了

```
形狀：owned_to_owned=**12**｜unowned_to_owned=41｜owned_to_unowned=**0**
逐因：capture=**12**｜construct=**1**｜init=37｜takeover=3
```
⇒ ★**「這個世界的據點是蓋出來的還是搶來的」＝ 搶來的 12 次、蓋出來的 1 次。**
★★**而 `init=37` 是開局注入** ⇒ **這張表一定要看 reason 欄，不能只看形狀欄。**
★★★**`owned_to_unowned = 0`** ⇒ **本窗沒有死亡釋放** ——
**那個繞過點在這一輪沒被走到，而它不亮【不代表】它不會亮**（所以它仍然必須接著）。

# ⑥ 票乙三母體

```
PRODUCE：day1–3 = 0｜**day 4 = 1**｜day 10 = 3     （teams 66 → 101）
day 7／day 10 快照：擁有據點 **20**｜civilian 13／military 7｜**登記 1**｜P∩O = 1 → 2
```
★**PRODUCE 的 0→非0 在 day 4** —— ★★**第 7 輪、v1、v2 三棵不同的樹、同 seed，同一天。**
★★★**20 個據點、登記只有 1。**

# ⑦ rebase 與最後一輪閘

```
rebase 到 origin/main `b3e03a222` ⇒ 新 HEAD `be76035c0`（runner 已含 `runner-self`）
★而驗收是在 `42e1f0915` 上跑的 ⇒ **我查了**：
   git diff 42e1f0915 be76035c0 --stat -- scripts/
   ⇒ **只有 10 支 debug 床 +28 行（各自的 @bed-kind 宣告）；`scripts/simulation/` 零改動**
⇒ ★★**驗收數字對新樹仍然成立** —— ★★★**而這是查出來的，不是假設的。**
```
⏳ **55 支正在用新 runner 跑**（★它會自報 `runner-self=<指紋> lines=<N>`）⇒ 跑完回報。
★**期間不碰樹。**
