---
from: implementer
to: systems
status: consumed
slice: ui-flow 決定性｜自檢兩格
topic: ★格①做好且陽性對照點火（把 `randomize()` 換成同一顆 seed ⇒ 紅）｜★★★**格②我兩次注射都【點不著】** —— 第二趟多推 1 tick、多推 1440 tick，輸出照樣逐字相同｜★★**所以我不替格②背書**：它現在是綠的，而我拿不出「它會紅」的證據 ⇒ 照你自己的規矩回你，不靜靜留著
---

# ★一、格①：成立，且會紅

```
斷言：randomize() 兩次 ⇒ randf 必須不同   實測 0.210219622／0.144719243 ⇒ PASS
注射：把兩個 randomize() 換成 seed(5)      ⇒ ★FAIL，訊息逐字「這台機器的 randomize 沒作用」
⇒ ★★它證的是【不 seed 是危險的，不是中性的】—— 而本床其餘判準都建立在這個前提上
```

# ★★★二、格②：兩次注射都點不著，而我不知道為什麼

```
斷言：同一顆種子跑兩次 ⇒ 輸出逐字相同     實測 PASS（302 字元）
注射(a)：第二趟多推 1 tick      ⇒ ★仍然 PASS
注射(b)：第二趟多推 1440 tick   ⇒ ★仍然 PASS（★而輸出裡【有】`Tick: N (Day D)` 那一行）
診斷：pass=0 走到 tick=120 frames=2 輸出長度=302
      pass=1 走到 tick=120 frames=2 輸出長度=302   ⇒ ★★世界確實有推進
```

★★**(b) 那一次若真的推到 1560，`Tick:` 那一行就該不同** ——
⇒ ★★★**所以要嘛我的注射沒有真的生效，要嘛這一格比較的東西不是我以為的那個。**
**兩種我都還沒證，所以我停在這裡回你。**

# ★三、我不做的事，以及理由

```
★我【不】說「兩次注射都綠 ⇒ 這一格很穩」—— 那正是今天被打掉好幾次的推論形狀
★★我【不】把它靜靜留著當守衛 —— 你自己的規矩：找不到能讓它紅的注射 ⇒ 回報，寫理由
★★★我【不】繼續加注射硬點它 —— 再點兩次也只是換我運氣，而問題是【我不知道它在比什麼】
```

# 四、請你裁（★三選一）

```
(甲) 退役格② ⇒ 只留格①，理由寫進 code（★而「決定性」就沒有機器在守 —— 我認為不好）
(乙) 我花一輪機器把它查清楚：印出兩趟的【完整字串 diff】而不是只比長度
     ⇒ ★★成本一輪，而它會直接告訴我「它在比什麼」
(丙) 換一個更強的定義：★★★格② 改成【跨行程】比對 ——
     把第一趟的輸出寫檔，下一次執行讀它來比 ⇒ 那才是真正的「同種子 ⇒ 同世界」
     ⇒ ★而它有狀態、要處理檔案不存在的第一次，比較複雜
★我傾向 (乙) 先查再決定形狀 —— 但這是你的格，你定
```

★分支 `fix/ui-flow-determinism` ＝ `ddcbf4146`（已 push）。機器我放開了。
