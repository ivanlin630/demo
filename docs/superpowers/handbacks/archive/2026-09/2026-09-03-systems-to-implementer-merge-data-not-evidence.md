---
from: systems
to: implementer
status: consumed
slice: 一句界線（給你補完 seed1337 那格之後用）
topic: ★blueprint 提示:紮根 applicable 勝=0 與 #10 not_in_ranked 住同一區(survival/applicable),兩線資料可互餵,計數票回來一起看;★★而我要加一條界線:併看【資料】≠ 互為【證據】——兩條線各自要有自己的母體與判準;★★★血證就是 seam1(兩件事碰巧都碰到 FLEE 的 applicable,而它的紅比 #10 早六週)
---

# ★①可以做的
`zhagen.appl_won/lost`（補完 seed1337 後）與 `#10 not_in_ranked` 的條件名計數 **可以放同一張表看** ——
它們**共用同一個決策入口**，★**一起看會比分開看快**。

# ★★②不可以做的
```
★不要用其中一條的結論去解釋另一條（例：「紮根都輸備戰 ⇒ 所以 #10 那 40% 也是備戰擠掉的」）
★★兩條線各自要有【自己的母體】與【自己的判準】——
   #10 的母體是 `current_task==IDLE 且 survival_committed_option != ""` 的隊×tick
   紮根那格的母體是 `applicable == true` 的次數（★目前只有 4）
★★★血證：seam1 —— 兩件事碰巧都碰到 FLEE 的 applicable，而它的紅【比 #10 早六週】
```
★**若兩線真的指向同一個機制，那應該是【兩份獨立的數字同時指向它】** —— **而不是一份數字被拿去補另一份的洞。**

# ③序不變
`補 seed1337 那格 → 樹靜止 → 我 revert+全閘+merge → 清除點＋tap（同一刀）→ 修 3 張守衛`
