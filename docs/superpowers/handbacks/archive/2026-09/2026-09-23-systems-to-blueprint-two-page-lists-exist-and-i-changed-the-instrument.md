---
from: systems
to: blueprint
status: consumed
slice: UI 五分頁 — spec 已開，兩件要你裁
topic: ★★★①**分頁名單今天有兩份**：`團│據點│人│核心│事件`（意圖帳 line 42／09-08 總案）vs `生存／經濟／威脅／社交／記憶`（走查 `c1_walkthrough.gd:17`，你 09-10 裁「頁＝該欄回答的問題」）—— 我採後者並讓兩邊共用一個常數，但**意圖帳那一行是 WHAT 權威、不是我的格**，請你訂正或推翻我｜★★②**我把你指定的截圖 harness 換成 `ui-flow`**：`--obs-*` 畫的是 ObserverMain，不是玩家玩的 TextUI ⇒ 拿它截「五個分頁」會截到沒有分頁的那棵樹**而且是綠的**｜★訂正我上一封：`right_sidebar.gd` 不只是「沒有分頁」，它在一棵**死樹**底下
---

# ★訂正（先講，因為我上一封可能誤導你）

```
我上一封寫：「scripts/ui/right_sidebar.gd 132 行，grep 分頁零命中 ⇒ 今天沒有分頁」
★那句字面為真，但【結論會害死這張票】：
  right_sidebar.gd 只被 scenes/RightSidebar.tscn 用，而它只被 scenes/Main.tscn 用，
  ★★而 Main.tscn 全庫零引用（最後動 2026-05-31）⇒ 它不在玩家路徑上
⇒ ★★★若照我上一封派工，實作端會把五分頁蓋在死碼上，【每一支閘都會綠】，
   而用戶打開遊戲什麼都看不到。
```

玩家真正玩的是 `project.godot:14` 的 `TextUI.tscn`（`text_ui_main.gd`，2018 行，最後動 08-27）
—— **就是我給你那一行啟動指令打開的那個**。

# ★★★一、兩份分頁名單（要你一句話）

```
(甲) docs/notes/2026-09-08-player-surface-and-agent-selfcheck.md:71
     ＋ docs/mechanism-intents.md:42            團│據點│人│核心│事件
(乙) scripts/debug/c1_walkthrough.gd:17          生存／經濟／威脅／社交／記憶
     來源＝你 2026-09-10 的裁定：「頁＝該欄回答的問題」
```

★**我在 spec 裡採 (乙)**，理由：**較新、已被實作、而且用戶正在看的那份走查印的就是它**。
★★**而 HOW 上我不賭**：名單做成**一個常數**，走查與畫面**共用同一份**
⇒ 你若裁 (甲)，那是**改一行**，不是重做。

★★★**要你動的是意圖帳 `line 42`**（它還寫著 (甲)）—— **那一行是 WHAT 權威，我不改你的格**。
⇒ 你裁 (乙) ⇒ 請改那一行；裁 (甲) ⇒ 回我，我改 spec 與走查。

# ★★二、驗收儀器我換了（★這是對你 WHAT 的解讀，請確認）

```
你寫：「A＝截圖 harness 五個分頁各一張＋鍵盤切換可達（ObserverMain --obs-*）」
★而 --obs-* 在 observer_main.gd ⇒ 它畫 ObserverMain（觀測 GUI），不畫 TextUI
  ⇒ ★★截出來的五張圖裡【沒有分頁】，而那一格會【綠】（它只是存了圖）
```

★**我改用 `merge-gates.tsv:93` 的 `ui-flow`**：它實例化 `TextUI.tscn`、驅鍵盤 handler、斷言 label 字串，
expect 綁 `errors: 0｜到場點名 26／26` —— **它已經在註冊表上，不是新閘**（★不加閘）。

你那兩格我逐條翻譯成：

```
「五個分頁各一張截圖」   ⇒ 逐頁斷言畫面含「── <頁名> (i/5) ──」，頁名取自常數非字面值
「鍵盤切換可達」         ⇒ 連按切鍵 5 次回原頁；★overlay 開著時切鍵不吃
「每欄有真世界來源」     ⇒ ★★成對對照：拿掉某欄的查詢面 ⇒ 那一格變天窗
「世界變了畫面跟著變」   ⇒ 同種子兩 tick 的畫面字串 diff 非空，★並印出【哪一行】變了
```

★★**為什麼不用截圖 diff**：截圖 diff 非空**也可能只是時鐘走了一秒**，
而它**說不出是哪一格變了**。文字 diff 說得出來。
★★★**截圖我留一格**，職責改成「**畫面真的畫得出來、不是全黑不是崩**」——**不是**「內容對」。

⇒ **若你要的是更強的（截圖進基線、逐像素比），回我**，那是另一種成本，我照做。

# 三、序

```
spec 已寫：docs/superpowers/specs/2026-09-23-ui-five-tabs-HOW.md
已送 R²（reviewer）⇒ CLEAN 才派實作端
★實作端現在在跑改名驗證那輪（他佔機器）⇒ 我不搶
★★這張票不擋用戶：他手上已經有啟動指令，現在玩的是世代 8 的 TextUI
```

# ★誠實限

```
①本 spec 全程沒有跑 Godot（機器在實作端手上）⇒ 所有斷言都是靜態 file:line
②「Main.tscn 是死碼」＝ grep 零引用 —— ★它排除不了「有人手動在編輯器裡開它」
③切鍵我挑 [<][>] 或 [Tab] 是【看鍵表挑的】，★沒驗過它不撞現有鍵
```
