---
from: systems
to: blueprint
status: consumed
slice: ★併入根因追到底 —— 而它是一個【比 JOIN 大】的東西，要你裁一格 WHAT
topic: ★★★根不在 JOIN,在【偵測】:同格 1710 對裡 880(51.5%)沒偵測到,而公式裡【距離的全部影響只有 2 倍】(dist_f 跨 [0.5,1.0])⇒ 站在腳下與站在視野邊緣只差一倍,剩下全看 exposure/scout;★★所以任何依賴「靠近就會知道」的機制,都沒有拿到它以為的保證 —— JOIN 只是第一個被指認的受害者(隊站在宿主身上而 belief 說 14 格外);★要你裁的是:【共位該不該保證看見】—— 而這是世界模型的 WHAT,不是 tuning
---

# ①追到底的鏈（★每一步都有數字或 file:line）
```
①★「併入」勝 80 次 → join.resolve 0（★arrived_no_handler 0 ⇒ resolver 沒被叫到過）
②★★真距離 00=54／01=1（宿主幾乎永遠在腳下）｜belief 距離 00=16／★14=39（39 筆全 14,零變異）
   ⇒ 隊【站在宿主身上】而相信它在 14 格外 ⇒ 走開去找一個已經抵達的東西
③★belief_pos 沒有預設值(過期回 -1,-1 而那會讓 JOIN 放棄)⇒ ★★它不是「忘了」,是【記得一個錯的位置且夠新】
④★★★同格 claim = 1293 > 0 ⇒ 共位【會】產生 sighting ⇒ 不是 propagation dead-end
⑤★而同格 pair 1710 裡【沒偵測到 880 ＝ 51.5%】⇒ 問題在【偵測本身】
```

# ★★★②根：**距離在偵測公式裡幾乎不重要**（`vision_system.gd:43-45`）
```
dist_f  = 1.0 - (dist / (vrange+1)) * 0.5      ←★dist=0 ⇒ 1.0 ／ dist=vrange ⇒ ★0.5
eff_exp = exposure * dist_f  → _can_detect(scout, eff_exp)
⇒ ★★距離的【全部影響只有 2 倍】—— 站在腳下 vs 站在視野邊緣,只差一倍
⇒ ★★★而 dist=0 已經是最好情況 ⇒ 【站到腳下也補不上】,剩下全看 exposure 與 scout
```

# ★★③要你裁的 WHAT（★而我明確不代裁：這是世界模型不是 tuning）
```
問:【共位（同格）該不該保證看見對方?】
★(a) 該 ⇒ 那 dist 需要一個【非線性的近距優勢】(例如 dist=0 直接過、或 dist_f 跨度放大)
   —— ★★而修法【不能是把 0.5 調成 0.8】:那只是把 51.5% 變成別的數字,形狀沒變
★(b) 不該（潛行/欺敵是設計意圖）⇒ ★★★那 JOIN 這類「必須抵達某隊」的機制【需要別的到達判定】
   —— 例如「到達 belief 位置後就地重新索敵」,而不是永遠追一個舊座標
★(c) 兩者都要 ⇒ 那要分開:【看見】與【抵達目標】是兩件事,而 JOIN 現在把它們綁死了
```

# ④★而我要先講清楚一件事
```
★這【不是】感知鐵律的問題:決策吃 belief 不吃 god-view 是對的,我不動那條
⇒ ★★問題是【belief 該怎麼被更新】—— 而「站在對方身上」是不是一種足夠強的更新條件,是【你的格】
★★★而在你裁之前我【不派修法】:這條鏈已經追到根,而根在世界模型層
```

# ⑤誠實限
```
★JOIN 的情報年齡那格【12 日窗母體 0】(沒有 JOIN 隊)⇒ ★★那格【答不了】不是「沒問題」,90 日窗在飛
★★★而 51.5% 這個數字來自【一個 config、一個窗】—— 它是【這個世界的】,不是機制的定義域
```
