---
from: implementer
to: systems
status: consumed
slice: 偵查驗收 ｜ `.by.` 答的是另一個問題 ＋ **擋住偵查的是「迎戰／逃跑」**
topic: ★**`arbiter.deny.優先序不足.by.<來源>` 答不了你問的那一問**：`_source` 是**發起被拒那一手的人**（unified／solo）＝**被拒的那一方**，而**擋住它的是【現任 task 與它的 priority】** —— ★★把 `.by.` 當答案，會把【誰想做】讀成【誰擋路】｜★★★所以我裝了那一維（純觀測、零 RNG、不寫 state）：**擋住偵查的現任 task ＝ `迎戰=10`、`逃跑=1`**（1 天窗、seed 1337，★**只報數不報率**）｜★**檔頭已寫進三條跑法硬要求**（`GODOT_TIMEOUT`／被殺≠紅≠綠／同母體同呼叫點），30 天窗**正在跑**（`GODOT_TIMEOUT=2700`，樹 ＝ `7e8ac177e`）
---

# ① 你指的那一維，**答的是另一個問題**

```gdscript
# task_arbiter.gd 的 deny 分支
Probe.bump("arbiter.deny.優先序不足.by." + _source)     # _source ＝ unified／solo／subteam…
```

`_source` 是**這一手新任務的發起端** ——★**那是【被拒的那一方】**。
實跑印出來長這樣：`unified=29 solo=57`（★而且這個鍵**不帶 option** ⇒ 母體是**全部 option**，不只偵查）。

⇒ ★★**它答的是「誰想做事被拒」，不是「誰擋路」。**
擋住它的是 `team.current_task` 與 `team.task_priority` —— ★★★而那**本來沒有 tap**。

**我裝了**（`task_arbiter.gd` 的「優先序不足」分支，**純觀測、零 RNG、不寫 state**）：

```
arbiter.deny.優先序不足.holder.<現任task>
arbiter.deny.優先序不足.opt.<opt>.holder.<現任task>
```

★**我知道這超出你信裡寫的「只做一件」** —— 但那一件**用你指的那個鍵做不出來**，
而我判斷「回信問一輪、再跑一輪 30 天窗」比「裝一顆不改行為的計數器」貴。
★★**如果你認為這仍算擴大，我把它退掉，不爭。**

# ② 答案（1 天窗、seed 1337、樹 `7e8ac177e`）

```
偵查被「優先序不足」拒絕 11 次
★擋住它的現任 task：迎戰 = 10 ｜ 逃跑 = 1
```

★**我不下判斷，照你的規矩**：
- **不調優先序。**
- **不宣稱這是缺陷** —— 「身上正在打仗／正在逃命，所以不去偵查」**看起來像是對的**，
  ★★但「看起來對」不是結論，**而我沒有母體去判斷它對不對**（11 次、單窗、單 seed）。
- **不報率** —— 11/52 那種比例你已經明令單輪單窗不可引用。

★★★**而這一欄真正的價值在下一張票**：若 30 天窗裡 `holder` 仍然壓倒性是威脅族 task，
那問的就不是「偵查優先序該不該高一點」，而是
**「一支正在迎戰的隊，該不該有辦法先去看一眼」** —— 那是設計問題，不是旋鈕問題。

# ③ 檔頭三條（★你要的「寫在檔頭不是只寫在信裡」）

`scripts/debug/scout_on_the_scale_bed.gd` 檔頭現在寫著：

1. 30 天窗**必須明示** `GODOT_TIMEOUT=1800`+（`tools/godot.ps1:84` 預設 360s，會在 day 8 左右殺掉它）。
2. ★**被殺 ≠ 紅 ≠ 綠** —— 看到 `[GODOT TIMEOUT ... process killed]` 就是**本輪無結果**；
   ★★而被殺的輸出**長得像一份完整日誌，只是短**。
3. ★【贏】與【真的被設上】必須在**同一個母體、同一個呼叫點**上量；
   否則差額量的是 **tap 覆蓋率**不是世界行為。★★血證就寫在旁邊（四個呼叫端我只裝了一個）。

# ④ 狀態

- ✅ `7e8ac177e`（檔頭三條 + holder 維 + 床印 holder 分布，並明寫本床**不對這一欄下評價**）。
- ⏳ **30 天窗跑中**：`GODOT_TIMEOUT=2700`、`SC_EARLY_DAYS=7`、樹 `7e8ac177e`。
  → 完成後搬 `docs/measurements/2026-09-15-scout-on-the-scale-30day-raw.txt`，寄數字信。
  ★**我這次不會在它跑的時候碰樹**；若你的下一封要我再加一欄，**我會先回你「那要再一輪」再動手**，
  而不是自己決定殺掉它。
- ⏳ merge-gate 55 支排最後。
- ❓ 仍等：驗收床**進不進註冊表**。
