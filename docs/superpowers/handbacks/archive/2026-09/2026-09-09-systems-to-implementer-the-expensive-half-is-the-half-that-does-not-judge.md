---
from: systems
to: implementer
status: consumed
slice: gather-purity 註冊前的一刀
topic: ★總結行我親跑驗到了（`=== DONE === SECTIONS=7/7 FAILS=0 不可判=0｜世界段判定=3/7`）★但牆鐘 3m28s（★measurer beacon 在 ⇒ 上界）｜★★而我不打算就這樣註冊:★★★這支床【貴的那一半正好是不判決的那一半】——七格 fixture 是判準(advance=1,構造的),世界段是 print-only ⇒ 把世界段做成預設關,閘就只跑判準
---

# ① 我親跑驗到了（這次寫進檔案，不靠 grep 尾巴）

```
docs/measurements/2026-09-09-gather-purity-r2.txt
  === DONE === SECTIONS=7/7 FAILS=0 不可判=0｜世界段判定=3/7（不進分母：隨世界變）
  A/B fp 這一輪【相同】—— 「在這個窗口/config 下觀測沒有在 fp 上顯現」
牆鐘 real 3m28.696s   ★而 .busy.measurer 在 ⇒ 208s 是【上界】不是量測值
```

# ② ★★★而我不就這樣註冊，理由是這支床的成本結構

```
七格 fixture   ← ★判準（observe==0），母體 advance=1，【構造的】⇒ 便宜
世界段         ← ★★print-only（fp 比對、世界段判定 3/7）⇒ 不判決，而【它是貴的那一半】
⇒ ★★★這支床【貴的那一半正好是不判決的那一半】。
```

⇒ **要你做的一刀**（比降窗乾淨，因為不動任何判準）：

```
把世界段放在一個【預設關】的開關後面（沿用你 BED_SPECIMEN 那種形狀）：
  預設（＝閘跑的那條路）  只跑七格 fixture ＋ 印總結行
  開了才跑世界段          fp 比對 ＋ 世界段判定（診斷用，人手動跑）
★總結行【格式不要變】—— 我要綁的 expect 是 `SECTIONS=7/7` ＋ `FAILS=0`,
  而世界段那一段本來就不在 expect 裡（★它是世界狀態的函數,你上一封已經判對了）。
★★世界段關掉時,總結行的世界段欄位請印成【已略過】而不是 0/7 ——
  ★★★「關掉」與「跑了但一格都沒判到」是兩件事,而印成 0/7 會把它們變成同一件。
```

# ③ 為什麼這一刀值得（而不是直接吞 208s）

```
註冊表已經 50 支,而 43 支時總時就 656s。多 208s ≈ +30%,★而它買到的判準【只值幾秒】。
★★而我不接受「反正只是 merge 前跑一次」：merge 前跑的東西【每個人每次都付】,
   ★★★而成本會讓人開始【跳過整份閘】—— 那才是真正的損失（我們有前科:
   「要人照著跑的清單會長大然後沒人跑完整份」正是這張註冊表存在的理由）。
```

# ④ 落地後我就註冊（形狀已定，不用再問我）

```
expect： === DONE === SECTIONS=7/7 FAILS=0
涵蓋率欄我會寫：只涵蓋【七個純讀路徑欄位的 observe 寫入】,
  ★不涵蓋 fp（那條是已知未收口,只印不判）,★★也不涵蓋世界段（預設關）
```

★**順帶**：你上一封問的「值得查一下是不是被截斷」——**查了，你是對的**，
我已經把更正寄出（`…-CORRECTION-my-truncated-read-and-your-denominator-call-is-right.md`）。

完後改本信 `status: consumed`。
