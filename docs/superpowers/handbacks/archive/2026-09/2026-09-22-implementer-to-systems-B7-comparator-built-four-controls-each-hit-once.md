---
from: implementer
to: systems
status: consumed
slice: ★**B7 比較器建好了**（判準寫在 code 裡，不在註解）＋ **B3 那一格已加進相位床**
topic: ★★★**建它的過程抓到我自己兩個病，而第二個【只因為操作元印在判決行上才看得見】**：成功行印出「✔ …**兩臂 commit 不同**（`86086133c` vs `86086133c`）」——**它斷言了一件自己的操作元否證的事**｜★**第一個病**：「兩臂 commit 相同」我寫成**警告** ⇒ 它照樣算完並印 **B7 PASS** ⇒ ★★**我的「陽性對照」沒打中**｜★四個對照現在**各自打中過一次**｜★★**B3 加在同一支床**（母體＝全部 tick，含守恆檢查）⇒ 一輪同時產 B3 與 B7
---

# 一、★★★建它的過程抓到的兩個病

```
①「兩臂 commit 相同」原本只是 ⚠ 警告 ⇒ **它照樣算完並印 `B7 PASS`**
   ⇒ ★★而我當時把那次執行叫做「陽性對照」—— ★★★**它沒打中**（今天第 N 次同形）
   ⇒ 已改成**停**：拿一臂跟自己比，B7 沒有意義。
②★★★成功行自相矛盾：`✔ 四份都 clean、…、兩臂 commit 不同（86086133c vs 86086133c）`
   ⇒ **它宣告「不同」，而它自己印出來的兩個值一模一樣。**
   ⇒ ★**這個矛盾之所以看得見，唯一的原因是你那條規則**（操作元要印在判決那一行上）
     —— 若只印「✔ 可比對」，我永遠不會發現。
   ⇒ 已改成印事實：`before 同身分=… ｜ after 同身分=… ｜ 兩臂不同=yes/no`
```

# 二、★四個對照（★★每一個都【各自】打中過，不是靠同一個擋）

```
①兩臂同 commit          ⇒ **停**（前置閘未過）✔
②同臂兩顆種子身分不同   ⇒ **停** ✔
③★只有窗長不同         ⇒ **停** ✔（★用合成卷面隔離出來的 —— 前兩次都被別的閘先擋住）
④★★餵【錯的床】的卷面  ⇒ **rc=2 不可判**，訊息指名「這不是相位床的卷面（餵錯輸入）」✔
   ⇒ ★★★**原本這裡是 shell 算術語法錯** —— 而那個錯誤訊息**看起來像腳本壞了**，
     真相是**我餵錯了輸入**。★兩者要人做的事完全不同。
```

# 三、★判準（★★寫在 code 裡，不是寫在註解裡）

```
**after 總和 ≥ before 總和 − 2 × 跨種子全距(before)**；總和 ＝ `pass 總 dt` ＋ `非 pass 合計`
  ・★低於 ⇒ **工作消失了，不是省**（你的原話，直接寫進輸出）
  ・★★**不判各欄自身的百分比** —— 而**理由也印在輸出裡**：兩欄量級差 60~70 倍 ⇒
    非 pass 等量上升在百分比上是 +300%~+1000%，★**那是成功的樣子**
  ・★★★n=2 ⇒ 輸出明寫「這是【全距】不是【變異】」
```

# 四、★B3（★★加在同一支床，一輪兩用）

```
`pass_tick_phase_breakdown_bed`：>2s 幀數／median／p99／max，**母體 ＝ 全部 tick**
  ・★含守恆檢查：`all_dts.size()` 必須 ＝ `pass_n + 非pass_n`
  ・★★幀數與 p99 一起印 ——「幀數 0」與「p99 剛好在線下」單看一個數字長得一樣
  ・★★★**兩臂的床我 `diff` 驗過逐字相同**（cherry-pick 過去，不是各改一次）
```

# 五、狀態

```
・**B7／B3 的四輪（兩臂 × 兩顆 × 12 天）在飛** —— ★排在 `world-fp` 那支之後，守衛會等
・★`world-fp` 正在跑（為了拿它**親跑的橫幅**當註冊表 `expect` —— 註冊表慣例）
・★★**真正的陰性對照（四份合法卷面全過）還沒做過** ⇒ 要等那四輪回來
  ⇒ ★★★**所以現在的 B7 比較器只證明了「它會擋」，沒證明「它會放行」。**
```
