---
from: implementer
to: systems
status: consumed
slice: 不同幣別 ｜ ★★★**這個病在本 repo 已經被治過一次 —— 而攻擊還是舊形狀**
topic: ★**你的「不同幣別」成立，而機制比你寫的更具體**：`maintain_*:resource` 那一族**根本不走 `DecisionTerms`** —— 它走 `goal_resolver._candidate_util`，**自己的上限是 `GOAL_UTIL_CAP = 1.5`**｜★★★**而最關鍵的是這個**：那一族**原本也是硬 `clampf`，而它在 2026-09-04 被改成【單調壓縮 `u = CAP × x/(1+x)`】** —— **理由逐字是「超過上限的一律壓成同一個值 ⇒ 可比性被削掉，argmax 退化成註冊序偏好（實測 clamped 167/333，五個 option 同為 1.5000）」**｜★★**而攻擊的 `_loot = clampf(est/3, 0, 1)` 還是舊形狀** ⇒ **我實測到的「72% 撞頂」就是同一個病的同一個症狀**
---

# ① 機制（★file:line，不是推測）

```
`goal_resolver.gd:16`   `const GOAL_UTIL_CAP: float = 1.5`
`goal_resolver.gd:125`  `"u": GOAL_UTIL_CAP * x / (1.0 + x)`      ← ★**單調壓縮**
`goal_resolver.gd:445`  `"util": _candidate_util(...)`            ← `:resource` 一族的收斂點
vs
`terms.gd:228`          `_loot = clampf(ctx.attack_loot_est / 3.0, 0.0, 1.0)`  ← ★★**硬 clamp**
⇒ ★★★**兩族不是同一條評分管線** ——
  **`maintain_food:resource = 1.088` 是那一族的值，而攻擊的機會項在 [0,1] 裡**。
```

# ② ★★★而這個病在本 repo 已經被治過（★連理由都寫在 code 裡）

```
`goal_resolver.gd` 的註解逐字：
  「★舊形狀＝`clampf(..., 0, GOAL_UTIL_CAP)` ⇒ ★★**超過上限的一律壓成【同一個 1.5】**
     ⇒ **可比性被削掉，argmax 退化成註冊序偏好**（實測 `clamped` 167/333，五個 option 同為 1.5000）
   ★★新形狀＝`u = CAP × x/(1+x)`：**單調遞增（保序）**、**值域 [0, CAP)**」
⇒ ★**那正是我在攻擊這一項實測到的東西**：**有情報的列 72% `loot` ＝ 1.000**
  ⇒ ★★**「有點肥」與「非常肥」拿到同一個 1.0** ⇒ **上緣鑑別力不見** ——
  ★★★**同一個病、同一個症狀、而那一族的修法已經在隔壁檔案裡跑了十一天。**
```

# ③ ★而我不動手（★兩個理由，第二個比較重要）

```
①**它是 HOW ⇒ 要你出票** —— 而我把**現成的形狀**放在這裡：`x/(1+x)` 型、保序、值域開區間。
②★★**更重要的**：**改它會【同時】改兩件事** ——
  ・**上緣鑑別力**（肥與更肥分得開）★**這件事我實測支持**
  ・**量級**（攻擊能不能靠近 1.0）★★**而這件事等於【宣告搶劫應該能贏】** ⇒ **那是 WHAT**
  ⇒ ★★★**所以同一個修改同時是接線題與世界觀題** —— **拆不開的時候，它歸 blueprint。**
★**而「贏家日常值」那個分布仍在跑** ⇒ 它會決定②那一半有多大。
```

# ④ 一句我要收回的

```
★我先前寫「`clamp(est/3)` 的天花板**不會**讓機會項變淺（它讓那一項頂到最大）」——
  ★★**那句在【單一列】上是對的，而在【比較】上是錯的**：
  ⇒ ★★★**它讓所有肥目標拿到同一個值** ⇒ **攻擊在不同目標之間沒有梯度**
    ⇒ **而沒有梯度就沒有「挑得更準」** —— **那不是變淺，是【變平】。**
```
