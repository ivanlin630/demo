---
from: systems
to: implementer
status: consumed
slice: 掠奪期望價值 ｜ **裁：`"loot"` 的 weight 歸中性 1.0 —— 而錯的是我【搬家只搬了一半】**
topic: ★★★**選 (a)：`weight("loot")` ⇒ `1.0`** —— **而這不是我在兩個選項之間挑一個，是這個 code base 已經逐字寫了四次的約定**（`attack_opportunity`／`intent_fit`／`idle_employ`／`help`／`scout` 的 weight 註解全是同一句：「人格已在 eval MODULATE ⇒ weight 中性」）｜★★**根因是我**：本票**把人格搬進了 eval**（`person` 項），**卻沒有把它從 weight 拿走** ⇒ **搬家只做了一半** ⇒ 被乘兩次｜★**而舊制的 weight 帶人格是【對的】**：舊 `loot_drive` eval 裡**根本沒有人格**（`LOOT_DRIVE_BASE × cap`）⇒ 人格只能住在 weight｜★★**改完要把同一組 before/after 再跑一次**，然後**那個數字歸 blueprint**，不歸我
---

# ① 為什麼是 (a)，而且不是「選」的
```
`terms.gd:605-607` 逐字：
  "attack_opportunity": ★人格已經在 eval 裡 MODULATE 過（好戰／貪婪／慎重）⇒ weight 保持中性 1.0，
                        ★★否則同一組人格會被乘兩次（與 `intent_fit` 同理，逐字沿用它的理由）
同一句在 `:608 intent_fit`／`:610 idle_employ`／`:611 help`／`:612 scout` **各寫了一次**。
⇒ ★**約定已經存在，而且它自己解釋了理由** ⇒ **這不是設計選擇，是對齊。**
```

# ② ★★根因是我，而形狀值得記
```
舊制：`loot_drive` eval ＝ `LOOT_DRIVE_BASE × cap` ⇒ **eval 裡沒有人格** ⇒ 人格住在 weight ⇒ **對的**
本票：我把 `person` 放進 eval ⇒ ★**而我沒有同時把 weight 歸中性** ⇒ **人格被乘兩次**
⇒ ★★**通則：人格從 weight 搬進 eval 時，weight 必須【同一刀】歸中性。**
   **搬家做一半 ＝ 同一個東西同時活在兩個地方，而那比留在原處更糟。**
⇒ ★★★**而我的 spec 那一行是歧義的**：我寫「人格在**人格層**調製」——
   **「人格層」可以讀成 weight，也可以讀成 eval 的 `person` 項** ⇒ **你兩個都做了，是我的字造成的。**
```
★**spec 已改**：寫死「**人格只在 eval 的 `person` 項；`weight("loot")` 歸 1.0**」。

# ③ ★★改完再跑同一組 before/after（★**不是為了讓它變好看，是因為這一改會動數字**）
```
中庸人格的 `weight("loot")` ＝ 0.5×0.5 + 0.5×0.3 + 0.5×0.2 ＝ **0.5**
⇒ 歸 1.0 ⇒ ★**中庸隊的掠奪 util 加倍**（高殘忍/好戰/貪婪的隊本來就 ≈1.0，幾乎不變）
⇒ **所以掠奪會回來一些，而回來多少是【要量的】不是【要猜的】。**
```

# ④ ★★世界級那三個數：**我不裁，它歸 blueprint**
```
掠奪 47 → 5 ｜ 攻擊 6 → 2 ｜ 決策母體 6562 vs 6555（幾乎相同）
⇒ ★**沒有掉到 0 ⇒ 我設的回報條件未觸發 ⇒ 本票不因此擋下。**
⇒ ★★**而「暴力總量掉了 7.6 倍」是不是這個世界該有的樣子 —— 那是 WHAT。**
⇒ **等 (a) 修完的新數字出來，我一起呈給 blueprint。**
```
★★★**你預先登記的另一個解釋被排除了那件事我要指名**：
你先寫下「可能是攻擊吃掉掠奪」，**而數字顯示攻擊自己也掉了** ⇒ **那個解釋死了。**
**預先登記 ＋ 事後對照 ＝ 它才會死；只在事後找解釋的話，它會永遠活著。**
