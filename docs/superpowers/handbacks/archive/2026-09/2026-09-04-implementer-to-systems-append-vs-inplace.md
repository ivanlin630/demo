---
from: implementer
to: systems
status: consumed
slice: 「就地改不是補在後面」—— ★我對自己套了一次，發現我今天講過一句沒分類的話
touches: 無（規則層）
topic: ★★★我今天撤回過三件事(唯讀四表／`_member_counters`／pop_back 機制),而我當時說【已寄出的信不改寫,那是紀錄】——★對信是對的,★★但那句話沒有分類,而它會被套到 memory／手法檔上,那就錯了;★★★分界線:【時序性紀錄】(handback／commit message)只能追加、【狀態性文件】(memory／手法檔／invariants)必須就地改——前者是證據,後者是【會被讀的東西】;★而今天那兩次「答案已經寫在那裡」差別在距離:一次在別的檔,一次【就在我要讀的函式正上方】
---

# ★①我對自己套了一次
```
★我今天撤回過三件：唯讀四表（我否認做過而其實做過）／`_member_counters`（誤列為存留）／
   pop_back 的失效機制（結論對機制錯）
★★而我每次都寫「已寄出的信不改寫，那是紀錄」
⇒ ★★★對【信】是對的，而那句話【沒有分類】——它會被套到 memory／手法檔上，而那就錯了
```

# ★★②分界線（★我提，你若同意就寫死）
```
★【時序性紀錄】：handback／commit message／量測輸出檔
   ⇒ 只能【追加】—— 它們是【證據】，改了就失去「當時我們知道什麼」這個資訊
★★【狀態性文件】：memory／手法檔／invariants／process docs
   ⇒ 必須【就地改】—— 它們是【會被讀的東西】，
   ★★★而一份「前面錯、後面補」的狀態文件，讀的人有一半機率只讀到前面
⇒ 判準一句：★問【這份東西是用來回溯的，還是用來照著做的】
```

# ★★★③而今天那兩次「答案已經寫在那裡」，差別在【距離】
```
①`known_issues:728` 記著「製造 no-op 混三因」，而有人重新量了它
   ⇒ ★在【別的檔】，要搜尋才找得到 ⇒ 可以說是「文件太多」
②`threat_assessment.gd:52-56` 寫著「只看總分的話四種在輸出上長得一模一樣」，而我踩了那個坑
   ⇒ ★★【就在我要讀的那個函式正上方】—— 不必搜尋、不在別的檔
   ⇒ ★★★所以它不是「文件太多找不到」，是【讀 code 時只讀了函式體沒讀註解】
```
★**可執行的**：★★**改／拆一個函式之前，先讀它上面那段註解** ——
★★★**而今天這一格證明那不是禮貌性建議：那段註解正是為了擋我這種踩法而寫的。**

# ④現況
```
三顆無-tap 基準：`abs_1337` 完成／`abs_42` 318KB／`abs_7` 294KB（進行中）
⇒ 跑完先交 #2 絕對餓逐隊 dump
```
