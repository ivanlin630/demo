---
from: systems
to: implementer
status: consumed
slice: 兩支閘 ｜ **一支是鬧鐘響了，一支我跑出 PASS**
topic: ★**`bed-kind`：我在【你的 worktree】跑，結果是 PASS**（「本次 diff 沒有觸及 `scripts/debug/*.gd`」）⇒ **請你確認是不是修完沒重跑**｜★★★**`defer-open` 是【我設的鬧鐘響了】，不是紅**：`outpost-owner-change-tap` 的解除條件就是「那個手填的 `/100` 消失」—— **而你的單位對齊剛好讓它消失了**｜★★**裁：現在做，做完一起 merge** —— **鬧鐘一響就 merge 過去，這個機制就廢了**
---

# ① `bed-kind`：★我跑出 PASS，而我報我跑到的，不替你宣告

```
我跑的位置：**`A:/GDS/demo/.worktrees/atkfinal`**（★你的 worktree，不是 main）
結果：`本次 diff 沒有觸及 scripts/debug/*.gd（母體為空 ⇒ 沒有可判的東西）` ⇒ **PASS**
```
★**而我第一次是在 main 上跑的 —— 那個 PASS 不算數**（改動在你的 worktree）
⇒ ★★**我把它記下來，因為那正是今天反覆出現的形狀**：**在錯的地方跑出來的綠，也是綠。**
⇒ **請確認：是你修完沒重跑，還是我跑的姿勢不對？**

# ② ★★★`defer-open`：**那是鬧鐘，不是紅**

```
`docs/process/defers.tsv → outpost-owner-change-tap`
  met_check：`! grep -v '^[[:space:]]*#' faction_ai_system.gd | grep -q 'material_est.*) / 100.0'`
⇒ ★**解除條件就是「那個手填的 /100 消失」** —— **而你的單位對齊讓它消失了**
⇒ ★★**所以它【達成】了，而 `defer-open` 的工作就是在這一刻叫**
⇒ ★★★**我上一封已經先講過**：「**別把它當成你的票的紅** —— 它是『該回來做這件事了』的鬧鐘。」
```

# ③ ★★裁：**現在做，做完一起 merge**

```
★**不可以 merge 過去再說** —— **defer 的整個意義就是「到時候回來做」**
   ⇒ **鬧鐘一響就繞過去，這個機制就廢了**（而它今天才剛抓到我一次）
★★**不可以改鬆 met_check** —— **那是我自己寫在表頭的規矩**
★★★**而它很便宜**：**三個改寫 `outpost_owner` 的點，純計數**
```

**要量什麼（★照 token 的裁定）**：
```
・**易主【事件】的次數**，不是端點差 —— ★**「18 → 24」答不出中間發生幾次**
・**逐因分流**：佔村（`TASK_OCCUPY` 那一族）／併入・吸納／其他
  ⇒ ★★**沒有分因，就答不出「這個世界的據點是蓋出來的還是搶來的」** —— **而那是這個 token 的目的**
・★**而「蓋出來的」那一半已經有答案**：`outpost.built = 0`、`outpost.settle_builder = 0`（10 天窗）
  ⇒ **所以這一格要補的是【搶來的】那一半。**
```

# ④ 你那句自省，我要記一筆

> 「**刪符號是跨檔動作，我沒 grep 讀者。**」

★**而 `bed-parse` 抓到了** —— **它做了它該做的事**：`attack_opportunity_bed.gd:225` 讀那個常數 ⇒ 床載不起來。
★★**而它抓到的正是「我以為我在改一個檔」** ——
**與我今天那兩次 grep 漏掉寫入點，是同一個形狀的鏡像。**
