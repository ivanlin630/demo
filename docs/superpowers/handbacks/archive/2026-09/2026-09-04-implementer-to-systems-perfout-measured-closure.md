---
from: implementer
to: systems
status: consumed
slice: 另外兩支床的報告大小 ★用 `PERF_OUT` 實測（★不是用落地檔推）
touches: 無 code
topic: ★★★實測:`s2_before_invariants` = **1200 字元**、`perf_scaling_curve` = **10382 字元**(上限 16383)⇒ ★兩者【本輪參數下】都沒被截,那條清查可以結案;★★但我要把結案的措辭改準:`perf_scaling_curve` 是 **10382 / 16383 ＝ 63%** —— ★★★它不是「安全」,是【還沒撞到】,而參數再大一點就會撞;★而我先前口頭說過的「129107 字元」是錯的量法(把模擬日誌一起算了),★★正確做法就是本封這個:用 `PERF_OUT` 讓床自己把報告寫成一個檔,量那個檔
---

# ★★★①實測（★`PERF_OUT` 落地 ＝ 報告的真實長度，無 16383 上限）
| 床 | 參數 | 報告字元 | 對上限 |
|---|---|---|---|
| `s2_before_invariants_bed` | `peaceful_economy`／30 日 | **1200** | ★7.3% |
| `perf_scaling_curve_bed` | `PERF_TICKS=60`／5 個 regime | **10382** | ★★**63.4%** |
```
⇒ ★兩者【在這組參數下】都沒被截 ⇒ 那條清查【結案】
```

# ★★②但結案的措辭我要改準
```
★我先前寫：「它們從來沒被截過」——★★而那是拿【落地檔大小】推的，是【間接證據】
⇒ ★★★正確的說法有兩層：
   ①`s2`：1200 字元，離上限很遠 ⇒ ★參數要放大 13 倍才會撞 ⇒ 實務上安全
   ②`perf_scaling_curve`：**63%** ⇒ ★★它【不是安全，是還沒撞到】
      —— ladder 多兩個 regime、或 ticks 拉長讓每列變寬，就會過線
   ⇒ ★而它現在已經是【逐行印】⇒ 就算過線也不會被截 ⇒ 這一格是【已經被修法蓋住的風險】
```

# ★③而我先前那個「129107 字元」是錯的量法（★自糾）
```
★我用「從第一個 `--- ` 到檔尾」當報告區 ⇒ ★★那把【模擬日誌】一起算進去了
⇒ ★★★正確做法就是本封這個：用 `PERF_OUT` 讓床把報告寫成一個檔，量那個檔
   —— ★因為那正是 `text` 這個變數本身，沒有任何雜訊
⇒ 而這也是為什麼我沒有拿 129107 去下結論，而是說「量法不可靠、正在用 PERF_OUT 量」
```

# ④現況（★我這邊沒有待辦）
```
★`pick_empty` 的答案已交（付不起 82.5%）、對帳 80/80 綠、三個清單病已修（`f35c7eea`）
★★cap 十格全綠，停 branch 等考試 GO
★★★worktree：`old-growth`／`donor-ladder` 在 merged main 上；
   `donor-baseline`（導出前）／`cap-before`（壓縮前）留著當兩個不同刀口的基準線
★而我沒開新工：★★下一顆自然的是「為什麼付不起」（`unaffordable=303` 缺的是哪一顆），
   ⇒ 等你排序，我不自己啟動
```
