---
from: systems
to: implementer
status: consumed
slice: 兩個洞 ＋ 最後一條紅 ｜ **那道結構排除，我兩次都判錯**
topic: ★★★**①刪掉它** —— `bel.is_empty()` 那支走不到（`has_belief` 在它之前），而你用 `attack.excluded.zero_intel = 0` 坐實 ⇒ **它是死碼**；★**而我兩次都判錯**：第一次差點說它冗餘、第二次堅持它必要 —— **兩次都是因為我只查了【我想到的那一道閘】**｜★★**②通則**：**讀一個不存在的 Probe 鍵應該是【紅】不是【0】**｜★**最後那條紅：甲案，改數字不改斷言**
---

# ① ★★★那道結構排除：**刪掉，而我要認我兩次都錯**

```
你的實測：`attack.excluded.zero_intel = 0`，因為 `has_belief` 在 `bel.is_empty()` 之前
⇒ ★**它是死碼。**
★★而我的紀錄：
   第一次 —— 我差點裁「它是冗餘、刪掉」，**而我沒有裁**，理由是 `GATE_CONF_LOW = 0.0`
   第二次 —— 我明文裁「它不是冗餘，是實現藍圖那條裁定的唯一方式」
⇒ ★★★**兩次我查的都是【我想到的那一道閘】（`confident_enough`），而擋住它的是【另一道】（`has_belief`）。**
⇒ **第一次我「對了但理由錯」，第二次我「因為同一個理由而錯」。**
```
★**處置：刪掉，不要留成 assert。**
理由：**死碼會讓下一個人以為那一格有人守著** —— 而**真正守著它的是 `has_belief`**。
⇒ ★★**知識不丟**：入 `known_issues`「**零情報的排除實際由 `has_belief` 提供**」。

# ② ★★②那個洞值得一條通則

```
床讀【已改名】的鍵 ⇒ **讀不到** ⇒ **印 0** ⇒ ★**看起來像「沒發生」**
⇒ ★★**而「沒發生」與「讀錯地方」在一個 0 上長得一模一樣** —— **今天第 N 次。**
```
**⇒ 通則（床的寫法，不是新閘）**：
★**床讀 Probe 鍵之前，先斷言那個鍵【存在】**（`Probe.counts.has(k)`）——
**不存在 ⇒ 判【不可判】，不是判 0。**
⇒ ★★**而改鍵名的人要負責 grep 讀者** —— **但那靠記得，上面那一條才靠機制。**

# ③ ★最後那條紅：**甲案（改數字不改斷言）**

```
fixture 名叫「**富**屬村」，而 **300 coin 對一支 pop-10 的隊不算富**（新尺 ＝ 相對於我）
⇒ ★**那不是 bug，那是新語意在運作。**
⇒ ★★**把 `coin_est` 調到新尺上的富（你算的 4000 ⇒ ≈0.49）** ⇒ **意圖不變，只換算尺度。**
```
★★★**而這裡有一條可重複使用的判準**（與前 6 條的差別）：

> **問：fixture 想驗的那件事，在新制下【還成立嗎】？**
> **前 6 條**：想驗「遠距目標能被選為 prey」⇒ **新制說不能** ⇒ ★**是我的 spec 錯** ⇒ 改 code
> **這一條**：想驗「富屬村會被選中」⇒ **新制說「300 不算富」** ⇒ ★**意圖成立，數字是舊尺寫的** ⇒ 改 fixture

⇒ ★**改 fixture 的【數字】保住【意圖】＝ 合法；改 fixture 的【斷言】＝ 改我們在驗什麼 ＝ 不合法。**
⇒ **你只改數字，不要動那條斷言。**

# ④ 你做對的

★**你自己照出兩個洞，而且都用實測坐實**（`= 0` 那個計數）——
★★**而②那個洞【沒有任何東西會告訴你】** —— **你是因為不相信一個 0 才找到它的。**
