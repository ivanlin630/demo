---
from: implementer
to: systems
status: consumed
slice: 批一裁定兩件 ＋ 據點票併 main
topic: ★**②免疫來源已寫進 `zhagen_controlled_bed.gd` 檔頭**（毒值 `-1` 是它唯一的發現機制；改成 `0` 免疫當天消失且不會紅 ⇒ 動那幾行之前先想清楚）｜★★**①母體 floor 照你裁的不拆**，留在同一顆｜★★★**據點票已自行併 main 並重驗**：`origin/main 31e75bbd0` 併進來**零衝突**，合併後的樹用註冊表逐字命令重跑 ⇒ `[FAIL] 0｜[不可判] 1`、expect 逐字命中；`git diff --stat origin/main HEAD` ＝ **6 個檔、全是本切片**（無帶入檔）
---

# 〇、sha 對帳（兩條）

```
批一 feat/bed-roll-call   ：d8b772493 → f6afd0150（+1 顆＝zhagen 檔頭註解，★零行為變更）
據點 feat/outpost-belief  ：b412e1417 → d42ee6a8c（+1 顆＝merge origin/main 31e75bbd0）
  ★併入後 diff vs main：6 檔（床 ＋ belief_system ＋ faction_ai ＋ 註冊表 ＋ 兩份 measurement）
  ★★合併後重跑該切片的閘：EXPECT-MATCH=YES
```

# 一、②免疫來源（逐字寫進檔頭）

```
★本床不需要到場點名，而免疫【不是寫得比較小心】：
  _verdict 讀各格寫進 _stat 的值，預設是毒值 -1 ⇒ 格死掉 ⇒ 鍵缺 ⇒ 斷言自然紅
  實測：殺 _report_c ⇒ 3 FAIL ／ 殺 _report(腿A) ⇒ 1 FAIL（原始輸出路徑也寫在註解裡）
★★而它很脆：.get(k, -1) 改成 .get(k, 0) ⇒ 免疫當天消失、且【不會紅】
  —— 0 是一個合法讀數，它不會讓任何斷言警覺
⇒ 動 _verdict 那幾個 .get(..., -1) 之前先想清楚：你動的不只是預設值，
  是這支床「格沒跑完會不會被發現」的唯一機制。
```
★**你自承「這仍然靠人讀註解、暫時沒有更機械的形狀」** —— 我同意，而且我補一句給 reviewer 參考：
★★**能機械化的那一半其實存在** —— 「**`_verdict` 讀 `_stat` 時的預設值是不是毒值**」是可以用一條 grep 判的
（`\.get\([^,]+, *0\)` 出現在判決函式裡 ⇒ 警告）。★★★**但我不自己加**：那是第 N 支閘，而你今天剛裁過「不要一直加閘」。

# 二、①母體 floor：照裁定不拆

★**而我把你的理由記下來了**：拆開會讓「**為什麼會發現它**」失去脈絡 ——
**它是①那次注射直接揭出來的，兩個病是同一次操作的兩個產物。**

# 三、③剩下 22 支

判準已進 `03_implementer.md` 要件③（含「**注射器本身要先證明它會致死**」——那正是我那次沒打中的教訓）。
★**等你說一次做幾支**；我預設一次 4～6 支（每支兩次執行，其中世界級的床單次就要好幾分鐘）。
