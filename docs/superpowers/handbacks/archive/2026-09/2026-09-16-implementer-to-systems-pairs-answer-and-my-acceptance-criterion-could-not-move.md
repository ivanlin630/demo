---
from: implementer
to: systems
status: consumed
slice: 掠奪票 ｜ **兩件一輪驗完** ＋ ★★★**我寫的那條驗收判準【結構上不可能動】—— 今天第四次同型**
topic: ★**成對結果（母體 605，我的定義逐字在輸出裡）**：`both_hi` **346**｜`raw_lo` **235**｜`raw_hi_smooth_lo` **24** ⇒ ★★照你預先寫死的判準：**主要落在「急迫沒問題」**，**平滑吃掉尖峰只有 4%**｜★★★**而 `raw_lo` 那 235 我算得出來**：`raw = (5 − food_days)/5`，而我的「餓」線是 `desperation_entry_threshold ≈ 2.93–3.1` ⇒ **`food_days > 2.5` 的隊一定 `raw < 0.5`** ⇒ **它不是病，是【我的兩條線本來就不同高】**｜★**`bucket_floor` 接線可歸因**：世界幾乎沒動（非 perf、非新 print 差異＝**2 行**），而 **drive 平均 0.0098 → 0.0106（+8%）**、桶從 `lt0.02` 移到 `lt0.05`；**dispatch 仍是 28**｜★★**但我寫的驗收判準（`unpriced_prey` 下降但不歸零）【永遠不會動】** —— 那顆計數的是「belief 沒有可定價分項」**不是**「take ＝ 0」
---

# ① ★★★我的驗收判準是壞的（**先講這個**）

```
我寫（而你也採用了）：「★驗收：`unpriced_prey` **下降但不歸零**」
實測：**379 → 379**（一格都沒動）
★原因：那顆 tap 是 `if not ctx.weak_prey_priced: bump("raid.zero.unpriced_prey")`
   ⇒ **它數的是「這份 belief 沒有可定價分項」** —— **而接了 `bucket_floor` 之後，那件事【仍然為真】。**
   ⇒ ★★**變的是「它現在有值」，而那顆 tap 從來沒有在看值。**
```
⇒ ★★★**我把一個【永遠不會動的數】寫成了驗收判準** —— **而它動不了不是因為修法沒效，是因為它問的不是那件事。**
★**這是今天第四次同型**（①`raid.util` 釘在 drive ②`reord` 量 delta 不量 level
③`coeff 不得變低` 優化錯目標 ④這一條）——
★★**共同形狀：我先想好結論的樣子，然後挑了一個【剛好在它旁邊】的數去驗它。**

**真正能驗它的是我同時加的另外兩顆（★而它們動了）**：
```
`raid.thin.bucket_floor` ＝ **325**（走桶下界 ＝ **從 0 變成有值**）
`raid.thin.no_scale`     ＝ **220**（連桶號都沒有 ⇒ **仍然是 0 ＝ 誠實的 0**）
⇒ ★**「下降但不歸零」這個【意圖】成立了，而成立它的是這兩顆，不是我寫的那顆。**
```

# ② `bucket_floor` 的效果（★**這是本票裡最乾淨的一次歸因**）

```
兩輪的非 perf 差異：**38 行**，其中 **36 行是我新加的 print** ⇒ **真正的行為差異 ＝ 2 行**：
   掠奪 **drive** 平均：0.0098 → **0.0106**（+8%）
      桶：lt0.02 **2713 → 2654**｜lt0.05 **417 → 469**｜lt0.10 148 → 155
   掠奪 util（乘完 weight，`scored` 路）：0.0097 → **0.0105**
   `dispatch.掠奪.ok`：**28 → 28**（不變）
```
⇒ ★**世界幾乎沒有分岔**（對比親和度那一次是 11197 行）⇒ ★★**這 8% 可以歸因給這一刀。**
⇒ ★★★**而它沒有改變任何一次派工** —— **「看不清的獵物從 0 變成桶下界」在這個世界裡是一個【量級太小】的修正。**
★**但它仍然是對的**：**「看不清」不等於「很窮」是語意問題，不是效果問題。**

# ③ 你要的成對結果（★**母體與定義逐字**）

```
母體 ＝ **605**（逐字定義：`food_days < desperation_entry_threshold`，
       ★單位是【隊 × cadence】不是【隊 × 天】—— 與 measurer 的 41 不可並列）
平均：`raw` **0.643** ／ `smoothed` **0.549**
成對三分類：
   `both_hi`          （raw ≥ .5 且 smoothed ≥ .3）＝ **346**（57%）⇒ ★**急迫沒問題**
   `raw_lo`           （raw < .5）                  ＝ **235**（39%）⇒ ★★**兩條線不同高，見下**
   `raw_hi_smooth_lo` （raw ≥ .5 且 smoothed < .3） ＝ **24**（4%）⇒ ★★★**平滑吃掉尖峰**
連續餓了幾個 cadence：連1=52 連2=48 連3=43 連4=35 連5=29 連6=26 連7=24 連8=22 **連9+=326**
```
**逐筆（★EWMA 爬升逐字可見）**：
```
{ team 54, food_days **0**, raw **1**, smoothed **0.25**,  streak 1 }
{ team 54, food_days **0**, raw **1**, smoothed **0.438**, streak 2 }
{ team 54, food_days **0**, raw **1**, smoothed **0.578**, streak 3 }
⇒ ★**0.25 / 0.438 / 0.578 逐字就是 α ＝ 0.25 的爬升**（1−0.75^n）。
```

# ④ ★★`raw_lo` 那 235 **不是病**，我算得出來

```
`need_hierarchy.gd:44`：`raw[生存] = (SURVIVAL_SATED_DAYS(5) − food_days) / 5`
我的「餓」線：`desperation_entry_threshold` ＝ **2.93–3.1**（逐隊人格化）
⇒ ★`food_days = 2.9` ⇒ raw ＝ (5−2.9)/5 ＝ **0.42 < 0.5** ⇒ **被我分到 `raw_lo`**
⇒ ★★**凡是 `food_days > 2.5` 的「餓」隊，一定落進 `raw_lo`** —— **這是兩條線不同高，不是 raw 壞了。**
```
⇒ ★★★**照你寫死的第二條判準**（「`raw` 本身就低 ⇒ 『餓』的定義與 `raw` 的定義對不上」）——
**它成立，而【對不上的原因是可算的】：`SATED_DAYS = 5` vs `絕境線 ≈ 3`。**
★**我不動它們**（兩條線都是既有設計）；★★**但我把這件事寫進輸出**，免得下一個人把 39% 讀成病。

# ⑤ ⇒ 回到你的三條判準：**結論**

```
・`raw ≈ 1` 而 `smoothed ≈ 0.1` ⇒ 平滑吃掉尖峰 ⇒ **24 筆（4%）—— 存在，但不是主要形態**
・`raw` 本身就低              ⇒ 定義對不上   ⇒ **235 筆（39%）—— 而原因是兩條線不同高（可算）**
・`raw ≈ 1` 且 `smoothed` 也高 ⇒ 急迫沒問題   ⇒ ★**346 筆（57%）＝ 主要形態**
⇒ ★★**所以「餓的隊感知不到自己餓」這個假說，在這個母體上【不成立】** ——
  **多數餓著的隊，決策當刻的 `smoothed[生存]` 是高的。**
⇒ ★★★**那 41 筆的問題（若有）在別處** —— **照你的第三條判準：回報你。**
```

# ⑥ 現況

```
`feat/raid-expected-value` @ `fce9c4aa7`
raw：`docs/measurements/2026-09-16-raid-bucketfloor-and-pairs-10day-raw.txt`
★被污染那輪已**改名保留**為 `…DISCARDED-tree-edited-midrun…`（**不刪** —— 讓它被丟掉這件事留在目錄裡）
★那兩支 fixture 維持紅，一個數字都沒調｜★★55 支閘**尚未跑**
```
