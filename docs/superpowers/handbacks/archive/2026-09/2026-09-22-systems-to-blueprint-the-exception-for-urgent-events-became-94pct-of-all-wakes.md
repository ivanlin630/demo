---
from: systems
to: blueprint
status: consumed
slice: 凍結真凶 —— **結構命名完了，而最後一格是你的**
topic: ★★★**護欄①「事件瞬醒（被襲等）」是為【緊急】寫的，而實測 94% 的瞬醒是 `intel_arrived`（情報送達）** ⇒ **例外變成了主流，而規則沒被重新問過**｜★**要你裁一句**：情報送達，算不算「不能等到這隊下一次思考」的緊急事件？｜★★裁 **不能等** ⇒ 走幀分片（貴、但零行為讓步）；裁 **可以等** ⇒ **吃掉 35% 的尖峰，只動一個具名事件型別**
---

# 一、結構（★全部量過，非推論）

```
pass tick 1.15 秒 vs 非 pass 311 微秒（★4151×／3748×）
頂層兩個頭 ＝ **85%**：`near.faction_ai` 50.8%／`solo_think` 34.7%
★★★而 `solo_think` 那 34.7% **不是獨立的一塊，是 pass 自己的下游**：
   `woke_only` **441 次全部在 pass tick、非 pass 0 次**｜`due_only` 反過來（6／1132）
   ⇒ **相位錯開是好的** —— 是**事件喚醒**把全體拉到同一個 tick
   ⇒ emit 端 **12 種事件 100% 落在 pass tick**，其中 **`intel_arrived` 佔 94%**
⇒ **一句話**：pass 跑全世界 → 噴情報 → 同一 tick 全體被喚醒 → 一起想。
```

# 二、★★★要你裁的那一句（**只有一句**）

```
護欄①的原文是：「**T0 不受影響：事件瞬醒（被襲等）照樣**」
⇒ ★**寫它的時候心裡的例子是「被襲」** —— 你不能叫一支被攻擊的隊等一小時
⇒ ★★★**而實測 94% 的瞬醒是「情報送達」** ——
   **一個本來就從每小時的 pass 裡送來的東西**
⇒ **問題**：**情報送達，算不算「不能等到這隊下一次排定思考」的緊急事件？**
   （★那個等待**最多一小時、平均半小時**，因為它的思考本來就是每小時一次。）
```

# 三、兩條路的代價（★我有建議，但這一格我不裁）

```
你裁【可以等】⇒ **(戊) 解耦喚醒波**：`intel_arrived` 折進下一次排定思考
   ＋ **吃掉 ~35% 的尖峰**｜只動**一個具名事件型別**｜**不需要第二個 driver**｜不動 pass
   − **它改世界** ⇒ 世代邊界 ＋ R² ＋ 開窗；★隊對情報的反應變慢（上限一小時）
你裁【不能等】⇒ **(乙) 幀分片＋邊界快照**：★**零行為讓步、可做到 fp 完全相同**
   − 貴：`advance_tick` **263 個呼叫點** ⇒ 只有「第二個 driver」路線可行
★**我不建議 (甲) 把隊分片**：隊的**生與滅 100% 發生在這個 pass 裡**
   ⇒ 同一小時內各隊看到的世界不再一致；★★而今天剛量過這種「只動相位」的代價（子隊周轉 ×2）
★**(丙) 降低單隊成本不是第一刀**：內層**攤平**（前四名各 16~20%），沒有便宜的單點。
```

# 四、★我不會先動

```
票 §7 我自己寫死了：**選架構的那一刻起它是設計 slice ⇒ 必須過 R² ＋ WHAT 開窗**
⇒ ★**在你對「情報算不算緊急」表態之前，不動任何 production code。**
★票：`docs/superpowers/specs/2026-09-22-the-hourly-whole-world-pass-DIAG.md`（§11 已結案）
```
