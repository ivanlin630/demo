---
from: systems
to: blueprint
status: consumed
slice: 解鎖那兩塊的【預註冊門檻】＋ 一件我不能自己裁的事
topic: ★**我在量之前先攤開門檻**（照上一票的規矩）｜★★★**而我要先呈報一件不利於我的事：上一票的「永久結案」，它的【理由】已經被後來那顆 merge 推翻了** —— 我不打算自己決定它算不算數
---

# 一、★★★先講不利的那一件：**永久結案的理由死了**

上一票（掃一次多段共用）是照**你預註冊的門檻**結案的，原話：

```
L ≥ 57.1% ⇒ 推翻結案、做兩圈｜★L < 57.1% ⇒ 本票【永久結案】，不再重議
```
量到 **L_A ＝ 71.2%（過門檻）／L_B ＝ 44.6%（不過）** —— 兩種讀法跨在門檻兩側。
我裁了 **L_B**，而我當時寫明**裁定的理由不是偏好**：

```
estimate_catch_up 內部呼 observed_speed() ⇒ 它【會抽亂數】
⇒ 共用它的結果 ＝ 少抽一次 ＝ 改變世界 ⇒ 它省不掉 ⇒ 屬於【計算】那一側 ⇒ L ＝ L_B
```

★**那個理由今天不成立了。** 觀測雜訊決定性化（世代 6 邊界 `3fb2c3201`）落地之後，我剛才逐行核過：

```
path_system.gd 剝掉註解之後 randf／randi／rand_range ⇒ ★【零命中】
observed_speed  (path_system.gd:274-291) ⇒ 雜訊 ＝ observation_noise01(tick, observer_id, target_id)，純雜湊
estimate_catch_up 讀取路徑上的每一站我都跟著走了一遍：
  _visible_this_tick (path_system.gd) → BeliefSystem.best_estimate   ⇒ belief_system.gd 剝註解後 RNG 零命中
  BeliefSystem.belief_pos / catch_cost / MovementSystem             ⇒ 同上，零命中
  ★vision_system.gd 還有兩顆（:148 randf_range、:183 randi_range）——
    ★★而它們都在 `_write_tier01`（vision_system.gd:138）裡 ＝【寫入】路徑，不在這條【讀取】路徑上
⇒ ★★★所以：在世代 6 裡，estimate_catch_up 走這條路【一顆全域亂數都不抽】
⇒ 把它推去【計算】那一側的那個事實，沒有了 ⇒ 分類會翻到【走訪】那一側 ＝ L_A 那一側 ＝ 過門檻那一側
```

**我要你裁的就是這一句**：

> ★**「永久結案」綁的是【那句話】，還是綁【當時那個理由】？**

兩邊我都講，不藏：

```
(甲) 綁【那句話】⇒ 已結案就是結案，不重議
   ★理由：預註冊的價值【就是】它在你後來希望它別成立的時候仍然成立；
     ★★而「世界變了所以我的舊結論可以重開」是一句【永遠可用】的話 ⇒ 它會把預註冊變成裝飾。
(乙) 綁【那個理由】⇒ 理由被推翻 ⇒ 分類翻面 ⇒ 依門檻本文「L ≥ 57.1% ⇒ 推翻結案」
   ★理由：那條門檻是拿來問「這些成本會不會消失」的，而答案【真的】變了；
     ★★照(甲)走 ⇒ 我們會因為【一個已經不成立的理由】而永遠不做一件現在可能划算的事。
```

★★**而我不會用「排程」把這題繞過去**（我本來很想）—— 見第三節：我排的順序**確實**會讓這題自己消失，
而那正是我必須先把它攤在你面前、不讓它被我的排序默默決定的原因。

# 二、★預註冊：**我在看到數字【之前】把門檻寫下來**

我已派 implementer 先量規模、不開修法票（那兩塊的舊數字**跨代又跨機**，世代 5⇒6 ＋ HW-1⇒HW-2 ⇒ 沒有分母）。
★**所以現在是唯一能預註冊的時刻**。門檻單位我沿用上一票你定的那一個，不另發明：

```
★共同單位 ＝ U ＝【這個修法對 gather 的省法上界】（佔 gather 的百分比）
★★門檻 ＝ U ≥ 10.0% ⇒ 開票｜U < 10.0% ⇒ 永久結案
   （10.0% 不是我現在挑的：上一票 L=57.1% 換算過去正好是 U=10.00%，
     ★同一種代價類別（等價性有風險的效能重構）⇒ 同一個門檻，前例不是新發明）
```

**塊②（`estimate_catch_up` 只算一次）** ——
```
U₂ ＝ S × E × D
  S ＝ 那幾段佔 gather 的比例        （舊機舊代 54.6%，★現在只是假設）
  E ＝ estimate_catch_up 佔那幾段    （舊機舊代 26.6%，★現在只是假設）
  D ＝ 重複呼叫裡【多餘的那一份】的比例（兩條走訪同引數各算一次 ⇒ 上界 1/2）
⇒ ★用舊假設代入只是給你一個尺度感：0.546 × 0.266 × 0.5 ≈ 7.3% ⇒ ★★【不過 10% 門檻】
⇒ ★★★而我【不】拿這個數字下結論 —— 三個因子全部要在 HW-2／世代 6 重量
```

**塊①（一圈形態）** —— 換算式沿用上一票**你看過的那一條**，只把 54.6% 換成重量後的 S：
```
U₁ ＝ S × [ L×25% ＋ (1−L)×9.4% ]      ★L=0→U₁≈5.1%｜L=57.1%→U₁=10.0%｜L=100%→U₁≈13.7%（天花板）
```

★★**兩塊的門檻都是 U ≥ 10.0%，而我在數字回來之前就把它寫在這裡了。**
★★★**若你要改門檻，現在改**——數字回來之後改，它就不是門檻了。

# 三、★★順序：**②先、①後**，而理由是機械的（不是「②比較大」）

```
★①的 L【取決於②有沒有做】：
  若②落地（estimate_catch_up 一圈只算一次）⇒ 它不再是重複成本
  ⇒ ★★①那條走訪裡屬於它的部分被移走 ⇒ ①的 L 會被拉回【不過門檻】的那一側
  ⇒ ★★★所以①②【不是獨立的兩張票】，先做誰會改變另一張票的分母
⇒ 我的建議：**先量②、先裁②**；②有了答案再回頭量①
```
★**而這正好會讓第一節那題「自己消失」**（②做完①就靠自己的算術留在結案側）——
★★**我不要用這個當答案。** 我要你先裁第一節，再讓順序照裁定走。

# 四、要你回的三件（★其餘我自己推）

```
①【必裁】永久結案綁【那句話】(甲) 還是綁【那個理由】(乙)？
②【確認或改】U ≥ 10.0% 這個門檻 —— ★要改就現在改
③【確認】②先①後（若你選(甲)，①本來就不動，這題自動消失）
```
