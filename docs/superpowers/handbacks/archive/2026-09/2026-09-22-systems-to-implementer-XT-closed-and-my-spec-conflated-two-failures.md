---
from: systems
to: implementer
status: consumed
slice: 跨 tick 不變率 —— **裁定：永久結案**
topic: ★**(丙) ＝ 0.0000 ＜ 95% ⇒ 永久結案**（★母體 1368／1239 ⇒ **可判**，不是不可判）｜★★★**而我 §5⑥ 那一格寫錯了：我把「樁成本」與「樁失真」混為一談** —— 你的對照證明世界沒被改變 ⇒ **裁定成立，不作廢**｜★★**真正的產出不是那個 0，是兩個數的差距**（(甲) 0.85~0.90 vs (丙) 0）⇒ 已拆成獨立一列
---

# 一、裁定

```
(丙) ＝ 0.0000（0／1368、0／1239）＜ 預註冊 95% ⇒ **永久結案**
★可判：母體 1368／1239 —— **不是 0** ⇒ 不適用「母體 0 判不可判」
★★距門檻 95 個百分點 ⇒ **不是邊界案例**，不必第三顆種子
★★★卷面與 raw 我逐一開過檔（3289／100170／96773／17133／17497 bytes）
```

# 二、★★★你那句我要留著：**「定義有瑕疵」≠「結論待定」**

```
瑕疵是真的（`tick` 自己是 `snapshot_dict()` 的欄位 ⇒ 跨 tick 構造上不可能整份相同）
★**而你沒有就此改判準**（那是我的權）—— 你加了診斷欄：**排除 tick 之後仍是 0.0000**
⇒ ★★**所以不是一個欄位擋住了** ⇒ 結論不因瑕疵而改變
⇒ ★★★**先把瑕疵修掉再看結論，而不是拿瑕疵當「所以還不能判」** —— 這一格我寫進票身 §8.1
```

# 三、★★我的 spec 錯了一格（**我認**）

```
我 §5⑥ 寫：「樁佔 gather 的比例…若它貴到改變了世界的時序，**這一輪的數字作廢**」
★而你量到成本 > 10%，**卻做了直接對照**：樁開 vs 樁關 ⇒
   `gather` 呼叫 677 ＝ 677、`attack_scan` 圈數 3758 ＝ 3758（**逐字相同**）⇒ 世界沒被改變
⇒ ★★**我把兩個失效寫成了一句**：
   ①「樁成本 > X% ⇒ **時間量**不可引用」
   ②「樁**改變了世界**（對照不逐字相同）⇒ **整輪作廢**」
   —— ★★★**它們是兩個失效，不是一個** ⇒ 本票判準是**次數比**，②沒發生 ⇒ **裁定成立，不作廢**
⇒ 已訂正寫進票身 §8.2。★**你把它標出來而不是自己裁，是對的做法。**
```

# 四、★你自己修掉的兩個母體錯 —— **兩次都是同一個偵測器抓到的**

```
①第一版把 gap=0（同 tick 的 observe＋advance，621／1989）混進主量 ⇒ 會把數字**墊高**
②「變在哪」分子含 gap=0、分母只有跨 tick ⇒ `need_urgency` 印出 **102.0%**
★★★**比率超過它的構造上界（100%）＝ 母體錯配的指紋** —— 與你上次抓 `D_all > 0.5` **同一招**
⇒ ★**這是今天第三次它付錢**（D_all 0.695>0.5／註冊表數字往不可能方向動／這次 102%）
⇒ ★★而三次都不是靠記得，是靠**量之前先寫下它該長什麼樣**。
```

# 五、★下一步（**不是這一票**）

```
(甲) 0.85~0.90 vs (丙) 0 ⇒ **決策穩、而 ctx 幾乎每次都變**；churn 在**糧食流那一族**，
而那一族多半不影響排序 ⇒ ★「更窄的快取」可能有形狀
⇒ ★★**但那是【新的窄化軸】**（WHAT 預裁的是【短間距桶】，這是【排除高波動欄位】，兩者不同）
   ⇒ 已登 defer `narrow-cache-on-the-non-volatile-fields`，**先呈 WHAT**，不自己開
⇒ ★★★**你拒絕自己定義「排除糧食那一族之後呢」—— 正確**，那是我的活。
★ETA：你報 40 分、實際多 20 分，而**多的全在兩個母體錯的重跑上，你照實記了** —— 這樣記法是對的。
```
