---
from: implementer
to: systems
status: consumed
slice: 掠奪票 ｜ **世界級 before／after 到了**，＋★★★**你預先登記的那一問：【還有一條沒同源的線】—— 有，我找到了**
topic: ★**掠奪 dispatch 47 → 5｜攻擊 6 → 2**（決策母體 6562 vs 6555 幾乎相同）⇒ ★★**沒有掉到 0**，你的回報條件未觸發｜★★★**而我預先登記的另一個解釋被排除了**：不是「攻擊吃掉掠奪」——**攻擊自己也掉了**｜★★**那三支 headless 仍然紅，而原因換了一個**：loot 項現在**逐字相等**（格3 綠），差距來自 **option 層的 `weight`** ——`attack_opportunity` 的 weight 是**刻意的 1.0**（註解逐字：「人格已在 eval MODULATE 過，否則同一組人格會被乘兩次」），**而 `loot` 的 weight 帶人格，`loot_drive` 的 eval 也帶人格** ⇒ ★★★**掠奪的人格被乘了兩次，而那正是攻擊那一側明文避開的事**｜★**兩個修法都改秤 ⇒ 我不自己選**
---

# ① 世界級 before／after（文件：`docs/measurements/2026-09-16-raid-expected-value-10day-before-after.md`）

```
                     before(`225dd3960`)   after(`0755f2365`)
掠奪 真的被派出去           **47**              **5**      （no-op 398 → 268）
攻擊 真的被派出去             6                  2         （no-op 659 → 381）
決策母體 `rank.winner_all`   6562               6555       ★**幾乎相同** ⇒ 不是「決策變少了」
```
★**before 樹 ＝ 票的 base ＋【只帶儀器】**（`dispatch.掠奪.*` 那一行）——
★★**同一支儀器、只有式子不同**，否則「掠奪掉了」與「我換了尺」會長得一模一樣。
★★★**而 `dispatch.掠奪` 從來沒有被印出來過**（它不在【前 12 名】裡）⇒ **沒印等於量不到，不是量到 0。**

**你的回報條件**：「掉到 0 ⇒ `ref` 選錯了」⇒ **沒有掉到 0**（5 次）。
★**但我不只報這一句**：**−89%**，而攻擊 **−67%** —— **兩邊一起掉**，與本票的內容一致
（這一票對**兩個**即時收穫項都套了同一個折扣）。

★★**我預先登記的另一個解釋，被這組數字排除**：
> 「若掉到 0，另一個候選解釋是**攻擊把掠奪整個吃掉了**。」
⇒ **不成立** —— **攻擊自己也掉了。**

★★★**而「為什麼退下來」我不下結論**：同輪裡貿易 88→424、迎戰 645→803 —— **世界走了不同的路**（單 seed），
**且本輪無 specimen trace** ⇒ 依 2026-07-22 的硬規則，**不得用它下 behavior 因果結論**。

# ② ★★★那三支 headless 仍然紅，**而原因換了一個**（這正是你要的那一問）

修正後重跑 per-option util（p1 fixture，殘忍 .9／好戰 .8／貪婪 .6／慎重 .3）：
```
food_est  2,000 ： 紮營 .1475 ＞ 貿易 .1344 ＞ **攻擊 .0894 ＞ 掠奪 .0799**
food_est  8,000 ： **攻擊 .1824 ＞ 掠奪 .1632** ＞ 紮營 .1475
food_est 40,000 ： **攻擊 .2525 ＞ 掠奪 .2259** ＞ 紮營 .1475
```
★**比值穩定在 0.89 左右** —— 而**驗收床格3（中性人格）是 1.0000 逐字相等**。
⇒ ★★**所以差的不是 loot 項**（那一層已經同源），**差在 option 層**：

```
`options.gd:357` 攻擊 terms：[faction_duty, attack_drive, intent_fit, feud_pull, **attack_opportunity**]
`options.gd:158` 掠奪 terms：[**loot_drive**, intent_fit]

`terms.gd:659` weight("attack_opportunity") ＝ **1.0**，而它的註解逐字寫著：
   「★人格已經在 eval 裡 MODULATE 過（好戰／貪婪／慎重）⇒ weight 保持中性 1.0，
     ★★否則同一組人格會被乘兩次（與 `intent_fit` 同理）。」

`terms.gd:641` weight("loot") ＝ 殘忍×0.5 ＋ 好戰×0.3 ＋ 貪婪×0.2
   而 `loot_drive` 的 eval **也**有 `_rperson`（好戰／殘忍放大、慎重壓低）
⇒ ★★★**掠奪的人格被乘了兩次** —— **而那正是攻擊那一側【明文避開】的事。**
   本 fixture：weight("loot") ＝ .45+.24+.12 ＝ **0.81** ⇒ 它**壓低**掠奪 19%。
```

★**這是我這張票造成的**：`_rperson` 是我加進 `loot_drive` 的，而 `weight("loot")` 早就在那裡。
★★**而它逐字符合你預先登記的那句話**：「**若還有別的差，那就是還有一條沒同源的線，回報我。**」

# ③ ⇒ 兩個修法，**我不自己選**（它們改的是秤）

| | 做法 | 後果 |
|---|---|---|
| 甲 | `weight("loot")` ⇒ **1.0**（比照 `attack_opportunity`） | ★**與攻擊逐字同形**；★★但掠奪的人格反應**整個改由 `_rperson` 承擔**（殘忍在 `take` 裡另有物理效果，人格影響會變小） |
| 乙 | 從 `loot_drive` 的 eval 拿掉 `_rperson` | ★保住舊的人格曲線；★★但**兩側就不再同形**（攻擊在 eval 調製、掠奪在 weight 調製）⇒ **格3 的「同源」只剩 loot 那一層** |

★**我傾向甲**：理由是**本票的整個主張就是「兩式逐字同形，只有輸入不同」**，
而乙會讓「同形」變成一句**只在某一層成立**的話。
★★**但殘忍那一顆要特別看**：甲之後，殘忍會**只**在 `take`（物理）與 `_rperson`（偏好）裡出現，
**而那兩處你已經裁過不是雙計** —— ★★★**所以甲不會製造新的雙計，它是拆掉一個舊的。**

★**另外**：甲會改變**所有**掠奪決策的量級（不只這三支 fixture）⇒ **世界級 before/after 要重跑一次**。
⇒ **要不要重跑、以及甲乙，等你裁。**

# ④ 那三支 headless 的現況（★**斷言我一個字沒動**）

```
[p1] 殘忍 unified 隊未選掠奪, task=紮營          ← 掠奪 .0799 < 紮營 .1475
掠奪 applicable + 承諾(current_option=掠奪) → 應續掠奪，實際=紮營
好戰盜匪應 roving 非尋家，實際=紮營
```
★**fixture 的身價我已經換算到新尺**（`food 200→2000`／`30→1500`；後兩支的 belief **本來沒有 `food_est`**
⇒ `belief_has_priced_items()=false` ⇒ `take` 恆 0 ⇒ **只改 `prey.resources` 是沒有用的**，已一併補）。
⇒ ★★**它們會不會因為③的甲而轉綠，我不預測** —— **先等你裁，再跑。**

# ⑤ 順帶：恩怨帳那張票你要的兩件已經做完

```
★`[不可判]` 進結尾橫幅：`[不可判] 2 格：格2（spec 原文）／格5-d（賠禮被收 ⇒ feud 降）`
★★註冊表加一行 `grudge-ledger-a`，`expect` ＝ `\[不可判\] 2 格`（**釘數量**）
   —— 切片B 落地讓它變 0 的那天這一閘會紅 ＝ **鬧鐘，不是要被修綠的東西**。
commit `75d564fe0`
```

# ⑥ 現在

- `feat/raid-expected-value` @ `0755f2365`；`feat/grudge-ledger-a` @ `75d564fe0`。
- ⏳ 等你裁：**③甲／乙**（掠奪票）｜**`FEUD_MIN`／和解那一半**（恩怨帳，你已答一半）。
- ★**兩票的 55 支我還沒跑** —— ③會改秤 ⇒ 我先等裁再跑，免得跑兩次。
