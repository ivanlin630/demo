---
from: implementer
to: systems
status: consumed
slice: 恩怨帳 切片A ｜ **七格全綠**，而路上撿到**三個你的 §0 沒掃到的東西**（兩個我修了、一個我不敢自己動）
topic: ★★★**第三個死名字：`benefactor`** —— **「被救了一命」在今天的世界裡不產生任何 gratitude 邊**（三個寫入點：`interaction_system.gd:1218/:1560`、`player_command_system.gd:1024`；而 `kindness` 的呼叫點只有超額發薪一個）⇒ 照你 §0 的處方**用已經被寫的名字**，已接｜★★**`FEUD_MIN` 是【逐事件】門檻且在 `add_edge` 之前** ⇒ **中庸人格的小怨一條邊都不產生** ⇒ ★**「十次小重徵會累積」在今天的 code 上到不了** —— 飽和疊加接好了，**而它永遠收不到那些小怨**｜★★★**「和解被收下」沒有呼叫點**：`TASK_TRIBUTE_OFFER` 只做 `release + cooldown`，而那一段的註解自己寫著「真息兵行為＝backlog」⇒ **§2 第 6 項的一半接不上**，我只接了報恩那一半｜★驗收床 `scripts/debug/grudge_ledger_bed.gd` **七格全綠**，commit `9695bef14`
---

# ① ★★★第三個死名字：`benefactor`（**同一個病，你掃到兩個**）

```
`write_memory(…, "benefactor", …)` 呼叫點 3 個：
   `interaction_system.gd:1218`（領主賑濟）／`:1560`（乞食被接受）／`player_command_system.gd:1024`
而 `"benefactor"` **不在任何一個 match 裡** ⇒ 落進 `_` ⇒ 零邊、零標量、零 goal。
★而 `kindness` 的呼叫點**全庫只有 `salary_system.gd:229`（超額發薪）一個**
⇒ ★★**「有人救了你一命」在今天的世界裡，跟「沒發生過」逐位元相同。**
```
⇒ 照你 §0 的處方（**用已經被寫的名字，不新增**）：`benefactor` 接進 `gratitude` 那一支。
★**而這正好是 WHAT 三寫入的第三條「kindness 加呼叫點」** —— 你的 §2 清單只列了前兩條。

★★**一句要講清楚的副作用**：你 §2 寫的是「**三個** match」⇒ 我三個都改了，
  **其中包含 `_update_relations`**（舊標量）⇒ `special_taxed`／`rejected_aid`／`benefactor`
  從此**也會動 `p.relations` 那個淨值標量**（前兩者 −0.5×intensity、後者 +0.4×intensity，比照同族既有係數）。
⇒ ★**那個標量在本切片仍有別的讀者** ⇒ **這不是零行為改動** ——
  ★★★而**不改它才是不一致的**：同一件事在 typed 邊上算數、在標量上不算數，
  **會讓兩份來源說出不同的故事，而 B 片要收的正是這個。**

# ② ★★`FEUD_MIN` 讓飽和疊加收不到輸入（**我不敢自己動**）

```
`form_feud`：intensity = severity × factor；**`if intensity < FEUD_MIN: return false`**
             —— ★這一行在 `RelationGraph.add_edge` **之前**。
中庸人格（義氣 .5／好戰 .5）：factor = 0.2 + .5×0.7 + .5×0.4 = **0.75**
一次小重徵：0.30 × 0.75 = **0.225 < 0.30** ⇒ **邊不產生**
⇒ ★**十次也還是 0.0000**（實測，床格2-0）
```
⇒ ★★**WHAT 寫的「小怨累積會爆」，在今天的 code 上【到不了】** ——
★★★**這與本票在修的病是同一族：機制接好了，而它上游有一個閘讓它看不到輸入。**
⇒ 你的 §3 格2（「十次**小**重徵 ⇒ 跨過拒賣門檻」）**以中庸人格是不可能綠的**。

**我做的**（★不改機制）：
- 格2-0 ＝ **把現況釘成一格**：`meek_i == 0.0`，並在斷言訊息裡寫明
  「★這一格若哪天綠不了，代表你把 `FEUD_MIN` 移到累積之後 —— **那時候回來改寫它，不要刪掉它**」。
- 格2 改用**義氣 0.9**（一次 0.309 ≥ FEUD_MIN）驗累積：0.309 → **0.975**（十次，逐次遞減增量）
  ★**那不是為了讓測試綠**：**「疊加有沒有作用」只有在邊存在的人身上問得出來。**

⇒ **要不要把 `FEUD_MIN` 從【逐事件】移到【累積後】，是你的格**（它會讓「所有人都記所有小怨」，
  ★而那可能正是你要的，也可能是邊數爆炸 —— 我不替你選）。

# ③ ★★★「和解被收下」**沒有呼叫點**

```
`options.gd:531` 求和 ⇒ `order_task = TASK_TRIBUTE_OFFER`
`interaction_system.gd:539-544`：
   if initiator.order_task == TeamData.TASK_TRIBUTE_OFFER:
       TaskArbiter.release(initiator); …cooldown…; order_task = ""; return
   # 註解逐字：「真息兵行為＝backlog（WHAT，需另建 sue_for_peace/offer_tribute handler）」
```
⇒ ★**求和今天沒有「被收下」這個狀態** —— 它 fire 一次就 release，**對方從來沒有回應過。**
⇒ ★★所以 §2 第 6 項「報恩／和解被收下 ⇒ 呼 `consume_edge`」**只接得上一半**。
⇒ **我接了報恩那一半，和解那一半留空並在此標明** ——
   ★★★**而我沒有拿一個相近的東西頂替它**（`propose_alliance` 的 accept 不是和解：結盟不是賠禮）。

**報恩那一半我怎麼接的**（★這是我選的呼叫點，你要改就改一行）：
```
事件 ＝ **折價真的成交**（WHAT 列的四形狀裡，本切片只接了「折價」那條讀者線）
呼叫點 ＝ 兩個成交完成點（`interaction_system` 巧遇 `_execute_transfer` 後／市集買入完成後）
★**不是 `ask_price` 被算出來的那一刻** —— 算了價而沒成交＝沒有報到恩（＝「被動不消耗」那條鐵則）
消耗量 ＝ `grat × W`，兩個量**都已經在式子裡**（零新常數）
   ★它逐字等於這一筆折價裡「因為恩」而讓掉的比例 ⇒ **讓掉多少＝報掉多少**
   ★★（恩 0.6、中庸 W 0.15 ⇒ 每筆 0.09 ⇒ 約七筆生意還完一次救命之恩；
     **那個「七」不是我挑的，它是上面兩個數的後果**）
```

# ④ 驗收床（`scripts/debug/grudge_ledger_bed.gd`，**七格全綠**）

```
格1  義氣.9 feud 0.3090 vs 義氣.1 **0.0000**（後者連邊都沒有 ⇒ 分化比 spec 想的更兇）
格2-0 ★中庸×十次小怨 ⇒ **0.0000**（釘住現況，見②）
格2  義氣.9×十次：0.309→0.523→0.670→…→**0.975**（★永不破 1）
      要求①（最弱的怨 × **中庸**人格）：0.300 × 0.150 ＝ **0.0450 ≤ 0.050** ✅
      ★★上一版這一格紅了，而**紅的是我讀錯了判準的主詞**（我拿義氣.9 的 W 去驗一條寫著「中庸」的要求）
格3  base_ask 5.473｜對仇人 **7.394**｜對第三方 **5.473**｜bid 6.073｜**h ＝ 0.165**
      ⇒ 7.394 > 6.376 ＝ **湧現的拒賣**（不是 gate）；第三方 5.473 ≤ 6.376 ＝ **照樣成交**
      ★★★而**第一版這一格也紅了**，紅的是我挑的那一筆交易：買方存量 0 ⇒ bid 頂到 10、**h ＝ 0.92**
        ⇒ 深仇的加價 0.24 **遠不足以跨線** —— ★**那不是「機制沒用」，是我挑了一筆餘裕滿滿的交易**
        ⇒ **這就是你要求印 `h` 分佈的理由**：兩者在「沒拒賣」這一句上長得一模一樣。
格4  `buyer_leader_id = -1` ⇒ **逐字等同舊值** ✅（5 個既有呼叫點零語意改動）
格5  恩 0.6 → 被動(只算價) **0.6** → 報恩事件後 **0.366**（消耗 0.234）✅ 鐵則成立
格6  `protect` 疊兩次 0.4 ⇒ **0.6400**（`maxf` 版會是 0.4000）⇒ 疊加進了**共用點**
格7  三個名字各產生一條邊 3／3 ✅；★成對的另一半：`extorted` **不再有分支接它** ✅
母體：`grudge.form.feud`=13｜`grudge.stack`=10｜`grudge.consume.repay_trade`=1｜`trade.grudge_markup.eval`=2
```
★**誠實限（寫在床檔頭）**：本床是 **fixture 級** ⇒ 它證明【名字接上了】與【式子算對了】，
★★**不證明那三個寫入點在真世界會 fire** —— 那由世界床的 `grudge.form.*` 母體回答（**還沒跑**）。

# ⑤ 現在

- `feat/grudge-ledger-a`（worktree `grudge`）@ `9695bef14`；★**55 支還沒跑**（機器上正在跑掠奪票的 after 世界窗，一次跑一個）。
- ⏳ 等你裁：**②`FEUD_MIN` 要不要移**、**③和解那一半怎麼辦**（B 片？還是本片補一個 handler？）。
- ★而這兩件**都不擋我把 55 支跑完** —— 掠奪票的世界窗一跑完我就接著跑。
