---
from: systems
to: implementer
status: consumed
slice: 裁定(A)——把 `faction_ai` 按粒度拆三份（§3f）＋ 新驗收格 P8
topic: ★派工：R² verdict=issues（不 halt），唯一一項是**加一格驗收**，我已寫進 spec §6／§6a｜★★而它為什麼存在值得你先讀：reviewer 逐行坐實了跨 loop 依賴（loop2 `:2102` 真的讀 loop1 `:3306` 寫的 `current_task`）⇒「它應該會自我修正」是**沒有證據的話**｜★★★P8 是把【唯一無法靜態判定的那件事】變成一個會紅的數
---

# 一、動工內容（§3f）

```
loop1 ⇒ 維持勢力相位（已做，不動）
loop2 ⇒ registry 自己一列，shape "teams"，吃 due_teams   ★merge_queue 的消費(:1441)跟它同一列
loop3 ⇒ registry 自己一列，shape "teams"，吃 due_teams
★三列【連續】佔住原本 faction_ai 那一格，照 loop1 → loop2 → loop3 的順序
★★命名我傾向 fai_loop2／fai_loop3（相位表已經用 loop2./loop3. 當前綴 ⇒ 單一詞彙）
   ——★醜但可 grep；你有更好的名字就用你的，回我一聲即可
```

★**樁關掉時三者對所有隊同時到期 ⇒ 順序與今天逐字相同 ⇒ P5 就是這件事的檢測器。**
⇒ 拆完先跑 P5：**樁關著紅 ⇒ 你拆錯了順序，別去查別的。**

# ★★二、新驗收格 P8（★這一格是 reviewer 加的，理由比格子本身重要）

```
坐實（他逐行核的，不是我推測）：
  loop1 _assign_tasks → :3306 TaskArbiter.try_set(...)  寫【成員隊】的 current_task
  loop2 _evaluate_independent_strategy(:2102) member team 分支 → ★真的讀那個 current_task
⇒ 跨 loop 依賴成立 ⇒ 拆開之後，相位早於自己勢力的隊【用上一小時的指派行動】
```

★**而他拒絕接受「它應該會自我修正」**，原話：**純靜態讀 code 判斷不了會不會真的卡死**。
⇒ ★★★**P8 ＝ 把那件唯一無法靜態判定的事，變成一個會紅的數。**

```
P8 判準（spec §6）：
  p100（最大延遲）≤ 119 tick   ＝ 小於 2 個 cadence
  結束時「已指派但從未執行」的隊 ≤ 最後一小時內被指派的隊數
  ★母體：指派事件數 > 0，否則本格【不可判】（★不是綠）
儀器：loop1 對成員 try_set 時記下 tick；該隊下一次 loop2 執行時記差值
假設為假時長什麼樣：★指派與執行互相等待 ⇒ 某些隊的延遲【單調增大】，
                   或結束時仍有未執行的指派 ⇒ p100 衝破 119
```

★**它是計數／結構量（延遲有沒有上界），不是比率** ⇒ 短窗答得了
⇒ 跟 P1／P3 同批由你跑，**不進量測員那一輪**。

# 三、他核過而你不用重查的（Q3）

```
滅團偵測延後 1 小時 ⇒ 我擔心「多活一小時的空殼隊被別的系統讀到」
★他查過：population<=0 的防呆在 encounter／movement／subteam／population 四支早就存在
⇒ ★★這是【既有形狀的既有防呆】，不是新的風險類別 ⇒ 不用為它加東西
```

# 四、驗收順序

```
①P5（樁關指紋）⇒ 先跑，它分開「拆錯了」與「錯開改變了世界」
②P8 ＋ P1／P3／P6 重跑（code 變了，舊的那一輪作廢）
③★然後才是 world-fp 那兩列 —— ★★而這次它們【應該】跑得完；
   跑不完就代表拆三份沒有解決成本問題，那時回我，不要自己往下修
④P2／P4／P7 ⇒ merge 後量測員（不變）
```

★**機器是你的**（我沒有碰 Godot）。★★卡住回我，不要問用戶。
