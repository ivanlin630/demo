---
from: systems
to: reviewer
status: consumed
slice: 讓秤能說「升級」（#35 修秤 (i)）
tier: R2
topic: ★藍圖裁 (i) 先做:候選集合納「既有設施升一級」與新建【同秤】;★★而我查出一個讓它【真的小】的前提:`_facility_score` 對等級【零參照】——它評的是「我想不想在這裡有這座設施」,不是「N→N+1 的邊際價值」⇒ 沿用同一個秤即可,不需要新估值模型(那是 (ii)＝規模經濟的地盤);★★★而要你重點打:同分怎麼辦 + 「秤不看成本」這個既有性質會不會讓 (i) 打折
---

# ★①背景（藍圖已裁，不用審）
**(i)＝讓秤能說「升級」**：候選集合納「既有設施升一級」，**與新建同秤**（不是 fallback、不是走廊）。
**(ii)＝把 `:595/:598` 兩個寫死名字的升級呼叫收進秤** ⇒ **已具名掛【規模經濟 R① 開場】觸發**
（★理由：那本質是**升級估值**——「第二座田值多少」是規模經濟的地盤；**在那個 arc 之前做＝用沒有估值模型的秤去仲裁升級**）。

# ★★②讓 (i) 真的小的那個前提（★我查過）
```
`_facility_score(state, team, tile, leader, facility)` —— ★對 `current_level_key`／等級【零參照】
   組成：`_facility_terrain_fit` × (1 + `_facility_deficit`) × `_facility_personality`（＋farming 的 survival-crush）
⇒ ★★它評的是【我想不想在這裡有這座設施】，而 `_facility_deficit` 本來就在問「我缺多少」
⇒ ★★★所以「再來一級」與「從零蓋」問的是【同一個問題】⇒ 可沿用同一個秤，**不需要新估值模型**
```
★**而 `_pick_facility:5164` 那道 `已有設施 → continue` 就是唯一要動的地方**（＋候選集合的表達）。

# ★★★③要你重點打的兩件
```
(a)★同分怎麼辦：新建 X 與升級 X 用同一個 score ⇒ ★★它們會【同分】
   ⇒ 而成本不同（升級不吃新 slot、新建要 slot ＋ 全額成本）
   ⇒ ★★★argmax 選到哪一個是【未定義】的 —— 請你判這是不是本刀要處理的，還是可以留給下游
(b)★「秤不看成本」是既有性質：`reject_cannot_afford` 在【選完之後】才擋
   ⇒ ★★而 implementer 的實測正是「選到的又幾乎全部付不起」
   ⇒ ★★★所以 (i) 做完之後，秤可能【選了升級但付不起】而效果打折
   ⇒ 請你判：這是 (i) 的驗收該涵蓋的，還是另一條既有病（我傾向後者，但不確定）
```

# ④驗收
```
①★自救活過來：`pick.farming` 從 0/3605 變成非 0（★★而要印【選到之後付不付得起】，否則只是換個地方卡住）
②★★g1a 保持通：mint 仍蓋得成（★不能為了救 farming 又把 mint 擠掉）
③★★★餓隊床：餓死不該出現 —— 而若出現，那仍是【證據】：秤缺急迫項 ⇒ 回報，禁開走廊
④`fp` 會變 ⇒ 差在哪要說得出來；★升級 vs 新建各被選幾次要分開印
```
