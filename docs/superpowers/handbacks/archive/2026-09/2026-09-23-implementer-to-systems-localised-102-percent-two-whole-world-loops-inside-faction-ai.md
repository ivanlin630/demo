---
from: implementer
to: systems
status: consumed
topic: ★★★定位完成：`faction_ai` 佔總增量 102.7%，而它的每次呼叫固定成本是【兩個與勢力無關的全世界掃描】（`:1385` 與 `:1471`）｜★你要的「世界忙碌度」也量了：錯開臂做的事【少 7.8%】而成本多 1.9 倍 ⇒ 排除「世界變忙」｜★★同一個病的第二層
---

# ★一、你要的那個分辨：排除了「世界變忙」

```
Γbatch_sum  錯開=119465  樁關=129576   ⇒ ★錯開臂處理的隊-次總量【少 7.8%】
Γcost       錯開=48618ms 樁關=25307ms  ⇒ ★★成本【多 1.9 倍】
Γcalls      37785 vs 1872              ⇒ 呼叫次數 20.2 倍
世界        teams 72 vs 80｜persons 136 vs 139
```

⇒ **做的事更少而花的時間更多** ⇒ 不是「世界變忙了」，是【每次呼叫的固定開銷】。
★這一格是你要求的，而它**改變了結論**：沒有它我只能說「慢了 1.9 倍」，不能說「慢在哪一類」。

# ★★二、而它幾乎【全部】落在一支

```
system        cost錯開   cost樁關  倍數   batch錯開 batch樁關
faction_ai     42128ms   18190ms  2.3×     585       576     ★佔總增量 102.7%
其餘 13 支                                              ⇒ 合計約 −2.7%（互相抵銷）
```

★**faction_ai 的工作量沒變**（585 vs 576 勢力-次）**而耗時 2.3 倍** ⇒ 固定成本在它身上。

# ★★★三、固定成本是什麼：兩個【與勢力無關】的全世界掃描

`_evaluate_all_body`（408 行）在**勢力迴圈之外**還有兩段：

```
:1385  for tid in state.teams:          ★建 merge_queue（子隊歸建；跳過野獸、處理 parent_team_id != -1）
:1471  for tid in state.teams.keys():   ★滅團處理（population<=0 → 遺財轉公庫 + 移除空殼團）
```

★**它們跟勢力一點關係都沒有** —— 是【世界粒度】的維護工作，只是住在勢力 AI 這支函式裡。
★★原本每小時跑 1 次；錯開之後**每次呼叫都跑** ⇒ 547 次／3 天 而不是 72 次。
★★★**這是同一個病的第二層**：上一層是「勢力粒度的工作被隊粒度驅動」，
這一層是「**世界粒度的工作被勢力粒度驅動**」。

★我先排除了兩個候選，不是猜出來的：
```
SpecimenTracer.heartbeat_sweep  ⇒ 第一行就是 `if not enabled: return` ⇒ 零成本，不是它
LaborSystem.ensure_fresh        ⇒ 在 per-team 迴圈【內】⇒ 總呼叫數不因錯開變多，不是它
```

# 四、修法方向（★我不選，這又是粒度問題 ⇒ 設計層）

```
(甲) 把那兩段搬出 _evaluate_all_body，成為 registry 上【自己的一列】，grp: "hour"
     ⇒ 語意最乾淨：它們本來就是世界粒度的維護 ⇒ 每小時一次
     ★★而它會改變 registry 的順序語意（多兩列）⇒ 要你裁擺在哪個位置
(乙) 留在原地但加【每小時只做一次】的閘
     ⇒ 較小的 diff，但把「兩種粒度住在同一支函式」這件事留著（下一個人會再踩）
```

★**我傾向 (甲)**：它讓「驅動的單位 ＝ 被寫入的單位」這條在這一層也成立，
而那正是 reviewer 那條判準（看寫到哪裡）要的形狀。
★★但 registry 順序是你 owner 的，而且擺錯位置會改變行為 ⇒ 你裁。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
