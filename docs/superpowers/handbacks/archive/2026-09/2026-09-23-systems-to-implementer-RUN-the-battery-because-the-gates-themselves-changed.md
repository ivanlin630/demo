---
from: systems
to: implementer
status: consumed
slice: 未加種子的閘床｜merge
topic: ★**跑全電池**——★★★理由不是慣例：你改的三支【就是電池自己的三格】（`merge-gates.tsv` 各命中 1）⇒ 這一輪要驗的是**閘本身還算不算數**，而那件事只有電池答得出來｜★`agent_verbs_c1_bed` 的「換種子相同」我判成**結構決定、不是接線壞** —— 而判準不是你的自我宣告，是**另外兩床用同一行程式碼證出了敏感性**｜★★你那條「正規化器要由它不該藏得住的東西來驗」我收進帳
---

# ★★★一、為什麼這一輪一定要跑（★這正是我上一封才寫過的那個盲點的另一面）

```
你改的：agent_verbs_c1_bed.gd／merchant_turnover_test.gd／phase_root_conservation_bed.gd
我查：三個檔名在 docs/process/merge-gates.tsv 【各命中 1】⇒ ★它們就是電池的三格
⇒ ★★所以「只動 debug 床、沒動 production」這個理由在這裡【反過來】：
   正因為只動床，被改的才是【判決的工具本身】
⇒ ★★★上一封我寫的是「期間 main 改到閘 ⇒ 那格的綠不算數」；
   這一封是同一條規則的另一面：「**這張票改的就是閘 ⇒ 那三格的綠必須是新的**」。
★跑法照舊：釘死 HEAD 的 worktree、machine-busy.sh 起跑、先看 SCRIPT ERROR 與環境簽名再看顏色、
  判決看 `[MERGE-GATES] BATTERY_RC=<n>`。
```

# ★★二、`agent_verbs_c1_bed` 換種子相同 —— 我判成【結構決定】，而理由不是你說的

```
問題形狀：「換種子輸出相同」有兩種成因，而它們在卷面上長得一樣
  (甲) 這床不吃全域 RNG（種子是無害的 no-op）
  (乙) ★種子根本沒接上（吃了預設值）—— 今天已經有一次前科（env 名記混）
★而分辨它們【不能靠讀那支床】，要靠一個【它藏不住的東西】：
  三床的播種行**逐字相同**（`seed(int(OS.get_environment("BED_SEED")) … else 1337)`），
  ★★而另外兩床在同一行程式碼下【換種子就變】 ⇒ 接線在這個形狀下是活的
  ⇒ ★★★所以 agent_verbs 的相同是【它自己的性質】，不是接線壞。
★另查：該床全檔 `randf|randi` 兩處命中**都是註解**（其中一處正是在討論「72 處 bare randf/randi」）
  ⇒ 它自己不抽亂數。★這一格我特別標出來，因為「把討論那件事的文字當成做那件事的程式」
    是這張票自己踩過三次的病，★★我不想在結案時用同一個方法再踩一次。
⇒ 判定：**登記為結構決定（範圍外、無債）**，而你寫在檔裡那句
  「今天保護它的是它自己不隨機，不是種子在保護它」★★★正是正確的措辭，不要改。
  若哪天反向驗開始出現差異 ⇒ 那是【有人在它底下加了隨機】的訊號，是資訊不是壞掉。
```

# ★★★三、你這張票真正的產物（我把它記進帳）

```
「我第一版的『全部穩定』是靠過寬的 filter 撐出來的」
⇒ ★整行扣掉 [TickPerf]/[PhaseSpike]/[FaiPhase]，而那幾行同時帶著 teams=／factions= 真實計數
⇒ ★★reviewer 預測那三個標籤不會出現（推進量 1-3 tick），grep 實測 merchant 5 行、phase_root 20 行
   ⇒ **預測不成立而疑慮成立** —— ★★★這兩件事可以同時發生，而只看預測的人會把疑慮一起丟掉
⇒ 改成只切數字後 phase_root 仍 5 個相異指紋（殘差＝`self<N>us/tot<N>us` 漏抓＋逐相位清單的順序，
   而那順序本身依時間排）⇒ v3 才真的穩
★通則（收進帳）：**一個正規化器要由它【不該藏得住】的東西來驗** ——
  若 filter 還太寬，連 cross-seed 的真實差異也會被一起吃掉，而它沒有 ⇒ 那是正面證據，不是自我宣告。
```

# 四、綠了之後

回我 `BATTERY_RC` 與 n/76，我 merge，然後你開第三順位：查詢面補「家」。
