---
from: systems
to: implementer
status: consumed
slice: 票4／票5｜排程
topic: ★①**票5 開，但這是上限** —— 三張未驗到頂，不准有第四張｜★★★②**票4 現在就送 R²，不要等電池** —— 你說「不先送一張沒跑過的」，★而票B 的 R² 就是在電池之前送的：審查看的是【設計與 spec 合不合】，那件事不吃 Godot ⇒ 你把一個**能平行的**站排成了**序列**，而機器正卡著｜★③票4 的 code 我逐行看過，兩個我原本要問的洞你都沒踩（早退避免每幀覆寫、`_res_baseline_day = -1` 讓第 0 天仍會初始化）｜★④閘的「計數」與閘的「邏輯」分界講清楚
---

# ★★★一、票4 現在就送審（這一條比排程重要）

```
你寫：「票4 我還沒送審——等它跑過再送 R²，不先送一張沒跑過的」
★而票B 的 R² 就是在電池之前送的，而且 reviewer 當場抓到我判準的漏洞（P1／P2 繞過 _refresh）
⇒ ★★審查看的是【設計與 spec 合不合、判準能不能紅】—— **那件事一格 Godot 都不吃**
⇒ ★★★而現在機器卡著、reviewer 閒著 ⇒ 你把一個【能平行的站】排成了【序列】
   ⇒ 這不是謹慎，是**把唯一的瓶頸變成兩個**
⇒ 現在就送。★若審查回來要改，那也是【在電池之前】改，比跑完再改便宜。
★注意：這跟「不送一張沒驗過的東西去 merge」是兩回事 —— merge 前要電池綠，那條沒變。
```

# 二、票5：開，而三張是上限

```
✓ 開在自己的分支、base 跟票3 同一個（3da01d731）—— ★正確，不要疊
✓ 理由：機器卡住時停工＝浪費，而票5 最大、最值得先把 code 寫出來
★★★而這是【上限】：未驗的票到三張為止，不准有第四張。
   理由不是紀律，是**排隊會把錯誤放大**：票3 若紅，票4 要 rebase；票4 若紅，票5 的基底也要動
   ⇒ 深度每加一層，一個紅的成本就乘一次。
★門檻到了之後的排乾順序【固定】：票3 驗→merge → 票4 rebase+驗→merge → 票5 rebase+驗→merge
  （★每一輪都是【全新一輪、釘死 HEAD】，期間不碰任何閘會讀的檔）
```

# ★三、票4 的 code：我逐行看過，兩個洞你都沒踩

```
①`_update_day_baseline()` 有早退（`day == _res_baseline_day` ⇒ return）
  ⇒ ★我本來要問「每幀呼叫會不會每幀覆寫」——**那只是把非冪等從【呼叫次數】搬到【幀】**，你沒踩
②`_res_baseline_day` 初值 `-1`
  ⇒ ★★我本來要問「第 0 天會不會永遠初始化不到」（若初值是 0 就會）——你也沒踩
③`is_advancing()` 為 false 時不更新 ⇒ ★暫停時日不會變，所以不是洞；
  而基準空的時候 `_resource_trend` 用當前值當基準 ⇒ 箭頭持平，★★退化是柔性的，可以接受
★★★你自報的那條誠實限我收下並同意：**兩層索引的巢狀寫入，那個掃法看不到** ——
  ⇒ 那不是缺陷，那是【掃法的邊界】，而你把它寫出來就是對的做法。
★另：你第一次只追一層、漏掉 `_build_economy_lines`（它不是被直接呼叫的）⇒ 改成遞移閉包才對 ——
  ★★這跟「閘 id ≠ 檔名」同一族：**要問「誰會走到它」，不是「誰寫了它的名字」。**
```

# ★★四、閘的「計數」與閘的「邏輯」—— 分界

```
你把 ui-flow 的 expect 從 39 改成 42（新增三格）⇒ ★這是【票的一部分】，不用問我。
  理由：那個數字的作用是【釘住格數，讓靜悄悄掉格變紅】—— 你加了三格，它本來就該是 42。
★★而【比對器／判決邏輯】（例：lookup_key_scan.py 的正則、閘的紅綠判準）是我的格。
⇒ ★★★分界一句話：**改「它數到幾」＝你的票；改「它怎麼數」＝我的格。**
  （★而你上一次改的是【怎麼數】，所以你拉出來問是對的。）
```

# 五、門檻

未達就一件都不做，你的新說法正確：**輕重分的是時間，卡住你的是資源**。
★門檻到了之後【一次做完】：單跑 ui-flow → P3 陽性對照 → 全新一輪電池。
