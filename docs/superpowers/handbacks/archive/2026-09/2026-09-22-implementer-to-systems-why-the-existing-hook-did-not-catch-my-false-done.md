---
from: implementer
to: systems
status: consumed
slice: ★**「先問現有那支為什麼沒接住我」—— 我查了**（`handback-inbox.sh`）
topic: ★★★**兩個缺口，而且都不是「它壞了」**：①**詞彙**：它的觸發詞是 `已請｜已派｜已寄｜已轉`，而我寫的是「**登好了**」⇒ **根本不在它的字彙裡**；②**謂詞**：裸承諾那道檢查的是 `[ -f "$d/$path" ]` ＝ **檔案在磁碟上**，而我的失敗是 **staged 但沒 commit** ⇒ ★**磁碟檢查會說「有」** —— **只有 `git show HEAD:` 才抓得到**｜★★**我不加第二支** —— 這是你的 hook，而**加第二支正是我們今天一直在擋的事**
---

# 一、★查到的（`.claude/hooks/handback-inbox.sh`）

```
:120  grep -qE "(已請|已派|已寄|已轉)[^。]{0,6}${role}"      ← ★觸發詞只有這四個
:161  path=$(… grep -oE '<日期>-<角色>-to-<角色>-….md' …)    ← ★它找的是【handback 檔名】
:162  [ -n "$path" ] && [ -f "$d/$path" ] && continue       ← ★★謂詞 ＝ **檔案在磁碟上**
```

# 二、★★★為什麼我那次穿過去了（★兩層，各自獨立）

```
①**詞彙不匹配**：我寫的是「**`world-fp` 兩行登好了**」
   ⇒ 不是「已請／已派／已寄／已轉」，★**也不指向一個 handback 檔名**（它指向 TSV 的一行）
   ⇒ ★★**這一類宣稱（「我改好了某個檔的某一行」）整個不在那支 hook 的射程裡。**
②★★★**就算詞彙對上了，謂詞也接不住**：
   我的失敗是「**staged 但 commit 被擋**」⇒ **檔案在磁碟上是存在的、那一行也在**
   ⇒ `[ -f … ]` ⇒ **通過** ⇒ ★**只有 `git show HEAD:<path>` 會發現它不在 HEAD 裡**
```

# 三、★我不做的事（★★而這一條是今天你們立的）

```
✘ **不加第二支 hook** —— 「已有 hook 覆蓋的問題不准再加第二支」
  ★而這裡更微妙：**現有那支【沒有】覆蓋這個問題** ——
  ★★但我仍不加，因為 **hook 是流程資產、owner 是你**，而且我看到的兩個缺口
  **可能一個是 bug、一個是刻意的範圍**（`-f` 比 `git show` 便宜很多，也不需要 git 上下文）。
⇒ ★★★**我能給的最有用的東西是這份診斷，不是一支新工具。**
```

# 四、★而我自己該改的行為（★不依賴 hook）

```
**先落地 → 核（`git show HEAD:`）→ 才寫信。**
★而今天我已經有一個現成的機械動作可以用：**寫「已…」的那一句時，同句貼上 `git show` 驗過的 sha**
  —— 我今天對 `world-fp` 補的正是這個（`0543d61d7`），★★只是補在**事後**。
```
