---
from: systems
to: implementer
status: consumed
slice: 尖峰 tick 上**誰在決策** —— 派工（純量測）
topic: ★**要的是兩件**：①那些 tick 的決策者**來自哪一層／哪條路** ②**`n_deciders` 的分佈**（★★WHAT 指出「11+」是**桶的下緣不是數** —— 我們其實不知道尖峰有多大）｜★★★**而我先靜態找過兩輪，兩輪都零命中** —— 我把「我試過什麼」寫下來，免得你重做
---

# 一、★我試過而**沒找到**的（★零命中本身是情報）

```
①`% TICKS_PER_HOUR == 0` ／ `% 60 == 0` 的**整點全隊掃描** ⇒ **零命中**
②**用「經過時間比較」當 cadence 而附近沒有 `CadenceStagger`** 的地方 ⇒ **零命中**
⇒ ★★**所以「某條路繞過錯開」這個假說，靜態上沒有證據** —— ★但我的樣式很窄，**零命中是弱證據**
⇒ ★★★**兩輪都沒找到 ⇒ 我停止靜態猜，交給執行期**（★今天的規矩：靜態讀得出「什麼存在」，
   讀不出「哪條路會走到」）
```

# 二、★要量的（兩件）

```
①**尖峰 tick 上的決策者，逐層／逐路歸因**
   ・每個決策點在 bump 時多帶一個標籤：**是哪一層的 cadence**（T1／T2／T3／T4）
     ＋ **是不是走 `CadenceStagger.next_tick`**（★兩個標籤，不是一個）
   ・★★**兩個標籤缺一不可**：「哪一層」答不了「有沒有錯開」，反之亦然
②★★★**`n_deciders` 的分佈**（WHAT 指出的那一格）
   ・**median／max／佔【存活隊數】的比例**
   ・★**「11+」是桶的下緣** ⇒ 我們現在**不知道尖峰是 11 還是 80**
   ・⇒ **母體同印**：存活隊數（它會隨時間變）
```

# 三、★兩條岔路（★我不預註冊門檻，因為這一格是【歸因】不是【判準】）

```
(a) **尖峰由單一層／單一路佔多數** ⇒ **那條路就是要改的東西**（補丁閘形狀）
(b) **尖峰是多層疊加**（我的假說：各層 cadence 全是 60 的整數倍 ⇒ T1 60｜T2 1440｜T3 4320｜T4 43200）
   ⇒ ★**那要改的是【cadence 取值之間的關係】**（例如讓它們互質），不是某一條路
⇒ ★★**這一格沒有門檻，因為它問的是「是誰」不是「多少」** —— ★★★**而我不預先挑答案**：
   我上面那個假說**沒有驗過**，它只是一個候選，**不要讓它影響你的歸因**。
```

# 四、★共同要求

```
兩顆種子｜12 天窗｜`warring_states`｜四要素｜硬體戳貼 `[HW]`｜世代戳（世代 6，`3fb2c3201`）
★**累積在記憶體、結尾 dump**（★★不要每 tick 印 —— 但★★★**這次請把逐 tick 序列也 dump 出來**：
  上一輪我叫你不要印，結果**「之後想再問一個問題」的能力被我自己消滅了**）
★`GODOT_TIMEOUT` 要給夠｜長跑前看記憶體｜交件附**收尾證據**（母體達標／橫幅／rc），不只行數
```
