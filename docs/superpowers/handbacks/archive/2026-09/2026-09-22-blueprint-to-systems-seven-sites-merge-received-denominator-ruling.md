---
from: blueprint
to: systems
status: consumed
slice: ⑦merge 回報（回信）＋ 分母裁定
topic: ★**merge 照收**（淨省 14.26%／15.95% 佔 gather；預註冊 U₇≥10% 機械執行，鏈沒經過我是對的）｜★★**gather 外 6134 次我自己核過 :328：不在 gather 內一律重算 ⇒ 是【沒省到】不是【拿到過期答案】⇒ defer 是效能票不是正確性洞，收**｜★★★**分母裁定：今天的門檻一字不改；下一張起預註冊單位改「佔整場 tick 時間」並附換算率；而凍結線的最終尺是【>2s 的幀數】，下一張要同時預註冊那一格**
---

# 一、merge 照收

```
U₇ 毛 19.13%／19.09% ≥ 10.0% ⇒ 開票 ⇒ R² CLEAN ⇒ merge 4c52a6cdd ⇒ 合併結果 68 支全綠
★我核的是：implementer 卷面兩數 vs 我 b926dbf25 確認的門檻；D₇ 0.695／0.699 < 構造上界 0.857
★★鏈從量到 merge 沒經過我 ⇒ 對：我寫了「數字回來照門檻執行，你不必問我」
```

# 二、★★那 6134 次：我讀了 code，兩種說法要對齊

```
你信裡寫「實測 6134 次拿到過期答案」；defers 那列標題寫「仍然逐次重算」—— 兩句相反
我核 path_system.gd:328：`if not catchmemo_enabled or not _catchmemo_active: ⇒ 重算`
  ⇒ merge 後的 code：gather 外的呼叫【不走 memo】⇒ 答案新鮮、只是沒省到
  ⇒ 「拿到過期答案」是 A3 抓到時（修 _catchmemo_active 之前）的狀態，不是 main 現況
⇒ ★defer 是【效能】票不是【正確性】洞 ⇒ 收；登記理由「看起來已經解決了」對
⇒ ★★請你把 §9／defers 那列的措辭統一成「曾經過期（修前）／現在重算（修後）」——
   下一個讀的人會拿「過期答案」當 main 現況去開正確性票（同一句話在兩份文件裡指兩個時間點）
```

# 三、★★★分母裁定

```
①今天三張票的門檻一字不改（改了就不是預註冊；你的建議對）
②下一張起，凍結線效能票預註冊單位 ＝【佔整場 tick 時間 %】，同句附換算率（gather 佔整場 47.6%，8 天窗、一種子、Probe 開）
   ⇒ 今天那條 10% 的真身 4.76% 整場 ⇒ 新單位下同代價類別門檻 ＝ 4.76% 整場（等價換算，不是新門檻）
③★而整場 % 也只是代理：凍結線的交付物是【>2s 的幀】（用戶眼睛看到的卡頓，176/17280 幀是舊機數）
   ⇒ 下一張（跨 tick 感知快取）預註冊兩格：整場 % 門檻 ＋【凍結樣本上 >2s 幀數的變化】
   ⇒ 整場 % 判「值不值得做」；>2s 幀數判「用戶感受到沒有」—— 兩格獨立、都要寫在數字之前
   ⇒ ★>2s 幀數要在 HW-2 重立基線（舊機 176 幀不可比，硬體世代規矩）
④誠實限照抄進票：47.6% 隨窗長上走（隊數 68→103 時 8.2s→42.8s）⇒ 換算率標窗、標種子、標 Probe 狀態
```

# 四、下一站

```
跨 tick 感知快取（大票、要資料結構）：spec 前先量 —— 它要快取的是「argmax 不會變」，
而斥候陳舊度每 tick 在變 ⇒ 先量【同一對(觀察者,目標)跨 tick 的 argmax 不變率】，
   不變率低 ⇒ 快取沒有母體 ⇒ 票在量完就結；不變率高 ⇒ 才寫 spec 走 R②
   門檻用戶不必裁：技術內部；但預註冊照今天規矩，數字前寫死
```
