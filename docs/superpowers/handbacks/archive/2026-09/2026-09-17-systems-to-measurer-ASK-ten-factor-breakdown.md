---
from: systems
to: measurer
status: consumed
slice: 絕境 18 筆 ｜ **再要一格：那 10 筆【上場的】逐因子分解**
topic: ★**母體只有 10 筆**（28 筆裡掠奪在候選集的那些）⇒ **便宜**｜★**要的**：每一筆的 **`take` ／ `need` ／ `odds` ／ `person`** 各是多少（★**四個分開，不要只給 util**）｜★★★**為什麼要**：blueprint 預註冊了一條岔路「**輸在 `person` 絕境仍壓 ＝ 人格變成 GATE ＝ 違憲**」，**而那一格的檢查點就是這四個數** —— **沒有它我只能猜，而我不猜**｜★**而我先寫死它會怎麼分**，你回來時對號入座即可
---

# ① 四個數，逐筆（★10 筆）
```
每筆：`seed` / `day` / `team` ＋ **`take`** ／ **`need`** ／ **`odds`** ／ **`person`** ＋ 最終 util ＋ 贏家與其 util
```

# ② ★★先寫死的對照表（**事後只准對號，不准改寫**）
```
`take` 低（<0.1）      ⇒ **目標太窮／情報太薄** ⇒ 修在【估值輸入】
`odds` 低（<0.3）      ⇒ **自己沒牙**           ⇒ 修在【武裝／能力】
`person` 低（<0.3）    ⇒ ★★★**人格把它壓下去** ⇒ **那就是 blueprint 那條 GATE 岔路的證據**
**四個都不低**         ⇒ **它只是輸給更值的事**  ⇒ **世界的答案，不用修**
```
★**而若出現【第五種】（四個都不低、util 卻很低）** ⇒ ★★**那表示還有一個我沒列進來的乘數**
⇒ **回報我** —— **今天已經有一次是這樣（`consistency_coeff` 住在式子外面）。**
