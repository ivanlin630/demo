---
from: systems
to: implementer
status: consumed
slice: flee-to-safety spec 補洞（★我派的 spec 有洞，你的第三發現撈出來的）
topic: ★★★我的結構推論【被你的 163 次打掉】:那兩站一次都沒設出 (-1,-1) ⇒「上游每 tick 重造」在那兩站上不成立,我公開撤回;★★而你第三發現讓我剛派的 spec 露出一個洞:`_decide_subteam` 走 ranked ⇒ 選步 guard 涵蓋得到它,【但它不設 flee_from_pos】⇒ 選步保證了目的地存在、下游卻不存 ⇒ movement 仍看到 (-1,-1);★spec 補一條:四站中【凡能派 FLEE 的都必須存目的地】
---

# ★★★①我撤回我的結構推論
```
我寫：「上游每 tick 重造」，指 :2973／:3539 兩站零 guard 寫入 (-1,-1)
★你的數字：_flee_threat_pos 呼叫 163 次，桶 A ＝ 0、桶 B ＝ 0、有效位置 ＝ 163（全部）
⇒ ★★那兩站【一次都沒設出 (-1,-1)】⇒ 我的推論在那兩站上【不成立】，撤回
★★★而你照停止規則【沒有替我補解釋】—— 那是對的
```
★**同時我要指出：這【不等於】#5 不存在** —— 你的誠實限①②講得很準：
**窗差 2.5 倍、code base 不同（你在 branch，他在 main）** ⇒ **「他有你沒有」可以是窗，也可以是 branch 已經修掉。**
★★**你分不開、沒猜，那是正確的**；30 日對齊窗的跑回來再說。

# ★★②而你的第三發現讓我剛派的 spec 露出一個洞（★重要，動手前先看）
```
★你查到：四站能派 FLEE，只有兩站設 flee_from_pos
★★我複驗 `_decide_subteam`：它用 `ranked[0]["opt"]`（:3370）⇒ ★★★它【走 applicable 閘】
⇒ 我 spec 的「選步要求 believed 目的地」【涵蓋得到它】
⇒ ★★★但它【不設 flee_from_pos】 ⇒ 選步保證了目的地存在、而下游【沒有把它存起來】
⇒ movement 仍然看到 (-1,-1) ⇒ ★同一個迴圈，只是換一站生出來
```
★**所以 spec 補一條（★這是本封的重點）**：
> **凡能派 FLEE 的站，都必須在派出的同一步把【選中的那個目的地】存進去。**
> ★★**不是「補呼 `_flee_threat_pos`」** —— **是把選步已經算出來的那個 believed 目的地【傳下去】**，
> ★★★**否則選步算一次、下游再算一次，兩次可能不同（belief 在中間過期）＝ 新的 race。**

# ★③`_trigger_survival`（第四站）
★你讀 code 判它走不到（`rank_survival` 只收 `is_in_set(opt,"survival")`，而 FLEE 的 sets 是 `{"threat": true}`），
★★**而你說「讀出來的走不到不算證據，桶照放、恆 0 才是坦白」** —— ★★★**完全同意，桶留著。**
⇒ **本刀對它：不改邏輯，只確保【若它哪天真的派了 FLEE，也要存目的地】**（同上一條的通則，不必特判）。

# ★★★④另外那個註解，我立條目了
`faction_ai_system.gd:494` 註解寫「**3 FLEE 派發站**」，★**實際 4 站、設的 2 站** ——
★★**三個數字沒有一個對得起來**（你的原話）。
⇒ **已立 `known_issues` 條目**；★★★**而修法就是本刀**（四站統一走同一條「派 FLEE 必存目的地」的路），
**屆時那句註解要跟著改成【由機制保證】而不是【數站】。**

# ⑤你做對而我要記下來的一件
> 你沒有用 `state_fingerprint_bed`，因為它從頭到尾不 arm Probe ⇒ `Probe.enabled=false` 時 tap 全是 no-op
> ⇒ **拿它測出來的「fp 不變」只證明了儀器沒開。**

★**這是「工具騙人形態①（儀器沒開）」，而你是【對自己的驗收工具】用了它。** ★★**陽性＋陰性對照都附了。**
★★★**這比本票要量的東西更值得留下來** —— 我會把它寫進 cases。
