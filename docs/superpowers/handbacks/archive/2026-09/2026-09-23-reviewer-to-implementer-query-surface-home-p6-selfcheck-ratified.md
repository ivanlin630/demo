---
from: reviewer
to: implementer
status: consumed
slice: 查詢面補「家」P6+比對器自檢 — 確認
topic: P6核過完全對得上我要的四件(印座標/不是無/不是半套/離家距離照印)。lookup_key_scan.py的selfcheck我也核過:regex lookbehind `(?<=[A-Za-z0-9_\)\]])` 逐一對7個樣本手算過——3個NEG(陣列字面值,`[`前是空白)正確不匹配,4個POS(字典索引,`[`前是識別字/)/])正確匹配,窄化邏輯對。selfcheck()直接呼叫真scan()不手抄第二份正則,設計對。merge-gates.tsv新增value-key-selfcheck獨立列(不改value-key原列)+跳脫方括號,做法正確。這是你自己主動抓到的一個真長期缺口(閘自己的自檢2026-09-09寫好從未接電),做得好。P6/新expect未跑我不代驗,照你的順序走
---

P6 核過，完全對得上我上一封要的四件：印座標／不是「家：無」／不是「【半套】」／離家距離照印。

`lookup_key_scan.py` 的 selfcheck 我也核了：

```
新 regex：(?<=[A-Za-z0-9_\)\]])\[\s*"(%s)"\s*\]
逐一手算 7 個樣本：
  NEG（陣列字面值，[ 前是空白）：
    ["糧食跑道（缺趨勢）"] ／ ["生產隊伍"] ／ ["攻擊指令"]（[ 前分別是空白／空白／逗號空白）
    ⇒ lookbehind 都不成立 ⇒ 正確不匹配
  POS（字典索引，[ 前是識別字/)/]）：
    d["統領"]（前是 d）／x.skills["野心"]（前是 s）／f()["好戰"]（前是 )）／
    arr[i]["忠心"]（前是 ]）
    ⇒ lookbehind 都成立 ⇒ 正確匹配
```

窄化邏輯對，7 個樣本沒有一個算錯。`selfcheck()` 直接呼叫真的 `scan()`，不手抄第二份
正則比對——避免「兩份各自漂、自檢替漂掉的那份背書」，設計對。`merge-gates.tsv` 新增
獨立列 `value-key-selfcheck`（不改 `value-key` 原列）＋跳脫方括號（`\[VALUE-KEY\]`），
做法正確。

這是你自己主動抓到的一個真長期缺口——閘自己 2026-09-09 就寫好的自檢，因為註冊表那一列
沒帶旗標，在電池裡從沒真正跑過。找到並補上，做得好。

P6 跟新 expect（39/39）還沒實際跑過我知道（機器記憶體未達門檻），不代你驗，
照你自己排的順序（先單跑 ui-flow 驗 P6，再全新一輪釘死 HEAD）走即可。
