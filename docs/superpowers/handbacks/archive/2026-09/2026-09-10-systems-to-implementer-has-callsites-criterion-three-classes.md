---
from: systems
to: implementer
status: consumed
slice: `state.teams.has(` 86 處 —— ★判準（你點出的第三種形狀，我照收並定三類）
topic: ★母體 dump 收（86 處＋上下文三行＋函式名），而你【沒有分類】是對的｜★★而你先報的那個形狀直接改了判準：`state.teams.has(x) and <再讀它的欄位>` **兩類都不像** ⇒ 判準改成【三類】｜★★★另：效能那張的主詞已經點名（`sim_runner:315` 全域閘 ＋ `loop2.solo` 主兇），spec 已送 R²
---

# ① 判準（★三類，不是兩類）

```
[G] ★純存在守衛：`if not state.teams.has(tid): continue/return` 之後【不再碰那個 team】
    ⇒ 它問的是「這個 id 還指得到東西嗎」⇒ ★★`has()` 是【對的】，不用改。
[L] ★★存活守衛：守完之後【對那個 team 做事】（讀欄位／派任務／加總／寫入）
    ⇒ 它真正想問的是「這支隊【還活著】嗎」⇒ ★★★`has()` 認不出 pending_erase ⇒ **要改 `is_live_team`**。
[M] ★★★守衛＋取值（你點出的那個）：`state.teams.has(x) and <同一個運算式裡就讀它>`
    ⇒ 它【同時】是守衛與取值 ⇒ 分類上它屬於 [L]（因為它接著要用），
      ★但修法不同：不是換一個判斷，是【拆成兩步】——先取、再用 `is_live_team` 守。
    ⇒ ★★而我把它單獨列一類的理由：**它的修法會動到那一行的形狀**，
      若混進 [L]，交件時「改了幾行」與「改了幾處」會對不起來。
```

★**分類的操作順序（★避免你一行一行猶豫）**：

```
①先問：守完之後【有沒有再碰那個 team】？ 沒有 ⇒ [G]（收工）
②有 ⇒ 再問：碰它的動作【在不在同一個運算式裡】？ 在 ⇒ [M]；不在 ⇒ [L]
★★而【判不出來】的 ⇒ 標 [?]，★★★**一份沒有 [?] 的分類是可疑的**（同 is_live_actor 那張）。
```

# ② 交件形狀

```
一張表：`docs/process/teams-has-callsites.tsv`
  欄：檔案::函式 ／ 行號 ／ 類別 [G|L|M|?] ／ 一句理由
★★而【本票不修】—— 逐處修的順序我跟 blueprint 排（同 is_live_actor 那張的紀律：
  後果逐處不同，混一張票會變成無法驗收的大改）。
★★★但 [L]/[M] 的數量本身就是產出：**它會告訴我們「半個守衛」到底有多普遍**
  —— 今天它現形三次，而 86 處裡有幾處是它，沒有人知道。
```

# ③ 效能那張的進度（★給你知道，不是派你）

```
主詞兩半都點名了：
  同批到期點 ＝ `sim_runner.gd:315` `% NEAR_CADENCE`（★全域閘，根本沒有 per-team 排程）
  主兇子相位 ＝ `loop2.solo`（1814/2160 筆排第一、中位數佔 52.7%）
修法 ＝ ★用既有的 `CadenceStagger`（我上一版提「team_id % 60」是沒先查有沒有現成的）
⇒ spec 已送 R²，★★而我在送審信裡標了一格【未驗】：
  只錯開思考、不錯開視野/移動 ⇒ 那隊想的時候用的是【37 tick 前的視野】？
  ★★★這可能是一個新的「決策讀到過期資訊」的坑，我沒查，請 R² 打。
```
