---
from: systems
to: implementer
status: consumed
slice: 尾部常態掠奪 ｜ **blueprint 收下：本票收在存在性 ⇒ 定稿、跑閘、合併 merge**
topic: ★**他的原話**：「**`f = 8000` 才有盜匪不是永久判定，是【現在不用旋鈕推】**」｜★★**複查格我已預繫成 defer**（`raid-threshold-recheck-after-grudge`，條件＝**恩怨帳的交易估值讀者接完**，met_check ＝ `ask_price` 帶 `buyer_leader_id`）⇒ ★★★**那時用【同一組 fixture】重量一次，`f = 1500` 的答案才是世界的答案**｜★**兩支常態 fixture 的斷言現在可以定稿**，而**身價那一檔要寫成具名條件**｜★最後一步：**一輪完整閘 ⇒ 兩票合併 merge**
---

# ① 兩支常態 fixture 定稿（★**把情境寫進斷言，不要留在腦子裡**）
```
斷言形狀（三句，缺一不可）：
  **常態**（`food_days` 高於 desperation 線）＋ **`f = 1500` 這一檔** ＋ **中庸人格** ⇒ **不搶**
  **常態** ＋ **`f = 8000` 這一檔** ＋ **尾部人格（好戰.9／貪婪.9）** ⇒ ★**搶**
  **常態** ＋ **`f = 8000`** ＋ **單高（.9/.5）** ⇒ **不搶**（★**這一句才擋得住「人人微凶」**）
★★**而旁邊逐字寫**：**「`f = 1500` 的尾部目前【不搶】，差 2.38 倍；
  待恩怨帳的名聲代價接上後複查（defer: `raid-threshold-recheck-after-grudge`）。」**
⇒ ★★★**那句話讓這一格【誠實地暫時】** —— **而它指向一個【存在的】鬧鐘，不是一個願望。**
```

# ② 最後一步
```
\u2460 **一輪完整閘**（掠奪票 ＋ 尾部人格票，同一條 branch）
   判準：`[TREE] HEAD=<sha> clean` ／ **橫幅一份** ／ `runner-self` 指紋 ／ `註冊表 N 支`
   ⇒ **FAIL 只剩 `bed-arm` ＋ `defer-open`（鬧鐘）＝ 可 merge**
\u2061 ★**寄我，我 merge，並在 merge 時收掉 `defer-open` 那一行。**
```

# ③ ★我要把這條線收尾時說的一句留在這裡
**這一票真正做到的事，不是「掠奪變強了」** ——
是 ★★**「貪婪」這個維度在掠奪上【從無到有】**（接線前尾部與單高逐字同分 0.0580）。
★★★**而門檻落在哪一檔，是下一輪（恩怨帳接完）才有資格回答的問題** ——
**我們今天沒有用旋鈕替它回答，那是這一票最該被記住的地方。**
