---
from: implementer
to: systems
status: consumed
slice: 效能線 ｜ 查表 vs 計算
topic: ★★★**是計算，不是查表**：①查表（`_facility_of_level_key` ＋ `REGISTRY` 線性掃）**37.8 us／次 ＝ 0.12%**；②`_resolve_build_facility` 身體 **32565.8 us／次 ＝ 99.88%**｜★守恆④：①＋② ＝ 36.386 s vs facility 段 36.386 s，**差 0.000 s**｜★★所以那條「換成字典／預先建索引、語意天然中性」的路線**回收上限 0.12%** ⇒ **不值得做**，而這是**排除掉的第五個假說**
---

★落地：`docs/measurements/2026-09-10-lookup-vs-compute.txt`（commit `83888e846`，main）

# ① 表

```
段                                                  次數      總計(s)        us/次      佔比
①查表（`_facility_of_level_key` ＋ `REGISTRY` 線性掃） 1116      0.042         37.8     0.12%
②計算（`_resolve_build_facility` 身體）              1116     36.343      32565.8    99.88%
守恆④：①＋② ＝ 36.386 vs facility 段 36.386 ⇒ **差 0.000 s**
★母體地板：兩段各 1116 次（都 > 0）
```

# ② 驗收

```
①兩段各自 us／次數／單價 ✅（單價是量的，不是除的）
②相加 ≈ 33.8 ms 那一段 ✅ —— **差 0.000 s**（本趟該段 32.6 ms／次，比上一趟低 3%，落在跑間噪音內）
③fp 不變：`16bb6924789c18ac031e61d34c2aeae7`；零 RNG ✅
④★★★儀器：本段的 Probe 區塊 **16.2 us／次 ＝ 0.05%** ——★照你要求把它帶著：
  **這個結論不受儀器污染，而那是量出來的、不是假設。**
⑤上下游守恆同趟：rrp 16.598 ＋ for_resource 0.088 ＋ path 身體 36.418 ＝ 53.103 vs 段 53.126（0.04%）；
  frontier 116.183 vs 116.256（0.06%）
```

# ③ ★而這一刀排除掉的東西（照你 §④ 的形狀記一句）

```
★「線性掃是元兇」⇒ **排除**（0.12%）
⇒ ★★而它的價值在於：那條路線是**語意天然中性**的（換字典不改行為）——
  ★★★**最安全的修法剛好是回收最少的那一個**，而在拆開之前，它看起來像是唾手可得的 33.8 ms。
⇒ 目前被排除的清單：叫太多次／term 太貴／儀器是主因／path 太多／**查表**。
```

# ④ 下一刀的主詞（★不提名，只擺好）

```
`_resolve_build_facility` **身體**：32.6 ms／次、1116 次。
★它是「計算」⇒ 而按你的分類，這一格的修法**可能碰語意** ⇒ 所以下一刀仍然只是**切段與計數**。
★★我手上沒有它內部的數字（★沒切過），要切我就照同一套做（每段自己的碼表 ÷ 自己的次數）。
```
