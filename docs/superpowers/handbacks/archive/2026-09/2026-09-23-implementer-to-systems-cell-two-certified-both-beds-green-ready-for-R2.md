---
from: implementer
to: systems
status: consumed
slice: ui-flow 決定性
topic: ★★**格②通過陽性對照了**：注射改成「第二趟換一頁」（不需要推進世界）⇒ **立刻紅**｜★兩支床連跑驗過：`ui-flow` 3/3 綠、擷取床 rc=0｜★★★而重取的「前」**只有檔頭樹 sha 變、20 行正文逐字相同** —— 那本身就是決定性的側證｜分支 `fix/ui-flow-determinism` ＝ `2eb158de8`，可送 R²
---

# ★一、格② 現在有資格算守衛

```
注射：第二趟把 _page_idx 改成 1（★不需要推進世界 —— 你裁的 (甲)）
結果：[UC][FAIL] 同一顆種子跑兩次，畫面【不同】⇒ 這支床不是決定性的   ★立刻紅
⇒ ★★比較器確實會開口
```

★**而我把「舊注射為什麼點不著」也寫進 code**，含你給的具名候選
（`sim_bridge.gd:56-57`「重要事件 → 停止推進」）——★★那是你查的，我沒寫成我的發現。

★★★**教訓也寫進去了**：
**注射若依賴一個【我還沒理解的機制】，它點不著時我分不出是【守衛不靈】還是【注射沒生效】
—— 而那兩件事的處置相反。**

# ★★二、最後驗證

```
ui_flow_test  ×3 ⇒ errors: 0｜到場點名 31／31（三次都是）
擷取床        ⇒ rc=0，自檢兩格都綠
重取「前」    ⇒ ★只有 `#UC tree=` 那一行變（cfba3a0ae → 2eb158de8），★★20 行正文逐字相同
```

# 三、這條分支到底裝了什麼（★給你 R² 用）

```
①_make_ui() 加 seed(1337) ＝ ★真正的修法（根因：Godot 每行程隨機播種）
②CrossRunReset.run() ★拿掉（你證的 no-op；留著會誤導下一個人）
③自檢兩格：①未播種 RNG 每次不同 ②同種子兩次輸出逐字相同（★兩格各自有陽性對照）
④撤回我寫進 code 的那條「2 個樣本的假事實」，撤回寫在原處
⑤推進迴圈：用完 guard ≠ 到達目標 ⇒ 印【不可判】＋目標／實得／frames
⑥檔頭印 seed 變數；UC_SELFTEST=0 的跳過會自己印出來
```

# 四、下一步

```
★票B 那條分支（feat/ui-tabs-b）還沒 rebase 到這個修法上 —— 你決定順序
★★而你提的「12 支沒播種的床」是獨立一票，你開，我不順手做
★★★機器我放開了
```
