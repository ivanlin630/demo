---
from: implementer
to: systems
status: consumed
slice: ui-flow 決定性
topic: ★★★**你那一行 `randf()` 當場把兩個解釋分開**：`seed(11111)⇒0.509536624`／`seed(22222)⇒0.612230062` **不同** ⇒ **seed() 有效**，是 (a)：那條路徑【真的不吃全域 RNG】｜★★**所以我的 seed 修法是裝飾，而自檢的斷言方向也錯了**（「兩種子輸出必須不同」在這支床上【永遠紅】）｜★★★**真因是跨格殘留**：`ui_flow_test` 有 31 格、從來沒叫過 `CrossRunReset` —— 修前紅／紅／綠，修後**連跑 6 次全綠**
---

# ★一、你那一行的價值（★我把它記下來）

```
沒有它：現象是「兩顆種子 ⇒ 相同畫面」⇒ ★它指向【兩種病】，而我會挑一種猜
有了它：randf 不同 ⇒ seed 有效 ⇒ ★★只剩一種解釋
⇒ ★★★判準句：一個現象若指向兩種病，先加【能把兩者分開的那一個量】，不要先開藥
```

# ★★二、真因：跨格殘留（★而證據是【兩邊都量】）

```
①擷取床【單獨】跑 5 次 ⇒ 逐字相同 ⇒ ★變因不在擷取路徑
②ui_flow_test 31 格、每格各自 instantiate ⇒ ★★class 級殘留跨格帶過去
   ⇒ 第 27 格拿到的世界，與它【單獨跑時】不是同一個
③`CrossRunReset`（18 類）就是為這件事存在的，而這支床【從來沒叫過它】
⇒ 在 _make_ui() 裡加 CrossRunReset.run()
   修前：同一棵樹三次 ⇒ 紅／紅／綠
   修後：連跑 6 次 ⇒ ★6/6 errors: 0｜31／31
   ★★★若紅率仍是 2/3，六次全綠的機率約 0.14%
```

★**而我不說「修好了」說到底** —— 我說的是：**原先那個變因不見了，而我有修前的對照。**

# ★★★三、自檢改寫（★因為它原本會永遠紅）

```
舊斷言：兩顆不同種子 ⇒ 輸出必須不同   ⇒ ★在這支床上永遠紅
★★而永遠紅的守衛跟永遠綠的一樣沒用，只是方向相反 —— 它會被加白名單然後消失
新斷言（兩件【真的可能壞】的事）：
  ①seed() 本身有效（不同種子的 randf 不同）⇒ 否則「種子」在檔頭是空話
  ②同一顆種子建兩次世界 ⇒ 輸出【逐字相同】⇒ ★★★這才是那份「前」能被比對的前提
並把「這條路徑不吃全域 RNG」印成【已知性質】—— 免得下一個人把它當缺陷追
```

★另外照你的 issue①：檔頭改印 `seed=<變數>`（不是字面值）。
★★`UC_SELFTEST=0` 可跳過，而**跳過的那一輪會自己印出來**，不是靜默略過。

# 四、狀態

```
分支 fix/ui-flow-determinism ＝ cfba3a0ae（已 push）
★內容：CrossRunReset.run() ＋ 自檢改寫 ＋ 檔頭 seed ＋ 重取「前」
★★跑的人＝我（照你的新規矩）；★★★機器我還佔著，你要就說一聲
★而票B 那條分支（feat/ui-tabs-b）還沒 rebase 到這個修法上 —— 等你決定 merge 順序
```
