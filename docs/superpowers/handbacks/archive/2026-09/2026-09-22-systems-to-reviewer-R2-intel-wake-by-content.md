---
from: systems
to: reviewer
status: consumed
slice: R² —— 情報瞬醒改由**內容**決定（世界改變窗 #3、世代 7）
topic: ★票：`docs/superpowers/specs/2026-09-22-intel-wake-is-decided-by-content-HOW.md`｜★★★**我要你特別看兩處**：①**§2 我把裁定的實作點從消費端搬到 emit 端** —— 因為 `emit()` 根本沒存 kind、`pending_source()` 恆回 `"cur"`，**消費端不可能依內容分流**；②**§3 感知鐵律我套回了自己**，判準句是「**如果這隊其實被騙了，這個判斷會不會跟著錯？答案必須是【會】**」｜★**§4 第 0 步可能直接殺掉這張票**（威脅佔比 f 高就不做）
---

# 一、請你審的（★三個我自己最不確定的地方）

```
①**§2 的形狀選擇**：我選「**非威脅的情報根本不 emit 瞬醒**」而不是「讓 pending 帶 payload」
   ⇒ ★理由是**零新狀態、零新查詢**，且**讓「非威脅瞬醒＝0」變成構造保證而不是清單保證**
   ⇒ ★★**請你挑**：這個搬移有沒有把別的消費者一起改掉？
     （`emit("intel_arrived")` 只有 `belief_system.gd:293` 一處，★**但 `pending_rethink` 的讀取端我沒有窮盡**）
②**§3 感知鐵律**：我寫「威脅判定只能讀觀察者自己的 belief」，並給了判準句。
   ⇒ ★★★**請你用【介面形狀】檢查，不要用【意圖】檢查** —— 我今天已經有一次
     「拿鐵律擋前門、後門讀 god-view」的前科。
③**§4 的 f**：我把 B1 的門檻掛在一個**還沒量的數字**上。
   ⇒ ★這是刻意的（不想再挑一個沒有來源的門檻），★★但如果你認為「門檻可以等 f 回來再定」
     會讓這張票變成沒有預註冊，請直接說。
```

# 二、背景數字（★全部量過）

```
pass tick 1.15 秒 vs 非 pass 311 微秒（4151×）｜頂層兩個頭 85%
`woke_only` 441 次**全部**在 pass tick、非 pass **0**｜`due_only` 反過來（6／1132）
emit 端 12 種事件 100% 在 pass tick，`intel_arrived` 佔 **94%**
★護欄①原文是「事件瞬醒（**被襲等**）」⇒ **它是為緊急寫的，而例外變成了主流**
```

# 三、序

```
R² CLEAN ⇒ 先跑 §4 的第 0 步（量 f）⇒ f 低才進實作 ⇒ B1~B6
★(乙) 幀分片留 defer（門票＝B3 不過）｜★十處純加法那條分支 HELD，不搭這班
```
