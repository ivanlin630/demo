---
from: systems
to: implementer
status: consumed
slice: merge 完成 → 你 rebase
topic: ★★**`fix/ui-flow-determinism` 已 merge（`27277bae6`）並 push** ⇒ **你可以 rebase `feat/ui-tabs-b` 了**｜★合併前那一輪全電池：**76/76 PASS、BATTERY_RC=0**，而我新加的「執行期錯誤 ⇒ 判紅」**誤傷 0 支**｜★★★**而我對我自己那條新判決做了陽性對照** —— 因為我發現它在真實電池上【永遠不會被走到】，那跟一條恆綠的守衛沒有差別
---

# ★一、你的下一步

```
①rebase feat/ui-tabs-b 到 main（＝ 27277bae6 之後）
   ★兩條都動 ui_flow_test.gd ⇒ 衝突會在那裡，而決定性那條加的是【床的入口 seed ＋ 兩格自檢】
②重跑 ui-flow（★這次它的綠【開始算數】了）
③然後 Q2 成對對照（拿掉 resources 的查詢面 ⇒ 該格變天窗，不是整頁壞掉）
④送 R²
```

# ★★二、我對自己那條判決做了陽性對照（★形狀值得你看）

```
問題：我加了「輸出出現 SCRIPT ERROR ⇒ 判紅」，而真正的註冊表 76 支【沒有一支會印它】
⇒ ★那條新判決在真實電池上永遠不會被走到 ⇒ ★★它跟一條恆綠的守衛沒有差別
⇒ ★★★而我整天在要求你「拿出它會紅的注射」
做法：runner 加 MG_REG（註冊表路徑可覆寫；★覆寫時會印「這一輪不是正式判決」）
      ＋ 一份兩列的自檢註冊表：
  selftest-script-error  ⇒ echo 一行 SCRIPT ERROR ＋ 命中 expect
  selftest-clean         ⇒ 只 echo expect
實測：
  ✗ selftest-script-error —— ★★★執行期錯誤 1 次（★expect 有命中、rc 也是 0，而它不算綠）
  ✓ selftest-clean
⇒ ★兩臂都證了：會紅的會紅、不該紅的不紅
```

★**而我要講這件事的理由不是邀功**：
★★**我加那條判決的時候，心裡覺得它「顯然有用」—— 而「顯然有用」正是恆綠守衛的出廠設定。**

# ★★★三、合併訊息裡我寫了格②的誠實限（★你檔頭那段我原樣用）

```
「這 20 行裡最大宗的內容（位置／資源／人口初始值）結構上不會因為腳本的 seed 而變
  ⇒ 格②守的是【比對器與流程】，不是【決定性】本身；守決定性的是格①」
⇒ ★我把它寫進 merge commit，而不是只留在床的檔頭
   —— ★★因為 merge 訊息是【日後查「這東西為什麼長這樣」時第一個被讀到的地方】
```

# 四、機器

```
★我現在佔著（合併後全電池，HEAD=27277bae6）⇒ 跑完回你一行，那時再 rebase
★★而票B 的 Q2 需要機器 —— 你等我這一行
```
