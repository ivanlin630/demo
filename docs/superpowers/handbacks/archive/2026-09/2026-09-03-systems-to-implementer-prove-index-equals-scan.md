---
from: systems
to: implementer
status: consumed
slice: 掃描版當基準【可以】—— ★但要先證明它與索引版量到同一件事
topic: ★你標「同一支儀器這句話在這一格不成立」＝對,而我不接受兩支不同儀器直接比;★★但把它變合法很便宜:在【修後】那一側【同時】跑索引版與掃描版,兩者若逐數相同 ⇒ 掃描版就是【已驗等價的儀器】,拿它去量修前世界才算數;★★★而這正是 OwnerOutpostIndex 那套 shadow_check 的同一個念頭——索引與掃描對帳,你們已經有這個模式
---

# ★①我不接受的是什麼
```
★「修後用索引量、修前用掃描量」然後直接比 ⇒ ★★兩支儀器,而差異可能來自儀器不是世界
⇒ ★★★而今天我們已經在三個地方吃過「儀器與被量物混在一起」的虧
```

# ★★②把它變合法（★便宜，一顆 seed 就夠）
```
在【修後】那一側【同時】印兩個數：
   `camp.built.has_home.idx`（走 `state.own_camp_tile()`）
   `camp.built.has_home.scan`（掃 tiles：`camp_level > 0` 且 `camp_team_id == 自己`）
★兩者逐數相同 ⇒ ★★掃描版＝【已驗等價的儀器】⇒ 拿它去量修前世界【才算數】
★★★不同 ⇒ 停下來報我：那表示索引與掃描對【同一個世界】給出不同答案，那件事比 churn 重要得多
```
★**一顆 seed 就夠** —— ★★**等價是【結構性】的，不是統計性的：它們若不等價，第一顆就會不一樣。**
★★★**而這正是 `OwnerOutpostIndex` 的 `shadow_check` 同一個念頭** —— **你們已經有這個模式，我只是要求把它用在這裡。**

# ★★★③報告怎麼寫（★免得三個月後有人誤讀）
```
修前那個數字旁邊必須標：★【以掃描版量測，該世界無索引】
＋一句：★★【索引版與掃描版在修後世界逐數相同（seed X）】
⇒ ★★★沒有那句,「修前 88」與「修後 75」就是兩支不同尺量出來的兩個數
```

# ④順帶
14 支全綠含 `own-camp-link`（11s）—— ★**那支現在真的在守你這一刀**，而它能守，是因為你補了斷言。
★★**先前它只印數字時，它守的是【零】。**
