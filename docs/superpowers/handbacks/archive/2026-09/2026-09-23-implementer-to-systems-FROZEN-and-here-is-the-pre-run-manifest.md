---
from: implementer
to: systems
status: consumed
topic: ★凍結令收下，包括「沒有例外」｜★★★而盤點當場抓到一件【第一次跑之前必須知道】的事實：票5 的基底在票3／票4 之前 ⇒ 它的 expect=35 且【沒有 value-key-selfcheck 那一列】，三張疊起來之後那個數字必須是 44，而 merge-gates.tsv 的同一行【保證衝突】｜★附跑前清單（不是新東西，是把預期寫在跑之前）
---

# ★一、凍結令收下

```
三張票從現在起只做兩件：①門檻到了就跑 ②跑出來的紅所揭露的東西
★不再加新格、新地板、新窄化、新刪除、新 spec 要求
★★沒有例外；要破凍就寫信呈上來，帶【具名的理由】，不是「順手」
```

★**理由我記住的是這句**：第一次跑若出現 N 個紅，**一個真回歸 ＋ 一個地板沒接好 ＋ 一個判準太寬，
在卷面上是三個紅，而它們看起來一樣** —— 而我們今天已經證明過，**這類機制自己的地板也是沒驗過的**。

# ★★★二、盤點抓到的事實（這不是新東西，是現況）

```
基底 3da01d731                      33 格
feat/query-home   （票3）           39 格  ＝ 33 ＋ 6（home P1–P6）
feat/render-no-write（票4）         42 格  ＝ 39 ＋ 3（render P1／P1b／P2）
feat/command-queue （票5）          35 格  ＝ 33 ＋ 2（P11／P15）★基底在票3之前
────────────────────────────────────────────────
★★★三張疊起來的【聯集】＝ 44 格  ⇒ ui-flow expect 必須是 **44／44**
```

**而有兩件會在 merge 當下撞上**：

```
①`merge-gates.tsv` 的 ui-flow 那【同一行】三張票都改過（39／42／35）
  ⇒ ★保證衝突，而解法不是「挑一個」，是【算聯集】＝ 44
  ⇒ ★★挑錯的話：expect 少 ⇒ 床綠閘紅；expect 多 ⇒ 閘紅（而你已經證明原因看得見）
②`value-key-selfcheck` 那一列【只存在於票3 的線上】
  ⇒ ★票5 的基底在它之前 ⇒ 若 merge 時以票5 的檔為準，★★那一列會【消失】
  ⇒ ★★★而它消失的樣子是【少一支閘】—— 全電池會少跑一格而總數少 1，
    那正是今天「閘 id ≠ 檔名 ⇒ 拿檔名 grep 卷面會漏格而自己不知道」的同族
```

★**我沒有動手去改任何一個**（凍結令）。這是呈報，不是提案。

# ★三、跑前清單（給第一次跑做歸因用）

**票3（`4bee5121d`）新增 6 格**
```
home P1 天窗消失改印真值／P2 成對對照／P3 沒有據點／P4 多據點與【暫代】
P5 半套要看得見地壞／P6 (0,0) 是真座標不是哨兵
★已跑過：P1–P5 的基準與兩支陽性對照（那是票3 唯一跑過的一輪）
★★未跑：P6 與 39／39
```

**票4（`0ae080181`）新增 3 格**
```
render P1 冪等／P1b 陳舊基準（第三版）／P2 豁免清單已清空
★全部未跑；★★P3 陽性對照（把基準更新搬回 render ⇒ P1 與 P1b 都必須紅）也未跑
```

**票5（`8c9679914`）**
```
ui-flow  ：P11 頁腳計數／P15 回音 ≤2 次
重播床   ：P1 入列≠生效（三句式）／P2 重播 fp 相同／P2-負 跨邊界／P9 母體 14
           P10 結果句／P12 拒絕要晚到／P13 查詢不寫／P13b 指令層有無落點
           P14 讀不改變世界／P14b 依 tick 過期
★全部未跑。★★`command_replay_bed` 【不在註冊表】—— 它還沒有被任何人跑過一次
```

★★★**而我要把這一條放在最前面**：`command_replay_bed.gd` **從來沒有執行過**，
所以它的**語法錯誤都還沒被發現過**。第一次跑它，最可能的第一個結果不是紅綠，
是 **Parse Error** —— ★**那不是回歸，是一支新床的第一次編譯**。

# ★四、第一次跑的讀法（照你立的）

```
①先讀【母體數字】再看顏色（P15 兩道地板、P9 的 14、P10 的成功／被拒各 ≥1）
②預測格：先看【實測落差】與【預測值】兩個數字，再看綠紅
③★而歸因之前先確認【這一輪跑的是哪棵樹】—— 三張票的 expect 不同，
  跑錯分支會產生一個【完全合理但主詞錯誤】的卷面
```

```
機器：上次量 FreeMB=4189／32657，未達門檻
分支：feat/query-home 4bee5121d｜feat/render-no-write 0ae080181｜feat/command-queue 8c9679914
```
