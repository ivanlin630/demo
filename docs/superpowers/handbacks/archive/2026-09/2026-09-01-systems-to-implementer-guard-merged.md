---
from: systems
to: implementer
status: consumed
slice: guard-active-path
tier: infra
topic: ★已 merge(550861a3),閘綠(憲法 74／裸 tick 母體 169／import 0 error);★★★而你那個【零隊世界】對照我抄進 cases——它不是「跑一跑看有沒有」,是【把被動路徑整條移除】然後看守衛還在不在;★★你自揭的 LOD_FAR parse error 我也記了:對照組壞掉的長相 ＝ 陰性結果的長相
---

# ★①已 merge
`550861a3` → main；`import 0 error`／`CONSTITUTION PASS(74, removed=1)`／`BARE-TICK PASS(母體 169)`。

# ★★★②零隊世界 —— **這個對照設計我抄進 cases**
> 沒有隊 ⇒ 不可能有人呼叫估算器 ⇒ **而計數器仍 fire**。

★**它不是「跑一跑看有沒有」** —— ★★**是【把被動路徑整條移除】，然後看守衛還在不在。**
★★★**那才是「它到底掛在哪條路上」的直接證明**，而不是間接推論。

# ★★③你自揭的兩個對照設計錯誤，第一個我立成判準
```
★你寫了 LOD_FAR —— 而那個常數【根本不存在】⇒ parse error ⇒ 床沒跑
★★而輸出【只是少一行】⇒ 看起來像「守衛沒 fire」＝正是你要驗的那個【陰性】
⇒ ★★★對照組壞掉與對照組回答「否」，在輸出上長得一模一樣
```
★**判準（已入 `03b_measurer-cases.md`）**：
> **陽性對照必須先證明它【自己跑起來了】**（例如印一行「對照組已執行」）——
> ★★否則「沒看到預期輸出」有兩個解，而你會選錯的那個。

★**同族血證就在 `bare-tick-gate.sh` 的檔頭**：「掃描器自己掛了 —— ★這不是『沒有候選』」。
★★**那條是我們自己寫的，而今天又踩了一次同型** —— ★★★**這就是為什麼它要進 cases 而不是靠記得。**

# ★④第二個（首次 dispatch 在第一個 cadence 邊界不是 tick 1）
★**那是正確行為，不是 bug** —— ★★而你把它寫進 commit 是對的：**下一個人量到「tick 1 沒 fire」時不會誤判。**
