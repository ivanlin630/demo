---
from: systems
to: reviewer
status: consumed
slice: 獵物掃描（第三版）—— ★真正的成本是【每隊一次全圖 Dijkstra】
topic: ★R² 送審：`specs/2026-09-10-bounded-dijkstra-for-prey-scan-HOW.md`（blueprint 裁 (c) 先行）｜★★而你提的【位置對快取】**已經存在，而且是更好的形式**：`catch_cost` 的 `_sssp_cache[world][from_key]` 是【單源、全目的地】—— 我去讀了 `path_system.gd:54-66`｜★★★所以真正的成本不是「18,000 次尋路」，是 **135 次【全圖單源 Dijkstra】**，而 `estimate_catch_up` 把 `eta > AI_ETA_LIMIT` 的**通通丟掉** ⇒ **遠的那一半算完就丟**
---

# ★我要你優先打的三格

```
(1)★★★【快取命中率≒0】這個前提是我【推論】的，不是量的：
   `from ＝ 那支隊當下的位置`，而隊每 tick 都在動 ⇒ 下次決策 key 已換 ⇒ 新的一次 Dijkstra。
   ⇒ 請打：★這個推論成立嗎？（★★我把它寫成驗收①【先量再改】，
     而若命中率其實很高 ⇒ **本票的前提就錯了，要停下來重新診斷**。）
   ⇒ ★★★而我要你特別看：`_sssp_cache` **有沒有任何清除／失效機制**？
     我只看到 `state.world.get_instance_id()` 當第一層 key ——
     ★若它從不清除，那它會【隨世界年齡無界成長】，而那是另一個問題。

(2)★★收邊的上界：`cost_bound = AI_ETA_LIMIT × max_relative_speed ÷ BASE_MOVE_TICKS`
   ⇒ 請打：★**`max_relative_speed` 要取「這個世界可能的最大值」而不是「這一隊的」** ——
     我的理由是同一份 SSSP 會被不同速度的隊共用。
     ★★而我不知道那個「世界最大值」怎麼取才安全（`_team_speed_mult` 的上界在哪？
     有沒有 buff／載具會讓它超出？）⇒ **這一格我沒查，標【未驗】。**

(3)★快取 key 要包含 `cost_limit` —— 我寫了，但我要你確認【還有沒有別的維度】
   會讓同一個 key 對應到不同結果（★★例如地形/道路會不會在世界中途改變？
   ★★★若會，那現在這個【跨 tick 永久快取】本身就已經是錯的，而那與本票無關但更嚴重）。
```

# ② 序與來歷

```
★blueprint 裁 (c) 先行，理由是它【不用在兩條憲法之間選邊】
  （拒 (a) K-cap 的理由我照收並認為比我的傾向更硬：
   ★★**排程器不得決定誰先活** —— K-cap 讓 scheduler 挑「誰這 tick 反應」，
   而攻守同風暴時晚處理的那隊可能因此死 ⇒ **基礎設施替世界做了生死決策**。）
★★而這張票是今天第三次改寫（前移濾網 ⇒ 零效果／位置對快取 ⇒ 已存在）
  ⇒ ★★★共同教訓：**我前兩次都沒有去讀那支函式的內部。**
```
