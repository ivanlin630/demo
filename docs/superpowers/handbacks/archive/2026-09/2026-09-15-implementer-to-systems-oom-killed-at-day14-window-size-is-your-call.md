---
from: implementer
to: systems
status: consumed
slice: 偵查驗收 ｜ **這次是 OOM 不是 timeout**（day 14/30）⇒ **窗長是你的裁**
topic: ★**第三輪死法不一樣**：不是 `GODOT TIMEOUT`，是 **harness 的低記憶體保護**把它殺了（day 14/30）⇒ **本輪無結果**｜★★**而規矩是「OOM 之後不得重試同一個窗長」** —— 我上一封也說過「9000 不夠就不自己再開一輪」⇒ **這一刀我不下**｜★★★**而你那條二次曲線的證偽點【這一輪答不了】**：它死在 day 14，**沒有跑到 day 30 就沒有總時** —— ★而 day 14 的時點也不可用（它是被 OOM 打斷的，不是跑完 day 14）
---

# ① 事實

```
狀態：killed —— "the system is running low on memory"（★harness 的保護，不是 wrapper 的 2700/9000 timeout）
跑到：day 14 / 30
輸出：444 KB（★又一份「看起來很完整、只是短」的日誌）
事後查：無殘留 godot 行程；此刻 FreeMB ≈ 14,641 / TotalMB ≈ 32,727
```

★**三輪三種死法**：360s timeout（day 8）／2700s timeout（day 21）／**OOM**（day 14）。
★★**全部都是【本輪無結果】** —— 沒有一輪是紅，也沒有一輪是綠。

# ② ★★這一刀我不下，理由有兩條而不是一條

1. **規矩**：OOM 之後**不得重試同一個窗長**（★否則就是拿同一個已知會爆的組合再賭一次）。
2. **我自己的話**：上一封寫「若 9000 s 還不夠，我不會再自己往上加一輪 ——
   那時要問的是『30 天窗這個尺寸對這支床是不是對的』，而那是你的裁不是我的。」
   ★**死法不同（OOM 不是 timeout），但那句話問的是同一件事。**

# ③ 給你選（★我有偏好，但不自己執行）

| | 做法 | 拿得到什麼 | 風險 |
|---|---|---|---|
| **甲** | 30 天窗**原樣再試一次** | 完整一份 | ★**違反「OOM 不得重試同窗長」**；而且前三輪已燒掉約 70 分鐘 |
| **乙**（我建議） | **窗縮到 21 天**（`SC_TICKS=30240`，早窗維持 7 天） | ★五格**全部答得出**：③的母體照樣大、④的早窗**完全不受影響**（它只用前 7 天） | 全窗的絕對數字變小（★**而我們本來就只報數不報率**） |
| **丙** | 30 天窗，但**關掉世界日誌**（大量 `[Move]`／`[SoloAI]` print） | 完整一份 + 省 I/O | ★**改的是被量的東西的輸出行為** ⇒ 我不喜歡在驗收輪動它 |

★**我選乙的真正理由不是省時間**：
**day 21 是上一輪【實測跑得到】的深度** —— ★★而 21 天窗是**已知可完成**的尺寸，
不是另一個猜測。★★★**三輪都死在「我猜一個上限」上，第四輪應該用【量到的】那個數。**

★**而乙有一個代價要先講明**：`SC_TICKS` 換了 ⇒ **窗變了** ⇒
**這份數與任何 30 天窗的數不可並排**。床會照舊印「窗 ＝ N tick ＝ N 天、從第 0 天起算」。

# ④ 你那條二次曲線：**這一輪答不了它**

- 它死在 day 14 ⇒ **沒有 day 30 的總時** ⇒ 證偽點沒被測到。
- ★**而 day 14 這個時點也不可用**：它不是「跑完 day 14 用了多久」，
  是「被 OOM 在 day 14 打斷」—— ★★**兩者不是同一個量**。
- ⇒ **模型仍然掛著【兩點擬合，待驗】**，我不拿這一輪去支持或反對它。

# ⑤ 現在的狀態（★沒有任何東西在跑）

- 樹 **`7e8ac177e`**，clean，**沒動**。
- (4a)/(4b) 補丁仍在 scratchpad，**沒有套進樹**（照你的裁掛多 seed 那輪）。
- ⏳ **等你一個字：甲／乙／丙**。★**在你回之前我不開新的一輪。**
- ⏳ merge-gate 55 支排在這之後。
- ❓ 仍等：驗收床**進不進註冊表**。
