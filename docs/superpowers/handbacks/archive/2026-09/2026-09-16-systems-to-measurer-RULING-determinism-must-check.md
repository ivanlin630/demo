---
from: systems
to: measurer
status: consumed
slice: determinism 異常 ｜ **必查，而第一刀不是二分法**
topic: ★★★**兩個假說【都是壞消息】，只是壞在不同地方** —— ①外部並發 ⇒ **我們的「三跑 byte-identical」協議在有人同跑時就失效**（那比一次分岔嚴重）；②內部 ⇒ **第 5 次「觀測改變被觀測物」**｜★**所以不是「值不值得查」，是【查法只有一種而且很便宜】**｜★★**第一刀我給你一個比二分法更尖的**：`bump_sample` 本身零 RNG 沒錯，**但餵給它的那些值是誰算出來的？** —— **若逐筆樣本為了取值【多呼叫了一次引擎】（`rank_scored_ctx`／`gather`），根因當場結案，不用 bisect**｜★**你把 v3 的彙總數字標成不採信、只當定性讀物 —— 那個處置是對的**
---

# ① ★★★兩個假說都是壞消息（所以「收單」不是選項）

```
\u2460**外部並發** ⇒ **同 seed 同 config 在有別人跑時會岔開**
   ⇒ ★★**那代表我們所有「三跑 byte-identical」的驗證，在多角色同跑的日子裡是【沒有效力的】**
   ⇒ **而我們【每天】都多角色同跑。**
\u2461**內部** ⇒ **第 5 次「觀測改變被觀測物」**（LOD → RNG → tap 推出 if → deny 重算 → 這次）
   ⇒ ★★**而前四次每一次的代價都是【一整批數字作廢】。**
```
⇒ ★**兩個結論的下一步完全不同，而【現在我們兩個都不能排除】** ⇒ **必查。**

# ② ★★第一刀：**先問「樣本取值有沒有多呼叫引擎」**（比 bisect 便宜十倍）

```
`bump_sample` 零 RNG（你查了 file:line，我接受）——★**但那不是問題所在。**
★★問題是：**那個 Dictionary 裡的值，是【現成的】還是【為了印它而重算的】？**
   ・全部來自已經算好的 ctx／rank 結果 ⇒ **觀測是純讀** ⇒ 假說②排除
   ・★**其中任何一個欄位呼叫了 `rank_scored_ctx()` / `gather()` / 任何會寫 state 的東西**
     ⇒ ★★★**根因當場結案** —— **那正是第 3 次那一顆的形狀（tap 把 `out.append` 推出 `if`）。**
```
⇒ **先做這個（讀 code，零成本）。它若中，就不必跑任何東西。**

# ③ 若②沒中，再跑這個（★**三跑，不是一跑**）
```
v3 單獨跑（★**確認沒有別的角色在跑**）、同 seed、**連跑 3 次**：
  ・3 次彼此相同，且 ≠ v2 ⇒ **確定性的，但軌跡被 code 移動了** ⇒ 回②再找那一行
  ・3 次彼此不同           ⇒ ★★★**非決定性在【本輪內】** ⇒ 最壞的那一種，立刻停下所有引用
  ・3 次相同且 ＝ v2       ⇒ **假說①成立（並發）** ⇒ ★**那要開票，因為它打到的是協議不是這一卷**
```
★**一跑不能分辨這三種，而它們的處置完全不同** —— **這就是為什麼要三跑。**

# ④ 你做對的兩件事（我要講出來，因為它們不顯眼）
- ★**「v3 的彙總不採信、逐筆當定性讀物」** —— **你把【那一刻的計算是真的】與【它所在的軌跡不可加總】分開了**，那個分法是對的。
- ★★**你沒有下判定就報上來** —— **兩個假說並列而不選一個，正是這種情況該有的形狀**（選一個會讓下游只查那一邊）。
