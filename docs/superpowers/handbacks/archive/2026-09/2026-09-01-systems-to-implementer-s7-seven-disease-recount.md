---
from: systems
to: implementer
status: consumed
slice: S7-seven-disease-recount
tier: probe
topic: ★★★S7 七病【現狀盤點】,只盤不修——而重點是:那份清單寫於 2026-08-20【S2 換根之前】,我抽驗兩條就已經跟抄本不符(病1 已修一半、病3 行號 525→896);★★真正的病:裸魔法數在換根之後【語意靜默改變】——寫死的 tick 數現在代表 1/6 的時間,而沒有任何東西報錯;★不要修,列出來就好
---

# ★★★①這張票不是「修七病」，是「**七病現在還剩什麼**」

★**清單原文**：`docs/superpowers/specs/2026-08-20-time-frequency-inventory.md` §盤點抓到的病（7 條）。
★★**而它寫於 2026-08-20 —— S2 換根【之前】**（`TICKS_PER_DAY 240` 時代；現在根是 `TICKS_PER_HOUR=60` ⇒ `TICKS_PER_DAY=1440`）。

★★★**我抽驗兩條，兩條都跟抄本不符**：
```
病1 turn 定義打架 ⇒ ★sim_bridge 那半【已在 S2 收掉】
     :4 有 S2 註記「這顆不跟隨根旋鈕 ⇒ 重錨會讓它靜默漂移 6 倍」;:10 已改為由 TimeScale 導出
病3 移動信念 ⇒ ★還在,但【行號從 :525 變成 :896】,值仍是 2.0
     goal_resolver.gd:896  const MOVE_TILES_PER_DAY: float = 2.0   # TEST VALUE
病7 debug 鏡像 ⇒ ★抄本說「×2 處」,我 grep 只剩【1 處】(ui_logic_test.gd:77 TICKS_PER_DAY := 24.0)
     ★★而它的 drift 從 10× 變成 60×(真值 240 → 1440)——★★★病沒變,病的【大小】變了
```
⇒ ★**所以這張票的產出是一張【對照表】，不是一個修法。**

# ★★②每條要三欄（★不要只給「還在/不在」）
```
| 病 | ①現況 = 還在／已修／變形 | ②file:line（★現值，不是抄本的） | ③★★換根後的【現值語意】 |
```
★★**第③欄是這張票真正的內容**，舉例：
> `player_trade 1000t` 抄本寫「＝4.17 天，不對齊任何單位」——
> ★**而換根之後它是 0.69 天。** ★★**數字一個字沒改，意思變了 6 倍，而沒有任何東西報錯。**

⇒ ★★★**那就是「裸魔法數」真正的病**：**不是它醜，是它【在換根時靜默改變語意】。**
★**所以第③欄決定 S7 的優先序**——**語意變了的排前面，只是醜的排後面。**

# ★③紀律
```
★窮盡搜索(★★不 head 不 glob —— 抄本的「×2 處」已經被我打掉一次,而那正是 head 型錯誤的形狀)
★只盤不修:一行 production 都不要改
★抄本與現況衝突時【以現況為準】,並把衝突【明列】——★★抄本錯過的地方就是下次會再錯的地方
```

# ★★④一條我先裁掉的修法（免得你盤完往錯的方向走）
**病3 `MOVE_TILES_PER_DAY = 2.0` 的修法【不是改成 5.0】。**
★**它是【手抄物理常數】——血統②，而那是用戶立法禁止的**（`feedback_no_handcopied_physics`）。
★★**修法形狀＝改接線，不是改數值**：估值必須（a）與物理同源推導 或（b）讀自身狀態。
★★★**把 2 改成 5，三個月後它會再爛一次** —— 而那次沒有人會記得它曾經是 2。
⇒ **盤點時把它標成「接線病」而不是「數值病」。**
