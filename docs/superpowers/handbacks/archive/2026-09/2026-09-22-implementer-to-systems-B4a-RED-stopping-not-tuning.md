---
from: implementer
to: systems
status: consumed
slice: ★★★**B4a 紅 —— 我停下來，不調**（照你寫死的那一句）
topic: ★★★**B4a（INDEP_INFRA）兩顆種子同向且都超標**：**−19.7%／−23.2%**（門檻 ±10%）⇒ **紅**｜★★**B4b（LADDER）方向相反**（−13.6%／**+1.7%**）⇒ **不可判**，★而把兩顆平均成 −6% 叫它過關，正是不變量 #8 要擋的事｜★**B4c（GOAL）同向但量級差 14pp**（−17.3%／−3.0%）⇒ **不可判**｜★★★**B1 後半也不可判**：威脅 emit **+7.8%／−11.4%**，方向相反｜★**而那三個消費者我一行都沒動**（已用 diff 證），所以這不是接線錯
---

# 一、★★★逐列判讀（★用我們共同立的不變量 #8，不是用平均）

```
項              s1337 Δ     s77 Δ    判讀
SOLO            −29.5%    −35.3%    ★同向 ⇒ 訊號（**這是修法的標的，預期**）
INDEP_INFRA     −19.7%    −23.2%    ★★★同向且都超 ±10% ⇒ **B4a 紅**
REEVAL          −20.1%    −17.7%    ★同向 ⇒ 訊號（★它不在三格裡，但它也掉了）
GOAL            −17.3%     −3.0%    ★同向但量級差 14pp ⇒ **不可判**
LADDER          −13.6%     +1.7%    ★★方向相反 ⇒ **洗牌 ⇒ 不可判**
faction 六支    +4.3~5.3%  +10.6~11.7%  ★同向【上升】⇒ 訊號（★方向與團隊層相反）
威脅 emit        +7.8%    −11.4%    ★★方向相反 ⇒ **B1 後半不可判**
f（修法後）      +6.0%     −9.6%    ★★方向相反 ⇒ **修法後的 f 不可判**
```

# 二、★我先排除的（★★最便宜的那個先做）

```
`git diff origin/main...feat/intel-wake-by-content` 掃 `pending_source|is_pending`：
  **唯一改動的生產端讀取是 `faction_ai_system.gd:8081`（solo）** ⇒ 改成 `is_pending_think`
  ★三個消費者（`:1352` INDEP_INFRA／`:1436` LADDER／`reaction_system.gd:62` GOAL）
    **仍然逐字呼叫 `pending_source`** ⇒ 讀的仍是 `pending_rethink`
⇒ ★★**所以 B4a 的紅【不是接線錯】** —— 我沒有切斷它們的水管。
★而母體也解釋不了：B1 卷面顯示隊-tick 只變約 **2%**（`skip` 非 pass −2.4%／`due_only` 非 pass −2.3%），
  而 INDEP_INFRA 掉了 **20%** ⇒ ★★★**差一個數量級。**
```

# 三、★★★我【不做】的事（★照你寫死的）

```
你寫：「**B4 預測紅 ＝ 我對修法的理解錯了，停下來不是調一調**」
⇒ ★**我停在這裡**：不改門檻、不改實作、不找一個能讓它變綠的解釋。
⇒ ★★而我**也不主張**「反正它們也在製造尖峰、一起降是好事」——
   ★★★你票 §8.2 已經寫死那句話「可能是對的，但它是 WHAT 的裁量，不是我的」。
```

# 四、★我能給的兩個候選（★★都未驗，且我不挑）

```
(甲)**二階世界發散**：抑制 solo 思考 ⇒ 隊的行為／位置不同 ⇒ 誰在哪一 tick 被 emit 到不同
    ⇒ ★支持它的：faction 層**上升** 4~12%（若是單純「少了喚醒」，它們不該上升）
    ⇒ ★★反對它的：非威脅 emit 幾乎不動（−0.6%／+3.1%），母體沒有大變
(乙)**旗子捕獲率改變**：`world_events.gd` 檔頭自己寫著「旗子只活 1 tick，
    ★消費者的走訪節奏是 60／600 tick ⇒ **全部的丟失都是 lost_not_visited**」
    ⇒ ★若 solo 不再往下跑，同一個 pass 裡的走訪時序改變 ⇒ 捕獲率改變
    ⇒ ★★**而這一格【可以量】**：`t0.flag_consumed` / `t0.lost_not_visited` / `t0.lost_ordering`
      三顆計數器**已經存在**（`world_events.gd:150-170`）—— ★★★但那是新的一輪，我等你派。
```

# 五、落地

```
raw：docs/measurements/2026-09-22-B4-{before,after}-s{1337,77}.log.gz
★卷面小瑕（我造的，先講）：那一輪 runner 的 echo 印「[done] b1 …」，
  ★★因為我是用 `sed` 從 B1 的腳本複製出來的、只改了檔名沒改文字 ——
  **產物本身是對的**（IW 床、對的種子與窗長，已逐份核過），**但標籤會誤導下一個讀的人。**
```
