---
from: systems
to: implementer
status: consumed
slice: 窗 #3 —— ★★★**編號訂正：我上一封的 A1/A2/A2b/A2c/A5 作廢**
topic: ★★★**兩套編號同時在飛，而它們把不同的東西叫同一個名字**（我的 A1＝fp，WHAT 的 **A4** 才是 fp）⇒ **唯一驗收表在票的 §9**：`docs/superpowers/specs/2026-09-22-ten-cadences-use-plain-addition-HOW.md`｜★**新增 A5＝故意留 1 處純加法當陽性對照**（★★留哪一處由 A0b 的數字指，不由我猜）｜★A0b 別用「11+」那個名字
---

# 一、★★★先停：**我上一封的編號不要用**

```
你手上那封（…-A0-red-is-my-premise-not-your-threshold.md）裡的 A2c／A2／A2b —— **作廢**
⇒ **唯一一張表在票的 §9**（WHAT 訂正版）。★★兩套表最兇的地方是
   **我的 A1 ＝ fp，而 WHAT 的表裡 A4 才是 fp** ⇒ 照著舊編號回報會**對錯格而且看起來正常**
⇒ ★**交件請直接用 §9 的 A0／A0b／A1／A2／A3／A4／A5 這七個名字。**
```

# 二、內容其實沒變的部分（放心，你已經做的不白做）

```
・「相位不再被永遠保留」＝ 現在叫 **A1**（100% ⇒ <10%，1/cadence 推導）—— 就是我說的 A2c
・「尖峰不得上升」併進 **A1** 的方向性那半
・「幅度」＝ 觀察欄（不變）
・A0（均勻性）＝ **觀察欄，不判**（你量到的東西留著，判準撤掉）
```

# 三、★A0b 兩處要改

```
①★★**不要用「11+ 同時到期」這個切點** —— 床**不模擬 gating**，
  床的「同時到期」與世界的「11+ 決策隊」**不是同一個母體** ⇒ 同名不同義，而它讀起來完全正常
  ⇒ **改成自比**：算 **before 的 per-tick union p99**，再數 **after 有幾個 tick 超過那個值**
  ⇒ ★不需要任何外來常數，before/after 同母體同定義
②★**順便印【每一站各自對尖峰的貢獻】** —— 下一格要用
★陽性對照的陽性對照（WHAT 要的）：把 stagger 換回純加法跑一次，**尖峰必須回來**
```

# 四、★★新增 A5：**故意留 1 處純加法**（＝改 9 處，不是 10 處）

```
目的：世界跑完之後，**那一條路徑的相位保留率必須仍然是 100%**
  ⇒ ★這證明「別處 <10%」不是儀器假象 —— ★★沒有它，全綠也可能是量錯
★★**留哪一處？由 A0b 的「每站貢獻」指：取【貢獻最小】的那一站**
  ⇒ ★因為留著就是**故意留一層不錯開**，它會繼續貢獻尖峰 ⇒ 要挑最便宜的
★★★**不要留那兩處 tile-scoped**（`labor_system.gd:162`／`decision_context.gd:614`）：
  它們的鍵是 `tile_id` ⇒ **母體是格子不是隊** ⇒ 對照「同隊同格」的相位保留率**對不上**
⇒ **回報你挑了哪一站＋它的貢獻數字**，我不預先指定。
```

# 五、序（不變）

```
A0b → 回報（尖峰不降 ⇒ 價值主張倒，不動 production）→ 綠才改那 9 處 → A1..A5
```
★`_mix` **這一刀先不修**（WHAT 提過可以順手修，我把爆炸半徑回報給他了：★**改 `_mix` 會動到全部 33 個已錯開的站**）——**他裁了我再轉給你**。
