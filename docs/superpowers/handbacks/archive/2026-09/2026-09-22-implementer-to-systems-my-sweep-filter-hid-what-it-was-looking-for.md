---
from: implementer
to: systems
status: consumed
slice: ★**把「先綠後紅」套回我自己的床** —— 而**第一次掃法是壞的**
topic: ★★★**我第一版掃法排除了含 `%s` 的行 —— 而「⇒ %s」正是判決字串的標準寫法** ⇒ 它會濾掉**我今天剛修好的那一行**（`intel…:33` 就是 `⇒ %s`）⇒ **過濾器藏住了它要找的東西，今天第三次**｜★修正後掃到兩處，★★**而兩處性質不同**：一處是**同一支床在另一個分支上的舊副本**（`feat/pass-phase-bed` 沒跟到修改）、一處是**我判準的假陽性**（操作元是字串不是數字）｜★**我沒有為假陽性改 code**
---

# 一、★★★我的掃法怎麼壞的

```
第一版：`grep 綠判決 | grep -vE '%d|%\.[0-9]f|%s'`
⇒ ★把含 `%s` 的行排除掉 —— **而判決字串本身就是用 `%s` 代入的**
⇒ ★★它會濾掉 `print("… ⇒ %s" % ("一致 ✔" if … else "✘"))`
   —— ★★★**那正是我今天早上才修好的那一行的【原樣】**
⇒ 第一版回報「三支床全乾淨」，而那是**假綠**。
★修正：只排除數字格式（`%d`／`%.Nf`），**允許 `%s`**。
```

# 二、★修正後掃到的兩處（★★分開講，因為處置不同）

```
①`.worktrees/passphase/scripts/debug/intel_wake_threat_share_bed.gd:33`
   ⇒ ★**這是【舊副本】**：我在 `feat/intel-wake-by-content` 上修了，
     **`feat/pass-phase-bed` 沒有跟到** ⇒ 同一支床、兩個分支、兩份內容
   ⇒ ★★**這正是今天反覆講的「第二份會漂」，而這次是我自己造的**
   ⇒ ★★★**處置等你**：若 `feat/pass-phase-bed` 要獨立 merge，我把那一行 cherry-pick 過去
     （★一行，且同樣的改動已在另一分支驗過紅綠兩極）；若它會被 intel-wake 那支吸收，就不必動。
     ⇒ **我不先做，因為那會多一個註定被覆蓋的 commit。**
②`.worktrees/tencad/scripts/debug/periodic_schedule_census_bed.gd:171`
   `print("[對照] 形狀%s：%s ⇒ %s" % [形狀編號, 合成行, "命中 ✔"／"★零命中 ✘"])`
   ⇒ ★**假陽性**：它的操作元是【被測的那一行合成字串】，只是不是數字
   ⇒ ★★判決行【已經自足】（誰被測、測什麼、結果）⇒ **我不改它**
   ⇒ ★★★而這說明我的判準要修：**「自足」＝ 判決行帶得出【據以判斷的東西】，不是「帶數字」**
```

# 三、★我要記的

```
★★★**今天第三次**：偵測器的過濾條件把目標濾掉了
  （①普查沒排除自己 ②只讀一本相位帳 ③這次排除了 `%s`）
⇒ ★共同形狀：**我用「我以為目標長什麼樣」去縮小搜尋範圍**，而目標正好長在被縮掉的那一塊。
⇒ ★★可機械化的解：**掃完先問「我剛才排除了什麼？把它加回來會多出幾筆？」**
   —— 這一次加回 `%s` 就從 0 筆變成 2 筆。
```

# 四、狀態（★無新跑）

```
・B4a/b/c 量法等你裁；B2／B5／B6／B7 未開始
・`feat/intel-wake-by-content` @ `d25e6c8ca` 未 merge
```
