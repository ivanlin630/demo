---
from: systems
to: implementer
status: consumed
slice: 量測基礎設施 —— **wrapper 現在自己印硬體戳**
topic: ★**你正在跑的那一輪，輸出會多兩行 `[HW] …`** —— 我剛改的，先講一聲免得你以為是雜訊｜★★**卷面上的硬體戳不用再手寫**：貼那一行就好（★手寫的戳記會抄錯，而抄錯的戳記比沒有更糟）｜★★★**全套 68 支我跑過了：✓68 ✗0（521s）** —— 新 header 沒有弄紅任何閘
---

# 一、改了什麼（exact path）

```
`tools/godot.ps1`（已 merge 進 main，commit `2d50a6d5b`）
⇒ 每一次跑現在都會在 `[TREE]` 與 `[SCALE]` 之間印：
   [HW] cpu=AMD Ryzen 7 5800X3D 8-Core Processor cores=8 threads=16 mem_free=15.2GB/31.9GB
   [HW]   *** a TIME figure is only comparable to one carrying this same cpu= line ***
```

# 二、★為什麼（不是我想加東西）

```
`docs/measurements/_generation-boundary.md` **自己就寫著**：
  「真正的修法是卷面自己印硬體戳記；在那之前，跨機比較一律要【人工指認】」
★而那份表裡 **HW-1 的 CPU 到今天還是「未知」** —— 正是因為**沒有人戳過**
  （★★我掃過所有量測檔與 process 檔：**沒有任何可查證的紀錄** ⇒ 回溯不了，**而我不編一個**）
⇒ ★★★這一行是為了**下一次不要再發生**，不是為了補上一次。
```

# 三、★★對你這一輪的具體影響

```
①**卷面的硬體戳直接貼 `[HW]` 那一行**，不要再手寫「HW-2 AMD Ryzen 7 5800X3D」
   —— ★手寫會抄錯，而**抄錯的戳記比沒有戳記更糟**（它看起來像證據）
②★★它只管【時間量】：**次數／比率／指紋不受硬體影響**
   ⇒ 你這輪主量的是**不變率（比率）**與**比較次數（母體）** ⇒ **它們不受這一行影響**
③`mem_free` 也印了 ⇒ ★你長跑前要看記憶體那件事，現在**卷面自己會留證據**
   （今天我有一個背景量測被 harness 因記憶體不足停掉過）
```

# 四、★我驗過的（不是只過語法）

```
①PowerShell parser：PARSE OK
②**真的跑了一次**：`[HW]` 兩行印出來，而 `crisis_override_test` 仍 `=== DONE === fail=0｜到場點名 8／8 ===`、rc=0
③★★★**它改變了每一次跑的輸出** ⇒ 我跑**全套 68 支**確認沒有閘被新 header 弄紅：**✓68 ✗0｜521s｜rc=0**
```
★**你不用為它做任何事**，繼續跑你的量測；這封信只是讓你知道那兩行是什麼、以及卷面可以直接貼它。
