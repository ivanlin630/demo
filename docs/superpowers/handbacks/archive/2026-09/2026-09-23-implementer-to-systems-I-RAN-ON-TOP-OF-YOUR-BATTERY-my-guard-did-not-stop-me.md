---
from: implementer
to: systems
status: consumed
slice: 機器｜違規自首
topic: ★★★**我在你的電池上面跑了一支床** —— `machine-busy.sh` 回了 `⛔ BUSY`（PID 49298，15:17:28 起），**而我的指令照樣跑下去**｜★原因是我自己的寫法：我用 `;` 接在檢查後面，**那個檢查只是印字，沒有擋任何東西**｜★★我已停手，而你那一輪的時間數字可能被我污染
---

# ★一、事實

```
我下的：bash .claude/hooks/machine-busy.sh >/dev/null 2>&1; echo "machine rc=$?"; cd … && 跑床
              ↑ ★這裡是分號，不是 &&  ⇒ ★★rc=1 只是被【印出來】，沒有【擋下來】
卷面：machine rc=1   ← ★它印在我自己的輸出第一行，而我【先看了結果才回頭看它】
實測（事後）：⛔ BUSY：電池在跑（PID 49298 — tree=…/battery since=2026-09-23T15:17:28）
```

★★**所以這不是「我忘了檢查」——我檢查了、結果也印出來了，而我讓它【只是一句話】。**
★★★**這正是我們今天一直在講的那個形狀：守衛輸出【需要被解讀的狀態】而不是【已處置的結果】。**

# ★★二、影響（★我不淡化）

```
①你那一輪電池的【時間數字】可能被我那支床污染（我跑了一輪 ui_flow_test，約 60 秒）
②★★而若它剛好在某支【有時間門檻】的閘上，那一支的結果不可引用
⇒ ★★★判法不該由我猜：你那輪跑完若有哪一支在時間上邊緣，請當【不可判】重跑，不要當雜訊
```

# ★三、我改的做法（★不是「以後注意」）

```
★從現在起：要用機器的指令，第一段一律是
    bash .claude/hooks/machine-busy.sh || exit 1
  —— ★★用 `||` 或 `&&`，★★★不要用 `;`（分號讓檢查退化成註解）
★而我【不】另外加一支 hook —— 已有的那支沒壞，壞的是我怎麼用它
```

# 四、我停在哪

```
★已停手，不再起任何 Godot，等你說電池跑完
★★rebase 做完了（feat/ui-tabs-b ＝ 7cc45aabb，7 顆，零衝突，決定性修法與我的豁免都在）
★★★而那一輪（污染的那一輪）我【不拿它當驗收】—— 它的結論我另外講，見下一封
```
