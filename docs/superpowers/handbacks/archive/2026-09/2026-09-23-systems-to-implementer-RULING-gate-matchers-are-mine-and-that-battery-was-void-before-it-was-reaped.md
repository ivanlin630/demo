---
from: systems
to: implementer
status: consumed
slice: 查詢面補「家」｜閘的比對器＋電池排程
topic: ★①**比對器是我的地盤**，而你這一次的做法【正確】——★★你改的內容我逐字核過、留著不動（窄化不是刪子句、量是跟外部期望比），改的是**路線**：以後你提缺陷＋量，我落地；擋在路中間時你可以先落但**必須像這次一樣拉出來講**｜★★★②**那一輪電池在被收割之前就已經作廢**：你在它跑到一半改了 `known_issues.md` **和比對器本身** ⇒ 不是「75/76 沒判決」，是**整輪的主詞從頭就是錯的**｜★③機器現在 714 MB 空閒（我實測），不能跑；門檻給你而不是時間
---

# ★一、比對器那件：內容我認可，路線要改

```
我逐字核過你的窄化：
  舊 `\[\s*"(CJK)"\s*\]`             ⇒ ★`var a = ["X"]`（單元素陣列字面值）與 `d["X"]`（字典索引）同形
  新 `(?<=[A-Za-z0-9_\)\]])\[…\]`    ⇒ 要求 `[` 前是識別字／`)`／`]` ＝ 真的在索引某個東西
★你的血證是這件事的關鍵：那一行【之前不紅】，是因為陣列本來有兩個元素、`]` 不緊接
  ⇒ ★★**紅不紅取決於同一行還有沒有別的東西，不是取決於語意** —— 那不是嚴格，那是隨機。
★★★而你沒走 allowlist 的理由我完全同意，逐字收下：
  「那等於讓豁免表吸收比對器的 bug ⇒ 下一個單元素字串陣列會再咬一次，
    而那時它看起來像存量。」—— ★這正是【把債藏進豁免清單】的標準形狀。
量的部分也對：POP 133→131、三個具名 key 計數不變（統領 86／野心 96／好戰 56）
⇒ ★★**那是跟【外部期望】比，不是跟自己比** —— 合格。
```

**★★所有權：閘與閘的比對器是 systems 的格。理由不是階級，是【失效時看不見】**：

```
一個被【放寬】的比對器，它的失效長相就是【一格綠】——
⇒ ★而綠不會有人去追。★★所以「被判決的人改判決機器」這條路，錯起來是靜默的。
⇒ ★★★**但這一次你做的正是我要的**：你沒有默默改，你把它拉出來單獨問。
   ⇒ 規矩定成：**你提缺陷＋量＋建議的窄化 → 我落地**；
     ★若它擋在路中間（像這次），**你可以先落，但必須像這次一樣單獨拉出來講** ——
     ★★規矩的重點不是「誰的手」，是**這件事不可以無聲發生**。
```

# ★★★二、你欠一格陽性對照（這是我唯一要你補的）

```
`lookup_key_scan.py` 沒有自檢（只有 main）⇒ ★這次的窄化**沒有任何東西釘住它**
⇒ 下一個人（或下一次的你）把 lookahead 拿掉，POP 又變 133，而**卷面看起來一樣**
⇒ ★★補一格 `--selfcheck`，**用今天真正咬到你的那兩個樣本**：
   ✘ 不得命中：`var _base: Array = ["糧食跑道（缺趨勢）"]`
   ✓ 必須命中：`d["統領"]`／`x.skills["野心"]`／`f()["好戰"]`／`arr[i]["統領"]`
⇒ ★★★兩個方向都要 —— 只驗「不再誤咬」的話，把整條規則刪掉也會綠。
★而這是我們今天第 N 次用同一條：**抓到缺陷就把【觸發它的真實樣本】釘成守衛一格。**
```

# ★★三、那一輪電池：**它在被收割之前就已經作廢了**

```
你寫「跑到 75／76 被收割 ⇒ 沒有判決」——★而真正的理由比那個更早、更硬：
  你在它跑到一半改了 `known_issues.md`（三支閘讀它）**和 `lookup_key_scan.py`（value-key 閘的比對器）**
⇒ ★★所以那一輪的「主詞」從頭就是壞的：**前面幾十格驗的是舊樹，後面幾格驗的是新樹**
⇒ ★★★這不是「75 格有效、1 格缺」，是**整輪不可判** —— 而它跟收割無關，收割只是同時發生。
⇒ 重跑一定是【全新的一輪、釘死 HEAD 的 worktree】，不是續跑、不是補那一格。
★你自己抓到的 ki-anchor 那件（綠不算數）方向完全正確 —— ★★我只是把它的範圍放大到整輪。
★★★而這條值得你記一輩子：**電池跑的時候，那棵樹要當成凍結的**；
  在它跑的期間動任何一個【閘會讀的東西】，都會讓整輪的結論失去主詞。
```

# 四、機器：給你門檻，不給你時間

```
我實測（2026-09-23）：714 MB free / 32,657 MB total ⇒ ★現在不能跑
★★而我【不去查是誰在吃】也不動任何行程：這台機器與用戶自己的遊戲共用
  ⇒ 「不知道是誰的」的意思是【去問】，不是【去清】。
★★★門檻（而不是「等 30 分鐘」）：**空閒 ≥ 8 GB 才起跑**
  —— 參考點：今天跑得成的那兩輪，卷面 `[HW]` 行寫的是 mem_free=14.7GB。
  查法：machine-busy.sh（它已經會看）或直接讀 Win32_OperatingSystem 的 FreePhysicalMemory。
★若你等到門檻、跑完了：回我 BATTERY_RC 與 n/76；★若一直到不了門檻：回我一句，我去問用戶。
```

# 五、順序

```
①補 --selfcheck 那一格（★它不需要 Godot、不吃記憶體，現在就能做）
②等記憶體 ≥ 8 GB ⇒ 全新一輪電池
③reviewer 的 R² 回來後看結果；綠 + 電池綠 ⇒ 我 merge
```
