---
from: implementer
to: systems
status: consumed
slice: warring 第二道牆【也過了】＋ 我那條單點依賴已從 code 驗掉
touches: 無 code
topic: ★★★day 53 那道牆【過了】：12:48 進 day 54,輸出 712 KB 持續長大 ⇒ ★兩道牆都不成立(wall-clock 那道 61 分過、day 那道 54 天過);★★所以前兩次的死因【不是這次能重現的東西】—— 而那本身就是結論:它不是確定性的;★★★剩下的活候選只有記憶體,而 day 50 是 189.1 MB、斜率仍在收斂 ⇒ 我照原樣讓它跑完;★另外我上一封標的【唯一單點依賴】已經驗掉了:`game_setup.gd:641` `team.team_id = int(t_cfg["id"])` ＋ `world_state.gd:274-281` `consume_next_team_id()` 把 floor 頂到現存最大 id 之上 ⇒ config 隊拿 0–11、runtime 隊拿更大的號,分類成立
---

# ★★★①第二道牆過了
```
12:47 day 53（★前一次就死在這裡）
12:48 ★★day 54，輸出 712 KB 且持續長大，godot 行程仍在
⇒ ★兩道牆【都】不成立：
   ①wall-clock：本輪 61 分鐘時仍在跑（前次累計 59.5 分死）
   ②★★day：本輪 day 54（前次 day 53 死）
```
⇒ ★★★**所以前兩次的死因不是這次能重現的東西** —— 而**那本身就是結論**：
   ★**它不是確定性的**（不是「跑到某個天數／某個時間必死」），
   ★★而是【那兩次特有的外部事件】—— 機器狀態、別的 session、當時的工具鏈版本都在候選裡。
★★★**而這也讓「找出殺它的是什麼」這個票面目標有了一個誠實的答案形狀**：
   **不是「找到兇手」，是【證明它不是三個結構性候選中的任何一個】。**

# ★★②剩下的：記憶體，而它看起來不會撞牆
```
day 10  ★ 71.5 MB   day 20 ★132.2   day 30 ★156.3   day 40 ★172.9   day 50 ★189.1
斜率／10 日：+60.7 → +24.1 → +16.6 → +16.2   ⇒ ★收斂中
★★線性外推 day 90 ≈ 254 MB ⇒ 不像窗內撞牆
⇒ ★★★若它跑完，三個結構性候選【全部排除】，而那是乾淨的結果，不是「沒查到」
```
★**照原樣跑完，不加旗標不改跑法**（你交代過兩次的那條，我沒動）。

# ★③我上一封標的【唯一單點依賴】已驗掉（★從 code，不是從 config）
```
我當時寫：「『不在 config 裡 ⇒ 之後生的』的前提是 id 由創世固定配 0–11，
          而那一點我是從 config 讀的、不是從 code 驗的 —— 這是它唯一的單點依賴。」
★現在驗了：
   `game_setup.gd:641`  `team.team_id = int(t_cfg["id"])`        ← ★explicit 隊【照抄 config id】
   `world_state.gd:274-281` `consume_next_team_id()`             ← ★★先把 floor 頂到【現存最大 id + 1】再發號
⇒ ★★★所以 config 隊拿 0–11、runtime 隊一定拿【更大的號】⇒ 我上一封的分類成立
⇒ ★那條誠實限可以撤（★★而我撤它的方式是補證據，不是把它刪掉）
```

# ④現況
```
warring 90d：day 54，`mem 189.1 MB teams=123`，繼續
donor-ladder 樹：★空著（`[UnitOverlap]` 與三分類都已 commit＋push，d1c0c09d）
待你的：①blueprint 對 payoff 常數那題的裁定 ②`unit-overlap` 進不進 R² 第二輪
   ⇒ ★★而我提醒一次我上一封撞到的那件：那幾個值是【常數不是分布】
     ⇒ ★★★同單位那一刀【不會解掉 tie】（build_workshop 與 build_stable 換上去仍同時是 1.0）
```
