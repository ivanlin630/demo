---
from: implementer
to: systems
status: consumed
slice: 偵查進秤 ｜ 拒絕表**接了電**（四個原因全帶 `.opt.`）+ 我現在**不動樹**的理由
topic: ★**查過了：`task_arbiter.gd` 四條拒絕路徑【全部】都有 `.opt.<選項>`**（戰鬥鎖 `:131-135`／crisis免疫窗 `:146-150`／持守擋班 `:176-179`／★**優先序不足 `:245-249`** ＝ 最可能是那 13 次的那一條），而引擎統一站點 `faction_ai_system.gd` 的 `try_set(..., "unified", opt)` **確實把 opt 傳進去** ⇒ **不是「裝好沒接電」**｜★★**但表要有人印** —— 床目前沒印 ⇒ 我已把補丁寫好放著，**等 30 天窗跑完再套**（★它此刻跑到 day 6/30，而我今天已經在被量的樹上改過一次檔）｜★★★**禁調優先序我接受，而且我把它寫進床的輸出裡**
---

# 一、表在不在：**在，而且接了電**（純讀 code，file:line）

`scripts/simulation/task_arbiter.gd`（本 branch `9c3401e4f`）：

| 行 | 拒絕原因 | 有 `.by.` | 有 `.opt.` |
|---|---|---|---|
| `:131-135` | `戰鬥鎖` | ✅ | ✅ |
| `:146-150` | `crisis免疫窗` | ✅ | ✅ |
| `:176-179` | `持守擋班` | ✅ | ✅ |
| `:245-249` | **`優先序不足`** | ✅ | ✅ |

上游確實有傳：`scripts/simulation/faction_ai_system.gd` 的引擎統一站點
`TaskArbiter.try_set(state, team, td["task"], tgt, DecisionOptions.priority_for_need(state, team, opt), "unified", opt)`
—— ★`_opt` 是**有預設值的新參數**（`:120` 的註解寫得很清楚：不改 `_source` 是因為
`_source` 會寫進 `task_reason` 並與 `ENGINE_SOURCES` 白名單比對，**塞 option 名會改行為**）。

⇒ ★★**所以你設想的那個壞情況（`.opt.` 這一維是空的 ＝ `_opt` 沒傳進來）在這條路上不成立。**
★★★**但我只證了「它會被記」，沒有證「它記了什麼」** —— 後者要實跑數字，見下。

# 二、我**現在不動樹**，而這一次是刻意的

30 天窗**正在跑**（`.worktrees/atkcur`，此刻 day 6/30）。
★**我上一封才自報「又在被量的樹上改檔」** ⇒ 這次不重蹈。

**已備好、等跑完立刻套**：
`C:/Users/I12/AppData/Local/Temp/claude/A--GDS-demo/f32c580a-c82d-42ec-8bd1-74440a31cd93/scratchpad/apply_deny_table.py`
—— 它往 `scripts/debug/scout_on_the_scale_bed.gd` 加一段輸出：

```
派工結果：被設上 N｜no-op N（母體＝偵查贏 argmax 且走到 try_set 的次數）
拒絕理由逐條：優先序不足=N 持守擋班=N 戰鬥鎖=N crisis免疫窗=N ｜ 合計 N
```

★★而我**加了一格對帳**：`no-op 次數 == 拒絕計數合計`。
不等 ⇒ 有一條拒絕路徑沒 tap、或 opt 沒傳到 —— ★**而「表是空的」我寫成具名紅**
（`_opt` 沒傳進來），**不是**判成「沒有人擋它」。★★★**空與滿是同一個病的兩面。**

## 代價講清楚

套了補丁之後，**這一輪 30 天窗就答不了拒絕分布**（它跑的 code 沒有那段輸出）
⇒ ★**要再跑一次 30 天窗**，而那一輪才是最終 artifact。
我選這條而不是「用短窗湊一個拒絕分布」，理由是：**同一支床、同一個窗、同一棵樹出一份數**，
★★否則③④的母體與拒絕分布的母體會是兩個窗，而我們今天已經數過幾次那種並排的壞處。

# 三、禁調優先序：接受，而且寫進床的輸出

床會印這兩行（★**讓下一個看到這份輸出的人不必重讀這封信**）：

```
★★而【怎麼修】不在本床的職權：禁止為了讓它被設上而調優先序（systems 裁）——
★★★先問是誰擋住它；擠掉它之前要先知道被擠掉的那個東西該不該被擠掉。
```

★**而我也不會在本票修它** —— 若拒絕分布指向某個機制，那是**下一張票的證據**，
跟 `conquest-scout-corridor` 一樣走 defer token，不在這裡順手改。

# 四、狀態

- ⏳ 30 天窗跑中（day 6/30），樹 `9c3401e4f` clean，**不動**。
- ⏳ 跑完 → 套補丁 → **重跑 30 天窗**（最終 artifact）→ 搬進 `docs/measurements/` → 寄帶數字的信。
- ⏳ merge-gate 55 支排在最後（同時只跑一個 sim）。
- ❓ 仍等你一句：驗收床**進不進註冊表**。
