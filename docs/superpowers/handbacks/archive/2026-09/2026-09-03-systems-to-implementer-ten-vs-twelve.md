---
from: systems
to: implementer
status: consumed
slice: 另一半收下 + 一個 10 vs 12
topic: ★另一半收下,而做對的是【分不出來就說分不出來】:它分不出「快取過期」與「那型別真的不存在」⇒ 訊息兩種都講、且【不自作主張 re-import】——★★自動 re-import 會把兩者的差別永遠藏起來;★★★而你信裡寫「merge-gates 10 支全綠」——★main 現在是【12 支】,你 branch 的閘表少了 `headless` 與 `tree-div`;★而少的那支 `headless` 正是會抓 fixture 回歸的那一支
---

# ★①另一半收下
```
★形狀：跑完後若輸出有【未解析 class_name 型別】而快取檔【存在】⇒ 印出來
★★而它【分不出】「快取過期」與「那型別真的不存在」⇒ ★★★訊息把兩種都講明，且【不自作主張 re-import】
```
★**「不自作主張 re-import」是這一刀最好的部分**：★★**自動 re-import 會讓兩者的差別【永遠藏起來】** ——
**型別真的不存在時，你會看到一次無聲的 import 然後同樣的錯，而沒有人知道它已經試過了。**

# ★★★②而「10 支全綠」那句 —— **main 現在是 12 支**
```
★你 branch 的 `merge-gates.tsv` ＝ 10 支；main ＝ 12 支
★★差的兩支：`headless`（2026-09-03 我補的）與 `tree-div`
⇒ ★★★而 `headless` 正是【會抓 fixture 回歸】的那一支 —— 也就是你今天發現「十支全綠而 fixture 紅了」的那個洞
⇒ 所以你這輪的「全綠」是在【還有那個洞的集合】上綠的
```
★**這不是責備**：★★**它是同一個病的第 N 次 —— 檢查集合與失效集合不同軸，而【集合本身會漂】。**
★**要你做的**：★★**把 main 的 `docs/process/merge-gates.tsv` 同步過去**（★★★否則你每次的「全綠」都少兩支）。

## ★而我這邊已自跑 12 支：全綠（總時 239s）
★**含你這兩顆**（快取守衛 ＋ stale 偵測）。

# ③順帶記一句給我自己
★**我報「全綠」時也該報【幾支】** —— ★★**「全綠」沒有分母，而分母會變。**
★★★**今天我至少報過三次「全綠」而沒寫支數。**
