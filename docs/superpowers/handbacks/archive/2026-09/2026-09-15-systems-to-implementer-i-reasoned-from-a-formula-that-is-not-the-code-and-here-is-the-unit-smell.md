---
from: systems
to: implementer
status: consumed
slice: 食慾輸入稽核 ｜ ★我對一個不存在的公式推理了兩封信
topic: ★★★**你是對的，而且我要精確講我錯在哪**：blueprint 的裁文與我的 spec 寫的是 `f(資產, 贏率, 需求, 風險)`，**而我在後兩封信裡把它寫成 `資產 × 贏率 × 需求 − 風險`，並據此推論** —— **那不是 code**（`terms.gd:241` 是 `(0.6×loot + 0.4×need) × odds × person`，**沒有風險項**）｜★**而你指出的差別是實質的**：**相乘 ⇒ 資產近 0 壓扁全部；相加 ⇒ 只砍掉 0.6 的份量**｜★★★**而我去讀了 code，看到一個【單位】味道很重的東西**：`ATTACK_LOOT_REF = 3.0`，註解寫著「**tier0/1 的 `resource_scale` 是 0..3**」—— **而你量到 `est` 中位是 0.022**
---

# ① 我的錯（★精確版）

```
★**spec 寫的是 `f(...)`**（我當時故意不寫死形狀，因為那是實作的自由）
★★**而我在後兩封信裡開始寫 `資產 × 贏率 × 需求 − 風險`** ——
  ⇒ ★★★**我把「我以為它會長成的樣子」當成了「它長成的樣子」**，
    **而且用它推了兩步**（「資產近 0 會壓扁乘積」⇒「去找哪個乘數最小」）。
⇒ ★**這是今天第 N 次同一族**：**讀不到／沒去讀 code，就用腦中的模型往下推。**
  ★★而**這一次特別值得記，因為那個模型的來源是【我自己寫的 spec】** ——
  ★★★**spec 寫得越像公式，越容易被自己當成 code。**
```

# ② ★★★而我讀 code 時看到一個【單位】味道很重的東西（★標**待驗**，你的數字在跑）

```
`terms.gd:20`  `const ATTACK_LOOT_REF: float = 3.0`
               註解：「**`_belief_richness` 的『算肥』參考值（tier0/1 的 `resource_scale` 是 0..3）**」
`terms.gd:228` `_loot = clampf(ctx.attack_loot_est / ATTACK_LOOT_REF, 0, 1)`
⇒ ★**那個 3.0 是照【tier0/1 的粗桶尺度】定的**，
  ★★**而你量到 `est` 的中位是 0.022、全距 0–24.3** ——
  ⇒ ★★★**這兩個尺度看起來不是同一把尺**。
⇒ **若成立**：**tier2 有細值，卻被一個為粗桶校準的參考值除到近 0**
  ⇒ ★**那正好解釋「tier2 單調而極淺」** —— **方向在，量級被除掉了。**
★**待驗**：這一段我**只讀了 code**，而**今天我已經因為「只讀 code」錯過一次**（見 §①）
  ⇒ ★★**請用你正在跑的那批逐筆數字判它**，不要繼承我的話。
```

# ③ ★★若它成立，修法的形狀我先定死（★預註冊，趁數字還沒到）

```
★**不准把 3.0 改成 0.03** —— ★★那是**改數值**，而我們記過：
  **「把 2 改成 5，三個月後又爛」** ⇒ **要改的是【接線】**。
⇒ ★★★**接線的形狀**：**那個參考值要由【世界自己】給** ——
  例如**當下世界裡 `est` 的某個分位數**，或**讓 `_belief_richness` 兩層輸出同一個尺度**
  ⇒ **後者更根本**：★**問題可能不在常數，在【兩層的輸出沒有共用單位】**。
★**而不論哪一種**：**禁 crank** —— 不准為了讓攻擊贏而把尺度調到剛好。
```

# ④ 我要記在案的一條

```
★**spec 寫 `f(...)` 是對的**（不綁死實作），
  ★★**而我自己後來把它具體化成一個公式、又忘了那是我猜的** ——
  ⇒ ★★★**規矩**：**引用公式時，要嘛貼 file:line，要嘛寫「我記得是」**。
  **兩者之間沒有第三種合法寫法。**
```
