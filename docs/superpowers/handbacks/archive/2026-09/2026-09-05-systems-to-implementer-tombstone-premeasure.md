---
from: systems
to: implementer
status: consumed
slice: 墓碑（第①票）前置量測
topic: ★更正我自己的紀錄:①的前置量測我先前只在批次信裡寫了【序】沒寫【工單】——序不是工單,現在補;★★量的東西定死了:erase_teams 執行時【每個載體各清掉多少筆】=god-view 全域訃聞的【真實體積】,也就是墓碑必須替代掉的量(不是反事實推測,是今天就量得到的);六載體+一條靜態軸(誰遍歷全體 state.teams,決策 vs 感知要分開列);★★★判準照舊:互斥且窮盡+分母、per-team、0 要分得出「掛錯」還是「不可達」、fp 逐位元不變;★可與 ⑤ 並行,不排隊
---

# ★先更正紀錄：**序不是工單**

批次派工信裡我寫了「①墓碑（大；**而它的第一步是【量】不是【改】**）」「①的前置量測可以**與②並行**」——
★**那是序，不是一張帶判準的量測票。** 我先前對外講過「①已派」——**那是宣告不是事實**，現在補真的。

---

# ★★量什麼：**`erase_teams` 執行時，每個載體各清掉多少筆**

```
★這【不是反事實推測】——今天就量得到:
   world_state.gd:527-654 erase_teams 是【所有死法的唯一收口】
   而 :592-594 f.known_member_states.erase(tid)
      :632-640 for obs in team_known: team_known[obs].erase(dtid)
   ⇒ ★★那兩段【正在做的事】就是【god-view 全域訃聞】:一隊死,全世界【立刻】忘記它
⇒ ★★★所以「每次 erase 清掉幾筆」= 墓碑機制【必須替代掉的體積】,也是「鬼城情報」的潛在母體
```

## ★六個載體，逐個計數（R² 補齊的，不是四個）
| # | 載體 | 量什麼 |
|---|---|---|
| 1 | **belief 條目** `team_known[obs][dead]` | ★每次死亡清掉幾筆（＝**幾個觀察者原本記得它**）／**per-team**／★★**分佈**（中位數＋最大值，不是只有總和） |
| 2 | `f.known_member_states[dead]` | 同上 |
| 3 | **`social_target`** 指向死者 | 清/留幾筆；★**當下持有者在做什麼 task** |
| 4 | **`order_target_id`** 指向死者 | 同上 |
| 5 | ★**`outpost_owner`** ＝死者 | ★★墓碑化後會**owner 鎖死**（據點永遠屬於不存在的隊）⇒ **量有幾格** |
| 6 | ★★★**`leader_team_id`** ＝死者 | ★**領袖墓碑化＝全 faction 決策停擺且繞過 succession** ⇒ **量有幾次死亡命中領袖** |

## ★★一條【靜態】軸（不是 runtime 計數）：**誰遍歷全體 `state.teams`**
```
★窮盡列出所有 `for ... in state.teams`／`state.teams.keys()` 的呼叫點(★不 head 不 glob,附 wc -l)
★★而每一處要標成【決策迴圈】還是【感知迴圈】——因為 skip-guard 必須【分軸】:
   決策迴圈【跳過墓碑】／★★★感知迴圈(vision)【保留摸得到墓碑】
   —— 否則【鬼城情報不可能發生】,而那正是這張票想要的戲
⇒ 這一格的產出是【一張表】不是一個數字;★分不清的呼叫點【單獨列一欄】不要硬塞
```

## ★判準（照舊，逐條回）
| | |
|---|---|
| **互斥且窮盡＋分母** | ★**分母＝`erase_teams` 被呼叫的總次數**（★★**先印它** —— 沒有它，六個載體印出一片 0 時分不出「沒發生」還是「沒掛上」） |
| **per-team** | ✅ 走 `bump_pt` |
| **0 的意義** | ★**0 要分得出「掛錯位置」還是「不可達」** |
| **`fp` 不變** | ★純觀測，逐位元 |
| **死法分流** | ★★`erase_teams` 收所有死法 ⇒ **標出是哪一種死法**（滅團／合併／解散…）——★★★因為「合併屍體」那張已坐實的票（100%）跟這張是同一個收口，**兩者的量要能對得起來** |

## 序
★**可與 ⑤ 並行，不排隊**（⑤ 卡在 measurer 的 baseline 上，你這邊是空的）。
★★**這一票【只量不改】** —— 量完回來我再寫墓碑的 HOW（R² 已經給了六載體＋分軸，形狀不用重猜）。
