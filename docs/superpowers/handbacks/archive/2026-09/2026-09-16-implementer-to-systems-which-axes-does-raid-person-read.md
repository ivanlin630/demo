---
from: implementer
to: systems
status: consumed
slice: 常態掠奪票（三拉桿）｜ **開工前一個必問**：★★★**掠奪與攻擊的 `person` 讀的【不是同兩個軸】**
topic: ★**實測 code**：`terms.gd` 掠奪 ＝ `maxf(好戰, **殘忍**)`（`_rmart`/`_rcruel`）／攻擊 ＝ `maxf(好戰, **貪婪**)`（`_mart`/`_greed2`）⇒ **兩側本來就不同軸**｜★★**而 spec §6 的新形狀寫的是【好戰／貪婪】、§2 的兇性容忍寫的是【好戰／殘忍】** ⇒ **同一張票裡兩個軸組並存**｜★★★**而驗收格寫「雙高（好戰 .9／貪婪 .9）的【掠奪】util ＞ 單高」** —— **貪婪【不在】掠奪的 `person` 裡** ⇒ **那一格照現況會【永遠綠不了】，除非我同時把掠奪的軸換成貪婪**｜★**換軸＝改語意，不是改形狀** ⇒ **我不自己換**｜★★另問：**這一票落在哪一條 branch**（它疊在尚未 merge 的掠奪票上）
---

# ① 現況逐字（★我核過 code，不是讀 spec）

```
`terms.gd` 掠奪（`loot_drive`）：
   var _rmart  := 好戰 ／ var _rcruel := **殘忍**
   _rperson := clampf(0.5 + (maxf(_rmart, **_rcruel**) − 0.5) − (慎重−0.5)×W, 0, 1.5)
`terms.gd` 攻擊（`attack_opportunity`）：
   var _mart := 好戰 ／ var _greed2 := **貪婪**
   _person  := clampf(0.5 + (maxf(_mart, **_greed2**) − 0.5) − (慎重−0.5)×W, 0, 1.5)
⇒ ★**兩側【本來就】不是同兩個軸** —— 掠奪吃殘忍、攻擊吃貪婪。
```

# ② ⇒ spec 裡三處互相打架（★**我只列，不自己選**）

```
§2  兇性容忍 ＝ `clamp(max(好戰, **殘忍**), 0, 1)`          ← 殘忍軸
§6  新形狀   ＝ `hi/lo := max/min(好戰, **貪婪**)`           ← 貪婪軸
§6 驗收格   ＝ 「雙高（好戰 .9／**貪婪** .9）的【**掠奪**】util ＞ 單高」
⇒ ★★★**而貪婪【不在】掠奪的 `person` 裡** ⇒ **那一格在現況下是【結構上不會綠】的**
   —— **除非同時把掠奪的 `person` 換成讀貪婪。**
```
★**而這正是今天一直在抓的那個形狀**：**驗收格看的軸，跟機制實際讀的軸不是同一個。**
★★**所以我先問，不先寫** —— **寫了就會產生一個「看起來接了、而永遠綠不了」的格。**

# ③ 三條路（★**我不挑**，但把後果寫清楚）

| | 做法 | 後果 |
|---|---|---|
| 甲 | **掠奪的 `person` 改讀 `max(好戰, 貪婪)`**（與攻擊同軸） | ★符合正典「常態好戰來自**貪婪／好戰**尾部人格」；★★**但殘忍從 `person` 消失** —— 而殘忍**仍在 `take`（物理）裡**，所以它不會完全不見 |
| 乙 | **§6 逐側各用自己的兩軸**（掠奪 好戰/殘忍、攻擊 好戰/貪婪） | ★改動最小；★★**但 §6 的驗收格要改成「好戰 .9／殘忍 .9」** 才驗得到掠奪 |
| 丙 | **掠奪 `person` 讀三軸**（好戰／貪婪／殘忍） | ★★★**我不建議**：那是把「軸該有幾個」偷偷改掉，而它不在任何裁定裡 |

★**我傾向甲**（理由：正典逐字寫的是**貪婪／好戰**，而 `take` 已經吃殘忍 ⇒ **殘忍不會消失，只是從偏好側退回物理側**）——
★★**但那是【語意改動】** ⇒ **要你（或 blueprint）一句話。**

# ④ 另外三件開工前的確認

```
① **branch**：這一票疊在**尚未 merge** 的 `feat/raid-expected-value` 上
   ⇒ ★**接著在同一條 branch 做**，還是**開新 branch 疊上去**？（我預設**同一條**，除非你說）
② **`ThreatAssessment._power_ratio` 我會直接呼**（照你 §2 ★★★）—— ★**不另起一份**，
   理由你已寫：那支裡面**修過「技能維不對稱」**，另寫一份就是重演它。
③ **三個拉桿各自的前後數**：我會**逐桿分開量**（①只接 take 權重 ⇒ 量；②再接成本 ⇒ 量；③再接雙高 ⇒ 量）
   ⇒ ★★**否則接完仍差 3 倍時，我們分不出是哪一個沒力**（那正是你要的 gap 分解表）。
```

# ⑤ 現況

```
`feat/raid-expected-value`：**最後一輪 56 支跑中**（兩支常態 fixture 已改斷言、絕境格全綠）
`feat/grudge-ledger-a` @ `20e57dccd`：`取 max intensity` 已改成飽和疊加（出處寫在斷言旁）
   ⇒ ★**它的 56 支需要重跑一輪**（上一輪的兩支紅都已處置）
★而我**還沒開始**常態掠奪票 —— **等 ③ 的一句話**（★不先寫，免得做出一個永遠綠不了的格）。
```
