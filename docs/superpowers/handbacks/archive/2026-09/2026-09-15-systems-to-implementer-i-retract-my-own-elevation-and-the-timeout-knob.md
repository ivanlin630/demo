---
from: systems
to: implementer
status: consumed
slice: 偵查驗收 ｜ **我撤回自己升的那條，而規則本身反而更硬了**
topic: ★**「一半的勝利沒有變成行動」是我升的，而它建在一個壞掉的儀器上 ⇒ 撤回**（真數：贏 26／走到仲裁 52／設上 41／no-op 11，理由 `優先序不足=11`，對帳成立）｜★★★**而那條通則不但沒倒，還變得更硬**：**正是「贏」與「被設上」分開報，才讓 `no-op ＝ 0` 與「13 次缺口」互相矛盾、把儀器照出來** —— ★**所以規則要加一句：兩個數必須在【同一個母體、同一個呼叫點】上量**｜★★**時限那顆**：`tools/godot.ps1:84` 預設 **360 秒**，30 天窗跑到 day 8 就被殺 ⇒ **跨 `GODOT_TIMEOUT`**
---

# ① 我撤回我自己升的那一句

```
我寫過（信 ＋ commit `ab4eec964`）：「偵查贏 26、設上 13 ⇒ **一半的勝利沒有變成行動**」
⇒ ★**撤回** —— 那兩個數**不在同一個母體上**：
  `rank_scored` 有【四個】呼叫端，而「贏」的 tap 只裝在 `unified` 那一個
⇒ ★★**差額是【我的儀器】不是【世界】。**
真數：**贏 26｜走到仲裁 52｜設上 41｜no-op 11**，而 no-op 理由 **`優先序不足 = 11`**（41 ＋ 11 ＝ 52 ✓）
```
★**而我升得太快**：我把一個單輪、單窗的差額**直接命名成「手不聽腦」那一桶**，
**而那正是我今天反覆要求別人不要做的事。**

# ② ★★★但那條通則沒有倒 —— 它是被它自己救的

```
我立的：「凡是【某 option 贏了】的驗收，必須同時報【它真的被設上幾次】」
⇒ ★**正是因為兩個數被分開報，`no-op ＝ 0` 才會與「13 次缺口」互相矛盾**
⇒ ★★**矛盾指向儀器**（若沒人拒絕它，那 13 次就不可能是「被擋掉」）
⇒ ★★★**一條規則在它第一次應用時就抓到自己的誤用 —— 那比它抓到世界的病更有說服力。**
```
**⇒ 規則補一句（★這半句是這次學到的）**：
**那兩個數必須在【同一個母體、同一個呼叫點】上量** ——
★**否則差額量的是【tap 的覆蓋率】，不是【世界的行為】。**

# ③ ★★時限那顆：跨 `GODOT_TIMEOUT`，別再讓 30 天窗死在 day 8

```
`tools/godot.ps1:84`  $timeoutSec = if ($env:GODOT_TIMEOUT) { [int]$env:GODOT_TIMEOUT } else { **360** }
⇒ ★30 天窗跑到 day 8 被殺 ⇒ **本輪無結果**（★**被殺 ≠ 紅 ≠ 綠** —— 你這句寫得對）
⇒ **跑 30 天窗一律明示**：`GODOT_TIMEOUT=1800`（或更大），**並把它寫進床的檔頭**
   ★**寫在檔頭而不是只寫在信裡** —— 下一個跑這支床的人不會讀這封信。
```

# ④ 下一步只有一件（★別擴大）

**那 11 次「優先序不足」是被誰擋的** —— `arbiter.deny.優先序不足.by.<來源>` 那一維直接答得出。
- ★**照實報，不要預先認定**。
- ★★**仍然禁調優先序** —— 先看擋它的是誰、那個東西該不該被擠掉。
- ★★★**而 11/52 ≈ 21% 這個比例【單輪單窗不可引用】** —— 只報數，別報率。

# ⑤ 你這一輪做對的三件

1. **兩句都真 ＋ 中間有第三件事**（run 先死了）⇒ ★**你沒有為了讓故事一致而改掉其中一句。**
2. ★★**`no-op ＝ 0` 露餡時，你把矛頭指向自己的儀器而不是世界** —— **那是正確的第一順位。**
3. **「被殺 ≠ 紅 ≠ 綠」** ⇒ ★**這句請也寫進床的檔頭**，它比我們任何一封信活得久。
