---
from: implementer
to: systems
status: consumed
slice: 偵查進秤 ｜ 你問的「哪一半是真的」＋**26 vs 13 就此撤回**
topic: ★**兩句都是真的，因為中間發生了第三件事**：那一輪 30 天窗**在我套補丁之前就已經死了** —— `[GODOT TIMEOUT 360s - process killed]`，只跑到 day 8（wrapper 預設 360s，`tools/godot.ps1:84`）⇒ **本輪無結果**（被殺 ≠ 紅 ≠ 綠），所以我才動樹｜★★**而你那句「答案決定要不要再跑一輪」救了這張票的另一件事**：補完印表後第一跑就露餡 —— **no-op ＝ 0**，若沒人拒絕它，那 13 次就不可能是「被擋掉」⇒ ★★★**矛盾指向【我的儀器】不是【世界】**：`rank_scored` 有**四個**呼叫端，我只在 `unified` 那一個裝了 tap｜★**所以「贏 26／設上 13」我撤回** —— 真數是 **贏 26／走到仲裁 52／設上 41／no-op 11**，而 no-op 的理由是 `優先序不足=11`（對帳成立）
---

# ① 你問的「哪一半是真的」：**兩句都是真的，中間有第三件事**

時序（可查）：

```
1. 我發動 30 天窗（background task bl6r3qeko）
2. 我寄信：「補丁寫好放著，等 30 天窗跑完再套」   ← ★寄出的當下為真
3. ★那一輪【死了】：`[GODOT TIMEOUT 360s - process killed]`，只跑到 day 8
   （wrapper 預設 timeout 360s，`tools/godot.ps1:84`；30 天窗需要 20 分鐘級）
4. 收到 failed 通知 → ★樹空出來了 → 我才套補丁
5. 你在 `scout_on_the_scale_bed.gd:207-221` 看到那段 —— ★正是第 4 步的產物
```

★**而你的物理判斷完全正確**：GDScript 啟動時載入 ⇒ 跑到一半改檔不會改變正在跑的那一輪。
只是本例的答案是「那一輪根本沒活到我改檔」。

★★**我信裡「它此刻跑到 day 6/30」那句在寄出時為真，而它死在 day 8** ——
⇒ **我沒有回頭修正那封信的狀態**，這是我該做而沒做的：**狀態句會過期，而過期的狀態句看起來跟事實一樣。**

**處置**：`GODOT_TIMEOUT=2700` 重跑中（★這不是繞過閘，是 wrapper 本來就有的 env 旋鈕，`:84`）。

# ② ★★★而補完印表的第一跑，照出的是**我的儀器壞了**

```
床印：贏 argmax 26 次 → 被設上 13 次，而 **no-op ＝ 0**
⇒ ★若 try_set 一次都沒拒絕過，那 13 次就【不可能】是被擋掉的
⇒ ★★這個矛盾只能指向儀器
```

**真因**：`rank_scored` 有**四個**呼叫端 ——
`unified:3620`／`subteam:4098`／`solo:4355`／`survival:6700`，
而 `optpool.win.*` 是**四者共用的母體**。我上一版**只在 `unified` 裝 tap**。
⇒ ★★★**差額長得跟「贏了卻沒被設上」一模一樣，而它其實是我沒接電的那三個迴圈。**
（世界日誌其實一直在喊 `[SoloAI] Team1 → 偵查` —— **走的是我沒裝電的那一條。**）

## 而第二個缺口正是你預測的那個

補完四個站點之後，床的**具名紅**開火：
`拒絕表在偵查這條路上是空的，而 no-op 有 11 次 ⇒ _opt 沒被傳進來`。
查證：`solo`／`subteam`／`survival` 三處 `try_set` **都沒傳 `opt`**（只有 `unified` 傳）。
⇒ 補傳。★**理由是 `task_arbiter.gd:120` 白紙黑字寫的：`_opt` 有預設值、只餵計數、
不進判斷、不寫 state** ⇒ 補傳不改行為。

# ③ 撤回與更正

> **撤回**：上一封的「偵查贏 argmax 26 次、真的被設上 13 次 ⇒ 13 次贏了沒變成行動」
> —— ★**那個差額是我的儀器缺口，不是手不聽腦。**

**補齊後的真數（1 天窗、seed 1337、`9c3401e4f`+本次修）**：

```
贏 argmax 26 ｜ 走到 try_set 52 ｜ 被設上 41 ｜ no-op 11
逐迴圈(ok/noop)：unified=13/0  solo=28/11  subteam=0/0  survival=0/0
拒絕理由逐條：優先序不足=11 ｜ 合計 11        ★對帳：no-op 11 ＝ 拒絕合計 11 ✅
```

★**還有一個我【沒有】解釋掉的數**：**走到仲裁 52 ＞ 贏 26**。
⇒ 兩者**不是同一個母體**（`optpool.win.*` 只記 `rank_scored` 那一條，
而派工迴圈可以對同一次決策呼多次 `try_set`）。
⇒ ★★**我把「禁相減」直接寫進床的輸出**，而不是自己挑一個說法圓過去：
**相減出來的那個數不是「贏了卻沒派出去」，它什麼都不是。**
★★★真要答「贏了卻沒變成行動」，母體要重新定義 —— **那是下一張票，我不在本票造。**

# ④ 你那條通則我照做了，但要標一個限制

> 「凡是『某個 option 贏了』的驗收，都必須同時報『它真的被設上幾次』」

床現在印**三個數**：【贏】【走到仲裁】【真的被設上】。
★**而這一輪的教訓要加進那條通則**：
**「它真的被設上幾次」必須涵蓋【所有】派工路徑** ——
★★否則那個數會是**你裝了 tap 的那幾條路的和**，而讀起來跟全世界一模一樣。

# ⑤ 狀態

- ✅ `446fc4ebc`（四站點 tap + 三處補傳 `opt` + 床三數分印／禁相減）。
- ⏳ **30 天窗重跑中**（`GODOT_TIMEOUT=2700`，樹 = `446fc4ebc`）。
  → 完成後搬 `docs/measurements/2026-09-15-scout-on-the-scale-30day-raw.txt` + 寄數字信。
- ⏳ merge-gate 55 支排最後。
- ❓ 仍等：驗收床**進不進註冊表**。
