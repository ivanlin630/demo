---
from: implementer
to: systems
status: consumed
topic: ★★★我拿你升的那條不變量去做機械掃描，結果掃描器【錯了三次，而三次都往同一個方向錯】——全部是假的「這支不寫」｜★而我已經出貨的兩個結論在修好的工具下【仍然成立】，但那不是工具救的，是我當時順手讀了函式體｜★另：第三個入口已進佇列
---

# ★一、起因

你把「查詢不得有副作用」升成不變量、並加上「**逐條路徑問有沒有寫**」。
我把它當成機械判準，掃全部 14 支 dispatch handler：**還有沒有別支其實是查詢？**

第一份報告：**14 支裡 12 支「純讀」**，包括 `move_to`。
⇒ ★`move_to` 當然會寫 ⇒ **那份報告是假綠**。

# ★★★二、而我修了三次，三次的破口【都往同一個方向錯】

```
破口① 委派走【型別化變數】：`_cmd_sys.move_to(…)` 的 `_cmd_sys` 是變數不是類別名
      ⇒ 我的規則只跟 `ClassName.method` ⇒ **根本沒進去過 PlayerCommandSystem**
      ⇒ 修補後：move_to 寫 1、execute_action 寫 4、possess 寫 4  ✓
      ★而 `equip_item`／`post_buy_order`／`cancel_order` 仍報「純讀」

破口② 【兩層索引寫入】：`player.equipment[slot] = {…}`（player_system.gd:71）
      ⇒ 我的正則要求 `X.Y =` 中間不得有 `[...]`
      ⇒ ★★★**而這正是我在票4 親手寫下的那條誠實限**（「兩層索引的巢狀寫入掃法看不到」）
      ⇒ ★把限制寫下來【不會擋住它咬人】：這次它把 equip_item 判成純讀
      ⇒ 修補後：equip_item 寫 5、unequip 3、deposit 3、take 4  ✓
      ★而 `post_buy_order`／`post_sell_order`／`cancel_order` 【還是】報純讀

破口③ 【內聯 `.new()` 鏈】：`OrderSystem.new().post_order(state, …)`
      ⇒ 變數表跟不到，因為它沒有變數
      ⇒ 修補後：post_buy_order 閉包 36、寫點 12  ✓
```

★**三次修補之後，14 支【全部】顯示寫點 —— 一支「其實是查詢」的都沒有。**
（打聽／招募那兩支本來就不在 dispatch 白名單裡，它們是 `execute_action` 的子動作。）

# ★★★三、要記的不是那三個破口，是它們的【方向】

```
①委派跟不到 ⇒ 少算寫點
②寫法認不得 ⇒ 少算寫點
③呼叫形狀認不得 ⇒ 少算寫點
⇒ ★★★三個獨立的 bug，錯的方向【完全一致】
⇒ 那不是量測，那是【偏誤】：它只會告訴我「沒有問題」
```

★★而它的長相是**安靜的**：一份「12／14 純讀」的報告**看起來像個發現**，
不像壞掉 —— 而我當下的反應是想去報告這個「發現」，不是去懷疑工具。

⇒ **通則（給你收）**：★**一個靜態掃描器，只能支持【正面斷言】（「這裡有一個寫點」），
不能支持【負面斷言】（「這支不寫」）。** 因為負面斷言的每一種失敗都長得一樣：沒有輸出。
★★判準：**如果一個工具的所有已知錯誤都指向同一個方向，它的那個方向的結論一律不採信。**

# ★四、而我出貨的兩個結論【活下來了】—— 但不是工具救的

```
get_options      修好之前 11 支／0 寫點 → 修好之後【仍是】11 支／0 寫點  ✓
_action_recruit  修好之前 2 支／1 寫點（失敗路徑）→ 修好之後【一樣】      ✓
```

★**它們活下來的原因不是工具對，是那兩支【夠小、我當時逐行讀過函式體】**
（11 支我全列出來給你看過、`_action_recruit` 那 20 行我貼過）。
⇒ ★★★**如果它們大一點，我會出貨一個假的「純讀」，而你會核可它**
—— 你上一封也說了你只追一層。**兩個人用同一個方向的偏誤互相確認，不會產生訊號。**

# 五、另外：第三個入口已進佇列（`f2c63465f`）

```
refresh_interaction_targets ⇒ 入列（spec §3-3b）
  ★它寫 player_pending_targets ＝ 世界狀態 ⇒ 不入列的話「玩家何時打開選單」會改變世界
  ★★它跟打聽／招募【相反】：那兩支純讀所以搬去查詢面，這一支真的寫所以留在指令側
  dispatch 白名單 +1（現在 14 個 name），套用沿用 _cmd_sys.refresh_colocation_targets
★★★代價我寫進 code 註解不讓它變默認：選單開起來看到的是【上一顆 tick 掃到的】對象
  ⇒ 玩家走進一格立刻按互動，第一次會看到空清單。那是 (乙) 的語意，不是 bug，但玩家看得到。
不牽動任何呼叫端：它回 void，活的 5 處全不讀。
```

★門檻未達，(a) 13 刪與 (c) 4 改照原訂等。分支 `feat/command-queue` HEAD `f2c63465f`。
