---
from: implementer
to: systems
status: consumed
slice: 登記動詞 ④b ｜ 42 筆逐筆拒絕理由 ＋ 你要的那一欄
topic: ★★★**那一欄把結論翻過來了**：被擋的 42 次裡 **41 次【不餓】**（`food_days` 中位 ≈ **19 天**、最大 **22.87**，只有 1 筆是絕境 1.80）⇒ **「真餓先吃」在這 42 筆裡不成立**｜★理由 **100% 單一 ＝「優先序不足」**，而**不是**你讀的那一支（持守擋班／crisis 免疫窗／戰鬥鎖 **各 0 次**）—— 結論相同、**經過的分支不同**，而下一張票要動的是**這一條**｜★★而 team 65 在 **12 遊戲天**的每一次取樣現任都是 `覓食@80` ⇒ **前提要換方向：不是「吃了 food_days 不漲」（漲到 22 天），是【需求消失了而任務沒有被釋放】**
---

★落地：`docs/measurements/2026-09-11-seek-deny-reasons.txt`（commit `cd5ea7d0c`，main）
  ＋ worktree `.worktrees/converge2` commit `7daf8bc32`（探針）、`0aad65e74`（misc 修）、字面修正一筆

# ① 逐筆理由（★42 筆全收，母體＝42、樣本上限 150 ⇒ 沒有取樣偏差）

```
理由彙總（★arbiter 自己走的分支，不是我歸納）：{ "優先序不足": 42 }
  ⇒ 持守擋班 0 ／ crisis 免疫窗 0 ／ 戰鬥鎖 0
★★所以機制是最單純的那一條：
   `priority(50) > team.task_priority(80)` 不成立
   ＋ 同層 self-replace 要求【相等】⇒ 50 ≠ 80
   ⇒ 落到最後一個 `return false`（`優先序不足`）
⇒ ★★★你那句「求居不是輸，是連比都不比」**結論成立**，
  ★但它**不是**經由 `≥PRIO_THREAT 不介入` 那一段 —— 那一段本窗一次都沒 fire。
  ⇒ ★★這件事有操作後果：**要改的是哪一行，兩者不同。**
```

# ② ★★★你要的那一欄（★而它確實把整件事分成兩個相反的結論）

```
{ "優先序不足.不餓": 41, "優先序不足.餓": 1 }   （餓 ＝ food_days < DESPERATION_DAYS 3.0）
逐筆的 food_days：多數落在 **11–22 天**（最大 22.87），只有 tick=40260 team=41 那一筆是 **1.80**
⇒ ★**擋住求居的不是飢餓，是一個【已經吃飽很久】的覓食任務還佔著 80 的位子。**
⇒ ★★所以 blueprint 那句「覓食憑什麼恆置頂」——**它不只是恆置頂，它是【吃飽了還置頂】**。
```

# ③ ★而 team 65 那一段，是本輪最該看的一格

```
tick 20160 → 37494（**17334 tick ＝ 12 遊戲天**）之間的**每一次取樣**：現任 ＝ `覓食@80`，
food_days 從 5.71 → 22.87 → 慢慢回落到 11.21。
⇒ ★**食物真的有進來**（22.87 天）⇒ 「覓食沒效果」這個假說**被否定**
⇒ ★★所以 interrupt 票 §③ 的前置問題**答案要換個形狀**：
   不是「吃完 food_days 不上升」，**是【需求消失之後那個任務沒有被釋放】**。
⇒ ★★★**誠實限**：這 32 筆是 **cadence 取樣**，不是逐 tick 連續證據 ——
  ★「覓食 episode 從不結束」**還沒被證實**，而**正在量**：
  `.worktrees/interrupt/scripts/debug/interrupt_premeasure_bed.gd`（前置量測 ①）就是答這一格。
```

# ④ 表四：身分在途中被換掉（★母體＝所有 `current_task` 寫入路）

```
逐路徑：{ "try_set": 2 } ｜ 換成什麼：{ "try_set.覓食": 1, "try_set.乞食": 1 }
  tick=38737 team=41 換成 覓食（由 unified，option=覓食）
  tick=43200 team=41 換成 乞食（由 unified，option=乞食）
⇒ ★release／transition 兩條路 **0 筆** ⇒ **換身分的全部是引擎自己**（不是清理、不是外部子系統）
★誠實限：`release()` 拿不到 state ⇒ 那條路的 tick 印 -1（不是 tick 0）
```

# ⑤ ★順手修到一個【儀器裝好沒接電】（★不是這張票的，但它會誤導 convoy 那條線）

```
`_note_convoy_rewrite` 在 `try_set` 的三處是**賦值之後**才呼叫（舊 `task_arbiter.gd:134/155/166`）
⇒ 它的早退條件 `new_task == team.current_task` **恆真** ⇒ ★**那三條路的 convoy 探針從來沒有計過**
  （release／transition 在賦值【之前】呼叫，所以一直活著）
⇒ 已移到賦值之前。★★**陽性對照**：修完這一趟 `convoy.rewrite.try_set = 6`
  —— ★★★而舊 code **在結構上不可能**印出這一列。
```

# ⑥ ★我自己的一顆錯（★先講，因為它會被引用）

```
第一版印出「**覓食=50**」——★那不是世界的事實：我把床裡的字面打成 **U+899A（覚）**而不是 **U+8993（覓）**
⇒ `priority_for("覚食")` 在 REGISTRY 查無 ⇒ 落到預設 `PRIO_DISPATCH` **50**。
★★抓到它的是**標籤自己看起來不對**（印成「食=50」少一個字），**不是數字不對** ——
  ★★★而 50 本身**完全合理**（它就是預設值）⇒ **只看數字的話這一格會被當成真的**。
⇒ 修正後（同床 1440 tick 微跑，★這一行不依賴窗長）：
  **求居=50　覓食=80　收留=50　｜　THREAT 門檻=70　SURVIVAL=80　DISPATCH=50**
```

# ⑦ 這一輪的固定三格

```
★fp = `4b080cea8aa5f8ce97e7ad9ef6822c58` —— 與【加這批探針之前】那一趟**逐字相同** ⇒ 儀器沒改世界
★★>2 秒幀數 = **247 / 43200**（★★★含 contention：measurer 的 `homeless_producer_bed` 同機在跑）
★純觀測：Probe-gated、零 RNG、不進判斷、不寫 state
```
