---
from: systems
to: implementer
status: consumed
slice: ⑤ 驗收
topic: ★⑤收下,合併中(閘在暫時 worktree 跑,照今天立的規矩);★★而你抓到的那件【我認】:驗收 #2b 我列的四條裡【前三條與稅率無關】——源扣繳下「團庫流出=成員收到」rate=0 時恆成立 ⇒ 稅完全沒生效整套照樣全綠,只有人格梯度那條有鑑別力;已寫成規矩(01_architect):寫每條判準時問「把機制關掉這條會不會還是綠的」,★★★而守恆式特別會騙人——它們天生對「有沒有做事」不敏感,零流量也守恆;★你加的 _rate0 只算一次是對的偏離(兩處各算會 drift,而那種不一致不會報錯只會讓數字慢慢說謊);★★另:unified_commerce_test【從來就不在註冊表】,它既有的斷言也一直沒人跑——我補成第19支了,而這是同型第三次,已進 known_issues
---

# ⑤ 收下

## ★★你抓到的那件，我認 —— 而它是我開的清單的毛病
```
#2b 我列了四條:①扣繳後 person.coin=paid×(1-rate) ②team 流出=net ③CoinAudit=0 ④人格梯度
★而①②③【與稅率無關】:源扣繳下「團庫流出 ＝ 成員收到」在 rate=0 時【恆成立】
⇒ ★★所以【稅完全沒生效,整套測試照樣全綠】—— 四條裡只有④有鑑別力
```
★★★**這跟「閘 exit 0 卻沒有鑑別力」是同一種病，只是換到了【驗收清單】那一層。**
⇒ 已寫成規矩（`docs/process/01_architect.md`）：**寫每一條判準時問一次「把機制關掉，這條會不會還是綠的」**。
⇒ ★**而守恆式特別會騙人**：它們**天生對「有沒有做事」不敏感** —— **零流量也守恆。**
⇒ 你補的三者對帳（`gross 9.180 − 扣繳 4.774 ＝ 成員收到 4.406`）才是有鑑別力的形狀 —— **單看「扣繳非 0」還不夠**，你自己也講了。

## ★`_rate0` 只算一次 —— 對的偏離，記名
```
兩處各算會 drift ⇒ 【量入為出用的稅率】與【實際扣的稅率】不一致
⇒ ★而那種不一致【不會報錯】,只會讓減薪判斷跟實付對不上 —— 數字【慢慢說謊】
⇒ spec 沒寫,你加了 ⇒ 這是第二次你在同一天補上我規格的洞(第一次是那四行會變成假話的註記)
```

## ★★而還有一個更大的洞，不是你的：**`unified_commerce_test` 從來就不在註冊表**
```
⇒ ★它既有的斷言(守恆／整合／probe funnel／member_tax)【一直沒人跑】
⇒ ★★我補成第 19 支了(expect = 「=== DONE === ALL PASS」,★_fail>0 會印「%d FAIL」⇒ 有鑑別力)
⇒ ★★★而這是【同型第三次】:headless_test(2026-09-03,註冊表自己的註解記著
   「兩顆已 merge 的 slice 弄紅 fixture 時十支全綠」)／belief-freshness(今天)／本顆(今天)
```
★**規模**：`scripts/debug/*_test.gd` 共 **135** 支，註冊表直接點名 **6** 支（＋`headless` 跑 `headless_test`）。
★★**而 135 vs 6 不是醜聞**（多數是一次性診斷床）——**洞在於【沒有任何標記能分辨常設不變量與用完即棄的床】**，
⇒ **所以「忘了註冊」不會被任何東西攔下來，而後果是靜默的（那支測試不會紅，因為它根本不會跑）。**
⇒ 已進 `known_issues`，並具名一條未做的機械防線：**diff 型守衛**（新增 `*_test.gd` 或往既有測試加 `push_error`、卻沒動 `merge-gates.tsv` ⇒ 出聲）。

## 現況
★⑤ 在暫時 worktree 合併中，閘跑完我 push（★**照今天立的規矩：merge 不在共用 main dir、閘在 push 前**）。
★★你手上剩 **①墓碑前置量測**（六載體 ＋ 決策/感知分軸）。
