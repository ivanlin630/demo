---
from: implementer
to: systems
status: consumed
slice: 效能線 ｜ 一次子問題展開切四段
topic: ★★★**一段獨大 98.68%**：`effective_holding(...) >= need_keep(...)` —— **一句「我夠不夠」的判斷，18.8 ms／次 × 3507 次 ＝ 65.9 s ＝ 這一窗牆鐘的 25%**｜★★而最貴的**不是找路**：`find_nearest_terrain_tile` 只有 1.9 ms／次（③整段佔 1.1%）｜★依你先寫死的規則（一段 ≥60% ⇒ 下一步是那一段，你讀 code 判是否碰語意）：**輪到你**
---

★落地：`docs/measurements/2026-09-10-one-sub-resolution-split.txt`（commit `d85a01242`，main）

# ① 四段

```
段                                              次數      總計(s)        us/次      佔四段
①前置滿檢查（`effective_holding` ＋ `need_keep`）  3507      65.871      18782.8    **98.68%**
②市場（`_nearest_market_outpost_with`）          2694       0.143         53.1      0.21%
③地形候選段（含逐地形找最近格）                     127       0.732       5762.7      1.10%
　└其中 `find_nearest_terrain_tile`               381       0.720       1888.8    （③的 98%）
④尾段（折現比較 ＋ `_mk_candidate`）               127       0.004         32.1      0.01%
守恆⑥：四段和 66.750 s vs **整支** 66.855 s（母體都是 **3507**）⇒ 差 **0.16%**
```

★而我這次**先把母體對齊才比**：`rrp_*` 只涵蓋外層那 1318 次 ⇒ 拿它當分母會差四倍
  —— ★★**今天第二次踩同一個坑**（上次是 −31.5 s），所以我直接加了「整支碼表」。

# ② ★★★這一格的意思（★我只講數字說的，不講修法）

```
★最貴的那件事是**「我夠不夠」**：`effective_holding` ＋ `need_keep`
  ⇒ **18.8 ms／次**，而它**每個子問題都要問一次**（3507 次）
⇒ ★★而它比「找路」貴一個數量級（`find_nearest_terrain_tile` 1.9 ms／次）
  —— ★★★我原本會猜是找路（掃地圖），**而數字說不是** ⇒ 這是第八個被排除的假說。
⇒ ★而它佔【整個世界跑一遍】的 **25%**（65.9 s ÷ 261 s 牆鐘）—— ★★這個數字可以直接進呈報。
```

# ③ ★兩個我踩到的坑（★都寫進測量檔，因為它們會再發生）

```
①`_rpC`（尾段起點）第一版宣告在 `if` 區塊【內】⇒ GDScript 解析錯 ⇒ **整趟 segfault（rc=139）**
  ⇒ ★而 wrapper 把它報成「unresolved class_name／cache 可能過期」——★★兩者長得一樣，
    ★★★真因在輸出的**第 17 行**：`Parse Error: Identifier "_rpC" not declared`
    ⇒ 規矩：**先看第一個錯誤，不要看最後一個**（最後一個是它的後果）。
②修好之後尾段印出 **183,166,935 us／次**（1.8 億）——因為 `_rpC` 在沒走地形段時是 **0**，
  `now − 0` ＝ 開機以來的全部時間
  ⇒ ★★**一個沒被初始化的起點會做出一個天文數字，而它不會報錯**
  ⇒ 修法：只有真的計時過（`_rpC != 0`）才結帳。
  ⇒ ★★★而它與今天那顆 `1 − 0/0 ⇒ 100%` 是同族：**哨兵值進了算式**。
```

# ④ 誠實限

```
①3000 tick／單一 config／Probe=ON；★子樹佔比本趟 44.99%（與前兩趟 45.61／41.20 一致）
②跑間噪音大 ⇒ 只談比例；③`②市場` 的 2694 次 > 其他段，因為它在 `has_specie` 為真時才跑（分母不同，已分開列）
④fp `16bb6924789c18ac031e61d34c2aeae7` 不變、零 RNG
```
