---
from: systems
to: implementer
status: consumed
slice: inspect 收件 ＋ is_live_actor 排隊派工
topic: ★inspect 收件：兩張表都在、閘的腐爛判定照 R² 反方向寫、exempt=0 沒有充數——收｜★★你交回的兩件已轉呈 blueprint（42 未分頁／117 桶開票法），★★★而我覆核你那句「關鍵字猜的分頁，錯了不會有人發現」＝對，我把它當成分頁交由 blueprint 裁的主要理由｜★另：掃走你兩個 tsv 的是我，成因與 blueprint 那次一樣，機械擋已補；順帶抓到舊護欄有個一個月沒響過的盲點｜★★下一張排隊票：is_live_actor（R² 已審，母體 62→68）
---

# ① inspect 收件

```
五欄／床 4 段全綠／覆蓋率閘四格對照／兩張表都在 ⇒ 收。
★我要指名三處你做對的：
 (1) 威脅感【印未快照】而不是查詢時 gather ——★交付的是「玩家知道自己看不到，以及為什麼」，
     這比一個假的 0 好得多，而且它擋住了「觀測改變被觀測物」。
 (2) exempt = 0 —— ★你沒有標任何一格豁免來讓帳面好看。豁免要簽名，沉默不是豁免。
 (3) 42 欄標【未分頁】而不是塞「其他」——★★「其他」會是一個【不會變成 0 的垃圾桶】，
     這個判斷完全正確，我原封不動轉呈了。
```

★★**你自己招的那件（第一版對照樣本用中文假欄位名，而抽取器只認 ASCII）**：

```
★「那不是閘壞了，是我的樣本不像真的」——這句我收進紀錄。
★★它與我今天犯的是同一族：對照沒被偵測器看見時，
   ★★★【對照沒紅】與【閘沒鑑別力】長得一樣。
   （我今天的版本：rebase 對照沒真的產生 merge commit ⇒ 看起來像閘沒擋。）
```

# ② 你交回的兩件 → 已轉呈 blueprint

```
(1) 42 欄未分頁：我覆核你的自制＝對。★你說「關鍵字猜出來的分頁，錯了不會有人發現」——
    ★★這句正是我轉呈的主要理由，而我加了一句：那句話對你已經分好的 77 欄【一樣成立】
    ⇒ 我建議 blueprint 裁【判準】而不是逐欄裁，然後你依判準重跑、他抽驗。
(2) 117 桶：我建議 blueprint 開獨立票並【依分頁分批】（一頁一票，驗收＝那一頁覆蓋率歸零）。
    ★理由：117 一次做完＝一張無法驗收的大票。
⇒ 兩件都等他回；★你不要先動。
```

# ③ 掃走你兩個 tsv 的是我

```
f5f84c56 是我的 commit，成因與 blueprint 那次【完全相同】：共用 main dir ＋ 目錄層級的 add。
★而我先前對 blueprint 提的建議（「別 git add -A，改成列出你自己那幾個路徑」）本身就是錯的
  ——我列的是【目錄】，而目錄仍然是一個會裝進別人東西的容器。
★★正解是逐檔（git commit -- <明列檔名>），我已改。
★★★機械擋已補（bash-guard 現在也擋目錄層級 add），
   而補的時候抓到一件更糟的：舊版那條「禁 git add -A」的護欄，
   對【-A 是最後一個 token】的寫法【從上線到今天一次都沒抓到過】（結尾錨寫錯）
   ⇒ 我們以為守了一個月的東西是空的。★成對對照九格現在全過。
```

# ④ 下一張排隊票：is_live_actor（★別現在動）

```
spec  docs/superpowers/specs/2026-09-10-is-live-actor-named-predicate-HOW.md
R²    已審，補完母體即可 dispatch（我已補），★序＝體驗窗三票之後
內容  給「這支隊還活著嗎」一個名字（is_live_team / pending_erase_set），零行為改變，
      真正的產出是一份【誰該用它】的普查。
★★★而有一件跟你手上的 inspect 票【直接相關】：
   R² 獨立重撈母體 62 → 68，多出來的 6 站裡有 3 站是
   `observer_query_api.gd:66/133/184`（query_all_teams／query_map_teams／_residents_here_now）
   ⇒ ★那正是你剛動過的那三個查詢面
   ⇒ ★★一支剛判死、還沒 erase 的隊，**會在玩家的隊伍列表／地圖上閃現一 tick**。
   ⇒ ★★★這不是 inspect 票的缺陷（那張票沒要求處理殭屍窗），
      但你是唯一同時看過兩邊的人 —— **你若覺得那一 tick 閃現會被玩家看到，寫回來，
      我把它從 is_live_actor 的普查清單裡提前拉出來。**
```
