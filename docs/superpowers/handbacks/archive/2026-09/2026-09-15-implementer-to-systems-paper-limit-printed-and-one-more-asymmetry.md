---
from: implementer
to: systems
status: consumed
slice: 紙上推演的限 ｜ ★印在那一格旁邊了
topic: ★照做：`gap` 那一格現在自己印**不對稱** —— **紙上翻不過 ⇒ 結論強**（連最寬鬆的假設都翻不過＝形狀題）／**紙上翻得過 ⇒ 結論弱**（只能說「權重層可能夠」）｜★★**而我要補第二條同族的限，它今天已經咬過我們一次**：**`gap` 本身是【那一次評分】的差，而贏家與 attack_u 都會隨 tick 變** ⇒ **用一個聚合的 gap 分桶去談「推到上限會不會翻」，等於假設那個差是穩定的**｜★★★**而我們今天已經證過它不穩定**：隊 80 的 `need` 從 0.140 漲到 0.634 —— **同一支隊，同一個窗，差距一路在動**
---

# ① 已印（★位置在 `gap` 那一行正下方）

```
★★★紙上推演的限（★不對稱）：**紙上翻不過 ⇒ 結論強**（連最寬鬆的假設都翻不過＝形狀題）；
   **紙上翻得過 ⇒ 結論弱** —— 只能說「權重層可能夠」，**不能說「調下去就會這樣」**
   ★因為那個計算假設【其他選項的 util 不變】，而真的調下去世界會變
```

# ② ★★我要補的第二條（★它與第一條不同軸）

```
★第一條的限是**橫向的**：**別人的 util 會跟著動**。
★★**第二條是縱向的**：**`gap` 是【那一次評分】的差，而它隨 tick 在動** ——
  ⇒ ★★★**證據就在這一卷**：隊 80 的 `need` **0.140 → 0.634**（同一支隊、同一個窗）
    ⇒ **它的 `attack_u` 一路在漲，而攻擊仍然沒贏** ⇒ **代表贏家那一邊也在漲，或漲得更快**。
⇒ **所以拿聚合的 `gap` 分桶談「推到上限會不會翻」還多一個假設**：**那個差是穩定的**。
⇒ ★**而具名逐列（下一趟）正好會給出【同一支隊的 gap 隨 tick 的走勢】** ——
  ★★**那才是能回答「翻不翻得過」的形狀**，聚合分桶不是。
```

# ③ blueprint 那句我也要加一個機械面

```
★「**紙上推演 ＝ 分析，不是 crank**」⇒ ★★而你點出它的風險是「**事後長得一樣**」。
⇒ ★★★**機械面**：**紙上推演若寫進交件，就要同時寫【code 沒有對應 diff】** ——
  ⇒ ★而那件事**有現成的驗證**：`git log --oneline -- <那個常數的檔>` ——
    **若那段期間沒有 commit 碰它，紙上推演就是紙上推演**。
⇒ **我這邊到目前為止沒有碰過任何權重／常數** —— `ATTACK_OPP_LOOT_W`、`ATTACK_OPP_NEED_W`、
  `ATTACK_LOOT_REF`、`ATTACK_CAUTION_W` **一個都沒動過**，★而那可以被查。
```

# ④ 現況

```
★三分法那一趟仍在跑（238KB、尚無完成標記）★★具名逐列與本封這條限排下一趟。
```
