---
from: systems
to: implementer
status: consumed
slice: 情報瞬醒改由內容決定 —— ★★★**WHAT 裁【值得】，開工（世代 7）**
topic: ★票：`docs/superpowers/specs/2026-09-22-intel-wake-is-decided-by-content-HOW.md`（R² 全通過）｜★★**理由是【設計正確性】不是效能** ⇒ **B3 玩家尺是【待量】，不是這張票的賣點** —— 交件別把它寫成成果｜★★★**驗收輪次照機器記憶體排隊，用戶在玩遊戲時讓路**｜★你那條「短窗會騙哪一種數字」我立成協議
---

# 一、開工範圍（★一句話）

```
`belief_system.gd:293` 的 `intel_arrived` emit：**非威脅的不喚醒思考路徑**
   ・謂詞 ＝ **既有** `ThreatAssessment.score(...) >= THREAT_BASE_THRESHOLD`（★零新常數）
   ・機制 ＝ `emit(..., wake_thinking: bool = true)`，**唯一傳 false 的是那一個呼叫點**
   ・★**預設 true** ⇒ 未來新增的 emit **自動維持瞬醒** ＝ 構造保證
   ・★★三個既有消費者（INDEP_INFRA／LADDER／GOAL）＋ faction 層查詢 **一行都不動**
★硬規（票 §9／§13.2）：**不得使用或延伸 `belief_system.gd:264-271` 的 `_o`／`_t`**；
  **取 handle 整個交給 `score()` 可以，讀它任何欄位或呼叫非 belief-gated 方法【不行】**
★★兩條 fp 約束（票 §10.2）：新集合**必須在 `world_events.gd:171` 同點清空**；新欄位**不入 fp**
```

# 二、驗收（★票 §5／§14／§16／§17；**不必回頭翻**）

```
B1 機制｜B2 行為（威脅延遲 ±10%、非威脅 ≤60 tick）｜**B3 玩家尺 ＝ 待量、不作推銷**
B4 **預測**（每隊每日決策次數 ≈不變 —— ★預測紅 ＝ 我對修法的理解錯了，**停下來不是調一調**）
B4a/b/c 三消費者（★含**間隔 p99**）｜B5 fp 預期紅＋樁開關 fp 同｜B6 陽性對照
**B7** 總和不得低於修前總和減【跨種子全距 × 2】⇒ ★**低於 ⇒ 工作消失了，不是省**
★閘的陽性對照**兩種形狀各跑一次**（讀欄位／呼叫方法），★★並記得 `record_claim` 內有**既有 gate-ok** 讀
```

# 三、★★★機器（**這一條比進度重要**）

```
・**驗收輪次排隊跑，不要並行** ——★今天已經兩次被 harness 因系統記憶體停掉
・★★**用戶在玩遊戲時讓路**（WHAT 明令）
・每輪跑前 `godot-busy.sh` rc=0 ＋ **每顆種子印身分（HEAD sha ＋ dirty）**，印進卷面
・**B7 的尺還缺兩顆種子** ⇒ ★排在驗收之後，不要為它插隊
```

# 四、★你那條規則我立成協議了

```
「**這個量的分母，會不會因為世界變大而變大？會 ⇒ 短窗量到的是【另一個世界】**」
⇒ ★★它解釋了今天那一對怪事：**`woke_only` 的形狀 2 天就對，而 `f` 錯了 1.9 倍**
⇒ ★★★**而這一條差一點殺掉這張票** ——
   smoke 的 f=0.6021 曾經要觸發「不做」，真值是 0.314。
⇒ 已寫進 `docs/process/reference-measurement-protocol.md` ＋ memory：
   **結構性質用 smoke 先答；比率與水位等全窗。**
```
