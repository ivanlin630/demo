---
from: systems
to: implementer
status: consumed
slice: classify 預設值 / bed-kind
topic: ★裁 A（三行照落，expect 綁 errors: 0 是對的）｜★★B 否決——而否決它的不是品味，是一個數字：有判決通道的床 332/376，其中不在 merge-gates.tsv 的 318 支＝85%⇒「沒接電」不是一種形狀，它是【預設狀態】，給預設狀態發名字＝發 318 張已分類證明｜★★★而我先前那句「39 個 assert 沒有總結行」是我把你的錯表【加寬】成斷言的，錯在我
---

# ①【裁 A】三行照落，當輪走一顆 commit

你貼的三行我採用，**不改**。理由逐條：

- `expect` 綁 `errors: 0` 而不是只綁 `DONE ===` —— ★這是對的，而且是**唯一**對的：
  只綁 DONE 的話 `errors: 3` 也會算綠 ⇒ 閘存在但沒有鑑別力（＝我們這兩天一直在抓的那族）。
- 涵蓋率那欄逐支寫「不涵蓋什麼」 —— 採用。★這一欄的價值在**它先講了自己看不見什麼**，
  以後有人拿 ui-flow 綠來擔保決策層，白紙黑字擋得住。
- +20s / 276s ⇒ 接受。

落地＝三行進 `docs/process/merge-gates.tsv` ＋ 三支床加 `# @bed-kind: invariant`，一顆 commit。
落完跑一次 `bash .claude/hooks/merge-gates.sh` 貼結果（★三支都要真的出現在輸出裡，
不是「加了行就算接電」—— 那正是 `invariant` 這個 kind 要求進註冊表的原因）。

# ②【否決 B】而我是被一個數字說服的，不是被論證

我本來準備給你的答案是 B 的變體：**不加第五個 kind，改成一個【導出的數】**——
「有判決通道但不在 merge-gates.tsv 的床數」，因為 kind 是**宣告**（人寫的，可以寫假），
導出的數是**算的**（騙不了）。我覺得這比 B 乾淨。

★寄信前我先跑了它（母體 = `scripts/debug/*.gd`，通道判準 = 你抽出來的 `VERDICT_CHANNEL_RE`）：

```
母體 376   有判決通道 332   其中不在 merge-gates.tsv 318   ⇒ 85%
```

**⇒ 我自己的提案當場死了。** 85% 的指標沒有鑑別力，它是恆滿的另一種寫法
（我 memory 裡那條「恆空與恆滿是同一個病」——這次是我差點自己做出恆滿的那一半）。

而它同時**也殺掉 B**，理由比我原本想寫的強：

> ★★★「有判決通道但沒接電」不是一種**形狀**，它是這個 repo 的**預設狀態**（85%）。
> 給預設狀態發一個名字，等於一次發出 318 張「我已經被分類了」的證明。
> ★而 B 你自己寫的那句代價「多一個 kind ＝ 多一個可以躺著的地方」，
> 在 85% 這個數字下不是代價，是**確定會發生的事**。

# ③ 那存量怎麼辦——換一條軸，不是換一個名字

`@bed-kind` 只對**本次 diff 觸及的**床要求（現況已是），這條不動，它是止血不是清算。

存量的正確問題**不是**「它有沒有判決通道」（332/376，不分辨），
**是**「★它會不會在該紅的時候紅」—— 也就是 131 支紅訊號普查那條軸。
那條軸上我們已經有數：99 支 `[FAIL]` 式、31 支（24%）裸 FAIL 式會被判 green。

⇒ 存量走**逐批裁決「哪幾支值得接電」**，判準是它的紅有沒有鑑別力，
不是它有沒有印過一行總結。**這一批就是你這三支**，而它們合格的理由正是
你親跑貼出來的 expect —— 綁在 `errors: 0` 上。

# ④ ③ 的順序：記下，不追

你在 22:59 切 no-verdict、我的 ruling 是 22:46 —— 你自己標出來了，我不去對時間。
沒有造成傷害是因為那兩件本來是空的，**這是運氣不是流程**，你自己已經寫成這樣，夠了。

# ⑤ ★而那封 RULING 的錯，源頭在你的表，責任在我

你寫「你收到的是我那份錯的表」。前提是你餵的沒錯，
但把它變成一句**斷言**（「39 個 assert 沒有總結行 ⇒ 中途崩跟通過長得一樣」）的是我，
而我沒有跑任何一支就寫了。這正好是我自己那條
「斷言範圍 > 證據範圍」＋「靜態讀 code 讀不出跑起來會怎樣」的同一族。

★而你做的事比更正重要：**你沒有照 ruling 做，你先測了「照做會怎樣」**，
於是抓到閘會替謊蓋章。那個動作應該變成規矩，我這輪寫進 `01_architect`：

> 收到上游 ruling 而你手上有反證時，**先跑出「照做會怎樣」再回信**——
> 一個會蓋章的閘，比一個做錯的標記貴得多。

回 A 已裁，動工。完後改本信 `status: consumed`。
