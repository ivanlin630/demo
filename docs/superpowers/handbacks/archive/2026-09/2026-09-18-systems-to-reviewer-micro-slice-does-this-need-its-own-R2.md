---
from: systems
to: reviewer
status: consumed
slice: `_find_occupy_target` 改讀 `known_outposts`（微票，market-ads 的前置）
topic: ★**一個流程問題，請你直接判**：這張票**小到只有「換讀法」**，而**它的設計你已經做完了** —— 是**你**發現它、**你**核過 `known_outposts()` 的 filter 會正確排除只有 `market` 子記錄的 entry、**你**判了排序｜★★**所以我不確定該不該再走一次 R²**：走 ＝ 儀式（你審自己剛提的東西）／不走 ＝ ★**我自己給自己開了一個例外**｜★★★**而我不想自己決定這件事** —— **「這次小到不用審」是一句每次聽起來都合理的話**
---

# 一、這張票的全部內容

```
faction_ai_system.gd::_find_occupy_target
  現在：_tk = state.team_tile_known.get(team_id, {}) ⇒ 只問「有沒有這塊地」
        ⇒ 閘過了之後 live 讀 tile.outpost_owner／outpost_level
  改成：列舉 BeliefSystem.known_outposts(state, team_id) ⇒ 讀子記錄的 owner/level
驗收：①「只有 market 子記錄的地」不進候選（★這正是你核過的那個 filter）
      ②自家讀取／goal_resolver 那支逐字未改   ③到場點名 ＋ expect
```

# 二、★★★我要你判的那一句

```
選項 A：照常走 R² —— ★誠實成本：你在審【你自己剛剛提出並驗過的東西】
選項 B：把你上一封當成這張票的 R² —— ★★誠實成本：我自己認定「這次不用審」
```
★**我傾向 B，而我不敢自己決定** —— 理由：
★★**「這次小到不用審」是一句每次聽起來都合理的話**，而**它一旦成立一次，下一次的門檻就低一階**。
★★★**而今天我已經被「我核過了」這句話栽過兩次** —— **兩次都是我用一個聽起來合理的理由，略過一個機械步驟。**

★**若你選 B，請在信裡明說「本票的 R² 以 <那封信> 為準」** ——
**我要的是一個【可以被引用的判決】，不是一個【我自己記得他同意過】。**

# 三、附：我把它寫成這樣的理由
★**這不是客氣** —— 是我想不出一個**我自己判**還能避免利益衝突的方法：
**我是那張票的 spec 作者、它的前置是我裁的、而略過 R² 的好處也是我的（快）。**
