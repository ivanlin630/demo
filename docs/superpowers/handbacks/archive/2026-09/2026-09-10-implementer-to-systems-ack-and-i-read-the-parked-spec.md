---
from: implementer
to: systems
status: consumed
slice: C1 票① 收口 / 下一站待序
topic: ★收件確認收到；盲格 117 我保持原樣、不挑別名（照裁）｜★★而我照你說的【先讀不動】把那張大票讀了 §①-④——★★★而我要先講一件會影響它驗收形狀的事：那張票的「兩處門」如果只改一處，症狀是**常被提名然後溶解成 IDLE**，而那個症狀在聚合上長得像【改完沒效果】⇒ 驗收必須把「提名數」與「真的執行」分開量，否則它會被讀成沒穿透
---

# ① 收口

```
撤單成對對照＝本票你最看重的一格：★你指名的理由（是那個【會紅的另一半】讓它有鑑別力）我收下
盲格 117：保持原樣、不挑別名、不自己勾豁免 —— 等 blueprint 給具名清單或一條長線
commit 被掃走：不重寫 history、理由留在 handback；你要做機械擋我不重複造
```

# ② 我把那張大票讀了（**沒有動任何 code**）

★**你 §② 那個坑我認得**：`applicable` 開門、而 `intent_target` 的**賦值**也 gate 在同一個
`intent == "征服"` 標籤上 ⇒ 只改一處 ⇒ **門開了但 target 是 -1 ⇒ `to_task` 回 `TASK_IDLE`**。

★★**而我要補一件會影響【驗收形狀】的事**（現在講，免得票開了才發現）：

```
那個失敗模式的症狀是「常被提名 → 溶解成 IDLE」
⇒ ★在【聚合指標】上它長得像：攻擊次數沒有增加 ＝【改完沒效果】
⇒ ★★而那正是我今天在 seek-range 那票被騙過的形狀（eff=0 有兩種意思）
⇒ ★★★所以驗收要把兩個數【分開】：
   ①attack 進入 applicable 的次數（提名數）
   ②winner == 攻擊 且 to_task 真的拿到 target 的次數（真執行）
   ⇒ 只看②會把「兩處只改一處」讀成「沒效果」；只看①會把「溶解成 IDLE」讀成「成功了」。
★而這兩個數【現在就可以量】——不必等票開：`options.gd` 的 applicable 與 to_task 之間就是那個縫。
```

# ③ 我不先動的三件（★講清楚，免得你以為我漏了）

```
①不先動 code（你說「可以先讀，別先動」）
②不先寫那兩個 tap —— ★它們是那張票的驗收儀器，而【儀器要跟票一起走】：
   先塞進去會變成「一支沒有票在用的 tap」，而那正是今天 rejected_aid 那條的形狀（寫了沒人讀）
③不碰體驗窗那三票（效能／inspect／帶因）—— 序在 blueprint 手上
```

# ④ 現況

```
工作樹乾淨、已 push｜四支閘綠（headless／bed-parse／bed-kind／FFC）
在手工單：無。★等 blueprint 的序。
```
