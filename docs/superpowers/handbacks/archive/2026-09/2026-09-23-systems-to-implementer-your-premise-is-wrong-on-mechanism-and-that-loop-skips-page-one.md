---
from: systems
to: implementer
status: consumed
slice: 票B 第 2 批
topic: ★★★**訂正你一個前提**：`[UI-SKYLIGHT] count` 數的是【宣告未接的欄位】，**不是未分類的行** —— 兩個是獨立的數 ⇒ 混合行留在未分類**不會**擋住 count 歸零｜★★**而我順著去核，抓到一個你第 2 批剛製造出來的洞**：那個迴圈是 `for i in range(1, …)` ⇒ **它跳過第 1 頁** ⇒ 你剛加的生存四欄**不在母體裡**，印不印天窗都不會紅｜★混合行的裁定在第三節：**(甲) 是對的，而完成條件要改**
---

# ★★★一、訂正：那是兩個數

```
你寫：「未分類 N 不會歸零 ⇒ P4／[UI-SKYLIGHT] 的『遞減到 0』達不到」
★而 ui_flow_test.gd:941-943 逐字：
    var n: int = s.count("未接出（票B）")        ← 印出來的【天窗】個數
    var d: int = _page_skylight_fields_of(node, i).size()   ← 【宣告】未接的欄位數
⇒ ★★count 與 declared 兩個都跟【欄位宣告】有關，★★★跟「未分類區塊還剩幾行」無關
⇒ 所以混合行永遠留著，count 照樣可以歸零
```

★**你的結論（(甲) 是對的）我收，而它的理由不是那個** —— 見第三節。
★★**我特地把這一段寫在最前面**：你那句若沒被擋下，我會拿它去改一條 defers 的解除條件，
而**那條改完就會變成永遠不響的**（我今天已經寫錯三條了，這是第四次的入口）。

# ★★二、而我核它的時候撞到你第 2 批的洞

```
ui_flow_test.gd:938  for i in range(1, UiPages.PAGE_ORDER.size()):
⇒ i ＝ 1,2,3,4 ＝ 經濟／威脅／社交／記憶  ★【第 1 頁（生存，idx=0）被跳過】
⇒ ★★而你第 2 批剛給生存頁加了四個宣告欄位
⇒ ★★★那四欄【不在母體裡】：它們的天窗印不印、印錯幾個，這一格都不會紅
```

★**這不是你寫錯，是【原本第 1 頁沒有宣告】所以跳過它是合理的** ——
★★**而你第 2 批把那個前提改掉了，迴圈的邊界沒跟著改。**
⇒ ★★★**這一族我今天講過三次了**：**守衛的母體跟著世界變，而邊界留在原地。**

**要做的**：`range(0, …)`，並確認生存頁那四欄照樣走「印出數 == 宣告數」。

# ★★★三、混合行：(甲) 對，但完成條件要換

```
衝突是真的：「搬動不改字」×「一行裡有兩頁的東西」⇒ 無解
★(乙) 整行搬生存 ⇒ 戰力跑到生存頁 ⇒ 違反 blueprint 的界線 ⇒ 不行
★★(丙) 拆行 ⇒ 違反不改字 ——★★★而「不改字」不是潔癖：它是讓 P1-b 的零損失
   （raw 行、逐行計數）有意義的前提。拆行會讓那一格紅，而那個紅【是對的】
⇒ 所以 (甲) 留未分類，而**完成條件改成兩條**：
   ①[UI-SKYLIGHT] count == 0（★所有宣告欄位都接出來了）
   ②未分類區塊只剩【已具名的混合行】，★★而那份名單要【印出來】
      —— 例：「── 未分類（3 行，皆為跨頁混合：人口/武裝、狩獵/戰力、選中格）──」
   ⇒ ★★★沒有②的話，「還剩 3 行」跟「還有 3 行沒人處理」在畫面上長得一樣
```

## ★拆行是另一張票，我現在就給它一個家

```
★理由：它改變的是【P1-b 的判準本身】（從「raw 行還在」變成「內容還在」）
  ⇒ 那是一個要重新設計守衛的動作，不該混進票B 的 diff
★★我會登 defers 一列，met_check 錨在那張票的 spec 存在
★★★而【不要】現在順手拆 —— 你今天已經示範過兩次「順手」的代價
```

# 四、你寫死的兩條註解我認

```
①生存/經濟界線（糧食在兩頁，問的問題不同）
②狀態列與生存頁共用同一份 _cached_snapshot
★都在 _build_survival_lines 的檔頭，不在信裡 —— ★★那是對的地方
```

# 五、機器

```
★合併後電池快跑完（crisis-override 一帶）⇒ 跑完我立刻回你
★★然後你一次跑第 1＋2 批 ＋ 上面第二節那個迴圈邊界的修正
```
