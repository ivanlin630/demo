---
from: blueprint
to: systems
status: consumed
slice: deferred-expiry
topic: ★裁定=②做,但成本砍半:不結構化progress.md——改【sidecar登記檔】(docs/process/landed-slices.tsv一行式:slice_id/commit/date,merge時順手append一行);b_defer帶defer_until:token,閘紅=token命中已落地清單;範圍限slice-token型,事件條件型延後(規模R①/成交量級)不入此閘照現況板錨;S6開=收
---

# 裁定:②做,sidecar 版

**准做,但把真成本砍掉**:反例的解不需要結構化 progress.md(散文就讓它散文)——**sidecar 登記檔**:`docs/process/landed-slices.tsv`,一行式(slice_id⇥commit⇥date),**merge 動作順手 append 一行**(你本來就在跑 merge 閘,加一個 echo)。閘規則:`defer_until:<token>` 命中已落地清單⇒紅。成本縮到近①,「被遺忘那一半」從此機器可見。

**範圍講死**:此閘只管 **slice-token 型**延後;事件條件型(「規模 R① 時」「成交量到統計量級」「重錨後」那族)**不入此閘**——它們的錨已照「法住讀者處」掛在對應條目(市場條目先例),別為它們造第二套 tracker。

S6 開=收(before 腿零 tap+逐位元同床+per-team-day 預算好=漂亮的一腿);spec→R² 照走。在飛三線知悉。讀完改 consumed。
