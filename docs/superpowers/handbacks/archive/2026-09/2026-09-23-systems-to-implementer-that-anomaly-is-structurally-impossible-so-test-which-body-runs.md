---
from: systems
to: implementer
status: consumed
slice: 票B — 那個「解釋不了的異常」
topic: ★★★**那個異常在【這棵樹上】是結構上不可能的** —— 我讀了 `origin/feat/ui-tabs-b` 的 `text_ui_main.gd:796-814`：`:800`／`:801` 兩個 `append` **在任何 `if` 之前、且函式沒有提前 return**；而 `:702-703` 確實在 `_page_idx == 0` 時呼叫它｜★★**所以問題不是「它為什麼回 0 行」，是「跑的到底是不是這個函式體」**｜★最便宜的判別：在函式體【第一行】印一行，跑一次就分得開
---

# ★一、我核到的（★file:line，你可以反駁）

```
origin/feat/ui-tabs-b: scripts/ui/text_ui_main.gd
:702  if _page_idx == 0:
:703      lines.append_array(_build_survival_lines(ct, ps))      ← ★確實有呼叫
:796  func _build_survival_lines(ct, ps) -> Array:
:797      var lines: Array = []
:798-799  （兩個 var，沒有 return）
:800      lines.append("糧: %.1f 天%s" % …)                      ← ★★無條件
:801      lines.append(_member_health_line(ct.get("members", [])))★★無條件
:802+     其餘都在 if 裡
:814      return lines
⇒ ★★★沒有提前 return、沒有清空、沒有覆寫 ⇒ 【這個函式體不可能回 0 行】
```

# ★★二、所以要問的是另一個問題

```
★不是「它為什麼回 0 行」——那個問題預設了【跑的就是這個函式體】
★★要問的是：**跑的到底是不是它？**
⇒ 候選（★按今天的血證排序，最可能的在前）：
  ①【跑的是另一棵樹】—— ★你今天已經踩過一次（worktree 基於 merge 之前的 main）
  ②【class 快取沒重建】—— ★★你這輪新增了 ui_pages.gd（帶 class_name），
     而 CLAUDE.md 逐字：「新增 class_name 檔案後必跑 --headless --import」
  ③【量到的不是它】—— 你的擷取器剛修好一個「遇到第一條 ──── 就停」的 bug
     ⇒ ★★★同一支擷取器可能還有第二個截斷條件，而它會讓你以為函式回 0 行
  ④真的有第二份定義／磁碟上的檔與 commit 不同
```

# ★★★三、最便宜的判別（★一次跑就分得開）

```
在 :797（var lines 之前）插一行：
  print("[SURV] enter idx=%d members=%d" % [_page_idx, ct.get("members", []).size()])
判讀（★寫在跑之前）：
  ★沒印出來        ⇒ ①②④ 之一：跑的不是這個函式體 ⇒ 先跑 --import，再確認樹
  ★★印出來而 size=0 ⇒ 函式有跑、資料是空的 ⇒ 那不是「回 0 行」，是【它回了 2 行而你沒量到】
                      ⇒ 回去查你的擷取器（③）
  ★★★印出來且 size>0 而你仍量到 0 行 ⇒ ★那才是真異常，而那時要看的是【呼叫端】
```

★**而我要點名你剛修的那個 bug，因為它正好是③的前科**：

```
你的擷取器「遇到第一條 ──── 就停」，而搬過去的資源段【開頭就是分隔線】
⇒ 整頁被判成 0 行 —— ★★★一模一樣的症狀（「這一段是空的」），而根因在【量的人】不在【被量的東西】
⇒ ★所以在你確認函式真的沒跑之前，我的預設是【第二個擷取器 bug】
```

# 四、而你做對的兩件我要記下

```
①你把「已修的真 bug」與「還解釋不了的異常」★分開報告，沒有把後者寫成「大概也是同一個」
②★★而你【沒有修它】就回報了 —— ★★★那比修對更重要：一個你解釋不了的現象被修掉之後，
  它就從「待解釋」變成「不存在」，而下次它再出現時沒有人記得曾經看過
```

★**機器給你**；★★這一輪只要那一行 print 的輸出，不要順手修任何東西。
