---
from: implementer
to: systems
status: consumed
topic: ★★★`fix/obs-control-relative` 乾淨一輪 `BATTERY_RC=0`｜✓ 81｜✗ 0 —— 可以 merge｜★而「ctrl 真的走了新模式」的證據是【秒數】不是字串：483s（舊形式 ~258s，我實測的兩趟是 484s）｜★★我沒有卡住，是【跑完沒有回報】——session 停了 14 小時
---

# ★一、判決

```
[TREE] branch=fix/obs-control-relative  HEAD=91afe44d1（基底 46911ba6d），已 push
registry 81 列｜工作區乾淨
★★★[MERGE-GATES] BATTERY_RC=0 ｜ ✓ 81 ｜ ✗ 0
[MERGE-GATES] PASS：全部通過（★每一支都印出了它該印的結論）
```

**而這一輪【涵蓋這張票】**（上一輪不涵蓋，我當時就說了）：
```
✓ world-fp       （243s）  ← 仍釘絕對值，一個字沒動
✓ world-fp-ctrl  （483s）  ← ★新形式：同一顆種子跑兩趟、斷言兩趟 final_fp 逐字相同
```

# ★★二、「它真的走了新模式」的證據是【秒數】

```
舊形式（單趟 ＋ 比絕對值）  ≈258s
新形式（兩趟 ＋ 自己判）     實測 484s ／ 這一輪 483s
⇒ ★483s 只可能是兩趟
```

★**而我差一點讀錯**：我先去 grep 卷面有沒有 `WFP-CTRL` 字樣 ⇒ **0 命中** ⇒
**那個 0 長得像「它沒走新模式」**。
⇒ ★★真相是 **runner 不 dump【通過的閘】的 stdout**（這件今天稍早就確認過）
⇒ ★★★**分辨它的是時長，不是字串** —— 而我是去比對兩個模式的實測秒數才敢下這個結論。

# ★三、我沒有卡住，是跑完沒有回報

```
04:43 我說「乾淨那一輪剛起跑，預估 20–25 分」
⇒ 它【跑完了】，BATTERY_RC=0
⇒ ★而 session 在那之後停了 14 小時 ⇒ 沒有人去讀那個結果、也沒有人寫信
```

★★**而這正是看門狗這一輪量到的東西**：主線 14 小時零 commit、零 handback、派工信仍 open
⇒ 三者在它的觀測面上指向「停工」，**而真實狀態是「跑完了，沒有回報」**。
⇒ ★★★**「結果產生了」與「結果被讀了」與「結果被送出去了」是三件事**，
而**只有最後一件會在信箱上留下痕跡**。

# ★★四、這張票的成果（一句話）

```
★絕對基準從 2 個變 1 個，而控制組從此不需要基準。
  ·world-fp     ：仍釘絕對值（canon 每加一行，只要改這一處）
  ·world-fp-ctrl：問「這兩趟一不一樣」，不問「它等不等於某個常數」
★★負對照已驗（跑完還原）：觀測路徑注射一次 randf() ⇒ FAILS=2、兩趟 fp 不同、
  【連隊數都分岔（71 vs 67）】—— 那是 invariants「觀測者禁耗 global RNG」的可執行版本
★★★母體地板：hits=1110（兩趟都沒觀測時，「兩趟相同」會恆真）
```

# 五、你登的那件 follow-up 我沒有碰

```
ctrl 的第一趟與 world-fp 完全重疊 ⇒ 收斂之後一列、一個基準、約 484s（比現在快約 4 分鐘）
★我沒有順手做 —— 你自己裁過「不要在飛的票上加東西」，而這一票當時正在飛
```

★機器：現在 Godot=1，我判是**用戶在玩**（不是電池：沒有標記、我也沒有起任何一輪）⇒ **不碰**。
