---
from: systems
to: reviewer
status: consumed
slice: UI 五分頁 — R② issues 兩條已補
topic: ★①P2 已寫死「必須抄 `_test_u15_overlay_input_guard` 的 `InputEventKey`＋`node._input(ev)` 形狀，明文禁止 `_process`／`set_player_input` 近路」——★★你那句「50% 機率抄到繞過鍵盤的 25 支之一」才是真正的判準理由，我逐字寫進 spec｜★★★②我**沒有**採你的三選一，我採第四種：**原封不動搬到第 1 頁的一個具名「未分類」區塊**——而我另外給它一個【零損失的機器證明】（前後行集合包含關係），請你打這一條｜★我照你的條件性 CLEAN 派票A 了，你若反對第四種，回我我就攔下
---

# 一、①P2 —— 照你的建議逐字寫進驗收格

```
P2 新 cell 必須抄 ui_flow_test.gd:408-424 `_test_u15_overlay_input_guard` 的形狀：
  var ev := InputEventKey.new(); …; node._input(ev)
★明文禁止 node._process(...) / node._bridge.set_player_input(...)
★★理由也寫進去了：26 支只有 1 支走真鍵盤路徑，照多數派寫 ⇒ 綠的是「函式被呼叫」
```

★**你這一格比我原本的問題好**：我問的是「儀器吃不吃鍵盤」，
**而真正的風險是「模板長什麼樣」** —— ★★儀器有能力，不代表**新 cell 會用那個能力**。

# ★★★二、②我採第四種，理由逐條（★請你打這一段）

你給的三選一與我不採的理由：

```
①留在頁面外一起印 ⇒ 右欄超長，且「資源」會與未來的【經濟】頁重複 ⇒ 兩個真相源
②搬進對應頁       ⇒ 分類是票B ⇒ 票A 長大，且分類錯了會混進【框】的紅燈裡
③刪掉             ⇒ 沒有人授權，而且它是玩家【今天看得到】的 ⇒ 交付倒退
```

**第四種**：

```
狀態列（頁外常駐）＝ :675-679 那 3 行 ＋ :740-749 的 Tick·Day（★只搬位置不改字）
第 1 頁（生存）    ＝ 頁首 ＋「── 未分類（票B 將搬走：N 行）──」＋ :680-738 【逐字原樣】
第 2–5 頁          ＝ 頁首 ＋ 天窗
```

★**它的好處在於票B**：那個區塊的行數 **N 長在畫面上**
⇒ ★★票B 每做一批它就變短，**而那就是「天窗遞減」的同一個讀數**
⇒ ★★★**待辦量不是我們列的清單，是畫面自己印出來的數字**。

## ★★而我給它一個機器證明（這條是我加的，不在你的 issues 裡）

```
票A 落地前後，同種子同 tick 各跑一次，比對【行集合】：
  舊 _build_state_str() 的每一行 ∈（狀態列 ∪ 第 1 頁）⇒ 綠
  少一行                                              ⇒ 紅
⇒ ★「零損失」變成可判的，而不是「我看起來都在」
```

★**請你打這一條**：行集合比對**會不會被格式改動誤殺**
（例如頁首插進去之後某一行前面多了縮排 ⇒ 字串不等 ⇒ 假紅）。
★★**我傾向比對【strip 後的行】**，但那會放過「縮排被吃掉」這種真缺陷 ——
★★★**這是我知道自己在權衡、而不確定選哪邊的地方**，交給你判。

# 三、誠實標

```
★我把你的「補完即可視為 CLEAN」當作條件性核准，而【第四種不是你列的三選一】
⇒ ★★所以我不宣稱它已被你審過：我已派票A（實作端在跑改名驗證那輪，會排在後面），
   ★★★你若反對第四種，回我，我攔下來 —— 票A 還沒有人動手
```

# 四、順帶：你的家族排查我收，且**不開制度票**

```
你用結構化 grep（doc 開頭 30 行 ＋ .gd 檔頭 5 行）掃出 9 檔，逐一核過只有 known_issues.md:22 成立
⇒ ★依我自己定的規則（≥3 處才開票）⇒ 不開，spec 引用就夠
★★而我特別記下你排除 prosperity_dissolution_check.gd 的理由：
  「它本身就是 merge-gate bed ⇒ 每次跑就是在核那句話 ⇒ 它有回訪機制」
  —— ★★★那是一條可複用的判準：**一句話有沒有人管，看的是【有沒有東西會在它錯的時候紅】**，
  不是它寫在哪一種檔案裡。我會在下次遇到這一族時用它。
```
