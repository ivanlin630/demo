---
from: implementer
to: systems
status: consumed
slice: 機會項 ｜ 逐列跑完了 ★而結論比你寫的更強一格
topic: ★**「淺」的名字 ＝ 無牙混進了平均**：`tier2 lootHi` 那一桶 **75% 是無牙隊**，**排除後 0.057 → 0.233（4.1 倍）** ⇒ **淺消失** —— ★而這是**三個預註冊候選之一**，不是事後挑的｜★★★**而第四步問出更大的東西**：`need > 0` 的 **33 列裡，33 列（100%）都是無牙**；反向 —— **有牙的 302 列裡，餓的是 0 列** ⇒ **需求那一半【從來沒有貢獻過】，不只是「92% 睡著」**｜★**想搶的沒牙，有牙的不餓**｜★★邊界我寫在結論旁邊：**first-N ＝ 偏早期的窗**，不可外推成世界通則
---

# ① 「淺」解了（★照預註冊的三步）

```
①重現：tier2 `lootHi` 平均 `opp` = **0.057**（n=73）｜tier1 `lootHi` = 0.262（n=120）
②無牙比例：**tier2 lootHi 裡 odds==0 的有 55/73 ＝ 75%**（tier1 lootHi 只有 16%）
③排除無牙後：**tier2 lootHi = 0.233**（n=18）⇒ ★**4.1 倍** ⇒ **「淺」不見了**
⇒ ★★所以那一桶**不是「知道得多卻不想打」**，是**那一桶裡多數隊打不動**
   ⇒ ★★★而 `odds == 0 ⇒ 整項 0` **是設計如此**（送死沒人幹）——
     **淺是【母體組成】造成的，不是【秤】造成的。**
```

# ② ★★★你那句要再強一格：不是「睡著」，是**從來沒醒過**

```
你寫：「**92% 的列 need = 0 ⇒ 那 0.4 的份量睡著 ⇒ 實際只有 0.6×loot**」。
⇒ ★**而逐列顯示更強的東西**：
   `need > 0` 的列 **33/400（8.2%）** ⇒ ★★**其中 odds == 0 的：33/33（100%）**
   反向：**有牙的 302 列裡，`need > 0` 的是 0 列**
⇒ ★★★**那 8% 醒著的 need，每一次都乘上一個 0** ⇒ **它對最終值的貢獻是【零次】**。
⇒ **所以「有效上限 0.6」還高估了**：★在這個樣本裡，**need 那一項的實際貢獻是 0**。
★**而這句話的世界版本是**：**想搶的沒牙，有牙的不餓。**
```

# ③ ★我同意不改 `need`，而我要指出「不改它」之後剩下的是什麼

```
★`need` 是 genuine（吃飽 ⇒ 不因餓而搶）⇒ **改它＝crank** ⇒ 同意，不碰。
⇒ ★★**而它留下的問題不是「need 該不該是 0」，是**：
  **`(0.6×loot + 0.4×need)` 這個【相加】在這個世界裡退化成單項** ——
  ★★★**而退化的原因不在公式，在【餓與武力的耦合】**：
  **兩個加數的「醒著」條件互斥。**
⇒ ★所以要呈 blueprint 的**不是「要不要調權重」**，是：
  **「餓而對方不肥也值得打」這個情境，世界裡有沒有辦法發生？**
  ⇒ ★★**若不能發生 ⇒ 那半個設計意圖是空的**（而那是設計層的事實，不是數值問題）。
```

# ④ 落地 ＋ 邊界

```
docs/measurements/2026-09-15-attack-opp-shallow-cause.md（★三步 ＋ 第四步）
docs/measurements/2026-09-15-attack-opp-shallow-cause-raw.txt
commit adf69494b｜herald @ c7b2a774e｜seed 1337／4320 tick｜因子逐列 n=400
★**邊界寫在結論旁邊不寫在附錄**：**樣本是 first-N ＝ 偏早期的窗** ⇒
  **「餓與有牙互斥」在這個窗、這個 seed 成立，不可外推**。
★★**「為什麼餓的隊剛好沒牙」是 behavior 因果 ⇒ 要 QA 故事稽核**，本卷不答。
★★★而你說「**一次呈報，不要讓 blueprint 分三次讀同一件事**」—— 逐列已跑完，**可以呈了**。
```
