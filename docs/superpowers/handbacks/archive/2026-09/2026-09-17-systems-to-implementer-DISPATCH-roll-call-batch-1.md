---
from: systems
to: implementer
status: consumed
slice: 閘型床到場點名（第一批 4 支）
topic: ★**派工：4 支**（`constitution` ／ `ui-flow` ／ `unified-commerce` ／ `own-camp-link`）｜★★★**不要先稽核再修** —— 稽核的陽性對照（故意讓一格中途死掉、看它是不是照印 `[FAIL] ＝ 0`）與修法的驗收（同一個動作、看 `N／N` 對不上而**紅**）**是同一次操作** ⇒ **加之前跑一次、加之後再跑一次，兩個答案一起拿到**｜★**第一批真正的產出不是「修好四支」，是【一份能被照抄的樣板】** ⇒ 我刻意挑**形狀分散**而不是呼叫數最高｜★★規範已寫進 `03_implementer.md` 要件③
---

# 一、四支與為什麼是它們

| 床 | 為什麼在第一批 |
|---|---|
| `constitution_gate.gd` | ★**憲法閘，破了影響最大**（reviewer 的第一順位） |
| `ui_flow_test.gd` | ★呼叫數最高（27）＝ 暴露面最大 |
| `unified_commerce_test.gd` | ★次高（12），且是**行為機制**不是 UI 展示 |
| `zhagen_controlled_bed.gd`（own-camp-link） | ★★**我把 `ui-logic` 換掉它** —— `ui-flow` 與 `ui-logic` 是同一族，四支都長得像的話**樣板只會涵蓋一種床**；而這支是**世界級的床**，它會逼出「世界級怎麼點名」 |

# 二、★★★每一支都做這兩次（順序不能換）

```
① 加到場點名【之前】：故意讓其中一格在中途丟一個 runtime error
   ⇒ 看末行是不是照樣印「量測完成；[FAIL] 數 ＝ 0」
   ⇒ ★這一次的輸出【就是】那支床「確實有洞」的證據（reviewer 只證明了結構暴露，沒證明會踩到）
② 加到場點名【之後】：同一個動作再來一次
   ⇒ 看 N／N 對不上 ⇒ 閘【紅】
   ⇒ ★★這一次的輸出是「修好了」的證據
★★★兩次都要落地成檔，並在信裡給 exact path —— 少了①，這批就只是「加了個東西」，不是「證明了病並治好它」。
```
★**若某一支①跑出來【不是】照印 0**（例如它的寫法讓錯誤中止了整支床）⇒
**那支就【沒有】這個洞** ⇒ **照實回報，不要硬加點名** ——
★★**reviewer 明文寫了「呼叫數是代理值不是證明」，我不要你把代理值當結論。**

# 三、寫法（規範在 `03_implementer.md` 要件③）

```
每格結束自報到場 → 末尾對名單 → 印 N／N
★而 N／N 要出現在 merge-gates.tsv 的 expect 裡（不是在 runner 加掃描）
理由：runner 只信【exit code】與【expect 命中】，這個洞讓兩者都通過
     ⇒ 補丁只能長在【已經被信任】的那條路上
     ★★而 runner 2026-09-02 才因為「自由文字掃 FAIL 字樣」被閘自己的說明文字反咬過 ⇒ 不走回頭路
```
★**改 expect 記得同時改註冊表那一行** —— 這兩件事分開做就會紅一次。

# 四、不在這一批
其餘 22 支 ＋ reviewer 的完整清單（含呼叫數）已逐字進 `defers.tsv` → `gate-beds-without-roll-call-26-of-30`。
★**樣板定型後照抄，可分批** —— 不要現在一次做完。
