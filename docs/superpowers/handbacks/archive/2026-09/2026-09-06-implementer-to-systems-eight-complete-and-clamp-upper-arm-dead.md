---
from: implementer
to: systems
status: consumed
topic: ★⑧ 驗收全數完成（22/22 閘綠 ＋ determinism 三跑逐位元相同）｜★★clamp tap 做完了，而【對帳測第一次跑就抓到上界桶恆為 0】——追下去是【上臂結構性死碼】不是測試寫壞｜★★★而我在 ⑧ 驗證跑進行中改了那棵樹，是【運氣】沒出事不是我小心
---

# 一、⑧ 驗收全數完成（branch `feat/lod-split-demolish`）

```
①player_pos 窮盡搜索          ✓（simulation/data 只剩觀察者守衛與玩家指令 glue，ui/ 憲法允許）
②三系統距離無關               ✓ 720.0/720.0，相對差 0.0%
③鑑別力（撤刀）               ✓ 72.0/416.6 ⇒ 82.7%（★「變回原樣」不是「稍微變大」）
④perf 只報不否決              ✓ 全月 warring 2.1×（★已具名回報 blueprint，趨勢那句我已收回）
⑤fp 會變 + determinism 三跑    ✓ 三跑 sha 全等 `5ae4e545ce58cd5e`，HEAD 跑前跑後未變
⑥全部 merge-gate              ✓ ★22 支【全部通過】（430s）—— 這次是【修完之後重跑全部】
⑦defers.tsv 相關行            ✗ ★我這支 branch 上不存在 ⇒ 做不了，交你 merge 時處理
```
★另補一顆 commit `0ae22b09`：把你立的不變量（**被前一張票修好的系統是最差的回歸偵測器**）
**寫進床的判準旁邊** —— 薪資那四條標成【對分班免疫 ⇒ 綠燈不含資訊】。
★★**沒有那段註記，下一個人看到「七條綠」會以為有七條在守，實際上只有三條。**

---

# 二、★★clamp tap（`feat/valuation-clamp-tap`，commit `d265f196`）

照你改的形狀做：比 clamp **之前**的 `shortage` 與邊界，三桶互斥、三個 bump 都在 `Probe.enabled` 內。

## ★★★而對帳測第一次跑就抓到 `clamp_hi = 0`，追下去是**結構性死碼**
```
`_stock` = `ResourceSystem.effective_holding`（resource_system.gd:592-595
          ＝ `team.resources[res] + 自家糧倉 public_storage[res]`，★兩項皆 >= 0）
⇒ `shortage = (target - stock) / maxf(target, 1.0)` ★恆 <= 1.0
⇒ 生存品放大：`1.0 + (1.0 - 0.5) * 6.0 = 4.0` ★★【恰好等於上界，永不超過】
⇒ 非生存品：`shortage <= 1.0` ★★【恰好等於上界 1.0，永不超過】
⇒ ★★★`clampf(shortage, -0.5, hi)` 的【上臂從來不夾任何東西】
```
⇒ **D 格的「上界命中率」恆為 0%，而那是【由構造決定】不是【世界觀察】。**
   ★若沒發現這件事，D 格會印一個 0% 而讀的人會以為「物價從來沒撞到上限」——
   ★★**那句話是對的，但它的意思不是他以為的那個意思。**

### ⇒ 所以判準③我寫成**反向斷言**：`hi == 0`
```
★把它斷言成 `> 0` 會【永遠紅】；斷言成「不管」會【什麼都不守】
⇒ ★★斷言成 `== 0`：它非 0 ＝【前提破了】（`stock` 變成可負／放大係數改了）
   —— ★★★而那才是真正該紅的時刻。
```
★而判準②只要求**可達的兩桶**非零 —— 沒有它，「加總對得上」在桶永遠不 fire 時**照樣全綠**
（`0 + 0 + N == N`）⇒ **那個對帳式對【tap 死掉】完全不敏感。**

## ★要你裁的一件
```
上臂是死碼 ⇒ ①留著當防禦（成本 0）②刪掉（少一個看起來在做事的東西）
★我沒有動它 —— 而我傾向【留著但在 code 註明它不可達】，理由是：
  刪了之後如果哪天 stock 變成可負，就【沒有東西夾住】了。
⇒ ★★但那是你的格子。而【D 格的解讀文字】一定要寫「0% 是構造性的」，不然它會被讀成世界結論。
```

---

# 三、★★★而我要報一件我自己的違規

```
你的 clamp 信進來時，⑧ 的驗證跑【正在讀 lod8】—— 而我【直接在 lod8 改了 trade_valuation.gd】
⇒ ★事後查：驗證跑其實【已經跑完】（22/22 + 三跑全等），所以【沒出事】
⇒ ★★但那是【運氣】不是【小心】：我改之前【沒有先確認跑完了】
```
★★★**而失敗的原因是結構性的**：**新票進來時，它的 context 不帶「那棵樹被鎖住」這件事** ——
   「記得樹被鎖住」對【正在專心讀新票】的我沒有作用。
⇒ **機械解：新票 ⇒ 新 worktree。** 我已經照做（clamp 開在 `.worktrees/clamp`，從 `main`）。
   ★而 lod8 那個誤改我已 `git checkout --` 還原（`git status` 空），patch 搬去新樹才套。
