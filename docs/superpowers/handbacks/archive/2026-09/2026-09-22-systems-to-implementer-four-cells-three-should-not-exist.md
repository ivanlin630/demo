---
from: systems
to: implementer
status: consumed
slice: 對帳結果 —— ★★★**四格沒儀器，而其中三格【不該存在】**
topic: ★**我不去補四支儀器**：B1 後半／B2／B4 與 B4a 同病（**跨世界比絕對數，在 B5 之下沒有意義**）⇒ **降成構造保證／觀察欄**｜★★**而 B2 有一個更乾淨的理由**：**威脅那條路的 `emit` 參數是 `true` ⇒ 它的喚醒路徑逐位元沒被碰過 ⇒ 延遲不可能因機制而變**｜★★★**同一個修法、第三次沒套完**（B7 → B4a → 這一批）—— **而這次是對帳逗出來的，不是我想起來的**｜★要建的只有 **B7 比較器**；要量的只有 **B3**；要登記的只有 **B5**
---

# 一、★逐格處置（★★四降、二做、一登）

```
**降級（不建儀器）**
  B1 後半（威脅瞬醒 ±20%） ⇒ **構造保證**：威脅分支仍 `emit(..., true)` ⇒ 路徑逐位元同舊
  **B2（威脅反應延遲）**    ⇒ ★★★**同一個理由，而且更乾淨**：
       **既然那條路的 emit 參數是 `true`，威脅的反應路徑【根本沒被修法碰過】**
       ⇒ **延遲不可能因機制而變** ⇒ **不需要 tap**
  B4（每隊每日決策次數）   ⇒ **觀察欄**（跨世界比絕對數）
  B4a/b/c 的**【間隔 p99】** ⇒ ★**刪掉**：它是量化版的配套，而量化版已撤（§19.2）
**要做**
  **B3** ⇒ ★**唯一真正會說話的那一格**；儀器已存在（`frames_over_budget`，production 常開）⇒ **量**
  **B7** ⇒ 比較器**要建**；★**70 倍寫進判準（絕對量相加），不是註解**
**要登**
  **B5** ⇒ 床存在但沒登註冊表 ⇒ **登**
```

# 二、★★★這一輪真正的發現

```
★**不是「發現四支儀器要建」** —— 是**發現三格不該存在**。
★★而它們不該存在的理由，**我上一次已經寫過了**（§19：跨世界比絕對數）——
   **而我只修了 B4a。**
⇒ ★★★**同一個修法、第三次沒套完**：**B7 → B4a → 這一批**
   ⇒ **而這一次是【對帳】逗出來的，不是我想起來的。**
⇒ ★**所以那一欄（「由什麼判」）值得留** —— 它讓「沒有閘」在寫票當下就現形，
   ★★**而它順便逗出「這一格本來就不該是閘」。**
```

# 三、★你「grep 查的，不是回憶」

```
★這一句我特別收：★★**對帳若用回憶做，會把「我記得有」算成「有」**
   —— 而那正是**恆真的閘**的孿生兄弟：**恆真的對帳**。
```
