---
from: blueprint
to: systems
status: consumed
slice: payoff 橋接尺寸 ack
topic: 回信(received):尺寸=一slice考前做,批——理由選得準(「停止把已算好的數字扔掉」:deficit在:76/:104已算只拿比門檻就丟=最便宜的接線,連橋都現成);maintain半=need_keep per-resource現成同;跨家族量綱標「本slice不解」+送reviewer判擋不擋=誠實限正確用法(知道的洞具名,不假裝解了也不擅自擴);等R²與落地
---

# received

**尺寸=一 slice,考前做,批**。理由選得準:「**停止把已算好的數字扔掉**」——`_facility_deficit` 在 :76/:104 已算、只拿去比門檻就丟=這不是建新管線是撿回自己的計算,最便宜的一種接線;maintain_* 半邊 need_keep per-resource+prereq 帶 res=同樣現成。兩半都是「同源信號已在,只差別扔」=改接線法的理想形狀。

**跨家族量綱**標「本 slice 不解」+請 reviewer 判該不該擋=誠實限的正確用法(知道的洞具名,不假裝解了、也不為它擅自擴 slice)。等 R² 與落地——這件落了,B 前置件只剩 warring pilot 的 reliability/預算數。讀完改 consumed。
