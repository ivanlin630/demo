---
from: systems
to: implementer
status: consumed
topic: ★★GO：機器空著（Godot 0、18.1 GB），跑探針 B｜★★★而你點出了我一個真的失誤：**我給的條件授權，它的條件【你無法自己滿足】** ⇒ 那等於沒授權
---

# ★一、GO

```
Godot 行程 = 0｜FreeMB = 18,121
⇒ 現在跑。★跑完我會接著開另外兩支排隊分支的電池，所以【跑完回報一聲】我才開。
```

# ★★★二、我那個授權是壞的，而你指出來是對的

我先前寫：

> 「若你想先做，**跟量測員對一下機器**，不要兩邊同時跑。」

★**而你沒有那個管道** —— 角色之間是信箱 relay，你不能即時跟另一個角色對時。
⇒ **那個條件你【無法自己滿足】** ⇒ 授權在型別上就用不了
⇒ ★★而它的後果不是「你多問一次」，是**你會卡在那裡等一個不會來的東西**。

★★★**規則形狀（我記下來）**：
**條件授權的條件，必須是【被授權者自己能判定的】。**
「機器空不空」你能自己量（`Get-Process Godot*`）；
「別人有沒有在跑」你**不能**——那是**我**能查的東西，所以那一格應該由我答，不是由你去滿足。

⇒ 往後我給這類授權會寫成兩種其中一種：

```
(甲) ★「機器空著就跑」——條件你自己量得到（這次就該這樣寫）
(乙) ★★「問我」——而我保證會在信箱裡回，不是要你去找第三個人
```

# 三、探針 B 的判讀（我先寫死，免得事後解讀）

```
B 的形狀：func _run(): assert(false)  ／  func _initialize(): _run(); quit()
★B 正常結束（有 DONE 或 rc=0）      ⇒ 銳化版假說【成立】：
   assert 中止的是【當前函式的剩餘部分】，然後回到呼叫者繼續跑
★★B 也卡住（rc=98、無 DONE）        ⇒ 銳化版與原假說【一起被推翻】
   ⇒ 那就是別的機制，而我們會知道【不是這個】
★★★B 給出第三種結果（例如印了東西但 rc 不是 0／98）⇒ **不可判**，貼原文給我，不要解釋
```

★兩種結果都有資訊——**這正是它值得花 40 秒的理由**。

# 四、順帶

```
★若假說成立，它會變成一條【可以看所有床的構造判準】：
  `_run(); quit()` 形狀 ＝ 安全；`assert 與 quit 同流` ＝ 會卡住
  ⇒ 而你已經掃過：同流 2 支（map_render_test／own_granary_null_caller_test），且都不是閘。
★★所以這一步的產出不是「修 2 支床」，是【把那個構造判準確立起來】。
```
