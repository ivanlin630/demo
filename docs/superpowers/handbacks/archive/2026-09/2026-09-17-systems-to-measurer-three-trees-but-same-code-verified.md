---
from: systems
to: measurer
status: consumed
slice: 0/18 那一卷 ｜ **三個 seed 跑在【三棵不同的樹】上 —— 而我驗過它們的 code 相同**
topic: ★★★**我查了三份 raw 的樹戳，它們【不一樣】**：`seed1337 = 4725bb819`／`seed2024 = f2715361a`／`seed777 = 9adc17db8` ⇒ **「3 seed 合計母體 18」是一個【跨樹加總】**｜★★**而我沒有因此作廢它** —— **我去驗了那三棵樹在 `scripts/` 上的差異：逐對都是 0 個檔**（中間那兩個 commit 是我的 hook／handback，沒碰 production code）｜★**所以它可以加總，而理由要寫進交件**：**不是「應該一樣」，是【逐對比過 `scripts/` 變動檔數 ＝ 0】**｜★★**我也跑了陽性對照**：同一個比法在 `docs/` 上抓到 5 個檔 ⇒ **它不是恆 0**
---

# ① 事實（★可覆核）
```
`…-desperation-violence-seed1337-raw.txt`  291277 bytes / 6834 行 ｜ **HEAD=4725bb819**
`…-desperation-violence-seed2024-raw.txt`  291618 bytes / 6429 行 ｜ **HEAD=f2715361a**
`…-desperation-violence-seed777-raw.txt`   302171 bytes / 6985 行 ｜ **HEAD=9adc17db8**
逐對 `git diff --name-only <a> <b> -- scripts/` ⇒ **0 / 0 / 0**
★陽性對照：同一個比法對 `docs/` ⇒ **5 個檔** ⇒ **它會抓到差異，不是恆 0**
```

# ② ★★交件要怎麼寫（**我把句子給你，因為它很容易寫成錯的**）
```
✗ **不要寫**：「三個 seed 同一棵樹」 —— **那是假的**
✓ **要寫**：「**三個 seed 跑在三棵不同的樹上（sha 逐一列出），
  而三棵樹在 `scripts/` 上逐對差異 ＝ 0（中間的 commit 只動 hook／文件）
  ⇒ 就【被量的那個世界】而言它們同源，故可加總。**」
⇒ ★★★**差別在**：**前者是一個【我沒查的宣稱】，後者是一個【我查了什麼、查到什麼】。**
```

# ③ 而這件事本身要記一筆（★不是你的錯）
**你照規矩蓋了樹戳** ⇒ ★**正因為你蓋了，我才查得出來它們不同** ——
★★**如果那三份 raw 沒有樹戳，這個「跨樹加總」會完全隱形，而結論長得一模一樣。**
⇒ ★★★**這就是判準 ⑩ 的價值**：**它不是用來擋人的，是用來讓「可不可比」變成一個【可以查的問題】。**
