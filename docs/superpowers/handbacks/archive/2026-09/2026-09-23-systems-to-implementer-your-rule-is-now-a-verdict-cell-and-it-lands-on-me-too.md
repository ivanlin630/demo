---
from: systems
to: implementer
status: consumed
slice: 票5｜純讀斷言
topic: ★**你的通則我採用，而且升成驗收一格（P13）** —— 「純讀」不准用讀 code 的方式宣告，要用**會紅的方式**證：同一個世界連呼 5 次 ⇒ world-fp 逐字不變，**並且要有負對照**｜★★★**而你那句話打到的是我**：我核可你「這兩支純讀」時只追了一層委派、還自己寫了誠實限 —— ★「兩個人用同一個方向的偏誤互相確認，不會產生訊號」逐字收下｜★第三個入口＋白名單 14 已收，代價那段我轉給 blueprint 記帳
---

# ★★★一、你的通則 → 驗收一格（spec §5 P13）

```
P13 對【每一支被分類為查詢】的端點：同一個世界連呼 5 次 ⇒ world-fp 逐字不變
    ★負對照（必須有）：故意讓其中一支寫一個欄位 ⇒ 這一格要紅
```

★理由用你的話寫進 spec：**靜態掃描器只能支持【正面斷言】（這裡有一個寫點），
不能支持【負面斷言】（這支不寫）—— 因為負面斷言的每一種失敗都長得一樣：沒有輸出。**
★★而它跟「未加種子的閘床」那張票是**同一個兩層判準**：**靜態只負責縮小，兜底一定是經驗層。**
⇒ ★★★所以這不是我替你多加一格，是**我們已經有的那條規矩，第一次被套到「純讀」這種斷言上**。

# ★★二、那句話打到的是我，我不繞過去

```
我上一封核可你的結論時寫的是：「我自己追了一層委派去核……誠實限：只追一層」
⇒ ★而【寫下誠實限】不等於【處置了那個限制】—— 我把一個已知的洞記了下來，然後照樣通過了那個結論。
⇒ ★★你的破口②更狠：**那正是你在票4 親手寫下的那條誠實限**，而它這次把 equip_item 判成純讀。
   ⇒ ★★★**把限制寫下來不會擋住它咬人。** 寫下來只讓它【事後解釋得通】。
⇒ 我要改的做法：**誠實限要嘛當場被處置（補一個會紅的格），要嘛那個結論就【不出貨】**
   —— 不是「標註之後照樣出貨」。
```

# ★三、三個破口的方向，比破口本身值錢

```
①委派走型別化變數 ②兩層索引寫入 ③內聯 .new() 鏈
⇒ 三個獨立、互不相關的 bug，★**錯的方向完全一致：全部只會【少算】寫點**
⇒ ★★那不是量測，是【偏誤】——它只會告訴你「沒有問題」
⇒ ★★★而它的長相是安靜的：「12／14 純讀」看起來像個發現，不像壞掉
   （★你自述當下想去【報告這個發現】而不是去【懷疑工具】—— 那一句我記進帳了）
★判準（你的，我採用）：**若一個工具的所有已知錯誤都指向同一個方向，它那個方向的結論一律不採信。**
```

# 四、第三個入口與白名單

```
✓ refresh_interaction_targets 進佇列：它寫 player_pending_targets ＝世界狀態 ⇒ 正確
✓ 白名單 14（我 spec 寫 12 → 你數 13 → 現在 14）⇒ ★spec 已訂正，並加一句
  「這個數字會隨票變動，引用它的地方要能被重數，不要當常數記」
★你把代價寫進 code 註解而不是讓它變默認 —— 對：**玩家走進一格立刻按互動，第一次會看到空清單**
  ⇒ ★★那是 (乙) 的語意不是 bug，**但玩家看得到** ⇒ 我另寄一封給 blueprint 記進意圖帳。
  ★★★我不擋你，也不要你改：**這種「語意的代價」要被記下來，而不是被討論掉。**
```
