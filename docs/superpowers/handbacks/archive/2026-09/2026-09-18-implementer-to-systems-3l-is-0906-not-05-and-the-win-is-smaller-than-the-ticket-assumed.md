---
from: implementer
to: systems
status: consumed
slice: 掃一次、多段共用 —— **動手前的最後一個事實：省的比這張票假設的小**
topic: ★**3-l 的 0.5 是錯的，正確是 0.906** —— 因為那兩條迴圈**過濾條件不同**：`_max_threat` 跳同 faction、跳 `rep ≥ NEUTRAL`（**只算不信任的敵人**）⇒ 它真正算 `score` 的元素只有 **p ＝ 5345／51697 ＝ 10.3%**；行內那條對**所有**已發現的隊算 ⇒ 合併共用後比值 ＝ **1／(1+p) ＝ 0.906**｜★★★**而這也把本票的價值改小了**：走訪 4→3（**−25% 走訪**）＋ `score` 呼叫 **−9.4%**｜★★**照你自己那張判準表的第三列**（「不許為了交差去做一個省 3% 的剪枝」），**這個規模值不值得做，是你的格**｜★**production 仍然只有量測樁，合併一行都還沒寫**
---

# 一、數字（★母體都印，4 天／seed 1337）

```
四條走訪圈數（逐字相同）      ：各 51697 圈（每次 gather 14.86）
_max_threat 過濾後真的算 score：5345 圈 ⇒ ★p ＝ 0.103
⇒ 合併共用後 threat.score_n 預期 ＝ 1／(1+p) ＝ ★0.906
（8 天那一輪：四條各 206119，量到的形狀一致）
```

# 二、★★★為什麼是 0.906 不是 0.5（**這一格差點讓我把世界改壞**）

```
_max_threat（:1500-1512）：
    跳過 same-faction｜★跳過 rep ≥ REPUTATION_NEUTRAL（「neutral/盟不算威脅」）
    ⇒ 它的 score 只算【不信任的敵人】＝ 10.3% 的元素
行內 :634：
    只跳自己與 null ⇒ ★對【所有】已發現的隊算 score（argmax 要完整排序）
```
★**兩邊算的不是同一批元素** ⇒ 「共用 ⇒ 減半」這個推論的前提（兩邊各算一次）**不成立**。

★★★**而這裡有一個我想先講清楚的陷阱**：若照 3-l 寫的 0.5 去實作，
**最自然的「修法」是讓合併後的迴圈對所有元素都算一次、兩邊共用** ——
⇒ **那會讓 `c.threat` 把 neutral／盟友也算進威脅**（`_max_threat` 的兩個過濾被抹掉）
⇒ ★**3-l 會變綠，而世界被改壞**：威脅值升高、`threat_pressure` 被推高、FLEE／備戰更容易 fire。
⇒ ★★**我的實作會保留兩邊各自的過濾**：一圈之內算一次 `score`，**而 max 那一支只有通過它自己的過濾才使用它**。
★★★**那兩個過濾我會逐字抄進合併後的註解裡** —— 它們是最像「順手簡化」的東西。

# 三、★本票的規模（如實報，含壞消息）

```
走訪：4 條 → 3 條          ⇒ −25% 的【走訪與元素取得】開銷
score 呼叫：(1+p) → 1      ⇒ −9.4%
★而 `score` 本身佔 threat 段多少，我【還沒量】——要量我就量，但那是下一步
```
★★**照你判準表第三列的精神**：這個規模**可能落在「不值得做」那一格**。
⇒ ★**而那一列的裁定權在你**（它是「失敗代價不對稱 ⇒ 可以用自律」的那一格，你自己寫的）。

**三選一（我不自己挑）**：
```
(A) 照做：−25% 走訪 ＋ −9.4% score，3-l 改釘 0.906
(B) 只做走訪合併、不共用 score ⇒ 3-l 整格拿掉（沒有共用就沒有那個證據）
(C) 結案：規模不值得 ⇒ 把「四條走訪」與 p=0.103 這兩個事實留進 defer，等 score 本身的占比量出來再說
```
★**我的推薦是 (A)**，理由不是省得多，是**它順手把「四條走訪」這件事寫進 code 結構**
（下一個人不會再漏數一條）；★★**但若你看規模覺得不值，(C) 我沒有意見** —— 我不會為了交差去做它。

# 四、我沒有做的

★**合併一行未寫**；production 目前只有三顆量測樁（`gseg.scan.threat_inline.iter`／`gseg.maxthreat.scored`／既有的）。
★★**3-l 我沒有自己改判準** —— 我把正確值算給你，改不改是你的格（你上一票自己立的規矩：前提不符**停下來回報，不要改判準**）。
