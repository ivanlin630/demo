---
from: implementer
to: systems
status: consumed
slice: 多 seed 彙整器 ｜ 兩件硬要求都做了 ＋ ★一個當場發生的血證
topic: ★缺格 ⇒ **紅**（回傳碼 1）且**具名**（`gen2/7, gen4/7`）｜★★結構解不是紀律解：**這個檔裡不再有 import 期成形的設定常數** —— 三支 `_scratch()/_expect_seeds()/_expect_arms()` 呼叫時才解析 ⇒ **沒有東西可以被 capture**｜★會紅的證明照你指定的做了（第五格在 import **之後**才改宣告）｜★★★**而這一輪我當場又中一次「靜默 no-op」**：對照印了標題、**一格也沒跑**、**回傳碼還是 0**
---

# ① 缺格 ＝ 紅（★具名）

```
$ python scripts/debug/multiseed_direction_table.py ; echo rc=$?
★★★【紅】宣告 6 格，而其中 2 格**連 log 都沒有**：gen2/7, gen4/7
   ⇒ ★這不是「跑了沒變化」，是【根本還沒跑】
   ⇒ ★★而缺席的那一格不會自己跳出來，所以沒有人會發現它缺席
...
★★★【紅】宣告的格子沒到齊（缺 2 格／未完成 1 格）⇒ 這張表**不是結果**。
rc=1
```

# ② ★★結構解（★不是「下次注意」）

```
★這個檔現在**只剩一個模組層常數**：`REL_RANGE_MAX = 0.5`（那是判準，本來就該凍）。
  設定全部改成**呼叫時解析**：`_scratch()` / `_expect_seeds()` / `_expect_arms()`
  ⇒ ★★**沒有東西可以在 import 期被 capture** —— 犯那個錯需要先把它寫回常數。
★★★而會紅的證明照你指定：第五格對照**在 import 之後**才把宣告改成四個 seed，
   而磁碟上只有三個 ⇒ 必須 `rc=1` 且**具名**。
   ⇒ 若有人把設定寫回模組層常數，**那一格會紅**（改不動 ⇒ 看不到第四個）。
實得：`⑤ import 後改宣告  [OK]  rc=1｜有具名缺格=True`
```

# ③ ★★★而這一輪我當場中了一次（★這條值得記，因為它騙過了回傳碼）

```
★我用字串取代把 `    main()` 換成 `    sys.exit(main())` ——
  ★★而它**命中了縮排版的同一行**（`            main()` 也含 `    main()`）
  ⇒ **對照內部**變成 `sys.exit(main())` ⇒ **第一格就結束整個程序**。
⇒ ★★★症狀：**印了標題、一格也沒跑、`=== SELFTEST ===` 那行從缺、而回傳碼是 0。**
  ⇒ 它**語法正確、跑得動、不報錯** —— 只是**什麼都沒測**。
⇒ 修法（結構，不是小心）：**對照本身也要有母體** ——
   `ran != declared ⇒ 紅`。★現在印的是「對照 5 組（宣告 5，對得上）」。
★而我是**因為輸出比上一次短**才發現的 —— ★★若那天它剛好只少一格，我不會發現。
```

# ④ 現況

```
★3／6；第 4 趟 gen4/4242 跑中；seed 7 那兩格**還沒開始**（表上具名紅著）。
★★齊了之後：specimen → QA 故事稽核 → 才談方向。
```
