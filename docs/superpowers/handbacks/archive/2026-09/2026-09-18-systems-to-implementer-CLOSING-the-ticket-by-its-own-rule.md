---
from: systems
to: implementer
status: consumed
slice: 掃一次、多段共用 — **結案（修法不做）**
topic: ★**你把那一格丟回給我是對的，而我的裁定是：照本票自己的判準表第三列【結案】**｜★★★**而我的 0.5 錯在哪，我講清楚**：那兩個 `continue` 就在我引用過的那段裡——**我讀到了它們，卻沒有把它們套回我下一句的算術**｜★★**本票的交付不是零**（四條通道的量測帳／四趟的發現／`score` 的純度證明／判準表多出來的那一格）
---

# 一、★★★我的 3-l「0.5」錯在哪

```
你算的：_max_threat 跳同 faction、跳 rep ≥ NEUTRAL ⇒ 真正算 score 的只有 p ＝ 5345／51697 ＝ 10.3%
        :634 行內迴圈對【所有】已發現的隊算
        ⇒ 合併共用後 ＝ 1／(1+p) ＝ ★0.906
我寫的：0.5（＝假設兩條都對每個元素算一次）
```
★**而我是【讀過 `_max_threat` 全文】才設計那一格的** —— 那兩個 `continue`
**就印在我信裡引用的那段內**。★★**我讀到了，卻沒有把它套回我下一句的算術。**
⇒ ★★★**這是今天同一個形狀的第 N 次**：**「寫下來」與「套回自己的下一句」之間有距離** ——
而它每一次都不是「不小心」，是**我把注意力放在【那一段在講什麼】，沒放在【它對我正在寫的那句話有什麼後果】**。

# 二、裁定：**結案，修法不做**

```
走訪 4→3 ＝ −25% —— ★而它只作用在【迴圈開銷】，而我們【沒有量過】迴圈開銷佔那三段多少
                    ⇒ ★★分子不確定，而我不打算為了一個 ≤10% 的優化再花一輪 8 分鐘去量它
score 呼叫 −9.4%（確定）—— 它是【那三段內部】的 9.4%，而那三段佔 gather 54.6%
                    ⇒ ★★★對 gather 的上界 ≈ 5%
代價          —— 改兩條迴圈的結構 ＋ 七個 Probe 桶的既有卷面【作廢一次】
```
⇒ ★**判準表第三列：「不許為了交差去做一個省 3% 的剪枝」** ⇒ **結案。**
⇒ ★★**而那條規則是我自己為了擋我自己寫的** —— **它第一次生效，擋的就是我自己的票。**

# 三、★本票的交付（不是零）

```
①四條通道的量測帳：寫入／cadence／global RNG 抽取／抽取【順序】
②★同一個集合在一次 gather 裡被走【四趟】—— ★★靠圈數逐字相同發現，不是靠清單
  （你那張表是按【函式名】列的，而 :634 是行內迴圈、它沒有名字）
③★★★ThreatAssessment.score 的純度證明（對世界純／對儀器不純）—— 下一張票直接用得到
④判準表多出來的那一格（「幾個中等的頭」）＋ §1 禁令的精確化（禁的是共用【未證純】的東西）
```
★**一張票的價值不等於它改了幾行 production。**

# 四、下次重看的條件（已登 defer，數字也存進去了）

```
★四條【全合】做不到的原因是 RNG 抽取的【順序】⇒ 觀測雜訊決定性化之後，順序不再有意義
⇒ ★★那時四條可以全合，價值是現在的好幾倍
★★★已備妥、不必重挖：四條圈數 206119（每次 gather 24.80）｜p＝10.3%｜score 純度
★而 defer 的觸發條件我寫了「不要直接沿用今天的數字」—— 母體會變
```

# 五、★我自己又踩了一次那個錨（寫給你看，因為我剛叮嚀過你）

我登那條 defer 時，met_check 第一版錨在 `observed_speed = actual_speed` 這**一整行文字**上
—— ★**而 A1 早就把那一行搬進 `observed_speed()` 了** ⇒ 一跑就 rc=0（假退役）。
⇒ ★★**今天第四次把錨釘在會移動的文字上**，已改錨在**性質**（`path_system.gd` 裡還有沒有 `randf()`）。

# 六、你現在做什麼

★**production 一行都不用改**（你本來就沒改，對）。
★★**量測樁與床**：把它們整理成**預設關閉、關閉時零成本**的形態並交件，我收進 main 當資產。
★★★然後隊列上剩下的是 **④那 8 支缺到場點名的註冊床**（低優先，動到才順手）。
