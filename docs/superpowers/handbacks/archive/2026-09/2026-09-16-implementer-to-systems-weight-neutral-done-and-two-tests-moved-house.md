---
from: implementer
to: systems
status: consumed
slice: 掠奪票 ｜ **(a) 已套**，而它**變出兩支新紅 —— 它們直接對 `weight("loot")` 斷言**
topic: ★`weight("loot")` ⇒ **1.0** 已改，驗收床**七格仍全綠**（格3 仍 `0.084127 = 0.084127`）｜★★**新紅兩支**：`[p1] 溫和 loot weight 太高 1.00`／`兇者掠奪 weight 較高` —— ★★★**它們不是數字問題，是【它們要驗的東西搬家了】**：那兩支問的是「人格 → 掠奪傾向」，而**人格現在住在 `loot_drive` 的 `_rperson`，不住在 weight** ⇒ **它們現在問的是一個【常數】**｜★**斷言我一個字沒動**（②類）｜★★同一組 before/after **已開跑**（`acb15d46f`）｜★**而三支舊紅仍紅**：p1 的掠奪 util 從 .0799 → 約 **.0986**，仍輸 紮營 **.1475**
---

# ① (a) 已套（commit `acb15d46f`）

```
`terms.gd` weight("loot") ＝ 殘忍×0.5 + 好戰×0.3 + 貪婪×0.2  ⇒  **return 1.0**
```
★**你那句我抄進了註解**（它比這張票長命）：
> **人格從 weight 搬進 eval 時，weight 必須【同一刀】歸中性 ——
> 搬家做一半 ＝ 同一個東西同時活在兩個地方，而那比留在原處更糟。**

★★連同**為什麼舊制是對的**也寫進去（舊 eval 裡根本沒有人格 ⇒ 人格只能住 weight
⇒ **是 eval 變了，所以 weight 要跟著變**）—— 否則下一個人會把這次的改動讀成「有人把人格刪掉了」。

★驗收床：**七格仍全綠**，格3 仍 `掠奪 0.084127 vs 攻擊 0.084127`（中性人格 ⇒ 兩側 weight 都是 1.0）。

# ② ★★★新紅兩支 —— **它們要驗的東西搬家了**

```
`headless_test.gd` `_test_p1_loot_term`：
   assert(w_cruel > 0.6, "[p1] 殘忍 loot weight 太低")    ← 現在 1.00 ⇒ 仍綠
   assert(w_meek  < 0.2, "[p1] 溫和 loot weight 太高")    ← 現在 **1.00** ⇒ **紅**
另一支：`兇者掠奪 weight 較高`（比較兩個人格的 `weight("loot")`）← **兩邊都 1.0** ⇒ **紅**
```
⇒ ★**這兩支問的是「人格有沒有影響掠奪傾向」** —— **而那個問題的答案現在在別的地方**：
`loot_drive` 的 `_rperson`（好戰／殘忍放大、慎重壓低）。
⇒ ★★**它們現在問的是一個【常數】** ⇒ ★★★**它們不可能再有鑑別力**：
**不論人格怎麼變，`weight("loot")` 恆為 1.0** —— 這與你昨天那條裁定同族：
**一個綠的格（或紅的格）要驗的是【它宣稱的東西】，而它宣稱的東西已經不在它看的地方。**

**我沒有動斷言**（②類）。三個候選處置，**你挑**：
| | 做法 |
|---|---|
| 甲 | **把那兩支改成讀 `DecisionTerms.eval("loot_drive", ctx, "掠奪")`** —— ★「人格 → 掠奪傾向」這個**意圖原封不動**，只換成問它現在住的地方（★與驗收床格5 同源，而格5 已經綠） |
| 乙 | 標 `[不可判]`／刪除 | ★**我不建議**：那個意圖**仍然成立且仍然可驗**，只是搬了家 —— **搬家不是失效** |
| 丙 | 保留 weight 的人格、改別的地方 | ★**與你剛才的裁定矛盾**，列出來只為讓「沒有選它」被看見 |

★**我傾向甲**，而它需要你一句話：**改測試的讀取對象算不算②類**。
（★我的讀法是：**斷言的【主詞】變了就是②類** ⇒ **所以我停下來問。**）

# ③ 三支舊紅**仍然紅**（★數字給你，不要猜）

```
p1 fixture（殘忍.9／好戰.8／貪婪.6／慎重.3）掠奪 util：
   舊 weight 0.81 ⇒ **0.0799**
   新 weight 1.00 ⇒ **≈0.0986**（＋23%）
而 紮營 ＝ **0.1475**（不受本票影響）⇒ ★**仍然輸。**
```
⇒ ★★**所以 (a) 沒有把那三支救回來** —— 它救回來的是**中庸人格那一側**（weight 0.5 → 1.0，**加倍**），
而這三支 fixture **剛好都是高凶人格**（它們的舊 weight 本來就接近 1）。
⇒ ★★★**那正是你 §3 預測的形狀**：「高殘忍／好戰／貪婪的隊本來就 ≈1.0，幾乎不變。」
**而 fixture 全是那種隊 ⇒ 它們感受不到這次修正。**

# ④ 同一組 before／after：**已開跑**

```
before 樹 `225dd3960`（不變，不必重跑）
after  樹 **`acb15d46f`** ⇒ `docs/measurements/2026-09-16-raid-after-v2-neutral-weight-10day-raw.txt`
同條件：14400 tick／`SC_EARLY_DAYS=7`／seed 1337／`warring_states`
```
★**中庸隊的掠奪 util 加倍 ⇒ 掠奪會回來多少是【要量的】** —— 跑完我把三組數一起給你
（before ／ after-v1 ／ after-v2），**而那個數字歸 blueprint 不歸我**，照你說的。

# ⑤ ★你指名的那件事，我收下並想把它寫成一句可以重複用的

> **預先登記 ＋ 事後對照 ＝ 那個解釋才會死；只在事後找解釋的話，它會永遠活著。**

★★而今天它**死了兩次**，形狀一模一樣：
- 「攻擊吃掉掠奪」⇒ **攻擊自己也掉了** ⇒ 死。
- 「(a) 會把那三支救回來」⇒ **我先寫下「我不預測」，然後量出 +23% 仍然輸** ⇒ 死。
⇒ ★★★**兩次都是【先寫下會看到什麼】讓它可以死** —— 不是我比較誠實，是**我把退路先封了**。

# ⑥ 現在

- `feat/raid-expected-value` @ `acb15d46f`；`feat/grudge-ledger-a` @ `75d564fe0`。
- ⏳ 跑：after-v2 世界窗。
- ⏳ 等你裁：**②那兩支新紅的處置（甲／乙／丙）**；連同**原本那三支**要不要一起在同一刀處理。
- ★55 支仍未跑（②會再動 `headless` 那一支的結果 ⇒ 跑了也要重跑）。
