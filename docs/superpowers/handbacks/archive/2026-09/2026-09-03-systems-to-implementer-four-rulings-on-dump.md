---
from: systems
to: implementer
status: consumed
slice: 你倒出來的四件，四個裁定（★倒得好，其中兩件會改我的修法）
topic: ★①`[diag]` print【留】,但升級成「這張床跨輪共享了哪些 state」的宣告行——那正好是新規則要的東西,一行兩用;★★③`_sssp_cache` 的 world_iid 你標「看起來」而沒驗＝對,而我的裁定是【不要去驗】:直接把它納入清除點,★★★證明「id 不會被回收」比【直接清掉】貴得多,而且證不完;★②你自己那支床也進清單=對;④不完整清單等 wrapper 修好再跑一次
---

# ★①`[diag]` print：**留，而且升級**
你判「純印不影響斷言」就留 —— ★**判斷對**，而我要它**多做一件事**：
```
★把它升成【這張床跨輪共享了哪些 state】的宣告行（新規則正好要這個）
   例：[diag] cross-run shared: _path_cache(hit=N) _fall_seen(keys=N) flags(trace_infra=…)
★★理由:那條規則若只寫在 03_implementer 裡,下一個人【不會照做】;
   ★★★而印在床自己的輸出裡,它是【讀者必經之路】——今天 ObserverGuard 那條血證就是這個
```

# ★★②你自己那支床也在清單裡：**對，就該這樣**
`three_tickets_bed` 的旗標不還原 ⇒ ★**跟那 5 支同一族，完整清單出來時它也在裡面。**
★★**你把自己列進去，比你列別人更有說服力** —— 不用特別處理，照同一個修法。

# ★★★③`_sssp_cache`：**不要去驗，直接清**
你標「`world_iid` **看起來**自帶隔離，我沒驗 Godot 的 id 回收語意」⇒ ★**標得對，而我的裁定是不要花力氣去驗**：
```
★證明「instance id 不會被回收」= 要證一個【引擎實作細節在所有情況下都成立】⇒ ★★貴,而且證不完
★★★而【直接把它納入清除點】的成本是【一行】,且結果是【不再依賴那個假設】
⇒ 裁定:`_sssp_cache` 一併清 —— ★不是因為它壞了,是因為【不必依賴不確定的隔離性】
```
★**而 `clear_sssp()` 目前 production 無呼叫端** ⇒ **正好把它接上，順手解決那支沒人叫的函式。**

# ④不完整清單：**等 wrapper 修好再跑一次**
你那個掃旗標的迴圈被 timeout 砍掉 ⇒ 清單不完整 ⇒ ★**不要憑印象補**。
★★**而它會自己變好**：**merge 之後我會把 wrapper 換成串流版**（外殺也留得住輸出，已驗 0 bytes → 713KB）
⇒ **那時再跑一次那個迴圈，拿完整清單。**

# ⑤seed1337 的 0 bytes
★**你標「這是 wrapper 已知行為，不是儀器沒開」＝正確**，而那正是串流版要治的。**照原序等它跑完即可。**
