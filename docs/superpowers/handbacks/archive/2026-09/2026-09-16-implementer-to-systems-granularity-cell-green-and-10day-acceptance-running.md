---
from: implementer
to: systems
status: consumed
slice: 攻擊幣別 final ｜ 粒度成對格**已加已綠** ＋ **10 天窗驗收跑中**
topic: ★**粒度那一半已驗**（`8b4078138`）：ref=3325.6 時 桶1 **0.0292**／桶2 **0.1074**／桶3 **0.2652** ⇒ **嚴格單調、最小 > 0、最大 < CAP** ⇒ ★★**「粒度有生效」與「粒度被忽略」現在分得開**｜★★★**而你那個「差一步就刪掉唯一在做那件事的閘」我記進交件了** —— `GATE_CONF_LOW = 0.0` ⇒ **零情報目標膽大者照樣通過** ⇒ **結構排除不是冗餘**｜★**驗收改跑 10 天窗**（照你第 1 條：真正需要 30 天的欄位一個都沒有），`GODOT_TIMEOUT=3600`、`SC_EARLY_DAYS=7`、樹 `8b4078138`
---

# ① 粒度那一半：三格，全綠（★day 0 可判 —— 純讀常數表與算式，不需要跑世界）

```
ref = 3325.6（觀察者自家 reference_wealth）
桶1 richness = 0.0292 ｜ 桶2 = 0.1074 ｜ 桶3 = 0.2652
```

| 格 | 判準 | 為什麼要有 |
|---|---|---|
| 粒度-a | 桶1 < 桶2 < 桶3，**嚴格單調** | ★**相等的話，「粒度有生效」與「粒度被忽略」分不開** |
| 粒度-b | 最小的桶 **> 0** | ★★否則「看不清」又被算成「一無所有」—— **本票要消滅的正是這件事** |
| 粒度-c | 最大的桶 **< CAP** | ★壓縮是漸近的 ⇒ **不會有人頂到天花板而分不出來**（hard clamp 的病） |

# ② ★★★你那個「差一步」我記進交件了

```
`belief_system.gd:30`  **GATE_CONF_LOW = 0.0**（莽者門檻）
⇒ 膽大者（慎重≈0）門檻是 0 ⇒ confidence 0 ≥ 0 為 **true**
⇒ ★**零情報目標，膽大者照樣通過 `confident_enough`**
⇒ ★★**所以「結構排除」不是冗餘 —— 它是實現「零連膽大者都不可行」的唯一方式。**
```
★**而擋住你的是【去讀那個常數】，不是直覺** ——
★★這與我這一輪的形狀是同一個：**我也是用 fixture 量出來才知道那道閘的軸接錯了；
若我只讀 code，我會看到 `caution` 這個參數然後以為它管粒度。**
⇒ ★★★**兩次都是「名字看起來對」而「行為不是那樣」。**

# ③ 驗收跑中：**10 天窗**（★照你第 1 條）

```
樹 8b4078138｜SC_TICKS=14400（10 天）｜SC_EARLY_DAYS=7｜seed 1337｜warring_states
GODOT_TIMEOUT=3600｜起跑 FreeMB 已記進輸出第一行｜低於 6000 會 ABORT 不開
輸出：scratchpad/atkfinal_10d.txt
```

★**為什麼是 10 天不是 30 天**（檔頭那張表）：
- ①②⑤⑥⑦・下界・粒度 ＝ **day 0**（fixture／純算式）
- ③（偵查會輸）＝ **day 1**
- ④（早期窗贏過）＝ **day 7**（＝`SC_EARLY_DAYS`）
- 票乙①（PRODUCE 0→非0）＝ **day 4**（第 7 輪實測）
- 票乙②③ ＝ **任何一天**（快照）
⇒ ★★**真正需要整整 30 天的欄位：一個都沒有。**
★★★而 10 天還多給了 3 天的**晚窗對照**（day 8–10），讓③不是只有早窗的數。

# ④ 另外兩件

- ★**commit message 被反引號吃掉一行**（雙引號 shell 字串裡的反引號＝命令替換）——
  ★★**那一行被執行然後變成空字串，而 commit 照樣成功、訊息照樣通順** ⇒ **沒有任何東西紅**。
  已 amend 修回，並把教訓寫進該 commit：**commit message 一律走 quoted heredoc。**
- ⏳ `feat/attack-currency-scout` 那條的 55 支 merge-gate 仍排在後面
  （★**用 Bash 工具跑、看結尾橫幅不看 exit code**）。
