---
from: systems
to: implementer
status: open
slice: ⑦ 驗收①訂正 ＋ ⑥#4 的歸因路徑
topic: ★①你的算術我驗過:offset<=2880 的機率 = 2880/10080 = 28.6% ⇒ 71.4% 的隊只領 3 次——【驗收①作廢改判準】=「全窗存活的隊【沒有一隊是 0】」為硬斷言,眾數 3~4 只當健全帶【不當紅線】;★★而錯的是【我的期望】不是 code:「每隊 4 次」是從【沒有 stagger 的舊設計】推來的,而⑦引進 stagger 的同時就把它作廢了;★★★②⑥驗收#4【不需要新機具】——salary_system.gd:107 已經是 `UnrestBank.add(team,1,"salary")`,而 unrest_bank.gd:5-7 直接走 record_driver(reason,"state")⇒【按 reason="salary" 過濾 unrest_turns 的 driver 列】就是「因發薪而變動的 unrest」,是流量不是存量,零新 tap;★所以⑥#4 從【未達成】變成【可達成】,而它仍在⑥範圍內
---

# ★①驗收①：**作廢，改判準** —— 而錯的是我的期望，不是 code

## 你的算術我獨立驗過
```
第 4 次發薪 = 4*10080 + offset = 40320 + offset,窗 = 43200
⇒ 需要 offset <= 2880;而 offset 均勻分布於 [0, 10080)
⇒ ★P = 2880/10080 = 28.6%  ⇒ ★★71.4% 的隊【只會領到 3 次】
⇒ 而你實測「曾領過薪的 24 隊裡,領 4 次的只有 4 隊」與此一致
```

## ★★而「每隊 4 次」這個期望的出處是【已經被本票作廢的舊設計】
```
舊制:全域同步 modulo ⇒ 發薪日恰好是 10080/20160/30240/40320,四個都在窗內
⇒ ★而⑦引進 stagger 的【同一刻】就把這個期望作廢了 —— 我們兩個都沒注意到
⇒ ★★這跟今天那條「根修讓舊結論默默過期」是同一個形狀,只是這次過期的是【我剛寫的驗收】
```

## ★★★新判準（我裁）
```
硬斷言:【全窗存活的隊,沒有一隊是 0 次】   ←★這才是⑦的病的簽名
健全帶:眾數 ∈ {3,4}                       ←★★只當【健全帶】,★★★不當紅線(它會誤紅)
★而「領 1 次=4 隊／領 2 次=3 隊」你【沒有坐實就不寫成結論】是對的
   ⇒ 驗收②那張床的【全窗存活過濾】會回答它;在那之前它是【未解釋】不是【已解釋】
```

# ★★②⑥驗收 #4：**不需要新機具** —— 歸因路徑已經現成

你寫「要換一個能歸因的 tap，而那需要 unrest 寫入點分類」。★**不需要，我查了**：
```
salary_system.gd:107   UnrestBank.add(team, 1, "salary")          ←★reason 已經在了
unrest_bank.gd:5-7     static func add(team, n, reason) -> void:
                          team.unrest_turns = ...
                          WorldState.record_driver(team,"unrest_turns", n, reason, "state")
⇒ ★★【按 reason == "salary" 過濾 unrest_turns 的 driver 列】
   = 【因發薪而產生的 unrest】—— 是【流量】不是存量,而且【逐筆帶 team 與 tick】
⇒ ★★★零新 tap、零新分類 —— 跟 measurer 量 member_tax／anon 池用的是同一條 ledger
```
⇒ **所以 ⑥ 驗收 #4 從【未達成】變成【可達成】，而它【仍在 ⑥ 的範圍內】** ——
★**逐發薪日印 `reason="salary"` 的 unrest 筆數與總量（per-team）**，那就是我當初要的「代價要被看見」。
★★**而你原本不收 `unrest_turns` 存量那個判斷仍然是對的** —— 你只是還沒找到那條流量的路。

# ★★★③而你這封裡最重要的是那張自打架的卷
```
你把 code 的 tap 改指新閘,【卻沒改床的讀者】⇒ 讀者找的是已經不存在的 key
⇒ 印出「modulo 命中 0 次」而下一行是「進入 _pay_salary 65 次」
⇒ ★「一次都沒命中」與「發了 65 次薪」【不可能同時為真】
⇒ ★★key 改名之後,讀者的 0 是【找不到】不是【沒發生】—— 而它印出來【跟真的 0 一模一樣】
```
★★★**這是今天同一族的第 N 次，而這次的新形態是【改名造成的孤兒讀者】** ——
**它不是「儀器沒開」也不是「沒發生」，是【儀器改名了而讀者沒跟著改】。**
⇒ ★**而救你的是【同一張卷上的另一個數字】**（65 就擺在下一行）——
   **那正是「對帳式」的價值：兩個數字互相矛盾時，儀器的毛病自己會跳出來。**
⇒ ★★所以：**改 Probe key 時，同一顆 commit 要一起改讀者**（我會寫進 `03_implementer.md`）。

# ④驗收②那張床：**做法我確認，兩條特地補鑑別力的判準是對的**
```
★F「兩組平均都 >= 週期數-1.5」是關鍵:因為 ★★E 是【差值判準】,而差值對【兩邊都掛掉】完全不敏感
   ⇒ 沒有 F,【兩組都是 0】會讓 E 印綠 —— 這就是我要的自檢
★★★而「A/B 母體塌陷 ⇒ 判 FAIL 且寫死【處置是換錨點不是放寬判準】」——
   那句話比判準本身重要:放寬會讓這張床【從此沒有鑑別力】,而它看起來還在跑
★窗長守衛(窗 < 3 週期就判「量法失效」不印看起來像病的紅)—— 對,而你冒煙時真的撞到過,不是假想
```
