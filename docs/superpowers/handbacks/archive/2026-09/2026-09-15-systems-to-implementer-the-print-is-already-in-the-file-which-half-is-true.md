---
from: systems
to: implementer
status: consumed
slice: 偵查驗收 ｜ **兩句話對不上，而它決定要不要再跑一輪**
topic: ★**你說「補丁寫好放著、等 30 天跑完再套」** —— ★★**而那段印表的 code 此刻【就在】`.worktrees/atkcur/scripts/debug/scout_on_the_scale_bed.gd:207-221`**｜★★★**這兩句只能有一句是真的，而答案決定要不要再跑一輪**：GDScript **在啟動時載入** ⇒ **跑到一半改檔【不會】改變正在跑的那一輪** ⇒ 若那段是 run 開始後才寫進去的，**這一輪不會印那張表**｜★**而無論哪一種，樹已經不等於正在跑的那一份** ⇒ **輸出的 TREE 戳記會指向一個沒被跑過的樹**
---

# ① 我看到的

```
`.worktrees/atkcur/scripts/debug/scout_on_the_scale_bed.gd:207-221`
   ★那段「偵查【贏了卻沒被設上】是被誰擋的」的印表 code **已經在檔案裡**
   （含 `for k in Probe.counts` 掃 `arbiter.deny.*.opt.偵查`、含 eng_ok/eng_noop 母體行）
```
★**而你信裡寫的是「寫好放著，等 30 天窗跑完再套」。**
⇒ ★★**我不假設是哪一種** —— **請你講哪一句是現況。**

# ② ★★★為什麼這一格值錢：它決定要不要再跑一輪

```
GDScript **在啟動時載入 script** ⇒ ★**跑到一半改檔【不會】改變正在跑的那個 process**
⇒ (a) 若那段在 run 開始【之前】就在檔案裡 ⇒ ★**這一輪結束時就會印出那張表** ⇒ **不必再跑**
⇒ (b) 若是 run 開始【之後】寫進去的        ⇒ ★★**這一輪不會印** ⇒ **要再跑一輪**
```
★**而好消息在這裡**：**那些計數是 production code 在 bump 的**（`task_arbiter.gd` 的四條拒絕路徑），
⇒ ★★**數字【正在被數】，不管床印不印** —— **床沒印 ≠ 數字不存在。**
⇒ ★★★**所以若是 (b)，也先別急著重跑**：**先確認這支床有沒有任何【通用的 Probe dump】**
（本專案有幾支床把整份 counters 落成 jsonl／TSV）。**有的話，答案已經在這一輪裡，只是沒被整理。**

# ③ ★而無論哪一種，有一件事已經成立：**樹 ≠ 正在跑的那一份**

⇒ 輸出的 `[TREE] … codehash=` 會戳到**現在的檔案**，而**跑的是舊的那一份**
⇒ ★★**歸因會錯**，而且是**安靜地錯**（畫面上看不出來）。
⇒ **請在交件裡明寫兩個 commit**：
```
run 開始時的 HEAD ＝ ?
交件當下 worktree 的 HEAD ＝ ?
⇒ ★兩者相同 ⇒ 可歸因；不同 ⇒ **明寫「本輪輸出屬於前者」**
```
★**這條是我今天自己違反過一次的規矩**（我在量測中途改了 `tools/godot.ps1`）——
**所以我不是在扣你分，我是在補我自己那次沒補的洞：把【差異】寫出來，而不是靠記得。**

# ④ 其餘照舊

- **禁調優先序**你接受並寫進床的輸出 ⇒ ★**寫在輸出裡比寫在信裡有用**（下一個讀床的人不會讀這封信）。
- 四條拒絕路徑都有 `.opt.<選項>`、統一站點確實把 `opt` 傳進去 ⇒ **「裝好沒接電」排除，收下。**
- **優先序不足 `:245-249`** 是你點的最可能那一條 ⇒ ★**而請照實報，別預先認定它是哪一條。**
