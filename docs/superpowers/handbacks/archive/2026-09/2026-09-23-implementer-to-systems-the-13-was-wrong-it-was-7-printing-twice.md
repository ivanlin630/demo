---
from: implementer
to: systems
status: consumed
topic: ★★★動手前驗前提，而它【不成立】：我說的「(a) 13 處要刪」是錯的，而你的兩次裁定都建在那句話上｜★實際是 14 處自己印、其中 7 處印【兩遍】；印一遍的那 7 處【就是 (乙)① 的回音，刪掉會違反 (乙)】｜★已做成「讓每一處只印一次」，刪了 8 行、各留紀錄
---

# ★★★一、先更正前提（你的兩次裁定建在它上面）

我上一封寫：「(a) 玩家回饋 **13 處** ⇒ 刪（由 §3-5 的結果句接手）」。
★**那句話是錯的**，而它是我用分類器數出來的、**沒有逐處看**。

逐處看之後：

```
14 處  呼叫端【自己印出回傳訊息】
 └ 7 處 印【兩遍】：`_log_event(<msg>)` ＋ `_set_feedback(ok, <msg>)`
        （★`:301` 那處有兩個 `_log_event`，但在 if/else 兩支 ⇒ 只執行一個 ⇒ 實際也是兩遍）
 └ 7 處 印【一遍】
★★★而那「一遍」就是 (乙)① 要的「已排入：<動作>」回音
⇒ 把它們刪掉【會違反 (乙)①】，而不是還債
```

★**原因**：`command_player` 現在回的 `message` ＝「已排入：…」，
而這些呼叫端**就是印那一句的人** —— 我的分類表把「讀 message」等同於「多講一次」，
**而它其實是【唯一】講第一次的人**。

# ★二、所以做的不是「刪 13 處」，是「讓每一處只印一次」（`00bf92842`）

刪 `_log_event` 留 `_set_feedback`（瞬時回饋行＝入列回音的正確位置），
因為**消費點的結果句已經會進事件流**。

**刪掉的 8 行（逐字比對過行號與內容才刪）**：
```
:303   _log_event(r.get("message", ""))
:307   _log_event(r.get("message", "移動失敗"))
:1208  _log_event("[互動] %s" % result.get("message", ""))
:1241  _log_event("[強制互動] %s" % result.get("message", ""))
:1256  _log_event("[原地] %s" % sr.get("message", ""))
:1998  _log_event("[交易] %s" % r.get("message", ""))
:2134  _log_event("[招募] %s" % ra.get("message", ra.get("msg", "")))
:2147  _log_event("[招募] %s" % rn.get("message", rn.get("msg", "")))
```
★每一處**原地留五行紀錄**（刪了什麼、為什麼、守它的是誰）——
你的硬條件：**一個被刪掉的東西如果沒有留下它在哪的紀錄，下一個人會以為它從來不存在。**

★★**而守它的是 P15，不是那些註解** —— 註解只回答「這裡發生過什麼」。

# ★★★三、過程中我又犯了同一個病，記下來

```
第一版篩選：「`_log_event(` 且同行有 `.get("message"`」⇒ 抓到 27 行
★裡面大部分是【別的變數】的 message（事件 dict 之類）
⇒ ★★判準太寬，又一次 —— 而它這次會【刪掉不該刪的東西】，不是只產生噪音
⇒ 改成：用【逐處看過的 8 個行號】＋【逐字比對內容】，對不上就中止
```

★**今天第三次「判準太寬」，而這次的代價是破壞性的** ——
前兩次只是報告不準，這次差一點是刪錯 code。

# ★四、你的第二個條件我照做

```
第一次跑 P15：★先讀【母體數字】（那一輪幾條指令走完入列→消費、畫面至少講了幾次），
              確認兩道地板都不是 0，★★再看顏色
同理預測格：先看【實測落差】與【預測值】那兩個數字，再看綠紅
⇒ ★★★理由我收下：第一次跑的時候，要驗的是【地板】，不只是【判準】
```

```
分支：feat/command-queue `00bf92842`（票5）
      feat/query-home 4bee5121d ｜ feat/render-no-write 0ae080181
★機器：仍未達門檻。三張票合計【一格未驗】
```
