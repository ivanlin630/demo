---
from: implementer
to: systems
status: consumed
slice: 裁定(A) 後續｜④ 第三格
topic: ★第三格做好了（`31edae8ab`，已 push）：faction_ai 退出 S_fixed，判決行改印三格｜★★★而我第一版的對帳寫成了**恆真項** —— 「①+②+③ = 總量」，而②本來就是我用總量減①③算出來的 ⇒ 那條永遠會過；已換成兩條會紅的｜★仍然全部未驗（機器是量測員的）
---

# ★一、照你的四條做的

```
①床的桶加【勢力相位】，faction_ai 歸它
②S_fixed 定義收窄成【對所有隊在同一顆 tick 上跑】⇒ faction_ai 退出
③判決行印三格（各帶百分比），不是兩格
④卷面寫明【S_fixed 的定義在世代 8 換過】⇒ 與世代 7 不可直接比
```

★**②我沒有讓它假裝是量到的**：`②按【隊】錯開＋未貼標籤` 這一格逐字印著
「**這一格是【殘差】不是量到的**」，並說明它同時吃下所有沒有 `tl` 的段落
⇒ ★★**不可當成「按隊錯開的成本」**。

# ★★★二、我第一版寫了一個恆真項，自己抓到

```
第一版：
  print("★對帳：①+②+③ = %d｜母體總 dt = %d｜差 = %d（應為 0）")
  if ①+②+③ != 總量: push_error(...)

★而②的定義就是 `rest_v = pass_dt_over2s - ceil_sum - fai_v`
⇒ ★★那條等式【恆真】。它不是判準，是把②的定義再唸一遍。
⇒ ★★★恆真項紅不起來 ⇒ 等於沒有守衛，而它【看起來】像有守衛（還印著「應為 0」）
```

★**換成兩條【假設為假時會長不一樣】的**：

```
if ceil_sum + fai_v > pass_dt_over2s:  ⇒ ①③是量到的,它們的和不得超過母體
                                          超過 ＝ 標籤重複計數／母體對不上
if rest_v < 0:                         ⇒ 同上的另一面
```

★★★**而這正是你今天要我套回自己身上的那條**：
**每寫一條判準，同句問「假設為假時這一格會長什麼樣」** —— 我第一版答不出來。

# 三、未驗（★清單照舊，我不說「應該沒問題」）

```
三個 commit（e0bc00c62 ①②／58eda1554 ④措辭／31edae8ab ④第三格）都沒跑過 Godot
機器：量測員的 freeze_sample_bed（我量過，2 隻）
★等機器空要跑：
  ①`--check-only` 兩支被改的 .gd ★★而這一輪我動的是【控制流】不只是字串 ⇒ parse 風險比前兩次高
  ②join_accept_measure_bed ⇒ 判準是那幾顆數與改名前【逐字相同】
  ③pass_tick_phase_breakdown_bed ⇒ ★★★看三格的數，以及那兩條新守衛【不要】在正常世界就紅
     （★若它們在乾淨世界就紅，那不是抓到缺陷，是我的門檻寫錯）
```

★分支 `feat/probe-key-loop1` ＝ `31edae8ab`。
