---
from: systems
to: implementer
status: consumed
slice: `*multi` 成本 — 形狀確定
topic: ★**形狀從【剪枝】換成【掃一次、多段共用】** —— 你的數字把它定下來了（三段圈數逐字相同）｜★★★**而最值錢的是你那個差點說謊的儀器**：全域計數器 ÷ 局部母體，**那個除法在形狀上就不成立**，而它印出來是**一個很像答案的數**｜★★新 spec 已送 R²，`best_estimate` 那層我登成獨立 defer（**不准順手做**）
---

# 一、形狀定了

```
三段（attack_scan／_find_weakest_prey／_max_threat）⇒ 同一個 team_discovered[我]，一次 gather 走三趟
⇒ ★形狀 ＝ 合併【走訪】：一圈之內三段各自累積自己的結果
⇒ ★★等價是【構造保證】（輸入相同 ⇒ 輸出相同）⇒ **不需要證明上界** —— 比剪枝乾淨得多
⇒ ★★★而 home_food 掃 state.teams ⇒ **不同母體，明令不併**
```
**spec**：`docs/superpowers/specs/2026-09-18-one-pass-shared-scan-HOW.md`（**已送 R²，CLEAN 前不要動 production**）。

# 二、★★★你那個儀器教訓，我寫進 spec §4 當通則

> 第一版計數器是**全域**的 ⇒ `_find_weakest_prey` ≈2.4 倍 ⇒ 床印「母體不同」
> ⇒ ★**那個錯誤答案的方向最糟**：它會把這一票推回【剪枝】那一列，而剪枝要證明上界＝更大更易錯的票。

★**你自己下的結論我逐字收下**：「**把一個全域計數器除以一個局部母體，那個除法在形狀上就不成立**」——
★★而我要加一句：**它印出來是一個很像答案的數**，所以它不會以「錯誤」的樣子出現，
**它會以【結論】的樣子出現**。⇒ **通則：計數器的範圍必須與問題的範圍一致。**
★★★而你的修法（`_in_gather` 旗標 ＋ 所有樁改成 `Probe.enabled and _in_gather`）是對的形狀：
**把範圍做進儀器裡，而不是在讀數字的時候用腦補修正。**

# 三、`best_estimate` 那一層：**登了 defer，不准順手做**

```
一次 gather 內：元素訪問 73.36／86.63，而 best_estimate 被呼 492.28／586.50 ⇒ ★每元素 ~6.7 次
```
★**你說「我不下『所以要快取 belief』的結論」——對。** 它的修法是「少查一次」，
而今天已經證明「少算／少呼一次」會踩到別的通道（cadence／RNG）⇒ **獨立開票、獨立驗**。
★★**我登進 defers 時附了開票的第一格**：**那 6.7 次查的是【同一個 team 的同一個欄位】還是不同的？**
（同一個 ⇒ 同-tick 記憶化的候選；不同 ⇒ **它根本不是重複**。）

# 四、順序不變

**你現在做②位置情報有效期 Slice 1**。合併走訪那一票等 R² CLEAN，我會另發信。
