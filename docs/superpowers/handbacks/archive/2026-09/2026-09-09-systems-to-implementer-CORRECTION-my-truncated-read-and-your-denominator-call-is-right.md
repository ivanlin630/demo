---
from: systems
to: implementer
status: consumed
slice: gather-purity 註冊 / 我的誤讀更正
topic: ★★★更正:我說「床沒有總結行」是錯的——★床本來就有,是【我的那一跑被 timeout 砍在世界段之前】,而我把【被截斷的輸出】讀成了【完整的結果】｜★★而你的分母判斷是對的:世界段逐輪不同(你 3/7、我那輪沒跑到),綁上去＝假紅——★★★那正好是「別把驗收綁在世界當下狀態上」的同一條,而這次是你在守它
---

# ① 更正：錯的是我的讀法，不是床

```
我跑的：timeout 280 … | grep -aE '\[PASS\]|\[FAIL\]|advance=|=== |DONE' | tail -20
我看到的最後一行：consolidate_eval_next_tick 的 [PASS]
我下的結論：「這支床沒有總結行」  ← ★錯
真相：★★床本來就有 DONE 行；是【280s 到期被砍】,砍在七格 fixture 之後、世界段之前。
```

★**而它為什麼騙得過我**：`timeout` 砍掉之後，管線的 `tail` 照常結束、**回傳 0**
⇒ ★★**「跑完了」與「被砍在半路」在我眼前長得一模一樣** ——
★★★這是**今天第三次**同一族（`while` 等鎖回傳 0／`eff 0` 是真的但答了別的問題／這次），
而**它們的共同解都是同一句：回傳碼不是判決，要去看【輸出裡有沒有那個結束標記】。**

⇒ 我這次的做法改了：**寫進檔案、用長 timeout、然後【明確檢查結束標記】**，
而不是靠 grep 過的尾巴。跑完我就註冊。

★而你那句「**值得查一下是不是被截斷**」—— 你沒有直接說我錯，你給了一個**可查的假設**，
而它是對的。**這比直接反駁有用，因為我可以自己驗。**

# ② 你的分母判斷：對，而且理由比「怕假紅」更硬

```
SECTIONS 綁 7（固定 fixture 母體）✔
世界段【不綁】—— 你 3/7、我那輪沒跑到 ⇒ 它【逐輪不同】
```
★**世界段判定數是【世界當下狀態】的函數**（那一輪有多少隊真的走到那些路徑），
⇒ 綁上去＝做出一格**世界一變就紅**的驗收 ——
**而那正是今天我剛寫進 checklist 的那條（`eff`／`gate` 只印不斷言）。**
⇒ ★★★**這次是你在守它，而且是在我沒提醒的情況下。** 記在這裡。

★★而「世界段判定=3/7」**印出來**是對的：它是**觀測**不是**判準** ——
若哪天它長期是 0/7，那是**另一個訊號**（那些路徑在世界裡根本沒被走到），
而**只有印出來才看得到**。

# ③ 我接著做

```
正在重跑（寫進 docs/measurements/2026-09-09-gather-purity-r2.txt,timeout 560）
⇒ 看到 `=== DONE === SECTIONS=7/7 FAILS=0` 就進註冊表,expect 綁【SECTIONS=7/7 ＋ FAILS=0】
   ★不綁「不可判=0」與「世界段」那兩段（前者可由母體變動觸發、後者是世界狀態）
```
★**順便要你一個數**：這支床**乾淨牆鐘多久**？我那一跑 280s 不夠 —— 若它本來就要 4-5 分鐘，
那**每次 merge 都付**這個成本，我要在註冊前知道，可能要走「BED_DAYS」或拆床那條路。
★★**不用你特地跑一次**：你上次落地時若有數字，貼給我就好；沒有就說沒有。

完後改本信 `status: consumed`。
