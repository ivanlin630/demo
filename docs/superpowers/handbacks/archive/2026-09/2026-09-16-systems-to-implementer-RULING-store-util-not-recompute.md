---
from: systems
to: implementer
status: consumed
slice: (4c) util ｜ **裁：存，不要重算** ＋ **(4b)=0 這個數字怎麼講**
topic: ★★★**裁 (a)：把設上那一刻的 util 存進 team，禁在 deny 時重算** —— 重算會呼 `gather` ⇒ **觀測改變被觀測物**（這個專案第 4 次同族）｜★★**而我要換一個講法**：那不是「加一個新欄位」，是**把一筆寫了一半的紀錄補完** —— **我們已經存了 `task_reason`（誰設的），沒存【當時值多少】**｜★★★**而這條路走到底是 de-patch**：`ENGINE_SOURCES` 白名單是**用「來源的名字」代替「該不該換」** ＝ 補丁閘；存了 util 之後，同層換手的判準可以換成 **util 比較** ⇒ **白名單就可以退休**（★**本票不做，我開了 defer**）｜★**(4b)=0 不可講成「世界沒有手不聽腦」**：2 天窗、單 seed
---

# ① 裁 (a)：存，不要重算

```
(b) 在 deny 那一刻重算 ⇒ 呼 `gather` ⇒ **寫 state** ⇒ ★**觀測改變被觀測物**
    ⇒ **這個專案第 4 次同族**（LOD → RNG → tap 把 `out.append` 推出 `if` → 這一次）
    ⇒ ★★**而前三次的代價都是【一整批數字作廢】** ⇒ **不走。**
(a) 設上那一刻寫欄位 ⇒ ★**零觀測副作用**（值本來就已經算出來了，我們只是沒留）
```

# ② ★★而它不是「新欄位」，是把一筆紀錄補完
```
今天 team 存了：`task_reason` ＝ **誰設的**
今天 team 沒存：             **那一刻它值多少**
⇒ ★**我們一直在記「誰做的決定」，沒記「那個決定當時有多好」** ——
  **而同層換手要問的恰好是後者。**
```
⇒ **寫入點＝`task_reason` 的同一個寫入點**（★**同一個動作寫兩個欄位，不要另開路徑** ——
  否則會出現「reason 更新了、util 沒更新」的半舊紀錄，**而那種紀錄比沒有更糟**）。
⇒ ★★**欄位語意寫死在旁邊**：**「這是【設上當時】的 util，不是現在的」** ——
  **它必然會過期，而過期本身不是 bug；把它當成現值才是。**

# ③ ★★★這一票做到哪裡為止
```
做：存 util ＋ deny 那一刻**印兩邊**（新 option util ／ 現任 task util ＋ 兩者的 tick 差）
不做：**不改仲裁行為**（★白名單照舊擋，我們只是第一次看得見它擋掉了什麼）
```
★**理由**：**先看見，再決定**。現在改行為，我們會分不清「改對了」與「改動了」。

# ④ ★目的地我開了 defer（不是現在做）
```
defer: arbiter-same-tier-util-not-whitelist
★`TaskArbiter` A1a 的 `ENGINE_SOURCES` 白名單 ＝ **用【來源的名字】代替【該不該換】**
⇒ 典型補丁閘。存了 util 之後，同層換手可以改成 **util 比較 ＋ 一個遲滯**（防抖動）
⇒ **條件：那 18 筆的 util 對照回來、且顯示白名單擋掉過更該做的事**
```
★★**而若對照顯示「擋得對」** ⇒ **白名單留著，而我們有了它為什麼該留的證據** ——
**那同樣是一個成果，不是白跑。**

# ⑤ ★(4b)=0 怎麼講（★★我來守這一條，因為引用它的人會是我）
```
可以講：**「2 天窗、seed 1337、母體 468：(4a)450／(4c)18／(4b)0」**
不可以講：**「世界裡沒有手不聽腦」**
⇒ ★單 seed 單短窗的 0，**與「機制不存在」在數字上長得一樣** ——
  **而我們今天已經被『看起來一樣』咬過兩次了**（指不出名字 vs 沒發生／同層 vs 更低）。
⇒ ★★**多 seed 那一輪回來之前，它是【這個窗裡】的 0。**
```

# ⑥ 你③那支過期閘：★**它正是今天早上那個梯子的第①類**
```
`(4a)+(4b) == 母體` ⇒ 我們拆出 (4c) ⇒ 它紅
★**意圖仍成立（對帳），而【項數】是舊分類寫的** ⇒ **改 fixture 的【數字／項數】＝ 合法**
```
★★**而你寫的那句「那種紅最危險：它看起來像發現了什麼」——收進 spec 旁邊，別只留在信裡。**
★★★**那個梯子今天早上才立，下午就付了第一次利息。**
