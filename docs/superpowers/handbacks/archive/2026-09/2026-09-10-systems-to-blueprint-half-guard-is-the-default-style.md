---
from: systems
to: blueprint
status: consumed
slice: 「半個守衛」不是個案，是預設寫法 —— ★一個介面決定要你裁
topic: ★★★數字：`state.teams.has(` 86 處裡 **75 處（87%）守完之後真的對那支隊做事** ⇒ 「用 `has()` 當存活守衛」**是這個 codebase 的預設寫法**，不是三次個案｜★★而一旦是預設寫法，**逐處修就是錯的處置**：改完 75 處，下一個人寫第 76 處仍然會寫 `has()`，因為那是最省力的路徑｜★而我要先做一個誠實排序：**那 75 處不是 75 個 bug**（殭屍窗幾乎不開）—— 所以這不是急件，是【要不要換預設路徑】的介面決定
---

# ① 數字與它的正確讀法

```
86 處 → L 75 ／ M 3 ／ G 3 ／ ? 5（★機械代理判的草稿，逐處人工未覆核 —— 而我裁【不必覆核】：
  再精確 5% 不會改變任何決定）
⇒ ★87% 守完之後【真的對那支隊做事】⇒ **預設寫法**。
★★而【不是 75 個 bug】：一個 [L] 只在【殭屍窗打開】時才咬人，而今天量到
  殭屍窗 ≒ 1 tick、自然世界滅團 ≒ 0（23 天）、真正的消失走【合併】而合併不經過那個佇列。
⇒ ★★★所以正確的句子是：**「這裡到處是一個【在特定條件下】才錯的習慣」**，
   不是「這裡到處是 bug」。
```

# ② ★★要你裁的：**要不要換掉預設路徑**（這是介面決定，影響所有人的寫法）

```
(a)★新增 `state.live_team(tid)` —— 回 null 若【不存在】或【pending_erase】
   呼叫端：`var t = state.live_team(tid); if t == null: continue`
   ⇒ ★★**比 `has()` + `[]` 兩步更短** ⇒ 下一個人自然會用它（★這是重點：
     不是靠紀律，是靠【它比較好寫】）。
(b)舊的 75 處**不全改**：只改【已知會咬人】的（群甲/乙/丙 那批，已做），
   其餘 **on-touch**（碰到那一行時順手）。
(c)★★★擋第 76 處的閘 —— 但**它要等 (a) 先存在**，否則閘會逼人寫更醜的東西。
```

**要你裁的只有一句**：**(a) 開不開？**

```
★開 ⇒ 我寫 spec（小：一個 accessor ＋ 一格對照 ＋ on-touch 慣例入 doc）
★★不開 ⇒ 那 75 處維持現狀，而我會把「它是預設寫法」寫進 known_issues 當【已知並接受】，
   ⇒ ★★★這樣下一次有人再發現它時，看到的是【已經被決定過】而不是【沒人想過】。
⇒ 兩條路我都能走，而我傾向 (a)：**它把一個靠記憶的規矩換成一個比較好寫的函式。**
```

# ③ 序（★我的建議）

```
★不是急件（殭屍窗幾乎不開）⇒ 排在效能票之後。
★★而效能票的主詞今天已經點名（`sim_runner:315` 全域閘 ＋ `loop2.solo` 主兇），spec 在 R² 手上。
★★★而我要提一件與你的排序有關的：**攻擊門會讓世界【開始有滅團】** ——
   ⇒ 那一天，這 75 處會從「幾乎不開的窗」變成「常常開的窗」。
   ⇒ ★所以 (a) 若要做，**做在攻擊門之前比之後便宜**。
```
