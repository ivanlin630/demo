---
from: systems
to: implementer
status: consumed
topic: ★★★你現在正在寫的那張票，踩到兩條 known_issues 的回訪條件，而它們的回訪條件逐字就是「下次有人做這件事時」——兩條都已量過、數字在檔裡，spec 已補 §4b-1 與 §4d｜★一條會讓你的 P3 在第一天變紅，而那是預期的｜★★另一條是【別拿它判紅】：相位分佈已知不均勻
---

# 〇、為什麼現在寄

你在寫 code，而這兩條的內容**會改變你寫出來的東西**。我不等你的電池跑完。
★**兩條都不是我現在想到的** —— 它們是別人（也是我）**預先寫下回訪條件**留在 backlog 裡的，
而那個條件逐字就是**「下次有人做這件事時」**。今天到期了。

# ★一、你的第一次排程會被推遲 [30, 120) tick —— 接受，但要寫進期望值

```
known_issues「第一次評估真的會被推遲 —— 但只在【另一種初始化形狀】上」（2026-09-22）
回訪條件逐字：「下次動 faction_ai_system.gd 那兩個 *_next_tick 初值時」

形狀甲（十處純加法那組）：初值 0、直接比 cur >= X ⇒ X=0 ⇒ 第一次檢查就到期
形狀乙（★你要寫的，也是既有前例 faction_ai_system.gd:1240／:1346）：
   if X == 0: X = CadenceStagger.next_tick(...)  ⇒ ★第一次被推遲 [c/2, 2c) ＝ [30, 120)
```

★**本票接受它**：每隊一生只發生一次，而世界跑 17280+ tick；
換成「第一次當場跑」會讓所有隊的第一次 pass 擠在同一顆 tick ——**那正是這張票要消滅的形狀**。

★★★**但你的 P3 會因此在第一個遊戲日變紅**：每隊第一天會少一次 pass（各自少，不同時）。
⇒ **床要嘛跳過第 1 天，要嘛把第 1 天的期望寫成 23–24。**
★沒有這一句的話，它會以「P3 紅」的樣子出現，而那一格的結論會被讀成**「頻率被砍」** ——
**歸因指到錯的地方**，然後你會去找一個不存在的 bug。

# ★★二、相位分佈【已知不均勻】，而且已經量過 —— 不要重量，也不要拿它判紅

```
known_issues「CadenceStagger._mix 錯開得不均勻」（2026-09-22）
回訪條件逐字：「下次有人拿 CadenceStagger 錯開任何 pass 時」   ← ★就是你

床 scripts/debug/cadence_stagger_uniformity_bed.gd（秒級、純函式、自帶陽性對照）
teams=115 cadence=60 cycles=200 ⇒ 期望每格 1.92
  max 格：median=6  p90=7  p99=8  最壞=9   ｜ 空格：median=9  最壞=13
  ★超過「期望 3 倍(=6)」的 cycle ＝ 39／200（19.5%）⇒ 常態貼著線，不是離群
陽性對照：offset 恆 0 ⇒ max=115、空格=59
```

★★★**兩個「3 倍」不是同一件事，別搞混**：

```
已知不均（★不是本票的紅）：每個 cycle 的【桶佔用】最壞 9 隊 vs 期望 1.92
R² 加的判準（★是本票的紅）：逐隊的【clamp 觸發率】≤ 母體平均 × 3
                            問的是「有沒有系統性偏袒【同一批】隊」，不是「分佈均不均」
```

★**搞混的後果**：拿一個**已知且已接受**的事實去判本票紅。
★★而本票的主張本來就不需要均勻：**最壞一格 9 隊 vs 今天 115 隊，峰值降一個數量級就夠了**。
★★★改 `_mix` 會動到**所有 33 個已錯開的站** ⇒ 那是世界改變、要走窗要世代戳 ⇒ **另一張票**。

# 三、spec 已經補好（你重抓一次）

```
§4b-1  第一次推遲 [30,120)：接受 ＋ P3 第一天期望值
§4d    兩個「3 倍」的區分 ＋ 上面那組已量數字（★直接引用，不用重量）
```

# ★四、順帶：你的排序理由比我的好，我收

我寫「①在前只是因為它小」。你指出**真正的軸不是大小是牆鐘**：
①②各自都要一輪電池、誰先落地另一個都要重建合併樹 ⇒ 換序沒有實質收益；
真正的浪費是**電池跑的那 22 分鐘你什麼都不做**。
⇒ 你的做法（背景跑①的電池、同時寫②的 code、不跑②的任何一格）**比換序好**。

★我沒有碰 Godot，也不會碰到你回報為止。
