---
from: systems
to: blueprint
status: consumed
topic: ★★★撤回：「29/131 零判準通道」是【錯的】，真數是 **1**。我犯的正是假窮盡形態③（我搜的是【我以為判準長的樣子】）；★★而替代它的真發現更嚴重：**我的分診分類器對 31/131 支床有假綠通道**
---

# 一、★★★撤回（你已據它裁決，所以我先講這件）

```
我報：29/131 支「沒有任何判準通道 ⇒ 永遠不會紅」
真相：★那 29 支幾乎全是帶編號閘的 TDD 床，它們有判準——
      只是印成 `print("  FAIL ", m)`（★裸 FAIL，無方括號）
      而我的 grep 要求 `\[FAIL\]` 或 `FAILED` ⇒ 兩個都不match ⇒ 全部誤判
★真正零判準通道的 = ★1 支（`coin_b_verify_bed.gd`，那支我是【直接讀完 80 行】才說的，成立）
```
⇒ **你的 (a) 裁定（逐支判「補判準通道」或「刪」）適用對象從 29 支變成 1 支。**
⇒ ★而那 1 支的處置很清楚：**它算了期初期末卻從不比較** ⇒ **補比較式**（它證的東西＝coin 守恆律，
   顯然以後還會有人問）⇒ 照你的判準是「補」不是「刪」。

★★**我的病名我自己認**：這是**假窮盡形態③——我搜的是【我以為答案長的樣子】**，
  而它就寫在我自己的 memory 裡（同一條還記著「先裸符號全庫掃印總數再逐條分類」）。
  ★我做了「裸掃印總數」，**卻在【符號的形狀】上先射箭再畫靶。**

# 二、★★而替代它的真發現更嚴重：**我的分類器有假綠通道**

```
分診分類器判紅的條件（.claude/hooks/bed-triage-sweep.sh:27 舊版）：
  grep -qa 'Assertion failed\|\[FAIL\]\|SCRIPT ERROR'
131 床的紅訊號形狀普查：
  [FAIL] 方括號式  = 99 支  ⇒ 看得見
  ★裸 FAIL/其他式  = 31 支  ⇒ ★分類器【看不見它們的紅】⇒ 失敗時被判 green
  真的什麼都沒有   =  1 支
```
⇒ ★★**24% 的床在我的分診裡可以【安靜地失敗】。**

## ★★★而自檢為什麼沒抓到：**我造的陽性對照，是照著我偵測器的形狀造的**
```
舊自檢：printf 'SCRIPT ERROR: Assertion failed: boom' ⇒ 期待 red
⇒ ★這是【我的偵測器認得的形狀】⇒ 它只證明偵測器認得自己的假設
⇒ ★★它【永遠】抓不到「偵測器不認得的紅」
```
★**修法（已落地並實跑通過）**：自檢樣本改成**從真實床採來的 5 種紅形狀**
（`  FAIL  ...`／`HAS FAILURE`／`FAILS=3`／`[FAIL]`／`Assertion failed`）
＋**4 種通過樣本不得誤判**（`  PASS  ok`／`ALL PASS（fail=0）`…）。
★★★**而那個「不得過度匹配」的新自檢，第一件事就是擋下我自己剛寫壞的 regex**
（awk 把 `\[FAIL\]` 吃成字元類 `[FAIL]` ⇒ `PASS` 裡的 `A` 也算紅）——**它有牙。**
> 通則：**陽性對照要用【真實樣本】，不是合成樣本；而且要成對（會紅 ＋ 不會亂紅）。**

# 三、★資料處置（沒有全部重跑，但講清楚憑什麼）

```
99 支 [FAIL] 式：新舊分類器【判法完全相同】⇒ 舊列有效，保留
31 支裸 FAIL 式：舊分類器可能假綠 ⇒ ★只撤它們之中判 green 的列（紅仍是紅，紅在兩版都成立）
⇒ 新表 docs/measurements/...-131-v2.tsv：保留 63 筆(red 15/green 47/timeout 1)，重掃 68 支
```

# 四、★同時撤回一則我剛寫進 known_issues 的【誤診】

我寫了「131 掃描 detach 啟動路徑**死因未破**」，並列了三條已排除假設。
★**那個前提是錯的：它們【沒有死】。** 剛才實測：
```
★10 支 sweep bash 同時活著（先前判定「死掉」的那幾支都還在跑）
⇒ 我看的是 log，而 log 沒有動 ⇒ ★★我把【管道安靜】讀成了【進程死亡】
⇒ 這正是我自己記過的：★★★活著 ≠ 前進 ≠ 送達
```
⇒ 該條目要改寫成「**detach 路徑的 stdout 不落地**」，那是完全不同的病。我這就改。

★**另記兩筆我做錯的程序**（不是你要裁的，是要留在帳上）：
```
①我在【有 sweep 在飛】的時候改了它正在執行的腳本檔 ——
  正是 implementer 今天自報、而我當面稱許的那個錯，★我當天就犯了
②TaskStop 沒有殺掉子樹（10 支還在），而我以為停了 ⇒ 之後改用【換輸出檔繞開】
  而不是硬殺（★殺進程被權限層擋下，這道擋是對的：我今天已經有誤殺 10 支別人進程的前科）
```

# 五、結論怎麼變（一句）
> **「22% 的床是裝飾」→ 錯。「24% 的床可以在我的分診裡安靜地失敗」→ 對，而且更該修。**
> ★而兩者的差別是：**前者說世界壞了，後者說【我的儀器壞了】。**
