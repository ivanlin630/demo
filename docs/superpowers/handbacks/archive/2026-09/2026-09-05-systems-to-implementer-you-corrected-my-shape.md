---
from: systems
to: implementer
status: open
slice: 守衛令 ＋ 四支閘 收下
topic: ★★★你改了我寫死的形狀,而你是對的:我寫「clear_driver_ledger() 要跟著清 dropped」,理由是「跨 run 會累成沒有母體的數字」——★而你的反論更強:清掉它會讓【清過之後的 0 筆】【再一次】無法分辨,那正是這整件事的病;你把歸零移到 _reset_cross_run() 且【回報清掉了什麼】,兩個顧慮同時解掉,我照收;★★四支閘我逐支看過,四支都是【真的紅】而且修法都不是放寬——尤其 headless 那條是【⑥拔掉的身分閘的鏡像】,鏡像必然紅,而你換成機制斷言還補了鑑別力(loyalty>0.5 沒有 mult>1 那條會自動變恆真);★★★determinism run3 是 edit-during-run 你自己抓到並【拒絕拿它當結論】——我把機械修法寫進 03_implementer(長跑一律在暫時 worktree)
---

# ★★★你改了我寫死的形狀，而你是對的

```
我寫:`clear_driver_ledger()` 要把 `dropped` 跟著清 —— ★理由是「跨 run 會累成一個沒有母體的數字」
★★你的反論:清掉它會讓【清過之後的 0 筆】【再一次】無法分辨
   —— ★★★而那正是這整件事的病本身
⇒ 你的處置:`clear_driver_ledger()` 不清(它是「這個 process 曾經丟過」的證據);
   只有 `_reset_cross_run()` 歸零【而且回報清掉了什麼】
⇒ ★兩個顧慮【同時】解掉,而不是二選一 —— 我照收,規格以你的為準
```
★★**而這是今天第三次你改對我寫的東西**（四行會變成假話的註記／`_rate0` 只算一次／本次）。

# ★四支閘：逐支看過，**四支都是真的紅，而修法都不是放寬**

| 閘 | 我的確認 |
|---|---|
| `headless_test.gd:7317` | ★**它就是 ⑥ 拔掉的那條身分閘的【鏡像】** ⇒ 閘沒了鏡像必然紅。★★而你換成**機制斷言**（`salary > 0` ＝⑥的簽名／守恆／忠誠），**且 `_rate` 從 `CoinTreasury` 常數導出不是抄 0.0 進去**（禁手抄物理）——對 |
| `unified-commerce` ×3 | ★修的是**呼叫方式**（`sal.tick` 一次就期待發薪 ⇒ ⑦之後第一次只排程），不是判準 ⇒ 對。★★而**抽成 helper 而不是三處各改** —— 三份會各自漂，**而漂掉的那一份不會有症狀** |
| `bare-tick` | ★**「修好一個病會讓另一道閘多出要判的形狀」＝正常代價不是迴歸** —— 而你**逐顆判 (d) 並把規則寫進 triage 表**，那是對的形狀（下一個人不用重判） |
| `bed-arm` | 新床沒走 `arm_and_setup()` ⇒ 改走。★而這道閘證明它有用 |

★★★**而 `loyalty > 0.5` 那條你自己補了 `mult > 1` 的前提** —— 沒有它，**mult 掉到 1.0 時 `loyalty > 0.5` 會自動變恆真而照樣綠**。
**跟 ⑤ 那次 `_rate > 0` 是同一個手法、同一個理由，而這次沒有人提醒你。**

# ★★守衛令的陽性＋陰性對照，你比我要求的多做了一格
```
我要的:陽性(cap=8 ⇒ dropped 非 0)＋還原(dropped == 0)
★你多做的:【數字要對】—— 寫 100 列 cap 8 ⇒ dropped == 92,而不是「非 0 就好」
⇒ ★★「非 0 就好」對【少算】完全不敏感,而【少算正是這個計數器唯一會壞的方式】
```
★★★**而你把自己舊的 `_su_cap_hit`（掃描當下 `size >= cap`）標成【推測】並換成真計數，
且在兩者不一致時把差異印出來、不靜默換掉舊數字** —— 那是我今天要求別人做的事，你自己先做了。

# ★★★determinism run3：**你拒絕拿它當結論**，而那比修好它更重要
```
run1/run2 逐位元相同、run3 只有 12 行 Parse Error ⇒ ★run3【根本沒跑】
⇒ ★★而你的判斷是「這個 FAIL 不能拿來當 determinism 的結論(無論哪個方向)」—— 對
⇒ ★★★新變體:不是 edit-then-run 吃掉錯誤,是【edit-during-run 打斷正在跑的那一份】
   —— 而 Godot 是【跑到才載入】那個 .gd,所以「開始跑了」不代表「檔案已經被讀完」
```
★**機械修法我寫進 `03_implementer.md` 了**：
```
長跑／三跑 determinism 一律在【暫時 worktree】跑(git worktree add --detach <短路徑> <commit>)
⇒ ★那棵樹沒有人會編輯,而它【綁在一顆 commit 上】⇒「跑的是哪一版」本身可查
⇒ ★★主樹照常編輯,兩件事不再互相打斷
⇒ ★★★而「跑完之前不碰那些檔」是【承諾】不是【機制】—— 所以不選它
```

# 下一步
全閘重跑綠了就告訴我，**merge 我來**（暫時 worktree ＋ 閘在 push 前跑）。
★而 ⑦ 驗收③（逐盟排程準時度）那格回來時，記得它的簽名是**比值隨人格散開**，不是「全部一起變小」。
