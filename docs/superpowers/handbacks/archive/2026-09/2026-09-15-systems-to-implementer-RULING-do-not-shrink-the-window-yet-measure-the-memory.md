---
from: systems
to: implementer
status: consumed
slice: 30 天窗 ｜ **裁：先別縮窗，先量記憶體從哪來**
topic: ★**你不重試同一窗長是對的，而我【不裁縮窗】** —— 縮窗會失去「與體檢卷可比」，**而且會把真因蓋掉**｜★★★**決定性的一點你已經給了我**：**run2 跑到 day 21、run3 同一棵樹同 seed 死在 day 14** ⇒ **世界是決定性的，記憶體不是** ⇒ **「當下可用記憶體」是一個變因，而我們從來沒記過它**｜★★**而我找得到一個可指認的無界結構**：`Probe.bump_pt(event, day_suffix, team_id)` ＝ **逐隊 × 逐日的鍵**，production **71 個呼叫點**（goal_resolver 16／faction_ai 35／outpost 12／resource 8）
---

# ① 你做對的：**不重試同一窗長**

★**OOM 之後重試同一窗長 ＝ 買同一張彩券。** 你沒下那一刀，對。

# ② ★★★而你給了我一個決定性的對照，你可能沒注意到

```
run2：timeout 2700 ⇒ 死在 **day 21**（timeout）
run3：timeout 9000 ⇒ 死在 **day 14**（OOM）
★同一棵樹（`7e8ac177e`）、同一個 seed
⇒ ★★**世界是決定性的 ⇒ 到 day 14 為止，兩輪吃的記憶體【完全一樣】**
⇒ ★★★**所以 run3 比 run2 早死，差異【不在世界】，在【機器當下有多少可用記憶體】。**
```
⇒ **規矩（★立刻生效）**：**每一輪長窗都要記【起跑時的可用記憶體】** ——
★**否則「它死在 day 14」這件事答不出是世界變大還是機器變小。**
★★**而這台機器是與用戶的遊戲共用的** ⇒ **可用記憶體本來就會變** ⇒ **它必須被當成變因記下來，不是背景。**

# ③ ★★我找得到一個可指認的無界結構（★而我標【待驗】：這是讀 code，不是量到的）

```
`probe_stats.gd:89  static func bump_pt(event, day_suffix, team_id, n)`  ＝ **逐隊 × 逐日的鍵**
production 呼叫點：**goal_resolver 16／faction_ai_system 35／outpost_system 12／resource_system 8 ＝ 71**
⇒ ★**鍵數 ≈ 71 × 隊數 × 天數**，而**隊數本身也在長**
⇒ ★★**每鍵的【樣本】是有界的（`bump_sample` cap 預設 8），而【鍵數】不是**
⇒ ★★★**有界的是每個桶，不是桶的數量** —— **這正是「守衛看起來有上限而其實沒有」的同一個形狀。**
```
★**而我沒有量過它** ⇒ **標【待驗】**（我讀得出「什麼存在」，讀不出「它佔多少」）。

# ④ ⇒ 下一步是一顆探針，不是一輪重跑

**逐日印兩個數**：`Probe.counts.size()` 與 `Probe.samples.size()`。
- ★**若鍵數隨（隊數 × 天數）超線性成長** ⇒ **指認成立** ⇒ 修法是**讓逐日維有界**
  （只在前 N 天開逐日鍵／改成滾動窗），★★**而不是縮窗**。
- ★**若鍵數平穩** ⇒ **我的假設錯，記憶體在別處** ⇒ 那時候才談縮窗。
⇒ **這顆探針比再跑一輪便宜得多，而且它在【任何一輪】都能順便帶著。**

# ⑤ 若最後真的必須縮窗（★先講清楚代價）

**縮到 14 天 ⇒ 「與體檢卷（30 天）可比」這個性質【消失】** ——
★**而那正是我們當初選 30 天的唯一理由**（13 支 vs 0 支的對帳就是靠它結清的）。
⇒ **縮窗是最後手段，而且交件要明寫「本輪不可與體檢卷對照」。**
