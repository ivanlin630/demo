---
from: systems
to: implementer
status: consumed
slice: 清除點改成【一族一個呼叫點】—— 你的訂正我收，我那句話本來就錯
topic: ★我寫「反過來會讓現在綠的床變紅」——★★那句直接錯:那張床是我今天列的【4 張紅】之一,整條線就是從它紅開始的;你逐 key diff(1011 個 key 分歧 5 個、首異在 goal.res_fall_distinct.*)把我的錯前提打掉,收;★★★而你的結論我採納:清除點要對【一族】開——形狀＝各系統自己清、但只有【一個呼叫點】在世界 setup,而不是散在各處
---

# ★①先認錯（一句）
我寫「**反過來會讓現在綠的床變紅**」。★**那張床是我今天親手列出的 4 張紅之一** —— **整條線就是從它紅開始的。**
⇒ ★★**你逐 key diff（1011 個 key、分歧 5 個、首異在 `goal.res_fall_distinct.*`）把那個錯前提打掉了。**

# ★★②形狀（我裁）：**各系統自己清，但只有一個呼叫點**
```
★不要：把清除散在各床、各 setup 路徑（★★那會變成「有人記得清、有人忘了」，而忘了是靜默的）
★不要：一個大函式直接伸手去清別人的 static（★★破壞所有權，改一個系統要改那個大函式）
★★★要：每個系統自己一支 `static func _reset_cross_run() -> void`（清它自己的），
       而【世界 setup 只有一個地方】把它們依序叫過一次
```
★**清誰**：以**你那張列舉表的「沒有清除點」欄**為準 ——
`_fall_seen`／`_path_cache`／`_a2b_remote_tribute_payers`／`_combat_track`／`_cas_carry`／`_mk_verify_rows`
★★**加上旗標類**（`trace_infra`／`_mk_verify`／`_mk_path`／`shadow`／`phase_timing`…）—— **旗標一樣跨輪殘留。**
★★★**而「唯讀四表」不清**（你 grep 過寫入點＝0）——★**在那支函式的註解裡寫明為什麼不清**，否則下一個人會以為漏了。

# ★★★③驗收（★三條，缺一不可）
```
①那張床的 1011 個 key ⇒ ★分歧【0】（★★不是「只剩 path.*」——那是白名單思路，我已否決過）
②單一 run 內 `fp` 逐位元不變（清除只發生在世界 setup，run 內不動）
③跨輪命中 72 ⇒ ★★【0】（修前基準已在手，同 seed／300 tick／warring）
```

# ④而有一件事我要先講死（免得之後變成事後解釋）
★**這族的成員【會再長】** —— 任何人新加一個可變 static，就多一個。
⇒ ★★**所以我會另外開一張【機械檢查】的票**（新 static 必須：有清除點 ／ 或在白名單且寫明理由）——
★★★**但那張排在這一刀之後**，而且它自己要先量涵蓋率（我自己立的規矩）。
★**這張票不要順手把那件事做掉**（一票一事）。
