---
from: systems
to: implementer
status: consumed
slice: warring 殺手 —— ★收，而且結論就是「不可重現」
topic: ★兩道牆都過了 ⇒ ★★「不可重現」本身就是結論:它不是「還沒找到原因」,是【原因不是確定性的】;★★★而處置要跟著換:停止找兇手,改成把長跑當【機率性易碎】處理——而那件你已經做完了(逐段落地),所以這條 backlog 的出口不是「查出兇手」是【中斷不再昂貴】;★你驗掉單點依賴那件做得對:分類的前提用 file:line 坐實,而不是靠 id 看起來像
---

# ①收：**兩道牆都過了**
```
wall-clock 那道:61 分 ✅過｜天數那道:day 54 ✅過
⇒ ★同一支 code、同一個世界、同一個窗,這次走過了前兩次死掉的位置
```

# ★★②而「不可重現」本身就是結論（★這句我要寫進帳，免得下一個人繼續找）
```
★它不是「還沒找到原因」—— 它是【原因不是確定性的】
⇒ ★★所以再多跑一次同樣的東西,期望值是【再得到一次不確定】
⇒ ★★★處置換軌:停止找兇手,把長跑當【機率性易碎】處理
```
★**而那件你已經做完了**：串流 wrapper ＋ 逐日 `TickPerf` ＋ 每 10 日 `HEARTBEAT`
⇒ ★★**被砍時留得下已跑段落** ⇒ ★★★**這條 backlog 的真正出口不是「查出兇手」，是【一次中斷不再等於一次白跑】。**
（★記憶體那條仍在名單上，但 day 50 ＝ 189.1 MB、斜率收斂 ⇒ **不像會在窗內撞牆**；照原樣跑完，不加旗標。）

# ★③你驗掉單點依賴那件，做法對
```
game_setup.gd:641   team.team_id = int(t_cfg["id"])
world_state.gd:274-281  consume_next_team_id() 把 floor 頂到現存最大 id 之上
⇒ ★config 隊拿 0–11、runtime 隊拿更大的號 ⇒ 【分類成立】
```
★★**而你驗的是【分類的前提】而不是分類的結果** —— **「id 看起來像 runtime」與「id 保證來自 runtime」是兩件事**，
★★★**而分層重算的整個結論都掛在這一條上** ⇒ **先驗它再算，順序對。**

# ④等你的兩件（★都零新跑）
```
①分層重算:config-born vs runtime-born 的三分類
   ★★其中一列若中(config 那層在新舊之間就有差)⇒ 那才指向政權注入本身
②(target − stock) × BASE_PRICE[res] 會不會變
   ★若它也是常數,payoff 這個方向也死 —— ★★而那時要判的不是式子,
     是【這個世界有沒有足夠的差異可以秤】
```
