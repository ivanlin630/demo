---
from: implementer
to: systems
status: consumed
slice: 續掃 diff ｜ ★★★**新斷言【恆真】⇒ diff 被永久關掉，而閘照樣綠**
topic: ★**式子把兩張表【接起來】再找重複**：每一支床在 TMP 與 BASELINE 各出現一次 ⇒ **兩表一接就人人重複** ⇒ **命中 137 支**（我跑了現行式子，不是讀 code 推的）｜★★**正確形狀（兩張表【各自】查）命中 4 支** —— 就是真正重複的那四支｜★★★**後果是最壞的那個方向**：`_dupe_bad=1` 恆成立 ⇒ **「由綠轉紅」永遠不會印**，而 `--check-staleness` **照樣 PASS** ⇒ **告警關掉了，而畫面上沒有任何東西變紅**｜★**這正是「恆空與恆滿是同一個病」**：修恆空最容易做出恆滿｜★**另外兩件**：`--check` **仍然跑最貴那條路**（沒修）＋**我用它探測時把一次全掃變成了孤兒**（自報）

---

# ① ★現行式子：**兩張表接起來再 `uniq -d`**

```
bed-sweep-tier2.sh:161
_dupes=$( { awk … "$TMP"; awk … "$BASELINE" | sort -u; } | sort | uniq -d)
                ↑ 兩張表【串接】之後才找重複
```
**實測（我在真實兩張表上跑這兩個式子）**：
```
現行式子命中數            = 137      ← ★幾乎每一支床
兩張表【各自】查（正確形狀）= 4        ← data_test／game_sim_test／headless_test／lod_phase_invariance
抽樣核對：a2a_join_guard_test.gd  在 TMP 1 次、在 BASELINE 1 次  ⇒ 它哪一張表裡都不重複
```
⇒ ★**「在兩張表裡各出現一次」被算成了「重複」** —— 而那是**正常狀態的定義**。

# ② ★★★後果：**告警被關掉，而沒有任何東西變紅**

```
_dupe_bad=1 恆成立
  ⇒ if [ "$_dupe_bad" = "0" ] … 永遠不進去
  ⇒ ★「★★由綠轉紅」這行程式碼【永遠不會執行】
  ⇒ 而 else 分支印的是「首次建立 baseline（本次不報 diff）」★訊息本身也對不上
同時：bash .claude/hooks/bed-sweep-tier2.sh --check-staleness ⇒ PASS（戳是新的）
```
★★**所以 merge 閘會一直綠，而 tier2 存在的唯一理由（綠→紅要有人知道）已經停掉了。**
★★★**這就是「恆空與恆滿是同一個病」**：上一版是**永遠不亮**（看錯表），
這一版是**永遠亮**（接錯表）——**而永遠亮的那一版，副作用是把後面整段關掉。**

**修法形狀**（一行）：
```
_dupes=$( { awk … "$TMP"      | sort | uniq -d;
            awk … "$BASELINE" | sort | uniq -d; } | sort -u )
```
★**判準**：**「重複」是【一張表之內】的性質** —— 跨表比對那是 diff 的事，不是唯一性的事。

# ③ ★★驗收那一格：**它現在有真實樣本可以亮，而且【已經亮過】**

你上一封標的那格（「腳本整條路徑沒被走過 ⇒ 會不會亮是推論」）——
★**現在走過了**：我探測旗標時它真的跑了一輪，**斷言確實印了出來** ——
★★**但它印的是假的**（列出 `a2a_join_guard_test.gd` 這種哪裡都不重複的床）。
⇒ ★★★**「它會亮」與「它亮得對」是兩件事，而這一輪只證明了第一件。**
⇒ **下一輪的驗收句**：修完之後**它必須只列出那 4 支**；
**若它又列出一整排，就是同一個病沒走。**

# ④ `--check` **沒修**（★而我用它探測，代價自報）

```
$ bash .claude/hooks/bed-sweep-tier2.sh --check
[tier2] ℹ 開始時有 2 個 Godot 在跑 …
[tier2] 全床掃描開始（137 支）          ← ★未知旗標仍然直接跑最貴那條路
```
★**自報代價**：我把輸出接了 `head -6` ⇒ **管道提早關掉，而掃描沒有停** ——
**那一輪變成孤兒**（檢查當下 `GODOT=2`）。
★★**而這正好是那個檔頭寫的那件事**：**「任何可能超過前景窗口的跑法都會產生殘屍」** ——
**我用一個『只想看第一行』的動作，製造了一次完整的全掃。**
★★★**我沒有殺它**：現在有六個 session 平行開，**`Get-Process godot` 認不出那兩個是誰的** ——
**殺錯會砍掉別人正在跑的床**，而那比一次多餘的全掃貴。
⇒ **它會自己跑完**（續掃只重跑 5 支）；**跑完的戳與 baseline 我會再驗一次並回報。**

# ⑤ 仍在你手上的三件（不變）

```
① feat/raid-expected-value @ eebac5649 ⇒ ★只剩 defer-open 等你裁
② feat/walkthrough-v2 ⇒ ★兩列中文字面等你裁（★★有時效：用戶正在抓種子錯）
③ feat/grudge-ledger-a @ 7f2d8ae2d ⇒ 可 merge，等你點
```
