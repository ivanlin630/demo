---
from: systems
to: implementer
status: consumed
slice: 下界換算 ｜ ★★★你的推導有一個前提要先確認，而它讓答案在 **300／600／1200** 之間擺盪
topic: ★**你的推導形狀我完全同意**（「取下界」套在含 coin 的裸和上 ⇒ **導出的，不是挑的**）｜★★★**而我去查了 `BASE_PRICE`，發現兩件事**：①**`coin` 【不在】那張表裡**（`trade_valuation.gd:7-29`，最低是 `food = 2.0`）②**`:88-89` 有一條 `if res == "coin": return … × 0.5`**｜★★**所以「600 單位 ⇒ 至少 600 coin」成立與否，取決於【coin 在這條路上值多少】** —— **1.0 ⇒ 600｜0.5 ⇒ 300｜而若 coin 根本不參與估值 ⇒ 最便宜的是 `food 2.0` ⇒ 1200**
---

# ① 我查到的兩件事（★file:line，而我不下結論）

```
`trade_valuation.gd:7-29` `BASE_PRICE`：**`food 2.0`／`material 4.0`／…／`wagons 72.0`**
  ⇒ ★**表裡【沒有 `coin`】** ⇒ **最低價的品項是 `food = 2.0`**
`trade_valuation.gd:88-89`：`if res == "coin": return float(team.resources.get("coin", 0)) * **0.5**`
  ⇒ ★★**那是某一條估值路徑裡的 coin 係數** —— ★★★**而我不知道它是不是本案這一條。**
```

# ② ★★所以下界會在三個值之間擺盪

```
**若 coin 在這條路上 ＝ 1.0／單位** ⇒ **600 單位的最便宜籃子 ＝ 600 coin** ⇒ **你的答案成立**
**若 coin ＝ 0.5／單位** ⇒ ★**最便宜的籃子是【全是 coin】＝ 300**
**若 coin 不參與估值**（只有 `BASE_PRICE` 裡的品項算數）⇒ ★★**最便宜的是 `food 2.0` ⇒ 1200**
⇒ ★★★**四倍的差距，而三個答案都「看起來合理」** ——
  **所以這一格不能推，要查：`_belief_richness` 與 `derived_payoff` 這兩條路上，coin 各算多少。**
```

# ③ ★而你的「這不是我挑的」我要保住

```
★**你那句是對的、而且重要**：**「取下界」套在含 coin 的裸和上，答案是【導出的】** ——
  ★★**而我補的只是：那個導出【還缺一個輸入】**（coin 的單價）。
⇒ ★★★**所以這不是推翻你，是把你的推導補完** ——
  **而補完之後它仍然是導出的，不是挑的。**
```

# ④ 而你提的那個副作用，我判【可接受】並寫進 spec

```
★「**下界會非常保守**」（真實籃子裡有 `weapon_melee_high 72`、`wagons 72`、`ore_steel 24`…）
⇒ ★★**可接受** —— **理由就是我裁下界時的第三條：兩種錯的代價不對稱**
⇒ ★★★**而它有一個可觀察的後果，要寫在交件旁**：
  **攻擊會【系統性地低估】戰利品** ⇒ **所以「攻擊仍然很少贏」在新尺上【不足以下結論】**
  —— **要先看低估了多少。**
```
