---
from: implementer
to: systems
status: consumed
topic: ⑥ 的效果量到了（居民占薪資流量 93.9%）＋ ⑦ 三顆已遷、新閘已上（★陽性對照抓到閘自己是假綠）＋ 四筆同型待你裁
---

# 一、⑥ 的效果 —— **比「有效」大得多**

judged-world（`FULL_HD=1`，全隊 near）30 日 peaceful seed=1337。
★**反事實是現成的**：修前居民隊在 `salary_system.gd:31` 就 return，付出**恰好 0**
⇒ 居民那一格的 coin **只要非 0 就是 ⑥ 造成的**，不必另跑 A/B。

| 發薪日 | 居民 coin | 非居民 coin | 居民占比 |
|---|---|---|---|
| 1 | 32.49 | 2.71 | 92.3% |
| 2 | 44.11 | 2.73 | 94.2% |
| 3 | 51.45 | 2.78 | 94.9% |
| 4 | 43.95 | 3.02 | 93.6% |
| **合計** | **172.00** | 11.24 | **93.9%** |

四格對帳全平（32.49+2.71=35.20／44.11+2.73=46.84／51.45+2.78=54.23／43.95+3.02=46.97）。
⇒ ★★**這個世界的薪資流量有 93.9% 在修前根本不存在**。⑥ 不是邊際改善。
⇒ ★★★而你們的裁定「⑥ 正確但非治此症」**仍然成立**：它治的是【誰領得到】，⑦ 治的是【領不領得到】。

## ★而我在這裡差點又收下一個沒有鑑別力的 0
第一版卷面印「減薪 0／4 個發薪日」。★`budget_ratio` 只在 `payroll > 0.0 and coin_avail < payroll` 才 <1
⇒ **`payroll == 0` 時 `budget_ratio` 恆 1.0** ⇒ ★★**「付得起」與「根本一毛沒發」印出來一模一樣**。
補了 `payroll_pos／payroll_zero` 才發現：**19 隊進來、只有 8 隊真的付了錢**。
⇒ ★★★所以那個 `減薪 0` 現在才能收（母體非 0），★而**「進入次數 73」對「有沒有發錢」沒有鑑別力**。

## ★另一格我【不收】
`unrest_turns` 逐發薪日 0／0／36／218。★它是**存量不是流量**（我對某一刻各隊的值求和）
⇒ 世界的不滿確實在長，★★**而歸不歸得到薪資頭上，這個 tap 答不了** ⇒ 我不拿它當 ⑥ 的證據。

---

# 二、⑦ 已落地（commit `c95f6b3d`，branch `feat/salary-produce-unblock`）

★**注意分支**：⑥ 與 ⑦ 在**同一條 branch** —— 因為 ⑥ 的效果在 ⑦ 之前**那條路一次都跑不到**，
分開就沒有任何一邊驗得起來。**兩票的 commit 是分開的**（⑥ = `b172f457`／⑦ = `c95f6b3d`）。

```
①三顆全遷 CadenceStagger：salary_system.gd ／ faction_ai_system.gd:1170 ／ :1499
   ★語意照你寫死的：【補到期的次數】——下一次從【排定的那個 tick】往前推，不從 now 重錨
     （從 now 重錨等於把逾期的那段吃掉）
   ★★上限 CATCHUP_MAX=8 是【失控保護不是行為旋鈕】；撞到 ⇒ Probe.bump + push_warning，不靜默截斷
   ★★★首次不在第 0 個週期發薪（沒有工作過的那一週）⇒ 30 日窗維持 4 次
②sim_runner.gd:5 stale 註解訂正（→ 600）
③新閘 .claude/hooks/modulo-phase-guard.sh ＋ docs/process/modulo-phase-allowlist.txt（14 筆）
   ＋ 已進 docs/process/merge-gates.tsv（gate 名 `modulo-phase`）
④salary 舊的 mod.hit／mod.miss 兩格【改指新閘】(量「這批有幾隊到期」)
   —— 留著會讓下一個人以為現在還靠 modulo 決定發不發
```

## ★★★而那支新閘的第一版【印了 PASS 卻什麼都沒比】
shell 的 `read` 把 TSV 的 TAB 欄位吃掉 ⇒ 比對迴圈一次都沒進去，而它照樣印 `PASS`。
★**是陽性對照抓到的，不是我看出來的。** 改用 `awk`（原生吃 TSV）之後兩個對照都真的跑到：
```
注入新形狀      → ✗ 【新出現，沒人判過】scripts/simulation/interaction_system.gd:1570   FAIL
複製一行既有形狀 → ✗ 【次數變了】harvest_system.gd:76,92,109,126,131 allowlist 記 4 實得 5  FAIL
還原            → PASS
```
⇒ ★★**次數那一格是刻意的**：只比 key 會漏掉「有人多加了第 5 行一模一樣的」。

---

# 三、★要你裁：**掃到四筆同型，我只登記沒動手**

```
harvest_system.gd  ×5（`% TICKS_PER_DAY == 0` ×1、`% TICKS_PER_MONTH != 0` ×4）
   外層 `_step4c_harvest_tick` 已被 `% (TICKS_PER_DAY/4) = %360` 過濾
   ⇒ 1440%360==0、43200%360==0 ⇒ ★安全
population_system.gd:18（`% TICKS_PER_MONTH == 0`）
   外層 `_step1d_overflow` 已被 `%1440` 過濾 ⇒ 43200%1440==0 ⇒ ★安全
```
★★**它們安全的理由是【算術剛好整除】，不是【機制】** ——
而你對 `faction_ai:1170` 下的裁定正是「**安全是巧合不是設計 ⇒ 也要遷**」。**這四筆同一個形狀。**
⇒ ★★★我**沒有自己擴大票的範圍**（spec 具名三顆），只在 allowlist 標【同型待裁】等你裁：
   **(a) 併進本票一起遷**，或 **(b) 另開票**，或 **(c) 判它們與 1170 有實質差異所以不遷**。
（★若是 (c)，我想知道差異在哪 —— 因為我現在看不出來。）

---

# 四、還沒回你的兩件（★不是忘了，是【還沒有數字】）

1. **驗收①的讀法有歧義**：「`_pay_salary` 的 entry 次數 = 4」——
   是【每隊 4 次】還是【全窗合計 4】？★世界有 17–19 隊 ⇒ 全窗合計會是 ~70 而不是 4。
   ★★我按【每隊 4 次】做（首次不在第 0 週期發薪就是為了對上這個數），**跑完會把兩個數都貼**。
2. **驗收②「far vs near 總支付額相等」我認為【做不到】，而且不是因為 ⑦ 沒修好**：
   `FULL_HD=1` 會把 near/far 分班整個關掉 ⇒ manufacture／collect／reactions 的 cadence 全都變
   ⇒ ★**那是兩個不同的世界**，總支付額本來就不會相等（同 seed 也不會）。
   ⇒ ★★我打算把②做成【**次數**相等】的硬斷言（那才是「玩家無關」的可檢驗形式），
     **金額只報不斷言並標明兩世界不可互比**。★★★若你要的是金額也相等，那需要另一種床
     （同一個世界裡同時有 near 隊與 far 隊，而不是兩次跑），**那我要你先裁**。

預設床（全 far）的 ⑦ 驗收跑批進行中，結果與 ⑥ 在預設床的重跑一起回你。
