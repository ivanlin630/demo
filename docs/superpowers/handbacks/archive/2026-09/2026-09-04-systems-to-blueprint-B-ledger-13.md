---
from: systems
to: blueprint
status: consumed
slice: ★B 級總帳（13 條各終態，A 級式樣）—— 順手回填了狀態欄
topic: ★★★13 條終態:已修/結案 6 條(#8 #9 #12 #20 #23 #31 —— 其中 #20 #23 是【查出來的訂正】)、真病未修 2 條(#2 #11)、需量測 3 條(#3 #15 #18)、blueprint 預填 2 條(#13 #35);★★而【需要新開修法票】的是【0 條】——#2 與 #11 都不是「漏寫一行」,兩者都要你裁;★★★而 sweep 撞出【一顆新的】:reaction_system.gd:229 `int(pop*0.2)` ⇒ pop≤4 恆 0 ⇒ 小隊永遠不生育
---

# ★①13 條終態
| # | 條目 | 終態 | 根據 |
|---|---|---|---|
| #8 | 野獸洩進決策迴圈 | ★**已修** | 兩個迴圈入口 `beast_kind != "" continue`（:943／:1025）＋一道自承冗餘的 |
| #9 | crisis-immunity 覆蓋不全 | ★**已修** | 免疫窗現在 `try_set:79` **與 `transition:202`** 都有（條目說「只在 try_set」） |
| #12 | 乞食死 rung | ★**結案** | 決策層結案 ＋ 今日施主線兩條存查（可接受的世界性質／假說已證偽） |
| #20 | 小 pop 截斷病 | ★**三顆血證全修**，★★**而撞出第四顆** | `_cas_carry`✅／`_pursuit_carry` 符號已不存在／capture 已護欄；★**新**：`reaction_system.gd:229` |
| #23 | 選敵不濾同 faction | ★**真病，後果暫時受限** | 函式無 faction 濾；★★**而我原以為它不可能 fire（函式名叫 independent）＝錯**：`:978` 那個 else（**有 faction 的成員隊**）也呼叫它 |
| #31 | `local_value` blind 呼叫 | ★**已修（型別強制）** | `state` 必填無預設 ⇒ **blind 呼叫編不過**；10 個呼叫點全傳 |
| ★#2 | crisis 漏偵絕對餓 | ★★**真病未修** | `_decision_crisis` 三判準全是 pop/flow，**沒有任何絕對量** ⇒ 「穩定的零」不觸發 |
| ★#11 | preempt 缺口 | ★★**真病未修（★而它是設計選擇）** | `PREEMPTIBLE_TASKS` **註解自己列了排除項**（JOIN/BEG/REVOLT/戰鬥/threat/survival）⇒ 不是漏一行 |
| #3 | market-seeker 卡空市場 | **需量測** | bail 桶已有（`:859`）；★**「bail 後是否再 seek 同一市場」沒量** |
| #15 | survival-latch churn | **需量測** | 仍每 tick 呼叫；承諾機制是緩解而**效果沒量過**（原始數字 Team26 88/56） |
| #18 | lone-survivor death-limbo | **需量測** | ★`grep limbo` 0 命中，★★**而症狀是行為不是欄位 ⇒ 不可用 grep 結案** |
| #13 | 凍結威脅無 resolve | **（你預填）** 框架訂正歸 belief 衰減層 | — |
| #35 | 礦村未鑄幣 | **（你預填）** 修 merged／急症走廊拆 | — |

# ★★②要你裁的只有兩件（★兩件都不是「漏寫一行」）
```
★#2：補【絕對量】判準到 `_decision_crisis` —— ★★修法明確，而它會讓 crisis【更常 fire】⇒ 平衡問題，你裁時機
★#11：要不要把 JOIN／BEG／REVOLT 也變成【可被生存打斷】—— ★★註解顯示那是刻意排除的
   ⇒ ★★★「餓到要死的隊卡在 JOIN 該不該被打斷」是世界該長什麼樣的問題，不是 HOW
```

# ★★★③而 sweep 撞出一顆新的（★不在 13 條裡）
```
`reaction_system.gd:229`  var minor_cap: int = int(t.population * 0.2)
⇒ ★pop ≤ 4 ⇒ `minor_cap = 0` ⇒ `minor_population < 0` 恆 false ⇒ ★★`_score_breed` 恆 0
⇒ ★★★【小隊永遠不生育】，而那是【截斷造成的硬零】不是【設計的門檻】——正是 #20 描述的病型
```
★**已入帳（狀態：已知未修 ｜ 回訪：觸發事件）**，**不自行修**（它動的是生育，而生育今天已經翻過一次案）。

# ④順手完成的
★**13 條【全部補上狀態欄】** ⇒ **144 條存量回填的第一批**（`ki-status` PASS）。
★★**而「查完 ≠ 修完」照 A 級經驗成立**：**需要新開修法票的是 0 條** —— #2/#11 是**裁定**，#3/#15/#18 是**量測**。
