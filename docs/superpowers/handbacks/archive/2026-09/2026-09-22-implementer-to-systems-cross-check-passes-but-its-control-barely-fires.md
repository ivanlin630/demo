---
from: implementer
to: systems
status: consumed
slice: 兩本帳互驗 —— **做了，過了**；★**但它的陽性對照只贏 0.03**
topic: ★**互驗結果**：`loop2.solo` 子樹 ÷ `solo_think` = **0.998**｜其餘根總和 ÷ `near.faction_ai` = **0.973** ⇒ ★★**兩本帳互相坐實，沒有一本在說謊**｜★★★**而我要你看的是另一件事**：我給互驗補的陽性對照（把 solo 子樹故意錯歸）**比值只有 1.516~1.530，而上界是 1.5** ⇒ **它是【勉強】點火，不是穩穩點火** ⇒ ★這個帶寬對【這一種】錯歸幾乎不敏感，門檻是你的欄
---

# 一、互驗（★照你說的，**沒有多跑一輪**）

```
做法：把第一本（`_fai_ph`）的 self 沿 `PHASE_PARENT` 【歸到根】，則
   根 `loop2.solo` 子樹  應 ≈ 第二本的 `solo_think`
   其餘所有根的總和      應 ≈ 第二本的 `near.faction_ai`
實測（2 天 smoke）：
   loop2.solo 子樹 = 6,284,379 us ｜ solo_think      = 6,295,527 us ｜ ★比值 0.998
   其餘根總和      = 11,192,599 us ｜ near.faction_ai = 11,525,309 us ｜ ★比值 0.973
⇒ ★★**兩本帳互相坐實** —— 而 0.973 那 2.7% 的差是「第二本包住了第一本沒掛 label 的膠水」，
   方向也對（第二本應該略大）。
```

# 二、★★★而這一格我要回報的是它的**弱點**

```
★一次就過的檢查沒有鑑別力 ⇒ 我用【同一份真實資料】做了陽性對照：
  把 `loop2.solo` 子樹故意錯歸到另一邊（＝「有人把 solo 登記成 faction_ai 的兒子」）
  ⇒ 比值 **1.516~1.530**，而我設的上界是 **1.5**
⇒ ★★★**它只贏 0.03** —— 也就是說：
   **這個帶寬對「整個 solo 子樹被歸錯父親」這種等級的錯，只差一點點就抓不到。**
⇒ ★而 solo 剛好佔 28% ⇒ 換成一個佔 15% 的子樹被歸錯，★★**這個互驗就是綠的**。
⇒ ★★★所以：**它現在的綠不代表「歸屬全對」，只代表「沒有錯到 35% 那個量級」。**
   帶寬是你的欄 —— 我把數字與這句話一起印在卷面上，不自己收窄。
```

# 三、床（`scripts/debug/pass_tick_phase_breakdown_bed.gd` @ `68181f957`）

```
到場點名 8／8｜fail=0
新增兩格：①兩本帳互驗（比值＋門檻聲明）②互驗自己的陽性對照（含「只贏多少」那一行）
★★而「只贏多少」那一行是【永久印在卷面上】的 —— 不是這一次的註腳
  ⇒ 下一個讀這張表的人不必自己去想「這個綠有多硬」。
```

# 四、順序不變

```
①三計數兩臂（1／4）⇒ ②相位 12 天 × 兩顆種子（乾淨 main）⇒ ③兩份一起交
★所有相位數字仍是 smoke（CPU 被佔）⇒ ★★**唯一可以現在引用的是【比值】**：
  它是同一輪內兩個數相除 ⇒ CPU 競爭對分子分母同向作用 ⇒ 比值穩，絕對值不穩。
```
