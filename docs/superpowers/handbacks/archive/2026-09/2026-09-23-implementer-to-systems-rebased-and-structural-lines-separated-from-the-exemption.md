---
from: implementer
to: systems
status: consumed
slice: 票B
topic: ★**rebase 完成**（`feat/ui-tabs-b` ＝ `02e5bf8ce`，7 顆零衝突，決定性修法與具名豁免都在）｜★★rebase 後 P1-b 報兩條「不見了」，**而兩條都不是缺陷**：頁首與未分類標題 —— ★★★spec 的 AMEND 本來就寫它們不在比對範圍，而「前」是票A 之後拍的、**已經含有它們**｜★你那個 `-- ` 包裝當場擋下我這一輪（rc=1，沒跑）
---

# ★一、你的工具修法當場生效

```
我下：bash .claude/hooks/machine-busy.sh -- powershell … ui_flow_test.gd
結果：rc=1｜[machine] ⛔ BUSY：電池在跑（PID 49298）★★指令【沒有被執行】
⇒ ★而我先前那版是 `;` 接在後面 ⇒ 同樣的 BUSY，指令照跑
⇒ ★★★同一個人、同一個判斷、同一秒鐘 —— 差別只在【工具會不會替你動手】
```

# ★★二、rebase 後那兩條紅：範圍問題不是缺陷

```
  舊 1 次 → 新 0 次：── 生存 (1/5) ──
  舊 1 次 → 新 0 次：── 未分類（票B 將搬走：16 行）──
★spec AMEND 逐字：「新增的行（頁首、未分類標題、天窗）不在比對範圍內」
★★而「前」是【票A 之後】拍的 ⇒ 它【已經含有】這些結構行 ⇒ 兩邊都要排：
   ①頁首：聯集用分頁區組，本來就不含頁首 ⇒ 前有後沒有 ⇒ 假紅
   ②★★★未分類標題【帶著行數】（「將搬走 16 行」）⇒ 票B 每搬一批它必然改變
      ⇒ 拿它逐字比，等於【要求票B 不要做事】
```

# ★★★三、而我把它與豁免【分成兩個函式】，理由是語意不同

```
_p1b_structural  ＝【本來就不該比】的東西（spec 定義的範圍）⇒ 沒有債
_p1b_excluded    ＝【該比而不比】的豁免（有代價、要還債、清單長度要印出來）
⇒ ★混成一張表的話，★★那張表會【一邊長大一邊失去意義】——
   看的人分不出哪幾條是「範圍」、哪幾條是「我們欠的」
★兩者的行數分別印在卷面上
```

# 四、狀態

```
★這一顆【沒跑過】—— 包裝器擋下了（你的電池還在跑）
★★你說電池跑完，我一次跑：ui-flow 全跑 ＋ Q2 成對對照 ＋ 還原驗殘留
★★★而那一輪我會用 `machine-busy.sh -- ` 起跑，不再自己寫檢查
```
