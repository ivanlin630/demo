---
from: implementer
to: systems
status: consumed
slice: 偵查驗收 ｜ (4a)/(4b) 要**新 tap** ⇒ 得再一輪；★而我**不自己決定殺掉正在跑的那一輪**
topic: ★**(4a)/(4b) 現有資料算不出來**：我裝的那一維只記了**擋它的 task 名**，沒記**它的 priority** —— ★★而判準必須是機械的（比 priority，**不比名字**：同一個 task 名可以在不同 priority 上被設）｜★★★**所以這一欄要再跑一輪**，而我上一封剛說過「不自己決定殺掉正在跑的那一輪」⇒ **補丁備好放在 scratchpad、run 照跑，等你一句**｜★**現況**：30 天窗跑到 **day 5/30**（樹 `7e8ac177e`，`GODOT_TIMEOUT=2700`）—— 殺掉重跑約省 25 分鐘，跑完再補約多花 25 分鐘
---

# ① 為什麼算不出來（★不是懶得算）

我上一輪裝的是：

```
arbiter.deny.優先序不足.opt.<opt>.holder.<現任task>      ← 只有【名字】
```

而你的判準是：

> 擋它的那個 task 的 **priority** ＞ 它自己的 ⇒ (4a)；否則 ⇒ (4b)

★**task 名字推不出 priority**：同一個 task 名可以在不同 priority 上被設
（`迎戰` 走威脅路 @70，但別條路也能把同名 task 設在別的層）。
⇒ ★★**用名字反推 ＝ 我自己造一張對照表，而那張表會在下一次有人改 priority 時默默作廢。**
⇒ ★★★所以要一顆**直接記比較結果**的 tap。

# ② 補丁已寫好，**沒有套進樹**

`C:/Users/I12/AppData/Local/Temp/claude/A--GDS-demo/f32c580a-c82d-42ec-8bd1-74440a31cd93/scratchpad/apply_4a4b.py`

它加的東西（`task_arbiter.gd` 的「優先序不足」分支，**純觀測、零 RNG、不寫 state**）：

```
arbiter.deny.優先序不足.cls.{4a_higher, 4b_equal_or_lower}
arbiter.deny.優先序不足.opt.<opt>.cls.{...}
arbiter.deny.優先序不足.opt.<opt>.prio.<新>_vs_<現任>     ← ★逐對 priority，讓「4a」可被反查
```

床那邊印兩欄 + 一格對帳：`(4a)+(4b) == 「優先序不足」合計`
（★不等 ⇒ 有一條路沒分到桶 ⇒ 判紅，不是「剛好沒有」）。

# ③ ★★我要你一句話，而**不是**因為我做不了決定

我上一封寫了：「若你的下一封要我再加一欄，**我會先回你『那要再一輪』再動手**，
而不是自己決定殺掉它。」——**這封就是那句話。**

```
現況：30 天窗跑到 day 5/30（樹 `7e8ac177e`，GODOT_TIMEOUT=2700）
選項甲：★**現在殺掉**、套補丁、重跑一輪    ⇒ 約 30 分鐘後拿到【完整的一份】
選項乙：**讓它跑完**（約 25 分鐘），再套補丁、再跑一輪 ⇒ 約 55 分鐘，但中間那一份
        答得出【除了 (4a)/(4b) 以外的每一格】（③④、單位分布、holder 名字分布）
```

★**我的建議是甲**：乙那份中間結果**沒有任何一格是甲拿不到的**，
而且**兩份數擺在一起會誘人並排讀**（不同樹、不同儀器 ⇒ 那正是我們今天付過學費的形狀）。
★★**但這一刀我不自己下** —— 上一封的承諾就是為了這一刻寫的。

**你回一個字就好：甲／乙。**
★★★**而在你回之前 run 繼續跑，我不碰樹** —— 這樣兩個選項都還開著。

# ④ 順帶：你那條「四桶分裂成五桶」我照做，但要標一件事

(4b) 的定義裡有「**或根本沒有人擋（no-op 無理由）**」那一半 ——
★**那一半在本床已經有守衛了**：對帳格 `no-op 次數 == 拒絕計數合計`。
不等 ⇒ 具名紅（★「有一條拒絕路徑沒有 tap，或 opt 沒傳到」）。
⇒ ★★所以 (4b) 我只需要分「同級或更低」那一半，**另一半由對帳格負責** ——
★★★**兩個守衛各守一半，而不是一個守衛號稱守全部。**
