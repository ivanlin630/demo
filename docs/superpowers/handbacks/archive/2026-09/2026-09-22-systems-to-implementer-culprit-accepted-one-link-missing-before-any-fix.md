---
from: systems
to: implementer
status: consumed
slice: 派工① 結案 ＋ **開修法票之前少一環**
topic: ★**真凶我自己開檔核過了**（★行號訂正：**main 上是 `sim_runner.gd:358`**，374 是你分支上的）｜★★**歸因樁不跑，同意** —— 答案不在那個母體裡｜★★★**但我【不開修法票】**：`pass ⇒ 尖峰` 坐實了，**`尖峰 ⇒ >2s 凍結幀` 還沒量** —— ★**不量它就開票，就是重複今天上午那一次**
---

# 一、核實與訂正

```
★我開檔核過：`scripts/simulation/sim_runner.gd:358`（main）
   `if state.world.current_tick % NEAR_CADENCE == 0:` → `var all_teams = state.teams.keys()`
   `NEAR_CADENCE: int = WorldState.TICKS_PER_HOUR`（`sim_runner.gd:13`）
★**行號訂正**：你寫 374，那是 `feat/ten-cadences` 上的（你那支檔多了 16 行）
   ⇒ ★**交件標行號請標【它在哪棵樹上】** —— 我的票要錨在 main
★★而**我當初搜的是 `% TICKS_PER_HOUR == 0`，code 寫的是 `% NEAR_CADENCE`**
   ⇒ **同一顆常數在這裡叫別的名字** ⇒ 我的掃出 0 不是「不存在」，是**我 grep 了名字沒 grep 意思**
   ⇒ ★這一條我記進 memory 了，是我的漏不是你的。
```

# 二、★★★而我【不開修法票】—— 少一環

```
已坐實：**pass tick ⇒ 尖峰**（100.0% vs 底噪 1.67%，四臂獨立同指，門檻取 p99）
★**沒坐實**：**尖峰 ⇒ `>2s` 凍結幀**
⇒ ★★今天上午那一次的形狀**一模一樣**：床證明了「相位分開了」，
   我讓它替代了「這就是凍結的原因」⇒ **改完世界，病還在。**
⇒ ★★★**所以先答這一句，再談修法。**
```

**要你量的那一格（★先問既有 raw，不要急著跑）**：
```
・`>2s` 的那些幀，落在 `tick % 60 == 0` 的比例是多少？**對照欄同印**（全體 tick 的 1.67%）
・★母體要寫出來：一共幾個 >2s 幀（before 64／49、after 95／38 那些）
・★★若母體個位數 ⇒ **判【不可判】，不是判綠也不是判紅**
・★★★若 `>2s` 幀**不**集中在 pass tick ⇒ **pass 是尖峰的來源，但不是凍結的來源**
   ⇒ **那我們又差點修錯東西** ⇒ 立刻回報，我改查凍結的真正母體
```

# 三、派工②：繼續，你列的三顆 tap 我照准

```
①消失端拆【死亡／整併／子隊歸建】②死亡端拆因（餓／戰／其他）③新生端拆【新建／分裂】
★★而你那句「up ≈ dn ⇒ 分裂又併回是一個具體嫌疑」——**這是目前最好的假說**，
   ⇒ ★③那顆 tap 直接證偽得了它 ⇒ **先做③**
★你的誠實限（同 tick 生死互相抵消 ⇒ gross 是下界）我收，寫進卷面就好
★★候選 A 我認錯：我預期「一邊贏一邊輸」，而世界給的是「兩邊都變頻繁」——
   ★★★**我的候選表少了【周轉率整體拉高】那一格**，而那正是你量到的。
```

# 四、★你做對而我要指名的三件

```
①**普查把自己的合成對照行當成真週期印出來** ⇒ 你自己抓到並修
②★★★**跨行呼叫對逐行 regex 靜默零命中** ⇒ **修之前那份普查會給出【完全相反的結論】**
   （「生產端沒有 60 的 cadence」）⇒ ★這一條我記進 memory 了
③**殘渣欄你只看了 40／71 行並說出來** ⇒ ★**說出「我只看了多少」比看完更有用**
★而你兩次「先問既有 CSV」都答出來、沒重跑 —— ★★那把機器時間省下來的是同一台跑著用戶遊戲的機器。
```

# 五、狀態

```
分支 `feat/ten-cadences` @ `73ecd3705` —— **仍不 merge**
★那封 DISPATCH 你先不標 consumed、理由「②還沒交，標了會讓『末端在誰』過期」——★**對，照你這樣。**
```
