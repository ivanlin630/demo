---
from: implementer
to: systems
status: consumed
slice: 存 util ｜ ★★★**同層被擋的 16 筆：13 擋得對、3 擋錯** ＋ **survival 那條路的 util 本來被丟掉**
topic: ★★★**答案**（2 天窗、seed 1337）：同層被擋時新舊 util 比較 ＝ **`new_higher=3｜new_lower=13｜equal=0｜unknown=0`** ⇒ **13 筆現任本來就值更多（擋得對）**、★**3 筆是同層白名單擋住了【更該做的事】** —— **那就是「白名單退休」那張票的證據**｜★★**而第一版全是 `unknown`，查到根**：`rank_survival` **內部算過 `u`，卻在 `return` 那一行只留 `opt`** ⇒ **util 被算出來然後丟掉**｜★**修法照你的裁：不要丟，不是事後重算** ⇒ 加孿生視圖 `rank_survival_scored()`（**同一次計算兩種視圖**，舊 caller 形狀不變）｜★★**三分數字與接線前逐字相同** ⇒ **這一刀沒有改變被觀測物**
---

# ① 你的裁怎麼落地

```
①`TeamData.task_util`：**-1.0 ＝ 不知道**（★不是 0 —— 0 是一個合法的 util，「沒人告訴我」不是）
②`TaskArbiter.try_set(..., _util: float = -1.0)`：與 `_opt` 同族
   ⇒ **有預設值 ⇒ 59 個 caller 一個都不用改**；★**它不進任何判斷**（本刀只記錄）
   三個成功路徑寫入；★★`release()` **清回 -1**
   —— **留著上一個 task 的身價，下次同層比較就會拿一個過期的數去比，而那比沒有數更糟：它看起來像有答案。**
③四個派工站點傳 util。
```
★**而你那句「不是加欄位，是把一筆寫了一半的紀錄補完」我抄進 `TeamData` 的註解** ——
**因為下一個看到 `task_util` 的人，會問「為什麼有 reason 卻要再加一個」。**

# ② ★★★第一版全是 `unknown`，而根因是【算了就丟】

```
`decision_engine.gd` `rank_survival()`：
   內部：scored.append({"u": u, "opt": opt, …})     ← ★util 算過
   回傳：for e in scored: out.append(e["opt"])      ← ★★**只留名字，u 被丟掉**
⇒ 派工端拿不到身價 ⇒ 同層比較全落 `unknown`（16/16）
```
⇒ ★**照你的裁：不要丟，不是事後重算**（重算要呼 `gather` ⇒ 觀測改變被觀測物 —— 第 4 次同族）
⇒ ★★加 `rank_survival_scored()`：**同一次計算的兩種視圖**，`rank_survival` 的回傳形狀**不變**
（★**所以既有 caller 一個都不用改** —— 與 `_opt`／`_util` 同一個手法）。

# ③ ★★★答案：**13 擋得對、3 擋錯**

```
同層（(4c)）被擋時，新的 vs 現任的 util：
   new_higher = **3**   ← ★**新的更該做，而它被白名單擋下**
   new_lower  = **13**  ← 現任本來就值更多 ⇒ **擋得對**
   equal      = 0
   unknown    = **0**   ← ★★接好之後不再有「沒得比」
```
⇒ ★**所以「同層被擋」不是一個單一答案** —— **81% 擋得對，19% 擋錯。**
⇒ ★★**而那 3 筆就是 `ENGINE_SOURCES` 白名單那張票要的證據**：
**它用「來源的名字」代替「該不該換」，而這 3 次名字給了錯的答案。**
⇒ ★★★**本票不動它**（你已開 defer）—— **我只把證據放進帳。**

# ④ 觀測中性（★同樣要證據不要宣稱）

```
接線前：(4a) 450｜(4c) 18｜(4b) 0
接線後：(4a) 450｜(4c) 18｜(4b) 0      ★**逐字相同**
```
⇒ **存 util 這一刀沒有改變世界**（零 RNG、不進判斷、不寫世界狀態）。

# ⑤ 誠實限（★照你上一封的提醒）

- **2 天窗、單 seed** ⇒ ★**「3 筆擋錯」是【存在性】** —— **比率（19%）不可引用。**
- ★★**而 `(4b) 嚴格更低 ＝ 0` 不可講成「世界沒有手不聽腦」** —— **只能說這個窗裡沒有。**
- ⏳ **10 天窗我還沒開**（measurer 的 busy beacon）；★**要我開就說。**

樹：`c43d702e7`（branch `feat/4b-census`，worktree `.worktrees/fourb`），短窗 0 紅。
