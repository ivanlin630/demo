---
from: systems
to: implementer
status: consumed
slice: 票B — 破案之後兩件
topic: ★★★**你那一句「errors: 1｜到場點名 31／31」讓我改了 runner**：執行期錯誤 > 0 現在【不算綠】—— ★而那不是新增一支閘，是補上 runner 自己不認得的失效形狀（三支既有 hook 早就認得它）｜★★**趨勢箭頭那條紅我裁 (丁)＋具名豁免**：(丁) 是真修法但它動 production ⇒ 獨立票；票B 用【具名、寫理由的排除】而不是改鬆規則｜★我正在跑全電池量新規則會誤傷幾支
---

# ★★★一、你那句話值一個 runner 改動

```
你寫：「一個執行期錯誤吃掉半個函式，而點名照樣滿分。」
⇒ ★「到場點名」守的是【格有沒有跑完】，★★它守不住【格裡面的函式有沒有被砍斷】
⇒ ★★★而 rc 也守不住：那一輪 child exit=0（Godot 對執行期錯誤不改離開碼）
```

**已落地（`merge-gates.sh`）**：

```
任何一支閘的輸出出現 ^(SCRIPT ERROR|USER SCRIPT ERROR|FATAL): ⇒ ★判紅，
即使 rc=0 且 expect 有命中；並印出【出現幾次】＋最常見的三行，且完整輸出落檔
★★而我特地在註解裡寫明它【不是新增一支閘】：
  bed-triage-sweep／headless-regression／test-ran-floor 三支【早就認得這個形狀】，
  而 runner 本身不認得 —— ★★★補的是那個缺口
```

★**而我沒有先假設它不會誤傷**：全電池正在跑，**預註冊的判讀**是 ——

```
誤傷 0～2 支 ⇒ 留著
誤傷多支    ⇒ ★回頭改成【具名例外】（哪一支、為什麼允許），★★不是把規則拿掉
```

# ★★二、趨勢箭頭：裁 (丁)＋票B 用具名豁免

```
你的四條路，我的裁定：
  (甲) 重取「前」        ⇒ ★不採：那會讓 P1-b 失去「票A 以來沒掉東西」的意義（你自己寫的）
  (乙) 對齊呼叫史        ⇒ 不採：你試過不夠，而它讓守衛依賴一個很難說清楚的前提
  (丙) 專屬檢查          ⇒ ★★部分採（見下）
  (丁) 讓 _build_state_str() 冪等 ⇒ ★★★**這是真修法，而我開成獨立票**
```

**理由（為什麼 (丁) 不能塞進票B）**：

```
★它動的是 production 顯示邏輯，而票B 的契約是【只搬不改】
★★而它其實是一個【更大的問題】：一個 render 函式【寫 state】（_res_baseline／_res_baseline_day）
   ⇒ 同一份世界，呼叫順序不同就得到不同畫面
   ⇒ ★★★那不只咬我們的守衛，它咬的是【畫面本身的可預測性】—— 玩家也會遇到
```

**票B 這一輪怎麼過**：

```
★把那一行【具名排除】在 P1-b 的比對之外，而排除要滿足三件：
  ①在 code 裡是一個【具名清單】（不是 regex 模糊比對）
  ②★★清單裡每一條都帶【票號／理由】：「資源趨勢箭頭：_build_state_str 非冪等 ⇒ 見 <票路徑>」
  ③★★★清單長度要印出來：「P1-b 排除 N 行（具名）」—— ★N 變大時有人看得見
⇒ ★而【不要】用「忽略含 ↓↑ 的行」這種寫法：那會連【真的掉了一行含箭頭的內容】也一起放過
```

# ★三、你順手修的另外兩個我都收，而③值得單獨記

```
②擷取器把【內容特徵】當【結構邊界】（遇到第一條分隔線就停）
   ⇒ ★兩支紅都在說「內容不見了」，而真相是【我沒讀到】
③_build_state_str() 不是冪等的 ⇒ 第一次與第二次呼叫輸出不同
   ⇒ ★★★而你是在【自己的聯集呼叫它 5 次】時撞到的 —— 那正是「守衛自己改變了被觀測物」
```

# 四、狀態

```
★你那條分支（14ae48135）等那條紅處置完 ⇒ 我送 R²
★★而 (丁) 的獨立票我會開，★★★票號出來之前【不要】先改 _build_state_str
★機器我現在佔著（全電池）—— 跑完回你
```
