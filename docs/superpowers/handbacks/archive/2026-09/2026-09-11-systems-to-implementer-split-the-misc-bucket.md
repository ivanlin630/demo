---
from: systems
to: implementer
status: consumed
slice: 單幀 ｜ ★切開 `loop3.misc`
topic: ★★★**一個叫「其他」的桶排第一** —— 它佔 spike 樣本 **86.3%**、自佔比中位 **29.6%**｜★★而這不是「其他很貴」，是**我們沒有把它切開** ⇒ **下一刀是切，不是優化**（與這條線之前每一次一樣）｜★而現在切一個桶是**例行工作**：相位樹、淨值、根守恆式**都已經在了**
---

# ① 做什麼

```
★把 `loop3.misc` **切成具名子相位**（切點你定 —— ★★我不提名，今天我在提名上錯過四次）
⇒ 每一段：**us ／ 次數 ／ 單價**（★單價量、不准除）＋ **母體地板**（是 0 明寫 0）
⇒ ★★★而它們**必須進 `PHASE_PARENT`**（父 ＝ `loop3.misc` 或它的真父）——
  ★否則根守恆式會紅（**而那正是它該做的事**）。
```

# ② 驗收

```
①★守恆：子相位相加 ≈ `loop3.misc` 的 tot（★★差額要能解釋）
②★★根守恆仍綠（`Σ(根 tot) ≤ 容器總時`）＋ 負 self **0 個**
③★每一列印【已登記兒子數】（★★那一欄是我誤判過一次之後加的：
  `self == tot` 有三個意思，而**兒子數把它們分開**）
④fp 不變（純儀器）＋零 RNG
⑤★★★母體定義印在檔頭（窗／容器／門檻／樣本數）——★而**跨表比較的前提是母體逐字相同**
```

# ③ ★而背景給你（★不影響切法，但影響你怎麼讀數字）

```
★單幀最壞值：**26.65 s → 11.86 s**（2.25 倍，**問題還在**），**週期性沒變**（每小時一次）。
★★而 Probe 開／關的對照**還沒完成**（OFF 那趟被 contention 污染、已請量測員重跑）
  ⇒ ★★★所以「這 11.86 s 裡有多少是儀器」**現在不知道** ——
  ⇒ 你的子相位數字**同樣是 Probe-ON 的**，**交件請帶這句**。
```
