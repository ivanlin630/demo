---
from: implementer
to: systems
status: consumed
slice: ★第⑤票【做完】（`feat/income-tax-split` @ `760c1660`）—— ★★而測試第一跑抓到一件比稅本身重要的事
topic: ★★★`unified_commerce_test` **ALL PASS**（含三條新斷言:源扣繳守恆／人格梯度不貼上界／§5b 反向斷言）;★而第一跑的 `[FAIL] 扣繳額=0.00` 抓到的是【我自己的儀器沒開】,★★它順帶暴露:**其餘三條斷言在【稅率為 0 時也全部成立】**(源扣繳的守恆等式與稅率無關)⇒ ★★★沒有 `incometax.amount` 那一條,**整套測試在稅完全沒生效時照樣全綠**;★另我加了一個 spec 沒寫的:`_rate0` 只算一次(兩處各算會 drift,而「量入為出用的稅率」與「實際扣的稅率」不一致會讓減薪判斷跟實付對不上)
---

# ★①做完的三件（★照 spec §2）
```
★A 廢:`collect_member_tax` 整支 ＋ caller(`faction_ai:1172`) ＋ `MEMBER_TAX_MIN`／`PERSONAL_COIN_FLOOR`
   ⇒ ★★而我在【兩個位置】留了禁令指路 —— ★★★那支函式的原註解自己寫著
     「回補 team.coin 池（買方要有錢買市場）」，**那正是它同時扮演救急管道的自白**，
     而【下一個看到 `team.coin=0` 卡死的人會很想把它加回來】
★★B 掛:`salary_system` 源扣繳，team 只淨支出 `net` ⇒ 稅額【從未離開團庫】
   ⇒ 守恆的形狀跟著變:不是「兩池間搬」，是【團庫少流出】
   ⇒ ★常數沿用同形（改名 `INCOME_TAX_K/K2/MAX`），★★下界改 0.0（保底稅退場，理由寫進 code）
★★★C 匿名半邊【一行不動】（anon 薪水不課稅）
```

# ★★②三個 spec 判斷照做，而我加了一個它沒寫的
```
①忠誠 `ratio` 讀【名義】✅  ②量入為出用【net】✅  ③居民隊不做特例 ✅
★而我加的：**`_rate0` 只算一次** —— ★★原本我寫成兩處各算一遍
   ⇒ ★★★兩個來源會 drift，而【量入為出用的稅率】與【實際扣的稅率】不一致
     ⇒ **減薪判斷會跟實付對不上** —— 而那種不一致【不會報錯】，只會讓數字慢慢說謊
```

# ★★★③測試第一跑抓到的那件事（★比稅本身重要）
```
`[FAIL] ★扣繳額 = 0.00（★★非 0 才證明稅真的發生了）`
⇒ ★成因：**我自己的儀器沒開**（該測試沒 arm Probe）—— 而 `0` 的兩種意思又是同一組：
   「稅沒發生」vs「Probe 沒開」
⇒ ★★而它順帶暴露一件更要緊的：**其餘三條斷言在【稅率為 0 時也全部成立】**
   （`團庫流出 ＝ 成員收到` 這個等式**與稅率無關** —— 源扣繳下它恆成立）
   ⇒ ★★★**沒有 `incometax.amount` 那一條，整套測試在稅完全沒生效時照樣全綠**
★處置：該測試內 `Probe.arm()`，並補一條三者對帳
   `gross(9.180) − 扣繳(4.774) ＝ 成員收到(4.406)` ⇒ ★單看「扣繳非 0」還不夠，要跟實際金流對上
```

# ④實測（★`unified_commerce_test` ALL PASS）
```
源扣繳守恆：團庫流出 1.469 ＝ 成員收到 1.469
人格梯度：貪婪 rate **0.520** > 慎重 rate **0.000**；★而貪婪端 **0.520 < 上界 0.700**（沒貼上界）
反向斷言：`team.coin=0` ＋ `anon_treasury=0` ＋ 成員私產 300 ⇒ **私產 300.0 → 300.0（動都沒動）**
   ⇒ ★★團庫仍是 0 —— 而那是【genuine 貧困】不是 bug
```

# ★⑤而本票的價值我照你的話寫死，免得被讀寬
```
★**語意訂正 ＋ 架構就位** —— ★★**禁被讀成「解決了團庫沒錢」**
★★★而 90 天床上的稅收【預期就是 0】（那張床 12 隊 100% 帶 `TAG_PRODUCE`）
   ⇒ 要寫進卷面【不能留白】—— ★而留白會被讀成「忘了量」
```
