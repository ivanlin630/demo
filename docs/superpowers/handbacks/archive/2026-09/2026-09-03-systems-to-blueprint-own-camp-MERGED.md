---
from: systems
to: blueprint
status: consumed
slice: ★★★own-camp MERGED —— 腿B 從 0/30 變 30/30 走回去
topic: ★你裁的三件全做了並驗到:ctx own-camp 欄位／紮根含回家移動腿／營地沒了走既有解承諾出口(禁死旗);腿B 0/30→30/30、腿C 三格全過、13 支閘綠 269s;★★而 spec 的兩半【不夠】,第三件是 implementer 實測撞出來的:第三支 applicable 進了候選集但【util 恆 0.0000】——因為選址素材只在舊兩支下計算;★★★而他修的是【讓同一套真值對第三支也算得出來】不是 crank 分數,那正好是「util 必＝真值」那條法的正用法
---

# ★①你裁的三件，逐條驗到
```
①ctx own-camp 欄位（self-knowledge，零 god-view）  ✅
②紮根 option 執行含【回自家營地】移動腿           ✅ 腿B 0/30 → ★30/30 走回去
③營地沒了走既有解承諾出口、禁死旗                 ✅ 腿C：出發 30/30、解承諾 30/30、★卡住 0/30
merge：13 支閘綠 269s（★已含我新註冊的第 13 道 cross-run-static）
```

# ★★②而「兩半」不夠 —— 第三件是實測撞出來的
```
第一次跑完仍 0/30：★第三支 applicable【進得了候選集】而【分數恆 0.0000】
根：ctx 的選址素材只在【舊兩支】下計算 ⇒ `settle_eta_days` 留 0
    ⇒ `terms.gd::rooting_drive` 第一行 return 0.0 ⇒ 30/30 輸給建設
```
★★**而他修的是【讓同一套真值計算對第三支也算得出來】，不是把分數 crank 到會贏。**
⇒ ★★★**那正是「util 必＝真值、禁 crank」那條法的正用法** —— **距離也因此自然經由 ETA 的 `_dist` 折進去**（就是 reviewer 指的那個機制）。

# ★★★③而有一顆儀器問題值得你知道（★它差點讓我們讀出相反的結論）
```
`zhagen` 那支逐條件 tap 【沒有跟上】option 新增的第三支
⇒ 它報 `applicable = 0`，而實際 ★30/30 都 applicable —— ★★兩個結論【完全相反】
```
⇒ ★**通則**：**option 的 `applicable` 增加一支，逐條件 tap 必須同步** ——
★★**否則儀器會用【舊的分支表】去描述【新的世界】，而它不會報錯，只會給出一個看起來完整的答案。**

# ④帳目
`#10` 這條線至此：**銜接無病（腿A 30/30）→ 真根是決策模型沒有「自己的營地」→ 已修並驗到 30/30 走回去**。
★**camp churn 的比例變化我列【觀察項】不列驗收**（只量到共存，沒量到因果）—— 下一輪 organic 床順手看即可。
