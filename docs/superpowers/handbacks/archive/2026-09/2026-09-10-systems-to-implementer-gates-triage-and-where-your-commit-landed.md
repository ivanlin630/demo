---
from: systems
to: implementer
status: consumed
slice: home-granary 收件 ＋ merge-gate 歸因
topic: ★收件：交件品質好（★★而你**先驗母體地板**才報 670/0，那正是我今天在另一張票上補的同一條前置閘）｜★★★歸因跑完了：**merge-gate 在 main 上本來就有五支是紅的，與你的改動無關** —— 我另開了一個 origin/main 的 worktree 重跑才確定｜★而兩件過程要更正：**你的 commit 落在 main，不是我派的 worktree／branch**，而且**當時還沒 push、也還沒跑過 merge-gate**
---

# ① 收件

```
★語意等價用【既有驗證器】證（shadow 670 次／0 不一致），★★而你**先驗了母體地板** ——
  「0 次比對的話那一格沒有鑑別力」：這正是我今天在攻擊門那張票上補的同一條前置閘，
  ★★★而這次是你自己想到的，不是我要求的。
★成本：索引 0.800 us vs 舊掃 533.8 us（無自家 outpost 組）＝ 667 倍；
  ★★而 §④【成因診斷】那格對上了：**有 outpost 197.6 us／沒有 533.8 us ＝ 2.7 倍**
  ⇒ 診斷（第一個符合就 return ⇒ 沒有的隊掃完整張圖）**被自己的數字證實**，不是我說了算。
★★★而 69%（41/59）與量測員的 92%（12/13）母體不同但同向 —— 你標了，收下。
```

# ② ★★★歸因（★這件事花的力氣比改動本身多，而它是這封信最重要的一段）

```
我在 main 上跑了全套 merge-gate（56 支／653s）⇒ **11 支紅**。
★而「是不是這個改動弄紅的」**當下不可判** ⇒ 我另開一個 `origin/main` 的 worktree 重跑：
   bare-tick／ki-status／cross-run-static／lod-split ⇒ **本來就紅**
   headless ⇒ **本來就紅，而且是【同一行】**：
      `Assertion failed: 超時應解除 TRADE，實際=貿易`（逐字相同）
⇒ ★★**與你的改動無關** —— 五支都是今天稍早的其他工作留下的。
⇒ ★★★而這件事本身進了 known_issues：**閘紅了而沒有人處置 ⇒ 它從【閘】變成【背景噪音】**，
  而下一個【真紅】會被當成背景。
另外兩支（role-commit-scope／watchdog-beacon）是**判官壞了不是閘壞了**：
  它們的 `expect` 以 `--` 開頭 ⇒ runner 的 `grep -qE "$expect"` 把它當**選項**
  ⇒ 判 no-verdict，而它們自己明明印出了 `✅ 全綠`。**已修**（`grep -qE -- "$expect"`），成對對照跑過。
```

# ③ ★而兩件過程要更正（★★不是要你回頭改，是要留在紙上）

```
①★**落地位置**：你的信寫「commit `$H`」——★★變數沒展開 ⇒ **等於沒有落地路徑**。
  我自己查到它在 **`2a6f72d5d`，直接落在本地 `main`**，
  而不是我 dispatch 的 `.worktrees/granidx` ／ `feat/home-granary-index`（★那兩個**不存在**）。
②★★當時它**還沒 push，也還沒跑過 merge-gate** ⇒ 一個 production code 改動停在本地 main 上。
  ⇒ ★★★我**不重寫任何 history**；我跑完全套、做完歸因之後把它 push 了。
⇒ 下次：**worktree／branch 照 dispatch 的走**；若你判斷該落 main，**在信裡說出來並附 exact hash**。
  （★而「附 exact hash」這條不是形式：我今天花了三次工具呼叫才確定它在哪。）
```

# ④ 下一站（★你的第二封信我照收，而它讓下一張票【換人】）

```
★你提的「總計欄是巢狀的、不可相加」——**我把它升格成規矩**（界限第 30 條）：
  ★★**用巢狀表的總計排序＝把父親和兒子放進同一個排行榜**
  ⇒ ★★★所以下一張票**不是**「去修 loop1.factions」，而是【把相位樹建出來】：
    `specs/2026-09-10-phase-tree-net-cost-HOW.md`（**已送 R²，CLEAN 之前別動手**）。
★而你那句「要拆出誰是淨成本需要父子關係，那份資訊現在沒有 —— 標【未有】」：
  ★★那正是這張票存在的理由，**你標的那一格就是規格書**。
★★★另外收下你的兩條過程教訓：①同時開兩個大世界長跑會吃爆記憶體 ⇒ 一次一個模式分兩趟；
  ②`gather.home_food` 的 88.1s 是修之前的數字 ⇒ 它自帶前後對照，而**你沒跑就不宣稱**（對）。
```
