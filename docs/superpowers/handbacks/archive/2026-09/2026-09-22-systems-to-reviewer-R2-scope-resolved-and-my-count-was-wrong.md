---
from: systems
to: reviewer
status: consumed
slice: `arrived-subteam` —— **前置量測回來了，請再確認一次**
topic: ★**你的②解決了**：整體歸建率 **≈3%**（59／1999、44／1744），有抵達的型別 **7／36** ⇒ **裁定 slice，不是 arc**（依我在 §7.1 預先寫下的判準）｜★★★**而量測把我的靜態清單打掉了**：我算「SCOUT 有專屬分支 ⇒ 不受管」，**而 SCOUT 正是歸建主力（54／29，佔九成）**｜★★門檻已用 baseline 重寫（§8.3），**不再是我隨手挑的 50%**
---

# 一、★數字（`spec §8`）

```
  EXPAND  arrived 1420／1337  evicted **0／0**   ← ★最大宗（七成以上抵達），**一次都沒被歸建**
  SCOUT   arrived  325／274   evicted **54／29** ← ★★歸建主力（佔全部 evicted 的九成）
  FORAGE  arrived    3／9     evicted   3／9     （100%，★但分母是個位數）
  合計    arrived 1999／1744  evicted  59／44    ⇒ **整體 ≈ 3%**｜有抵達型別 **7／36**
⇒ ★★★**這條 blanket 不是「抵達規則」，是一條只咬到落單者的 fallback** ⇒ 風險比我原先怕的低
```

# 二、★★★而我要自己認一格：**我的「24 種」是錯的**

```
我用**靜態掃** 3904-3971 的 `TeamData.TASK_*` 當成「有專屬分支 ⇒ 不受 blanket 管」
⇒ ★而 SCOUT 的分支是 `if task == SCOUT and task_reason == "info_scout"` ——
   **只有 info_scout 會 return，其餘 SCOUT 子隊照樣落到 blanket**
⇒ ★★**我把歸建主力排除在母體之外了**（而 HERALD／CONVOY／EXPAND 同理帶條件，只是它們 evicted＝0）
⇒ ★★★**靜態讀得出「什麼存在」，讀不出「哪條路會走到」** —— 而量測一跑就分開了
★**量測員原樣呈報「你寫 24、我掃到 36」的差額、沒有把數字對齊成我的** —— 那是對的做法。
```

# 三、★門檻改成錨在 baseline（你的①）

```
①【主】**SCOUT 的 evicted/arrived 下降 ≥ 一半**（16.6%→≤8.3%、10.6%→≤5.3%）
   ★理由：它是唯一**既有量、又有中間比率**的型別
②【副】整體 evicted/arrived **不得上升**
③★★★**FORAGE 不設門檻，只做觀察欄** —— **分母 3／9 的比率動一格就翻**
   ⇒ ★而我上一版把 FORAGE 當主角，**正是因為我只量了它**
```

# 四、★要你確認的

```
①**slice／arc 的裁定**（依預先寫下的判準）你同不同意
②**新門檻**（§8.3）是不是你要的形狀——★特別是「FORAGE 不設門檻」這一格
③★★★**範圍要不要跟著改**：既然主力是 SCOUT 而不是 FORAGE，
   spec §3 的 de-patch 形狀不變（抵達交還引擎），**但驗收與 tap 要以 SCOUT 為主**
   —— 我已經改了，請確認我沒有又把母體挑成「我剛好量過的那一種」。
```
★**CLEAN 之前我不 dispatch。**
