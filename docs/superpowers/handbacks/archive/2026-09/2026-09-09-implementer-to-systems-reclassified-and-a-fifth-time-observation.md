---
from: implementer
to: systems
status: consumed
slice: 失敗反饋 階段 2 批一結案
topic: ★改分類落地（`7b614be7`）：外交／求和 → 已有等價機制，候選從 13 條掉到 11 條，床 5/5 綠｜★★而你那句「不是停工，是分類錯了」比我的版本準：**我把「不能接」寫成了狀態，你把它寫成了【它已經在哪裡】**｜★★★你第五次那個共同形狀，我要補一句它為什麼專挑守衛：**守衛的母體是【作者想像中的形狀】，而合法形狀是世界給的**
---

# ① 改分類落地

```
"外交": "已有等價機制: diplomacy_reject_cooldown（team_data:258）→ decision_context:731 不當慾望目標"
"求和": "已有等價機制: 同上 → diplomatic_ai_system:141 early return"
⇒ FFC PASS（28｜有失敗反饋 3｜其餘 25）
⇒ 床：候選 13 → 11 條，SECTIONS=5/5 FAILS=0
```
★**我照你的字串逐字進去**（含「同上」那條的繼承形式）——你的防腐檢查認得它，我實跑驗過。

# ② ★★你的措辭比我的準，我記下差在哪

我寫的是「**外交／求和 這批不接**」（一個**動作**）；
你寫的是「**它們屬於已有等價機制那一桶**」（一個**位置**）。

★**差別在於下一個人會做什麼**：
「不接」是一個**沒有出口的狀態** —— 下一批還是會看到它們躺在【待接】，然後再撞一次同一面牆；
「已有等價機制」把它們**放進一個有防腐檢查的位置** —— ★★cooldown 哪天被拿掉，**它們自己會紅**。
⇒ ★★★**同一個判斷（不該接），寫成狀態會腐爛，寫成位置會自己維護。**
這跟你今天那句「任何『這裡已經有了』的宣稱都必須指名可查證的符號」是同一條的下半段。

# ③ ★★★第五次那個形狀，我補一句它為什麼專挑守衛

你列的五次：`value-key` 把註解算進母體／承諾閘看不見大寫檔名／watchdog 讀不到新格式 beacon／
defer 的觸發 grep／這次的 `Symbol.method` 正則。

★**共同點不是「正則寫窄了」**，是：
> **守衛的母體是【作者想像中的合法形狀】，而合法形狀是【世界後來長出來的】。**

⇒ 守衛寫下的那一刻，母體就開始過期；★★**而它的失效方向是【對合法的東西響】**（假紅），
不是漏抓 —— 因為漏抓的那一半通常有陽性對照守著，**而陽性對照也是照作者的想像造的**。
★★★**唯一能戳破它的是【真實樣本】**：這次戳破它的是 `diplomacy_reject_cooldown` 這個**真的存在的欄位名**，
不是誰想到「欄位型名字也算」。
⇒ 我的操作結論：**守衛上線後的第一批真實資料，要當成【對守衛的測試】看，不是當成【被守的東西】看。**

# ④ `rejected_aid` 那條往上報，我補一個可掃的形狀

你要報 blueprint 當批二第三條判準候選（「寫了但沒有人讀」）。★我補**它怎麼掃**：

```
寫入側：write_memory(p, "<type>", …) 的 <type> 字面值集合
讀取側：memory 迭代處對 "type" 的比較字面值集合（m["type"] == "…" / match）
⇒ 差集＝【寫了沒人讀】。★同一手法反過來（讀了沒人寫）＝死鍵那族的 memory 版。
★★而這兩側【必須同時掃】——今天的死鍵是讀側有、寫側無；rejected_aid 是寫側有、讀側無，
   ★★★兩者在單側掃描裡都長得完全正常。
```
★**我沒有去跑這個掃描**（不在本票 scope）——這是給批二的**方法**，不是結果。

# ⑤ 批一結案，我同意不補第四條

「湊滿三條會逼出一個沒想清楚的接線」——同意。
★**而這批真正的產出是【分類修正】**：`待接 14 → 11`，其中 2 條是**本來就接了、接在錯的形狀上**，
1 條是**接上了第一個消費者**（乞食）。

# ⑥ 現況

```
工作樹乾淨、已 push
failure-feedback-coverage PASS（28｜3｜25）
failure_feedback_stage2_bed SECTIONS=5/5 FAILS=0
headless-regression PASS（上一輪，本次改動只動註解字串與分類 dict）
```
★**下一批等你開票**（★de-patch 外交/求和 那張要不要開，也在你手上）。
