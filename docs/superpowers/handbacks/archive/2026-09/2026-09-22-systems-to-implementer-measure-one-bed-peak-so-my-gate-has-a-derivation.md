---
from: systems
to: implementer
status: consumed
slice: ★★★**我的 6GB 門檻沒有來歷 —— 量一支床的峰值，把它推導出來**
topic: ★**現況 `MEM free=4GB`，而我的門檻是 `≥6GB`** ⇒ **鏈被我擋著**｜★★★**而那個 6GB 是我挑的，不是推導的** —— **我今天一整天在罵「沒有推導的門檻」，而我正用一個沒有推導的門檻擋住整條鏈**｜★**而電池是 68 支【依序】跑 ⇒ 要的是【單支峰值】不是總和** ⇒ 量一支就知道｜★★成本：一支短床、秒級
---

# 一、★★★我要認的

```
我寫：「`MEM free ≥ 6GB` ⇒ 跑電池」，理由是「成功那次卷面上是 15.2GB，
   而兩次被砍都在低記憶體 ⇒ 取一個明顯有餘裕的門檻」
⇒ ★**那是【手感】，不是【推導】** —— ★★**而它現在正擋著整條鏈**（free=4GB）
⇒ ★★★**今天我用同一句話退過別人的門檻** ——
   **「只印一個數字、門檻一改就得重跑」「門檻要同源推導，不是重新手感選一個」**
   **而這一條是我自己的。**
```

# 二、★要量的（★小，且它讓門檻變成推導的）

```
**跑【一支短床】，記下 Godot 行程的【峰值記憶體】**
   ⇒ ★**電池是 68 支【依序】跑** ⇒ **決定門檻的是【單支峰值】，不是 68 支的總和**
   ⇒ 例：`powershell` 迴圈每 0.5s 取 `WorkingSet64` 的 max，或跑完取 `PeakWorkingSetSize`
★**順便記**：跑之前與跑之中的 `MEM free`（★兩個操作元都印，判決才讀得懂）
⇒ ★★**然後門檻 ＝ 單支峰值 × 2**（★倍率我先寫死，來歷：留一倍給 OS／其他行程的波動）
   ⇒ **而那時它就是推導的，不是我挑的。**
```

# 三、★風險與時機

```
★**一支短床在 4GB 下風險低**（被砍的兩次都是【12 天世界跑】，量級完全不同）
★★**但仍照規矩**：跑前印 `godot-busy` 與 `MEM free`；**被砍 ⇒ 判【不可判】不是【紅】**
⇒ ★★★**而若它真的被砍** ⇒ **那本身就是答案**：**4GB 連一支短床都不夠** ⇒ 門檻該更高。
★**不必等 Wardogs 結束才做這一件** —— 它就是用來決定「要不要等」的。
```
