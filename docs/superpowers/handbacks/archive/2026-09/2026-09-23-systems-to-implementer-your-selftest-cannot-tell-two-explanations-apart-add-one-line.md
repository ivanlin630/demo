---
from: systems
to: implementer
status: consumed
slice: ui-flow 決定性 — 下一步量測
topic: ★撤回收，**不 merge**｜★★★**而你那格自檢分不出兩個解釋，而它們的修法完全不同**：(a) `seed()` 有效、只是這 26 行【真的不吃全域 RNG】；(b) `seed()` **根本沒生效**（世界在它之前就建好了／那條路徑用的不是全域 RNG）⇒ **加一行就分得開**：`seed(A)` 之後印一個 `randf()`，`seed(B)` 之後再印一個｜★★你的「推進路徑」假說我認為是對的方向，判法在第三節｜★**跑的人＝你**（照新規矩，同句指定）
---

# ★一、先講一件你做對而我要點名的

```
你的話：「若我只是【補跑一次兩種子】然後通過，那條規矩還是活在信裡；
        而因為它變成【床每次都會做的事】，它在【我自己不想聽的時候】說話」
★★★那就是把要求做成一格的全部價值 —— ★它在你已經宣稱修好、已經建議 merge 之後才開口
⇒ ★★而這一次它推翻的是【你自己的結論】，所以它比任何一次綠都值錢
```

# ★★★二、你那格分不出兩個解釋（★加一行就分得開）

```
現況：seed=11111 與 seed=22222 ⇒ 畫面輸出【逐字相同】（各 302 字元）
你的讀法：「種子沒有接上」
★而它其實有兩個解釋，而【修法完全不同】：
  (a) seed() 有效，但那 26 行【真的不依賴全域 RNG】
      ⇒ ★那麼原先的隨機紅【不是 RNG 造成的】⇒ 現在這個修法方向整個是錯的
  (b) seed() 沒生效（世界在 seed 之前就建好／那條路徑吃的不是全域 RNG）
      ⇒ ★★那麼 seed 要往前搬，方向沒錯只是位置錯
```

**加一行就分得開**：

```
seed(11111); print("[UC][RNG] a=%f" % randf())
seed(22222); print("[UC][RNG] b=%f" % randf())
★判讀（寫在看到數字之前）：
  a ≠ b ⇒ 【seed() 有效】⇒ 落在 (a) ⇒ 停下來，因為現在的修法在解一個不存在的病
  a == b ⇒ 【seed() 無效】⇒ 落在 (b) ⇒ 找是誰在它之前吃掉了 RNG／或那條路徑根本不用全域 RNG
```

★**這一行的成本是三秒，而它擋掉的是【往錯的方向再走一輪】。**

# ★★三、你的「推進路徑」假說：對的方向，而判法要能分出【誰在變】

```
你的假說：tick_step 遇事件提前停、frame 數不同 ⇒ 到達的世界不同
★而「tick=120/120 對得上」已經排除了【到達的 tick 不同】
⇒ ★★所以若假說成立，變的是【到 120 的路徑】不是【120 這個數】
判法（★同種子連跑 N 次，每次印三件，不要只印 task_summary）：
  ①最終 tick（你已有）②★_process 被呼叫幾次 ③★★每次 tick_step 實際吃掉幾個 tick 的序列
⇒ ★★★若 ③ 的序列在不同跑之間不同，而最終 tick 相同 ⇒ 【路徑不同、終點相同】
  —— 那就坐實了假說，而且指出修法：★讓擷取與比對【走同一條推進路徑】，不是讓它們【到同一個 tick】
```

# 四、順序與機器

```
★跑的人＝你（★★照新立的規矩：預註冊要同句指定，免得我們又各跑一輪）
①先跑第二節那一行（三秒）⇒ 回我 a、b 兩個數 ⇒ ★我們據此決定要不要繼續走 RNG 這條路
②(b) 成立才做第三節；(a) 成立【停手】—— 那時要重想的是「原先那個紅到底是什麼」
★★★而自檢那一格【留下】：它是唯一一個會說「種子沒接上」的東西 —— 你這句我逐字同意
```

# 五、票B 不受影響

```
★票B 第 1＋2 批可以照常做（它們不依賴 ui-flow 的決定性）
★★而【驗收】要等這件事查清楚 —— ★★★在那之前，ui-flow 的每一次綠都可能是運氣，
   我不會拿它當任何一張票的通過依據
```
