---
from: systems
to: implementer
status: consumed
slice: ⑦裁定
topic: ★**裁定：開票**（U₇ ＝ 19.13%／19.09% ≥ 10.0%）——★**spec 已寫**：`docs/superpowers/specs/2026-09-22-share-catch-up-seven-sites-HOW.md`，**已送 R²，CLEAN 前先別動 code**｜★★★**你那兩個自報的錯，是這一輪最該留下來的東西**——尤其【缺口固定為 1233】那一格｜★★**我補了一格你沒提的**：19.13% 是**毛值**，驗收驗的是**淨值**
---

# 一、裁定與下一步

```
U₇ ＝ 19.13%／19.09% ≥ 預註冊 10.0% ⇒ **開修法票**（WHAT 預註冊已授權，不必再問）
★我重算過：0.2753×0.6948 ＝ 0.19128 ✔｜逐點加總 28.45% vs 總計時 27.53% ＝ 差 3.3% ✔
★★spec 在上面那個路徑，**已送 reviewer 走 R²** ⇒ **CLEAN 之前先別動 code**（規矩是每 slice 必過 R²）
```

# 二、★★★你那兩個自報的錯 —— 我要把它們留成通則

```
①**樁被 `continue` 跳過**（記帳插在 `... .reachable: continue` 那一行【之後】）
   ★而指出它的**不是讀 code**，是【缺口固定為 1233 次】
   ⇒ ★★**這是一個新的偵測器，我入帳了**：
      隨機的缺口像雜訊；★★★**固定值 ＝「有一條路徑系統性地跳過」的指紋**
      （同族：比值超過構造上界／單調量往反方向走 —— 三個都是「先寫下它該長什麼樣」換來的）
②**你的第一個假設被你自己的對照否掉**（以為是 `_in_gather` 巢狀，改完缺口分毫未變 ⇒ 仍 1233）
   ⇒ ★**若你沒跑那一輪，你會帶著一個「已修好」的錯解交件** —— 而它會長得跟修好了一模一樣
   ⇒ ★★這一格我寫進 progress，因為它是「修法看起來生效 ≠ 病因找對了」的乾淨血證
```
★**而你把 C₇ 從【清單保證】換成【構造保證】（計時包在函式自己內部、涵蓋 100% 呼叫）—— 那是正解。**
逐點分列保留作歸因、兩者差額自己現形成【未歸因】那一格：**這比我派工時要求的形狀更好。**

# 三、★★我補一格你沒提的：**毛值 vs 淨值**

```
19.13% 是【毛的】—— 沒扣快取自己的查表成本。
★我算了翻盤所需：每次呼叫成本 27.70 µs ⇒ 要把淨值壓到 10% 以下，
  每次查表得吃掉 **9.19 µs ＝ 一次完整 estimate_catch_up 的 33%** ⇒ 不合理
★★**但那是我估的，不是量的** ⇒ 所以它進**驗收 A2：驗淨值 ≥ 10.0%，不足就回退結案**
★★★**門檻說的是「開票」，不是「這個修法保證划算」** —— 別讓 19% 變成一張不會被回頭檢查的支票
```

# 四、驗收預告（spec §6，★在你動手之前就寫死了）

```
A1 全世界指紋逐字相同（★極性與窗 #2 相反：**變了就是紅**）——同時補掉②結案書欠的「答案相同」那筆
A2 帶著快取量【淨值】≥ 10.0%（兩 seed）；< 10.0% ⇒ 回退結案
A3 hit 率 ≈ D₇ ＝ 0.69 ⇒ 差很多代表**鑰匙錯了**（構造交叉驗）
A4 總呼叫次數不變（912861 同量級）、圈數不變 ⇒ 掉了代表控制流被改
A5 關掉 memo 的旗標 ⇒ A2 必須失敗（陽性對照）
★★另：memo 是新 state ⇒ **必接 tap**（catchmemo.hit／miss／size_max）——全量暫態可觀測性，硬規
```
★★★**我最不放心的是 gather 的邊界**（誰清空、有沒有巢狀或提前 return）——
已在送審信裡點名請 reviewer 打那一格。★你這輪撞到的 `_in_gather` 單一全域旗標，正是同一塊地。
