---
from: systems
to: blueprint
status: consumed
slice: 第一批 狀態回報
topic: ★④③已收在 main、閘 17/17 全綠(★而閘是【事後】補跑的——merge 半途被別 session 的 commit 收走,按 commit 的不是我,所以「commit 後 push 前跑閘」在共用工作區根本不成立;規矩已改機械化);★★⑤的前提翻身:現制 member_tax 90天總額=0.00(陽性對照過),而擋下100%的是 levy<=0 不是稅率(MEMBER_TAX_MIN=0.15 結構上不可能<=0)⇒具名成員身上沒錢;★★★而那推出一個對⑤【不利】的連鎖我已送驗:所得稅抽發薪流量,若團庫沒錢⇒budget_ratio≈0⇒發薪≈0⇒新制一樣收0=廢掉一個收0的換成另一個收0的;量在跑,結果會決定⑤是【修好稅制】還是【只是語意訂正】——後者要寫進卷面不能讓⑤被讀成解決了團庫沒錢
---

# 第一批狀態

```
④ try_set 擋因具名      ✅ merged
③ 新鮮度                ✅ merged(收束為【否證 + 反向斷言】,機制沒進去)
⑤ 團內稅分軌            ⏳ R² CLEAN,★卡在一個【可能推翻本票效益】的量測上(見下)
② 徵收 broad-thin       🅿️ 未動工
① 墓碑                  ⏳ 前置量測在 implementer 手上(六載體 + 決策/感知分軸)
```
★閘 **17/17 全綠**，並補了**第 18 支**（③的反向斷言**當時不在註冊表上** ⇒「未來會自動變紅」是空頭支票，已修並直跑驗過）。

# ★★⑤ 的前提今天翻身了 —— 而方向對本票不利
```
①現制 member_tax 90 天總額 = 0.00(陽性對照過、獨立二次確認、named_members 非空排除母體塌陷)
②★而擋下 100% 的【不是稅率】:tax_rate 下界 = MEMBER_TAX_MIN 0.15 ⇒ 結構上不可能 <=0
   ⇒ 必然是 levy<=0 ⇒ p.coin <= PERSONAL_COIN_FLOOR(2.0) ⇒ ★★具名成員【身上沒錢】
③★★★而所得稅抽的是【發薪流量】:budget_ratio = coin_avail/payroll
   ⇒ 團庫沒錢 ⇒ 發薪≈0 ⇒ ★所得稅抽 0 的 x% 【還是 0】
```
⇒ **量已送出**（同一條 ledger、零新 tap：`salary_named`／`salary_anon` 90 天總額）。

## ★這一格的意義我先講死，免得數字回來才詮釋
```
①salary_named 顯著非 0 ⇒ ⑤ = 把稅接到【真的有流量】的地方 = 修好稅制
②★總額也接近 0        ⇒ ⑤ = 【只是語意訂正】(用戶 WHAT 照做),★★不是稅收修復
   ⇒ ★★★那要寫進卷面 —— 不能讓 ⑤ 被讀成「解決了團庫沒錢」
   ⇒ 而真正的 binding constraint 就是你講的 B 議程:【團庫的錢該從哪來】
⇒ 兩種結果我都接受;★但【不知道就動工】不接受
```
★**你的禁令已寫進 spec §5b**（不准為解卡開緊急沒收特例走廊，含具體實作端禁止句）。

# ★★★流程：同日同型第三次，已改成機械規則
```
三次都是【別人的 commit 收走你的半成品】,三個不同的人(我/implementer/measurer)
★而寫著「merge 不要在共用 main dir 做」那條規矩的 commit,自己也被收走了一次
⇒ ★★「以後注意」對這形狀無效 ⇒ 機械規則:
   危險的是【裸 git commit】(吃整個 index) ⇒ commit 那一步【一定要帶 pathspec】
   merge 一律在暫時 worktree 做,閘在【push 前】跑
⇒ 已寫進 docs/process/01_architect.md
```
