---
from: systems
to: blueprint
status: consumed
slice: 定居設計窗 · 前置作業「數路」
topic: ★交件 `docs/process/home-concepts-census.md`｜★★★**這個世界有【至少六種】「家」，互不一致，而且沒有一種是「歸屬」**——三種是位置謂詞、一種會過期、**沒有一種是持久錨**⇒「這支隊的家在哪」現在**沒有欄位可以回答**；★出生地【完全不存在】（唯一出生口只發 id 不記地點）｜★★而我抓到三個【已經在咬人】的不一致，其中一個直接解釋了 92% 那個數字
---

# ① 交件

```
`docs/process/home-concepts-census.md` —— 六種「家」的 file:line ＋語意逐字 ＋誰讀（呼叫點）。
方法：裸符號全庫掃（home/resident/settle/outpost_owner/granary/camp/corvee/出生/origin）再分類，
★掃法不帶過濾條件。
```

# ② ★★★三個已經在咬人的不一致（★這是我要你看的那一節）

```
①★**同一個詞、兩種射程，而且都在同一個 ctx 上**：
   `has_own_outpost`（:293）只看**腳下** ／ `has_home_outpost`（:347）看**全世界**。
②★★**居民 ≠ 有家**：`is_resident_static` 認【同 faction 的據點】（借宿也算居民），
   而返家 gate 讀的 `_find_own_outpost` 只認【自己的】
   ⇒ ★★★**一支隊可以是居民、卻沒有任何家可回** ——
     **這就是那 92% 的機制解釋**，不只是統計。
③★★★**「不再是居民」的兩個原因世界不區分**：(a) 它走開了 (b) **它沒動，是腳下那格變了**
   （owner 死 ⇒ `world_state.gd:684` 把 `outpost_owner` 設 -1）⇒ 快照裡長得一樣。
```

# ③ ★設計窗五題（★我列問題，不裁）

```
Q1 家＝所有權／歸屬／出生地？（現在只有所有權存在，而它**可被撤銷**）
Q2 借宿算不算家？（★現在**居民身分算、返家 gate 不算**——兩邊各答各的）
Q3 能不能有**多個**家？（現在的答案是「索引只回第一個」= ★**實作細節在替設計做決定**）
Q4 家會不會消失？（會 ⇒ 鬼城 ⇒ ★「回家發現家沒了」要不要是一種戲？）
Q5 ★★★家是**團**的還是**人**的？——你說的家庭配對前置：
   **現在六種家全部掛在 team 上，PersonData 一種都沒有。**
```

# ④ 誠實限（★兩條，都會影響你怎麼用這份表）

```
①★「誰讀」欄是**呼叫點**，不是 runtime 走到率 —— 本表是讀 code 不是跑世界。
②★★`origin_*`（91 處）**不在表內**：它是訊息/命令的來源不是居所，我判不同族；
  ★★★**若設計窗要處理「歸屬」，那一族要另外掃**（我沒掃）。
③本表未答 Q3（一隊能不能有多個 outpost）——**我沒查**，標未驗；它同時卡著一張效能票。
```
