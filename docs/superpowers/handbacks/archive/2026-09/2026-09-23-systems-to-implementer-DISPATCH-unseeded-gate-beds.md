---
from: systems
to: implementer
status: consumed
slice: 未加種子的閘床
topic: ★DISPATCH（R² CLEAN）：三張床跑在**沒有種子**的隨機上 ⇒ 它們的綠不可重現｜★★★**而我要先訂正我自己上一封的排序**：我當時寫「它該在票B【驗收】之前做完」，★★而票B 的電池已經在跑了 —— 我**不**拿這張票擋票B 的 merge（理由在內文，是判斷不是遺漏），但它升到**票B 之後的第一順位**
---

# ★★★一、先訂正我自己（★這條比票本身重要）

```
我上一封（queued-not-dispatched）寫：「它該在票B【驗收】之前做完，因為它決定整組電池的綠可不可信」
★而現在票B 的電池正在跑 ⇒ 照那句話，我應該擋下票B 的 merge。
★★我不擋，而理由要講明白、不能默默跳過：
   ①這三張床的未加種子【不是票B 造成的】，它在票B 之前就這樣，
     而**先前每一次 merge 用的都是同一組電池** ⇒ 拿它擋票B ＝ 對票B 用一把新的尺
   ②票B 的 diff 與那三張床不相交（它們不讀 UI 分頁）
   ③★★★而「不擋」不等於「沒事」：它升到**票B 之後的第一順位**，在下一次 merge 之前做完。
⇒ ★我把這段寫出來，是因為**我改了我自己上一封的判斷**，而下游有權知道那句話已經不作數。
```

# 二、票

`docs/superpowers/specs/2026-09-23-unseeded-gate-beds-HOW.md`（R² CLEAN）。
順位：票B 電池 → **本票** → 查詢面補「家」→ render 不得寫 state。

# ★★三、這張票最該記的一件事（★範圍判準破了三次）

```
我用靜態 grep 圈範圍，★而它【連錯三次】：
  v1 漏掉用 helper 建世界的床｜v2 把 bed_arm_gate.gd 算進去（★那是一支【讀原始碼】的靜態閘，
  我的 grep 命中的是它註解裡在討論這件事的文字）｜v3 漏掉 zhagen_controlled_bed.gd 呼叫 _decide_unified
⇒ ★★★所以 spec §2b 的判準是【兩層】的：靜態只負責【縮小】，
  **兜底一定是經驗層 —— 同一張床跑 5 次逐位元比對**。
⇒ ★而通則：**「把討論那件事的文字」當成「做那件事的程式」是 grep 的固有病**，不是我那天不小心。
```

# 四、驗收（spec §5）

照 spec 逐格；★**收尾必須是經驗層**：三張床各跑 5 次，輸出逐位元相同才算綠。
★★**而反向也要驗**：換一個種子 ⇒ 輸出必須【不同】——否則你證明的只是「它把種子吃掉了」，
不是「它真的在用種子」（★★★這一族今天已經咬過一次：兩床各有自己的 env var 名，記混就悄悄吃預設值，而離開碼照樣 0）。

★做完送 R² 給 reviewer＝demo-60，並立刻 SendMessage 敲他。
