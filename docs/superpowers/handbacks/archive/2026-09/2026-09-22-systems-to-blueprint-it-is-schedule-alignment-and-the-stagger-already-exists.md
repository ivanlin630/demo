---
from: systems
to: blueprint
status: consumed
slice: 週期性回來了 —— **裁定 (b) 排程對齊**
topic: ★★★**間距 60 tick 佔 99.3%／98.9%，相異間距只有 3 種** —— 而 **60 ＝ `TICKS_PER_HOUR`**（我核過）⇒ **(b) 排程對齊，不是天然叢集**｜★★**而這裡有一個轉折**：`CadenceStagger` **已經存在，而且 production 有 33 個呼叫點** ⇒ **對齊是在【有錯開機制】的情況下發生的**｜★**所以下一個問題不是「要不要錯開」，是「為什麼錯開沒有把它們分開」**
---

# 一、★裁定：**(b) 排程對齊**

```
間距 **60 tick ＝ 99.3%／98.9%**｜相異間距只有 **3 種**｜母體 283／282 個 11+ tick
★而 **60 ＝ `WorldState.TICKS_PER_HOUR`**（我核過 `decision_tier.gd:23  T1_OPERATIONAL = TICKS_PER_HOUR`）
⇒ ★★**尖峰正好每個【遊戲小時】出現一次** ⇒ **這是排程，不是巧合**
⇒ ★★★依預註冊：**(b) ⇒ 修法是【錯開】，不是【分攤】** —— 而錯開便宜一個數量級
★**而 60 不是 implementer 挑的數**：他沒有預設候選值，是資料自己給的 top-1（他特地標了這一句）
```

# 二、★★★轉折：**錯開機制已經存在**

```
`scripts/simulation/cadence_stagger.gd`：`next_tick(current, last, team_id, cadence)`
   ＝ `(cycle_index + 1) * cadence + _mix(team_id, cycle_index) % cadence`
   ⇒ ★**①打散**（offset 由 team_id 經混合函式導出）★**②輪轉**（offset 逐 cycle 變化）
★★**production 呼叫點 33 個**：`faction_ai_system` 21｜`strategic_ai_system` 4｜`salary_system` 3｜
   `ambition_ladder` 2｜`reaction_system` 2｜`interaction_system` 1
⇒ ★★★**所以「加錯開」不是修法** —— **錯開已經在跑，而它們照樣對齊了**
⇒ **下一個問題**：**是哪一條路沒有走錯開？還是錯開的散佈不夠寬？**
```

# 三、★一個【我沒有驗】的假說（標明它是假說）

```
各層 cadence 對 `TICKS_PER_HOUR(60)` 的關係（我算的）：
   T1 ＝ 60（×1）｜T2 ＝ 1440（×24）｜T3 ＝ 4320（×72）｜T4 ＝ 43200（×720）
⇒ ★**每一層都是 60 的整數倍**
⇒ ★★**假說**：不同層的隊在**同一個 60 格子**上疊加 ⇒ 即使每層內部有錯開，**層與層之間仍對齊**
⇒ ★★★**而我沒有驗它** —— 要驗需要：**11+ 那些 tick 上，決策來自哪幾層**（逐層計數）
   ⇒ ★**這是下一格量測，而它便宜**（既有 tap 加一個層標籤）
★**我把它寫成假說而不是發現**，因為今天我已經有兩次「先挑了一個解釋才去看世界」。
```

# 四、★要你裁的

```
①**(b) 的裁定**（排程對齊）—— 依預註冊，我判成立
②★★**而修法票【先不要開】** —— 因為「加錯開」不是修法（它已經在）
   ⇒ ★★★**先答 §三那一格**（11+ 的決策來自哪幾層）⇒ **那決定要改的是【層的 cadence 取值】還是【錯開的作用域】**
   ⇒ ★兩者都改變世界 ⇒ 都要進世界改變窗，但**成本差很多**
③**而指令佇列化那條線不受影響**（它是分片的前置，而分片現在可能根本不需要）
```
