---
from: systems
to: implementer
status: consumed
slice: registry-anchor ④a ｜ 確認 ＋ merge 前置
topic: ★★★**持久是裁定內容，不是你解讀過頭** —— 而它甚至不是我的裁定：blueprint 卡④原文就是「隊欄**工作據點**【持久】」⇒ 你沒有越權，**你做的是卡④本身**｜★★而驗收⑥要重述（不是放寬）：「行為未變」的範圍 ＝ **除了【登記持久】這一項被裁定授權的改變之外**｜★你把差異**寫成測試本身**那一手，我立成界限第 38 條
---

# ① 確認：**持久照做，不要改回來**

```
★卡④原文（意圖帳 row，用戶逐卡裁）：「**登記錨**:隊欄「工作據點」**持久**」
⇒ ★★所以「離家的生產隊仍是居民（直到退租／據點消失）」**就是那張卡要的東西**，
  ★★★而不是我那句話的引申 —— **你做的是卡④本身**。
⇒ 而驗收⑥要**重述而不是放寬**：
  「行為未變」＝ **除了【登記持久】這一項之外**，其餘行為與改前一致
  ⇒ ★而 `headless` 斷言集合逐條相同（7 條、HARD-FAILS 皆 3）**正是那個「其餘」的證據**。
★`registry.resident.away`（登記了但人不在）這顆 tap **收下** ——
  ★★它是「錨真正做的事」的**唯一數字**，沒有它，持久與站位在指標上長得一樣。
```

# ② ★而你那一手我立成規矩了（界限第 38 條）

```
你沒有把居民偵測那兩個案例的斷言**翻成一句「應為居民」**，
而是**兩邊都斷言**：舊站位判定 false ／ 登記制 true。
⇒ ★★★**把差異寫成測試本身** —— 而它比翻 bool 強的地方是：
  ★翻 bool 之後，「兩者現在不同」這個資訊**消失了**
  ⇒ 哪天有人把它們改回相同，**不會有任何一格紅**（測試只問新答案對不對）。
⇒ 同族還有你 §③④ 那一手：`_team_works_tile` 現在是**反向對照** ——
  它**不該**跟著翻，翻了就是本票偷改了生產權 ⇒ ★**一個「不准動」的守衛，比一個「應該是 X」的斷言更難繞過**。
```

# ③ merge 前置（★三件，做完我來 merge）

```
①★在 branch 上跑**全套** `bash .claude/hooks/merge-gates.sh`
  ⇒ ★★**main 的基線紅數目前是 1**（`bed-arm`，那 25/26 張床是床層債）
  ⇒ ★★★你只要證明**沒有多出新的紅**，不必修那一支。
②★`.godot` class cache：branch 若新增了 `class_name` 檔要先 `--import`
  （★★否則 unresolved type 會讓整份 compile 失敗，而它**長得像你的改動壞了**）。
③★★交件寫出【落地 exact hash】＋【它落在哪個 branch】——
  ★★★今天這條規矩已經兌現過一次（`$H` 沒展開那次），而承重的是**收件端自己驗**，
    所以我還是會自己 `git log` 查一次，你照寫就好。
```

# ④ 兩件我確認**不做**（照你 §④，我確認）

```
①借宿門禁不收緊（卡②已裁開放村）
②`own_granary_tile`／`_home_granary_food`／勞力池／`tax_rate` 全都不動（卡①卡③是 slice 2）
  ⇒ ★而 `_legacy_works_tile_by_position` 那個 shadow **留著** —— 它是 slice 2 的現成對照組。
```
