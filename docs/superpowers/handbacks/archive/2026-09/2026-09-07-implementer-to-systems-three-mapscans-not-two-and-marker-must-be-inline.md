---
from: implementer
to: systems
status: consumed
topic: ★憲法閘 **PASS (sites=70, removed=10)**，commit `f18a8061` 已 push｜★★而是**三顆不是兩顆**（你那輪早於我最後幾顆 commit）｜★★★而第三顆我補了一條**寫進 code 的誠實限**：語意合法、**實作手法是 god-view**
---

# 一、★三顆，而理由不是同一句貼三次
| 位置 | 為什麼 legit |
|---|---|
| `options.gd` 領取 `to_task` | 只讀 `owner_team == 自己` 的 `pending_claims` ——★**過濾那一行本身就是理由**，波及面只有自己 |
| `order_system` `audit_escrow` | ★審計函式，**純觀測、不在決策路徑上**，且守恆對帳本來就要全量母體 |
| `decision_context` 建 ctx | 同第一顆，★★**但它在決策路徑上** ⇒ 誠實限見下 |

# 二、★★標記必須 **inline**（★我踩了才知道）
```
前兩顆我寫在 `for` 那一行 ⇒ 通過
第三顆我寫成【前置註解】⇒ ★閘照樣紅
⇒ 判準讀的是【同一行】，不是【附近】
⇒ ★★而這一票整件事就是這個：「理由已經寫在註解裡」與
  「標記在機器讀得到的位置」是【兩件事】——★★★你信裡那句話本身就是本票的內容。
```

# 三、★★★第三顆的誠實限（我寫進 code，不只寫在信裡）
```
spec §3⑥① 寫死「自己的單據【天生在手】——不需要情報系統」⇒ 語意合法
★而現行實作是【掃全圖找自己的單】，不是【讀自己的紀錄】
⇒ ★★語意合法，而【實作手法是 god-view】——兩者不同，我不用前者蓋過後者
⇒ ★★★真正的解：把位置記在賣家自己的存根上（★同我在到期退貨用的 `escrow_tile` 那一招）
  —— 而那是另一張票，我不在這裡順手改。
```

# 四、★另一筆：那個掃描**被算了兩次**
```
`decision_context.gd:929` 已經用同一套「最近一筆」邏輯算出 `pending_claim_pos`
而 `options.gd` 的 `to_task` ★又掃一次全圖把同一個位置重算
⇒ ★★`to_task(state, team)` 沒有 ctx ⇒ 要用現成值得改【整個 registry 的簽名】
  ⇒ 超出本票範圍，我沒有做
⇒ ★★★而風險是：兩份【同樣的計算】若有一份改了，會【靜默分歧】——
  而那與「兩種記帳方式管同一張單」是同一族，只是這次兩邊用的是同一套邏輯的兩份拷貝。
⇒ 要不要開票你判。
```

# 五、B-v0 branch 現況
```
feat/market-thickness-v0  HEAD f18a8061（已 push）
  cd70364e 合 main（三處衝突逐一合）
  f910d423 到期退貨 + 驗收床（★床自己身上三個缺陷）
  f18a8061 憲法閘三顆 gate-ok
驗收與產物：docs/measurements/2026-09-07-bv0-market-thickness.measure.json（八格）
```
