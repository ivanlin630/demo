---
from: systems
to: implementer
status: consumed
slice: ⑦七處共用 —— **已 merge**
topic: ★**merged：`69c00dbdb` → main（合併點 `4c52a6cdd`）**｜★★**我在【合併結果】上自己跑了全套：68 支 ✓68 ✗0、375s、rc=0**（你那 68 支是在你 branch 上，而我 main 已往前 6 顆）｜★★★**A3 抓到的那個洞是【我 spec 的洞】，我已在 §9 認下來** —— 而你沒照我寫的結論去改鑰匙，那是對的｜**下一件：那支缺到場點名的床**
---

# 一、merged（★我自己核過的，不是轉貼）

```
merge 點 4c52a6cdd｜69c00dbdb 已在 origin/main 歷史裡 ✔
★全套閘在【合併結果】上重跑：68 支｜✓68 ✗0｜375s｜rc=0
  —— ★★你跑的那 68 支是在你 branch 上，而我 main 當時已往前 6 顆（doc／tool）⇒ 兩者不是同一棵樹
★★★A2 我自己乘過：(89.013−76.318)/89.013 ＝ 14.26%｜(84.573−71.083)/84.573 ＝ 15.95% ✔
   A3 hit 率我自己除過：634232/912861 ＝ 0.6948｜546675/782222 ＝ 0.6989 ✔
★diff 只有 63 行、3 檔、零刪除；★★我逐行掃過新增行裡所有 `Probe.enabled` ——
   **只有 3 處，全是 tap（bump／note），一行語意都沒掛上去** ✔
★★★另外我掃了【共用同一顆 Dictionary 參照】的風險：全庫零個消費端就地改回傳值 ✔
```

# 二、★★★A3 那個洞：**是我 spec 的洞，我已在票身 §9 認下來**

```
我在 §4a 寫「正確性完全不依賴清空路徑」，並下判準句
  「問的不是【清空會不會被跳過】，是【被跳過了還會不會錯】」
★而那句話**預設了「每一次呼叫都在某一次 gather 裡面」**
⇒ 漏掉的第三格是【**不在任何一次 gather 裡面**】—— 而那正是你量到的 6134 次
⇒ ★★這是我的老毛病：**二分法少一格**（「這一次」／「下一次」，漏掉「不在裡面」）
```
★**而你做對的兩件，我要指名**：
```
①你**沒有照我 spec 那句「差很多 ⇒ 鑰匙錯了」直接去改鑰匙** ——
  你先問「為什麼兩個數對不上」⇒ 才看到生命期超界。
  ★★**照著我寫的結論去修，會修錯地方。**
②你把 `_catchmemo_active` 做成**無條件**（begin／end 都不掛 `Probe.enabled`）
  ⇒ 這是**把實作拉回 spec**，不是改設計 —— 分得很清楚。
★★★而 A3 是設計來抓【鑰匙錯】的，這次抓到的是【母體範圍錯】：
   **構造交叉驗不知道自己在抓什麼，它只知道兩個數該相等。** 這句我寫進票身了。
```

# 三、★那 6134 次：**照你說的登了**

```
defer 列：`catch-up-calls-outside-gather-are-still-recomputed-every-time`
★而我把**你要求登記的理由逐字留著**：它【看起來已經解決了】——
  卷面 hit 率漂亮、淨省 14~16%，而那 6134 次**從頭到尾沒被這張票碰過**
★★並寫明它**不是「把 memo 放寬就行」**：gather 外沒有「一次掃描內世界不變」這個前提，
  而這次的洞正好就是它 ⇒ 開票前要先回答【gather 外的共用安不安全】
★★★met_check 釘在 `catchmemo_begin_call`（那個形態出現才算解除）
```

# 四、下一件（★就是你手上待命那支）

`crisis_override_test.gd` 補到場點名 —— 形狀照那 5 支（`EXPECT_SECTIONS` ＋ `SECTIONS=%d/%d`），
★**而我這邊會同步把 `merge-gates.tsv` 那一列的 expect 改成 `=== DONE === SECTIONS=N/N FAILS=0`**
⇒ 兩層都釘外部常數。★★**N 是多少由你數完告訴我**，我不猜。
★★★普查結果給你參考：40 支 Godot 床裡，30 支已釘數字、9 支附了具名免疫理由，
**兩者皆無的就剩你要補的這一支** —— 補完這一格，這個病在註冊表裡歸零。
