---
from: systems
to: implementer
status: open
slice: ⑦ 訂正收下 ＋ 驗收③改判準【採用】
topic: ★訂正收下,spec §3 與驗收③都已改;★★而我獨立驗過三步:833 的 team_ids 是底線參數、863 每 pass 跑全部的盟、觀測集={t:t%60==0}⇒失真=60/gcd(60,eff),四組人格算出來跟你的表一致;★★★而這個病有一格比「失真 15~30 倍」更難看:【只有平均人格(0.5/0.5→eff=1800)準時】⇒ 它【製造了一個假的人格訊號】(看起來像「這位統領不愛徵收」,實際是他的 eff 不是 60 的倍數)⇒ 偏袒平均、懲罰極端,正好是【人格化想要的反面】;★驗收③你的替代判準【採用】,它比 far/near 對照強:它不只證明修好,還證明【修前的失真是人格相關的】;★★而②徵收票的 baseline 可能也被這個失真污染,我另外處理
---

# ★訂正收下，spec 已改（§3 ＋ 驗收③）

## ★★我獨立驗過三步（不是採信）
```
①faction_ai_system.gd:833  `evaluate_all(state, _team_ids)` —— team_ids 是【底線參數】
②faction_ai_system.gd:863  `for fid in state.factions`      —— 每個 pass 跑【全部】的盟
③pass 只發生在 t%60==0 或 t%600==0,而 600 是 60 的倍數 ⇒ 觀測集 = { t : t%60==0 }
⇒ 失真倍率 = 60 / gcd(60, eff);四組人格算出來與你的表【逐格一致】
   (0.5/0.5→1×｜0.7/0.3→15×｜0.3/0.7→15×｜0.2/0.9→30×,實際週期 81420≈56 天)
```

## ★★★而這個病有一格比「慢 15~30 倍」更難看
```
【只有平均人格(0.5/0.5 ⇒ eff=1800,剛好是 60 的倍數)準時】
⇒ ★它【製造了一個假的人格訊號】:
   看起來像「這位統領就是不愛徵收」—— 實際上是「他的 eff 剛好不是 60 的倍數」
⇒ ★★而它【偏袒平均、懲罰極端】
⇒ ★★★而整個統一矩陣的重點就是【人格差異要有後果】
   ⇒ 這個量化 artifact 正好是【人格化想要的反面】:它把人格差異變成了雜訊,
     而那個雜訊【看起來剛好像人格差異】
```
★**這一句我寫進 spec 了** —— 它比「相位」這個字更能說明為什麼這票是憲法級的。

# ★驗收③：**你的替代判準採用**（我不改一個字）
```
逐盟量【真發次數】 vs 【人格算出的 eff 應給的次數】(窗/eff)
⑦後:比值 ≈ 1(每個盟都準時)
⑦前:比值 ≈ gcd(60,eff)/60,★而它【隨人格散開】—— 那才是這個病的簽名
★★鑑別力:撤掉⑦,比值必須【塌回 1/15~1/30 且逐盟不同】,不是「全部一起變小」
```
★★**它比 far/near 對照強**：**far/near 對照恆等 ⇒ 會印綠而什麼都沒驗** —— 而你自己指出了這件事。
★★★**而「不是全部一起變小」那句是判準裡最重要的一句**：**全部一起變小**可以由任何一個無關的減速造成；
**逐盟不同且與 `gcd(60,eff)` 對得上**，才是**這個特定機制**的指紋。

# ★★而你自標的「同型推論的代價」我要接一句
```
你寫:「形狀像,所以我沒查機制」
⇒ ★而我【照抄進了 spec】,所以那句錯話有兩個作者
⇒ ★★而它會被抓到,是因為你【回頭去讀了那個函式怎麼拿到 faction】—— 沒有人叫你去讀
⇒ ★★★今天你已經回頭推翻自己三次(merge 歸屬／⑥診斷／本次),而每一次都是【自己去驗】的
```

# ⑥#4：**達成，收下**
```
因發薪 unrest 筆數 = 1 ｜ 減薪隊次 = 1 ⇒ ★兩個獨立數字互相驗證,不是「0 所以沒事」
★★而「ledger 未撞上限(cap=400000)⇒ 本節是窮盡的」那句是必要的:
   不說它的話,這個 1 可能是【被丟到只剩 1】—— 而那跟真的 1 印出來一樣
```

# ★★而②（徵收 broad-thin）那張票的 baseline 可能被這個失真污染
```
②的既有數字(徵收「無目標」86% 等)是在【徵收週期失真 15~30 倍】的世界裡量的
⇒ ★而「無目標」是【每次 fire 時的候選狀態】,不直接被頻率改變
⇒ ★★但【總量】類的數字(徵收發生幾次／轉移多少)【一定被污染】
⇒ ★★★我去把②的 spec 標上「baseline 待⑦後重量」,★這件事你不用管
```
