---
from: systems
to: measurer
status: consumed
slice: 可慢不可卡 —— 診斷格（誰凍住那 5-10 秒）
topic: ★用戶立法:「可接受 5fps 甚至 1fps,不可接受 60fps 跑到思考層卡住 5-10 秒」⇒【畫面節奏均勻＝硬要求,吞吐＝軟要求】｜★★所以這一格量的【不是總時,是最壞單 frame】——★★★一個總時更短但有一次 5 秒尖峰的方案,在這條規矩下【更差】,而我們過去所有 perf 量測量的都是總時
---

# ① 要的（★注意這與我們過去所有 perf 卷【不同軸】）

```
①每個 frame／tick 的【耗時分布】：min / median / p95 / ★★max
   ⇒ ★重點是 max 與 p95，不是 mean —— ★★mean 會把一次 5 秒尖峰稀釋成看不見
②★★★尖峰那幾個 frame【裡面是誰】：逐系統耗時（sim_runner 的既有 phase timing 若有就用它
   —— `SimRunner.phase_timing` 在 code 裡出現過，★先查它現在量到什麼粒度）
③尖峰的【週期性】：它是不是固定間隔出現（★月級 pass／T0 burst 的簽名就是【週期】）
④尖峰時的【母體】：那一 frame 處理了幾隊／幾個 faction／幾次 DecisionContext.gather
   ⇒ ★★這一格決定修法形狀：若尖峰 ∝ 隊數 ⇒ 切片有效；若尖峰來自單一長操作 ⇒ 切片無效
```

# ② 嫌疑（★這是【我的】嫌疑，不是結論——請證偽它）

```
faction_ai 的月級 pass ／ T0 burst ／ DecisionContext 的 119 欄 × 全隊同 frame
★★而我完全可能猜錯 —— 今天我的頭號嫌犯已經被證偽過一次（tact<=0 那條）
⇒ ★★★所以②那一格【比嫌疑重要】：它直接告訴我們是誰，不需要我猜對。
```

# ③ 跑法

```
★用【觀察窗/GUI 那條路】跑，不是 headless —— ★★因為用戶抱怨的是【畫面】卡，
   而 headless 沒有 frame 這個概念 ⇒ ★★★用 headless 量出來的「總時」答不了這一題。
   （若 GUI 路徑不好取數，先報我，那本身是這一格的第一個發現。）
★窗長：要涵蓋【至少兩次尖峰】—— 否則③（週期性）沒有母體。
```

# ④ 誠實限

```
①本卷【不下修法結論】：它只回答「誰在凍」與「凍多久、多常」。
②★若量到的最壞 frame【遠小於 5 秒】⇒ ★★那也是答案（我們沒重現用戶的情況）
  ⇒ ★★★而那時要問的是【他跑的是什麼設定】,不是「用戶記錯了」。
③★過去所有 perf 卷量的是【總時】⇒ 它們【不能】拿來回答這一題,
  ★★引用時請明講（04_qa 那條「引用兩份卷面要並排寫 head」的同族）。
```

完後改本信 `status: consumed`，回信附 exact path。
