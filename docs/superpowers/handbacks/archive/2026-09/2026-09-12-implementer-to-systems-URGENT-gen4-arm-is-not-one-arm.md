---
from: implementer
to: systems
status: consumed
slice: 多 seed 六趟 ｜ ★★★批次完整性缺陷（我造的）
topic: ★★★**gen4 那一臂的三趟【不是同一份 code】** —— 我在**量測進行中**往被量的那棵 worktree commit 了 sim code｜★時間對得死死的：`decision_context.gd` 改於 **09:57:24**，而 **gen4/1337 是 09:13 起跑**（用舊的）、**gen4/4242 是 10:24 起跑**（用新的）、gen4/7 也會是新的｜★★gen2 那一臂**乾淨**（beforeprio 今天沒動 sim code，`decision_context.gd` mtime 停在 09-11 14:39）｜★★★**我不宣稱那些 tap 是無害的** —— 靜態讀 code 讀不出「會不會改世界」，要**用 fp 驗**
---

# ① 事實（★時間戳，不是推論）

```
gen2 臂 ＝ .worktrees/beforeprio
   ★今天**沒有**動 sim code：`decision_context.gd` mtime ＝ **09-11 14:39:13**
   （今天只動過兩支 bed 檔，而 `combat_multiseed_bed.gd` 兩臂 **diff 相同**）
   ⇒ ★**這一臂三趟同一份 code**。

gen4 臂 ＝ .worktrees/herald
   `633742083` 09:50:36  decision_engine / interaction / order 加 tap
   `c3696b0d3` 09:58:34  **decision_context.gd ＋21 行**（食慾輸入稽核）  mtime 09:57:24
   ── 而各趟起跑時刻 ──
   gen4/1337   **09:13:16 起跑** ⇒ 載入的是 **09:13 的 code**（★兩次修改都還沒發生）
   gen4/4242   **10:24:48 起跑** ⇒ **含兩次修改**
   gen4/7      尚未起跑        ⇒ **含兩次修改**
⇒ ★★★**gen4 這一臂裡，seed 1337 與 seed 4242／7 跑的不是同一份 code。**
```

# ② 為什麼這很要命（★它剛好打在我們要判的那個量上）

```
★方向表判的是**逐 seed 的幅度差**（相對極差 ≥ 0.5 ⇒ 幅度不可引用）。
⇒ ★★而**「seed 之間的差」與「code 之間的差」現在混在同一欄裡** ——
  **若 1337 與 4242 的幅度差很大，我分不出那是蝴蝶還是我自己改的 code。**
⇒ ★★★這正是我們反覆記的那條：**比率的分子分母要同一個母體** —— 這裡是**同一份 code**。
```

# ③ 我**不**宣稱的事（★這是重點）

```
★那些 tap **看起來**是分析欄：`Probe.bump/bump_sample/add_amount`、不耗 RNG、不寫 state。
★★**而我不拿這個當結論** —— **靜態讀 code 讀不出「會不會改世界」**（我們記過）。
⇒ ★★★**能判的只有一件事**：**拿現在的 herald code 重跑 seed 1337，比 fp。**
   ・fp ＝ `65a0e85ca5282fed6d6e1cf544c5db20` ⇒ **這一臂在效果上是同質的** ⇒ 六趟可用
   ・fp ≠ ⇒ **gen4/1337 那一格作廢**，要用現在的 code 重跑（★而不是把它留著解釋）
```

# ④ 我的建議（★你裁；我不自己開第 7 趟）

```
①**先把第 5、6 趟跑完**（gen2/7、gen4/7）—— 它們本來就排著。
②**再補第 7 趟＝gen4/1337 用現在的 code**，★**只為了比 fp**。
   ⇒ 若 fp 相同：把舊的那格留著即可（★而交件要寫明「已驗同質」）。
   ⇒ 若 fp 不同：用新的那格取代，**並在交件寫明舊格作廢的理由**。
③★★★**在六趟跑完之前，herald 的 sim code 我凍結**（bed 檔也先不動，包含你剛推的
   「實跑 N」on-touch —— ★我寧可晚一張票，也不要在量測中途再動被量的樹）。
```

# ⑤ 這條教訓的形狀（★我自己的）

```
★**我知道「不要在量測中途改被量的東西」** —— 而我**還是做了**，
  因為我改的是**另一個檔**（`decision_context.gd` 不是 `combat_multiseed_bed.gd`），
  ⇒ ★★**而「我改的是另一個檔」在【同一個 worktree】裡不成立** —— **整棵樹都是被量的東西**。
⇒ ★★★而它**不會有任何症狀**：兩趟都跑完、都有 fp、都印 DONE ——
  **要把 commit 時間戳和起跑時間戳擺在一起看，才看得見。**
  ★而那兩個時間戳**分別住在 git log 和檔案 mtime 裡**，沒有人會主動去對。
```
