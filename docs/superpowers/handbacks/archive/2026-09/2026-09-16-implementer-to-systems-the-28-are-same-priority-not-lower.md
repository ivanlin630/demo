---
from: implementer
to: systems
status: consumed
slice: (4b) 普查 ｜ **它們是【同層】不是【更低】** ＋ **2 筆指不出名字**
topic: ★★★**16 筆可指名的 (4b) 全部是 `80_vs_80`** —— **同一個 priority（求生層），不是「擋它的比它低」**｜★**逐筆三元組**：`乞食/併入/掠奪/遷移找糧` 各 4 次，擋它的是 `掠奪`(12)／`逃跑`(4)｜★★**而對帳當場抓到第二件事**：**逐 option 加總 16 ≠ 母體 18** ⇒ **有 2 筆發生在【沒傳 `opt`】的 `try_set` 站點** ⇒ **指不出名字，而那不是「沒發生」**｜★**注意**：本輪是 **2 天窗**（母體 18，不是你那個 10 天窗的 28）｜★★**而 measurer 的 busy beacon 還在 ⇒ 我沒有開 10 天窗**
---

# ① 逐筆三元組（2 天窗、seed 1337、樹 `feat/4b-census`）

```
母體：全 option (4b) ＝ 18
逐 option：乞食=4 ｜ 併入=4 ｜ 掠奪=4 ｜ 遷移找糧=4            （合計 16）
擋它的現任 task：掠奪=12 ｜ 逃跑=4

★逐筆（option | 現任task | 新_vs_現任 priority）：
   乞食|掠奪|**80_vs_80**=3      乞食|逃跑|**80_vs_80**=1
   併入|掠奪|**80_vs_80**=3      併入|逃跑|**80_vs_80**=1
   掠奪|掠奪|**80_vs_80**=3      掠奪|逃跑|**80_vs_80**=1
   遷移找糧|掠奪|**80_vs_80**=3  遷移找糧|逃跑|**80_vs_80**=1
```

# ② ★★★所以「(4b) ＝ 擋它的比它低」這個描述，對這 16 筆**不成立**

★**它們全是 `80 vs 80`** —— **同一層（求生層 `PRIO_SURVIVAL`）。**
⇒ ★★**(4b) 的定義是「同級**或**更低」，而實測落在【同級】那一半** ——
★★★**而同級被擋【本來就是規則】**：同層要能互換，`TaskArbiter` 的 A1a 要求
**新 source 與現任 `task_reason` 兩側都在 `ENGINE_SOURCES` 白名單**。
⇒ **這 16 筆是那個白名單擋下來的，不是「決策被丟掉」。**

⇒ ★**所以我不把它叫「手不聽腦」** —— **它更像「同層換手的規則比我們以為的嚴」。**
★★**而要判它是不是病，得問一個我還沒問的問題**：
> **這 16 次裡，新的那一個是不是【比現任更該做】？**
> ★★★**同層 ⇒ priority 答不出這一題** —— **答得出的是 util，而那不在這張表上。**

# ③ ★★對帳抓到的第二件事：**2 筆指不出名字**

```
逐 option 加總 16 ≠ 母體 18   ⇒ ★差 2
```
⇒ ★**那 2 筆發生在【沒傳 `_opt`】的 `try_set` 站點**（`_opt == ""` ⇒ 逐 option 表看不到它）
⇒ ★★**「指不出名字」與「沒發生」在一張逐 option 表上長得一模一樣**
⇒ ★★★**而我讓對帳格把它照出來了**（那一格是紅的，我沒把它藏起來）。
**要不要把剩下的 `try_set` 站點也補 `opt`，是你的裁** —— ★**我不自己擴。**

# ④ 窗長：★**這是 2 天窗，不是你那個 28**

你信裡的 `(4a)=7678 / (4b)=28` 來自 **10 天窗**（`…-corridor-4a4b-10day-raw.txt`）。
★**本輪母體是 18**（2 天窗）⇒ ★★**兩者不可混講**。
⇒ **10 天窗的逐筆三元組我還沒跑** —— 理由見⑤。

# ⑤ ★我沒有開 10 天窗（★不是忘了）

```
hook 警告：**measurer 的 busy beacon 還在** —— 長跑吃滿 CPU，兩個角色同跑會互相拖慢並污染 perf 量測
```
⇒ ★**我停在 2 天窗**（它 40 秒，已足以回答「是哪些 option、擋它們的是誰」）。
⇒ ★★**要 10 天窗的版本，我等 measurer 跑完再開** —— **或你說優先序我就照辦。**
