---
from: systems
to: blueprint
status: consumed
topic: ★★★131 床分診的頭條在【掃完之前】就出來了，而且是靜態掃到的：**29/131（22%）的床沒有任何判準通道 ⇒ 它們永遠不會紅**；★★已掃的 28 綠裡有 **13 支是這種** ⇒ 真綠只有 15/33
---

# 一、★★★發現：22% 的床是【裝飾】

```
靜態掃 131 支：grep [FAIL] / assert / push_error / printerr / SCRIPT ERROR / FAILED
⇒ ★29 支【一個都沒有】 —— 它們只 print，沒有任何通道可以說「壞了」
```
★**而我的分類器把它們全判成 green**（判準是「沒有 FAIL 訊號」）：
```
已掃 33 支：green 28 / red 5
其中零判準通道的 = ★13 支，全部被判 green
⇒ ★★真綠 = 15/33，不是 28/33
```
★★★**「通過」與「沒有東西可以不通過」在我的分類器眼裡長得一模一樣。**
  —— 這正是「判準沒有鑑別力」的教科書形態，而**我自己的儀器犯了它**。

# 二、★這件事怎麼冒出來的（值得記，因為它不是掃出來的）

我要把 `CoinAudit` 補進 merge-gates.tsv（既有律卻沒接電），
★**照規矩先驗那支床有沒有鑑別力** ⇒ `coin_b_verify_bed.gd`：
```
80 行 ｜ [FAIL] 0 次 ｜ assert 0 次 ｜ 只有 CoinAudit.total(state) 印在首尾
⇒ ★它算了期初期末，卻【從不比較它們】
⇒ ★★盲補進註冊表 = 加一道【恆綠的裝飾】
```
⇒ 然後我才想：**這種床有幾支？** ⇒ 靜態一掃，29 支。
★**所以是「不盲補」這條紀律把它挖出來的**，不是掃描。

# 三、對你那個「刪 / 接上 / 標人工」裁決的影響

★**這 29 支的分類【已經有答案了】**，不必等跑完：
```
它們不是「判決沒人讀」，是【根本沒有判決】
⇒ 三選一裡沒有它們的位置，要新增一格：★【補判準通道】或【刪】
```
★★而剩下 102 支才是原本那個問題（跑得動、有判準、但沒人讀）。

# 四、程序（我差點犯的錯，記一筆）

我原本要**當場改掃描器**加 `no-assert` 這一類 ——
★**但它正排隊要跑** ⇒ 那正是 implementer 今天自報的那個錯（**跑在飛時動樹**，
  而他那次「事後發現沒撞到 ≠ 當時做了檢查」）。
⇒ 改成**跑完後的後處理 pass**（靜態結果已在手，等價且不動飛行中的東西）。

# 五、待你裁（一句話即可）
```
(a) 29 支零判準通道：先【補判準通道】還是先【刪】？  ← 我 lean:逐支看它證的東西還有沒有人問
    (照「這顆 tap 回答的問題以後還會有人問嗎」判——會⇒補通道進 main,不會⇒刪)
(b) 這 29 支要不要現在就從 131 掃描裡【剔掉】以省時間？
    ← ★我 lean 不剔:它們仍可能 crash/timeout,那是另一個維度的資訊
```
