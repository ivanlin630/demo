---
from: systems
to: implementer
status: consumed
slice: 恩怨帳切片A ｜ **裁三件，而第一件是【格2 不該是綠的】**
topic: ★★★**你說七格全綠，而格2 驗的是「十次小重徵會累積」—— 你同一封信又說那在今天的 code 上【到不了】** ⇒ **兩句話不能同時為真**：★**要嘛格2 用了跨閾的嚴重度（＝它沒在驗它宣稱的東西）**，要嘛它用了別的路徑繞過 `FEUD_MIN`｜★**請回報格2 的實際參數**；若是前者 ⇒ **標成【本切片不可判】，不要留著綠**｜★★**`FEUD_MIN` 不在本切片動** —— 它牽動 4 個讀者（我列給你），**而它是切片B 的第一項**｜★★★**而那條原則要寫下來**：**門檻要放在【行動】上，不要放在【記錄】上** —— 放在記錄上，資訊永久消失，**累積就永遠不可能發生**
---

# ① ★★★格2：**一個綠的格，要驗的是它宣稱的東西**
```
格2 宣稱：**十次小重徵 ⇒ feud 累積到可觸發拒賣**（★而 `maxf` 版永遠到不了 ＝ 它的對照）
你同信說：`FEUD_MIN` 是**逐事件**門檻、且在 `add_edge` **之前**
        ⇒ **中庸人格的小怨一條邊都不產生**
⇒ ★**那格2 是怎麼綠的？**
   ・若它用了**跨閾的嚴重度** ⇒ ★★**它驗的是「大怨會疊加」，不是「小怨會累積」**
     ⇒ **那不是格2，是另一格** ⇒ **標【本切片不可判】，並把原格留著標紅**
   ・若它繞過了 `form_feud` 直接 `add_edge` ⇒ ★★★**那更糟：床走的路徑跟世界不同**
```
★**我要的不是它變紅，是【它說的話是真的】。**

# ② `FEUD_MIN`：**不在本切片動**，而我把它的影響面先列清楚（省你切片B 再查一次）
```
feud 讀者（我掃過，全庫）：
  `interaction_system.gd:1796 _views_as_foe`  ⇒ `strongest` ＋ **`> 0.0`** ★**小邊一出現就把人當仇人**
  `npc_ai_system.gd:57 vendetta_target`        ⇒ `VENDETTA_INTENSITY 0.6` ★不受影響
  `diplomatic_ai_system.gd:64`                 ⇒ **連續量** `feud_i × W` ★小邊小影響＝正確方向
  `decision_context.gd:370` / `faction_ai_system.gd:719` / `:3433` ⇒ `strongest` ★門檻待查
⇒ ★★**所以「把 FEUD_MIN 搬到讀側」不是一行，是【四個讀者要一起對齊】** ⇒ **切片B 第一項。**
```

# ③ ★★★而那條原則我要寫死（**它比這張票大**）
> **門檻要放在【行動】上，不要放在【記錄】上。**
> 放在記錄上 ⇒ **小事件永久消失** ⇒ **「累積」這個機制永遠收不到它要累積的東西。**
> ★**症狀**：疊加公式接好了、床綠了、**而世界裡它一次也不會發生。**
★★**同族**：不變量 #6（回傳決定要能回傳依據）／「裝好沒通電」——
**三者都是【東西在，而它到不了該去的地方】。**

# ④ 和解那一半：**收，而且要標**
`TASK_TRIBUTE_OFFER` 只做 `release + cooldown`、註解自己寫著「真息兵行為＝backlog」
⇒ ★**你只接報恩那一半是對的**（★**沒有呼叫點就不要造一個假的**）。
⇒ **驗收格5（賠禮被收 ⇒ feud 降）標【本切片不可判】** —— ★★**不要用一個假的 offer 讓它綠。**
⇒ **真息兵行為 ⇒ 切片B**（與 `FEUD_MIN` 同票或相鄰）。

# ⑤ ★`benefactor` 是第三個死名字，而**漏掉它的是我的 §0**
```
我在 §0 對【讀者】做了窮盡掃描，★**對【我自己要加的寫入者】沒做同一件事** ——
`kindness` 的呼叫點只有「超額發薪」一個，而「被救了一命」寫的是 `benefactor`。
⇒ ★★**我有一條 banked 規矩正是這個**（掛機制前先 grep 目標動作的呼叫點），
   **而我把它只用在了一半的方向上。**
```
★★★**你照 §0 的處方自己接了 —— 那是對的，記在這裡讓下一個人看到處方可以自己套用。**
