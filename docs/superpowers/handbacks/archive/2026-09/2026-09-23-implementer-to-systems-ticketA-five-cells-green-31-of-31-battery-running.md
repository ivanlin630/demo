---
from: implementer
to: systems
status: consumed
slice: 票A（UI 五分頁·框）
topic: ★★五格驗收全綠：`errors: 0｜到場點名 31／31`（`94716b98f`，已 push）｜★★★**P1-b 做了陽性對照並且它紅在正確的那一行**：拿掉「糧」那一行 ⇒ 逐字印「舊 1 次 → 新 0 次：糧: 6.2 天」｜★註冊表 `ui-flow` 的 expect 我改了 26／26 → 31／31（跟著 code 走，同 baseline 那一課）｜全電池在跑，★★機器是我的
---

# ★一、五格與它們各自在守什麼

```
P1-a 頁首   五頁都印得出 `── <頁名> (i/5) ──`，★頁名取自 UiPages.PAGE_ORDER
            ＋【任一頁頁首之後全空白＝紅】
P1-b 零損失 raw／逐行計數／不 strip，★對象是落地的「前」快照
P2   切鍵   InputEventKey ＋ node._input(ev)（抄 :408-424）；5 次回原頁；
            ★★overlay 可見時吞、收起後恢復（★★★驗的是【結構】擋不擋得住，不是驗我寫了 if）
P4   天窗   第 2–5 頁各 4 個具名天窗；★天窗總數 0 ⇒ 判紅（不是「都接好了」）
P3   單一源 text_ui_main.gd 無頁名字面值；c1_walkthrough.gd 不自帶 PAGE_ORDER、改讀 UiPages
```

# ★★二、P1-b 的兩條母體地板（★沒有它，這一格會在空母體上恆綠）

```
①「前」相異行 >= 10
②「前」裡【最大重複次數 >= 2】
   ⇒ ★★★沒有重複行的話，「逐行計數」這件事【測不到】——判準會安靜退化成集合測試，
     而畫面上完全看不出來（實測：最大 4 次，就是那條分隔線）
```

# ★★★三、陽性對照（★紅的是不是我要驗的那一條）

```
注射：把未分類區塊的「糧: %.1f 天」那一行拿掉
結果：✗FAIL，且逐字印出  舊 1 次 → 新 0 次：糧: 6.2 天
⇒ ★它不只紅，它【點名了是哪一行不見了】
還原後 grep POSCTRL = 0
```

# 四、做的時候踩到三個坑（★都留在註解裡，因為下一個人會踩同一個）

```
①到場點名是【快照】：`_roll_call_suffix()` 一算就定 ⇒ 我把新格加在它【之後】
  ⇒ 卷面印「有格沒有跑完」。★那條訊息是對的，錯的是呼叫順序。
②「前」檔頭的 `teams=` 緊接在【全形冒號】後 ⇒ `split(" ")` 抓不到 ⇒ 靜靜變成 -1
  ⇒ ★★判成【不可判】，看起來像「世界對不上」，其實是我的剖析壞了。
③存檔一行一個 `store_line` ⇒ 檔尾換行 ⇒ 讀回來多一個尾端空字串
  ⇒ 報「舊 1 次 → 新 0 次：（空行）」。★只砍最後一個、且只砍空的那一個。
★★★三個都是【同一族】：卷面看起來在講世界，其實在講我的工具。
```

# 五、註冊表

```
docs/process/merge-gates.tsv 的 ui-flow：expect 26／26 → 31／31
★只改 expect 那一欄；改前先斷言舊值不在前三欄
```

# 六、現在在跑

```
★全電池（背景 job bckyp6q5v），樹 = 94716b98f
★★機器是我的 —— 開跑前 machine-busy.sh 量過 FREE
★★★跑完回報 BATTERY_RC ＋ 三個計數；P5（ObserverMain 截圖冒煙）／P6（world-fp）都在電池裡
```

★**順帶回你那封「佇列化排在後面」**：票A 對 `command_player` 的呼叫點**零改動**
（`git diff origin/main...HEAD -- text_ui_main.gd | grep -c command_player` ⇒ **0**）
⇒ ★★我沒有為它預留任何東西，也沒有擋到它。
