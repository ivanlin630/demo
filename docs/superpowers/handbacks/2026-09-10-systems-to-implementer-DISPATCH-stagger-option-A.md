---
from: systems
to: implementer
status: consumed
slice: 錯開每小時思考 pass —— ★DISPATCH（重裁後定案）
topic: ★blueprint 重裁：**選項 A**（per-team 到期檢查移出 60-tick 全域閘、改每 tick 檢查，**只此一個 pass**）｜★★而 R² 算出來的那件事你要先讀：原本的做法會讓 **98.3% 的隊思考頻率砍半** —— ★★★而根因不是工具壞，是**它被放在一個只看得到 60 倍數的取樣格上**；檢查改成逐 tick 之後，`CadenceStagger` 就回到它原本正確的行為（長期均值恰好 60）｜★選項 B（桶）**憲法處死**：思考頻率檔次化 ＝ 決策層 LOD ＝ 零 LOD 禁項
---

# ① 動手前必讀的三件

```
①★R² 的推導（我獨立重算過，59/60 ＝ 98.3% 確認）：
   candidate = (cycle+1)*60 + offset，而外層只在 60 的倍數檢查
   ⇒ offset ≥ 1 就跳過一個檢查點 ⇒ 間隔 120 而非 60。
   ★★根因：**offset 的解析度細於檢查頻率** ⇒ 精細資訊被捨去，只剩「offset 是不是 0」一個 bit。
②★★所以修法的核心【不是換工具】，是**把取樣格拿掉**：檢查改逐 tick
   ⇒ 間隔 ＝ 60 + (off_new − off_prev)，★偏移相消 ⇒ **長期均值恰好 60**；
   `MIN_GAP = 30` 夾住 wrap 那一次的下限。
③★★★**只移這一個 pass**：視野／移動／forced_event 超時**維持每小時**。
   （★動整個全域閘 ＝ 一次改很多件事，歸因不了 —— 第⑧票的教訓逐字適用。）
```

# ② 視野時滯：★blueprint 已裁【誠實時滯，非退化】

```
第 97 tick 才想的隊，用的是 tick 60 的視野（≤59 tick ≒59 分鐘）。
★blueprint 的判詞：這個世界的資訊哲學本來就是 belief 有齡衰減，
★★而「全隊都拿零時差視野」正是【同步腦假象】的一部分 ——
★★★**拿 59 分鐘前的世界圖像做決策 ＝ 中世紀的正常，不是 bug。**
⇒ 所以你**不要為它做任何補償**；把它【印出來】並寫進誠實限（驗收⑨，只印不判）。
```

# ③ 驗收（★兩格是新的，別漏）

```
②★**總吞吐改用【新 tap】**：掛在到期檢查【之後】（只在真的往下跑思考時 bump），
   沿用 `%04d` per-team 格式。
   ★★而舊的 `pass.byteam`（`sim_runner:312-314`）**不得**再當這一格的證據 ——
   R² 查實：它 bump 在 `_run_systems` 之前，量的是「有沒有出現在 all_teams」，
   ★★★**錯開前後都會回報逐隊相同，卻量不到我們要守的東西**。
⑧★★★**實際間隔分布**（R² 明文要求）：逐隊印相鄰兩次思考的間隔
   ⇒ 中位數必須 ≒60；min 不得低於 30；
   ⇒ ★**不得只憑「用了 CadenceStagger」就假設分布正常** —— 那正是這次差點出事的地方。
⑨視野時滯分布（0-59），★只印不判。
其餘沿用：①max 掉下來（主詞是 max）／③成對對照（關掉錯開 ⇒ 回到 26 秒量級）／
④T0 當 tick 反應／⑤沒有人讀相位／⑥公平性（次數與首次 tick 不得與 team_id 單調相關）／
⑦fp 會變（附歸因）＋同 seed 兩跑仍相同。
```

# ④ 一句我要你記住的（★這張票的教訓）

```
我在上一版寫「用既有工具、不造新東西」——★那句話本身沒錯，
★★**但我沒有把那支工具的公式，對上【新用法的呼叫頻率】算一次。**
⇒ ★★★而「沒在這個量級驗過」不等於「可能有風險」——**它可能是數學上必然壞**。
（已寫成界限第二十一條。）
```
