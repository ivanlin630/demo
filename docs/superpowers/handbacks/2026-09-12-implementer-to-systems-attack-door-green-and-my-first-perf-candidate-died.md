---
from: implementer
to: systems
status: consumed
slice: 攻擊門 ｜ 驗收（10 天窗，FAILS=0）
topic: ★★★**全綠**：門開 **2.53% → 75.48%**（母體 7957 次 gather，逐字同母體）｜`IDLE 陷阱 0／0`｜**`nO` 三筆逐筆具名**：三筆都是 `舊門=intent`、掃過 4 個候選而 **無位置 2／追不上 2** ⇒ **沒有任何一筆「三種都不是」** ⇒ 你要的可證偽判準**成立**｜★★**而我的第一個 perf 候選【死了】**：`attack_scan` **無領袖呼叫 ＝ 0 次**、有領袖 **7957 ＝ 每次 gather 一次 ＝ 與改動前相同** ⇒ **掃描次數沒有增加** ⇒ ★剩下的候選是**下游**：攻擊 option 現在 **75% 的時間進候選**，10 天提名 **3132** ⇒ **每次 rank 多評一個 option（含四個 term）**
---

★落地：`docs/measurements/2026-09-12-attack-door-acceptance-10d.txt`（commit `bceca2d90`，main）
  ｜worktree `.worktrees/attackdoor`／`feat/attack-door`（`78204b246` 實作 → `09a116d51` 判準可證偽化）

# ① `nO` 那三筆（★你要的形狀：逐筆具名，而不是一個解釋）

```
tick=12780 team=32 舊門=intent｜掃過 4｜無位置 2 追不上 2（同派系 0／無 belief 0／養不起 0）
tick=13020 team=32 舊門=intent｜掃過 4｜無位置 2 追不上 2
tick=13020 team=32 舊門=intent｜掃過 4｜無位置 2 追不上 2
⇒ ★**可行集合空 3／非空 0**；★★**無法解釋 0 筆** ⇒ 判準綠。
⇒ ★★★語意：那三次**舊門開的是【授權】**，而世界上**一個打得到的對象都沒有**
  ⇒ 舊制下它們會走進 `to_task` 然後回 `TASK_IDLE` —— **那正是本票要消滅的東西**。
★而我把「第四種」也先寫進床了：`scanned == 0`（連一個可掃對象都沒有）—— 本卷沒出現。
```

# ② ★★perf：**我的第一個候選死了**（★而這是進度）

```
`attack_scan` 呼叫：**有領袖 7957｜無領袖 0**
⇒ ★「無領袖隊現在也會掃」（spec 風險④的**成本面**）＝ **0 次** ⇒ **不是成本來源**
  （★★而它的**行為面**也因此**本窗不成立**：那些隊一次都沒拿到 fallback target）
⇒ ★★**有領袖 7957 ＝ 每次 gather 一次 ＝ 與改動前相同**（舊制 `find_prosperity_prey` 本來就每次跑）
  ⇒ **掃描次數沒有增加**；掃描規模 `discovered` 合計 **184431**（平均每次 ≈ 23 個）
⇒ ★★★**剩下的候選是下游**：攻擊 option **75% 的時間進候選** ⇒ **每次 rank 多評一個 option ＋ 它的四個 term**
  （10 天提名 **3132**）⇒ ★而要坐實它需要**單價**（每 option／term 的 us）＝ **相位樹** ＝ measurer 的線。
```

# ③ 其餘格（★含三個「不可判／不可比」，我不讓它們混進綠）

```
②IDLE 陷阱 **0／0** ⇒ 綠（結構性主張成立）
★提名 3132 → 贏 12 → 派出 3 → 真的開打 23 ⇒ ★★★**四個數不可逐站相減**（開打的母體不是子集）
③強弱比 母體 **3**、全 **0.00** ⇒ ★只報不判；★★而 0.00 ＝ `self_armed == 0`
  ⇒ **無武裝的隊也在攻擊** —— ★★★這一格**要 blueprint 看**（它是世界的事實，不是驗收門檻）
④勒索 **65**（10 天）⇒ **不可比**：before 側同指標**沒有量**（要另一趟）
⑤滅團 **0** ⇒ **不可判**（母體地板）
⑦最壞單幀 **3.658 s**（>2 秒 64／14400）⇒ ★**與 30 天窗那個 31.0 s 不可比**（兩個窗）
fp ＝ `744d696aa925e429000dad944d7598db`（10 天窗）
```

# ④ 我建議的下一步（★等你裁）

```
①★**勒索 before 側**：同窗同 seed 跑一趟 `main` ⇒ 那一格才判得出「應下降」是否成立（falsifiable）
②★★**perf 單價**：交給 measurer 的相位樹（我這條線的候選已排到只剩它）
③★★★**無武裝的隊在攻擊**：這格我只報，處置要 blueprint —— ★而它可能正是「敗北模型」那條線的入口
```
