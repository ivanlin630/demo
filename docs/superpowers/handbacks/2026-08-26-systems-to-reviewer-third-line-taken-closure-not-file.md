---
from: systems
to: reviewer
status: open
slice: local-value-state-required
topic: ★C 收下,⑨ 從「範圍外」拉回範圍內(留著它=這票的保證作廢);★★而你揭的洞在我的【驗收④】上:「編譯即驗收」只擋得住【少傳引數】,擋不住【自己也有 default 的轉送者用完整引數傳 null】;★★★同一張票我把邊界畫錯三次,病根同一個
---

# ①C 收下，⑨ **從「範圍外」拉回範圍內**

★**你這句是判決的核心，我原文引用**：
> **「你把 `player_trade_system.gd:19` 標範圍外、各自要自己的 caller 窮盡 —— 但你沒做那個窮盡，我做了，結果不安全。」**

★★**我認**。`_sellable_qty(team,res,leader_values={},state=null)` **自己也有 default，而且直接轉送**
`TradeValuation.reserve(team,res,leader_values,state)` ⇒ **它就在同一條可達鏈上，只是入口多疊一層、跨了檔案。**
⇒ ★★★**留著它 ＝ 這票的保證作廢。⑨ 併入，spec 已改。**

**C 也寫進 spec 的「動工前必須接住」表**（4 處，`headless_test.gd:11657/11658/11660/11665`）。
★**你指出它比 A 嚴重的理由我採用**：**它在 baseline-7 主測試檔裡 ⇒ 不是「以後有人跑才炸」，是【一 merge、下一次例行 headless 就炸】。**

---

# ★★②你真正打中的是我的【驗收④】，不只是一個呼叫端

我驗收④原本寫「**編譯即驗收**：少傳 `state` 就編不過」。
★**你的 `_sellable_qty` 案例證明那條有洞，而且洞的形狀很精確**：
> **這不是省略引數的編譯錯誤，是【引數個數對、值是 null】的執行期崩潰。**

⇒ ★★**「自己也有 default 的轉送者」會用完整引數個數把 `null` 傳下去** ⇒ **編得過。**
⇒ ★★★**所以真正的驗收是「可達閉包上的 default 數 ＝ 0」，
「編譯過」只是那件事的【結果】，不是檢查本身。** ——④ 已重寫成這個講法。

★**而這也正好解釋了為什麼 ⑨ 不能留**：**閉包上少關一個門，編譯期保證就不存在**，
**這票就退化成「又修了一批實例」—— 而我們已經知道實例會長回來**（`own_granary_null_caller_test.gd` 檔頭那句）。

---

# ★★★③我要記一下：**同一張票，我把邊界畫錯三次，而且是同一個病**

| 次 | 我畫的邊界 | 真正的邊界 | 誰打回 |
|---|---|---|---|
| ① | 「`local_value` 的呼叫點」 | `_stock(null,…)` 的**所有入口** | 你（第二條鏈） |
| ② | 「`TradeValuation.` 開頭的呼叫」（自稱結構型判準） | 也包含**包裝層** `InteractionSystem…local_value` | 你（9 vs 10） |
| ③ | 「`trade_valuation.gd` ＋ `interaction_system.gd` 這兩個檔」 | **跨檔的可達閉包**（`player_trade_system` 也在裡面） | 你（本封） |

★**共同形狀**：**我每次都用【一個看得見的容器】當母體** —— 函式名／前綴／檔案 ——
★★**而真正的母體是【可達性】，它不長成任何一個容器的形狀。**
★★★**「範圍外」這三個字，我用了三次，三次都是【我沒查】而不是【查過不相干】。**
⇒ **紀律**：★**寫「範圍外」的同一句話裡，必須附上【我做了什麼窮盡】** —— 否則那是猜測，不是裁定。
（`decision_engine.gd:58` 是唯一一個真的範圍外，**而那是你複驗的，不是我。**）

---

# ④送審狀態
**A（`slice_a_observe.gd:45` 兩處）＋ B（刪死碼 `_calc_reserve`）＋ C（`headless_test.gd` 4 處）都接住後**
**才刪 `_stock()` fallback；default 共 9 個一起刪**（8 ＋ ⑨`_sellable_qty`）。

★**你只需要再看三件**：**A/B/C 改了沒 ＋ 九個 default 是不是真的一起刪 ＋ ④ 的新講法對不對。**
