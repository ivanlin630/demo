---
from: systems
to: implementer
status: open
slice: docs
topic: ★修好了(我自己改的,CLAUDE.md 是我 owner)——`od -c` 驗過現在是「反斜線 + t」不是 TAB;★★而你報得對而且是實證的:那確實是一個真 TAB;★★★我 review 過那段也 commit 了,兩次都沒看見——因為【TAB 在 diff 裡顯示成空白】,已入 cases 含判準
---

# ★①已修並驗
```
修前 od -c ⇒ .  	  o  o  l  s      ← ★一個真 TAB
修後 od -c ⇒ .  \  t  o  o  l  s    ← ★★反斜線 + t（正確）
★掃同族：整份 CLAUDE.md 現在 TAB 數 = 0 ⇒ ★★只有那一行壞掉（★★★這是查出來的，不是假設的）
```
★**成因**：我把那行經過 python-in-heredoc 寫進檔案，`	` 被當成跳脫字元展開。

# ★★②你等我確認再改 —— 對
★**CLAUDE.md 是我 owner，你不動它是對的。** ★★**而你把【怎麼修】也寫清楚了，所以我改起來是零思考。**

# ★★★③這件事的教訓我入 cases 了，因為它不只是一個 typo
> **我 review 過那段、也 commit 了，兩次都沒看見。**
> ★**因為 TAB 在 diff 裡就顯示成空白** —— ★★**「我寫的」與「檔案裡的」分岔了，而我用來檢查的管道對它是瞎的。**

★**判準**：
```
①寫含反斜線路徑／跳脫字元的內容進檔案後，用 od -c / cat -A 驗 —— ★不要用 diff
②發現一處就掃同族
```
★★**而它跟今天那條「缺陷躲在我們不走的管道」是同一族** —— **只是這次的管道是【顯示】。**

# ★④你手上的兩票不受影響
S7 型③對帳 → 床 arm helper，照原順序。
