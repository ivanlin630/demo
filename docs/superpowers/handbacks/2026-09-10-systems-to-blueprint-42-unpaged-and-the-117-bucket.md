---
from: systems
to: blueprint
status: consumed
slice: inspect 票交件轉呈 —— 兩件要你裁
topic: ★inspect 票落地(df01410a)：五欄掏好、覆蓋率閘上線(腐爛判定照 R² 的反方向寫)、兩張表都在(ctx 119：已接 2／待接 117／豁免 0 ＋ 補充母體 2 項)｜★★implementer 交回兩件【他不該自己決定】的：(1) 42 欄「未分頁」——他關鍵字只分得出 77 欄，而他連那 77 欄也標成【只是建議】(關鍵字猜的分頁，錯了不會有人發現) (2)「待接 117」這個桶本身要不要開票清空｜★★★另有一件我自己的事故：我把他兩個新 tsv 掃進了我的 commit(同日第二次，第一次是你)
---

# ① 交件（已在 main）

```
五欄  query_team: target{move/combat/social 三個各有語意不合併} / goal_state / work_morale
                  / threat{snapshot:false, note:...}
      query_outpost: residents_here_now[]（★位置謂詞：此刻在這裡，不是屬於這裡）
床    observer_inspect_depth_bed 4/4；★成對對照兩處
      （有目標 vs 沒目標看得出差別／隊走了⇒居民清單變空＝證明印的是位置不是歸屬）
閘    .claude/hooks/ctx-coverage-gate.sh —— ★腐爛判定照 R² 的【反方向】另外寫
      （豁免欄位不得出現在任何玩家可見查詢面），四格成對對照全過
表    docs/process/ctx-exposure.tsv（119 列：exposed 2／exempt 0／todo 117）
      docs/process/ctx-supplement.tsv（事件流【已接】／關係圖全貌【待接】）
```

★**威脅感那格值得你單獨看一眼**：他**沒有**在查詢時 gather（那等於「觀察一次就跑一次引擎」，
而「觀測改變被觀測物」今天已有血證）⇒ 面板印 `{snapshot:false, note:"每 tick 重算"}`。
★★**所以那一格交付的不是「玩家看得到威脅感」，是「玩家知道自己看不到，以及為什麼」。**
要真的看到需要一張「存威脅快照」的票 ⇒ **這是第三件可能要開的票，我列在下面。**

# ② ★★要你裁的（一）：42 欄「未分頁」

```
他用關鍵字自動分了 77 欄（生存／經濟／威脅／社交／記憶），★剩 42 欄關鍵字答不出來。
★★他標成「未分頁」而【不是】塞進「其他」——理由是：其他會是一個【不會變成 0 的垃圾桶】。
★★★而他連那 77 欄的自動分類也自標為【只是建議】：
   「關鍵字猜出來的分頁，錯了不會有人發現。」
⇒ 我覆核這個自制：對。**分頁＝玩家在哪一頁找得到它＝WHAT。**
```

**選項**：
```
(a) 你逐欄裁 42 欄（★而我建議連那 77 欄也抽驗，因為「錯了不會有人發現」對它們一樣成立）
(b) 你只裁【分頁的判準】（例如「以玩家會問的問題分，不以欄位名稱分」），
    由 implementer 依判準重跑一次並附「判準怎麼套用」的說明，你再抽驗
★我傾向 (b)：119 欄逐欄裁很貴，而判準錯了 (a) 也一樣要重來。
```

# ③ ★★要你裁的（二）：「待接 117」這個桶

```
閘現在容得下 todo，而你裁的是【全要】⇒ ★todo 是一個【應該要清空】的桶。
而清空它 ＝ 把 117 欄真的端到查詢面上，那【不在 inspect 這張票的五欄裡】。
★★他沒有偷偷做掉，也沒有標成豁免充數（exempt = 0）——這個克制是對的。
```

**選項**：
```
(a) 開一張獨立票「ctx 逐頁端出」（依分頁裁定，一頁一批）
(b) 併進票②（人話層）——★但那張的重點是【解釋】不是【端出】，混在一起會讓兩件事互相拖
★我傾向 (a)，且【依分頁分批】：一頁一票、每票的驗收就是那一頁的覆蓋率歸零。
★★理由：117 一次做完 = 一張無法驗收的大票（這是我們今天已經說過兩次的形狀）。
```

# ④ 第三件（我列出來讓你知道它存在，不急）

```
「存威脅快照」票 —— 讓 threat_react/threat_id 有一份可讀的快照，
★而它有一個真實的設計成本：快照要存在哪、多久更新一次、會不會變成另一個 god-view 入口。
⇒ 不建議現在開；等分頁裁定下來、玩家真的開始看那些頁之後再談值不值得。
```

# ⑤ ★★★我自己的事故（要你知道，不是要你處理）

```
我把 implementer 的兩個新 tsv 掃進了我的 commit f5f84c56。
★成因與你那次【完全相同】：共用 main dir ＋ 目錄層級的 add。
★★而我先前給你的建議（「別用 git add -A，改成列出你自己那幾個路徑」）
   ★★★【本身就是錯的】——我列的是【目錄】，而目錄仍然是一個會裝進別人東西的容器。
⇒ 正解是你採用的那個：**逐檔**（git commit -- <明列檔名>）。你的習慣比我的建議對。
⇒ 機械擋已補（bash-guard 現在也擋目錄層級 add），★而補的時候順手抓到一件更糟的：
  **舊版那條「禁 git add -A」的護欄，對【-A 是最後一個 token】的寫法從上線到今天
  一次都沒有抓到過**（結尾錨寫錯）—— 也就是說，我們以為守了一個月的東西是空的。
```
