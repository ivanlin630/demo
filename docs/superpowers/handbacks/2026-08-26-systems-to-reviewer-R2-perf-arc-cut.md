---
from: systems
to: reviewer
status: open
slice: perf-arc-cut
tier: design
topic: R②請審效能 arc 的【切法】(不是某一刀):★★★我主張第一件不是刀是分母(scaling 曲線+熱點分解),否則每刀無法歸因、「50+ 隊到了沒」是不可達驗收;★★而 team_discovered 有 48 個 production 讀者跨 13 檔 ⇒ 不做分解就換迭代源=改 48 處而多數無效;★三問在末段
---

# 送審：`docs/superpowers/specs/2026-08-26-perf-arc-cut-HOW.md`（★審【切法】不是某一刀）

用戶裁定效能 arc 即刻開（五刀：T0 事件驅動／髒旗快取／掃描剪枝-換迭代源／錯峰／隊數收斂），**目標零 LOD、50+ 隊**。

## ★★★①我主張：**第一件不是刀，是分母**
**沒有 scaling 曲線** ⇒ ①每刀的效果無法歸因 ②★**「50+ 隊到了沒」回答不了 ＝ 不可達驗收**。
★**slice 0 已派 measurer（純量測、零 code 改：config-only 隊數階梯 ＋ 既有 `phase_timing`）。**

## ★★②而 `team_discovered` 那一刀特別需要先分解
```
world_state.gd:25  team_discovered: Dictionary   # int → Array[int]
production 讀者 48 處、跨 13 檔（★機械查：grep -c，不是目測）
```
⇒ ★★★**N² 的成本不是「它存在」，是【誰在每 tick 走它】** —— **不做分解就換迭代源＝改 48 處而絕大多數毫無效果。**

## ★★★三問（★你今天三次都抓到我沒問的那件，所以我先把我最不確定的攤開）
1. ★**「報曲線形狀不只單點」這個驗收夠不夠可執行？**
   我寫的是「**斜率變了才是治了 N²；截距降低只是常數項**」——★★**但我沒定義要幾個點、怎麼算斜率才不算過度解讀。**
2. ★★**第五刀「隊數收斂」我標成【不是純 perf 刀，驗收要含行為面】** ——
   ★**請驗這個判斷**：它會改變世界內容（隊變少 ＝ 故事變少），**混在 perf arc 裡驗會不會讓行為退化被 perf 數字掩護過去。**
3. ★★★**我最擔心的**：**五刀之間會不會有「A 刀讓 B 刀的收益消失」的關係**（例如②髒旗快取讓③剪枝無關緊要）
   ⇒ ★**若有，序就不能照用戶列的順序照做，而我現在沒有能力判斷** —— **你讀 code 比我快，麻煩看有沒有這種互斥。**

★**slice 0 不等你**（純量測、不含設計承諾）；★★**五刀的序與第一刀等你 CLEAN。**
