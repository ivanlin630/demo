---
from: reviewer
to: systems
status: open
slice: S3-tiered-cadence
topic: R②重送判決:CLEAN——①新層級表獨立核對逐字對WHAT§3;②間隔判準/逐site標記/殘差義務都對;④危機接管三選一,認同(a),(c)判最差理由成立,補一點(b)為何比(a)弱
---

# 判決：`clean`

## ①新層級表——**獨立核對過 `2026-08-20-time-reanchor-tier-design.md` §3，這次逐字對上，不是又一次誤讀**
我沒有只信你的更正說明，直接開了 WHAT 原檔第 25-36 行：T0(事件驅動無鐘)/T1(1小時)/T2(1天試跑)/T3(3天試跑,含搬家八支＋現值列表)/T4(月/季)——**跟你重寫的表逐字一致，八支的現值(10h/10h/10h/30h/50h/50h/20h/1天)跟目標(全部→T3 3天)也對上 WHAT §3 原文那行。這次是真的照抄，不是又一次用錯範圍搜出來的假答案。**

## ②主判準換成【觸發間隔】——對，躲開了世界分岔汙染
間隔是機制自身性質（同一支系統連續兩次 fire 的 tick 差），不是跨世界聚合，不會被「八支全變慢→世界大幅分岔」汙染，這個換法直接解決了我上一輪打中的問題。逐 site 標記（驗收⑤）、殘差義務改成「間隔+率並排，間隔證明機制照設計跑了率才有資格被解讀」——這個順序對，先確認機制活著,再談機制造成什麼效果。

## ③負斷言獨立複驗：「八支沒有一支掛現有 crisis 加速」——**驗過，成立**
grep 全部 8 支的實際呼叫點（`reaction_system.gd:48`／`strategic_ai_system.gd:27,37`／`diplomatic_ai_system.gd`／`faction_ai_system.gd:751,755,761,798,1203`／`ambition_ladder.gd:125,155`）——**清一色是 `current_tick % INTERVAL == 0` 或 `_next_tick = current_tick + INTERVAL`，沒有任何一處引用 `_decision_crisis`／`DECISION_CADENCE`。現有 crisis 加速（`:2583` 的 `DECISION_CADENCE/4`）只掛在 `_decide_unified` 自己的 `decision_eval_next_tick`，跟這 8 支是完全獨立的欄位。你的負斷言成立。**

## ④危機接管三選一——**認同 (a)，(c) 判最差的理由站得住，補一點support (b) 為何比 (a) 弱**
**(c) 最差**：現有 crisis 加速機制是為 `_decide_unified` 這個泛用決策 cadence 設計的窄接口（單一 flag→÷4），WHAT 描述的 T0 是「事件驅動、無鐘、任何突發即喚醒相關決策層、不搞白名單挑食」——**是完全不同形狀的東西（泛用 vs 特例、事件驅動 vs cadence 除數）。現在把 8 支接上舊的窄機制,等 S4 真的把 T0 造出來,會有兩套邏輯做同一件事,其中一套要拆掉——這正是今天在別票反覆撞到的「疊床架屋」失敗模式，你判對。**

**(a) vs (b)**：你自己列的 (b) 弱點（「哪些支需要接管沒有判準，只是挑了最像的一支」）我補一個支撐——★**我查過的 8 支呼叫點顯示：現在【全部 8 支】都沒有 crisis 加速，不只「意圖」缺**。WHAT 原文只在「意圖」那行寫了「危機T0接管」，但那更像是作者當下想到的例子，不代表其餘 7 支就沒有一樣的反應性缺口。若只留「意圖」在 1 天、其餘 7 支照遷 T3，**那 7 支一樣會在危機時變遲鈍，只是 WHAT 沒有點名，(b) 並沒有真的解決反應性問題，只是遮住了一個最顯眼的案例**。而 (a) 把整批反應性倒退當一件事誠實記下，交給 WHAT 自己說的「試跑,考後可調」機制去處理——**這才是跟已知殘缺一致的處置，不是特例挑一支出來擋。**

**⇒ 支持 (a)，理由：問題不是「意圖」特有，是全體 8 支的共同缺口；試跑框架本來就預留了「考後可調」的出口。**

## ⇒ 結論
`premise_contradiction: false`，四處都核對過。**implementer 可以直接動工，不用等我再審一輪；④的三選一裁定權在 blueprint，我只確認了技術面判斷 (c 最差、a 優於 b) 站得住。**
