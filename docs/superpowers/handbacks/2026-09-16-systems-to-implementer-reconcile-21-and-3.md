---
from: systems
to: implementer
status: open
slice: 掠奪 ｜ **要一條對帳式：`21 = ? + ? + ?`**
topic: ★★★**`21 次派工全部第 1 順位` 與 `won_anyway ＝ 3` 不可能是同一件事的計數** —— 若每次派工都是第一名，贏 argmax 就至少 21 次｜★**所以兩者之間有一個【定義或母體的差】，而我要它有名字**｜★★**對帳式**：`21 ＝ 新 rank 贏 ＋ 承諾慣性沿用 ＋ 其他（各自具名）`｜★★★**這比再跑一輪重要** —— **blueprint 已經被迫對用戶更正兩次，而這三個數長得像可以互相佐證，它們不能**
---

# ① 我要的東西（★一條式子，不是一段話）
```
**21（solo 路派工）＝**
  \u2460 **當次新 rank 且掠奪贏 argmax** ＝ ?
  \u2461 **沒有新 rank，沿用上一次的決定**（承諾慣性／持守／latch）＝ ?
  \u2062 **其他** ＝ ?（★**每一項給一個名字，不要「其他」吞掉三成**）
★**而 `won_anyway = 3` 要對上①** —— 若 ① ≠ 3，**告訴我它們的定義差在哪一個字上。**
```
★★**我的猜測（★標明是猜測，可被你推翻）**：`won_anyway` 是【更窄】的定義
（「**有 coeff 也贏**」＝ 只數那些 coeff 真的被算進去的 rank），**而派工計數不管那個**。
★★★**若是這樣，那不是矛盾，是【兩個定義】** —— **而那正是要寫下來的東西。**

# ② ★而我要先講清楚我為什麼不接受「大概是因為…」
```
今天這條線上的每一個錯，都是**兩個看起來一樣的東西**：
  共同因子 vs 壓抑源 ／ 標籤換了 vs 機制換了 ／ 贏了 vs 被輪到 ／ 相同 vs 恆真句
⇒ ★★**而每一次拆開它的，都是【一條對帳式或一個逐筆樣本】，沒有一次是靠解釋。**
```

# ③ 其餘兩件照舊（順序不變）
```
\u2460 `unpriced_prey` ⇒ 接 `bucket_floor`（★同一個計算點，★★驗收：下降但**不歸零**）
\u2461 `reorder_same_need_first` 的反事實（只改回那一格，印 `main_layer_of` 與重排次數前後）
```
