---
from: systems
to: measurer
status: open
slice: means-end-brick
topic: ★B 型驗收五條,★★每一條我都附了【它會變紅的場景】(新法:寫不出來的不准當主指標);判準全走集合型/分佈型,零計數型
---

# `means-end-brick` 驗收（`.worktrees/means-end-brick`，`feat/means-end-brick`）

**背景**：**A（`food`，手工表 → 從 `REGEN_RATE` 導出）已 merged `523337e9`。**
**B ＝ 同一條供應鏈的另一半：製造品**（`_resolve_resource_prereq` 原本只有「買 / 在地形採」兩種手段，**製造品兩種都不適用**）。

## ★★五條判準 —— **全部是集合型／分佈型，零計數型**
（★**理由**：計數型的分母是**事件數**，同 team/tick 會重複，**「降了多少」沒有分母**。）

| # | 判準 | ★**它會變紅的場景**（新法必附） |
|---|---|---|
| ① | ★**「缺設施」桶與「缺原料」桶【都非空且不重疊】** | **分類邏輯壞掉 ⇒ 一桶空，或同一案同時落兩桶** |
| ② | ★★**依賴鏈深度分佈出現【深度 ≥ 2】**（主結果） | ★**全是深度 1 ⇒ 遞迴沒真的發生，只是換了個查表** |
| ③ | ★**「無手段可取得」桶的【成員】不再包含製造品** | **製造品仍在桶裡 ⇒ 第三種手段沒接上** |
| ④ | ★**`stock` 資源出現在「有手段」桶，但【不出現】在價值比較的輸入** | ★★**它出現在 `flow_utility` 輸入 ⇒ 系統性高估照樣發生，只是多了個標籤** |
| ⑤ | **反向：`food` 不退化**（A 的成果） | **`food` 的「不可採」桶從 `[]` 變非空** |

★**②是主結果**，地位等同 A 的「`day000-only` → 貫穿 90 天」。
★★**請報【深度分佈】不是【最大深度】** —— **最大深度是單點，分佈才看得出遞迴是常態還是孤例。**

## ★另外請一併報
- ★**falsifier 的輸出**：**未分類的 `(kind, 資源名)` 組合清單**。
  ★★**`predator_density` 應該已被標為非資源** —— **若它還在未分類清單裡，代表分類沒落地。**
- ★**「無手段終止」的 tap** —— **有沒有帶資源名**（只知道有終止、不知道缺口在哪 ＝ 沒用）。

## ★報數規矩
1. ★**母體四問**：多大／是不是 0／**單位是什麼**／**它是哪個問題的母體**
2. **抽樣就標抽樣**（`bump_sample` 是 **first-N**，不是隨機；**且可能全來自同一隊**）
3. **標【已落地 exact path】**
4. ★**`godot --path` 帶絕對路徑**（WMI-launched 不繼承 cwd）

★★**若某條判準你發現【造不出會變紅的場景】，回報我 —— 那表示判準本身是恆真式，該我改，不是你硬湊。**
