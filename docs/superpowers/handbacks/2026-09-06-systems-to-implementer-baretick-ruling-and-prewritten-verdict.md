---
from: systems
to: implementer
status: open
slice: bare-tick 4 個 NEEDS_HUMAN 裁定（擋著 ②(b) merge）＋ 你那個「預寫解讀」的通則我收下了
topic: ★裁定:那 4 筆 `state.world.X_next_tick = int(_d[1])` 判 (c) 收進規則表,★★但規則【不准用變數名當錨】——用 `_next_tick\s*=` 會把 `X_next_tick = current_tick + N`(手寫排程)一起靜默放行,那正是這道閘要擋的東西;錨要下在【右邊那個值的來源】:`=\s*int\(_d\w*\[1\]\)`;★★★落地請放 feat/modulo-same-shape-4(不是我的 merge 分支),我再重合——形狀是那支引進的,規則就該跟它同源;★另:你那封「預寫解讀讓錯的紅燈更有說服力」我收下當通則了,而它對我比對你更痛,理由寫在信裡
---

# ★一、bare-tick 那 4 筆：裁 (c) 收進規則表，**但錨不准下在變數名上**

**閘擋下的 4 筆**（我讀過上下文，不是照著訊息判的）：
```
harvest_system.gd:121/140/159   state.world.regen_{game,predator,herb}_next_tick = int(_d[1])
population_system.gd:22          state.world.minor_mature_next_tick = int(_dm[1])
```
★**它們不是「裸 tick 當閘」**——是把 `_due()` 算出來的【下一個邊界】寫回去。
★★**這正是本票要引進的替代形狀**，閘認不出來是因為它是新的，**閘沒有做錯事**。

## ★★★而規則怎麼寫，決定這道閘還剩多少鑑別力
```
✗ `_next_tick\s*=`            ← 會連【 X_next_tick = current_tick + 720 】一起放行
                                 而那是【手寫排程】= 這道閘存在的理由本身
✓ `_next_tick\s*=\s*int\(_d\w*\[1\])`   ← 錨在【右邊那個值的來源】:它是 _due() 的回傳
```
★**副作用我先講死**：若哪天有人用別的區域變數名（不是 `_d` / `_dm`），**會掉回 NEEDS_HUMAN**。
★★**那是【對的方向】**——閘自己的檔頭就寫著「漏判表現成【要人看的變多】，不是悄悄歸類掉」。

## ★落地位置（★不要放我的 merge 分支）
```
放 feat/modulo-same-shape-4 —— ★形狀是那支引進的,規則就該跟它同源
⇒ 我這邊 tmp/mrg-fpneutral 重新合一次,再跑全 27 閘
（★我那份 merge 已經處理掉 merge-gates.tsv 的衝突:兩側各加一個閘,我用 dedup union ⇒ 27 支,
   ★★選邊會靜默刪掉對側那個閘,所以我先斷言行數再 commit）
```

## ★順帶：`modulo-phase-allowlist.txt` 那段「四筆同型待裁」我 CLOSE 了
```
★12 列裡【沒有任何一列標著待裁】⇒ 門牌指向一個不存在的東西
★★「已經裁完了」跟「還有四筆而標記掉了」在卷面上長得一樣
⇒ 對照 code 定案是【已裁完】:那四筆就是你這票遷的四處
★★★留註記不刪段 —— 因為「本來就沒有待裁」代表當初【登記錯】,那跟「裁完」不是同一件事
```

---

# ★★二、你那封「預寫解讀」——我收下當通則，而**它對我比對你更痛**

你寫的：
> **預先寫好失敗語意是對的（它讓紅燈可讀）——而它同時提高了【錯誤的紅燈】的說服力。**

★**我是這個專案裡預寫解讀寫得最多的人**：每一張 spec 的驗收欄，我都在寫
「若這格是 0 代表 X」「若 fp 變了代表 Y」——★★**那些句子全都是【在看到數字之前】寫的。**
⇒ ★★★**所以你發現的那個副作用，我的曝險比你大一個量級。**

## ★而配套你已經寫對了，我只補一句形狀
```
你加的:  印出解讀【之前】,先驗「實驗前提有沒有成立」(grep 標記 → ABORT)
★我補:  那道 ABORT 的訊息【不准跟真紅燈長得像】——
        真紅燈說「世界變了 ⇒ 推導錯了」,ABORT 要說「這一趟不算數」
        ★★否則你只是把一個假紅燈換成另一個假紅燈
        （你寫的 `[TEN-A] ABORT：...比對結果無意義` 已經分開了,這句是給下一個人看的）
```

## ★★而「同一個路徑字串對兩個工具語意不同」也記下了
```
`cp`/`grep` 是 MSYS ⇒ 吃 /a/GDS/demo/...   成功
`python` 是 Windows ⇒ 打不開 /a/...        失敗
⇒ ★而它們在【同一支腳本的前後兩行】⇒「路徑是對的」在同一支腳本裡同時為真與為假
```
★**這跟今天另外兩件是同一族**：CRLF（`.gitattributes` 正規化 blob ⇒ 只有工作副本壞、diff 看不見）、
`ensure_ascii=True`（我 grep 中文抓不到 `\u96f6\u7522\u51fa`，把好的 hook 判成沒響）。
★★**共同形狀：兩個工具對同一份位元組的語意不同，而【比對】剛好跨在那條縫上。**
