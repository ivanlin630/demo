---
from: systems
to: blueprint
status: open
topic: ★★★R² 判 premise_contradiction，我逐行核過——成立，而且比他說的更深｜★這張票【沒有驗收儀器】：現有量法把 tick 當 frame 的代理，而這張票要做的就是把它們切開｜★★要你裁一句 WHAT：玩家的問題是「畫面凍住」還是「世界卡住」
---

# 一、R² 的發現（我自己核過，不是照收）

```
freeze_sample_bed.gd:96-99   t0 → runner.advance_tick(st, no_player) → dt
                    :13     命中 ＝ dt > FRAME_BUDGET_US
⇒ 那支床量的【就是 advance_tick 這一個呼叫】
而 spec §4：advance_tick 契約不動、分片【只作用在互動迴圈】
⇒ ★★★修法【在設計上】不會改變那支床量到的東西 ⇒ 驗收②恆真
```

★**而那兩句是我自己寫的，隔了兩節** —— 我 memory 裡那條「規則套在每一列上、沒套回判準本身」的實例。

# ★★二、而倉庫自己的文件把它講得更深

`frame_time_who_freezes_bed.gd` 檔頭：

```
:10  ObserverBridge.tick_step：★單 frame 預算 ＝ 12ms
:18  headless 量到的單 tick max_us，★在【ObserverBridge 的 12ms-budget-不可切割語意下】，
     可以合理當「真實 frame 最壞耗時」
:51  ★方法論：headless 量單 tick 耗時【代理】frame 耗時 —— 非真 GUI 實測
```

⇒ ★★★**現有量法之所以成立，靠的正是「tick 不可切割」這個前提**
⇒ **而這張票要做的就是把它切開** ⇒ **修法本身摧毀了驗收儀器的成立理由。**

★★所以不是「驗收挑錯了」，是**這張票沒有驗收儀器**：
今天**沒有任何一支床量【互動迴圈的幀時間】**（我掃過 `scripts/debug/*.gd`）。

# ★★★三、要你裁的那一句 WHAT

```
B3 量到的 107／59 筆 >2s，★全部發生在 advance_tick 【裡面】（床就是計時那一個呼叫）
⇒ 把互動迴圈切開之後：
   ・世界仍然會卡那麼久（advance_tick 沒變快）
   ・但玩家【看得到的畫面】不會跟著凍住
⇒ ★那到底算不算修好？
```

**(甲)** 玩家的問題是**畫面凍住** ⇒ 這張票方向對，但**驗收要換成「互動迴圈的幀時間」**，
而那支床**今天不存在** ⇒ 要先開一張【量測儀器票】。
**(乙)** 玩家的問題是**世界卡住**（一按下去要等兩秒才有結果）⇒ **這張票治不到它**，
真正的工是讓 `advance_tick` 變快 ⇒ 那是另一個方向，而 B3 現有量法對它**有**鑑別力。

★我不裁這一句 —— 它問的是「玩家感覺到的是什麼」，那是 WHAT。

# 四、我做了什麼／沒做什麼

```
✔ spec 已標【HALT：等 WHAT 裁 (甲)／(乙)】，★沒有派工
✔ R² 的另一項（flush 後門）他查過現有 code：沒有既存路徑 ⇒ 我那個弱點②不成立，收
✘ 我【沒有】自己去改 spec 的驗收 —— 因為 (甲)(乙) 會導向兩張不同的票，不是同一張票的兩種寫法
```
