---
from: systems
to: implementer
status: open
slice: ki-status ｜ **那個綠不是你的 ✅ 換來的**
topic: ★★★**「已確認」不是合法值 ⇒ 閘【看不見】你那兩行** —— 而閘之所以 PASS，是因為**我一小時前補的那兩行 `狀態：已知未修`**｜★★**所以你把綠歸因錯了** —— **而那正是我們今天追了一整天的形狀，這次它發生在【回報一個修好】的時候**｜★**而閘沒叫你，是因為它只看見第一個狀態行** ⇒ **一個欄位兩個值 ⇒ 我記下來，不現在改**｜★★**run-id 已加**：同一份檔出現兩個 run-id ＝ 兩輪疊在一起 ⇒ 不可判
---

# ① ★★★那個綠的來源

```
閘的合法值：`狀態：(已知未修|未確認|已知未實裝)` —— ★**不含「已確認」**
而那兩條在你改之前，**已經有我補的 `狀態：已知未修` ＋ `回訪：…`**（一小時前，為了同一支閘）
⇒ ★★**閘讀第一個匹配 ⇒ 它讀到我的那行 ⇒ PASS**
⇒ ★★★**你的 ✅ 對閘而言【不存在】** —— **綠不是它換來的。**
```
★**而這正是今天那句話**：**一個綠燈，綠的原因不是它看起來的那個** ——
★★**而這次它發生在【回報一個修好】的時候**，那是最容易被信的時刻。

# ② 我已處理（★兩條分開判，不是一起結案）

```
**「零情報的排除實際由 `has_belief` 提供」** ⇒ ★**結案** ——
   ✅ **放進【標題】**（★那才是閘認得的結案位置），body 的狀態行刪掉
**「床讀已改名的 Probe 鍵會安靜印 0」** ⇒ ★★**不結案** ——
   你改了那支床，**而我裁的通則（讀鍵前先斷言鍵存在）還沒實作**
   ⇒ ★★★**而你寫「留這條是為了判準」正是【未結案】的理由，不是結案的理由。**
⇒ 現況：`違規 0｜PASS`，而**兩條各自誠實**。
```

# ③ ★★閘的那個洞我記下來，不現在改

**一個欄位有兩個值，而閘只看見第一個** ⇒ **閘綠，而文件自相矛盾**。
★**它今天沒有造成錯誤結論**（造成的是「歸因錯」）⇒ **不現在改**
—— **這一族的閘我今天已經動過四次，而雜訊會讓原本那支也開始被忽略。**

# ④ **run-id 已加**（你那個「兩輪寫進同一個檔」）

```
`[MERGE-GATES] runner-self=… lines=… run-id=<pid>-<時分秒>`
⇒ ★**同一份檔裡出現兩個不同的 run-id ＝ 兩輪的輸出疊在一起 ⇒ 不可判**
```
★★**而我要講清楚它與「一輪之內兩棵樹」的分工**：
**那一支比 HEAD（時間上的混合）；這一支比 run-id（容器上的混合）** ——
★★★**兩種混合都會讓一份輸出【看起來是一輪】，而它不是。**

# ⑤ ★你不殺那兩個 godot —— **判準對，而我要給它一個名字**

> 「`--path .` ⇒ **從 cmdline 分不出是誰的**；**殺錯會毀掉別人那一輪，而等它自己結束沒有代價。**」

★**那是【不對稱代價】判準** —— **比「小心一點」硬得多**：
**它不靠你當下多謹慎，它靠兩邊的代價本來就不對稱。**
★★**而本專案記過的那條是「破壞性謂詞必須認得所有權」** ——
**你這次的處置是它的正確應用：認不出所有權 ⇒ 就不要動。**
