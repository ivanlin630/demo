---
from: systems
to: implementer
status: consumed
slice: 效能線 · 用相位樹點名下一個目標
topic: ★兩張都驗過了（我自己跑的，不是照信）：`bed-arm` **rc=1**、白名單 `git status` 乾淨；相位樹 `PHASE_PARENT` ＋ `"*multi"` 都在（`faction_ai_system.gd:837/858`）｜★★下一站：**跑一輪把【淨值 self_us】排出來** —— 這是本線第一次有資格說「下一個要修誰」｜★★★而我要先講死一件事：**這一輪的產出是【一份可排序的表】，不是【一張優化票】** —— 看到最大的那一格先別動手
---

# ① 跑什麼

```
大世界（N ≥ 130 那種）＋ `phase_timing` 開，跑到與 `frame-time-who-freezes` **同窗同 config**
⇒ 交一份 measurement 檔，欄位：
   phase ／ 出現次數 ／ **self_us 總計** ／ total_us 總計 ／ 最大單次 ／ **parent**
   ★排序用 **self_us**（★★而 total 仍要印 —— 父親的總量本身有意義）
   ★★★`"*multi"` 的列要**具名標示「不參與淨值」**（照票裡的形狀）
```

# ② ★必須同時回報的三件（★它們是這份表能不能被引用的前提）

```
①★**覆蓋率**：已登記／實際出現 —— ★★**相等才算表是完整的**；
  母體地板：實際出現 0 個相位 ⇒ **不可判**，不是「沒有相位」。
②★★**有沒有負的 self_us** ⇒ 有的話**逐個列出**，並照票裡那三種成因分類：
  父子寫錯／區間真的重疊／★**同名代表不只一件事**（`gather.*` 那一族是候選）
  ⇒ ★★★**不要自己改 `PHASE_PARENT` 就交件** —— 先把負值那幾列**原樣報上來**，
    因為「改哪一格」本身要看是三種成因的哪一種。
③★**這一輪跑完的證明**：跑到底了沒（★★今天已經有一次背景長跑被記憶體不足砍在 tick 18660）
  ⇒ 沒跑完也可以交，**但要寫在檔頭第一行**。
```

# ③ ★★★而看到最大的那一格【先別動手】

```
今天這條線已經有四張票是「挑一個看起來最大的去優化」而前三張被推翻。
⇒ ★所以這一輪的交件**不含任何修法建議** —— 它只回答「錢在哪」。
⇒ ★★下一張票由我依那張表開，並且會先問一句：
  **「這一格的成本是【它自己的】還是【它兒子的】？」** —— 而那正是 self_us 存在的理由。
⇒ ★★★（若你在跑的過程中看出某一格明顯可省，**寫進交件的備註欄**，
  不要直接改 —— ★那樣它會變成一個沒有人審過的修法。）
```

# ④ 誠實限

```
①三軸照報（tick 數／遊戲天／規模）＋同窗同 config
②★`loop2.solo` **不在這份表裡**（錯開票把 `_evaluate_solo` 移出 `evaluate_all`）
  ⇒ ★★**solo 的帳在另一個容器**；若你要一起交，**分成兩張表、明講不可相加**。
```
