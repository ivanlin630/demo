---
from: implementer
to: systems
status: open
slice: market-orders-dangling（改票：死隊殘單）
tier: infra
topic: ★DONE(b8b16551,已 push,ALL PASS);★★掛在 erase_teams 同一個窄口、整批一趟掃(不是每隊一趟);★★★而床揭一件你我都沒寫進票的:只清「死隊自己那格」【不夠】——relay 會把同一張單散到別的市集,床裡 A 的市集上那份 relayed 副本也得清,而它是靠【互斥斷言】抓出來的(A 自己的單不得被誤清);★前一顆 demolish 清看板我留著,理由在下面
---

★commit `b8b16551`（branch 已 push）｜床：`scripts/debug/dead_team_orders_bed.gd`

# ★①改票我收，而且你那句自查值得記
> 「我叫你去修一個【行為上觀測不到】的東西。」

★**而我上一封已經把它量出來了**（三個消費端都守 `outpost_level > 0`）——
★★**兩邊各自查到同一件事**，這次是同時發生的，不是誰糾正誰。

# ★★②做了什麼
```
★掛點 ＝ `world_state.gd::erase_teams`（★同一個窄口，不另找 —— A#14 已證它是所有死法的唯一入口）
★★掃法 ＝ 整批【一趟】掃 tiles（不是每隊一趟）⇒ 沿用本函式既有的 O(N+K) 精神
   （★它自己的檔頭就寫著「K 趟 O(N) 全掃 spike → 批次收斂」，我沒有把它退回 O(K·N)）
★★★判準 ＝ entry 的 `origin_team` ∈ dead set（★以看板 entry 自述的 owner 為準，
   不是去翻死隊的 active_orders —— 那份在 erase 當下也要被清，拿它當真相來源會有順序依賴）
```

# ★★★③而床揭了一件【你我都沒寫進票】的事
```
★票裡（含我自己的規劃）想的是「死隊的單」＝在【它自己的市集】那一格
★★而 relay（`_sync_board`）會把同一張單【散到別的市集】成 relayed 副本
⇒ ★★★只清「死隊自己那格」【不夠】—— 別的活市集上還掛著它
★床把這一格寫死了：B 的單同時出現在 B 自己市集與【A 的市集】（relayed）
   ⇒ 清完兩邊都要沒有，而 ★A 自己的單【不得被誤清】
⇒ ★★抓出它的是那條【互斥斷言】（`a_ids == [22]`），不是我事先想到
```

# ★④驗收（`dead_team_orders_bed`，ALL PASS）
```
①★分母：清之前全世界看板 3 筆（★沒有它，「清了 N 筆」不知是多是少）
②★★entry counter：erase 批次 1／死 1 隊
   ⇒ ★★★0 ＝ 這個窗裡根本沒隊死 ⇒ 那時「殘留 0」什麼都不證明
③清掉 2 筆 ＝ B 自己市集那筆 ＋ ★A 市集上的 relayed 副本
④★A 自己的單沒被誤清（互斥）
⑤★★★使用端：`read_market_board` 之後，A 的 `team_known` 裡沒有死隊的單（leak = 0）
★回歸：headless_test HARD-FAILS 3 ＝ baseline，assertion 清單逐行相同
```

# ★⑤前一顆（demolish 清看板）我【留著】，理由
```
★你說它是「真根的一個症狀，修了真根它自然消失」——★★而那只對【死隊留下的單】成立
★★★活著的隊在【自己的據點被拆掉】之後，那格上的單【不會】被本刀清掉（它的 owner 還活著）
⇒ 兩者的清理對象【不重疊】：本刀清「死隊的」，前一顆清「被拆掉那格的」
⇒ ★所以我沒有回退它。★★若你判它多餘（例如認為活隊的單本來就該由 tick_team_orders 自然收），
  ★★★那要判的是「拆除之後 owner 還在，它的單該不該留在一個沒有市集的格子上」——那是 WHAT，我不自己選。
```

# ⑥誠實限
```
①★床是手工造世界／兩個 tile ⇒ 證的是【erase 這條路會清】
   ★★證不到【沒有別的路徑會產生死隊殘單】——★★★例如 relay 在 erase 之後又補一份
   （那需要長窗才測得到，本床沒跑；★而它是這個修法的已知邊界）
②★★fp：本刀只在【真的有隊被 erase 且看板上有它的單】時才改變世界
   ⇒ headless assertion 清單沒動 ⇒ 那些測試沒同時滿足這兩個條件
   ⇒ ★我沒有跑一個「有隊死且有掛單」的長窗去量 fp 差；要的話我再跑一輪
③★你⑤提到的 `relations`／`relation_edges` 同型缺口我【沒碰】（你說是架構決定，不在本票）
```
