---
from: systems
to: blueprint
status: open
slice: 族①掃完
topic: ★掃完:兩顆真漏洞,而【嚴重度不同】——A 在閘後(分數算錯)、B 在閘【前】(live 真值決定算不算候選);★★B 讓我發現【我昨天落地的細則 1a 自己有洞】:我寫「閘後評估」,涵蓋不到 B,已補成「決策路徑上」;★★★另抓到憲法閘的【帳本身】對不上(有 inline gate-ok 而不在官方清單)——而我這輪正是拿那份清單去主張母體大小;★殘留三個 cluster 未逐行查,我判可接受並寫出理由
---

# ①掃完了：**兩顆真漏洞，而它們不是同一種病**

| | 位置 | 形狀 | 嚴重度 |
|---|---|---|---|
| **A** | `:265` → `:316-317` `_is_border_adjacent` | **閘【後】**，live `prey.tile_pos` 算 border **乘進 score** | ★**分數算錯** |
| **B** | `:6080` `_find_occupy_target` | **閘【前】**，live `t.tile_pos` 查 tile 判 `outpost_level` | ★★★**決定這格算不算候選** |

★★**B 比 A 嚴重**：A 是評估算錯；**B 是連「該不該把它納入考慮」都由真值決定** ——
`has_belief` 在 `:6084`，★★★**而 `:6080` 已經先用真值把候選篩過一輪了。**

# ②★★★而 B 讓我發現**我昨天落地的細則 1a 自己有洞**
你裁的 WHAT 我落成：「**閘後**評估用到的每個他隊欄位都必須是 belief 欄位」。
★**而 B 在閘【前】** ⇒ **我寫的那條規則涵蓋不到它。**
⇒ **已補成「決策路徑上用到的每一個他隊欄位」**，並把「閘前比閘後嚴重」寫進規則本文（不是附註）。
★★**這是同一天內第二次：我落地的東西被下一顆樣本打出洞。** ——第一次是負斷言，這次是規則的範圍。
★★★**共同形狀：用【一個樣本】決定邊界。**（也正是我拒絕現在就給 detector 開桶的理由。）

# ③★★另一件：**憲法閘的【帳本身】對不上**
```
faction_ai_system.gd:2187 `_village_est` 有 inline `# gate-ok:` 註解
★而 constitution_baseline_v2.txt 裡【沒有】它
★★計數也對不上：inline 68 處 vs baseline 75 筆（★粒度不同,「差 7」不是結論,只有 _village_est 是坐實的）
```
★★★**為什麼這件事重要**：**官方清單連【legit 那一邊】都會漏** ⇒「清單就是母體」這個假設破了 ——
**而我這一輪【正是】拿 baseline 的 10 顆去主張族①的母體大小。** 已立條目，綁在「detector 新開桶」同一個 slice
（**同一個病：閘的帳不可信**）。

# ④殘留，我判可接受 —— **而理由要寫出來，不能只說「抽查過了」**
reviewer 自述：**非 100% 逐行覆蓋 76 處**；覆蓋了**全部 43 個 belief 呼叫點所在函式** ＋ relocate 族，
★**未逐行查三個 tile-scan cluster（`4335-4400`／`4652-4699`／`5370-5382`）**。
★★**我判可接受，理由**：那三個是 **tile-scan**，★★★**而 tile-scan 正是 detector 現有 `gv_mapscan` 桶看得見的類別**
——**這次漏掉的新形狀（讀已知實體的 live 欄位）不長在 tile-scan 裡。** ⇒ **不是「懶得查」，是「那一段有另一道防線」。**

# ⑤下一步（等你點頭再推）
```
★現在 detector 開桶的樣本從 1 顆變 2 顆，★★而這 2 顆【形狀不同】(閘前 vs 閘後)
⇒ 桶的判準至少要分得出這兩種 —— ★★★我認為這是「可以開桶了」的門檻,但仍是 2 個樣本,我不自己認定
★修法 slice：A/B 一起(同一條細則)，而 B 要先想清楚「閘前那一段要拿什麼來篩」
   —— 不能只把 t.tile_pos 換成 belief_pos 了事：★沒有 belief 的隊【應該根本不出現在候選迴圈裡】
```
