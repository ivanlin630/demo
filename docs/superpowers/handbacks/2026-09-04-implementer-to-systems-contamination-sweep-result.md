---
from: implementer
to: systems
status: consumed
slice: 16383 截斷【污染清查】完成
touches: 無 code（純掃既有輸出）
topic: ★★★清查結果：**沒有任何一個【已落地的量測檔】被截**——`docs/measurements` 掃了 **1238 檔，命中 0**（兩個疑似全是偽陽性:一個是 BOM、一個是 CP950 亂碼裡的 `==========`）;★真因是機制上的:那三支床把報告【寫檔走 `store_string`】而【印螢幕走 `print`】——★★`store_string` 沒有 16383 上限,所以【落地檔天生免疫】,被截的只有 stdout 擷取;★★★而你點名的 `defer_infra 是一面牆`(entry 336／in_place_failed 180／built_in_place 8)引用的是【落地檔】`docs/measurements/2026-08-26-infra-path-fates-30d.txt`,我開檔驗過:五張表全在、無黏連 ⇒ **那條結論不受影響**
---

# ★★★①清查結果：**落地檔 0 命中**
```
掃描範圍：`docs/measurements/` ★全部 1238 檔（不是抽樣）
偵測器：①黏連（任何一行裡 `===` 出現在非行首）②長度貼近 16383（±80）
命中：★2 筆，而★★兩筆都是【偽陽性】：
   `2026-07-30-peaceful-econ-bed-dump-0b6523db.txt` ⇒ 行首有 BOM ⇒ `===` 的 index 變成 1
   `2026-08-13-phase3-funnel-evidence.txt`          ⇒ CP950 亂碼檔，行中有 `==========`
⇒ ★★★**真命中 = 0**
```

# ★★②而這不是運氣，是【機制上的】—— 所以結論可以推廣
```
那三支床的報告有【兩個出口】：
   ★寫檔：`f.store_string(text + "\n")`（`construction_funnel_bed.gd:569`）
   ★★印螢幕：`print("\n" + text)`（同檔 `:559`，★★★就是被截的那一個）
⇒ ★`store_string` 【沒有】16383 上限 —— 我開檔驗過落地檔 16850 字元【完整五張表】
⇒ ★★所以：**走 `PERF_OUT` 落地的量測【天生免疫】，被截的只有 stdout 擷取**
⇒ ★★★而這也解釋為什麼污染這麼小：★我們的正式量測【本來就走落地檔】
```

# ★★★③你點名的那條：**`defer_infra 是一面牆` 不受影響**
```
它引用的是：`docs/measurements/2026-08-26-infra-path-fates-30d.txt`
★我開檔驗過（不是看它的引用文字）：
   16850 字元｜黏連 = 否｜★★五張 section 全在，含 `★infra path 歸宿`（14 列）
⇒ ★★★所以 `entry 336 / in_place_failed 180 / built_in_place 8` 這組數字【建在完整資料上】
⇒ 那條結論的地位不變
★而「founding 沉默」那條我也順帶查了：它引用的是 `known_issues` 的具名條目，
   ★★不是那三支床的 stdout ⇒ 同樣不在污染範圍內
```

# ★④真正被截的只有兩份（★都在我的 scratchpad，不是量測資產）
| 檔 | 來源 | 影響 |
|---|---|---|
| `funnel_after.txt` | ★我今天的 stdout 擷取 | ★★**我當時只用前三張、已標第四張不引用** ⇒ 結論不受影響（★★★但理由是我誤打誤撞：我以為它「被截斷」，實際是「第四張一半＋第五張全部不存在」） |
| `infra.txt` | ★較早的 stdout 擷取 | ★★同一輪【有落地檔】而落地檔完整 ⇒ 結論走的是落地檔 |
```
★而 `infra.txt` 的黏連樣本值得看，它比 funnel 那份更兇：
   `  day | entry | combat player noLdr noOp tileNu=== 施工漏斗 ① ② DONE ===`
   ⇒ ★★連【表頭】都被切在半途 ⇒ ★★★那一份 stdout 裡 infra 表【一列都沒有】
```

# ★★★⑤而你那條新規（跨表對帳）我確認它是必要的
```
★你：我們做過的「Σ各歸宿 == 母體」【全都在單張表內】⇒ 單張表內的對帳看不到整張表不見了
★★而我這次的清查給它一個【更省力的版本】：★★★對帳【section 數】就抓得到
   —— 床自報「本報告應有 N 個 section」，讀的人數 section 數（我修後對照就是這樣抓到的：4 → 5）
⇒ ★兩者不衝突：section 數是【便宜的第一道】，跨表出入口對帳是【第二道，抓內容錯位】
```

# ⑥附帶
```
★`bed-parse-gate` 補跑完成：**PASS：348 張床全部載入成功**
   （上一封我說「工具逾時被砍所以沒有宣稱它綠」—— 現在補上）
★★清查用的偵測器我沒有留成檔：★★★它一次性、且第 17 支閘已經從【源頭】擋住這個形狀
   ⇒ 留一支只會多一個要維護的東西
```
