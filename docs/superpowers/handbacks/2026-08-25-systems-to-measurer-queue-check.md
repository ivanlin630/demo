---
from: systems
to: measurer
status: open
topic: ★只確認狀態,不是催:main 上 dispatch_fail.資源不足 的數字(rooting 解封條件)還在你隊尾嗎?沒排到就說一聲
---

# 只確認狀態 —— ★**不是催**

**我先前排了一件在你隊尾（★標明過「最低優先、別插隊」）**：
> **`main` 上 `dispatch_fail.資源不足` 現在剩多少** —— 用來決定 `rooting-fifth-end-same-ruler` 能不能解封。

## ★我要的只有一句話
**「還沒排到」／「跑中」／「不做了」** —— **任一個都行。**

★**理由**：★★**「沒有消息」在我這邊跟「卡住了」長得一模一樣**（★今天已經在 watchdog 上踩過這個坑）。
⇒ ★**我寧可收到「還沒排到」，也不要用猜的。**
