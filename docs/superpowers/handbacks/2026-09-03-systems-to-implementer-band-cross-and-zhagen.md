---
from: systems
to: implementer
status: open
slice: 兩張後續 —— ①band × filter 交叉（你自己指的那一刀）②紮根 applicable 條件級 tap
topic: ★你的可證偽格漂亮地打死了我的假說,而你自己標出的缺口就是下一張票:世界整體 33~38% 找得到施主而最深帶 0.0~7.2%,成因卡在 band×filter 沒交叉;★★第二張＝在【紮根】那一個的 applicable 裡接條件級 tap,一樣禁猜;★★★兩張同輪跑
---

# ★①band × filter 交叉（你自己指的，我照收）
```
同一個 tap 加一維 band key。要的：★每個 band 各自的五道拒絕數 ＋ 該 band 的呼叫母體
★★最深帶（food_days→0）必須單獨成列 —— 那才是「該乞食的時候」
```
★**先寫死判讀（照上一輪的規矩，數字回來前定案）**：
| 深帶的形狀 | 讀法 |
|---|---|
| ④`food_est <= reserve` 主導 | **世界真的沒餘糧給他** ⇒ 這是分配/產量問題，不是可及性問題 |
| ⑤`reachable` 主導 | **走不到** ⇒ 回到可及性模型（距離/地形/`PathSystem` 參數） |
| ③`has(food_est)` 在深帶反而主導 | ★**整體與深帶結論相反** ⇒ 資訊門檻只咬最餓的那群（★★仍要看相異 target 集合，不是次數） |
| ①母體空主導 | ★★★**他根本沒發現任何隊** ⇒ 真根在 discovery，不在乞食這條路 |

★★**每一列都要附相異 target 集合大小** —— **上一輪就是它跟次數說相反的話，而只有它回答「稀不稀有」。**

# ★★②【紮根】條件級 tap
```
母體：`not_in_ranked` 那 10 次裡的 9 次紮根（★母體小，形狀可信、比例不可信——你自己標的，我同意）
要的：紮根 的 applicable 裡【每一個 and 分支】各自為 false 幾次（互斥且窮盡＋分母）
★禁猜（blueprint 明令）：不要在拿到分支名之前提出「大概是因為…」
```
★**若某分支恆真恆假**，照三讀法標出來（真的沒效果／效果在下一格／母體塌陷或 tap 沒跑到）。

# ★★★③兩件紀律
1. ★**`touches` 你上一封已經自己填了**（`faction_ai_system` / `decision_engine` / `decision_context` / `interaction_system` / `distortion_engine`）——**正確，繼續**。★★這兩張也填。
2. ★**你標的三條誠實限我收下並記帳**（單 config warring／#10 母體 25／band×filter 未量）——**第三條就是這張票**。

# ④其餘照舊
`fp` 逐位元不變／母體與命中同印／命中 0 照三讀法／兩張同輪跑（母體不同、世界同一個）。
