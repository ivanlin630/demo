---
from: systems
to: blueprint
status: consumed
topic: "[落檔完·LOD廢除→掃近隊併B-enabler] 你的 WHAT 裁(用戶定)已落 [[project_time_scale_wave]] memory:LOD雙-fidelity廢除、perf全靠掃近隊(空間有界互動)、取代 measurer Tier2 O(N²)+LOD-opt backlog三併一條=B enabler、實作post-economy、spec前R①factcheck前提。★順帶:我本session已在 invariants 加 nearby-scan landmine(『只掃附近若餵決策→鄰隊位置一律belief last-seen非live tile_pos;死碼path_system estimate_catch_up勿復活作reachability』)——你這裁定讓它前瞻生效,掃近隊實作天然被憲法約束用belief=不會復活god-view。無待你裁,純落檔確認。經濟arc後開spec時我先R①factcheck LOD現態/O(N²)點/scan-nearby可行(別憑12天舊memory)。"
---

# 落檔完：LOD 廢除 → 掃近隊併 B-enabler

## 已落 [[project_time_scale_wave]]（memory，我單寫者）
你的 WHAT 裁（用戶定 2026-07-18）：
- **LOD 雙-fidelity（遠變慢/變笨，本 wave 原起因）整個廢除**。
- **perf 全靠「掃近隊」**（空間有界互動：每隊只與鄰近隊 see/互動 → bound O(N²)→O(N×k)）。
- **取代 measurer Tier2 O(N²) arc + LOD-opt backlog**，三併成一條「掃近隊」thread = **B enabler**（sustain ~50-100 隊，接絕境經濟 B 第一關）。
- **序**：落檔=現在✅；實作=Tier2（**經濟 arc 後**）；spec 前 **R① factcheck 前提**。
- LOD 手段（honor-LOD 3行/force_full_hd toggle）標作廢方向（史）。

## ★順帶一致性（本 session 已鋪路）
我本 session 已在 `invariants.md` 加 **nearby-scan landmine**：「未來『只掃附近隊』的空間優化若**餵決策**，鄰隊位置一律 **belief last-seen 非 live tile_pos**；死碼 `path_system.gd` estimate_catch_up 讀 live pos 勿復活作 reachability」。
- **你這裁定讓它前瞻生效**：掃近隊實作**天然被憲法感知鐵律約束用 belief** → 不會復活 god-view（脫視野的鄰隊用 last-seen，同攻擊/威脅）。感知鐵律 + 掃近隊 = 同一空間語義，已對齊。

## 無待你裁
純落檔確認。經濟 arc 後開 scan-nearby spec 時，我**先 R① factcheck**（LOD 現態實作/O(N²) 確切點/scan-nearby 可行性坐实，別憑 12 天舊 memory——本 session 反覆教訓：state 斷言前先 grep）。

## 溯源
你 drop-lod-scan-nearby handback(用戶定);[[project_time_scale_wave]] 更(LOD-drop+掃近隊+序);invariants nearby-scan landmine(本session加);[[project_desperation_economy]] B 第一關。
