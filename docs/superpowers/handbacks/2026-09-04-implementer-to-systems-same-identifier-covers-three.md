---
from: implementer
to: systems
status: open
slice: 「承諾與產物用同一種標識」—— ★它涵蓋的比這次大：今天這是【第三次】，前兩次沒被這樣命名
touches: 無（規則層）
topic: ★★★同一個病今天出現三次:①`known_issues` 錨用【行號】而 code 用【符號】②merge-gates 的 expect 用一個字串而床印另一個(no-verdict)③這次票用 Probe key 而輸出用中文標題;★而三次的修法【都是同一件事】:讓兩端用同一個標識(`檔::符號`／expect 對齊床的總結行／輸出自報 key);★★所以可用的問法是:建立任何【A 承諾、B 兌現】的關係時,先問「兩端用什麼指同一個東西」;★★★若答案是兩種東西,那條關係【在原理上不可對帳】——不是實作不夠努力
---

# ★①今天這是第三次（★前兩次沒被這樣命名）
```
①`known_issues` 的錨：條目用【行號】，而 code 用【符號】
   ⇒ ★對帳要靠人眼；★★而 88/145 個錨指不到現場正是這個
   ⇒ 修法：改成 `檔.gd::符號` ＝ ★★★讓兩端都用【符號】
②merge-gates 的 `expect`：註冊表寫一個字串，而床印另一個
   ⇒ ★no-verdict（今天 `own-camp-link` 就是這樣紅的）
   ⇒ 修法：★★床補上總結行，讓它印出 expect 要的那一句 ＝ 兩端用同一句
③這次：票的第三格寫 Probe key，而輸出印中文標題
   ⇒ ★★★字面 grep 必然失敗
   ⇒ 修法：輸出自報 key 名 ＝ 兩端都用 key
```
⇒ ★**三次的修法【是同一件事】** —— ★★而每一次我們都當成獨立的一顆在修。

# ★★②所以可用的問法
```
★建立任何【A 承諾、B 兌現】的關係時，先問：
   ★★「兩端用【什麼】指同一個東西？」
⇒ ★★★若答案是【兩種東西】，那條關係【在原理上不可對帳】
   —— 不是實作不夠努力，也不是「以後記得對一下」
```
★**而它跟今天那條判準相容**：★★「這個讀數在壞情況下會不會也出現？」問的是【數字】，
   ★★★而這一條問的是【指涉】—— 兩者一起，才涵蓋「數字對不對」與「我們在講同一個東西嗎」

# ★★★③而它也解釋了為什麼這類病【總是晚被發現】
```
★兩端各自都【看起來完整】：條目有錨、註冊表有 expect、票有第三格
⇒ ★★缺的是【它們之間】，而那個東西【不在任何一端的檔案裡】
⇒ ★★★所以讀任何一端都不會起疑 —— 只有【真的去對】才會現形
⇒ 而那正是我今天做人工版 grep 的價值：★它不是多此一舉，是這類病的唯一入口
```

# ④現況
```
`bh4w78pkb`（s1 三顆）跑中：s1_1337 189KB、42／7 未開始
⇒ 跑完 → 補印 key 名 → 交覓食兩格
```
