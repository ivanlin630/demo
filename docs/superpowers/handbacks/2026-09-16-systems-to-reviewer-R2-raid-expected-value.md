---
from: systems
to: reviewer
status: open
slice: 掠奪走期望價值 ｜ **R² 設計審**
topic: ★spec：`docs/superpowers/specs/2026-09-16-raid-expected-value-HOW.md`｜★★★**我在 §0 撤回了自己呈報給 blueprint 的一句話**：我寫「掠奪不讀贏率」，**而 `terms.gd:315` 的 `cap` 與 `decision_context.gd:975` 的 `attack_win_odds` 是逐字相同的式子** ⇒ **掠奪缺的是兩樣不是三樣**｜★**最想被打的是 §2「低風險」那格**：我主張風險差**不必調係數**，因為**掠奪打最弱、攻擊打最富＝目標不同，差別自己就出來** —— 若你認為這撐不住，現在說｜★★**另一個弱點**：驗收格3 要求兩者 util「同量級、比值落在 `RAID_TAKE_FRACTION` 附近」，**而那個常數我在 §5 說不准手填** ⇒ **我用一個還沒有值的東西當驗收基準**
---

# ★請特別看這三處
```
\u2460**§2 低風險 ＝ 目標不同，不是係數不同**
   ★我的理由：掠奪打 `_find_weakest_prey`、攻擊打 `prosperity_prey` ⇒ **同一條式子餵不同輸入**。
   ★★**若這不成立**（例如兩者實際上常常打到同一個目標），**整個 §2 就垮了一格**，而我沒有量過那件事。
\u2461**§3 格3 的循環依賴**：驗收基準是 `RAID_TAKE_FRACTION`，而 §5 說那個值要從世界推。
   ★**所以這格在拿到那個數之前【不可判】** —— 我認為那是誠實的，**但你可能認為那是把驗收推遲。**
\u2462**§0 的撤回**：`cap ≡ attack_win_odds` 逐字相同 ⇒ ★**我呈報給 blueprint 的診斷有一項是錯的**。
   ★★我已在 spec 裡明寫。**若你認為還有別的地方被那個錯誤污染，請指出來** ——
   **我自己查不到它，因為我是帶著那個錯誤讀 code 的。**
```
★**另**：`terms.gd:536` 有同一條 `cap` 的**第三份**。本票只動掠奪那一處（三處變兩處），**:536 不動**。
