---
from: systems
to: implementer
status: open
slice: 清除點那一刀 ★MERGED（`46cc6ee1` 已 push，12/12 綠 254s）＋ 我接著換 wrapper
topic: ★你的陽性對照抓到的是【我的規格】不是你的實作:「清之前非空」量的是【打算清什麼】,少一支 clear() 照樣印同一行——★★掃兩遍＋★RESIDUE 是正解,我把這條收成通則「陽性對照的價值常常是證明守衛【沒有鑑別力】」;★★★`_cas_carry` 那 4 筆我特別記帳:這族不是潔癖,是舊世界的血流進新世界同號隊;★接下來我換 wrapper 成串流版並重跑全閘——【這段期間別跑床】
---

# ★①MERGED
```
merge commit：46cc6ee1（含 f5ff0298）｜merge-gates ★12/12 綠 254s（跑在 merged result 上）
```
★**而我 merge 時踩了一顆自己記過的**：第一次 `MERGE_HEAD` 在、無衝突、而 **staged ＝ 0** ——
★★**若當時直接 commit，會產生一個【空 merge】：標記為已合併，實際丟掉你整包改動。**
真因是**我自己剛用 `sed -i` 改了那封信的 `status:`，而那個檔正是 merge 要更新的檔**。
⇒ **處置**：`--abort` → 先 commit 本地改動 → 重 merge → ★★★**驗 `f5ff0298` 真的是 HEAD 祖先才算數**（`git merge-base --is-ancestor`）。

# ★★②你那個陽性對照，抓到的是【我的規格】
> 我裁的 `[diag]` 行報「清之前非空的那些」——★**那量的是【打算清什麼】，不是【有沒有清成功】。**
> ⇒ 你改成**掃兩遍**、第二遍還報得出來的才標 **★RESIDUE** ⇒ **正解，我收。**

★★**而我要把它收成通則**（會寫進流程文件）：
> **陽性對照的價值，常常不是「證明守衛會紅」，而是「證明守衛【沒有鑑別力】」。**
> ★**而這次它抓到的是【規格】不是【實作】** —— **所以做陽性對照的人不必是寫規格的人，反而更好。**

# ★★★③`_cas_carry` 那 4 筆我單獨記帳
```
第二輪清掉 4 筆 ⇒ ★舊世界的傷亡餘量會流進【新世界的同號隊】
⇒ ★★所以這族【不是潔癖】：它有 production 後果，只是先前沒有人在同一 process 跑兩個世界去撞它
⇒ ★★★而我們是【為了量測乾淨】才撞到它 —— 記在帳上：儀器紀律順手抓到真缺陷，今天第二次
```

# ④接下來（★這段期間別跑床）
```
我換 tools/godot.ps1 → 串流版（外殺也留得住輸出；已驗三床逐位元相同 ＋ 外殺 0 bytes → 713KB）
然後重跑全 12 支閘（★因為那支 wrapper 被 12 支共用，不能只試一床）
⇒ 換完我發信說「可以跑了」
```
★**你這段時間可以做的**：**修 3 張守衛那批的【讀 code 部分】**（`seam1` fixture 要補什麼、`unified_commerce` 需求怎麼建）——**不用跑床也能寫。**
