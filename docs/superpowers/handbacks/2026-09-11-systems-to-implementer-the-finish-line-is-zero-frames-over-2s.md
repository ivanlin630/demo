---
from: systems
to: implementer
status: open
slice: 單幀 ｜ ★終線（blueprint 給的，我轉為可量的判準）
topic: ★★★**終線 ＝ 「單幀 > 2 秒」的次數 ＝ 0**（用戶包絡）—— ★而它不是「平均變好」也不是「倍數改善」，**是一個計數歸零**｜★★所以每一份交件都要多印**一個數字**：**這一窗裡有幾格 > 2 秒**｜★而現況是 **max 11.86 s** ⇒ **離終線還很遠**，而那正是切開那個桶要回答的
---

# ① 終線（★把它變成每一輪都印的那一格）

```
★**> 2 秒的幀數 ＝ 0** ⇒ ★★而它要**逐輪印**，不是最後才量：
   `這一窗的 tick 數 ／ 其中 > 2s 的有幾格 ／ max ／ 中位`
⇒ ★★★而**「> 2s 的格數」是主詞** —— max 只是它的上界，**平均完全不算數**。
⇒ 現況（ON 趟、90 天）：**max 11.86 s** ⇒ ★那格顯然不是 0，**而我們還不知道它是幾**
  ⇒ **這一輪就要把它印出來**（★它是一個除法都不用的計數）。
```

# ② ★而它改變了「切開 `loop3.misc`」這張的讀法

```
★切開之後，**不要**只看「誰最大」——★★要看**誰在那些 > 2s 的格子裡最大**
  ⇒ ★★★**母體換成【超標的那些幀】**，而不是全部 spike 樣本
  （★而這正是今天一直在做的同一件事：**先把母體講清楚，再談誰第一**）。
⇒ 所以請印**兩張**：
   ①全 spike 樣本的子相位排序（★沿用現有形狀）
   ②★★**只在 > 2s 那些幀上**的子相位排序 —— ★★★**終線只跟第二張有關**
```

# ③ 不變的

```
★禁提名嫌疑犯（切段計數，讓數字點名）／單價量不准除／母體地板／根守恆式要綠
★★而 Probe 開關對照仍在量測員手上（OFF 趟被 contention 污染、已請重跑）
  ⇒ ★★★你的數字**仍是 Probe-ON 的**，交件請帶這句。
```
