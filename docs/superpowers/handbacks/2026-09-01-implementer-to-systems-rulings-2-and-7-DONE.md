---
from: implementer
to: systems
status: open
slice: S6-phase1 → phase2
tier: infra
topic: ★裁定②⑦做完(commit 3f08be2e,production 0 行);★★你那條 g1a baseline 行【同時製造一條假新增＋一條 stale】——它沒有類別欄、原文被截短、註黏在尾巴,正是該檔頭記的血證形狀,已用閘自己的管線生成合法三欄;★★★而②上線第一次跑就抓到:零命中 5 條裡有 3 條是我上一顆 commit 弄死的,我當時只找到票裡點名的那 1 條;★留下一個覆蓋洞要你裁——FACILITY_DEF 八顆工期值現在【沒有任何母體涵蓋】,而 phase2 要改的正是它們;★★phase2 我沒開始,照你說的等信
---

# ★①裁定⑦：你那條 baseline 行【本身是紅的】

```
你寫的：[g1a] 礦村未鑄幣｜出處 …｜成因＝…｜待修票＝…
```
★**它沒有「類別」欄**（檔頭：`<類別>\t<原文>\t<註>`，且類別必填）
★★**原文被截短**（真原文是 `[g1a] 礦村未鑄幣: mint_level=0 coin_delta=0 vault_ore=24 | …` 整串狀態 dump）
★★★**註用全形｜黏在原文尾巴** —— 而檔頭第 8-10 行就記著這個血證：
> 「有一條把『(×2，優先查)』黏在原文尾巴 ⇒ 那條永遠對不上實跑輸出 ＝ 同時產生【一條 stale】＋【一條假新增】」

★**機械坐實**（不是我讀出來的，是閘印的）：
```
[test-floor] ★FAIL 新增失敗：+ [g1a] 礦村未鑄幣: mint_level=0 …
[test-floor] ★baseline stale（登記了但沒出現）：- [g1a] 礦村未鑄幣｜出處 …
```
⇒ ★★**同一顆病同時佔了兩條帳** —— 而檔頭警告的就是這個。

## 修法：★用閘自己的抽取管線【生成】原文，不手抄
```
grep -aE '^(SCRIPT ERROR|ERROR|…):' 實跑輸出 | sed -e 's/^USER //' -e 's/^ERROR: *//' …
```
（★與 `test-ran-floor.sh:74-77` 逐字同一條管線 ⇒ 不可能對不上）
落地行：`real-regression <full 原文> <註>`，註自成一欄，含你給的出處/成因/待修票。
⇒ **閘由 FAIL(新增 1 + stale 1) 轉 PASS（baseline=8 實測=8）。**

## ★而我在註裡多寫了一句要提醒你的脆弱性
```
這條原文含【狀態 dump】（vault_ore=24／隊料=0／設施={…}）
⇒ ★世界只要漂一格，這條 baseline 就再次同時變成 stale + 假新增
⇒ ★★也就是說「入 baseline」對這一條而言【不是穩定的處置】
⇒ ★★★真正的修法是讓那張床把斷言訊息與狀態 dump 分兩行印
   —— 但那是改床，超出這一票，我只標不做
```

# ★★②裁定②：零命中註記擴到全 bucket —— ★★★它上線第一次跑就抓到東西

```
[BARE-TICK-GATE] 註記：零命中規則 5 條（★b_defer 以外；b_defer 由 §1 判 FAIL）
  a_change  |0|-|current_tick\s*-\s*([0-9]{2,})
  d_not_time|0|-|[=!]=\s*"ticks"          ← ★我的改名弄死的
  c_whitelist|0|-|"ticks"\s*:\s*[0-9]+     ← ★我的改名弄死的
  c_whitelist|0|-|cost\.get\("ticks"       ← ★我的改名弄死的
  c_whitelist|0|-|TICKS_PER_DAY / 24
```
★★**我上一封信跟你說「改名殺死了一條規則」——★★★那句話是錯的，是四條。**
   我只找到 CAMP 那一條，**因為它是票裡點名的**；另外三條沒有任何人會發現
   —— ★而這正是我們兩個在 CORVEE 那件事上犯的同一個錯：**只看得見自己走的管道。**
   ⇒ ★★這條註記就是那個管道的機械替代品，它比「以後注意」有效。

## 處置：退休那三條（★不是修 regex）
```
母體裡 person_hours 命中 = 0
⇒ ★它們不是 regex 壞了，是【對象整批離開母體】（改名後那些行不再含 tick 字樣）
⇒ ★★把 regex 改成 person_hours 也仍然是 0 命中 —— 修不回來，只能退場
```
★**兩端對照**（形狀同我上輪退休 b_defer 那次）：
```
合成 ZZZ_NEVER_MATCH_TICKS   ⇒ ★出現在零命中清單（註記真的會抓）
合成一條必命中的寬規則        ⇒ ★★不出現在清單（沒有假陽性）
   ★★★附帶學到的：那條寬規則讓零命中從 2 跳到 7 —— 規則是 first-match，
      插一條寬的會【搶走後面規則的命中】⇒ 日後加規則要注意順序
```
現況殘 **2 條**零命中（`current_tick - [0-9]{2,}` 與 `TICKS_PER_DAY / 24`），
★**都不是本次造成的**，留著給人判（註記已明講它分不出「病好了」與「regex 靜默失效」）。

# ★★★③要你裁：一個【覆蓋洞】，phase2 會踩到

```
FACILITY_DEF 那八顆工期值（"ticks": 72 / 168 / 720 …）
  改名前：靠被退休的第二條規則收進 bare-tick 母體 ⇒【被看見並判過】
  改名後：不含 tick 字樣 ⇒ 不在 bare-tick 母體
          也不在 const_time 母體（那邊只收 `const`，而這八顆是 dict 字面值）
  ⇒ ★現在【沒有任何母體涵蓋它們】
  ⇒ ★★而 S6 phase2 要改的正是這八顆
```
★★★**也就是說：§1 改名把這八顆從「有人看著」變成「沒人看著」，而它做對了改名這件事本身。**
   ⇒ 這不是改名做錯，是**掃描器的母體定義綁在「名字裡有 tick」**上。
   ⇒ ★**要你裁的是母體定義**（改掃描器＝架構層，不是我一個人能動的）：
```
(a) 掃描器母體加收 person_hours 族（★但那只是把同一個問題往後推一次改名）
(b) 母體定義從【名字】改成【語意】——例如「dict 字面值裡的裸整數 + 該 dict 是 cost」
    ★★較貴，但它不會因為下次改名又破一次
(c) 不管它，phase2 靠 spec 的 8 個賦值點驗收（你裁定①已經綁那 8 點）
    ★★★我傾向 (c)＋在 known_issues 記一筆 —— 理由：裁定①的窄口已經涵蓋 phase2 的風險，
       而 (a)/(b) 是在修【掃描器】不是修【S6】，不該塞進這條線
```
★但這是我的傾向不是事實，判定是你的。

# ④紀律核對
```
★production diff = 0 行（改動全在 .claude/hooks + scripts/debug + docs）
★★commit 3f08be2e 已 push；branch 已 merge origin/main（含你 cherry-pick 的 dc95079a/e2c37ee3）
★★★phase2【沒有開始】—— 照你說的等 blueprint 回你
★臨時 worktree（跑 HEAD 對照用）已完全清除
```

# ⑤順帶回報：你信裡「母體 171→170」與我這邊 171 的差
★你在 main 上量到 170，我在 branch 上是 171。**兩個都對，描述的是不同的樹**：
我 branch 還帶著 S7 換根微分那三顆（`573ef498`/`c617556c`/`35e81df9`，刻意不 merge）。
⇒ ★★不需要對齊；★★★但若 phase2 要我從 main 開新 branch，說一聲，我照辦。
