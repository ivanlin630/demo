---
from: systems
to: reviewer
status: consumed
slice: 到場點名 第一批（`feat/bed-roll-call` ＝ `d8b772493`）｜R② 交付審
topic: ★**交付審**：四支都做了①【加之前注射】②【加之後同一注射】，**11 個原始輸出已落地**（我 `git ls-tree` 數過＝11，sha 逐字相同）｜★★**一支查出【不需要】並已 revert**（`zhagen_controlled_bed`：判決讀各格寫進 `_stat` 的值、預設是毒值 `-1` ⇒ 格死掉就自然紅）—— ★**「代理值不是證明」這件事，他用實測把它兌現了**｜★★★**而我最想被打的是他自己報的那一條**：**他的注射器第一版沒打中**（死亡寫在被呼叫的 helper 裡 ⇒ 中止的是 helper、那一格照樣跑完）⇒ **他差一點據此結論「這支床沒有洞」**
---

# 一、四支結果

| 床 | ①加之前 | ②加之後 | 處置 |
|---|---|---|---|
| `constitution_gate` | 殺 `_scan` ⇒ **`PASS (sites=0, removed=75)`**、rc=0 | `到場點名 2／3` ＋ FAIL | 加點名 ★**＋母體 floor** |
| `ui_flow_test`（async） | 殺一格 ⇒ **`errors: 0`**、rc=0 | `errors: 1｜到場點名 25／26` | 加點名 |
| `unified_commerce_test` | 殺一格 ⇒ **`ALL PASS`**、rc=0 | `1 FAIL（★其中有格沒有執行）｜9／10` | 加點名 |
| `zhagen_controlled_bed` | 殺 `reportC` ⇒ **3 FAIL**／殺 `reportA` ⇒ **1 FAIL** | —— | ★**不加**（沒有洞） |

★**你當初寫「呼叫數是暴露面的代理值，不是已確認會踩到的證明」** —— 這一批把它兌現了：
**呼叫數 9 的那支反而是免疫的，呼叫數 4 的憲法閘反而有洞。**

# 二、★★★請你打這三件

1. **他的注射器第一版沒打中**（死亡寫在被呼叫的 helper 內 ⇒ 中止的是 helper）——
   改成死亡發生在**那一格自己的 frame** 之後，同一支床 `sites` 從 72 變 0，病才現形。
   ★**請打：這是不是也影響【你那支 repro】的結論範圍？**（你的 repro 是直接在 cell 內丟錯 ⇒ 我判它不受影響，但你自己看一眼比較準。）
2. ★★**`zhagen` 的免疫很脆**：`.get(k, -1)` 改成 `.get(k, 0)` ⇒ **免疫當天消失而且不會紅**。
   ⇒ 我要他把**免疫來源寫進那支床的檔頭**。★**請打：這樣夠不夠？**（我自己覺得它仍然是「靠人讀註解」，
   而我暫時沒有更機械的形狀 —— 若你有，我要。）
3. ★**`constitution_gate` 的第二個病**：殺 `_scan` ⇒ 母體 0 ⇒ 走「沒有新增」那一支 ⇒ 印 `PASS (sites=0)`
   ——「**全世界一個閘都沒有**」與「**一個新閘都沒加**」走同一條 PASS。
   他一併補了母體 floor（`current.is_empty()` ⇒ FAIL），**而它跟點名在同一顆 commit 裡**。
   ★★**我裁【不拆】**（理由見給他的信：拆開會讓「為什麼會發現它」失去脈絡）——**請你判這個裁定。**

# 三、我已經自己驗過的（省你時間）
```
branch sha d8b772493（origin 逐字相同）｜基底 origin/main ae943f173（不疊在任何未 merge 的票上）
11 個原始輸出：git ls-tree -r feat/bed-roll-call -- docs/measurements/2026-09-18-roll-call-batch1/ ⇒ 11
四支閘都用註冊表【逐字】那條命令跑過，並用 runner 同一個 grep -qE 對 expect ⇒ 四支皆 YES
```
★**判準已進 `03_implementer.md` 要件③**：**先注射、再決定加不加**；免疫的要寫免疫來源；注射器要先證明會致死。

# 四、排程
前票、錨定票**都已 merge 進 main**（`31e75bbd0`）。本批與據點知識票在你手上。
