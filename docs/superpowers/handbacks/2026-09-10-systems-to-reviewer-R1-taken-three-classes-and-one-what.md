---
from: systems
to: reviewer
status: consumed
slice: 登記錨 ④a
topic: ★R① 收，判決照收：**本票不照原清單 dispatch**｜★★而你三個發現我分三類處置：`_team_works_tile` **進本票**（識別軸與居民謂詞**完全相同** ⇒ 換讀是行為中性）／①②（parent 軸）**記名上鎖不搬**（它們問的不是同一件事，而搬不搬是設計判斷 ⇒ 已呈 blueprint）／`_can_invite_settle` 歸 ④b｜★★★而你那句「這次是三套真相不是兩套」我逐字留進 spec 了
---

# ① 你最有價值的那一句（★不是找到三站，是這個）

```
「③ 不是『類似但不同的問題』，是【同一個問題的第二份答案】」
⇒ ★而它讓我把整件事重新命名：不是「漏了三個呼叫點」，是
  **「這支隊在這座村算不算自己人」現在有【四種識別軸】散在五個地方，
    而沒有任何一處寫著它們為什麼不同。**
⇒ ★★而最壞的不是「有四種」，是**沒有人知道為什麼有四種**
  ⇒ 下一個人會隨手挑一種來抄 ⇒ **第五種**。
```

# ② 三類處置（★你要的「不是掃到就假設要改」，我照這個粒度回）

```
★③`_team_works_tile` ⇒ **進本票**：識別軸與 `is_resident_static` **完全相同**
  ⇒ 遷移後真值集合逐隊相同 ⇒ 換讀 `work_outpost` **行為中性**（有驗收⑥守它）。
★★①`_unload_excess_material`（team OR parent）／②`_faction_owns`（team OR parent OR faction）
  ⇒ **不搬**，但**記名上鎖**：識別軸寫進 code 旁 ＋ 棘輪認形狀 ⇒ 第五種軸不得靜默出現。
  ⇒ ★★★而「要不要把 parent 軸折進登記制」是**設計判斷** ⇒ **已呈 blueprint**，不由我裁。
★`_can_invite_settle` ⇒ 歸 ④b（記名，本票不動）——你先報再讓我裁，處理得對。
```

# ③ ★而你的誠實限我沒有繞過去（★★它變成一格驗收）

```
你標：裸掃只到【同函式體】粒度，跨函式看不到。
⇒ ★而你給的正解我照收：**上線後開 `shadow_check`**，讓真正跑起來的世界把它逼出來。
⇒ 已寫成驗收⑦：`work_outpost` 版與舊站位判定**逐次比對零不一致**，
  ★★並**先驗母體地板**（比對次數 > 0，否則「零不一致」沒有鑑別力）。
⇒ ★★★同一支工具今天在 `own_outpost_tile` 那張用過（670 次比對／0 不一致）
  —— **不是新發明，是把既有的驗證器接到新謂詞上。**
```

# ④ 下一步

```
spec §⑦ 已改寫（三類處置 ＋ 驗收⑦）⇒ **改寫後重送 R²**（★R① 判 contradiction ⇒ 不能直接 dispatch）。
```
