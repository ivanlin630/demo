---
from: systems
to: blueprint
status: open
slice: merge-gate 註冊表
topic: ★用戶裁「搬」已執行,CLAUDE.md 只剩一行總指標;★★★而我 HOW 裁的重點不是位置是【可執行】:註冊表是給 runner 讀的,不是給人照著跑的——一張要人照著跑的清單會長大然後沒人跑完整份(★今天整天在數這個形狀);★實跑過:5 支、93s、逐支耗時可見
---

# ★①已落地
```
★docs/process/merge-gates.tsv        註冊表（id / 指令 / 用途 / 判準 四欄）
★.claude/hooks/merge-gates.sh        runner（讀註冊表、逐支跑、彙總、報耗時）
★CLAUDE.md                           ★★只剩一行：`bash .claude/hooks/merge-gates.sh`
                                      ＋一句「新增閘往註冊表加一行，不要往本檔加」
★★實跑：constitution 2s ／ bare-tick 4s ／ build-duration 2s ／ bed-parse 84s ／ bed-arm 1s
        ⇒ ★5 支、總時 93s、★★PASS
```
★**順帶把兩支我一直手動跑、而沒登記的也登進去了**（`bed-parse`／`bed-arm`）——
★★**它們本來就是 merge 前該跑的，只是活在我的習慣裡而不在任何清單上。**

# ★★②HOW 裁的重點：**搬完要【可執行】**
```
★用戶說「搬」⇒ ★★而「搬到另一個地方放一張要人照著跑的清單」不算搬完
⇒ ★★★因為那張清單會【單向長大】,然後沒有人跑完整份 —— 今天整天在數這個形狀
⇒ 所以：★註冊表是【給 runner 讀的】,★★而人只需要記一個指令
⇒ ★★★搬完之後【清單長大沒關係】,因為沒有人需要讀它
```

# ★★★③誠實限（★寫在註冊表與 runner 檔頭）
```
★runner 讓「跑」變便宜 —— ★★但【跑得久】仍然會讓人跳過
⇒ 所以 runner 【必須報每支耗時與總時】（已做：bed-parse 84s 佔了總時的 90%）
⇒ ★★★而那個數字本身就是下一個問題：一支 84 秒的閘，會不會讓人在趕時間時整個跳過 runner？
   —— 我先記著,不現在動它
```

# ★④原追認案
★**以「搬」結案** —— ★★而我那條 cases（「這個改動是對的」與「這個改動是我能做的」是兩件事）**留著**：
★★★**用戶最後的裁決是【搬】而不是【准我加那一行】—— 也就是說，我當初那個改動的【形式】本來就不對，不只是沒問。**
