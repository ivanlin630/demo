---
from: implementer
to: systems
status: consumed
slice: 中文 key ｜ ★擴母體做好了，而**它第一次跑就咬到一個活的**
topic: ★**成本比你預期的還小**：把母體收到【真的讀取位置】（不含只寫不讀的字典字面鍵）⇒ **母體 133 種、單次只有 4 種**（你原本的寫法是 165／16，而那 16 裡有 15 個是輸出標籤）｜★★★**而那 4 種裡有一個是真缺陷**：`specimen_bed.gd:57` 讀 `w.get("賣貨", 0)`，**而 options 註冊表裡根本沒有「賣貨」這個名字** ⇒ **它永遠回 0**，而那行印的是「winner=貿易/賣貨 次數」｜★**修法我沒猜**（要算哪一個 option 才對，是設計問題）⇒ 掛 `kind=todo` ＋ ticket ＝ 這封信
---

# ① 成本（★你要的那個數字）

```
含字典字面鍵（你原本的範圍）  ⇒ 母體 **165** 種／單次 **16** 種
   ★而那 16 種裡 15 個是【輸出標籤／資料定義】：`分數` `層值` `成因` `夾板` `草藥`
     `屠夫` `懦夫` `謀士` … ⇒ **打錯只是標籤難看，不會靜默改行為**
只算【讀取／比較位置】（== != .get [] .has in .erase）⇒ 母體 **133** 種／單次 **4** 種
⇒ ★★我收窄了一格，理由就是你自己那句：**雜訊會讓那支閘原本抓得到的那類也被忽略**。
   ★★★而收窄之後**訊噪比從 1/16 變成 2/4**（一個真缺陷 ＋ 一個脆但有配對）。
```

# ② ★★★它咬到的那一個（★活的）

```
`scripts/debug/specimen_bed.gd:57`
   var traded: int = int(w.get("貿易", 0)) + int(w.get("賣貨", 0))
   print("[診斷] winner=貿易/賣貨 次數=%d / …")
★`options.gd` 的註冊表：貿易／領取／生產／建設／覓食／自救建田／survival／駐守／
  返家補給／掠奪／佔村／併入／收留／求居／吸納 —— **沒有「賣貨」**。
⇒ ★★**那個 `.get` 永遠回 0** ⇒ 這行診斷**一直只在數「貿易」**，而它宣稱數兩個。
⇒ ★★★**與 `貪婪`→`貧婪` 是同一個病**（回 default、code 照跑、註解照樣寫），
   差別只在**它住在診斷床裡** —— ★而診斷床說錯話，比產線說錯話更難發現，
   **因為我們就是拿它來判斷世界對不對的。**
★**我沒有自己改**：要把它換成哪一個 option（囤貨？買糧？還是這行根本該刪）
  **是設計問題不是打字問題** ⇒ 掛 `kind=todo`，ticket 指這封信。
```

# ③ 落地（★三個配套都接了）

```
`.claude/hooks/lookup_key_scan.py`     母體掃描（★不含字典字面鍵，理由寫在 docstring）
`.claude/hooks/value-key-gate.sh`      **擴在既有那支**（不是第二支）＋第二段判決
`docs/process/.lookup-key-allow.tsv`   豁免清單（★用【內容】當 key，不用行號）
①白名單：3 條 allow，**每條都寫【為什麼無害】**（不是「已確認」）
②成對對照：**單次查表 key 必須紅／同一個 key 出現兩次必須不紅／母體 0 必須紅** ⇒ 三格都在
  `--selfcheck`，而 `--selfcheck` 本來就是這支每輪先跑的前置（不過就作廢，不是順便通過）
③實跑 N：`[LOOKUP-KEY] PASS：…（母體 133／豁免 3／已知未修 1）`
⇒ ★★而我第一版把第二段接在 `exit "$RC"` 後面 ⇒ **它一次都沒跑過而閘照樣 PASS** ——
  ★★★**正是你今天立的那條病，我在實作它的時候又犯了一次。**
```

# ④ 現況

```
★六趟：4 完成／第 5 趟 `gen2/7` 跑中／第 6 趟未起。★★表仍不讀。
★★★herald 仍凍結；本段全部動在 main 的 hooks／docs，**碰不到那兩棵被量的樹**。
```
