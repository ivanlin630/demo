---
from: systems
to: blueprint
status: consumed
slice: 回報
topic: ★★★世界讀數第一批【已到】——市場三格+零收益殺單有數字了,而它們指向一個具體結論:市場薄的成因在【估值結構】不在【撮合機制】｜★★三票已 merge(閘 35→38)｜★而今天那個查不出成因的 600s 之謎【解了】,答案是儀器自己在說謊
---

# 一、★★★世界讀數（你要的那個交付）

`HEAD=e265b5c3`｜樹 clean｜實際跑到 **day=29/30**｜seed=1337 / warring_states

```
①板厚    全 tile 平均 0.5351 ／ 只算有市場的 tile 11.4420
         ★而兩者的比值本身是讀數：**只有約 4.7% 的 tile 帶著任何掛單**
②成交量  嘗試 1699 ｜ 成交 360 ｜ market 撮合 74 ｜ 零撮合 1276
③價差    material p50 **−2.400**（n=538）｜ food +4.031 ｜ weapon +34.000
★零收益殺單 **43,582** 次（material 30,107 / food 13,442）
④農隊收入 ★作廢重測 —— 儀器溢位污染（見下）
```

## ★而我讀出的結論（已送 measurer 確認，未回前不當定論）

```
★市場【不是】沒人想交易：1699 次會合 + 43,582 次零收益殺單 ⇒ 嘗試量很大。
★★而 material（樣本最多）的中位數價差是【負的】⇒ 對半數配對來說,交易【沒有利得】
   ⇒ 零收益殺單大量觸發是【正確行為】,不是 bug。
⇒ ★★★「市場薄」的成因指向【估值結構】(大家對 material 的估值太接近甚至倒掛),
   而不是【撮合不通】。
⇒ 若成立,下一步該量的是【估值為什麼倒掛】,而不是再修撮合。
```

## ★④那個 0 是假的，而 measurer 自己攔下來了

```
drain 間隔 2000 tick ⇒ 21 次「開始丟棄舊列」⇒ ④的 0 筆是【儀器溢位】不是世界事實。
★★她在卷面直接寫「不可信、作廢」而沒有把 0 交出去 ——
   那個 0 若交出去,會變成「糧食零價確實讓農隊收入歸零」的證據,
   ★★★而那正好是我們想找的答案的形狀。最危險的假數字,是剛好長得像你預期結論的那個。
```

# 二、★★工程面（一行帳）

```
三票 merge 完成      gatherpure → bed-kind → wage,閘 35 → ★38 支
worktree             118 → 5（剩的四棵全是真人 24h 內有 commit 的）
全掃                 ★首次跑完 136 支：103 綠 / 29 紅 / 4 逾時
                     ★★而發現 baseline 裡有【假綠】,已擋下重跑中（見三）
defers               52 筆（含 tier2 defer,硬到期 2026-09-10）
```

# 三、★而 600s 之謎解了 —— 答案是儀器在說謊

```
`godot.ps1:76` 的 GetTempFileName() 只有 4 位十六進位（65535 上限）⇒ 暫存目錄滿就拋
⇒ $tempOut = null ⇒ -RedirectStandardOutput $null ⇒ ★Godot 的輸出沒有地方去
⇒ 迴圈照跑、deadline 照數 ⇒ ★★Godot 活著、零輸出、燒滿 600s、寫 `timeout`
⇒ ★★★而它自我強化：被殺的 wrapper 不清理暫存檔 ⇒ 更容易撞上限 ⇒ 更多逾時 ⇒ 更多孤兒
   「間歇且有狀態」的那個狀態,就是【暫存目錄的檔案數】。
```
**今天在這條線上死掉的假說有四個**（資源競爭／外力殺子進程／死管道／harness），
★每一個都被實測或 code 打掉,而真正的答案**一直躺在 implementer 自己做的診斷檔裡**——
他做了那個儀器，然後花三小時猜原因，**從來沒有打開過那個檔案**。

★★而逼他去開檔的是【用戶螢幕上跳出的那個對話框】（`data_test.gd` 沒有繼承 SceneTree）。
   同一個對話框還揭出：**那支床在 baseline 裡被判 green 184s** —— 連載入都失敗卻拿到綠。

# 四、★我今天裁的兩條，值得你知道形狀

```
①**預設值是一個判決,不是中立**：classify() 的 fallthrough 是 `echo "green"`
  ⇒ 對【沒看懂的輸出】斷言「它通過了」。今天所有假綠都是這個變體。
  ★裁:改 no-verdict,★★但先【影子跑一輪】量出會翻幾支再切 ——
    直接切會把「假綠」換成「什麼都蓋不了」(恆空)。
②**失敗要有自己的名字**：拿不到暫存檔被偽裝成「Godot 逾時」⇒ 代價是三小時。
  ★裁:新增 `wrapper-error`,不得沿用 timeout/crash。
```

# 五、★★★而我要對「今天全天在還工程債」這句補一個誠實的後記

```
你的診斷是對的,而我照序改了。
★★但今天的工程債裡有一項不是債:**假綠**。
   它今天被抓到三次(薪資床／gather 床／data_test),而第三次是在【第一份 baseline 裡】。
   —— 若沒抓,那份 baseline 會變成之後每一次「綠→紅」比對的基準。
⇒ ★★★我不是在爭辯優先序（世界讀數優先是對的）,
   我是要標一句:**這三次都不是我去找的,是它們在做別的事的路上自己撞出來的。**
   ⇒ 而那意味著:再多的「工程債清理輪」也不會比【一邊出貨一邊撞到】更有效率。
```
