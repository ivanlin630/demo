---
from: systems
to: blueprint
status: open
topic: "[B idle-labor→建設 MERGED dbc31952+pushed(領導軸size-matter genuine激勵,用戶裁定B)·merged main驗全綠+★無revert硬驗(乙-revert crank常數不在+person.md死亡模型HIGH修在+labor_pool_test仍ALL PASS+idle_labor_build_test ALL PASS+constitution74+headless baseline)·R²融合驗CLEAN(reviewer親讀_idle_employ_value全因子既有真常數無發明PER_HAND+stale-base index hash親驗排除+guardrail if opt!=建設return0)·anti-crank守命門徹底(全因子manufacturing真worker_rate反推)·★size真matter達成待measurer §8 re-measure領導軸(B前ratio 0.38-0.45→B後追平?誠實measured才宣稱同SLICE A,若沒追平=誠實finding非paper over)·§8 re-measure並行(implementer flag Probe診斷>590s已交代timeout)·§5合量待B §8+勞力池+甲一起·你可宣布B mechanism落地但size真matter §8證後才對用戶驗收領導軸"
---

# B idle-labor→建設 MERGED — size 真 matter 待 §8 re-measure

**MERGED dbc31952 + pushed**。領導軸 size-matter genuine 激勵（用戶裁定 B）。

## merged main 驗全綠 + ★無 revert 硬驗
- 乙-revert crank 常數**不在**（terms.gd:61 僅 revert comment、無 const 定義）+ person.md 死亡模型 HIGH 修**在** + **labor_pool_test 仍 ALL PASS**（labor-pool 保住）+ idle_labor_build_test ALL PASS + constitution 74 + headless baseline。**stale-base 無害**（3-way merge 保 main、reviewer index-hash 親驗）。
- **R² 融合驗 CLEAN**：reviewer 親讀 `_idle_employ_value` 全因子既有真常數（K_MFG/LABOR_SCALE/RATES/need_oracle）**無發明 PER_HAND**（anti-crank 徹底、非縮小版乙）+ guardrail `if opt!="建設":return 0`。

## ★size 真 matter 達成待 §8 re-measure（誠實、同 SLICE A）
- B 前領導軸 ratio **0.38-0.45<1**；**B 後大隊 idle_labor→建設早建 facility 吸收勞力→產出↑→ratio 追平/≥1？** ＝**measurer §8 re-measure**（並行、implementer flag Probe 診斷>590s 已交代 timeout）。
- **若沒追平＝誠實 finding**（idle_employ_value 量級不足/need_weight too low）**非 paper over**（[[feedback_genuine_value_not_crank]]）。
- **你可宣布 B mechanism 落地**（genuine、anti-crank）、但 **organic 領導軸 size-matter §8 證後才對用戶驗收**。

## 序
§8 re-measure → 領導軸追平判 → 我 + 你判 size 真 matter 達成。**§5 合量待 B §8 + 勞力池 + 甲一起**（有餘糧條件、tap-attribution 一次 run）。
