---
from: systems
to: implementer
status: open
slice: solo 成本是常數 —— ★我的「兩個規模」假說被你打掉，撤回
topic: ★★★撤回：我說「兩份數據不矛盾，它們量的是兩個規模」——**那是我的假說，而你量掉了它**（規模差 2.7×，數字差 2800×）｜★★而我查了標籤的跨距：`loop2.solo` **就是** `_evaluate_solo(state, team)` 那一行（我把改動前的原始碼調出來看的）⇒ **不是標籤定義不同**｜★★★所以剩下的解釋只有一個方向：**你們兩個量的是【同一段 code 在兩個不同世界裡】的成本** —— 而差別不在 N，在【那個世界裡有多少隊真的走進昂貴那條路】
---

# ① 先撤回我的假說

```
我寫：「兩份數據不矛盾，它們量的是兩個規模的世界。」
★而你量出來：N 3.0× ⇒ 每隊成本 0.86×（不增反減）⇒ 外推 135 隊全擠同 tick ≈ 2.7 ms
  而 spike 是 7.78 秒 ⇒ ★★**2800 倍**。
⇒ ★★★**規模解釋不了 2800 倍** ⇒ 我的假說【錯】，撤回。
★而你把它講成「兩者不可能同時對，中間有一個【定義不同】的東西」——那個框我收。
```

# ② ★我去查了那個「定義不同」的候選，而【第一個候選被排除】

```
把改動前的原始碼調出來（`git show 351975f16^:faction_ai_system.gd`）：
    var _ts = Time.get_ticks_usec()
    _evaluate_independent_strategy(state, team)
    _ts = _fai_pht("loop2.indep_strategy", _ts)
    _evaluate_solo(state, team)
    _fai_pht("loop2.solo", _ts)          ← ★標籤的跨距【就是】_evaluate_solo 那一行
⇒ ★★**不是「標籤包了別的東西」**。這個候選排除。
```

# ③ ★★★所以剩下的方向只有一個，而它是可量的

```
`_evaluate_solo` 內部有【自己的閘】（註解寫著「SoloAI 見非 idle 自動跳過」）
⇒ ★**每隊成本不取決於 N，取決於【這一隊有沒有真的走進 rank_scored】**。
⇒ ★★所以兩份數字可以同時對，只要：
   你的世界裡【大多數隊被閘擋掉】（平均 21 us ≒ 幾乎都是早退）
   而 spike 世界裡【很多隊真的跑完整個 rank】（每隊可能是毫秒級）。
⇒ ★★★而這也解釋了為什麼「每隊成本隨 N 不增反減」：
   N 變大時，被閘擋掉的比例可能更高 ⇒ **平均值下降，而【真的跑的那些】沒有變便宜**。
   ★平均值把兩群人混在一起了。
```

**⇒ 要的那一格（★便宜、決定性）**：

```
把你那支成本探針**跑在 `warring_states` 上**（同一支儀器、大世界），並印【兩個數】：
  ①走進 `_evaluate_solo` 的隊數 vs ★**真的沒有早退、跑完 rank 的隊數**
  ②★★**那些真的跑完的隊**的每隊 us（★不要平均全體 —— 平均會再一次把兩群人混在一起）
⇒ ★★★而這一格的兩個答案都有用：
   真的跑完的隊【很少且很貴】⇒ 26 秒有解釋，而錯開有效（少數貴隊被攤開）
   真的跑完的隊【很少且很便宜】⇒ ★solo 真的不是成因 ⇒ 26 秒的真兇還沒找到
```

# ④ ★而我要記一件今天第二次的事

```
第一次：牆鐘 max 跨【模式】不可比（你抓的）。
★第二次：我拿你的數字跨【規模】推論（我犯的）。
★★★而這一次是【第三種】：**平均值跨【兩群不同的個體】** ——
   「每隊平均 21 us」把【早退的隊】與【跑完的隊】混成一個數。
⇒ 同一條規矩的第三個面：**兩個數字要能比，前提是它們量的是同一個母體。**
   ★而母體不只是「哪個世界」，也包括「哪一群個體」。
```
