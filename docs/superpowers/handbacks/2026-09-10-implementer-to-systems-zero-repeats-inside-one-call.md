---
from: implementer
to: systems
status: open
slice: 效能線 ｜ 遞迴一次呼叫之內的重複
topic: ★★★**0.0%** —— 頂層 1318 次／子問題展開 **2826** 次／相異 **2826** 個 ⇒ 一次呼叫之內**沒有任何子問題被算第二次**｜★★所以連**最安全的那一種 memo**（語意天然中性、活在一次呼叫之內）**也沒有東西可省** ⇒ **memo 整條路線關掉**（第七個排除，如你預測的形狀）｜★深度分佈：**全部是深度 1** ⇒ **不是「深」，是「寬」**（同一層順序展開 2.14 個不同子問題）
---

★落地：`docs/measurements/2026-09-10-recursion-inside-one-call.txt`（commit 見 git log，main）

# ① 三個數字（★兩個原始數都印，不是只印比率）

```
頂層呼叫（`_resource_prereq_candidates`）      **1318** 次
子問題總展開（`_resolve_resource_prereq`）     **2826** 次 ⇒ 每次頂層 **2.14** 次
其中相異（鍵＝隊｜goal_type｜res）              **2826** 個 ⇒ 每次頂層 **2.14** 個
⇒ ★重複率 **0.0%**
深度分佈：**深度1 ＝ 1318（全部）** ⇒ ★★**寬而不深**
★鍵的說明：鍵含 `goal_type`，而一次頂層呼叫只服務一個 goal ⇒ **拿掉它結論不變**（不是靠鍵撐出來的 0%）。
```

# ② ★★memo 整條關掉（★而它關得比你預期的更乾淨）

```
①跨 tick／同 tick：2.1%／12.9% ⇒ 收益封頂、要付語意風險 ⇒ 上一輪已裁不做
②★一次呼叫之內：**0.0%** ⇒ 連**不必付語意風險**的那一種也沒有東西可省
⇒ ★★★所以這不是「memo 收益不夠」，是**這條路上根本沒有重複計算** ——
  ★而這兩件在提案階段長得一模一樣（「加個快取吧」），在數字上卻是兩個結論。
⇒ 被排除清單（七）：叫太多次／term 太貴／儀器是主因／path 太多／查表／跨-tick memo／**呼叫內 memo**。
```

# ③ 現在剩下的（★我不提名，只把地形擺好）

```
`_resolve_build_facility` 整支 **72.995 s／1779 次**，而它由兩塊組成：
  ③`material/tools` 遞迴 `_resolve_resource_prereq`：**72.4%**（★而它裡面沒有重複可省 ⇒ 是**真的計算量**）
  ①`derived_payoff`：**27.2%**（★你上一封說「若遞迴那條關掉，它自動變成主詞」）
⇒ ★★而「遞迴那條」現在的狀態是：**不是浪費，是工作量** ——
  ⇒ 要再往下只有兩種問法：**(a) 它算的東西是不是必要的**（那會碰語意 ⇒ 你的決策）
    **(b) 它裡面哪一段最貴**（那是再切一層 ⇒ 我可以照同一套做）
⇒ ★★★而 `derived_payoff` 12.5 ms／次**還沒被看過一眼** —— 照你「不要同時切兩段」，我等你派。
```

# ④ 守恆與誠實限

```
resolver 四段 72.840 vs 整支 72.995（**0.21%**）；迴圈身體四段 37.800 vs 37.813（**0.03%**）
fp `16bb6924789c18ac031e61d34c2aeae7` 不變；零 RNG
★誠實限：3000 tick／單一 config／Probe=ON（本路儀器 ≈21%，而本段的 Probe 區塊已量 ＝ 16.2 us／次）
★★跑間噪音仍大（同段在三趟裡 33.9／36.3／30.4 ms/次）⇒ 只談比例
```
