---
from: systems
to: reviewer
status: consumed
slice: C1 票① agent 動詞補課（player_reachable: yes）
topic: R² 請審｜★★★核心是用戶追加的【資訊完整性格】,而我把它的母體釘成【`DecisionContext` 的 119 個 `var` 欄位】—— 理由:那是【引擎替那具身體看世界時真的讀了什麼】,而不是【我們想像玩家需要什麼】｜★★要你審:119 這個母體是不是【對的那一份】,還是它同時太大(含內部快取欄)又太小(漏了引擎沒讀但玩家該看的東西)
---

# R²：`docs/superpowers/specs/2026-09-10-agent-verbs-c1-ticket1-HOW.md`

用戶裁「好 那由我玩」＋追加「UI 需有所有資訊，否則又像之前一樣我會很沮喪」。

## 已 grep 過的現況

```
player_command_api.gd 動詞：move_to / cancel_move / execute_action / respond_to_forced /
                            equip / unequip / deposit_item / take_team_item / dispatch
市場：★0 命中（而 execute_action 的 "trade" 是【隊對隊直接交易】,不是板上掛單/撮合）
附身/離身：★0 命中   時間控制：★0 命中
DecisionContext 的 var 欄位：★119   player_query_api 的查詢動詞：★18
```

## 請你審三件

1. **★★★母體選 `DecisionContext` 的 119 個欄位，對不對。**
   我的理由：**它是引擎替那具身體看世界時【真的讀了什麼】** ⇒ 世界給的清單，
   而不是我們想像玩家需要什麼（★今天已經栽過六次「母體是作者想像中的形狀」）。
   ⇒ ★**但我擔心它同時【太大】又【太小】**：
   ```
   太大：119 裡可能有【內部快取／中間量】(例如 gather 過程用的暫存),玩家看了沒有意義
   太小：★★可能有【引擎沒讀、而玩家該看】的東西（例如純敘事的事件流）
   ```
   ⇒ **請判：要不要先分類 119（決策輸入／內部中間量／其他），還是先逐格打勾再說。**

2. **★時間控制我裁「不要在 `sim_runner` 裡加狀態，把『推進幾步』留在呼叫端」。**
   理由：否則會多出一個「**誰在控制時間**」的第二真相源。
   ⇒ **請判這個切法會不會讓 REPL 那端變得無法測**（自檢腳本要怎麼斷言「推進了 N tick」）。

3. **★撤單：我 grep 不到獨立的撤單函式**（只看到 `tick_team_orders` 內的過期/移除）。
   我在 spec 裡寫「**若真的沒有，那是本票唯一的新機制，而它要與既有移除路徑共用**」。
   ⇒ **請確認我沒有漏看**；★若真的沒有，**請判「共用既有移除路徑」是不是對的形狀**。

## 我知道的盲區

- 「附身」的**語意**我沒定死（改 `player_id`？換控制權？）⇒ spec 寫「**沿用既有語意，不要發明第三種**」，
  ★**而既有語意是什麼，我沒查完**。
- `player_api_mapper.gd` 我**完全沒讀** ⇒ ★★它可能已經有一層對應表，而我在 spec 裡沒有提到它。

CLEAN 才 dispatch。
