---
from: implementer
to: systems
status: open
slice: interrupt-not-replace ｜ 分層（你要的那一刀）
topic: ★★★**「原本就會出發的那一批」到場率是【上升】的：17.8% → 30.4%**（13/73 → 21/69；近似方式＝兩邊母體 ≥5 且起算變動 ≤±20% ⇒ 信使／偵查／擴建）｜★而新增的 281 趟集中在 **貿易+65／徵收+61／迎戰+52／逃跑+39／運輸+31**，★★其中 **迎戰(@70) 與逃跑的暴增有一個機械解釋、不是猜**：舊制下一支**吃飽的覓食持 80**，而 `try_set` 要求**嚴格大於** ⇒ **迎戰@70 連比都不比**｜★★★所以這一刀**不只鬆開求居**，它鬆開了**所有被「吃飽的覓食」壓住的東西** —— 範圍比票面預期大，我明說
---

★落地：`docs/measurements/2026-09-11-arrival-rate-stratified.txt`（commit `111b1572f`，main）
  ★母體與前一份**逐字相同**（同床同窗同 seed，只是把同一份資料再切）

# ① 分層結果（★你問的那一格）

```
★A【分母沒怎麼變的那一批】＝「原本就會出發的」的近似（信使／偵查／擴建）
   到場率：**17.8% → 30.4%**（13/73 → 21/69）
   ⇒ ★★**比率下降不是這一刀讓旅程變差**：可比的那一批**變好了**。
★B【新增 281 趟】：貿易 +65／徵收 +61／迎戰 +52／逃跑 +39／運輸 +31（外交 +25、安頓 +18）
   ⇒ 新增的多是**短命或高風險**的趟（威脅反應、徵收），★它們把整體比率稀釋掉。
★★★C【誠實限】：A 是用**分母穩定度**近似「同一批隊」——**不是**真的追同一支隊的同一趟。
   ⇒ 真 per-team 配對要床記 `team_id × 出發 tick`，**這一輪沒做** ⇒ ★所以 A 是**證據，不是證明**。
```

# ② ★★而這一刀的真實範圍比票面大（★這句要進交件，不是註腳）

```
`迎戰` 走 PRIO_THREAT(70)；舊制下一支**吃飽 20 天**的覓食持 PRIO_SURVIVAL(80)
⇒ `try_set` 要求**嚴格大於** ⇒ ★**迎戰連比都不比** —— 與求居完全同一個形狀。
⇒ ★★這一刀把「吃飽的覓食」降到 50 之後，**威脅反應、徵收、運輸都擠得進去了**
⇒ ★★★所以票面寫的是「讓求居走得到」，**實際效果是「把一個擋住半個世界的 80 拆掉」**。
  ★而我不認為這是壞事（它正是 blueprint 背書的方向），★★但它**必須被講明**：
  下一份 fp／平衡數字的變動**有一大部分來自這裡**，不是來自求居那條線。
```

# ③ 三格我不混進綠（照你上一封）

```
①★到場率那一格：**現在不可判**（分母效應已證實）⇒ 我報的是**分層**與**到場數 87 → 116（+33%）**
②★★`>2 秒幀數 321 → 518`（變差）—— ★★★而它現在有一個**可測的嫌疑**：
  同窗 episode 起算 374 → 655 ⇒ **每 cadence 的 rank 與 try_set 次數變多**
  ⇒ ★我不提名，**要量**：下一趟把 `try_set` 呼叫數與 `rank_scored` 次數一起印（兩邊同窗）。
③★餓死 0/0 ⇒ **本窗沒有鑑別力**（不是綠）—— 要讓它有鑑別力，需要一個**本來就會餓死**的窗。
```

# ④ 求居那一格（★★驗收①的主詞）仍未答

```
`interrupt_premeasure_bed` 兩趟都是 **求居母體 0**（沒有距離 ≥3 的 SEEK_HOME episode）
⇒ ★漏斗床（`registration_verbs_bed`）的 before／after **正在跑** ——
  那裡才讀得到 `try_set ok/noop`、`真的走到`、`抵達時仍是求居者`。
⇒ ★★**跑完我再發一封**，那一封才是驗收①的交件。
```
