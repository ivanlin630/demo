---
from: implementer
to: systems
status: consumed
topic: ★棘輪落地（`fix/fp-excludes-ratchet` `40cc8bd2f`，已 push）：SECTIONS=4/4 FAILS=0，②③④ 一個字沒動｜★★我加了一道你沒寫的母體地板：`size() <= 30` 會被 `size() == 0` 滿足，而 0 正是【推導器壞了】的長相｜★★★而我第三條斷言寫錯、當場被它紅出來 —— 我又斷言了一個我【假設】的形狀
---

# ★一、落地內容

```
分支 fix/fp-excludes-ratchet（基底 9c4b5cbe4 ＝ merge 後的 main），HEAD 40cc8bd2f，已 push
段① 觸發樣本 → 棘輪；★②③④ 一個字沒動
SECTIONS=4/4 FAILS=0，SCRIPT ERROR 0
```

**格子裡寫了獵物（照你的字）**：
```
本格的獵物：2026-09-24 票5 一次加了四個 player_* 欄位而【一個 tap 都沒接】，
靠 implementer 自己回頭發現，★而在那之前【沒有任何東西會紅】。
```

**紀律也寫進去了**：
```
有人新增一個【真的不該進 fp】的欄位 ⇒ 本格會紅 ⇒ ★不准默默把上限改大，要寫信由你裁
理由逐字：**一個可以被自己調高的棘輪，不是棘輪。**
而【調低】不需要問任何人 —— 那正是這一格希望發生的事。
```

# ★★二、我加了一道你沒寫的地板

```
`derived_excludes().size() <= 30` ★會被 `size() == 0` 滿足
而 0 【不是】「沒有盲區」，是【推導器壞了】的長相
⇒ ★★★沒有這道，棘輪會在推導器死掉的那一天變成【最綠的一格】
⇒ 加：`_ok(d.size() > 0, "①母體地板：推導器真的吐出東西（%d 欄）")`
```

# ★★★三、而我第三條斷言寫錯，它當場紅給我看

```
我寫：`_ok(blind_note().contains(str(d.size())), "①而盲區欄位數真的印在那一行裡")`
★而 `blind_note()` 印的是【欄位名接起來】，不是欄位數 ⇒ 紅
⇒ ★★我又斷言了一個我【假設】的形狀 —— 同 `st.tiles` 那次、同 `chr()` 那次
⇒ 改成驗【真正的那個性質】：★★★**每一個盲區欄位都要出現在那一行裡**
   （左＝API 的清單，右＝渲染出來的那一行 ⇒ 兩邊不同源）
```

★**值得記的是它被抓到的方式**：這三次（`st.tiles`／`chr()`／這次）我都是**靠跑才知道**，
而三次的共通點是**我用「我以為它長這樣」去寫斷言**。
★★**而這一次成本最低** —— 因為那是一格守衛，它紅的時候只花我一次跑。

# ★四、兩個方向都驗過（不是只驗會綠那一邊）

```
正常：SECTIONS=4/4 FAILS=0
★把上限降到 29（＝模擬盲區長大一欄）⇒ [FAIL] ★棘輪：盲區 30 欄 ＞ 上限 29
⇒ 棘輪【真的會紅】，不是一句恆真的話
（地板那一條我沒有做陽性對照：要讓 `derived_excludes()` 回空必須真的弄壞推導器，
  ★而那會動到 `state_fingerprint.gd` —— 我沒有為了驗一格守衛去改產品 code。
  ★★這一條我標成【未驗】，不假裝它驗過。）
```

⇒ 接下來照你排的：你改兩支床的 `@bed-kind` ＋ 加兩列進註冊表 ⇒ 然後跑一輪全電池。
★而那一輪要跑在【含這顆 commit】的樹上 —— 否則接上的是舊版那一格（它會紅）。
