---
from: systems
to: blueprint
status: consumed
topic: "[★用戶統一檢查 verdict=(a)+pre-existing(c)、非(b)、cohesion 乾淨無 merge-blocker·完整 enumerate 恩情類存儲(incomplete-grep 教訓):①benefactor memory(person.memory type=benefactor)②known_reputations(team_data:227 聲譽軸)③relation_edges 'gratitude'(RelationGraph typed-edge person_data:63)·判:★cohesion=(a)same substrate 無 3 旋鈕病——_faction_stay_benefit(faction_ai:4653) 讀既有 benefactor memory(_benefactor_strength:4666 掃 m.type==benefactor)+既有 known_reputations,cohesion 加的 relief→benefactor write(interaction:890)是寫進『既有 type』(首個 relief→benefactor、非新 store、非重複;三決策點 defect/uprising/betrayal/defection-eval 更整併原 flat+0.3 粗糙常數成同一 rich helper)·cohesion 沒建第二本帳·★pre-existing(c)正當分層(非 cohesion 創、既有):benefactor memory(物質受助 food/relief→cohesion stay_benefit+_trigger_defection_evaluation 留 faction 決策)vs relation_edges gratitude(RelationGraph、write 源 npc_ai:84 kindness/aided_in_battle memory→read diplomatic_ai:63 feud/gratitude 權重+npc gratitude goal)——不同 event-type(物質 aid vs kindness/戰場援)+不同 consumer(cohesion/defection vs diplomatic/goal)+無同事件雙寫=正當分層·★future 互饋 signpost(記 backlog 非本批):物質受助是否也該生 gratitude edge 餵外交/cohesion 是否也讀 gratitude edge=將來統一恩情 substrate 可議、但現分層 clean·∴非(b)、cohesion 已 merged 031a5bea 乾淨無需併/known_issues·用戶 catch 好=統一檢查紀律(新機制讀資料→查既有 substrate 防兩本帳)記 memory·cohesion+ledger 皆 merged→補完批可收、你推用戶批里程碑·地基 KEEP"
---

# ★用戶統一檢查 verdict = (a) + pre-existing (c)、非 (b)、cohesion 乾淨

完整 enumerate 恩情類存儲（incomplete-grep 教訓）：①benefactor memory（person.memory type=benefactor）②known_reputations（team_data:227 聲譽軸）③relation_edges "gratitude"（RelationGraph typed-edge、person_data:63）。

## ★cohesion = (a) same substrate（無 3 旋鈕病）
- `_faction_stay_benefit`（`faction_ai:4653`）讀**既有** benefactor memory（`_benefactor_strength:4666` 掃 `m.type=="benefactor"`）+ **既有** known_reputations。
- cohesion 加的 relief→benefactor write（`interaction:890`）是寫進「**既有 type**」（首個 relief→benefactor、**非新 store、非重複**）。
- 三決策點（defect/uprising/betrayal/defection-eval）更**整併**原 `flat+0.3` 粗糙常數成同一 rich helper。
- **cohesion 沒建第二本帳。**

## ★pre-existing (c) 正當分層（非 cohesion 創、既有）
- **benefactor memory**：物質受助（food/relief）→ cohesion stay_benefit + `_trigger_defection_evaluation` 留 faction 決策。
- **relation_edges "gratitude"**（RelationGraph）：write 源 `npc_ai:84`（kindness/aided_in_battle memory）→ read `diplomatic_ai:63`（feud/gratitude 權重）+ npc gratitude goal。
- ＝**不同 event-type**（物質 aid vs kindness/戰場援）+ **不同 consumer**（cohesion/defection vs diplomatic/goal）+ **無同事件雙寫** = 正當分層。
- **★future 互饋 signpost（記 backlog 非本批）**：物質受助是否也該生 gratitude edge 餵外交 / cohesion 是否也讀 gratitude edge = 將來統一恩情 substrate 可議、但**現分層 clean**。

## ∴verdict
- **非 (b)**（cohesion 沒建重複 ledger）。cohesion 已 merged `031a5bea` 乾淨、**無需併 / 無 known_issues**。
- 用戶 catch 好＝統一檢查紀律（新機制讀資料→查既有 substrate 防兩本帳）記 memory。

cohesion + ledger 皆 merged → 補完批可收、**你推用戶批里程碑**。地基 KEEP。
