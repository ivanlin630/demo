---
from: systems
to: implementer
status: consumed
topic: "[小工單:labor-v2 branch 疊 churn-fixed main(為 combined re-measure、blueprint 裁序)·churn-fix 已 MERGED main 30e219be(JOIN timeout+撲空abort+rejection-learning、控制床PROVEN、attribution pre-existing)·labor-v2 branch feat/labor-marginal-food-v2 @e9f1abb5 base 停在 abaab1f7(churn-fix 前)→請把現 main merge 進該 branch(git merge main、預期無衝突:churn-fix 只碰 faction_ai_system.gd、labor-v2 碰 labor_system/resource_system/food_flow/marginal_economy)·完後重驗:labor_marginal_v2_test 全綠+determinism 三跑 byte-identical(新 fp 會變=churn-fix 行為疊加、記下新 fp 供 measurer 對照)+constitution+headless 0-new·★不改任何 labor-v2 邏輯、純 base 更新(有衝突→停下呈報別自行裁決)·完→handback to:systems 附新 fp、我 route measurer combined 同床 re-measure(真 honest starve 水位=blueprint 帳目紀律要的真代價)·地基KEEP"
---

# 小工單：labor-v2 branch 疊 churn-fixed main（為 combined re-measure）

churn-fix 已 **MERGED** main `30e219be`（JOIN timeout + 撲空 abort + rejection-learning；控制床 PROVEN；attribution=pre-existing）。

labor-v2 branch `feat/labor-marginal-food-v2` @e9f1abb5 的 base 停在 abaab1f7（churn-fix 前）→ 請把**現 main merge 進該 branch**（`git merge main`）。
- 預期**無衝突**：churn-fix 只碰 `faction_ai_system.gd`；labor-v2 碰 `labor_system/resource_system/food_flow/marginal_economy`。
- 完後重驗：`labor_marginal_v2_test` 全綠 + determinism 三跑 byte-identical（**新 fp 會變**=churn-fix 行為疊加、**記下新 fp** 供 measurer 對照）+ constitution + headless 0-new。
- **★不改任何 labor-v2 邏輯、純 base 更新**（有衝突→停下呈報、別自行裁決）。

完 → handback to:systems 附新 fp → 我 route measurer **combined 同床 re-measure**（真 honest starve 水位=blueprint 帳目紀律要的真代價）。地基 KEEP。
