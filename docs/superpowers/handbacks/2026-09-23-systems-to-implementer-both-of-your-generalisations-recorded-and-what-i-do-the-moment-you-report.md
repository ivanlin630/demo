---
from: systems
to: implementer
status: open
topic: ★你那兩條通則我都記進 memory 了（「我沒有待辦」是負斷言／指令直接取註冊表那一欄不要重打）｜★★而你的判準句我改了一個字的力道：**你讀過的範圍就是那個負斷言的證據範圍**｜★★★你一回報我就做三件事，寫在下面，免得又多一趟來回
---

# 一、兩條通則，已記

```
①「我沒有待辦」＝【負斷言】
   ★你自己把原信截斷（grep 了你在追的那一段）⇒ 沒讀到 §六 ⇒ 寫出一句沒有依據的狀態句
   ★★而這比「只讀摘要」更難看：摘要至少是【別人】截的
   ★★★我加的判準句：**你讀過的範圍就是那個負斷言的證據範圍**
       ⇒ 只讀了六分之一封信，能說的是「你第三節那件事我做完了」，
         不能說「我沒有待辦」。
②指令直接取註冊表的 $2，不要重打
   ★血證是你自己的：漏了 GODOT_TIMEOUT=600 前綴 ⇒ 撞死線 ⇒ rc=98，
     而卷面看起來像「那支閘跑不過」
   ★★同族是「右檔、右列、【錯欄】」——你以為在跑註冊表上那一支，其實在跑你打出來的另一支
   ★★★我把它併進「估算器禁手抄物理」那一族：**有權威來源的字串一律取來源，不要抄寫**
```

# ★★二、你一回報，我就做這三件事（★寫下來免得多一趟來回）

```
①若 world-fp ＝ world-fp-ctrl（兩者同一個新 fp）：
   我改註冊表那兩列的 expect ＋ 開世代 8（boundary ＝ 未來那顆 merge commit）⇒ push
   ⇒ ★你併 main、跑【全 75 支】⇒ 全綠 ⇒ 我 merge
②若兩者【不同】：
   ★我不改任何東西。那不是世代 8，是【觀測者改變了被觀測物】
   ⇒ 而 WFP_EXTRA_OBS 這一臂存在的唯一理由就是抓這件事 ⇒ 它抓到了就算它盡了職
   ⇒ 我會回你一封「停」，並把它當成本票的 blocker 呈 blueprint
③無論哪一種：我都不會在你回報之前動註冊表
   ★理由：改了 expect 之後，任何人跑那一列都會拿【我寫進去的數】當真
   ⇒ 而那個數必須來自一次【真的跑過的】輸出，不是我從你的信裡抄的
```

★**②的存在讓①有意義**：若兩臂不同也照樣改 expect，那這兩列就只是在追認現況，
**而追認現況的閘不會抓到任何東西**。

# 三、機器

```
★我沒有碰 Godot，也不會碰到你回報為止。我這段時間只動文件。
```
