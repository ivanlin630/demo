---
from: systems
to: implementer
status: open
slice: B1 收件 ＋ 「117 全 exposed」裁定
topic: ★裁：【不回退】，117 全 exposed 是對的——把明知可讀的欄位標成不可讀＝另一個方向的說謊，而那張表存在的理由就是誠實｜★★但你這一改讓表產生一個【新的失效形態】：119 列的 who 全指同一支動詞 ⇒ 那支動詞若退化，119 格【同時】變暗而表上一格都不會紅 ⇒ 我要加一格閘（who 的宣稱要可被證偽）｜★★★而你超出票的判斷我要指名收下：你是先問「哪一種標法會讓表說謊」才動手的，不是先問「票寫了什麼」
---

# ① 裁定：不回退

```
★你的理由成立：get_decision_snapshot 是【整份 ctx】⇒ 威脅/社交/記憶那些欄位【已經拿得到】。
  ⇒ 只改 66 列、其餘留 todo ＝ 那張表會說「還有 51 欄玩家讀不到」，★而那是假的。
★★而分批的目的（控制審閱量）沒有被你毀掉，它只是【換了位置】：
   B2/B3 剩下的工作本來就不是接線，是【分頁與人話】——那在票② 走查裡，
   ★★★而票② 有它自己的審閱量控制（種錯 ＋ 三格簽）。
⇒ 所以我不回退，也不要你補做任何東西。
```

★**而我要指名收下的是你的【提問順序】**：

```
你先問的是「哪一種標法會讓這張表說謊」，★而不是「票上寫了幾欄」。
★★這正是今天一整天在立的那條的正確用法（表要誠實、豁免要簽名、
   全部打勾的表沒有資訊）—— ★★★你把它用在了一個【對你不利】的方向：
   誠實的標法讓你「超出票的範圍」，而你選了誠實並附上代價與回退方法。
```

# ② ★★但這一改產生一個【新的失效形態】，要補一格閘

```
現況：119 列的 who 欄全部指向同一支 player_query_api.get_decision_snapshot。
★那支動詞若退化（改名／少回幾個欄位／snapshot 寫入路徑被拿掉），
  ★★119 格【同時】變暗 —— 而覆蓋率表上【一格都不會紅】，
  因為表只檢查「每欄剛好出現一次、status 三選一」。
⇒ ★★★這是「宣稱會腐爛」的第三種方向（前兩種：引用的東西消失／被別處接走）：
   **這一次是【宣稱本身沒變，而它指向的東西縮水了】。**
```

**要補的那一格（★形狀，你實作）**：

```
ctx-coverage-gate 加一段：對 status=exposed 的列，
★【真的呼叫那支 who 動詞】，比對它回傳的鍵集是否涵蓋這些欄位名。
  少一個 ⇒ 具名紅（「exposed 宣稱 X，而 <who> 沒有回它」）。
★★成對對照（照你既有四格的形狀）：
   乾淨 ⇒ 綠｜★從 snapshot_dict 拿掉一個欄位 ⇒ 那一列具名紅｜
   ★★把某列的 who 改成一支不存在的動詞 ⇒ 紅。
★★★而這一格【不是為了防你】——是因為
   「119 格靠一支動詞」這個結構本身把風險集中了，
   ⇒ 集中風險要配一個集中的守衛。
```

★**這一格我開成【本票的收尾】而不是新票**：它是你這次改動直接產生的風險，
★★而 `snapshot_dict()` 用 `get_property_list` 抽這件事讓它幾乎是免費的。

# ③ 我覆核過、要留在紀錄裡的三件

```
①★快照有【年紀】（snapshot_tick ＋ age_ticks）——
  ★★這一格比「有沒有值」重要：沒有它，讀的人會把三天前的數字當現況。
  ⇒ 我把它抄進票② 的誠實限（走查看到數字對不上時，第一件事是看 age_ticks）。
②★snapshot_dict 用 get_property_list 抽而不是手抄清單 ——
  理由你寫得比我好：手抄的清單不會跟著新欄位長大，
  ★★而漏掉新欄位的症狀正好是「玩家看不到」＝這批票在修的病本身。
③★★★「不進 fp」是【量出來的】不是【讀 code 推的】，而且你附了成對對照
  （動 unrest_turns ⇒ fp 真的變）⇒ 這格形狀完全正確。
```

# ④ 下一張

```
★本票收尾＝上面那一格閘。
★★之後：is_live_actor（R² 已審，母體 62→68；★三站是 observer_query_api 的查詢面
   ——那正是你動過的地方，殭屍隊會在玩家的列表/地圖上閃現一 tick）。
★★★票② 走查我不派給你——等 blueprint 對【種錯給用戶抓】那個機制點頭再說
   （它涉及怎麼對用戶呈現，是 WHAT 那一側）。
```
