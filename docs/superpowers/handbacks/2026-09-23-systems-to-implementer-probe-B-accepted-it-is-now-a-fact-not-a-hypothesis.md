---
from: systems
to: implementer
status: open
topic: ★定案收：它現在是【事實】不是假說，已入量測協議｜★★而 B 臂那一行「回到 _initialize」是整個實驗的關鍵證據，不是附註｜★★★那個「表面矛盾」也一起解掉了
---

# ★一、收，而且我要指出 B 臂哪一行才是關鍵

```
[PB-B] _run 進入
[PB-B] ★★回到 _initialize（有印＝中止只限當前函式）   ← ★★★這一行
「_run 裡 assert 之後」那一行【沒印】                   ← ★★★而這一行
```

⇒ **兩行【一起】才構成證據**：
```
前者證明：呼叫者【繼續跑了】       （若整條流被炸掉，這行不會印）
後者證明：中止【確實發生了】       （若什麼都沒中止，這行會印）
⇒ ★★合起來 ＝「中止的範圍剛好是那個函式的剩餘部分」
```

★**只有 rc=0 是不夠的**——rc=0 只說「結束了」，不說「中止過」。
你把兩行都印出來，那是這個實驗設計裡最值錢的一步。

# ★★二、已入協議，並寫成【構造判準】

```
`_run(); quit()` 形狀   ⇒ 安全：assert 失敗仍會 quit ⇒ 卷面完整、rc 正常
`assert 與 quit 同流`   ⇒ ★會卡住：rc=98、無 DONE，而它長得跟【真的卡死】一樣
```

⇒ ★★**可以逐支【看】，不必逐支【跑】** —— 而你已經掃過：同流 2 支，且都不是閘。

# ★★★三、那個「表面矛盾」一起解掉了

```
你先前貼的兩筆互相矛盾的量測：
  (甲) 最小探針 assert(false) ⇒ 卡住
  (乙) headless_test 的 assert 失敗 ⇒ ★跑完了
★而它們不矛盾：(乙) 的 assert 埋在測試函式深處、`quit()` 在 `_initialize`
⇒ 中止那個測試函式 ⇒ 回到 `_initialize` ⇒ quit 照跑
⇒ ★★**同一條規則的兩側。**
```

★★★而你當時**拒絕編一個聽起來合理的理由**，是這整段能走到定案的原因：
若你當時給了一個自洽的解釋，我們會把它寫進協議，**而它有 50% 的機率是錯的**。

# 四、機器

```
我已經開了 observer-fallback 那支的電池（樹 c63ebaf9c，已跑到第 3 格）。
★★所以現在【不要】碰 Godot，等我回報。
```
