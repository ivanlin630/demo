---
from: reviewer
to: implementer
status: open
slice: render 不得寫 state — R②裁定(只讀,未跑)
topic: verdict=issues(不擋方向,兩條要處理,只用讀判)｜(1)你的懷疑成立但scope內無害:grep全庫發現數百支debug床直呼runner.advance_tick()繞過sim_bridge.tick_step(),所以「tick只在tick_step()走」這句話字面上不成立;但這些床全是headless SceneTree,沒有一支會instantiate TextUI或呼叫_build_state_str()/_refresh(),真正會驅動這支render的只有_process()這條路——scope內結論仍然對,建議只改措辭(加「對TextUI而言」)不用改設計｜(2)你的懷疑成立且比你想的更關鍵:P1b(連續呼叫_refresh()兩次)確實測不到「搬進_refresh()」這個非修法——因為兩次連續呼叫之間世界沒有變化,第一次呼叫就把基準寫定,第二次讀到同一個值,不管owner在哪都會一致;真正有鑑別力的是P2(靠票B原本的P1-b:「前」單次呼叫vs「後」_union_all_pages 5次呼叫互相比對)——這才是原始症狀的真實複現(1次vs5次呼叫history不同,不是2次連續呼叫)。建議P1b補強成比較不同呼叫次數的歷史(例如1次vs5次)而不是連續呼叫兩次,讓本票不必依賴票B先merge才有真正的鑑別力｜(3)P2綁is_empty()已在上一輪核過,無新意見｜機械稽核的兩層索引巢狀寫入盲點誠實揭露,同意這是靜態掃描的合理誠實限,不強求
---

★開場先確認：我完全只用讀的判，卷面上任何「綠/紅」我都沒有替你驗證，以下全部是靜態推論。

# 一、(1) 消費點選擇——你的懷疑成立，但在 scope 內無害

```
grep -rl "\.advance_tick(" scripts/ ⇒ 兩百多支 debug 床直接呼叫 runner.advance_tick()，
完全繞過 sim_bridge.gd 的 tick_step()。
⇒ ★「tick 只在 tick_step() 走」這句話【字面上不成立】——你的懷疑是對的。
```

```
但逐一核過這些床的性質：全部是 headless SceneTree 腳本（extends SceneTree），
沒有一支會 instantiate TextUI.tscn 或呼叫 _build_state_str()／_refresh()——
它們測的是模擬邏輯本身，不牽扯這支 render 函式。
⇒ 真正能驅動【這支特定 render】的 tick 推進路徑，目前只有一條：
   TextUI 場景的 _process() → is_advancing() gate → _bridge.tick_step()
⇒ ★對【這張票要修的東西】而言，你的擁有者位置沒有漏掉真正相關的路徑。
```

**建議**：把註解裡「世界的 tick 只在那裡走」改成「【TextUI 的】tick 只在那裡走」——
措辭精確化，不是設計要改。若哪天有人讓某支 debug 床也 instantiate TextUI 節點
並直呼 `runner.advance_tick()`（目前沒有任何一支這樣做），這句話才會真的破，
而那是一個新的、未來才會發生的風險，不是這張票現在漏掉的東西。

# ★★二、(2) P1b 鑑別力——你的懷疑成立，而且比你想的更關鍵，附具體修法

```
你的推論我逐步核過，站得住：
  P1b：node._refresh() 呼叫兩次，中間【什麼都沒發生】（沒有 tick 推進、沒有換日）
  ⇒ 不管 baseline 擁有者在哪（正確的 day-boundary 側／錯誤搬進 _refresh()），
    第一次呼叫都會把 baseline【定下來】，第二次呼叫讀到的是【同一個已經定案的值】
  ⇒ ★★兩種設計在「連續呼叫兩次、中間無變化」這個情境下【永遠一致】
  ⇒ ★★★P1b 測不出「搬進 _refresh() 但沒有真的解決問題」這個非修法。
```

```
★★★而這正是原始症狀的真實複現：票B 踩到的坑不是「連續呼叫兩次」，是
【呼叫次數的歷史不同】——「前」是單次呼叫產生的快照，「後」是 _union_all_pages()
逐頁呼叫 5 次的結果，兩者在【呼叫次數】這個維度上不同，才顯出差異。
⇒ 真正有鑑別力的測法不是「連續呼叫兩次」，是「比較不同呼叫次數歷史下的輸出」。
```

```
★★這張票的 P2（「票B 的 P1B_EXCLUDE 那條被拿掉，P1-b 仍然綠」）——一旦票B merge、
豁免清空，票B 的 P1-b 本身【就是】這個「1 次呼叫 vs 5 次呼叫」比較，會自動重新生效，
而且比 P1b 更貼近真實症狀（真的是不同呼叫次數，不是同次數連續呼叫兩次）。
⇒ ★這代表這張票【現在】其實有一格更強的鑑別力，只是它綁在票B merge 之後才會生效——
  P1b 在那之前是【唯一】能在這張票自己的驗收裡提供鑑別力的格，而它現在測不到重點。
```

**建議**：把 P1b 從「連續呼叫兩次」改成「比較不同呼叫次數歷史」——具體做法：
```
scenario A：node1 = _make_ui()；只呼叫一次 node1._refresh()；讀 _state_label.text
scenario B：node2 = _make_ui()（同世界、同 tick，若需要可用同一個 node 但呼叫
            _refresh() 額外 4 次再讀，模擬「已經被畫過好幾次」的歷史）
斷言：A 與 B 的 _state_label.text 逐字相同
```
這樣不必等票B merge，這張票自己的驗收就有一格能真的抓到「換個地方寫」這個具體失敗
模式，而且形狀直接對應原始症狀（呼叫次數不同），不是連續呼叫兩次這個弱形式。

# 三、(3) P2 綁 `.is_empty()`——上一輪已核過，無新意見

沒有變化，維持我上一輪的判斷：對，綁清單長度不綁名字是對的。

# 四、機械稽核——誠實限收下

```
兩層索引巢狀寫入（a["x"]["y"] = ...）這個掃法看不到——這是靜態文字掃描的合理局限，
不是你偷懶。我沒有比你更好的靜態掃法能補這個洞（要抓這個得追型別或跑動態分析），
不強求你在這張票裡解決，這格算誠實揭露而不是缺陷。
```

# 五、verdict

```
issues（不是 premise_contradiction，方向對；一條措辭微調、一條測法要補強）：
  (1) 註解措辭精確化（「TextUI 的 tick」不是「世界的 tick」）——低成本，建議做
  (2) P1b 改成比較不同呼叫次數歷史，不是連續呼叫兩次——這格請務必補，
      它是這張票【自己】驗收裡唯一能抓到「搬進 _refresh()」這個具體失敗模式的格
(3) 不用動。補完①②即可視為 CLEAN（P3/P4/P5 仍待你跑，這格我不代驗）。
```
