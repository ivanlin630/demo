---
from: systems
to: implementer
status: consumed
slice: 招募匿名｜扣錢搬 0 人
topic: ★**先修這張（小），游標真值那張暫停**——用戶回報「不 work」，而真相是**扣了 50 coin、搬了 0 個人、印「招募成功」**｜★★★而它是**三個獨立的錯疊在一起**：①用【代理量】代替真正的量（`population > 1` 代替「真的有匿名」）②**不檢查結果就回報成功** ③回報的字與事實不符 —— ★而**玩家只看得到第三個**｜★★守它的不變量是「**收費與交付必須成對**」
---

# ★一、三個錯，逐一（我核過 file:line）

```
①`player_command_system.gd:73-75`
   func _target_has_anon(tgt): return tgt.population > 1
   ⇒ ★`population > 1` 是【代理量】：它問的是「人夠多嗎」，而要問的是「**真的有匿名嗎**」
   ⇒ 對方全具名時它照樣回 true
②`:1160` `AnonTierSystem.transfer_proportional(tgt, pt, 1)` ⇒ ★★**回傳值沒有人看**
   （`anon_tier_system.gd:199-201`：沒有匿名可搬 ⇒ 搬 0）
③`:1162-1165` 照印「招募成功（花費 %d coin，新人口 %d）」⇒ ★★★**而人口一個都沒增加**
⇒ 玩家的經驗：付了錢、什麼都沒發生、而畫面說成功了。
```

# ★★二、修法（★三個都要修，不要只修第一個）

```
①`_target_has_anon` 改問真正的量：**`AnonTierSystem.total_pop(tgt) > 0`**（或該系統的等價查詢）
  ⇒ ★不要用「人口減具名數」這種【自己算一次】的寫法——那是製造第二份真相
②`transfer_proportional` 的**回傳（真的搬了幾個）要被讀**
  ⇒ **moved == 0 ⇒ 不扣錢、回 `ok:false` ＋ 原因人話**（★拒絕禁靜默：要說「那支隊沒有可招募的人」）
③成功訊息只有在 **moved > 0** 時才印，而且★**把 moved 印出來**（「招到 N 人」）
  ⇒ ★★玩家從此能分辨「招到 0 人」與「沒招成」——而今天這兩者在畫面上是同一句話。
```

# ★★★三、守它的不變量：**收費與交付必須成對**

```
P1 [成對] 呼叫 recruit_anon N 次 ⇒ ★**coin 減少的總額 ÷ 單價 ＝ 真的搬過來的人數**
   ｜負對照：把 moved 檢查拿掉（還原今天的行為）⇒ **必須紅**
   ★★母體地板：那一輪要**同時**有【招得到的目標】與【招不到的目標】各至少一個
     ⇒ 否則「成對」會在【全部都招得到】的樣本上恆真，而**招不到那一支才是今天的病**
P2 [★訊息與事實一致] 招不到時：`ok:false`、**不扣錢**、訊息說得出原因
   ｜負對照：把回傳改回 `ok:true` ⇒ 必須紅
P3 [world-fp] ★這張票**會改行為**（不再白扣錢）⇒ fp 會變是預期內的
   ⇒ ★★**基準重設要與造成它的改動原子落地**（今天踩過一次，不要再踩）
P4 [電池] merge 前全電池；★★交玩前照 merge 檢查表第 8／9 步
```

# 四、不在本票

```
✘ 「花錢買人而對方沒有意願」這件事本身 —— ★`:38` 自書 STUB，blueprint 已登 WHAT 待裁
  ⇒ ★★本票只保證【它不再對【不存在的交易】收費】，不處理【這個交易該不該存在】
```

★**而這一族值得你記一句**：**一個動作回報「成功」之前，要問它【改變了什麼】** ——
★★答案若是「什麼都沒搬」，那就不是成功。**執行了 ≠ 生效了**，同「活著≠前進≠送達」。

★#4（事件流對自家隊全盲）是中票，**排在游標真值之後** —— 我寫 spec，你先不要碰。
