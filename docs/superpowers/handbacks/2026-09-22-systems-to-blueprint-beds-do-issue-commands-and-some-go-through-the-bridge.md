---
from: systems
to: blueprint
status: consumed
slice: 指令佇列化 —— **「headless 床零指令」不成立**
topic: ★★★**三支床在下指令，而且【一部分正好走 bridge】** ⇒ 佇列插在 `dispatch` 會打到它們｜★**你的結論（不推世代邊界）可能仍成立，但理由不是你給的那個** —— 而理由換了，風險也換了｜★★**真正的代價**：走 bridge 的那 14 個呼叫點，多半是「呼叫完【立刻】斷言結果」⇒ **佇列化 ⇒ 斷言看到的是還沒套用的世界 ⇒ 床變紅**
---

# 一、★數字（我掃的）

```
**直接呼叫** `execute_action(`（繞過 bridge）：
   `headless_test.gd` **18**｜`qa_probe.gd` **4**
**經過 bridge**（`command_player`／`_cmd_api.dispatch`）：
   `headless_test.gd` **9**｜`ui_flow_test.gd` **4**｜`agent_repl.gd` **1**
⇒ ★★**「零指令」不成立**；而且**兩條路都有**，★★★**同一支床（`headless_test`）兩條路都走**
```

# 二、★★你的結論可能仍成立 —— **但理由要換**

```
★你的理由：「headless 床零指令 ⇒ fp 逐字相同 ⇒ 不推世代邊界」
⇒ ★★**前半不成立** ⇒ 這個理由**不能用**
★★★**而正確的理由可能是**：**佇列插在 `dispatch`** ⇒
   **直接呼叫 `execute_action` 的那 22 處【繞過佇列】⇒ 它們的行為不變**
   ⇒ ★所以**若世代邊界只由那些床決定**，結論仍成立 —— **但那要查，我還沒查**
⇒ ★★**我把「結論對、理由錯」明講出來，因為下一個人會沿用理由而不是結論。**
```

# 三、★★★真正的代價：**走 bridge 的那 14 處會紅**

```
那 14 處（`headless_test` 9 ＋ `ui_flow_test` 4 ＋ `agent_repl` 1）多半是
   **呼叫 ⇒ 立刻斷言結果**（★這是測試的自然寫法）
⇒ **佇列化之後，指令要到【下一個 tick 邊界】才套用**
⇒ ★**斷言會看到「還沒套用」的世界** ⇒ **床變紅，而那不是缺陷，是語意改了**
```
**⇒ 兩條岔路，要你裁**：
```
(甲)**佇列提供「立刻沖洗」給測試用**（`flush_now()`）
    ⇒ ★★**代價**：決定性保證被削弱 —— **有一條路可以繞過邊界** ⇒ 而繞過的那條路**很好用**
    ⇒ ★★★**而「很好用的繞道」最後會出現在 production**（今天我們已經看過同型：
       `Probe.enabled` 那條繞道就是「很自然」才被寫出來的）
(乙)**改寫那 14 處**：指令 ⇒ **推一個 tick** ⇒ 再斷言
    ⇒ ★**貴一點，但它讓測試與 production 走同一條路**
    ⇒ ★★而那正是今天 `bed-arm` 閘在做的事（**不准新床自己拼順序**）
★**我建議 (乙)** —— 理由是 (甲) 造出來的正是我們今天花一整天在抓的那種東西。
```

# 四、★誠實限

```
①我**沒有**逐處讀那 22＋14 個呼叫點，只數了它們 ⇒ ★「多半是立刻斷言」是**推測**，不是核過
   ⇒ ★★**開票前要逐處確認**（★而那正是這張票的工作量所在）
②`player_pending_targets`／`state.player_id` 在 `scripts/simulation/` 有 **123 處**被讀
   ⇒ 佇列化改變「指令何時可見」⇒ ★**那 123 處還沒查**
```
