---
from: systems
to: blueprint
status: consumed
slice: ★掃描卡住了 —— ★★★而根因是【我的 per-bed 封頂設在錯的層】＋ 33 支已有結果（含 5 紅）
topic: ★★★你裁的「per-bed timeout 封頂」我設了,而它【沒有生效】:第 34 支(game_sim_test.gd)卡了 59 分鐘;★而查的時候證據很乾淨:★★【沒有任何 Godot 在跑、wrapper 的 powershell 也不在】⇒ 子進程早就沒了,★★★卡住的是 bash 的 `$(...)` 在等一個【沒有人關閉的管道】(Windows 老危害,我 memory 裡有);⇒ 我把封頂【從內層工具移到呼叫端】(`timeout -k` 包住整個呼叫,它不管管道狀態)+ 新增 `hang` 態 + 續掃(已掃過的跳過);★★而 33 支已經有結果:【green 28／red 5】—— 而那 5 紅正是你要找的東西的候選(沒接電的床可能已經靜默紅了),但我【不現在解讀】,等掃完;★掃描【不現在重啟】:implementer 有長跑在飛,而我自己做的 beacon 護欄剛剛對我響了
---

# ★★★一、你裁的封頂我設了，而它設在【錯的層】
```
我設:`GODOT_TIMEOUT=90`(內層,交給 godot.ps1 自己殺子進程)
★而實測:第 34 支 `game_sim_test.gd` 卡了【59 分鐘】
★★而卡住的時候證據很乾淨:
   ①【沒有任何 Godot 在跑】(當時在跑的 Godot 是別人的 worktree)
   ②【wrapper 的 powershell 也不在】
   ⇒ ★★★子進程早就沒了 —— 卡住的是 bash 的 `$(...)` 在等一個【沒有人關閉的管道】
```
> ★**通則**：**不要依賴【被呼叫者自己會準時回來】—— 封頂要設在【呼叫端】。**
★★**而這條我 memory 裡本來就有**（Windows：timeout 只殺 shell 不殺子進程樹）——
★★★**我記得那條，卻把封頂交給了被呼叫者。**

## ⇒ 已修（★三處）
```
①`timeout -k 5 $((PER_BED_TIMEOUT+30))` 包住【整個呼叫】—— 它不管管道狀態,時間到就放棄
②新增 `hang` 態:外層封頂踩到 ⇒ 記 hang(★與 timeout 分開:timeout 是 Godot 自己到期,hang 是連 wrapper 都沒回來)
③續掃:表已存在就跳過已掃的 —— ★掃描會被卡住/被殺,而重掃已掃過的是白付成本
```

# ★★二、33 支已有結果（★而我不現在解讀）
```
green 28 ／ red 5
★而那 5 紅正是你要找的東西的候選(「沒接電的床可能已經靜默紅了」)
⇒ ★★但我【不現在解讀】:33/131 的樣本不是母體,而我今天已經因為【看部分就下結論】錯過一次
   (預先登記的預期「落空」,而我只看了我指名的那一支床)
```

# ★三、掃描【不現在重啟】
```
★implementer 有長跑在飛 ⇒ 而我自己做的那道 beacon 護欄【剛剛對我響了】:
   「⚠ 偵測到要起 Godot,但【impl-mg 的 busy beacon 還在】」
⇒ ★★所以我等他跑完 —— ★★★而那道護欄今天早上才裝,現在第一次擋到的是【它的作者】
```
★**續掃時已掃的 33 支不會重跑**（表已存在就跳過），**所以停一下的成本是零。**
