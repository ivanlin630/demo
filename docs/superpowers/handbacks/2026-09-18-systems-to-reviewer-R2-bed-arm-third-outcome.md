---
from: systems
to: reviewer
status: consumed
slice: `bed-arm` 的第三種結果（★裁定 ＋ R② 求審）
topic: ★**implementer 的第二個軸把 27 拆成三堆**：真盲區 2／照抄就好 5／**不讀 `Probe` 的 20**｜★★★**而我【不】把那 20 支丟進現有白名單** —— 白名單的語意是「**未遷移存量**」，而這 20 支不是「還沒遷移」，是**這條規則對它們不適用** ⇒ **丟進去就是把兩件事混成一格**（★我今天已經因為同一個病被抓過：把違規混進「別動它」清單）｜★★**我裁：閘要有【第三種結果】，而且它必須是【推導出來的】不是【宣告的】** —— 判準：這支床有沒有讀 `Probe`｜★請打這個裁定，特別是它的誠實限
---

# 一、三堆（implementer 分的，我認）

```
A 真盲區 2 支：手工建世界 ＋ 真的讀 Probe ＋ 沒 arm     ⇒ ★遷移（修的是真的）
B 5 支      ：setup ＋ 已自己 arm，只是沒走 helper
              ★★其中 2 支的 arm 在 setup 【之後】⇒ 那一段【現在就是盲的】＝ 真缺陷，不是形式問題
C 20 支     ：★從來不讀 Probe ⇒ 「arm 在 setup 之後」對它們【沒有任何後果】
```

# 二、★★★我的裁定：第三種結果，而且要【推導】不要【宣告】

```
現在：只有兩格 —— 已遷移（helper）／未遷移（★白名單，人工維護 271 筆）
我裁：加第三格 —— ★「本閘不適用：這支床不讀 Probe」，★★由閘【自己掃出來】，不進任何名單
```
★**為什麼不能丟白名單**：白名單的語意是「**未遷移存量**」＝ **欠債**；
而 C 類**不欠債** —— ★★**把它們放進去，271 會變成 291，而那 20 筆是【假的債】** ⇒
**下一個人看到「白名單又長了 20」會以為情況變糟，而世界沒有變。**
★★★**（同一個病我今天犯過：把 3 處真違規混進「合法的自家讀取」清單 —— 標籤錯了，比漏掉更糟。）**

★**為什麼要推導不要宣告**：
```
宣告（進名單）⇒ 哪天這支床開始讀 Probe，名單不會自己更新 ⇒ ★免疫消失而沒有人知道
推導（每次掃）⇒ ★★那一天它自動掉回「需要遷移」那一格 ⇒ 閘紅 ⇒ 有人被叫醒
```
★★**這跟我們這幾天在做的「免疫要能在消失的那一刻被發現」是同一條。**

# 三、★★★請你打這個誠實限（它是這個裁定最弱的地方）

implementer 自己標的：
> 「我數的是 `Probe.counts／samples／peaks／amounts`（讀）與 `bump／note／enabled`（寫）的**字面出現**
> ⇒ **若某支床是透過別的 helper 間接讀 Probe，我的數字會低估**。
> ★兩支 A 類我逐支開檔確認過；★★**C 類 20 支我只做了字面掃描。**」

⇒ ★**推導式判準會繼承同一個弱點**：**間接讀 Probe 的床會被誤判成「不適用」** ⇒ **那是一個假的免疫。**
★★**我打算的處置**：判準用**檔內零 `Probe` 字面**（保守方向：只要出現過就算「適用」），
並**把這條誠實限印進閘自己的輸出**（「不適用 N 支｜判準＝檔內零 `Probe` 字面；★間接讀會被低估」）。
★★★**請打**：①這個保守方向對不對 ②**有沒有更好的判準**（例如掃它呼叫的 helper）
③**印在輸出裡夠不夠**，還是這 20 支該逐支開檔核一次（★那是一次性的，可做）。

# 四、範圍
A(2) ＋ B(5) 的遷移**與這個裁定無關**，我已請 implementer 先做（★其中那 2 支「arm 在 setup 之後」是**現在就有的真缺陷**）。
