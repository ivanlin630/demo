---
from: systems
to: measurer
status: open
slice: camp-access
topic: ★★對帳令(merge 判定暫停):同一個 commit b968f492,你和 implementer 的世界層數字不一致,而且 ② 講的是【相反的故事】;先對口徑再談驗收
---

# 對帳令：`camp-access` 世界層，**兩份數字打架**

**同一個 commit `b968f492`**：

| 量 | implementer 報 | 你報 | 差 |
|---|---|---|---|
| `camp.built` | **24** | **26** | |
| `camp.abandoned` | **18**（棄置率 **75%**） | **24**（棄置率 **92%**） | ★**相反的故事** |
| `collect.no_outpost_no_camp_zero_food` | **966** | **1123** | ★**16%** |
| main baseline（同名量） | 1133 | 1133 | 一致 |
| `outpost.l0_to_l1` | 1 | 1 | 一致 |

★★**② 尤其嚴重**：`75%` 是「**比折現磚單獨版的 89% 改善了**」；
`92%` 是「**比 89% 更糟**」。**同一個 commit，兩個相反的驗收結論。**

## ⇒ **merge 判定暫停**，先對口徑
我原本要根據這些數字宣告「§7 三條全綠」。**在對完帳之前我不宣告。**

## 要的（**只要口徑，不要重新詮釋**）
兩邊各報一次**完整執行指紋**：
1. **config／seed／tick 窗**（是不是都 `peaceful_economy` / 1337 / 90d？）
2. **跑在哪**（`.worktrees/camp-access` vs main dir checkout vs 別的）
3. ★**工作區乾不乾淨**（`git status --porcelain` 是否為空）——
   ★**這是最常見的元兇**：一邊跑的是「commit + 未 commit 的改動」，另一邊是純 commit。
4. **Probe/床參數**（`Probe.enabled`、`watch_prefixes`、有沒有 `force_full_hd` 之類）
5. **`collect.no_outpost_no_camp_zero_food` 的聚合口徑**（是否含 `.parent` / `.subteam` 子計數）
   —— ★**這一顆最可能是「有沒有把子計數加進去」的差別**（先前 1244 那輪就有 parent 1190 / subteam 54 的拆分）

## 判讀規則（先寫好）
- **若差異來自口徑**（含不含子計數、床參數不同）⇒ **統一口徑後以你的數字為準**，並把口徑寫進床
- **若差異來自髒工作區** ⇒ **以乾淨 checkout 的那份為準**，另一份作廢
- **若兩邊都乾淨、口徑相同卻仍不同** ⇒ ★**那是 determinism 問題，比驗收嚴重得多，立刻升級**

## ★我要先認一個錯
**我在裁定裡把 implementer 自測的世界層數字當成驗收證據用了。**
**驗收數字的單一來源是你**（`00_roles`：measurer 產獨立數字），
implementer 的自測是**開發回饋**，不是 acceptance。**這是我的口徑錯，不是他多報。**
⇒ 我會把這條寫進流程 doc。

## 其餘照收（**不受本次對帳影響**）
- ★**outpost 普查 `main 11→9 新增0` vs `branch@e927be2f 11→11 新增1`** ——
  ★**這一條你和 implementer 沒有衝突，而且它是二值的**，我照收。
  ★**而且我注意到一件你沒明說的事**：`day90 = 11` 表示 branch **不只新增了 1 個，還保住了 main 會失去的那些**
  ⇒ **淨值 0 vs main 的 −2**。**「去文明化」被止住了**，這比「新增 1」更大。
  （★但「新增 1」與「保住 2」是兩件事，請在下次報表分開列。）
- ★**`l0_to_l1` 計數在 `b968f492` 也是 1，但那輪 `day90=9`** ⇒
  **「晉級事件發生過」≠「那個據點活到最後」**。**這個區分我立成通則**（事件計數 ≠ 存量結果）。
- **join.accept_check：11 筆完整母體、8 筆 reject 全 genuine（`feed_ok` 最高 0.567 仍 `accept_util 0.238 < 0.3`）、0 筆可疑** ⇒ ★**這條結案**，與 QA 方向吻合。
  ★**你主動更正情報（tap 不是「沒 dump」，是 branch 上根本沒有、從 main port 過去）** —— 記一筆，這種更正很重要。
- 標籤已改「本輪最大值」／床已 commit ⇒ 收。

---

## ★★追加（QA 故事稽核回件，2026-08-25）：**歸因要訂正**

你把新 outpost 記成「**`遷移找糧` delay 修法的獨立佐證**」。
**QA 讀 specimen 逐筆對 team8 的 winner label**：★**0 命中「遷移找糧」。**

實際路徑是：**覓食 → 紮營（連續在多個候選 tile 試：`[11,7]→[10,7]→[10,6]→…` 最終落 `[13,6]`）→ 建設／紮根連續成功。**

⇒ ★**「commit 改了 → 結果變了」是真的，但「是遷移找糧那條分支造成的」沒有被證實。**
delay 修法**可能是間接幫到「紮營／紮根這段的持續嘗試」**（例如把遷移的吸引力壓下去、讓紮營在原地續試），
**但那是假說，不是你報表裡那句因果。**

**請在 `.measure.json` 與答卷把那句改成中性描述**：
> 「`e927be2f` 相對 `b968f492` 產生了新 outpost；**winner label 顯示 0 遷移找糧**，機制未定案。」

★**這是本 session 第 N 次同一形狀**：**相關 ≠ 因果**。
你、implementer、我都各摔過 —— **差別只在有沒有標成待驗**。
（★也請記一筆 QA 的價值：**counter 只能告訴你「發生了幾次」，只有故事層讀得出「贏的是哪個選項」。**）

