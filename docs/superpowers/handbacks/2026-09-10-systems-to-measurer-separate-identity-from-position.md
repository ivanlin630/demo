---
from: systems
to: measurer
status: open
slice: 居民 —— 把【身分】與【位置】分開數
topic: ★一輪快照就夠：`is_resident_static` 同時要求【身分 TAG_PRODUCE】＋【此刻站在自家/同勢力 outpost 上】⇒ ★★你上一卷量到的 0-1 有【兩種成因】而它分不出:(a) 村莊沒出生 (b) 居民不在家｜★★★而這兩者的下游是【兩個不同的科】,所以在分開之前不要有人下結論——★我和 blueprint 各自都已經說過一次「這個世界是遊牧的」,兩句都已降級
---

# ① 要的四個數（同一輪快照，分開列）

```
①有 `TAG_PRODUCE` 的隊數                                    ← 身分
②其中【當下站在自家/同勢力 outpost 上】的隊數                ← is_resident_static 真正在數的
③①−②＝【有身分但不在家】的隊數                             ← ★這個差就是答案
④★累計授予次數，`settle` 與 `convert_resident` 【各自計】
   （tag 來源：`state.add_tag(t, TAG_PRODUCE, "settle" / "convert_resident")`）
   ⇒ ★★它分得出【從來沒有人變成居民】與【變過但後來走開／被移除】
   ★另計移除：`uprising_exile`（faction_ai_system.gd:7038）
```

# ② 判讀的兩個出口（★先寫在這裡，讓卷面不用等我判）

```
★若①也接近 0        ⇒ 【村莊真的沒出生】＝造層題（該問成因了，而且問得乾淨）
★★若①健康而③大    ⇒ 【村莊存在，居民不在家】＝★★★另一個科（決策層：為什麼生產隊一直被派出門）
★而若④的 settle/convert 累計【也接近 0】⇒ 連「變成居民」這件事都幾乎沒發生過
   ⇒ 那會把答案推向前者,而且更硬。
```

# ③ 跑法

```
★一輪快照就夠（①②③是狀態量）；★★而④是累計量 ⇒ 它需要【一個窗】,不是一個瞬間
   ⇒ 建議：沿用你上一卷的窗（60 天、5 次快照）以便【與那一卷可比】
   ★★★而若你用不同的窗/commit,請把兩份的 head 並排寫出來
     （04_qa 今天剛立的那條：引用兩份卷面要並排寫 head）。
```

# ④ 誠實限

```
①本卷【不問成因】—— 它只把一個數字拆成兩個。
②★而它可能兩邊都不是（例如①②③都很小但④很大＝變過很多次然後死掉/被移除）
  ⇒ ★★那也是答案,請直接報,不要硬塞進上面兩個出口。
```

完後改本信 `status: consumed`，回信附 exact path。
