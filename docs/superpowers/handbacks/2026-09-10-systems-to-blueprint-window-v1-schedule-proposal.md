---
from: systems
to: blueprint
status: consumed
slice: 家設計窗 v1 → 排程提案
topic: ★★★**三個待驗我先查了，兩個當場坐實、一個部分答**：`tax_rate` **零個寫入點**（死常數坐實）／`TAG_EXILE` **零個讀者**（它不是閘也不是權重，是**只寫不讀**）／徵收有 `_a2b_remote_tribute_payers`「dispatch＋settle 對帳」的痕跡 ⇒ 不是純瞬移，但兌現點我沒讀｜★★而排程提案的核心是一句話：**④a 必須自帶【遷移】，否則登記錨上線那天全世界同時失去居民身分**｜★序：④a → ①+③ → **攻擊門** → ④b → ② → ⑤ → ⑥（⑥可穿插）
---

# ① 三個待驗（★先給答案，因為它們會改變切法）

```
①★**`tax_rate` 現況誰寫 ⇒ 沒有人**：production 裡 `tax_rate =` 的寫入點【零個】，
  只有 `team_data.gd:188` 的欄位預設 0.3。
  ⇒ ★★**「疑死常數」坐實**：它是一個**穿著變數名字的常數**
  ⇒ ★★★所以卡①的「tax_rate＝活變數」**不是調參，是接線** —— 而它與「食物併稅軌」
    是**同一張票的兩半**：併軌讓它有意義，接線讓它會動。**分開做，其中一半會像沒效果。**
②★**`TAG_EXILE` 是閘還是權重 ⇒ 都不是**：全庫只有兩處 ——
  `team_data.gd:43` 常數本身 ＋ `manpower_system.gd:206` **一個寫入點**（俘虜脫隊）。
  ⇒ ★★**零個讀者** ⇒ 它是一張**只寫不讀的標籤**。
  ⇒ ★★★所以卡⑥「EXILE 閘性待驗」的答案是：**沒有閘性可驗** ——
    而這改變了那一卡的形狀：**不是「要不要收窄它」，是「要不要讓它有意義」**（那是新機制，不是明文）。
③**徵收/進貢是不是瞬移 ⇒ 部分答**：`faction_ai_system.gd:20`
  `_a2b_remote_tribute_payers`（註解：**遠距徵收 dispatch 的 payer id、settle 對帳**）
  ⇒ ★存在一條【dispatch ＋ 事後對帳】的路 ⇒ **不是純瞬移**。
  ⇒ ★★而**兌現點我沒有讀**（貨在哪一刻進誰的手）⇒ **標未驗**，它歸在卡⑤那一張裡查。
```

# ② ★★★排程提案的核心：**④a 必須自帶【遷移】**

```
卡④寫著「**按站位判定全改讀此欄**」。
⇒ ★而上線那一刻，`work_outpost` 這個新欄位**每一支隊都是空的**
  ⇒ **全世界同時失去居民身分**：生產停、返家 gate 全關、勞力池空、救濟找不到對象……
⇒ ★★所以 ④a 的範圍**不是「加欄位＋改讀者」，是「加欄位＋改讀者＋把現況搬進去」**：
  世界載入／首次 tick 時，把**當下符合舊站位判定的隊**寫成登記（一次性）。
⇒ ★★★而這一格要有它自己的對照：**遷移前後，居民數必須相同** ——
  ★不是「差不多」，是**逐隊比對**（★★否則「錨上線」與「世界崩了」在指標上長得一樣）。
```

# ③ 切法（★六卡 → 七張，序已排）

| # | slice | 內容 | 為什麼在這個位置 |
|---|---|---|---|
| 1 | **④a 登記錨（地基）** | 欄位 `work_outpost` ＋ **遷移** ＋ 讀者全切換 ＋ 棘輪擋新寫的站位判定 | ★全窗地基；★★而**遷移**讓它可以獨立上線 |
| 2 | **①＋③ 稅軌與勞力池** | 食物併一般稅軌 ＋ `tax_rate` 接線活變數 ＋ 勞力池只收主隊＋登記居民 | ★它們是錨的**頭兩個消費者** ⇒ ★★它們綠了才證明錨**在起作用**而不只是**存在** |
| 3 | **攻擊門**（既有 spec，已 R² CLEAN） | — | ★★★世界開始有滅團 ⇒ ④b 的「據點滅→流離」**才有真樣本**；★而 1 先做 ⇒ 攻擊門期間新寫的 code 一開始就走對路 |
| 4 | **④b 動詞** | 招攬（`invite_settle` 落效）／上門請求／自願離開（引擎自選下齣）／驅逐（新動詞＋大怨入帳）／據點滅→流離 | 需要 3 提供的樣本；★驅逐要 5 的帳 ⇒ 見下 |
| 5 | **② 供養請求制 ＋ 恩義/怨帳** | 請求制弱義務 ＋ **帳要有讀者**（叛離／互助／報復） | ★最大一張；★★而「驅逐入大怨」把它與 4 綁在一起 ⇒ **4 與 5 可合可分，我建議分：先讓驅逐能發生，再讓它被記住** |
| 6 | **⑤ 本城** | 領主欄 ＋ 單/多點秤選 ＋ 遷都事件廣播 | ★而**物資不瞬移**我建議**拆出去**：它是後勤 arc 的東西（convoy／在地倉），★★塞進本窗會讓這一張變成兩個 arc |
| 7 | **⑥ 明文＋儀器** | 階級雙向流動明文／不做時效取得（記進意圖帳＝**已完成**）／流民不疊標籤（登記空＝狀態，儀器分開數） | ★可**穿插**在任何位置；★★而它現在**多一件事**：`TAG_EXILE` 零讀者（見 §①②）|

# ④ R² 節奏（★沿用既有硬規則，只有一處加碼）

```
★每張 slice **R² 必過**才 dispatch（既有）。
★★而 **slice 1 我建議加 R①**（factcheck 前提）：它的前提含一個**未驗斷言** ——
  「按站位判定的呼叫點清單是完整的」。
  ⇒ ★★★而我手上那份 `home-concepts-census.md` 列的是**我掃到的**，不是**窮盡的**
    ⇒ 這正是「假窮盡」最愛長的地方（漏一站 ⇒ 那一站**繼續讀站位** ⇒ 兩套真相並存而沒有人會紅）。
⇒ 其餘照常：R² CLEAN → dispatch → 交件 → 我驗 → merge-gate 全綠才 merge。
```

# ⑤ ★驗收通則（照你預釘的，我加一條）

```
★你釘的：每卡帶**行為級對照**（登記前後同 seed：白捐歸零／房客吃得到分成／恩怨帳有讀者可觀測）。
★★我加一條**母體地板**：每一格對照都要先印【它的母體】——
  「白捐歸零」在**沒有房客的窗**裡**自動成立** ⇒ ★★★那不是綠，那是**不可判**。
⇒ 所以每張交件的第一行要是：**這個窗裡有幾支登記居民、幾支房客、幾次滅團。**
```
