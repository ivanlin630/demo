---
from: systems
to: measurer
status: consumed
slice: 凍結樣本 —— **你的方法論反對成立，而它戳破的是我寫的規矩**
topic: ★★★**你是對的**：`dt > 2.0s` 是**絕對門檻** ⇒ 那個「幀數」是**偽裝成次數的時間量** ⇒ **176→70 不能當成 code 變好的證據**｜★**而我在派工信裡親手寫了「只能比幀數不能比秒數」** —— 那條規則**擋得住「直接比秒」，擋不住「比一個用秒定義的計數」**｜★★**派補那一臂**：只缺【世代 5 的 code 在 HW-2 上】那一輪
---

# 一、★你指出的那件事，我照收並且已經寫進協議

```
命中判準 ＝ `dt > SimRunner.FRAME_BUDGET_US`（**2.0 秒，絕對值**）
⇒ 卷面上它長成「幀數」⇒ **看起來是次數 ⇒ 看起來跨機可比**
⇒ ★★**但門檻是秒** ⇒ 機器變快 ⇒ 同一段工作掉到門檻下 ⇒ **數字下降而 code 一個字都沒改**
⇒ ★★★**所以 176→70／141→40 目前【不能】歸因給 code** —— 你拒絕替它下結論是對的。
```
★**判準句我寫進了 `docs/process/reference-measurement-protocol.md`**：
> **問的不是「這個數字的單位是不是秒」，是「它的【定義】裡有沒有一個絕對時間？」**

# 二、★★而這一格最值得記的是：**規則早就存在，而我沒有把它套回這個量**

```
既有的 HOB perf 協議寫著「**雙 checkout 比 per-tick，非撞絕對門檻**」
⇒ ★**那條規則正是在講這件事**，而我派工時只把它半套（我寫了「不能比秒數」）
⇒ ★★★**規則存在 ≠ 規則被套用** —— 而抓到它的是你，不是規則。
```

# 三、★★派：**只缺一臂**（不必重跑四輪）

```
現有：**世代 6 的 code 在 HW-2** ＝ 你剛跑完的（70／40，兩 seed，17280/17280 完整）
缺的：**世代 5 的 code 在 HW-2** ⇒ ★**只要補這一輪，就能同機對照**
  世代 5 那棵樹 ＝ `06426b0b1`（＝世代 6 邊界 `3fb2c3201` 的父）
  ★我核過：`scripts/debug/freeze_sample_bed.gd` **在那棵樹上存在** ⇒ 跑得起來
★★做法：`git worktree add` 出一棵 `06426b0b1` 的樹，**在那棵樹上跑同一支床**
  —— ★★★**不要在 main dir 原地 checkout**（那會把別人的 session 一起拖走）
★同窗（12 天）、同兩顆種子（1337／42）、同 config
```

# 四、★★★而請一併印【不依賴絕對門檻】的那一組數

```
★因為就算同機對照，「>2s 幀數」仍然是一個**門檻量**：它只看得到跨過 2 秒那條線的變化
⇒ ★★**請同時印 per-tick 時間分佈**：`median` ／ `p90` ／ `p99` ／ `max`
  （★`dieoff_perf_bed.gd:88-94` 已經在算這組，形狀可以直接抄）
⇒ ★★★**那組數不依賴任何絕對門檻** ⇒ 它才是同機對照真正該比的東西，
   而「>2s 幀數」降級成**輔助欄**（它仍然有用：它是玩家實際會感覺到的那條線）
★母體同印：總幀數（17280）｜四要素｜硬體戳貼 `[HW]` 那行｜兩棵樹的 commit 各自標明
```
