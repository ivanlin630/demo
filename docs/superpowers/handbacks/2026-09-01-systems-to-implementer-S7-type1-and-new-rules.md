---
from: systems
to: implementer
status: open
slice: S7-reconcile-type1
tier: probe
topic: ★派型①(名 vs 值)——★★而我照自己剛自綁的紀律先 grep 了:known_issues:653 已記一條同族(gather 命名是「取脈絡」而實為 mutator),所以這一票【從它開始】不是從零掃;★★★三條新規矩要你知道:新條目必帶回訪條件、開跑前先 grep known_issues、而我派票端也綁了同一條
---

# ★①已 grep `known_issues`：**有相關條目**（★這一行從今天起是我的票的必填欄）
```
:653  gather 命名/語義是「取脈絡」但【實為 mutator】（EWMA 推進＋cache 寫＋cadence 重排）
      ⇒ ★★這就是型①（名 vs 值）的一個【已記錄實例】
      ⇒ ★★★所以這一票【從它開始】,不是從零掃 —— 它同時是樣本也是判準的校準物
:56   resource_candidate「穿著 facility 的名字」⇒ ★同族候選,一併看
```

# ★★②型①要盤什麼
```
★型① ＝【名 vs 值】：常數/函式的名字所【宣稱】的東西 vs 它【實際】是什麼
★★三個已知實例當校準：
   ①CORVEE_DAYS = 3 而實際 18 天（★單位對、量值不對）
   ②HP_REGEN_PER_TICK 實為 per-cadence（★單位錯）
   ③gather 名為讀、實為寫（★★★動作性質錯 —— 而這一型最危險：讀寫之分決定能不能在觀測路徑上呼叫）
★母體先寫死再數（★而我今天已經看你示範兩次「名字列舉會發散」⇒ token 邊界，不是子字串）
```

# ★★★③這一型的判準比前兩型難，我先講死一件
```
★「名字宣稱什麼」是【詮釋】—— 兩個人可以讀出兩個答案
⇒ ★★所以只收【機械可判】的：名字裡含明確的單位詞/動作詞,而實際語意可查證
   例：*_DAYS / *_PER_TICK / *_TICKS（單位詞）；get_* / gather_* / peek_*（宣稱唯讀）
★★★而「名字取得不好」不算 —— 那是品味,不是缺陷
   ⇒ 判準：★若照名字寫 code 會寫出 bug ⇒ 算；★★只是不好聽 ⇒ 不算
```

# ★④三條新規矩（blueprint 立 2026-09-01，我已落地）
```
①★known_issues 新條目【必帶回訪條件】三選一：到期 token／觸發事件／量測窗
   ⇒ ★★缺則不算合格條目（模板已在該檔檔頭）
②★開跑前先 grep known_issues —— 已寫進 03_implementer.md
   ★★血證就是今天：你量的「製造 no-op 混三因」,:728 早就記著
③★★★而我把同一條綁在【派票端】：我的票必須有「已 grep known_issues：<結果>」一行
   ⇒ 理由：把義務放在派票端才有牙齒 —— 收票端要記得,派票端只要照模板
```
