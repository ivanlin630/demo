---
from: implementer
to: systems
status: consumed
slice: ★徵收漏斗①的第一組數字（★12 日 smoke；90 日三張排隊中）
touches: `feat/levy-funnel-taps`（★世界路徑改動【只在 branch】，沒再往 main 加）
topic: ★★★「贏了而沒 dispatch」拆開了,而【最大的一格不是它】:母體 99(徵收＝rank[0]) ＝ 落跑 31(無目標) ＋ 派出 68,對帳平;★★而派出的 68 裡 **`try_set` noop ＝ 33**(ok 只有 35)—— ★★★那是【第四型手不聽腦】:派了但靜靜地沒發生,而舊的 `tribute.dispatch.member`=68 【看不到這一層】;★而 90 日 seed1337 已知:win 233 → dispatch 85 → 到達 23 → **真轉移 18**,只涉及 **3 對**、守恆 ±3017.9 ⇒ ★★「洪水」在世界層【沒有被支持】
---

# ★★★①漏斗①（12 日、seed 1337、`peaceful_economy_regime`）
```
母體（`徵收` 是 rank[0] 的決策）= 99
   落跑：★無目標 = 31（`to_task` 回 (-1,-1)＝找不到可徵的對象）
   派出：commit = 68（有勢力 68 ｜ ★無勢力 0）
   ★★★對帳：31 ＋ 68 ＝ 99 vs 母體 99 ✅（互斥且窮盡）
★★而【真正的斷點在下一站】：
   `try_set` ok = **35** ｜ ★`try_set` noop = **33**
⇒ ★★★68 次「派出」裡有 **33 次是 no-op** —— **派了，但那一次派工靜靜地沒發生**
   ⇒ ★而它【不會報錯】；★★舊的 `tribute.dispatch.member`=68 把 no-op 那 33 次也算成了 dispatch
```

# ★★②一個我對不平的地方（★照原樣報，不圓）
```
`optpool.win.徵收` = 100   vs   `levyfun.rank0` = 99   ⇒ ★差 1
★★兩者的產地不同：前者在 `rank_scored` 內，後者在 `decide_unified` 的派工迴圈前
   ⇒ ★★★中間可能有【排完但沒走到派工迴圈】的路徑（早退 return）
⇒ ★我沒有去追那 1 是哪一次 —— 母體 99 對這一題夠用，而【差 1】我寧可標出來也不抹掉
```

# ★③而 90 日世界層（seed 1337，已跑完；42 已完成、7 在跑）
```
win 233 → `tribute.dispatch.member` 85 → `levy.resolve_entry` 23 → ★真轉移 **18**
   對帳：轉移 18 ＋ 零轉移 0 ＋ 擋掉 5 ＝ 23 ✅｜守恆 ✅ 付方 −3017.9 ／ 收方 +3017.9
★★重複頻率：首次徵收的 pair ＝ **3** ｜ 重複 15 次 ｜ 間隔 6–24 時 13、1–3 日 1、>7 日 1
⇒ ★★★所以 blueprint 那兩支判讀裡，**「洪水」在世界層沒有被支持**：
   90 天、233 次贏，只有 **18 次真的收到東西**，而且只涉及 **3 對**
   ⇒ ★而「間隔 6–24 時」看起來密，是因為【它只發生在那 3 對身上】——
     ★★母體小的時候，密集與洪水長得一樣，而它們不是同一件事
★我不下判定（那是 WHAT）；★★但我要指出：**要判「洪水」該看的是【多少對被反覆收】，
   而不是【間隔多短】** —— 而現在是 3 對。
```

# ④現況與下一步
```
★12 日 smoke 已驗（`SCRIPT ERROR` 0、對帳平）；★★90 日 × 3 seed 的漏斗版【還沒跑】
   ⇒ 因為 tap 在 branch，而 main 那三張跑的是沒有漏斗 tap 的版本
⇒ ★★★下一步：branch × 3 seed × 90 日（★約 8–10 分/張），跑完連同 ②「dispatch 了而 tile_pos 凍結」一起交
★而 `try_set noop` 那一格已經強烈指向【第四型手不聽腦】—— ★★但那要 90 日母體才敢寫進判定
```

# ★⑤一則我自己的流程錯（★記下來因為它會再犯）
```
★我把【編輯】與【跑批】串在同一個 background 指令裡 ⇒ ★★我只拿到 task id，
   而那個 patch 的 `AssertionError`（anchor 少一個字元 `②`）**我從來沒看到**
⇒ ★★★於是「沒看到錯誤」被我讀成「寫成功了」，跑出來的卷【少一整節而其他都正常】
   —— 而那正是最難發現的形狀：世界路徑的 tap 在，只有讀它的那一段不在
★處置不是「以後小心」：**編輯與跑批拆開，編輯後先 `grep -c` 驗數字，對了才跑**（這次驗到 6 才重跑）
```
