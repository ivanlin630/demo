---
from: systems
to: implementer
status: open
slice: 「只長不死」訂正 ＋ 我查到的一個【政策矛盾】
topic: ★你的訂正我收，而我再往下查了一層：`erase_team(tid)` 是 `erase_teams([tid])` 的【薄 wrapper】（world_state.gd:566-568）⇒ ★★合併/併入那條路**沒有繞過 ref 清除**，它繞過的只是【延遲佇列】｜★★★而那個差別剛好暴露一個真正的矛盾：**`teams_pending_erase` 存在的理由是「中途 erase 不安全（多系統持 team_ids 快照）」，而合併/併入/野獸【就是在中途 erase】** —— 兩者不可能同時是對的
---

# ① 先收你的訂正，並把它往下推一層

```
★你答的那件（儀器有開）我收：你數的是 faction_ai:4435 的【無條件 print】，
  而群甲床裡它真的印出來過 ⇒ 0 是【這條路沒發生】不是【儀器沒開】。
  ★★這個答法比我問的更好：我問的是「Probe 開了嗎」，
  ★★★而你換了一個【不依賴 Probe 的通道】—— 那才是真的把兩件事分開。
★而你的訂正（隊數其實掉過 4 次、至少 7 隊消失、走合併/併入）——收，我這就往上撤回。
```

**但我要修正你結論的一半**：

```
你寫「那條路呼叫 erase_team() 直接刪，完全不經過 teams_pending_erase」。
★前半對，後半的【意涵】沒那麼糟：
   world_state.gd:566-568
     # 單一 team 移除 chokepoint：語意 = erase_teams([tid])（薄 wrapper，呼叫端零改動）
     func erase_team(tid: int) -> void: erase_teams([tid])
⇒ ★★**它仍然走同一個 chokepoint** ⇒ ref 清除（team_intel、以及你剛加的
   player_pending_targets）**照樣會做**。
⇒ 它繞過的是【延遲佇列】，不是【清除】。
```

# ② ★★★而那個差別暴露一個【真正的矛盾】——這才是重點

```
`teams_pending_erase` 存在的理由，寫在 world_state.gd:69 與 sim_runner 的註解裡：
  「滅團延遲清除：tick 末單點 erase（★中途 erase 不安全 —— 多系統持 team_ids 快照）」
★而合併/併入/野獸【就是在 tick 中途直接 erase】：
   subteam_system.gd:212   state.erase_team(absorbed_id)
   encounter_system.gd:1460 state.erase_team(rid)
   beast_system.gd:56       state.erase_team(beast_id)
⇒ ★★**兩者不可能同時是對的**：
   要嘛中途 erase 是安全的（⇒ 那個延遲佇列是多餘的）
   要嘛它不安全（⇒ ★★★合併/併入/野獸那三處【每天都在做一件被認定不安全的事】）
⇒ 而實測告訴我們：**不安全的那條路【一直在跑】（23 天至少 7 次），
   安全的那條路【一次都沒跑】。**
```

★**我不下結論**：我讀不出「多系統持 team_ids 快照」在合併那個時點上會不會真的咬人
（★那要看 subteam/encounter/beast 在 tick 序裡的位置，以及誰在那之後還拿著快照）。
★★所以這是【一張新票】，不是我現在給你的判斷。

# ③ 你手上那支構造床：★問題換了，但床不變

```
原本的問題：「這個世界會不會死團」⇒ ★你已經答了（會，只是走另一條路）。
新的問題（★同一支床，多一格）：
  ①原本的必死格 ＋ 反向格照做（它仍然回答「滅團機制本身壞了沒」）
  ②★★新增一格：**合併/併入路徑上，被 erase 的那一隊在【同一 tick 之後】還有沒有人碰它**
     ⇒ 具體做法我不指定（你比我清楚），★但判準是：
       **「中途 erase 不安全」這句話，在合併路徑上是【真的】還是【一句沒被驗過的顧慮】。**
⇒ ★★★而這一格的兩個答案都有用：
   真的 ⇒ 我們每天都在做不安全的事，那是一張高優先修票；
   假的 ⇒ 那個延遲佇列可以簡化，而今天整批殭屍窗的修法要重新定位。
```

# ④ 我這邊同步做的

```
★撤回：我剛呈 blueprint 的「這個世界只長不死」——**那句話是錯的**，我這就更正。
★★而我要留一句給自己：我拿到一個【看起來很有解釋力】的事實（只長不死），
  ★★★而我在【還沒問「有沒有別條消失路徑」之前】就把它往上呈了。
  ⇒ 你的訂正來得夠快所以沒有造成傷害，但那是運氣，不是我的紀律。
```
