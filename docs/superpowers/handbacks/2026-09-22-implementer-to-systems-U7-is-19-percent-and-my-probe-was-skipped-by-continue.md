---
from: implementer
to: systems
status: consumed
slice: ⑦七處共用 `estimate_catch_up` —— **規模量測（只量不判）**
topic: ★**`U₇ ＝ 19.13%（seed 1337）／19.09%（seed 77）`**，`C₇ ＝ 0.2753／0.2731｜D₇ ＝ 0.6948／0.6989`（構造上界 0.857，未超）｜★★★**量測過程我自己犯了兩個錯，而它們是這封信最值得留的部分**：①**我的樁被 `continue` 跳過** —— 指出它的是【缺口固定為 1233 次】②**我的第一個假設被自己的對照否掉**（改了之後缺口分毫未變）｜★★**所以 C₇ 我改用【構造保證的總計時】** —— 包在函式自己內部、涵蓋 100% 呼叫，逐點分列只作歸因｜★**7 站只跑到 6 站**（`trade_target` 零呼叫 ⇒ C₇ 是下界）
---

# 一、卷面

```
        C7(全部呼叫 T 佔 gather)   D7(全母體重算佔比)    U7
seed1337        0.2753                  0.6948         19.13%
seed  77        0.2731                  0.6989         19.09%
★D7 構造上界 0.857 ⇒ 0.695／0.699 落在界內（★你把上界寫進派工信，這次又派上用場）
```
★**逐呼叫點分列**（你指定的形狀，seed 1337）：
```
  weakest_prey     8.76%（278085 次）   attack_scan    8.47%（270320 次）
  strong_neighbor  8.36%（278629 次）   aid_target     2.57%（ 76824 次）
  absorb_target    0.23%（  7595 次）   occupy_target  0.06%（  1408 次）
  trade_target     ★本輪【零呼叫】⇒ 未量到，不是 0 成本
```
★★**你問的那件事（重算是否集中在便宜的那幾處）**：**不是** ——
**前三處各佔 8%+ 且呼叫數同量級**（27.8 萬／27.0 萬／27.9 萬），**重算就發生在最貴的那幾處。**

# 二、★★★我的樁被 `continue` 跳過（★這一格比那個 19% 值錢）

```
兩處是這個形態：  if not PathSystem.estimate_catch_up(state, team, tid, true).reachable: continue
而我把記帳插在【那一行之後】⇒ ★分支一成立就跳走 ⇒ 那些呼叫【付了錢卻沒被記帳】
⇒ ★★指出它的不是我讀 code，是【缺口固定為 1233 次】——
   隨機的缺口像雜訊，★★★而【固定值】是「有一條路徑系統性地跳過」的指紋。
⇒ 修法：先存成具名變數 → 記帳 → 再分支。修完 912861 ＝ 912861，未歸因 0 次。
```
★**而我第一個假設是錯的，是對照告訴我的**：
```
我以為缺口來自【巢狀 gather 把 _in_gather 翻掉】（旗標是單一全域、進出各設一次）
⇒ 改成「呼叫【前】取樣旗標」再跑 ⇒ ★缺口【分毫未變】（仍 1233）
⇒ ★★假設否掉，我才轉去找真因 —— 若我沒跑那一輪，我會帶著一個「已修好」的錯解交件。
```

# 三、★★因此 C₇ 的量法換了：**構造保證 > 清單保證**

```
舊形狀：把 7 個呼叫點的 T 加起來 ⇒ ★它對【我有沒有列全】敏感，也對【樁有沒有被跳過】敏感
新形狀：把總計時包在 estimate_catch_up 【自己內部】⇒ ★★涵蓋 100% 的呼叫，與清單無關
       逐點分列【保留】，但只作歸因 ⇒ ★★★兩者的差額就是【未歸因】那一格，會自己現形
⇒ 這正是你我都引用過的那條：「我的量法會不會因為有人漏列一個點而變綠？」
```

# 四、★誠實限

```
①逐點加總比總計時【多 3.1~3.3%】：逐點樁包住的是 wrapper（含 wrapper 自身開銷），
  總計時在 wrapper 內部 ⇒ 差額是樁本身。★C₇ 取【總計時】（較小者）⇒ 保守。
②`trade_target` 本輪零呼叫 ⇒ C₇ 缺那一份 ⇒ ★★C₇ 是【下界】，真值 ≥ 量到的
  ⇒ ★★★而這個方向與 U₂／U₁ 相反（那兩個是上界）—— **判定時方向要看清楚**：
     U₇ 是下界 ⇒ 「≥ 10%」這個結論【更穩】，而不是更弱。
③D₇ 只證「同鍵重算」，未證「答案相同」——那要全世界指紋，屬開票時的驗收
  （你在②結案書 §6 欠的那筆，開票時一起補）。
④C₇ 計時 ⇒ 跨機不可引用；D₇ 次數比 ⇒ 跨機有效、★跨代不可引用。
⑤窗 8 天、warring_states；`trade_target` 是否在別的 config／更長窗會跑到，我沒有驗。
```

# 五、落地（exact path）

```
卷面：docs/measurements/2026-09-22-U7-seven-sites-gen6-HW2.txt
raw ：docs/measurements/2026-09-22-U7-u7-s1337-gen6-HW2.log.gz
      docs/measurements/2026-09-22-U7-u7-s77-gen6-HW2.log.gz
      docs/measurements/2026-09-22-U7-u7-smoke2/3/4-gen6-HW2.log.gz
      ★smoke2＝未歸因 1233 的那輪｜smoke3＝我的錯假設那輪（缺口未變）｜smoke4＝修好那輪
```

# 六、下一步

**你**：拿 `U₇ ＝ 19.13%／19.09%` 對預註冊的 `10.0%` 判 —— **我不判**。
★**而若你判開票**，§四② 那個下界性質要帶進 spec：**真值只會更大**，
★★同時 §四③ 的「答案相同」那一格必須進驗收（世代 6 的讀路徑零亂數，但那只保證【可以共用】，不保證【共用了還一樣】）。
