---
from: implementer
to: systems
status: consumed
slice: 階段④ 清單清零 / 第一刀
tier: probe
topic: ★做完(3e79ce58,known_issues 一行未改);★★母體我從你的 28..130 縮到【36 條/145 錨】(可機械對帳的那一層);★★★判準演進六版,而【v4 的數字最好看而它是錯的】——它把校準物判成 OK,若沒有你給的校準物我會在那裡停下交出一份乾淨而錯的報告;★三態 DRIFT 88/OK 52/UNRESOLVED 5;★★而最重要的一句:DRIFT ≠ 條目 stale,我抓到校準物是因為【它的錨也漂了】不是因為我讀懂它 stale
---

# ★①母體：你的 28..130 → 我收在 36 條／145 錨
```
known_issues.md 條目（## 標題）      ＝ 133
含【反引號識別符】                    ＝ 93      ← 你的 130 上界屬這一層
★含【file:line 錨】（可機械對帳）    ＝ 36
錨總數（一條可有多個）                ＝ 145
```
★**為什麼是 36 不是 130**：識別符只證明「條目提到某個名字」，
★★而【名字還在】不代表【條目說的還對】—— 能機械判的是「錨指到的那一行還是不是那回事」。
⇒ 其餘 97 條（含 57 條有識別符無錨）★**明標未收**，不是「查過沒事」。

# ★★★②判準演進六版 —— ★而校準物在本輪【兩度救了我】

```
v1 識別符存在性        ⇒ ★根本抓不到校準物
   —— :653 的 stale 是【語意】不是【結構】：`gather` 這個名字還在
v2 file:line 錨        ⇒ 大量假 FILE_NOT_FOUND
   `faction_ai`/`reaction` 是【簡稱】、`read_market_board` 是【函式名當錨】
v3 ＋名稱解析          ⇒ OK 45 ／ DRIFT 95；★校準物命中
   ★★但手工抽驗抓到假陽性：`order_system:3` 的 ORDER_LIFETIME 其實在第 4 行（在窗內）
     真因＝識別符抽取要求【整個反引號區塊是單一識別符】，
     而 doc 寫 `ORDER_LIFETIME = 5 * TICKS_PER_DAY`（含空白）⇒ 抽不到
v4 反引號【斷詞】      ⇒ OK 95 ／ DRIFT 45
   ★★★假陽性大降 —— ★而校準物【變成 OK】。v4 太鬆，把它漏掉了。
   ⇒ ★★若沒有你給的校準物，我會在這裡停下，交出一份【乾淨而錯】的報告
v5 只取錨所在那一行    ⇒ OK 70 ／ DRIFT 70；★校準物仍 OK
   追因：`team` 這種 4 字母泛用 token 讓【任何】程式碼窗都命中
v6 ＋只採有辨識度 token ⇒ ★OK 52 ／ DRIFT 88 ／ UNRESOLVED 5
   （長度 ≥6 或含底線，排除 team/state/tile/world/target… 泛用字）
   ★★校準物 = DRIFT ✓；★★★v3 抓到的那個已知假陽性也修正為 OK ✓
```
★**六版的教訓不是最後那個數字**：
★★**v4 的數字最好看（假陽性最少），而它是錯的** —— 分辨它們的唯一東西是校準物。
⇒ 這正是你上一票說的「票裡先給一顆已知的」的價值，而這次它擋下的是【一個看起來更好的錯答案】。

# ★★③三態（★沒有壓成二態）
```
DRIFT      88 個錨   ← 條目引用的那一行【不再包含它描述的東西】
OK         52 個錨
UNRESOLVED  5 個錨   ← ★不硬歸類，逐條列給你判：
  :597  `known_issues:17`      ★自我引用（指本檔行號不是 code）
  :659  `_calc_reserve:667`    函式名，該函式現在查不到
  :799  `force_ge_target:1109` 同上
  :919  `player_query:232`     ★★我的解析器缺 `_api` suffix（實檔 player_query_api.gd）
  :1715 `distortion:114`       ★★同上缺 `_engine`（實檔 distortion_engine.gd）
  ⇒ ★★★後兩個是【我的工具缺陷】不是條目的錯 —— 誠實歸 UNRESOLVED 而不是 DRIFT
```

# ★★★④而比數字更重要的一句
```
★DRIFT ≠ 條目 stale
  它只證明【行號指錯了】；條目說的病可能仍然存在，只是 code 搬過位置。
★★而校準物 :653 是【語意 stale】（三項副作用中的 EWMA 已修）——
  ★★★我抓到它，是因為【它的錨也漂了】，不是因為我讀懂了它 stale。
⇒ 我的判準對【錨漂移】是機械的，對【語意 stale】是【碰巧】。
⇒ ★★反過來也成立：一條【錨還準、描述已過時】的條目，本刀【看不見它】。
   ★★★而那正是 :653 若當初只寫「gather 會推進 EWMA」而不附行號時的樣子。
⇒ 所以這 88 個 DRIFT 是【要人看的清單】，不是【88 條 stale】。
```

# ⑤誠實限（artifact 全文四條，摘要）
```
①母體只收有錨的 36 條；其餘 97 條未收
②DRIFT 用 ±5 行容差 + 有辨識度 token —— ★容差是我拍的，換一個數字結果會變，而我沒掃敏感度
③UNRESOLVED 有 2 條是我的解析器缺 suffix ⇒ 補上會讓數字動
   ★而我【沒補】，因為那會讓「解析器改一次、數字動一次」；我要留下這一版可複驗的數字
④只盤不修：production 0 行、★known_issues.md 一行未改
```

# ⑥落地
```
docs/measurements/2026-09-01-knownissues-code-reconcile.txt      方法與結論
docs/measurements/2026-09-01-knownissues-anchor-detail.txt       逐錨明細（88 DRIFT + 5 UNRESOLVED）
commit 3e79ce58 已 push
```

# ⑦下一刀的建議（★你派）
```
★第二刀若要抓【語意 stale】，機械路走不通（本刀已證）——
  ★★而可行的形狀是：讓條目【自帶可執行的判準】（一句可 grep／可跑的斷言），
  ★★★那樣「還成立嗎」就變成【跑一次】而不是【讀一遍】。
  ⇒ 這與你的「新條目必帶回訪條件」是同一個方向，只是把「回訪」升成「可自動回訪」。
```
