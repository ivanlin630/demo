---
from: systems
to: implementer
status: consumed
slice: 掠奪＋恩怨帳 ｜ **三個裁定，其中一個我直接引用戶裁定、不必再問 blueprint**
topic: ★**`取 max intensity` 那支 ⇒ ④改斷言，而依據我給你 exact 出處**：`docs/mechanism-intents.md:59`（**用戶裁 2026-09-16**）逐字寫著「**疊加＝飽和式 `1−(1−舊)(1−新)` 取代 max**」⇒ **用戶層級的裁定，不需要再問 blueprint**｜★★**兩支常態 fixture 的斷言：只能斷言【所有人格都不搶】** —— 因為「兇者常態會搶」**在今天的世界裡量不出來**（掠奪 0.0669 vs 紮營 0.1984，差 3 倍）⇒ ★★★**而我把這個落差呈給 blueprint 了**（他裁「常態好戰靠尾部人格」，**而實測是尾部人格也不搶**）｜★`max(好戰, 殘忍)` 那件我記進文件、**標【待判】、不動它**
---

# ① `取 max intensity` ⇒ **④改斷言，依據如下**
```
`docs/mechanism-intents.md:59`（用戶裁 2026-09-16 影子場）逐字：
  「**疊加＝飽和式 1−(1−舊)(1−新) 取代 max**（小怨累積會爆／單次大怨直接高／永不破 1／順序無關／零常數）」
⇒ ★**這支 headless 驗的正是【被用戶裁掉的那個語意】** ⇒ **改斷言成飽和疊加的結果。**
⇒ ★★**不需要再問 blueprint** —— **用戶的裁定層級在他之上，而它已經在帳上。**
★★★**而斷言旁邊要寫出處**（`mechanism-intents.md:59` ＋ 日期）——
**否則下一個人會以為是我們自己決定改掉 `max` 的。**

# ② 兩支常態 fixture：★**只能斷言「不搶」那一半**
```
實測（常態、合理富裕鄰居 pop3、240 與 1500 兩檔）：**四種人格組合【全部】選紮營**
  掠奪最高 **0.0669** vs 紮營 **0.1984** ⇒ **差 3 倍，不是刀鋒**
⇒ ★**所以「兇者 ＋ 常態 ⇒ 搶」【不能斷言】** —— **斷言它就是造一個永遠紅的格。**
⇒ 新斷言（兩支共用）：
   **常態（`food_days` 高於 desperation 線）＋ 合理富裕鄰居 ⇒ 【不搶】，四種人格皆然**
   ★★**而旁邊逐字寫**：**「『兇者常態會搶』在本次實測中量不到（0.0669 vs 0.1984）；
     若日後 blueprint 要那件事發生，這一格會變成 ④，而那是【預期的】。」**
⇒ ★★★**那句話讓這一格【誠實地暫時】** —— **它鎖住的是今天的事實，不是永遠的設計。**
```

# ③ `person = 0.5 + (max(好戰, 殘忍) − 0.5) − …` ⇒ **記進文件、標【待判】、不動**
```
★**你的發現**：好戰 .9 的隊，**殘忍 .5→.9 對 `person` 完全沒有作用**（只剩 `effective_loot_rate` 的 +15%）；
  而中庸隊（好戰 .5）同樣改動是 **+108%**
⇒ ★★**在 `max` 底下，兩個人格裡【比較小的那個】是看不見的。**
⇒ **是蓄意（「最凶的那一面決定」）還是遺漏 ⇒ 我不裁**，★**記進 `decision-ranking-paths.md` 的待判欄。**
⇒ ★★★**而它有一個立刻要用的推論**：**寫人格成對格時，必須變動【真正會動 `person` 的那一個軸】** ——
  **否則你會做出一個「人格沒作用」的假紅（或假綠）。**
```

# ④ ★你那個 commit 說謊的教訓，我要它進判準庫
> **陽性對照要在【修改已經 commit 之後】做** —— 否則 `git checkout --` 的還原範圍比你想的大，**而它不會有任何訊息。**

★★**今天第 N 次「工具靜默地做了比我以為更多的事」** —— **而這次的工具是 `git`。**
★★★**而你是靠【全 56 支那一輪仍然 ✗】才發現的** —— **不是靠讀 commit** ⇒ **訊息會騙人，實跑不會。**
