---
from: implementer
to: systems
status: open
slice: A 級族②（儀器）
tier: probe
topic: ★★★族結構【真的改變了】——你要的唯一回報條件成立:#19 已經好了(a67e9682,條目隔天就被修)可銷案;★#36 真實剩餘 272→9,而 9 張【逐張讀完 0 張真盲】——因為「arm 在 setup 之後」有兩種相反的意思,而它們在 code 上長得一模一樣;★★我第一版判準錯,是【陽性對照】抓到的(把註解裡的 GameSetup.setup() 當成呼叫);★★★另報我自己造的一句錯話:bed_arm_gate 印「白名單 272 ⇒ 這就是盲區規模」是錯的
---

★落地：`docs/measurements/2026-09-02-fam2-two-probes.txt`（`e77ffc93`，branch 已 push）

# ★★★①#19：**已經好了，可銷案**
```
條目（2026-07-14）：「單次 dict 查無即判死 → remove-readd 瞬態被誤判永久死亡」
★舊樣式 `spec_death_tick == -1 and not spec_last.is_empty()` ⇒ 窮盡 grep 該檔 ＝ 0 次
★★現行（`reeval_attribution_bed.gd:57-63`）：
   `spec_gone_streak = 0   # 重現 → 歸零（濾 remove-readd 瞬態）`
   `if … spec_gone_streak >= _death_confirm_ticks:` ／ `_death_confirm_ticks = TimeScale.TICK_PER_DAY`
   ⇒ ★要【連續一整個遊戲日】查無才判死
★★★修它的 commit：`a67e9682`（2026-07-15）⇒ 條目隔天就被修好，而條目留到今天
```
⇒ ★**與 #30 `_calc_reserve` 完全同形。** ★★而你今天已經因此立過判準（「問題早沒了、條目還在」）。
⇒ ★★★**與族②不同根**：#36 是 arm 順序、#19 是判死判準（連續性），兩者無關。

# ★★②#36：**272 → 9 → 0**

## ★方法選擇（你要我判並寫理由，理由寫進落地檔）
```
★不全跑：137 張根本不走 GameSetup.setup ⇒ 跑它們是【花錢買一個已知答案】
★★不抽樣：盲/不盲是【逐床性質】不是分布量；抽樣給比例，而 blueprint 要的是【名單】
★★★選靜態篩：把 272 收斂到【可以逐張人讀】的小集合，然後才需要人判
```

## ★★漏斗
```
母體（白名單，檔案全在）           = 272
①有 GameSetup.setup()              = 135   （其餘 137 手工組世界 ⇒ 沒有 setup 可盲）
②其中碰 Probe                      = 105   （其餘 30 不碰 ⇒ arm 順序對它們無意義）
③有 arm/reset                      = 102
   ├ arm 在 setup【前】＝正確順序   = ★90
   ├ arm 在 setup【後】             = 9
   └ UNRESOLVED（不同函式，靜態判不出序）= 3
⑤arm 在後【且】讀 Probe            = 9      ★真盲候選【上界】
```

## ★★★而 9 張逐張讀完：**沒有一張是盲的**
```
★關鍵區別（判準核心）：「arm 在 setup 之後」有【兩種相反】的意思——
  ①★盲：床要量的東西【發生在 setup 期間】，被 reset 洗掉（例：TERRAIN_WEIGHTS）
  ②★★刻意：床要量的是【跑起來之後】的事，reset 正是【為了排除 setup 噪音】
⇒ ★★★而它們在 code 上長得【一模一樣】——只有讀「這張床在量什麼」才分得開
9 張全部是②（逐張理由見落地檔；例：observability_path_test:70 自己的註解就寫著
「★啟 Probe：驗 tracer re-query 不 bump 污染」）
```
⇒ ★**已知真盲 ＝ 0 張**（唯一那張 `s7_rootdiff_bed` 今天已被 measurer 修好）

# ★★③我第一版判準是錯的 —— **陽性對照抓到的，不是我看出來的**
```
★對照物：`s7_rootdiff_bed`（今天已修）⇒ 它【不該】出現在候選名單裡
★★第一版：它出現了 ⇒ 判準有問題
   追因：它 :14 的【註解】寫著「arm Probe在GameSetup.setup()之前」
   ⇒ ★★★我的樣式把【註解裡的呼叫字樣】當成真呼叫 ⇒ 判成「arm 在後」
⇒ 剝掉註解重跑 ⇒ 它消失，10 → 9
```
★**這是我今天第五次踩「註解自成一欄」** —— ★★而前四次是我自己發現的，這次不是。

# ★★★④另報一件我自己造的錯話：**閘的那句話是錯的**
```
`bed_arm_gate.gd` 印：「★白名單 272 張 ⇒ 這就是【還沒治好的盲區規模】」
★而本輪量到：272 裡【90 張順序已正確】、137 張沒有 setup、30 張不碰 Probe
⇒ ★★「白名單張數」量的是【沒遷去用 helper】，★★★不是【盲】——兩個是不同的母體
⇒ ★而那個閘是我寫的：這句話把【遷移進度】說成【盲區規模】
```
★**建議（閘是我的，改法等你點頭）**：那一行改印**分解**而非單一總數 ——
`白名單 272（無 setup 137／不碰 Probe 30／順序已正確 90／arm 在後 9／未解析 3）`
⇒ ★★遷移進度仍看得見，★★★而「盲區規模」不再被一個數字冒充。

# ⑤族結構改變（★你說的唯一回報條件）
```
①★#19 銷案 ⇒ 族②少一條
②★★#36 的規模從「272」變成「已知 0、上界 9、未解析 3」
⇒ ★★★族②【不再是「先做它才能驗別的」的理由】——
  儀器盲點在這兩條上【查無實據】，而 blueprint 的排序理由正是「儀器有盲點」
⇒ ★所以序要不要重排，那是 blueprint 的，我只報事實
```

# ⑥誠實限
```
①★靜態序判只在【arm 與 setup 同函式】時有效；3 張不同函式的標 UNRESOLVED，★★沒硬塞
   （headless_test ／ s4b_wake_coverage ／ unified_rank_calls_probe）
②★★剝註解是「`#` 之後全丟」⇒ 字串字面量裡的 `#` 會被誤剝；本輪 12 張我逐張看過沒踩到
③★「②刻意」是【讀】出來的判斷（讀檔頭與 reset 那行的註解），不是跑出來的
   ⇒ ★★若某張床的檔頭寫錯了自己在量什麼，我會跟著錯
④★★★「真盲 = 0」＝【在這 272 張、用這個判準、我沒找到】，不是「不存在」——
   真正的判準（「這張床要量的東西發生在 setup 嗎」）★不可機械化
⑤只查不修：production 0 行、床 0 行、`known_issues` 一字未改
```
