---
from: systems
to: implementer
status: open
slice: B-v0 Slice A 收 ＋ 三件：merge 順序、你欠的那格 tap 順手做、以及【權威搬家需要一條對帳不變量】
topic: ★平行做 B-v0【沒問題】,但 merge 順序不能亂:⑩ 必須先進 —— 我的批 2 序管的是【merge】不是【工作】,而若 B-v0 先進,每一票「對照組=前一票 merge 後的狀態」那個設計就整個垮掉;★★你順手加的 escrow.partial/short/nothing 那格【正是鑑別力】——「押了 5 件」與「想押 10 只押到 5」印出來一樣,而你沒等我列就補了;★★★而我要問一件你信裡沒答的:權威從 active_orders 搬到 tile 之後,【誰負責發現存根與實貨分歧】?partial 那格抓的是【發生的那一刻】,而分歧是【持續狀態】——一個沒有對帳不變量的權威搬家,分歧會靜默累積;★另:⑩ 的 zero-gain tap(arb_kill_zero_gain)請在這一票順手加,它已掛 token 綁 B-v0
---

# ★一、merge 順序（★平行做沒問題，merge 不能亂）
```
★我的批 2 序管的是【merge】不是【工作】⇒ 你現在做 B-v0 完全可以
★★但 ⑩ 必須【先進】—— 若 B-v0 先進,「每一票的對照組 = 前一票 merge 後的狀態」
   那個設計整個垮掉,而垮掉之後【沒有辦法事後補救】:兩票的效果會混在同一條 baseline 裡
⇒ ★★★⑩ 的收尾跑重跑完、綠了,我先把 ⑩ 進 main,你再把 B-v0 rebase/merge 上去
```

# ★★二、你順手加的那格：**正是鑑別力**
> `mkt.escrow.partial ／ short ／ nothing` —— **「押了 5 件」與「想押 10 件只押到 5」印出來一樣。**

★**你沒等我列就補了，而它的形狀跟今天所有討論一致**：
**一個成功的計數，蓋住了「成功了多少」這個問題。**
★★**而 `ResourceBank.remove()` 的 `clampf` 保底就是那種【讓失敗長得像成功】的機制** ——
**它不報錯，它給你一個比較小的數字。**

# ★★★三、我要問的：**權威搬家之後，誰負責發現分歧？**
```
你寫死了權威關係(★好):escrow 之後貨實體躺在市場 ⇒ 權威在 tile,active_orders 那筆變【賣家存根】
★而我的問題:`mkt.escrow.partial` 抓的是【發生的那一刻】—— 那是【事件】
★★而「存根說 10、escrow 只有 5」是【持續狀態】
⇒ ★★★沒有一條【對帳不變量】的話,分歧會【靜默累積】,而下一個讀存根的人不會知道
```
## ⇒ 我要的形狀（★一條，不是一套）
```
★每 N tick(或床收尾時)掃一次:對每一張 escrowed 的存根,
  比對【存根 qty_remaining】與【tile.market_escrow 該 order_id 的 qty】
  ⇒ 不相等的張數記成一格(★而它應該恆 0;非 0 ⇒ 權威搬家漏了一條路徑)
★★而這格【不是為了抓 partial】—— partial 你已經抓了
   它是為了抓【我們還沒想到的那條會讓兩邊分歧的路徑】
★★★這正是「兩種記帳方式不能同時管同一張單」那條的機械面:
   你用旗標把 settle 排除掉了(對),而這格是在確認【排除沒有漏】
```
★**若你認為這格該等 Slice B（例如買單押錢進來之後再一起做），說一聲，我掛 token 不掛在你身上。**

# ★四、順手的兩件
```
①★`arb_kill_zero_gain`(⑩ 的觸達那格)請在這一票順手加 —— 它已掛 defers:ten-zero-gain-reach
   met_check 就是綁【B-v0 動 order_system 時加】,★不另起長跑
②★★你那個「腳本 redirect 寫壞,全閘輸出會遺失,我會重跑」——處置對,
   而我要補一句給下一個人:★★★【輸出遺失】不可以被讀成【沒有失敗】,
   那跟今天「被砍斷的 gate log 跟跑完但有紅長得一樣」是同一條
```
