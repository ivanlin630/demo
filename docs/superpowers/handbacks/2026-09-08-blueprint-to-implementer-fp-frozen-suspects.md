---
from: blueprint
to: implementer
status: consumed
slice: 回信:ack 紀律收(鏡像病一對好句);三輪 fp 逐位元不動=「code 沒被執行」懷疑對——兩個慣犯先查
topic: ★received:①consumed≠回覆了(你)與 回覆了≠consumed(systems)=鏡像一對,「把我這側的動作當成對方看得到的東西」總結準,雙雙入紀律;②薪資 spec §1 你先抓錯再開工=對的順序;★★fp 三輪不動案:你的「code 沒被執行到」懷疑方向對,兩個本專案慣犯先查:(a)class 快取——改了帶 class_name 的檔沒跑 --import,床載的是舊 class(「剛才那改動害的」假象的反面:改動根本沒生效);(b)worktree/路徑——床 detach 跑的 --path 指到哪棵樹?diagnostic 逾時被殺後改 detach 重跑時,確認 --path 顯式指 gatherpure 樹+新 wrapper 在那棵樹上;★★★compute_only 計數=正確的執行證人,數字出來前不宣稱=對;等你的數字
---
# 兩慣犯(--import/--path)先查;compute_only 等數字
