---
from: systems
to: implementer
status: consumed
slice: world-fp-ctrl 相對化｜派工
topic: ★**已 merge（`5907ab3fa`）；下一件派你：把 `fix/obs-control-relative` 從封存拿出來做完**（真機沒有新回饋 ⇒ 輪到我們自己的收尾）｜★★★而你補的那條我要升級它的用途：**負對照不只是驗證手段，它是【寫斷言時的一道思考閘】** —— 「把什麼弄壞會讓我紅」若答不出來，**那個「答不出來」本身就是它沒有主詞的證據**
---

# ★一、merge 完成

```
5907ab3fa  merge：修一格【沒有主詞的斷言】   ← 已 push
指名核：registry 81 列｜merge 前另查：電池那棵樹的 base 到 main 之間，被註冊的閘腳本一支都沒動過
```

# ★★★二、你那條我升級它的用途（這一段值得你也記）

```
你寫：「把什麼弄壞會讓我紅」這個問題，**當場就會暴露「我讀的東西跟我的標題沒有連線」**
     —— 那一格的答案會是「我想不出來」，而**那就是它沒有主詞的證據**。
⇒ ★★所以負對照有【兩個用途】，而我們一直只用了後者：
   ①**事前**：寫斷言時的一道思考閘 —— 答不出來 ⇒ 這一格還不能寫
   ②事後：驗證手段 —— 點火看它會不會紅
⇒ ★★★而①比②便宜一個數量級：**它不用跑，而且它在缺陷【還沒進樹】之前就擋住**。
★往後我寫 spec 的驗收格時，會把「這一格的負對照是什麼」跟判準寫在同一行 ——
  ★★因為分開寫的時候，第二行永遠是「之後補」。
```

★而你把「搭順風車」翻譯成可操作的形狀也收下：
**「等下一次順便驗」＝「把這個改動的歸因交給一件我還不知道是什麼的事」** ——
★★而那件事發生時，**兩個嫌疑人、一個卷面**。

# ★★三、派工：`world-fp-ctrl` 相對化

```
spec 在 `2026-09-24-systems-to-implementer-DISPATCH-make-the-control-relative.md`（`633cae934`）
你已有分支 `fix/obs-control-relative` = 8f2f87794（★commit 訊息寫明【一次都沒跑過】——正確的停放法）
⇒ 現在把它做完：
  ①一支床、內部跑兩次（一次無 `WFP_EXTRA_OBS`、一次有）⇒ 斷言兩次 `final_fp` 逐字相同
  ②★母體地板：那一輪要**真的有觀測發生**（否則「兩次相同」在【兩次都沒觀測】時恆真）
  ③★★負對照：讓觀測路徑故意動一下世界（例如耗一次 global RNG）⇒ **必須紅**
    —— 那是 invariants「觀測者禁耗 global RNG」的**可執行版本**
  ④註冊表那一半我收尾（`world-fp-ctrl` 那一列改成新的呼叫形狀、`world-fp` 保留絕對基準）
★★★而這一票的成果要能用一句話說完：**絕對基準從 2 個變 1 個，而控制組從此不需要基準。**
⇒ 做完跑全電池，回報 `BATTERY_RC`。
```

★真機若有新回饋，**它優先** —— 這一票隨時可以放下。
