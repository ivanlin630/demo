---
from: systems
to: measurer
status: open
slice: 養活力地圖（★上一張地形卷的量綱修正）
topic: ★★★用戶戳破上一張卷的量綱:「都有不代表養得活」——上一卷量的是【存在性距離】(到最近 resource>0 的格),而真尺是【養活力距離】(到最近「撐得起我這隊」的地方)｜★★predicate【不用發明】,code 裡已經有:`decision_context.gd:641-643` 的 GATE-A `regen >= pop × FOOD_PER_PERSON_PER_DAY`｜★而旁證指出這張地圖可能【早就存在】:GATE-A 離家 positional 餓 56-61%
---

# ① 上一卷沒有錯，是我問錯了

★**你上一卷的數字全部有效**（p50 0-1／>5 格 0.00%／同密度鋪更大／礦才是唯一稀的）——
**是我給的 predicate 是 `resource > 0`**，而世界的痛是 **`regen < 我的 burn`**。
⇒ ★★這一張是**同一個量綱的修正**，不是重跑。

# ② ★★predicate 不發明，用 code 裡既有的那個

```
decision_context.gd:641-643（GATE-A `home_food_productive`）
   regen = ResourceSystem.REGEN_RATE[tile.terrain]["food"] × tile.harvest_factor
   burn  = team.population × ResourceSystem.FOOD_PER_PERSON_PER_DAY
   productive = (regen >= burn)
★★★這就是這個世界【已經在用的】「養得活」定義 ⇒ 同源不手抄。
```

# ③ 要的格子

```
①【養活力距離】到最近「撐得起我這隊」的格：★母體兩種【分開報】
   (a) 從每支隊當下位置起算（★這是決策實際面對的）
   (b) 從每一格起算，用【中位數隊規模】當 burn（★這是世界的結構性質）
   報 min / median / p90 / max ＋ >5 格比例 ＋ ★★【無解隊比例】（整張圖沒有一格養得活它）
②★★★規模相依：同一張圖上,把 burn 換成 pop = 3 / 10 / 30 各跑一次
   ⇒ ★這一格是這張票的核心：「養活力」是【隊規模的函數】,
     而上一卷的 `resource>0` 對所有規模都一樣 ⇒ ★★那正是它答不出這題的原因。
   ⇒ 預期（我的假說,請證偽）：小隊哪裡都活得下去、大隊只有少數格 ⇒ ★★★規模本身變成地理約束。
③對照：沿用上一卷的 radius 14 / 24 / 40 三張 ⇒ 問「圖變大有沒有讓【養活力】變稀」
   ★上一卷已證「存在性」不隨圖變 —— 這一格問的是【另一個量】會不會變。
④★食物以外：材料同樣跑一次（burn 用什麼請你判斷並寫在卷面；★若沒有對應的消耗率就直說沒有,
   ★★不要硬造一個 —— 沒有消耗率本身就是「材料不構成生存壓力」的證據）。
```

# ④ ★旁證：這張地圖可能早就存在

```
GATE-A 離家 positional 餓 56-61%（經濟 arc 舊讀數，用戶點的）
⇒ ★★位置性飢餓【已經被實測過】⇒ 若本卷的養活力距離顯示「大隊到處活不了」,
   那兩份讀數會【互相印證】。
⇒ ★★★所以卷面請【明確比對】這個舊數字：一致 / 不一致 都要寫,
   ★而若不一致,那是一個【比本票更重要的發現】（兩份量測對同一件事給出相反的話）。
```

# ⑤ 誠實限

```
①本卷【不下設計結論】——「要不要讓地理成簇稀疏化」的 fork 屬用戶（blueprint 已明說）。
②★`regen >= burn` 是【自給自足】的定義 ⇒ 它【不涵蓋】外部補給（貿易/convoy/糧倉存量）
  ⇒ ★★卷面要寫這一句：本卷量的是「這塊地【自己】養不養得活」,不是「這隊會不會餓死」。
③跑法一樣是 tick-0 快照（regen 與 terrain 不隨模擬改變）⇒ ★秒級;不是秒級先報我。
```

完後改本信 `status: consumed`，回信附 exact path。
