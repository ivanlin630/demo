---
from: systems
to: blueprint
status: open
slice: ⑦merge 回報 ＋ **一個沒人問過的分母**
topic: ★**merged `69c00dbdb` → 合併點 `4c52a6cdd`；我在【合併結果】上自己重跑全套 68 支全綠**｜★★**淨省 14.26%／15.95%（毛值 19.13% 扣掉查表成本後）**｜★★★**而我順手補了一件今天三張票都沒人問的事：那個「%」的分母是什麼** —— **`gather` ＝ 整場 tick 時間的 47.6%** ⇒ 我們一整天用的那條 10% 門檻，真身是 **4.76% 的整場**
---

# 一、merge 回報

```
69c00dbdb → main（合併點 4c52a6cdd，已在 origin）
★我在【合併結果】上自己重跑全套：68 支 ✓68 ✗0｜375s｜rc=0
  —— implementer 那 68 支跑在他 branch，而 main 已往前 6 顆 ⇒ **不是同一棵樹**
★★七格預註冊驗收全綠；A2 淨省 **14.26%（1337）／15.95%（77）** ≥ 10.0%
★★★而驗收 A3 抓到的是**我 spec 的洞**（不是實作的錯），我已在票身 §9 認下來：
   我寫「正確性完全不依賴清空路徑」，**而那句話預設了「每次呼叫都在某次 gather 裡面」**
   ⇒ 漏掉第三格【不在任何一次裡面】⇒ 實測 6134 次拿到過期答案。**二分法少一格，老毛病。**
```

# 二、★★★而我要講的是這個：**那個「%」的分母，今天沒有人問過**

```
今天三張票的門檻全是「**佔 gather** 的百分比」——
而那個單位是從更早那張票**繼承**下來的，**沒有人回頭問過 gather 是整場的多少**。
★我去算了（用已落地的驗收 log，不必重跑）：
   八天逐日 tick 總時：8.2／14.4／18.6／18.8／22.7／28.8／32.8／42.8 秒 ＝ **187.14 秒**
   gather.* 合計 ＝ **89.01 秒**
⇒ ★★**`gather` ＝ 整場 tick 時間的 47.6%**
```
**換算今天每一個判定**：
```
                        佔 gather    →   佔整場
  ★我們用的門檻 10.0%      10.00%         4.76%
  U₂（已結案）              8.39%         3.99%
  U₁（已結案）              9.44%         4.49%
  U₇ 毛值                  19.13%         9.10%
  ★★U₇ 實測淨值（已 merge） 14.26%        6.78%
```
★★★**所以這張票買到的是【整場 tick 時間的 6.78%】，而我們整天用的那條線其實是【4.76% 的整場】。**

# 三、★這件事為什麼值得你知道（而不是只當雜訊）

```
①**單位是繼承來的，不是選出來的** —— 它一路傳了三張票，而沒有人重新問過它指向什麼
   ⇒ ★而我今天自己還寫了兩次「★這一格最容易被講反」，**卻沒查過自己在用的分母**
②**gather 佔了將近一半** ⇒ 它不是一個角落，它是**最大的一塊**
   ⇒ ★★剩下沒收的那 6134 次（gather 外）就坐在這 47.6% 旁邊，而它**看起來已經解決了**
③★★★**我不建議現在改門檻** —— 改了就不是預註冊的門檻了（那正是今天立的規矩）
   ⇒ 建議：**下一張票的預註冊，單位直接寫「佔整場 tick 時間」**，並把換算率一起寫進去
```

# 四、★誠實限

```
①一顆種子、一個 config（warring_states）、8 天窗、**Probe 開**（計時需要樁，樁本身 ~1.36%）
②「整場 tick 時間」＝ 逐日 `[TickPerf] avg × ticks` 加總 ⇒ **不含 setup／world gen**
③★早期天數便宜很多（8.2 秒 → 42.8 秒，隊數 68 → 103）
  ⇒ **窗越長，gather 的佔比還會往上走** ⇒ 47.6% 是這個窗的數，不是常數
```
