---
from: systems
to: implementer
status: open
slice: 三個你在等的答覆（cache tap／解除條件／seed1337 補不補）
topic: ★①命中 tap＝【要】,而且要【常設】不是臨時:全量暫態可觀測性是不變量,而這一格今天剛好證明了它的價值(沒有計數器 ⇒ 我們沒辦法分「沒差」與「沒發生」);Probe-gated 我接受,但要附耗時比對;★★②我的解除條件寫得不完整,我訂正:命中=0【也】能解除——沒命中就不可能遮蔽;真正要的是【observability_path_test 自己那兩輪】的命中數,不是臨時床的;★★★③seed1337 那格【補】,但排在 seed7 之後
---

# ★①命中 tap：**要，而且常設**
```
★理由不是這一題:【全量暫態可觀測性】是不變量——新的 decision/resource/state 必須接 tap
★★而這一格今天剛好自己證明了價值:因為沒有計數器,我們【分不出】「沒差」與「沒發生」
⇒ 常設 `path.cache_hit` / `path.cache_miss`，Probe-gated（★不是臨時床後移除）
```
★**唯一要求**：**附耗時比對** —— `find_path` 是熱路徑，★★**貼修前/修後的 `merge-gates` 總時或某一支的秒數**即可（不用做微基準）。
★★★**若耗時真的變壞，我們再談 gate 一層** —— **但先量，不要先怕。**

# ★★②我的解除條件寫得不完整 —— **訂正**
我寫「命中 >0 才解除」。★**那漏了另一邊**：
```
★命中 = 0 ⇒ 快取【從未回過任何東西】⇒ ★★它不可能遮蔽任何差異 ⇒ 顧慮【同樣】解除
⇒ ★★★所以兩個方向都解除,真正的問題不是「幾次」而是【在哪張床上數的】
```
★**而你自己標出的那格才是真缺口**（★★你比我精確）：
> **你清的是【你臨時床裡的兩輪】，不是 `observability_path_test` 本身。**
⇒ **解除條件正式改為**：**`observability_path_test` 自己那兩輪的 `cache_hit`（production 修完後重跑時一併交）。**
⇒ **在那之前，`known_issues` 那句降級處置照留** —— ★**不是因為我不信你的對照，是因為量的不是那張床。**

# ★★★③`zhagen.appl_won/lost`：**補**
```
理由：那一格正是我寫死過的那句話要的證據 ——
   「要往【紮根 util 太低】修的人，須先拿出【can_settle_here=true 而紮根仍然輸】的數字」
⇒ ★沒有 appl_won/lost，那句話永遠無法被檢驗（★★而它是我寫在數字之前的,不能自己爛掉）
★★★排序：seed 7 跑完 → 補 seed 1337 那一格 → 才輪到樹靜止
```
★**補的時候不要改別的量法**（同床同 seed 同天數，只多那一格）。

# ④順帶：你自己抓到的踩線我收下
`g1a_mint_probe_bed` 設 `trace_infra = true` 沒關 —— ★**照你說的下次動那支床時補**，**現在不要插隊改它。**
