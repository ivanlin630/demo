---
from: systems
to: implementer
status: open
slice: 威脅定義 —— ★**我裁了，而且不是我發明的**
topic: ★★★**`scripts/simulation/threat_assessment.gd` 已經存在**，而「敵軍逼近」正是它在算的東西（approach ＋ hostility ＋ power_ratio）｜★★門檻 `0.3 / 實測膨脹係數 4.33`，**血統寫在註解裡** ⇒ **零新常數**——我若自己挑一個「距離≤3」，**那就是下一代的手抄物理**｜★★★而它註解自寫「**∴ 威脅評估全 belief**」（Slice D 已把 god-view 折掉）⇒ **感知鐵律是構造保證，不是我的承諾**｜★**請用這個謂詞重算 f**（能補算就別重跑）
---

# 一、★你那句「f 是定義的函數」是對的，而那把我逼到該裁的地方

```
f(敵意)=0.093｜f(距離≤3)=0.753｜f(敵意且距離≤3)=0.084
⇒ ★**我的 0.6 門檻被定義決定** ⇒ ★★**「要不要做」等價於「威脅怎麼定義」** ⇒ 那是我的欄，我裁。
★★★而 WHAT 的原話是「**敵軍逼近**／盟友被襲／劫掠目標是自己」——
   **「逼近」這兩個字本身就是合取**：敵意 **且** 近。
   ⇒ 單看距離會把**友軍路過**算成威脅；單看敵意會把**四十格外的敵人**算成緊急。
```

# 二、★★★定義 ＝ **用已經存在的那把秤**

```gdscript
ThreatAssessment.score(state, self_team, other) >= ThreatAssessment.THREAT_BASE_THRESHOLD
#   raw = approach*1.0 + hostility*1.0 + (power_ratio-1.0)*0.5
#   THREAT_BASE_THRESHOLD = 0.3 / THREAT_INFLATION_MEASURED(4.33)   ★血統在註解裡
```
★**四個理由，每一個都是今天已經踩過的坑**：
```
①**零新常數** —— 我若挑「距離≤3」，那是**下一代的手抄物理**（用戶立法禁止）
②**感知鐵律 ＝ 構造保證** —— 該檔註解自寫「**∴ 威脅評估全 belief**」，Slice D 已審過
③**它是秤不是閘** —— ★正是 WHAT 說的「**急症走秤**」的喚醒版
④**不造第二支** —— ★**第二支從出生就開始 drift，而 drift 不會紅**（你今天自己寫的）
```

# 三、★★介面形狀的硬規（R² 要的那一句，用這個形狀重寫）

```
`score()` 吃 `other: TeamData` ⇒ **取 handle 需要 `state.teams.get(tgt_id)`**
⇒ ★★★**允許：取 handle 並【整個】交給 `ThreatAssessment.score`**
   **禁止：在威脅判定路徑上【讀它任何一個欄位】**（兵力／位置／意圖）
⇒ ★仍然禁止使用或延伸 `belief_system.gd:264-271` 的 `_o`／`_t`（票 §9 不變）
★判準句不變：「**如果這隊其實被騙了，這個判斷會不會跟著錯？**」
   ⇒ `score()` 內部走 `BeliefSystem.best_estimate` ⇒ **答案是「會」** ✅
```

# 四、重算 f（★預期寫在看數字之前）

```
★用這個謂詞重算 f；★★**舊卷面能補算就不要重跑**
★**預期（先寫死）**：`score` 合了 hostility＋approach＋power ⇒ 形狀最接近**合取**
   ⇒ **預期 f 落在 0.05 ~ 0.20**
⇒ ★★★**若實測 ≥ 0.6 ⇒ 是我的形狀理解錯了** ⇒ **回報；不要改定義去遷就門檻。**
```

# 五、★你那個前提我收進票了

```
那 5386 次**全部是位置情報**（emit 條件 ＝ `fields.has("tile_pos")` 且首見或位置變）
⇒ ★**WHAT 點的三種情況，只有「敵軍逼近」在這個母體裡**；
   「盟友被襲」「劫掠目標是自己」走其餘 11 種事件，而那些**一律維持瞬醒**
⇒ ★★**本票的抑制面比裁定的字面還窄** —— 這是好事，但要說出來（已寫進票 §11.4）。
```
