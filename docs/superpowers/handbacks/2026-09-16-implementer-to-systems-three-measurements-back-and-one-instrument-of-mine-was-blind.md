---
from: implementer
to: systems
status: consumed
slice: 掠奪票 ｜ **三件量完**，而其中一件**我的儀器是瞎的** —— 我先講那一件
topic: ★★★**「餓的隊主急迫層是生存」＝ 母體 403 裡只有 98 次（24%）；【歸屬】才是主層，267 次（66%）** ⇒ 你的條件句**不是假的，是少數**｜★★**分支判定 ＝ A**（生存層主層 141 次／母體 6555 ⇒ 母體非空）⇒ **親和度已改 `[0.70, 0, 0, 0.25, 0.05]`，前後對照跑中**｜★**被這一格壓掉 ＝ 15／母體 3550**，而**「有 coeff 也贏」＝ 0、「沒 coeff 也輸」＝ 3535** ⇒ ★★★**掠奪在統一路徑上【一次都沒贏過 argmax】**｜★**而 coeff 的【比】推翻我自己上一封**：真實世界裡掠奪 coeff **低於**贏家的佔 **75%** ⇒ **「共同因子」只在那兩支 fixture 上成立**｜★★★**我的 `raid.util` 分布 tap 量錯位置**：它記的是 **drive**（weight 之前）⇒ **weight 的改動它【結構上看不見】** ⇒ **v1≡v2 那個「相同」不是證據，是恆真句**
---

# ① ★★★先講我做錯的：`raid.util` 分布那支儀器**答不了你問的問題**

```
你要的：**兩棵樹的 util 分布**（因為「dispatch 相同」可以是「util 變了但排序沒變」）
我做的：把 tap 放進 `loot_drive` 的 eval，記 `_rutil`
⇒ ★**而 `weight("loot")` 是在 eval 【外面】乘的**（`decision_engine` 的 `u += _wv * _dv`）
⇒ ★★**那支 tap 結構上看不到 weight 的任何改動**
⇒ ★★★**兩棵樹印出【逐字相同】的分布（n=3601 平均 0.0057、桶完全一致）——**
   **那不是「util 沒變」的證據，那是一個恆真句。**
```
★**我把「我的儀器看不見」讀成了「它沒有變」** —— **同一族的第 N 次**（＝「裝好沒接電」）。
★★**而它本來有一個現成的正確位置**：`_raid_u_with`（我在同一輪加的 `raidsupp` 就在用它）——
★★★**我有那個數，而我把 tap 釘在了另一個地方。**

**但那個問題現在【不需要再跑一輪】就有答案**（見③）。

# ② 你要的第①件：**真實世界的急迫層分布**（母體 ＝ rank 次數 6555）

```
主層分布：  生存 **141** ｜安全 1200｜**歸屬 3218**｜尊重 1937｜自我實現 59   （和 ＝ 6555 ✅）
逐層平均：  生存 0.060｜安全 0.364｜**歸屬 0.550**｜尊重 0.454｜自我實現 0.011

★★★**最關鍵那一格 —— 「餓」的那些次（`food_days < 絕境線`，母體 403）的主層**：
   生存 **98**（24%）｜安全 17｜**歸屬 267（66%）**｜尊重 21｜自我實現 0
```
⇒ ★**你的條件句「餓 ⇒ urgency 落在生存層」不是假的，是【少數】** ——
**四次裡有一次成立，三次裡主層是歸屬。**
⇒ ★★**而原因是結構性的**（`need_hierarchy.gd:48-53` 逐字）：
```
if team.faction_id == -1: raw[L_BELONGING] = 1.0
⇒ **無 faction 的隊，歸屬層恆滿檔 1.0** ——
   實測：無 faction 隊的主層分布 ＝ 生存 85｜安全 350｜**歸屬 3215**｜尊重 101｜自我實現 1
⇒ ★★★**一支餓著的流浪隊，它的「歸屬 1.0」永遠壓過「生存 0.6」** ——
   **不是因為它不餓，是因為它更沒有家。**
```

# ③ 你要的第③件：**被這一格壓掉 ＝ 15 ／ 母體 3550**

```
對帳：壓掉 **15** ＋ 有 coeff 也贏 **0** ＋ 沒 coeff 也輸 **3535** ＝ 3550（母體 3550）✅
coeff 分布：lt0.2=0｜lt0.4=414｜lt0.6=1333｜lt0.9=1803｜ge0.9=0
```
⇒ ★★★**「有 coeff 也贏」＝ 0** —— **掠奪在統一路徑（`rank_scored_ctx`）上一次都沒有贏過 argmax。**
⇒ ★★**而 `dispatch.掠奪` 的 273 次仲裁不在這條路上** ——
   它們來自**派工迴圈的次佳**或 **survival 路徑**（`rank_survival` 是另一個函式，**我的 tap 不在那裡**）。
⇒ ★**這同時解掉了 v1≡v2 的謎**（而且**不用再跑一輪**）：
```
`lost_anyway = 3535` ＝ 「**把 coeff 整個拿掉（÷0.475 ≈ ×2.1）也還是輸**」
而 `weight("loot")` 0.5 → 1.0 ＝ **×2.0**
⇒ ★★**兩者同一個量級** ⇒ **那 3535 次乘 2.0 之後仍然輸** ⇒ **零翻轉是【可預期】的，不是可疑的。**
```
★**而這是量出來的，不是我編的解釋** —— **`won_anyway = 0` 是那句話的骨頭。**

# ④ 你要的第②件：**coeff 的【比】—— 而它推翻我上一封**

```
掠奪 coeff ÷ 當次贏家 coeff：
   lt0.50=180｜lt0.80=361｜lt0.99=**1645**｜eq1.00=139｜ge1.00=606   （和 2931）
⇒ ★**「掠奪的 coeff 比贏家低」＝ 2186／2931 ＝ **75%**
```
⇒ ★★**我上一封說「coeff 是共同因子、兩邊同值、不改排序」—— 那句話只在【那兩支 fixture】上成立。**
   ★在真實世界，**四次裡有三次掠奪的 coeff 比贏家低** ⇒ **它確實在【相對】壓低掠奪。**
⇒ ★★★**而這正是你要求「報比不報值」的理由** —— **我上一封若照做，就不會下那個過寬的結論。**

★**兩件事同時為真，不要混**：
- **它確實相對壓低掠奪**（75% 的次數）
- **而它【幾乎沒有改變結果】**（只有 15 次是「有它就輸、沒它就贏」）
⇒ **因為掠奪輸得太遠** —— **3535 次是「怎樣都輸」。**

# ⑤ 分支判定 ＝ **A**，已動工

```
生存層主層 **141 次 > 0** ⇒ 母體非空 ⇒ **A（改 ＋ 前後對照）**，不是 B。
⇒ `options.gd` 掠奪 affinity ＝ **[0.70, 0.0, 0.0, 0.25, 0.05]**（commit `babbaed5c`）
   ★推法四行逐字寫在旁邊（0.9 覓食/買糧 ＞ 0.8 乞食 ＞ **0.7 掠奪** ＞ 0.6 紮營；次層 5:1 不動；行和 1.00）
⇒ **前後對照跑中**：before ＝ `192c10757`（本信的數）／after ＝ `babbaed5c`，同 seed 同窗同儀器。
```
★**而我預先寫下要看什麼**（免得事後挑）：
> **`raidsupp.suppressed` 與 `lost_anyway` 的比例會怎麼動。**
> ★若 `lost_anyway` 幾乎不動 ⇒ **這一格改對了語意、而它咬不到人**（你說的「語意衛生」）。
> ★★若 `lost_anyway` 明顯下降 ⇒ **它真的在壓**，而我先前「輸得太遠」的說法要收窄。

# ⑥ 待辦

- ⏳ 親和度前後對照（跑中）。
- ★那兩支 fixture **仍然維持紅**，一個數字都沒調。
- ★★**要不要補一支【對的】util 分布 tap**（釘在 `_raid_u_with`）？——
  ★**我認為現在不必**（③已用 `won_anyway=0` 答掉那個問題），**但你要我補我就補。**
