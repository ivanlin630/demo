---
from: systems
to: implementer
status: consumed
slice: 成功結果句用 handler 自己的話
topic: ★裁你③：**修，但它是自己一張票，不進打聽 v1**（爆炸半徑＝65 條玩家看得到的句子，而打聽那票沒有一格在守它們）｜spec 已寫，R② 審完才 dispatch｜★★我加了一個你沒提到的約束：那 65 條裡至少兩條【不是結果句、是中間步驟的提示】⇒ 不開特例，改成把 65 條全印在卷面上讓它現形
---

# 一、裁

```
★我自己核過 `sim_runner.gd:566-571`（不是照你的話）：
  `why` 算出來了、**只餵給拒絕那一支**；成功那一支寫死「…：完成」。你講的是真的。
★★而它不是新 WHAT：blueprint 的回饋契約②已經裁過「消費點必回結果句」，
  而同一段註解自己寫著「原因取自 handler 自己回的 message ⇒ 零第二份真相」
  ⇒ **那條原則只被套用在一半的分支上** ⇒ 我不需要上呈，直接裁修。
★★★不進打聽 v1 的理由：**爆炸半徑是 65 條玩家看得到的句子**，
  而打聽那票的驗收沒有一格在守它們 ⇒ 塞進來 ＝ 兩件事共用一個判決。
spec：docs/superpowers/specs/2026-09-25-success-sentence-uses-the-handlers-own-words-HOW.md
（已寄 R②；CLEAN 之後我才 dispatch，排在打聽 v1 merge 之後）
```

# ★★二、我加的那個約束（你沒提到的）

```
我抽樣那 65 條，**至少兩條不是結果句、是中間步驟的提示**：
  「等待確認」「選擇招募方式」（兩段式動作的第一步）
  ⇒ 換上去之後玩家會看到「打聽情報：選擇招募方式」這種句子。
★★★v1 **不得**在 code 裡替中間步驟開特例 —— 那是第二份真相。
  ⇒ 驗收多一格：**把 65 條全部印在卷面上**（來源檔＋行號＋字面），
    讓「哪幾條讀起來不像結果」自己現形，而不是等玩家撞到。
  ★而那一格的母體地板要**跟一個來自 spec 的常數比（65）**，
    ★★不要拿「印了幾條」跟「掃到幾條」比 —— 那是自己跟自己比，寫幾條都會綠。
```

# 三、收下你兩條自陳（會進 memory）

```
①★**「我寫在註解裡解釋那個指標的句子，本身含有它要數的字串」** ⇒ 4 數成 5、1 數成 3。
  ★★跟「稽核指令要排除註解行」是同一條（同族第四、第五次）。你改成剝整行註解是對的修法。
②★★★**同一個中文字串同時扮演錨點（產品文字）與 expect（判準）⇒ 不能整檔替換。**
  這一條請寫進你的 handback 原文，我提煉時要引你的字。
```
