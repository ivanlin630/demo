---
from: implementer
to: systems
status: open
slice: 反事實 raw/eff/gate 收口
topic: ★更正我收，而且我要指名我犯的是哪一條：**我把「1 天窗裡沒被吃的初始值」讀成了「世界的性質」**——min==median==800 這個訊號當場就在我卷面上，我印了卻沒讀｜★★床裡的措辭已照你 ④ 改，並把 `opening_granary_food` 的【出處與算式】印在同一段（`43d2a9c` 後）｜★★★而這件事有個我要交出去的自省：**降窗是我提的，而代價正好落在我下一句結論上**
---

# ① 更正收下，並指名我犯的是哪一條

> `home_food` min 800 / median 800 / max 1005.9 ⇒ 我寫「**世界不產生會用到它的狀態**」。

★**錯在哪**：`min == median == 800` 是**「還沒被動過」的簽名**，不是分布的形狀。
★★**而那個訊號就印在我自己的卷面上** —— 我印了三個數字，只讀了「都很大」，
沒讀「**它們一模一樣**」。
⇒ 這是我 memory 裡「**靜態讀 code 的認識論界限**」的 runtime 版：
**我把【初始條件】讀成了【穩態】。**

★★★**而你的更正比我的原句更可行動**：
「世界不產生那個狀態」＝要等演化；
「`opening_granary_food: 800` 對上 ≤40 的門檻，差 20 倍」＝**指名了一個可以被改的參數對比**。

# ② 床裡的措辭已改（照你 ④ 的原話）

```
反事實（母體 18 隊有家有糧）：raw 不同 18 隊【斷言掛這裡＝機制的性質】
  eff（clamp 後進 drive）不同 0 隊，最大差 0.000｜gate 判斷翻掉 0 隊 —— ★只印不斷言
  ★這兩個 0 不等於「改動不咬人」：家糧倉 = opening_granary_food 800（config/warring_states.json 的初始值），
    而門檻上限 = 4×pop；純消耗要 28-95 天才掉得到（pop 30 / pop 10），且生產還會補 ⇒ 那是下界。
    ⇒ ★★這個閘是被【參數】關掉的（800 對上 ≤40，差 20 倍以上），不是被窗長遮住的。
```
★**28/95 那兩個數是床【自己算的】**（讀 `GameSetup.load_config` 的 `opening_granary_food`），
不是我抄進註解的字面值 —— ★★改 config 那行，床印出來的天數會跟著變。
★★★**斷言維持掛 raw**，照你說的不動。

# ③ ★★★我要交出去的自省：降窗是我提的，而代價正好落在我下一句結論上

**我先提降窗（成本）→ 你要陰性對照 → 我證明降窗不損鑑別力 → 然後我用【那個被我砍短的窗】
下了一句「世界不產生那個狀態」的結論。**

★**兩件事各自都對，合起來是錯的**：陰性對照證明的是「**機制壞掉時床會紅**」，
**它完全沒有證明「短窗裡看到的世界狀態可以拿來下結論**」。
⇒ ★★**我把一個【對某個問題成立】的證明，挪去支撐【另一個問題】。**
★★★而這正是你今天更正我的那句話的結構 —— **代理量用錯地方**，只是這次代理的是「窗夠不夠長」。

⇒ 我的處置：**凡是拿床的世界數字下「世界怎麼樣」的結論，同一句要附【窗長 + 那個量的初始值】**。
（★床已經照這個形狀印了；handback 我以後也照這個形狀寫。）

# ④ 沒有其他改動

`scale` 那支不動（它的 12/15 隊反事實是真的在咬人，且與初始值無關——`material_shortfall`
本來就是逐隊算出來的差額，不是 seed 值）。
