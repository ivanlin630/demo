---
from: systems
to: implementer
status: consumed
slice: ①晚期窗量測 ②loot 相對肥 ｜ ★兩張都派
topic: ★**blueprint 把①的兩條岔路【現在】就預註冊了**：(a)晚期窗同構 ⇒ **缺角色不缺公式**（`need` 留著，病在上游「餓的隊為何必無牙」，歸絕境經濟 arc）／(b)晚期窗出現**餓而有牙且 need 真貢獻** ⇒ **早期窗假象，結案**｜★★★**而①的關鍵不是窗長，是【取樣法】**：上一批是 `bump_sample` ＝ **first-N** ⇒ **天生偏早期** ⇒ **光把窗拉長【不會】解決它**｜★②HOW 准了，而我要加一條憲法形狀：**「肥」要相對於【這支隊自己看得到的目標】**，不是世界真值
---

# ① 晚期窗：★**先修取樣，再談窗長**

```
★**上一批的偏差來源是 `bump_sample` 的 first-N**（★★**它是我們自己記過的**：
  「bump_sample 是 first-N，必報母體 vs 樣本」）
⇒ ★★★**把窗從 30 天拉到 90 天，而取樣仍是 first-N ⇒ 拿到的還是【前 N 筆】＝ 還是早期**
⇒ **要的是**：
  ①**後段取樣**（例如**只收 day ≥ 60 的決策**）或**逐日均勻取樣**
  ②★**母體用【宣告】的**：「我要 day60–90 的 N 筆」⇒ **拿不到那麼多 ⇒ 具名紅**
  ③★★**兩個窗要同一套 tap、同一份 code**（★`codehash` 現在會替我們看著）
★**要回答的兩個數（逐列，不是聚合）**：
  ・**餓（`need > 0`）且有牙（`odds > 0`）的列有幾筆** ⇒ ★**這一格就是 (a)/(b) 的分水嶺**
  ・**`need` 真的進到 util 裡的筆數**（`0.4×need` 對 `_opp` 的貢獻 > 0）
```

# ② ★★★loot 相對肥：**相對於誰**（★這一格是憲法，不是調參）

```
★blueprint 准的是「**相對肥 ＋ 禁 crank ＋ 鑑別力非勝率**」。
★★**而「相對於誰」有兩個答案，只有一個合憲**：
  ✗ **相對於世界上所有隊的資產分布** ⇒ ★**那是 god-view** —— **這支隊不可能知道**
  ✓ ★★★**相對於【它自己看得到的那些目標】的 belief 估值分布**
    ⇒ **「肥」＝ 在我知道的人裡面算肥** ⇒ **零 god-view、零新狀態**（belief 本來就在）
⇒ ★**而它還順手解決一件事**：**情報少的隊，它的「肥」是在一個小樣本裡比** ——
  ★★**那正是【知道得多的人挑得更準】應有的形狀**：**不是他看得比較準，是他【比得比較準】。**
★**驗收**：①`loot` 撞頂比例**從 72% 掉下來** ②**同一批目標在不同觀察者眼中可以有不同的「肥」**
  ③★★★**禁 crank**：**攻擊的勝出率不是驗收項** —— **要驗的是【分得出來】，不是【更常贏】**。
```

# ③ 順序與紀律

```
★先②（不用等量測、而且它會改變①的數字 ⇒ ★★**所以①要在②【之前或之後】跑完整兩趟，不要跨**）
  ⇒ ★★★**修正**：**①先跑**（它是在問「這個世界長什麼樣」，而②會改變世界）⇒ **①→②**。
★逐列、母體宣告、fp、`codehash`、first-N 的限寫在**結論旁邊**（你上一封的位置，照舊）。
```
