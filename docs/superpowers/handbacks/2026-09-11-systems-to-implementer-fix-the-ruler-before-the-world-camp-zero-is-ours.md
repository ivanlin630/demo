---
from: systems
to: implementer
status: open
slice: 遠行到場率 ｜ ★先修尺，再判世界
topic: ★★★**紮營 0% 不用查了，我查到了**：`faction_ai_system.gd:6297-6300` ＋ `:6442-6447` —— **紮營成功 ＝ 腳下那格能立營（全函式沒有 `move_target`）**，走到半路就立營 + release ⇒ 床的 `tile_pos == move_target` **永遠不會成立**｜★所以那個 0 **是真的 0，而它答的是另一個問題**｜★★**動作**：①紮營**退出母體** ②**每個任務用它自己的成功判準**，不是一個全域到場判準 ③信使 8.9% **同一個嫌疑**（訊息可能半路就送到）⇒ 先驗再列
---

# ① 已坐實的一格（★你不用重查，直接照改）

```
`:6297-6300`  if team.current_task == TASK_CAMP: if establish_crude_camp(...): release(); return
`:6442-6447`  establish_crude_camp() 只看 **腳下 `team.tile_pos`**（無主／無營／非山地）
              ⇒ ★**全函式沒有出現 `move_target`**
⇒ 紮營隊**在半路立營**就算成功 ⇒ ★床的到場判準對它**不適用** ⇒ 0% 是尺的產物。
```

# ② 要改的（★三步，順序有意義）

```
①**紮營退出「長程到場率」母體** —— ★並在表上寫「**判準不適用**」，
  ★★**不要寫 0%**：0% 與「不適用」在表上長得一樣，而它們會導致完全不同的下一步。
②★★★**逐 task 的成功判準**（這是本信的重點）：
  ・貿易 → 走到交易點？（★驗：它的結算讀不讀 `tile_pos == move_target`）
  ・外交／信使 → **訊息送達**可能發生在**半路同格相遇** ⇒ ★若是，到場率對它也不適用
  ・求居 → 走到村子 ⇒ ★這個**應該**真的要到場（本票的驗收就靠它）
  ⇒ ★每一項**貼 file:line**：**成功是在哪一行被判定的**。
③改完**重印一次全類表** —— ★而那張表才是可以拿去當「世界生命徵象」的基線。
```

# ③ ★而這件事的一般形狀（★寫進床的檔頭，下一個人會再踩）

```
★**一個全域的成功判準套在成功條件不同的任務上，會產出看起來很嚇人的 0%。**
★★而它最壞的性質是：**去查儀器會發現儀器好端端的** ——
  數字是真的、床沒壞、母體也不是空的 ⇒ ★★★**它只是回答了另一個問題**。
⇒ 床的檔頭要寫一行：**「到場」的定義是什麼、以及它對哪些 task 不適用。**
```

# ④ 不變的（★免得這封信被讀成改方向）

```
①需求化優先序那一刀**照做**（`options.gd:563-565` 類別查表 → 需求導出）
②它落地後**同 seed 重量全類到場率** ＝ 免費的成對實驗（blueprint 裁，不另開刀）
③本票驗收①仍然從**漏斗床**讀（`try_set` ok／noop ／真的走到），不是從這張到場率表。
```
