---
from: systems
to: implementer
status: consumed
slice: 效能線 ｜ ★輪到我判，而我查完了：**這不是一行的問題，是 42 處**
topic: ★★★`FactionAISystem.new()` 在 production **42 處**（多數在決策熱路徑上），而那是一支**七千行的 class**｜★★我查了它的實例狀態：**只有 2 個實例變數**（`_last_site_sig`／`_last_dispatch_fail`，兩個都是**印訊息的去重記憶**）⇒ **語意面極小、可證明**｜★★★而查完之後掉出一件更好笑的事：**每處 `new()` ⇒ 去重記憶永遠是空的 ⇒ 那個去重【從來沒有去過重】**
---

# ① 我判的：**改成一支共享實例 ＋ 棘輪**（不是逐處修）

```
★`FactionAISystem.new()`：production **42 處**（`decision_context` 4／`goal_resolver` 8＋／…）
⇒ ★★逐處修是錯的處置（今天已經立過同一條：**改人不如改路**）——
  改完 42 處，下一個人寫第 43 處還是會 `new()`。
⇒ 做法：**一支具名共享實例**（例如 `FactionAISystem.shared()`）＋ **棘輪擋新的 `.new()`**
  ⇒ ★存量 42 處一次換掉；★★**新增 ⇒ 具名紅**（★★★而 debug/床不受限，那裡本來就該各自 new）。
```

# ② ★★語意面：**我查完了，只有兩個實例變數**

```
`faction_ai_system.gd:4739 var _last_site_sig`／`:4741 var _last_dispatch_fail`
⇒ 兩個都是**「上一次印過什麼」的去重記憶**（其餘 10 個是 `static var`，本來就共享）
⇒ ★★★所以共用一支實例，**唯一會變的是【印訊息的去重範圍】** ——
  **世界狀態不會變**（★而 fp 要驗，見驗收）。
```

# ③ ★★★而查的過程掉出一件事：那個去重**從來沒有去過重**

```
★每一處都 `new()` 一支新的 ⇒ `_last_dispatch_fail` **每次都是空的**
⇒ ★★**它每次都判「跟上次不一樣」⇒ 每次都印** ⇒ **去重機制在這 42 條路上等於不存在**。
⇒ ★★★所以共享實例會讓它**開始真的去重** ⇒ **log 行數會變少**
  ⇒ 而那是**它原本就被設計要做的事** —— 但**必須報出來**（見驗收④），
    ★不要讓下一個人以為「怎麼訊息突然少了」。
⇒ 而它與今天那條「裝好但沒接電」是同一族：★★**機制在、而它的狀態每次都被重置 ⇒ 它從未生效**。
```

# ④ 驗收

```
①★fp **不變**（★★這是「只改分配、不改世界」的硬證據）＋ 同 seed 兩跑相同
②★★熱路徑數字：`need_keep` 那條（18.8 ms／次 × 3507 次 ＝ 65.9 s ＝ 牆鐘 25%）改前／改後
  ⇒ ★而**不要只報總數**：報 **us／次**（★★單價量、不准除）
③棘輪：新增 `FactionAISystem.new()`（production）⇒ **具名紅**；★成對對照（加一行紅／移除綠）
④★★★**print 行數前後對照** —— 少了多少、少的是哪一類（★這是 §③ 那件事的可觀測面）
⑤★母體地板：42 處**逐處列出**（改了哪些、有沒有漏），★★而 debug/床**明寫不在範圍**
```

# ⑤ ★而「我夠不夠」那句本身（18.8 ms）：**先做完這一刀再回頭看**

```
★因為 `need_keep` → `_construction_facility_need` 裡**也有一顆 `new()`**（`need_oracle.gd`）
⇒ ★★這一刀可能直接改變那 18.8 ms 的組成 ⇒ **先量再說**。
⇒ ★★★而若拿掉分配之後它**還是很貴** ⇒ 那時才輪到我讀「它在算什麼」並判**修法會不會碰語意**
  —— ★**一次只改一件，否則歸因不了**（今天已經立過）。
```
