---
from: implementer
to: systems
status: open
slice: 半守衛普查 —— 分類（★機械代理草稿）
topic: ★數字：86 處 → **L 75／M 3／G 3／? 5** ⇒ ★★「半個守衛」不是三次個案，是【這個 codebase 的預設寫法】：**87% 的 `state.teams.has(` 守完之後真的對那支隊做事**｜★★★而我把這一欄【明寫成機械代理判的草稿】，並把兩個已知限度直接放進 [?]：①跨函式（守衛在 `_check_*`／API、真正做事的是呼叫端）②只比較／記帳｜★逐處人工覆核【還沒做】—— 這份是給你先看數量級的
---

# ① 數字

```
母體 86 處（docs/process/teams-has-callsites-raw.txt 有每處上下文三行）
  [L] 存活守衛（守完之後對那支隊做事）    75   ★87%
  [M] 守衛＋取值（同一運算式就讀它）        3   ★修法不同：要拆成兩步
  [G] 純存在問句（不再對它做事）            3
  [?] 判不出來                              5
表：docs/process/teams-has-callsites.tsv（欄：site／line／class／reason，每列附證據行）
```

# ② ★★這個數字本身就是一個發現

```
「半個守衛」今天現形三次（vision:27／interaction:245／你 R² 那輪點到的）
⇒ ★而 86 處裡有 75 處是同一個形狀 ⇒ ★★它不是三個個案，是【預設寫法】。
⇒ ★★★所以修法的形狀也要跟著換：逐處改 75 行是【打地鼠】，
   而真正的問題是**「取一支隊來用」這件事沒有一個【正確的入口】**
   —— 現在每個人都自己寫 `has()` ＋ `state.teams[x]` 兩步。
⇒ 我的建議（★不是我要做，是給你排）：先給一個 `state.live_team(tid) -> TeamData`
   （回 null 代表「不可用」，把 has ＋ pending 檢查 ＋ 取值收成一步）
   ⇒ 之後那 75 處是【逐步遷移】，而新寫的 code 一開始就走對的入口。
```

# ③ ★★★而我要標清楚這份分類的身份

```
它是【機械代理判出來的草稿】，不是逐處人工判讀。
代理 ＝ 守衛之後，同一函式內這個 id 有沒有被①取出那支隊②當參數派出去 ⇒ 有 ＝ L
★兩個已知限度我沒有藏起來，直接放進 [?]（5 處）：
  ①【跨函式】守衛在 `_check_controlled_team`／`_check_player_with_team` 這種函式裡，
    ★★真正對那支隊做事的是【呼叫端】⇒ 我的工具只看同一函式內 ⇒ 判不了
  ②【只比較／記帳】id 還出現但沒被取出來做事（`c.intent_target = _prey` 這種）⇒ 語意問題
★★★而我沒有把它們塞進 L 讓數字更好看 —— 那正是你說「一份沒有 [?] 的分類是可疑的」在防的事。
```

# ④ 我沒做

```
★沒有修任何一處（照你的紀律：後果逐處不同，混一張票會變成無法驗收的大改）。
★★逐處人工覆核也沒做 —— 若你要，我可以做，但建議【先決定修法形狀】：
   若走 §② 那個 live_team 入口，逐處覆核的價值會低很多（因為遷移本身會強迫逐處看一次）。
```
