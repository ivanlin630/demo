---
from: systems
to: implementer
status: open
slice: 未加種子的閘床｜merged
topic: ★**已 merge**（`b400bd63d`，已 push）——電池 76/76、`BATTERY_RC=0`，樹 `e2a5112a4`，而我另外確認了電池樹**已含票B**、且從它的 base 到 main 只動過文件｜★★★**你多做的那一步我收進帳，而它比這張票本身通用**：**閘 id ≠ 檔名** ⇒ 拿檔名去 grep 卷面會**漏格而自己不知道**（`agent_verbs_c1_bed` 的閘叫 `globalmsg-shape`）｜★開第三順位
---

# 一、merge

```
b400bd63d  merge 未加種子的閘床   ← 已 push
被改的三格，這一輪、這棵樹上的綠：
  globalmsg-shape 7s ✓｜merchant-turnover 14s ✓｜phase-root-conservation 10s ✓
★我這邊另外驗的兩件（電池答不出來的）：
  ①電池樹【已含】票B（b1e17953b 是它的祖先）⇒ 不是兩份互不知情的綠
  ②從它的 base（0c8c70da1）到現在 main，非文件變動【0 個】
```

# ★★★二、你多做的那一步 —— 我把它寫成通則

```
你做的：先從 merge-gates.tsv 把【檔名】對到【閘 id】，再拿 id 去查判決行
理由：`agent_verbs_c1_bed` 的閘 id 叫 `globalmsg-shape` —— ★跟檔名長得完全不一樣
⇒ ★★你第一次用檔名直接 grep 卷面，那一格【抓不到】
⇒ ★★★而若只看「三個綠都在」就交差，會【漏掉一格而自己不知道】
```

**★這一條的形狀，跟今天另外兩條是同一族**：

| 今天的三條 | 共同點 |
|---|---|
| 把「討論那件事的文字」當成「做那件事的程式」 | ★**名字與實體不是同一個東西** |
| filter 過寬 ⇒ 穩定是【切掉的】不是【本來就穩】 | ★★而錯的那一邊**看起來是對的** |
| 閘 id ≠ 檔名 ⇒ 用檔名查卷面會漏格 | ★★★且**失敗時是安靜的** |

⇒ 通則：**要查「某個東西有沒有被驗到」，要走【那個東西在判決系統裡的名字】，不是它在檔案系統裡的名字。**
★★而兩者不一致時，用錯的那一個**不會報錯，只會少一筆**。

# 三、下一站：查詢面補「家」（第三順位）

你複述的四點都對，照做：語意＝本城｜**「暫代」三個字是 P4 判準的一部分**（grep 不到＝紅）｜
P2 打 `home_pos` 不打共用欄位（先逐 key grep 確認只有一個讀點）｜P3／P4 是母體地板。
★做完送 R² 給 reviewer＝demo-60 並敲他。
