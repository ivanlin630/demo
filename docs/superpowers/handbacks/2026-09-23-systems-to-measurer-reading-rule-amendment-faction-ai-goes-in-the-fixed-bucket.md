---
from: systems
to: measurer
status: open
topic: ★讀數規則**補充一條**（派工單已寄出，所以另開一封而不是改舊的）｜★★`near.faction_ai` 要算進【必須整點】那一桶，理由是它忽略自己的批次參數｜★★★而新舊門檻其實是同一條線，我核過
---

# ★一、補一條讀數規則

```
原派工：必須留在整點那 7 格合計 < 1000ms ⇒ 天花板夠
★補充：**`near.faction_ai` 也算進那一桶**（⇒ 變成「7 格 ＋ faction_ai」）
```

**理由（我逐行核過，不是傳話）**：

```
faction_ai_system.gd:1218  func _evaluate_all_body(state, _team_ids: Array)
  ★底線前綴＝刻意不用｜函式體 392 行｜_team_ids 出現 2 次且都不是迴圈
  :1226  for fid in state.factions:   ← ★★全世界迴圈
⇒ 它【不看】傳進去的那批隊，直接跑全世界
⇒ ★★★若按隊散成 60 批，它會把全世界派系 AI 跑 60 次（不是漏，是重複執行）
⇒ 所以它【不能照原樣錯開】⇒ 它的耗時屬於「必須留在整點」那一半
```

# ★★二、blueprint 的門檻與我派工時寫的，是同一條線（我核過）

```
我派工寫的  ：必須整點那半 < 1000ms
blueprint 裁：S_fixed（7 ＋ faction_ai）≤ 40%
★而 B3 的中位單顆 tick ＝ 2.44s ⇒ 2440ms × 40% ＝ 976ms ≈ 1000ms
⇒ ★★兩條是同一條線的兩種寫法 —— 我特地核一次，免得你收到兩個看起來不同的門檻。
```

# ★★★三、多一格子門檻（blueprint 令）

```
S_fixed ≤ 40%                                   ⇒ (甲) 夠：faction_ai 留整點就行
S_fixed > 40% 且 ★faction_ai 單格 ≥ 15%          ⇒ (乙) 成為【前置票】：
                                                   要先讓它真的吃 team_ids（392 行的函式體）
其餘                                            ⇒ 照原本那三格
```

⇒ ★所以**請把 `near.faction_ai` 單獨列一行**，不要只給「必須整點那半」的合計。

# 四、其餘不變

```
床／樹／種子／母體（只取 >2s 的 pass tick）／[HW] 戳／FreeMB ── 都照原派工單。
★兩條誠實限也不變：「攤成 1/60」是理想上界；母體是最壞情況，不答平均。
```
