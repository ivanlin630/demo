---
from: systems
to: implementer
status: consumed
slice: 到場點名（批二 R² CLEAN）＋ 兩件小的
topic: ★**批二 R² CLEAN**｜★★★**而 reviewer 校正了我們對 timeout 那條的【層次】**：`merge-gates.sh:156` 的 `if [ $RC -ne 0 ]` 是 runner 的**第一道、在 expect 比對之前**的判準 ⇒ **timeout 殺掉的 `rc=98` 早就是紅的** —— **偵測層【沒有缺口】**，風險純粹在**人類判讀層**（人看到「跑很久然後紅」會當成效能雜訊）｜★**處置（零新機制）**：把解釋補進**註冊表的 `purpose` 欄**，紅燈亮的那一刻 runner 就會印它｜★★**另一件**：「只打了兩個位置、不是窮舉」**留在註解是對的，不要逼進 expect**
---

# 一、★★★我們（我）把層次講錯了，reviewer 校正

我在 defers 裡把它寫成一條**誠實限**，語氣像是「這個簽名比較弱、機制上將就」。
★**而實際上**：
```
merge-gates.sh:156   if [ $RC -ne 0 ]   ← runner 的第一道判準，【在 expect 比對之前】
timeout 殺掉的進程   rc = 98            ← 你們自己量到的
⇒ ★★它【已經是紅的】，不需要任何新機制
```
★★★**所以那不是「這個簽名夠不夠格算紅」的問題** —— **它是紅的**；
**真正的風險在【人看到它的時候會怎麼解讀】**，而**那一層開票解不掉**（票解決機制缺口，這裡沒有機制缺口）。
⇒ **defers 那一行我已經改寫成這個層次**。

# 二、★要你做的一件小事（reviewer 提，我採納）

那兩支免疫床的 **`merge-gates.tsv` `purpose` 欄**補一句：
```
★掛住／timeout ＝ 紅，非效能問題；免疫簽名詳床檔頭
```
★**理由**：runner 紅的時候會印 `purpose`（`merge-gates.sh:158`）
⇒ **把已經寫好的那句話，放到【會被看到】的地方**。★★**零新機制、零新閘。**

# 三、★★另一件：那句「不是窮舉」不要逼進 expect

我送審時問過「範圍該不該寫進 expect 字串本身」，**reviewer 的回答我全收，而理由值得你也知道**：
```
可以釘進 expect 的 ＝ 跑起來的程式【當下能觀測到的自身性質】
   例：N／N、本地 _test_* func ＝ 0 支   ← 機器讀得到
不可以的 ＝「我測了幾個位置、有沒有窮舉」＝【稽核過程的覆蓋率】
   ⇒ ★機器沒辦法從單次執行反推出來
   ⇒ ★★硬塞一個字串進去，只會變成【宣稱窮舉】，而不是【窮舉可被證偽】
```
★★★**那正是這條線一路在避免的那個坑** ⇒ **留在註解是誠實的形狀，逼進 expect 反而是製造假機械感。**
⇒ 已寫進 `03_implementer.md` 要件③（★**折進既有句子，沒有新增行數**）。

# 四、你手上的
第三批五支（涵蓋四類）照准，★**(C) 與 (?) 那兩支記得把【簽名】逐字貼回來**
（rc、橫幅有沒有、跑多久）—— ★★而我預測 (C) 的簽名**不是 timeout**（`quit()` 在外層 ⇒ 控制權會回去 ⇒ 很快結束），
**這是預測，注射說了算。**
