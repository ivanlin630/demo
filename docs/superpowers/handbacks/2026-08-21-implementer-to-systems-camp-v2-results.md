---
from: implementer
to: systems
status: open
slice: camp-stay-brick v2
topic: 四端同秤已接完 — ★驗收 2 綠 1 紅（#1 仍 0，但現在【每一階都有數字】：贏 8 → 開工 1 → 完工 0）；headless 9→15 仍不可 merge；三個裁定點
branch: feat/camp-access
commit: 70198bc0
---

# 四選項同秤：做完了，世界也回話了

## ①接線（全部呼**同一個** `DiscountedFlow.flow_utility`，不是四份正規化）
| 選項 | 舊尺 | 新尺 |
|---|---|---|
| 覓食 | `f(food_days)` 存量、位置盲 | 腳下 tile **真實可採日流**（★自家營地 vs 荒地**不同價**）|
| 遷移找糧 | 同一顆存量函數 | 目標 tile 真實可採日流 |
| 併入 | **只秤名聲** | **host 村被動流（現成、delay 0）**，名聲降為乘數 |
| 佔村 | **死常數 1.0/0.3** | 那村的**實際產能** |
| 紮營 | 折現真流 | 同上（delay 由工期折現）|

★**血統①守住**：覓食日產仍用既有 `_forage_subsist_buffer / FORAGE_FLOOR_DAYS`，**沒有第二份產能常數**；位置差異由該 tile 實際食物池表達。**cap／δ 一律沒動。**

★**你插播那條我照收並改了寫法**：`host_rep` 恆 0.5 ⇒ 名聲乘數目前是**常數 0.75、零資訊量**。
∴ 帳上寫 **「投靠第一次會看飯；名聲那半仍是常數」**，不寫「名聲磁鐵開始起作用」。

## ②★世界層驗收：**2 綠 1 紅**（同床 peaceful 90d seed 1337、baseline ＝ current main）
| # | 驗收 | 結果 |
|---|---|---|
| 1 | `outpost.l0_to_l1 > 0`（二值） | **0 ⇒ 紅** |
| 2 | `camp.abandoned < camp.built` | **18 < 24 ⇒ 綠**（折現磚單獨版是 25/28）|
| 3 | 零採集 < 同床 main baseline | **966 < 1133 ⇒ 綠**（折現磚單獨版 1244＝反向，四端同秤把它壓回來）|

附帶：`camp.won_argmax` 12 → **69**；母隊人口 35 → **49**；`pop=1` 村仍 12。
`camp.lost_to` 從「覓食一家獨大」變成 **備戰 137 / workshop 72 / survival 67 / 併入 52** ＝ 四端真的在同一把尺上競爭。

## ③★★#1 那個 0 的完整斷點鏈（每階都有數字，不是無解釋的 0）
```
紮根贏 argmax                = 8
→ 真的開工 l0_to_l1_start     = 1     ★8 贏只有 1 個開得成
→ 完工 crude_camp             = 0     ★那 1 個也沒完成
   （construct.complete 19 筆全是 upgrade_facility）
營地端：built 24 → abandoned 18（75%）
```
⇒ **不是「紮根不 applicable」也不是「秤輸」**（它 applicable 且贏 8 次），
是**贏 → 幾乎開不了工 → 開了也沒完工**。前段在 commit/仲裁端，後段在工期端（與 75% 廢棄同源：人走了工地就停）。

## ④★自我訂正（我自己的儀器騙了我一次）
上一輪 `root.commit_drop.no_settle_site = 1101` **是假象**：該 hook 對**每個成功 try_set** 都跑，
我沒過濾選項 ⇒ 把所有非紮根 commit 都算進去。已加 `current_option == "紮根"` 過濾重量。
**教訓同你反覆講的：tap 的分母沒對齊語意，數字就會騙人。**

## ⑤ headless：**9 → 15，仍不可 merge**（照實列，不用「預期改變」帶過）
新增六條，分兩族：
- **覓食語意族（3 條）**：`T1:覓食 base 恆 1.0`×2、`覓食=1.0`
  ⇒ 這些測試**斷言覓食 base 是常數 1.0**，而 spec 要的正是「**覓食不再是常數、要看位置**」。
  ★**我沒有改這些測試**（你明令不准改測），**交還你裁**：這是「測試綁著舊法條」還是「我改過頭」。
- **投靠/掠奪族（4 條）**：`義氣隊未投靠`、`投靠玩家未寫 forced_event`、`Path 3 應投靠`、`p2b1 義氣 homeless 未投靠`、
  `承諾(掠奪)→實際紮營`。★**這族沒有如預期回綠**——即使 join 現在會看飯。
  合理懷疑：**那些測試場景裡 host 也沒有飯**（`join_host_flow` 低）⇒ 紮營仍贏。**我沒有調參去讓它綠。**
  `承諾(掠奪)` 那條照裁定標 **`known-blocked-by: 承諾泛化磚`**。

## ⑥ 其餘閘
det×3 **`578d6770f00b9e9385c83646a3b10110`** 穩定（intended-change）｜憲法 **PASS 74**。

## ⑦ 交還你裁的三點（我一個都沒自己決定）
1. **覓食常數那三條測試**：改測 vs 改我。
2. **投靠族沒回綠**：要不要我量那些合成場景裡的 `join_host_flow`（一輪 tap 就知道），還是你直接裁。
3. **#1 的 funnel**：`8 → 1 → 0` 兩段斷點分屬 commit 端與工期端，**是否開新票**（我判斷它已超出「四端同秤」這刀的範圍）。
