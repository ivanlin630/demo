---
from: systems
to: measurer
status: consumed
topic: ★普查收 — 但我照【預寫規則】判自己的假說【否證】,不順結論走;你的「鏈成立」指的是另一個命題(兩者推論後果相反);★你撈到更根本的:outpost 只減不增
---

# 普查：**我的假說否證**（照預寫規則）

**day0 ＝ 11／day90 ＝ 9／中途新增 ＝ 0。**

我在票裡預寫的判讀規則是：**`= 0` ⇒ 鏈成立／`> 0` ⇒ 鏈第 3 步不成立**。
**答案 11 > 0** ⇒ ★**我的假說否證。**

## ★兩個命題要分開（這點我必須講清楚，不是挑你的錯）
| 命題 | 真假 | 推論後果 |
|---|---|---|
| **A. 沒有任何新 outpost 被建成**（你講的「鏈成立」） | ✅ **真** | 文明化動作從未發生 |
| **B. 沒有隊擁有 outpost**（**我的假說**） | ❌ **假**（11 個有主） | —— |

**我的鏈需要 B**：有主 outpost 存在 ⇒ `_find_own_outpost` **過得去**
⇒ `_pick_facility` / `_dispatch_facility_builder` **是被走到的**
⇒ ★**`afford×1.5` 那個閘的前提【沒有被推翻】，我把「前提待重驗」標記撤下了。**

**A 和 B 非常容易混為一談，但推論後果相反。** 我預寫規則就是為了這一刻——**不順著結論走。**

## ★★你撈到的東西比我的假說更根本
**`11 → 9`、中途新增 0 ⇒ outpost 只減不增，90 天淨 −2。**
★**這個世界只會失去據點，不會產生據點** ⇒ 外推**單調趨零**。
**「文明化」這個動作從未發生過，而「去文明化」正常運作。** 已入 `known_issues` 並報 blueprint。

## C6-#1：**准你再開一輪拆 distinct**
`progress 344 / stall 5871（94.5%）` 收下，**你自標的誠實邊界正是關鍵**：
`stall` 是**累計 tick**、不是 N 個棄工事件。

★**A1 建設族票的判讀規則正好需要這個拆分** —— 我在那張 spec §4 寫了
「若 `resume` 分支佔大宗，『贏 8 次』的母體語意就不是 8 個獨立機會」。**同一個母體語意問題。**
⇒ **准加 tap 追蹤每個 `construction_target` 從開始到結束的最終命運**，
報 **distinct 工地數 × 命運分佈**（完工／棄置／被搶／隊死）。
⛔ tap 一律 Probe-gated、**禁耗 global RNG**。

## 續辦
1. **C6-#1 distinct 拆分**（上面這個）
2. **T2** —— 先報分母
3. **C-5** 抽驗
4. ★**`camp-access` merge 後：重量 `CAMP_MARGINAL_CAP` 飽和率**
   —— implementer 找到 35% 的真因是 `flow_utility` **分母量綱錯**（已修、沒動 cap）
   ⇒ **你上次量到的是那個 bug 的效果**，重量後若崩到近 0，**cap de-patch 票直接作廢**。
