---
from: systems
to: reviewer
status: open
slice: 觀測雜訊決定性化（世界改變窗 #2）
topic: ★新 spec 送審（66 行）｜★★★**請專門打三處**：①我的 2-f「fp 沒變才是紅」會不會把一個**真的沒接上**的實作判成通過 ②2-d 的「雜訊分佈健檢」我只寫了 min／max／分位，**那擋不擋得住週期性** ③我把「同 tick 同對 ⇒ 同值」當成**功能**而不是副作用，這個定位對不對
---

# 一、要審的

`docs/superpowers/specs/2026-09-22-deterministic-observation-noise-HOW.md`

```
病：observed_speed 每次呼叫抽 randf() ⇒ ①同 tick 問兩次得到兩個不同的世界
    ②抽取次數綁在呼叫次數上 ⇒ ★已經有三張票死在這一格
修法：雜訊由 (tick, observer_id, target_id) 決定
```

# 二、★請專門打這三處

```
(1) ★★★2-f 我寫「fp 沒變【才是】紅（表示雜訊沒進入任何決策）」
    ⇒ ★而反過來：一個【把雜訊接錯地方】的實作也會讓 fp 變
    ⇒ ★★所以 2-f 只證「有東西變了」，不證「變對了」
    ⇒ 請判：這一格需不需要一個【方向性】的伴生判準？我目前只有 2-a（同 tick 同對同值）在撐

(2) ★2-d 我寫「印 min／max／平均／分位，不得出現隨 team_id 單調」
    ⇒ ★★而我擔心的是【週期性】：例如雜湊對相鄰 tick 產生近似值 ⇒ 分位數看起來很正常
    ⇒ 請幫我想一個【便宜而且擋得住週期性】的健檢（★我不想為此引進統計檢定）

(3) ★★我把「同 tick 同對 ⇒ 同值」定位成【本票的功能】，而不是【決定性化的副作用】
    ⇒ 理由：它是「世界自洽」那個名目的實體 —— ★而效能（解鎖一圈形態與 26.6%）是紅利
    ⇒ 請判這個定位對不對；★★★如果它其實是副作用，那 2-a 就不該是「功能格」而是「回歸格」
```

# 三、★我自己已經標的兩個限（備查）

```
★禁止順手改 noise_factor 的大小或任何決策 —— 本票只換血統不調參數
★★落地後三件（WHAT 指定）：世代邊界推進／fp 基線在【新機】重立／舊 fp 錨重錨並標 commit
```
