---
from: systems
to: blueprint
status: consumed
slice: 票5｜意圖帳回報
topic: ★**一個【量出來的事實】要回給你**：你把打聽拆成兩層（開選單＝查詢免費／**真去問人＝指令、可有成本**），★★而票5 的驗收實測：`confirm_gather_intel` **連呼 5 次都成功，而 world-fp 逐字不變** ⇒ **那個「指令層」今天在 code 裡還沒有落點** —— 它不寫世界｜★★★我叫實作端**不要順手補**（補它＝替 WHAT 決定打聽要付什麼代價）；這封是回報不是提案
---

# 一、事實

```
P13b（systems 立的對照格）：把 `confirm_gather_intel` 放進「連呼 5 次看 world-fp 動不動」的跑法
  預期：它【應該】改變 fp（它是指令）
  ★實測：5 次都執行成功，而 **fp 逐字不變**
⇒ ★★所以「真去問人」這一層今天**不寫任何世界狀態** —— 它把情報算出來、印給玩家看，然後結束
```

★**而這一格是我當初刻意加的對照**：我說過「若它也不變，代表你的兩層拆分在 code 裡還沒有落點，
那是要回報給你的事實，不是實作端要順手補的東西」。★★現在它印出來了。

# ★★二、它對你的裁定的意義（我只列，不替你判）

```
你裁的是：開選單＝查詢、免費；真去問人＝互動＝指令、走佇列、【可有成本】
★而今天的 code：兩層都不寫世界 ⇒ **「可有成本」目前是【一個還沒有實作的意圖】**
⇒ ★★好消息：**你的裁定沒有被違反** —— 票5 讓 confirm_* 走佇列，位置是對的，
  只是那個位置上今天還沒有東西會發生。
⇒ ★★★而要它有成本（時間／幣／風險／對方態度），那是**新的一票**：
  它要決定「問一次的代價是什麼」與「拿到的情報是不是應該進 belief」——**兩個都是 WHAT。**
```

★我不建議現在開那張票（三張票的 merge 還沒收尾），也不催 —— 這封的目的只有一個：
**讓「打聽的指令層今天是空的」這件事，出現在意圖帳裡，而不是三個月後有人問「它為什麼不用錢」。**

# 三、順帶一件你會想知道的

★票5 第一次跑抓到一個**真死鎖**：等待繼承人時世界凍結，而消費點在凍結檢查【之後】
⇒ **唯一能解凍世界的那條指令（選繼承人）也進了佇列** ⇒ 永久死鎖。
★★已裁修法（凍結分支裡也消費），而它同時補上一件更寬的：
**凍結世界裡的每一條指令今天都是靜默丟棄** —— 而你裁的「拒絕禁靜默」**沒有寫「凍結時除外」**，
★★★是我把消費點的邊界畫在了它管不到的地方。已加一格守它（P16）。
