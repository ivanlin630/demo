---
from: systems
to: implementer
status: consumed
topic: [★slice A 補·順手] SpecimenTracer dump 加 leader values(性格顯性化驗收要);+品質及格線知會
---

# slice A 補：SpecimenTracer 加 leader trait dump（順手，別另起輪）

補 `2026-07-14-systems-to-implementer-full-sliceA-dispatch.md`（你正做）。用戶加驗收品質及格線＝**性格透過戰略選擇顯性化**（非只沒餓死），measurer 要抽驗霸主/謹慎/中庸 leader 的行為分化 → 需 leader trait log。

## 順手加（小）
`SpecimenTracer`（`scripts/debug/specimen_tracer.gd`）的 `_snapshot`/entry dump **加 leader values**（野心/慎重/求生欲/好戰/貪婪 等關鍵 trait）——specimen-gated、純觀測、零行為變。讓 measurer dump 一眼看出「這隻 leader 什麼性格、對應什麼資源分配/option 選擇」。reviewer 早輪標過「caution-trait log 缺」，這次一併補齊，免 measurer 又 incomplete「無工具篩人格」。

## 知會（非動作，驗收標準）
你整包 slice A 做好後 measurer 會驗「性格顯性化」（同一霸主該從焦慮普通人變雄心開國之君）。∴ **層5 預算分配 + 候選2 門檻人格化務必真讓行為分化**（非全隊同一套）——這是及格線，非 optional。做時心裡有數：層0 只保命，層2/5/候選2 才讓性格長出戰略面。

## 併入原 dispatch
此為原 full-slice-A dispatch 的補充項（trait log），**不改其餘**，一起做完一起 handback。
