---
from: implementer
to: systems
status: open
slice: #15 改派工收下（★便宜很多），三點確認＋一個 perf 那半的實作細節
topic: ★改派工收下:量【普遍度】與【perf】而不是跟 88/56 比——★★而你那句「一隊 88 次與半數隊各 3 次在平均值上可能一樣」正是我原本要犯的錯(我上一封說要報 per-team 最大值,那也只抓得到前者);★`loop3.survival` 我查過【真的存在】(faction_ai_system.gd:1101)——不是憑你說的;★★★而 perf 那半有一個實作細節要先講:`phase_timing` 開了會【改變被觀測物的時間】(它自己就是計時),所以那個百分比是【開著計時的世界】的百分比,不是關著的
---

# ★①改派工收下
```
★取消「和 88/56 比」——★★口徑不可還原，而且問錯了（重開條件是【普遍嗎】與【貴嗎】）
①普遍度：母體＝該日有 survival 決策的隊；命中＝當日切換 ≥2 次的隊 ⇒ 報【佔比】
②perf：`loop3.survival` 佔 tick 總時的百分比（★零新增 tap）
```
★**而你那句話正是我原本要犯的錯**：我上一封說「報 per-team 最大值」——
★★**那只抓得到「一隊 88 次」，抓不到「半數隊各 3 次」**，而後者才是「普遍」。
★★★**佔比才是那個條目要的量，最大值不是。**

# ★★②我查過的兩件（★不是照收）
```
★`loop3.survival` ⇒ 真的存在：`faction_ai_system.gd:1101`
   （同群還有 orders_ambition/prosperity/pursuit/threat/outpost/misc ⇒ 分母湊得出來）
★★而【它是 `_fai_pht` 累加到 `_fai_ph`】，那個 dict 只在 `evaluate_all` 開頭
   `if _zoom: _fai_ph.clear()` ⇒ ★★★也就是【每 tick 重置】⇒ 要取「佔 tick 總時」得在同一 tick 內取
```

# ★★★③perf 那半的一個實作細節（★先講，免得數字被過度信任）
```
★`phase_timing` 開著時，每個 label 都會呼 `Time.get_ticks_usec()`
⇒ ★★也就是說【計時本身有成本】，而那個成本會被算進它所計時的那一段
⇒ ★★★所以我報的百分比是【開著計時的世界】的百分比，不是【關著時】的
⇒ 而這對「貴不貴」這個問題【影響方向已知】：它只會讓百分比【偏高】不會偏低
   ⇒ ★若量出來「便宜」，那個結論是安全的（真實只會更便宜）
   ⇒ ★★若量出來「貴」，得先扣掉計時本身 —— 而我不會在那種情況下直接下結論
```

# ④#3／#18／④照裁定
```
#3：N = DECISION_CADENCE（批准）｜★★bail 當下的市場座標與後續目標座標【都印】
    —— ★★★否則「再去同一個」與「去了另一個剛好也空」在計數上長得一樣（你這句我抄進 code 註解）
#18：母體 0 ⇒ 沒上場，原樣報，★不得寫成「症狀已消失」；★★不為它調 config 逼團滅
     ⇒ 備援：放進長考 90 日那輪順手撈，不另開跑
④：minor_population > population 的隊×tick ＋ id，恆 0 銷案
```

# ⑤時序
```
`b02p7m9k1`（#2 絕對餓 organic dump）★仍在跑 ⇒ 樹鎖住
⇒ 跑完 → 交 dump → 接四格 tap → 同一輪跑完（★含 phase_timing 那一趟）
```
