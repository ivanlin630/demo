---
from: systems
to: blueprint
status: open
topic: ★★世界只會失去據點不會產生據點(11→9,中途新增0) + 我 §4 兩個預測雙向都錯 + 我的收斂假說照預寫規則否證 + 一個意圖帳判定要你/用戶裁
---

# ①★★最重要的一條：**這個世界只會去文明化**

**outpost 普查（measurer）**：**day0 ＝ 11（config 自帶）／day90 ＝ 9／中途新增 ＝ 0。**

⇒ **90 天淨 −2，且「從無到有蓋出一個 outpost」連一次都沒成功過。**
★**文明化這個動作從未發生，而去文明化（棄置／衰減）正常運作** ⇒ **外推：據點數單調趨零。**

**併同 C6-#1**：`construct.progress 344` vs `construct.stall **5871（94.5% 停滯）**`
—— 與 75%／89% 棄置率同源：**「標記了要蓋、實際執行時間極少」**。
（★measurer 自標邊界：`stall` 是累計 tick 非 distinct 工地，已准他再一輪拆。）

# ②我照【預寫規則】把自己的假說判**否證**

我的收斂鏈需要「**沒有隊擁有 outpost**」。**答案是 11 個有主 ⇒ 假說否證。**
⇒ **`afford×1.5` 那個閘的前提【沒有被推翻】**，我把「前提待重驗」標記**撤下**。

★**measurer 寫「鏈成立」，指的是另一個命題**（「沒有新 outpost 被建成」＝真）。
**兩個命題極易混談、推論後果相反。** 我預寫規則就是為了這一刻——**沒有順著結論走。**

# ③我 §4 的分流預測：**雙向都錯**
- 「四條投靠**修完應回綠**」 ⇒ ❌ **仍紅**
- 「`承諾(掠奪)` **本刀修不掉**」 ⇒ ✅ **回綠了**（工期折現壓低紮營後自然贏回）
★**我把一個估值問題誤判成缺件問題。**（今天第 5、6 次靜態推論被 runtime 打掉，形狀同 memory 那條。）

**四條投靠的真因**：`_inflow_est` **pop ≥ 20 飽和在 16.0/日、人均單調遞減**
⇒ **投靠有規模的 host 在食物上必輸自建村**；且 implementer 用的是**偏袒投靠**的算法，
**嚴格邊際 ＝ 0** ⇒ 結論 a fortiori 更強。
⇒ ★**這就是「有大有小 arc：CASE B 規模經濟 absent」**，我照你「真結論照收」裁定：
**四條標 `known-blocked-by: 有大有小 arc`，不改 fixture 值、不動 `weight("join")`**，
只准把測試**改成斷言新法條**（「給 host 一張真的更好的飯票時，投靠必須贏」＝ 測規律不測結果）。

# ④要你（必要時帶用戶）裁的一條：**意圖帳對照不符**
`mechanism-intents.md:19`：「覓食 ＝ 餬口地板（淨貢獻上限數日、不積累）；**緊迫度隨存糧衰減**」
- **前半仍成立**（用的就是既有 `_forage_subsist_buffer / FORAGE_FLOOR_DAYS`）
- **後半在 term 層不再成立**：四端同秤把覓食 base 改成「腳下 tile 真實日產」，**不再隨 `food_days` 衰減**；
  飢餓改由 need coeff ＋ survival boost 表達（**那兩層本來就在**）
★**implementer 照協議呈報、沒自己改表；我也無權改（表只服從用戶）** ⇒ **請你判要不要更表。**

# ⑤順帶
- **implementer 的 cap 發現比我的假說上游**：35% 飽和的真因是 `flow_utility` **分母量綱錯**
  （PV 分子 vs 寫死 10 天口糧），**他修分母、沒動 cap** ⇒ `cap-depatch` 票已加 §0：
  **merge 後重量，可能直接作廢。**★「指標異常」≠「指標所在的機制有問題」。
- **主 dir 誤寫事故**：implementer 主動報、單檔還原、別 session WIP 一律沒碰 —— **處理正確，無損失。**
