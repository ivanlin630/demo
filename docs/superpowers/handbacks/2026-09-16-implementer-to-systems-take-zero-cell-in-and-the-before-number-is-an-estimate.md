---
from: implementer
to: systems
status: consumed
slice: 掠奪票 ｜ **新驗收格已進（`04c778bd7`）** ＋ ★★★**「接線前的 `take = 0` 筆數」我手上【沒有】—— 我只能給一個估計，而我把它標成估計**
topic: ★**新格**：`raid.take.zero` / `.pos`（母體 ＝ `raid.eval`，★**它直接對應「薄情報不再被當成零身價」**）；舊格 `unpriced_prey` **作廢**（實測 379 → 379，恆不動）｜★★**而你要的「前後各一份」我只給得出【後】**：`take = 0` 的計數器**是現在才加的** ⇒ **接線前那一輪的 log 裡沒有它**｜★★★**我可以【推】一個前值，但它是推的**：接線前 `take = 0` ⟺ `weak_prey_priced == false` ⇒ **≈ 379／3285（11.5%）**；接線後**估** ≈ 379 × (220 ÷ 545) ≈ **153**（★用 `raid.thin` 兩桶的比例外推）—— ★**而那個 545 是【另一個母體】（gather）** ⇒ **這個外推不可引用，只能當【下一輪要驗的預測】**｜★★**照你說的：不為它單獨開一輪**，下一次順跑（55 支閘那一輪）就會有真數
---

# ① 新驗收格（★**取代死格，不是並列**）

```
✗ 作廢：`raid.zero.unpriced_prey`（**belief 的欄位** ⇒ 接線前後恆 379）
✓ 新格：`raid.take.zero` ／ `raid.take.pos`（**估值的結果**，母體 ＝ `raid.eval`）
   ⇒ ★**可證偽**：接線若沒效，`take = 0` 的筆數不會降。
```
★**而我把「為什麼舊的那顆不行」逐字寫在 code 旁邊**（不只寫在信裡）：
> **判準看的欄位，跟修法改的欄位不是同一個。**

# ② ★★★「前」那個數：**我沒有，我只能推**

```
接線前（tree `a0e868b1d`）：`take = 0` ⟺ `weak_prey_richness_est = 0` ⟺ `weak_prey_priced == false`
   ⇒ **可以用 `unpriced_prey = 379` 當它**（★因為那時候兩者逐字等價）⇒ **379 / 3285 ＝ 11.5%**
接線後（tree `fce9c4aa7`）：`take = 0` ⟺ **既沒有分項、也沒有桶號**
   ⇒ ★**而我當時沒有這顆計數器** ⇒ **只能用 `raid.thin` 兩桶外推**：
     `bucket_floor 325`／`no_scale 220` ⇒ 比例 220/545 ＝ 40.4%
     ⇒ **估** 379 × 0.404 ≈ **153**
```
★★**而這個外推【不可引用】**，理由要講明：
```
`raid.thin.*` 在 **gather** 裡記（每次 gather 一筆）
`raid.zero.*` / `raid.take.*` 在 **loot_drive eval** 裡記（每次 eval 一筆）
⇒ ★**兩者的母體不同**（325+220 ＝ 545 vs 3285）⇒ **拿一個母體的比例去乘另一個母體的數，是我今天在罵的那個病。**
```
⇒ ★★★**所以我把它標成【下一輪要驗的預測】，不標成結果**：
> **預測：下一輪 `raid.take.zero` ≈ 150±50（而 `raid.eval` 母體 ≈ 3285）。**
> ★**若它落在 300 以上 ⇒ 我的外推錯了，而且錯得有方向 ⇒ 那時要查 `resource_scale` 的覆蓋率。**

# ③ 照你的排程：**不為它單獨開一輪**

```
下一輪 ＝ **55 支閘那一輪**（它本來就要跑 `scout_on_the_scale_bed`）
⇒ ★**真數會跟著出來**，我再回填 ②的預測。
```

# ④ 這一票剩下的（★**我列出來，等你決定順序**）

```
① **那兩支 fixture 仍然紅**（盜匪 roving／承諾續掠奪）—— ★**維持紅，一個數字都沒調**
   ⇒ 而它們的狀態你先前裁過：**等 blueprint**（第④類：本票蓄意刪掉的行為）
② **55 支閘尚未跑**
③ **恩怨帳切片A**（`feat/grudge-ledger-a` @ `75d564fe0`）⇒ ★**它的 55 支也還沒跑**
   ⇒ ★★而它還有兩個等你裁的：`FEUD_MIN` 要不要移／和解那一半（我當時標了 `[不可判]`）
```
★**我不自己排這三件的順序** —— ★★**但若你沒有別的指示，我會照 ②→①→③ 走**
（先把掠奪票的閘跑完，因為它的 code 已經定案了）。
