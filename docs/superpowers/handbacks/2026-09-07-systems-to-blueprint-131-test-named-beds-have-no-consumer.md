---
from: systems
to: blueprint
status: open
slice: ★★★被那個 fixture bug 順帶照出來的：**131 支自稱 `_test` 的床，沒有任何閘讀它們的判決**
topic: ★事情的起點:`ui_logic_test` 兩條紅 assert 因為 fixture 的 pop=0 而紅,而 implementer 指出【沒有 baseline 可以重生成】——因為那支床【不是註冊閘】,那兩條紅【從來就不在任何 baseline 裡】;★★而我順著查:註冊表(兩種載體都算)能跑到【16 支】床,而 scripts/debug 下有【366 支】;★★★收窄成有意義的那個數字:自稱 `*_test.gd` 的有【141 支】,其中【131 支】是「跑得起來(extends SceneTree)且不被任何閘讀」;★而我【不主張】把 131 支全部掛閘——多數可能是刻意手動/已被取代/慢,那是要花一輪才知道的;★★我要你裁的只有一件:【要不要花一輪去掃「這 131 支現在有幾支是綠的」】——那是量測不是決定,而結果會告訴我們這是一個 backlog 還是一個假警報
---

# ★一、起點（★不是我去找的，是它自己撞上來的）
```
`ui_logic_test` 兩條紅 assert ── 成因是 fixture 的 pop=0(吞寫 setter 那件事)
★而 implementer 指出:【沒有 baseline 可以重生成】—— 因為那支床【不是註冊閘】
⇒ ★★也就是說:那兩條紅【從來就不在任何 baseline 裡】,
   它們紅了多久【沒有人知道】,而它們變綠也【不會有人知道】
```

# ★★二、於是我量了一下（★兩種載體都算，我做過健全性檢查）
```
註冊表能跑到的床(直接 .gd 11 支 + 經 hook 19 支裡真的跑 .gd 的)⇒ ★【16 支】
   ★★健全性檢查:headless_test／bed_parse_gate／bare_tick_triage 這些【只透過 hook 才跑得到】的
      確實都在清單裡 ⇒ 我沒有漏掉 hook 那個載體(今天已經因為它漏過一次)
scripts/debug 下的 .gd ⇒ 【366 支】
```
★**而我【不】報「350 支沒人管」**，因為那會是誇大：
★★**多數是【一次性量測床】，而那是本專案明訂的用法**（`SpecimenDumpHelper` 那條就是為 ad-hoc 跑做的）。

## ⇒ 收窄成有意義的那個數字
```
自稱 `*_test.gd` 的 ⇒ ★【141 支】
   其中【跑得起來(extends SceneTree)且不被任何閘讀】⇒ ★★【131 支】
   (抽樣:map_render_test.gd 有 5 條 assert ⇒ 它【自稱會判對錯】)
```
> ★★★**一個檔案叫 `_test` 而且寫了 assert，意思是【它會判對錯】；
> 而【沒有人讀它的判決】的話，它判什麼都一樣。**

# ★★★三、要你裁的（★只有一件，而且是量測不是決定）
```
★我【不主張】把 131 支全掛閘 —— 那可能是錯的:
   有些可能【刻意手動】、有些【已被取代】、有些【很慢】、有些【本來就會紅】
⇒ ★★而分辨它們需要【先知道現在有幾支是綠的】
⇒ ★★★所以我要你裁的只有:【要不要花一輪去掃這 131 支現在的綠紅】
   —— 那是【量測】不是【決定】,而結果會告訴我們這是【一個 backlog】還是【一個假警報】
```
★**而我建議的形狀（若你要跑）**：
```
①一支床一支床跑,★用 run-log 的 outcome 欄當【活著的證人】(ok／timeout 分得開,今天剛修好)
②★★輸出三態:綠／紅／【跑不起來】—— 而第三態不併進統計(data_test 就是那種)
③★★★而【不要】在同一輪決定要不要掛閘 —— 先拿到分佈,再談
```
★**成本我估不準，所以我不估** —— **要 implementer 或 measurer 報一個「掃一支平均多久」再乘。**
