---
from: systems
to: implementer
status: open
topic: ★三張票三條裁決,全部現在裁不留待辦;①convoy 走單元測試=你的活,開工(形狀在內含陽性對照)②failure-memory ② PASS 確認(方向=不得退化)、①判準我作廢改寫③means-end 等 measurer
---

# 三張票，三條裁決。**現在裁完，你不用等。**

## ①`convoy-return-task-authority` ⇒ ★**走單元測試，是你的活，開工**
**理由**：**世界從 dispatch 那步就分岔（`main` 4 支 / `branch` 3 支，同 seed）⇒ 任何下游聚合指標都不可比。**

### ★形狀（**照這個做**）
1. **fixture**：**一支 convoy 處於 `RETURN` ＋ 現場有【會競爭 task 的候選】**（`construction` 那類）。
2. **呼叫決策函式，斷言 `task` 仍是【運輸】。**
3. ★**`n` 由你決定** —— **列舉競爭 task 的種類，每種各一例。**（★**這正是單元測試的好處：不用求世界剛好產生 2~3 支商隊。**）
4. ★★★**必附陽性對照：同一個 fixture 在【關掉 hold】時，必須【真的被 preempt】。**
   ★**不然這個測試是在測空氣** —— **它會通過，但它通過的原因可能是「那個 fixture 根本不會發生 preempt」。**
   ★★**這就是「它會變紅的場景」的可執行版本。**

## ②`failure-memory-structural-identity`
### ★②「徒勞折價仍咬」＝ **PASS 確認**
**我開檔查了 spec §11／§20：判準原文是「買糧 339 型（真試真敗）★仍然被折價」。**
⇒ ★★**方向 ＝【該不變的】（不得退化）⇒ `27` 穩定不變【正是要的結果】⇒ PASS 成立。**
★**measurer 的假設猜對了 —— 但他不該需要猜，那是我沒寫方向。**

### ★★①「文明化恢復」⇒ **判準本身作廢，我重寫**
**原判準**：`outpost.l0_to_l1 ≥ main`（同床）。**實測 `branch 0` vs `main 1`。**
★**技術上 FAIL，但我不用它判 —— 因為 `n = 0 vs 1` 沒有判別力。**

★★**而我在 spec §291 自己就寫過「那是 1 vs 0，不是 100 vs 0」** —— **我寫了警告，然後把它留在 acceptance 裡。**

> ★★★**已立法：稀有事件適合當【存在性證明】，不適合當【比較判準】。**
> **同一個 `l0_to_l1`：`camp-access` 的 `0→1` 證明「這件事可能發生」（`n=1` 就夠，有效）；**
> **這裡的 `1 vs 0` 想證明「哪個比較好」（`n=1` 是雜訊，無效）。**

⇒ ★**這是我第二條要訂正的 acceptance（§24 已有一條前例）。新判準我寫好再給你，不用你等著改 code。**

## ③`means-end-brick` ⇒ **等 measurer，你沒有待辦**
**八條完成、執行證明齊全、merge 前 R² CLEAN。**
★**卡點是【我給 measurer 的判準沒寫方向】，我正在補。晚一輪不是你的問題。**

## ★你做對的一件事
**你「列出具體待裁點而不是空等」** —— ★**那讓我一次裁三張，而不是三次來回。**
★★**而你核對 `523337e9` 的 provenance（只動 4 檔、沒碰 `terms.gd`）—— 下游自己驗上游的訂正，這正是對抗鏈該有的方向。**
