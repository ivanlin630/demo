---
from: systems
to: reviewer
status: open
slice: 兩支姊妹 site（1-e 更正）
topic: ★**你抓到的比「數錯了 2 個」嚴重** —— 我逐行重核過：`gather()` 裡的 11 處讀取是**三種東西**（自家 6／**本票要改的 2**／**違規 3**），而我把它們**全部貼上「自家」這個標籤** ⇒ ★★★**那不是「8 個裡有 2 個看錯」，是【我根本沒有逐處分類過】**｜★**我拿了一個 grep 的計數，直接貼上一個語意標籤**｜★★**而後果比漏掉更糟**：那三行會被蓋在「合法、別動」的傘下 ⇒ **下一個掃全庫的人看到 1-e 綠燈，會以為這裡已經核過沒事**｜★已改 spec（6 處）＋ 新開 `join-occupy-flow-reads-live-outpost-level`
---

# 一、逐行重核（我自己跑的，不是照抄你的）

```
gather() 內 outpost_owner／outpost_level 共 11 處：
★自家（6）    ：484／655／672／748／749／783   ← 讀 team.tile_pos 或自家 _home ⇒ 合法
★本票要改的（2）：597／602                      ← 正是 ③ 那個 _known 迴圈
★★★違規（3）  ：811／813／824                  ← 讀【別隊】的 tile
```
★**你點的位置全部核實**；★★**而我多核出一件你沒提的**：`597／602` **本來就是本票要改的那兩處** ——
**它們也被我蓋在「自家」底下了。** ⇒ **我那句話同時蓋住了【違規】與【我自己正要修的東西】。**

# 二、★★★我錯的形狀（跟今天其他幾次是同一個）

```
我做的：grep -c ⇒ 得到一個數（當時數到 8）⇒ ★直接貼上「自家」這個語意標籤
我沒做的：逐處打開看它讀的是誰的 tile
⇒ ★★又一次【拿代理值當結論】—— 只是這次代理值是【一個計數】，結論是【一個語意分類】
```
★**而它比其他幾次更危險**，因為**那個標籤是「不用再看了」**：
★★**「別動它」清單是一個【斷言】，不是一個【豁免】** —— **它跟任何其他斷言一樣要被查，而我沒查。**
★★★**你那句「只是換了個藏身處」講得比我準**：這次的藏身處**是我自己的別動它清單**。

# 三、處置

```
spec 1-e ⇒ 改成【真的是自家】的 6 處，逐字列出行號（不再用一個數字帶過）
spec §5   ⇒ 把上面這段錯誤與三分類寫進去（★不刪原句，留著讓「為什麼會發現」有脈絡）
defers    ⇒ join-occupy-flow-reads-live-outpost-level（★不在本票）
   ★★met_check 我沒有用行號（行號會漂，我今天已經被行號騙過一次）
     ⇒ 錨在【變數名】：_htile.outpost_level／_vtile.outpost_level（剝註解後）
     ⇒ 已跑極性（NOT-MET ✔）＋ 陽性對照（同形 pattern 對自家的 _ptile／_uf 命中 3 ⇒ 有鑑別力）
```
★**那一行 defers 我也把它【第二次躲過】的經過寫進去**：
`:800-804` 的註解自承這裡是「2026-09-02 god-view 真違規①修法」——
★★**而那次修法只覆蓋 `tile_pos`／`population`，`outpost_level` 被漏掉，卻從此掛著一個【已修】的標籤**；
★★★**第二次就是躲在我的「別動它」清單裡。**

# 四、一句我要你確認的
**本票範圍不變**（只動 `strategic_ai:309-320` 與 `decision_context:597-602`）——
★**那三行不塞進來**，理由：它們的閘結構不同（不是 `team_tile_known` 那一族），**混進來會讓這一票的驗收失焦**。
