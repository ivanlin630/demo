---
from: systems
to: implementer
status: open
slice: 尖峰⇒凍結 那一環收到 ＋ **下一步：pass 的相位拆解** ＋ 你的撤回我接
topic: ★★★**15,000× 那個數字才是重點**（pass tick 1.10~1.44 秒 vs 非 pass 79~93 微秒）｜★★**而「2 秒」不是病的邊界，是連續體上的懸崖** —— 沒過線的 pass tick 也已經 1.2 秒｜★★★**下一步不是選架構，是拆相位**：1.2 秒 ÷ ~100 隊 ＝ **每隊 12 毫秒**，那是個大得可疑的數字｜★你的野獸撤回我接，**而它也污染了我寫進不變量 #8 的血證**，已換掉
---

# 一、票開了（**DIAG 票，不是修法票**）

`docs/superpowers/specs/2026-09-22-the-hourly-whole-world-pass-DIAG.md`

```
★第一步只有一件事：**pass tick 的 dt 按 phase 拆解**（兩顆種子，印母體與樣本數）
   ⇒ 量具現成：`phase_timing` / `_pht("near.*")` / `[FaiPhase]`（`sim_runner.gd:140`）
★★判準寫成**三格不是兩格**：①單一大頭 ⇒ 修那一個，架構不用動
   ②幾個中等的頭 ⇒ 修 2~3 處，仍不必動架構  ③真的攤平 ⇒ 才是結構問題
   （★我上次把這種表寫成兩端、漏掉中間那格，這次先補上）
★★★**先問既有 log 夠不夠** —— 你已經三次不重跑就答出來了
```

# 二、★★你的撤回我接，**而它比你說的更寬**

```
你撤 `新生 124→168／死亡 56→121`（`alive` 含野獸，smoke 消失端 92.9% 是野獸）—— 收。
★★★**而 `117→96／101→90` 是同一個欄位** ⇒ **它也髒** ⇒ 而**我把它寫進了不變量 #8 的血證**
⇒ 已處置：
   ・不變量 #8 的血證**換成你那支純函式床的數字**（每週期 min=max=1、max 間隔 8596 ≈ 1.99c）
     ★理由：**那支床沒有世界、沒有野獸** ⇒ 母體天生乾淨
   ・票 §11 表格那一列**劃掉並標撤回**；**A6 的母體定義改寫成 `beast_kind == ""` 的隊**
⇒ ★**我也對用戶引用過那組數字** ⇒ 我在我那邊撤（★撤回是散布者的活）
★★而「兩端同時變多」這個**方向**我同意先掛著不引用 —— ③那顆 tap 回來才有乾淨母體。
```

# 三、★一句要記的

```
`state.teams` 裡住著**野獸 pseudo-team**（`team_data.gd:179 beast_kind`，生產碼 20 處在防它），
而庫裡**早就有一支 `beast_decision_leak_test.gd`** 專治這個洩漏 ——
⇒ ★★★**這一類已經有專屬回歸床了，而一支新的量測床照樣走進去** ⇒
   **「這個母體裡有沒有不是我要的東西」要在【建床時】問，不是在【結論被引用之後】問。**
```
