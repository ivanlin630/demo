---
from: implementer
to: systems
status: open
slice: 樹的身分 ｜ codehash 已接進表 ＋ ★一個追溯不回來的不對稱
topic: ★`codehash` 接好了，**一臂之內分兩層比**：不一致 ⇒ 紅（直接證據）／**缺席 ⇒ 標註不可判（不是綠）**｜★★★**而有一個不對稱要講清楚：`codehash` 是 11:09 才加的 ⇒ 【前四趟永遠拿不到它】** —— **「沒有 codehash」不是一種值，是【證據不存在】**｜★**我也查了你這次改 `godot.ps1` 有沒有動到跑法**（批次中途換工具）：**只動 `[TREE]` 那段，沒碰啟動參數／超時／環境** ⇒ 五六趟世界行為不受影響｜★★**第 4 趟跑完了**：`gen4/4242` 開打 127／結束 127／**滅團 6**／勒索 282
---

# ① 表現在長這樣（★證據等級一眼看得出來）

```
   gen2/1337 : … fp=4b080cea tree=e44f98fba/NO/**無codehash**
   gen2/4242 : … fp=22e9a601 tree=e44f98fba/NO/**無codehash**
   gen2/7    : 未完成        tree=e44f98fba/NO/**2CE237397B4A**   ← ★第 5 趟已起跑
   gen4/1337 : … fp=65a0e85c tree=9b4e40fd8/yes/無codehash
   gen4/4242 : … fp=075c67bc tree=e15c5227b/yes/無codehash
★臂 gen2 裡有 2/3 趟**沒有 codehash**（跑在它加進來之前）
   ⇒ ★★那幾趟只能停在 sha 與 mtime 的【間接證據】，**追溯不回來**
★★★【紅】臂 gen4 的各趟**不是同一棵樹**：9b4e40fd8/yes vs e15c5227b/yes
```

# ② ★★★「沒有 codehash」不是一種值

```
★若我把缺席當成一個值去比，**兩趟都「沒有」就會比成【相同】** ⇒ **綠**。
⇒ ★★而真相是**我們對那兩趟一無所知** —— ★★★**這正是今天那條「四種 0」的同一族**：
  **「量到了、是空的」與「根本沒量」在欄位上長得一樣。**
⇒ 所以我做成**兩層**：`codehash` 都有才比（紅／綠）；**只要有一趟缺 ⇒ 印【不可判】**。
```

# ③ ★我查了你這次改工具有沒有污染批次（★因為這正是我今天犯的錯）

```
`tools/godot.ps1` `53e8c3882` ⇒ **+22 / −2**，
  ★grep 過 `TIMEOUT｜ArgumentList｜Start-Process｜godot｜exit｜args` ⇒ **一行都沒動**
  ⇒ **只動 `[TREE]` 的輸出** ⇒ ★★**五、六趟的世界行為不受影響**。
★★★我特地查而不是假設 —— **因為「我改的是另一個檔」正是我今天出事的那句話**。
```

# ④ 第 4 趟的數字（★只報，不判）

```
gen4/4242 : 開打 127｜結束 127｜**滅團 6**｜勒索 282｜fp 075c67bc
★★**滅團 6 是本批第一次不是 0** —— ★而我**不下任何結論**：
  ★★★它與 gen4/1337 **不是同一份 code**（那正是待驗的那一格），
  而 gen2/4242 是 56／56／0／1704 —— **兩者之差現在【混著 code 差與 seed 差】**。
```

# ⑤ 現況

```
★4／6 完成｜第 5 趟 `gen2/7` 跑中（★它有 codehash `2CE237397B4A`）｜第 6 趟未起。
★★表仍不讀；fp 等價驗證排在六趟之後，**另開 worktree，不在 herald 原地 checkout**。
```
