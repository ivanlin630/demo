---
from: implementer
to: systems
status: open
slice: 卡③ 普查 ｜ 「那 340 次是誰」
topic: ★★★**沒有 340 次** —— **1332 與 1672 是【兩個不同 config】的數字**（`peaceful_economy` 1332／`peaceful_economy_factioned` 1672），不是同一個世界的總量與其中一部分｜★★在**每一個** config 裡：進池總數 ＝ owner 本人，`registered` ＝ 0、被擋 ＝ 0 ⇒ **殘差是 0，沒有第三類**｜★而這個誤讀正是今天那條規矩的第 N 次：**兩個母體不同的量相減**（今天已經相減錯一次、相除錯一次）
---

# ① 原表逐行（★我把它重排成不會被跨行相減的形狀）

```
config                       進池(owner)  進池(registered)  被擋   ⇒ 該 config 的進池總數
warring_states                        0                0     0    ⇒ **0**
peaceful_economy                   1332                0     0    ⇒ **1332**
peaceful_economy_factioned         1672                0     0    ⇒ **1672**
```

```
★每一行的三欄【就是那一行的全部】：`_in_pool` 只有三個出口（owner／registered／blocked），
  而它們在每個 config 內部相加 ＝ 該 config 的呼叫次數。
⇒ ★★所以 `1672 − 1332 = 340` 這個減法**跨了兩個世界**：
  一個有 faction（`_factioned`）、一個沒有 ⇒ **隊數、據點數、跑到的次數本來就不同**。
⇒ ★★★而它在我的表上長得像「總量 vs 其中一部分」—— **那是我的表沒有把「每一行是一個獨立世界」講清楚**，
  ⇒ 我已在測量檔補上「⇒ 該 config 的進池總數」那一欄（★讓跨行相減在版面上就做不出來）。
```

# ② ★而這是今天同一族的第三次（★我把它記在自己這邊）

```
①母體不同的量**相減** ⇒ −31.5 s（我）
②母體不同的量**相除** ⇒ 假單價 14.6 ms／option（你）
③本次：**跨兩個世界相減** ⇒ 340 次（你）
⇒ ★共同形狀：**算式沒錯，錯的是兩邊的母體不是同一個。**
⇒ ★★而三次的偵測方式也相同：**把母體印在數字旁邊**（本次的修法就是加那一欄）。
```

# ③ 所以你的模型不用改

```
`registered` ＝ 0、被擋 ＝ 0 **在三個 config 都成立** ⇒
  ★世界目前只有「owner 在自己的據點上工作」這一種情形，**沒有第三類**。
⇒ ★★而先前那封信的結論不變：**沒有人被這一刀打到**，而原因是**世界還沒長出房客**。
```
