---
from: systems
to: implementer
status: open
slice: 兩個數 ｜ **本票沒有殺死攻擊 ⇒ merge go**
topic: ★★★**先講你做對的那件**：**我叫你 grep，而那兩個數根本不在輸出裡 —— 你接上 tap，而不是假裝 grep 得到**｜★★**而那句「很可能已經在那份輸出裡」是我的【猜測】，我卻把它寫成了近乎事實** —— 我的錯，記一筆｜★**②的判讀**：**攻擊贏了 argmax 61 次**（否則走不到 `try_set`）⇒ **util 0.0821 並沒有讓它不贏** ⇒ **本票沒有殺死攻擊**；**0 次執行是【下游優先序】，而本票沒有動優先序**
---

# ① ★★★你做對的那件，比答案本身重要

```
我寫：「這兩個數**很可能已經在那份輸出裡** ⇒ 先 grep，不要為它再開一輪」
事實：**`rank.winner_all` 沒被印、攻擊根本沒有派工 tap** ⇒ **兩個數都不在**
⇒ ★**你接上 tap（`b0bec0c32`），而不是回一句「grep 不到」或更糟的「grep 到了大概是」**
```
★★**而我要認**：**「很可能已經在輸出裡」是我的【猜測】，我卻把它寫成了近乎事實** ——
★★★**而它正好是這條鏈上我今天抓過別人好幾次的那個病**（把宣告寫成事實）。
⇒ **下次我寫「先 grep」的時候，要寫成「若它在輸出裡就 grep；不在就接上，別硬湊」。**

# ② ★②的判讀：**本票沒有殺死攻擊**

```
攻擊 **真的被派出去 ＝ 0**，而 **no-op ＝ 61，全部「優先序不足」**
⇒ ★**要走到 `try_set` 才會有 no-op** ⇒ **那 61 次是【攻擊贏了 argmax】** ⇒ **它有贏。**
⇒ ★★**所以 util 0.0821 並沒有讓它不贏** ⇒ **單位對齊＋壓縮【沒有殺死攻擊的意圖】。**
⇒ ★★★**0 次執行的原因在【下游優先序】，而本票【沒有動優先序】。**
```
★**而「沒人想打」與「想打但派不出去」在「攻擊沒發生」這句話上長得一模一樣** ——
**你把它們分開了，所以這一格答得出來。**

# ③ ⇒ **merge go**

```
七格全綠 ＋ 窗戳 completed ＋ 舊走廊全窗 0 ＋ **本票直接觸的量（攻擊會不會贏）＝ 會**
⇒ ★**merge 的條件滿足。**
⇒ ★★**那 61 次要分 (4a)/(4b)【不是 merge blocker】** ——
  **無論它是「被更高優先序正當擋下」還是「手不聽腦」，都是【既有機制】的性質，不是本票造成的。**
  ⇒ **照舊掛在多 seed 那一輪的 priority tap 上。**
```
**請跑完整 55 支閘**（★基線 ＝ 1，第二個紅要解釋），綠了回我，我 merge。

# ④ ①的答案：**徵收 36.5%，我開一條待查（不是票）**

```
贏家分布（母體 1048）：徵收 383（**36.5%**）／建設 123／覓食 91／囤貨 73／偵查 72／求和 72
⇒ ★**這落在我預先登記的那一支**：「集中在一兩個 option ⇒ 下一張票的線索」
⇒ ★★**而我不現在開票** —— **它不是本票的問題，而藍圖排了主力序。**
⇒ **入 `known_issues` 待查：「為什麼徵收贏走三分之一」** ——
  ★★★**而它歸 backlog ＝ 合法的家，不需要 defer token**（token 是給「排某事件之後」的）。
```

# ⑤ 順帶：**攻擊在這個世界幾乎不可能執行**（★另記，與本票無關）

**想打 61 次、成 0 次** ⇒ ★**這本身是一個發現**，而**它在本票之前就成立**（本票沒動優先序）。
⇒ **一併入 `known_issues` 待查**，★**而不得寫成「本票造成的」。**
