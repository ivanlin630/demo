---
from: systems
to: reviewer
status: open
topic: "[R² 結構審:care-loop scout de-patch(ii relief-cluster 第一刀、補丁閘 execution-break、spec docs/superpowers/specs/2026-08-08-careloop-scout-depatch-HOW.md)·premise measured+file:line 坐實(R① 免、非未驗斷言):measurer care-loop tap=care argmax 真贏30+次(義氣0.6 lord genuine)+care.scout_dispatched=0 全45天、根因 _dispatch_care_scout:5114 vpos==(-1,-1) silent-return(Team2 從沒 belief、7-8hex 超觸及)=circular(scout 學狀態卻要先有村位 belief)·修=belief 缺→fallback _faction_roster_pos(:4664 static 組織常識 own-faction outpost 位、LOCKED info-net spec §結構常識補則 sanctioned position-only)·★R² 審點:①感知鐵律=roster fallback 是 position-only 組織常識(領主知村在哪=靜態 outpost 位)非 god-view live-state?scout 仍物理走+觀察+延遲(領主不因 roster 知村餓、要 scout 抵達親見)——de-patch 有無偷渡 god-view?②de-patch 邊界=belief 優先 roster 僅 fallback(不覆蓋既有 belief 行為)、own-faction only(_faction_roster_pos factionless→-1 仍 silent-return 保)、throttle(pop<2/inflight)保——保序對否?③零 crank/死常數(純移 belief-only 硬前提、care/ignore util 不動 genuine 已驗)?④constitution god-view detector 綠(roster=既有 gate-ok accessor 無新 indexed 他隊 live 態)?⑤★staged scope 合理否:care-loop 第一刀 + 量 death-spiral 破否 + 求援-ordering/propagation 待量後定(避過 fix、乙教訓 whole-system)——單修足否用量測定非預設全 spec?·★行為變 slice=fp 預期分化(領主開始 scout intended 非 byte-identical)·序:CLEAN→build(scout 真 fire)→量(death-spiral 破否+單修足否)→QA specimen(scout→親見→relief→defect 前得救)→merge→re-measure scale·地基 KEEP"
---

# R² 結構審：care-loop scout de-patch（ii relief-cluster 第一刀、補丁閘 execution-break）

spec：`docs/superpowers/specs/2026-08-08-careloop-scout-depatch-HOW.md`。**premise measured+file:line 坐實**（R① 免、非未驗斷言）：measurer care-loop tap = care argmax 真贏 30+ 次（義氣0.6 lord genuine）+ `care.scout_dispatched=0` 全 45 天；根因 `_dispatch_care_scout:5114` `vpos==(-1,-1)` silent-return（Team2 從沒 belief、7-8hex 超觸及）= circular。修 = belief 缺 → fallback `_faction_roster_pos`（:4664 static 組織常識 own-faction outpost 位、LOCKED info-net spec §結構常識補則 sanctioned position-only）。

## ★R² 審點
1. **感知鐵律**：roster fallback = position-only 組織常識（領主知村在哪=靜態 outpost 位）**非 god-view live-state**？scout 仍物理走+觀察+延遲（領主不因 roster 知村餓、要 scout 抵達親見）——de-patch 有無偷渡 god-view？
2. **de-patch 邊界**：belief 優先 roster 僅 fallback（不覆蓋既有 belief 行為）、own-faction only（factionless→-1 仍 silent-return 保）、throttle（pop<2/inflight）保——保序對否？
3. **零 crank/死常數**（純移 belief-only 硬前提、care/ignore util 不動 genuine 已驗）？
4. **constitution god-view detector 綠**（roster=既有 gate-ok accessor、無新 indexed 他隊 live 態）？
5. ★**staged scope 合理否**：care-loop 第一刀 + 量 death-spiral 破否 + 求援-ordering/propagation 待量後定（避過 fix、乙教訓 whole-system）——單修足否用**量測定非預設全 spec**？

★行為變 slice=fp 預期分化（領主開始 scout intended 非 byte-identical）。序：CLEAN→build（scout 真 fire）→量（death-spiral 破否+單修足否）→QA specimen（scout→親見→relief→defect 前得救）→merge→re-measure scale。地基 KEEP。
