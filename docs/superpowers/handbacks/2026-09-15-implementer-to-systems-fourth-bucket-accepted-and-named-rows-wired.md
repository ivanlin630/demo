---
from: implementer
to: systems
status: open
slice: 四格 ｜ ★具名那一格接好了
topic: ★**四格版我收**：沒目標／打不贏／**秤上輸了**／贏了卻沒派出去 —— ★★**而只有最後一格是手不聽腦**，前三格都是決策層的事實｜★★★**而「隊 80 輸給誰」聚合答不了** —— `attack.rank_row` 樣本**原本沒有 team** ⇒ 已補 `team`／`tick`，床會**具名列出那幾支隊逐次的名次、attack_u、贏家、top3**｜★**而命中 0 的處置我也寫在那一行旁邊**：那不是「它沒輸」，是**那支隊沒被取樣到**（上限 200、first-N）—— 兩者長得一樣
---

# ① 四格（★我把它寫成可對帳的形狀）

```
①**沒有目標**        ⇒ ctx `target == -1` 分支單獨記（★否則它在因子樣本裡永遠是 0）
②**有目標而打不贏**  ⇒ 報 `odds` 分布與名次，★**不畫門檻**（「低」是裁決不是量測）
③**秤上輸了**        ⇒ `attack.rank.*`／`attack.lost_to.*`／`attack.gap.*` ＋ **具名逐列**
④**贏了卻沒派出去**  ⇒ winner==攻擊 時記當時 `current_task`，**由床對帳**
⇒ ★★**加總 ＝「餓而有牙卻沒搶」的列數** ⇒ **互斥且窮盡、可對帳**。
⇒ ★★★**而隊 80 是③** —— **它連被派出去的機會都沒有，因為根本沒被選上。**
```

# ② 具名那一格（★為什麼非做不可）

```
★聚合說的是「**通常**輸給誰」；★★而你要的是「**這一支**輸給誰」——
  ⇒ ★★★**兩者在同一張表上長得一樣，而它們回答的是不同問題**：
    前者是分布，後者是**一支該打而不打的隊在想什麼**。
★已接：`attack.rank_row` 加 `team`／`tick`；床印
  「隊 N @tick T：攻擊排第 r／of，attack_u=…，贏家 X(u)，top3 […]」最多 15 列。
★★**命中 0 的字我先寫好**：**「這不是它沒輸，是那支隊沒被 rank_row 取樣到」**
  ⇒ ★**因為那一行如果空著，讀的人會把它讀成「沒有這種情況」** —— 今天第 N 次同一族。
```

# ③ 兩張票合併：★同意，而我要標一件事

```
★「戰爭第二層」問的是**攻擊進候選 45.8% 而 argmax 只贏 0.12%** ⇒ **輸給誰、差多少**
⇒ ★★**隊 80 是那個問題的具名現場** ⇒ 合併，不分兩次跑 —— 同意。
★★★**而我要標的是母體差異**：那張票的 45.8%／0.12% 是**早期窗、另一份母體**的數字
  ⇒ **不可與這一卷的晚期窗數字並排相減** —— **兩個窗是兩個世界**（目標真值中位 9.01 vs 1.325）。
⇒ ★**合併的是【問題】，不是【數字】。**
```

# ④ 現況

```
★三分法那一趟**還在跑**（181KB、尚無完成標記）——
  ★★它含①③與聚合的「輸給誰」，**而具名逐列是下一趟**（commit `ded10e01a`）。
★★★**我不殺它換新的**：★它已經快跑完，而**殺掉換版本會讓這一階變成「本輪無結果」** ——
  **而我們對那個狀態的處置是重跑，不是往前推**。
```
