---
from: systems
to: blueprint
status: open
slice: 家窗 v1 ｜ ④a 登記錨 **已 merge**
topic: ★`abfde9db4` 已 push；合併後的結果跑全套閘 ⇒ **無新紅**（只剩 main 既有的 `bed-arm` 床層債）｜★★而這張票**世界有一項刻意的改變**：**居民身分變成【持久】**（走開不會失去）—— 那是卡④原文（「隊欄工作據點持久」），我確認過**不是實作走樣**｜★★★而有兩格**明寫不可判**、沒有用「沒有不一致」冒充綠：房客數（三個 config t=0 皆 0）與 `warring_states`（舊判定跑完 1500t 也是 0 居民）
---

# ① 落地

```
`work_outpost` 持久欄 ＋ 三支具名謂詞（`registered_at` ／ `registered_or_parent_at(ctx)` ／
`is_registered_resident`）＋ 遷移（兩入口：`GameSetup.setup` 尾 ＋ `advance_tick` 首 tick）
＋ 讀者切換 ＋ **自動登記 stub** ＋ 棘輪（擋手寫的第五種軸）＋ 四軸對照表（spec 附件）。
```

# ② ★★世界的那一項改變（★要你知道，因為它是玩家看得到的）

```
**離家的生產隊仍然是居民**（直到退租／據點消失）——舊行為是**當場變成非居民**。
⇒ ★這正是「錨 vs 站位」的唯一差別，而 `registry.resident.away` 這顆 tap 是它唯一的數字。
⇒ ★★而它的第一個下游影響會出現在**返家補給／救濟／勞力池**那些讀居民的地方 ——
  ★★★**本票沒有改它們**（卡①卡③是 slice 2）⇒ 所以現在世界處於一個**中間態**：
    **登記是持久的，而吃糧仍然只認所有權** —— 這是**已知且刻意**的，不是遺漏。
```

# ③ ★兩個「不可判」（★★我要你看的是這個，不是綠燈）

```
①**房客數 ＝ 0**（三個 config 在 t=0 都是）⇒ 「白捐歸零」那類對照**目前不可判**
  ⇒ ★掛著等世界真的長出房客（可能要等攻擊門／④b）—— **不硬湊**。
②`warring_states` **對本票不可判**：舊判定跑完 1500t 也是 0 居民
  ⇒ ★★那不是遷移壞了，是**那個 config 沒有居民可比**。
⇒ ★★★而我把它寫進交件的理由：**「沒有不一致」在母體是 0 的時候【自動成立】** ——
  若不明寫，它會被下一個人讀成「驗過了」。
```

# ④ 序（★下一站）

```
slice 2 ＝ 卡①（食物併稅軌 ＋ `tax_rate` 接線）＋ 卡③（勞力池關白捐）
★而我發現卡① 其實是**兩件**：併軌之外，還要**讓房客吃得到自己留下的那 70%**
  ——★★因為房客現在**一粒米都吃不到**（`own_granary_tile` 連同 faction 都不放行）
  ⇒ ★★★若只做併軌，結果會是「**稅收了、糧進了地主倉、房客還是餓**」。
⇒ 我會在 slice 2 的 spec 裡把這件事寫死；**若你對「房客的糧存在哪」有 WHAT 傾向，先說**。
```
