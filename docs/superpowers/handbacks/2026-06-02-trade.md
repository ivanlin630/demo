# Hand Back: Trade Preview

## 實作摘要
- `scripts/simulation/interaction_system.gd` — 新增 `preview_trade()` 方法，根據雙方 food/coin 比例計算玩家付出/獲得的預覽資料
- `scripts/simulation/player_query_api.gd` — 新增 `get_trade_preview()`，呼叫 InteractionSystem.preview_trade 並包裝成 query envelope
- `scripts/ui/sim_bridge.gd` — 新增 `query_trade_preview(target_team_id)`，委派給 _query_api
- `scripts/simulation/player_command_system.gd` — "trade" case 改為儲存 pending_trade_target 並回傳 requires_preview=true；新增 "confirm_trade" 和 "cancel_trade" cases
- `scripts/simulation/player_command_api.gd` — execute_action 新增 pass-through：若 result 含 requires_preview / preview_target_id，轉入 payload（原本 map_command_result wrapper 會丟失這些欄位）
- `scripts/ui/popup_layer.gd` — 新增 `show_trade_preview()`，顯示付出/獲得明細，確認/取消按鈕
- `scripts/ui/main.gd` — `_on_interact_execute` trade 分支：由 placeholder 改為完整實作（呼叫 query_trade_preview、show_trade_preview）

與 spec 的差異：
- `player_command_api.gd` 需額外修改以 pass-through requires_preview 欄位，計畫中未提及但為必要修正

## 連動風險
- `scripts/simulation/player_command_api.gd`：修改了 execute_action 的 payload 建構邏輯，其他使用 result.payload 的地方若有依賴固定欄位應確認不受影響（目前已知只有 main.gd 讀取 payload）
- `state.player_state["pending_trade_target"]`：新增 state key，若其他系統讀取 player_state 做清理（如 clear_pending_targets）可能需要一併清除此 key

## 待主 session 確認
- `preview_trade()` 的邏輯（food/coin 比例）是簡化版，與真正的 `resolve_trade_direct()` 計算邏輯不完全一致；確認是否需要更精確的預覽
- pending_trade_target 在玩家移動或場景切換時是否需要自動清除（目前 cancel_move 不清除）
