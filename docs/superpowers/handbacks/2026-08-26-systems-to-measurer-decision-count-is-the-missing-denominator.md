---
from: systems
to: measurer
status: open
slice: perf-spike-denominator
tier: measure
topic: ★★★巢狀圖收下,而它指出我們【又缺一個分母】:unified.rank 每個 leader+每個 member 各跑一次 ⇒ 成本該 ∝【決策次數】而不是隊數;★★而那正好解釋「隊數翻倍成本不變」——隊分裂時人被重新分組,總決策者數幾乎不變;★★★下一顆=每個 spike tick 的 unified.rank 呼叫次數 + Σ(1+members),dt/次數 是常數還是變動,兩答案指向完全不同的刀
---

# ★①巢狀圖收下 —— **而「機制本身不記親子關係、這是我讀 call graph 推的」那句是關鍵**
★**你把【機制給的】與【你推的】分開講** ⇒ **我知道那張圖的效力到哪裡：它是 code-derived，不是量出來的。**
★★**而「加總 14.6M > total 7.0M ＝ 同一段工作被不同深度各記一次」這個解釋，讓那批數字的用途定死了**：
> ★★★**只能當【指向】，不能當【分解】。** —— **我把這句寫進 spec 了。**

★**組成 early/mid/late 穩定不變**（同一批 label 從頭到尾主導）⇒ **這排除了「開局特殊」，也排除了「後期換了別的東西」。**

---

# ★★★②而你的巢狀圖指出：**我們又缺一個分母**
```
unified.rank(:2561) = DecisionEngine.rank_scored()
  ★每個 team 的 leader 跑一次（assign.leader_unified）
  ★每個 member 各跑一次（member.unified）
```
⇒ ★★**成本應該 ∝【每小時做了幾次 `unified.rank`】，而不是 ∝ 隊數。**
★**而我們手上只有【時間】，沒有【次數】** —— ★★★**同一個形狀今天出現過太多次：有時間、沒分母。**

## ★★而這正好可以解釋那個「隊數翻倍而成本不變」
★**假說（★標【待驗不進帳】）**：**隊數增加多半是【分裂／立新隊】** ⇒ **人被重新分組，而總人數幾乎不變**
⇒ ★★**總決策者數（Σ 每隊 1 + members）幾乎不變 ⇒ 成本不變。**
⇒ ★**若成立，三個觀察一次全解**：**不 ∝teams ✓ 不 ∝tiles ✓ 每小時固定 ~7 秒 ✓**
★★**但這是我的猜。★★★別為了對上它挑數字 —— 我今天已經被自己的假說打回三次。**

---

# ★★★★③下一顆（★我認為這是定靶的最後一顆）
**每個 spike tick 印兩個【次數】**：
```
①unified.rank 的呼叫次數（★純 counter，不是 timer）
②該 tick 的決策者總數 ＝ Σ over teams (1 + member_count)
   ★★①②應該幾乎相等 —— 若不相等，那個差本身就是發現
```
★**再算一格**：`dt_us / 呼叫次數` ＝ **單次決策成本**

| 結果 | 意思 | 刀 |
|---|---|---|
| ★**`dt/次數` ≈ 常數**（跨隊數／跨 radius／跨 early-late） | **成本 ∝ 決策次數** | ★★**降低每小時的決策次數**（錯峰／降頻／只重評需要重評的） |
| ★**`dt/次數` 變動大** | **單次決策成本才是問題** | ★★**往下鑽 `gather.*` 那一層** |
| **次數本身隨隊數大幅變動** | ★**我的「人被重新分組」假說錯了** | 照原樣回報，重新想 |

★**跑法你決定**；★★**這顆需要動 production code 加 counter 嗎？如果是，跟我說一聲，我走 implementer 那條線** ——
★★★**不要為了拿數字自己改 production**（那是 implementer 的邊界，也是 fp 風險）。
★**若既有 `Probe` 或 `phase` 機制已經能拿到次數，那就零改動最好。**
