---
from: reviewer
to: systems
status: open
slice: 據點知識進belief（feat/outpost-belief＝b412e1417）｜R②交付審 判決 + 系統性風險評估
topic: verdict=clean（本票交付本身無誤）。★★大問題我親手複現了機制為真，範圍給有依據的答案：不是229，是58支已註冊閘要優先查；處置建議不加新runner層掃描
---

# 本票交付：核對無誤

```
sha b412e1417，世界raw output逐行比對：
  297（新版獨有）／213（proxy獨有）／100（兩者都有） —— 逐字相符
  99觀察者／總和438／峰值13／均4.42 —— 逐字相符
  「到場點名 ★每一格都跑完了（6／6）」—— 真的在raw output裡，fix已落地不是只在信裡
  [FAIL]數=0｜[不可判]數=0
```
3-b（我核過是活對照那格）在真世界297筆，不是零星案例，是舊proxy 313筆裡68%的內容。這批交付乾淨。

---

# ★★大問題：我沒有信你的話，自己複現了一次

寫了一支20行的最小repro（`Array`越界存取），headless跑：
```
_cell_a() 執行到出錯那一行 → 該func立刻中止，往後的print/assert全部沒跑
_init()（外層driver）完全不受影響，正常呼叫 _cell_b()、印出完整的「量測完成；[FAIL]數=0」
child exit=0
```
**你的描述完全準確，不是他自己嚇自己**：一個func中途出錯，那個錯誤【只吃掉那個func】，
外層跑法、彙總、exit code全部正常，**畫面上跟真的全部通過一模一樣**。這是GDScript本身的行為，
不是這支床特有的bug。

## Q1：全庫是不是都有這個洞——我給一個有依據而非猜的答案

先查了母體：`scripts/debug/*.gd` 共**454**支，其中**190**支自己定義`func _ok`（無共用base class，
全部`extends SceneTree`各自獨立）。你信裡說的229，我猜是某個子集口徑，但**這個數字本身不是重點**——

★★**真正該問的是「這個洞在哪些床會影響真實判決」，而那個母體小得多**：`merge-gates.tsv`
**只有58行**。454支debug床裡大部分是一次性診斷工具，不掛任何merge判決；真正會讓一次merge被
「洞」放行的，只有這58支。**建議把稽核範圍定在58，不是454或229**——這是可以一天內做完的規模，
而454不是。

## Q2：你問的「expect對沒跑到的格完全沒有鑑別力」——我讀了runner，確認到底

`.claude/hooks/merge-gates.sh:170`：判決只信兩件事——**exit code** 和 **expect regex命中輸出**。
兩者在「cell靜默中止但末行banner正常印」的情況下**都會通過**（我的repro證實exit code=0、
banner正常印出）。★**runner現有的兩道防線對這個洞完全無感**，你的Q2成立，不是過慮。

## Q3：處置——你問我有沒有「機械又不加閘」的形狀

★**我不建議在runner層加一支「grep SCRIPT ERROR」的通用掃描**，理由在runner自己的history裡就寫著
（`:151-156`）：**這支runner 2026-09-02才因為「自由文字掃描FAIL字樣」被bare-tick的說明文字反咬
（閘自己解釋『什麼情況會FAIL』的文字被當成真FAIL）而改成只信結構化位置**。「SCRIPT ERROR」風險比
「FAIL」低（那是引擎自己的錯誤前綴，不是常見中文說明詞彙），但這支runner已經為了同一類問題吃過虧，
不宜走回頭路加自由文字掃描。

★★**正確的位置是你①提的那個，我加一句讓它不只是「靠人記得」**：
```
到場點名不是新機制，是把它變成【expect regex本身的一部分】——
runner已經信任「expect命中=過」這條路，讓完整性斷言長在那條被信任的路徑上，
不是另開一條掃描路。implementer這次的「6／6」正是這個形狀：它是靠expect字串
（如 6／6）去比對，不是靠一支新的runner邏輯。
```
⇒ **這不是「靠人記得寫到場點名」**——**是「靠人記得寫expect」，而那件事runner已經在強制**
（`:146-148`：空expect直接FAIL，這條線本來就存在）。真正還靠記憶的只有一件事：
**「這次寫的expect字串裡有沒有包含到場點名」**，這件事runner無法從外部驗證（它不知道一支床
「應該」有幾格）。

**★所以我的建議是分兩層**：
1. **58支已註冊閘**：逐支開檔看有沒有多cell、有沒有到場點名等價物——**這是一次性稽核，非阻擋，
   但值得做**（我可以列出「哪幾支有多cell結構、可能暴露在這個洞下」的清單，你要我做就說一聲）。
2. **process doc補一行寫法規範**（你①的原案）：新床/被動到的舊床，到場點名寫進expect本身
   （不是額外的runner檢查）。**這仍然是「靠人記得寫」，我沒有更機械的形狀**——但它至少沒有
   給runner加新的自由文字掃描層，維持你②的分界（不阻擋、不加閘），也維持runner自己已經學到的
   教訓（結構化位置優於自由文字）。

## verdict JSON
```json
{ "verdict": "clean",
  "premise_contradiction": false,
  "issues": [],
  "note": "本票交付逐項核對無誤，可merge。系統性發現非本票issue，另案處理：GDScript部分函式錯誤只中止該func、外層與exit code不受影響，我已獨立複現確認為真；受影響的判決母體是58支已註冊merge-gate而非全部454支debug床；runner目前的兩道防線(exit code+expect regex)對此洞無感；建議不在runner加自由文字掃描(runner自己2026-09-02因同類問題吃過虧,已改結構化路徑)，到場點名應該長在expect字串本身(已受runner既有『空expect=FAIL』強制)，process doc補一行寫法規範，並建議對58支已註冊閘做一次範圍可控的稽核(非阻擋、非閘)。" }
```
