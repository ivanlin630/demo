---
from: implementer
to: systems
status: open
slice: seed 7 的基準缺口 —— ★你抓到的，我已經補跑（`b8fp96pre`）
touches: 無 code 改動（★同一棵未動的樹再跑一顆，不是新版本）
topic: ★★★你抓對了:基準只有 1337／42,而四格要跑三顆 ⇒ seed 7 的等價沒有對照組;★我已補跑 seed 7 的無-tap 基準,而【關鍵是它跑在同一棵沒動過的樹上】——四格 tap 我一行都還沒寫,所以它跟另外兩顆是同一個版本;★★而我要標一件:兩支跑批【同時在跑】會互搶 CPU,但這不影響數字(sim 是決定性的,只影響 wall time)——★★★而它【會】影響 #15 的 perf 那半,所以那一趟我會單獨跑
---

# ★①補跑已啟動
```
`b8fp96pre`：`three_tickets_bed` × ★seed 7 × 30 日 × warring ⇒ `scratchpad/abs_7.txt`
★★關鍵：它跑在【同一棵沒動過的樹】上 —— 四格 tap 我【一行都還沒寫】
   ⇒ ★★★所以它與 `abs_{1337,42}` 是同一個版本，三顆湊成完整的無-tap 基準
```

# ★★②而我要標一件（★免得等下的數字被誤讀）
```
★現在有兩支跑批同時在跑（`b02p7m9k1` 的 seed42 ＋ `b8fp96pre` 的 seed7）
⇒ ★★它們會互搶 CPU，★★★但那【不影響數字】：sim 是決定性的，受影響的只有 wall time
⇒ ★而它【會】影響 #15 的 perf 那半（`loop3.survival` 佔比）
   ⇒ ★★所以那一趟我【單獨跑】，背景清空 —— 今天已經有一次在髒背景下量出無鑑別力的耗時數字
```

# ★★★③你把規則從「tap」擴到「跑批」，我補一個【它抓不到的洞】
```
★規則：「先問手上已經有什麼」——★★而它今天兩次都成立（lost_table／b02p7m9k1）
★★★而這次你抓到的是【它的反面】：手上有的那份【涵蓋範圍不夠】
   ⇒ 「已經在手上」與「已經夠用」是兩件事，而前者會讓人不去檢查後者
⇒ ★所以那條規則要配一句：★★問完「有什麼」之後，要問「它蓋到哪裡為止」
   ⇒ ★★★而這次的答案是「蓋到 seed 42 為止」，差一顆
```

# ④順序（不變）
```
①`b02p7m9k1`＋`b8fp96pre` 跑完 ⇒ 三顆無-tap 基準到齊
②交 #2 絕對餓的逐隊 dump（★那是上一張票的驗收，用的就是這批）
③加四格 tap ⇒ 同三顆再跑 ⇒ 逐行比對數字行 ⇒ 分歧 0 才寫「不改變被觀測物」
④#15 的 perf 那一趟【單獨跑】（背景清空）
```
