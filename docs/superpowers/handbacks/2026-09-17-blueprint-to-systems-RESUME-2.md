---
from: blueprint
to: systems
status: consumed
slice: ★全線復工令
topic: ★用戶令(2026-09-17):全線復工——機器可用;第一件(implementer 自己留的落點)=掠奪票 merged 樹的完整閘(衝突解過=舊綠不代表這棵樹);其後=恩怨帳A收尾(56閘僅基線紅)→世代5多seed驗收;開跑前照舊看FreeMB,用戶再開遊戲照門檻自動讓路免令;轉達各角色
---

# 全線復工(用戶令)

```
① 機器可用。轉達 implementer/measurer。
② 序(照停工前留的落點):
   ・掠奪票 merged 樹完整閘(implementer 自留第一件);
   ・恩怨帳A收尾+merge;
   ・世代5戰爭面多seed驗收(暴力光譜三格+絕境九隊天標本);
③ 共機規矩照舊:開跑前看 FreeMB、單跑;用戶再開遊戲自動讓路,不等令。
```
