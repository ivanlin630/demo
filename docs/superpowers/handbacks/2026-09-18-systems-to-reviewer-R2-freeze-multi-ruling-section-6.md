---
from: systems
to: reviewer
status: open
slice: 凍結終線｜`*multi` 感知蒐集塊（`feat/freeze-multi-perception`）
topic: R② 審 spec 新增的 §6 裁定｜★核心是一句可能會被我講過頭的話：「**任何讓 `gather` 少跑的修法都會改變世界**」——我的根據有一半是**別人那次量的**，我把它標成待驗前提並排了量測，請你看這個標法夠不夠｜★★另外兩處請專門打：(丙-2)「沒有第二份算式」是不是真的、以及分岔「＝0 就直接做」有沒有第三格
---

# 一、要審的東西

`docs/superpowers/specs/2026-09-18-freeze-multi-perception-block-HOW.md` **§6**（新增，121 行檔的尾段）。
前情：implementer 照 §1 先做陽性對照 ⇒ **閘有鑑別力**；然後他第一版修法（同-tick 記憶化）**被那支閘抓到**（fp 與逐 tick 行為軌跡都變）。

# 二、我裁了什麼（三句）

1. **形狀 (丙-2)**：每欄一支 `static` 純函式為唯一權威，**`gather` 自己也呼它**（組合者，不得留第二份 inline 算式）。
   引的先例：`decision_context.gd:313 pick_recon_target`／`:1069`（gather 呼它）／`options.gd:586`（to_task 呼同一支）。
2. **否決 (甲)**（可記憶化母體只有 44 次、其中 65.9% 與重算不同）、**(乙) 另開票**（需要資料結構，登 defers）。
3. ★★★**前置地雷**：`gather(advance=false)` 至今仍寫 cache／cadence ⇒ **少呼一次就會改排程** ⇒ 甲乙丙**三個都踩同一顆**。

# 三、★請你專門打這三處（我自己覺得最可能錯的地方）

```
(1) ★我把「呼叫次數本身會改變後續排程」當成本票的地基，
    而我的根據是 known_issues 那條【別人那次量的】記錄——★★而它自己寫著一條誠實限：
    「tap 只答這一行被跑到幾次，不答寫進去的值有沒有真的不同」。
    ⇒ 我的處置＝標成【待驗前提】＋排一個三格量測（沒執行／執行了但寫同值／執行了而且改值）。
    ⇒ ★★★請判：這個標法夠不夠，還是我在 §6.3 的語氣已經把它當成【已證】在用？
       （我最怕的是我寫了「待驗」然後在下一段自己把它當前提——這個病我今天犯過。）

(2) ★(丙-2) 我宣稱「沒有第二份算式 ⇒ drift 不存在」。
    ⇒ 這是【構造保證】還是【紀律保證】？如果有人把 `gather` 的某一欄留成 inline，
      那一格會不會有東西紅？★★我目前【沒有】為它排任何一格 —— 這是不是一個洞？

(3) ★分岔我寫成兩條：量到 0 ⇒ 直接做；>0 ⇒ 前置票＋呈報 blueprint。
    ⇒ ★★我的二分法今天已經吞掉過第三格。請幫我找：有沒有一格是
      「寫同值，但【寫的動作本身】還是改變了什麼」（例如 setter 有副作用／dirty 標記／
      或者那一輪剛好沒到期而換個窗口就會到期＝母體問題不是性質問題）？
```

# 四、順帶兩個我自己已經改掉的（不必審，備查）

- 格 1-h 我原本寫「關閉 ⇒ fp == **釘死的乾淨基線字串**」——★**自己抓掉**：那會讓任何合法的世界改動都把床打紅 ⇒ 噪音 ⇒ **連旁邊那支真的紅也會開始被忽略**。改成**同一輪內三跑**（關閉×2 逐字相同 ＋ 開啟×1 必須不同），比較的兩邊只差**注射旗標**一個變因。
- §1 的判準改成**四格**（implementer 實測補：咬到欄位 ≠ 改到行為），已併進 `03_implementer` 既有那條注射規則，**沒有新增條目**。

★**沒有 CLEAN 我不 dispatch (丙-2)**。而 §6.4 那個量測**不含設計選擇**（純取證），我已經先請他去做，不等你。
