# Hand Back: 個人 NPC AI

## 實作摘要

- `scripts/simulation/npc_ai_system.gd`（新建）：完整 NpcAiSystem 類別，含：
  - `write_memory`：寫入記憶、修剪（MEMORY_MAX=20）、更新 relations、觸發 goals
  - `_update_relations`：8 種記憶 type 對應 delta 計算
  - `_trigger_goals` / `_activate_goal`：記憶觸發目標（revenge/gratitude/protect）
  - `generate_birth_goals`：依 values 門檻生成初始目標
  - `check_goal_alignment`：目標與任務對齊分數
  - `_goal_task_delta`：7 種目標 × 多種任務的 delta 表
  - `get_goal_task_override`：目標驅動任務覆蓋（protect/revenge/escape_war）
  - `cleanup_goals`：目標 target 死亡時轉移/回退
  - `_find_revenge_redirect`：仇恨對象死後重定向到其隊伍 leader
  - `_fallback_birth_goal`：依 values 決定回退目標類型
- `scripts/simulation/sim_runner.gd`（修改）：加 `_npc_ai_system` 欄位、`_step7b_npc_goal_cleanup` 步驟插入 `_step7` 之後
- `scripts/simulation/interaction_system.gd`（修改）：`_end_combat` 中戰敗方全員寫 `looted` 記憶、護衛方全員寫 `aided_in_battle` 記憶
- `scripts/debug/headless_test.gd`（修改）：加 Tasks 1-3 驗證（write_memory/relations/birth_goals/revenge_trigger/goal_alignment）

### 與 spec 的差異

- Tasks 1-3 合併成一次 commit（程式碼已全部在同一個檔案，分批 commit 無實際意義）
- Task 4 Step 4（salary_system.gd 薪水超付寫 kindness）**未實作**：`salary_system.gd` 不存在，`_pay_salary` 函式不存在。詳見「待主 session 確認」。
- `aided_in_battle` 記憶觸發點改為「護衛 team 協助勝戰時」（`_end_combat` 中掃 escort teams），而非 spec 的「支援者戰鬥勝利後」（spec 未指定具體 context，採最合理解釋）。

---

## 連動風險

- `reaction_system.gd`：目前 `check_goal_alignment` 已寫完但**未被呼叫**。若未來 reaction_system 需要根據 goal alignment 影響 loyalty/stress，需在 reaction_system 內加呼叫點。
- `faction_ai_system.gd`：spec 提到「`check_goal_alignment` 由 team-ai-redesign 的 task 影響 loyalty 時呼叫」——這個呼叫點在 team-ai-redesign branch 實作，非本 branch 負責。需確認 team-ai-redesign branch 整合時正確呼叫。
- `get_goal_task_override`：已實作但**未被任何系統呼叫**。若要讓個人目標影響 NPC 任務選擇，需在 sim_runner 或 faction_ai_system 加呼叫點。
- `generate_birth_goals`：已實作但**未在 world_generator 中呼叫**。目前 NPC 初始 goals 由 world_generator 設定（headless_test 驗證 goals.size() > 0 通過），但非透過 NpcAiSystem。若要統一邏輯，可在 world_generator 中對每個 PersonData 呼叫 `NpcAiSystem.new().generate_birth_goals(p)`。

---

## 待主 session 確認

### 設計決策

1. **salary_system.gd 不存在**：Task 4 Step 4 要求修改 `salary_system.gd` 的 `_pay_salary` 函式，但此檔案未建立。`salary`/`coin` 欄位存在於 PersonData，但薪水支付邏輯尚未實作為獨立系統。建議：
   - 選項 A：建立 `salary_system.gd`（另開 branch），在 pay_salary 時寫 kindness 記憶
   - 選項 B：暫緩薪水記憶，等 salary system 實作完再補

2. **`generate_birth_goals` 呼叫點**：目前 world_generator 已有 goals 初始化邏輯（test 通過），但並未呼叫 NpcAiSystem。是否要整合？或保持兩套並行？

### 建議後續 task

- 在 `world_generator.gd` 呼叫 `generate_birth_goals`，統一初始目標生成邏輯
- 在 `faction_ai_system.gd` 或 `reaction_system.gd` 加入 `check_goal_alignment` 呼叫，讓目標真正影響 loyalty
- 考慮 `get_goal_task_override` 的呼叫時機（sim_runner 哪個 step？）
- 建立 `salary_system.gd` 並補上薪水超付的 kindness 記憶
