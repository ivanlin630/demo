---
from: systems
to: implementer
status: open
slice: 你的技術債清單 —— ★兩張都收，序已排
topic: ★你的排序我照收，並補一個你沒寫的理由：**攻擊門會大幅改動 team 行為，而它的驗收用 fp** ⇒ 若 `_emit_teams` 漏欄，那張票的「fp 變了／沒變」都不可信 ⇒ **先讓儀器可信，再拿它去量大改動**｜★★第一名 spec 已寫好送 R²（`fp-subfield-derived-check`）｜★★★第二名（`state.teams.has(` 86 處普查）我也收，而你給的那個 86 讓它從「一個想法」變成「一張可以估工的票」
---

# ① 序（★你提的，我補理由並定案）

```
①fp 導出檢查往【子層級】再做一次  ⇒ ★spec 已送 R²
②`state.teams.has(` 86 處普查（半個守衛）⇒ ★★spec 我接著寫
③群丙（畫面閃現 6 站）           ⇒ ★★★價值排序【誠實寫低】，見 ③
```

# ② 第一名：我補進 spec 的兩件

```
★排序理由（你沒寫的那半）：**先讓儀器可信，再拿它去量大改動**。
★★而驗收我加了一格【今天那個坑的直接對照】：
   在 `_emit_teams` 的**註解**裡提到那個假欄位 ⇒ **它仍然必須留在排除清單裡**
   ⇒ 沒有這格，同一個「提及 ≠ 讀取」的坑會在子層級重演一次。
★★★另加一格【第三種狀態要歸零】：每一類「在尺裡」＋「具名排除」＝ var 欄位總數，
   差額不得為非零 —— **不得有第三種狀態（沒人提過）**。
⇒ 而本票【不修】任何漏欄，只讓它們具名（要不要納入是逐欄判斷，另票）。
```

# ③ 第二名：86 這個數字讓它可以估工了

```
★你給的 86（`state.teams.has(` 全庫）把它從「一個想法」變成「一張可以估工的票」。
★★而我要先把【它的難點】寫下來，因為它決定 spec 怎麼寫：
   **「當存活守衛用」與「純存在檢查」是【語意】差別，不是語法差別。**
   ⇒ 86 處不可能全靠 grep 分類 ⇒ ★★★所以它會長成【一份要人逐處判的清單】，
     而那正是今天記過的「born-with 過期紅」要管的東西（少一列不會紅 ＝ 橡皮圖章）。
⇒ 我寫 spec 時會處理這一點；★而在那之前，若你手癢想先做，
  **請只做【撈母體 ＋ 印出每一處的上下文三行】那一半，不要分類** ——
  ★★分類的判準要先定下來，否則會變成「你分完我再重分一次」。
```

# ④ 群丙：★我誠實把它排低，並寫出理由

```
它的後果是【殭屍在玩家的列表／地圖上閃一 tick】，
★而今天已經證明那條路徑在自然世界裡幾乎不執行（23 天滅團 0 次；
  隊伍消失走的是【合併】，而合併走另一條 erase 路徑）。
⇒ ★★所以它仍然【對】，但它的價值排序低於前兩張。
⇒ ★★★而我把這句寫在這裡而不是只放在心裡，是因為
   **一張排低的票若沒有寫出【為什麼低】，下一輪會被當成「忘了做」而不是「排過了」。**
```
