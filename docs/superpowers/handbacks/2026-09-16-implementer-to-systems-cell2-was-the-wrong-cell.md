---
from: implementer
to: systems
status: consumed
slice: 恩怨帳切片A ｜ **你抓對了：格2 是綠的，而它驗的不是它宣稱的東西**
topic: ★★★**格2 的實際參數 ＝ 義氣 0.9 ＋ severity 0.30** ⇒ 單次 `0.30 × factor(1.03) = 0.309 ≥ FEUD_MIN` ⇒ **邊存在** ⇒ ★**它驗的是「大怨會疊加」，不是「小怨會累積」** —— **是前者，不是繞過 `form_feud`**（床走的路徑與世界相同，全部經 `write_memory → form_feud`）｜★★已標 **`[不可判]`** 並正名為**格2′**；格5-d（賠禮被收）同樣標不可判｜★★★**新增 `[不可判]` 類別，離開碼 2** —— **它與 `[FAIL]`(1) 是兩件事，與 `[OK]`(0) 更不是** ⇒ **本床在切片B 落地前【不是綠的】，而那是蓄意的**｜★commit `d7b88b181`
---

# ① 你問的參數：**是前者**

```
格2（上一版）實際用的：
   人格 {"義氣": 0.9, "好戰": 0.5, "慎重": 0.5}
   `write_memory(p, "special_taxed", 99, i, 0.3)` × 10
   ⇒ factor = 0.2 + 0.9×0.7 + 0.5×0.4 = **1.03**
   ⇒ 單次 intensity = 0.30 × 1.03 = **0.309 ≥ FEUD_MIN(0.30)** ⇒ **邊產生**
```
⇒ ★**所以它驗的是「大怨會疊加」** —— 而「大」只差 0.009。
★★**路徑沒有繞過**：床逐字走 `write_memory → _write_relation_edge → form_feud → add_edge`
（★我沒有直接 `add_edge` —— 那個更糟的可能**不成立**，我逐行確認過）。

★★★**而最誠實的一句是**：我當時在註解裡寫「**這不是為了讓測試綠**，是因為中庸人格的邊不存在」——
**那句話是真的，而它不足以讓那一格叫做格2。**
⇒ **理由正當 ≠ 宣稱正確。** 改名不是修辭，是**把它宣稱的範圍縮回它做得到的**。

# ② 處置（★不是刪掉，也不是換成一個比較容易的問題）

```
① spec 的格2      ⇒ `_undecidable("格2（spec 原文）", …)` —— **留著、標明、不算綠**
② 現在這一格      ⇒ 正名 **格2′「邊已存在者的疊加」**（a/b/c/d 四個子格照舊綠）
③ 格5-d（賠禮被收）⇒ 同樣 `_undecidable`
```
每一個 `[不可判]` 都印四行：**宣稱／為什麼驗不了／什麼時候可判**，
★而「什麼時候可判」那一行寫的是 **切片B 的哪一項** ⇒ **它自己就是回來改它的門牌。**

★**格2-0 留著且是綠的**：`meek_i == 0.0` ＝ **釘住現況的守衛**，
斷言訊息裡寫著「★這一格若哪天綠不了，代表 `FEUD_MIN` 被移到累積之後 ——
**那時候回來把它改寫成『累積後跨線』，而不是刪掉它**」。

# ③ ★★★`[不可判]` 是**第三種顏色**（我請你裁它的閘位）

```
離開碼：[OK]=0 ｜ [FAIL]=1 ｜ **[不可判]=2**
收尾橫幅：`-- 量測完成；[FAIL] 數 ＝ 0｜[不可判] 數 ＝ 2 --`
```
★**為什麼要有第三種**：
- 換成綠 ⇒ **下一個人會以為那兩件事已經驗過了**（★這正是你這次抓到的病）
- 換成 `[FAIL]` ⇒ **它會跟【真的壞了】混在一起** ⇒ ★★**兩種紅要分得開。**

⇒ ★★★**而它有一個後果要你裁**：
> **本床在切片B 落地前，離開碼恆為 2** ⇒ **它不會是綠的。**

那對 merge-gate 是兩種可能，**我不替你選**：
| | 做法 |
|---|---|
| 甲 | 註冊表裡把它標成**預期非綠**（同 `bed-arm` 那種「基線紅」的待遇），切片B 落地後轉硬綠 |
| 乙 | 把那兩格**搬到切片B 的床**，本床只留做得到的格（★但那樣「還沒驗」就**沒有門牌**了——我不建議） |

# ④ 你 §2 的 feud 讀者清單：**收下，一個字沒動**

```
`interaction_system.gd:1796 _views_as_foe` ⇒ `strongest` ＋ `> 0.0`  ★小邊一出現就當仇人
`npc_ai_system.gd:57 vendetta_target`      ⇒ 0.6 門檻，不受影響
`diplomatic_ai_system.gd:64`               ⇒ 連續量，小邊小影響＝正確方向
`decision_context.gd:370` / `faction_ai_system.gd:719` / `:3433` ⇒ `strongest`，門檻待查
```
★**所以「把 FEUD_MIN 搬到讀側」不是一行，是四個讀者一起對齊** —— 已記，**本切片一個字不動。**

# ⑤ 你的原則：**我認，而且它已經在床裡了**

> **門檻要放在【行動】上，不要放在【記錄】上。**

★它逐字寫進了格2 的 `[不可判]` 輸出（**讓它每一次跑都被唸一遍**，而不是只活在一封會被 consumed 的信裡）。
★★**而它今天在本票裡有兩個互相印證的例子**：
- **記錄側的門檻**（`FEUD_MIN`）⇒ 小事件永久消失 ⇒ 累積永遠不會發生。
- **名字沒接上**（`special_taxed`／`rejected_aid`／`benefactor`）⇒ 事件發生了、而**沒有被記下來**。
⇒ ★★★**兩者的症狀一模一樣：「公式接好了、床綠了、而世界裡一次也不會發生。」**

# ⑥ 現在

- `feat/grudge-ledger-a` @ `d7b88b181`（床：`[FAIL] 0`／`[不可判] 2`）。
- ⏳ 掠奪票的 **after 世界窗仍在跑**（一次跑一個）⇒ 它跑完我接著：掠奪三支 headless → 兩票各自的 55 支。
- ⏳ 等你裁：**③ `[不可判]` 的閘位（甲／乙）**。
