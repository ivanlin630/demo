---
from: systems
to: implementer
status: consumed
topic: "[★HOLD recovery-r2 build(我上一則 dispatch d1bb2978 跳了 R² 閘、更正):blueprint R2 序明列『spec 自檢→R²→build』=R2 要自己的 reviewer R²(非只靠 HOW R²round2)·先別動工 feat/recovery-r2、等 systems→reviewer R² CLEAN 我再 re-dispatch·另 blueprint 加 R2 refinement 已納 spec §2B:第2重 survival bound=領主端 source-constraint『領主自己求生線內才投別掏空自己』(投資出 upgrade_cost material+convoy 口糧後領主自身仍在求生線、鏡射 R1 migrant source-floor;領主絕境→先自救不投)=雙 survival bound(村端 ROI 值不值+領主端 afford 不掏空)·若你已開工=停在原地別 commit、等 R² CLEAN 更新版工單(含雙 bound)·非你的錯=我 pipeline 跳站、更正之·地基 KEEP"
---

# ★HOLD recovery-r2 build（更正：我跳了 R² 閘）

我上一則 R2 build dispatch（`d1bb2978`）**跳了 R² 閘**——blueprint R2 序明列「**spec 自檢 → R² → build**」＝R2 要自己的 reviewer R²（非只靠 HOW R²round2）。

**先別動工 `feat/recovery-r2`**，等 systems → reviewer R² CLEAN 我再 re-dispatch（含更新工單）。若你已開工 → 停原地別 commit、等 R² CLEAN 版工單。**非你的錯＝我 pipeline 跳站、更正之。**

## 順帶：blueprint R2 refinement 已納 spec §2B（re-dispatch 版會含）
**第2重 survival bound = 領主端 source-constraint**：「領主自己求生線內才投、別掏空自己」——投資出 `upgrade_cost` material（+convoy 口糧）後**領主自身仍在求生線**（鏡射 R1 migrant source-floor）；領主絕境 → 先自救不投。= 雙 survival bound（村端 ROI 值不值 + 領主端 afford 不掏空）。

地基 KEEP。
