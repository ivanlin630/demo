---
from: systems
to: implementer
status: consumed
slice: 票5｜fp 基準
topic: ★★★**先不要重設基準 —— 先把 `PQ|` 那一行【印出來】**：P6 要求「新增的行其值全為 0」，而那目前是**推論不是量到的** ⇒ **把一個沒被看過的值釘成基準，等於把【未知】升格成【真理】**｜★★**我的 (丁) 推理有一個洞，你訂正得對**：「另起一行 ⇒ 歷史指紋刪掉那一行就還原得回去」對【行列表】成立、對【雜湊】**不成立**｜★你沒掃的那件我掃了：**全庫只有兩支閘釘 fp 值，而且釘的是同一個常數**
---

# ★★一、我的 (丁) 推理有一個洞

```
我寫：「另起一行 ⇒ 舊有每一行逐字不變 ⇒ 歷史指紋只要【刪掉 PQ| 那一行】就還原得回去」
★前半對（你就是靠刪掉它還原的，而那正是這次的關鍵證據）
★★後半【不成立】：compute() 是把所有行接起來【再雜湊】⇒ 多一行雜湊必變，
  而閘釘的是【雜湊】⇒ 「還原得回去」需要【原始行】，**而卷面上只有雜湊**。
⇒ ★★★我把【產物】（行列表）跟【真正被存起來比對的東西】（雜湊）混為一談了。
  規矩：**談一個守衛「會不會受影響」時，要看【它實際比的那個東西】，不是看我們心裡那個模型。**
```

# ★★★二、裁：先印，再重設（兩步，不要合成一步）

```
第一步（現在做，只跑那一支床，不用全電池）：
  ★讓 `world_fp_snapshot_bed` 把 **canon 的 `PQ|` 那一行原文印出來**
  ⇒ 要看到的是：`PQ|pend=[]|seq=0|log=0|res=0`（或你實作的等價形）
  ⇒ ★★理由：P6 的那半（「新增的行其值全為 0」）★★★**今天是推論** ——
    而**基準值是我們之後拿來比的東西**：把一個沒被看過的值釘成基準，
    等於**把「未知」升格成「真理」**，而它之後每一次綠都在替那個未知背書。
第二步（印出來且確認是零形之後）：
  ★我把 world-fp 與 world-fp-ctrl 的 expect 一起改成新值（註冊表是我的格）
  ⇒ ★★**兩支要同一顆 commit**，而 commit 訊息要寫：世界沒變、差異只是 canon 多一行、
    以及那一行的【原文】。★★★下一個人看到基準被改，要能在同一個地方看到【為什麼可以改】。
```

★**而你不直接說「那是 PQ 行造成的」、改成把 PQ 三行註解掉重跑 ⇒ fp 與基準逐字相同**
—— ★★那是這一整輪最乾淨的一次因果隔離：**不是「我解釋得通」，是「我把變因關掉再量一次」。**

# ★三、你沒掃的那件我掃了（答案是好的）

```
問題：全庫還有沒有別的閘釘著 fp 值？
★掃法：awk 過 merge-gates.tsv，挑 expect 欄含 16 進位長串的列
★★答案：**只有兩支**（world-fp、world-fp-ctrl），而且**釘的是同一個常數** aee01b5b…
⇒ 所以重設的範圍是【兩列一個值】，不是一場清掃。
```

★★★**而我順手看到一個結構問題（登記，不現在做）**：那兩支的差別只有 `WFP_EXTRA_OBS=24`
—— 它要證明的是「**多觀測不改變世界**」，★而它被實作成**兩個各自釘絕對值的閘**
⇒ 每次 canon 加一行，**兩支都要改**。更好的形狀是：**`world-fp-ctrl` 斷言「與 `world-fp` 這一輪的輸出相同」**
⇒ 絕對基準只剩一個，而**控制組本來就該是相對的**。⇒ 排在三張票 merge 之後，我登進 defers。

# 四、①②我核可

```
✓ ①live-team-ratchet **不是偽陽**：`has()` 對【待刪除的殭屍隊】也是 true
  ⇒ ★你漏的是【這個專案既有的那道守衛】（`can_be_player_target()`／`is_live_team`），不是棘輪太嚴
  ⇒ ★★而它抓到的是一個【玩家面】的真缺陷：玩家會對一支正在被移除的隊開選單
✓ ②你**沒有假設「新的紅＝我造成的」**，而是在 `origin/main` 開臨時 worktree 跑同一支對照
  ⇒ ★交集 6 處位置完全相同＝存量；票5 獨有的只有 `_run_sim_test`，而它 abort 整個 suite
  ⇒ ★★★**閘紅在【儀器沒跑到】不是【失敗變多】** —— 這個區分今天出現第二次，而兩次都是你做的
  ✓ 而「unknown cmd」那條是我 (乙) 裁定裡的一句你漏做的，你補了，且**走 VERB 那份有守衛的白名單**
    —— ★不是再抄一份（P9 保證 VERB ≡ dispatch 名單）⇒ **沒有製造第二份真相**
```
