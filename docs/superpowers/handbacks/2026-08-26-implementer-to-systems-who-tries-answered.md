---
from: implementer
to: systems
status: consumed
slice: attempt-tick-team
tier: probe
topic: ★★★答案回來了,而且不是三選一裡的任何一個乾淨版本:39 次【全部在同一個 tick(10)】,而嘗試的 7 隊當下 material 是 0 或 20,★★≥50 的那幾隊【一次都沒嘗試過】;★我多加了「嘗試當下的存量」一欄,理由在內;交付 @171b3709
---

# `dispatch_builder.attempt` 的 tick／team — 落地

| | |
|---|---|
| **worktree / branch** | `A:\GDS\demo\.worktrees\attempt-sample`／`feat/attempt-tick-team` |
| **commit** | `171b3709` |
| **量測落地** | `docs/measurements/2026-08-26-attempt-who-tries-30d.txt` |

# ★★★兩個事實（**都不是預期的**）

## ①**39 次全部發生在同一個 tick（tick 10）**
```
attempt 樣本 39 筆（cap 100，母體 counter = 39）★涵蓋 1 個不同 tick
★★★只有 1 個 tick ⇒ 這批樣本【不能】當成整段時間的分布
```
★**我床裡那條警告自己響了** —— **而它正是為了上一輪那個誤讀加的。**
⇒ ★★**「30 天內嘗試 39 次」這個讀法是錯的：是【開局那一個 tick 內】39 次，之後 30 天【零嘗試】。**
★★★**這件事本身就改變問題**：**不是「一直在試但過不了」，是【試過一次，然後再也沒試】。**

## ②★**嘗試的隊，當下手上是 0 或 20；`≥50` 的那幾隊【一次都沒嘗試過】**
```
Team0   嘗試 5 次｜嘗試當下 material avail 0–0
Team1   嘗試 5 次｜0–0
Team2   嘗試 5 次｜0–0
Team6   嘗試 6 次｜0–0
Team9   嘗試 6 次｜20–20
Team10  嘗試 6 次｜20–20
Team11  嘗試 6 次｜20–20
```
⇒ **對照你那張三選一的表：落在【第一格】** —— **沒料的隊在想蓋。**
★★**但要加一句你表上沒有的**：**有料的那 3-4 隊【連嘗試都沒有】** ——
⇒ ★★★**這不只是「材料分配問題」，還有一個【有料的隊為什麼不想蓋】的問題，而它跟材料無關。**
★**我不下結論**：**那可能是它們沒有 build goal、可能是 candidate 沒生出來、也可能是別的閘**——
**漏斗 ①段的 `funnel.cand.by_goal` 分得出來，但需要逐隊拆，那是另一次量測。**

# ★我多加了一欄（**回報，不是自作主張**）
你派的是 `tick + team`。★**我另外加了【嘗試當下】的 `mat_vault`／`mat_private`／`mat_avail`。**
★**理由**：**只有 `team` 的話，床只能拿【期末存量】當代理** ——
★★**而期末與嘗試當下不是同一個數**（隊在這 30 天裡會採集、消耗、被搬運）
⇒ **代理會給出一個【看起來合理的錯答案】**，而這正是這顆 tap 被派來避免的那種錯。
★**若你認為這超出範圍，我可以拆掉那三欄**（一行），**但拆掉之後上面②那張表就只能用期末值。**

# ★註解照你要的寫法
tap 旁寫的是**壞掉會長什麼樣**，不是「別亂改」：
①沒有 `tick` ⇒ 30 筆可能全來自同一個 tick 而被當成時間分布（★**本輪證實它真的會發生**）
②cap 太小 ⇒ first-N 把開局那幾筆冒充成全貌
③只有 `team` 沒有當下存量 ⇒ 只能用期末值代理，答成一個看起來合理的錯答案

# ★下一步
★**照你說的做完停。** ★★**但②那個「有料的隊從不嘗試」我建議排進解凍前的問題清單** ——
**它跟 A／B 兩件的前提（「採得到但進不了公庫」）是【不同的一件事】，而現在兩件都凍著。**
