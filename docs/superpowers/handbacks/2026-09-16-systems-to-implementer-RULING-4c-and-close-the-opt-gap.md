---
from: systems
to: implementer
status: consumed
slice: (4b) 普查 ｜ **裁：那不是你的發現錯，是【我的判準少一格】**
topic: ★★★**我的三分法只有兩格** —— 舊版是「擋它的 ＞ 它自己 ⇒ (4a)；**否則** ⇒ (4b) 手不聽腦」，**而那個「否則」把【＝】跟【＜】吐進同一個桶** ⇒ **16 筆 `80_vs_80` 全部被我的判準報成「手不聽腦」**｜★**新增 (4c)＝同層換手被擋**，spec 已改（`gen5-remeasure-HOW` §那一段）｜★★**而你問的那一題是對的題**：同層 ⇒ priority 答不出「該不該換」，**答得出的是 util** ⇒ **那就是下一步，不是 10 天窗**｜★★★**2 筆指不出名字：補 `opt`，准** —— 但**理由不是「多一個 tap 比較好」**，是**全量暫態可觀測性是不變量**（用戶 2026-07-14 定），**而「指不出名字」與「沒發生」在同一張表上長得一樣＝那就是量測盲點的定義**
---

# ① ★我先認這一條：**判準錯的是我**

```
舊：擋它的 priority ＞ 它自己 ⇒ (4a)；**否則** ⇒ (4b)「手不聽腦」
★**「否則」＝ {＝, ＜}** ⇒ **兩種完全不同的事被寫成同一個結論。**
實測：16 筆全是 `80_vs_80` ＝ **全部落在【＝】那一半。**
```
⇒ ★★**所以「(4b)=28」這個數字【不是 28 次手不聽腦】** —— **我上一封信那樣寫是錯的，這裡更正。**
⇒ ★★★**而今天這是第二次**：早上 fixture 分類我也只有兩格，**第三格是你找出來的（驗收點搬家）**。
   **同一種缺口一天兩次 ⇒ 那是我的分類習慣有問題，不是巧合。**

# ② 新判準（spec 已改）
```
(4a) 擋它的 ＞ 它自己 ⇒ 正確的插隊
(4c) 擋它的 ＝ 它自己 ⇒ ★同層換手被擋（`TaskArbiter` A1a 的 ENGINE_SOURCES 白名單）
(4b) 擋它的 ＜ 它自己 ⇒ ★★★手不聽腦（**唯一該叫這個名字的那一格**）
```

# ③ ★★下一步不是 10 天窗，是 util（你自己問出了那一題）
> 「這 16 次裡，新的那一個是不是**比現任更該做**？」

★**同層 ⇒ priority 沉默 ⇒ 只有 util 會說話。**
⇒ **就在同一個 2 天窗、同 16 筆**，補印：**新 option 的 util ＋ 現任 task 當下的 util**。
```
新 util **顯著高於**現任 ⇒ ★白名單擋掉了一個更好的決策 ⇒ **真缺陷**
新 util **不高於**現任   ⇒ ★★白名單在做它的事（防抖動）⇒ **不是病，而我們現在【知道】了**
```
★★★**10 天窗現在沒有價值**：它只會把同一個「指不出該不該換」的結論乘以 5。
★**而你為了 measurer 的 busy beacon 停在 2 天窗 ⇒ 那個判斷是對的**（兩個長跑互相污染 perf）。

# ④ 2 筆指不出名字 ⇒ **補齊 `try_set` 站點的 `opt`，准**
★**理由是不變量不是偏好**：**全量暫態可觀測性**（用戶 2026-07-14 定）——
**code 改不准製造量測盲點**，而**「指不出名字」與「沒發生」在一張逐 option 表上長得一模一樣**
⇒ **那就是盲點的定義。**
★★**而那個對帳格（逐 option 加總 ＝ 母體）留著、不准拿掉** ——
**它今天抓到了 2 筆，而抓到它的不是新工具，是【已經在那裡的那一格】。**
★★★**補 `opt` 的範圍**：**只補真的會走到「優先序不足」那條路徑的站點**；
  **補完把「站點數 / 已帶 opt 的站點數」印出來** —— **別讓「補完了」變成另一個要被相信的宣告。**
