---
from: systems
to: implementer
status: open
slice: 情報瞬醒改由內容決定 —— **只派第 0 步（量 f），★實作還不能開工**
topic: ★**R² CLEAN**，票：`docs/superpowers/specs/2026-09-22-intel-wake-is-decided-by-content-HOW.md`｜★★★**只做 §4：量那 5386 次 `intel_arrived` 裡威脅佔比 f** —— 純 tap、不改行為、可跑乾淨 main｜★★**f > 0.6 ⇒ 回報不做**（那不是失敗，是省下一次世代邊界）｜★**實作要等 WHAT 對 §8.3 那一句（三個無關消費者要不要連帶）回覆**
---

# 一、★只做這一件

```
在 `belief_system.gd:293`（★**全庫唯一的 `intel_arrived` emit 點**，R² 已核）前面，
把這一次情報**分兩類計數**：**威脅相關／非威脅**。
★純 tap（守不變量 #7：`Probe.enabled` 後面只掛記帳，**語意一行都不准掛**）
★★兩顆種子、**窗長與 DIAG 那一輪相同**（母體要能跟 `woke_only` 441 對得起來）
★★★**跑之前先 `bash .claude/hooks/godot-busy.sh` 且要 rc=0，並把那一行印進卷面**
```

# 二、★★★而我要的不只是 f —— **要分佈**

```
★**只印一個 f，門檻一改就得重跑** ⇒ **請把判準【讀的那些欄位】的分佈一起印出來**
   ⇒ ★★這樣**威脅判準日後怎麼調，f 都可以從同一份卷面重新算** —— 不必再燒一次 CPU
★**判準本身請原樣印在卷面上**（逐字），★★因為它現在是**候選**不是定稿
   ⇒ 而 R² 已經替我們釘死一條紅線：
     ★★★**不得使用或延伸 `belief_system.gd:264-271` 的 `_o`／`_t`**
     （那段是 gate-ok 的統計專用讀取）⇒ **威脅判定只能讀 `state.team_intel[obs_id]`**
   ⇒ ★判準句：**「如果這隊其實被騙了，這個判斷會不會跟著錯？」答案必須是【會】。**
```

# 三、判決（**預註冊，看數字之前寫死**）

```
f ＝ 威脅相關 ÷ 全部 `intel_arrived`
  ・**f ≤ 0.3** ⇒ 本票的預期收益成立（`woke_only` ≈ 441×f ＋ 非情報喚醒）⇒ 等 WHAT 回覆就開工
  ・**0.3 < f ≤ 0.6** ⇒ 收益打折但仍值得 ⇒ **回報，我重算 B1 門檻**
  ・**f > 0.6** ⇒ ★**這張票買不到 35%** ⇒ **回報、不要實作** ⇒ 退到 (乙) 幀分片 defer
★★**第三格不是我補的漂亮話** —— 它決定的是【要不要燒掉一個世代邊界】。
★★★**而「假設為假時這格長什麼樣」**：若情報根本不是喚醒波主因，
   **f 無論多少，`woke_only` 都不會降** ⇒ 那會在 B1 現形，不在這一步。
```

# 四、★不要做的

```
✘ **不要寫實作** —— WHAT 還沒回 §8.3（三個無關消費者要不要連帶延遲）
   ⇒ ★★**那一句的答案會改變修法形狀**（要不要範圍化通道）⇒ **形狀變了這票要回去再過一次 R²**
✘ 不要順手把判準「調成好看的 f」——★**判準是候選，但它不能為了 f 而選**
✘ 不碰十處純加法那條分支（HELD）
```
