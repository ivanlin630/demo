---
from: systems
to: reviewer
status: open
slice: ui-flow 決定性修正 — 第二輪 R²
topic: ★分支 `fix/ui-flow-determinism` ＝ `2eb158de8`；格①（未 seed 兩行程 randf 必須不同）陽性對照已點火、格②也點火了｜★★★**而我對格②有一個疑慮，它是這封信的主體**：格②守的是「**同種子 ⇒ 逐字相同**」，而它的注射是「**第二趟換一頁**」⇒ ★**那證明的是【比對器會動】，不是【不決定性會被抓到】** —— 兩者是不同的軸｜★★若那條軸上根本點不著火（先前兩次注射都失敗），那誠實的說法是「這支床的輸出對種子不敏感」，而不是「格②守住了決定性」
---

# 一、實作端交付（★他的數字，我沒有重跑）

```
格①：把 randomize() 換成同一顆 seed ⇒ ★紅（陽性對照點火）
格②：注射改成「第二趟換一頁」（不需要推進世界）⇒ ★紅
兩支床：ui-flow 3/3 綠｜擷取床 rc=0
重取的「前」：★只有檔頭的樹 sha 變、20 行正文逐字相同
```

# ★★★二、我的疑慮：格②的陽性對照點在【另一條軸】上

```
格②的斷言：同一顆種子跑兩次 ⇒ 輸出逐字相同   ← ★它要守的是【決定性】
它的注射：  第二趟【換一頁】⇒ 輸出不同 ⇒ 紅   ← ★★這證明【比對器活著】
⇒ ★★★而「比對器活著」與「不決定性會被它抓到」是兩件事：
   前者只要兩份輸入不同就會紅；後者要求【種子不同時輸出真的會不同】
```

**而我們已經有反證**（同一天、同一支床）：

```
先前兩次在【種子軸】上的注射都點不著：
  seed 11111 vs 22222 ⇒ 輸出逐字相同（302 字元）
  多推 1／1440 tick ⇒ 逐字相同（★後來證明那次注射根本沒動到世界，但前者沒有這個瑕疵）
⇒ ★所以【那條軸上點不著火】這件事，本身是有證據的
```

⇒ **請你判這一句**：

```
(a) 若那 20 行【真的對種子不敏感】⇒ ★★格②在它自己那條軸上是【恆真】的
    ⇒ 誠實的措辭是：「本床輸出對種子不敏感；格②守的是比對器與流程，不是決定性」
    ⇒ ★★★而那句話要寫進【床的檔頭】，不是寫在這封信裡
(b) 若你能找到一個【種子軸上會點火】的注射 ⇒ ★那格②就名副其實，請點名它
```

★**我不主張拿掉格②** —— 它守著「輸出不再隨機變動」這個可觀察事實（44% → 0）。
★★**我主張的是【它的名字與它守的東西要對得上】。**

# ★三、第二格（比較輕）：「前」的檔頭帶樹 sha

```
他說重取的「前」只有【檔頭的樹 sha】變、正文逐字相同
⇒ ★那表示這個檔【每次樹變都會產生 diff】，而正文其實沒變
⇒ ★★請你判：樹 sha 該不該留在檔頭？
   留 ⇒ 可追溯，但它會製造【看起來有變而其實沒變】的 diff
   不留 ⇒ 乾淨，但失去「這份前是哪棵樹取的」
★★★我傾向【留】，而把它移到檔案【最後一行】⇒ ★diff 一眼看得出是不是只有那一行變
   —— 但這是風格judgement，你若認為無所謂就說無所謂
```

# 四、範圍

```
只動兩支 debug 床 ＋ 重取的「前」快照；零 production
★而註冊表 ui-flow 那一列的警語【還沒拿掉】—— 我等這張 merge 之後才改它
```

verdict：CLEAN ⇒ 我 merge（併前跑全電池）；issues ⇒ 逐條帶 file:line。
