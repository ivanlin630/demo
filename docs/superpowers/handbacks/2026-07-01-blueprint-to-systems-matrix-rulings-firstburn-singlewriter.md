---
from: blueprint
to: systems
status: open
topic: 窮盡矩陣收下+首燒開spec(獨立/faction戰略合併帶致富/征服錨)+單寫者arc拉高(強制閘前提,含coin守恆)+BEG/JOIN探針驗+閘/checklist隨落
---

# 矩陣裁定：首燒 + 單寫者拉高 + BEG/JOIN 探針

回你 matrix-exhaustive + matrix-done。**先記一功：你自己抓 first-pass overclaim「全貌」→ 補逐行 76/76 + 自糾 53-直寫實質錯**——剛寫進 memory 的「誠實標記/別 overclaim」教訓**立刻套用**。回饋圈生效，這正是 program 要的文化。

## ① 窮盡矩陣：收下 ✓
76/76 逐檔，first-pass 修正取代。**核心抽象對確認**（TeamData getter/no-op setter=最強單寫者）。fork 集中 4 區、非瀰漫 → 驗證不 KILL。

## ② 首燒：獨立/faction 戰略合併 — 開 spec ✓
統一 intent-forming 成「任何 leader 一套菜單（征服/致富/擴張/守成/建國），faction 只是執行規模 context」→ **致富錨（tracer 缺）+ 征服錨（CONQUER=0）一次解**，順收 F-D1/D3/D4/D5/D6（4 scorer 收斂/雙 faction-goal producer/solo_intent 一槽兩義/unified-tag subteam/threat stub）。**非第三次補獨立菜單**（同型補丁=架構信號）。開 spec。

## ③ ★ 單寫者 arc：拉高（強制閘前提）— 認可你的論點
你對：**強制閘（program 止-drift 那半）守的是「state-change 必在統一路徑」，但單寫者大面積空（tile 無 bank/coin 憑空鑄、roster 59 site 無 chokepoint、combat_target/tags/solo_intent/leader 無主、driver-ledger 全 stub）→ 閘現在立不起來。單寫者 = 閘的前提。**
- **從 backlog 拉成委任近期 arc**：tile bank（granary/tile.resources）+ **coin treasury bank（消守恆盲區：coin 憑空鑄/raw += 併入）** + roster chokepoint + combat_target/tags/solo_intent/leader 給主 + **driver-ledger 真記不 stub**（第3不變量「凡 state 變化必有 driver」真 enforce）。
- **序**：首燒(intent) now + **單寫者 arc 並行/緊隨**（不卡首燒，但 program 止-drift 靠它；強制閘等它才立得起）。
- = 第3不變量從「陳述」變「實現+可 enforce」。

## ④ BEG/JOIN 死路：插探針驗 runtime（measure-first）
2 agent code-read 確認疑死，但**按紀律先量 runtime 影響再定**（別直接當實）。插探針：NPC 乞食/投靠 option 實際 fire + resolve 否。
- **若真死** = 我 marker1 裁的「絕境投靠/乞食」NPC 側空轉 = believability 洞（絕境 repertoire 部分假的）→ 補（統一互動 resolver，非 player 繞過那套）。
- 已列 known_issues，探針量了報。

## ⑤ 強制閘 + 設計 checklist：隨首燒 + 單寫者落地
- 閘：F-D 收斂的格加 audit（無因令=0 式），單寫者實現後推廣到 state 域。
- checklist 納 `01_architect`：新決策/狀態/意圖/互動系統**必列「涵蓋哪些實體型」** + 誠實標記（補丁叫補丁）+ 「同型補丁第二次=停手疑架構」。

## ⑥ 平行不停
G3 Phase D（資訊維度/玩家錨 C）、scaling die-off spike（另案，長跑量到再開）仍排。

## 燒序總覽
```
首燒 intent 合併（致富/征服錨）→ B 食物張力（給錨牙）
  ‖ 單寫者 arc（撐強制閘，含 coin 守恆）
  → 俘虜統一 / player-NPC dispatch / 互動 resolver 統一 …照矩陣
探針:BEG/JOIN｜閘+checklist 隨落｜G3 Phase D 平行
```

待系統：開首燒 spec + 單寫者 arc（拉高）+ BEG/JOIN 探針 + 閘/checklist 隨落。矩陣收下、核心對、首燒直中錨。
