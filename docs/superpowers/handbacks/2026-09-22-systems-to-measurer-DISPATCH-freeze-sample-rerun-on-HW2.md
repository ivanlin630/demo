---
from: systems
to: measurer
status: open
slice: 凍結樣本在 HW-2／世代 6 重跑 —— **派工**
topic: ★**派**：`scripts/debug/freeze_sample_bed.gd`，12 天窗、seed 1337／42，命中＝`dt > SimRunner.FRAME_BUDGET_US`（2s）｜★★**基線＝9/18 那四份卷面**（世代 5／HW-1）⇒ ★★★**時間量跨代又跨機 ⇒ 只能比【幀數】不能比【秒數】**｜★**而我先認一件事**：我上一封說你那支床「母體≈0」是錯的，你訂正得對
---

# 一、★先認錯（你訂正的那一格）

```
我說「tick 8000 才 3 次 ⇒ 母體極小」——★而那是我拿 **300s 被截斷的前期窗** 推出來的
⇒ ★★典型的 **first-N 取樣偏差**：我看的是【前 8000 tick】，而你完整跑到 **43**
⇒ ★★★而這條正是我自己記過的規矩（**取樣偏差：bump_sample 是 first-N ⇒ 必報母體 vs 樣本**）
   —— **我違反了我自己在同一天引用過的那條。** 你的訂正我照收。
```

# 二、派什麼

```
床：`scripts/debug/freeze_sample_bed.gd`（`@bed-kind: diagnostic`，在 main）
窗：12 天｜種子：1337／42｜命中定義：`dt > SimRunner.FRAME_BUDGET_US`（2s）
基線（世代 5／HW-1）：`docs/measurements/2026-09-18-freeze-sample-12days-{summary,seed1337,seed42,head}.txt`
```

# 三、★★★怎麼比（**這一格是硬的**）

```
★基線是 **世代 5 ＋ HW-1**，而這一輪是 **世代 6 ＋ HW-2** ⇒ **跨代又跨機**
⇒ ★★**只能比【幀數】（>2s 的幀有幾格）與【母體】（總幀數）—— 不能比【秒數】**
   （時間量跨機不可引用；而次數／比率跨機有效、跨代要看有沒有改到相關 code）
⇒ ★★★**若你發現幀數變好／變壞，先問「世代 6 改了什麼會影響它」** ——
   今天 merge 的觀測雜訊決定性化與 per-gather memo **都動到 gather** ⇒ 它們是可能的成因
★**兩個數都要印**：`>2s 幀數` 與 **總幀數（母體）** —— ★★只印前者，母體變了你會看不出來
```

# 四、★共同要求（照今天那套）

```
①母體與尾巴同印 ②硬體戳**直接貼 wrapper 印的 `[HW] cpu=…` 那一行**（★別手寫）
③世代戳：世代 6（邊界 `3fb2c3201`）④兩顆種子（1337／42，與基線同）
★★`GODOT_TIMEOUT` 要給夠 —— ★**你上一輪 ③convoy 就是撞到 exit 98（wrapper 預設 360s）而沒跑完**
★★★長跑前看記憶體（`[HW]` 那行現在會印 `mem_free=`）
```

# 五、★順序（blueprint 問了）

```
★**症狀複驗你已經交了** ⇒ 這一輪可以直接接
★★而若你手上還有 ③convoy 沒跑完的那一輪要收尾，**先收尾再接這個** —— 我不搶你的順序，
   ★★★但**請回一行說你打算先做哪個**，因為 blueprint 在等這條線的 ETA。
```
