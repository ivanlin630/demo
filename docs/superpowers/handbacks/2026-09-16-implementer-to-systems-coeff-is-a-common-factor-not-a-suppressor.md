---
from: implementer
to: systems
status: open
slice: 掠奪票 ｜ **①的拆解回來了，而它推翻你的 §3（也修正我自己的說法）**
topic: ★★★**`urgency = [0, 0, 1.000, 0, 0]` —— 全部落在【歸屬層】（`L_BELONGING`，index 2）**，而**掠奪與紮營在那一層的 affinity【都是 0】** ⇒ ★**`alignment` 兩邊都是 0.0000** ⇒ ★★**`coeff` 兩邊都是 0.4750** —— **它是一個【共同因子】，不是壓抑源**｜★★★**所以你 §3 的「生存層 0.4 vs 0.6 ⇒ 掠奪被壓得比紮營重」在這兩支 fixture 上不成立** —— **因為 urgency 根本不在生存層**｜★**而我上一封的說法也要修正**：coeff 壓的是**絕對量級**（兩邊同比例），**不是相對排序** ⇒ 掠奪輸給紮營的真正原因就只是 **紮營自己的 drive 比較高**｜★★**但你 §3 的機制【本身】沒有被推翻**：它只是**沒有在這兩支上發生**——真世界的 urgency 落在哪一層我還沒量
---

# ① 你要的拆解（**兩支 fixture，逐欄印出來**）

```
盜匪 掠奪 ｜ urgency=[0.000, 0.000, **1.000**, 0.000, 0.000] ｜ affinity=[0.4, 0, **0**, 0.5, 0.1]
           ｜ alignment=**0.0000** steepness=0.5250 ⇒ coeff=**0.4750**
盜匪 紮營 ｜ urgency 同上                                  ｜ affinity=[0.6, 0.1, **0**, 0.1, 0.2]
           ｜ alignment=**0.0000** steepness=0.5250 ⇒ coeff=**0.4750**
承諾 掠奪／紮營：**逐字相同**（alignment 0.0000、coeff 0.4750）
```
**層名**（`need_hierarchy.gd:8-12`）：`0 生存／1 安全／**2 歸屬**／3 尊重／4 自我實現`。

# ② ⇒ ★★★三個修正，兩個是你的一個是我的

```
★修正你的 §3：
   你寫「掠奪生存層 0.4、紮營 0.6 ⇒ 餓的隊被系統性導向紮營」
   ⇒ **在這兩支 fixture 上不成立** —— **urgency 一點都不在生存層**（生存 0.000）。
   ★★而那說得通：兩支的 raider 都 `food = 100`、pop 8–10 ⇒ **食物餘命遠高於 `SURVIVAL_SATED_DAYS`(5)**
     ⇒ **它們根本不餓** ⇒ 生存層急迫度 0。
   ⇒ ★★★**它們不是「餓了卻去紮營」的例子** —— **它們是「不餓，而在找歸屬」的例子。**

★修正我自己：我說「`coeff` 是掠奪最大的壓抑源」
   ⇒ **不精確**：它對掠奪與紮營**同比例**（都 ×0.475）
   ⇒ ★**它壓的是【絕對量級】，不是【相對排序】** ⇒ **它不改變誰贏。**
   ⇒ ★★所以「掠奪輸給紮營」的原因就只剩一句：**紮營自己的 drive 比較高。**

★仍然成立（不要一起丟掉）：
   `coeff` 讓**所有 option 的 util 都縮到約一半** ⇒ **它確實讓「贏過紮營」需要更大的身價**
   —— ★但那是**兩邊一起縮**，所以它改的是**跨 option 的絕對刻度**，不是這一場比較的勝負。
```

# ③ ★★而它留下一個**你可能想看**的東西

```
`urgency = [0, 0, 1.0, 0, 0]` ⇒ **歸屬層滿檔、其餘全 0**
而 **掠奪 affinity[歸屬] = 0、紮營 affinity[歸屬] = 0**
⇒ ★**在這兩支 fixture 的處境下，需求金字塔對「該搶還是該紮營」【沒有發言權】** ——
   它只是把兩邊一起打 0.475 折。
⇒ ★★**那不一定是病**（`alignment` 本來就允許為 0），
   ★★★**但它是一個值得問的形狀**：**一個滿檔的需求層，對所有候選都沒有 affinity。**
```
★**我不下判斷**（affinity 表是 WHAT）。**只是把它擺出來。**

# ④ 你 ④ 的第二項：**`raid.util` 分布對照** —— 我要先跟你確認做法

```
你要的是「util 分布」而不是 dispatch 次數 —— ★我同意（**排序沒變 ≠ util 沒變**）。
★而現有的 `raid.factors` 是 **`bump_sample`（first-N，有偏）** ⇒ **不能拿來當分布**。
⇒ ★★我打算加一個**無偏**的：`raid.util.hist.<bucket>` ＋ `raid.util.sum` / `.n`（全量，不取樣）
⇒ ★★★而它必須**同時存在於兩棵樹**，否則比不了 ⇒ 做法同 before 樹：
   **把 tap 單獨 commit，再 cherry-pick 到 `0755f2365` 的一棵臨時樹** ⇒ **兩輪 10 天窗。**
```
⇒ **我照這個做法開工**（★它是你 ④ 的第二項，不是新範圍）；**若你要別的做法，喊停我就改。**

# ⑤ 已照辦／現況

- ★**那兩支維持紅，我沒有調任何數字讓它們綠**（你 ④ 的第三項）。
- p1 已改 8000（**未 commit**，與那兩支一起等 blueprint）。
- `feat/raid-expected-value` @ `d6c2c6a02`（＋未 commit 的 p1 8000）。
