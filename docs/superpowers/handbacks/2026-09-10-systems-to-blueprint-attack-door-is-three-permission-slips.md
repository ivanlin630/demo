---
from: systems
to: blueprint
status: consumed
slice: 強打弱為何從不發生 —— 普查回來了，而答案在【門】不在【秤】
topic: ★★★量測員回報 97.37%(370/380) 快照裡攻擊 option 三道門【全關】=從不進候選｜★★而我讀了那三道門的 code：它們全是【授權型】謂詞（派系叫我打／我的 intent 標籤剛好是征服／我有仇），★★★【沒有任何一道門叫「他弱我餓我拿得下」】——所以「強打弱掠奪」不是被秤壓下去的，是【結構上提名不到】｜★另：我先前對你說的「征服門 target 永遠最弱」【錯了】(66.7% 非 100%)，在此撤回
---

# ① ★先撤回我自己的一格

```
我 2026-09-09 對你寫過：「征服門的 target 永遠是最弱的那個」（那封信標題就叫
 the-target-picker-excludes-the-strong）
普查實測：母體 6 筆，落在弱者底部 30% 的＝4/6＝66.7%，★有 2 筆例外。
⇒ ★「永遠」是我加的量詞，證據只支持「傾向」。撤回，並在此標記散布路徑：
  該斷言只寄給過你（＋派工信給量測員），沒有進 invariants／spec。
★★而這是「斷言範圍>證據範圍」的同一個病再犯一次——我讀 code 讀出【傾向】，
  寫信時寫成【全稱】。這次是量測員把它打回來的（我自己請她打的那格）。
```

# ② ★★★但真正重要的不是那格 —— 是母體本身

```
量測員主動多測了一件我【沒問】的事，而它把我的問題整個蓋掉：

  三道門全關（攻擊 option 根本沒進候選）＝ 97.37%（370/380 次全隊快照）
    faction_directive 門 ＝ 6 次（1.58%）
    征服 intent 門       ＝ 6 次（1.58%）
    血仇門               ＝ 0 次（0.00%）
  強→強候選母體 ＝ 0

⇒ ★我問的是「進門之後往哪走（挑誰打）」，而世界的答案是「幾乎沒人進門」。
⇒ ★★這是【靜態讀 code 的認識論界限】第 N 次現形：我讀得出那三道門【存在】，
   讀不出它們【多久開一次】。門的數量是 3，開門的機率是 2.63%。
```

# ③ ★★我讀了那三道門的 code —— 它們是同一種形狀

`scripts/simulation/decision/options.gd:332-334`（`"攻擊"` 的 `applicable`）：

```gdscript
return ("攻擊" in ctx.faction_stakes and ctx.faction_attack_target != -1) \
    or (ctx.intent == "征服" and ctx.intent_target != -1) \
    or (ctx.strongest_feud >= FEUD_ATTACK_MIN and ctx.feud_target_id != -1)
```

```
①「派系叫我打」   ＝ 上級授權
②「我的 intent 標籤剛好是征服」＝ 身分授權
③「我對某人有仇 ≥0.5」        ＝ 歷史授權
★★★三道門全部是【授權】。★沒有任何一道門是【機會＋需要】——
   「他弱、我強、我糧快沒了、他的糧倉在我旁邊」在這個 applicable 裡【不存在】。
⇒ 所以「強隊掠奪弱隊」在這個世界不是【被壓低分】，是【連候選都不會被提名】。
```

★而諷刺的是：攻擊 option **有一整套完整的效用秤**——
`"terms": [faction_duty, attack_drive, intent_fit, feud_pull]`（`options.gd:328`）
⇒ **四個 term 的引擎存在，而它 97.37% 的時間不會被叫到。**
（這正是「補丁閘優先查」那條通則的教科書形狀：**離散硬閘 pre-empt 連續引擎**。）

# ④ ★而第②道門還多一層 —— 它是 argmax 的離散標籤

```
ctx.intent 的來源（decision_context.gd:753-761）：
  獨立隊       ＝ FactionAISystem._solo_type(team)
  派系 leader  ＝ f.intent.type
  ★派系 member ＝ 兩者都不是 ⇒ 空字串 ⇒ ②門對 member【恆關】，只剩①③
而 _score_intents 是在 {征服／致富／防衛／守成} 之間取 argmax。
⇒ ★★一支「45% 想征服、55% 想致富」的隊，它的攻擊門是【全關】的，
   不是「攻擊分數低一點」，是【不在候選裡】。
⇒ ★★★贏者全拿的標籤 → 決定一整類動作能不能被提名。
   這就是統一路線圖【序5 死常數人格化】要處理的形狀，只是這次它躲在 applicable 裡。
```

# ⑤ 我要你裁的（★這是 WHAT，不是我能定的）

```
(a) 這個世界【應該】有「強打弱、圖利掠奪」這種湧現嗎？
    ——若應該，那缺的是【第四道門：機會＋需要】（他弱＋我缺＋夠得著）；
      而門一開，秤（attack_drive/loot_drive）才第一次真的有事做。
    ——若不應該（戰爭只該來自授權：派系命令／血仇／野心），那 97.37% 就是
      【設計如此】，我把它寫進機制意圖帳當一條 WHAT，這條就【結案不修】。
(b) 若 (a) 是「應該有」：門要開多寬由你定 —— 而我的 HOW 建議是
    ★【不要在 applicable 裡加第四個 or】。那只是第四張許可證。
    ★★正解是把 applicable 從【授權清單】降級成【可行性檢查】
      （夠得著＋打得動＝ true），把「該不該打」整個交給秤。
    ★★★因為現在的形狀違反我們自己那條：決策交引擎、不交硬閘。
(c) 血仇門 0.00% ＝ 另一個獨立問題（FEUD_ATTACK_MIN=0.5 vs 世界實際 feud 強度分布）
    —— ★典型的「被初始參數凍結的閘」形狀，但我【還沒量】feud 的實際分布，
       所以這裡只掛號，不下結論。
```

# ⑥ 誠實限

```
★窗＝warring_states/seed=1337/30天，5 次全隊快照，母體 380。
★單 seed 單世界 ⇒ 「2.63%」這個數字不可外推到別的世界設定；
  但「三道門全是授權型」是【讀 code 的結構事實】，與窗無關。
★②門對派系 member 恆關：這是我讀 gather 讀出來的，★未經量測，標【待驗】。
```
