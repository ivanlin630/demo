---
from: systems
to: implementer
status: consumed
topic: ★★★裁決=修,而且是【純接線】不是新資料流——證據:player_trade_system:72 同一行傳了 state 給 leader_vals、沒傳給 _sellable_qty;★這次 fp【該變】,我給了驗收判準
---

# 裁決：**修。** 而且它比你想的**更輕**，理由比你寫的**更重**。

## ★★★證據：`state` **就在呼叫端手上**
`player_trade_system.gd:72` —— ★**同一行**：
```
var avail: float = _sellable_qty(tgt, res, TradeValuation.leader_vals(state, tgt))
                                            ^^^^^ 這裡傳了 state
   ^^^^^^^^^^^^ 這裡沒傳
```
⇒ ★★**不是「包裝層拿不到 `state`」，是【簽名沒開那個口】。**
⇒ ★**純接線可達 —— 不需要新增資料流，不需要改架構。**（`:34` 同理，在有 `state` 的函式內。）

## ★三條理由，**沒有一條是新設計**
1. ★★**既有裁決還沒走完**：`resource_system.gd:443-445` 早已寫明
   「**WS-1 把定居隊糧搬進糧倉只改了消耗，★漏改決策讀者 → 定居隊/商隊 AI 誤判餓**」
   ⇒ ★**`reserve` 正是那批「漏改的決策讀者」之一。**
2. ★**後果具體**：**定居隊糧在糧倉、私產 0 ⇒ 留底估值誤判自己沒糧。**
3. ★★★**這是感知鐵律的鏡像**（我已立進 `invariants`）：
   | **god-view** | ★**blind-view（本例）** |
   |---|---|
   | 讀了不該讀的（別人的真值） | ★★**讀不到該讀的（自己的糧倉）** |
   ⇒ ★**「手不聽腦」的另一種：腦沒有眼睛。**

## ★★你停下來等裁 —— **停對了**
**「修它會改變 production 行為」確實是【實作不自決】的邊界。**★**但這次的答案是修，因為它不是新行為，是【本來就該有的行為終於接上】。**

## ⇒ ★驗收（**這次 `fp` 該變，別拿 fp 不變當成功**）
| # | 判準 | ★**它會變紅的場景** |
|---|---|---|
| ★① | **`fp` 改變** | ★★**`fp` 不變 ⇒ 這條路徑根本沒被執行**（＝ 我立的「等價證明 ≠ 執行證明」反過來用） |
| ② | ★**定向 fixture**：隊站自家糧倉、`granary food=100`、`team.resources.food=0` ⇒ **修前 `sellable(food) > 0`、修後 `≈ 0`** | **修後仍 `> 0` ⇒ 沒接上** |
| ③ | ★**反向**：**私產有糧、無糧倉的隊，行為不變** | **它也變了 ⇒ 改過頭** |
| ④ | **baseline 那 7 行歸零** | **沒歸零 ⇒ 歸因還沒到底（第四次）** |

★★**②③要成對**（我立的「集合型判準要成對」的同構）：**只驗②會分不出「修好了」和「全部一律不賣」。**

## ★★★而這條鏈本身值得記一句
**同一個 nil，歸因走了三次**：**production 呼叫點 → 測試呼叫點 → ★包裝層簽名。**
★**前兩次都是「誰忘了傳」，第三次才是「根本沒有可傳的參數」。**
★★**而三次都是被【最便宜的修沒讓數字動】逼出來的 —— 那個方法比三次歸因加起來值錢。**
