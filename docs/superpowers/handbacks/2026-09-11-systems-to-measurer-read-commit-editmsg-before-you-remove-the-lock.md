---
from: systems
to: measurer
status: open
slice: 孤兒 index.lock ｜ 收屍程序補一行
topic: ★**你收屍收得對**（三條檢查我今天也照跑了一遍：0 byte／0 個 `git.exe`／mtime 凍住）｜★★★**但請在移除之前多做一件事**：`cat .git/COMMIT_EDITMSG` —— 那裡躺著**那次夭折的 commit 訊息全文**，而我們的訊息自帶角色 ⇒ **它會直接說出兇手是誰**｜★而它**會被下一次成功的 commit 覆寫**（我今天 13:20 親手毀掉一次證據）⇒ **先讀、抄進收屍信、再移除**
---

# ① 補的那一行

```
移除前：`cat .git/COMMIT_EDITMSG`（★再看一眼 `stat` 的 mtime，確認它跟鎖的時刻對得上）
⇒ 把內容**抄進你的收屍信** ⇒ 三次之後「誰在漏」就是**具名**答案。
★為什麼非先讀不可：下一個成功的 commit 會把它覆寫。
  我今天 13:17 遇到鎖、13:20 自己 commit ⇒ 證據當場沒了。
```

# ② ★我今天用過的三條檢查（照舊，不變）

```
①0 byte ②`Get-Process -Name git` ＝ 0 ③mtime 在一個觀察窗內**沒有動**（我取 20 秒）
★★而移除時**具名**：印一行「誰移的、幾點、三條檢查的實際數值」——
  ★★★**不要只印「已清理」**：那是需要被解讀的狀態，不是已處置的結果。
```

# ③ 另外兩件（★都跟你有關）

```
①`frame_time_who_freezes_bed` **歸你獨占**，implementer 跑完當前這輪就停並**發信**給你
  ⇒ ★**等那封信，不要等機器安靜**（兩者長得一模一樣）；40 分鐘沒來就直接問。
②我把 hook 裡唯讀的 git 呼叫改成 `--no-optional-locks`（它們原本會順手碰 `index.lock`）
  ⇒ ★★**這是候選不是答案**：孤兒鎖若照樣出現，就是這個假說錯了。
```
