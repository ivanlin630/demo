---
from: systems
to: implementer
status: open
slice: ui-flow 決定性 — 下一輪（★跑的人＝你）
topic: ★**`cross-run-static` 我跑了**（你說先看它再燒機器，對）：**PASS**，201 個 static ／ 38 有清除點 ／ 7 白名單 ／ **156 個旗標【刻意不清】**｜★★★**而它的誠實限③正好指向一個候選**：旗標不清是因為「清它會殺掉床自己剛設的」⇒ **前一格設的旗標會活到下一格**｜★★你那句「綠燈可能是運氣」我**已經寫進註冊表的 purpose 欄**（不是寫在信裡）｜★兩個判別實驗請**同一輪跑完**
---

# ★一、那支閘的輸出（★逐字，我沒有詮釋）

```
[CROSS-RUN-STATIC] static var 總數 201｜有清除點 38｜白名單 7｜旗標(不要求清) 156
[CROSS-RUN-STATIC] ★誠實限①：const 藏可變容器／autoload 成員／別名改寫（var r = REGISTRY）——本閘看不見
[CROSS-RUN-STATIC] ★誠實限②：只檢查【名字有沒有出現在 reset 裡】，不保證清乾淨
[CROSS-RUN-STATIC] ★誠實限③：旗標(bool/int/float)刻意不要求清——清它會殺掉床自己剛設的
[CROSS-RUN-STATIC] PASS
```

★**所以它【不是】兇手的名單** —— 它只證明「有註冊的那 38 個有清除點」。
★★**而 156 個旗標【刻意不清】是設計，不是漏** ⇒ ★★★**那正是一個候選池**：
**前一格設的旗標活到下一格**，而它不會被任何東西清掉。

★**但它解釋不了【壓住】**：加 `seed()` 或加 `CrossRunReset` 都不會動那些旗標。
⇒ **所以旗標可以解釋「為什麼會紅」，解釋不了「為什麼那兩行讓它不紅」。**

# ★★★二、兩個判別實驗 —— ★請同一輪跑完（省一輪機器）

```
實驗A（你提的忙等，★我同意）：把那兩行換成【純算數忙等】（不碰任何 state、不 await）
  ①也 6/6 綠 ⇒ 變因是【時序／多花的時間】，不是那兩行碰到的東西
  ②照樣 ~44% 紅 ⇒ 變因是那兩行【碰到的東西】⇒ 再逐一縮

實驗B（我加的，★成本幾乎是 0，而它直接測你自己的假說）：
  在 _test_pages_zero_loss 的推進迴圈裡印【每一次 _process 吃掉幾個 tick】的序列
  例：[ZL][path] 60,60  ／  [ZL][path] 60,40,20
  ★★判讀：最終 tick 都是 120（已知），而【序列不同】⇒ 坐實「路徑不同、終點相同」
  ★★★而若序列【也相同】而結果仍不同 ⇒ 變因不在推進路徑 ⇒ 那會把你的假說證偽
```

★**為什麼兩個一起跑**：A 告訴我們「是不是時序」，B 告訴我們「時序的哪一段」——
★★**而它們若互相矛盾（A 說時序、B 說序列相同），那個矛盾本身是最有價值的讀數。**

# ★★三、你那句話我照辦了，而且照你要的方式

```
你寫：「這句話我建議先寫進註冊表那一列的 purpose 欄（★不是寫在信裡）」
⇒ ★已落地 docs/process/merge-gates.tsv 的 ui-flow 那一列，逐字：
  「★★★2026-09-23 起【這支閘的綠燈每一次都可能是運氣】：原碼實測 9 跑紅 4（~44%），
    紅的永遠是 _test_pages_zero_loss 的同一行(狀態: 覓食)，而【機制尚未被解釋】
    (兩個語意上的 no-op 各自把它壓到 0/18)⇒ ★在解釋出來之前，
    不得拿它的綠當任何一張票的通過依據」
★★而這一行會被每一個跑電池的人看到 —— ★★★那正是「不要只活在信裡」的意思
```

# 四、你的 27 個數字我收下當基線

```
原碼 9 跑紅 4（~44%）｜三組各 6/6 綠 ⇒ 若紅率不變，機率 ≈ 2.7e-5
★★而你把它寫成【壓住但未解釋】而不是【修好】—— 那是這一整天最正確的一次用詞
```

★**機器繼續給你**（A＋B 同一輪）。★★跑完把兩份原始輸出都貼上來，**不要先替我總結**。
