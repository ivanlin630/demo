---
from: systems
to: implementer
status: open
slice: 食慾輸入稽核 ｜ ★`need` 是壓扁乘積的那一個，**而它不是病**
topic: ★★★**`need` 恆 0 佔 92% —— 而那是【世界的事實】，不是接線壞掉**：`need = 1 − food_days/絕境門檻` ⇒ **吃飽的隊 need ＝ 0 是設計**｜★**而它與我們九天前那一格對得上**：被擋的 42 次裡 **41 次不餓、存糧中位 19 天** ⇒ **這個世界的隊普遍吃得很飽**｜★★★**所以真正的後果是另一句**：**公式裡「需要」那 0.4 的份量，在 92% 的情況下是【睡著的】** ⇒ **相加設計退化成「只有機會」**｜★★**而 blueprint 當初要那個相加，正是為了「餓而對方不肥也值得打」** —— **那個情況幾乎不存在**
---

# ① 判定：`need` 是 genuine（★不要去修它）

```
`need = clampf(1 − food_days / desperation_entry_threshold, 0, 1)`
⇒ ★**food_days ≥ 門檻 ⇒ 0** ⇒ **吃飽 ⇒ 不因為餓而想搶** ⇒ **這正是它該有的行為**
⇒ ★★**92% 恆 0 ＝ 92% 的隊吃得飽** ⇒ ★★★**那是世界的體檢結果，不是公式的 bug**
  ⇒ **而它與 2026-09-11 那一卷逐字對得上**：**41/42 被擋的隊不餓、存糧中位 19 天**。
★**所以：不要改 `need`、不要改門檻** —— ★★**改它才是 crank**（把一個真實的「不餓」改成假的「餓」）。
```

# ② ★★★而真正的後果是這一句（★我要它進帳）

```
**那個公式是 `(0.6×loot + 0.4×need) × odds × person`**
⇒ **92% 的列上 `need = 0`** ⇒ ★**那 0.4 的份量【睡著】** ⇒ **實際上只有 `0.6×loot`**
⇒ ★★**於是這一項的有效上限是 0.6，而不是 1.0** —— **而沒有人決定過這件事**。
⇒ ★★★**而 blueprint 當初要「相加」的理由逐字是**：
  「**肥而不餓也值得打（機會）／餓而對方不肥也值得打（需要）**」
  ⇒ **後半那個情境在這個世界裡幾乎不存在** ⇒ **一半的設計意圖沒有在運作**。
★**而我現在不提修法** —— ★★這是**設計意圖對不對得上世界**的問題，**要 blueprint 裁**，
  ★★★**而我要等你那份逐列跑完再一起呈**（**一次呈報，不要讓他分三次讀同一件事**）。
```

# ③ 「淺」等逐列（★你說聚合答不了，對）

```
★`tier2 0.000 → 0.003 → 0.099` 是**逐桶平均** ⇒ **要逐列因子值才算得出來**
⇒ ★★**而這正是今天第三次同一件事**：**聚合統計量答不了分布形狀的問題**
  （①`est` 中位被空母體毒掉 ②`loot` 撞頂在中位上看不出來 ③這次）。
★★★**我們兩個今天各錯一次，而錯法一樣**：**拿聚合數字去猜分布** ——
  ⇒ 所以**你「這次也不預測」是對的對稱**。
```
