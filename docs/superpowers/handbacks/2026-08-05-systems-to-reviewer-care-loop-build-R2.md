---
from: systems
to: reviewer
status: consumed
topic: "[R² merge-gate 審 領主主動照護 loop build(feat/lord-care-loop a24d4c71、HOW R² 你上輪 CLEAN+2必查項、現審 build diff+★逐行核 firsthand 呼叫點鎖 co-location=最高風險 god-view 防線)·四塊:①holding ledger 第4 kind(_ensure_holding_ledger once-guard+_step_contact_ledger holding refresh-and-keep 必查項①:逾時 fire 後不 resolved+refresh dispatched_tick+放回 kept)②care/ignore competing util(_pick_care_reaction 責任 vs 疏忽 argmax 非 if/elif)③★必查項② firsthand 觀察 write INLINE④觀察→distribute reuse·★★systems 親驗 firsthand 呼叫點=_tick_info_scout:1595 if scout.tile_pos==target.tile_pos co-location 分支內(:1600-1623:synth distress posted-order-independent+:1618 firsthand honest+:1623 care.firsthand_distress)=INLINE 非外部函式、但請你獨立逐行核(讀 branch HEAD live 非 commit 歷史、確認非又被抽外部)·gate:care_test 5/5(firsthand 傲村+觀察→賑濟端到端+★零 god-view 需 co-location)+headless 0-new+constitution 74(firsthand co-location 內未新增 gv)+determinism FCE1BAC4 byte-identical·★審點:①★★firsthand write 逐行鎖 co-location 分支內(讀 food/pop 跟 distribute-descan _resident_food_runway god-view 差別只 co-location gate、分支外=違憲換皮)②holding refresh-and-keep 語意乾淨(逾時不永久丟、持久監看)③care/ignore 真 competing argmax④零死常數/程度界線(非保姆國家)⑤distress belief 帶時戳去重·R² CLEAN→measurer 量 cohesion①natural 真考(moderate ex-ante 床責任 lord 村留人/疏忽村叛離)→QA→merge→推用戶·地基 KEEP"
---

# R² merge-gate 審 領主主動照護 loop build

HOW R² 你上輪 CLEAN + 2 必查項 → build 完（`feat/lord-care-loop` `a24d4c71`）→ R² merge-gate 審 diff + **★逐行核 firsthand 呼叫點鎖 co-location（最高風險 god-view 防線）**。

## 四塊
- ①holding ledger 第 4 kind（`_ensure_holding_ledger` once-guard + `_step_contact_ledger` holding **refresh-and-keep** 必查項①：逾時 fire 後不 resolved+refresh dispatched_tick+放回 kept）。
- ②care/ignore competing util（`_pick_care_reaction` 責任 vs 疏忽 argmax 非 if/elif）。
- ③★必查項② firsthand 觀察 write INLINE。
- ④觀察→distribute reuse。

## ★★systems 親驗 firsthand 呼叫點（但請你獨立逐行核）
`_tick_info_scout:1595 if scout.tile_pos==target.tile_pos`（co-location 分支）**內**（`:1600-1623`：synth distress posted-order-independent + `:1618` firsthand honest + `:1623` care.firsthand_distress）=**INLINE 非外部函式**。**請你獨立逐行核**（讀 branch HEAD live 非 commit 歷史、確認非又被抽外部/god-view 直讀）。

## gate 全綠
care_test **5/5**（firsthand 傲村 + 觀察→賑濟端到端 + ★零 god-view 需 co-location）+ headless **0-new** + constitution **74**（firsthand co-location 內未新增 gv）+ determinism **FCE1BAC4 byte-identical**。

## ★審點
1. **★★firsthand write 逐行鎖 co-location 分支內**（讀 food/pop 跟 distribute-descan `_resident_food_runway` god-view 差別只 co-location gate、分支外=違憲換皮）。
2. **holding refresh-and-keep 語意乾淨**（逾時不永久丟、持久監看）。
3. **care/ignore 真 competing argmax**。
4. **零死常數 / 程度界線**（非保姆國家）。
5. distress belief 帶時戳去重。

## 序
R² CLEAN → measurer 量 **cohesion ①natural 真考**（moderate ex-ante 床：責任 lord 村留人/疏忽村叛離）→ QA → merge → blueprint 推用戶。地基 KEEP。
