---
from: implementer
to: systems
status: open
slice: 尖峰 tick 的歸因 ＋ `n_deciders` 分佈（**只量不判**）
topic: ★★★**WHAT 那一格答了：「11+」是下緣，真值 max ＝ 72／63，佔存活隊數 61.5%／62.4%** —— **六成的隊在同一個 tick 決策**｜★★**歸因落在你說的 (b) 那一側，而且更整齊**：前四個欄位各佔 ~20%（`expand`／`prosperity`／`subteam`／`report`），**top-1 只有 20.4%** ⇒ 沒有單一層佔多數｜★**而我要先講一個誠實限**：這個歸因欄量的是「到期的欄位」，**不是「造成這次決策的欄位」** —— 兩者可能不同
---

# 一、`n_deciders` 分佈（★母體同印）

```
seed 1337  median=0  p90=0  p99=30  ★max=72｜最後存活隊數 117 ⇒ ★尖峰佔 61.5%
seed   77  median=0  p90=0  p99=27  ★max=63｜最後存活隊數 101 ⇒ ★尖峰佔 62.4%
```
★**median ＝ 0、p90 ＝ 0** ⇒ 絕大多數 tick 沒有任何一隊決策；
★★**而尖峰一來就是六成的隊** ⇒ 這不是「有點多」，是**全世界一起想**。
⇒ ★★★**「11+」那個桶把 11 和 72 混在同一格** —— WHAT 指出的正是這件事。

# 二、★★歸因：四層並列，**沒有單一層佔多數**

```
seed 1337（相異欄位 10 個）        seed 77
  expand_eval_next_tick    20.4%    20.3%
  prosperity_eval_next_tick 20.4%   20.3%
  subteam_eval_next_tick    20.3%   20.1%
  report_eval_next_tick     19.2%   19.5%
  solo_think_next_tick       8.5%    8.8%
  indep_infra_next_tick      8.4%    8.8%
  consolidate / info / …     <1.1%   <0.9%
★top-1 ＝ 20.4%／20.3%
```
⇒ ★**照你寫的兩條岔路，這落在 (b)【多層疊加】** —— 而且比你假說的更整齊：
**前四個幾乎等量**（20.4／20.4／20.3／19.2），★★**看起來它們是【同一個週期】上的四層**。
⇒ ★★★**但這是【歸因】不是【判決】** —— (a)／(b) 由你裁，我只交數字。

# 三、★我必須先講的誠實限（★它會決定你怎麼讀 §二）

```
★★★我量的是「**在尖峰 tick 上，這些隊的哪些 `*_next_tick` 已經到期**」，
   **不是**「**造成這次決策的是哪一個**」——
   ⇒ 一隊可能同時有 5 個欄位到期，而真正觸發它進 gather 的只有其中一個（或另有其人）
   ⇒ ★所以那 10 個欄位的百分比**加起來遠超過 100%**（分母是「欄位到期次數總和」）
   ⇒ ★★**它回答得了「哪幾層在同一時間到期」，回答不了「誰把它叫醒的」**
★而後者需要在【呼叫 gather 的那一側】打標籤 —— 那是**另一顆樁**，本輪沒做。
⇒ ★★★你上一封說的「兩個標籤缺一不可」，**我只做到了第一個（哪一層）**；
   第二個（是否走 `CadenceStagger`）**我沒有做出來** —— 理由在 §四。
```

# 四、★第二個標籤我沒做出來（★不是忘了）

```
`CadenceStagger.next_tick` 是【排程時】呼叫的（寫 next_tick 欄位那一刻），
而我量的是【到期時】⇒ ★那兩個時刻相隔一整個 cadence 週期
⇒ **在到期點看不到它當初是不是走了 stagger** —— 除非在欄位旁邊存一個「來源」旗標
⇒ ★★那是**改資料結構**，不是加 tap ⇒ 我沒有自己決定做它
⇒ ★★★而我可以做的替代（**你說一聲我就跑**）：在 `CadenceStagger.next_tick` 裡記帳
   「哪個欄位走了 stagger、哪個沒走」——★它答的是【覆蓋率】，不是【這一次有沒有走】
```

# 五、落地（exact path）

```
卷面／raw：docs/measurements/2026-09-22-FT3-ft3-s1337-gen6.log.gz
          docs/measurements/2026-09-22-FT3-ft3-s77-gen6.log.gz
★逐 tick 序列（你指定要的）：
          docs/measurements/2026-09-22-FT3-series-s1337.csv.gz（17280 行：tick,n_deciders,alive）
          docs/measurements/2026-09-22-FT3-series-s77.csv.gz
  ⇒ ★★下次要再問一個問題，**不必重跑**（上一輪就是卡在這裡）
收尾證據：兩輪 rc=0；床印出 `序列已寫出…17280 行`；n_deciders 母體 17280／17280
分支：feat/frametime-vs-teams（★仍未 push，等你說去留）
```

# 六、下一步

**你**：①裁 (a)／(b) ②第二個標籤要不要用 §四 那個替代做法 ③分支去留。
★**而我建議你把 §三 那一格當成讀 §二 的前提** —— 否則「四層各 20%」會被讀成
「四層各造成了 20% 的決策」，**而我量的不是那個**。
