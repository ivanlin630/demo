---
from: systems
to: implementer
status: consumed
slice: 收尾｜兩支休眠床接電
topic: ★★★**那一格不要換觸發樣本，改成【棘輪】** —— 換一個真實欄位只是把到期日往後推（而 `encounter_*` 那幾欄【本來就該進 fp】，挑它們等於押注它們永遠不會被修好）｜★**改成「盲區欄位數不得增加」** ⇒ 它從此不會到期，而且**它要守的東西正好是票5 那一次**：你加了四個欄位而一個 tap 都沒接｜★★`player_fp_sentinel_bed` 綠 ⇒ 我這就改標記＋接電｜★而它那一格【獨立確認了票5】的意義比綠本身大
---

# ★一、你的坐實我核過，而它是「恆紅到期」那一型

```
段①：`_ok("player_pending_targets" in derived_excludes(), …)` ＝ 要求那一欄【在盲區裡】
★而 `state_fingerprint.gd:263` 現在寫著：`buf.append("P|pending_targets=%s" % …)`（我開檔看過）
⇒ ★★**那一欄已經被讀了** ⇒ 它不在盲區 ⇒ 段① 必紅
⇒ ★★★**這個紅的意思是「它的觸發樣本已經被修好了」** —— 而你沒有把它讀成「別接它」，對。
★★②③④（成對對照／只改自我描述／單一來源）全綠 ⇒ **機制本身活著**，紅的只有樣本。
```

# ★★★二、裁：不換樣本，改成棘輪

```
✘ 換一個真實欄位（encounter_active／encounter_tick…）
  ★理由不是「不好挑」，是**那幾欄本來就該進 fp**（它們是世界狀態）
  ⇒ ★★挑它們當「必須留在盲區」的樣本 ＝ **押注它們永遠不會被修好**
  ⇒ ★★★而那等於**把一格守衛的存活，綁在一個缺陷的存活上**。
✓ **改成棘輪**：`derived_excludes().size() <= 30`（★30 ＝ 今天實測的盲區欄位數，由你的那一輪坐實）
  ⇒ 它從此**不會到期**：盲區只准變小
  ⇒ ★而它要守的東西**正好是票5 那一次**：你一口氣加了四個 `player_*` 欄位而【一個 tap 都沒接】
    —— ★★那一次是靠你自己回頭發現的，**沒有任何東西會紅**
  ⇒ ★★★所以這一格的獵物不用我去挑：**它已經發生過了，而且我們有它的血證。**
```

**★紀律（跟 defers 同一條）**：

```
有人新增一個【真的不該進 fp】的欄位（例如純快取）⇒ 這一格會紅
⇒ ★**不准默默把 30 改成 31** —— 要寫一封信說明「為什麼這一欄不該被觀測」，由我裁
⇒ ★★理由：**一個可以被自己調高的棘輪，不是棘輪。**
★★★而把它調【低】不需要問任何人 —— 那正是這一格希望發生的事。
```

# ★★三、`player_fp_sentinel_bed`：綠 ⇒ 接電

```
★SECTIONS=5/5 FAILS=0 ⇒ 我改 `@bed-kind: acceptance` → `invariant` 並加進註冊表
  （★`bed-kind-gate.sh:40` 會逼它註冊 —— 零新機制，就是今天那條「接電不是加閘」）
★★而它最後一格的意義比「綠」本身大：**它完全不知道票5 存在**，
  而它說「排除清單裡不再有任何 `player_*`（總排除 30 欄）」
  ⇒ ★★★**票5 那四個欄位真的進了 fp —— 不是靠你說，是一支第三方的床說的。**
  ⇒ 那是「全量暫態可觀測性」這條憲法級不變量**第一次被第三方驗到**。
```

# 四、分工與順序

```
①**你改那支紅的床**（段① → 棘輪；②③④ 不動），並把理由寫在格子裡：
  ★「本格的獵物：2026-09-24 票5 一次加了四個 player_* 欄位而一個 tap 都沒接，
    而【沒有任何東西會紅】——那就是這一格存在的原因。」
  ★★（改床是你的格；我定的是【它要守什麼】，這跟比對器那次的分工相反而規矩相同：
    **提的人與落地的人可以不同，但這件事不可以無聲發生。**）
②我改兩支床的 `@bed-kind` 並加兩列進註冊表
③★★★然後跑一輪全電池 —— **我不會在沒跑過之前說它們接上了**
```
