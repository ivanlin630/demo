---
from: systems
to: implementer
status: open
slice: C1 票② 文字版畫面走查 ＋ 常駐狀態列 —— ★DISPATCH
topic: ★派工：`specs/2026-09-10-agent-verbs-c1-ticket2-walkthrough-HOW.md`（R² 已審＋blueprint 已點頭，補完即就緒不再送審）｜★★序：**B1 那格收尾閘先做完**，然後這張｜★★★這張票有一格【不是程式】：走查開場要**明示**「這份裡埋了 N 顆已知錯（N 講明、位置不講）」——blueprint 的理由是**暗埋＝對老闆設局，而信任是體驗窗的本錢**
---

# ① 派工

```
spec  docs/superpowers/specs/2026-09-10-agent-verbs-c1-ticket2-walkthrough-HOW.md
狀態  R² 審過（兩格已補）＋ blueprint 對「種錯」點頭並改了兩處粒度 ⇒ 就緒
序    ★先做完 B1 的收尾閘（exposed 列的 who 宣稱要可被證偽），再做這張
```

# ② ★★這張票是兩件事的【批准閘】

```
①分頁 —— 119 欄現在全部標【暫定】，★要在這次走查被真人簽了才拿掉
②看不看得懂 —— ★★而【拿不拿得到】已經由你 B1 那支動詞解決了
   ⇒ 所以這張票【不是再接一次線】，是把已經拿得到的東西【排成人看得懂的畫面】。
```

# ③ ★★★不是程式的那一格（blueprint 定案，逐字照做）

```
①★【明示】走查開場就講：「這一份裡埋了 N 顆已知錯（★N 講明、位置不講），抓到幾顆算幾顆。」
   理由（blueprint）：★★暗埋 ＝ 對老闆設局；抓不到時他的感受是【被耍】不是【被保護】，
   ★★★而信任是體驗窗的本錢。
   ⇒ 明示【不毀對照】：他仍然不知道在哪，鑑別力還在，而走查從【考試】變成【遊戲】。
②★N ＝ 1-2 顆，不要多。
③★★漏抓的那一顆所在的【那一頁】重審 —— **不整份作廢**（他的時間也是預算）。
   ⇒ 這一條是 blueprint 改掉我原本寫的「整份簽不算數」，★★★我原本那個粒度太粗。
★埋法建議：某頁漏印一個原本該有的欄位／某隊 current_task 印成一個不存在的字串。
★★埋的位置與內容要記在【交件裡】（不是只記在你腦裡）——否則沒有人能判他有沒有抓到。
```

# ④ 走查腳本三條硬規（★這三條決定它有沒有用）

```
①★只呼叫【公開查詢動詞】——不得直接讀 state。
  ⇒ 這樣走查同時是【你 B1 那支動詞的驗收工具】：接不出來的欄位會在畫面上開天窗。
②★★天窗要具名印出：「〈欄位名〉：未接出（TODO:<票路徑>）」——
  ★★★沉默的空白會被讀成「這個世界沒有這個東西」。
③★世界要是【真的跑過的】（有隊、有據點、有市場單）——
  ★★開局第 0 tick 的空世界【每一頁都很好看】。
```

# ⑤ 狀態列（blueprint 裁：`current_task` 的家）

```
四件：附身對象名／位置（格＋據點名）／current_task（＋一行 intent 摘要）／世界時間＋速度檔
形態：REPL＝`status` 動詞回這四件；GUI＝頂欄（★GUI 不在本票）。
★★而第四件【兩半都讀不到】（R² 掃 16 支動詞：world_time／speed 皆零命中）⇒ 本票要補：
   ①世界時間 ⇒ player_query_api 加薄 wrapper 讀 state.world.current_tick
   ②★★★速度檔 ⇒ 記在【呼叫端／REPL 自己的 session】，**不進 WorldState／sim_runner**
      （票① 的設計是「不在 sim_runner 加狀態」，那條理由是對的——加了就是第二個真相源）
      ⇒ getter 讀的是【呼叫端的記憶】，不是 state。
★狀態列的四件事【必須來自既有查詢面】，不得為它另開一條讀 state 的捷徑。
```

# ⑥ 驗收

```
①走查腳本可重跑（在 repo 裡，一行指令）——★一次性的貼圖不算交付
②五頁＋狀態列都印得出來；天窗具名、不靜默
③狀態列四件事都來自公開查詢動詞（最小腳本證明）
④★★成對對照：把某一頁的一個欄位【從查詢面拿掉】⇒ 走查那一格必須變成天窗
   ★★★R² 覆核過這格【落在真行為區間】：它防的是【畫面接的是快照/快取不是即時查詢】——
   若走查把結果存成「跑一次往後重播」的快取，拿掉欄位後畫面【仍會顯示舊值】。
⑤fp 不變（本票只讀不寫）
```

# ⑦ 你 B1 寫的誠實限，我整段搬進本票（★走查會撞到）

```
★快照是【上一次決策時】的值，`age_ticks` 就是那個誤差，而它會隨 cadence 變大
⇒ ★★走查看到「數字跟畫面不一致」時，【第一件事是看 age_ticks】，不是懷疑欄位接錯。
★只有【玩家控制的隊】有快照 ⇒ 走查若想看別隊的 ctx：沒有，也不該有（那是別人的腦）。
★★★補充母體那張表仍有【關係圖全貌 todo】⇒ 兩張表都綠才算完成 ⇒ 走查那一頁會有天窗，
   而那個天窗【是對的】，不要為了畫面好看把它補掉。
```
