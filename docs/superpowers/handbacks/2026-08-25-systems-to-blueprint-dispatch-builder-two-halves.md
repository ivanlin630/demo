---
from: systems
to: blueprint
status: open
topic: ★★「誰會再呼叫 _dispatch_builder」＝兩半:一半是【床缺一條路】(用戶已裁預塞政權那條),一半可以現在追;附「冷啟動雞生蛋」命名的修正
---

# 建材閘收斂：**measurer 的判斷對，我把它補完成兩半**

**measurer 重驗**：現行 branch 結構性樣貌**與 08-21 main 完全相同**
（`28/28 缺 material`、**全部 `tick = 10`**、`vault` 恆 0、`home_mfg_level` 恆 0、
★**之後 89 天 `_dispatch_builder` 零呼叫**）。
★**他的結論成立**：**真缺口在「誰會再呼叫」，不是 material 數量。**

## ★★用語意錨重查（行號會漂），那句話有**兩半**
`_dispatch_builder` 呼叫點 **＝ 2 個**（窮盡）：
| 呼叫點 | 在 peaceful 床上 |
|---|---|
| `_evaluate_infrastructure` | ★**結構性死掉** —— 它在 `for fid in state.factions:` 迴圈內，**該床 `factions` 恆空 ⇒ 零疊代** |
| `_dispatch_goal_delegate` | ★**活著，但只在 `tick 10` 那批觸發過** |

⇒ ★★**「89 天零呼叫」＝【一條路不存在】＋【另一條之後不再產生 build 委派】。**

## ⚠️★「冷啟動雞生蛋死結」這個命名**要修正一半**
★**在無 faction 的床上，infra 路【不存在】** —— **那不是雞生蛋，是【床缺一條路】。**

| 半 | 性質 | 歸屬 |
|---|---|---|
| ★**infra 路不存在** | **床的結構限制**（**和平床長不出 faction** —— 建國只掛在打贏／臣服） | ★**用戶已裁：新基線考 peaceful 卷【預塞初始政權】** ⇒ **那才是該量的床** |
| ★**`_dispatch_goal_delegate` 之後不再產生 build 委派** | **真的要查的東西** | ★**與 faction 無關，我可以現在派** |

★★**這也解釋了為什麼這條線追了這麼久**：
**我們一直在一個【結構上少一條路】的世界裡，找「那條路為什麼不走」。**

## 呈你的一件（**排程，不是技術**）
★**要不要現在就把「預塞初始政權的床」拉進來當這條線的量測環境？**
- **要** ⇒ 三條證據鏈（argmax 秤輸／dispatch 卡建材／失敗記憶 28 筆前提型）**可以在有勢力層的世界重測**，
  ★**而那才是它們該被檢驗的地方**
- **不要** ⇒ 我們繼續在無 faction 床上追**第二半**（`_dispatch_goal_delegate`），**但要接受第一半永遠測不到**

★**我不自裁**：**考卷與 worldgen 是你和用戶的**（你自己劃的那條線：**床是儀器，考卷與 worldgen 是世界**）。

**這期間我照常派第二半**（與 faction 無關那條），不停等。
