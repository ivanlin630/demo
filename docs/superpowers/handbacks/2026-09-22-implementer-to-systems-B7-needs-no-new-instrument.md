---
from: implementer
to: systems
status: consumed
slice: B7 守恆格 —— ★**現有的床已經印得出來，不必造新儀器**
topic: ★**`pass_tick_phase_breakdown_bed` 已經同時印 `pass 總 dt` 與 `非 pass 合計`** ⇒ B7 ＝ 兩臂相減，**零新 tap、零新床**｜★★**而 B7 有一個會騙人的模式要先寫死**：`非 pass 合計` 目前只有 **5.3M us**，而 `pass 總 dt` 是 **372.6M us** ⇒ ★★★**兩邊的量級差 70 倍** —— 「非 pass 等量上升」在【百分比】上會是一個巨大的數字（+300% 之類），**而那是正常的、不是異常**｜★所以 B7 的判準要寫成**絕對量相加 ≈ 0**，不是「兩邊各自變動 ±x%」
---

# 一、★現成的數字（乾淨卷面，12 天 × 兩顆種子）

```
                pass 總 dt          非 pass 合計       比
seed 1337    372,620,297 us        5,296,013 us      70.4×
seed   77    347,786,609 us        5,474,657 us      63.5×
⇒ ★B7 ＝ `Δ(pass 總 dt) + Δ(非 pass 合計)`，兩個數字**同一支床、同一輪**就有
⇒ ★★不必新增任何 tap —— ★★★而「不必新增」這件事本身值得寫進票，
   否則下一個人會為了 B7 再造一支床（而第二支從出生就開始漂）。
```

# 二、★★★B7 會騙人的那一格（★先寫死，免得驗收時被它嚇到）

```
若抑制生效，被推遲的工作會落到非 pass tick 上：
  ★預期：pass −(1−f)×34.7% ≈ −14%（你的算式）⇒ 約 **−52M us**
  ★★而非 pass 目前總共只有 **5.3M us** ⇒ 它要吸收 52M ⇒ **+1000%**
⇒ ★★★**非 pass 那一欄會【爆炸性上升】，而那正是成功的樣子。**
   ⇒ 若有人用「±x%」去看它 ⇒ **會把成功判成災難**。
⇒ 判準寫法（建議，裁在你）：
   **B7 綠 ＝ `|Δ(pass) + Δ(非pass)| ÷ |Δ(pass)| ≤ 0.15`**（相對於【被搬動的量】，不是相對於各欄自身）
   ★而 `Δ(pass) + Δ(非pass)` **顯著為負 ⇒ 那不是省，是漏**（你那句我照收）
   ★★顯著為正 ⇒ 抑制本身引入了額外成本（也要看得見）
```

# 三、狀態

```
・12 天 × 兩顆種子的 f（新判準）**仍在飛**（守衛在等孤兒進程）
・★實作未開始（等 WHAT 裁「值不值得」）
・★★而 B7 要真的有數字，需要【抑制之後】再跑一輪同一支床 —— 那是實作之後的事
```
