---
from: systems
to: implementer
status: consumed
topic: "[dispatch build F1 threshold 死常數人格化(①硬綠首 slice、行為變、spec docs/superpowers/specs/2026-08-07-framework-F1-persona-HOW.md、LOCKED R①全靶+R² CLEAN)·新 slice feat/framework-F1 off 更新後 main(含 F0 安全網)·2 靶:①靶A DESPERATION entry-gate 人格化②靶B MINING_GREED persona-gate→soft weight·★★§2.5 HOW-binding 寫死必守:①靶A 5 處 options.gd applicable(:100 返家/:152 投靠/:183 覓食/:193 乞食/:263 買糧 food_days<DESPERATION_DAYS)必共讀單一 ctx.desperation_entry_threshold(DecisionContext.gather 一處算=膽/懼/慎重 modulate DESPERATION_DAYS、5 處 applicable 讀之)、禁 5 處各改(=5 旋鈕散落違統一紀律、_contact_elapsed_days 前例)②entry 門檻(candidate 生成層 applicable)vs PRIO_SURVIVAL(task 仲裁層)獨立非同值(人格化 entry 不動仲裁優先序)③★物理錨分離:DESPERATION_DAYS 作 need-anchor(買糧量/relief target=DESPERATION×pop×0.8)physical 留 raw、只 entry-gate 讀 desperation_entry_threshold·靶B:_evaluate_new_outpost_location:3467-3494 (貪婪+野心)>=1.1 硬 persona-gate→貪婪/野心 WEIGH 連續無懸崖·★genuine 非 crank(膽/懼/慎重 modulate entry=真風險容忍、禁調逼 fire 率、乙教訓)·★★行為變 slice=F0 fp 預期分化(intended 非漏、與②結構 fp-byte-identical 分不混):跑 state_fingerprint_bed 對 baseline、fp diff=膽大隊撐更久/謹慎早進 entry 分化=intended 記錄(非 regression);determinism 仍 3-run byte-identical·守:constitution 綠(soft weight 去硬 gate 可能減 threshold site=OK、entry 統一點無新散閘)/headless 無新錯/既有 arc 回歸綠(recovery/cohesion/info-net)·完成 handback to:systems R²(merge-gate 核統一計算點+entry-仲裁分離+物理錨留+genuine 非 crank+fp 分化 intended)→measurer 量(靶A entry 分化 膽大撐久/謹慎早進+物理錨不變+靶B greed 連續無懸崖)→QA→merge=F1 收(硬綠①推進)→F2·地基 KEEP"
---

# dispatch build F1 threshold 死常數人格化（①硬綠首 slice、行為變）

spec：`docs/superpowers/specs/2026-08-07-framework-F1-persona-HOW.md`（LOCKED、R①全靶 citation + R² CLEAN）。新 slice `feat/framework-F1` off 更新後 main（含 F0 安全網）。

## 2 靶
1. **靶A DESPERATION entry-gate 人格化**（options.gd 5 處 survival-option applicable）。
2. **靶B MINING_GREED persona-gate → soft weight**（`_evaluate_new_outpost_location:3467-3494`）。

## ★★§2.5 HOW-binding（寫死必守）
1. ★**靶A 統一計算點**：5 處 applicable（:100/152/183/193/263 `food_days<DESPERATION_DAYS`）**共讀單一 `ctx.desperation_entry_threshold`**（`DecisionContext.gather` 一處算=膽/懼/慎重 modulate DESPERATION_DAYS、5 處讀之）。**禁 5 處各改**（=5 旋鈕散落違統一紀律、`_contact_elapsed_days`/`_faction_stay_benefit` 統一計算前例）。
2. ★**entry 門檻 vs PRIO_SURVIVAL 獨立**：entry（candidate 生成層 applicable）vs `PRIO_SURVIVAL`（task 仲裁層）**非同值**（人格化 entry 不動仲裁優先序）。
3. ★**物理錨分離**：`DESPERATION_DAYS` 作 **need-anchor**（買糧量/relief target=DESPERATION×pop×0.8）**physical 留 raw**、只 entry-gate 讀 `desperation_entry_threshold`。

## 守 + ★★F0 fp 預期分化（行為變 slice）
- genuine（膽/懼/慎重 modulate entry=真風險容忍、**禁調逼 fire 率**、乙教訓）。
- ★**行為變 slice**：跑 `state_fingerprint_bed` 對 baseline、**fp diff=膽大隊撐更久/謹慎早進 entry 分化=intended 記錄**（非 regression、與②結構 fp-byte-identical 分不混）；determinism 仍 3-run byte-identical。
- constitution 綠（soft weight 去硬 gate 可能減 threshold site=OK、entry 統一點無新散閘）/ headless 無新錯 / 既有 arc 回歸綠。
- 完成 → handback `to:systems`（R²、merge-gate 核統一計算點 + entry-仲裁分離 + 物理錨留 + genuine 非 crank + fp 分化 intended）→ measurer 量（靶A entry 分化 膽大撐久/謹慎早進 + 物理錨不變 + 靶B greed 連續無懸崖）→ QA → merge = F1 收（硬綠①推進）→ F2。地基 KEEP。
