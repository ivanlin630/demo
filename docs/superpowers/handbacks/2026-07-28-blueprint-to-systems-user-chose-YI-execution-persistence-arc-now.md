---
from: blueprint
to: systems
status: consumed
topic: "[★用戶裁乙=執行層持守(真手統一、修A1)本arc現在做·spec已更新R①-corrected scope段(supersede過度宣稱)·本arc=執行層持守(重寫try_set仲裁成持守-aware=真架構build如means-end新子系統非收既有)+決策層bonus-collapse(早期slice)·危機axis排除/progressive-only/重掃機制/當真build切slice/whole-system-first·背水一戰保住·你proceed:重掃現有機制→HOW架構spec(執行層持守-aware仲裁+決策層bonus)→plan/slice→R²·規模認知已誠實(真build非收既有)R①該過·A1由此arc執行slice收] 用戶裁乙:執行層持守現在做(不另開後)。spec已更新(頂端加R①-corrected scope段supersede §4過度宣稱)。★本arc定案scope:①危機/急迫axis排除(PRIO階層/combat_lock/emergency_guard/CRISIS_FLOOR留原樣、反持守不收);背水一戰保住(危機axis+人格)。②progressive-only(build/upgrade/campaign有進度;FLEE+一次性timeout排除走既有);寫HOW前重掃現有機制(27不齊)。③★執行層持守=真架構build(重寫try_set從整數tier成持守-aware+COMMITMENT_BONUS寫回+~29 call site+新鮮度)=如means-end是新子系統、非『收既有27』——規模認知已誠實上修(R①該過了,不再假『免費收既有』)。決策層bonus家族(COMMITMENT/SOLO/FOUND+survival_committed_stall~5-6→沉沒+人格加權)=早期slice。★你proceed:重掃現有機制(補齊27)→HOW架構spec(執行層持守-aware仲裁+決策層bonus-collapse,規模當真build)→plan/slice→R²每slice→implementer。whole-system-first(整個建完當whole才measure)。★A1手不聽腦由本arc執行層slice收(用戶原concern現在做)。★過度宣稱是我WHAT的錯、R①事前接住=紀律進步(這次抓我)。material PARK。決策層slice你本可proceed、現在執行層也in-scope一起規劃。R①若再翻某塊回報我WHAT我調;純HOW/slice你自主。go。"
---

# ★用戶裁乙：執行層持守（真手統一、修 A1）本 arc 現在做

## 定案
- **用戶裁乙**：執行層持守**現在做**（不另開後續 arc）。
- **spec 已更新**：頂端加「★★R①-corrected scope」段，supersede §4 過度宣稱。

## 本 arc 定案 scope
1. **危機/急迫 axis 排除**：PRIO 階層 / combat_lock / emergency_guard / CRISIS_FLOOR **留原樣**（反持守/守命、不收）。**背水一戰保住**（危機 axis + 人格）。
2. **progressive-only**：sunk-cost/前瞻只套有進度/終點動作（build/upgrade/campaign）；FLEE + 一次性 timeout 排除、走既有。**寫 HOW 前重掃現有機制**（27 不齊）。
3. **★執行層持守 = 真架構 build**：重寫 `try_set`（整數 tier → 持守-aware）+ `COMMITMENT_BONUS` 寫回 + ~29 call site + 新鮮度 = **如 means-end 是新子系統、非「收既有 27」**。**規模認知已誠實上修**（R① 該過了，不再假「免費收既有」）。**決策層 bonus 家族**（COMMITMENT/SOLO/FOUND + survival_committed_stall ~5-6 → 沉沒+人格加權）= **早期 slice**。

## 你 proceed
重掃現有機制（補齊 27）→ HOW 架構 spec（執行層持守-aware 仲裁 + 決策層 bonus-collapse，規模當真 build）→ plan/slice → R² 每 slice → implementer。**whole-system-first**。

## A1
手不聽腦由本 arc **執行層 slice 收**（用戶原 concern 現在做）。

## 邊界
過度宣稱是我 WHAT 的錯、R① 事前接住 = 紀律進步（這次抓我）。R① 若再翻某塊 → 回報我 WHAT 我調；純 HOW/slice 你自主。material PARK。**go。**

## 溯源
用戶「乙」；spec `2026-07-26-persistence-unification-design.md`（已加 R①-corrected scope 段）；`2026-07-28-...-R1-CONTRADICTION`（已 consumed+回）。
