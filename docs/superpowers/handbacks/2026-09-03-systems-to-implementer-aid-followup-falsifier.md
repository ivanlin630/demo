---
from: systems
to: implementer
status: open
slice: 施主濾網逐條計數 —— 追一格（★可證偽的那一格）
topic: ★接前一封（two-counts）,原票不變、這封只【加一格】;★★blueprint 已預置修向(量到才生效),而那個修向建立在一條假說上:「通過的施主壓倒性是【互動過的老熟人】」——★★★所以我要一格能【打死它】的:通過②的 target 集合大小 vs 通過③的集合大小;若③的集合裡有一堆從沒互動過的隊 ⇒ 假說當場死,別讓它靠「數字看起來合理」過關
---

# ★①前一封不變
`#10 第一格只問哪個 `applicable` 條件擋、禁猜` ＋ `_find_aid_target` 五道濾網逐道拒絕次數 —— **照原票跑。**
★blueprint 已回：**母體是 `team_discovered` ＋ `PathSystem` 可達 ⇒ 他原本「只認鄰居」的擔心【他自己撤了】**，那一格不用查了。

# ★★②只加一格 —— **能打死假說的那一格**
我上一封的懷疑是：③`bel.has("food_est")` 在擋，理由是 `food_est` 只在 `interaction_system.gd:1067`（互動）寫入。
★**而那條理由若成立，它會做出一個【可證偽的預測】**：
```
★★★通過③的 target，應該【壓倒性】是曾經互動過的隊
⇒ 要的數字（同一輪，很便宜）：
   ②`has_belief` 通過的【不同 target team_id 集合大小】
   ③`bel.has("food_est")` 通過的【不同 target team_id 集合大小】
   ★兩個都要【集合大小】，不是次數 ——★★次數會被「同一隊被看很多次」灌水
```
★**判讀先講死（免得數字回來以後才挑解釋）**：
| 結果 | 讀法 |
|---|---|
| ③集合 ≪ ②集合，且③集合小到個位數 | ★**支持假說**：認得一堆人，但只「知道存糧」少數幾個 |
| ③集合 ≈ ②集合 | ★★**假說死** ⇒ `food_est` 不稀有，擋人的是④⑤，別再往資訊層修 |
| ②集合本身就 ≈ 0 | ★★★**兩個假說都不成立**，真根在更上游（**根本沒發現別人**）⇒ 回報就停，不要往下推 |

# ★★★③一件【不要做】的事
blueprint 已經預置了修向（**施主候選降到外觀層富態信號**，例如據點等級／糧倉外觀＝親見可得）。
★**但那條修向【尚未生效】——它明寫「量到 `food_est` 真擋才生效」。**
⇒ ★★**這輪你不要動任何 production 判準**，純觀測。
★★★**知道結論長什麼樣，是這輪最大的污染源** —— **量出來的東西要能反對它。**

# ④其餘照舊
`fp` 逐位元不變／母體與命中同印／分帶（**最深帶 `food_days`→0 單獨看**）／命中 0 照三讀法。
