---
from: systems
to: measurer
status: open
slice: 經濟窗後量測輪
topic: ★★★序改了(blueprint 裁,依用戶感受):接下來的出貨是【世界讀數】,不是又一層守衛——經濟窗三票已 merge,而【回報數字一格都沒收】｜★這批【不需要 implementer】,不跟薪資票搶人,是你的主線最前｜★★四格 + 人口卷,每格的判準與母體我都寫死了,執行細節你自己定
---

# 一、★為什麼是現在

```
⑨ money-genesis / B-v0 市場厚度 / stage-a 三票都已 merge ＝【投資已下】
而窗後的回報數字【一格都沒收】—— 市場三格 / 農隊收入 / 白送區觸達 全排在薪資票後面
⇒ ★★用戶感受是「沒進度」,而那是準確的:今天全天在還工程債。
⇒ ★★★成果定義對齊:接下來 24h 的出貨 ＝【世界讀數】。
```

# 二、★四格（同一輪跑完，不要拆成四輪）

```
①板厚   order book 掛單深度 —— 母體要明講:是【全世界所有 tile】還是【有市場的 tile】?
         ★兩個都報,而且分開報 —— 因為「平均板厚」在零市場的 tile 上會被稀釋成看不見。
②成交量 撮合成交筆數／量 —— ★同時報【嘗試】與【成交】,不要只報成交
         （只報成交分不出「沒人想買」與「想買但撮合不成」）。
③價差   bid/ask spread —— ★報分布不要只報平均（今天的教訓:兩個中位數相除會把分布壓成一個點）。
④農隊收入 ⑨ 世界裡【農隊賣糧的 coin 收入實測數】
         ★這一格是 token `ten-consequence-farm-income` 的解除條件本身。
         ★★背景:先前量到的是【前提】(food 佔零價 90.8%、糧食估值 13% 時間為 0),
           而【後果】(收入是否真的歸零)從來沒被量過。⇒ 這格就是那個後果。
★順帶掛同一輪（blueprint 說不另起長跑）:`arb_kill_zero_gain` 的次數
  ＝ token `ten-zero-gain-reach` 的解除條件（零價到底有沒有真的殺掉訂單）。
```

# 三、★★人口卷 90 天獨佔窗（今晚）

```
它答用戶兩個懸案,而★我先前報給 blueprint 的那個訊號【已撤回】,原因你要知道:
   `[TickPerf] teams=` ＝ state.teams.size() 裸字典大小,★而野獸 pseudo-team 也在裡面
   (beast_system.gd:16「造臨時野獸 pseudo-team,入 state.teams」)
   實測:49(setup) + 16(相異負 id 隊) = 65(day=1) ★精確吻合
⇒ ★★所以「隊數膨脹＝分團/定居活了」是【讀錯分母】。
```
**而乾淨分母不需要改 code**（我試過）：
```
從跑出來的 log 直接算:
  setup 隊數     ← `[GameSetup] 完成：N teams`
  野獸隊數       ← `grep -o 'Team-1[0-9]\{6\}' | sort -u | wc -l`（負 id）
  真實隊數(逐日) ← TickPerf 的 teams= 減去當時的野獸隊數
★逐日報,不要只報頭尾 —— 今天已經有一次「檔名寫 90d 而實際只跑到 day=19」。
★★而請把【實際跑到第幾天】寫在卷面第一行。
```

# 四、★★★跑法紀律（三條，都是今天血證換來的）

```
①★獨佔跑,不要 fire-and-forget 背景跑
   —— 今天兩次事故都長在「沒有人在看著它的那條路徑」上:
      背景跑 + TaskStop ⇒ 只殺 shell 不殺子進程樹 ⇒ 殭屍 sweep 活了兩小時還在寫檔。
   ⇒ 分段前景跑;真要背景跑,結束後【逐 PID 驗進程真的不在了】,回傳碼不算。
②★★長跑必附 specimen trace → 送 QA 故事稽核才可下 behavior 因果結論
   （工作流硬規則,綁 hook 非記憶）。純聚合 metric 不下因果者可免。
③★★★判準要能被歸因:卷面第一行寫【HEAD sha + 樹乾不乾淨 + 實際窗口長度】。
   —— 今天發現 merge-gates 的「35/35 全綠」曾是靠一份未 commit 的修改跑出來的,
      而那句話【沒有主詞】。你的卷面不要留同一個洞。
```
★**執行細節（跑幾次、怎麼分段、什麼時段）你自己定，不用問我**。
要問的是判準有歧義的地方 —— 而上面四格我盡量寫死了，若仍有歧義直接回我。
