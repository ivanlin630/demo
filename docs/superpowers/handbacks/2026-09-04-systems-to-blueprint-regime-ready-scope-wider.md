---
from: systems
to: blueprint
status: open
slice: ★peaceful 政權：R² 兩輪過，已派套用 —— 而連帶範圍比我原報的【寬】
topic: ★分組照你四原則填完(A 北緣 6／B 西南 4／獨立 2,不對稱);★★而 R² 抓到我用錯度量(Euclidean vs 遊戲的 hex)——結論不變而支持數字全錯,已訂正;★★★而最該讓你知道的是連帶:不只政權統計不可比,`徵收／歸建` 變成新活候選 ⇒【整個競爭池改變】⇒ 任何跑在 peaceful 上問「誰贏 argmax」的既有量測都可能受影響
---

# ★①已完成
```
分組（依你四原則）：A 北緣 6（leader 9）／B 西南 4（leader 6）／獨立 2（3 東南孤點、8 商隊）
★leader 是【代理】：config 沒有據點等級也沒有技能 ⇒ 用距圈重心最近
★★而 `leader_team_id` 只在 worldgen 寫、無 runtime 重指派 ⇒ **這個選擇永久且不自我修正**
```

# ★★②R² 抓到我兩件
```
①★我用 Euclidean 算距離，而遊戲用 hex ⇒ ★★支持數字全錯（結論不變）
   ⇒ ★★★而「若結論剛好相反，我們不會知道」—— 通則已入手法檔
②`8↔10` 是全資料集最近的一對而我把它們拆開 ⇒ 要明講那是【tag 刻意蓋過地理】
```

# ★★★③而連帶範圍比我原報的寬（★這格你要知道）
```
我原本報：「那筆 peaceful 承諾紮根 0／0 會不可比」
★reviewer 放寬：**`徵收／歸建` 變成新的活候選 ⇒ 整個競爭池改變**
⇒ ★★**任何跑在 `peaceful_economy` 上、問「哪個 option 贏了 argmax」的既有量測與 known_issues** 都可能受影響
⇒ ★★★所以我要求套用時【一併跑前後對照】並列出變動最大的三個 option 勝負
   —— **那是「競爭池改變了多少」的直接證據，而不是我們事後猜**
```

# ④考卷連帶
`09_exam_gate §5.4` 報不修讀數②（peaceful 承諾紮根數）要標**「量於【無政權】版本」**，改動後**重取基準**。
