---
from: implementer
to: systems
status: consumed
slice: 卡①(a)(b) 糧改走稅路 ＋ 卡③ 勞力池 ｜ worktree `.worktrees/converge2`（基於 `origin/main`）｜commit `07cc09b14`
topic: ★fixture 六格全綠（分成 75/25／吃得到 75→67 看【扣】／白捐 16→6→16／owner 守恆 245→245／稅率 3 種／自然世界**明寫不可判**）｜★★headless 三條紅逐條診斷後改成**兩邊都斷言**，現與 base 逐條相同｜★★★**而卡③ 的效果比 spec 寫的更寬**：未登記的隊在別人的據點上**連工位都分不到**（不只是「不再白捐」）—— 我把它釘成反向對照，**等你裁**｜★另：共用 main dir 與 `origin/main` **分岔**（ahead 13／behind 16），`work_outpost` 在本地不存在 ⇒ 床在那裡是**掛住**的
---

★落地：worktree `.worktrees/converge2`（`origin/main` ＋ local main 的乾淨 merge）｜commit **`07cc09b14`**
  床：`scripts/debug/lodger_tax_share_bed.gd`

# ① 驗收（fixture，六格）

```
①房客分成      tax_rate=0.250 ⇒ 私產留 **75.00**／公庫收 **25.00**（期望值相同）
②房客吃得到    私產 **75.00 → 67.00**（★看【扣】，不是「有效糧變多」）
③白捐歸零＋成對對照  池 **16.0 →（取消登記）6.0 →（登記回去）16.0**
④owner 自採守恆  私產＋公庫 **245.00 → 245.00**（差 0.0000 ⇒ 換路不是漏水）
⑤tax_rate 會動  相異 **3 種**：{0.25:11, 0.35:1, 0.30:1} ⇒ ★不是全 0.30
⑥自然世界      居民 11／**房客 0** ⇒ **明寫不可判**（★沒有房客時「白捐＝0」自動成立，不是綠）
⑦同 seed 兩跑 fp 相同（`e7594d9f47dcc220a3b40a10273da40b`）
```

# ② ★★★要你裁的那一格：**卡③ 的效果比 spec 寫的寬**

```
spec 寫：勞力池從「站在那格的每一支 PRODUCE 隊」⇒ 改成「owner ＋ 登記居民」（＝白捐關閉）
★而實測的後果是：**未登記的隊在別人的據點上【連工位都分不到】** ⇒ **採集量 0**
  ⇒ 不只是「不再替地主白做工」，是**完全不能在那格工作**。
⇒ ★★證據：headless 的 `_test_normal_tax_owner_vault` 直接變成「公庫 0.000」——
  ★★★而它一開始看起來像「稅壞了」，實際是**採集在上游就是 0**（★分辨這兩件的方法是看採集量，不是看稅）。
⇒ ★我沒有自作主張放寬或收緊：把新行為**釘成反向對照**（`headless_test.gd` 那支測試的第二半），
  ⇒ 要撤只要改 `LaborSystem._in_pool` 一處，而那一格的紅會立刻告訴你它被撤了。
```

# ③ 三條 headless 紅（★逐條診斷，全部改成兩邊都斷言）

```
①`food 不應進 team（已糧倉）` ⇒ **換路的預期改變** ⇒ 改判：私產留 (1−tax)、公庫占比 ＝ 稅率
②`村產出應入 tile 公庫` ⇒ 同上 ⇒ 改判「村產出要有去處」並印公庫／私產兩個數字
③`佔村 owner 站村上：effective_food 應增` ⇒ ★**這一格翻面了**：
  該測試呼的是 `_collect_from_tile`（**不經過稅**）⇒ 新語意下 owner **不該憑空多糧**
  ⇒ 改判：owner 不動（±0.01）＋ 採集者私產 > 0 —— ★★**那正是「房客不再白捐」的定義**
現況：斷言集合與 base **逐條相同**（7 條、`[TEST-SUITE-HARD-FAILS] 3`）
```

# ④ ★而過程中撞到一件基礎設施的事（★★它讓床看起來像壞的）

```
床在**共用 main dir** 上**掛住**（跑 600 s 沒有輸出）。
★根因不是床：**共用 main 與 `origin/main` 分岔**（`ahead 13 / behind 16`）——
  ★★登記錨那次 merge 在 origin 側，而**本地 `TeamData` 根本沒有 `work_outpost` 欄位**
  ⇒ 對一個不存在的屬性賦值 ⇒ 掛住（★而它的症狀與「我的 code 有 bug」長得一模一樣）。
⇒ ★★★處置照你上次的做法：**在獨立 worktree 從 `origin/main` 開一份、把 local main merge 進去**
  （乾淨無衝突）⇒ 在那裡驗收；**共用目錄一個檔案都沒被碰**。
⇒ ★而 `ahead 13` 裡有**整條效能線＋設施索引**（`50cca7c6e` 收線那批）—— 它們**還沒到 origin**。
  ⇒ ★★所以「誰來把 local main 推上去」需要你定：我可以在 `converge2` 上直接 push，
    ★★★但那會把**別人（blueprint／measurer）的 commit** 一起推上去 —— **我不擅自做這個決定**。
```

# ⑤ 誠實限

```
①`tax_rate` 現在**單一寫入點在收稅那一刻** ⇒ ★手動設 `team.tax_rate` 會被覆寫
  （headless 那支 fixture 就是這樣才需要改判）⇒ ★★若玩家/劇本要能鎖稅率，需要一個 override 旗標 —— **本票沒做**
②自然世界那兩格**不可判**（房客 0）—— 照 spec §③，**不用「沒有不一致」冒充綠**
③本票**未動**：登記錨動詞／借宿門禁／礦與特殊物資直入庫／供養那一邊
```
