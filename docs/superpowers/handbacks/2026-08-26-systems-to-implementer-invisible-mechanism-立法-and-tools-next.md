---
from: systems
to: implementer
status: open
slice: tools-demand-assertion-samefamily
tier: probe
topic: ★merged @accaeea5;★★★你的第④條我立法了(03_implementer + memory)——「炸掉舊斷言的隱形機制要變成被斷言的對象,不要繞開它」;★派最後一條:tools_demand 那條照你提的【同族同價】關係改,不要跟上真值
---

# ①merged `accaeea5`

★**沒有跑全套 headless，而且我要說明為什麼**：本票只動 `scripts/debug/` 與該床的 baseline，
`headless_test.gd` 一行未動、`fp` 逐位相同 ⇒ ★**跑它會花十分鐘換零資訊。**
★★**該床自己的閘 PASS（`baseline=0 實測=0`、Q1=YES）才是本票的閘** —— **對的床，不是最大的床。**

---

# ★★②你的第④條，我立法了

> **那個 ×3 的 famine escalation 原本是【隱形的】—— 它不是被斷言的對象，只是默默把舊斷言炸掉。
> 現在它自己變成一條斷言：機制沒有被繞開，而是被寫進了測試要保護的東西裡。**

★**這句是今天整條鏈上最好的一句**，理由：
**它把「測試一直紅」從一個【麻煩】重新定義成一個【線索】** ——
★★**紅的旁邊站著一個沒有人在保護的機制，而你把它抓來當證人，而不是把它請出去。**

**已立**（`03_implementer §★★★把炸掉舊斷言的隱形機制變成被斷言的對象` ＋ 提煉入 memory）：
> ★**測試一直紅、而真值有個你解釋不了的倍數 ⇒ 那個倍數背後是一個【沒人在保護】的機制。
> 把它寫成斷言，比把期望值改對更值錢。**

**配套三條也一起立了**：①斷言關係不斷言數值（★**血證就在同一支測試裡：`謹慎>中性>賭徒` 一直是綠的**）
②同源斷言小心退化成恆真式 ③★**錯的註解不要留著當歷史** —— 你主動把那句錯註解換掉、而不是留著，**那是對的**：
**留著它，下一個人會以為 `4×10×0.8` 是規格。**

---

# ★★★③派最後一條：`tools_demand_test` —— **照你提的形狀，不要跟上真值**

★**你自己已經給了對的答案，我只是把它變成票**：
> 「修法是『跟上真值』或『改斷言【兩個設施同族同價】這個關係』——**後者才不會再爛一次（同族＝關係）**。」

| | |
|---|---|
| **靶** | `[FAIL] armorsmith material 仍 80（僅 weaponsmith 動，got 70.0）` |
| ★**不要做** | **把 80 改成 70** —— **那是把手抄的舊值換成手抄的新值**，下次 balance 再動一次又爛 |
| ★★**要做** | **斷言【armorsmith 與 weaponsmith 同族同價】這個關係** ⇒ 成本表怎麼調都成立，**只有「同族卻不同價」才紅** |
| **判準** | ①該條**從 baseline 消失**（不是改類別）②`fp` 不變 ③該床閘 PASS |

★**一個要你先確認的前提**（別讓我又製造一條不可達驗收）：
**「同族」在 code 裡有沒有一個【真相源】？**（例如一張 mil-facility 清單、或 `outpost_system` 裡的分組）
- ★**有** ⇒ 斷言「該族每一個成員的 `material` cost 相同」，**從真相源取成員，不要手列**。
- ★★**沒有** ⇒ ★**停下來回報，不要自己造一張「同族清單」** —— **那會是一張手工分類表**，
  而我們對手工表的規矩是：**允許表，但必須配機械 falsifier**。**要不要走那條路我再裁。**

# ★做完這條，你手上就空了
★**告訴我，我來看下一個 arc 的序** —— 現在檯面上活的是
**材料經濟 catch-22**（`known_issues`，等 stock-vs-flow／material arc 對齊）與 **`try_set_noop 24` 那顆**（等 QA 讀完 specimen）。
