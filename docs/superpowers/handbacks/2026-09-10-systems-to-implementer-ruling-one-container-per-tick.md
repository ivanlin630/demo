---
from: systems
to: implementer
status: consumed
slice: 相位樹 ｜ ★裁【兩個容器】—— 把相位帳的生命週期從 `evaluate_all` 提升到 **tick**
topic: ★★★裁：**一個 tick 只該有一本帳**。`_fai_ph` 現在由 `evaluate_all` 開場 clear、結尾 dump ⇒ 錯開票把 `_evaluate_solo` 移出去之後，**solo 的時間掉在帳本外面** —— 而那不是遺漏，是**帳本的邊界畫錯了**｜★★連帶要改 spike 判準（`evaluate_all` 單次 > 100ms ⇒ **tick 總時 > 100ms**）｜★而這會**再一次換母體** ⇒ 這是**最後一次**，且每張表檔頭要印母體定義
---

# ① 裁：容器統一

```
現況：`evaluate_all`（`faction_ai_system.gd:833-848`）開場 `_fai_ph.clear()`、
      結尾判 `total > 100_000` 才印 ⇒ **帳本的生命週期綁在 `evaluate_all` 上**。
★而錯開票把 `_evaluate_solo` 移到 `sim_runner` 的 `tick_solo_think` ⇒ **它在帳本外面**。
⇒ ★★所以 `from_solo_body` 不是「還沒接」，是**它落在另一本帳裡**。
⇒ ★★★裁：**帳本屬於 tick，不屬於 `evaluate_all`** ——
   在 `sim_runner` 的 tick **開始**清、tick **結束**判並印。
   ⇒ 這樣 faction 側與 solo 側**在同一本帳裡**，樹才完整、`self_us` 才有意義。
```

**★而 spike 判準要跟著改**：

```
`total > 100ms` 的主詞從 **`evaluate_all` 單次** 改成 **這個 tick 的總時**
⇒ ★★母體會變大（含 solo 的 tick 也會進來）
⇒ ★★★**這是最後一次換母體** —— 之後要再換必須有裁定，理由見 §③。
```

# ② 你已經做到的（★我驗收的部分）

```
★守衛移到 `rank_scored` 入口 ⇒ 母體 733 → 1096 ＋ 成對對照三格綠 ⇒ **正解形狀**
  （★★而它證明了那條規矩：守衛要守在【母體的邊界】上，不是守在某個呼叫端）。
★`from_subteam` 107 次／6.79 s **已進樹**。
★★★而「榜首沒換人（leader 211.96 s）」這件事**現在還不能當結論** ——
  ⇒ 容器統一之後，solo 那一整塊會進來 ⇒ ★**第一名可能再換一次**，
    而那正是做 §① 的理由。
```

# ③ ★★★而我要立一條，因為母體今天已經換了三次

```
v2 → v3 → v4，樣本 396 → 196 → 675，而每一次「不可逐格相比」都是**事後才講**。
⇒ ★規矩：**每一張相位表的檔頭必須印【母體定義 ＋ 樣本數】**，逐字寫出：
   「母體＝<哪一個容器> 的單次總時 > <門檻> 的 tick／樣本 N ＝ <數字>」
⇒ ★★而**跨表比較的前提是母體定義逐字相同** —— 不同就**不可逐格相比**，
   ★★★而那句話要印在**表上**，不是靠信裡提醒（★今天已經有兩次是靠人記得）。
```

# ④ 驗收

```
①★容器統一後：`from_solo_body` **出現在同一張表上**（★★母體地板：它的次數 > 0）
②★★守恆：新母體下，`rank_scored` 四個 src 的次數相加 ＝ 該窗 `rank_scored` 總呼叫次數
  ⇒ ★`unknown` **必須是 0**（★★守衛已經在入口，若還有 unknown ⇒ 守衛沒接上）
③★★★重排一次榜，並**明寫它與 v4 不可逐格相比**（母體變了）
④fp 不變（★純儀器；依界限第十八條附一格行為證據）
⑤★spike 門檻改動後，**印出這一窗有幾個 tick 進了母體**（★★否則「樣本 675」這個數字沒有分母）
```

# ⑤ 不做

```
①★不趁機改 `100ms` 這個門檻的值（★★改值＝再換一次母體，而 §③ 剛立完）
②不動任何相位的實作（本票仍然是**讓表可以排序**，不是優化）
③★不把 solo 的舊表與新表相加（**兩個容器的數字永遠不可相加**，即使統一之後也不能回頭加）
```
