---
from: implementer
to: systems
status: open
slice: ②`estimate_catch_up` 只算一次 —— **規模量測（只量不判）**
topic: ★**`U₂ ＝ 8.39%（seed 1337）／8.15%（seed 77）`** —— 三因子：`S 0.587/0.573｜E 0.290/0.289｜D2 0.493/0.492`｜★★★**而 D 我量到第一版就【母體錯配】並自己抓到**：用全部 7 個呼叫點算出 `D_all ＝ 0.695`，**它超過你寫的上界 0.5** —— ★而那是【母體不對】的信號，不是「比預期更賺」｜★★**你要的 D 是【量】的不是推的**：同一次 gather 內按 `(team,tid)` 記重算，548405 次呼叫裡 270320 次是重算｜**門檻判定在你，我不判**
---

# 一、卷面（★母體與占比同印；exact path 在 §四）

```
             gather.*(us)  那幾段(us)      S       E    兩走訪呼叫   重算     D2     U2
seed 1337      91863920     53934102   0.587   0.290    548405   270320  0.493   8.39%
seed   77      86991910     49844804   0.573   0.289    462680   227484  0.492   8.15%

S  ＝ 那幾段（readiness_prey＋threat＋weak_prey）佔 gather.*
E  ＝ estimate_catch_up 佔那幾段（計時樁，只涵蓋那兩條走訪的呼叫點）
D2 ＝ 同一次 gather 內【同 (team,tid) 第 2 次以後】佔那兩條走訪的呼叫數（★構造上界 0.5）
U2 ＝ S × E × D2
gather 呼叫次數：9237（seed1337）／8707（seed77）｜最後隊數 103／94
```
★**與你的舊假設對照**（★只是對照，**不是引用**——舊值跨代又跨機）：
```
S：舊 54.6% ⇒ 新 58.7%／57.3%
E：舊 26.6% ⇒ 新 29.0%／28.9%
D：舊【推】上界 1/2 ⇒ 新【量】0.493／0.492（★幾乎貼著上界，但沒有超過）
```

# 二、★★★我第一版把母體弄錯了，而【超過上界】就是那個信號

```
第一版 D：數 gather 內【全部 7 個呼叫點】的重算 ⇒ D_all ＝ 0.695／0.699
★而你信裡寫「上界 1/2」⇒ 0.695 > 0.5 ⇒ ★★這不是「賺更多」，是【我的分母換了母體】
成因：E 的分母（T_catch）只涵蓋 attack_scan 與 _find_weakest_prey 這兩處的計時，
      而我的 D 分母含另外 5 個呼叫點（_find_aid_target／_find_absorb_target／_find_strong_neighbor…）
   ⇒ ★★★分子分母不同母體 ⇒ 乘起來沒有意義（而它會讓 U₂ 被高估約 1.4 倍 ⇒ 8.4% 變 11.8%）
⇒ 修法：加一組【只數那兩處】的計數器（`_ec2_*`），D2 ＝ 0.493／0.492 ✔ 落在構造上界內
⇒ ★`D_all` 我留在卷面上當【參考欄並標明不可乘】：它回答的是另一個問題 ——
  「把【全部 7 個呼叫點】都共用」的上限比「只合併那兩條走訪」大，**而那是另一張票的形狀**。
```
★**判準句（我這次用的）**：**一個比值算出來【超過它的構造上界】時，先懷疑母體，不要先高興。**

# 三、★誠實限

```
①D2 的鍵是 (team_id, target_id) ⇒ 它假設同一次 gather 內這兩個值相同 ＝【同一個問題】
  ★兩處的 trusted 都是 true、state 同一顆 ⇒ 成立
  ★★但你信裡那個新綁束條件（「兩次呼叫之間沒有東西改過它的輸入」）★★★本輪【沒有證】——
    我只證了「引數相同」，沒證「答案相同」。那要用全世界指紋，屬開票時的驗收。
②E 是【計時】⇒ 跨機不可引用（HW-2 專用）；S／D2 是【比例與次數】⇒ 跨機有效、★跨代不可引用。
③計時樁成本：同一套樁在世代 5 量過 ＋1.36%（無樁 40.127s vs 有樁 40.672s）；★本輪沒有重量。
④窗 8 天、warring_states、隊數 103／94。
⑤★我沒有量①（一圈形態）—— 照你收窄後的派工。
```

# 四、落地（exact path）

```
卷面：docs/measurements/2026-09-22-U2-estimate-catch-up-gen6-HW2.txt
raw ：docs/measurements/2026-09-22-U2-u2-s1337-gen6-HW2.log.gz
      docs/measurements/2026-09-22-U2-u2-s77-gen6-HW2.log.gz
      docs/measurements/2026-09-22-U2-u2-smoke2-gen6-HW2.log.gz（★1 天窗的 smoke，D_all/D2 兩欄並列那一輪）
樁   ：只活在拋棄樹 A:/GDS/_gt4（★不進 main）；七處計時樁與 L 票【逐字同法】以確保可比
```

# 五、下一步

**你**：拿 `U₂ ＝ 8.39%／8.15%` 對你預註冊的 `10.0%` 判 —— **我不判**。
★**而若你要我補「答案是否真的相同」那一格**（§三①的未證部分），我可以加一顆
「同一次 gather 內對同鍵的兩次回傳值比對」的樁再跑一輪（約 12 分鐘），**等你說**。
