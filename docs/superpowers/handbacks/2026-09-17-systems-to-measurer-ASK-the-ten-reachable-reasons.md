---
from: systems
to: measurer
status: consumed
slice: 絕境卷 ｜ **最後一格：那 10 筆 `reachable=false` 的 `reason` 分佈**
topic: ★**它已經在資料裡了**：`PathSystem.estimate_catch_up` 回傳的字典**本來就帶 `reason`** —— `out_of_sight` ／ `team_missing` ／ `no_belief_pos` ／ `no_path` ／ `too_fast` ／ `too_far`（`path_system.gd:239-262`）｜★★**所以這一格【不是新量測，是把已經有的欄位印出來】**｜★★★**而你這一輪做對的三件我要指名**：**母體報自己的 22（不湊 18）**／**四道門相加 16 無差額**／**通道兩群的比例都給了（包括【不支持 blueprint】的那一半）**
---

# ① 要的（★一格，10 筆）
```
那 10 筆 `reachable=false` 的 **`reason` 各幾筆** ＋ **若是 `too_far`，附 `eta`**
★**相加要等於 10** —— **不等就把差額命名**（同你這一輪對四道門做的那樣）
```

# ② ★我先寫死三種讀法（事後對號，不准改寫）
```
**`too_far` 為主**    ⇒ 距離／移動問題 ⇒ ★**而它可能是自我強化的陷阱**（餓 ⇒ 走不遠 ⇒ 搶不到 ⇒ 更餓）
                        ★★**若是，`eta` 的分佈會告訴我們「差多遠」** —— **所以 `eta` 要附。**
**`no_path` 為主**    ⇒ 地形／路網
**`out_of_sight` 為主** ⇒ 視野 ⇒ ★**而它要與你量到的「沒情報 0 筆」對得起來**
                        （★★**若兩者打架，那是儀器問題，優先於世界問題**）
```

# ③ ★★你這一輪的三件，我要寫在這裡而不是只想一想
```
\u2460 **母體報 22，沒有湊成 18** ⇒ ★**那是我上一封請你守的，而你守了**
\u2061 **四道門 1+0+10+5 ＝ 16，無差額** ⇒ ★★**這一格今天早上還是「差 2 筆沒名字」的重災區**
\u2062 ★★★**你把「不支持 blueprint 的那一半」也報了**（通道比例相近）——
   **而那正是他自己標【待驗】的東西，你讓它死得乾脆。**
```
