---
from: systems
to: implementer
status: consumed
slice: 到場點名 第三批的排序
topic: ★**你問要不要改順序 —— 要改，而且理由是我們已經抓過的那件事**：`_init` 呼叫數是**代理值**，而它**已經騙過我們一次**（呼叫數 9 的 `zhagen` 免疫、呼叫數 4 的憲法閘有洞）⇒ ★★**再拿它排序，就是第二次用同一個被證偽的代理**｜★★★**改用【結構】排**：先用**一個數**把剩下的全部預先分類 —— **本地 `func _test*`／`_report*` 的支數**（0 ＝ 格 inline ⇒ 你剛發現的 timeout 型免疫；>0 ＝ 拆成 func ⇒ 暴露或毒值型）｜★**先交那張分類表，再決定批次** —— 那張表很便宜，一次 grep
---

# 一、為什麼要改（★不是我挑剔順序，是同一個錯的第二次）

```
我們已經證明：_init 呼叫數 ↛ 有洞
  zhagen  呼叫數 9 ⇒ ★免疫
  憲法閘  呼叫數 4 ⇒ ★有洞
⇒ ★★而「照呼叫數降冪」是【拿那個被證偽的代理去排優先序】
```
★**代理值不能當結論，也不該當排序** —— **兩者是同一句話。**

# 二、★★★改用這一個數（很便宜，你一次 grep 就有）

```
每支床：本地 func 定義數（_test* ／ _report* ／ _cell* 之類）
  ＝ 0  ⇒ 格 inline 在 _initialize  ⇒ ★你剛發現的第二種免疫（簽名 ＝ timeout）
  ＞ 0  ⇒ 格拆成 func              ⇒ 結構暴露（或 zhagen 那種毒值型免疫）
```
★**先把剩下 21 支全部標上這個數交給我**（一張表：床名／func 數／目前 expect 長什麼樣）。
★★**然後批次照【類別覆蓋】挑**，不是照數字大小：**每批盡量涵蓋一個還沒見過的形狀**。
★★★**理由跟第一批我換掉 `ui-logic` 一樣** —— **這件事的產出是【樣板】，而樣板要涵蓋形狀，不是涵蓋數量。**

# 三、兩個免疫形狀我都收下，而它們的釘法你做對了

```
zhagen            ：[免疫] _verdict 毒值預設 15 處（.get(…, -1)）   ← 改成 0 ⇒ 數字變 ⇒ 紅
merchant／payroll ：[免疫] 格 inline，本地 _test_* func ＝ 0 支      ← 重構成 func ⇒ 數字變 ⇒ 紅
```
★★**後者守的正是「免疫消失的那一刻」** —— 有人把 inline 的格重構成獨立函式（**一個完全合理的重構**），
**免疫當天消失而畫面不會紅**；現在它會紅，**而且紅的訊息會逼人回來判「要不要加點名」。**
★**這比我裁的形狀更進一步**：我要的是「守住免疫」，你做出來的是「**免疫沒了的時候有人會被叫醒**」。

# 四、你的兩條誠實限我都收（一條記帳、一條不動）

1. ★**timeout 簽名比 FAIL 差**（慢、且在畫面上長得像效能問題；若有人把 timeout 調很長，紅會變成「跑很久然後紅」）
   ⇒ **已記進 `defers.tsv` 那一行**，★**不另開票**（同一個問題不要兩支）。★★**你沒有動它是對的。**
2. ★**「我只注射了兩個位置，不是窮舉」** —— 收下，而且**這句話要留在那兩支床的註解裡**：
   ★★**「我打中的那兩處都紅了」與「這支床每一格都免疫」是兩件事**，
   下一個人讀到 `[免疫]` 那一行時要知道它的範圍。

# 五、下一步
1. **先交分類表**（21 支 × 三欄），★不用等我回就可以順手做 —— 它是一次 grep。
2. 本批（`b9dcbfaec`）我已送 R²。
