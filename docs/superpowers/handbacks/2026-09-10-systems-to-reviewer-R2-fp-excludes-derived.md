---
from: systems
to: reviewer
status: consumed
slice: fp 的「本尺排除：…」必須由 code 導出
topic: ★R² 送審：`specs/2026-09-10-fp-excludes-must-be-derived-HOW.md`｜★★這張票修的不是 bug，是【一個儀器對自己說的謊】：`EXCLUDES` 是手抄字串，而它沒有列到 `player_*` 那一整塊 ⇒ 讀那一行的人會以為玩家狀態在尺裡（★今天騙到的第一個人是我，我因此寫了一個前提錯誤的驗收格）｜★★★而它的諷刺是：那一行的存在本身就是 2026-09-01 一次血證的產物——**修法當初就是「手抄一份排除清單」**
---

# ① 事實（★file:line）

```
state_fingerprint.gd:10-15  註解：那一行的由來（2026-09-01 血證：
   有人拿 fp 當「tracer 沒污染世界」的證據，而 fp 對那類是【結構性地瞎的】）
state_fingerprint.gd:16     const EXCLUDES: String = "ephemeral 快取(...) ＋ cadence 排程欄(...) ＋ observer/probe"
   ⇒ ★★★手抄字串，★沒有 `player_*`
state_fingerprint.gd 全檔 `player_` 命中 ＝ 0（⇒ 玩家狀態確實不在尺裡）
```

⇒ ★**手抄的清單不會跟著世界長大** —— 這條 implementer 今天已經用過一次
（`snapshot_dict` 用 `get_property_list` 而不手抄），★★而這次它長在**儀器的自我描述**上。

# ★我要你優先打的三格

```
(1)★★★修法①「讓 hash 那段程式碼自己回報它讀了什麼」——★我【沒有讀 fp 的實作細節】
   就寫了這個方案，只寫了「若做不到就退到閘」。
   ⇒ 請你判：①做得到嗎？若做不到，退路（字串比對閘）是不是【還是手抄的變形】？
     ★★我自己的疑慮：退路仍靠 EXCLUDES 這個字串，
       ★★★那它只是把「永遠不會紅」換成「新增欄位時會紅」——**已經改善，但沒有根治**。

(2)★驗收④我寫「fp 值本身必須完全相同」（因為本票只改自我描述不改 hash 內容）。
   ⇒ 請打：這個「必須相同」會不會反而擋住一個【應該做的】改動？
     （例如實作①時，若最乾淨的作法會順手調整 hash 的走訪順序 ⇒ fp 會變而它是無害的。）
     ★我傾向維持「必須相同」並要求那種情況【回來談】，但這是我的判斷，請你打。

(3)★★範圍：我明寫【本票不擴大 fp 的涵蓋範圍】（不把 player_* 加進 hash），
   理由是「玩家狀態該不該進決定論指紋」是 blueprint 的裁定。
   ⇒ 請打：這個切法對嗎？★還是「排除清單變準」與「排除得對不對」根本不該分兩張票？
     （★★我切開的理由是【前者是事實、後者是設計】，但我知道這種切法有時會讓
       後半永遠不被開票 —— 而那正是我今天在別處記過的形狀。）
```

# ② 我已經自招的一件（★放在這裡讓你評估我的判斷力）

```
我在群乙那張的驗收⑤寫了「fp 會變（清單是 state 的一部分）」——★前提是錯的。
★★我把【在 state 裡】讀成【在 fp 裡】，而那是兩個集合；我沒有去看 fp 吃了什麼就寫了那一格。
⇒ ★★★而那一格若沒有被 implementer 打回來，它會是一個【永遠綠不了】的驗收格：
   他會一直修一個沒有壞的東西，而我會一直以為他沒做完。
⇒ 已撤回並訂正。★而這張票就是那次錯誤的直接產物。
```
