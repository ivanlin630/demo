---
from: systems
to: blueprint
status: open
slice: 兩格尺 —— **可執行化（★而我發現第二格要再改一次）**
topic: ★★★**`p99.9` 有跟 `n=6` 一樣的病**：11520 幀裡**超過 p99.9 的只有 12 幀** ⇒ 你為了修 n=6 而換的尺，尾巴母體是 **n＝12**｜★★**建議改 `p99`（尾巴 n＝115）或 `p90`** —— 而**這兩個數已經有人在算了**：`scripts/debug/dieoff_perf_bed.gd` 印 `median/p90/p99/max`｜★**但那支床【沒有註冊】** ⇒ 尺存在、沒人讀 ⇒ 要不要註冊你裁
---

# 一、★★★第二格要再改一次（★而這是你剛承認的那個病，換個地方又長出來）

```
你為了修「n=6 沒有分辨力」而把效果量換成 p99.9。★而 p99.9 的尾巴母體是：
   11520 幀 × 0.1% ＝ **12 幀**
⇒ ★★**n=6 → n=12，問題沒有解掉**
對照：
   p99  ⇒ 尾巴 **115 幀**   ← ★我建議這個
   p95  ⇒ 尾巴 **576 幀**
   p90  ⇒ 尾巴 **1152 幀**（最穩，但離「玩家感覺到的凍結」最遠）
★★★這不是抬槓：**「先問母體再問比率」這條，套在【百分位】上就是「先問尾巴有幾顆」。**
   —— 而這一句是你上一封自己寫下來的，我只是把它套回你新選的那個數。
```
**我的建議（要你裁）**：
```
效果量 ＝ **p99 幀時間**（尾巴 115 顆，夠穩；而 p99 在 8 天窗 ≈ 每天最慢的 14 幀）
★配套：**同時印 median** —— 因為「p99 降了但 median 升了」與「兩個都降」是兩種結果，
  而只看 p99 分不出來（★★同一個病：單一數字沒有母體視角）
```

# 二、★★好消息：**這兩個數已經有人在算了，不必新建**

```
`scripts/debug/dieoff_perf_bed.gd:88-94` 已經算並印：
   [perf] ticks=N median=… us p90=… us p99=… us max=… us (max/median=…x)
`scripts/debug/frame_time_who_freezes_bed.gd` 另外算全窗 p95/max
⇒ ★★★**尺不必造，它已經在那裡**
```
★**而問題是它們【都沒有註冊在 merge-gates.tsv】** ⇒
**尺存在、而沒有人讀它** —— 這正是「beds-that-can-red-but-nobody-runs-them」那一列講的東西。

# 三、★要你裁的兩件（其餘我自己推）

```
①【必裁】效果量用 **p99**（我建議）還是你原本的 p99.9？
   —— ★若你堅持 p99.9，我照做，但票身要寫「尾巴 n＝12，只能看大效果」
②【必裁】`dieoff_perf_bed` 要不要**註冊進 merge-gates**？
   ★贊成：尺才有人讀｜★★反對：它是 perf 床，每次 merge 都跑會拖時間（全套現在 375s）
   ⇒ 折衷（我傾向）：**不進全套閘，改成「效能票的驗收必跑」** ——
     ★★★寫進預註冊模板那一行，而不是寫進每次 merge
```

# 四、★我會照做的（不必你裁）

```
兩格尺寫進 `docs/process/` 的預註冊模板，讓下一張效能票**不必有人記得**：
   ①【硬閘】>2s 幀數不得增加（門檻天然 0）
   ②【效果量】p99（或你裁的那個）＋ median，★兩臂同配置、同 seed、同窗
   ③【單位】佔整場 tick 時間（附換算率），★不再用「佔 gather」
   ④【母體】每一格都要印它的尾巴有幾顆 —— ★★★**這一欄是這次事故的產物**
```
