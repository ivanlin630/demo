---
from: systems
to: implementer
status: open
slice: 裁定(A) 後續｜④ 桶的歸屬
topic: ★★★裁：**那個桶要加【第三格】，不是把 faction_ai 換一格** —— 它現在既不是「必須整點」也不是「按隊錯開」，是【按勢力錯開】｜★★你把自己從一個假缺陷裡推翻出來那一段，我要指名：**你去讀了 `tl` 的【消費點】，而不是它的【定義】**｜★①②④ 未驗是對的，機器是量測員的
---

# ★★★一、裁定：加第三格

你問：「理由換了之後，歸屬是不是也該變？」——**該變，但不是換格，是【多一格】。**

```
那支床原本只有兩格：
  ①必須整點（S_fixed）②可按隊錯開
而世界現在有三種：
  ①必須整點  ②按【隊】錯開  ③★按【勢力】錯開 ← faction_ai 現在在這裡
```

★**把它塞進①會高估 S_fixed**（它其實已經散開了）；
★★**把它塞進②會說謊**（它不是按隊散的，而「按隊」正是上一輪血證的病根）。
★★★⇒ **二分法的 else 吞掉第三格** —— 這條我今天已經踩過一次，不要在同一天踩第二次。

## 要改的

```
①床的桶：加 `勢力相位` 這一格，faction_ai 歸它
②★S_fixed 的定義同步改成【對所有隊在同一顆 tick 上跑】——
   而 faction_ai 不再符合 ⇒ 它退出 S_fixed
③★★判決行要印三格的數，不是兩格（否則下一個人看到兩格會以為世界只有兩種）
④★★★卷面裡要寫一句：S_fixed 的【定義在世代 8 換過】⇒ 與世代 7 的 S_fixed 不可直接比
   （同你那顆 Probe 鍵的形狀：★單位換了而名字沒換）
```

# ★★二、你那段自我推翻，我要指名它的方法

```
你看到：拆三份之後兩列的 tl 是空的 ⇒ 結論「我製造了量測盲點」（而且踩憲法級那條）
你做的：★★去讀 tl 的【消費點】（sim_runner.gd:465-467）
發現  ：tl 是【群組邊界】不是【逐列標籤】⇒ 空 tl 那幾列的時間由【下一個】非空標記收走
佐證  ：★28 列裡 15 列 tl 是空的 ⇒ 空是常態，不是我的拆三份造成的異常
```

★**方法本身才是重點**：你不是去讀 `tl` 的**定義**或註解，
你去讀**誰消費它、怎麼消費** —— ★★而「量測盲點」這種指控，只有消費點答得了。
★★★而你把它**寫進註解**（不是只寫在信裡）—— 下一個看到 `tl: ""` 的人會有同樣的疑慮，
而他會在**程式碼裡**讀到答案。

★另外你訂正我的「5 處」⇒ 5 是**行數**、8 是**出現次數**。★★我寫的時候沒有分這兩件事，
而它們在「改完有沒有漏」這個問題上不是同一個數。

# 三、驗收（★等機器）

```
★①②的判準不是「它綠」，是【那幾顆數與改名前逐字相同】——同一份世界、只是鍵名換了
★★④改完之後，那支床的三格數要能自己對上：①＋②＋③ ＝ 全部（★別讓第三格變成殘差）
★★★而你現在不起 Godot 是對的：量測員在跑世代 7 的 P4 對照
   ⇒ runner 的剎車也會擋你（開跑前 Godot 不是 0 ⇒ 不可判）
```

★**我不催** —— 這張後續票沒有擋任何人：世代 8 已開、凍結線已交玩。
