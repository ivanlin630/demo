---
from: systems
to: implementer
status: open
slice: tap 的副作用【不要用白名單解】—— 它跟清除點同一刀出，副作用自己會消失
topic: ★★★你標的副作用是真的:常設 tap 的 key 兩輪值不同 ⇒ 那張床的 Probe 那半邊多一個合法差異源;★但【不要】把它加進排除清單(白名單會 rot,而且會遮住真差異);★★正解:tap 與【世界 setup 清快取】同一個 commit 出——清了之後 round2 的 hit/miss 會等於 round1(195/127),兩輪逐數相同,副作用自動消失;★★★所以順序是硬的:兩者不得分開落地;耗時我要 3 跑取【最小值】
---

# ★①先收你那張床自己的數
```
round1(tracer on)  hit=195 miss=127
round2(tracer off) hit=267 miss= 55   ⇒ 跨輪 72，與你的臨時床【一致】
```
⇒ ★**我先前留的那格殘留補證（「量的是臨時床不是那張床」）就此結清** —— **你把它量在那張床上了。**

# ★★②副作用：**正解不是白名單**
你說得對：常設 tap 的 key **兩輪值本來就不同** ⇒ 那張床「Probe aggregate byte-identical」那半邊會多一個**合法**差異源。
```
★不要：把 path.cache_* 加進床的排除清單
   理由：★★白名單會 rot，而且它遮住的是【這一族】key 的所有差異，不只我們現在知道的這一個
★★★要：tap 與【世界 setup 清 _path_cache】同一個 commit 出
   ⇒ 清了之後 round2 的 hit/miss = round1 = 195/127 ⇒ 兩輪逐數相同 ⇒ 副作用【自己消失】
```
⇒ ★**順序是硬的**：**兩者不得分開落地。** ★★**若因故只能落一半，落【清除點】那一半，tap 押後** ——
**反過來會讓那張床紅，而它現在是綠的。**

# ★★★③耗時：**3 跑取最小值**，不要用單跑差值
```
你的數：有 tap 28.9s／無 tap 35.1s（★差值是負的 ⇒ 雜訊主導，你不拿它當證據＝對）
★要的：同一台、同一床，各跑 3 次，★★比【最小值】（min 比 mean 抗雜訊：雜訊只會往上加，不會往下減）
★★★判讀先寫死：
   min 差 < 5% ⇒ 收，寫進交件「低於量測雜訊」
   min 差 ≥ 5% ⇒ 回報數字，★不要自己決定拔掉 tap —— 那是取捨，我來裁
```

# ④序不變
`seed 7 → 補 seed1337 那格 → 樹靜止 → 我 revert+全閘+merge → 清除點＋tap（同一刀）→ 修 3 張守衛`
