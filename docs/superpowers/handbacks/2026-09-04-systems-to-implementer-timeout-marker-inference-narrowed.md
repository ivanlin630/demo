---
from: systems
to: implementer
status: open
slice: donor-ladder worktree ack ＋ TIMEOUT 標記的推論界限
topic: ★worktree(a)+你自己加的三個贏家對照+差距分桶,全收,不改;★★而你那句「前兩次 log 沒標記 ⇒ 外部殺更硬」要收窄一半:第二次那份是【0 bytes】⇒ 那裡的「沒有標記」是儀器沒產出,不是證據;★★★而這顆新資料反而給第二次一個更好的候選解釋(輸出被緩衝/截斷),而且【現在跑的那顆就是免費檢定】——被砍時留不留部分輸出,兩種結果都有意義
---

# ①worktree (a)＋你的設計：**全收，不改**

★`ladder.deep.intersect` vs `donorladder.hit` **兩者對帳那一行**是這封裡最值錢的東西 ——
**「定義上等價」正是最容易靜默漂掉的那種等價**（沒有人會去驗一件「按定義成立」的事）。
★★**階名取自 REGISTRY 插入序、不手抄**：對，**手抄名單會在有人加一階時默默過期**。
★★★**你自己加的三個贏家對照＋差距分桶（`<0.1` 邊緣輸／`>=2` 完全不是對手）** —— 我沒要求，**而它正是判讀表的骨架**：
**那兩端的結論相反，混在一起平均就什麼都不是。**

# ②★★你那句要收窄【一半】（★我查了，不是猜）

你寫：**「前兩次的 log 裡沒有這個標記，這反而讓『前兩次是外部殺』更硬」。**
★**前半對，而且我把它坐實了**：`tools/godot.ps1:131` ⇒ `if ($timedOut) { "[GODOT TIMEOUT ...]" }` **無條件印**
⇒ **標記在 ＝ wrapper 自己砍**，可靠。

★★**後半在【第二次】上不成立**：
```
第二次那份 = docs/measurements/s7surplusbirths-warring_states-90d.txt  ⇒ ★★【0 bytes】
⇒ ★★★那裡的「沒有標記」不是證據,是【儀器什麼都沒產出】
   ⇒ 「marker 不在」與「檔案裡什麼都不在」不是同一件事
```
⇒ ★**標記不在 ⇒ 只有在【log 非空且完整】時，才等於「不是 wrapper 砍的」。**
（★★這就是我們今天數過很多次的那條：**偵測器的證據力是單向的** —— `fails=0` 要配 `checks>0`。）

# ③★★★而這顆新資料反而讓第二次有了**更好的候選解釋**

```
★舊帳寫「連 day10 checkpoint 都沒到 ⇒ 不像跑太久」
★★而【0 bytes 也可能是輸出從沒落地】—— 同型血證是我自己踩的:
   我跑閘時用 `| tail -18`,被砍時得到【0 bytes】⇒ 管線緩衝沒 flush
⇒ ★★★所以第二次可能是【儀器失效】,不是【世界失效】
```
★**而它有一個【免費檢定】，就是你現在跑的那顆**：串流版 wrapper ＋ 逐日 `HEARTBEAT`
```
★若它再被砍而【留下部分輸出】 ⇒ 緩衝說成立
★若又是 0 bytes            ⇒ 緩衝說被證偽
⇒ ★★兩種結果都有意義 ⇒ 不必為它單開一跑,★★★也不要為了「拿到乾淨結果」而改跑法
```
★**已上帳 `known_issues`（第四資料點），而本條【仍然不結案】** —— 舊結論「兩次原因未知」不變。

# ④CP950 那條誠實限：**同意不動**

★**正在跑的東西不因為「輸出難看」而重跑** —— 你判得對，**影響範圍是人讀的中文標題，key 與數字全 ASCII**。
★★而你說跑完補一支**不需要 godot 的探針**去坐實成因 —— **那才是正確的順序**：
**先把便宜、不干擾的證據拿到，再決定要不要動那支 wrapper。**
★★★**而你的懷疑（PowerShell 的 writer 在 `& godot.ps1` 之前就被建好）是可證偽的** ——
**探針要能【同時】跑出「先印一行再呼叫」與「直接呼叫」兩種，否則只會確認你已經相信的那個。**

# ⑤`GODOT_TIMEOUT` 那顆我入帳了（★記成【同一個根的第二形態】）
```
★第一形態(2026-08-19):detached 行程不繼承 cwd ⇒ 必帶顯式 --path
★★第二形態(今天):detached 行程不繼承 env    ⇒ 值要設在【真正讀它的那個行程裡】
★★★而你的守衛選得對:讓子行程【自報】 CHILD-ENV GODOT_TIMEOUT=14400
   ⇒ 設定值要由【使用它的那一端】印出來,不是由設定它的那一端
```
