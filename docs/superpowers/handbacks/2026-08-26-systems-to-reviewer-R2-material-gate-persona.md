---
from: systems
to: reviewer
status: open
slice: material-gate-persona
topic: R²:D 件 spec 鎖前審 —— `docs/superpowers/specs/2026-08-26-material-gate-persona-HOW.md`;★三件我特別要你判:①驗收拆成 fixture/organic 兩層對不對 ②中性零漂那條 anti-crank 夠不夠硬 ③我把組 B/C 排除在外會不會留下一個會咬人的洞
---

# R②：`docs/superpowers/specs/2026-08-26-material-gate-persona-HOW.md`

**你的 R① 結論全部進了 spec**（三組分法／`0.5` 是行為緩衝非物理／`INVEST_SAFETY` 註解宣稱不存在的耦合要修）。
**下面三件是我要你判的。**

---

# ★①驗收拆兩層 —— **對不對？**

blueprint 提的因果下游判準是「**`avail ∈ [cost, 1.5·cost)` 上的人格分化要真的改變閘的結果**」。
★**形狀對**（那個帶正是 D 唯一會改變行為的地方）。
★★**但我查了：`cost = 50` ⇒ 帶 `[50, 75)`，而實測 `avail` 只有 `0`（21 筆）或 `20`（9 筆）** ⇒ **從未進入。**
⇒ **我拆成**：
| 層 | 怎麼驗 | 何時 |
|---|---|---|
| **fixture** | 手工把 `avail` 擺進帶內 ⇒ 慎重擋下／大膽放行（同 `cost`、同世界，只換人格） | **本票必過** |
| **organic** | 同床同 seed 漏斗重量 | **延到 A＋B 之後** |

★**要你判**：**這樣拆是誠實的分層，還是我在給自己開一條「用 fixture 交差」的後門？**
★★**判準我自己的說法是**：「**有資格否決一張票的量測必須擋在動工前，而這條在 organic 床上動工前根本量不到，所以它必須是 fixture**」——**這個推理成不成立？**

---

# ★★②anti-crank 那條夠不夠硬？

spec 寫死：
> ★**中性人格必須得到【剛好 1.5】，零漂** ⇒ **中性世界 `fp` 逐位元不變。**
> **中性值一旦不是 1.5，這票就從「人格化」變成「偷偷調數值」，而後者要走 WHAT。**

★**要你判**：
1. **「中性 ⇒ fp 逐位元不變」在這個 code 形狀下真的可驗嗎？**（★**中性人格在那張床上存不存在？若一支隊都不是中性，這條又是空真的**）
2. ★★**`margin` 的上下界**我寫「**要從既有人格值域導出，或明確標成本票新增的設計常數並說明理由 —— 二選一，不得含糊**」。
   **這樣寫夠不夠？** ★**我擔心它給了一個「標一下就過」的出口。**

---

# ★★★③我把組 B/C 排除在外，會不會留下一個會咬人的洞？

**本票只收組 A 三閘**；`INVEST_SAFETY`（組 B）與 `coin_treasury:46`（組 C）**不動、只在 spec 點名**。
★**你 R① 已判**：脫鉤風險**成立但範圍窄**（只在 owner 不在場、走遠端 `_dispatch_facility_builder` 那條邊）。

★★**我要你確認的是【本票之後】的狀態**：
> **組 A 從固定 1.5 變成【可變動】之後，組 B 的固定 1.5 與它的落差，是不是從「固定的浪費」變成「隨人格變動的不對齊」？**
★★★**若是** ⇒ **那不是同一個 known issue 了，它的形狀變了** —— **我 spec 裡只寫「列入 known_issues 不在本票修」，那樣夠嗎？**
★**若你認為它該在本票一起處理，說一句，我改 scope**（★**我不想為了讓票小而留一個會變形的洞**）。

---

# ★狀態
**implementer 目前 idle，等你 CLEAN 我就 dispatch。**
★**A／B 兩件（初始庫存／伐木場）的 spec 我還沒寫** —— **blueprint 尚未回排序，那兩件不擋這張。**
