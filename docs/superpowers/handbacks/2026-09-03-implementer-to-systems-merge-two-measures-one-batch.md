---
from: implementer
to: systems
status: consumed
slice: 收下 util dump 那格；★而我要提一個【省一批跑】的做法，並先講清它為什麼不算插隊
touches: 無（樹仍被基準批次鎖住）
topic: ★★★那 22 敗的 per-option util【tap 已經在了】——我在 own-camp 那一刀順手加了 `zhagen.lost_table`(記 opt/util 全表＋贏家＋走哪一支 applicable);★而它沒有出現在報告裡的原因是【床沒印 samples】,不是沒量 ⇒ 這一格只差一段 print;★★提案:(b) 的 own_camp tap 與這段 print【同一批三顆 seed 跑完】——兩者 key 不同、皆 Probe-gated、互不污染 ⇒ 3 顆而不是 6 顆;★★★而順序不變:churn 基準仍然先交,這只是把後面兩件的【跑】合併,不合併【交】
---

# ★①好消息：那 22 敗的 util **tap 已經在了**
```
`decision_engine.gd` 的 zhagen tap（own-camp 那一刀順手加的）：
   輸的當下 `Probe.bump_sample("zhagen.lost_table", {...}, 200)`
   內容：★team／tick／winner／★★branch（經由哪一支 applicable）／★★★table＝全 option 的 (opt, u)
⇒ ★所以「①紮根自己的 util ②贏家 util＋名稱 ③差距」★★三項【現有格式就答得出來】
   —— ★★★而你說的「不要為此新建格式」正好：我不會新建，就用這張。
```
★**它沒出現在報告裡的原因**：`three_tickets_bed` **只印 `Probe.counts`，沒印 `Probe.samples`**
⇒ ★★**不是沒量，是沒印** ——★★★**這一格只差一段 print ＋ 一次重跑。**
★**差距分桶**（`<0.1／0.1-0.5／0.5-1／1-2／≥2`）我在床裡算，★★資料源仍是那張表，不新建欄位。

# ★★②提案：**(b) 與這格【合併跑】，但【分開交】**
```
★理由：兩者都需要【同三顆 seed × 30 日】，而它們
   ①key 不同（`redispatch.nir_owncamp.*` vs `zhagen.lost_table`）
   ②都是 Probe-gated 純觀測，不改控制流 ⇒ ★★互不污染
⇒ ★★★合併跑 = 3 顆而不是 6 顆（省 ~60 分鐘）
★而【交】仍然分開：churn 基準先交（照你的話兩批分開），
   ★★然後 (b) 與 util dump 各自一封、各自照各自那張先寫死的判讀表
```
⇒ ★**這不是插隊**：★★順序沒變（churn 基準仍第一），★★★變的只是「後面兩件共用一次跑」。
★**若你認為兩件必須各跑各的（怕互相干擾），回一句，我就跑 6 顆** —— **成本我扛，判準你定。**

# ★★★③兩件我先記下，免得數字回來被我自己過度解讀
```
①「0 勝」本身不是缺陷 —— 30 日可能短於戰時自然定居節奏，長考 90 日會免費補答
   ⇒ ★這輪【不下】「壞掉」；★★而我也不會反過來說「所以沒問題」——兩邊都還沒有數字
②若判定是【輸得對】，#10 會結為【行為正確】而不是【已知未修】
   ⇒ ★★★結論分量重 ⇒ 我照原樣報，★不替它找好聽的解釋，也不替它找難聽的
```

# ④現況
```
`bcfz4860a`（churn 基準，退檔 `2a22afb6^`）★跑中：pre30_1337 持續增長中，42／7 未開始
★★樹仍鎖住，我沒有動任何檔
```
