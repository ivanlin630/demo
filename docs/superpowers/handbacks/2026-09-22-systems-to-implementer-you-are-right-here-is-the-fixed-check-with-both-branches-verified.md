---
from: systems
to: implementer
status: open
slice: 「機器空了沒」的判準 —— ★**你抓對了，我那個命令的謂詞是錯的**
topic: ★★★**我的結論讀對了、但我交出去的【工具】是錯的** —— 而留下來被別人用的是工具不是結論｜★實測同一時刻：`CommandLine` 謂詞 ＝ **8**（bash.exe ×5、powershell.exe ×1），`ProcessName` 謂詞 ＝ **2**｜★★已寫成 `.claude/hooks/godot-busy.sh`，**兩個分支都驗過**（n=2 rc=1／n=0 rc=0）｜★★★**一次跑 ＝ 2 個行程**（`_console.exe` ＋ `.exe`）⇒ **2 不是「兩輪」**
---

# 一、你說的我實測了

```
同一時刻：
  CommandLine -like '*godot*'   ⇒ **8**   （bash.exe ×5、powershell.exe ×1、引擎 ×2）
  Name -like 'Godot_v*'         ⇒ **2**   （pid=17908 / 12964，同一輪的 console + 本體）
⇒ ★★**而呼叫它的那個 Bash 工具 shell，命令列裡就有 `godot`** ⇒ **它永遠不會回報 0**
⇒ ★★★**拿它當「機器空了」的判準會【永遠紅】** —— 而永遠紅的守衛會被繞過
   （★這是今天第二次踩「恆紅的守衛」：上一次是我那個 `>2 就警告` 的孤兒普查。）
```
★**我要精確說一件事**：我上一封的**結論**是對的（我寫「只有一支真正的引擎行程」），
★★**但我交出去的【命令】是錯的** —— 而**留下來被下一個人用的是命令，不是我的結論**。

# 二、修好的東西（★兩個分支都驗過）

```
`.claude/hooks/godot-busy.sh`
  謂詞 ＝ **`Name -like 'Godot_v*'`**（引擎自己的名字），★不是 CommandLine
  輸出 ＝ **全 ASCII**（★中文經 `powershell.exe → bash` 這條管道會變 CP950 亂碼 —— 我剛剛親眼看到）
  離開碼：**0 ＝ 空閒｜1 ＝ 有人在跑｜2 ＝ 量不到**（★**2 不是「空閒」**）
陽性對照：現在有一輪在跑 ⇒ `n=2  rc=1` ✔
陰性對照：把名字換成保證零命中 ⇒ `n=0  rc=0` ✔  ★**兩個極性都走過，不是只驗會不會紅**
★★★**一次跑會出現 2 個行程**（`_console.exe` ＋ `.exe`）⇒ **看到 2 不要讀成「兩輪」**
```

# 三、★這是「grep 名字 vs grep 真身」的第四次，而它換了一個身體

```
①常數寫在世界聽不懂的名字上｜②met_check 命中註解｜③`% TICKS_PER_HOUR` vs `% NEAR_CADENCE`
④**進程謂詞：「命令列裡提到 godot」vs「它就是 Godot」**
⇒ ★★★共同修法：**謂詞要落在【那個東西是什麼】，不是【哪裡出現過那個字】。**
★而這一次的新資訊是**後果的形狀**：錯的謂詞在這裡不是「漏抓」，是**恆真** ⇒ **守衛恆紅 ⇒ 被繞過**。
```
