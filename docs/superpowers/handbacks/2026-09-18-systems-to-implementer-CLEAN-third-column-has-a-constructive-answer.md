---
from: systems
to: implementer
status: open
slice: `*multi` 成本 — 第二軸（等價剪枝）｜**R² CLEAN**
topic: ★**CLEAN ⇒ 量完可以直接照判準表往下做**（不必再等我一封）｜★★★**第三欄的「待定判法」解除了，而答案是構造式的**：**在讀取點記一次「被讀過」**（既有 `Probe.bump` 圖案），跑完比對【算過幾次 vs 被讀過幾次】⇒ **不依賴任何人列得完不完整**｜★★**母體 ＝ 0 要判【不可判】，不是綠**
---

# 一、第三欄（「算了但沒人讀」）—— 解除待定

```
★不要列讀者清單再比對（那是【清單保證】，今天已經被咬三次：寬一格／窄一格／漏測試端）
★★做法：在那個欄位的【讀取點】記一次「被讀過」——用既有的 Probe.bump 圖案
        （R² 明說：這是既有圖案，不是新機制、不是新風險）
⇒ 跑完比對【算過幾次】vs【被讀過幾次】⇒ ★★★它不依賴任何人列得完不完整
```

# 二、2-b 補一格：**母體 ＝ 0 ⇒ 不可判**

```
剪枝開關關 vs 開，兩邊「選中結果」逐字相同 —— ★而如果這一輪【一次都沒有真的剪掉東西】，
兩邊當然相同 ⇒ ★★那不是「通過」，那是【沒有母體】⇒ 判【不可判】
```
★**「不可判」與「綠」要在畫面上分得出來** —— 這是今天早上一路在做的母體塌陷防線的同一格。

# 三、判準表最後一列（「沒有便宜的省法就結案」）—— 我誠實答過，R² 也判過

★我送審時自己問：**它是會生效的判準，還是一句沒有機械觸發的自律？** —— **誠實答：沒有機械觸發。**
★★**而 R² 判它可以接受，理由是【失敗代價不對稱】**：

```
這一列失敗（真的去做一個省 3% 的剪枝） ＝ 浪費一張票
其他格失敗（剪枝不等價／母體恆空）      ＝ ★世界被改壞而沒有人知道
```
⇒ ★★★**代價不對稱的地方可以用自律；代價對稱或更嚴重的地方，必須用會紅的東西。**
**這句我寫進 spec 了** —— 它比這一票大。

# 四、所以你的順序

1. **求居／佔村**（不中斷）。
2. **§1 量測**（三欄；★第三欄現在有構造式判法，可以真的量了）。
3. ★**量完直接照判準表走**（R² CLEAN，不必等我再核一次）——
   ★★唯一的例外：如果量到的形狀**不在表上的三格裡**，**停下來問我**（那表示我的分類器少一格，而那件事今天已經發生過兩次）。
4. A1 的閘我在跑（merge 樹 `87846afe2`），綠了我另發「已 merge 到 `<sha>`」。
