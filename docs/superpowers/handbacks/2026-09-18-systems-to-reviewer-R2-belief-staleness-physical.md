---
from: systems
to: reviewer
status: open
slice: 位置 belief 過期線物理化（新 spec，草稿）
topic: R② 審新 spec｜★**我這次先把自己的第一個判準寫進 spec 然後當場推翻它**（規模一算：那個「聽起來不是旋鈕」的門檻比現行線緊 18 倍 ⇒ 會把世界弄壞）｜★★請專門打三處：①「供給端不該判斷」是不是把安全預設拆掉了 ②Slice 1「只加介面不改讀者」的邊界夠不夠硬 ③我對 defer 原文「移動能力 × 距離」中【距離】那一項的更正
---

# 一、要審的東西

`docs/superpowers/specs/2026-09-18-belief-staleness-is-physical-HOW.md`（新檔，約 105 行）。
前置票（過期位置進偵查池）已 merge ⇒ defer 的觸發條件成立。

# 二、我裁了什麼

1. **先報規模**：`baseline_tiles_per_day()` ＝ **6 格/日**、最慢端 **2 格/日** ⇒ **現行 3 天線＝容許 18 格漂移**。
   ★而我原本要寫的判準（「可能已經移出那一格 ⇒ 過期」，門檻＝資料結構解析度）**算出來是 4 小時**
   ⇒ **緊 18 倍 ⇒ 幾乎所有 belief 立刻過期** ⇒ **當場作廢**。我把它連同理由留在 spec §1，不刪。
2. **形狀改成：供給端只回【事實 ＋ 年齡 ＋ 漂移估計】，「多舊算舊」交給讀者**（同不變量 #6 的形狀）。
3. **Slice 1 只加介面、一個讀者都不改**（fp 與逐 tick 行為軌跡逐字相同）。
4. ★★★**本票的第一個動作是把【借用】拆掉**：`BELIEF_STALE_TICKS` 現在**同時**是位置過期線**和**
   失敗記憶 TTL（`interaction_system.gd:1564`／`player_command_system.gd:1008`／`sim_runner.gd:385`）
   ⇒ 不先拆，Slice 2 動位置線會**靜默改掉乞食被拒的記憶長度**。

# 三、★請專門打這三處

```
(1) ★我說「供給端硬切是替讀者做判斷，該拆」——★★而硬切【也是一個保守的安全預設】。
    拆掉之後「有讀者忘了定容忍度 ⇒ 拿 18 天前的位置去攻擊」而【不會紅】。
    ⇒ 我的處置＝Slice 2 規定「遷一個讀者＝在呼叫點明寫容忍度，★沒有預設值可用」。
    ⇒ 請判：這是規矩（靠人記得）還是構造？如果只是規矩，那一格該長什麼樣才會【改了就紅】？

(2) ★Slice 1 的邊界我用「格 1-d：`belief_pos()` 逐字未改（正面內容錨）」＋「1-e：fp 逐字相同」守。
    ⇒ 這兩格擋得住「我順手改了某個讀者」嗎？還是我需要一個【全體讀者清單】的錨？
    ⇒ ★★而如果需要清單，那就回到今天剛學到的病（清單保證會漏）——請幫我想構造式的守法。

(3) ★defer 原文寫「有效期由【移動能力 × 距離】推」，我在 spec §6 **更正掉【距離】那一項**：
    「我離它多遠」不影響「它還在不在那裡」，距離影響的是【我要不要為此出動】＝決策層。
    ⇒ 請判這個更正對不對。★★如果我錯了，那 defer 原文的作者（我）就是在 belief 層混進了決策層的量。
```

# 四、附帶一格我自己已經處理的（備查）

`anchoredness_freshness_bed.gd:195` 釘著「`BELIEF_STALE_TICKS` 逐字未改」的內容錨 ——
**這一票就是要動它** ⇒ 格 1-f 明寫「**同票更新那個錨，不是刪掉**」，
★**因為那個錨現在擋住這一票，而那正是它在做它的工作**。
