---
from: systems
to: implementer
status: consumed
slice: 跨 tick 快取【前置量測】—— 派工
topic: ★**派：同一隊、相鄰兩 tick，`gather` 產出有多常不變**（純量測、不動 production）｜★★★**比法是硬性的：用現成的 `DecisionContext.snapshot_dict()`，不准手抄欄位清單**（手抄會讓不變率**虛高**，而那正好偏向「快取很安全」那一側）｜★★**門檻 95% 已預註冊**（WHAT 確認），你只量不判｜★記憶體我實測過：**FreeMB 15814 / 32657（48.4%）** ⇒ 現在可以長跑
---

# 一、票身

`docs/superpowers/specs/2026-09-22-cross-tick-gather-invariance-MEASURE.md`（先讀 §3 與 §5）

# 二、★★★比法（這一格沒有商量空間）

```
用 `DecisionContext.snapshot_dict()`（`decision_context.gd:1454-1466`）
★它自己的註解就寫著：「用 get_property_list 而不是手抄清單；
  ★★手抄的清單【不會跟著新欄位長大】，而漏掉新欄位的症狀正好是『玩家看不到』」
⇒ ★★對它的結果取穩定序列化後的雜湊，逐 tick 比【同隊上一 tick】
⇒ ★★★判準句：**「我的比法會不會因為【有人新增了一個欄位而沒列進來】而變成『不變』？」**
   —— `snapshot_dict()` ⇒ 不會；手抄清單 ⇒ 會，**而且它會把數字推向「快取很安全」**。
```

# 三、卷面必帶（★母體與尾巴同印，今天立的規矩）

```
①**母體 ＝ 比較次數**（Σ 每隊每 tick 有前一 tick 可比的次數）——★不是隊數、不是 tick 數
②(丙) 整份不變率 ⊕ (甲) 第一名不變率，**兩欄並列**
③★**變的時候變在哪**：最常變的前 5 個欄位名
   —— ★★這一欄決定「更窄的快取」有沒有形狀（第一名幾乎不變而整份常變 ⇒ 變的是不影響排序的欄位）
④四要素 ＋ 硬體戳 **HW-2** ＋ 世代戳 **世代 6（邊界 `3fb2c3201`）**
⑤兩顆種子、8 天窗、`warring_states`
★★★⑥**樁自己的成本**：`snapshot_dict()` 每隊每 tick 跑一次不便宜 ⇒ **印出它佔 gather 的比例**；
   若它貴到改變了世界的時序 ⇒ **這一輪作廢**（★不是「差不多啦」）
```

# 四、★門檻（已預註冊，**你只量不判**）

```
(丙) ≥ 95% ⇒ 值得開快取票（省的是整個 `gather`，而 gather ＝ 整場 tick 時間的 43.7%）
(丙) < 95% ⇒ ★**那個快取形狀從一開始就錯** ⇒ 永久結案（不是「調低門檻再說」）
★★95% 講死：**每 20 次借用會有 1 次借到錯的**。
```

# 五、★三件我今天踩過的，你不必再踩

```
①**`GODOT_TIMEOUT` 要給夠** —— wrapper 預設只有 **360s**，超過就 **exit 98 而且一行結果都沒有**
   （★rc=98 ＝ wrapper 逾時、rc=124 ＝ 呼叫端逾時，**兩個都不是床壞掉**）
②**記憶體**：我剛實測 FreeMB 15814/32657（48.4%）⇒ 可以跑；
   ★但今天我有一個背景量測**被 harness 因系統記憶體不足停掉**過 ⇒ **長跑前自己看一次**，
   ★★用戶在玩遊戲時讓路（WHAT 今日明令）
③**檔名不要用 `0/1` 表示開關** —— 我今天差點把一個結論做反，
   因為我從 `memo0/memo1` 推語意，而它其實是 `MEMO_OFF=` 的值（★與直覺相反）
   ⇒ ★★★**標籤與內容是兩條獨立通道**，檔名請直接寫 `-memoON-／-memoOFF-`。
```
