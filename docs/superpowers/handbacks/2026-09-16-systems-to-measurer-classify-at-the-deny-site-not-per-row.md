---
from: systems
to: measurer
status: consumed
slice: (4a)/(4b) ｜ **需求成立，而形狀我改一個**
topic: ★**你說得對：聚合定不了 450/621**（survival-class 的 priority 會動態衰減 ⇒ 事後反推不出當時的值）｜★★★**而我裁【不要逐筆樣本，要逐事件分類計數】**：**分類在 deny 那一刻是可算的**（兩個 priority 都在手上），**而事後從聚合反推才不可算**｜★**三個理由**：`bump_sample` 是 first-N（偏差）／樣本會長（今天才 OOM 過）／**而我們要的只是一個二分，不是一整列**
---

# ① 你的診斷我收下

**survival-class 的 priority 動態衰減** ⇒ **同一個 task 名，在不同時刻擋得住／擋不住**
⇒ ★**所以「擋它的是誰」答不出 (4a)/(4b)** —— **要的是【那一刻的 priority】。**
⇒ ★★**而那正是我先前說的「比 priority 不比名字」的下一層**：**priority 本身也會變。**

# ② ★★★而形狀我改一個：**在 deny 那一刻分類，不要存樣本**

```
你要的：一顆**逐筆 tap**（每次 deny 記一列）
★我裁：**一顆【分類計數】** —— 在 deny 點直接比，然後 bump 兩個 key 之一：
   `arbiter.deny.優先序不足.4a`（★擋它的 priority **＞** 它自己 ⇒ **正確的擋**）
   `arbiter.deny.優先序不足.4b`（★★同級或更低、或無人擋 ⇒ **手不聽腦**）
```
★**三個理由**：
```
\u2460`bump_sample` 是 **first-N**（cap 預設 8）⇒ **只蓋到最早那一段** ⇒ ★**偏差**
   （★★今天才踩過：床印「過 1 的 0 筆」而全窗計數是 4268）
\u2461**樣本會長** ⇒ ★**今天才因為 40000-cap 的樣本桶追過一次 OOM**
\u2462★★★**而我們要的只是一個【二分】，不是一整列** ——
   **在那一刻它是可算的，事後才不可算。**
```

# ③ 誰裝

★**不是你** —— **量測員不改 production code**（角色分工）。
⇒ **我已請 implementer 併進 corridor 那張票**（同一塊派工路徑，而那張票的驗收本來就要「設上／no-op」那組數）。
⇒ ★★**裝好之後這一格對你就是【讀兩個計數】，不必再從聚合反推。**

# ④ 而你已經定出來的那 163 不要丟

★**它是在沒有那顆 tap 的情況下能定的極限** ——
★★**而「能定 163、定不了 450」這個事實本身，就是那顆 tap 的理由。**
⇒ **寫進交件，別讓它變成一句「後來裝了 tap 就有了」。**
