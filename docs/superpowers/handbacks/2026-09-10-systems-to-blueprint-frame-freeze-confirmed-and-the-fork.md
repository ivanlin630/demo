---
from: systems
to: blueprint
status: open
slice: 效能診斷回來了 —— ★用戶抱怨的那件事【被坐實而且更糟】
topic: ★★★數字：spike 中位數 **7.78 秒**／p95 **15.45 秒**／max **26.65 秒** —— 用戶說的是 5-10 秒，**實測比他抱怨的更糟**｜★★兇手單一而明確：`near.faction_ai` **每次貢獻 80-95% 的耗時**，**每小時（60 tick）固定發生**，從第一小時就有、貫穿 90 天；且**隨隊數惡化**（63 隊 0.4-9 秒 → 135 隊 8-26 秒）｜★而修法方向（跨 frame 切片）**要你先裁一件 WHAT**：切片會【改變世界】
---

# ① 數字（量測員交件）

```
spike 樣本：中位數 7.78s ／ p95 15.45s ／ ★max 26.65s
節奏     ：★★每小時（60 tick）固定發生 —— 從第一小時就有，貫穿 90 天
兇手     ：`near.faction_ai`，★每次貢獻 80-95% 的耗時（單一、明確，不是分散）
規模關係 ：63 隊 0.4-9s → ★★★135 隊 8-26s（**隨隊數惡化**）
```

⇒ ★**用戶抱怨的「60fps 跑到思考層後卡住 5-10 秒」被坐實**，而且**實測更糟**。
⇒ ★★而「可慢不可卡」那條憲法級規矩，**現在有了它的第一個具體對象**。

# ② ★★★要你裁的 WHAT（★這是真的 fork，不是技術細節）

```
修法方向（你與用戶都已定調）＝【跨 frame 分攤】，不是【做得更快】。
而 faction_ai 的形狀是：**每小時、所有隊【在同一個 tick】一起思考**。
⇒ 最直接的切法：**錯開**（每隊在自己的 tick 上做它那一小時的評估，
   例如 `team_id % 60`）⇒ 一次 26 秒的凍結，變成 60 次小工作。
★★★而它【會改變世界】：
   ①每隊思考的【時刻】不同 ⇒ 誰先看到誰、誰先出手，順序變了
   ②fp 會變（★而今天剛把 fp 擴到 103 欄，所以這件事現在看得很清楚）
   ⇒ ★**同一顆 seed 跑出來的世界，會與今天的不一樣。**
```

**要你裁的**：

```
(a) ★**接受世界改變**（我建議）——理由不只是效能：
    ★★「所有隊在同一 tick 一起思考」本身就是一個【同步世界腦】的假象，
    ★★★錯開之後決策不再 lockstep，**那更像一個世界**而不是更不像。
    ⇒ 代價：今天所有以 fp 為基準的回歸，基準線要重取一次（★而 R² 已查證沒有硬寫 fp 值）。
(b) 不接受世界改變 ⇒ 那就只剩【在同一邏輯 tick 內跨 frame 算、tick 邊界一次套用】，
    ★成本高很多、且對 headless 步進沒有幫助（★★而 headless 正是我們所有量測的跑法）。
(c) 你有第三條路 ⇒ 給我，我來寫 spec。
```

★**我不自己決定的理由**：(a) 改的是**世界長什麼樣**，那是你那一側；
★★而我能給的是「它會怎麼改、代價多少」，上面寫完了。

# ③ 序（★我的建議，等你點頭）

```
①這張【現在最優先】——它是用戶下一次開玩就會撞到的東西，
  ★而體驗窗其餘三票（inspect／帶因／C1①②）都已落地。
②★★攻擊門那張仍然排它之後 —— 而它們有一個好的先後關係：
  ★★★**先把 26 秒的凍結拆掉，攻擊門帶來的額外候選評估才不會直接把它變成 40 秒。**
   （攻擊門會讓 applicable 率從 2.63% 上到 >20% ⇒ 更多 option 進 rank ⇒ 更貴。）
⇒ 這一點我原本沒想到，是量測員的「隨隊數惡化」那一欄讓我看出來的。
```
