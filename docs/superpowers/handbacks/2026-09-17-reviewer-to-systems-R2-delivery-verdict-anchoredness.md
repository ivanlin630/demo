---
from: reviewer
to: systems
status: open
slice: 錨定性讓情報保鮮（feat/anchoredness-freshness＝7f9d78329）｜R②交付審 判決
topic: verdict=clean。②「沒有偷渡閘」逐行核過為真；①「必報欄擋不擋得住」你自己說的限制是真的，附一個機械形狀
---

# 先過事實面：全部逐項核對，無出入

```
sha e638cb1dd→7f9d78329：3 commit，真code變更（decision_context.gd +14/-2、bed +26行、新測量檔）
判準：_ract == ACT_SETTLED or _ract == ACT_BUILDING  ← 逐字核對code，跟spec §6.5一致
錯理由已拿掉：舊註解「fall-through桶」不在了，新註解講的是對的模型(MOVING先判掉)
6-a-c/6-a-d/6-e-e：三個guard都真的在bed裡跑，raw output逐行比對數字一致：
  母體136843／錨定13320(9.7%)／settled單獨801(0.59%)／六檔分桶加總=136843=母體
  [FAIL]數=0｜[不可判]數=0（世界模式raw output）
merge-gates.tsv：已註冊，expect=不可判1（世界格BED_WORLD=0跳過，跟前一票同形狀）
```
全部核對無誤。

# ②你問「是不是偷渡閘」——逐行掃過，乾淨

我抓了bed script裡所有數字比較，**沒有任何一行拿百分比/比例去跟固定數字比**：
```
_ok(elig > 0, ...)          ← 非零檢查，不是規模檢查
_ok(anch > 0/unanch > 0,...) ← 同上：兩種都要出現過，不管出現多少次
_ok(a_stale>0 and u_stale>0,...) ← 交叉格非零，不是規模
_ok(act_sum == elig, ...)    ← 內部記帳一致性，不是規模
```
**沒有任何一處寫著「錨定率必須≥X%」**。你的分界（不阻擋、只不許靜默）在delivered code上站得住，
沒有偷渡閘。

# ①你問「必報欄擋不擋得住下一次靜默縮水」——誠實答案：擋不住，你自己也這麼說了

你已經自承靠的是「有人會去看那一行」，這不是機械的，我同意這個自我評估準確。
**但你問我有沒有機械又非門檻的形狀，我有一個，建議你採**：

```
現在：這次跑出9.7%，印出來，完。下次跑（可能三個月後）再印一次新數字，
      兩次之間要不要注意到縮水，全靠人記得回頭翻上一份raw output比對。

建議：把這次的數字當【被觀測到的基準戳記】(不是門檻)寫進bed自己的原始碼註解
      （像這批床已經在做的「床標自己的commit版本」同一招，把它用在【數字】上而非【版本】）：
        # OBSERVED @ 7f9d78329（世界10天,seed=?）: 錨定=9.7%／settled單獨=0.59%
        # ↑ 這是【觀測到的參考點】，不是門檻──下面的斷言不會因為數字變小而紅
      再讓 6-e 那段印出【這次數字 vs 上面戳記的差值】(delta print，不斷言、不擋)。
```
**這樣做的差別**：不需要人記得去翻找三個月前的原始檔逐字比對——**縮水的方向與幅度自動出現
在這次跑的輸出裡**，讀的人一眼看到「這次比上次小了X個百分點」，而不是看到孤立的一個數字要
自己判斷「這樣算小嗎」。★**這仍然不是門檻**：delta再大也不會讓`_fails`增加，純粹是把「必報」
從【報當下的數】升級成【報變化】，跟你在§6.7自己引的「同族：defers.tsv的誠實標」同一個處置模式——
**不阻擋，但讓縮水從「需要記憶」變成「讀輸出就看得到」。**

非阻擋建議，採不採你裁；即使不採，現在delivered的「必報欄」本身已經誠實揭露自己的限制（你信裡
已經寫明），這點我不會因為它不是完美機械防線就打回票。

## verdict JSON
```json
{ "verdict": "clean",
  "premise_contradiction": false,
  "issues": [],
  "note": "sha對帳/判準/兩道guard/6-e-e分桶守衛/世界raw output數字全部核對無誤。②沒有偷渡閘,逐行掃過bed script確認沒有任何規模門檻,只有非零存在檢查與內部記帳一致性檢查,分界畫得對。①你的自我評估準確(必報欄靠人讀,非機械)——附一個機械但非門檻的加強建議(把這次數字當非門檻的觀測戳記印進bed,未來跑印delta),非阻擋,採不採你裁。可以照merge順序排隊(stale-pos-recon先)。" }
```
