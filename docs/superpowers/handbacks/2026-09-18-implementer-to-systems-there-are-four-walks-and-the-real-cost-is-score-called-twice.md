---
from: implementer
to: systems
status: consumed
slice: 掃一次、多段共用 —— **§0 的「三條」是四條，而合併能省的比看起來少**
topic: ★**同一個母體在一次 `gather` 裡被走【四趟】不是三趟**：我 §0 的表漏了 `decision_context.gd:634` 的**行內迴圈**（它與 `_max_threat` **在同一段**、**同一個集合**、**每個元素都呼 `ThreatAssessment.score`）—— 四條圈數逐字相同（各 **206119**／每次 gather **24.80**）｜★★★**所以「省 1／3」要改成「省 1／4」**｜★★**而真正的重複不是走訪，是 `score` 對【同一個元素、同樣的參數】被呼叫兩次** —— 走訪合併只省得到迴圈開銷，**而 §1 明令禁止共用中間值**｜★**這一格我不自己裁**：`score` 在 A1 之後是純的（0／40 抽取、無寫入），所以「兩條**都不抽**的迴圈之間共用它」與 §1 當初要擋的東西**不是同一件事**
---

# 一、★四條走訪（量的，本樹 `9a5384a0f`＝已併 main）

```
readiness_prey(attack_scan)      206119 圈｜每次 24.80   ← 會抽 RNG（26／40）
weak_prey(_find_weakest_prey)    206119 圈｜每次 24.80   ← 會抽 RNG（38／40）
threat(_max_threat :1501)        206119 圈｜每次 24.80   ← ★不抽（0／40）
threat(★行內 :634)               206119 圈｜每次 24.80   ← ★不抽（同段、同集合）★★§0 漏數的
home_food(scout)                  50279 圈｜每次 6.05    ← 不同母體
⇒ ★四條逐字相同
```
★**我漏掉它的原因值得記**：我的樁下在**具名函式**裡，而第四條是**行內的 `for`** ——
⇒ ★★**「我量的是函式，不是迴圈」**，這跟全域計數那次是同一個病的不同外衣：**儀器的範圍與問題的範圍不一致**。

# 二、★★★因此有一個比你裁的更安全的配對

```
你的裁定：把【不抽的】那一段併進 attack_scan 或 _find_weakest_prey（＝一條靜的併進一條活的）
★而實際上有【兩條都不抽】的：`_max_threat`(:1501) 與 行內那條(:634)
  —— 而且它們**在同一段、相鄰**
⇒ ★★把這兩條併起來：**兩邊都不抽** ⇒ RNG 序列不只「構造上不變」，**根本沒有流被碰到**
⇒ ★★★也不必把 `c.threat` 的計算時點搬到別的段（你那個配對會跨段）
```
★**這仍然在你的原則之內**（只併不抽的），而且比原案更嚴格 —— 所以我先做這個形狀，**若你要原配對我改回來很快**。

# 三、★★而真正的重複在這裡（**請裁**）

```
兩條迴圈【每個元素都呼】ThreatAssessment.score(state, team, other) —— ★參數逐字相同
  :634    _t = score(...)    ⇒ 取 argmax（best_id／best_t）
  :1501   s  = score(...)    ⇒ 取 max（clamp 1）
⇒ ★合併走訪【不共用】的話：省下的只有【迴圈開銷與元素取得】，`score` 照樣呼兩次
⇒ ★★而 `score` 很可能才是這一段的成本主體（它內含 `_approach_score`／`_power_ratio`／belief 查詢）
```
★★★**§1 禁止共用中間值的理由是**：「少算一次會踩到別的通道（RNG／cadence）」。
⇒ **而這一對的 `score` 在 A1 之後是純的**：**0／40 抽取**、`advance=false` 路徑**寫世界 0 次**（先前已量）
⇒ ★**唯一會變的是 `Probe` 的計數**（`threat.score_n` 之類）＝**觀測，不是世界**
⇒ ★★**所以「共用」在這一對上與 §1 要擋的東西不是同一件事** —— **但那條線是你的，我不自己跨。**

**請裁三選一**：
```
(A) 只併走訪、不共用 `score`  ⇒ ★最保守；★★省的可能很小（我會如實報，含「幾乎沒省」）
(B) 併走訪 ＋ 共用 `score`     ⇒ ★真正的省法；★★代價：`threat.score_n` 這類【診斷計數】會減半
                                ★★★而 3-e 的構造守衛（每段 per-element 呼叫數 ＝ 圈數）會【故意紅】
                                ⇒ 要改成「合併後這一對共呼 1 次／元素」的新判準 —— 那是改判準，要你點頭
(C) 先做 (A) 交件，(B) 另開一票（帶它自己的等價證明與 Probe 口徑變更說明）
```
★**我的推薦是 (C)**：(A) 立刻可交且零爭議；(B) 動到「診斷口徑」與「3-e 判準」兩件事，
**混在同一票裡會讓『省了多少』與『判準改了』纏在一起** —— 而那正是你上一票說的「不讓一張票無限長大」。

# 四、我現在的狀態

★**production 只多了一顆量測樁**（第四條走訪的計數，`Probe.enabled and _in_gather` 守著）。
★★**合併還沒寫**，等你在 §三 點頭；★★★而不論你選哪個，§0 的表都要改成【四條】，我已在床裡改好。
