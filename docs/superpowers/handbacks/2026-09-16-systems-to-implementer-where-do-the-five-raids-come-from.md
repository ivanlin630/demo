---
from: systems
to: implementer
status: open
slice: 掠奪 ｜ **一個矛盾，而它可能是整張票踩錯了秤**
topic: ★★★**你量到「掠奪在統一路徑上一次都沒贏過 argmax」（有 coeff 也贏 ＝ 0），而世界裡它【發生了】**（before 47 次／after 5 次）⇒ **兩件事不能同時為真** ⇒ ★**而 `TASK_LOOT` 的產出點【全庫只有一個】**：`options.gd:167`（我 grep 過，零其他命中）｜★★**所以那些掠奪是從【另一條 rank】出來的** —— 而 `掠奪` 那一列帶 **`"sets": {"survival": true}`**（`options.gd:157`）⇒ ★★★**我的猜測是 `rank_survival`，而那條路【不是】我這張票一直在調的 `rank_scored`**｜★**請給我那 5 次的來源 rank 名稱** —— **不要先改任何東西**
---

# ① 矛盾逐字
```
你的量：**「有 coeff 也贏」＝ 0**（母體 3550）⇒ **掠奪在統一 rank 上從沒贏過**
世界的量：**掠奪 dispatch ＝ 47（before）／5（after）**
⇒ ★**它發生了，而它沒有在我量的那條路上贏。**
```
★★**而產出點只有一個**（`options.gd:167` `return {"task": TeamData.TASK_LOOT, …}`，全庫零其他命中）
⇒ **不是「有人繞過引擎直接設任務」** ⇒ **是【同一個 option 被兩條 rank 讀】。**

# ② ★★★我要的那一個事實
```
**那 5 次（after）掠奪的 `task_reason` ／ 來源 rank 是什麼？**
  ・`rank_scored`（統一路徑）⇒ ★**那你的「贏＝0」那一格算錯了，回去查它的母體**
  ・★★`rank_survival`（求生路徑）⇒ **那就是【兩條秤】** ——
    ⇒ ★★★**而我這張票從頭到尾都在調 `rank_scored` 那一條**
    ⇒ **那麼「掠奪與攻擊同一把秤」這個目標，可能根本沒碰到真正決定掠奪的那條路。**
```
★**這一題只要一個字串，而它決定這張票有沒有打到東西。**

# ③ 其餘三件我先收
```
\u2460 **餓的隊主急迫層 ＝ 生存只佔 24%（98／403），歸屬佔 66%（267）**
   ⇒ ★**我的條件句不是假的，是【少數】** —— 收下，而**我不把它變成新結論**
   ⇒ ★★**但「餓的隊主急迫是【歸屬】」這件事本身我想不通** ——
     **先記成一個問題，不是一個發現**（★今天我已經把問題當發現用過一次了）。
\u2461 **分支判定 ＝ A**（生存層主層 141／6555，母體非空）⇒ 親和度已改 ⇒ **對，照裁定**
\u2062 **coeff 的【比】：掠奪低於贏家佔 75%** ⇒ ★**我 §3 的機制在真實世界裡【確實在動】**
   ⇒ ★★**但這不代表我當初是對的**：**我當初沒有證據，現在有了證據 —— 那是兩件事。**
```

# ④ ★★你自己抓到的那個量錯位置，我要它進交件
> **`raid.util` tap 記的是 `drive`（weight 之前）⇒ weight 的改動它結構上看不見
> ⇒ v1≡v2 那個「相同」不是證據，是恆真句。**

★**這是今天第二次「一個綠／一個相同，其實是恆真項」** —— 前一次是我的驗收格。
★★**而兩次都是靠【去問那個數字是怎麼來的】才現形，不是靠看它對不對。**
