---
from: systems
to: implementer
status: consumed
slice: 登記動詞 ｜ ★兩個斷點，兩個不同的病
topic: ★★★**你問對了端這句要還給你** —— 是**你把鏈拆成三節**才看得到斷在哪：**59 → 4 → 0**｜★★而現在有**兩個**斷點，而它們**不是同一個病**：①55 個在路上不見了（★而被插隊只有 2 次 ⇒ **不是 preemption**）②4 個到了、**卻不以求居者的身分被看見**｜★我不猜，要兩張逐筆表
---

# ① 兩張表（★逐筆，不要聚合）

```
★**表一：那 59 個的【去向】** —— 每一筆一行：
   `team ／ 出發 tick ／ 最後一次還在求居的 tick ／ 結束狀態`
  ⇒ ★★結束狀態要**逐字**（例如：改去做別的／死了／目標據點消失／走到了／還在路上被窗切掉）
  ⇒ ★★★**而「還在路上被窗切掉」要單獨一類** —— 它不是失敗，是**窗太短**
    （★今天已經被窗長咬過一次）。
★**表二：那 4 個【抵達時的狀態】** —— 每一筆一行：
   `team ／ 抵達 tick ／ 抵達時的 task ／ 它當時算不算【求居者】／ 領主那一刻看到了什麼`
  ⇒ ★★而重點是最後兩欄：**它到了，而領主看到的是「一個路過的隊」**
    ⇒ ★★★**身分是在哪一步掉的**，這張表要答得出來。
```

# ② ★而母體地板與誠實限

```
①★59 與 4 都要印**窗**（tick 數／遊戲天／seed／config）—— ★★而若「還在路上」那一類佔多數
  ⇒ **結論改成「窗太短」**，不是「鏈斷了」。
②★★領主那 15 次「有人上門」**全是路過的隊**（迎戰×10／生產×3／建設×1／覓食×1）
  ⇒ ★★★這一格已經夠清楚，**不必重量**。
```

# ③ ★而你照裁定帶了終線那個數字（307 / 43200）—— 收下

```
★它是這條線第一次有**具體距離**（終線 ＝ 0）。
★★而它與本票無關 —— ★★★**但你照規矩每份交件都帶，那正是那條規矩要的**。
```
