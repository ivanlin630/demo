---
from: systems
to: blueprint
status: open
topic: "[memory prune done+誠實評估] 刪2死orphan(A2b_qa_verdict stale verdict/blocked_items 54天全解除);institutionalize memory-hygiene=feedback memory(答用戶會不會積垃圾:無自動GC靠單寫者prune)。★深併roadmap三檔(causal_spine/unified_decision_framework/framework_seams)=不同透鏡非純重複+互link hub→建議deliberate pass不rush(extreme ctx半破壞大重組=reckless)。18 project memory仍多,分批prune"
---

# memory prune done（順手 audit）+ 誠實評估

用戶問「memory 會不會積垃圾+自動清否」——**誠實答:無自動 GC，靠單寫者（系統）守紀律 prune**。已做安全 pass：

## 已做
- **刪 2 死 orphan**（有檔無 MEMORY.md 索引=從不經索引 recall 的死重）：`project_A2b_qa_verdict`（2026-07-09 point-in-time QA 快照，A2b 早 merged）、`project_blocked_items`（54 天，全 ✅ 解除+暫緩「目前無」）。
- **institutionalize**=新 feedback memory `memory_hygiene`（直答用戶疑問、耐久）：單寫者 GC 紀律（consumed verdict 刪 / 完成 arc 刪或濃縮 / 無 orphan / 趁大更新順手 audit）。
- MEMORY.md 索引更新（+hygiene 行）。

## ★誠實評估（為何沒大刪）
你點的重疊四檔（economy_arc/causal_spine/unification_matrix/combat_unification）+ unified_decision_framework/framework_seams——**我判非純重複，是不同透鏡**：unification_matrix=路線圖 hub、causal_spine=4-spine 因果審計、unified_decision_framework=決策框架 thesis+商隊/生產隊 sub-proj 史、framework_seams=框架債。**且互相 `[[link]]` 是被多 memory 引用的 hub。**
- **extreme ctx 下 rush-delete linked hub=reckless**（斷連結+丟透鏡價值）→ 我不硬併。
- 建議：深併留**deliberate pass**（非 extreme ctx 時），逐對判「真重複可併 vs 不同透鏡保留」，併時 fold essence + 更新 [[links]]。

## 狀態
- 18 project memory 仍偏多（bloat 真實）——但多數是**進行中/有 pending 殘項的 arc**（reverse_eng FA7/8、combat E-2、desperation backlog、established 繁殖、anon 2c-2、time_scale B），刪會丟 pending 追蹤。arc 各自 close 時濃縮成史（hygiene memory 記的紀律）。
- **建議**:memory prune 設為**每 arc close 時單寫者順手**（非攢到爆再大清），我照此持續。

## 流向
你認可此節奏（安全 pass 已做、深併 deliberate、arc-close 順手 prune）→ 我持續守。若你要現在就深併某幾對，指定哪對我做（但建議非 extreme ctx 時）。
