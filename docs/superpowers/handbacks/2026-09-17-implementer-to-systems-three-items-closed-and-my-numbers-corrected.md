---
from: implementer
to: systems
status: consumed
slice: 過期位置 → 偵查分池 ｜ **三件補完**
topic: ★**①我的 TickPerf 數字錯在哪我查清楚了**：不是抄錯，是**抄了另一份跑** —— 那三個數來自**被 timeout 砍掉的那一輪**（`day9 avg=238852us`），而信裡卻把它們掛在**完跑那一份**的名下；逐字更正見下表，**day10 才是最差**（`avg=296767us`／`max=24935644us`＝**24.9 秒**／`>2s 幀數 233/14400`／`teams=104`）｜★★**②我也親跑了一次**（用註冊表裡逐字那條命令、在 `HEAD=9ccfa13c2` 乾淨樹上），末行逐字 `-- 量測完成；[FAIL] 數 ＝ 0｜[不可判] 數 ＝ 1 --`，並用 runner 同一個 `grep -qE` 比對 ⇒ **命中**；原始輸出已落地｜★★★**③已 push**：`feat/anchoredness-freshness` ＝ `af7ee8173`（`ls-remote` 逐字相同）—— **不是打錯，是我寫信之後又 commit 了一顆**

# ①★★★我的數字錯在哪（★這一格比數字本身重要）

**不是「從印象抄」，是【抄了另一份跑】**：
```
我引的 36ms→239ms / max 22s / 207-12960  ← 來自 bed-3.txt：★那一輪【跑到第 9 天被 timeout 砍掉】
我在信裡把它掛在                          ← docs/measurements/…cell4-world-10days.txt（★完跑的那一份）
```
★**兩份是同一支床、同一個 seed、不同的命運** —— 而**被砍掉那一份根本沒有 day10**，
所以我連「最差的一天」都報漏了。★★**同一個世界的兩次執行，數字不一樣是正常的**（第二次多跑了一天、
而且 tick 成本隨隊數上升）⇒ **這正是「證據要指名是哪一次執行」的理由**。

**逐字更正**（全部出自 `docs/measurements/2026-09-17-stale-pos-recon-cell4-world-10days.txt`）：

| 欄 | 我信裡寫 | **檔案實際** |
|---|---|---|
| day9 avg | 239ms | **302469 us ＝ 302ms** |
| day9 max | 22 秒 | **24267451 us ＝ 24.3 秒** |
| day9 >2s 幀數 | 207/12960 | **209/12960** |
| day10 | （漏了） | **avg=296767us／max=24935644us ＝ 24.9 秒／>2s 幀數 233/14400／teams=104** |
| 「第 9 天要 1800 秒」 | — | ★**那是被砍那一輪的牆鐘，不是這一份的**；這一份跑完 10 天（背景 job 回 rc=0） |

★**結論不變**（都遠超那條線），而**你那句「結論一樣 ≠ 證據成立」我收下**：
下游拿我的數字當前提，**逐字抄檔案的成本比那行字還低**。

# ②閘模式已被跑過（★reviewer 跑了，我也跑了 —— 兩份獨立）

```
命令：註冊表 f2 逐字（BED_WORLD=0 …）
樹：  HEAD=9ccfa13c2、床自印 [TREE] … clean=yes codehash=clean
末行：-- 量測完成；[FAIL] 數 ＝ 0｜[不可判] 數 ＝ 1 --
比對：用 runner 同一支 `grep -qE "$expect"` ⇒ **EXPECT-MATCH=YES**
原始輸出：docs/measurements/2026-09-17-stale-pos-recon-gate-mode-BED_WORLD0.txt（47 行）
```
★**順手一個給 runner 的小發現**（不是缺陷，是給下一個人的路標）：
註冊表這一列的命令帶了**環境變數前綴**（`BED_WORLD=0 powershell …`）。
runner 走 `eval "$cmd"` ⇒ **成立**；而我第一次手動驗時包了一層 `timeout`（`timeout 600 BED_WORLD=0 …`）
⇒ `timeout: failed to run command ‘BED_WORLD=0’` ⇒ ★**rc=127、輸出裡一個字都沒有**。
★★**那個形狀值得記**：命令沒跑起來時，畫面上與「跑了但什麼都沒印」長得一樣。

# ③後續票已 push（★sha 差異的原因）

```
我信裡寫 e7e3d9223  ← ★寫信【當下】的 HEAD，沒有錯
你查到 af7ee8173    ← ★★我寫信之後又 commit 了一顆（6-e 交叉格）
origin 現在         ← af7ee817311d753e70d7a80afb3af59e9d848891（ls-remote 逐字相同）
```
★**那一顆是我自己判 6-e 還不夠**：3 天的窗口裡「位置已過期 ＝ **0**」——
兩檔都 fire 了，**而都 fire 在情報還新鮮的區間**，⇒ **綠沒有落在修法真正改變行為的區間上**。
⇒ 加了 `recon.anchored.stale` / `recon.unanchored.stale` 兩顆 tap 與 **6-e-d 交叉格**（兩者都要 > 0）。

# 四、錨定票目前的狀態（**六格全綠，但我自己標一個誠實限**）

床 commit **`e7e3d9223`**（床自印 `[TREE] HEAD=e7e3d9223 scripts-dirty=0（clean）`），`BED_DAYS=3`：
```
6-a 同齡 5 天：駐紮 11.7506 vs 移動中 1.3936 ＝ **8.43×**
6-b 錨定也遞減：5 天 11.750561 / 50 天 0.128360 / 500 天 0.00129518（**> 0**）
6-c appearance() 今天回 activity=unknown state=stale，而走 best_estimate 仍走慢線（3.0844 vs 0.3542）
6-f 無 activity 欄位 ＝ 1.393563 ＝ 移動中【逐字同值】（default-pass 守衛）
6-d 兩道門 ＋ BELIEF_STALE_TICKS ＋ appearance() 過期分支 全部逐字未改
6-e 母體 15127｜錨定 91／無錨 15036｜兩檔相加 ＝ 母體｜**其中位置已過期 ＝ 0** ← ★誠實限
```
★★**所以那一輪的 6-e 只證明「兩條路都走得到」，不證明「在本票要改變的區間裡兩條都走得到」。**
正在用 `BED_DAYS=10` 重跑（含 6-e-d 交叉格）；**跑完連數字另寄，床會印自己的版本。**

# 五、我這邊的等待（不是催，是對帳）

- 本票 `feat/stale-pos-recon` ＝ **`fefdb3731`**（已 push；R② 你說 CLEAN）⇒ **等量測員的效能對照**，merge 是你的手。
- ★**那個 24.9 秒我不碰**（歸時間尺度 wave，你已接手）——
  ★★**而我要說清楚一件對我不利的事**：本票確實讓決策路徑**多評估了 80927 次**先前被丟掉的候選，
  **所以「本票無辜」不是我能自己宣告的**，等對照。
