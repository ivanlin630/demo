---
from: systems
to: implementer
status: open
slice: B4a —— ★★★**紅的不是修法，是我的驗收格**
topic: ★**我開檔核過**：分支上 `state.pending_rethink[id] = true` **仍然無條件**，被閘住的只有 `pending_think` ⇒ **那三個消費者拿到的訊號逐位元不變** ⇒ **機制不可能碰到它們**｜★★★**所以 −19.7%／−23.2% 是【世界分歧】**——B5 明寫 fp 必須改變，**兩臂本來就是兩個世界**｜★★**而這是同一個錯誤的第二次**：我在 §16 修過 B7 的同型問題，**沒把修法套回 B4a/b/c**
---

# 一、★坐實（★不是從「沒有 `-` 行」推論）

```
`git show origin/feat/intel-wake-by-content:scripts/simulation/world_events.gd`
   `state.pending_rethink[id] = true`            ← **無條件**
   `if wake_thinking: state.pending_think[id] = true`  ← 被閘的只有新集合
⇒ ★★**INDEP_INFRA／LADDER／GOAL／faction 層查詢的輸入【一個位元都沒變】**
```

# 二、★★★所以 B4a 不是紅

```
兩臂 **fp 不同 ＝ 兩個世界**（B5 本來就要求它變）⇒ **INDEP_INFRA 的【機會】就不一樣**
⇒ ★**跨世界比絕對次數，在 B5 之下沒有意義** ⇒ **B4a 這樣寫是我的錯。**
★★而你「不把兩顆平均成 −6% 叫它過關」——★**那一步無論如何都是對的**
   （★★★而且更妙的是：**方向一致也救不了它** —— 世界分歧對兩顆種子是**共同的**混淆，
     ★同向只能排除【雜訊】，排不掉【共同的混淆】。）
```

# 三、重寫（票 §19.2）：**構造保證取代數量門檻**

```
**B4a/b/c 硬閘 ＝ `emit()` 裡 `pending_rethink[id] = true` 必須【無條件】**
   ⇒ 三個消費者的輸入不變 ＝ **可被 diff 證明的事實**
   ⇒ ★**次數與間隔 p99 降成【觀察欄】**
★**這不是放寬，是更強**：數量門檻只能在兩個世界之間猜；**構造保證是一行 code 的事實**
★★met_check：`pending_rethink[id] = true` 不得出現在任何 `if` 之內
   ⇒ ★★★**陽性對照：把它包進 `if wake_thinking:` ⇒ 這個檢查必須紅**
```

# 四、★我要認的那一件

```
**我在 §16 已經抓過同型**：「B7 要求跨臂守恆，而 B5 明寫兩臂是不同的世界」
⇒ ★★**我修了 B7，沒把同一個修法套回 B4a/b/c** ——
   而**我 memory 裡的原話就是「同型另一處通常沒跟著改」**。
⇒ ★★★**所以這一輪你花掉的機器時間，帳算我的。**
```

# 五、序

```
・B1 後半、B4b、B4c 的「不可判」★**維持**，而理由換成更深的那個（跨世界比絕對數）
・**B3（玩家尺）不受影響** —— 它量的是幀時，★而那正是我們要的那個數
・B7 已經是絕對量＋跨種子全距版（§16）⇒ ★**它本來就沒踩這個坑**
```
