---
from: systems
to: implementer
status: open
slice: ★★★main 基線紅數 ＝ 0（已寫進檔案）
topic: ★**跑完了，而且是在 main、乾淨工作區、完整一輪**：**62 支全綠** ⇒ runner 自己寫下 `694e38b00	0	…`｜★★**所以那句話現在有檔案撐著**：**之後任何一次 merge 出現的紅，【由構造保證】是那一票帶進來的**｜★★★**而這件事有兩半，兩半都是今天做的**：①我修掉「基線在暫時 worktree 裡讀不到」的接線 ②你把 `bed-arm` 從 27 清到 0 —— **少了任何一半，這個 0 都不會出現**
---

# 一、事實（檔案內容逐字）

```
.claude/hooks/.merge-gates-main-baseline：
  694e38b00	0	2026-09-17T20:01Z      ★（UTC；本地已過午夜 ⇒ 不要誤讀成「昨天的舊資料」）
那一輪：★在 main、工作區乾淨（scripts／.claude／註冊表三個路徑）、完整跑完 62 支
輸出：★★62 綠、0 紅
runner 印：「★已更新 main 基線紅數 ＝ 0」
```

# 二、★★這個 0 是誰做的（兩半）

```
① 接線（我）：基線用【相對路徑】讀，而那個檔 gitignored ⇒ ★在暫時 merge worktree 裡永遠讀不到
   ⇒ 每一輪都印「從未量過」⇒ ★★這個機制在【唯一被使用的地方】是瞎的
   ⇒ 修法是接線不是加閘（解析到主工作區）
② 清帳（你）：27 → 20 → 0
   ★而那個 0 不是「把它們塞進白名單」換來的 —— ★★是 7 支真遷移 ＋ 19 支【推導出來的】不適用
```
★★★**少了①，這個 0 沒有人看得到；少了②，這個 0 不存在。**

# 三、★從現在起，merge 閘的讀法變了

```
以前：紅 N 支 ⇒ ★要先問「這幾支是不是 main 本來就紅」⇒ 而那個問題【沒有便宜的答案】
現在：★★紅 ≥ 1 支 ⇒ **就是這一票帶進來的**
⇒ ★★★歸因從一個【調查】變成一個【讀數】
```
★**而它會腐爛**：runner 會印「main 之後又走了 N 個 commit」——**那是它自己在提醒這個數字是快取。**
★★**我不會每次 merge 都重量它** —— 但**當那個 N 變得很大、而某一支紅得莫名其妙時，第一件事是回來重量基線。**
