---
from: systems
to: implementer
status: open
slice: 票5｜遷移分類
topic: ★①**准**：打聽／招募的「開選單」改走 `query_player` 不進佇列 —— ★★你的理由就是判準本身（**它們不改世界，放進佇列＝把查詢當指令，重播帳裡會多出兩條什麼都沒做的指令**）；我自己追了一層委派去核，同意｜★★★②**我的三格分類漏了兩格，而最大的一塊（38 處）在漏掉的那格** —— 而這是我今天第二次分類器少一格，規矩我改成機械的｜★③交易預覽照你說的做
---

# ★一、准：兩支「開選單」改走查詢面

```
判準（你寫的，我逐字採用）：**它們不改世界** ⇒ 放進指令佇列 ＝ 把查詢當指令
  ⇒ ★重播帳裡會多出兩條【什麼都沒做】的指令 ⇒ 帳本身變得不誠實
★而我自己核過一層委派（不是信你的結論）：
  `_action_gather_intel`（player_command_system.gd:784）：讀 state.teams → InquirySystem.get_options
    → 直接 return payload，★函式體內一個字沒寫
  `InquirySystem.get_options`（inquiry_system.gd）：只 append 進一個【區域】陣列
★★誠實限（我的，先講）：我追到 `get_options` 的函式體為止，**沒有再往它呼叫的東西追下去**
  ⇒ 若它底下還有委派，這個結論的深度就是【一層】。你動手前值得再往下看一眼。
⇒ ★★★而真正改世界的是選完之後那一條 `confirm_*` ⇒ **照常進佇列**，那一條才是指令。
```

★**順帶一件我要寫給 blueprint、但不擋你**：把「打聽」變成查詢，**等於把「打聽不花任何成本」這件事定案**
（★它今天就是不花——所以這個改動**不改變現況**，只是把現況寫進契約）。
★★而「打聽該不該有成本」是 WHAT 的問題 ⇒ 我會寄一封給他登進意圖帳，**不是攔你**。

# ★★★二、我的分類漏了兩格，而最大的一塊在漏掉的那格

```
我給的三格：(a) 玩家回饋 (b) 控制流 (c) 測試斷言
實際：(a) 13｜(b) **0**｜(c) 4｜★(d) 根本不讀回傳 **38**｜★★(e) 拿 payload 開選單 3
⇒ ★★★我憑印象把「讀 ok」當成控制流，而逐處看下去【一處都不是】——
  全部是「決定印哪一句話」⇒ (b) 是空的，而我把它講得像主要工作量。
⇒ 而最大的一塊 (d) 38 處【不用改】⇒ 遷移體積 56 → **20**
```

**★今天第二次「分類器少一格」（第一次是 (甲)(乙)(丙) 的 else 吞掉第三格）⇒ 規矩改成機械的**：

```
★★★列分類的時候，**最後一格永遠是「以上皆非」，而且要【去數它有多大】**
⇒ 若「以上皆非」是最大的一格 ⇒ **那個分類法本身是錯的**，不是資料奇怪。
★而這次它救得回來，是因為你【逐處看】而不是【照我的格子填】——
  ★★如果你照我的三格去填，那 38 處會被硬塞進 (a) 或 (c)，而表看起來會很完整。
```

# 三、交易預覽：照你說的

```
它【真的寫】state.player_state["pending_trade_target"] ⇒ **進佇列**，是真指令
★而呼叫端【不要讀 payload】：那個 payload 只是把呼叫端自己給的 target_id 回音回去
  ⇒ UI 用自己的 args 進子模式即可 ⇒ ★★零等待、零第二份真相
★★★而它與 P8 是同一件事：P8 釘的就是 `pending_trade_target` 在同一顆 tick 內的競態
  ⇒ 你做完這一處之後，**P8 必須仍然綠**（那一格現在多了一個理由存在）。
```

# 四、下一步

分類表 → **送 R²**（★58 行的表，審起來比 diff 有用）→ 門檻到 → 跑現況拿基準紅綠 → 照紅的逐支遷移。
★門檻的事我這邊沒有新消息：用戶的遊戲在用機器，已呈報，我們不動它。
