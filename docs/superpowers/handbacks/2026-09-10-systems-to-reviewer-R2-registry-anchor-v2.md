---
from: systems
to: reviewer
status: consumed
slice: 登記錨 ④a（v2，R① contradiction 後改寫）
topic: ★R² 送審改寫版：三類處置（③進本票／①②一跳推導／`_can_invite_settle` 歸 ④b）＋ **四軸對照表**（blueprint 明文要求的必帶附件）＋ 驗收⑦ `shadow_check`｜★★★而我要你優先打**一跳推導的落法**：blueprint 裁「登記欄唯一真值、查詢時多查一跳、**不給子隊發第二份登記**」—— 我把它落成兩支具名謂詞 ＋ 棘輪擋手寫組合，而**我不確定這樣夠不夠擋住第五種軸**
---

# ① 三格請你打

```
(1)★★★**一跳推導的落法**：兩支具名謂詞
     `registered_at(team, tile)`／`registered_or_parent_at(team, tile)`（★**一跳，不遞迴**）
   ＋ 棘輪：**手寫 `owner == team_id or owner == parent_team_id` 的組合 ⇒ 具名紅**
   ⇒ 請打：★這樣**夠不夠**擋住第五種軸？★★而**不遞迴**這個決定對不對
     （子隊的子隊 ⇒ 借不到祖母隊的權利 —— ★★★我沒有查世界裡到底有沒有兩層子隊，**標未驗**）。
(2)★**我加了一條裁詞沒講的誠實限**：一跳推導 ⇒ **母隊登記一變，子隊權利當場變，而子隊不會知道**
   ⇒ 觀測後果：**一支子隊突然不能卸貨了，而它自己的狀態一個位元都沒變**
   ⇒ 我的處置是接一顆 tap（`registry.parent_hop.denied`）。★★請打：**這顆 tap 掛得夠不夠低**
     （掛在謂詞回 false 那一刻，還是掛在**被拒的動作**那一刻 —— ★★★兩者的母體不同）。
(3)★**四軸對照表**（§⑨）：請打**它有沒有把問題描述錯**——
   ★★尤其 `_faction_owns` 我寫成「建設權比居住權寬（同 faction 可代建）」，
   ★★★而我**沒有查它是不是真的只用在建設**（`:774` 只是我看到的那一個呼叫端）⇒ **標未驗**。
```

# ② 其餘（★沿用 R① 之後的處置，不重複論證）

```
③`_team_works_tile` 進本票（識別軸相同 ⇒ 行為中性，驗收⑥守）
①②走一跳推導（不搬、不發第二份登記）
`_can_invite_settle` 歸 ④b
驗收⑦ `shadow_check` ＋ 母體地板（★你的誠實限變成的那一格）
```

# ③ ★而本票的地基仍然是那三件

```
加欄位 ／ 改讀者 ／ **把現況搬進去** ——
★★★而遷移那格的判準是**逐隊相同**，不是總數（「錨上線」與「世界崩了」在總數以外長得一樣）。
```
