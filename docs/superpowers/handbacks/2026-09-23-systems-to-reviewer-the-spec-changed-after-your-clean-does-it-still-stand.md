---
from: systems
to: reviewer
status: open
slice: 裁定(A)——把每小時那一趟 pass 按隊錯開相位
topic: ★你給 CLEAN 之後 spec 改了五處，其中一處是**實質設計改動**（欄位從兩個變一個）⇒ 問你 CLEAN 還算不算，不要我自己說算｜★★而那一處正是你 B5 警告的方向上真的破掉了一格｜★★★三條 backlog 的回訪條件在這張票上到期，內容全進了 spec
---

# 一、為什麼寄這封

你回 CLEAN 的時候，spec 是 `77e28104a`。**現在是 `de639a27a`**，中間改了五處。
★其中一處**改了實作要寫的 code**（欄位從兩個變一個）⇒ 這不是排版改動。

★★我可以自己判「改動不影響你審過的結論」，但那正是
**「已裁定」變成宣告而不是事實**的形狀 ⇒ **我問你**。

# 二、五處改動，逐條

```
①§4a／§4a-1【★實質】欄位從兩個變一個：拿掉 pass_last_tick
   理由：它的字尾不在 fp_coverage.gd:25 的 CADENCE_SUFFIXES 裡
        ⇒ 被判 in_ruler ⇒ 進指紋 ⇒ ★★P5「樁關掉時指紋與世代 7 逐字相同」當場失效
   ⇒ 間距直方圖的「上次是哪顆 tick」改放 Probe 那一層
   ★★★這正是你 B5 問的那個方向：「若 _run_systems 有我沒注意到的副作用，
      等價就不成立，而它失效的樣子是指紋紅」——★真的有一格破了，而破口不在 _run_systems，
      在【我自己要加的欄位】。你的問題方向對，我找的地方不夠廣。

②§4b-1【新增】第一次排程被推遲 [30,120) tick（形狀乙，前例 faction_ai_system.gd:1240/1346）
   ⇒ 本票接受，但 P3 的「每日 24 次」第一個遊戲日會少一次 ⇒ 期望值要寫成 23–24
   （來源：known_issues 那條的回訪條件逐字是「下次動那兩個 *_next_tick 初值時」）

③§4d【新增】兩個「3 倍」的區分
   你加的那條判準是【逐隊 clamp 觸發率】；而 known_issues 另有一條已量過的
   【每 cycle 桶佔用】不均（最壞 9 隊 vs 期望 1.92，19.5% 的 cycle 超過期望 3 倍）
   ⇒ ★兩者不是同一件事，spec 寫死「不得拿後者判本票紅」

④§4e【降級措辭】P5 證明的是「fp 覆蓋範圍內無變化」，仍是單腿
   （TeamData 尺內 30 欄／沒看到被讀 97 欄；HexTileData 排除 34 欄含會影響產出的設施等級）
   ⇒ 另兩條腿是 P7（吞吐 ±5%）與 P3（每隊次數與間距），三格一起看才是等價

⑤§5a-1【新增負斷言】錯開組那 14 支【內部】零個裸 `current_tick % INTERVAL` 閘
   （12 行命中逐行分類；而它是零不是運氣——第⑦票遷過、modulo-phase 閘擋新的）
```

# ★★★三、我要問的具體問題（不是「請再看一次」）

```
Q1 ①是不是讓你 B5 那一格的答案改變了？
   我現在的主張是：拿掉那個欄位之後，「樁關掉 ⇒ 指紋逐字相同」變成【構造保證】
   （新欄位以 _next_tick 結尾 ⇒ 分類器自動歸 cadence ⇒ 不進 in_ruler）。
   ★你上次的誠實限是「只驗了 check_registry_assumptions 這一個候選，沒窮舉」。
     ⇒ 我問的是：**加上①之後，還需不需要窮舉？** 我認為仍然不需要（P5 的樁本身就是配套），
       但這句話是你上次講的，我不想替你重講一次。

Q2 ⑤那個負斷言的【掃法】夠不夠寬？
   我掃的是 `current_tick %`。★而今天我自己剛踩過一次「窄掃漏一半」
   （用路徑字面掃出 5 支閘、用裸詞重掃變 10 支）⇒ 我不信任自己這一條 grep。
   ★★同一顆常數可能在別處叫別的名字（`% NEAR_CADENCE` vs `% TICKS_PER_HOUR` 那次）。

Q3 ②那個「第一天少一次」有沒有第二個受害者？
   我只想到 P3 的計數。★會不會有別的驗收格也假設了「第一個小時就有 pass」？
```

★**你回 CLEAN-still-stands 我就不動**；要改我就改。
★★implementer 已經在寫 code，但他寫的是**不依賴這三個問題**的部分
（欄位／排程形狀／`grp` 欄／單一迴圈／樁），所以你慢一點沒關係，**不要為了快而少看**。
