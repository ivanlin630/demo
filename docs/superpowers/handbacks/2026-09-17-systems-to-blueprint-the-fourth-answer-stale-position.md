---
from: systems
to: blueprint
status: open
slice: 絕境 ｜ **第四種答案：它們知道獵物是誰，但【不知道它在哪】**
topic: ★★★**最大宗是 `no_belief_pos` 6/10（60%）** —— **而我事前寫死的三種（`too_far`／`no_path`／`out_of_sight`）沒有一種中**（`too_far` 只有 3 筆、`no_path` 與 `out_of_sight` 皆 0）｜★★**而我查出了它的機制，它是一個【兩道門標準不一致】**：`has_belief` **只看有沒有 claim、不看新鮮度**；而 `belief_pos` **超過 3 天就回「未知」** ⇒ **候選者先被當成「有情報」收進來，再被當成「不知道在哪」丟掉**｜★**這也解釋了為什麼 `has_belief=false` ＝ 0 筆**：**那道門擋不住任何東西**｜★**已開票**（未裁，因為它牽動 `attack_scan` 同形的門）
---

# ① 完整的圖（★現在到底了）
```
**22 筆「餓且有牙」的機會**
  ├─ **16 筆沒上場**
  │    ├─ **10 到不了** ← ★**6 不知道在哪（位置情報過期）**／3 太遠（eta 0.95–3.21 天）／1 太快
  │    ├─  5 不夠弱
  │    └─  1 沒發現
  └─ ** 6 筆上場** ← 全部 `take < 0.1`（目標太窮）
⇒ **動手 0 次**
```
★★**所以「絕境者不動手」的最大單一原因是【位置情報過期】** —— **而不是不想、不敢、或打不過。**

# ② ★★★機制（我核過 file:line）
```
`belief_system.gd:106 has_belief`  ⇒ **只看 `claims` 非空** —— ★**不看多舊**
`belief_system.gd:123-141 belief_pos` ⇒ **`now − last_tick > BELIEF_STALE_TICKS`（＝ 3 天）⇒ 回 (-1,-1)**
⇒ ★**兩道門對「有沒有情報」用了兩個不同的標準**
⇒ ★★**而絕境的隊【不移動也不偵查】** ⇒ **它的位置情報必然會過期** ——
  **於是它永遠停在「我知道誰弱，但我不知道它在哪」。**
```
★★★**這是一個【自我強化】的形狀，而它不是我先前猜的那個**：
我猜的是「餓 ⇒ 走不遠」；**真的是「餓 ⇒ 不出門 ⇒ 情報過期 ⇒ 連目標都找不到」。**

# ③ 我開了票，但**沒有裁**
`prey-gate-belief-freshness-mismatch` —— ★**因為對齊到哪一個標準是設計選擇**：
```
**兩道門都看新鮮度** ⇒ 候選集更小、但誠實（不知道在哪就不是候選）
**兩道門都不看**     ⇒ 隊會朝【它記得的舊位置】出發（★**那其實是有戲的**：撲空、追丟）
**第二道改讀 `belief_pos != -1`** ⇒ 最小改動，語意最一致
⇒ ★★**三種都自洽，而它們產生的世界不一樣** ⇒ **你的格。**
```
★**而它牽動 `attack_scan` 同形的門** —— **所以裁的時候要一起裁，不要只改掠奪那一邊。**
