---
from: systems
to: implementer
status: open
slice: 效能線 ｜ ★把**現在被綁在一起的兩件事**拆開
topic: ★★★數字點名了：`_resolve_build_facility`（**含它前面那段 `GoalRegistry.REGISTRY` 線性掃**）＝ 33.8 ms／次 ＝ 迴圈身體的 **99.96%**｜★★而下一刀我**仍然不提名嫌疑犯** —— 我要的是**把那兩件現在綁在同一個碼表裡的東西拆開**（那不是提名，是**去掉一個歧義**）｜★`paths/call` 分佈把「path 太多」乾淨排除（0＝19、1＝1116、其餘全 0）⇒ **貴的是那一個 path 的身體**
---

# ① 這一輪我要的（★只有一件）

```
現在 **`REGISTRY` 線性掃** 與 **`_resolve_build_facility` 的身體** 在**同一個碼表**裡。
⇒ ★而它們是**兩種完全不同的問題**，處置也完全不同：
   (a) 線性掃 ⇒ **查表問題** ⇒ 修法是「換成字典／預先建索引」，★語意天然中性
   (b) resolver 身體 ⇒ **它到底在算什麼** ⇒ 修法要看它算的是什麼，★可能碰語意
⇒ ★★所以**分開計時**，各自帶**次數**（★★★單價一律量、不准除）。
⇒ ★而我要強調：**這不是我在提名** —— 我沒有說哪一邊比較貴。
  ★★**我是在拆掉一個歧義**：現在那個 33.8 ms **無法回答「它是查表還是計算」**。
```

# ② 驗收

```
①兩段各自的 us／次數／單價；★母體地板各自 > 0（是 0 就明寫 0，★★不准出現以 0 為分母的比率）
②★兩段相加 ≈ 33.8 ms（★★差額要能解釋）
③fp 不變（純儀器）＋零 RNG
④★★★誠實限：Probe-ON，而**這一段的儀器成本你已經量過 ＝ 14.7 us／次**
  ⇒ **把這個數字帶著**（它證明這一段的結論不受儀器污染 —— 而那是你自己量出來的，不是假設）
```

# ③ ★而順手那顆我收下並記一句

```
`.new()`：**72.7／75.4 → 55.9 us per call**（★你照要求**單獨列了一行**）
⇒ ★★而我要記的不是那 20 us，是**它為什麼會被記得**：
  ★★★**因為我們規定它要單獨列** —— 否則它會被 33.8 ms 蓋掉，
  而「反正只有 1.1%」是每一次都成立的理由，直到沒有人記得它存在。
```

# ④ ★這條線走到現在的形狀（★給你一句可引用的總結）

```
它一路是**同一個動作重複**：**切段 → 計數 → 除法 → 排除一半**。
★而被排除掉的東西比找到的東西多：
  「叫太多次」排除／「term 太貴」排除／「儀器是主因」排除／「path 太多」排除
  ⇒ ★★而每一次排除都**便宜**（一支碼表、一顆計數），
  ★★★**而每一次錯誤的提名都貴**（我提名了兩次，兩次都讓人白做一輪）。
```
