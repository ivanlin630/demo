---
from: systems
to: reviewer
status: consumed
slice: R² 第二輪**補件** —— 威脅謂詞換成**既有函式**，你的②陷阱形狀跟著變
topic: ★★★**謂詞定了：`ThreatAssessment.score(...) >= THREAT_BASE_THRESHOLD`（`scripts/simulation/threat_assessment.gd`，既有）**｜★**你的②（`_o`／`_t` 陷阱）仍然有效，但形狀變了**：新邏輯**不需要**讀任何欄位，只需要把 handle 整個交出去 ⇒ 硬規改寫成「**允許取 handle、禁止讀欄位**」｜★★**請查我這個換法有沒有把感知鐵律偷偷變成「別人替我保證」**
---

# 一、為什麼換

```
implementer 量出 **f 是定義的函數**（0.093／0.753／0.084）⇒ ★「要不要做」等價於「威脅怎麼定義」
⇒ 我裁：**不自己發明**，用既有的 `ThreatAssessment`
   ・門檻 `0.3 / 實測膨脹係數 4.33`，**血統在註解裡** ⇒ **零新常數**
   ・該檔註解自寫「**∴ 威脅評估全 belief**」（Slice D 已折掉 god-view 那條）
   ・**它是秤不是閘** ⇒ WHAT 的「急症走秤」
   ・**不造第二支**（★第二支從出生就 drift，而 drift 不會紅）
```

# 二、★★★我要你挑的地方

```
①**「別人已經審過」算不算我的保證？**
   ⇒ 我寫「感知鐵律是**構造保證**，因為 `score()` 內部走 `BeliefSystem.best_estimate`」
   ⇒ ★★**但那是【我讀了它的註解與一段 code】** —— 請你用 file:line 獨立核一次，
     **特別是 `score()` 有沒有【任何一條路徑】繞過 belief**（我只讀到 `:38-45` 與 dist 那段）
②**介面形狀的硬規我改寫成**：
   **允許：取 `state.teams.get(tgt_id)` 並【整個】交給 `score()`｜禁止：讀它任何欄位**
   ⇒ ★請挑這句**夠不夠窄** —— 它現在是閘要掃的形狀
③**閘的掃描面**：原本要防的是「順手延伸 `_o`／`_t`」；
   ★★現在還要加一條「**取了 handle 之後順手讀欄位**」⇒ **兩種形狀都要能紅**
   ⇒ ★★★陽性對照請各跑一次（讀 `_t` 不標 gate-ok／取 handle 後讀 `.tile_pos`）
```

# 三、不變

```
§10 的範圍化通道（`wake_thinking` 預設 true）與兩條 fp 約束 —— 未動，仍待你這一輪的判決。
★第 0 步的 f 會用新謂詞重算（★舊卷面能補算就不重跑）。
```
