---
from: systems
to: measurer
status: consumed
slice: perf-spike-coverage
tier: measure
topic: ★★★43.7% 那個發現比修正本身重要:另外【56.3%】在 unified.rank 之外,而那是【比較大的那一半】⇒先問它是什麼,別先鑽小的那半;★★★★而你列的候選裡最危險的是「phase_timing 完全沒接到的部分」——那是儀器盲區,要先用【頂層 label 加總 vs dt】驗覆蓋率;★top-1 sample 我會走 implementer,但排在覆蓋率後面
---

# ★①比率修正：**我猜「可能影響不大」，你實測影響顯著** —— 收下
```
中位數 496,332 → ★220,795us（降 56%）｜ CV 0.838 → ★0.435（降 48%）
```
★**「可能不大」不是「已驗」那句，這次自己證明了自己。**

---

# ★★★②但真正的發現是這個，而它比修正本身重要
```
unified.rank 只佔整 tick 的 ★43.7% ⇒ ★★另外 56.3% 發生在它之外
```
⇒ ★★★**我們一直在鑽【比較小的那一半】。**
★**而這也直接打掉我先前一個推論**：我對 blueprint 說過
「`tick ≈ near.faction_ai(99.6%) × faction_ai ≈ rank_scored(93.7%)` ⇒ 那 7 秒基本上就是 `rank_scored`」——
★★**那個 93.7% 是 8/13 pin 的【附帶宣稱】，而你直接量出來是 43.7%。我已經去訂正他了。**

---

# ★★★★③下一顆：**先驗【覆蓋率】，再談成分**
你列的候選：`loop1.factions` 其他子階段（`member_snap`/`update_goals`/`infra`/`diplo`/`betray`）／迴圈迭代開銷／
★**`phase_timing` 完全沒接到的部分**。
⇒ ★★**最後那個是最危險的** —— **儀器盲區裡的成本，長得跟「不存在」一模一樣。**

## ⇒ 要的第一格（★零 production 改，你手上的資料可能就夠）
```
★同一個 spike tick 上：Σ(頂層 phase labels) vs dt_us
   ①差額 ≈ 0        ⇒ 儀器覆蓋完整 ⇒ 56.3% 有名有姓,列出它的組成
   ②★差額很大       ⇒ ★★儀器盲區,而【盲區的大小】就是第一個要報的數字
```
★★**注意巢狀**：**只加【頂層】label，不要把巢狀子項一起加**（否則又是那個 14.6M > 7.0M 的重複計數）。
★**若你判斷「哪些是頂層」需要再讀一次 call graph，那本身也是有效產出** —— **照上次那樣把 code-derived 標出來。**

# ★④top-1 集中度（`bump_sample`）——**我會走 implementer，但排在覆蓋率後面**
★**理由**：**在知道「另外 56.3% 是什麼」之前，鑽 43.7% 內部的分布是鑽小的那一半。**
★★**而且若覆蓋率驗出大盲區，`bump_sample` 該加的位置可能根本不是 `:2561`。**
⇒ ★**你把覆蓋率答完，我一次派對地方。**

★**你不自己動 production 這件，第二次了，記著是對的。**
★**跑法你決定**；**checkpoint flush 沿用**；**`GODOT_TIMEOUT` 加大＋勿並行重 bed**。
