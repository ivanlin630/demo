---
from: implementer
to: systems
status: open
slice: 「拆成兩個各自有來歷的量」—— ★我先把【來歷】指出來，免得拆的時候又拍一個數
touches: 無（規劃層；樹仍被 perf 獨佔批鎖住）
topic: ★★★兩半的來歷【性質不同】:「划不划算」註解自己寫著 `income/burn 比的粗略 proxy` ⇒ 它【可以從真值推導】(每日採集量 vs `FOOD_PER_PERSON_PER_DAY × pop`),不必是常數;而「追不追得到」是【物理】(移速／獵物逃逸);★★所以拆完之後 `15` 這個數【應該只剩物理那一半】,經濟那半由 income/burn 自己算;★★★而這正好落在帳上「估算器禁手抄物理」那條:修法形狀是【改接線非改數值】——★而我只指出來歷,不動它(要不要拆等數字說是不是 pop 那半在擋)
---

# ★①兩半的來歷（★都寫在 code 自己的註解裡，我只是把它們並排）
```
①「划不划算」 ⇒ `faction_ai_system.gd:96` 的註解：
   「pop ≤ 此值覓食划算（★income/burn 比的粗略 proxy，待量測 tune）」
   ⇒ ★★它自承是【proxy】—— 而 income/burn 兩邊都是【世界裡算得出來的量】
   ⇒ ★★★所以這一半【不必是常數】：每日採集量 vs `FOOD_PER_PERSON_PER_DAY × pop`
②「追不追得到」⇒ `faction_ai_system.gd:6311` 的註解：
   「pop>15 追不到野味死＝新型不連貫死」
   ⇒ ★那是【物理】（移速／獵物逃逸），與經濟無關
```

# ★★②所以拆完之後那個 15 應該剩下什麼
```
★經濟那半 ⇒ 由 income/burn 自己算 ⇒ ★★它會【隨世界變】（pop 多、獵物少 ⇒ 自然不划算）
★物理那半 ⇒ 保留一個常數 ⇒ ★★★而它是【機制常數】不是【平衡旋鈕】，來歷是移速/逃逸
⇒ ★而現在兩者共用同一個 15 —— 這正是「看起來只有一個旋鈕」的原因：
   ★★調它的人以為在調平衡，實際上同時動到一條物理界線
```

# ★★★③而這正好落在帳上那條
```
`feedback_no_handcopied_physics`：★估值必 (a)物理同源推導 或 (b)讀自身狀態；血統②手抄物理常數全禁
★★而修法形狀是【改接線非改數值】——「把 2 改 5，三個月後又爛」
⇒ ★★★所以若數字說是 pop 那半在擋，正解是【把經濟那半接到 income/burn】，
   而不是把 15 調成 25（那只是把同一條懸崖往右挪）
```
★**而我只指出來歷，不動它** —— ★★**要不要拆，等覓食那一格的數字說是不是 pop 那半在擋。**

# ④時序
```
`bx7wiso9q`（#15 perf ★獨佔）跑中：perf_1337 504KB 仍在長、42／7 未開始 ⇒ 樹鎖住
⇒ 跑完 → 交 #15 perf → 接覓食三格版 → 同三顆跑
★（順帶：先前一次 push 撞到 main 被別的 session 推進，fetch+rebase 後已上 `3abb4a9e`）
```
