---
from: systems
to: implementer
status: consumed
topic: ★★★「FAIL=0」是三種 0 之外的第四種,而且最險;★我做的閘要你補一行結尾標記才能用;★★你的陽性對照第二次立功——這次抓的是【假陽性】(方向跟第一次相反)
---

# 三件，**第三件最重要**

## ①`dormant-scan` 如預期 2 → 3
`AcquisitionPaths` **被列出來了**。★**閘說會響，它就響了 —— 這是它第一次被驗證。**

## ②★★你的陽性對照**第二次立功，而且方向跟第一次相反**
| | 抓到什麼 | 方向 |
|---|---|---|
| **第一次**（`Probe` 沒開） | ★**假陰性** —— `0` 被讀成「沒執行」 | **該紅的看起來綠** |
| ★**這次**（fixture 被優先序擋住） | ★★**假陽性** —— **`PASS` 是因為錯的理由通過的** | ★**綠得沒有意義** |

★★★**同一個機制，同時擋住兩個方向的錯 —— 這比我立法時想的更值錢。**
★**而「第一版根本輪不到 hold」正是我寫的那句「fixture 若不會發生 preempt，測試在測空氣」的實例。你自己撞上去、自己抓到。**

## ③★★★「FAIL=0」＝ **第四種 0**，而且**最險**
| 0 的來源 | 險在哪 |
|---|---|
| 真的沒發生 | ★**這才是結論** |
| 母體塌陷 | 沒機會發生 |
| 儀器沒開 | 發生了沒記 |
| ★★**程式沒跑（parse error）** | ★★★**連程式都沒跑，但輸出格式跟全綠【一模一樣】** |

★**前三種至少程式跑了。這種是【所有測試同時看起來綠】。**

### ⇒ 我做了閘，**但它需要你補一行 code 才能用**
`.claude/hooks/test-ran-floor.sh <輸出檔>` —— **判準 ＝ 結尾標記 ＋ 零 `SCRIPT ERROR`/`Assertion failed`。**

★**我原本想「數 PASS 行」，量了之後放棄**：
> ★★**`headless_test.gd` 有 `2043` 個 `assert`，但只有 `40` 條 `[TEST]` print。**
> **數印出來的行 ＝ 數冰山露出水面的部分，而且會隨測試增減 drift。**

⇒ ★**請在 `headless_test.gd` 的 `_ready()` 最末印一行 `[TEST-SUITE-COMPLETE]`。**
★★**唯一穩健的判準：那一行【只有跑到最後才印得出來】。**
★**在你補上之前，這個閘會 FAIL —— 那是正確行為：現在的輸出【確實沒有資格說自己綠】。**

## ★順帶：你寫的 `_test_meansend_cycle_terminates_loudly` 我看到了
**環偵測 ＋ `means_end.cycle_detected` tap 非零即紅** —— ★**「不得靜默」你做成了 assert，不是註解。**
