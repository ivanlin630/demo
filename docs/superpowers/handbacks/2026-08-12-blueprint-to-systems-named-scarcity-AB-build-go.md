---
from: blueprint
to: systems
status: consumed
topic: "[R①硬數據+R² CLEAN→named-scarcity出口A+B spec LOCKED→build GO·spec=docs/superpowers/specs/2026-08-12-named-scarcity-relief-AB-design.md·★底層已banked=接線為主低風險:promotion(_try_promote_advisor:1696/promote_util:1667/PROMOTE_THRESHOLD:1661/test已有)+train→tier-up鏈(training_system:30 while try_promote/tierup_chain_e2e_bed已有)build前已wired·★build兩接線:B=訓練util取代ambient_train_drive flat 0.5(decision_context:442 # TEST VALUE)→接officer-need訊號(named-scarcity缺記名+想派沒人、可含threat)、bounded(officer夠→util低不練、machine demonstrate非always-train)·A=promotion門檻desperation-relative relax(現絕對0.3、真絕境:急需+無夠格候選+無時間→放寬拔平民)、平民弱officer=既有_apply_promotion_skills依src_tier天然結果(平民skill空、不額外做)、bounded只真絕境非逢缺必補·★§4.5必查(同iii順序gate):自平衡硬量測=A只絕境fire弱officer+B首選(缺才練officer夠不練)+平衡從真代價湧現、非文字·命門:所有util非死常數(用戶定)、genuine非crank·量測§5:B端到端(缺officer真訓→tier-up→promote好officer+officer夠不練bounded)+A(絕境fire弱officer+非絕境不fire)+named-scarcity前後紓解genuine非玩壞+硬數據禁預設5×教訓+determinism/regression/constitution·序:build→QA→merge→前後對照→我推用戶·★收官必回blueprint一封(誤看停工教訓)·地基KEEP"
---

# named-scarcity 出口 A+B spec LOCKED → build GO

R①硬數據 + R² CLEAN。★底層 promotion + tier-up 鏈**已 banked** = **接線為主、低風險**。

## build（兩接線）
- **B**：訓練 util 取代 `ambient_train_drive` flat 0.5（`decision_context:442 # TEST VALUE`）→ 接 **officer-need 訊號**（named-scarcity 缺記名 + 想派沒人、可含 threat）。**bounded**（officer 夠→util 低不練、machine demonstrate 非 always-train）。
- **A**：promotion 門檻 **desperation-relative relax**（現絕對 0.3、真絕境→放寬拔平民）。平民弱 officer = 既有 `_apply_promotion_skills` 依 src_tier 天然結果（平民 skill 空、不額外做）。**bounded 只真絕境非逢缺必補**。

## ★§4.5 必查（同 iii 順序 gate）
自平衡**硬量測**：A 只絕境 fire 弱 officer + B 首選（缺才練/officer 夠不練）+ 平衡從真代價湧現、非文字。

命門：所有 util 非死常數（用戶定）、genuine 非 crank。量測 §5：B 端到端 + A + named-scarcity 前後紓解 genuine 非玩壞 + 硬數據禁預設（5× 教訓）+ determinism/regression/constitution。

序：build → QA → merge → 前後對照 → 我推用戶。**★收官必回 blueprint 一封**。地基 KEEP。
