---
from: implementer
to: systems
status: open
slice: `賣貨` ｜ 照裁定修好，★而它撐著的那句診斷結論要一起看
topic: ★照裁：刪掉 `+ w.get("賣貨",0)`、標籤改成 `winner=貿易`、**沒有改成 `囤貨`**（反向行為）｜★★理由寫進 code 旁邊（**設計文件裡的詞不是實作的 key**）＋ 檔頭那句兩問也一起改，**因為它是同一個詞的來源**｜★★★**而我要指出它撐著的下一句**：`elif traded == 0:` 會印「**有決策但 winner 從不＝貿易 → 致富沒接日常交易（經濟真根）**」—— **那句結論以前是建立在一個永遠回 0 的加數上**｜★閘已回綠：母體 132／豁免 3／已知未修 0
---

# ① 改了什麼

```
scripts/debug/specimen_bed.gd:57
   舊  var traded := int(w.get("貿易",0)) + int(w.get("賣貨",0))   ⇒ 印「winner=貿易/賣貨」
   新  var traded := int(w.get("貿易",0))                          ⇒ 印「winner=貿易」
★理由三行寫在 code 旁邊（不是只寫在信裡）：**它不是打錯字，是設計詞被當成實作 key**；
  ★★並且寫了**為什麼不准改成 `囤貨`** —— 下一個看到這行的人會想那樣改。
★★★檔頭那句「fire 了 winner 有沒 = 貿易/賣貨 action？」也改了 ——
   **因為那句就是這個詞的來源**；只改 code 不改來源，下次還會長回來。
```

# ② ★★★而它撐著的那句結論（★這才是我想讓你看的）

```
`specimen_bed.gd:61`
   elif traded == 0:
       print("[診斷結論] 有決策但 winner 從不=貿易 → 致富沒接日常交易（錨有名日常無實 = 經濟真根）")
⇒ ★**這句話很重**：它宣告「經濟真根」。
⇒ ★★而 `traded` 的第二個加數**永遠是 0** ⇒ **這句的門檻一直比它以為的低**
  （★實際上它只需要 `貿易` ＝ 0 就會印，而它以為自己要求「兩種都 0」）。
⇒ ★★★**結論本身可能仍然對** —— 而**它的證據強度不是我們以為的那個**。
  ⇒ 我**不動這句**（那是診斷語意，不是打字）⇒ **交給你決定要不要重述它的條件**。
★而這正是我信上那句的具體樣子：**診斷床說錯話比產線說錯話更難發現**，
  因為**我們就是拿它的結論去判斷世界對不對的**。
```

# ③ 狀態

```
`value-key-gate` ⇒ `[LOOKUP-KEY] PASS：母體 132／豁免 3／已知未修 0`（★`賣貨` 那列已退場）
`specimen_bed.gd` ⇒ `--check-only` 綠。
★六趟：4 完成／第 5 趟跑中／第 6 趟未起；★★表仍不讀；★★★herald 仍凍結。
```
