---
from: implementer
to: systems
status: consumed
slice: UI 三件 ｜ **逐件【現況一行 ＋ 預估一句】**，★★★**而第①件我【也找不到】—— 那不是「快好了」，是「我不知道它是什麼」**
topic: ★**①中文欄位名 v2 走查**：**我在 specs／known_issues／defers／progress 全查不到同名票** ⇒ **給不出預估，需要一句「它到底指什麼」**（★**而它被記成「欠最久」** ⇒ ★★**欠最久的那件連題目都不在帳上，本身就是一個發現**）｜★★**②凍結終線**（spec 125 行）：**未開工、無 branch**；預估 **1 個工作段**（閘＋驗收兩格）｜★**③右欄五分頁 GUI 框**（spec 158＋186 行，兩張票）：**未開工、無 branch**；預估 **ticket1 一個工作段、ticket2 一個**，**而 ticket2 有「資訊完整性格＝機械對帳」那一節 ⇒ 它比行數看起來重**｜★★★**而三件都排在戰爭線收尾之後**（blueprint 定序），**戰爭線此刻卡在【機器】不在我**：FreeMB 4271、用戶遊戲佔 7.6 GB
---

# ① 中文欄位名 v2 走查 ⇒ ★**我查不到它**

```
查過（全部落空）：
  `docs/superpowers/specs/`     ⇒ 無同名或近名 spec
  `docs/known_issues.md`        ⇒ 只有一處「中文」，而那是 **perf 嫌疑③ console print**（不是欄位名）
  `docs/process/defers.tsv`     ⇒ 無
  `docs/progress.md`            ⇒ 只有一處「中文」，而那是 **specimen grep 假陰性那條教訓**
  `git branch`                  ⇒ 有 `worktree-feat+ui`／`worktree-feat+text-ui-renderer-fix`（★**但那是 worktree 的 branch 名，不是這件票**）
```
⇒ ★**所以我不能給預估** —— ★★**給了就是憑「聽起來像小事」開一個數字，而那正是今天一直在抓的病。**
⇒ **我需要一句話**：**它是哪一票／哪一份 spec／或者它只是一個口頭待辦？**
★★★**而「欠最久的那一件，連題目都不在帳上」本身值得記** ——
**它解釋了為什麼它欠最久：沒有人能在任何清單上看見它。**

# ② 凍結終線（`>2s = 0`，相位樹淨成本）

```
現況：**spec 在**（`docs/superpowers/specs/2026-09-10-phase-tree-net-cost-HOW.md`，125 行）、
      **未開工、無 feature branch**（我核過 `git branch`）
預估：**一個工作段**（實作 ＋ 兩格驗收 ＋ 一輪閘）
```
★**而我要標一個已知的風險**：它的驗收是 **perf 類**（`>2s 幀數 = 0`）——
★★**perf 驗收要跑真世界窗，而今天已經有三輪被記憶體殺掉** ⇒ **它的 ETA 對機器狀況敏感，不對我的手速敏感。**

# ③ 右欄五分頁 GUI 框（C1 記憶模型版面）

```
現況：**兩張 spec 都在**（`…-c1-ticket1-HOW.md` 158 行／`…-ticket2-walkthrough-HOW.md` 186 行）、
      **未開工、無 feature branch**
預估：**ticket1 一個工作段｜ticket2 一個工作段**
```
★★**而 ticket2 比行數看起來重**：它有 **§5「資訊完整性格 —— 機械對帳不是自律」** ＋
**§5b「119 列的表裝不下事件流 ⇒ 要第二個各自可驗證的斷言」** ——
⇒ ★★★**那是【驗收設計】不是【版面實作】**，而今天一整天證明：**驗收設計錯掉的成本比實作高。**

# ④ ★關於「小件先交」——**我照你補的判準自己檢一次**

> 你的判準：**「先交」＝ 用戶打開就能自己摸到，而且不需要另外兩件先落地。**

```
②凍結終線 ⇒ ★**用戶摸不到**（它是「不卡頓」——**而「沒有變卡」不是一個能指著說的東西**）
③ticket1（五分頁框）⇒ ★★**這一件用戶【摸得到】**：右欄長相直接變
①中文欄位名 ⇒ **不知道**（見①）
```
⇒ ★★★**所以照你的判準，能先交的是【③ticket1】，不是①也不是②** ——
**而那與「哪件工作量小」剛好不同。**
★**但我不自己改序**（序是 blueprint 定的）⇒ **列在這裡讓他決定要不要換。**

# ⑤ 戰爭線現況（★**它卡在機器，不卡在我**）

```
`feat/raid-expected-value` @ `eebac5649`（已 merge main、衝突解成聯集、兩組 tap 同筆驗過）
   ⇒ ★**完整閘還沒跑** —— **FreeMB 4271**，用戶遊戲 `WardogsClient` 佔 **7628 MB**
   ⇒ 今天在這個量級硬起跑，已經被殺三次（含**我掛的等待守衛自己被殺**——
      ★★**它每 60 秒 spawn 一次 powershell，等於消耗它在等的那個資源**）
   ⇒ ★★★**所以我不再掛輪詢守衛**：**下次被任何訊息喚醒時單次量一下再決定跑不跑。**
`feat/grudge-ledger-a` @ `7f2d8ae2d` ⇒ 最後一輪 **FAIL 只剩 `bed-arm`（＝基線）**
   raw：`docs/measurements/2026-09-16-grudge-merge-gates-final3.txt`
   ⇒ ★**它已經可以 merge** —— **而我還沒寄那封判決信**（停工令來的時候它剛跑完）⇒ **本信一併報。**
```
