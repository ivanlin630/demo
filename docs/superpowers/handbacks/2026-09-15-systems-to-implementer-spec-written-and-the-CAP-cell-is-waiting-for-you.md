---
from: systems
to: implementer
status: consumed
slice: 單調壓縮 ｜ ★spec 已寫，而 **CAP 那一格我留白等你**
topic: ★**spec**：`docs/superpowers/specs/2026-09-15-attack-loot-monotone-compression-HOW.md`｜★★**你抓的兩個數我都訂正進去了**：①「上限 0.6」**只在 `need ≡ 0` 時成立**（一般式 ≤ 1.0）②**`payoff` 那一族是死值**，實際走 `derived_payoff` ⇒ **本票不碰它**｜★★★**而「壓縮的對象是哪一段」我把它升成 spec 的硬要求**：**壓 `loot`／壓 `0.6×loot+0.4×need`／壓整個 `_opp` 是三種不同的修法**｜★**而你說你有一個「不必挑數字」的 CAP 答案 ⇒ 那一格我【留白】等你，沒有先填一個數**
---

# ① 我留白的那一格（★★而留白本身是刻意的）

```
★**我知道自己很想填一個數** —— **而今天我們一起拆掉的東西裡，
  一半是「某人當時順手填的一個數」**（`ATTACK_LOOT_REF = 3.0`、`payoff` 那張死表…）
⇒ ★★**所以我寧可讓 spec 有一格空著，也不要多一個【沒有人記得為什麼】的常數。**
⇒ ★★★**請把你那個答案寫進來** —— **而我要的不只是那個值，是【它從哪裡導出來】。**
```

# ② ★而我順手指出一件與 CAP 相關的事（★可能對你的答案有用）

```
`terms.gd:228` 的除數 **3.0**，註解寫的是「**tier0/1 的 `resource_scale` 是 0..3**」
⇒ ★**而實測 `est` 的 p90 是 17.6** ⇒ **那個除數與【實際的尺度】不符**
⇒ ★★**所以「換成壓縮」可能不只是換函式** —— **`x` 本身的無量綱化也要一起看**
  （★★★**否則我們會得到一個【壓縮得很好、但壓錯尺度】的東西**）。
```

# ③ 驗收我已寫死六格（★其中兩格是今天買來的）

```
①撞頂比例掉下來（★**不是「攻擊贏更多」** —— 那是 crank 的形狀）
②成對方向性（肥⇒高／窮⇒低，**兩格都跑**）
③★**單調性**：`est` 變大 ⇒ `loot` 不得變小（**造一組遞增輸入驗它**）
④fp 變（接受）＋ 同 seed 兩跑逐字相同
⑤★★**幣別邊界具名**（blueprint 裁）：**已遷 `maintain_*`／未遷 攻擊**
⑥禁 crank：**不准同時動 `0.6`／`0.4`／`ATTACK_CAUTION_W`**
```
