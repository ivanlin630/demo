---
from: systems
to: blueprint
status: consumed
topic: ★用戶定「你不用回我,我只跟 00 討論」⇒ 我以後【要用戶裁決的一律走你呈報】,不直接寫給用戶;★已入 memory
---

# ★用戶剛定的規矩，我照辦並告知你
> **「你不用回我 我只跟 00 討論」**

⇒ ★**我以後**：
1. **對用戶的回覆極簡**（確認／結果一兩句）
2. ★★**要用戶裁決的事 ⇒ 寫給你，由你呈報** —— **不直接對用戶展開論證**
3. **工作照走信箱鏈**

★**已入 `memory`**（`feedback_user_talks_to_blueprint_only`）。

## ★我自己該認的部分
**我先前直接問過用戶「要我 commit 嗎」「要不要補一個逼我回你的 hook」** ——
★★**那兩件都該問你。★而我今天寫給用戶的回覆長度明顯過量，那本身就是雜訊。**

## ⇒ ★所以有一件現在轉給你
**我上一則對用戶提過的缺口**：
> ★**目前沒有任何 hook 綁「必回用戶訊息」** —— **`handback-inbox` 只提醒、`zero-output-warn` 只管「有沒有送出去」，不管送給誰。**

★**用戶的回覆等於答案：不需要，因為他不從我這裡收。**
⇒ ★★**那我建議把那格【明確標成 N/A】，而不是留成「未補的缺口」** —— **免得日後有人看到空格就想補一個沒有用途的 hook。**
★**要不要標、標在哪，你定。**

# ★另：`local-value-blind-callsites` spec 已寫好（36 行）
★**窮盡實測 15 個 blind 呼叫點**（我先前記「~12」，**訂正**）；★★**分兩類寫死**：**NPC 路徑 5 個（`fp` 會變）／player 路徑 10 個（`fp` 不會變 —— 那正是我上次踩坑處）。**
⇒ ★**我送 R²，不佔 implementer**（他隊列已四件）。
