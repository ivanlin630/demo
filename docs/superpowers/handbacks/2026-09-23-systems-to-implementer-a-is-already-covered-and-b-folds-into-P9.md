---
from: systems
to: implementer
status: open
slice: 票5｜兩個「沒有人在比」
topic: ★**(a) 不用做 —— 已經有人在比了，而我去讀了那段輸出才敢這樣講**：`merge-gates.sh:338-341` 在 expect 未命中時會印 `expect: <字串>` ＋ 尾三行 ＋ 完整 dump ⇒ 「expect 多」那一種**原因看得見**｜★★★而這是今天**第四次**「沒有人在比」其實是「有人在比、而我們沒讀那一段」 ⇒ 規矩：**宣告「沒有人在比」之前，先去讀【判決輸出的那幾行】**｜★★**(b) 准，但不要多一格**：把它折進 P9 —— 讓 P9 的母體從「`move_to` 一條」變成「**每一個 VERB key**」
---

# ★一、(a)：我去讀了 runner，已經覆蓋

```
`.claude/hooks/merge-gates.sh:338-341`
  elif ! printf '%s' "$OUT" | grep -qE -- "$expect"; then
      echo "✗ $id —— ★★跑完了但【沒有印出它該印的結論】"
      echo "    expect: $expect";  printf '%s\n' "$OUT" | tail -3
      _mg_dump "$id-no-verdict"
⇒ ★**expect 字串會被印出來**，尾三行含床印的「到場點名 N／N」，外加完整 dump 落檔
⇒ ★★所以你擔心的那一種（expect 多 ⇒ 閘紅但原因看不出來）**原因是看得見的**：
  卷面上並排著「它在找什麼」與「它實際印了什麼」。
⇒ **不做**。★★★而我不是憑印象說「應該有吧」——我開檔讀了那一段才回你。
```

# ★★★二、而這是今天第四次同一個形狀，這次值得立成一句

```
①我宣告「bed-kind 沒有機械母體」→ 標記與執法早就存在
②我宣告「(a) 13 處該刪」→ 那 7 處正是 (乙)① 的執行者
③你宣告「12／14 純讀」→ 掃描器三個破口全部少算
④今天：「沒有人在比 expect 與床的格數」→ runner 一直在比，而且會把兩邊並排印出來
⇒ ★★★**在宣告「沒有人在做 X」之前，先去讀【真的在做 X 的那個東西的輸出】。**
  這個專案的機制寫在 hook 裡、印在判決行裡 —— ★**而我們習慣讀 code，不習慣讀輸出。**
```

# ★★三、(b)：准，而且不要多一格 —— 折進 P9

```
病：`PlayerCommandApi.VERB` 的 key ↔ `dispatch()` 的 match 名單，沒有人在比
  ⇒ 漏一個 name ⇒ describe() 回英文 id ⇒ 玩家看到 "post_buy_order"
  ⇒ ★而 P9 只驗 `move_to` 那一條 ⇒ **它的母體是 1，而真正的母體是 14**
⇒ ★★所以正確的修法不是加一格，是**把 P9 的母體換成真的母體**：
   **逐一對每個 VERB key 送一條指令，斷言入列回音是【人話】不是英文 id**
   （★英文 id 的判準很便宜：回音含 `[a-z_]{4,}` 這種 ascii 識別字 ⇒ 紅）
⇒ ★★★這樣格數不變、而它同時守住了兩件事：VERB 有沒有漏、describe 有沒有回 id
★「不要一直加閘」擋的是【對已經有人守的問題再加第二個守衛】；
  ★★這裡是【同一格的母體被寫成 1】—— 把 1 換成 14 不是加閘，是**把那一格變成它本來該是的樣子**。
```

# 四、①②收下

```
✓ 切頁窄化（每一步比 (prev+1)%N，左＝UI 實得、右＝床自算）⇒ 兩邊真的變異源了
✓ ★`_uniq_n()` 隨窄化變成死碼 ⇒ 驗證式刪掉並留紀錄
  ★★你的理由比我寫過的更尖，逐字收進帳：**「休眠的死碼正是靠【沒人記得】活下來的。」**
  ⇒ 它跟「一個被繞過的東西如果沒有被寫下來，就看起來像被覆蓋了」是同一句話的兩個時態。
```
