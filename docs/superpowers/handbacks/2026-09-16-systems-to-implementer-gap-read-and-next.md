---
from: systems
to: implementer
status: open
slice: 三拉桿 ｜ **收，而我把 gap 表讀成【缺項】並呈給 blueprint**
topic: ★**八格全綠 ＋ 三格同時成立** ⇒ 收｜★★★**我讀你的 gap 表**：②主觀成本 **−1.8%** 不是「係數太小」，是**它天生動不了** —— 掠奪**專打弱者**（`_find_weakest_prey`：`pop_est < 0.7×我方`）⇒ `retaliation_risk` 本來就接近 0 ⇒ **沒有空間可壓** ⇒ **成本側缺的是【道德／名聲代價】，而那一半我在 spec 裡明文標過「不接」**｜★**而它現在有來源了：恩怨帳**（已在飛）⇒ **順序上本來就排在後面**｜★★**你換軸當場弄紅自己的絕境格、並且立刻改掉** —— **那正是對照該有的樣子**
---

# ① ★你那一格「接線前逐字同分」是這一輪最有力的證據
```
接線前：尾部（好戰.9／貪婪.9）與單高（.9/.5）**逐字同分 0.0580**
⇒ ★**因為舊軸是好戰／殘忍 ⇒ 貪婪對掠奪【完全沒有作用】**
⇒ ★★**所以「貪婪的盜匪」在昨天的世界裡【不存在】** —— **不是很少，是不存在。**
★★★**而這一格比「數字變大了」有力得多**：**它證明的是【一個維度從無到有】。**
```

# ② 我不要你現在做的事（★先講死）
```
✗ **不要調係數把 `f = 1500` 那一檔推過去** ——
  ★**那會讓「盜匪存在」變成一個旋鈕的產物**，而不是「搶劫對某些人划算」的結果。
✗ **不要現在接道德／名聲代價** —— ★★**它的來源是恩怨帳，而那條線還沒 merge。**
```

# ③ 下一步（★兩件，順序固定）
```
\u2460 ★**一輪完整閘**（掠奪票 ＋ 本票合併 merge 的那一輪）——
   判準照舊：`[TREE] clean` ／ **橫幅一份** ／ `runner-self` ／ `註冊表 N 支`
   ⇒ **FAIL 只剩 `bed-arm`（＋ `defer-open` 那個鬧鐘）＝ 可 merge**
\u2061 ★★**那兩支常態 fixture 的斷言，現在可以回頭看了** ——
   **「兇者常態會搶」在 `f = 8000` 成立、在 `1500` 不成立**
   ⇒ **斷言要把【身價那一檔】寫成具名條件**，★★★**否則它又是一支「看情境而定」卻沒說是哪個情境的測試。**
```
★**而 blueprint 那一句（8000 才有盜匪、1500 沒有，可不可接受）在飛** ——
**票路徑**：`docs/superpowers/handbacks/2026-09-16-systems-to-blueprint-gap-table-says-missing-term-not-magnitude.md`
