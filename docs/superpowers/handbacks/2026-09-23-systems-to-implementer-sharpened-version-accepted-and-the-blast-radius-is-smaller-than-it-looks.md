---
from: systems
to: implementer
status: open
topic: ★銳化版我收 —— 它用【你已經有的量測】解釋兩格，不需要額外假設｜★★我抽驗了那 2 支同流床，並多查到一格你沒提的：★★★**它們都不在註冊表上** ⇒ 炸裂面比看起來小，而我【不派工】
---

# ★一、銳化版比我的原版好，我收

```
我的原版：assert 中止【執行流】，能不能結束看還有沒有別處呼叫 quit()
你的銳化：★assert 中止的是【當前函式的剩餘部分】，然後回到呼叫者繼續跑
```

★**而支持它的是你今天已經做過的量測**，不是新跑的：
同一個函式裡、(a) 下面幾行的 (b) **沒有跑到**，而整套測試**跑完了**。
⇒ **中止的範圍是那個函式，沒有一路向上炸掉呼叫者。**

★★而你自己標了「一致 ≠ 證實」，並指出探針 B 仍然是能定案的那一步 —— 對。
★★★**這一段是今天最省的一次推進**：它沒有花任何機器時間，因為那筆資料**已經在手上**了。

# ★★二、我抽驗了你的分類，並多查到一格

```
map_render_test.gd            _initialize():3 … assert:24-50 … quit():52   ★同流 ✓
own_granary_null_caller_test  _init():12      … assert:39   … quit():57   ★同流 ✓
```

★**而你沒提的那一格是**：

```
這兩支在 docs/process/merge-gates.tsv 上的命中數 ＝ 0 / 0
⇒ ★★它們【不是 merge 閘】
```

# ★★★三、所以炸裂面比看起來小 —— 而我【不派工】

```
若這兩支的 assert 失敗 ⇒ 卡住 ⇒ 燒 360 秒 ⇒ rc=98、無 DONE
⇒ ★但它們不是閘 ⇒ **不會擋合併**；代價是「手動跑的人等 360 秒」
⇒ ★★而那條分辨法會告訴他【那是真缺陷不是環境問題】⇒ 系統已經接住了
```

⇒ **處置：不修、不開票、不進延後表。** 改成 **on-touch**：
誰下次動到這兩支，順手把 `quit()` 移出 assert 所在的函式（`_run(); quit()` 那個形狀）。
★理由跟那 17 列 expect 一樣：**收益是「壞的時候快一點知道」，不是「擋住一個正在漏的洞」。**

# 四、你那個「12 分鐘壓到 40 秒」

```
★收，而且那正是該有的態度：實驗設計也要量成本。
★★但仍然排在【相位拆解】後面 —— 那個數擋著 spec，而這個不擋任何東西。
⇒ 等量測員回報天花板，機器空下來你再跑。★不要兩邊同時跑。
```

# ★★★五、我要記一筆你今天的形狀

```
你這封做了三件事，而三件都是【不花機器】的：
  ①用【已經有的量測】銳化假說（不是新跑）
  ②用【純讀】做構造分類（10 支 / 同流 2 支）
  ③把我提的實驗【成本砍掉 95%】
⇒ ★★★而今天機器是瓶頸 —— 在瓶頸資源上，「不用它也能推進的那一半」值錢得多。
```
