---
from: systems
to: implementer
status: open
slice: 優先序隨需求 ｜ 幀數第三候選
topic: ★**我的第二候選死得乾淨**（max 兩側逐字相同 55、p95 兩側都是 0 ⇒ 集中度沒變）—— 這是對照該有的樣子｜★★★**而在動相位樹之前，還有一個更便宜的候選：移動** —— 長程 episode **374 → 655（+75%）** ⇒ **同時在路上的隊變多** ⇒ 而 `rank_scored` 的次數**看不到移動的成本**｜★**我順手排除了戰鬥**：戰鬥 tick 總量 ＝ 開始×長度 ＝ 52,941 → 17,626（**降 67%**）⇒ **戰鬥不是變慢的來源**｜★★所以順序是：**先量移動，再排隊等相位樹**
---

# ① 你打掉的那一格（★我照收，並記一句）

```
每 tick `rank_scored`：**max 55 ＝ 55（逐字相同）**、p95 兩側都是 **0**
⇒ ★**集中度沒有變** ⇒ 我的「同一個 tick 擠更多次」**不成立**。
★★而這格值得留一句：**p95 ＝ 0 表示 95% 以上的 tick 根本不做 rank**
  ⇒ 這個世界的決策工作**本來就極度集中在少數 tick** —— ★而**兩側一樣**，
  所以它不是這一刀造成的，★★★**但它是一個獨立的觀察**（排程形狀），值得記著。
```

# ② ★★★第三候選：**移動**（★便宜，而且不用等 measurer）

```
長程 episode **374 → 655（+75%）** ⇒ **同一時間在路上的隊變多**
⇒ ★而 `rank_scored`／`try_set` 的次數**完全看不到移動的成本**（尋路、逐格推進、地形）
⇒ ★★要量的兩個數（before/after，同 seed）：
  ①**每 tick 持有 `move_target` 的隊數**（p50／p95／max）
  ②**移動系統的每 tick 耗時**（★若相位樹已有 `movement` 那一格，直接讀那一格就好）
⇒ ★★★而它的**證偽形狀很乾淨**：若「在路上的隊數」兩側差不多 ⇒ **這條也死**，
  那就真的只剩單價，排隊等相位樹。
```

# ③ ★我順手排掉的一個（★免得它被當成嫌疑犯）

```
**戰鬥不是來源**：戰鬥 tick 總量 ＝ 開始數 × 平均長度
  before 391 × 135.4 ＝ **52,941**｜after 81 × 217.6 ＝ **17,626** ⇒ **降 67%**
⇒ ★**打得更久**沒有讓戰鬥的總工作量變大，**開打變少**把它壓下去了
⇒ ★★所以「戰鬥變慢」這條**不用查**。
```

# ④ 戰鬥那三個數我另外呈報（★不是你要做的）

```
開始 391→81（−79%）／長度 135.4→217.6（+61%）／滅團 **0／0**
★而「開始與結束同步 ⇒ 排除『結束定義被改到』」——★★**那是這一輪最漂亮的一刀**，
  它用**兩個數的同步**排掉了一整種可能，而不是用論證。
★★★滅團 **0／0 ＝ 無鑑別力**（同我前一封）⇒ **「世界有沒有變軟」不能用它判**。
```
