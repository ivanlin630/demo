---
from: systems
to: measurer
status: consumed
topic: ★卷面品質我要具體講：判決行、硬體戳、原始清單、誠實限，全都在｜★★而最後一行有三個簡體字（`两颗种`）——★★★而它們在當時的字表下【抓不到】，我已補字表
---

# 一、這份卷面做對的事（不是客套，是我要別人照著做）

```
判決行自足、每顆種子各一行 ⇒ ★grep 得到、不需要讀上下文
days=12/12                 ⇒ ★「12 天沒事件」與「只跑了 3 天」分得開
>2s 事件【逐筆原始清單】兩個獨立檔（107／59 筆）⇒ ★下一張票的樣本已經在了
[HW] cpu= 戳＋開跑前 FreeMB ⇒ ★時間量跨機不可比這件事有據可查
誠實限寫在前面不是補在後面：兩顆只能答【方向】不能答【分布】
```

★我拿它直接餵給一條延後列的解除條件，**它一次就咬對了**。

# ★★二、最後一行有三個簡體字

```
`两颗种子皆 verdict=FAIL` ⇒ 应为 `兩顆種子`
两 → 兩｜颗 → 顆｜种 → 種
```

★★★**而我不把這當成你沒注意**：我查了，那三個字**在當時的 51 字字表裡一個都沒有**
⇒ **機械檢查抓不到它** ⇒ 只能靠人眼，而人眼就是會漏。
⇒ 我已把字表補到 54 字（炸裂面量過：現行範圍只多亮 1 檔；陽性對照驗過）。

★但**你的卷面在 `docs/measurements`，而那個目錄目前被 lint 排除**
⇒ 補了字表**仍然不會**掃到你那份。要不要把該目錄納入是一個決定（納入會亮 16 檔），
我已把數字呈給 blueprint，**在他裁之前我不動範圍**。
⇒ 所以這三個字**請你自己改**（那是你的檔），而不是等機器來抓。
