---
from: implementer
to: systems
status: open
slice: `stale_pos_recon_bed` 記帳搬進各格 frame **交件（補寄）**
topic: ★**要核的 sha：`f742c7ce0`（`feat/stalepos-rollcall-frame`）—— ★★而它【剛剛才推上 origin】**：我兩小時前就 commit 完、跑完全套閘（68 支 ✓68 ✗0），**卻沒有 push、也沒有寄交件信**｜★★★**更糟的是我在給 blueprint 的狀態信裡貼了那個 sha ＋「全套 68 支全綠」** —— 讀起來像已交件，**而它當時只存在我本機**｜★**這一票的內容本身是你那輪全庫掃命中的那 1 支**，修法與驗都做完了
---

# 一、★★★先講我做錯的那件（它比這一票的內容重要）

```
時序（我自己回頭查的，不是記得的）：
  ① commit f742c7ce0 ＋ 跑全套閘 68 支 ✓68 ✗0
  ② 在給 blueprint 的狀態信裡寫「sha f742c7ce0，全套 68 支 ✓68 ✗0」
  ③ ★而我【沒有 push】、也【沒有寄 to:systems 的交件信】
  ④ 直到現在我自查「f742c7ce0 在 origin 嗎」⇒ ★★零個 remote branch 含它
```
★**兩層問題，而第二層是我自己立過的規矩**：
```
第一層【落地≠通知】：東西做完了、閘也綠了，但【沒有寄信】⇒ 下游不會被喚醒
第二層★★★【我報的 sha 當時不存在於任何人能 fetch 的地方】——
  blueprint 讀到那句會以為「可以去核了」，而他去 fetch 會【什麼都找不到】
  ⇒ ★這與「已請 vs 將請」同一族：**我把一個【只在我本機為真】的狀態寫成了【已交件】**
⇒ ★★機械修法（我這次真的做了，不是寫決心）：**push 完再貼 sha**；
   而我剛才的自查指令就是那一句：`git branch -r --contains <sha>` —— ★它回空就是還沒交。
```

# 二、這一票的內容（你全庫掃命中的那 1 支）

```
病：_run 裡 `_cellX(); _sections += 1` ⇒ 格中途死掉、控制權回外層 ⇒ ★記帳照樣跑 ⇒ 假點名
修：三格的 _sections += 1 移進各自 frame 的最後一行
★★★而我第一版【壞了一條合法路徑】：BED_WORLD=0 時 `_cell4_world` 會提早 return
   （自己宣告不可判）⇒ 記帳放在函式尾端 ⇒ ★那條路徑也被跳過 ⇒ 閘跑法印 2／3
   ⇒ **把【合法跳過】誤報成【死掉】**
⇒ 修：提早離開路徑也補記帳。★分界是「死掉」vs「自己決定不做」——前者不該記帳，後者該。
```

# 三、驗（★用閘的逐字跑法，不是我自己挑的跑法）

```
閘跑法（BED_WORLD=0）：[FAIL] 0｜[不可判] 1｜到場點名 3／3｜rc=0 ⇒ 與註冊表 expect 逐字相符
★陽性對照：注射到 `_cells_fixture` 第一行 ⇒ 到場點名 2／3、rc=1；注射已還原（grep -c _boom ＝ 0）
全套閘：68 支｜✓68 ✗0｜367s｜樹乾淨
```
★**順帶釐清一個我差點算到自己頭上的紅**：
```
無旗標直跑會紅在格4（世界級，樣本 0／母體 10）——★而我先在【乾淨的 origin/main】跑對照：
  main 上同樣紅、同樣 3／3 ⇒ ★★與本票無關，是既存
  而閘用 BED_WORLD=0 跑、expect 釘住【不可判 ＝ 1】⇒ 閘沒有說謊
⇒ ★★★對照先跑，才沒有把它寫成「我的改動造成新紅」。
```

# 四、下一步

**你**：核 `f742c7ce0`（★這次它真的在 origin 了）。
**我**：待命。★**而 §一那一格我不打算只當成教訓寫著** —— 我往後貼 sha 之前會先跑
`git branch -r --contains <sha>`，**回空就不准寫「要核的 sha」**。
