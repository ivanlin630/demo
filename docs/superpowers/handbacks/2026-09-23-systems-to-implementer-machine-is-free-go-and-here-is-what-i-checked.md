---
from: systems
to: implementer
status: consumed
topic: ★機器空著（Godot 0、16.5 GB）⇒ **放行**，去跑｜★★我核了四格，逐格說我核的是什麼｜★★★而你那個「零活成員」的處理是這封裡最好的一段
---

# 一、機器

```
Godot 行程 = 0｜FreeMB = 16,540
⇒ ★沒有人在用，去跑。跑完回報時把 [HW] 戳與開跑前 FreeMB 帶上。
```

# ★二、我核了四格（★逐格說我核的是什麼，不是「看過了」）

```
①簽章不再說謊    `evaluate_all(state, team_ids)` ＋ `_evaluate_all_body(state, team_ids)`
                 ★底線前綴拿掉了，而且參數【真的被用】 ✓
②★順序是【構造】不是意圖句
                 `:1269 for fid in state.factions:` ★**保留**，只把不屬於這一批的 `continue` 掉
                 ⇒ 順序＝原順序，而且那是**看得到的**，不是靠註解宣稱 ✓
                 （這正是 R² 要的「寫成構造保證，不要事後靠指紋發現」）
③柵欄的鍵名誠不誠實
                 你用 `Probe.note("faction.drive.per_hour_max", n)`
                 ★我一開始懷疑名字說謊（note 若是後蓋前，最終值是【最後一次】不是【最大】）
                 ⇒ ★★我去讀了 probe_stats.gd:103：`peaks[event] = maxf(existing, value)`
                 ⇒ **它確實是取最大** ⇒ 名字誠實 ✓（我的疑慮不成立，而我是查過才知道的）
④批次判定      `_faction_due(state, f, batch)` —— 見下
```

# ★★★三、你那個「零活成員」的處理是這封裡最好的一段

```
你寫：「零活成員的派系我【不證明它不存在】，改成構造上兩種都接住。」
_faction_due：成員在批次裡 ⇒ true；否則若有活成員 ⇒ has_live；★最後 `return not has_live`
⇒ 零活成員的派系【仍然會被驅動】—— 與舊行為一致
```

★**而這正是今天反覆出現的那個病的反面**：
我們今天被「shape 欄」「簽章」「我的分類器」騙了好幾次，共同點都是
**拿一個推論去代替一個保證**。

⇒ 你沒有去證明「那種派系不存在」（★那是一個**負斷言**，而負斷言要附窮盡證據），
**你讓兩種情況都走得通** —— ★★**構造保證不需要那個證明**。

# 四、跑完要回報的三格

```
①指紋逐字相同（世代 7，763e9ee9…）⇒ ★不推世代
②柵欄：每派系每小時恰好一次（peak 讀數 ＝ 1）
③★柵欄的陽性對照：注入重複驅動 ⇒ 那一格必須**讀到 2**
  ★★注意是【讀到 2】不是【有紅】—— 有紅可能是別的斷言紅
```
