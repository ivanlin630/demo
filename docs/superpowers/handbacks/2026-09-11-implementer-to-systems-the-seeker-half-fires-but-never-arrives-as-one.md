---
from: implementer
to: systems
status: open
slice: ④b ｜ 兩半分開數（你問的那一端）
topic: ★★★**你問對了端**：鏈是 **求居派出 59 → 真的走到 4（6.8%）→ 領主看到求居者 0**｜★★而領主那 15 次「有人上門」**全是路過的隊**（迎戰×10／生產×3／建設×1／覓食×1）⇒ **「亂世的領主沒空收人」那句是在錯的那一端下的**｜★半路被搶只有 **2 次嘗試**（乞食／覓食）⇒ **preemption 不是主因**｜★>2 秒幀數 **307 / 43200**（照裁定起，每份交件都帶）
---

★落地：`docs/measurements/2026-09-11-seek-half-chain.txt`（commit `8b2a9a869`，main）
  ＋ worktree `.worktrees/converge2` commit `7075d8a70`（五顆純觀測 tap ＋ 終線計數）

# ① 鏈（30 天窗、seed 1337、預設 config 未改）

```
求居【真的被派出】**59** → 【真的走到】**4** → 領主看到**求居者** **0**
領主看到「有人上門」**15** 次，而那 15 次的 task ＝ 迎戰×10／生產×3／建設×1／覓食×1（★沒有一筆是求居）
半路被別的 task 搶（`try_set` 嘗試）＝ 乞食×1、覓食×1 ⇒ ★**只有 2 次** ⇒ 不是被搶走的
```

# ② ★★★兩個斷點（★它們在結果上都長成「領主沒看到求居者」，而處置不同）

```
(A)**派出 → 抵達**：**55／59 沒走到**，而**不是被搶**
   ⇒ ★候選成因：距離太遠／cadence 重評換了 option ⇒ ★★**我還沒量到是哪一種**（明寫）
(B)**抵達即失去身分**：`movement_system._step_team` 抵達時清 `move_target` 並走 `_on_arrival`
   ⇒ ★村主評估的那一刻，它**已經不是 `TASK_SEEK_HOME`**
   ⇒ ★★★**「上門請求」在領主眼中結構上不存在** —— 而收留的 applicable 只看
     「有沒有未登記的 PRODUCE 隊站在這」⇒ 它看到的永遠是**路過的人**。
```

# ③ ★而這改寫了上一封的結論（★我自己撤回一半）

```
上一封我寫：「輸給補給／維持類 ⇒ 領主在忙自己的供給 ⇒ (a) genuine」
⇒ ★**秤本身沒有問題**（組成無壓扁、`fail_mult`＝1）—— 這半仍然成立
⇒ ★★但「領主不收**求居者**」這句**不成立**：**它從來沒有遇到過求居者**
  ⇒ ★★★所以 (a) genuine 的適用範圍要縮小成：**領主不收【路過的人】**，
    而**「領主會不會收一個真的來求居的人」這個問題【還沒有被問過】**。
```

# ④ 下一刀（★兩條，我不自己選）

```
①**修 (B)**：讓「求居」在抵達之後**還看得出來**（例如抵達不清 option／或村主端改看 `current_option`）
   ⇒ ★這是**接線**，不是 crank ⇒ 它會第一次讓「領主遇到求居者」這件事**可能發生**
②**量 (A)**：把 55 次沒走到拆成【太遠】與【換了 option】⇒ ★而那會決定要不要動 cadence／距離門檻
⇒ ★★而我建議 ①先於②（★★★因為 (B) 是**結構上不可能**，而 (A) 只是**機率低**）——但**序由你定**。
```

# ⑤ 誠實限

```
①30 天窗、單一 seed；60 天窗的同一組數字**還沒跑**
②`seek.arrived` 只數【抵達當下仍是 `TASK_SEEK_HOME`】⇒ ★它是**下界**，不是抵達總數
③五顆全是 Probe-gated 純觀測 —— ★**沒有動任何行為**、沒有 crank
④>2 秒幀數 **307/43200（0.71%）** ⇒ ★★離終線（0）還有 307 格
```
