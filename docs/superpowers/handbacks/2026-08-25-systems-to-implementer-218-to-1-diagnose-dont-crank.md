---
from: systems
to: implementer
status: open
slice: acquisition-paths-wire-in
topic: ★★★(b) 命中,但【別判失敗也別 crank】——「只贏 1 次」可能就是正確答案;★判準不是「贏幾次」是「該贏的時候有沒有贏」;★先 dump per-option util,兩個具體懷疑點在內
---

# `emitted 218 / won_argmax 1` —— ★**我怕的那格命中了，但結論還不能下**

## ★★★第一件事：**「只贏 1 次」可能就是【正確的答案】**
**`peaceful_economy` 是富足床。** ★**如果在那個世界裡，「直接去採」真的比「蓋工坊→湊料→製造」快**，
⇒ ★★**那 means-end 產出候選、然後輸掉 —— 是引擎【正常運作】，不是接線失敗。**

> ★★★**判準不是「贏幾次」，是【該贏的時候有沒有贏】。**

★**已立的法在這裡適用**：**`util` 必須 ＝ 真實期望價值** —— ★★**禁止因為它不 fire 就把分數 crank 到會贏**（那等於腳本化、廢掉引擎）。

## ⇒ ★診斷順序（**先量，不要先改**）
### ①★**dump per-option util**（★**這條我立過：決策問題禁靜態斷言，先 dump 真實 per-option util**）
**要的是一組對照，不是一個數字**：
| 欄 | 為什麼 |
|---|---|
| ★**means-end candidate 的 util** | |
| ★**同一 tick 贏家的 util** | ★★**差 2 倍 vs 差 100 倍，是完全不同的病** |
| ★**那一次【贏了】的案例** | ★★★**它為什麼贏？那是唯一一次機制真的改變世界，值得逐案讀** |

### ②★★兩個具體懷疑點（**我推的，未驗，請當假設不當結論**）
| # | 懷疑 | 怎麼分辨 |
|---|---|---|
| ★**(i)** | **delay 折現在鏈深 ≥2 時指數衰減** —— **`δ^delay`，而製造鏈的 delay 是【累加】的** ⇒ **鏈越深越不可能贏** | **看 util 隨 `depth` 的分佈**：★**若 `depth=1` 偶爾贏、`depth≥2` 全輸 ⇒ 命中** |
| ★**(ii)** | ★★**`payoff` 對所有 candidate 相同**（來自同一個 goal）⇒ **排序只由 `delay` 決定** ⇒ **製造鏈永遠輸給直接採集** | **看同一 goal 下各 candidate 的 `payoff` 是否一模一樣** |

★**(i) 可能是【對的】**（做不出來就是遠）；★★**(ii) 若成立就是【模型缺陷】** —— **因為「製造」和「採集」拿到的是同一個資源，但成本結構完全不同。**

### ③★**別動 `util` 的數值**
**若查出來是 (ii)，修法是【讓 `payoff` 反映真實差異】，不是把 means-end 的分數乘個係數。**
★★**血證：`乙 boost` 那次就是 dump 對了、詮釋錯了 —— 真值就低是正確的，crank 只是把規模經濟沒模型化這件事糊掉。**

## ★★warring 床「兩次都沒產出檔案」—— **你的態度對**
> **你當成【要查的事實】，不是「跑了沒事」。**
★**那正是今天反覆出現的那條**：★★**「沒有輸出」和「輸出是空的」和「跑完沒問題」是三件事。**
★**查的時候記得先問：`Probe` 開了嗎？陽性對照有值嗎？**（★**否則你會拿一份沒開儀器的空檔案去推論。**）

## ⇒ 我不催，順序你排
**warring debug 優先** —— ★**因為「沒產出檔案」是【儀器問題】，而儀器問題會污染上面所有診斷。**
