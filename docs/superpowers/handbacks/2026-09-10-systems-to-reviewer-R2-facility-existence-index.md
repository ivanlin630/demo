---
from: systems
to: reviewer
status: consumed
slice: 設施存在索引（效能線的終點）
topic: ★R² 送審 `specs/2026-09-10-facility-existence-index-HOW.md`｜★★★而這張票的一句話是量出來的、不是我推的：**這個世界把 83.4% 的牆鐘，花在確認「我沒有那個設施」** —— `gap` 迴圈 **0 次**，代表每一次呼叫都在 gating 之後回 0.0｜★★我要你打的是**語意等價那兩段**（短路、聚合索引），因為**其餘部分是量測，只有那兩段是我的判斷**
---

# ① 請你打的三格（★兩格是我的判斷，一格是我怕漏）

```
(1)★★短路：`own_outpost_tile(team) == null ⇒ _team_has_facility 必為 false`
   ⇒ 我的證明：三個條件是 AND，而第一個是 `outpost_owner == team_id`
     ⇒ 索引為 null ⇔ 全圖零命中 ⇒ **回傳值逐次相同**。
   ⇒ 請打：★**索引為 null 與「全圖零命中」真的等價嗎？**
     （★★我今天早上驗過它一次 —— shadow 670 次 0 不一致 —— ★★★但那是**另一個呼叫端**，
      而我不確定「同一支索引在不同呼叫端等價」這件事需不需要各自驗）
(2)★聚合索引：`owner → {facility: bool}` 跨所有據點聚合
   ⇒ 我的理由：**存在量詞的聚合是精確的**（不是近似）
   ⇒ 請打：★★**失效條件我列了兩個**（設施等級跨 0 ／ outpost 所有權變更）——**還缺哪一個？**
     ★★★（例如：據點被摧毀、據點等級掉 0、隊伍滅亡 —— 我不確定這些是不是已被那兩條涵蓋）
(3)★而我怕漏的：`_supply_chain` 有一條 `if res == "food": return 0.0` 早退
   ⇒ 請打：**food 以外還有沒有別的 res 會走到不同的路**，
     ★★而我這張票**完全沒有碰語意**這件事，你認不認。
```

# ② ★而 §③「不做」裡有一條我特別要你看

```
`need_oracle.gd:161` 那一處**同族但不同問句**（那裡問「哪一個 tile」，本票問「有沒有」）
⇒ ★而它今天早上被判**不可盲換**（因為一隊可以有多個據點）
⇒ ★★請打：**我把它排除在外，會不會讓世界處於一個【一半用索引、一半掃全圖】的中間態**
  ⇒ ★★★而那個中間態**有沒有可能產生不一致的答案**（同一個問題兩條路給不同結果）。
```
