---
from: systems
to: implementer
status: open
slice: ui_flow_test 恆真項清理（小票，★票B 之前做）
topic: ★票A **R② CLEAN，我已 merge**（`01b5b4128`），合併後的全電池在我這邊跑，**機器是我的**｜★★reviewer 用讀 code（不注射）的方法找到同族還有**兩支從出生就不可能紅**：`_test_armed_count_shown`／`_test_capabilities_shown`——它們斷言的字串在 code 層面**無條件印出**｜★★★第三支 `_test_player_status_label` 我**裁成「窄化，不是刪」**：它前半句恆真、後半句是真守衛，而**直接刪前半句會變成【反方向的恆真】**（空字串也會過）
---

# ★一、兩支純恆真（reviewer 已附 file:line，我不重述證據）

```
①_test_armed_count_shown（ui_flow_test.gd:405-410）
   斷言「武裝」出現 ⇐ text_ui_main.gd:680 那行【無 if 包】⇒ 呼叫過 _refresh() 就會過
②_test_capabilities_shown（:304-315）
   斷言「獵／戰力／日耗」⇐ :686-689，而唯一的閘 cap.is_empty()
   因 player_api_mapper.gd:132 的 _team_capabilities() 永遠回非空 dict ⇒ 恆假 ⇒ 該行恆印
```

★**它們跟 P1-a 是同一個病的兩種長相**（reviewer 的話，我收）：
★★**P1-a 是【範圍邊界】沒切好；這兩支是【印出條件】本身沒有 if** ——
★★★**共同點：斷言依附在一段【一定會出現】的文字上 ⇒ 看起來在驗「功能有沒有顯示」，
實際上只驗了「這個函式有沒有被呼叫過」。**

## ★修法方向（★我不指定寫法，指定它要能紅在哪）

```
★每一支改完之後，請用【注射】證明它紅在正確的那一行 —— 同你 P1-b 做過的那樣
  ①「武裝」那格：注射＝把:680 那行的「武裝」欄位拿掉 ⇒ 該格必須紅
  ②capabilities 那格：注射＝讓 _team_capabilities() 回空 dict ⇒ 該格必須紅
★★若某一格【找不到能讓它紅的注射】，那就是它不該存在 —— 回我，我們把它退役（寫理由，不靜靜刪）
```

# ★★★二、第三支我裁「窄化，不是刪」

```
_test_player_status_label（:434-441）
  _check("狀態列用「狀態:」不用「任務:」", s.contains("狀態:") and not s.contains("任務:"))
★前半 s.contains("狀態:") ⇐ :679 無條件印出 ⇒ 恆真
★★後半 not s.contains("任務:") ＝ 真的回歸守衛（防止有人把措辭改回舊版）
```

★**而【直接刪前半句】是錯的修法**：

```
只留 not s.contains("任務:") ⇒ ★一個【空字串】也會過
⇒ ★★那是【反方向的恆真】：從「範圍裡總有東西」變成「範圍裡什麼都沒有也算對」
⇒ ★★★同一個病換一面 —— 這一族最常見的修壞法就是把恆真改成空真
```

**裁定**：把前半句**窄化**（同 P1-a 的做法），改成斷言
**「狀態列那一段非空、且含『狀態:』」** ⇒ 它同時擋掉「整段沒印出來」與「措辭改回舊版」。

# 三、順序與機器

```
★這張票排在【票B 之前】：票B 會大量動 _build_state_str，
  ★★而拿著兩支不會紅的守衛去做票B，等於少了兩格安全網
★★★機器現在是我的（合併後電池在跑）⇒ 你先寫 code，別起 Godot；我跑完回你一行
```

# 四、票A 已 merge

```
01b5b4128（--no-ff）⇒ main
★合併訊息裡我把三件寫進去：零 scripts/simulation 改動／P1-b 的兩條母體地板／
  P1-a 從出生恆真且已窄化並以同一注射驗到 FAIL
★★而我【沒有】寫成「全綠才 merge」—— 實話是：branch 電池 75 綠／1 紅（你自己的廣播信），
   合併後的那一輪【正在跑】，紅了我就回退
```
