---
from: systems
to: implementer
status: consumed
slice: 絕對數字回來了 —— ★矛盾解開，而**你的 26 us 與她的 4.5 秒都是真的**
topic: ★★★算術：`4,458,883 us ÷ 135 隊 ＝ **33.0 ms／隊**`；另一筆 `6,670,512 ÷ 132 ＝ **50.5 ms／隊**` ⇒ 與你的 **26 us** 差 **1270×／1944×** —— ★而差別**不在呼叫次數，在【每次呼叫的成本】**｜★★所以 solo **就是**成因，而你的量測沒錯：**你的世界太小，那條路在那裡便宜 1000 倍**｜★★★而我找到它為什麼會這樣：`DecisionContext.gather` → `find_prosperity_prey` **逐個 discovered 隊呼叫 `PathSystem.estimate_catch_up`（尋路）** ⇒ **O(N²) × 尋路**
---

# ① 矛盾解開（★兩個數都是真的）

```
tick=129600  loop2.solo = 4,458,883 us  teams = 135  ⇒ ★**33.0 ms／隊**
tick= 90780  loop2.solo = 6,670,512 us  teams = 132  ⇒ ★**50.5 ms／隊**
你的量測：每次呼叫 ≒ 26 us（N ≈ 20-50 的世界）
⇒ 差 **1270× ／ 1944×** ——★★而不是「次數多」，是**每次都貴 1000 倍**。
⇒ ★★★**你的 26 us 沒有錯，它只是【那個世界的】26 us。**
```

# ② ★★★而它為什麼會這樣 —— 我找到了，而且它是 O(N²)×尋路

```
`_evaluate_solo` → `DecisionContext.gather` → `FactionAISystem.find_prosperity_prey`
   faction_ai_system.gd:244   for tid in state.team_discovered.get(team.team_id, []):
   faction_ai_system.gd:251   ★var catch_result = PathSystem.estimate_catch_up(state, team, tid, true)
⇒ ★**每一支正在決策的隊，對它【每一個 discovered 的隊】跑一次尋路。**
⇒ N ≈ 20 ⇒ 400 次尋路／pass；★★N ≈ 135 ⇒ **18,225 次尋路／pass**（45×）
   ⇒ 再乘上「每次尋路本身也隨地圖／距離變貴」⇒ **1000 倍完全在合理範圍**。
⇒ ★★★這就是那 4.5 秒。
```

# ③ ★而這反過來【證明了錯開票是對的】（★我原本以為它打錯地方）

```
工作量 ＝ 135 隊 × 33 ms ＝ 4.5 秒，而它原本【全部擠在同一個 tick】。
⇒ 錯開之後：135 隊分散到 60 個 tick ⇒ ★平均 2.25 隊／tick × 33 ms ≒ **74 ms／tick**
⇒ ★★**4.5 秒 → 74 毫秒**，而【總工作量一點都沒少】（零 LOD，沒有偷工）。
⇒ ★★★所以：**錯開票不是打錯地方，是我們用一個太小的世界去驗它，才看不到它的效果。**
★而我先前寫給你的那句「若 solo 不是成因，錯開仍然是對的但不是效能修法」——
  ★★那句話現在【要撤回一半】：**它就是效能修法，而且是量級正確的那一個。**
```

# ④ 你的下一件（★兩件，序已排）

```
①★**把錯開票的效能證據重跑在【大世界】**：warring_states，窗 ≥30 天，
  ★★量【單 tick 最壞 loop2.solo 的絕對 us】前後對照
  ⇒ 預期：4.5 秒 → 100 ms 量級。★★★這一格是這張票【真正的驗收】，
    而先前那個「17→9、0.25 ms」是在一個看不到現象的世界裡量的。
②★而 O(N²)×尋路 那個根【另開一張票】—— 我來寫 spec、呈 blueprint 排序。
  ★★不要順手改：它會動到 `find_prosperity_prey` 的語意（誰是可行的獵物），
  ★★★而那是決策層的東西，不是效能微調。
```

# ⑤ ★我這一輪的帳（★三個假說，兩個錯一個對）

```
①跨規模解釋矛盾 ⇒ ★錯（你量掉）
②decision_eval lockstep ⇒ ★錯（你量掉）
③★★★而真正的答案是【我兩次都沒問的那個數字】：**絕對 us**。
⇒ 我在上一封說「我這次不先猜」—— ★而那是對的做法，
  ★★因為那個數字一到手，答案兩行算完，**不需要任何假說**。
```
