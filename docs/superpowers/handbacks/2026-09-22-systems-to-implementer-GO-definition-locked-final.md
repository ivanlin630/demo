---
from: systems
to: implementer
status: open
slice: 跨 tick 量測 —— **GO（定義已訂正並鎖定）**
topic: ★**HOLD 解除，可以跑**｜★★**最終定義：同一隊的【相鄰兩次 gather】**（不論隔幾 tick）—— 母體 ≈ 9134，**不是**我原本寫的「相鄰兩 tick」（那個母體 ≈ 0）｜★★★**門檻 95% 對【全母體】；tick 間距分桶只作診斷欄，不得當判準** —— 而**我原本提的「按間距定門檻」是錯的**，WHAT 堵掉了它
---

# 一、跑什麼（票身 §7 為準：`docs/superpowers/specs/2026-09-22-cross-tick-gather-invariance-MEASURE.md`）

```
【主量】同一隊的**相鄰兩次 `gather`**（不論隔幾 tick）⇒ `snapshot_dict()` 逐欄比對 ⇒ 不變率
   母體 ＝ **≈ 9134**（9237 − 103）
★★**為什麼這才是對的量**：**它就是快取的反事實** ——
   「跳過這一次 gather、沿用上一次的結果」，結果一不一樣。
   ⇒ 而「相鄰兩 tick」問的是一個**快取根本不會遇到的情境**（而且它不存在）。
```

# 二、★★★門檻（WHAT 裁，已鎖）

```
★**全母體**不變率 ≥ 95% ⇒ 值得開快取票｜< 95% ⇒ 永久結案
★★**tick 間距分桶【只是診斷欄】** —— 必印，但**不得拿來當判準**
   ⇒ ★★★**我原本提的「按間距分桶定門檻」是錯的**：那等於**看完數字再挑一個會過的桶**，
      而那正是預註冊要防的事。**WHAT 堵掉了它，我收。**
★若分桶露出「短間距桶過 95% 且母體夠」⇒ **那是另一張更窄的票（帶 TTL 的快取）**，
   **新開**，★**不回頭改本票的讀法**。
★★**母體 0 或極小 ⇒ 判【不可判】，不判綠。**
```

# 三、卷面（§5 不變，加一欄）

```
①母體 ＝ **比較次數**（＝ 每隊「有前一次 gather 可比」的次數）
②主量不變率 ⊕ 第一名不變率，兩欄並列
③★**變的時候變在哪**：最常變的前 5 個欄位名
④★**新增：tick 間距分佈**（median／p90／max，或分桶）—— **診斷用，不是判準**
⑤四要素 ＋ HW-2 ＋ 世代 6（`3fb2c3201`）｜兩顆種子｜8 天窗｜`warring_states`
⑥★樁自己的成本（佔 gather 的比例）—— 貴到改變時序 ⇒ 該輪作廢
★★**硬體戳現在 wrapper 會自己印**（`[HW] cpu=…`）⇒ 直接貼那一行，別手寫
```

# 四、★一句給你

```
★**你沒有做錯任何事** —— 卡住是因為我 spec 了一個量不到的量，而我是去查 `_gt6` 為什麼是空的才發現的。
★★**下次遇到「這個量好像量不出來」⇒ 那通常是規格錯了，不是你錯了** ⇒ 直接回我，不要硬湊。
```
