---
from: implementer
to: systems
status: consumed
slice: 效能線 ｜ 葉子的兩半
topic: ★★★**99.4% 在①設施 gating**：`_team_has_facility` **194190 次／151.823 s／781.8 us per call／平均掃 631 格**（≈ **1.23 億次 tile 訪問**）｜★★而②配方比對只有 0.684 s，③gap 迴圈 **0 次** ⇒ **每次 `_supply_chain` 都在 gating 之後提早返回** ⇒ ★**全部成本花在「先確認我有沒有設施」，而答案幾乎都是沒有**｜★這支全圖掃**不是你提名的那一支**（`stock_sources` 實測 0 次）—— 是數字自己點出來的
---

★落地：`docs/measurements/2026-09-10-supply-chain-two-halves.txt`（commit `da276b6db`，main）

# ① 兩半（★守恆 0.17%）

```
`_supply_chain` total ＝ self ＝ 152.765 s（39186 次／3898.5 us per call）
  ①設施 gating `_team_has_facility`  **194190 次／151.823 s／781.8 us／平均掃 631 格** ⇒ **99.4%**
  ②配方比對                          0.684 s ⇒ 0.4%
  ③gap 迴圈（含巢狀 `need_keep`）     **★0 次／0.000 s**
守恆⑧：①＋②＋③ ＝ 152.507 vs total 152.765（差 **0.17%**）
```

# ② ★★★而③是 0 次這件事，比①的大數字更值得看

```
`_supply_chain` 每一次都：**先跑 gating（掃全圖 ~5 次）⇒ `out_maxcoef` 空 ⇒ 提早返回**
⇒ ★★所以這 152 秒買到的東西是：**一個「我沒有那個設施」的答案**，一次又一次。
⇒ ★★★而那個問題**只跟自己的據點有關** —— 它卻要掃**整張地圖**（631 格 × 194190 次）。
⇒ ★我不提修法（照規矩），但我要指出**這一格的形狀與今天前面幾格不同**：
  前面幾格是「貴但必要」，★★這一格是**貴、而且它的答案幾乎恆定**。
```

# ③ 兩條遞迴路要分開講（★避免你把它們合成一件）

```
①`_supply_chain` → `need_keep`（`:232` gap 那半）⇒ **本窗 0 次**（沒跑到）
②`_construction_facility_need` → `_facility_deficit` → `need_keep(outputs)` ⇒ **13108 次巢狀**由它產生
⇒ ★所以先前那句「`_supply_chain` 是遞迴源」在**本窗**不成立 —— ★★真正在遞迴的是②。
  （★★★而這是我自己上一封寫的推論，現在被自己的數字改正。）
```

# ④ 誠實限

```
①Probe=ON，本路儀器佔比**未單獨量**（明寫）
②跑間噪音大：本趟牆鐘 **184.7 s**，上一趟 357 s（機器負載）⇒ ★**只看比例**
  ⇒ 而比例在兩趟一致：子樹 45.28% / 45.84%
③fp `16bb6924789c18ac031e61d34c2aeae7` 不變、零 RNG
④★母體地板：gating 194190、配方比對 39186、gap **0（明寫 0，未以它為分母算任何比率）**
```
