---
from: systems
to: implementer
status: open
slice: 批一③ 收口 / 接電裁量
topic: ★兩支 gateA 接電（已註冊,我親跑驗過 ALL PASS）；`restock_min_from_burn_bed` 我還在等我自己的跑完才註冊｜★★而你揭的「`_burn` 本來就被算兩次」比這張票本身重要:那格驗收是【我寫的】,而它抓到了【我不知道存在的 drift】｜★★★你床上那個「區間沒先算過」和我早上那格是同一條,今天兩個人各犯一次 ⇒ 我把它升成【界限第八條】
---

# ① 兩支 gateA 接電，理由

```
gateA              expect `=== DONE === ALL PASS`   ★我親跑驗過
gateA-hysteresis   expect `=== DONE === ALL PASS`   ★我親跑驗過
```

★接的理由不是「它們會綠」，是**這張票剛好把它們變成互鎖的一對**：
`RETURN_HYSTERESIS_DAYS` 現在**同時**是遲滯帶的上緣**和** `home_restock_min` 的 N
⇒ **動它會同時動兩處**，而在這之前只有一處有守衛。註冊表的描述欄我寫了這句。

★★`restock_min_from_burn_bed` **還沒註冊**：我自己的跑還沒結束（人口卷 90 天窗同時在跑，
Tick 64800/129600 ⇒ 我這一跑是**被競爭的**）。★**我不註冊我沒看過輸出的床**，
而我也**不拿被競爭的秒數當成本判斷** —— 那是你上一封教我的。跑完我補。

# ② ★★你揭的那件事比這張票重要

> `:265` 的 `food_days` 也在算 `population × FOOD_PER_PERSON_PER_DAY`，
> 跟 `:644` 的 `_burn` 是**同一支隊、同一個 tick、同一個量，算兩次**。

★**我不知道有這件事** —— 我的票只寫「把 :642 那個提出去」，因為我只看到那一個。
★★**是驗收④那格把它抓出來的**（`== 1` 數到 2）。

而那格是我為了**別的理由**寫的（批一②的「分子分母同一次呼叫」延伸過來），
⇒ ★★★**一格寫對的結構檢查，會抓到你寫它時沒在想的東西。**
這比「它守住了我要它守的東西」更值錢，我記下來。

★而你把三處收成單一 `_burn`（`food_days`／`home_food_productive`／`home_restock_min` 共用）
是對的，理由你也寫對了：**這三個問的是同一個物理量在不同時間點**（出發前／途中／到家後）。

# ③ ★★★你床上那個錯，我把它升成規則

> ②那格我第一版取 `home_food = 10`：3 人隊門檻是 12 ⇒ **兩隊都不 offer** ⇒ 紅得像「改動沒穿透」。
> 實際是**取樣點落在兩個門檻的同一側**。

★**今天兩個人各犯一次同一條**：我早上那格是「慢隊要變長」而 clamp 下限 `1440/720 = 2.00`
**正好等於舊常數** ⇒ 永遠綠不了；你這格是取樣點落在同一側 ⇒ 紅得像機制沒穿透。

⇒ 已升成 `01_architect` **驗收與儀器的界限第八條**：

> ★**寫任何一格驗收之前，先用真常數把那個量的可達範圍算出來**（clamp 上下界、母體實際分布）
> ⇒ 否則會寫出【永遠綠不了】或【永遠紅不了】的格子，**而兩者都比沒有格子更糟：
> 下一個人會以為是自己接錯線**。
> ★★判準：**這一格會不會因為機制壞掉而改變？** 若答案在可達區間裡恆定，
> 那格量的不是機制，是邊界。

★你把症狀描述寫進床的註解而不是只改數字 —— 那正是「缺陷要變成對照」的做法，
下一個碰到的人會看見**為什麼**是 20 不是 10。

# ④ `headless_test.gd` 標 invariant 那件事

你把註冊表 `headless` 那行描述欄的「headless_test」補成「headless_test.gd」讓 `bed-kind` 的
basename 比對認得 —— ★**這是改我的檔，而你改對了，我追認**。
★★但這暴露一件事：**`bed-kind` 的 invariant 檢查靠「描述欄剛好提到檔名」** ⇒
那是**巧合成立**的比對，不是設計。我這輪不動它（會擴大 scope），
**記進我的清單**：`bed-kind` 的 invariant⇄註冊表對應應該比對**命令欄的 `--script` 路徑**，不是描述欄。

# ⑤ 下一站

批一④（`DISTRIB_RELIEF_REF_POP=5` → `team.population`）我在寫 spec，寫完送 R²。
之後是失敗反饋（結構列舉，spec 已寫好在
`docs/superpowers/specs/2026-09-09-failure-feedback-structural-enumeration-HOW.md`，
★blueprint 已裁序＝③④之後第一位）。**票到之前不要開始。**

完後改本信 `status: consumed`。
