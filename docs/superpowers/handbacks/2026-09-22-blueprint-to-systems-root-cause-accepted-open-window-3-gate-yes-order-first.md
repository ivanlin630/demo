---
from: blueprint
to: systems
status: open
slice: 卡頓根因 — 三件裁定
topic: ★**①修法＝那 10 處改走 CadenceStagger.next_tick（零新旋鈕）；改變世界 ⇒ 開世界改變窗 #3、世代 7；驗收預註冊在下（含一格「決策頻率不變」的副作用對照）**｜★★**②閘：加，因為沒有既有 hook 覆蓋、前例在旁（bed-arm）；形狀窄：禁 `*_next_tick = current_tick + …`，陽性對照＝修前那 10 處必須紅**｜★★★**③序：插在指令佇列化之前；窗 #3 只放它（佇列化若剛好好了就搭，不等）**
---

# 一、修法票（世界改變窗 #3）

```
形狀：10 個賦值點改 `CadenceStagger.next_tick(current_tick, last, team_id, CONST)`；helper 不動；常數不動（週期不變，只散相位）
世代：merge ＝ 世代 7 邊界；fp 基線重立、舊錨重錨標 commit（照世代規矩）
驗收（數字前寫死，HW-2、12 天、種子 1337＋42、Probe 關為主）：
  A1【機制】11+ 決策隊的 tick 數 ⇒ ≤ 修前的 5%（283／282 → ≤ 14）；60-tick 間距佔比 < 20%
  A2【玩家尺】>2s 幀數 ≤ 1／遊戲日 且 p99 < 1s ⇒ 凍結線可交玩；否則 ⇒ 分片＋快照票開（defer 門票觸發）
  A3【副作用對照】每隊每日決策次數（各路徑）median 變動 < 5% —— 錯開只准動相位不准動頻率；動了＝改到不該改的
  A4【fp】指紋床 fp 改變＝預期的紅（記世代 7）；樁開/關 fp 逐字相同（不變量 #7）
  A5【陽性對照】只改 9 處留 1 處 ⇒ 該欄位的隊仍同相（A1 對那條路不過）⇒ 證明 A1 會紅
★兩顆種子同側才算過；母體與尾巴同印
```

# 二、閘：加

```
理由：沒有既有 hook 覆蓋這個形狀（你兩輪靜態零命中正是證據）；前例 bed-arm（新床不得自己拼順序）
形狀：grep 呼叫點 `_next_tick\s*=\s*[^=]*current_tick\s*\+`（剝註解）零命中；★裝閘前先在修前樹跑一次必須命中 10 ⇒ 陽性對照
不擴：不掃「所有加法」，只掃 `*_next_tick =` 這個賦值形狀；再多一格就是雜訊
```

# 三、序

```
窗 #3 ＝ 這一票（spec → R² → 實作 → 驗收 → merge）；指令佇列化不阻塞、若同時 R² CLEAN 就同批
分片＋邊界快照 ⇒ defer，門票＝A2 不過
equip_mobilize 整體票、subteam-idle 修法票、failure-memory ⇒ 排窗 #3 之後
```

# 四、記一筆（你入 memory）

```
「量測給了名字，靜態才找得到路」：兩輪靜態零命中是因為那句加法【長得像正確的排程】；抓到它靠的是 implementer 量出四個具名欄位
⇒ 通則：找不到補丁閘時，先量【誰在那一格】拿到名字，再去讀那個名字的 code；別再掃第三輪形狀
```
