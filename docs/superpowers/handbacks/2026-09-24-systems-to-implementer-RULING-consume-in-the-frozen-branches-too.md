---
from: systems
to: implementer
status: open
slice: 票5｜凍結世界死鎖
topic: ★**裁 (乙)，而理由比「解死鎖」更寬**：沒有它，**凍結世界裡的每一條指令都是【靜默丟棄】** —— 而「拒絕禁靜默」在那兩個狀態下同樣適用（玩家等繼承人時按移動，該看到「你現在得先選繼承人」）｜★★P3 判準改法准：**消費的【位置】可以有三處，消費的【實作】仍然只有一份**｜★★★**新增 P16（缺陷變對照）**：凍結時送非解凍指令必須收到拒絕句、再送解凍指令世界必須真的解凍，★母體地板＝那一輪要真的處在凍結狀態｜★spec §3-2 我改好了，訂正的是【列舉的邊界】
---

# ★一、裁 (乙)，而我要把理由寫得比你寬

```
你的理由：解死鎖、不動「記遞增後的 tick」那條規則 —— ★成立
★★而它其實修的是一個更大的洞：**凍結世界裡的每一條指令，今天都是【靜默丟棄】**
  ⇒ 不只是「選繼承人那一條卡住」，是**玩家在那兩個狀態下按什麼都沒有回音**
  ⇒ ★★★而 blueprint 裁的「拒絕禁靜默」**沒有寫「凍結時除外」** ——
    是我在 spec 裡把消費點的邊界畫在了它管不到的地方。
⇒ 所以 (乙) 不是「為了解死鎖而破一次例」，是**把回饋契約補到它本來就該覆蓋的狀態上**。
```

★**(甲)(丙) 不選的理由我同意你的**：(甲) 上移會讓「遞增後」出現例外（那條規則的價值就在沒有例外）；
(丙) 讓 `respond_to_forced` 不進佇列 ＝ §3-4 明文禁止的旁路。

# ★★二、P3 判準改法：准

```
舊「production 路徑上只有【一處】dispatch 呼叫」
新「只有 `_consume_player_commands()` 這一支函式會呼叫 dispatch」
⇒ ★你那句就是判準本身：**消費的【位置】可以有三處，消費的【實作】仍然只有一份。**
⇒ ★★而新判準比舊的【更嚴】：舊版只數呼叫點個數（★又是「數數」），
  新版釘的是**誰有資格呼叫** —— ★★★同今天那條：**判準要指名，不要數數。**
```

# ★★★三、新增 P16（spec §5 已寫進去）

```
P16 [凍結狀態不得靜默]
  凍結（等待繼承人）⇒ 送一條【非解凍】指令（例：移動）⇒ ★必須收到【拒絕的結果句】不是沉默
  ⇒ ★★再送解凍指令 ⇒ 世界必須真的解凍（下一顆 tick 推得動）
  ★★★母體地板：那一輪要**真的處在凍結狀態**（斷言 `awaiting_heir` 成立）
    —— 否則這一格會在一個從未凍結的世界上恆綠。
```

★**為什麼要有這一格**：死鎖是**症狀**，而**病是「凍結分支不在消費點的母體裡」**。
★★修了症狀而沒有守衛 ⇒ 下一次有人在 `_advance_tick_body` 之前再加一條 early return，同樣的病會回來，
★★★**而它回來的樣子仍然是「玩家按了沒反應」——一個沒有任何東西會紅的症狀。**

# 四、spec §3-2 我改了，而訂正的是【列舉的邊界】

```
★不是「R② 沒審出來」—— spec 寫的那兩個分支都對，★★**錯的是母體的邊界**：
  我把邊界畫在 `_advance_tick_body`【內】，而真正的控制流在它【外】就分岔了。
⇒ ★★★通則（已寫進 spec）：**列舉「有幾條路」之前，要先問「這個函式【會不會被呼叫】」。**
⇒ 它是「列舉挑引擎決定的軸」的新樣本，而這一個**只有跑起來才看得到**：
  靜態讀 `_advance_tick_body` 的人，看到的兩條路都是對的。
```

# ★五、兩件我要另外處理的

```
①**P13b 印的是事實，而那件事要回報 blueprint**：`confirm_gather_intel` 5 次都成功而 fp 不變
  ⇒ 他裁的「真去問人＝指令、可有成本」那一層，**今天在 code 裡還沒有落點**
  ⇒ ★我寄信給他，**你不要順手補**（補它＝替 WHAT 決定打聽要付什麼代價）
②**那支從沒執行過的床沒有 Parse Error** ⇒ ★「第一次編譯」那次豁免**沒有被用到**
  ⇒ ★★而它現在【到期】而不是【用掉】：**那支床已經編過了，沒有東西還需要被豁免。**
  ⇒ ★★★所以往後它任何一次 Parse Error 都是真缺陷 —— 結論跟我上一封一樣，但理由要寫對。
```

★其餘全綠我收：重播床 10／10、11 道母體地板全過、P2 重播 fp 逐字相同（6 條真指令）、ui-flow 44／44 只剩那兩格。
