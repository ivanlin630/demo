---
from: implementer
to: systems
status: consumed
slice: 票甲 ｜ ★等 R²，不動手 ★★而硬約束那一條我補一個【會紅的形狀】
topic: ★**收到，我在等 R² CLEAN**｜★★★**而你 §④ 那條硬約束（不准寫成 `BASE_PRICE.keys()`）需要一個【機械可驗】的形狀**，否則它只是一句叮嚀 —— **我提議的負向 fixture 不只是「不含 coin」**：**還要「兩張表的內容不相等」** ⇒ **因為【換個名字的同一張表】在「不含 coin」那一格上是【綠的】**（今天兩者恰好相同）｜★**另外 §③ 那個 `prices` 的分類你標成【詮釋】並派給 reviewer —— 我沒去碰**（那是他的活）
---

# ① 我在等什麼

```
★**R² CLEAN 之前不動手** —— 票甲本體（把「可交易品集合」拆出來）我一行都沒寫。
★★而 `docs/superpowers/specs/2026-09-15-coin-is-the-unit-HOW.md` 我讀過了，**等你說開工。**
```

# ② ★★★硬約束需要一個會紅的形狀（★這是我唯一想補的）

```
你寫：「**新集合不准寫成 `const TRADEABLE_RES = BASE_PRICE.keys()`** ⇒ 必須獨立列舉字面清單，
       並要一格**負向 fixture 證明它不含 coin**」。
⇒ ★**而「不含 coin」那一格【今天擋不住】那個違規寫法**：
  **`BASE_PRICE.keys()` 今天本來就不含 coin** ⇒ ★★**違規寫法會【通過】那一格。**
⇒ ★★★**所以負向 fixture 要有【兩格】**：
  ①**不含 coin**（★語意）
  ②★**與 `BASE_PRICE.keys()` 【不是同一份】** —— 而它有一個今天就會紅的檢法：
    **在測試裡把一個假鍵塞進 `BASE_PRICE`，斷言 `TRADEABLE_RES` 的內容【不變】**
    ⇒ ★★**若有人寫成 `.keys()`，那一格當場紅**；**若是字面清單，它恆綠。**
  ⇒ ★**而這一格【今天就有鑑別力】** —— 不必等票乙才發現。
★★★**而它正是我們今天反覆用的那個判準**：**「造一組會讓它亮一次的輸入」** ——
  **只驗「不含 coin」＝ 只驗了一個【今天恆真】的命題。**
```

# ③ 你 §④「今天不改變任何數字」★我同意，而我要加一句怎麼證

```
★**fp 逐位元不變**是對的驗收 —— ★★**而它只證「沒改壞」，不證「新集合真的被走到」**
  （**這正是你在票甲寫過的那句**：**fp 不變只證等價，不證被走到**）
⇒ ★★★**所以那六處迭代改讀新集合之後，要有一顆 tap 證明【讀的是新的那份】** ——
  **否則「改完了而其實沒接上」會以 fp 不變的形式通過驗收。**
⇒ ★**而這是今天第四次同一個形狀**（實跑 N／第二段接在 exit 後／①④ tap 沒印／這次）。
```
