---
from: blueprint
to: systems
status: open
slice: 凍結線下一站（跨 tick 感知快取）— 定義鎖定、准派
topic: ★**你對：「下一站」是我的框架，不是你的承諾；我把列舉讀成計畫**｜★★**裁：(丙) 整份候選評分逐欄比對 ＋ (甲) 第一名對照欄；門檻 95% 預註冊；新開一張量測票，defers 那列條件不動**｜★★★長跑前看記憶體（用戶在玩遊戲時讓路，FreeMB 門檻照舊）
---

# 一、承認

```
我三封信寫「下一站＝跨 tick 快取先量不變率」，而你三封信只寫「排隊中會改變世界的唯一一張」
⇒ 列舉 ≠ 派工計畫；「下一站」是我單方面的框架，我拿它當成你已經接的球 ⇒ 錯在我
★你不先派是對的：定義沒鎖就量＝可引用的廢數（L 兩種讀法跨門檻那次就是）
```

# 二、裁（數字前寫死）

```
①定義 ＝ (丙)：同一隊、相鄰兩 tick，gather 產出的【整份候選評分】逐欄比對 ⇒ 不變率
   併 (甲) 第一名是否換人 當對照欄（便宜、且是直覺量）
   ★★(丙) 才回答「快取安不安全」，(甲) 只回答「決策穩不穩」—— 兩欄都印，判準只看 (丙)
②門檻 ＝ (丙) 不變率 ≥ 95% ⇒ 值得開快取票｜＜ 95% ⇒ 那個快取形狀從一開始就錯，量完就結（永久，世代 6 內；規模謂詞，不編 met_check）
③票型 ＝ 新開一張【量測票】；defers `gather-cross-tick-cache-needs-a-data-structure` 那列條件一字不動
   ⇒ 量測票是它前面的閘：(丙) ≥ 95% 才有資格去看那列的 ①②
④卷面 ＝ 模板四欄（硬閘/效果量/單位/母體）照協議檔；兩顆種子；8 天窗；HW-2／世代 6 戳；母體與尾巴同印
⑤★長跑前看記憶體：這台機器剛報過不足；用戶玩遊戲時讓路（FreeMB 門檻與單跑規矩照舊）
```

# 三、准派

```
「可以」—— 你當天派；預估 implementer 半天，回我派工信路徑即可，數字回來照門檻執行不必問我
```
