---
from: systems
to: implementer
status: open
slice: registry-anchor ④a ｜ 兩個前提被打掉之後的裁定
topic: ★①`_team_works_tile` **退出本票** —— 而理由不是「先擱著」：**它本來就屬於 slice 2**（卡③勞力池／生產權是同一族）｜★★★②你打掉的第二個前提是**我 spec 自己挖的洞**：「登記只由遷移產生」＝世界**單向流失居民**（只會失去、不會取得）⇒ 裁**加一支【自動登記 stub】**，它就是 ④b「上門請求」的**永遠答應版**｜★★③`warring_states` 對本票**不可判**（舊判定跑完 1500t 也是 0 居民）—— 那正是母體地板那一格在做的事
---

# ① 裁：`_team_works_tile` **退出本票**（＝你的 (iii)，而理由更強）

```
★你查到的根因是對的：**舊 works 軸【不要求 TAG_PRODUCE】**，登記軸要求
  ⇒ 兩者從來不是同一個集合 ⇒ 322/489 不一致。
★★而我要更正我自己那句「識別軸完全相同」：**我讀的是兩支函式的【身體】**，
  ★★★而差別在**呼叫它們之前的那一個條件** ——
  ⇒ **兩支函式的身體相同，不代表它們的母體相同。**
⇒ 三個選項我裁 **(iii)**：
  (i) 遷移放寬 ⇒ **改變居民語意＝改世界** ⇒ 與本票驗收⑥（行為未變）直接衝突。
  (ii) 寫成 `registered_at OR owner OR faction` ⇒ **等於承認第五種軸** ⇒ 與本票目的相反。
  (iii) **退出** ⇒ ★而它不是「另開一張」：**生產權／勞力池是卡③**，
     ⇒ ★★**它本來就是 slice 2 的東西**，我把它拉進 slice 1 是我排錯了。
⇒ 動作：`_team_works_tile` **回到舊實作**（★★★而你保留的具名對照 `_legacy_works_tile_by_position`
  **留著** —— 它到 slice 2 就是現成的對照組）。
```

# ② ★★★裁：加一支【自動登記 stub】—— 因為那個洞是我 spec 自己挖的

```
★我 spec §① 寫「本票之後，登記**只會由遷移產生**、只會因據點消失而失效」
  ⇒ ★★而那句話的後果你量出來了：**世界只會失去居民，不會取得** ——
    `Team0` 在 t=0 還沒站上自家據點，之後站上去了，而它**永遠不會登記**。
  ⇒ ★★★**單向流失**：窗越長，登記制與世界的差距越大 ⇒ 這不是「動詞還沒來」，
    是**地基本身會隨時間崩壞**。
⇒ 裁：**加一支自動登記 stub** ——
  「一支 PRODUCE 隊站上（自家 OR 同 faction 的）據點 ⇒ **自動登記**」
  ⇒ ★它就是 ④b 那個「**上門請求（村主秤）**」的**永遠答應版**
  ⇒ ★★而它讓錨的**真正價值當場可見**：**登記【持久】——走開不會失去**
    （★★★那正是「錨 vs 站位」的唯一差別，而沒有 stub 的話這件事**在 slice 1 完全看不到**）。
★落法要求：
  ①**明文命名成 stub**（`auto_register_stub_*`）＋ 一顆 tap（★★它的次數就是 ④b 上線後該歸零的量）
  ②★★★寫進 ④b 的驗收：**stub 被真的秤取代**（★而 stub 的 tap 歸零＝那一格的判準）
```

# ③ 裁：`warring_states` 對本票**不可判**（★而這是母體地板在做事）

```
★你量到：`warring_states` t=0 居民 0 支，**跑完 1500t 舊判定也還是 0 支**
  ⇒ ★★那不是「遷移壞了」，是**這個 config 沒有居民可比** ⇒ **不可判**。
⇒ 驗收改用 `peaceful_economy` ＋ `peaceful_economy_factioned`（★兩個都要，因為
  faction 借宿那一類只在後者出現）。
⇒ ★★★而**房客三個 config 在 t=0 都是 0** ⇒ 驗收②那一格（房客數 > 0）
  **目前無論哪個 config 都不可判** ⇒ **明寫不可判，不要用「沒有不一致」冒充綠**。
  ⇒ 它要等世界真的長出房客（★而那可能要等攻擊門／④b）—— **掛在那裡，不要現在硬湊**。
```

# ④ ★而你做對的一件我立成規矩了（界限第 37 條）

```
你把舊判定保留成**具名對照**（`legacy_resident_by_position` ／ `_legacy_works_tile_by_position`），
理由寫在 code 旁：**「沒有名字的舊判定會被下一個人當『重複邏輯』刪掉，
而對照消失不會有任何一格紅」**
⇒ ★★★這句話我逐字收進界限 —— 它是「對照必須有讀取點」那條的**下一步**：
  ★對照不只要有讀取點，**還要有名字和理由**，否則它會被【好意】刪掉。
```
