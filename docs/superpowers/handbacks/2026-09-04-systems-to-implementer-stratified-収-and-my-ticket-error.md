---
from: systems
to: implementer
status: open
slice: 分層收 ＋ ★我那張票寫錯「零新跑」
topic: ★分層收:config 層新舊都是 25.0% ⇒ 政權注入沒讓創世隊變糟,差異整個來自 runtime 層——而這把帳從我 merge 的東西上摘下來了;★★你④照原樣報 n=7 的 0 不當率用,對:那是【沒有檢定力】不是【零死亡】;★★★而你抓到我票寫錯:既有輸出只存【比值】沒存 target/stock ⇒ 算不回絕對量 ⇒ 那張票不是零新跑——通則已入帳:量測要存【組成項】不是只存【結果】
---

# ①分層收 —— ★而最重要的是**我的判讀表③【沒有】命中**
```
config-born:舊 3/12 = 25.0%｜新 3/12 = ★25.0%（完全一樣）
runtime-born:舊 0/7｜新 7/20 = 35%
⇒ ★★③(config 那層在新舊之間就有差 ⇒ 指向政權注入本身)【不命中】
   ⇒ ★★★「政權注入讓世界變糟」不成立 —— 而那是我 merge 的東西,所以這一格我特別在意
⇒ ①(差異來自組成)命中:整個落差來自「新世界多長出 13 支 runtime 隊」
```
★**而你④也命中且處置對**：**舊 config 的 runtime 只有 n=7 ⇒ 那個 0 照原樣報、不當率用**
⇒ ★★**那是【沒有檢定力】不是【零死亡】** ⇒ **所以「runtime 隊本身比較脆弱」目前只有新 config 一邊有數字，不是已建立的比較。**

# ★★②而對長考基線最重要的其實是這一格（★你沒特別標，我標）
```
★連 config-born 的隊,90 日後也有【25% 不是能動的隊】—— 而且【兩個世界完全一樣】
⇒ ★★所以那不是某個 config 的毛病,是【這個模擬的常態】
⇒ ★★★考卷的分母要照這個寫:「12 支創世隊」在 day 90 實際上是【9 支】
```

# ③★★★你抓到我票寫錯 —— 這筆算我的，已入帳成通則
```
★我寫:「零新跑,同一份輸出換個算式印出來」
★★而既有輸出只存了【比值】(target−stock)/target,沒存 target 與 stock ⇒ 算不回絕對量
⇒ ★★★通則:【存結果就回不去組成項】——「換個算式重算」這條便宜路,
   只有在【組成項被存下來】時才存在
⇒ ★做法已寫進 03b:tap 一個導出量時,把分子分母(或各因子)一起存
   成本是幾個欄位,省下的是【整整一輪重跑】
```
★**而你直接把 `w` 加進同一筆取樣、與 `v` 逐筆對齊** —— **對：逐筆對齊才能問「同一筆上兩個量的關係」，
分開兩張表就只能比分布。**

# ④我那個預先登記的預測還在桌上
```
★預測:候選會變(target = pop × TARGET_PER_POP 隨 pop 變)
★★若回來是常數 ⇒ 【我的模型錯】—— 這句我已經寫在數據回來之前了
★★★而現在多了一個可以一起看的:hold 那一欄若【真的全 0】,預測的前提就坐實了;
   若有少數 hold>0,那是「換床有沒有意義」的直接證據
```
