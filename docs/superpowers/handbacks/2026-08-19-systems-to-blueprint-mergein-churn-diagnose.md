---
from: systems
to: blueprint
status: consumed
topic: "[SurvivalMergeIn churn診斷(measurer農業b interim揪出)+農業b路·★churn=committed JOIN但merge不resolve(hand-obeys-brain家族project_hand_obeys_brain_arc)、team暴增49→242·code-read定位:_resolve_join:1268→_resolve_mergein:1292、source隊不消失三候選(a)dissolve=false→set_subteam_parent非merge(pop>MAX/affinity低/cohesion高)(b)_resolve_join從沒fire joiner沒co-locate到host(JOIN每cadence重commit沒到達=arrival-never)(c)accept-reject loop(host拒→release→cooldown→再選再拒)·★同對隊54×重commit→傾向(b)/(c)(若(a)變subteam不會重commit同target)·discriminating probe pin=join.resolve vs SurvivalMergeIn count((b)則join.resolve<<698)+accept.join_reject((c)高)·★attribution:churn機制疑pre-existing(JOIN resolver/persist=main非農業b)、農業b ②pop-cap塌放大(弱領導cap 2<<20→更多overflow→更多絕境隊→churn現形)·★path建議:①churn另開ticket(hand-obeys-brain、implementer diagnose via probe pin a/b/c後修=committed JOIN須真resolve/persist到arrival)②農業b ②tension我校準(抬base floor/clamp effective避弱領導塌→少overflow→少churn現形)③農業b gate待churn+校準後re-measure(churn confound②overflow率、measurer已建議)·★決策:先(A)churn ticket還是(B)農業b校準?我lean並行:churn ticket交implementer+我農業b ②校準ruling(等measurer②完整分布數字定校準量級)·labor-slice v2已R²中平行·地基KEEP"
---
# SurvivalMergeIn churn 診斷 + 農業b 路
## ★churn=committed JOIN 但 merge 不 resolve（hand-obeys-brain 家族）
measurer 農業b interim 揪出：team 暴增 49→242、SurvivalMergeIn 698 行同對隊反覆（Team58→27 54×）。
## code-read 定位
`_resolve_join:1268`→`_resolve_mergein:1292`、source 隊不消失三候選：
- **(a)** dissolve=false→`set_subteam_parent`非 merge（pop>MAX/affinity 低/cohesion 高）。
- **(b)** `_resolve_join` 從沒 fire、joiner 沒 co-locate 到 host（JOIN 每 cadence 重 commit 沒到達=arrival-never）。
- **(c)** accept-reject loop（host 拒→release→cooldown→再選再拒）。
★同對隊 54× 重 commit→**傾向 (b)/(c)**（若 (a) 變 subteam 不會重 commit 同 target）。discriminating probe=`join.resolve` vs SurvivalMergeIn((b)則 join.resolve<<698)+`accept.join_reject`((c)高)。
## ★attribution
churn 機制**疑 pre-existing**（JOIN resolver/persist=main 非農業b）、**農業b ② pop-cap 塌放大**（弱領導 cap 2<<20→更多 overflow→更多絕境隊→churn 現形）。
## ★path 建議
1. **churn 另開 ticket**（hand-obeys-brain、implementer diagnose via probe pin a/b/c 後修=committed JOIN 須真 resolve/persist 到 arrival）。
2. **農業b ② tension 我校準**（抬 base floor/clamp effective 避弱領導塌→少 overflow→少 churn 現形）。
3. 農業b gate 待 churn+校準後 re-measure（churn confound ② overflow 率、measurer 已建議）。
★**決策**：先 (A)churn ticket 還是 (B)農業b 校準？我 lean 並行：churn ticket 交 implementer + 我農業b ② 校準 ruling（等 measurer ② 完整分布數字定校準量級）。labor-slice v2 已 R² 中平行。地基 KEEP。
