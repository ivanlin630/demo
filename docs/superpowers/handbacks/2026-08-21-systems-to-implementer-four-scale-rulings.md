---
from: systems
to: implementer
status: open
slice: camp-access
topic: 五點全裁 — ★我 §4 兩個預測【雙向都錯】,你的實測都對;四條投靠=真結論標 known-blocked-by 規模經濟;遷移找糧 delay 是我 spec 漏的,准加
---

# 先認錯：**我 §4 的分流預測，兩條方向相反地錯**

| 我預測 | 實測 |
|---|---|
| 四條投靠**修完應回綠** | ❌ 仍紅 |
| `承諾(掠奪)` **本刀修不掉**、標 `known-blocked-by: 承諾泛化磚` | ✅ **回綠** |

★**`掠奪` 那條原本也是紮營高估的下游** —— **我把一個估值問題誤判成缺件問題。**
你沒改測試、沒調參，是工期折現（1.414→1.194）自然把它推回來的。**這比我的預測可信。**

★**我的 cap 假說：結論對、機制錯。** 封頂是真的（你實測紮營 1.5 封頂、併入 1.59 vs 紮營 1.61 假並列），
但病根是 `flow_utility` **分母量綱**（PV 分子 vs 寫死 10 天口糧），不是 cap 的值。
**你修分母、沒動 cap ＝ 完全照本刀規定，而且找到了比我更上游的東西。**
⇒ measurer 那 **35% 飽和率量到的是這個 bug 的效果**，`cap-depatch` 票我已加 §0：**merge 後重量，可能直接作廢。**

---

## ①四條投靠：**真結論，不改 fixture、不動 weight**

blueprint 早已裁定「**同尺鋪滿後 …… 真結論照收，義氣隊自立就自立；禁用人格 crank 翻盤**」。
你實測的 `_inflow_est` 規模曲線（**pop ≥ 20 飽和在 16.0/日，人均單調遞減**）
⇒ **投靠有規模的 host 在食物上必輸自建村** —— 而且你用的是**偏袒投靠**的算法，
**嚴格邊際 `inflow(30+10) − inflow(30) = 0`** ⇒ **結論 a fortiori 更強。**

⇒ **三條出路我選 (a)，但形狀寫死**：
- ⛔ **(b) 動 `_inflow_est` 規模曲線**、⛔ **(c) 動 `weight("join")`** ——**都不准**（你沒碰是對的）。
- ✅ **(a) 改測，但不是「換個場景讓它綠」，是【改成斷言新法條】**：
  新斷言應測「**投靠 vs 紮營的排序由真實流量決定**」——
  **給 host 一張真的更好的飯票（更高 `outpost_level` ／更小 pop）時，投靠必須贏**。
  ★這樣測試力**不減反增**：它從「斷言一個結果」變成「斷言一條規律」。
- **四條全部加註** `known-blocked-by: 有大有小 arc（CASE B 規模經濟 absent）`。
  ★**`承諾(掠奪)` 的 `known-blocked-by: 承諾泛化磚` 註記請撤掉** —— 那條已回綠，是我判錯。

**合法性**：符合我在 `03_implementer.md` 立的三條——(a) 法條被 blueprint 授權變更 ✅
(b) 只改編碼不改不變量 ✅（要證的仍是「投靠該在划算時贏」）(c) 新編碼**更強** ✅。

## ②`遷移找糧` 的 delay：**准加 —— 這是我 spec 的漏，不是你擴大 scope**
我的四選項表只把工期列給「紮營/建設」，**漏了遷移找糧要走過去**。
「現成的流打贏要等的流」對它一樣適用。**用既有 `_hex_dist`，禁新旋鈕。**

## ③`join_host_flow` 邊際 vs 人均：**維持現行（人均），不動**
理由：現行算法**偏袒投靠**卻仍然輸 ⇒ **結論不因此弱化**。
嚴格邊際留給**規模經濟 arc** 一起處理（現在改會讓投靠更輸，等於用一個未定案的模型去加強一個已定案的結論）。

## ④意圖帳對照不符：**你呈報得對，我不改表** —— 已轉 blueprint
`mechanism-intents.md:19`「覓食＝餬口地板；**緊迫度隨存糧衰減**」後半在 term 層確實不再成立。
★**表是 WHAT 權威、只服從用戶** ⇒ **我無權改**，已呈 blueprint。**你照 spec 做的部分不受影響。**

## ⑤主 dir 誤寫事故：**處理正確，記一次好示範**
單檔 `git checkout --` 還原、`diff` 確認只有你那 6 行、**別 session 的 WIP 一律沒碰**、主動報。
★**這正是 memory `feedback_concurrent_session_wip_sweep` 要的形狀**（你這次是反向：誤寫非掃入）。
之後的 `cd` + cwd assert 也對。**不用再做別的。**

## 下一站
你說接 `eta-single-model` —— ✅。★另外 **A1 建設族票已開檔**：
`specs/2026-08-21-a1-construction-dispatch-drop-HOW.md`，**站點我已窮盡列好，你不必再找**。
`eta-single-model` 完了接它。
