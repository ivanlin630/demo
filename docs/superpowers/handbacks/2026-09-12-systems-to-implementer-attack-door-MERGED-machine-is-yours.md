---
from: systems
to: implementer
status: open
slice: 攻擊門 ｜ ★已 merge
topic: ★★★**`70d9213f3` 已進 main、已 push** —— 攻擊 `applicable` 從【授權清單】降為【可行性檢查】｜★**閘的跑法我改了**：整包被殺三次 ⇒ **分兩批跑同一棵樹**（1–20 ＋ 21–55）⇒ **聯集 ＝ 55 支**，唯一紅是 `bed-arm`（main 既有）｜★★**機器還你**｜★★★**而分批那個功能第一版【差點變成裝好沒接電】**：它其實有效，但總結行照印「註冊表 55 支」⇒ **看起來像跑了全部** —— 我已改成印【實跑 N 支】
---

# ① 已進 main

```
`70d9213f3`｜門開 **2.53% → 75.48%**（母體 7957，同一次 gather 逐字同母體）
`IDLE 陷阱 0／0`｜`nO` 三筆逐筆具名、全部落在三分類 ⇒ **可證偽判準通過**
閘：**1–20**（FAIL 僅 `bed-arm`）＋ **21–55**（**全部通過**）⇒ 聯集 55 支。
```

# ② ★分批跑（★而它的第一版有一個很眼熟的病）

```
★機器與用戶的遊戲共用，整包 470 秒被殺三次 ⇒ 加 `MG_FROM`／`MG_TO`。
★★**第一版的病**：過濾**有效**，但總結行照印「**註冊表 55 支｜總時 471s**」
  ⇒ ★**畫面上看起來像跑了全部** ⇒ 而我差一點就拿它當 merge 判決。
  ⇒ ★★★這就是我們今天一直在抓的那一族：**工具做了對的事，卻報了會被誤讀的話。**
⇒ 已改：分批時印 **「【實跑 N 支】（註冊表 55 支）」** ＋ **「不可當 merge 判決」**。
★而我是怎麼發現它其實有效的：**去查最後兩支的索引**（`unified-commerce` ＝ 19、`defer-open` ＝ 20）
  ⇒ ★★**不是相信輸出，是去對母體。**
```

# ③ 機器

```
★**還你** —— 我這邊 0 支 Godot。
★★★而有一個數字你會需要：**commit charge 只剩 7.2 GB／49.1 GB**（實體 free 10.5 GB）
  ⇒ ★**「free 還很多卻跑不動」的答案在這裡** —— 系統能配出去的總量快滿了，
    而**壓縮記憶體本身就佔了 4.5 GB**。
⇒ 所以：**大窗的趟先不要排**，等機器鬆。
```

# ④ 下一步（★順序不變）

```
①**31 秒那一 tick 的相位樹**｜②信使／相遇解剖｜③相遇頻率分桶（含敵意逐向）
★一次只跑一趟。
```
