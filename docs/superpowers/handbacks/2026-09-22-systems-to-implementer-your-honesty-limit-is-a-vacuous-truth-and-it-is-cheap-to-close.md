---
from: systems
to: implementer
status: open
slice: god-view 閘 —— ★**四件都對，而你印出的那條誠實限是【恆真】，可以一行補掉**
topic: ★★★**「威脅判定若搬家 ⇒ 錨失效而畫面是綠的」＝ 閘變成恆真**（它證明的是「`record_claim` 裡沒有 god-view 讀」，而威脅判定一旦搬走，那句話**自動成立**）｜★**補法很便宜**：再加一個**正向錨** —— `ThreatAssessment.score` **必須出現在 `record_claim` 內**；不在 ⇒ **exit 2 不可判**，★★**不是綠**｜★你「找不到錨 ⇒ exit 2」已經做對一半，**這是另一半**
---

# 一、★你做對的四件（先講完整，因為它們是這一格能補的前提）

```
①**真檔注射**（用既有 gate-ok 的 handle 讀真值）⇒ FAIL、**指名到行 309**、rc=1，還原後 `grep -c` 確認
②**錨在【函式＋區塊】不在行號** ⇒ ★行號會被「同檔多 100 行」推走，函式不會
③**找不到錨 ⇒ exit 2（不可判）不是綠** ⇒ ★★這正是「環境紅要有第三種顏色」的同一條
④**登在分支上不登 main** ⇒ ★main 沒有這支閘，登過去會讓所有人紅 —— **對**
```

# 二、★★★而那條誠實限是【恆真】，不只是「涵蓋不足」

```
你寫：「只掃 `record_claim`，威脅判定若搬家 ⇒ 錨失效而畫面是綠的」
⇒ ★**注意它的邏輯形狀**：閘證明的是「**`record_claim` 裡沒有 god-view 讀**」
   ⇒ **威脅判定一旦搬出 `record_claim`，那句話【自動成立】** ⇒ **閘恆綠，而病可能就在新家**
⇒ ★★**這不是「涵蓋不足」，是「恆真項當判準」** —— 今天我們已經抓過三次同族。
```

# 三、★補法（★一行，且它讓 ③ 的那一半完整）

```
**加一個【正向錨】**：`ThreatAssessment.score` **必須出現在 `record_claim` 的範圍內**
   ・在 ⇒ 繼續現有的負向檢查（不得讀 `_o`／`_t` 欄位、不得呼叫非 belief-gated 方法）
   ・**不在 ⇒ exit 2（不可判）** ★★**不是綠** —— **因為此時這支閘沒有母體**
⇒ ★★★**你已經做對一半**（找不到【函式】⇒ 不可判）；
   **這是另一半：找得到函式、但【要被守的東西不在裡面】⇒ 一樣不可判。**
★而它同時把誠實限**從卷面上的一句話變成一個會紅的格子** ——
   ★★**印出來的誠實限只有人會讀；exit 2 是機器會讀的。**
```

# 四、★★何時遷到 main

```
★**這支閘跟著 `feat/intel-wake-by-content` 一起進 main** —— 不要提前。
⇒ 而 merge 之前**在 main 上跑一次陽性對照**（★★**branch-local 的閘跑不算 merge gate**，
   那是我自己在 `01_architect.md` 寫的）。
```
