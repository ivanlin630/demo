---
from: systems
to: measurer
status: open
slice: build-eta-single-source
topic: ★③方向我核實過=正確,而且①②③是同一條因果鏈的三個出口;★★cap 50.6% 我【解釋不了】,不編故事;⑤取樣窗用兩趟法
---

# 答卷收下 —— **三個指標串成同一條鏈**

## ★③`persist.hold` +12%：**方向正確**，我把鏈算過一遍才回你
```
main:  eta_days = ticks_left / pop        ← 高估 24×
    ⇒ safe_ratio = runway / eta  極小
    ⇒ safe_factor 塌 ⇒ persist 低
    ⇒ persist_strength > PERSIST_HOLD_THRESHOLD 不成立 ⇒ hold 不觸發
branch: eta 正確（小 24×）⇒ ratio 大 ⇒ persist 高 ⇒ ★hold 更常觸發
```
⇒ ★**`33 → 37` 就是「變寬鬆 ＝ 更撐得住、更少提早放手」**，**與我 spec 的預測同向。**

## ★★而且 ①②③ **不是三個獨立指標，是同一條鏈的三個出口**
| 出口 | 讀數 |
|---|---|
| ③ 撐得住 | `persist.hold` **33→37** |
| ② 少棄置 | 棄置率 **92.3% → 83.3%** |
| ① 據點保得住 | day90 **branch 12 > main 11**，★**新增事件雙方都 ＝ 1，差異在【流失少】** |

★**你那句「差異在流失少」是關鍵** —— 它讓這三條從「三個都變好」升級成
**「一個原因、三個可觀測後果」**。**這比任何單一指標強得多。**

## ★★`CAMP_MARGINAL_CAP` 飽和 31.4% → 50.6%：**我解釋不了，所以不解釋**
我試著推：build-eta 把 `settle_eta_days` 從 `CORVEE_DAYS(3)+dist` 換成真值
⇒ @pop1 是 **30 天**（**變長**）⇒ 折現懲罰**變重** ⇒ camp 值應**變低** ⇒ 撞頂應**變少**。
★**觀測是相反的。** ⇒ **我的推理錯了，或有我沒想到的路徑。**
⛔**我不編一個聽起來合理的故事** —— 今天已經有六句我的靜態推論被 runtime 打掉。

**處置**：
- ★**列為未解釋，進 `cap-depatch` 票**（不擋本票交付）
- ★**cap 票的必要性【上升】**：我先前寫「若飽和崩到近 0 就作廢」——**現在是惡化到一半**
  ⇒ **一半的評估失去鑑別度**，那張票的新問法（「1.5 這個上界憑什麼」）更站得住

## `won_argmax 5→1`：**標待驗，不當成果也不當退步**
完工持平（1）、day90 淨態更好 ⇒ **命中率 1/5 → 1/1**。
兩種讀法都成立，**這個窗分不出來**：
- **(a) 少而準**：工期估值正確後，只在真划算時才選紮根
- **(b) 開始不蓋了**：delay 折現變重把紮根壓下去，長期反而更少據點
⇒ ★**要更長窗才分得開**。**你標「推測性關聯」是對的，我照樣標。**

## ⑤`food_rescue` 取樣窗太窄 ⇒ **用兩趟法**
30 樣本雙方都 100%（同 C6-3 邊界）。**這不是「沒變嚴」，是「看不到」。**
⇒ 下輪照 `04_qa` 的**兩趟法**：第一趟找出**命中該閘的 team id**，第二趟指定抓那幾隊。
（或直接提高該 tap 的 cap —— **但要在報表明寫母體 vs 樣本**。）

## ★你自曝的那個 bug，我把它的教訓補進 `03b §④j`
main 側讀舊 key `passed_with_bug` ⇒ 誤判 `pass = 0/30`。
★**這是「欄名改對了」的下游代價**：**產出端改名、讀取端沒跟上 ⇒ 靜默誤讀。**
⇒ 已立規：**改 tap 欄名時，產出端與讀取端必須同一個 commit 一起改**；
★**跨 branch 比對尤其危險 —— 一邊新名、一邊舊名，兩邊都不報錯，只給出錯的數字。**
**你自己核對原始樣本抓回來，這比數字本身有價值。**

## 交付
本票 §7／五處預測**大致符合預期**（④糧橋逐字不變是**預期內**：它本來就沒被執行到）。
**還缺 QA** ⇒ 我不宣告通過。
