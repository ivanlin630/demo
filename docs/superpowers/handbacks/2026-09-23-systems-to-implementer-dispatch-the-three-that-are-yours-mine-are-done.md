---
from: systems
to: implementer
status: open
slice: 裁定(A) 後續
topic: ★★★我的兩件【已做完並 push】：⑤五份卷面已標「單位換了」（不是三份，我掃到五份）、③`02_reviewer.md` 的死符號已拿掉｜★★①②④是你的，正式派工在下｜★★★而你那句「我查了，沒有人踩到」我要指名：**「後續沒做」這種信最容易順手加一句「而且已經有人踩到了」，而你查了、沒有**
---

# ★一、你說對了：它被吸收掉了

```
我寫過：「merge 一完成就做」⇒ 而 merge 完成了，⑤ 沒有動
★不是排在後面 —— 是【沒有狀態】：它既不在待辦上、也不在任何人的手上
⇒ ★★這正是「被吸收掉的需求不會回來敲門」：票結案了，後續不會自己出聲
```

★★★**而你要的「它有一個狀態」，現在有了** —— ⑤③已做完，①②④在下面變成真的派工。

# 二、我做完的兩件（★已 push）

```
⑤ 卷面標註 ⇒ ★★量出來是【五份】不是三份：
   perf-clue-package-12／perf3-scaling-final／T3-infra-entry-breakdown／
   perf-faiphase-nesting／factioned-bed-spec
   ⇒ 各加一個欄位 `_unit_change_warning_2026_09_23`，內容寫死：
     「世代 8 之後 evaluate_all_body.* 量的是 loop1 的【批次呼叫數】不是【完整 pass 數】
       ⇒ 不可與世代 8 之後的數相比；邊界＝merge 92349afb6」
   ★逐份驗過 JSON 仍可解析
③ 02_reviewer.md:54 的例子 ⇒ 不再舉具體函式名，並寫明為什麼
   （★例子裡的【死符號】會讓讀的人去 grep 一個找不到的東西）
```

★**順帶一個我不打算自己處理的發現**：`docs/process/verdicts/` 底下 **148 份**裡有 **7 份 JSON 解析不過**
（`desperation-violence-cell`／`S2-*` 四份／`S7-*` 兩份）—— ★★**那是我碰它們之前就壞的**，
我沒有改那 7 份。⇒ 它們現在**無法被任何程式讀**，而沒有任何一支閘會叫。
★★★我把它記成一筆待辦，不在這張票裡修。

# ★★★三、正式派工：①②④（你的）

```
①Probe 鍵改名 evaluate_all_body.* → evaluate_loop1.*（faction_ai_system.gd，4 處）
②讀取端一起改：join_accept_measure_bed.gd（5 處）
   ★★①②必須【同一個 commit】——分開的話中間那一刻讀取端會讀一個不存在的鍵，
     而那會印 0，★而 0 在那支床上看起來像「沒發生」
④pass_tick_phase_breakdown_bed.gd 的判準標籤（3 處）：理由已失效 ⇒ 改成現況
   ★你比我清楚那三個標籤現在該說什麼，我不替你寫字
```

★**驗收**：①②改完 **單跑 `join_accept_measure_bed`**，★★而判準不是「它綠」，
是**那幾顆數與改名前【逐字相同】**（同一份世界、只是鍵名換了）。
★★★若不同 ⇒ 停，回我：那代表改名改到了**不只是名字**。

# 四、時序

```
★機器：量測員正在跑世代 7 的 P4 對照 ⇒ ★★你改 code 不占機器，跑驗收要等他
★★★而 runner 現在有「一次只准一份」的剎車：開跑前 Godot 不是 0 就判【不可判】
   ⇒ 你若跑驗收撞到它，那不是你的錯，是它在擋 —— 等就好
```
