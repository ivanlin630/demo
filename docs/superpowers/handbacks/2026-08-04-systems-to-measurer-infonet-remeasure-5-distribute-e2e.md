---
from: systems
to: measurer
status: consumed
topic: "[資訊網whole RE-measure #5=★症1端到端閉環 on FACTION bed(distribute de-scan後,39b7b33d,arc最後一哩):branch feat/info-network-whole 39b7b33d,worktree(★godot --path禁原地checkout,★GODOT_TIMEOUT=1200 implementer踩過360s stale-file坑,長跑必設)·de-scan移goal_resolver god-view live-read(_resident_food_runway領主直掃殘留)+死常數門檻→relief憑送達belief(buy-order qty)×人格honor·★★核心=症1端到端閉環(前輪letter delivered 8/8領主真聞但distribute卡god-view+死常數→本輪該通):①distribute.dispatch>0(領主聞distress→憑belief+人格賑濟,前輪全0)②food_delivered/convoy送糧>0③★糧真到resident runway回升(端到端真效果feedback_verify_execution_end非只candidate)·全arc迴歸:letter delivered保持+scout 35/40+Part1+3商業+人格分化(honor高救子民/務實派傲撐)+constitution_gate綠(god-view移除該不增violation)+determinism byte-identical(GODOT_TIMEOUT=1200 fresh)+economy不爆·bed:lord_distribution_bed/peaceful_economy_bed(faction症1)+warring(scout/商業/regression seed-cascade已知)·canonical WarringHarness掛specimen中性+specimen trace逐motive→action→outcome(求援信→賑濟→糧到 vs 死因)餵QA故事稽核·誠實measured回systems別下accept·★這是arc culminating measure,綠→我路QA故事稽核(回溯三因果jia/famine/anomaly+whole出verdict ref機械閘)→blueprint推用戶驗收"
---

# 資訊網 whole RE-measure #5 = ★症1 端到端閉環 on FACTION bed（arc 最後一哩）

distribute de-scan 交付（`39b7b33d`）：移 god-view live-read（`_resident_food_runway` 領主直掃殘留）+ 死常數門檻 → relief 憑送達 belief（buy-order qty）× 人格 honor。**前輪 letter delivered 8/8、領主真聞、但 distribute 卡 god-view+死常數 → 本輪該通。**
**branch** `feat/info-network-whole 39b7b33d`、worktree（★`godot --path`、禁原地 checkout、**★`GODOT_TIMEOUT=1200`**——implementer 踩過 360s stale-file 坑、長跑必設）。

## ★★核心＝症1 端到端閉環（前 4 輪一直卡的最後一哩）
**bed**：`lord_distribution_bed.gd` / `peaceful_economy_bed.gd`（faction 症1 場景）。
1. **`distribute.dispatch > 0`**（領主聞 distress→憑 belief+人格賑濟；前輪全 0）。
2. **`food_delivered` / convoy 送糧 > 0**。
3. **★糧真到 resident runway 回升**（端到端真效果、[[feedback_verify_execution_end]]、非只 candidate 生成）。

## 全 arc 迴歸
- letter `delivered` 保持 + scout 35/40 + Part1+3 商業（trade.deal/order_fulfilled/board.relay）不退。
- 人格分化（honor 高救子民 / 務實派傲撐）+ **constitution_gate 綠**（god-view 移除、該不增 violation）+ determinism byte-identical（`GODOT_TIMEOUT=1200` fresh）+ economy 不爆（reserve 守）。
- warring（scout/商業/regression=seed-cascade 已知類）。

## canonical harness + specimen（餵 QA）
- canonical `WarringHarness.run()` 掛 specimen 中性 + faction bed specimen trace 逐 motive→action→outcome（**求援信→賑濟→糧到 vs 死因**）→ 餵 QA 故事稽核。

## 交付
- **誠實 measured 回 `to:systems`**（別下 accept）。escaped_defects 記。
- **★這是 arc culminating measure**：綠 → 我路 **QA 故事稽核（回溯三因果 jia/famine/anomaly + whole、出 verdict ref、機械閘）→ blueprint 推用戶驗收**。灰/regression → 報 `to:systems`。
