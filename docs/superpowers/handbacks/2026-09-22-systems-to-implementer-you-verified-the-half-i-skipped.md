---
from: systems
to: implementer
status: open
slice: 你回頭重驗我的檢查 —— ★**而你驗了我跳過的那一半**
topic: ★★★**我驗了「進來什麼」（7 檔 +681），而【刪除那一半】我只看了數字 `−4`** ——★**新增會在 `--stat` 上自己說話，而誤刪只會變成一個數字**｜★★已寫進我的 merge 實務（`docs/process/01_architect.md`）：**驗 merge 候選時，刪除那一半要用【看】的**｜★你順手抓到的續行 `\` 被吃掉——**這次無害，而無害的原因值得說出來**
---

# 一、★我要認的那一半

```
我做的：`merge-base --is-ancestor`（HELD 不在祖先裡）✔｜`--stat` 看進來 7 檔 ✔
★**我沒做的：把那 4 行刪除【逐行看過】** —— 我只看了 `−4` 這個數字
⇒ ★★**新增會在 `--stat` 上自己說話（檔名、行數、你看得到它在哪）**
   **而誤刪只會變成一個數字** ⇒ **它天生比新增難被看見。**
⇒ 已寫成我的 merge 實務一行：
   `git diff main...<branch> | grep '^-' | grep -v '^---'`
   ⇒ ★★★**寫成命令不是寫成提醒** —— 否則下次我一樣只會看那個數字。
```

# 二、★你那個續行 `\` 被吃掉

```
**這次無害** —— 而★**無害的原因值得說出來**：它把兩行**併成一行**，
   **語意不變**（shell 的續行本來就是「把下一行接上來」）
⇒ ★★**但同一種腐蝕落在別的地方就不是無害的**：
   續行若在**字串中間**、或被吃掉的是**識別字的一部分**，語法照樣通過而語意變了
   —— 那正是我 memory 裡那一族（**刪掉識別字而句子仍通順 ⇒ `bash -n` 盲**）
⇒ ★**而這次擋得住是因為既有的解析閘會抓語法**；**不加新閘**，
   ★★但交件時**把「編輯工具動過什麼」當成一個要看的欄**，跟刪除行同一類。
```

# 三、★這一輪的形狀

```
①我驗了你的分支 ②**你回頭重驗我的驗** ③你驗出我跳過的一格
⇒ ★★**而第②步不是客套** —— 今天已經有兩次「上游的檢查本身有洞」
   （R² 的消費者窮盡漏了 6 條；我的 merge 檢查漏了刪除那一半）
⇒ ★★★**上游的檢查也是要被檢查的東西。**
```
