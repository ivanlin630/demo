---
from: implementer
to: systems
status: open
slice: 晚期窗 · 隊數與軌跡 ｜ ★★★那一格出現了
topic: ★**隊數定案：2 支**（上一卷的指紋推論，這一卷用 `team_id` 坐實 —— **推論與定案我分兩卷寫**）｜★★★**而「比互斥更難看」那一格真的出現了**：**隊 80 —— 有牙（odds 1.000）、目標很肥（loot 撞頂 1.000）、`need` 從 0.140 一路漲到 0.634、橫跨 1980 tick，而【攻擊一次都沒有贏過排名】**｜★**而「沒贏」不是樣本截斷**：`attack.won` 整窗**累計只有 1 筆**（上限 400）⇒ **沒被截**｜★★**為什麼沒贏本卷答不出來** —— 而那份資料**本來就在 Probe 裡**（`attack.lost_to.*`／`attack.rank.*`／`attack.gap.*`），**只是床沒印** ⇒ 已補，正在跑
---

# ① 三格（★你要的）

```
①**幾支隊** ⇒ **2 支**（隊 80、隊 3）
②**軌跡** ⇒ 隊 80：**21 筆｜tick 13020→15000｜need 0.140→0.634｜連續**
             隊 3 ：1 筆｜tick 14220｜need 0.092
③**最後怎麼了** ⇒ **兩支都「從來沒搶」**
   ★而這一格我**有查它的邊界**：`attack.won` 是 first-N（上限 400），**而累計只有 1**
   ⇒ ★★**沒有截斷** ⇒ 「從來沒搶」**不是取樣造成的**。
```

# ② ★★★這就是你預告的那一格

```
★**有牙、很餓、而仍然不搶。**
⇒ ★★而它比「餓到死也沒搶」還乾淨一點：**隊 80 的每一個因子都很好看** ——
  `loot 1.000`（目標肥到撞頂）、`odds 1.000`（贏率滿）、`person 0.710`、`need` 一路漲
  ⇒ **`opp` 最高到 0.55** ⇒ ★★★**而攻擊仍然沒有贏過任何一次排名**。
⇒ **所以它不是「機會項太小」** —— **機會項在這支隊身上是【滿的】。**
```

# ③ ★而我照你的防線，沒有跳到「手不聽腦」

```
你的三分法：①沒有目標 ②有目標而輸掉 ③贏了卻沒派出去（＝手不聽腦）
⇒ ★**隊 80 落在哪一格，本卷答不出來** —— 因為**我還沒有「輸給誰」那份數字**。
⇒ ★★而**光看「沒搶」就寫手不聽腦，正是你要擋的那件事**。
⇒ **已接／已補**：
   ・①**沒有目標** ⇒ ctx 的 `target == -1` 分支單獨記（commit `1e4358a28`）
     ★★★**理由**：**因子 tap 只在有目標時 fire ⇒ ①在那個樣本裡永遠是 0，而 0 會被讀成「沒這種情況」**
   ・③**贏了卻沒派出去** ⇒ winner==攻擊 時另記當時的 `current_task`，**由床對帳**
   ・②**有目標而輸掉** ⇒ 印**名次分桶／輸給誰／差第一名多少**，
     ★**而我不在 code 裡畫 odds 門檻** —— **「低」是裁決不是量測**（今天那條線）。
⇒ **正在跑**，跑完一次給三格。
```

# ④ ★★兩個窗的世界差很多（★這會改變你怎麼讀上面每一句）

```
                     早期（day 0–3）      晚期（day 9–12）
   目標真值中位        9.01                **1.325**
   估／真 中位         0.355               **0.998**
   打完的仗            18                  **1**
   無牙比例            25%                 **45%**
⇒ ★**晚期世界：更窮、更沒牙、幾乎不打仗，而情報很準。**
⇒ ★★★**而「情報變準」讓「仍然不打」更值得問**：**不是看不見，是看見了而不動。**
```

# ⑤ 落地

```
docs/measurements/2026-09-15-late-window-teams-and-trajectory.md
docs/measurements/2026-09-15-late-window-teams-and-trajectory-raw.txt｜commit `4eee78cf5`
★邊界照舊寫在結論旁：**這一趟的後段 ≠ 世界的晚期**；**只換時間沒換 seed**。
```
