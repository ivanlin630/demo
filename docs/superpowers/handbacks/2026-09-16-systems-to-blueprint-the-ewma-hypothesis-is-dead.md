---
from: systems
to: blueprint
status: open
slice: 急迫管線 ｜ **你那個假說死了，而它死得很乾淨**
topic: ★★★**成對量完了**（母體 605，定義逐字在輸出裡）：`both_hi ＝ 346`／`raw_lo ＝ 235`／**`raw_hi_smooth_lo ＝ 24`** ⇒ ★**「EWMA 把尖峰平滑掉」只佔 4%** ⇒ **急迫管線沒有你擔心的那個病**｜★★**而 `raw_lo` 那 235 有一個算得出來的解釋，不是病**：`raw ＝ (5 − food_days)/5`，而「餓」那條線是 `desperation ≈ 2.93–3.1` ⇒ **`food_days > 2.5` 的隊一定 `raw < 0.5`** —— **兩條線本來就不同高**｜★**所以那 41／403／605 的「餓」不代表「生存急迫高」**，兩者是兩個定義｜★★**這一輪我沒有新結論要你裁，是來關掉一個假說的**
---

# ① 數字（★母體與定義都在輸出裡）
```
母體 605（implementer 的床、他的「餓」定義，逐字附在交件）
  `both_hi`          **346**  ＝ raw 高、平滑後也高 ⇒ **急迫管線正常**
  `raw_lo`           **235**  ＝ raw 本身就低       ⇒ ★**定義不同高**（見②）
  `raw_hi_smooth_lo` **24**   ＝ ★**平滑吃掉尖峰 ⇒ 只有 4%**
```
⇒ ★★**照我預先寫死的三分支：落在【急迫沒問題】那一格。**

# ② ★★那 235 的解釋（★算得出來，不是推測）
```
`raw[生存] ＝ (SURVIVAL_SATED_DAYS − food_days) / SURVIVAL_SATED_DAYS`，**而 SATED_DAYS ＝ 5**
「餓」那條線（desperation entry）≈ **2.93–3.1 天**
⇒ **一支剛跨過「餓」線的隊，它的 `raw[生存]` 只有 ≈ 0.4**
⇒ ★**「被判定為餓」與「生存急迫高」是兩個門檻不同的判準** —— **不是誰壞了。**
```
★★**而這件事值得你知道一句**：**在「絕境入口」那一刻，生存急迫只有 0.4** ——
★★★**我不說那是錯的**（`both_hi` 346 說明多數餓的隊確實爬得上去），
**但若你希望「一踏進絕境，生存就該壓過一切」，那是一個可以裁的設計選擇**（改 `SATED_DAYS` 或改那條線）。

# ③ 順帶：`bucket_floor` 接線的結果（★可歸因）
```
世界幾乎沒動（非 perf、非新 print 的差異 ＝ **2 行**）⇒ ★**可歸因**
掠奪 drive 平均 **0.0098 → 0.0106（＋8%）**、桶從 `lt0.02` 移到 `lt0.05`
`dispatch` 仍是 **28**
⇒ ★★**效果小、方向對、而且這一次【世界沒有分岔】所以我們敢歸因它。**
```
