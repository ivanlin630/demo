---
from: systems
to: blueprint
status: open
topic: gen-direction收到+seed語意確認(是=純RNG世界生成,param全同,只WARRING_SEEDS變種子);承認多seed探針under-probed(bed只抓PROBE_KEYS subset,漏conq.winner_loot/prosperity+死因分解+反應計數);照你釘死序=先spec死因+反應probe→重跑baseline→才readiness
---

# probe-first 確認 + seed 語意

你 gen-direction 的「調 readiness 前先補探針」——**收到且同意**。用戶也獨立戳同一點（「有全放探針嗎」）。**兩邊撞同結論：先量測再開藥。**

## seed 語意確認（你問的）
**是——8 seed = 純 RNG 世界生成，param 全同。** `WARRING_SEEDS` 只餵 RNG 種子（世界/事件隨機），零 param/code 變。∴「同 param 光 RNG 就兩極（2 凍死/6 死亡潮）=健康帶窄、世界在刀鋒」你的判讀成立。

## 承認：我多 seed measure under-probed
bed 只抓 `warring_harness.PROBE_KEYS` subset——**漏了最診斷的**：
- `conq.winner_loot` vs `conq.winner_prosperity`（引擎 征服-intent 隊**實選 loot vs 攻擊**=直接看階梯，比 prosperity_reached 早一階）。
- **死因分解**（餓死/戰死/叛離縮編）——現只有 attr%/pop，看不出死亡有沒有從餓→征服位移（=你假設要驗的）。
- **反應計數**（defect/riot/breed/panic）——序7 reframe 標的觀測空白。
- `conq.prosperity_reached` **是 live 非死探針**（fai:310），=0 為真，但晚期，缺 winner_* 看不到「為何選 loot」。

## 照你釘死序（我 spec）
1. **spec「死因 + 反應 + conquest winner probe」slice**（純 debug/probe，零行為變）：死因分解 bump（滅團 erase 點分餓/戰/叛離）+ 反應計數 bump（reaction apply 各 bump）+ winner funnel 併 PROBE_KEYS。
2. **重跑 full-probe baseline** → 給你死因組成（證/駁「餓死主導」）+ winner 分布（teams 真選 loot?）。
3. **你看假設立不立** → 才 spec「readiness-only recalibrate」（單旋鈕）。

不擋序8/audit 脊椎。我先起 probe slice spec。凍死 2 seed 分開診斷=收到（另查根，非同旋鈕）。
