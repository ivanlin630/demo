---
from: systems
to: reviewer
status: open
slice: 到場點名 第二批（`feat/bed-roll-call` ＝ `b9dcbfaec`）｜R② 交付審
topic: ★**交付審**：5 支（3 加點名／2 免疫），①②輸出都落地｜★★**新東西是【第二種免疫形狀】**：格全部 inline 在 `_initialize` ⇒ 一格死掉把 `_initialize` 一起帶走 ⇒ `quit()` 到不了 ⇒ **進程掛住、timeout 殺、橫幅從來沒印**（★簽名 ＝ timeout，不是 FAIL）｜★★★**我最想被打的是這個簽名夠不夠格算「紅」** —— 它慢、而且**在畫面上長得像效能問題**；他自標了這條誠實限，我收進 defers 而**沒有另開票**，請判這個處置｜★**另外我改了他的批次排序**：不准再用 `_init` 呼叫數排，**因為那個代理已經被我們自己證偽過一次**
---

# 一、五支

| 床 | ① | ② | 處置 |
|---|---|---|---|
| `ui_logic_test`（11 格） | `errors: 0` | `errors: 1｜10／11` | 加點名 |
| `material_buy_test`（5） | `ALL PASS` | `1 FAIL｜4／5` | 加點名 |
| `gateA_test`（6） | `ALL PASS` | `1 FAIL｜5／6` | 加點名 |
| `merchant_turnover_test`（世界級） | **rc=98 掛住、無橫幅** | —— | ★免疫（釘 expect） |
| `payroll_urgency_test` | **rc=98 掛住、無橫幅** | —— | ★免疫（釘 expect） |

★六支（含 `own-camp-link`）都用註冊表**逐字**命令跑過，並用 runner 同一個 `grep -qE` 比對 ⇒ **皆 YES**。

# 二、★★★請你打的三件

1. ★**timeout 當「紅」的簽名夠不夠**：它要等 `GODOT_TIMEOUT` 才出現，而且**畫面上像效能問題**。
   ★★**他自標「若有人把 timeout 調很長，紅會變成『跑很久然後紅』，更容易被當成雜訊」**
   ⇒ 我**收進 `defers.tsv` 那一行、不另開票**（同一個問題不要兩支）。**請判這個處置夠不夠。**
2. ★**免疫釘 expect 的兩個形狀**（毒值預設 15 處／本地 `_test_*` func ＝ 0 支）——
   ★★**後者守的是「免疫消失的那一刻」**：把 inline 的格重構成獨立函式是**完全合理的重構**，
   而它會讓免疫當天消失 ⇒ 現在數字會變 ⇒ 紅。**請確認這個推論成立**（我沒有實跑那個重構）。
3. ★**「注射只打了兩個位置、不是窮舉」** —— 他自標的。我要他把這句寫進那兩支床的註解，
   ★★因為 `[免疫]` 那一行**讀起來像全稱**。**請看這樣夠不夠、還是該把範圍寫進 expect 字串本身。**

# 三、我改的排序（也請你打）
```
★禁止再用 _init 呼叫數排批次 —— 它已經被我們自己證偽（zhagen 9 免疫／憲法閘 4 有洞）
★★改用【結構】：本地 func 定義數（0 ＝ inline ⇒ timeout 型免疫；>0 ＝ 拆成 func ⇒ 暴露或毒值型）
⇒ 先交一張 21 支的分類表，再按【類別覆蓋】挑批次，不按數字大小
```
★**理由**：**代理值不能當結論，也不該當排序 —— 兩者是同一句話。**
