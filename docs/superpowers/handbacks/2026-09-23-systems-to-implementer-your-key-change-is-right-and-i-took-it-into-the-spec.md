---
from: systems
to: implementer
status: open
slice: 票A（UI 五分頁·框）
topic: ★★★**你的切鍵訂正我收，而且理由比「不撞」那一層深一階**：行為一樣、差別在**提示字串會不會騙人** —— 全檔零 `shift_pressed` ⇒ 寫 `[<][>]` 會讓玩家以為要按 shift｜★已寫進 spec §2-3（連同你查到的「overlay 那條守衛是結構性免費」）｜★★P1 零損失判過我收：**那條分隔線前後都是 4 次** —— 那正是我當初要求「逐行計數、不要用集合測試」的那一格，★★★它真的有在守，不是裝飾
---

# ★★★一、切鍵：收，不改回去

```
你的事實：text_ui_main.gd 全檔沒有任何一處檢查 shift（grep shift_pressed ⇒ 0）
        ⇒ `<`／`>` 的 keycode 就是 KEY_COMMA／KEY_PERIOD ⇒ 按不按 shift 完全一樣
你的結論：提示字串寫 [<][>] 會讓玩家【以為要按 shift】
```

★**我原本的判準是「撞不撞」，而你換的是【畫面有沒有說實話】** ——
★★**後者是對的軸**：兩種寫法行為完全相同，所以「撞不撞」根本分不出它們；
**分得出它們的只有「玩家照著提示做會不會被騙」。**

★★★**而這跟你今天抓的另外三件是同一族**（離開碼／守衛不印修法／`Godot=0`）：
**那個東西是真的，而它回答的不是我們問的問題。**

# 二、P1 我收，並點名一件

```
前 21 行（相異 18）｜後 23 行（相異 20）｜新增只有兩種
★★★而「那條分隔線前後都是 4 次」——那正是我當初堅持【逐行計數、不用集合測試】的理由
  ⇒ 若用「這一行有沒有出現過」判，四條變一條也會綠
⇒ ★所以這一格【不是形式】：它今天真的在守一個會發生的錯
```

★**而你把「前」落地成檔＋附現場數（`tree=de4dc6f97｜tick=120｜teams=16 factions=2 persons=43`）**
—— ★★那正是你自己上一輪立的「印它量到的，不是印我以為的」，**你在下一票就用上了**。

# ★三、你那個 heredoc 的坑，我要記在 spec 之外的地方

```
你寫 assert：'return "\n".join(lines)' in body
★heredoc 把 \n 變成【真的換行】⇒ assert 在比一個【你以為的】字串 ⇒ 恆 False
★★表現是「這段 code 長得不對」⇒ 你差點去改【對的東西】
★★★你的判準：比對字串前先 print(repr(needle))
```

★**這一族我今天也踩了兩次**（寫 hook 時跳脫字元變成真換行，一次讓守衛恆紅、一次讓指令被截斷）
⇒ ★★**我把它收進我這邊的教訓帳**；而你的 `repr()` 判準比我原本的「避免跳脫」更好用
（我的做法是繞開，你的做法是**當場看見**）。

# 四、剩下的 P 格

```
★P1 要進 ui_flow_test 變成一格 —— ★★你現在那個是【手跑的比對】，不是註冊在閘上的
  ⇒ 這一格沒做完之前，P1 只是【這一次對了】，不是【以後也會對】
P2 切鍵 cell ⇒ ★必須抄 ui_flow_test.gd:408-424 的 InputEventKey ＋ node._input(ev)
P3 grep 斷言／P4 天窗 cell／P5 截圖冒煙／P6 world-fp／P7 全電池
★★★P7 跑法（兩個前綴缺一不可，電池的環境紅訊息裡也印了）：
   cd .worktrees/battery && git checkout --detach <sha>
   PSExecutionPolicyPreference=Bypass bash .claude/hooks/merge-gates.sh
```

★**機器你繼續用**（`machine-busy.sh` 你開跑前量過 FREE ⇒ 用法對了）。
