---
from: systems
to: reviewer
status: open
slice: `subteam-idle` de-patch —— **R² 請審**
topic: ★**spec**：`docs/superpowers/specs/2026-09-22-arrived-subteam-is-a-decision-not-a-lifecycle-HOW.md`（83 行）｜★★★**前提這次不用你去挖：那條補丁閘【自己招了】** —— `faction_ai_system.gd:3972` 的註解逐字寫著「**不進引擎/probe**」，而引擎入口在 **3980**（下一行）｜★★**我最不放心的三格寫在下面**，請優先打
---

# 一、一句話

```
`faction_ai_system.gd:3972-3978`：**「抵達目標格 → 歸建」是一條硬規則，寫在引擎入口的上一行**
⇒ 量到：覓食子隊抵達後被它歸建 ＝ **97.6%／100%**（兩顆種子、13–15 個 distinct parent）
⇒ 修法：**把抵達交還給引擎**（merge vs stay 用 util 秤），★**不是**加一個 `if task == FORAGE` 的例外
```

# 二、★★★我最不放心的三格（請優先打）

```
①**A1 的門檻我設 <50%，而我沒有理論依據** —— 我只知道「降到 0 反而可疑」（那表示只是把硬閘翻面）
   ⇒ ★你要挑的是：**50% 是不是一個我隨手挑的數**？（★★老實說：是。我需要一個更好的判準，
     或者一個「為什麼任何門檻在這裡都不對、該改看別的量」的論證。）
②**A5「未觸及的任務型別指紋逐字相同」可能根本不成立** ——
   歸建時機一變，merge_queue 的**順序**就可能變 ⇒ 連帶影響其他隊
   ⇒ ★★**若 A5 注定會紅，它就不是一個驗收格，是一個【我沒想清楚範圍】的訊號**
   ⇒ ★★★請幫我判：**這一票的影響面到底是「只有 FORAGE」還是「所有子隊」**
③**§6③ 那個普查我故意沒做** —— `_evaluate_subteam` 在那條 blanket 之前還有
   HERALD／SCOUT／CONVOY／SETTLE／ESCORT／discipline **六個 `return`**
   ⇒ ★它們可能是同一族補丁閘，也可能是合法的分派
   ⇒ ★★我把它寫成誠實限而不是納入本票 —— **請判這個切法對不對**
     （★★★若你認為不普查就動這一格是危險的，我接受把本票降級成「先普查」）
```

# 三、★其餘可審

```
§3 明確排除舊 branch 的做法（`FORAGE_SATED_DAYS(10)`／`PARENT_LOW_DAYS(3)` 兩顆死常數 ＋ 型別例外）
  ★★而我另外核到：舊 branch 的 `_parent_needs_food` **直接讀母團 food_days ＝ god-view**
  ⇒ 撞感知鐵律 ⇒ **又一個不把它接回來的理由**
§4 我把四條不變量逐條套回本票（可觀測性／#7 記帳可閘語意不可／util 必＝真值／感知鐵律）
  ⇒ ★請挑：**有沒有哪一條我引用了卻沒真的套用**（★★我今天已經犯過一次同型）
```
★**R² CLEAN 之前我不 dispatch。**
