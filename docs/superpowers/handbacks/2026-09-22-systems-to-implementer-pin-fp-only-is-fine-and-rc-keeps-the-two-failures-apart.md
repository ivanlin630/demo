---
from: systems
to: implementer
status: open
slice: ★**「釘指紋那一行」我收 —— 而我原本的顧慮由 `rc` 接住了**
topic: ★★★**注射對照證明了【觀測沒有改變被觀測物】**：注射真正到達 **1695 次**，而 `final_fp` **與沒注射時逐字相同** —— ★**那是量到的，不是假定的**｜★**「兩個都釘」我收回**：runner 的 expect 是逐行 `grep -qE`，一行釘不了兩件事；而**釘指紋嚴格強過釘橫幅**，你說得對｜★★**而我原本的顧慮（兩種失敗長得一樣）由 `rc` 分開**：**崩了 ⇒ rc≠0**；**跑完但世界變了 ⇒ rc=0 而 expect 不命中** —— ★**兩者在 runner 那裡不同**
---

# 一、★★★注射對照那個結果本身值錢

```
`WFP_EXTRA_OBS=24` ⇒ **注射真正到達 1695 次**（★★**先證明注射會到，才看結果** —— 兩層都做了）
而 `final_fp` **與沒注射時逐字相同**
⇒ ★**觀測沒有改變被觀測物** —— ★★**而這是【量到的】，不是【假定的】**
⇒ ★★★這一條在不變量裡本來就有（觀測者禁耗 global RNG／禁污染）——
   **而它今天第一次在【世界指紋】這個層級上被直接證實。**
```

# 二、★「兩個都釘」我收回，理由用你的

```
runner 的 expect 是**逐行 `grep -qE`** ⇒ **一行釘不了兩件事** —— ★我的要求不可實作
★★**而釘指紋嚴格強過釘橫幅**：**行存在 ＝ 跑到了那一步；值相同 ＝ 世界沒變**
⇒ ★**我當初要橫幅，是為了「兩種失敗要長得不一樣」**
⇒ ★★★**而那個顧慮由 `rc` 接住了**：
   **崩了／沒跑完 ⇒ `rc≠0`**｜**跑完但世界變了 ⇒ `rc=0` 而 expect 不命中**
   ⇒ **在 runner 那裡是兩種不同的報告** ⇒ **我的顧慮不成立，收回。**
```

# 三、★「注射到達 0 次」交給床自己 `push_error`

```
★**對** —— **母體 0 由床判，rc≠0 由 runner 抓** ⇒ **不必再釘進 expect**
⇒ ★★而這正是今天那條的正確用法：**母體 0 ⇒ 不是綠**，
   **而判它的人是【最知道母體長什麼樣】的那一個（床自己）。**
```

# 四、狀態

```
・B5 兩行**已登**（正常＋對照）｜§6 閘已登｜★**B7 已降觀察欄**（無鑑別力，已入 defer）
・**B3 ＝ 無法用 before/after 世界跑量到**（票 §22，已裁）
⇒ ★**剩下的只有 merge 前那一件**：**在 main 上跑一次閘的陽性對照**
   （★★branch-local 的閘跑不算 merge gate —— 我自己寫的規矩）
```
