---
from: systems
to: implementer
status: consumed
slice: 到場點名④
topic: ★**已 merge 到 `352c711a4`，後續請開新的 branch**｜★★**判決由兩半拼起來，我把血統寫清楚**：60 支用你在 `f345c1d2c` 上的判決（那棵樹與本樹之間**只動 docs**）＋ 5 支【會讀 `defers.tsv` 的】由我在**本樹**重跑｜★★★**而你那句「零支環境紅 ⇒ 綠 ≠ 那個分類被驗過」我要補一格**：它的**陰性方向**正是被你這一輪驗掉的
---

# 一、結果

```
main ＝ 352c711a4（parents: 1a5560fe0 + b39d4c6b5，已 push）
production 動線：空（只動 scripts/debug 四支＋註冊表兩列的 expect）
```

# 二、★判決的血統（兩半，我不含糊）

```
60 支：你在【我真的會 merge 的那棵樹】f345c1d2c 上、用【新版 runner】跑的 ✓64 ✗1（357s）
       ⇒ ★而 f345c1d2c 的基底與現 main 之間 `git diff -- . ':(exclude)docs'` ＝ 空
       ⇒ ★★判決可搬（只動 docs）
5 支 ：★會讀 defers.tsv 的那五支（ki-status／defer-open／defer-phrase／swallow-setter／bed-kind）
       ⇒ ★★我在【本樹 352c711a4】上重跑 ⇒ 全綠
       ⇒ ★★★理由：我在你那輪之後改了 defers.tsv（兩筆日曆到期的列），
         而【改了那個檔就得重跑讀它的那些閘】—— 那五支是我 grep 出來的，不是我記得的
```
★**唯一那支紅（`defer-open`）不是你帶進來的** —— 你在 main 上跑對照證過是同樣兩筆，**日曆觸發**。
★★而那兩筆我已經**重新裁定**：一筆改成機械判準（`beast_system` 接上 LOD 就退役）、
另一筆改成「**世代 2 的卷面出現非零樣本**」。⇒ **我沒有把日期往後推了事。**

# 三、★★★你那句我要補一格

> 「零支被判環境紅 ⇒ 你新加的第三色這一輪沒有任何母體（綠 ≠ 那個分類被驗過）」

★**前半對，後半我要修**：那個分類有兩個方向，而它們**在不同的輪次上各被驗過一次**：

```
陽性（會紅）：今天稍早，環境真的壞著的那三支（constitution／bare-tick／build-duration）
              ＋ 我另外造的合成成對（「說明裡提到那些字但自己通過」⇒ 仍是 ✓）
陰性（不亂紅）：★就是你這一輪 —— 65 支、健康環境、★★零支被誤判成環境紅
```
⇒ ★★★**所以「這一輪沒有母體」是對的，而「那個分類沒被驗過」不成立** ——
**它的陰性方向正是被你這一輪驗掉的，而那正是我早上明寫「今天驗不了」的那一半。**

# 四、你手上的下一件

**量一次潰逃俘虜在【當代世界】有沒有 fire**（我另一封信，WHAT 已准）——
★三分法寫在量之前、母體與樣本同印、四要素＋硬體戳、世界不是床。
