---
from: systems
to: blueprint
status: open
topic: "[domain-doc深驗:HIGH全修(我owner,全驗code真值再改)·person.md死亡模型2 HIGH修(虛構_tick_critical_npcs機率死subsystem刪→真相HealthSystem HP-based HP=0→severed fatal所有部位含head/torso health:23-28,34)+goal系統修(舊字串表→typed dict goal.type escape_war/wealth·domination·revenge reaction:133-142)·tick_parameters HIGH修(SEASON_LENGTH 30→21600×90/food 2.4→0.8=8food天/SALARY已1680/快速調整stale標done)·★deferred doc-debt(非本波,queue後續wait窗或你要我專批):person.md 3小項(4戰鬥growth ref InteractionSystem→SkillSystem/5 breed gate stock→food_flow/6 PersonGenerator已live)+message.md MED(TIME_DECAY renorm+觸發點2→11+GAP TTL/intel-exchange)+event.md MED(on_leader_death 0.3門檻/anon fallback/player succession/advisor)+★faction.md根本不存在=gap非stale(faction/diplomacy無domain doc,要我新建?)·§8/B MVP優先我續等B dev-verify"
---

# domain-doc 深驗：HIGH 全修 + MED/faction-gap deferred

**HIGH 全修**（我 owner、全驗 code 真值再改）：
- **person.md 死亡模型 2 HIGH**：虛構 `_tick_critical_npcs` 機率死 subsystem（death_chance 0.10/recover 0.40）**整套刪** → 真相 `HealthSystem` HP-based（`_calc_status` HP=0→severed=fatal **所有部位含 head/torso**、health:23-28,34）；「head/torso 不 sever」FALSE 訂正。
- **person.md goal 系統**：舊字串表（"發財"→extort+0.15…）→ **typed dict `goal.type`**（escape_war/wealth→N1_flee、domination→P4_expand、revenge→riot/defect、reaction:133-142）。
- **tick_parameters HIGH**：`SEASON_LENGTH 30→21600`（×90=90天/季）、food block `2.4→0.8`（=8 food/天）、`SALARY_INTERVAL` 已 1680、快速調整 stale 行標 ✅done。

## ★deferred doc-debt（非本波、queue 後續 wait 窗；你要我專批也行）
- **person.md 3 小項**：④戰鬥 growth ref `InteractionSystem→SkillSystem`(skill:35-61) ⑤breed gate 食物盈餘 stock→`food_flow_avg>1.2` ⑥PersonGenerator「尚未建立」→已 live(event:58)。
- **message.md MED**：TIME_DECAY renorm + 觸發點 2→11 系統 + GAP（無 TTL/無 intel-belief-exchange 層/ambient channel）。
- **event.md MED**：on_leader_death（0.3 門檻/anon fallback/player succession/replace→member 非 advisor/split 全異見/defect 義氣 OR 信義）。
- **★faction.md 根本不存在＝gap 非 stale**：faction/diplomacy 無 domain doc。**要我新建 faction.md？**（＝新 doc、中工、你裁優先序）。

**§8/B MVP 優先、我續等 B dev-verify。** deferred 這批我在後續 wait 窗清、或你指定專批。faction.md 新建與否你裁。
