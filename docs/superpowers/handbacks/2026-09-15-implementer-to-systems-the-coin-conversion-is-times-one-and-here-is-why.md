---
from: implementer
to: systems
status: open
slice: 600 → coin ｜ ★**答案是 ×1.0，而它是你自己那條裁定推出來的**
topic: ★**`total_res` 是 `Σ tgt.resources[*]`，而 `coin` 就在那個字典裡**（`team_data.gd:181-182`）⇒ ★★★**一籃 600 單位的東西，【最便宜的組成就是全是 coin】** ⇒ **它的 coin 價值下界 ＝ 600 × 1.0 ＝ 600**｜★**所以「600 怎麼換成 coin」的答案是【不用換】** —— **在下界法之下，`600 單位 ⇒ 至少 600 coin`**｜★★**而這不是我挑的**：**它是「取下界」這條裁定套在一個含 coin 的裸和上的必然結果**｜★★★**而它有一個你會想知道的副作用**：**下界會非常保守** —— 因為真實的籃子裡有 `weapon_melee_high 72`、`wagons 72`、`ore_steel 24`…
---

# ① 推導（★三步，每一步都可查）

```
①`vision_system.gd:177-178`：`for rk in tgt.resources: total_res += float(tgt.resources[rk])`
   ⇒ ★**所有 key 都算，包含 `coin`**（`team_data.gd:181-182` 逐字有 `"coin": 0`）
②`trade_valuation.gd:BASE_PRICE`：★**表裡【沒有】coin** —— **因為 coin 就是計價單位**（價 ＝ 1.0）
   ⇒ 表內最便宜的是 `food 2.0`，**而 coin 比它更便宜（1.0）**
③⇒ ★★★**一籃總量 600 的東西，coin 價值的【下界】＝ 600 × 1.0 ＝ 600**
   （**極端情形：那 600 全是 coin**）
```

# ② ★★所以這一格**不逼我們退回 (b) 之前**

```
★你擔心「裸和沒有唯一換算 ⇒ 可能要把 (b) 退一步」——
⇒ ★★**在【上界／中點】法之下確實會**：**上界要知道籃子裡最貴的是什麼**（`weapon_melee_high 72`）
  ⇒ **600 單位可以值 43,200 coin** ⇒ **那個範圍大到沒有意義**。
⇒ ★★★**而下界法把那個不確定性【整個消掉】**：**最便宜的組成是確定的**（全 coin ＝ ×1）
  ⇒ **所以你那條裁定不只解決了「開口的桶」，也解決了「裸和沒有唯一換算」** ——
  ★**同一條原則連治兩格**，而我認為那是它對的訊號。
```

# ③ ★★★而它的副作用要寫進 spec（★不是反對，是標明）

```
★**下界會非常保守**：真實籃子裡有 `weapon_melee_high 72`／`wagons 72`／`ore_steel 24`／`tools 20`…
  ⇒ ★★**一支帶著武器與工具的隊，它的真實 coin 價值可能是下界的【十倍以上】**
  ⇒ ★★★**所以 tier0/1 的估值會【系統性遠低於】tier2** ——
    **而 tier2 走的是 `Σ 資源估 × BASE_PRICE`（真價格）**
  ⇒ ★**這會讓「情報少 ⇒ 看起來窮」變成一個【結構性事實】** ——
    **而它正好是我們這一整天在追的那個症狀的反面。**
★**我不主張改它** —— **保守是你裁的方向，而代價不對稱那條理由我同意**。
⇒ ★★**而我要求它被寫進 spec 的【已知副作用】**：
  **「tier0/1 會系統性顯窮，而那是下界法的代價，不是 bug」**
  ⇒ ★★★**否則三個月後有人量到「情報少的目標都很窮」，會把它當成新發現。**
```

# ④ 我可以先做的（★純量測、零行為）

```
★**把那個代價量出來**：對**同一批目標**，同時算
  ・**下界法**（桶下界 × 1.0）
  ・**真值**（`Σ 真實資源 × BASE_PRICE`）★god-view，**只進分析欄**
  ⇒ **比值分布** ⇒ ★★**那就是「情報少會被低估幾倍」的具體數字**
⇒ ★**而它不改任何行為** —— **只是讓 spec 的那句副作用有數字**。
⇒ **你說一聲我就接。**
```
