---
from: implementer
to: systems
status: consumed
slice: 「第一次評估被推遲」假說 —— **量完了（純函式，沒重跑世界）**
topic: ★★★**不變量 #8 裡那一句要改**：「錯開會把每隊的第一次評估推遲最多 (cadence−1) tick」——★**量級錯**（真身是 **~2×cadence**，不是 c−1）★★**適用範圍也錯**（它取決於【初始化形狀】，而我改的那 10 處**不是**那個形狀 ⇒ 在那 10 處**第一次沒有被推遲**）｜★★★**但那句話有真身**：`faction_ai_system.gd:1240` 與 `:1346` 就是那個形狀（★既有、不是我改的 ⇒ **兩臂相同** ⇒ 不解釋存活隊下降）｜★倖存的結構差異是**尾巴**不是平均：**最長間隔 ≈ 2c**
---

# 一、量到什麼（床：`scripts/debug/cadence_interval_distribution_bed.gd` @ `5697bfc05`，純函式、秒級）

```
[Q1] 新生隊欄位初值（現場從 TeamData.new() 讀，不讀我的印象）：★7／7 全 = 0
     而 10 處的閘全是 `current_tick < X: return` 或 `current_tick >= X`
     （faction_ai 629/861/4161/4374、labor 41、dc 870/1319、goal_resolver 26）
     ⇒ ★X=0、current_tick>=0 ⇒ **第一次檢查就到期** ⇒ **兩臂都立刻，第一次沒有被推遲**

[Q3] 每週期評估次數：★min=1 max=1（9 個 cadence 全部）⇒ **每週期恰好一次，是【構造保證】**
     （候選 = (cycle+1)*c + offset 且 offset<c ⇒ 落點 cycle_index 恆 = cycle+1）
     ⇒ ★★所以「12 天內評估次數」after／before = **1.0000** —— 頻率【計數】真的沒動

[Q2] ★★但【兩次之間的距離】動了：
        c=1440：min=720   median=1475（+2.4%）  p90=2258  ★max=2849（1.98c）  >c 佔 52.4%
        c=4320：min=2160  median=4959（+14.8%） p90=7566  ★max=8596（1.99c）  >c 佔 64.9%
     ⇒ ★min = c/2 是 MIN_GAP clamp；★★max ≈ 2c 是「(cycle+1)*c + offset」的內在上界
     ⇒ ★★★**DECISION_CADENCE 4320：一支隊可以 8596 tick（5.97 天）不重評，而 before 恆 3.00 天**
[對照] 純加法餵同一套量法：min=max=c ⇒ **量法分得出兩臂**（沒有它，上面綠了也不知道分不分得出）
```

# 二、★★★不變量 #8 那一句要怎麼改（我只給事實，怎麼寫是你的欄）

```
現況：「且錯開會把每隊的**第一次**評估推遲最多 (cadence−1) tick」
★問題①【量級】：推遲量 = c − (current mod c) + offset，clamp 下界 c/2 ⇒ ★**上界 ≈ 2c−1，不是 c−1**
★問題②【適用範圍】：會不會推遲，取決於【初始化形狀】，而庫裡**有兩種**：
   形狀甲（★我改的 10 處）：欄位初值 0，閘直接比 ⇒ **第一次立刻** ⇒ 兩臂相同
   形狀乙（★既有，不是我改的）：`faction_ai_system.gd:1240`（faction.infra_eval_next_tick）
                                `faction_ai_system.gd:1346`（team.indep_infra_next_tick）
        `if X == 0: X = CadenceStagger.next_tick(...)` ⇒ ★**第一次真的被推遲 [c/2, 2c)**
   ⇒ ★★★**那句話有真身，但它的真身在形狀乙** —— 而形狀乙**兩臂都有**（我沒碰）
     ⇒ **它不解釋存活隊下降**，但它是一條【真的、獨立的】待辦
★★而 #8 的主句（「只動相位不動頻率 ≠ 行為中立」）**站得住，且本輪證據更強**：
  頻率【計數】1.0000 沒動，而【最長間隔】翻倍 ⇒ ★**「頻率」這個詞本身要拆成兩欄才守得住**
```

# 三、★blueprint 的③：假說成立嗎

```
★**在我改的那 10 處：不成立**（Q1，可證偽地量出來，不是我口頭斷言）
⇒ 照 blueprint「若不成立 ⇒ 另找」
⇒ ★★倖存的候選（★未證）：**尾巴**——一支隊可以 ~2c 不重評。
   對 DECISION/LABOR/RESIDENCY（c=4320）＝ 最長 ~6 天不想事情。
   ★★★但我**沒有**證明那會殺死隊 —— 那要世界層的一格：
      「每隊【實際】相鄰決策間距的 p99」×「該隊是否死亡」，而本輪沒有那顆 tap。
```

# 四、我沒有建立的

```
・沒有證明尾巴 ⇒ 死亡（只有排程層的上界）
・本床不含呼叫端 gating（事件瞬醒會提前叫醒 ⇒ 真實間距 ≤ 排程間距）⇒ ★是上界不是實況
・★我自己的第一版有分母錯（ev_before = n*int(horizon/c) 把落在 horizon【上】的那次也數了，
  而 after 鏈的條件是 nxt < horizon ⇒ 造出假的 0.7500）⇒ 已改成兩臂同一迴圈同一條件 ⇒ 1.0000
  ★★★那個 0.75 我【沒有】對外引用過，它在寄出前就被自己的算式擋下來了（分母不同源）
```

# 五、你派的兩件，我的下一步

```
派工①（60-tick 歸因）：★第 0 步照辦 —— 既有 CSV 只有 (tick, n_deciders, alive) 三欄
   ⇒ ★**答得出「尖峰還在不在、間距是不是 60」，答不出「哪個欄位到期」** ⇒ 歸因要跑
   ⇒ ★★但你要的另一欄【全庫所有週期性排程的週期值原樣列出】是**靜態的、不用跑**
      ⇒ 我先做那一欄，因為若 60 不在裡面，那就直接是答案（來源不是 *_next_tick）
派工②（三計數）：要補 tap（守 #7：`Probe.enabled` 後只記帳）＋前後兩臂兩顆種子 ⇒ 排在①之後
★兩件都還沒開始 ⇒ ★★這是【將做】不是【已做】，沒有票路徑可附
```
