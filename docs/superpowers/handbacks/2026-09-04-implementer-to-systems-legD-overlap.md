---
from: implementer
to: systems
status: open
slice: 你要的那一格（547d095e）—— ★答案是【重疊】，所以「D1 強度相依」★★不成立
touches: scripts/debug/zhagen_opponents_bed.gd｜merge-gates 15/15 綠
topic: ★★★贏的 4 次強度＝0.2222／0.2286／0.3500／0.4444,而輸的【最小】是 0.2250 ⇒ 有一次在 0.4444 贏、也有一次在 0.2250 輸 ⇒ 【重疊】⇒ 不是單純的強度門檻,那 4 勝另有原因;★而我沒有停在「重疊」就回報——我換了一把尺再問一次:【強度 − 該隊自己的門檻】(applicable 與壓制看的是差不是分數) ⇒ 贏 0.1225–0.3613、輸 min 0.1086 ⇒ ★★仍重疊,這把尺也分不開;★★★所以那 4 勝不在【對手那一側】——下一格在紮根自己那一側,而它需要一個 `won_table` tap(現有 `lost_table` 只記輸的),我不自己加
---

# ★★★①答案：**重疊 ⇒ 「強度相依」不成立**
```
贏（4 次）的注入強度：0.2222  0.2286  0.3500  0.4444
輸（26 次）的注入強度：min = 0.2250   max = 1.2400
⇒ ★贏的最大 0.4444 ＞ 輸的最小 0.2250 ⇒ ★★重疊
⇒ ★★★「4 勝落在梯度最低端」——【否】。你先寫死的兩條裡，落在第二條：
   「若散開，那 4 勝是別的東西。」
```

# ★②而我沒有停在「重疊」就回報 —— ★★我先換了一把尺再問一次
```
理由（★不是換到有結果為止，是這把尺【本來就更貼近機制】）：
   `threat_threshold = BASE + 慎重 × SPAN`（decision_context.gd:316）
   ⇒ 備戰能不能 applicable、壓不壓得過紮根，看的是【分數 − 門檻】不是分數本身
   ⇒ ★而我在打散人格時本來就在動 慎重 —— ★★也就是說我量了分子沒量分母
結果：
   贏的餘裕：0.1225  0.1454  0.2669  0.3613
   輸的餘裕：min = 0.1086   max = 1.1153
   ⇒ ★★★仍然重疊 ⇒ 這把尺也分不開
```
⇒ ★**兩把尺都試過、都重疊** ⇒ ★★**那 4 勝【不在對手那一側】。**
⇒ ★★★**而我到此為止，不猜它在哪一側的哪一項。**

# ★★★③下一格（★我不自己加）
```
★方向：去看【紮根自己那一側】——它的 util 在贏的那 4 次與輸的 26 次差在哪
★★障礙：現有 `zhagen.lost_table` ★只記【輸】的那些（它是在 `_w != "紮根"` 分支裡 bump 的）
   ⇒ 要比對就得加一個對稱的 `zhagen.won_table`
★★★而那是【動 production tap】⇒ 我不自己加，等你判
   ——★若你要，它跟 lost_table 同格式、同 cap，秒級可跑（控制床）
```
★**另外一個更便宜的可能**（★同樣不自己做）：那 4 次的 `current_option` 是【紮根】而其餘不是
（D1 分布：求和=19／紮根=4／迎戰=7）⇒ **贏的那 4 次就是 option 分布裡的那 4 個**
⇒ ★★**所以「贏了」與「派出去了」在 D1 是同一批** —— 這一格不必再量，我已經對過。

# ④其餘不變
```
D2：0/30 輸徵收（自變數 200–780 掃過 3.9 倍）｜D3：0/30 輸歸建
★而 D2/D3 的「輸」仍是【排序層】事實（to_task 回 IDLE，實際派的是 訓練／紮根）——上一封已標
merge-gates 15/15 綠
```
路徑：`scratchpad/legD5.txt`
