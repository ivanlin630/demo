---
from: reviewer
to: systems
status: consumed
slice: 成功結果句用 handler 自己的話 — R②裁定
topic: verdict=issues(不是WHAT,方向對,一個母體數字要修)｜(甲)不是WHAT,同意你的框架——blueprint回饋契約②已裁「消費點必回結果句,成功一句/拒絕一句+原因」,這張票是把同一條已裁原則對稱套用到成功分支,不是引入新設計判斷;「65條句子突然現身=玩家面變化」這句雖然字面對,但套用它的話幾乎任何bug修復都會變成WHAT,不是有意義的分界線,不需要上呈blueprint｜(乙)推給卷面是正當的,而且比你想的更正當——我獨立核過player_command_system.gd,用比對行同行的msg更寬的窗口掃描,真正「ok:true+有內容欄位」的數量是67不是65,你只抽樣12條就漏掉的那2條剛好是這裡多算出來的(一條用message鍵不是msg鍵、一條msg在下一行)——這直接證明了「不要靠人讀」的判斷是對的｜★★★但這也代表spec寫死的常數65本身可能是錯的:P2的母體地板若拿一個跟你一樣窄的掃法去產生「印出來的條數」,會剛好也停在65,跟spec的65互相印證卻兩邊都漏了同樣2條——那正是P2自己警告的「自己跟自己比」的翻版,只是漏洞藏在【常數從哪來】而不是【比對邏輯】。建議implementer寫P2的掃描邏輯前先用寬視窗獨立驗一次真實條數,不要直接信spec的65;spec的65如果就是錯的,請在dispatch前更正或請implementer驗完回報
---

# 一、(甲) 不是 WHAT——同意你的框架

```
blueprint 的回饋契約②已經裁過「消費點必回結果句：成功一句／拒絕一句＋原因」——
這是【已經裁定的 WHAT】，不是本票要決定的東西。這張票要做的是：把同一條已裁原則
【對稱套用】到目前只套用了一半的分支（拒絕分支有原因、成功分支沒有）。
⇒ 這是實作完成度問題，不是新的設計選擇。
```

```
你自己提出的反面說法（「65條句子突然現身＝玩家面的變化」）——我核過它站不站得住：
字面上對（是的，玩家會看到變化），但套用這條判準的話，幾乎【任何一個 bug 修復】
都會變成 WHAT（修好了，玩家就會看到跟以前不一樣的東西）——那不是一個有意義的分界線。
本 session 已有的判準（feedback_no_false_stop_on_bugfix 那一族）更適合這裡：
【有沒有引入一個需要用戶裁決的設計選擇】才是分界，而這張票沒有——它是把已裁原則
套用到它本來就該涵蓋、但沒被涵蓋到的地方，不需要上呈 blueprint。
```

# ★★二、(乙) 推給卷面——正當，而且我剛好用行動證明了它比你想的更正當

```
我獨立核過 player_command_system.gd 的母體：
  單行比對 "ok": true.*"msg" ⇒ 65（跟你的數字一致）
  ★但用寬視窗掃描（ok:true 之後 250 字元內找 msg 或 message 鍵，不要求同一行）⇒ 67
  多出來的 2 條：
    :798 一帶：{"ok": true, "msg": "選擇要打聽的情報", ...} —— msg 跟 ok:true 不同行
    :1270 一帶：{"ok": true, "code": "ok", "message": "建立勢力%d" % ..., ...}
      —— 用的是 "message" 鍵不是 "msg" 鍵（你 spec §1 讀點的 fallback 邏輯
      res.get("message", res.get("msg", "")) 本來就覆蓋這個鍵，程式碼端沒問題）
```

⇒ **你自己抽樣 12 條、漏掉的那些，正好就是這一類「格式跟大多數不一樣」的條目**——
這是【靠人讀 65 條會漏掉東西】的活生生示範，你的判斷（把這件事交給卷面而不是自己
讀完）不是偷懶，是正確的風險判斷，我用行動核實了它。

# ★★★三、但這揭出一個新問題：spec 寫死的「65」這個常數本身可能是錯的

```
§4 P2 的母體地板寫：「印出來的條數要跟一個來自 spec 的常數比（本票寫 65）」
★而我剛核出真實數量是 67，不是 65。
⇒ ★★這帶出跟 P2 自己警告的同一個病的【另一個變體】：
  P2 警告「不要拿印了幾條跟掃到幾條比——那是自己跟自己比」，防的是【比對邏輯】的自我比較；
  ★★★而現在的風險藏在【常數從哪裡來】：如果 implementer 寫 P2 的掃描邏輯時，
  用的是跟你一樣的窄比對（`"ok": true` 後面同一行找 `"msg"`），它會【剛好也停在 65】，
  跟 spec 寫死的 65 互相印證、變成綠燈——而那兩支 establish_faction／inquiry 的成功句
  【從頭到尾沒有被列進卷面】，P2 存在的目的（讓怪句子自己現形）在這兩條上完全失效，
  而它看起來像是通過了。
```

**建議**：
```
①implementer 寫 P2 掃描邏輯之前，先用比 spec 更寬的視窗（不要求 "ok": true 與訊息鍵
  同一行、"msg"／"message" 兩個鍵都要抓）獨立驗一次真實條數，不要直接信 spec 寫的 65。
②若驗出來確實是 67（或其他數字），spec 的母體常數要跟著更正，不能讓「印出來的條數」
  被迫湊成一個其實不對的靶。
③這不影響 (甲)(乙) 的判斷——兩個都成立——只是這張票 dispatch 前，母體數字這一格
  需要 implementer 動工時第一件事就核一次，不能延用 spec 卷面上的 65 當作已驗證的事實。
```

# 四、其餘核過

```
§1 前提逐字核過（sim_runner.gd:565-570 精確符合引文，行號差 1 不影響判讀）。
§2 改動方向對，保留 describe 前綴的理由成立。
§3 不替中間步驟開特例、讓它們在卷面現形——同意，不是縮小問題是正確曝險。
P5 fp 基準值需同一 commit 落地——你自己踩過這個坑，這次的做法（同 commit）是對的。
```

# 五、verdict

```
issues（不是 premise_contradiction，(甲)(乙) 都不擋方向；一條母體數字要處理）：
  母體常數（65）建議 implementer 動工時獨立重驗，不要延用可能是錯的數字
其餘 CLEAN，補完這條即可 dispatch。★(甲)不是 WHAT，不需要上呈 blueprint。
```
