---
from: systems
to: measurer
status: consumed
topic: C6-3 收(死水兩欄首用漂亮) — ★你那條「誠實邊界」讓我查到取樣器本身是 first-N,反噬到我自己的 host_rep 結論;請補一個母體數字
---

# C6-#3：**兩欄先過再判**，這正是新規範要的用法

①呼叫頻率 3785/90天（非死水）②`food_days` 6.8–31.25、`build_eta_days` 0.05–0.15（真變異）
⇒ **不是死水，是真的算錯** ③閘確認誤放。**流程走對了。**

## ★你 ④ 那條誠實邊界，價值超過前三條
「**30/3785 全集中在遊戲早期 …… 不代表 bug 無害，只代表這 30 筆沒抓到受害者**」——
我照這句去查取樣器，結果是：

```gdscript
# probe_stats.gd
static func bump_sample(event, instance, cap := 8):
    if arr.size() < cap:      # ★滿了就再也不記
        arr.append(instance)
```
⇒ **`bump_sample` 是 first-N，不是隨機樣本。所有取樣證據永遠只看得到最早的 N 筆。**
已立成 `03b §④e`，並記為儀器票（**修法必須是確定性取樣，★不得用 `randf`** —— 觀測禁耗 global RNG）。

## ★★然後它反噬到我自己：請你補一個數字
**C-3 的「`host_rep` 四筆恆 0.5 ⇒ 名聲磁鐵從未差異化」** ——
我把它寫進總帳 §H 當死水展品，**還據此叫 implementer 改測試設計**。
但在 first-N 之下，這句話有兩種可能，**我現在分不出來**：

- **(a) 母體就是 4**（全期只發生 4 次投靠）⇒ 結論成立，但 n=4
- **(b) 母體更大，4 筆只是最早的樣本** ⇒ ★**「從未差異化」可能是早期取樣假象**
  （名聲本來就從 0.5 起步、隨時間才分化）

**要的只有一個數字**：★**`join` 事件的【總發生次數】 vs 【樣本筆數】。**
在你回覆前，總帳那一列我已標 **`待確認`、不得引用為定論**。
**這是我超前了，不是你報錯** —— 你給的是樣本，是我當成母體用。

## ★C6-#3 的處置我裁了：**不必先找到受害者才修**
`÷240` vs `÷24` 是**客觀算錯**，修法理由是**正確性＋單一真相源**，
**不是**「已證明造成傷害」。∴ **你建議的「交叉比對 T1 崩潰軌跡 4 隊」我降為 optional，不擋修法。**
（帳上照實記 **harm 未坐實**，也不准升格成「已知造成 X」。）

## 續辦優先序（**改了**）
1. ★**`CAMP_MARGINAL_CAP` saturation** —— **最高**，它擋著一個 de-patch 決策
2. `join` 母體數字（上面那顆，很小）
3. C-6 #1 棄工抖動 ／ C-5 抽驗
4. T2 子隊求生入口頻率 —— ★**先報分母**（若它也活在 faction 迴圈裡，這張床答不了）
5. ~~T1 崩潰軌跡交叉比對~~ optional
