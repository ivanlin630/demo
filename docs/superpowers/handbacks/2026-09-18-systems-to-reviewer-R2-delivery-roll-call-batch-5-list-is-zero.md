---
from: systems
to: reviewer
status: consumed
slice: 到場點名 第五批（`feat/bed-roll-call-2` ＝ `c9a118102`）｜R② 交付審
topic: ★**原清單清零**：26 支 ＝ 19 已處理 ＋ 4 本來就有等價守衛 ＋ **本批 4 支**（1 加點名／3 釘免疫欄）｜★★**`team-ui` 值得你單獨看一眼**：它**沒有失敗計數器**（靠 `print`／`assert`）⇒ ★**到場點名是它唯一的「有沒有跑完」訊號** —— 在別的床上點名是**第二道**，在它上面是**唯一一道**｜★★★**第三軸第三次被實測確認**（`build-duration`／`ki-anchor` ＝ rc=0、1 秒、無橫幅；對照 `minor-merge` ＝ rc=98 掛住 151 秒）｜★`bed-kind` 又抓了他一次（3 支沒宣告）—— **而那正是那支閘該做的事**
---

# 一、交付

```
team-ui        ⇒ 加點名（①注射 2／3 ＋ roll-call 紅行；正常 3／3）
minor-merge    ⇒ 釘免疫欄（inline 型：rc=98、掛住 151 秒）
build-duration ⇒ 釘免疫欄（quit(_run()) 型：rc=0、1 秒、無橫幅）
ki-anchor      ⇒ 同上
★bed-kind：3 支沒宣告 ⇒ 已逐支判並補 slice:
```

# 二、★★`team-ui` 那一點我想聽你的判斷

```
一般的床：失敗計數器（_fail）是第一道，到場點名是第二道（防「計數器停在假的 0」）
team-ui  ：★沒有計數器 —— 只有 print／assert
         ⇒ ★★到場點名是它【唯一】的完成訊號
```
★**問題**：這是不是代表 `team-ui` 這類床**本來就該有計數器**（而點名只是補丁）？
★★**還是說「靠 assert 的床 ＋ 點名」本身就是一個合法的形狀**？
★★★**我傾向後者**（`assert` 會停、`push_error` 會叫，這在要件②裡已經分過），
**但這一題我沒有把握，而它會影響下一個寫床的人抄哪一種。**

# 三、★第三軸第三次確認（我要把它從「預測」正式改成「已知」）

```
批三 bed_arm          ：rc=0、8 秒、無橫幅
批五 build-duration／ki-anchor：rc=0、★1 秒、無橫幅
對照 inline 型（批二 merchant／payroll、批五 minor-merge）：rc≠0、掛住 151 秒
⇒ ★★★「quit() 在死亡點的裡面還是外面」決定紅的簽名 —— 三個樣本、兩類，沒有例外
```
★**我原本是拿它當【預測】寫進派工信的** ⇒ **現在它可以當【已知事實】用了**，
★★而我要問你：**這種「預測 → 三次確認 → 升格成事實」的東西，該不該進 `03_implementer` 要件③**？
（★我的顧慮是那份已經卡在行數上限，**而這件事的操作價值是「看到 rc=0 別以為它跑成功了」**。）

# 四、狀態
姊妹票的 merge 閘跑中（`e226a7fcd`）。本批在你手上。
