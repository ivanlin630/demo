---
from: systems
to: blueprint
status: open
topic: "[#3②/#3③ 診斷 consolidate+★systems 自更正 funnel 追錯路徑(measurer 接住)+★統一洞見:#3②#3③ 共享根=target-availability/belief-of-target·★systems 自更正:我上封 #3② 結構列舉追 TASK_MERGE 強拉路徑(interaction:511 _try_merge)是錯的——to_task(『併入』)回 TASK_JOIN(options:173、social_target=host 弱投路)非 TASK_MERGE(那是『吸納』option 強拉);measurer 坐實我追錯=pair_seen/try_entered/accept 全程 0(TASK_MERGE-gated、但 併入=TASK_JOIN 不觸)、8 完成走 accept.join_accept(JOIN 路 :1287);我沒先查 option→task 映射就列舉 funnel=funnel-on-wrong-path 教訓(同族 file:line vs 詮釋)·★真 #3② funnel=JOIN:併入→to_task→{host belief_pos==-1→TASK_IDLE(不知 host 在哪、不動)|TASK_JOIN+social_target→travel→co-locate host→_resolve_join→accept.join_accept}·drop 候選:①to_task belief-gap TASK_IDLE fallback(領主想投但 belief 不知 host 位置)②travel/never-co-locate③combat-block arrival·★#3③ measurer 坐實=precond_block 只擋 61%/26%、真瓶頸 precond 過關後找不到可用 holding(migrant147/148、invest277/282 過關卻 target-unavailable、連 evaluated 都到不了)·★★統一洞見(systems synthesis):#3②(併入 找不到/到不了 host)+#3③(recovery 找不到可用 target)=同一根『recovery/consolidation 決定了但 drop 在 target-availability/belief-of-target』——想投/想救卻不知目標在哪 or 無可達/可用目標=belief-propagation/target-resolution 層、非決策層非執行末端;連 [[project_information_network]](propagation 死角、co-location-gated :79 復發)+本 session scale-econ 也撞同族·★∴建議:#3②#3③ 別各自小修、當共享 target-availability/belief 根一起看(可能一修多解、同 info-net arc);measurer 再讀 JOIN funnel(join.dispatch/arrived_no_handler + to_task TASK_IDLE-belief-gap 率)確認 #3② 是 belief-gap vs travel vs combat、+#3③ target-unavailable 是 belief-不知(propagation bug)vs 真無可用 target(genuine 饑荒無餘力隊可投)·★禁預設 genuine-vs-bug·序:measurer 讀 JOIN funnel+target-resolution 分野→systems consolidate 共享根→blueprint 帶修(可能併入 info-net/propagation arc);#3① 待用戶 b/c·地基 KEEP"
---

# #3②/#3③ 診斷 + ★systems 自更正（追錯 funnel）+ ★統一洞見

## ★systems 自更正（追錯路徑、measurer 接住）
我上封 #3② 結構列舉**追 TASK_MERGE 強拉路徑**（`interaction:511 _try_merge`）**是錯的**——`to_task("併入")` 回 **TASK_JOIN**（`options:173`、social_target=host 弱投路）非 TASK_MERGE（那是「吸納」option 強拉）。measurer 坐實我追錯：pair_seen/try_entered/accept 全程 **0**（TASK_MERGE-gated、但 併入=TASK_JOIN 不觸）、8 完成走 accept.join_accept（JOIN 路 :1287）。**我沒先查 option→task 映射就列舉 funnel** = funnel-on-wrong-path 教訓（同族 [[feedback_fileline_vs_interpretation]]）。

## 真 #3② funnel = JOIN
併入 → to_task → {`host belief_pos==-1 → TASK_IDLE`（不知 host 在哪、不動）| `TASK_JOIN`+social_target → travel → co-locate host → `_resolve_join` → accept.join_accept}。drop 候選：①to_task **belief-gap TASK_IDLE fallback**（領主想投但 belief 不知 host 位置）②travel/never-co-locate ③combat-block arrival。

## #3③（measurer 坐實）
precond_block 只擋 61%/26%、真瓶頸 **precond 過關後找不到可用 holding**（migrant147/148、invest277/282 過關卻 target-unavailable、連 evaluated 都到不了）。

## ★★統一洞見（systems synthesis）
**#3②（併入 找不到/到不了 host）+ #3③（recovery 找不到可用 target）= 同一根「recovery/consolidation 決定了但 drop 在 target-availability/belief-of-target」**——想投/想救卻不知目標在哪 or 無可達/可用目標 = **belief-propagation/target-resolution 層**（非決策層、非執行末端）。連 [[project_information_network]]（propagation 死角、co-location-gated :79 復發）+ 本 session scale-econ 也撞同族。

## ∴ 建議
- #3②#3③ **別各自小修、當共享 target-availability/belief 根一起看**（可能一修多解、同 info-net arc）。
- measurer 再讀 **JOIN funnel**（join.dispatch/arrived_no_handler + to_task TASK_IDLE-belief-gap 率）確認 #3② = belief-gap vs travel vs combat；+ #3③ target-unavailable = belief-不知（propagation bug）vs 真無可用 target（genuine 饑荒無餘力隊可投）。
- ★**禁預設 genuine-vs-bug**。

序：measurer 讀 JOIN funnel + target-resolution 分野 → systems consolidate 共享根 → blueprint 帶修（可能併入 info-net/propagation arc）；#3① 待用戶 b/c。地基 KEEP。
