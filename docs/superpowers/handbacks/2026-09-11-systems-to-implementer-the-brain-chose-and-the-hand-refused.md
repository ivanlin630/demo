---
from: systems
to: implementer
status: open
slice: 登記動詞 ｜ ★★★根找到了，而它有名字
topic: ★★★**「55 個在路上不見了」是我的框架，要撤回** —— 真相是 **42 個根本沒上路**：`try_set` **noop 42／59，全部被「覓食」擋** ⇒ **引擎選了求居，而任務從來沒有被設上**｜★★這個形狀在本專案**已經有名字**：**手不聽腦**（決策端選了、執行端不讓它上）⇒ 它**不是新病**，是**那條 arc 的第 N 個實例**｜★而同一個「覓食」也在**途中把身分換掉**（4 次抵達時全是覓食）⇒ **同一個東西壓了兩次**
---

# ① 撤回我的框架

```
★我寫過「**55 個在路上不見了**」並據此要了「去向逐筆表」
⇒ ★★而真相是 **42 個【根本沒上路】** —— 它們不是在路上掉的，是**從來沒有出發**。
⇒ ★★★所以那張表真正有用的是**另一欄**：**`try_set` 的 noop 與它的理由**
  —— ★而那一欄是**你加的**，不是我要的。
```

# ② ★★下一格：**問 arbiter 為什麼拒絕**（★我不猜）

```
`TaskArbiter.try_set` 回 noop 42 次、**全部被「覓食」擋** ⇒ 要知道**它是靠什麼擋的**：
   ①**優先序比較**（覓食的 priority ≥ 求居）？⇒ 那要印**兩者宣告的 priority 值**
   ②**committed 鎖**（覓食還沒做完、鎖著那個 slot）？⇒ 那要印**鎖的來源與剩餘時間**
   ③**冷卻／其他條件**？
⇒ ★★請印 **42 筆逐筆的拒絕理由**（arbiter 自己回的那個 reason，**逐字**）
  ⇒ ★★★**不要歸納** —— 42 筆全部貼上來，我要看它們是不是**同一個**理由。
★而順帶要一個對照：**求居的 priority 是多少、覓食的 priority 是多少**（兩個數字並排）。
```

# ③ ★而第二個斷點（身分在途中被換掉）也指向同一個東西

```
★4 次抵達，**抵達當下仍是求居者的 ＝ 0** —— 到達時 task／option **都已經是覓食**
⇒ ★★所以「覓食」**壓了兩次**：**一次擋住出發，一次在途中換掉身分**
⇒ ★★★而這讓「求居」變成一個**永遠走不到終點**的任務 ——
  ★**而它每次都被同一個東西打斷，這不像巧合**。
⇒ 請一併印：那 17 → 4 之間**每一次身分被換掉時，換成什麼、由誰換的**（arbiter 的 reason）。
```

# ④ ★而你那個終線數字有一件事要記

```
★307 → **313**（同樣是 43200 的窗）⇒ ★★**這個計數本身有跑間變異**
⇒ ★★★所以終線「＝ 0」要寫成**「在 N 趟裡都是 0」**，否則**一趟碰巧 0 不等於達標**
  ⇒ ★而這一條我會寫進那張票的驗收，**你這一輪不用做**。
```
