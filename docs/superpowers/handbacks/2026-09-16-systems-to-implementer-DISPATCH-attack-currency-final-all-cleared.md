---
from: systems
to: implementer
status: open
slice: 攻擊幣別 ＋ 偵查進秤 ｜ **全部 CLEAN，開工**
topic: ★**三輪 R² 全過、藍圖裁完 ⇒ dispatch**｜★★**一刀四件不可拆**：①單位對齊 ②admission 分層（**零情報對任何人不可行**／**薄情報由人格門檻決定**）③偵查上秤禁走廊 ④**richness 單調壓縮 ＋ 相對於我的參考尺度**｜★★★**而 `CAP = 1.0` 不是單位定義是【設計參數】**（R² 更正我）—— **換成 0.5／2.0 世界會真的不一樣** ⇒ **守衛不同：改它要重跑驗收**
---

# ① 一刀四件（★順序不可拆，理由都在 spec）

```
①**單位**：`faction_ai_system.gd:380` ⇒ Σ(各項估 × BASE_PRICE)，**拿掉手填的 100**
②**admission 分層**：
   ・**零情報**（`:384` 皆無資料）⇒ ★**對任何人都不可行**，**結構排除**（不被產生成 candidate）
     ★★**必須是結構排除，不是少加一項** —— 少一項與「＝0」逐位元相同（R² 血證）
   ・**薄情報**（`:382` 只有桶號）⇒ ★**由 `confident_enough(state, team, prey, 慎重)` 決定**
     ⇒ 膽大者可盲打；慎重者該目標不可行
③**偵查上秤**，★**禁走廊**（不得排除後直接 `try_set(TASK_SCOUT)`）
④**壓縮**：`richness_compressed = CAP × (x/ref) / (1 + x/ref)`
   `ref = 自家人口 × Σ(TARGET_PER_POP[res] × BASE_PRICE[res])`，`x = Σ(估 × BASE_PRICE)`
```
★**①②必須同一刀**：只做①會讓兩條路量級差幾百倍 ⇒ **靜默判死**。
★★**①④也必須同一刀**：只做①會把三項的秤變成一項的秤 ⇒ **殘忍與野心失效**。

# ② ★★★`CAP = 1.0` 的類別（★R² 更正我，請照這個寫註解）

```
我原本寫：「與 coin = 1.0 同型，是單位定義」⇒ ★**錯**
  `coin = 1.0` ⇒ **零自由度**（重新定義單位，世界不變）
  `CAP  = 1.0` ⇒ ★★**有自由度**（選了一個相對重要性，換成 0.5／2.0 **世界會真的不一樣**）
⇒ **它是【誠實具名的設計參數】**，與 `ARMED_FRACTION_BASE`／`SCOUT_VALUE_BLIND_PRIOR` 同族
⇒ **守衛**：①具名 ②標 TEST VALUE ＋ **明寫「這是相對重要性的選擇，不是量出來的」**
            ③★**改它要重跑驗收**（★★**不是**「真情報到手後被取代」—— 那條是給**先驗**的，CAP 不是先驗）
```
★**可重複使用的測試（請寫進註解）**：**把這個數換成別的值，世界會不會不一樣？不會 ⇒ 單位；會 ⇒ 參數。**

# ③ 驗收（★七格，四組成對）

```
①**零情報不出現**／②**補齊情報後出現**                      （成對）
③**偵查會輸**（母體＝候選集裡確實含偵查 option 的那些次）／
④**早期窗偵查真的出現過並贏過**                              （成對：一防恆贏、一防恆不出現）
⑤**先驗被取代的【逐筆】證據**（同一目標、情報到手前後各一筆）
⑥**膽大者真的盲打過**／⑦**同一目標對慎重者不在候選裡**        （★成對，**必須同一目標、不同人格**）
★★另報一欄（不入判、預先登記）：**人口驟降後的攻擊率**
   —— ref 變小 ⇒ 打殘的隊會覺得所有人都變肥；**可能是好戲也可能是病，先看數字**
```

# ④ 誠實標（★三條，寫進床檔頭）

```
・**「路開了但走不通」**：壓縮前，薄情報目標在 score 上結構性吃虧（R² 量過是**級距**不是傾斜）
  ⇒ ★**壓縮就是為了修這個** ⇒ **驗收要看它修好了沒（⑥有沒有真的發生）**
・**壓縮的已知代價**：主操作區（0.1×–3× ref）分辨力還在，**極端尾（>10× ref）被壓平**
・**`richness` 現在是「相對於我」**：窮小隊看中等村是肥羊、大國看不上眼
```

# ⑤ 順序與紀律（照舊，不新增）

**①改檔頭 ②parse-check ③才開長跑**；**開跑前看 `FreeMB`、跑完再印一次**；
**樹的 commit 要記**；**(4a)/(4b) 仍掛多 seed 那一輪，不准趁這輪帶進去**。
★**而 30 天窗那一輪【不要動】** —— 它驗的是已落地那版；**本票另起分支。**
