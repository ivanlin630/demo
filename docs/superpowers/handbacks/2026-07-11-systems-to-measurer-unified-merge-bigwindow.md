---
from: systems
to: measurer
status: open
topic: [big-window 確認] 統一併入 @32bc09b funnel ~0——排除樣本量,結構確認→to:blueprint(supersede c2-bigwindow)
---

# 量測：統一併入 big-window（確認結構性 marginal 非樣本）

（supersede `c2-bigwindow-confirm-v2`——build 已進 §HOW-6 統一併入 @32bc09b。）

§HOW-6 統一併入實作完整+驗綠但 3mo 單 seed funnel ~0（7 層修+統一+3 weight 變體全 completion≈0）。**big-window 確認結構性抗拒非樣本**（觸決策樹升 user，須實證非 1-seed）。

## 跑（長跑 tooling，別撞 bg-task kill）
- worktree `feat/consolidation-s-a @32bc09b`（§HOW-1~6 全 + 6 站 merge 漏斗探針）。**先 rebase/merge 最新 main 拿新 bed**（resume/detach/progress）。
- **`tools/godot-detach.ps1` + `WARRING_RESUME=1` + `WARRING_PROGRESS`**（03b SOP §大窗）：18 seed×3mo 單批脫離、輪詢、resume。seed=1 先估耗時。★`--path .worktrees/consolidation-s-a`，禁原地 checkout。

## 量（6 站漏斗 + 關鍵率）
- `merge_appl.food_lt3`→`surv_ok/surv_fail`（**併入勝 survival rank 的率%**=核心）→`mv_reached`→`pair_seen`→`mergein.dissolve/subteam`→**merge 完成**。
- **對照**：`join.resolve`（個人投靠端）、其他 survival option 勝率（覓食/乞食——證絕境小隊選它們非併入）。
- gate#1 非搬餓 + 隊數不崩 + determinism。

## 判準（→to:blueprint，他升 user）
- big-window merge 完成仍≈0（併入 survival-rank 勝率極低、覓食/乞食常勝）→ **結構抗拒確認**（絕境小隊理性選即時覓食非行軍併入）→ blueprint 升 user 願景 fork (a)/(b)/(c)。
- 若 big-window merge 顯著>0 → 3mo 是樣本假象，有機政體交付 → signoff。
數字 to:blueprint。
