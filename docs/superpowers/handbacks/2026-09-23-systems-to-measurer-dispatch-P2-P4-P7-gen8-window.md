---
from: systems
to: measurer
status: consumed
slice: 裁定(A)——每小時那一趟 pass 按隊錯開相位｜世代 8 量測窗
topic: ★派工：P2／P4／P7（12 天 × 2 種子，世代 8）｜★★★三件【會讓你量到假東西】的事先講：①卷面要印母體三欄 ②有一行是 defer 的解除條件，格式不能改 ③跨世代不要引用 `evaluate_all_body.*` 那幾顆鍵——它們換了單位｜★而 P1／P3／P5／P6／P8 已由 implementer 答完，**不要重跑**
---

# 一、量什麼

```
世代 8（邊界 92349afb6，main 上已開）｜HW-2｜★12 天 × 2 種子（1337／42）
P2 玩家尺：headless B3 ⇒ >2s ≤ 1／日 且 p99 < 1s
P4 母體衛生：extinct／starve／combat 帶主詞、同量級；子隊外出終止原因分佈不變
P7 吞吐：ticks／真秒 ±5%（★對照【錯開前】的基線）
```

★**為什麼是全窗不是短窗**：這三格全是**比率與水位**（分母會隨世界長大）
⇒ 短窗量到的是【另一個世界】。★★而結構性質那幾格（相位落在哪、每週期幾次、指紋）
已經由 implementer 用短窗答完 ⇒ **不要重跑**。

# ★★★二、三件會讓你量到假東西的事

```
①★卷面必須印【母體三欄】：真隊／野獸 pseudo-team／在外子隊（不變量 #9）
   ⇒ 「存活隊數」沒有主詞就是錯的；而 implementer 那一輪的 config 裡野獸＝0，
     ★你這一輪是 warring_states，★★野獸很可能不是 0
②★★P2 的卷面【必須】含一行：`[B3-FREEZE] gen=8 … verdict=PASS|FAIL`
   ⇒ 分片票／互動幀時儀器票那條 defer 的 met_check 直接 grep 它（內容錨、不挑檔名）
   ⇒ ★沒有那一行 ⇒ 兩張 HALT 的票【永遠不會被喚醒】，而它看起來就像「還沒做完」
③★★★不要跨世代引用 Probe 鍵 `evaluate_all_body.*`
   ⇒ 它在世代 8 之後量的是【loop1 的批次呼叫數】不是【完整 pass 數】——**單位換了**
   ⇒ 兩邊都非零、都好看、都不報錯，而把它們擺在一起比是【無效的】
```

# 三、已知的、不要當成發現的三件

```
★第一個遊戲日每隊會少一次 pass（首次排程被推遲 [30,120) tick，一生一次）⇒ 預期
★★相位分佈【不均勻】是已知且已量過的（最壞一格 9 隊 vs 期望 1.92）⇒ 不是紅
★★★而 implementer 量到 [TickPerf] >2s 幀數 ＝ 0／18720（3 天窗）——
   ★他自己拒絕把它講成「凍結消失了」，理由是【沒有同配置的世代 7 對照】
   ⇒ **P2 就是去把那個對照補上的那一格**
```

# 四、交付

```
①卷面落地 docs/measurements/（路徑你定，★但②那一行的格式不能改）
②★三格各自的判決行要帶操作元（數字與門檻在同一行）
③★★你量到的若與「已知的那三件」衝突 ⇒ 回我，不要自己解釋
★★★機器：現在空著（Godot=0）。跑之前請先量一次並在卷面印出來——
   今天我因為「宣稱一個我沒量的狀態」被糾正過兩次。
```
