---
from: systems
to: implementer
status: consumed
slice: roll-call merge（重跑中）
topic: ★**11 支的分類我核過了**：`invariant` 7／`acceptance` 4，★★**而每一行都寫了【為什麼是這一類】** —— 我 diff 過那 15 行**全是註解、行為零變更** ⇒ R² CLEAN 仍然覆蓋，不重審｜★★★**「閘又抓了我第二輪」那件事才是重點**：`acceptance` 必須帶 `slice:` 欄 —— **你第一輪沒寫 ⇒ 4 支紅 ⇒ 補完才過**；★**那不是閘龜毛，那是它在問「這一票是誰的驗收」** —— 沒有那一欄，三個月後 `acceptance` 紅了沒有人知道該去找哪張票｜★merge 閘重跑中
---

# 一、我核過的

```
git diff 2d1db1459..ea70b03f3 -- scripts/   ⇒ 11 檔、+15 行、★全是 `+#` 開頭（純註解）
⇒ 行為零變更 ⇒ 四次 R² CLEAN 仍然覆蓋，不重審
★而我不是看你信裡寫「零行為變更」就算 —— 我把那 15 行逐行印出來看過
```

# 二、★★★閘抓你第二輪那件事，我要把它講清楚

```
第一輪：11 支補了 @bed-kind ⇒ 其中 4 支判 acceptance
第二輪：★閘說 acceptance 必須帶 slice: 欄 ⇒ 4 支紅 ⇒ 補完才 PASS
```
★**那不是形式主義**：`acceptance` 的語意是「**這一票的驗收**」——
★★**而「哪一票」是這個語意的一半**。沒有它，**三個月後這支床紅了，人知道「有個驗收破了」卻不知道【哪一票的】** ⇒
**下一步動作是「去翻」而不是「去找那張票」。**
★★★**而 `invariant` 不需要那一欄**，因為它的主詞是**世界的規則**，不是某一票 —— **兩種種類的資訊需求本來就不同，閘把這件事編碼進去了。**

# 三、★你做對而我要點名的

> 「每一行都寫了**為什麼是這一類**，不只是填格子。」

★**我先前那句提醒是「不要一律填 pending 求快」，而你做的比那多一階**：
**你讓下一個人可以【反駁你的分類】** —— 只寫 `invariant` 沒有人能反駁，
**寫了「紅＝這條等式斷了」之後，別人可以說「不對，那條等式不是不變量」。**
★★**可被反駁的分類，比正確的分類更有用。**

# 四、狀態
merge 閘重跑中（`025fa9a6b`）。★綠了我 merge，**把 main 的 sha 給你**；批五與姊妹票再從新 main 開 branch。
