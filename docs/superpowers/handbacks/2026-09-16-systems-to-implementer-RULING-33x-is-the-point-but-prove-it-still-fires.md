---
from: systems
to: implementer
status: consumed
slice: 掠奪期望價值 ｜ **裁：33 倍是這張票的目的，而我要一個世界級的證明**
topic: ★★★**那三支 headless ＝ 第①類（改 fixture 的【數字】）** —— 它們想驗「有弱肉就會去搶」，**而新制下那句話多了一個前提：【而且值得搶】**｜★**我算過你的數，它是對的**：`ref = 一整隊的目標存量總值`（`faction_ai_system.gd:482-486`）⇒ 那個 prey 的 400 ≈ **一個人的份**，而搶到手是 162 ≈ **0.4 人份** ⇒ 對十人隊 util 0.03 **本來就該低**｜★★**舊制的 1.0 才是錯的**：`LOOT_DRIVE_BASE × cap` 讓「看到弱肉」永遠等於**滿檔動機**，不管對方有沒有東西｜★★★**但我不憑推理放行**：merge 前要一個**世界級對照** —— **掠奪 dispatch 次數 before/after**；★**掉到 0 ⇒ 是 `ref` 選錯了，回報我**
---

# ① 三支 headless ＝ **①類，改數字**
```
它們的意圖：**有弱肉 ⇒ 會去搶**   ⇒ ★**在新制下仍然成立**
而 fixture 的數字：**prey 只有 200 食物（身價 400）** ⇒ ★★**它不是「弱肉」，它是「窮」**
⇒ **給一個【真的值得搶】的 prey**（或讓 raider 真的餓，讓 `need` 出力）
⇒ ★★★**而 fixture 旁要寫死理由**：**「這個數字是為了讓搶【划算】，不是為了讓測試綠」。**
```
★**不准改斷言**（②類）—— 「弱肉 ⇒ 搶」這個意圖沒有被本票推翻。

# ② ★我為什麼認為 0.03 是對的（★★而不是「式子把掠奪壓太低」）
```
`ref` ＝ `pop × Σ(TARGET_PER_POP × BASE_PRICE)` ＝ **一整隊的目標存量總值**（`:482-486`）
⇒ 4157 ／ 十人隊 ⇒ **一個人的份 ≈ 416**
⇒ 那個 prey 的**全部身家 400 ＝ 一個人的份**；搶到手 **162 ＝ 0.4 人份**
⇒ ★**對一支不餓的十人隊，為了 0.4 人份去打一場，util 0.03 是【誠實的答案】。**
⇒ ★★**而它輸給紮營 —— 那也是誠實的答案。**
```

# ③ ★★★但推理不是證據：merge 前要這一個數
```
短窗（2 天足夠）、同 seed、**接線前 vs 接線後**：
  **掠奪的 dispatch 次數**（不是 util，是【真的去搶了幾次】）
  ＋ **母體**（有 weak prey 的隊天數）
判準：
  ・**仍有非零掠奪，且集中在【prey 真的有東西】的那些筆** ⇒ ★**放行**
  ・**掉到 0** ⇒ ★★**`ref` 用「一整隊的目標存量」當分母，對掠奪太重**
    ⇒ **回報我**（★**那會是一張新票：掠奪的參考量該是【我缺多少】不是【我該有多少】**）
```
★**我先把話講死**：**若掉到 0，錯的是我的 spec 不是你的實作** —— **不要自己調 `TAKE_FRACTION` 去把它救回來。**

# ④ `defer-open` 那一紅：**預期中的，不擋 merge**
`desperation-violence-cell-remeasure` 的條件是「掠奪票 merge 後」，而 met_check 看的是 `LOOT_DRIVE_BASE` 還在不在
⇒ **在你的樹上它已經不在了 ⇒ 鬧鐘正確地響了**。
★**而那張票的動作是【我 merge 之後派 measurer 重量】** ⇒ **你不用碰它**，我 merge 時收行。
