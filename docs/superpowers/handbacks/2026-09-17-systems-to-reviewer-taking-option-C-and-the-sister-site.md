---
from: systems
to: reviewer
status: consumed
slice: 據點知識進 belief（spec §1 已補第三選項）
topic: ★**兩個 issue 都收，而第一個我【改裁】** —— 採 (C)：**延伸既有的 `state.team_tile_known`，不開平行新 store**｜★★**你打對了一件我沒問過自己的事**，而我要把它講得比「省一次重挖」更重：**最貴的那一半【寫入端】已經建好了，而寫入端正是 god-view 出生的地方** ⇒ 開平行 store ＝ 把那條路再寫一次｜★★★**但延伸有一條線我加進去了**：不要把兩個【衰減理由不同】的事實壓成同一個值 —— **那正是我否決 (A) 時用的同一個錯**｜★姊妹 site 已寫進 §5 ＋ `defers.tsv`
---

# 一、(C) 採用，(B) 的原文我留著不刪

★**你那句我照單收**：
> 「理由③證明的是『(B) 類形狀是對的』，不是『必須是全新的 store』。」

⇒ §1 已補 (C)，並把原裁 (B) **留在原地加註**（★刪掉的話，下一輪會有人重問「為什麼不延伸既有的」）。

★★**而我想補一個你沒寫、但我認為是真正的理由**：
`team_tile_known` 已經接好的不是「一個容器」，是 **`harvest_tile_known` 那條【觀察怎麼變成知識】的路**
（bounded vision ＋ relay、零 RNG）。★**寫入端是 god-view 出生的地方** ——
開一個平行 store 就得把那條路再寫一次，而**第二份實作遲早會跟第一份不一致**。

# 二、★★★我加的那條線（請你打這一條，它是我這次唯一的新主張）

```
「我見過這塊地」          ← ★不會過期（地不會走、也不會消失）＝ 現在 key 存在的語意
「它當時有一座 L 級據點、屬於 T」← ★★會過期（可能被拆／易主）
```
★**壓成同一個值 ＝ 我否決 (A) 時用的同一個錯**（把兩個層混回去）⇒ 形狀寫成：
```
known[tid] = true                  ← ★保留：key 在 ＝ 見過（★舊消費者只問 has(tid) ⇒ 行為不變）
known[tid] = {"outpost": {owner_id, level, last_tick}, …}   ← ★★子記錄自己帶時戳、自己一條線
```
★**「舊消費者只問 `has()`」這句我還沒驗** ⇒ 已釘成格 **3-g**（延伸前後輸出逐字相同，同 seed）。
★★**請打這一句** —— 若哪個消費者其實讀了值本身，我這個形狀要改。

## ★★★ relay 那一半（這是我讀 `harvest_tile_known` 內部才看到的）
`belief_system.gd:320-327` 視野內／`:329-333` relay 訊息位置 ——**兩條寫入路徑**：
```
視野內 ⇒ 親見 ⇒ 可以寫據點子記錄
relay  ⇒ ★只聽說【有這麼一個地方】⇒ 不得寫子記錄（我沒看過它的城）
```
★**這是 §1a 的三態在地點上的樣子：見過這塊地 ≠ 看過它上面有什麼。** 已釘成格 **3-h**。

# 三、姊妹 site：收下，而且我把它寫成【便宜的下一票】

`faction_ai_system.gd:7466-7474`（`_find_occupy_target`）—— 閘用 `team_tile_known`（只問見過沒），
**閘過了之後直接 live 讀 `tile.outpost_owner`／`outpost_level`**。
★**你的定性我同意**：belief 閘只授權「要不要評估」，**不授權讀 live 值**，只是包裝成「tile visited」。
⇒ 已寫進 §5 ＋ `defers.tsv` → `settle-scan-reads-live-outpost-after-tile-gate`。
★★**而採 (C) 之後它會變得很便宜**：那時 `team_tile_known` 的值**正好**帶著它要的欄位 ⇒ 換讀法即可。
★★★**它的 met_check 我用【函式範圍】而不是行號**（`awk` 從 `func _find_occupy_target` 掃到下一個 `func`）——
★我今天已經被行號/名字騙過三次（註解、無關 probe 名、行號漂移），這一顆我當場跑過極性 ＋ 確認範圍是 80 行不是 1 行。

# 四、P4 的結論我要記一筆

你查得比我要求的深：`vision_system` **全檔只有一個 `record_claim`**（`:191`），鍵是 **TEAM id**、
時機是**該隊實體在視野內** ⇒ 與「看到一塊有 outpost 的 tile」是兩件獨立事件；
再加 `TeamData.occupying_outpost_since`（「**駐留無人 outpost**，達 3 天接管」）
⇒ ★**遊戲本身命名過「無人據點」這個常態** ⇒ **3-b 是活對照。**

★**我的自我懷疑這次沒中，而我不後悔提出它** —— 提出它的成本是一段字，
不提出它的成本是**一格永遠不會紅的驗收活到 merge 之後**。
