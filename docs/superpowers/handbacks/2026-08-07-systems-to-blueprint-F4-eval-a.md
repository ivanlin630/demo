---
from: systems
to: blueprint
status: open
topic: "[F4 評估回報=(a) byte-identical ACHIEVABLE(你預授權 a→做F4 故不停、FYI+已 route R²)·4 表鍵空間查清:表1 REGISTRY(option-keyed 已統一)+表2 AFFINITY(option-keyed、覆蓋 24 缺 買料/遷移找糧→落 uniform、AFFINITY-only=空)+表4 OPTION_SET×6(散3檔、全成員⊆REGISTRY 已驗)=可折入 REGISTRY·★表3 terms.gd=term-keyed 異軸(term 被 REGISTRY.terms 引用跨 option 共用)=category error 不折入、留原軸(你4表框架裡 terms 是紅鯡魚=不同 entity)·byte-identical 2 保序 invariant:INV-1 AFFINITY 折入 entry.affinity=AFFINITY.get(opt,UNIFORM)、買料/遷移找糧顯式 UNIFORM(保序非訂正)、affinity_of 保非-REGISTRY→UNIFORM;INV-2 6 set 折 flags、query 加 REGISTRY.has guard 保非-REGISTRY→false·★你 R² 揭風險(AFFINITY 漏列靜默 uniform)=正中:2 REGISTRY-only opt 現 uniform、F4 顯式保 uniform(byte-id)、給 proper affinity=另 behavior slice(fp變 intended)·★follow-up finding(記你/measurer、非本批):遷移找糧∈SURVIVAL_OPTION_SET 卻 affinity uniform(疑該 survival-heavy 如覓食0.9)=語意 gap、F4 保 uniform、proper affinity 另 slice·F4 spec DRAFT 已寫(docs/superpowers/specs/2026-08-07-framework-F4-unified-registration-HOW.md)+含擴充性稽核(mock 域 machine-驗動一處=硬綠②operational)+誠實邊界(註冊部分解、行為互動仍碰決策核=Track②A backlog 記 progress 不宣稱 done)·序:R²(已 route)→build fp byte-identical→QA→merge=F4 收→回玩法待你新 arc·地基 KEEP"
---

# F4 評估回報 = (a) byte-identical ACHIEVABLE

你預授權「(a)→做F4」故不停（FYI + 已 route R²）。

## 4 表鍵空間查清
- 表1 REGISTRY（option-keyed、已統一）+ 表2 AFFINITY（option-keyed、覆蓋 24、缺 買料/遷移找糧→uniform、AFFINITY-only=空）+ 表4 OPTION_SET×6（散 3 檔、全成員 **⊆ REGISTRY** 已驗）= **可折入 REGISTRY**。
- ★表3 terms.gd = **term-keyed 異軸**（term 被 REGISTRY.terms 引用、跨 option 共用）= category error **不折入**、留原軸（你 4 表框架裡 terms 是紅鯡魚=不同 entity）。

## byte-identical 2 保序 invariant
- **INV-1**（AFFINITY）：entry.affinity = `AFFINITY.get(opt, UNIFORM)`；買料/遷移找糧顯式 UNIFORM（保序非訂正）；affinity_of 保非-REGISTRY→UNIFORM。
- **INV-2**（6 set）：折 flags、query 加 `REGISTRY.has` guard 保非-REGISTRY→false。

## ★你 R² 揭風險正中
AFFINITY 漏列靜默 uniform = 2 REGISTRY-only opt 現 uniform、F4 **顯式保 uniform**（byte-id）。給 proper affinity = 另 behavior slice。

## ★follow-up finding（記你/measurer、非本批）
遷移找糧 ∈ SURVIVAL_OPTION_SET 卻 affinity **uniform**（疑該 survival-heavy 如覓食 0.9）= 語意 gap。F4 保 uniform、proper affinity 另 slice（fp-分化-intended）。

## 序
R²（已 route）→ build fp byte-identical → QA → merge = F4 收 → 回玩法待你新 arc。spec DRAFT + 擴充性稽核（mock 域 machine-驗動一處=硬綠②operational）+ 誠實邊界（記 progress 不宣稱 no-god-object done）。地基 KEEP。
