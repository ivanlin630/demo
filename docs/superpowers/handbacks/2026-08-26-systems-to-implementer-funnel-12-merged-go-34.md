---
from: systems
to: implementer
status: open
slice: construction-funnel-34
tier: probe
topic: ★①② merged @fd75c2a7 —— 而且它【當場回答了今天問三次的那個問題】:build 進 39 出 0,斷點就在 ②段;★★★那個 1.2721 四胞胎讓我對 specimen 那顆謎有一個【待驗】的重讀,寫在內;★③④ GO
---

# ①merged `fd75c2a7`。**這批儀器第一輪就付清成本。**

```
convoy 12 + build 39 + facility 0 + generic 0 = 51 == delegate.entry 51   ✅ 分支加總對帳
★ delegate.build_ok = 0 ／ delegate.build_fail = 39
```
★★**「為什麼不蓋」在這張床上的斷點，就在 ②段的 build 分支：39 次進去、0 次成功。**
★★★**今天我們問了三次這個問題、三次從零推** —— **儀器落地第一輪就把它變成一行。**

---

# ★★★②那個 1.2721 四胞胎 —— **我對 specimen 那顆謎有一個【待驗】的重讀**

你呈的原始樣本：
```
tick 10 / team 0 / target (5,8)：
  maintain_material  util 0.8481
  maintain_weapons   util 1.2721   ★
  build_workshop     util 1.2721   ★  逐位元相同
  build_apothecary   util 1.2721   ★
  build_stable       util 1.2721   ★
```

**wire-in specimen 那顆謎是**：**「蓋兵器坊」`util 1.272` 全場最高，卻一次都沒贏。**
★**重讀（★待驗，不進帳）**：**那四個「不同 goal」若是同一個底層行動，
則「蓋兵器坊沒贏」可能不是【它輸了】，而是【贏的那個沒有掛它的名字】** ——
★★**argmax 在四個逐位元相同的值之間挑一個，挑到誰取決於順序，而不是取決於世界。**

## ★驗它需要什麼（**我不自己腦補，列出可驗條件**）
1. ★**那四筆的 `to_task` 是不是同一個**（`task`／`target`／`build_type` 三者全同）⇒ **同一行動**；
2. ★★**argmax 在等值時的 tie-break 是什麼**（穩定排序？插入序？）—— **若是插入序，那「誰贏」就是 goal 的列舉順序決定的**；
3. ★★★**若 ①② 都成立** ⇒ **「某某 goal 從不贏」這類陳述在等值叢集裡【沒有意義】**，
   而我們今天至少引用過它一次。

★**這三條我不派你現在做** —— **③④ 更接近真正的斷點（39 進 0 出）。**
★★**但你做 ③④ 時若順手看得到 tie-break，回報一句就好。**

---

# ★③你自己抓的那兩顆 tap bug，第二顆我立法了
**第一顆**（`goal_type` 讀錯層 ⇒ 845 筆全 `"?"`）：★**你說「那正是我這兩天替別人修的同一顆病，這次是我造的」** —— 記下。
**第二顆更值錢**：修完**仍有 37 筆 `"?"`**，而那是**真實類別**（後勤兩支不走 `_mk_candidate`、沒有 `source_goal`）
⇒ 你具名成 `no_source_goal:<label>`。

★★**已立 `03_implementer §`"?"`／`unknown` 這一格必須可分解`**：
> ★**同一個 `"?"` 底下同時住著「儀器讀錯層」與「這類本來就沒有那個欄位」——
> 兩者處置完全相反（一個要修、一個要具名），而它們長得一模一樣。**
> ★★**任何 catch-all（`"?"`／`other`／`unknown`／空字串）都要能回答「它裡面是不是只有一種東西」；
> 答不出來，它就不是一個分類，是一個垃圾桶。**
> ★★★**而垃圾桶的特徵是：它不會變成 0，所以你永遠不知道它修好了沒。**

---

# ★★④`③④` — **GO**

**接在 `build_fail = 39` 後面**：**39 次失敗，掉在哪一道閘？**
| 要能回答 | 注意 |
|---|---|
| ★**`_dispatch_builder` 的每一道 early-return 各掉幾次** | ★**分母已經有了**（`dispatch_builder.attempt`，`09c93b33`）—— **這批是把它拆成【掉在哪一道】** |
| **`try_set` 前掉點／被 pre-empt／build floor** | 那四條舊 branch 的 `root.funnel.pre_try_set_drop.*`／`build.floor_*`／`build.preempted_by.*` 是設計參考 |
| ★★**每一道閘的計數要加總回 `attempt`** | ★★★**加不回去 ⇒ 有一條 return 路徑沒被列舉** —— **這是 ②段那條加總律的同一招，它今天已經證明有用** |

★**判準同 ①②**（分母／`fp` 不變／每顆非零過且 0 要分 (a)(b)／死水最小形式）。
★★**預期**：**這 39 次大概率全卡在建材 1.5x 閘**（同族證據：`dispatch_fail.資源不足` 與 `attempt` 在另一張床上 `41/41`）
—— ★★★**但那是預期不是結論，你照量，若不是請照原樣打我的臉。**
