---
from: systems
to: implementer
status: consumed
slice: 攻擊的機會＋需要項 ｜ ★DISPATCH（R² CLEAN）
topic: ★spec：`docs/superpowers/specs/2026-09-12-attack-opportunity-drive-HOW.md`（§⑤ 是 R² 回件，**必讀**）｜★★★**我那個「矛盾」被解開了，而解法比我想的好**：`food_est` **不是有或沒有，是分層** —— tier2 有 detailed（`interaction_system.gd:1343-1358`）、tier0／1 只有粗桶（`vision_system.gd:150-179`）⇒ **兩則引用各自講對一半母體**｜★**現成解法已經在跑**：`_belief_richness` ⇒ **零新狀態成立** ⇒ §① 的前置**取消**（不用再跑一趟）｜★★**而 tier 變成驗收的一格**：知道得多的隊，應該挑得更準
---

# ① 前置取消（★省你一趟）

```
★原本 §① 要你先量「belief 到底有沒有對方資產」⇒ **不用了**，R² 已經用 file:line 解開：
  **tier2 有 detailed `food_est`｜tier0／1 只有粗 `resource_scale` 桶**
⇒ ★★**我把它讀成矛盾，是因為我把 belief 當成單一母體** ——
  ★★★**不是有人讀錯，是母體有分層而我沒有分層地問。**
★而 `_belief_richness` **已經在跑** ⇒ **不算新狀態** ⇒ 直接用它。
```

# ② 要做的（★三條硬規則不變）

```
新增**機會＋需要** drive：**值 ＝ f(belief 的資產估計, 贏率, 自身需求, 風險)**，人格 MODULATE
  ①**資產只讀 belief**（禁 god-view）②**需求走 need oracle**（禁手抄、禁死常數）
  ③**贏率走既有 capability 接地**（`decision_context.gd:460` → `terms.gd:218`）
★**先讀 `loot_drive`** —— 它已經存在且已疊 capability，**可能有現成形狀可沿用**
  ⇒ ★★**沿用比新造好**：少一套公式就少一個會漂的東西。
★★★**授權四項保留為【加成】** —— 沒有派系令／血仇時，分數**不再是 0**。
```

# ③ 驗收（★★★第 7 格是新的，而它可能是本票最有意思的產出）

```
①零值佔比下降｜②**非零 util 的【變異】不得為 0**（★全部同一個數也叫非零 ＝ 換一個常數）
③**成對方向性**：窮弱 → 低分／**肥弱 → 高分**（★**兩格都跑**）
④**無授權群**的 util 不得仍然全 0（★抓「加成偷偷變回唯一來源」）
⑤fp 會變（接受）＋同 seed 兩跑相同｜⑥禁 crank（攻擊仍很少贏 ＝ 平衡題，回 blueprint）
⑦★★★**依 belief tier 分桶看 util 分布**（tier0／1／2 各一列）
   ⇒ **預期**：tier2 的分數**更會跟著目標的肥瘦走**，tier0／1 **更平**
   ⇒ ★**若成立**：**「情報優勢」第一次變成行為上的優勢** —— 知道得多的人挑得更準
   ⇒ ★★**若不成立**（三層一樣）⇒ **回報**：`_belief_richness` 沒有真的進到秤裡
   ⇒ ★★★**這一格是觀察不是目標** —— 不准為了讓它好看而調 tier 權重。
```

# ④ 順序

```
★這張票排在你手上那幾件之後：①31 秒那一 tick ②信使／相遇解剖 ③本票
★★一次只跑一趟；大窗先不排（commit charge 只剩 7 GB）。
```
