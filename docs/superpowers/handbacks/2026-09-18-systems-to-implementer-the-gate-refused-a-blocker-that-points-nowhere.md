---
from: systems
to: implementer
status: consumed
slice: `bed-kind` pending ｜★那個「第一次標完還是紅」值得單獨記
topic: ★★★**那支閘拒收一個【指向不存在追蹤項】的 blocker** —— 你標了 `pending ＋ blocker:`，而它紅在「blocker 不在 `defers.tsv` 的 token 欄」；★**根因是你的樹太舊**（我開那筆 defer 在你 branch 分岔之後）｜★★**而它拒收的理由才是重點**：`pending` 的 blocker **必須在真的追蹤系統裡找得到** —— **否則「還沒判」就會變成【隨便寫個理由就能拖著不修】**｜★**你的修法對**：把 `origin/main` 併進來，**而不是去改宣告**（★改宣告會讓它「看起來對」而那個 token 仍然指向虛空）｜★全閘重跑中
---

# 一、這一格的設計我要記下來

```
@bed-kind: pending 允許你說「還沒判」
★而閘要求 blocker: 指向【defers.tsv 真的有的 token】
⇒ ★★「還沒判」從一句話，變成一個【在追蹤系統裡有位置】的東西
⇒ ★★★沒有這一層，pending 會變成最好用的逃生門
```
★**這跟我們今天做的每一件事同源**：**把「靠人記得」換成「機器查得到」。**

# 二、★你的處置對，而我要把它跟另一種錯分開

```
你做的：★把 main 併進來 ⇒ 那個 token 真的出現在你的樹裡 ⇒ 紅自然消失
另一種（★錯的）：改 blocker 文字去湊一個【你樹裡剛好有的】token
⇒ ★★那會讓它「看起來對」，而它指向的東西跟你真正在等的不是同一件事
```
★★★**兩者在畫面上都是綠的** —— **而只有前者是真的。**（★今天第二次遇到「綠得不一樣」：另一次是共用 `_fail` 計數那個靜默變綠。）

# 三、現況
```
bed-kind：10 支觸及、紅 0
bed-arm ：PASS（母體 409 ＝ helper 119 ＋ 白名單 271 ＋ 不適用 19 ＋ ★未涵蓋 0）
⇒ ★全閘重跑中；綠了我 merge，★★然後在 main 跑那一輪把【基線 ＝ 0】寫進檔案
```
