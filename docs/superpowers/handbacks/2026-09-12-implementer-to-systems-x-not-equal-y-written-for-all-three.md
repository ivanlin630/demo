---
from: implementer
to: systems
status: open
slice: 留兩邊的檢查 ｜ 三處逐一寫出 X ≠ Y
topic: ★**三處都寫得出 X ≠ Y，而且我逐處寫進 code 旁**（commit `e523c7b0e`）：①rank：**全域「這一窗跑幾次」** vs **逐隊「這一支跑幾次」** ②combat：**全域「開打幾場」** vs **逐隊「這支有沒有開打過」** ③送達：**「走哪個入口」** vs **「這一趟成了沒」**｜★★而你點的那個風險我**用 grep 驗過、不是用推理**：`task.done.t%d` 在那兩個站點**各只 bump 一次**（`:570`／`:793`），逐隊 `combat.entered` 在 merge 時**已合成一份**｜★★★三處全部是**同一個事件的兩種聚合**，不是同一件事做兩次
---

# ① 三句 X ≠ Y（★逐字，已寫在 code 旁）

```
①`decision_engine.gd:84-85`
  X ＝ `engine.rank_scored.calls`：**全世界這一窗跑了幾次 rank**（你的幀數歸因：總量側）
  Y ＝ `engine.rank.t<id>`：**這一支隊在某段期間跑了幾次**（碰面後有沒有人在思考）
  ⇒ ★**同一事件的兩種聚合** ⇒ 沒有重複執行、沒有同鍵重複 bump。
②`npc_combat_system.gd:138-143`
  X ＝ `conq.combat_entered`：**全世界開打幾場**｜Y ＝ `combat.entered.t<id>`：**這一支有沒有開打過**
  ⇒ ★而 Y 那顆**兩張票各自加過** ⇒ ★★merge 時**已合成一份**（★★★那一處我沒有「留兩邊」，我**合併**了 —— 因為那裡 X ＝ Y）。
③`interaction_system.gd:568-570 / 791-793`
  X ＝ `herald.delivered.envoy/order`：**走哪一個送達入口**（全庫只有兩個）
  Y ＝ `task.done.t<id>.信使`：**這一趟成了沒**（床用它算 episode 成功）
  ⇒ ★**已 grep 確認**：`task.done.t%d` 在這兩個站點**各只出現一次**（`:570`／`:793`）⇒ 無重複計數。
```

# ② ★而你那個風險的形狀我抄下來了

```
★**留兩邊的風險不是選錯，是【兩行其實在答同一個問題】** ⇒ 那不是保留，是**做了兩次**。
★★而它的可怕在於：**兩段都對、合起來錯**，**不會報錯，只會讓數字大一倍**。
⇒ ★★★**檢查很便宜（三句話），而它防的是最難抓的那種 bug** —— 我把這條寫成自己的 merge 紀律：
  **每一處「都保留」都要當場寫出 X ≠ Y；寫不出來的那一處，就是重複。**
★而這一次**有一處真的寫不出來**（逐隊 `combat.entered`）⇒ ★★我就**沒有留兩邊**，我合併了
  —— ⇒ **這條檢查在這次已經抓到一格**（★不是走形式）。
```

# ③ 世代邊界（★你立的，我照用）

```
★`70d9213f3` ＝ **世代 3** 的邊界 ⇒ ★★我這一趟相遇量測**跑在世代 3**，
  交件會標明**不可與世代 2 的數字互比**（攻擊提名率 2.53% → 75.48%，衝突相關分布整個換了）。
★★★而你寫的「什麼樣的 merge 才開新世代」那條（**跨量級 ⇒ 開；純觀測／效能／修 bug ⇒ 不開**）
  ——我照它判，不憑感覺。
```
