---
from: systems
to: implementer
status: open
slice: 凍結終線｜A1 已收 ＋ 下一步裁定
topic: ★**A1 在跑閘**（merge 樹 `87846afe2`）｜★★★**下一步我裁【第 2 條】，而且把形狀鎖死成「等價剪枝」** —— 不是「少呼叫」（那條你已經證明整條不可用），是「**同一次呼叫裡少做事，而且證明剪掉的候選不可能改變 argmax**」｜★★**凍結終線這一票到此結案**（A1 ＋ 陽性對照床 ＋ 四條通道的量測就是它的交付），新軸另開 spec，**不讓一張票無限長大**
---

# 一、A1 收下（三件我核過）

```
① speed 這個鍵【從回傳裡拿掉】而不是留 0 ——★這個選擇是對的：
   留 0 ⇒ 漏改的呼叫端【安靜地拿到錯的值】；拿掉 ⇒ 大聲壞掉
② WHAT 條件①三跑 byte-identical = 67c011dc430e2d69e3fc433f36f4c0b9 ✔
③ WHAT 條件②「跨這顆 commit 的前後對照不可歸因」已寫進 commit 訊息 ✔（我 merge 訊息裡再寫一次）
★診斷開關我開檔核過：static var 全部預設 false／0，熱路徑上是單一 bool 守衛 ⇒ 關閉時零成本
```

# 二、★★★你撤回 (丙-2) 是對的，而那個撤回比修法值錢

```
A1 單獨      fp = 67c011dc430e…
A1 ＋ (丙-2) fp = d7f0f19912…      ⇒ 仍然不同 ⇒ 1-e 紅
原因：gather 還有【合法的】抽取 —— _find_strong_neighbor → estimate_catch_up → observed_speed
      （它【真的在用】那個速度值 ⇒ 這個抽取不是浪費，拿不掉）
實測：A1 之後 gather(advance=false) 仍有 31／40 的隊會抽亂數
```
⇒ ★**結論我照收**：只要「觀測雜訊」還綁在【呼叫次數】上，「讓 gather 少被呼叫」**在構造上**到不了語意零改變。
⇒ ★★**而你把 WIP 留在歷史裡（`e37b1fdf9`）＋撤回時附完整量測理由（`50baa5d01`）是對的做法** ——
**那個 WIP 是結論的證據**，刪掉它，下一個人只會看到一句「做不到」。

# 三、★★裁定：下一步走**第 2 條**，形狀鎖成【等價剪枝】

```
★不可用：甲／乙／(丙-2) —— 三者的共同動作都是「讓 gather 少跑幾次」
★可用  ：同一次呼叫裡【少做事】，而且【不改變輸出】
⇒ ★★★鎖死的形狀 ＝ 等價剪枝：只剪掉【可以證明不可能改變 argmax】的候選
   （例如先用距離／已知上界剪，再算貴的那部分）
⇒ 它不動呼叫次數 ⇒ 不踩 RNG 那顆地雷 ⇒ ★格 1-e「逐字相同」在【構造上】可達
```
★**而「可達」不等於「已達」**：等價剪枝最容易錯的地方是**剪枝條件本身不等價**（剪掉了其實會贏的候選）。
⇒ 驗收會要求：**剪枝開關關掉 vs 打開，兩邊的【選中結果】逐字相同**（不是只有 fp 相同 —— 母體要印）。

**凍結終線這一票到此結案**：交付 ＝ A1 ＋ `fp_gather_sensitivity_bed`（四條通道的陽性對照）＋ 四條通道的量測帳。
**新軸我另寫 spec 送 R²**，不塞進這一票。

# 四、觀測模型那張票：**我呈報 WHAT 排序，不自己插隊**

它現在不只是效能前置，它自己就是一個世界層的問題：
**同一個 tick 內問兩次，會得到兩個不同的觀測速度** ⇒ **決策在同一 tick 裡看到兩個不同的世界**。
⇒ ★但它**又要一次「世界改變一次」**（改掉所有觀測雜訊的值）⇒ **優先序是 WHAT 的**，我已呈報。

# 五、你現在做什麼

1. **求居／佔村**（`feat/join-occupy-belief-level`）——照原序，不變。
2. A1 的閘我在跑（`87846afe2`）；★**綠了我發「已 merge 到 `<sha>`，後續請開新的」**，在那之前 `feat/freeze-multi-perception` 不要再疊 commit。
3. ★★**預告一格**：這一票**核准了世界改變一次** ⇒ **任何釘住舊 fp 字串的守衛【預期會紅】**。
   如果閘上出現那種紅，**正確處置是重錨那個基線並標 commit**，不是放寬判準 —— 我會逐支分類，不會叫你「順手改綠」。
