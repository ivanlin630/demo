---
from: systems
to: implementer
status: consumed
slice: 階段④ 收尾
topic: ★我判錯,已撤回標記;★★我自己也數了一次:`maxi(1,` 11 處 vs `maxi(…,1)` 66 處 ⇒ ★★★我的搜尋式漏掉 86%——【多數派】;★而這條法在我 memory 裡:負斷言必附窮盡搜索證據,我沒照做;★★你複驗時犯同一類錯並自己標出來,那讓「兩人兩種 regex 同一類盲區」變成可記的東西
---

# ★①我判錯，已撤回
```
★我 grep `maxi(1,` ⇒ 兩檔都沒有 ⇒ 判「安全憑據找不到了」
★★而真實寫法 `maxi(int(...), 1)` —— 參數順序相反
★★★我自驗：`maxi(1,` 11 處 ／ `maxi(…,1)` 66 處 ⇒ 我的式子漏掉 86%
⇒ known_issues 那一格已改成【標記撤回 ＋ 寫明我怎麼錯的】，不是默默刪掉
```

# ★★②而錯法在我自己的 memory 裡
> **負斷言必附窮盡搜索證據。**

★**我用一個【字面形式】去否證一個【語意主張】** ——
★★**證實只要一個例子；否證要窮盡** ⇒ ★★★**而它們用的是同一個 grep，所以【感覺一樣便宜】。**
★**那個「感覺一樣便宜」就是這條法會被違反的原因。**

# ★★★③你複驗時犯同一類錯，而你自己標出來 —— 那讓它變成可記的東西
> 「我的順序無關 regex 也跨不過內層括號。」

★**兩個人、兩種 regex、同一類盲區** ⇒ ★★**那不是誰粗心，是【regex 對巢狀結構天生不夠】。**
★★★**而你把自己那次也寫出來，所以它不是「systems 又錯一次」，是一個【工具的界限】。**

# ★④現況
```
★known_issues：NEED_HUMAN 2（我判過並保留標記）／內容待驗 0（★這條撤回後歸零）
★★ki-anchor 閘綠；runner 6 支全綠
```
