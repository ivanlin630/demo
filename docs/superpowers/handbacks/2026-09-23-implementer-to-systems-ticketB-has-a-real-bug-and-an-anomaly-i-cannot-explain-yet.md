---
from: implementer
to: systems
status: consumed
slice: 票B
topic: ★票B 第 1＋2 批第一次跑起來：**抓到我自己的一個真 bug（已修）＋一個我還解釋不了的異常（未修）**｜★★修好的那個：我的「分頁區」擷取器**遇到第一條 `────` 就停**，而搬過去的資源段【開頭就是分隔線】⇒ 整頁被判成 0 行｜★★★沒修好的：`_build_survival_lines()` 在執行期**回傳 0 行**，而它的函式體【無條件 append 兩行】
---

# ★一、修好的那個（★它同時造成兩支紅）

```
症狀：FAIL 第 2 頁的【分頁區】非空（0 行）＋ FAIL 零損失（一堆行「不見了」）
根因：★我的 _page_body 寫成「頁首之後、遇到第一條 ──── 就停」
     而票B 把資源段搬到經濟頁時，★★那一段【開頭就是一條分隔線】
⇒ ★★★整頁被判成 0 行 ⇒ 兩支紅都在說「內容不見了」，真相是【我沒讀到】
修法：錨在【Tick: 那一行】—— 頁尾＝「Tick 行 ＋ 它前面那條分隔線」
結果：五頁的分頁區都非空（6／9／4／4／4 行）
```

★**教訓**：**我的擷取器把一個【內容特徵】當成【結構邊界】** ——
★★而分隔線是內容會用的東西，它不能拿來當「頁尾」的定義。

# ★★★二、沒修好的異常（★我停在這裡，不猜）

```
[DIAG2] survival 回傳 0 行｜page_idx=0     ← 執行期實測，三次都一樣
而 scripts/ui/text_ui_main.gd 的函式體（定義數＝1，return 在最後）：
  func _build_survival_lines(ct, ps) -> Array:
      var lines: Array = []
      var food_days ... var starving ...
      lines.append("糧: %.1f 天%s" % ...)        ← ★無條件
      lines.append(_member_health_line(...))     ← ★無條件
      if ps.get("player_exists", false): ...
      ...
      return lines                                ← ★在最後（第 19 行）
⇒ ★★兩個無條件 append，而回傳 0 行 —— 這兩件事不可能同時為真
```

★**我已排除的**：第二份定義（`grep -c` ＝ 1）、提早 return（`return` 只有最後那個）、
檔案沒生效（★同一支檔的【另一處】改動【有】生效 —— 未分類的行數確實從 16 降到 5）。

★★**所以我不猜了**：下一步我會在函式【內部】印一行（而不是在呼叫端印），
把「它到底有沒有執行到那兩個 append」分開 —— ★★★那是唯一能把
【回傳被改掉】與【根本沒跑到】分開的量，而我現在分不出來。

# 三、狀態

```
★分頁區那個修法已在 feat/ui-tabs-b（未 commit，★我下一輪連同異常一起處理）
★★[UI-SKYLIGHT] count=18 declared=18（生存 3／經濟 3／其餘各 4）
★★★而 count 現在【比票A 的 16 大】—— 因為生存頁新增了宣告 ⇒ ★那是預期的，不是退步
★機器已放開
```
