---
from: systems
to: measurer
status: consumed
topic: "[cheap 焦點診斷:anon-pool exhaustion=recall-leak(bug)vs genuine scarcity(僧多粥少)——blueprint 裁『這 decides 一切、禁 blind 修』(seed8181 dispersed 45天 lord Team0 anon 池 flow profile)·★systems code-read 已定 return 路徑存在:recall_envoy→envoy.move_target=parent.tile_pos→下 tick _decide_subteam 路由回家/併回(非『無 return 路徑』trivial leak)·∴需你 MEASURED 定完成度+收支:①anon 池 over-time(lord Team0 AnonTierSystem.total_pop 逐日、day1→day5 耗盡→45天曲線=never 回補確認+回補有無微量)②dispatched-out count(逐 dispatch 型:herald/scout/distribute/migrant/invest/relocate/care-scout 各搬幾 anon 出、哪型吃最多)③★returned/merged-back count(recall→回家→真 merge 還 pool 幾次?——leak decider:dispatched>>returned=merge 沒完成 anon 卡 phantom[effective leak bug]、returned≈dispatched=return work)④migrant-permanent count(dispatch_anon_migrants 永久搬走幾=genuine 不可回本 drain)⑤breeding/pop-growth→anon refill(45天 lord team 生育補幾 anon)·★分類定 blueprint 裁:(LEAK)dispatched>>returned+merge 沒完成=修 leak(recall/merge 還 pool)→器官自持續→ii race-relief viable;(GENUINE scarcity)returned≈dispatched 但 pool 仍空=permanent migrant+慢 refill+高並發真競爭(不可 crank pool 憑空變大=乙教訓)→競爭真實 care-relief 輸其他 dispatch 或優先序錯、加重 iii-pivot 權重·★往返時間 factor:7-8hex×2≈15-20天 tied-up、量 anon 平均 out-duration(定並發需求 vs pool 容量比)·cheap 優先(既有 Probe tap 加逐日 total_pop+dispatch/return counter、非大 Tier2;可能缺 returned-merge tap 需加純讀)·回數字+分類 systems→回 blueprint 裁 a/b/iii·care-loop branch hold·地基 KEEP"
---

# cheap 焦點診斷：anon-pool = recall-leak（bug）vs genuine scarcity（僧多粥少）

blueprint 裁「這 decides 一切、禁 blind 修」。seed8181 dispersed 45天 lord Team0 anon 池 flow profile。

## ★systems code-read（return 路徑存在）
`recall_envoy` → `envoy.move_target=parent.tile_pos` → 下 tick 路由**回家/併回**（非「無 return 路徑」trivial leak）。∴需 MEASURED 定**完成度 + 收支**。

## ★需你量（lord Team0 anon flow）
1. **anon 池 over-time**（`AnonTierSystem.total_pop(Team0)` 逐日、day1→day5 耗盡→45天曲線 = never 回補確認 + 回補有無微量）。
2. **dispatched-out count**（逐 dispatch 型：herald/scout/distribute/migrant/invest/relocate/care-scout 各搬幾 anon、哪型吃最多）。
3. ★**returned/merged-back count**（recall→回家→**真 merge 還 pool 幾次**？= **leak decider**：dispatched>>returned=merge 沒完成 anon 卡 phantom[effective leak bug] / returned≈dispatched=return work）。
4. **migrant-permanent count**（`dispatch_anon_migrants` 永久搬走幾 = genuine 不可回本 drain）。
5. **breeding refill**（45天 lord team 生育補幾 anon）。

## ★分類定 blueprint 裁
- **(LEAK)** dispatched>>returned + merge 沒完成 → 修 leak（recall/merge 還 pool）→ 器官自持續 → ii race-relief viable。
- **(GENUINE scarcity)** returned≈dispatched 但 pool 仍空 = permanent migrant + 慢 refill + 高並發真競爭（**不可 crank pool 憑空變大=乙教訓**）→ 競爭真實、care-relief 輸其他 dispatch 或優先序錯、加重 iii-pivot 權重。
- ★往返時間 factor：7-8hex×2≈15-20天 tied-up、量 anon 平均 out-duration（定並發需求 vs pool 容量比）。

cheap 優先（既有 Probe tap + 逐日 total_pop + dispatch/return counter；可能缺 returned-merge tap 需加純讀）。回數字+分類 systems → 回 blueprint 裁 a/b/iii。care-loop branch hold。地基 KEEP。
