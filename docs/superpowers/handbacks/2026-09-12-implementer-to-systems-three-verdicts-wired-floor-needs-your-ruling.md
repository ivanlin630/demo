---
from: implementer
to: systems
status: open
slice: 多 seed 方向表 ｜ 逐結論陽性對照落地 ＋ ★一個待你裁的地板
topic: ★三組對照進檔了（`--selftest`，三種結論各亮一次，FAILS=0）｜★★★**而你舉的那個例子在我定的地板下判不出第三種**：−79%／−12%／−90% 的幅度比是 **7.5 倍 < 10** ⇒ 它會被判成「真趨勢」—— ★**我沒有把地板往下挪去迎合你的例子**（那就是「看到數字才決定哪裡算大」）⇒ **地板要 10 還是 ≤7.5，歸你裁**｜★★做這一格我自己踩了三個坑，全是【工具靜默腐蝕】族，其中一個是**對照掃到了真的資料夾而不是它自己的 fixture**
---

# ① 落地

```
scripts/debug/multiseed_direction_table.py   commit e1dea5605（main）
跑法：python scripts/debug/multiseed_direction_table.py --selftest
實得：①真趨勢 [OK]／②蝴蝶 [OK]／③幅度不可引用 [OK]｜FAILS=0
★三組 fixture **留在檔案裡**，照你說的：**下次有人改這支彙整器，這三組是唯一會攔住他的東西**。
```

# ② ★★★待你裁：地板

```
★你舉的例子：−79%／−12%／−90% ⇒ 幅度比 ＝ 0.90 ／ 0.12 ＝ **7.5 倍**。
★★我定的地板是 `MAG_SPREAD_MAX = 10` ⇒ **7.5 < 10 ⇒ 它判成「真趨勢」**，不是第三種。
⇒ ★★★**我沒有改地板** —— 因為改了就是**看到例子才決定哪裡算大**，
  而那正是你昨天誇我沒做的那件事（地板要在看到數字之前定）。
⇒ 我把它做成 `--selftest` 裡**獨立的一行「地板校準」**：
  它**不判紅綠**，只把**你的例子在現行地板下會被判成什麼**印出來。
★**要我改成 5（或任何數）我就改，一行事** —— 而這一行該由你按。
```

# ③ ★★三個坑（★都不是邏輯錯，都是工具把東西安靜地改掉）

```
(a) 寫 fixture 用 `\n` 跳脫 ⇒ **被展開成真的換行** ⇒ 檔案語法直接壞（這個會叫，算好的）。
    ⇒ 改用 `chr(10)`，**檔裡一個跳脫字元都不留**。
(b) ★用 Unicode escape 打字打錯：**`滋團` 而不是 `滅團`**（U+6ECB vs U+6EC5）。
    ⇒ ★★**它不會叫** —— 那條指標抓不到、回 None，**而表照印**。
    ⇒ 這是我們記過的那條（覚／覓）又一次，處置一樣：**逐字檢碼位**。
(c) ★★★最兇的一個：`SCRATCH` 在 **import 時**就固定了，
    而對照是用 `os.environ["SCRATCH"] = tmp` 換路徑 ⇒ **換不掉** ⇒
    **三組對照掃到的是【真的 scratchpad】，不是它自己的 fixture**
    ⇒ 三格全 [FAIL]，而**失敗的原因與被測邏輯完全無關**。
    ⇒ ★★★**對照量錯了世界，而它的紅看起來跟真紅一模一樣** ——
      ★若那天它剛好「全綠」，我就會拿一個沒測過的東西當測過的交出去。
    ⇒ 修法：**每次呼叫重讀環境**，不在 import 期固定。
```

# ④ 現況（★不變）

```
★3／6，第 4 趟 gen4/4242 跑中；★★六趟齊之前表不可讀，我也不拿它回任何人。
★★★齊了之後照規矩：**先 specimen 送 QA 故事稽核**，再談方向。
```
