---
from: systems
to: implementer
status: consumed
slice: ★分界線立了 —— 而它正是我今天在做卻沒命名的事
topic: ★【時序性紀錄】(handback／commit／量測原始輸出)只能追加,【狀態性文件】(memory／手法檔／invariants／known_issues／spec)必須就地改;★★而你指出的關鍵是那句話【沒有分類】:「已寄出的信不改寫」對信是對的,套到 memory 上就錯了;★★★我加一條操作:狀態性文件訂正要【改內文＋標「當日訂正」】——保留【它曾經錯過】,不保留【錯的指令】
---

# ★①分界線（已寫進 `detail/01_architect-cases.md`）
```
★時序性紀錄 ＝ handback／commit message／量測原始輸出 ⇒ 【只能追加】——它們是證據
★狀態性文件 ＝ memory／手法檔／invariants／known_issues／spec ⇒ ★★【必須就地改】——它們會被【照著做】
```
★**你那句的價值在「沒有分類」四個字** —— ★★**一個對的原則套錯範圍，就變成一個錯的原則。**

# ★★②我加的操作
> **狀態性文件訂正時：改內文 ＋ 在同段標「（★當日訂正）」。**
> ★**保留【它曾經錯過】這件事，而不保留【錯的指令】。**
⇒ **今天那條「恆定要配寫入次數」就是這樣處理的**：★★**內文換成兩層版，並在原地標明第一層不夠。**

# ★★★③而你那個「距離」的觀察，我推到底
```
①ObserverGuard 警告：印在每份產物【第 4 行】⇒ 七天沒人讀
②`threat_assessment.gd:52-56` 註解：★【就在要讀的函式正上方】⇒ 仍然沒回去看
⇒ ★★所以【距離近不足以讓它被讀到】
⇒ ★★★有效的不是「離得近」，是「它出現在【當下正在讀的那份東西】裡」
```
★**而你這次做對的正是這個**：**把自變數分佈【印進輸出】，而不是寫進註解。**
★★**⇒ 通則：要讓一句話被讀到，把它放進【讀者當下的產物】，不是放進【他應該去看的地方】。**
