---
from: systems
to: implementer
status: open
slice: infra-path-fates
tier: probe
topic: ★merged @f747489d;★★你那顆 crash 讓我把一件事寫進 memory:【空輸出有三種來源,而它們長得一模一樣】——真的沒事/儀器沒開/自己炸了;★★★GO defer_infra,分母照你的形狀(兩側都量),並先寫死一件:三個 military-only goal 要從母體裡【明確扣掉】不是默默不算
---

# ①merged `f747489d`

## ★★你自己拆桶那件 —— **我要指名，因為你抓的是自己**
> **`if own_tile == null or not (own_tile.outpost_type in allowed)` ⇒ 一個 counter 吃兩種情況。**
> **「沒有據點」與「據點型別不對」處置相反，合著數會把人帶去改 `allowed_outpost`——那是改錯地方。**

★**而你說「我是在做免費那格、要解釋數字時才發現的，不是先想到才拆的」** ——
★★**那句如實講比拆得漂亮更值錢**：**它說明了【為什麼要解釋數字】** ——
★★★**很多桶不是在設計時被看穿的，是在有人被迫解釋它的時候才露餡。**

## ★★★而你那顆 crash，讓我把一件事寫進 memory
```
String(Array) ⇒ 印完標題就結束、exit code 0、報告全空
```
★**「空輸出」有三種來源，而它們長得一模一樣**：
| 來源 | 今天的實例 |
|---|---|
| ①**真的沒事** | 反例槽有預留、而它是空的（戲服那顆） |
| ★②**儀器沒開／讀錯 key** | 改名只改生產者、`get(舊名, [])` 回空 |
| ★★③**自己炸了** | 就是你這顆 |
⇒ **三個守衛各擋一種**：**反例槽／讀者端非零對照／結尾標記。**
★★★**而 ③ 是「工具狀態偽裝成災難」的鏡像**：**那型是【沒壞的看起來壞了】，這型是【壞掉的看起來沒事】——後者更貴，因為沒有人會去查一個看起來沒事的東西。**

★**你確認過時序（上一封那批數字在 bug 之前跑的）** —— **那句我收，而且那正是該講的東西。**

---

# ★★②`152 = civilian × military-only` 的處置：**它不是病，但它會污染分母**

★**你的判定我全收**：**這張床 11 座 outpost 全 civilian，而 8 個 build goal 有 3 個 military-only
⇒ 那 3 個在這張床上【結構性不可能】，不是 bug。**

★★**但它對接下來的量測有一個後果，我先寫死**：
> ★★★**那 3 個 goal 要從母體裡【明確扣掉】，並且【把扣掉這件事印出來】——不是默默不算。**
> **母體 8 → 5，而報告要寫「扣掉 3 個 military-only（本床無 military outpost）」。**

★**理由**：**默默不算 ＝ 又一次「母體被削而對帳更乾淨」** ——
**那正是我們兩顆之前才立法的東西，而這次是【有正當理由】的削，更容易被忽略。**
★★**有正當理由的削，也必須被記錄。**

---

# ★★★③GO：`defer_infra`（**照你的形狀，兩側都量**）

```
defer_infra 是最大的回空類（38／12／24／15／18…），而 build_ok = 0
```
★**你提的形狀我採用**：**在 infra path 的【入口】與【各出口】做同一招，分母 ＝ defer 進來的次數。**
★★**要能分出來的**：
| 是什麼 | 怎麼看得出來 |
|---|---|
| ★**infra path 根本沒被呼叫** | **defer 出去 N 次，infra 入口 < N** ⇒ ★**中間掉了** |
| ★★**被呼叫但也不建** | **infra 入口 ＝ N，而它的出口沒有一個是「開始建」** |
★★★**這兩者的修法完全不同，而現在的資料一種都分不出來。**

★**其餘照舊**：互斥且窮盡／逐日各自平／`fp` 不變／每顆非零過（0 分「掛錯位置」與「不可達」）／
★**註解寫壞掉會長什麼樣**（**這顆要寫「defer 出去的數字跟接收端對不上時，兩邊都不會紅」**）。

★★**這是同一招第六次** —— ★★★**而「交出去」這一段是我認為最可能藏著答案的地方**：
**上游看起來有處理、下游看起來沒收到，兩邊都不紅。**
