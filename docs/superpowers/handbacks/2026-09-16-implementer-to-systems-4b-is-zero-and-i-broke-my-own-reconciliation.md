---
from: implementer
to: systems
status: consumed
slice: (4b)/(4c) 三分 ｜ ★★★**(4b) 嚴格更低 ＝ 0** ＋ **無名那 2 筆有名字了：`prosperity`**
topic: ★★★**三分之後：(4a) 450｜(4c) 同層 18｜(4b) 嚴格更低 【0】**（2 天窗、seed 1337）⇒ **這個窗裡【一次手不聽腦都沒有】**｜★**那 18 筆全是 `80_vs_80`**：`乞食/併入/掠奪/遷移找糧` 各 4，擋它的 `掠奪`12／`逃跑`4｜★★**無名的 2 筆現在報得出發起站點 ＝ `prosperity`**（征服攻擊那條 `try_set` 沒傳 `opt`）⇒ **盲點先有名字，再決定要不要動 59 個 caller**｜★★**而我順手修了一支【我自己造的過期閘】**：§H 的對帳寫的是 `(4a)+(4b) == 母體`，**(4c) 一拆出來它立刻紅**
---

# ① 三分（2 天窗、seed 1337、樹 `d42ff6b58`）

```
(4a) 更高        450      ← 規則
(4c) 同層         18      ← ★規則（A1a 白名單：兩側 task_reason 都要在 ENGINE_SOURCES）
(4b) 嚴格更低      **0**    ← ★★★病的定義，而它是 0
```
⇒ ★**你那個「否則」桶一拆開，手不聽腦就不見了** ——
★★**因為它從來沒在那裡，是判準把【＝】算進了【＜】。**

**(4c) 逐筆**（全部 `80_vs_80`）：
```
乞食|掠奪=3  乞食|逃跑=1   併入|掠奪=3  併入|逃跑=1
掠奪|掠奪=3  掠奪|逃跑=1   遷移找糧|掠奪=3  遷移找糧|逃跑=1
```

# ② 無名那 2 筆：**`prosperity`**

```
[4c] 指不出 option 的（依發起站點）：**prosperity=2**
     ⇒ 具名 16 ＋ 無名 2 ＝ 母體 18 ✅
```
★**那是征服攻擊的 `try_set(..., "prosperity")`** —— **它沒傳 `opt`。**
★★**照你的裁，我先讓盲點有名字**（`<cls>.unnamed.by.<source>`），**沒有去動 59 個 caller**
⇒ ★★★**而現在它已經不是盲點了**：**它有母體、有名字、有對帳** ——
**要不要再往下補 `opt`，可以等到它真的擋住某個結論的時候再做。**

# ③ ★★而我順手修了一支**我自己造的過期閘**

```
§H 原本：`_ok((4a)+(4b) == 母體)`
(4c) 一拆出來 ⇒ 450 + 0 ≠ 468 ⇒ **它當場紅**
```
★**而它紅的原因不是世界，是【我們自己改了定義】。**
⇒ ★★**判準沒跟著分類一起改，就會變成一支「因為我們改了定義」而紅的閘** ——
★★★**而那種紅最危險：它看起來像發現了什麼。**
⇒ 已改成 `(4a)+(4b)+(4c) == 母體`，並在旁邊寫下上面那句。

# ④ 下一步：**util，不是 10 天窗**（★你已經裁了，我確認接法）

> 同層 ⇒ priority 答不出「該不該換」，**答得出的是 util**。

★**而那 18 筆的形狀已經給了一個具體的問法**：
```
`掠奪`(現任) 擋掉了 `乞食／併入／掠奪／遷移找糧`
⇒ ★問：**那一刻，新的那個 option 的 util 是不是高於現任的 util？**
   ・是 ⇒ **同層白名單擋住了一次【更該做的事】** ⇒ 那才是病
   ・否 ⇒ **擋得對**（現任本來就更該做）
⇒ ★★而 util 在 rank 當下算過 ⇒ **deny 那一刻拿得到「新的那個」，拿不到「現任的」**
   —— ★★★**現任的 util 是上一次 cadence 算的，而它沒有被留下來。**
```
⇒ ★**所以這一步不是「加一個 tap」那麼簡單**：**要嘛把現任的 util 存進 team（新欄位），
要嘛在 deny 那一刻重算一次（★而重算會呼 `gather` ⇒ 它會寫 state ⇒ 觀測改變被觀測物）。**
⇒ ★★**兩條路都有代價，而我不自己選** —— **spec 要怎麼寫，你裁。**

# ⑤ 現在

- 樹 `d42ff6b58`（branch `feat/4b-census`，worktree `.worktrees/fourb`），**短窗 0 紅**。
- ★**沒有開 10 天窗**（measurer 的 busy beacon 還在）。
- ⏳ 等你裁 ④ 那兩條路。
