---
from: systems
to: reviewer
status: open
slice: 恩怨帳 切片A ｜ **R² 設計審**
topic: ★spec：`docs/superpowers/specs/2026-09-16-grudge-ledger-sliceA-HOW.md`（commit `4ee63e887`）｜★★**前提複驗改了四件事，而其中兩件是同一個病**：真事件寫的是**另一個名字**（`special_taxed`／`rejected_aid` 都不在任何 match 裡）⇒ **「接線 `extorted`」會接到一個全庫零呼叫點的名字上＝看起來接好了的接法**｜★**我最想被打的是 §1.4**：我把「拒賣」定義成**索價高到撮合必失敗的湧現**而不是 gate，而**驗收格3 是它的判決不是它的裝飾** —— 若你認為這個定義撐不住，現在說比 implementer 做完再說便宜
---

# ★請特別看這四處

```
\u2460**§1.4 拒賣＝湧現** ⇒ 我拒絕加 gate（去補丁閘），代價是**它可能根本不發生**。
   ★我已經把「拿不出樣本就回報我、不要加 gate 補上」寫進 spec ——
   ★★**但那是【事後的誠實】，不是【事前的正確】**。你認為這個定義本身有問題就現在說。
\u2461**§1.1 飽和疊加放共用 `add_edge`** ⇒ `gratitude` 與 `protect` **一起改**。
   ★我判斷那是 WHAT 本來就涵蓋的（「同類疊」＝全 type），**而我把 protect 那格寫進驗收**（格6）。
   ★★若你認為 protect（主從關係）不該飽和疊加，那是 WHAT 的洞，我轉 blueprint。
\u2462**§0 訂正② 我推翻了 WHAT 的「新 type `neglected`」** ⇒ 用既有的 `rejected_aid`。
   ★理由：**新的是那條邊，不是那個名字**；名字已被三個地方寫了，再造一個＝兩個名字指同一件事。
   ★★**這是我改了 WHAT 的字面** ⇒ 若你判它越界，我轉 blueprint 裁。
\u2463**§0 訂正④ `_views_as_foe` 用 `strongest`** ⇒ 我**只**要求新讀者不沿用，**沒有順手修它**。
   ★我判斷那是切片B（不順手改別人的讀者），**而它現在是個真缺陷躺在那裡** —— 你認為該現在修就說。
```

# ★我自己知道的弱點（先招）
- `0.325` 是「表內相鄰兩值中點」—— ★**那確實是一個推法，但它推的是【序】不是【量】**。
  若你有更好的非手填推法，給我。
- `W_feud`/`W_grat`/`W_price` 我**只給形狀不給係數**（§4）⇒ ★**我把調參面推給了 implementer**，
  而 WHAT §2 說「唯一風險＝六選項人格權重成調參面」。★★這是我明知而為，請判它可不可接受。
