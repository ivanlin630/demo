---
from: implementer
to: systems
status: open
slice: churn 修前基準（新儀器量舊世界）完成 —— ★樹已還原並驗過
touches: 無 production 改動（退檔已還原，merge-gates 12/12 綠 242s）
topic: ★★★camp.built 三 seed【一致下降】:88→75(-14.8%)／73→68(-6.8%)／86→66(-23.3%);★camp.abandoned 降2持平1:63→45／44→44／55→37;★★l0_to_l1(真的升成據點)方向【不一致】3→7／6→5／6→7 且母體只有 3-7 ⇒ 不判;★★★walk_to_own_camp 修前恆 0(那條腿當時不存在)、修後 1/3/1 ⇒ 這是【新腿真的被走過幾次】的絕對數,很小,我不解讀多寡
---

# ★①做法（★與交件同時附上，讓它可複驗）
```
production 8 檔 `git checkout 2a22afb6^ -- …`｜★床保持 HEAD（儀器不變）
先 1 日 smoke（0 parse error）→ 三顆 30 日 → `git checkout HEAD -- …` 還原
★★還原後 merge-gates ★12/12 綠 242s、`git diff --stat HEAD -- scripts/` 空 ⇒ 樹乾淨
★★★誠實限：基準跑的 zhagen tap 是【兩支版】（第三支讀 own_camp_pos，不退就 parse 不過）
   —— 而 churn 桶不受影響，那正是本輪要的
```
路徑：`scratchpad/pre30_{1337,42,7}.txt`（修前）／`post30_{1337,42,7}.txt`（修後）

# ★★②churn 對照（★同儀器、同 seed、同 30 日）
```
             camp.built            camp.abandoned        outpost.l0_to_l1
seed1337     88 → 75  (−13, −14.8%)  63 → 45 (−18, −28.6%)   3 → 7
seed42       73 → 68  (−5,  −6.8%)   44 → 44 (  0,   0.0%)   6 → 5
seed7        86 → 66  (−20, −23.3%)  55 → 37 (−18, −32.7%)   6 → 7
─────────────────────────────────────────────────────────────────
合計        247 → 209 (−38, −15.4%) 162 → 126 (−36, −22.2%)  15 → 19
```
## ★照【逐 seed 方向一致】那個判準逐項讀
```
★`camp.built`（紮營發生次數）：★★三 seed【全部下降】⇒ 方向一致
★`camp.abandoned`（蓋了就丟）：降 2、持平 1（seed42 44→44 一模一樣）⇒ ★★不是三 seed 一致
★★`outpost.l0_to_l1`（真的長成據點）：+4 / −1 / +1 ⇒ ★★★方向不一致，而母體只有 3–7
   ⇒ ★這一格【不判】——母體個位數，正負一顆就翻面
```
⇒ ★★**只有 `camp.built` 那一項達到你定的「三 seed 方向一致」標準。**
⇒ ★★★**而我不把它讀成「churn 治好了」**：**「紮營變少」與「亂蓋變少」是兩個不同的量**，
   **後者（abandoned）沒有三 seed 一致。**

# ★★★③`walk_to_own_camp`：修前恆 0（★因為那條腿當時不存在）
```
修前 0 / 0 / 0   ⇒ ★這【不是】「沒發生」，是【那段 code 不存在】——0 三讀法的第三讀
修後 1 / 3 / 1   ⇒ ★★這是新腿【真的被走過幾次】的絕對數
```
⇒ ★**很小，而我不說它是多還是少** —— ★★**要判多寡得先有分母**（「有多少次隊承諾紮根且不在營地」），
   ★★★**而那個分母這輪沒量**（zhagen 那組的母體是「IDLE 且 committed==紮根」，不是這個）。
⇒ ★**若你要那個分母，它是一個 tap，可以併進下一批**（我不自己加）。

# ④`own_camp_lost_release`：修前 0（同上，機制不存在）→ 修後 7 / 6 / 7
```
★這是「營地沒了 ⇒ 解承諾」那條出口【真的被走過】的次數 ⇒ ★★它不是 0，代表那條路不是死碼
★★★而它跟腿C（控制床 30/30 解承諾）是同一個機制在 organic 世界的計數
```

# ⑤下一批（★照你批准的合跑）
```
①`three_tickets_bed` 加印 `zhagen.lost_table`（22 敗的 per-option util，★資料已在，只差印）
   ＋差距分桶 <0.1／0.1-0.5／0.5-1／1-2／≥2（★床裡算，不新建欄位）
②`_decide_unified` nir 分支加 `redispatch.nir_owncamp.<opt>.<yes|no>`（★問 `state.own_camp_tile != null`）
⇒ ★同三顆 seed 一次跑完，★★兩封分開交
```
