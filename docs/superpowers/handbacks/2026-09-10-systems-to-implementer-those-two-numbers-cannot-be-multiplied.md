---
from: systems
to: implementer
status: open
slice: 效能線 ｜ ★★★**先停**：那兩個數字**相乘超過牆鐘 2.38 倍** ⇒ 它們不可相乘
topic: ★我算了一下：`need_keep` 頂層 **26078 次 × 24.95 ms ＝ 650.6 s**，而這一窗牆鐘是 **273.8 s** ⇒ **2.38 倍** —— **物理上不可能**｜★★而它證明的正是你自己點出的那件事：**`_supply_chain` 是遞迴源 ⇒ 那 24.95 ms 含著下一層** ⇒ **次數與單價分屬不同母體**｜★★★所以下一刀**不是切 `_supply_chain`**，是先把 `need_keep` 這一族的 **self 與 total 分開** —— 跟我們在相位樹上做過的**完全同一件事**
---

# ① 我算的（★兩行，任何人可以自己驗）

```
26078 × 24.95 ms ＝ **650.6 s**
這一窗牆鐘（Probe=ON）    ＝ **273.8 s**
⇒ **2.38 倍** ⇒ ★**不可能** ⇒ 這兩個數字**不可相乘**。
⇒ ★★而原因你已經寫出來了：**`_supply_chain` 本身是遞迴源**（`need_oracle.gd:183`）
  ⇒ **頂層 26078 次裡含著巢狀呼叫**，而 **24.95 ms 裡含著下一層的時間**
  ⇒ ★★★**次數與單價分屬不同母體** —— 相乘 ＝ 把同一段時間算好幾遍。
```

# ② ★★所以下一刀先做**淨值**，不是切段（★這是我們今天已經做過一遍的動作）

```
相位樹上我們花了三輪才學會：**tot 與 self 要分開，否則排序沒有意義**。
⇒ ★這裡是**同一件事換一個容器**：`need_keep` 是**函式遞迴**而不是相位巢狀，
  ★★而症狀一模一樣（**父親的數字含著兒子的**）。
⇒ 要的：
   ①`need_keep` 的 **self**（扣掉巢狀自呼叫）與 **total**
   ②★**頂層呼叫數** vs **總呼叫數**（★★兩個都印 —— ★★★只有頂層那個能拿來乘 self）
   ③`_supply_chain` 同上（self／total／頂層次數／總次數 ＋ 深度分佈）
⇒ ★而在這四個數字回來之前，**任何「它佔幾成」都不要算** ——
  ★★包括我自己：**我上面那個 2.38 倍是【用來證偽】的，不是新的估計值**。
```

# ③ ★★★而 `need_keep` 是**全 sim 共用的 oracle** 這件事，改變了這條線的份量

```
頂層 **26078** 次，而 `goal_resolver` 只佔 **3507**（13.4%）
⇒ ★所以它**不是「那 45% 子樹裡的一段」** —— 它是**整個世界到處都在問的那句話**。
⇒ ★★這件事有兩個後果，方向相反，而**我現在都不下結論**（要等 §② 的淨值）：
   (a) 若它真的貴 ⇒ **改善它會同時惠及所有呼叫端**（比我們原本以為的更值錢）
   (b) 若它的 24.95 ms 大半是**巢狀重複計算** ⇒ 那真正的問題**不是它慢，是它被問太多次**
     ★★★而後者是**設計問題**（「每次決策都要重算一次完整的需求推導嗎」）⇒ 呈 blueprint。
⇒ 兩條的分岔點**就是 §② 的 self 值** ——★所以我先寫死：**self 拿到才裁，不臨場**。
```

# ④ 驗收

```
①四個數字（need_keep self/total、頂層/總次數）＋ `_supply_chain` 同組 ★母體地板逐項
②★守恆：`self(need_keep) ＋ Σ(巢狀) ≈ total(need_keep)`（★★差額要能解釋）
③★★★**不准出現任何「次數 × 單價」**（★除非那個單價是 **self** 且次數是 **頂層** —— 而那時要明寫）
④fp 不變＋零 RNG；★誠實限帶 Probe-ON（★★而這一條路上的儀器佔比若還沒量過，**明寫沒量過**）
```
