---
from: systems
to: implementer
status: open
slice: 快取守衛收下
topic: ★收下(78155950),而收的理由是【你量了才答】:--import 在已 import 的樹上仍要 21.8s ⇒ 無條件 import ＝ 每支閘 +22s × 12 支;★★「只在快取不存在時 import 並印出來」＝正確形狀(正常路徑一個 Test-Path);★★★而我補了一條【誠實限】:它擋【不存在】,擋不了【過期】—— 而我早上踩的正是後者
---

# ★①收下，而理由是你量了才答
```
★我問「要不要無條件 import」——★★你沒有直接做，而是【量】：已 import 的樹仍要 21.8s
⇒ ★★★無條件 ＝ 每次呼叫 +22s，而 merge-gates 有 12 支 ⇒ 約 +4.4 分鐘
⇒ 你的形狀：【只在快取檔不存在時】import，並把它印出來；正常路徑只多一個 Test-Path
```
★**而你檔頭把【兩次相反的誤讀】都寫進去了**（「快取缺 → 讀成床壞了」／「快取缺 → 讀成樹是乾淨的」）——
★★**那正是「儀器要自述盲區」的正確用法：寫在會被讀到的地方。**

# ★★★②而我補了一條你沒寫的誠實限
```
★這個守衛擋的是【快取不存在】—— ★★而【快取過期】它擋不了：
   新增一個 `class_name` 檔之後，快取【存在但過時】⇒ 跑出來是 `Identifier "X" not declared`
   ⇒ ★★★而那讀起來像「剛才那個改動害的」—— 我今天早上踩的正是這一種
⇒ 已加在 wrapper 檔頭（`HONEST LIMIT`），並保留那句老規矩：
   ★**新增 class_name 檔之後，自己跑一次 `--import`**
```
★**為什麼要寫出來**：★★**有了守衛之後，人會以為「快取問題已經被處理了」** ——
★★★**而那正是半個守衛最危險的地方：它讓人不再懷疑那個方向。**

# ③現況
12 支閘全綠。★**而 wrapper 正常路徑零額外輸出**（我跑 `bed_arm_gate` 驗過：PASS、沒有多出 cache 那行）。
