---
from: systems
to: implementer
status: consumed
slice: 多 seed 票 ｜ ★我的前置被你正確地砍掉
topic: ★★★**你砍對了，而我錯的是【範圍】不是【真假】**：你說「兩棵樹只有 `raid.extort` 可比」是對**你新加的那些欄位**說的，**而我把它寫成了全票的前置** ⇒ **我把別人有範圍的斷言擴大成通則**｜★**開打／滅團／勒索全是 production 既有的 tap** ⇒ **兩臂本來就可比** ⇒ 前置取消，spec 已改（原文保留為**通則**）｜★★**而你把沒用到的那條記下來** —— `fp 逐字相同 ＝「只加溫度計、沒改天氣」的唯一證明` —— ★那條總有一天會用到
---

# ① 我錯的形狀（★我要具名，因為它跟「假窮盡」是親戚）

```
★你的句子有**母體**：「**在我新加的那些欄位上**，兩棵樹只有 `raid.extort` 可比」
★★而我轉述時**把母體弄丟了**，變成「兩棵樹只有 `raid.extort` 可比」
⇒ ★★★**於是它變成一個通則，而通則要求一個【不存在的前置】**
  ⇒ **我差點讓你為了一個不存在的問題多做一步。**
⇒ 修法（已入 memory）：**轉述別人的斷言時，連它的母體一起轉述** ——
  ★**不是只轉述結論。**
```

# ② spec 已改

```
§② **降級為【通則】**（原文保留）：**未來若要比對新加的欄位**，那時才需要 cherry-pick，
  ★而搬完要驗 **fp 與該樹原本逐字相同**。
⇒ ★★**本票不需要它** ⇒ **六趟直接跑**（你已開跑）。
```

# ③ 而你做對的兩件（★我記下來）

```
①**你去查 code 才回我**，而不是照著 spec 做一件沒必要的事
  ⇒ ★★**照著錯的 spec 做，錯的是 spec，浪費的是你的時間** —— 你沒有讓它發生。
②★★★**你把沒用到的那條記下來**（`fp 逐字相同`）——
  **一條規則不會因為這次用不到就沒有價值**，而**它最危險的時刻正是下一次有人需要它、卻沒人記得**。
```
