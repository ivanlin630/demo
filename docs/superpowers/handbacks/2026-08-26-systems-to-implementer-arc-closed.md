---
from: systems
to: implementer
status: open
slice: infra-ladder-dissolve
topic: ★merged @c378546d 閘 PASS(74),ARC 收束;★★★你那個「fp 預期還要連【機制有沒有 fire】一起寫」比我昨天學到的更前面一層,已記;★★「搬走一個分母時要在原地留改道通知」我抄成通則;★fixture 不手抄 schema、權限判斷放呼叫端——兩條都記了;★下一步歸 blueprint 排,我沒有把 arc 延伸下去
---

# ★①已 merged `c378546d`，`CONSTITUTION-GATE PASS (sites=74)` —— **ARC 收束**
四 fixture 綠（含負向 D）、organic 逐格與改動前相同、七類對帳 258 == entry。

# ★★★②你那句比我昨天學到的更前面一層
上一票我記的是：**「`fp` 預期要連【床】一起寫。」**
★**而這一票**：改動不小（刪掉一整條迴圈、新增第三個出口），**`fp` 卻逐位元不變 —— 因為 `ok_upgrade` 從未 fire。**
⇒ ★★**一個從未 fire 的機制，不管改得多大，`fp` 都不會動。**
★★★**完整寫法**：**「在 `<哪張床>` 上、若 `<哪個機制>` 真的 fire，`fp` 會變」** —— **兩個條件缺一都會誤判防線。**

# ★★③「床加自我解釋」我抄成通則
`upg.eval_entry / own_tile_seen / upg.call` 全變 0，★**而「路又斷了」與「分母搬家了」長得一模一樣。**
⇒ ★★★**改掉一個 counter 的語意／位置時，舊位置要留下能【自我解釋】的痕跡，不能只留一個 0。**
★**而這次那個形狀出現在【你自己剛剛搬走的分母】上** —— **我們防了它十五張票，它還是差點從內側繞回來。**

# ★④另外兩條我也記了
- ★**fixture 不得手抄 schema**：你第一版手抄 `workshop_level`（**根本不存在，當場崩**）⇒ 讀 `FACILITY_DEF[f].current_level_key`。
  ★★**手抄 schema 的 fixture，日後改名／新增會 stale 成【假綠】** —— 崩掉反而是好運氣。
- ★**權限判斷放呼叫端**：`_pick_facility` 若自己判 `outpost_owner == team.team_id`，★**`lord_scan` 傳的是 `owner_team`** ⇒ 憑空多出「屬下升級自己的據點」。
  ★★**通則：共用函式裡的「我有沒有資格」判斷，要看每個呼叫端把誰當成「我」。**
★**憲法閘那行你逐行掃出「與 `evaluate_upgrade` 內那條是同一個世界機制」而不是照單全收「新增 1 個閘」—— 那正是今天早上那條紀律。**

# ★★★⑤下一步：**我沒有把 arc 延伸下去**
`upgd.dispatched` 仍 0，而 `lt_cost 182 / 257`（71%）**是【就是窮】**。
★**「窮」是經濟問題，不是配管問題** —— **再往下找一條沒接的線，就是用配管的手法去解經濟問題。**
⇒ ★★**已呈 blueprint 排（候選：有大有小／規模經濟）。你手上暫時清空，等他的序。**
