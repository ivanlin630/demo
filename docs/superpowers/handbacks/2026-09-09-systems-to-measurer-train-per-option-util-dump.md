---
from: systems
to: measurer
status: open
slice: TASK_TRAIN 為什麼贏不了
topic: ★★★第一件事【不是】量 util,是把「不被選」拆成兩個原因:(a) 根本沒被 offer（applicable=false）(b) 被 offer 了但輸掉——★兩者的修法完全不同,而合在一起看都是「0 隊被選中」｜★★blueprint 已預註冊 genuine 結局:若 TRAIN 的 util 是【真值算出來的低】⇒ 結案入帳不修（戰亂世界沒空練兵＝誠實特性）;只有 util 項【機械壞】(恆 0／沒接線／被閘擋) 才是票
---

# 背景

exp 卷的真根：`TASK_TRAIN` **六次快照有五次 0 隊被選中** ⇒ **不是執行端擋住，是決策端幾乎不選它**。
blueprint 裁：照「**決策問題先 dump per-option util**」老法辦，**禁靜態開藥**。

# ① ★★★第一件事：把「不被選」拆成兩個原因（★這格先於 util）

```
(a) TRAIN 根本【沒有被 offer】       ⇒ options 的 applicable 閘擋掉 ⇒ ★問題在【閘】
(b) TRAIN 被 offer 了但【util 輸掉】 ⇒ ★問題在【秤】
★★兩者的修法【完全不同】,而它們在「0 隊被選中」這個數字上【長得一模一樣】。
⇒ ★★★所以卷面第一格要報：
     每次決策輪,TRAIN 出現在候選陣列的隊數 / 有做決策的隊數
   ★而若 (a) 佔絕大多數 ⇒ ★★後面的 util dump【母體會是空的】,不要硬跑
     —— 直接回報「母體不足」,那本身就是答案（今天你已經這樣做過一次,做得對）。
```

# ② 若 (b) 成立才做的：完整選項面 dump

```
母體：★【TRAIN 出現在候選陣列】的隊（不是全部隊）
每一筆要有：
  ①TRAIN 的 util ＋★【它的組成逐項】（哪一項貢獻多少）
  ②★★當輪【贏家】的 option 與它的 util ＋ 組成逐項
  ③差距（贏家 util − TRAIN util）
★★★而【組成逐項】是這張票的核心：
   總 util 低有兩種——【每一項都合理地小】(genuine) vs 【某一項是 0 或沒接線】(機械壞)
   ⇒ 只看總分【分不出】,而那正是 blueprint 預註冊的兩個結局的分界。
```

# ③ blueprint 預註冊的結局（★照抄進卷面，讓判讀不必等他）

```
genuine：TRAIN 的 util 是【真值算出來的低】（每一項都合理地小、只是打不過生存/威脅）
   ⇒ ★結案入帳【不修】——「戰亂世界沒空練兵」＝誠實特性,與用戶終審同向。
機械壞：某個 util 項【恆 0 / 沒接線 / 被閘擋】
   ⇒ ★★那才是票。
★★★而你只要把【組成逐項】給出來,這個分界【自己會現形】—— 不用你下判斷。
```

# ④ 誠實限

```
①★本卷不下「該不該讓 TRAIN 更容易贏」的結論 —— 那是 WHAT,而且 blueprint 已預註冊了
  「util 低若是真的就不修」⇒ ★★禁把 util 往上 crank（那是把引擎腳本化）。
②母體若不足（TRAIN 幾乎不出現在候選裡）⇒ ★直接說,不要用少數幾筆硬撐出一個分布。
③窗長：★要涵蓋【至少一次 TRAIN 真的出現在候選】的情形 ⇒ 若短窗撈不到,先報我,
  ★★不要自己一路加長到撈到為止（那會變成【挑一個會出現的窗】＝取樣偏差）。
```

完後改本信 `status: consumed`，回信附 exact path。
