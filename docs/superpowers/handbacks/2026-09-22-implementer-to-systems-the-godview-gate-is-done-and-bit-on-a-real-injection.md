---
from: implementer
to: systems
status: open
slice: ★**票 §6／§9.1 的閘做完了**（★★不依賴 B4a/b/c 的量法，所以我沒等）
topic: ★★★**真檔注射驗過**：在威脅判定後插一行 `var _inj = _t.tile_pos`（＝用既有 gate-ok 的 handle 讀真值）⇒ **FAIL、指名到行 309、rc=1**，已還原並 `grep -c` 確認｜★**錨在【函式＋區塊】不在行號**：找不到錨 ⇒ 判 **不可判（exit 2）**，★★**不是綠**｜★**已登註冊表，而是登在【分支上】** —— main 現在沒有這支閘，登過去會讓所有人紅｜★★★**誠實限印在卷面**：只掃 `record_claim`，威脅判定若搬家 ⇒ 錨失效而畫面是綠的
---

# 一、★閘的形狀（★★照票，不是我發明）

```
**放行**：`var _tgt: TeamData = state.teams.get(tgt_id)`（取 handle）
        `ThreatAssessment.score(state, _ob, _tgt)`（★整個交出去）
        `_ob.xxx`（★觀察者【自己】的東西）
        `if Probe.hot_detail:` 區塊裡的 `_o`／`_t`（★既有 gate-ok，**必須放過**）
**抓到**：`record_claim` 內、gate-ok 區塊外的 `_tgt.` ／ `_t.` ／ `_o.`
★而你票 §9 那句「後門連 file:line 都現成擺在那裡」正是這支閘的理由 ——
  ★★新的威脅判定若順手用了 `_t`，**它會長得跟既有那段一模一樣**。
```

# 二、★★★驗證（★兩層，而第二層才算數）

```
第一層（合成對照，★照我偵測器的形狀造的 ⇒ 只證明它認得自己）：
  陽性 3／3：讀欄位 `_tgt.population`／呼叫方法 `_tgt.is_alive()`／`_t.tile_pos`
  陰性 3／3：取 handle／交給 `score()`／讀 `_ob.tile_pos` —— **都放行，沒有誤咬**
★★★第二層（真檔注射，★這才是票 §9.1 要的）：
  在 `belief_system.gd` 的威脅判定【之後】插一行 `var _inj = _t.tile_pos`
  ⇒ **`掃到違規 1 處`｜`★行 309：var _inj = _t.tile_pos`｜GATE FAIL｜rc=1**
  ⇒ 還原後 `grep -c "_inj"` ＝ **0**
★而真檔跑的結果是 **0 處違規** ⇒ 我的實作沒有踩到那個後門。
```

# 三、★錨的設計（★★今天的教訓寫進 code）

```
・函式範圍 ＝ 由 `static func ` 邊界求得（★不是寫死 260~391）
・gate-ok 區塊 ＝ 由 `if Probe.hot_detail:` ＋ 縮排求得（★不是寫死 266~277）
・★★**找不到任何一個錨 ⇒ `push_error` ＋ `exit 2`（不可判）** —— ★★★**不是綠**
  ⇒ 而「錨腐爛時顯示為綠 ＝ 病好了」正是我今天記過的那條
・樣式在執行期組出來（`"_tg" + "t."`）⇒ **閘不會掃到自己的說明文字**
・掃描前剝註解 ⇒ 註解裡寫 `_t.` 不會誤判
```

# 四、★我自己知道的洞（★★印在卷面上，不是只寫在信裡）

```
★**只掃 `record_claim`** ⇒ 若有人把威脅判定搬到別的函式，**錨會失效而畫面是綠的**
⇒ ★★我沒有把它做成「全檔掃」，因為那會把既有 gate-ok 區塊以外的**合法** `_o`／`_t`
  也一起咬（那段本來就在 `record_claim` 裡）—— ★**擋掉正確寫法的閘會被繞過**（今天剛學到）
⇒ ★★★**處置等你**：要不要加一條「威脅判定必須留在 `record_claim`」的結構斷言，是你的欄。
```

# 五、狀態

```
・分支 `feat/intel-wake-by-content` @ `6861b6da8`（★未 merge）
・**B4a/b/c 量法仍等你裁** ⇒ B2／B5／B6／B7 未開始
・★而這一格我沒等你 —— 因為**它不依賴那個裁定**（票 §6 的形狀你已經寫死了）
```
