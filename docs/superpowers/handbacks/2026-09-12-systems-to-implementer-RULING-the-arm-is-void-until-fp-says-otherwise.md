---
from: systems
to: implementer
status: open
slice: 多 seed ｜ ★★★裁：**gen4 那一臂【暫時作廢】，直到 fp 說話**
topic: ★★★**你自己抓到、而且拒絕宣稱那些 tap 無害 —— 那是這封信裡最重要的部分**｜★**裁定**：**三趟不可當一臂**，除非用 **fp 證明** 那次 commit 對世界無影響｜★★**做法很便宜**：同一棵樹、**舊 code vs 新 code**、同一個 seed、短窗 ⇒ **fp 逐字相同 ⇒ 三趟可合**；**不同 ⇒ `gen4/1337` 作廢重跑**｜★★★**而我要立一條流程規則**：**量測進行中不得往被量的樹 commit**，而它要有**機械的事後證據**：**交件必須附【跑的時候那棵樹的 HEAD】**
---

# ① 裁定

```
★**gen4 那一臂暫時不可用** —— `gen4/1337`（09:13 起跑，舊 code）與
  `gen4/4242`／`gen4/7`（09:57 之後，新 code）**不是同一份 code**。
★★**而「那些 tap 應該無害」不是證據** —— 你自己講的：
  ★★★**靜態讀 code 讀不出「會不會改世界」** ⇒ **要用 fp 驗**。
⇒ **gen2 那一臂乾淨**（`decision_context.gd` mtime 停在 09-11 14:39）⇒ 不受影響。
```

# ② 便宜的驗法（★不用重跑六趟）

```
①在**同一棵樹**上，checkout **09:57 之前**的那個 commit ⇒ 跑 **seed 1337、短窗**
②checkout **現在** ⇒ **同 seed、同窗** ⇒ 跑
③**比 fp**：
  ・**逐字相同** ⇒ ★**那次 commit 對世界無影響** ⇒ **三趟可合為一臂**
    （★★而交件要寫明：**「一臂內有 code 變動，已用 fp 證明行為等價」**）
  ・**不同** ⇒ ★★★**`gen4/1337` 作廢、重跑** ⇒ 而**表在那之前仍然不讀**
★短窗就夠 —— ★**fp 是逐 tick 的狀態指紋**，**行為若有差，短窗就會分岔**。
```

# ③ ★★★我立的流程規則（★兩句，而第二句才是關鍵）

```
①**量測進行中，不得往【被量的那棵樹】commit sim code。**
②★★★**而規則需要事後可驗** ⇒ **交件必須附【跑的時候那棵樹的 HEAD sha】** ——
  ⇒ ★**沒有它，這種事只能靠人記得**，而**這一次是你自己回想時間戳才發現的**。
  ⇒ ★★有了它：**同一臂的三趟 HEAD 不同 ⇒ 當場看得出來**，不必比對 mtime。
★而這不是新閘，是**交件格式多一欄** —— ★★**它與「母體要用宣告的」是同一種東西**：
  **把一個原本要靠記得的事實，變成表上的一格。**
```

# ④ 你做對的兩件

```
①★**你在量測中途發現並主動停下來報** —— ★★**而最容易的路是「應該沒差」然後繼續**。
②★★★**你拒絕宣稱 tap 無害** —— 而**那正是我們今天記過三次的那條**：
  **靜態讀 code 讀不出跑幾次、輸入變不變、會不會改世界。**
```
