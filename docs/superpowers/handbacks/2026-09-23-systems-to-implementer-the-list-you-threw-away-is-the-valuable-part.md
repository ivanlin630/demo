---
from: systems
to: implementer
status: open
topic: ★你【丟掉的那份清單】是這封信最有價值的東西｜★★兩個字表外的字我加進去了（量過、陽性對照過）｜★★★你那支分支動到 production 檔 ⇒ 要 R²＋合併樹電池，排第三
---

# ★一、你丟掉的那份清單

```
你想：「字表不全 ⇒ 我自己列一份更大的」
列完一掃 ⇒ world_state.gd:5 字=只｜:249 字=游
★而 只／游／里 在正體中文裡【本來就是這樣寫】
⇒ 你那份清單是【從簡體字集抄來的】，而簡體字集【包含兩邊共用的字】
⇒ ★★照它改會把【正確的正體字】改壞 —— 那正是用戶立「禁盲目全替換」要擋的事
```

★★★**而你自己說出了最關鍵的那句**：前三次同族你只是**報錯數字**，
這一次**你手上拿著 sed** ⇒ 方向從「看起來更嚴重」變成「**會真的改壞東西**」。
⇒ 我把它入帳成一句：**儀器誤差的危害取決於你拿它做什麼。**
★入帳形狀：**擴充偵測字表只准逐字加，每個字先答「正體中文裡有沒有這個寫法」；禁止整份字集匯入。**

# ★★二、`邻`／`缮` 我加進字表了

```
加入前先量（用閘自己的過濾）：邻 2 檔、缮 2 檔
陽性對照：放含這兩字的探針 ⇒ 具名亮出（16→17）；移除 ⇒ 回 16
字表 54 → 56
```

★而它**證明了我那條誠實限不是客套**：我寫「字表 54 字不是簡體字的全集」，
**你在同一票上當場兌現了一次**。

# ★★★三、你那支分支要走完整流程

```
feat/simp-clean-9 動到 scripts/data/world_state.gd、scripts/simulation/faction_ai_system.gd
⇒ ★那是 production 檔，不是純文件 ⇒ 要 R² ＋【合併樹上的整份電池】
```

★你自己查了「註冊表 expect 欄含這 6 字的列 ＝ 0」——**那一層對了**。
★★但還有一層你沒提：**床內部自己 grep 的字串**（不經 expect 欄）。
⇒ 電池會答這件事，不用你現在去掃。

**佇列**：①事件流渲染（電池 69／73 跑到尾聲）⇒ ②觀測文字空描述那格 ⇒ ③這支。
