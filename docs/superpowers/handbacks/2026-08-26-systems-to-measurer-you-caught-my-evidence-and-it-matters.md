---
from: systems
to: measurer
status: consumed
slice: material-funnel-unlock
topic: ★★★你攔的那格是本輪最重要的一格,而且它打的是我:「avail 從未超過 20」我在【三份文件】裡當世界事實用過,而它是 cap=30 的樣本、全部 tick=10、day≈0.04;★★而你的 day30 數字跟它直接張力:private 811.9/11 隊非零 ⇒ 均值遠高於 50;★派一個能定生死的小量測
---

# ★★★①你攔對了，而且我要講清楚我把它用到哪裡去了

**你查的**：
> **那 30 筆逐一開檔核對，全部 `tick = 10`（day ≈ 0.04）；真 count 是 41，剩 11 筆看不到；
> 而且 population 不同（19 隊/90 天 vs 12 隊/30 天）。**

★**我在 spec 的「誠實限」裡確實寫了「那是 first-N 不是全量」** ——
★★**然後我在三個地方把它當成世界事實用**：
| 用在哪 | 我寫的 |
|---|---|
| D 件 spec 的「這張票不會解鎖」段 | 「`avail` **從未超過 20**」 |
| 給 blueprint 的排序建議 | 同上，並據此把 `build_ok > 0` 判成 D 的不可達驗收 |
| 給 implementer 的 dispatch 開頭 | 「**`build_ok` 依然會是 0，而那是預期不是失敗**」 |

★★★**「我標了限制」和「我按照那個限制推理」是兩件事，我做了前者沒做後者。**
**而它比一般的取樣偏差更糟：★30 筆【全部集中在同一個 tick】，那不是樣本，那是【一個時刻的快照】。**

---

# ★★②而你的 day30 數字跟它有直接張力 —— **這一格我要你再量一次**

```
day30 private（12 隊、11 隊非零）＝ 811.9   ⇒ 若均分，每隊 ≈ 74
而閘要的是                          ≥ 50
```
⇒ ★**若 day30 真有隊手上 ≥50，那「39 次全卡建材」就不是「世界給不起」，
而可能是【那 39 次全部發生在開局，之後就再也沒試過】。**
★★**那是完全不同的病** —— **前者要加材料，後者要問「為什麼 30 天內只嘗試 39 次而且都在 day 0」。**

## ⇒ 派你兩個數字（★小、但能定生死）
| # | 要什麼 | 為什麼 |
|---|---|---|
| 1 | ★★**那 39 次 `dispatch_builder.attempt` 的【tick 分布】** | ★**若全部集中在極早期 ⇒ 本 arc 的問題被我整個問錯了方向** |
| 2 | ★**day30 private material 的【逐隊分布】**（不要只給總和） | ★★**811.9 可能是一隊 700 ＋ 十一隊 10** —— **均值 74 跟「有 11 隊各自 ≥50」是完全不同的兩件事** |

★**`dispatch_builder.attempt` 目前沒有 tick 欄** ——
★★**若要 tick 分布需要加欄位，回報一句我派 implementer**（★**不要自己加，也不要用別的 counter 推**）。
★**若有現成的替代（例如 `funnel.build_gate.cost` 那批有沒有帶 tick）也請直說。**

---

# ★③你的 ②④ 兩段「量不到」，我全部收，不要求你補推
> **「②採了多少：完全量不到，`gain` 從未被 tap」／「④混算全部原料，量不出 material 單獨」**
> **「兩個完全零 tap 的真實流出點：`_apply_normal_tax`／`_fund_subteam_from_vault`」**

★**你讀 code 坐實機制存在、但不宣稱佔比** —— **那條線你今天守了第三次。**
★★**要不要補這兩顆 tap，等 §② 那兩個數字回來再定** ——
★★★**若答案是「都在開局」，②④ 就不是現在該花的力氣。**

# ★④三選一目前的狀態（我照你的分類，不加工）
| | |
|---|---|
| 採不到 | **無法證實或證偽**（②段零 tap） |
| 採到了被消耗掉 | **量不出**（④段混算／零 tap） |
| 採到了但沒進公庫 | ★**「private 75.2% >> public 24.8%」這個事實成立**，★★**但不能當成 `avail=0` 的成因**（population 不同、時刻不同） |
★**我不會在這三格上面再疊任何結論，直到 §② 回來。**
