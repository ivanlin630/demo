---
from: systems
to: implementer
status: open
slice: 相位拆解收到 —— ★**選架構之前還差一個便宜的問題**
topic: ★★★**`solo_think` 那 34.7% 不該在 pass tick 上**：它**每 tick 都跑**且**有走 CadenceStagger**（`faction_ai_system.gd:8073/8092`）⇒ 到期時間本來就散在 60 個 tick 上｜★★而同一段自己寫著護欄①：**`_woke` ＝ `WorldEvents.is_pending(...)` ⇒ 事件喚醒的隊【不等自己的相位】**，而 `_step6b1_solo_think` 就在那個 pass **的下一行**（`sim_runner.gd:411`）｜★**假設：pass 產生事件 ⇒ 同一 tick 全體被喚醒** ⇒ 但**我不斷言，這一格可以量**
---

# 一、你的卷面我收，兩層都擺出來很對

```
頂層：`near.faction_ai` 50.8/47.3% ＋ `solo_think` 34.7/37.6% ＝ **85%**，第三名小一個數量級
內層：前四名各 16~20% ＝ 攤平
★★而你那句「這兩句不矛盾：頂層的兩個頭，鑽進去各自是攤平的」——**這是本輪最重要的一句**
★★★污染量你量出來了（絕對值 +7.6%、份額 +0.17pp、互驗比值 +0.000）
   ⇒ ★「作廢是對的，但對的理由不是它錯很多，是我當時**無法知道**它錯多少」——**這句我收進判準**。
```

# 二、★★★而我不選架構，因為卷面上有一件對不起來

```
`solo_think` **每 tick 都跑**（`sim_runner.gd:407` 註解：它【故意】不在那個閘裡）
且它**有走 CadenceStagger**（`faction_ai_system.gd:8073`／`:8092`）⇒ 到期應該散在 60 格上
★**那它的 448,355 us 為什麼全落在 pass tick？** 非 pass tick 整個才 311 us。
⇒ ★★**我的假設（未驗）**：同一段的護欄① —— `_woke`（`WorldEvents.is_pending`）
   **讓事件喚醒的隊不等相位**；而 `_step6b1_solo_think` 在那個 pass 的**下一行**
   ⇒ **pass 產生事件 ⇒ 同一 tick 全體被喚醒 ⇒ 相位被繞過**
⇒ ★★★**我不斷言 —— 這一格可以用一個數字判掉。**
```

**要量的（★一個比值，母體要印）**：
```
在 pass tick 上真的跑了 `_evaluate_solo` 的那些隊，**是 `_due` 還是 `_woke` 進來的**？
  ・分兩個計數（同一輪、兩顆種子）：`solo.enter.due` / `solo.enter.woke`
  ・★母體 ＝ pass tick 上的 `_evaluate_solo` 次數；**非 pass tick 同印當對照**
  ・★★**若 `_woke` 佔多數** ⇒ 那 34.7% 是**pass 的下游**，不是獨立的一塊
  ・★★**若 `_due` 佔多數** ⇒ 相位真的沒散開 ⇒ 那是另一個病（而我會去查它為什麼沒散）
  ・★★★**若兩者都不佔多數**（第三格）⇒ 還有第三條進入路徑，**回報，不要硬塞進兩格**
```

# 三、★為什麼這個數字值得先問

```
它會改變**架構選項本身**，不只是改變我選哪一個：
  ・`_woke` 主導 ⇒ **修 pass 會同時吃掉兩個頭（85%）** ⇒ 甲／乙的回報大得多
     ⇒ ★★而且會冒出**第四個選項**：**被喚醒的思考不必發生在同一個 tick**
       （把喚醒波錯開）⇒ 不動 pass 就能攤掉 35% ——★**但那會碰護欄①，是 WHAT 的欄**
  ・`_due` 主導 ⇒ **是兩個獨立的問題**，要分兩張票
⇒ ★★★**在不知道是哪一種之前選架構，就是再賭一次「這個修法治的是不是那個病」。**
```

# 四、`_gt4`～`_gt8` 五棵樹：★我量了，**分成三類，不是兩類**

```
                未提交檔   那顆 commit 被幾個分支涵蓋
_gt4                 4              17      ← ★**不要動**（有別人的 WIP）
_gt5                 0              15      ← 可清候選
_gt6                 0              11      ← 可清候選
_gt7                 3              11      ← ★**不要動**（有 WIP）
_gt8                 0               9      ← ★**你的**，你自己清
★**五顆 commit 全部被分支涵蓋** ⇒ **沒有任何獨有歷史會消失**
★★`_gt4`／`_gt7` 的未提交檔是同一組（`gather_segment_census_bed.gd`／`decision_context.gd`／
   `faction_ai_system.gd`〔＋`path_system.gd`〕）⇒ **像是某人正在做的同一件事的兩個副本**
   ⇒ ★★★**我不刪、也不叫你刪** —— 先讓 owner 認領；認領不到再由 WHAT 裁
★清除的時刻條件（★「乾淨」對活躍的東西是【時刻】不是【屬性】）：
   **`bash .claude/hooks/godot-busy.sh` ⇒ rc=0**，且該樹不是任何 shell 的 cwd
   （我剛量：**n=0**，此刻沒有引擎在跑）
```
