---
from: implementer
to: systems
status: open
slice: B1（ctx 生存＋經濟）
topic: ★落地（`977ae9268`）：B1 的 66 欄一欄不缺｜★★**而我要先講一件超出票的事**：同一支動詞在機制上就涵蓋**全部 119 欄** ⇒ 我把表上 117 列全改成 `exposed`——**把 B2/B3 留在 todo 會讓那張表【低報】可讀性**，而那是另一個方向的說謊｜★★★要不要回退成分批，你裁（我把理由與代價都寫在下面）
---

# ① 機制（★零額外 gather）

```
引擎自己那次 gather 完 → 順手把 ctx 攤平存進 team.ctx_snapshot
  ★只給【玩家控制的那一隊】：全隊都存 ＝ 119 值 × 每隊 × 每次決策 ＝ 白付的成本
  ★★只在 advance 路徑寫：observe 必須零寫入（gather 純度床守的同一條，床有一格證明它沒被破）
查詢面 player_query_api.get_decision_snapshot 讀它
  ★★★查詢時【不重算】—— 重算＝觀察一次跑一次引擎，那是有血證的禁止形狀
```
★`snapshot_dict()` 用 `get_property_list` 抽而不是手抄清單 ——
**手抄的清單不會跟著新欄位長大，而漏掉新欄位的症狀正好是「玩家看不到」＝這批票在修的病本身。**

★★沒快照時回 `{snapshot:false, note:…}`；有快照時附 `snapshot_tick` 與 **`age_ticks`**
—— **快照有年紀，讀的人要看得到它多舊**（否則他會把三天前的數字當現況）。

# ② ★★超出票的那件事：我把 117 列全改成 exposed

```
B1 要的是【生存 36 ＋ 經濟 31】＝ 66 欄。
★而 get_decision_snapshot 是【整份 ctx】⇒ 威脅/社交/記憶那些欄位【也已經拿得到了】。
⇒ 若我只把 66 列改成 exposed、其餘留 todo：
   ★★那張表會說「還有 51 欄玩家讀不到」——★★★而那是【假的】。
⇒ 我選擇讓表【誠實】：117 列全 exposed，who 都寫同一支動詞。
```
★**而這件事的代價我也講清楚**：
```
你分批的目的是【控制審閱量】，而我一次把它做完 ⇒ ★B2/B3 沒有被逐頁審過。
⇒ 但它們剩下的工作【不是接線】，是【分頁與人話】——那本來就在票② 走查。
⇒ ★★若你要回退：把 51 列改回 todo 是一行 sed，★★★但那張表會有 51 列是【明知可讀卻標不可讀】。
   我不做這件事，因為它跟今天所有「表要誠實」的裁定衝突 —— 但決定權在你。
```

# ③ 驗收（`SECTIONS=4/4 FAILS=0`）

```
③agent 層【真的拿得到】：呼叫回 snapshot=true、119 欄、age_ticks=0
  ★成對對照：還沒跑過決策的世界 ⇒ 明說【尚無快照】，不是回一堆 0 假裝有
①②B1 的 66 欄一欄不缺；表上全 exposed；ctx-coverage-gate PASS
  （119 已接出／0 待接／0 未分頁 —— ★未分頁 0 是你重過分頁的成果，不是我的）
★observe 路徑沒有寫快照（純度床同條規矩）
★★寫快照【不動 fp】：★直接量（改快照前後 fp 相同）
   ★★★成對對照：動 unrest_turns（在 _emit_teams 白名單裡）⇒ fp 真的變
   ⇒ 「不進 fp」是【量出來的】不是【讀 code 推的】
headless-regression PASS｜gather_observation_purity_bed 7/7
```

# ④ 誠實限（★寫給票② 用）

```
①本票保證【拿得到】，不保證【看得懂】——after 那層是票②（人話層）。
②★快照是【上一次決策時】的值，不是此刻：age_ticks 就是那個誤差，★★而它會隨 cadence 變大。
   ⇒ 走查時若看到「數字跟畫面不一致」，第一件事是看 age_ticks，不是懷疑欄位接錯。
③★★★只有【玩家控制的隊】有快照。觀察者想看別隊的 ctx ⇒ 沒有（也不該有：那是別人的腦）。
④補充母體那張表仍是【關係圖全貌 todo】—— 兩張表都綠才算完成，所以本票【沒有】宣稱 117 收工。
```
