---
from: systems
to: implementer
status: open
slice: live_team 便捷式 ＋ 棘輪閘 —— ★DISPATCH
topic: ★R² 判 CLEAN｜★★而他對我問的第一格給了一個我沒想到的答案：**那個判準已經做出來過一次了** —— 你那份 86 列 tsv 就是它的產出（表頭已寫判準與盲區，5/86 誠實落進 [?]）⇒ 棘輪閘要做的**不是重新發明判準，是把同一套邏輯包成可重跑的腳本**｜★★★而他順手給了一條免費補強：同一支腳本多掃一條「`live_team(x)` 之後下一行沒判 null ⇒ 具名警」
---

# ① 派工

```
spec  docs/superpowers/specs/2026-09-10-live-team-ratchet-HOW.md（R² CLEAN，★可直接動）
序    ★做在攻擊門之前（攻擊門會讓世界【開始有滅團】⇒ 那 75 處會從「幾乎不開的窗」
      變成「常常開的窗」⇒ 之前做便宜）
```

# ② ★★三件（第一件是重點）

```
(1)★**便捷式** `WorldState.live_team(tid) -> TeamData`（不存在【或】pending_erase ⇒ null）
   ⇒ ★★比 `has()` ＋ `[]` 兩步更短 —— **寫法要贏在人體工學，不贏在紀律**。
   ★既有 `is_live_team(tid) -> bool` 保留（純布林那一類仍需要）。
(2)★★**棘輪閘**：★★★**判準【不要重新發明】——你那份 tsv 表頭寫的就是它**：
     「守衛之後、同一函式內，那個 id 有沒有 ①被取出 ②被當參數派出去 ⇒ 有 ⇒ L」
   ⇒ 用【縮排】定函式範圍（GDScript 靠縮排，比括號配對的語言好抓）。
   ⇒ ★**任何跑出 [?] 的新行，判為【需要人看】不是【通過】**
     —— 沿用你自己表頭那條「沒有 [?] 的分類是可疑的」。
   ⇒ 存量 86 處進 baseline；★★baseline 每列帶 owner ＋【觸發條件＝那一行被碰到時】
     （不是日期：日期會過期而沒有人處理）。★R² 確認 `merge-gates.sh` 有現成先例。
(3)★**同一支腳本多一條規則（R² 的免費補強）**：
   `live_team(x)` 之後【下一行】沒有 `if ... == null` ／ `if not ...` ⇒ **具名警**。
   ⇒ ★★它把「忘記判 null」這個風險用【你本來就要建的機制】接住，成本幾乎為零。
```

# ③ 驗收（★④ 是最容易做錯的那格）

```
①live_team 三格：正常隊回物件／pending_erase 回 null／從不存在回 null
  ★三格都要 —— 前兩格【回不同答案而理由不同】，只驗第三格會漏掉殭屍窗那格。
②★棘輪會咬：新增一行 `has(` ＋ 後續動作 ⇒ 具名亮警；移除後回綠。
③★★不得亂咬存量：那 86 處不得亮警，且 baseline 每列要能對回 tsv。
④★★★不得亂咬 [G]：純存在守衛（`has()` 之後不再碰那個 team）不亮警
  —— ★而偽陽的處置是【加進 baseline 並寫理由】，**不是放寬判準**。
⑤fp 不變 ＋ 一格行為證據（既有測試全綠）—— ★依界限第十八條，不單腿。
```

# ④ 而 R² 那句話我要轉給你（★因為它是對你上一份交件的評價）

```
「這不是紙上論證 —— 這個判準已經真的掃過全部 86 處，只有 5 處落進 [?]，
  而那 5 處不是被硬塞進 G 或 L，是誠實標成看不出來。」
⇒ ★★**你那份表的價值不只是 86 個數字，是它讓下一支閘【不必重新發明判準】。**
```
