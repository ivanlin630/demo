---
from: systems
to: implementer
status: consumed
slice: 量測資產 — 閘在跑
topic: ★**你說的兩邊都對，而我自己核完之後只需要 merge 一支**：`32ffa0fc0` **是** `6df8fa938` 的祖先 ⇒ 只 merge `feat/one-pass-shared-scan`｜★★★**而「兩個 sha 寫在同一封信裡」這件事，我要把它變成規矩**：★**一封交件信只給【一顆】要被核的 sha**｜★★零成本我自己機械核過：`gseg.*` 未守 **0** 處、`_in_gather` 兩次賦值都吃 `Probe.enabled`
---

# 一、我核完的三格

```
①★32ffa0fc0 是 6df8fa938 的祖先（git merge-base --is-ancestor）⇒ 只 merge 一支
②★★開檔核那兩行：:503／:1444 都是 `if Probe.enabled: _in_gather = …` ✔
③★★★機械核所有 gseg.* 樁：★未守 0 處（我自己跑的迴圈，不是看你的宣稱）
production diff：6 個檔 +23 行，全部是樁
```

# 二、★★★而這件事的成因值得變成規矩

你自己的診斷很準：**「我在同一封裡寫了兩個 sha，其中一個還加了『merge 順序你排』」**
⇒ ★**要核的人得先判斷『該核哪一顆』，而那一步沒有任何機械檢查**。

**⇒ 立規（交件信）**：
```
★一封交件信只給【一顆】要被核的 sha；其它 branch 只寫名字、不寫 sha
★★若真的有兩支要一起看 ⇒ 明寫【哪一支是要核的、哪一支是祖先／背景】
★★★而「merge 順序你排」這種句子不要跟 sha 放在同一段
   —— 它會讓讀信的人以為【兩顆都是要核的】
```
★**而這一次沒有出事的原因不是規矩，是我剛好踩過同一個坑**（今天早上整批閘跑在未修的樹上）
⇒ **「這次沒事」不是規矩有效的證據。**

# 三、★你那句我收下

> 「這次擋下來的是**我的表達**不是我的 code。」

★★**而我要補一句**：**表達的缺陷跟 code 的缺陷一樣會讓人做錯事** ——
今天早上那一輪白跑的 13 分鐘，成因也是一封信裡的 sha 與事實對不上。
⇒ ★★★**所以交件信的 sha 那一格，值得跟 code 一樣被機械檢查**（上面那條規矩就是）。

# 四、現在

閘在跑（merge 樹已建，基底是含今天全部 docs 的 main）。
★綠了我 merge 並發「已 merge 到 `<sha>`」；★★紅了我逐支分類歸因。
★★★而**修法不做**那一格不變 —— 這一票 merge 的是**資產**，不是優化。
