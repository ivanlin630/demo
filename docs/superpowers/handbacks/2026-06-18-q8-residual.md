# Hand Back: Q8 殘留落差 N-1 / N-2 / N-3

Branch: `feat/q8`（基於 `ca4bbf5`，含本 plan 文件）

## 實作摘要

### N-1 子隊面板死路引導 promote_anon
- `scripts/simulation/player_api_mapper.gd`：`map_subteam_panel` DTO 新增 `anon_count`（= `AnonTierSystem.total_pop(pt)`）。守「UI 只經 API」——UI 不直讀 WorldState，改吃 DTO 判 anon。
- `scripts/ui/text_ui_main.gd`：`_build_subteam_str` 在 `dispatch_candidates` 空時，若 `anon_count > 0` 顯「（無可用隊長——先到互動選單『拔擢匿名→記名』培養成員）」；無 anon 則退回原死路字（不誤導）。
- `scripts/debug/ui_flow_test.gd`：新增 `_test_n1_subteam_promote_anon_hint`（全 anon 隊 → 含引導字；清光 anon → 退回舊字）。

### N-2 choose_heir 不吃 stale 候選
- `scripts/simulation/player_command_system.gd`：
  - `get_forced_response_options` choose_heir：**重查活候選**——只回「仍在 `state.persons` 且仍在該隊 `named_members`」的 pid。team 由 `fe.team_id` 取（權威；choose_heir 時玩家 person 已死，`_get_player_team` 可能 null）。
  - `respond_to_forced` choose_heir：
    - 活候選空（全死）→ 走終局：`FactionAISystem.new()._handle_player_leader_death(state, fe.team_id 對應 team)`（mirror leader-death 無繼承 → `game_over`），fall through 清 forced。team 失效時直接 set `game_over`。
    - 選到已死候選（單一 stale）→ **early return `ok=false`，不清 forced**，讓玩家重選。
    - 正常活候選 → `_action_choose_heir`（既有路徑，內部接位 + 清 forced）。
- `scripts/debug/headless_test.gd`：新增 `_test_forced_heir_stale`（(a) 單一 stale：options 不含死者、選死者 ok=false 且 forced 保留、重選活的成功；(b) 全死：respond → game_over + forced 清）。

### N-3 camp/train query 反映真 gate
- `scripts/simulation/player_query_api.gd`：`_build_available_actions`
  - camp：原列出條件（無主 + 非山地 + 未開發）補上 `OutpostSystem.new()._check_distance(state, self_tile.tile_pos, camp_type)`（鏡射 `_action_camp` 距離前置；`camp_type` 取 `player_state.build_type`，預設 civilian）。
  - train：原（有 anon）補 `coin >= PlayerCommandSystem.TRAIN_COST_COIN`（鏡射 `_action_train` coin 前置）。
- `scripts/debug/ui_flow_test.gd`：`_test_camp_action_reachable` 更新——清掉附近 outpost 證 gate 通過時仍可達；另放相鄰 outpost 證距離太近時 camp 不列（雙向）。

## 端到端驗證
- headless：`=== DONE ===`，無 SCRIPT ERROR，`_test_forced_heir_stale` N-2 OK，`動作 UI 覆蓋審計 OK（51 actions）`（`_test_action_ui_coverage` 不破）。
- ui_flow：`errors: 0`，N-1 引導字 / 退回字、N-3 camp gate 通過可達 + 距離太近不列、train 可達 全 PASS。
- game_sim_multi：4 情境 coin_eq delta=0、invariant 違反=0、SCRIPT ERROR=0。
- 註：`ui_logic_test.gd` 2 個 vision-dist FAIL 為 pre-existing baseline，未動。

## 連動風險
- `OutpostSystem._check_distance`：query 層每次 build available_actions 都會掃全 tiles（O(tiles)）。原 NPC 建造路徑已這樣掃，量級相同；玩家面板低頻刷新，無效能疑慮。無守恆/不變量影響（純讀）。
- `FactionAISystem._handle_player_leader_death`：N-2 全死路徑復用此既有終局函數（不新增終局語意），與 encounter / famine 觸發的玩家 leader 死亡同路徑。

## 待主 session 確認
- 偏差（皆為使對齊 plan 意圖的健壯化，非新規則）：
  1. N-2 `get_forced_response_options` 與全死路徑的 team 解析改用 `fe.team_id`（plan MVP 用 `_get_player_team`）。原因：choose_heir 時 `state.player_id` 指向已死 leader，`_get_player_team` 可能回 null → 會把「有活候選」誤判成「全死終局」，或 `_handle_player_leader_death(null)` crash。`fe.team_id` 是 forced event 權威來源（`_action_choose_heir` 本就用它），更穩。
  2. N-3 camp 測試需主動清/置 outpost 以製造 gate 通過/擋下情境（gate 變真後測試須自備合法場景）；功能可達性未降。
- 無新增 config / 世界模型改動。
