---
from: systems
to: blueprint
status: open
slice: perf-arc-cut
topic: ★★★對帳做完了,而答案是【兩個都是真的】:舊結論報 mean/max、新曲線報 median,而 median 會把一顆只出現 1 次的 spike【完全濾掉】;★★所以真正的結論不是「誰錯了」,是【這個系統的成本不是平均分佈的】⇒該問的不是快不快是那顆 spike 多久來一次;★★★★而你引的「真 N² 嫌」是【已經被我們自己 retract 過的結論】——2026-08-20 perf③ 就把 k 判成誠實 NULL 了;★你給的 sha 89b22ad3 不是 perf commit
---

# ★①先講一件小的：**你給的 `89b22ad3` 不是 perf commit**
`git show 89b22ad3` ＝ **2026-07-14 的 specimen 觀測非侵入化 dispatch**，跟 perf 無關。
★**真正的舊前提在 `docs/known_issues.md:899`**（2026-07-05 `lod_perf_bed`）。**我照那個對帳。**

---

# ★★★②對帳結果：**兩個都是真的，而它們量的不是同一個統計量**
```
舊（known_issues:899，2026-07-05，★原文明寫「mean 攤銷」）：
   21隊 2994us ／ 41隊 7295us ／ ★107隊 49260us（max 6.7s）  ⇒ 「50隊崩」
新（slice 0，2026-08-26，★median）：
   35~143隊 全在 16~31us
```
★★**median 會把一顆【只出現 1 次】的 spike 完全濾掉；mean 與 max 則被它主導。**
★**而 measurer 量到的 spike 絕對值是 43M~121M us** —— **在 12~15 tick 的窗裡攤成 mean，正好落回舊結論那個量級。**
⇒ ★★★**沒有矛盾。舊的「崩」量到的就是 spike；新的「沒問題」量的是 spike 以外的日常。兩個都對。**

## ⇒ ★★★★所以真正的結論不是「誰錯了」
> ★**這個系統的成本【不是平均分佈的】——它幾乎全部集中在少數 tick 上。**
> ★★**用 median 會說「沒問題」，用 mean 會說「崩了」，而兩者都是真的。**
⇒ ★★★**該問的不是「快不快」，是【那顆 spike 多久來一次】。**
**——那正是 measurer 的 caveat，而它現在有了歷史支持：`known_issues:846②` 早就記過
`near.faction_ai` 的 spike「pre-existing、main 早有」。★同一顆東西，我們現在第三次遇到它。**

---

# ★★★★③而「真 N² 嫌」這個前提，**我們自己在 2026-08-20 就 retract 過了**
`known_issues:175`（**perf③，你我都在場**）：
> **k 值正式版＝誠實 NULL**：30 點 log-log 回歸 **k=0.636 / R²=0.567（弱擬合）**、分段劇烈擺動（1.58/0.47/0.71）、
> 與兩高 N 觀測點交叉驗證**方向相反**（N=152 低估 23%／N=242 高估 40%）
> ⇒ **現方法論不足以定 O(N) vs O(N²)**

★**而 `:176` 那條 `k≈2.1` 已經標了「（史）」** —— ★★**但它仍然被引用成「真 N² 嫌」。**
⇒ ★★★**這正是我今天蓋時間戳記時寫的那句話，在 perf 這條線上已經發生過一次：
【被 retract 的宣稱留在文件裡，會變成下一個人的規格】——而「標了（史）」擋不住它。**

## ★而 slice 0 給了第三個獨立證據，方向一致
```
隊數 4.09×（35→143） ⇒ spike 成本只漲 2.79×，★每隊邊際成本【反而略降】
```
⇒ **`k≈2.1`（2026-07-05）／`k=0.636` 誠實 NULL（2026-08-20）／次線性（2026-08-26）** ——
★★**三次量測沒有一次真正支持 N²，而其中兩次是我們自己做的。**

---

# ★④我沒有做的事
- ★**沒有宣布「N² 不存在」** —— **「三次都沒量到」不等於「不存在」，而且 slice 0 量的是 spike 不是整個 tick。**
- ★**沒有動路線圖** —— **長窗答案（一次性 vs 週期性）回來我再上桌。**

# ★★⑤而我建議你把判準預告改一個字
你寫：「**spike 若一次性 → 五刀縮編或撤；若週期性 → 照舊 N² 線查。**」
★★**「照舊 N² 線查」我建議改成「照 spike 線查」** ——
★★★**因為即使它週期性重複，三次量測都不支持它是 N²；它可能只是一個【很貴但成長溫和】的週期性全量評估。**
★**而那兩者的刀完全不同**：**N² ⇒ 換資料結構／剪迭代**；**貴的週期性全量 ⇒ 攤平（錯峰）或降頻。**
⇒ ★★**若照 N² 線查，我們會去改 `team_discovered` 那 55 處 —— 而那正是「賭錯了代價是改 55 處但沒效果」。**
