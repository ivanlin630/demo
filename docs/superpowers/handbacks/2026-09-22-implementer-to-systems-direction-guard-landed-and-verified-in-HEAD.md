---
from: implementer
to: systems
status: open
slice: ★**方向守衛做完並核在 HEAD**（`4104b5013`）—— 放進既有的床，已登註冊表
topic: ★**兩極都驗**：正常 `574／0／0／696`（對角線）rc=0；**只反轉生產那一行** ⇒ `817／718` 落**反對角**、FAIL、rc=1｜★★★**關鍵設計是「是否威脅」必須【獨立於 `_wake`】算一次** —— 若兩者共用同一個運算式，**比較方向一反、兩邊一起動 ⇒ 什麼都測不到**｜★★**這是刻意的重複**：tap 手上握一份【意圖】的獨立副本，**生產邏輯偏離它時交叉表才會破**｜★誠實限寫進註冊表描述欄：**兩邊【都】反的話它也瞎**
---

# 一、★卷面（★兩極）

```
正常：`威脅×喚醒 = 574｜威脅×不喚醒 = 0｜非威脅×喚醒 = 0｜非威脅×不喚醒 = 696`  rc=0
注射（`>=` 改 `<`，★只改生產那一行）：
      `威脅×喚醒 = 0｜威脅×不喚醒 = 817｜非威脅×喚醒 = 718｜非威脅×不喚醒 = 0`
      `FAIL：方向守衛破…⇒ ★比較方向反了`  rc=1   ⇒ ★已還原並 grep 確認
```

# 二、★★★為什麼「獨立算一次」是整件事的關鍵

```
★若我讓交叉表直接用 `_wake` 去分類「是不是威脅」：
  方向一反 ⇒ **分類跟著反** ⇒ 對角線**依然完美** ⇒ ★★**守衛全綠而語意全錯**
⇒ 所以 tap 裡另外算 `_is_threat = _threat_score >= THREAT_BASE_THRESHOLD`
⇒ ★**這是【刻意的重複】** —— 平常我們說「第二份會漂」，
  ★★★**而這裡【要的就是它會漂】**：tap 那份是【意圖】，生產那份是【實作】，
  **兩份分開才量得出「實作漂離了意圖」。**
★★誠實限（寫進註冊表描述欄）：**有人把兩邊都反 ⇒ 它也是瞎的**
  —— 它擋的是**單方面漂掉**，不是**有人故意改兩處**。
```

# 三、★它為什麼可以當閘（而 B1 不行）

```
・**單棵樹**：它不需要 before ⇒ merge-gate 跑得動
・**短窗**：`IW_DAYS=1` 就有 1,270 筆母體 ⇒ 秒～分鐘級
・**問語意不問文字**：改寫成 `not (x < T)` ⇒ 仍綠（B6 的文字檢查會紅）
⇒ ★★**B6 與它是互補的**：B6 擋「掏空謂詞／寫成常數」，方向守衛擋「語意反轉」
```

# 四、★已核（★★這次先落地、核過、才寫信）

```
`4104b5013` 已 push；`git show HEAD:` 核過：
  ・`docs/process/merge-gates.tsv` 有 `intel-wake-direction` 一行
  ・`belief_system.gd` ＋ `intel_wake_threat_share_bed.gd` 的改動都在
  ・★expect（`威脅×不喚醒=0 且 非威脅×喚醒=0`）**對捕獲的 stdout 驗過命中**
```
