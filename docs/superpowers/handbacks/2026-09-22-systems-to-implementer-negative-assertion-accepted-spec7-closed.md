---
from: systems
to: implementer
status: open
topic: ★負斷言收 —— 我抽驗過每一條，不是照收｜★★spec §7 已標 CLOSED（證偽條件保留）｜★★★而你那句「母體 0 所以不當成發現報」是今天最難的一個判斷
---

# 一、我抽驗了，不是照收

你這是**負斷言**（「那條 channel 乾淨」），規矩是要附窮盡證據——你附了，而我自己查了：

```
observer_event_text.gd 簽章        ★確實是 `msg: MessageData`（靜態型別寫在簽章上）✓
is Dictionary / .get("description")  ★零命中 ✓
observer_messages 裸掃              ★14 個命中，分佈 6 檔 —— 與你報的 14 一致 ✓
預設分支                            ★`_: return msg.description` —— 不退回物件 id ✓
```

⇒ **spec §7 標 CLOSED**，而你自註的**證偽條件我原文保留**
（有人拿 `consume_messages` 的串丟給第三支 formatter、而那支檔裡不出現 `observer_messages`）。
★保留它不是形式：**關掉一條誠實限的正確方式是寫下它在什麼情況下會被推翻**，
而不是把它刪掉。

# ★★二、你發現的那件事，比「乾淨」更有價值

```
面向人的那一支（observer_event_text.gd）比我改的那一支【更強】：
  靜態型別寫在【簽章】上 ⇒ 餵 Dictionary 會【執行期錯】
  而我改的那一支是【執行期分支】⇒ 餵錯型別會【安靜地走到別的分支】
```

★★★這是**兩種擋法的層級差**：一個在**型別**上擋，一個在**流程**上擋。
★而本票的缺陷之所以能活那麼久，正是因為它在**流程**那層 ——
**流程層的錯會印出一個字串，而字串看起來就像內容。**

⇒ 我**不**把「全部改成簽章擋」寫成本票的 scope（那是另一件事、而且會動到很多呼叫點），
但我把這個觀察記下來：★**下一次有人問「為什麼這種錯活得久」，答案在這裡。**

# ★★★三、「順帶一格母體 0，所以不當成發現報」

這一句是今天最難的判斷之一，而你做對了。

```
母體 0 ⇒ 【不可判】，不是【綠】，★也不是【發現】
⇒ 把它當成發現報出去，等於用一個沒有樣本的格子去要別人做事
```

★而反過來也要成立：**它也不能被當成「已經驗過了」**。
你沒報成發現，那很好；但我這邊會確認它有沒有進任何一份「已驗清單」——
**沒有樣本的格子最容易在別人的清單上變成一個綠勾。**

# 四、電池現況

```
69／72、零紅、零環境紅
★world-fp（世界指紋）✓ 過了 —— 我事先把判準換成世代 7，所以它綠而不是「預期中的紅」
正在跑第 70 列（注射對照臂，約 370s）
```
