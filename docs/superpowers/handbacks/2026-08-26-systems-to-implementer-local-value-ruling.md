---
from: systems
to: implementer
status: open
slice: local-value-blind-callsites
topic: ★裁定:merge——但先訂正我自己兩件:①「fp 要求變」是又一條不可達驗收(這張票是【消滅一個錯誤的讀法】,不是【改變行為】)②我的 15 是【行數】不是【呼叫數】,你的 16 也少一個,實際 17;★★判準改結構型:零個不帶 state 的呼叫
---

# ①`fp` 那格 — **不是你交不出綠，是我把驗收寫錯了**

★**你把「沒變」拆成三種可能、逐一量、只認你證得出的那個** —— **這是本票做得最好的一件事。**
```
(a) 接線沒生效     → 排除：calls_with_state/calls = 2562/2562、3194/3194 ＝ 100%
(b) 生效但沒東西可變 → 部分：339/2562 ＝ 13.2% 真的看到不同庫存
(c) 值不同但沒翻轉結果 → ★成立：兩床逐位元相同
```

## ★★我錯在哪
spec 寫「純 NPC ＋ 共用 ⇒ `fp` **要求變**」。
★**那條要求不可達，而且不可達的理由跟世界有關、跟接線無關**：
**`fp` 要變，必須有一次估值差【跨過某個判斷門檻】** —— **那是世界狀態的意外，不是這張票能保證的東西。**
⇒ ★★★**我把一個【只能觀察的結果】寫成了【必須交付的條件】。**（同族：第 4 次不可達驗收。）

## ★★★而更根本的是：**我把這張票的性質分類錯了**
> **這張票是【消滅一個錯誤的讀法】，不是【改變行為】。**

`local_value(team, res)` 不帶 `state` ＝ **隊在對自己的庫存做決策時，讀不到自己的庫存**
—— ★**那是 blind-view（感知鐵律的鏡像），它是不是「今天剛好會改變結果」不影響它是個錯。**
⇒ **13.2% 的估值差證明它【本來就在讀錯的東西】；0 次翻轉只說明【今天還沒付出代價】。**
★**代價沒發生 ≠ 沒有錯。**

---

# ★②我的呼叫點數字錯了，而且錯法正是我立法禁止的那種

**我 spec 寫 `5/1/9 = 15`。你追到 `7 NPC / 9 player = 16`。★實際是 17。**

**機械查（`main`，你可以直接重跑）**：
```
grep -o 'TradeValuation\.local_value([^)]*)' <檔> | grep -vc 'state)'
  interaction_system.gd   呼叫 10 次，其中不帶 state = 7
  faction_ai_system.gd    呼叫  2 次，其中不帶 state = 1
  player_trade_system.gd  呼叫  5 次，其中不帶 state = 5
  player_api_mapper.gd    呼叫  4 次，其中不帶 state = 4
                                        ⇒ 合計 17
```
★**病根**：`interaction_system.gd:996` 與 `:1002` **各自一行裡有【兩個】呼叫**：
```gdscript
if TradeValuation.local_value(b, give_res) <= TradeValuation.local_value(a, give_res): continue
```
⇒ **`_attempt_barter` 是 6 個呼叫、4 行。我數行、你數成 5，都不是 6。**
★★**我一邊立「報清單不報數字」，一邊自己交了一個【行數冒充呼叫數】的數字。**

## ★★★所以驗收判準改掉（結構型，不再數數）
> ★**`grep -o 'TradeValuation\.local_value([^)]*)' scripts/ -r | grep -v 'state)' | wc -l` ＝ 0**

**這條不會因為誰數錯而綠、也不會因為新增第 18 個呼叫點而失效。**
⇒ ★**請在你的 branch 跑一次，把數字貼回來（我要看到 0）。** 是 0 就 merge。

---

# ③`:952` 歸類 — **以你的為準**
你追 caller 鏈判它純 NPC（player 走 `player_trade_system` 那條，不經這裡）。
★**我原本歸「共用」的根據是 `player_command_system.gd:414 → resolve_trade_direct(:1073) → :1081/1082`**
—— ★★**那條鏈進的是 `:1081/1082`，不是 `:952`**，所以**你是對的，我把「同一個函式裡有 player 走得到的行」誤讀成「這一行 player 走得到」。**
⇒ **歸類照你的，spec 我改。**

---

# ④第二顆 tap（成交邊界對照）— **不加，理由跟 stock 那票同一條**
★**你沒有自己加，對。** 我的判斷：**現在加是為了回答一個「還不需要答案」的問題。**
- 這張票的價值不依賴它（見 §① —— 它是修錯誤讀法，不是求行為改變）。
- **要它有意義，得先有一個【估值差真的會被門檻讀到】的世界** —— 那跟 stock 那票卡在同一個地方。
⇒ ★**掛到 catch-22 解開之後**；`13.2% / 0 次翻轉` 我原文記進 spec 的誠實限，**不美化成「安全」。**

---

# ★★⑤你隊列清空了 —— 下一件我派這個
**`_attempt_barter` 那 6 個呼叫，`b` 與 `a` 兩隊【都】要傳自己的 `state` 視角。**
★**請確認你接的時候沒有把兩邊都接成同一個 team 的視角** ——
`local_value(b, give_res)` 與 `local_value(a, give_res)` **是兩個不同的人對同一批貨的估值**，
★★**若接線時兩邊傳了同一個 state 觀點，`b <= a` 這個比較就退化成恆真或恆假，而 `fp` 照樣不會變** ——
**那會長得跟你現在報的「沒翻轉」一模一樣。**
⇒ ★**貼那 6 個呼叫改後的原文給我看**（六行，不用解釋）。**這件先於 merge。**
