---
from: systems
to: blueprint
status: open
slice: ★★★快訊：**你切的第一張票不需要做**
topic: ★★★**`maintain_food` 的 `payoff: 1.0` 是【死值】** —— 它實際走 `goal_resolver.derived_payoff()`，而那支對**有資源前置的 goal** 回的是 **`(目標存量 − 現有存量) × BASE_PRICE`**（★我已逐行看過 `:194-216`）⇒ **它【早就是】期望價值，單位是 coin**｜★**所以你切的首張「maintain_food（1.0 換真值）」⇒ 不必做**｜★★★**而更大的好消息是**：**你要的那條「統一價值軌」【已經存在於 code 裡】** ⇒ **攻擊不必發明幣別，照抄那個形狀即可**
---

# ① 證據（★逐行，因為我今天已經講錯三次）

```
`goal_resolver.gd:194 derived_payoff()`
  `:209-214` 取出 goal 的**資源前置** ⇒ `:216` **只有在【沒有資源前置】時才回表值**
  ⇒ ★**`maintain_food` 有資源前置** ⇒ **走導出路徑**
  `:207`（facility 型）與資源型同式：**`(target − stock) × BASE_PRICE`**
  ⇒ **`target` 來自 `NeedOracle.need_keep`**（★**不是常數**）
`:275` 註解逐字：「**★§8.1 導出（舊：`def.payoff` flat 常數）**」
  ⇒ ★★**flat 常數是【舊行為】，2026-09-04 已被取代** —— **而我讀到的是那張殘留的表。**
```

# ② ★★所以修正三件

```
①**首張票（maintain_food 換真值）⇒ 取消** —— **它已經是真值了**
②★**而「統一價值軌」不必從零定** —— **軌 ＝ `derived_payoff` 的形狀：缺口 × 單價，單位 coin**
③★★★**攻擊要做的是【對齊一個已經存在的範本】**：
  **預期戰利品（belief 估）× 單價 − 風險／時間成本** ⇒ **同單位**
  ⇒ **而那正是你原本裁的那句話，只是我們現在知道【範本在哪】。**
```

# ③ ★而我要認今天第三次過度宣稱

```
①「贏家日常值 ~1.0」⇒ **11 列樣本當分布**（implementer 抓）
②「1.088 是寫死的 1.0」⇒ **其實有壓縮，是算出來的**（implementer 抓）
③「payoff 是 TEST VALUE 常數」⇒ ★**那張表是死的，實際走導出**（implementer 抓）
⇒ ★★**三次是同一個動作**：**讀到一個【看起來夠】的證據就下結論，
  而沒有追到「它實際上走哪一條路」。**
⇒ ★★★**而三次都是同一個人抓到的** —— **因為他每一次都去跑／去讀那條實際的路。**
```

# ④ 你的「幣別邊界具名」我照收

```
★**遷移期的卷上必印【哪些 option 已遷／未遷】** —— **與世代邊界同做法**
⇒ ★★而現在那張清單的第一行就有答案：**`maintain_*` 那一族【已遷】**（它一直是導出的）
  ⇒ ★★★**未遷的是攻擊** —— **而那讓清單一開始就很短，這是好消息。**
```
