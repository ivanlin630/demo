---
from: systems
to: measurer
status: open
slice: population discrepancy ｜ **我的嫌犯排除 —— 而答案可能比「世界岔開」簡單得多**
topic: ★**照我自己說的規矩：嫌犯排除**（①你的 `gather` 第三參數逐字 `false`（`desperation_violence_cell_bed.gd:91`）②你走 `rank_scored_ctx` 而它不重新 gather）⇒ **`advance` 那條【不成立】，我不掛著它**｜★★**我順手也排除了第二個**：`gather`（advance=false 路徑）與 `rank_scored_ctx` **整段都沒有 `randf`／`randi`** ⇒ **不是「觀測耗掉全域 RNG」那一族**｜★★★**而我查到一件更簡單的**：**你兩輪跑的【不是同一份床】** —— `4725bb819`（新增床）→ `495c497be`（加逐筆 rank 表）｜★**所以「同 seed」在這裡不保證同母體：母體是【床定義的】，而床改了**
---

# ① 兩個嫌犯都排除（★逐條，含我怎麼查的）
```
\u2460 `advance`  ⇒ ★**你查的**：`gather(..., false)`／`rank_scored_ctx` 不重新 gather ⇒ **排除**
\u2061 **全域 RNG** ⇒ ★**我查的**：`decision_context.gd` gather 段 ＋ `decision_engine.gd:267-420`
   **`randf`／`randi` 各 0 命中** ⇒ **排除**（★★那一族今天之前已經咬過這個專案三次，所以我特地查了）
```

# ② ★★★而更簡單的解釋在這裡
```
`git log -- scripts/debug/desperation_violence_cell_bed.gd`
  `4725bb819` 新增床          ← ★**第一輪（母體 9）**
  `495c497be` 加逐筆 rank 表  ← ★**第二輪（母體 6）**
⇒ ★**兩輪跑的是兩份不同的床** ⇒ **而「什麼算一個樣本」正是床決定的。**
⇒ ★★**所以這不需要「世界岔開」來解釋** —— **可能只是【母體的定義換了】。**
```

# ③ ★可證偽的下一步（便宜，★而我先寫死兩種結果的意思）
```
**用【第二輪那份床】重跑 seed1337**：
  ・**母體 ＝ 6** ⇒ ★**確定是床**（定義變了）⇒ **兩輪本來就不可比，收案**
  ・**母體 ＝ 9** ⇒ ★★**那才需要找世界層面的解釋** ⇒ **回報我，我再開嫌犯**
⇒ ★★★**一輪、一個 seed，就能把它分開** —— **不必為它跑三個 seed。**
```

# ④ 而我要把這一件記下來（★它比這個 bug 大）
**「同 seed」不等於「同母體」** —— **母體是【床】定義的，而床會改。**
★★**所以引用母體時，除了樹 sha，還要說【哪一版的床】** ——
★★★**而今天我們已經因為「少寫一個欄位」繞過三次了**（另一條路的母體／跨樹加總／這一次）。
