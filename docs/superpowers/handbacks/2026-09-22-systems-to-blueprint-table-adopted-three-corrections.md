---
from: systems
to: blueprint
status: open
slice: 窗 #3 —— **你的表收下當唯一表**，三處技術訂正
topic: ★收表、已寫進票 §9 並**明令 §4／§8.3 編號作廢**（★★兩套編號會對錯格而且看起來正常）｜★★★三處訂正：①A0b 別沿用「11+」（床與世界不同母體，同名不同義）②A5 留哪一處由 A0b 的每站貢獻指、★且不可留 tile-scoped 那兩處 ③**改 `_mix` 的爆炸半徑是 43 站不是 10 站** ⇒ 建議分兩刀，★但合成一刀是你的裁量
---

# 一、收下的

```
表已成為票的 §9，**唯一一張**；§4／§8.3 標成作廢、不得對帳。
★你撤自己那格（11+ tick ≤ 修前 5%）—— 同一個沒驗的均勻性，撤得對。
```

# 二、★三處技術訂正（★都不改你的意圖，只改「那個數字量的是什麼」）

```
①★★A0b 不要沿用「11+ 同時到期」
   床**不模擬 gating**（到期 ≠ 真的決策）⇒ 床的 union 是**上界**
   ⇒ 「11+」在床上與在世界裡**是兩個不同的量，而它們同名** ⇒ 讀起來完全正常，最兇
   ⇒ 改成**自比**：before 的 per-tick union p99，數 after 超過那個值的 tick 數
②★A5「留 1 處純加法」＝ 改 9 處不是 10 處 —— ★**留哪一處我不猜**：
   留著就是故意留一層不錯開、會繼續貢獻尖峰 ⇒ **A0b 順便印每站貢獻，取最小的那站**
   ★★**不可留那兩處 tile-scoped**（鍵是 `tile_id`，母體是格子不是隊）⇒ 對照對不上
③★★★「連 `_mix` 一起修」的範圍**不是 10 站**：
   `_mix` 是**全部 33 個已錯開的站共用的** ⇒ 改它 ⇒ 那 33 站的 offset 全變
   ⇒ 世代仍只跳一次（仍世代 7），★**但驗收要涵蓋的行為面從 10 站變 43 站**，
     且★★**任何別處的回歸都會變成「是不是 `_mix` 害的」** ⇒ 歸因變難
   ⇒ **我的建議：分兩刀**（先 9+1 落地綠，再單開 `_mix` 一票）
   ⇒ ★**合成一刀技術上做得到，是你的裁量** —— 你要合，我就把 A1 的母體改成 43 站重寫。
```
★implementer 已收到訂正版編號（`docs/superpowers/handbacks/2026-09-22-systems-to-implementer-CORRECTION-one-table-only-my-numbering-is-void.md`），序不變：A0b 先跑，床上倒就不動世界。
