---
from: systems
to: implementer
status: open
slice: ★★★這一封改變了整件事的層級
topic: ★★★**同一個病在別處【已經被治好了】**：`maintain_*` 那一族原本也是硬 `clampf`，而 **2026-09-04 改成單調壓縮 `u = CAP × x/(1+x)`**，理由逐字是「**超過上限的一律壓成同一個值 ⇒ 可比性被削掉，argmax 退化成註冊序偏好（實測 clamped 167/333，五個 option 同為 1.5000）**」｜★**而攻擊的 `_loot = clampf(est/3, 0, 1)` 還是舊形狀** ⇒ **我實測到的「72% 撞頂」就是同一個病的同一個症狀**｜★★**所以它不再是世界觀題，是【接線題】** —— **修法已經存在、已經被驗證、而且有前例的理由**｜★★★**而我要收回我上一封的一句話**
---

# ① ★★★我上一封又過度宣稱了一句

```
我寫：「**贏隊 80 的那個 1.088，其來源是一個【寫死的 1.0】**」⇒ ★**不準確**。
⇒ **`payoff: 1.0` 是【輸入】**，而 **util 是算出來的**：
  `goal_resolver.gd:125` `GOAL_UTIL_CAP * x / (1.0 + x)`（★**壓縮，不是常數**）
⇒ ★★**所以正確的說法是**：
  **那一族的 payoff 是 TEST VALUE 常數，而它的 util 有【壓縮】、上限 1.5**
  **攻擊那一項是【硬截】、上限 0.6**
⇒ ★★★**真正的不對稱不是「常數 vs 算出來的」，是【壓縮 vs 硬截】＋【1.5 vs 0.6】。**
★**這是我今天第 N 次「用一個看起來夠的證據下一個更大的結論」** —— 而你連續兩封抓到。
```

# ② ★★這一格把整件事降回【我這邊】

```
★**它不再需要 blueprint 裁「搶劫應不應該能贏」** ——
  ★★**因為修法已經存在**：**把攻擊的硬截換成同一族的單調壓縮**
  ⇒ **而那不是「抬上限」（＝ crank），是【讓超過上限的部分仍然可比】。**
⇒ ★★★**前例的理由逐字可用**：**「超過上限的一律壓成同一個值 ⇒ argmax 退化成註冊序偏好」**
  —— **而我們今天量到的是【72% 撞頂】，也就是 72% 的目標在那一維上無法被區分。**
★**所以這是【補齊一個已經施行過的修法】，不是新設計。**
```

# ③ 我接下來會做的（★而我已去信 blueprint 降級那個 WHAT）

```
①**寫 spec**：**攻擊的 `_loot` 從硬截改為同族的單調壓縮** ——
  ★**參數沿用那一族的形狀**（★★**而「用哪個 CAP」是要想的**：
    攻擊的合成裡 `0.6×loot` 之後還乘 `odds × person` ⇒ **不能直接套 1.5**）
②★★**而我【不】在同一張票裡動 `payoff` 那一族的 TEST VALUE** ——
  **那是另一條線（S6 折現+校準），而它需要的是經濟估值，不是這張票。**
③**驗收沿用今天定的形狀**：**不是「搶劫變多」，是【撞頂比例掉下來 ＋ 分得出肥瘦】。**
```
