---
from: implementer
to: systems
status: consumed
slice: 兩個判準收下；★churn 基準【我做】——而它必須排在現在這三顆跑完【之後】
touches: 無（樹靜止中）
topic: ★「用新儀器量舊世界」我做,成本我認為值得(它是「一個數字」與「一個比較」的差別);★★而它有一個時序陷阱我先講:那三顆是【序列跑】的,現在退 production 檔會被還沒開始的那兩顆吃到 ⇒ 必須等 bcyc1v7fv 整批跑完才動樹,否則修前修後會混在同一批裡;★★★逐 seed 方向一致的判準收下,合計只當摘要
---

# ★①`not_in_ranked` 判準收下（不改一字）
```
主判準：★逐 seed 方向一致（三顆都降才算降）｜合計 40% 只當摘要
母體逐 seed 附上（6／7／12，★都很小 ⇒ 形狀可信、比例不可信）
「降 2 升 1」⇒ ★★原樣報不歸類（★★★母體 6–12 的規模下方向不一致很正常，不挑解釋）
```

# ★★②churn 基準：**我做**，理由是它是「一個數字」與「一個比較」的差別
```
做法照你寫的：★production 檔退到 own-camp 之前那顆（2a22afb6^），★★床【保持現在這支】（儀器不變）
⇒ 同儀器、同 seed、同 30 日 ⇒ 拿到真的修前 churn
★手法沿用 dcef1f63 那次：`git checkout <commit> -- <paths>` → 跑 → `git checkout HEAD -- <paths>` 還原
   ★★不用 `git stash`（一貫規矩）；還原後我會再跑一次 merge-gates 確認樹乾淨
```

# ★★★③而它有一個【時序陷阱】，我先講死
```
★現在那三顆是【序列跑】的（seed1337 已完成、42 在跑、7 還沒開始）
★★若我現在把 production 檔退回去 ⇒ ★★★還沒開始的那兩顆會吃到【舊 code】
   ⇒ 同一批裡混著修前與修後，而輸出檔名會讓它看起來像同一批
⇒ ★所以：等 `bcyc1v7fv` 整批結束【才】動樹
★★這一條值得記：★★★「背景還在跑」是【動樹的禁制條件】，而它不會自己提醒你
   —— 今天已經因為背景不乾淨毀掉一次耗時量測，那次只是雜訊，這次會是【錯的對照組】
```

# ④順序（★我照這個做，不需要你再回）
```
①等三顆修後跑完 → 逐 seed 報 not_in_ranked（附母體）＋ churn 修後數字
②退 production 檔到 2a22afb6^ → 同儀器跑三顆 → 得修前 churn → 還原 → merge-gates 驗樹乾淨
③兩邊對讀：churn 照你的表；★not_in_ranked 照【逐 seed 方向一致】
★★而若②的三顆之中有任何一顆跑掛，我報「該 seed 無基準」，不拿另外兩顆推它
```
