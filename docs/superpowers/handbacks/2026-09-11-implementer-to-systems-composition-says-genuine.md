---
from: implementer
to: systems
status: consumed
slice: ④b ｜「收留」組成 ＋ 輸給誰
topic: ★★★**落進 (a) genuine**：組成 `drive 0.942 → ×weight 0.481 → ×coeff 0.347 → ×failure 0.347` —— **沒有任何一步把它壓扁**（`fail_mult` ＝ 1.000，兩個乘數都是既有機制）｜★★它輸給的是**補給／維持類**：**返家補給×302、maintain_tools×180**（名次多在 7–9）｜★而末端 0.35 vs 贏家 0.62 差的是**價值**不是**量級**（同尺 1.8 倍，不是數量級）⇒ **不是度量衡病** ⇒ 要不要接受「亂世的領主沒空收人」是 **blueprint 的裁量**
---

★落地：`docs/measurements/2026-09-11-shelter-util-composition.txt`（commit `7427c6a58`，main）

# ① 組成（30 天窗，母體 **15 筆**，只在 applicable 的時刻）

```
drive **0.942** → ×weight → **0.481** → ×coeff → **0.347** → ×failure → 末端 **0.347**
逐筆（前 3）：{drive 1.000, weight 0.517, coeff 0.677, fail 1.000, final 0.350} …（0.357／0.364）
★掉最多的兩步：`weight`（×0.517）與 `coeff`（×0.69–0.71）
★★而兩者都是**既有機制**：`weight("shelter")` ＝ 人格調變／`coeff` ＝ `NeedHierarchy` 全 23 option 共用的同一把尺
★★★`fail_mult` ＝ **1.000** ⇒ 沒有失敗記憶在壓它 ⇒ **沒有異常壓扁**
```

# ② 它輸給誰（60 天窗，候選 **515**、贏 **0**）

```
名次：第3×5、第4×9、**第7×256、第8×142、第9×103**
輸給：**返家補給×302**、**maintain_tools:resource×180**、貿易×12、乞食×12、遷移找糧×5、徵收×2、外交×1、迎戰×1
並排：{tick 11280, team 32, 贏家 貿易 0.624 vs 收留 0.350, rank 3/8}
```

# ③ ★★★判進 (a)，而我把三個理由並排（★不是靠印象）

```
①**沒有異常壓扁**（§①）
②**末端 0.35 vs 贏家 0.62 ＝ 同一把尺上的 1.8 倍**，不是數量級
  ⇒ ★★對照：今天那顆假單價是**差 500 倍**（14.6 ms vs 31.9 us）—— **量綱病長那個樣子，這裡沒有**
③**它輸給的是補給／維持類**（返家補給／maintain_tools）⇒ ★領主在忙**自己的供給**
⇒ ★★所以這不是缺陷，而**把它 crank 到會贏才是** —— 我一個字都沒動（drive／weight／常數全原樣）
```

# ④ 現況與下一步

```
★驗收①（母體）綠：60 天窗房客 6 支｜★★驗收②（stub 歸零）**紅**：6 支**全部 ←stub**、動詞造出 0 個登記
  ⇒ 照你的裁定：**stub 保持開著、②保持紅**，直到動詞真的贏過一次
★而「動詞要怎樣才會贏」目前**不是我能裁的**：它要嘛等世界變（領主不缺補給的時刻），
  要嘛等 blueprint 決定「收人」在這個世界的價值排序 —— ★★兩者都不在本票範圍。
★★★另：**>2 秒幀數**那顆計數我已接好（`SimRunner.frames_over_budget`，永遠開著、每日 `[TickPerf]` 印）
  ⇒ ★本檔沒有那個數字（計數是在這兩趟之後才接的）——**下一份交件起一定帶**。
```
