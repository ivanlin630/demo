# 個人 NPC AI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 實裝 PersonData 的記憶寫入、relations 更新、個人目標（生成/觸發/干預/轉移）完整邏輯。

**Architecture:** 新建 `NpcAiSystem` 類別集中所有個人 NPC AI 邏輯；在現有事件（loot、aided_in_battle 等）觸發點呼叫 `write_memory`；`check_goal_alignment` 由 team-ai-redesign 的 task 影響 loyalty 時呼叫。

**Tech Stack:** Godot 4.2.2 GDScript

**依賴：** `2026-05-27-data-structure-update.md` 完成（salary、coin、relations、goals dict 格式、memory）。

---

## File Structure

| 動作 | 檔案 |
|---|---|
| Create | `scripts/simulation/npc_ai_system.gd` |
| Modify | `scripts/simulation/interaction_system.gd` |
| Modify | `scripts/simulation/salary_system.gd` |
| Modify | `scripts/simulation/sim_runner.gd` |
| Modify | `scripts/debug/headless_test.gd` |

---

### Task 1: NpcAiSystem — write_memory 與 relations 更新

**Files:**
- Create: `scripts/simulation/npc_ai_system.gd`

- [ ] **Step 1: 建立 npc_ai_system.gd**

```gdscript
# scripts/simulation/npc_ai_system.gd
class_name NpcAiSystem

const MEMORY_MAX: int = 20   # TEST VALUE

func write_memory(p: PersonData, type: String, subject_id: int,
        tick: int, intensity: float) -> void:
    p.memory.append({
        "type": type, "subject_id": subject_id,
        "tick": tick, "intensity": intensity,
    })
    _trim_memory(p)
    _update_relations(p, type, subject_id, intensity)
    _trigger_goals(p, type, subject_id)

func _trim_memory(p: PersonData) -> void:
    while p.memory.size() > MEMORY_MAX:
        p.memory.pop_front()

func _update_relations(p: PersonData, type: String,
        subject_id: int, intensity: float) -> void:
    if subject_id == -1: return
    var delta: float
    match type:
        "betrayal":          delta = -intensity * 0.8
        "kindness":          delta =  intensity * 0.4
        "master":            delta =  intensity * 0.5
        "witnessed_atrocity":delta = -0.1
        "looted":            delta = -intensity * 0.6
        "extorted":          delta = -intensity * 0.5
        "aided_in_battle":   delta =  intensity * 0.5
        _:                   delta = 0.0
    var cur: float = float(p.relations.get(subject_id, 0.0))
    p.relations[subject_id] = clampf(cur + delta, -1.0, 1.0)
```

- [ ] **Step 2: headless_test.gd 加驗證**

```gdscript
var _npc_sys := NpcAiSystem.new()
var _mp: PersonData = state.persons.get(1)
_npc_sys.write_memory(_mp, "looted", 0, 0, 0.7)
assert(_mp.memory.size() > 0, "memory 應有記錄")
assert(_mp.memory[0]["type"] == "looted", "記憶 type 應為 looted")
assert(float(_mp.relations.get(0, 0.0)) < 0.0, "relations[0] 應為負值")
print("[NpcAI] write_memory/relations 驗證通過")
```

- [ ] **Step 3: 執行測試**

```powershell
.\tools\godot\Godot_v4.2.2-stable_win64_console.exe --headless --script scripts/debug/headless_test.gd 2>&1 | Select-String "NpcAI|SCRIPT ERROR|DONE"
```

- [ ] **Step 4: Commit**

```powershell
git add scripts/simulation/npc_ai_system.gd scripts/debug/headless_test.gd
git commit -m "feat(npc-ai): add write_memory and relations update"
```

---

### Task 2: NpcAiSystem — 目標系統（生成/觸發）

**Files:**
- Modify: `scripts/simulation/npc_ai_system.gd`

- [ ] **Step 1: 加入 _trigger_goals、_activate_goal、generate_birth_goals**

在 `npc_ai_system.gd` 加：

```gdscript
func _trigger_goals(p: PersonData, type: String, subject_id: int) -> void:
    match type:
        "betrayal", "looted", "extorted":
            _activate_goal(p, "revenge", subject_id)
        "kindness", "aided_in_battle":
            _activate_goal(p, "gratitude", subject_id)
        "master":
            _activate_goal(p, "protect", subject_id)

func _activate_goal(p: PersonData, goal_type: String, target_id: int) -> void:
    for g in p.goals:
        if g["type"] == goal_type and g["target_id"] == target_id:
            g["active"] = true
            return
    p.goals.append({ "type": goal_type, "target_id": target_id, "active": true })

func generate_birth_goals(p: PersonData) -> void:
    if p.values.get("貪婪",  0.5) > 0.6:
        p.goals.append({ "type": "wealth",      "target_id": -1, "active": true })
    if p.values.get("求生欲",0.5) > 0.6:
        p.goals.append({ "type": "escape_war",  "target_id": -1, "active": true })
    if p.values.get("野心",  0.5) > 0.65:
        p.goals.append({ "type": "domination",  "target_id": -1, "active": true })
    if p.values.get("好戰",  0.5) > 0.55:
        p.goals.append({ "type": "merit",       "target_id": -1, "active": true })
    if p.values.get("義氣",  0.5) > 0.65:
        p.goals.append({ "type": "peace",       "target_id": -1, "active": true })
```

- [ ] **Step 2: headless_test.gd 加驗證**

```gdscript
var _gp: PersonData = PersonData.new()
_gp.values["貪婪"] = 0.8
NpcAiSystem.new().generate_birth_goals(_gp)
assert(_gp.goals.size() > 0, "birth goals 應生成")
assert(_gp.goals[0]["type"] == "wealth", "高貪婪應生成 wealth 目標")

var _npc2 := NpcAiSystem.new()
var _rp: PersonData = state.persons.get(1)
_npc2.write_memory(_rp, "looted", 0, 1, 0.7)
var _has_revenge: bool = false
for g in _rp.goals:
    if g["type"] == "revenge" and g["active"]: _has_revenge = true
assert(_has_revenge, "looted 記憶應觸發 revenge 目標")
print("[NpcAI] 目標生成/觸發驗證通過")
```

- [ ] **Step 3: 執行測試**

```powershell
.\tools\godot\Godot_v4.2.2-stable_win64_console.exe --headless --script scripts/debug/headless_test.gd 2>&1 | Select-String "NpcAI|SCRIPT ERROR|DONE"
```

- [ ] **Step 4: Commit**

```powershell
git add scripts/simulation/npc_ai_system.gd scripts/debug/headless_test.gd
git commit -m "feat(npc-ai): add goal generation, activation, and birth goals"
```

---

### Task 3: NpcAiSystem — 目標衝突、干預、轉移

**Files:**
- Modify: `scripts/simulation/npc_ai_system.gd`

- [ ] **Step 1: 加入 check_goal_alignment**

```gdscript
func check_goal_alignment(p: PersonData, task: String) -> float:
    var delta: float = 0.0
    for g in p.goals:
        if not g.get("active", false): continue
        delta += _goal_task_delta(g["type"], task)
    return delta

func _goal_task_delta(goal_type: String, task: String) -> float:
    match goal_type:
        "wealth":
            if task in ["貿易", "生產", "製造", "掠奪"]: return 0.005
        "escape_war":
            if task in ["攻擊", "掠奪"]: return -0.015
            if task in ["逃跑", "rest", "貿易"]: return 0.005
        "domination":
            if task in ["攻擊", "掠奪"]: return 0.005
        "merit":
            if task in ["攻擊"]: return 0.005
        "peace":
            if task in ["攻擊", "掠奪"]: return -0.01
            if task in ["外交", "貿易"]: return 0.005
        "revenge":
            if task in ["攻擊", "掠奪"]: return 0.005
        "gratitude":
            if task in ["外交", "貿易"]: return 0.003
        "protect":
            if task in ["攻擊"]: return 0.008
    return 0.0
```

- [ ] **Step 2: 加入 get_goal_task_override**

```gdscript
func get_goal_task_override(state: WorldState, p: PersonData) -> String:
    for g in p.goals:
        if not g.get("active", false): continue
        match g["type"]:
            "protect":
                var tgt: PersonData = state.persons.get(g["target_id"])
                if tgt != null and tgt.team_id != p.team_id:
                    return "move_to_protect"
            "revenge":
                var tgt: PersonData = state.persons.get(g["target_id"])
                if tgt != null and tgt.team_id != p.team_id:
                    if randf() < p.values.get("好戰", 0.5) * 0.3:
                        return "攻擊"
            "escape_war":
                var team: TeamData = state.teams.get(p.team_id)
                if team != null and float(team.resources.get("food", 0)) < 20:
                    return "逃跑"
    return ""
```

- [ ] **Step 3: 加入 cleanup_goals 與 _find_revenge_redirect、_fallback_birth_goal**

```gdscript
func cleanup_goals(state: WorldState, p: PersonData) -> void:
    for g in p.goals:
        if g["target_id"] == -1: continue
        if not state.persons.has(g["target_id"]):
            match g["type"]:
                "revenge":
                    var new_target: int = _find_revenge_redirect(state, p, g["target_id"])
                    if new_target != -1:
                        g["target_id"] = new_target
                    else:
                        g["type"] = _fallback_birth_goal(p)
                        g["target_id"] = -1
                "gratitude", "protect":
                    g["type"] = _fallback_birth_goal(p)
                    g["target_id"] = -1

func _find_revenge_redirect(state: WorldState, p: PersonData,
        dead_id: int) -> int:
    for tid in state.teams:
        var t: TeamData = state.teams[tid]
        if t.leader_id == dead_id or dead_id in t.named_members:
            if t.leader_id != p.id:
                return t.leader_id
    return -1

func _fallback_birth_goal(p: PersonData) -> String:
    var candidates: Array = [
        { "type": "wealth",     "value": p.values.get("貪婪",  0.5) },
        { "type": "escape_war", "value": p.values.get("求生欲",0.5) },
        { "type": "domination", "value": p.values.get("野心",  0.5) },
        { "type": "merit",      "value": p.values.get("好戰",  0.5) },
        { "type": "peace",      "value": p.values.get("義氣",  0.5) },
    ]
    candidates.sort_custom(func(a, b): return a["value"] > b["value"])
    return candidates[0]["type"]
```

- [ ] **Step 4: headless_test.gd 加驗證**

```gdscript
var _npc3 := NpcAiSystem.new()
var _cp: PersonData = state.persons.get(1)
# 確保有 revenge goal 且 active
_npc3._activate_goal(_cp, "revenge", 9)
var _align: float = _npc3.check_goal_alignment(_cp, "逃跑")
assert(_align < 0.0 or _align == 0.0, "revenge+逃跑不衝突（返回 0 或負）")
var _align2: float = _npc3.check_goal_alignment(_cp, "攻擊")
assert(_align2 > 0.0, "revenge+攻擊應 aligned（> 0）")
print("[NpcAI] check_goal_alignment 驗證通過")
```

- [ ] **Step 5: 執行測試**

```powershell
.\tools\godot\Godot_v4.2.2-stable_win64_console.exe --headless --script scripts/debug/headless_test.gd 2>&1 | Select-String "NpcAI|SCRIPT ERROR|DONE"
```

- [ ] **Step 6: Commit**

```powershell
git add scripts/simulation/npc_ai_system.gd scripts/debug/headless_test.gd
git commit -m "feat(npc-ai): add goal alignment, override, cleanup with fallback"
```

---

### Task 4: 整合到 SimRunner 與現有事件

**Files:**
- Modify: `scripts/simulation/sim_runner.gd`
- Modify: `scripts/simulation/interaction_system.gd`
- Modify: `scripts/simulation/salary_system.gd`

- [ ] **Step 1: sim_runner.gd 加入 NpcAiSystem**

```gdscript
var _npc_ai_system: NpcAiSystem
# 在 _init() 加
_npc_ai_system = NpcAiSystem.new()
```

在 `_step7_person_reactions` 後加：
```gdscript
_step7b_npc_goal_cleanup(state, near_teams)
```

```gdscript
func _step7b_npc_goal_cleanup(state: WorldState, team_ids: Array) -> void:
    for tid in team_ids:
        var team: TeamData = state.teams.get(tid)
        if team == null: continue
        for pid in ([team.leader_id] as Array) + team.named_members:
            var p: PersonData = state.persons.get(pid)
            if p: _npc_ai_system.cleanup_goals(state, p)
```

- [ ] **Step 2: interaction_system.gd — 在 loot 後寫記憶**

找到 loot/掠奪結算的地方（looting team 對 target team），加：

```gdscript
var _npc_ai := NpcAiSystem.new()
var _victim_team: TeamData = state.teams.get(target_id)
if _victim_team:
    for pid in ([_victim_team.leader_id] as Array) + _victim_team.named_members:
        var vp: PersonData = state.persons.get(pid)
        if vp:
            _npc_ai.write_memory(vp, "looted", looting_team.leader_id,
                state.world.current_tick, 0.7)
```

- [ ] **Step 3: interaction_system.gd — 在戰鬥支援後寫記憶**

找到 InteractionSystem 結算戰鬥勝利（absorb/戰敗），在己方勝利後加：

```gdscript
# 支援者記憶（被支援的 team 全員）
var _npc_ai2 := NpcAiSystem.new()
for pid in ([supported_team.leader_id] as Array) + supported_team.named_members:
    var sp: PersonData = state.persons.get(pid)
    if sp:
        _npc_ai2.write_memory(sp, "aided_in_battle", supporter_leader_id,
            state.world.current_tick, 0.5)
```

- [ ] **Step 4: salary_system.gd — 薪水超付寫 kindness 記憶**

在 `_pay_salary` 的 `ratio >= 1.0` 分支加：

```gdscript
if ratio >= 1.0 and ratio > 1.2:   # 超付 20% 以上才算恩惠
    NpcAiSystem.new().write_memory(p, "kindness", team.leader_id,
        0, (ratio - 1.0) * 0.5)
```

- [ ] **Step 5: 執行測試**

```powershell
.\tools\godot\Godot_v4.2.2-stable_win64_console.exe --headless --script scripts/debug/headless_test.gd 2>&1 | Select-String "NpcAI|SCRIPT ERROR|DONE"
```

- [ ] **Step 6: Commit**

```powershell
git add scripts/simulation/sim_runner.gd scripts/simulation/interaction_system.gd scripts/simulation/salary_system.gd
git commit -m "feat(npc-ai): integrate memory writes into loot, combat, salary events"
```
