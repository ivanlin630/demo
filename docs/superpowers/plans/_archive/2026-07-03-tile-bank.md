# S1 tile-bank — Plan

> Spec：`docs/superpowers/specs/2026-07-03-tile-bank-design.md`（先整份讀,含判斷器盤點紀律）。
> commit 結構:①TileBank+30 站點收編（純 refactor,pointwise CLEAN）②mint-cap 燒 ore 修（行為修,單獨）③anon_treasury off-map ledger（行為修,單獨）④docs。

## Task 1 — TileBank + 站點收編
1. 新 `tile_bank.gd`（`class_name TileBank`,static;**跑 `--import` 重建快取**）:`deposit/withdraw/set_amt(tile,res,amt,reason)`,cap clamp 單點（吃 `_get_storage_cap` 邏輯,outpost 原函數委派或搬家）;`tile.resources` 自然池同傘。
2. 30 直寫站點收編（investigator 已驗:resource_system/outpost/manufacturing/interaction/faction_ai/harvest/ambush/encounter…grep `public_storage\[`+`\.resources\[.*\] =` on tiles 全掃,勿漏）。reason 命名沿 ResourceBank 慣例。CI-scan pattern 註解附。
3. headless 測:cap clamp 等價、deposit/withdraw 守恆、殘量 grep=0。
4. **pointwise CLEAN×3 seed（1337/42/7×3月）必須**——clamp/順序位元等價,不 CLEAN 即語意破打回。

## Task 2 — mint 守恆 connect + mint-cap 修（單獨 commit）
1. mint 產 coin 走 TileBank+CoinAudit minted 軌（slice1 ledger 既有）。
2. 滿 cap:跳過鑄造不燒 ore（或按可鑄量部分耗 ore,實作選簡）。測:滿 cap 前後 ore/coin 帳平。
3. CoinAudit 等式 `after=before+minted` 全池綠。

## Task 3 — anon_treasury off-map ledger（單獨 commit）
`_route_extinct_assets` `_nearest_valid_tile` 全無 → 記 ledger sink（reason=`extinct_no_tile`,顯性非靜默;或擴半徑,選侵入小者）。測:off-map 滅團 coin 有軌。

## Task 4 — 驗收
回歸:headless（1 FAIL pre-existing 容忍）+0 SCRIPT ERROR、framework 7/7（S5 mint 魂）、CoinAudit delta=0+minted 正確、InvariantAudit 0。pointwise:Task1 commit CLEAN;Task2/3 行為修段月線 sanity。

## Handback
`2026-07-03-tile-bank.md`:站點數前後、pointwise 證、mint/off-map 修影響量、CI-scan pattern、殘量清單（facility levels/occupied_by 等另型欄）。

## 注意
Godot wrapper;`--import` 新 class 後必跑;pointwise bed 2500 背景輸出落檔;1 FAIL pre-existing。硬約束:banker pattern mirror 零新判斷器;禁碰 asm 值/決策函數/envoy/馬 breed。
