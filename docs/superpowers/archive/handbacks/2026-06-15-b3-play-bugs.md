# Hand Back: B3 玩測 bug 批修

分支：`feat/b3-play-bugs`（3 commits，未 merge）

## 實作摘要

- `scripts/ui/encounter_view.gd`：attack_select 模式 `_lbl_actions` 改顯操作提示（新 static `_attack_select_hint`）；加 `KEY_ESCAPE` 取消回 idle（對齊提示中的「Esc取消」字樣）。
- `scripts/simulation/player_api_mapper.gd`：`map_controlled_team` 加 `armed_count`/`anon_total`；新 static `_armed_count`（named+leader + anon人口×`armed_anon_ratio`）。
- `scripts/ui/text_ui_main.gd`：`_build_state_str` 人口行加「武裝: N」。
- `scripts/simulation/encounter_system.gd`：新 `_check_player_wiped`，在 `resolve_encounter_end` 三個結算出口（normal/draw/beast）呼叫；玩家隊 population<=0 → 既有 `FactionAISystem._handle_player_leader_death`（無 named→`game_over`，有→`choose_heir` forced_event）。
- 測試：`ui_logic_test._test_attack_select_hint`、`headless_test._test_controlled_team_armed`、`headless_test._test_u10b_player_wiped`、`ui_flow_test._test_armed_count_shown`，皆已註冊。

## 驗證

- `headless_test`：`=== DONE ===`，`controlled_team armed OK` + `U10b player wiped OK`，唯一 SCRIPT ERROR = Bug8 baseline（food 應進公庫，本批不動）。
- `ui_logic_test`：errors 0（attack_select 4 PASS）。
- `ui_flow_test`：errors 0（status 含「武裝」PASS）。

## 連動風險

- `_handle_player_leader_death`：U10b 走既有玩家死亡鏈，與 health_system / faction_ai `_promote_successor` 同入口。若同 tick 既有偵測點（leader_id==-1）與 encounter 出口都觸發，第二次呼叫 `_handle_player_leader_death` idempotent（再設 game_over/覆寫同 choose_heir），無重複扣口；但若中間玩家身分已轉移則行為未測 → 屬既有 D2 議題，未擴大。
- `_check_player_wiped` 在 beast/draw 出口也呼叫（plan 只測 normal 路徑）：玩家 vs 獸全滅、draw 全滅亦會正確 game-over，無回歸風險但缺自動測。

## 待主 session 確認

- attack_select 提示 GUI 真視覺是否清楚（字長/換行）→ 標待人工 run-verify（組字/flow 已自動測）。
- `_armed_count` 為 mapper 靜態簡化（named + anon×ratio），未引 npc_combat 真戰力公式。若日後需與戰鬥實際武裝數一致，建議統一來源。
- 命令缺口（調薪/武裝anon set/成員 equip）= B4 另批；交易介面 = 功能另 spec，均不在本批。
