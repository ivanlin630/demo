# Hand Back: G1a 鑄幣觀測 + W8 驗證

branch: `feat/g1a-mint-obs`（已 push origin，未 merge）

## 實作摘要
- `scripts/simulation/outpost_system.gd`：`_tick_mint` gold/silver 兩段各加 `[Mint] tile(x,y) ore→coin +N (mint_lv=)` 觀測 log。**轉換/守恆邏輯不變**。
- `scripts/debug/headless_test.gd`：加 `_test_mint_conserving()`（端到端：mint_lv=1、ore_gold=10/ore_silver=20，跑 `_tick_mint`，assert coin>0 且 coin_eq delta<0.01），註冊於 `_initialize()`。
- `docs/known_issues.md`：W8 更正 — 鑄幣機制 ✅ 已存在且守恆（`_test_mint_conserving` 證），原「產出鏈完全休眠」= 無 log 致錯覺 + 實機建造罕見。殘留「實機鑄幣罕見」標為經濟平衡/G1c 需求驅動後觀察，非機制 bug。

## 與 spec 的差異
- 無。完全照 plan 兩 Task 執行。

## 驗證
- 新 worktree 先 `--import` 建 class 快取（fresh worktree 必跑）。
- `headless_test.gd` 全綠：`=== DONE ===`、無 SCRIPT ERROR、無 assert fail、`mint conserving OK`、`[Mint]` log 出現、各 coin 守恆測試 OK、InvariantAudit OK（population/faction/subteam 雙向）。

## 連動風險
- `_tick_mint` 只多 print，無行為變更 → 對 resource/economy/coin_eq 無連動風險。
- `[Mint]` log 在實機 sim 罕見格才印（驗證：headless 測試 + 既有 CoinStorage/coin_eq 測試各觸發一次），不會洗版。
- 無已知連動風險。

## 待主 session 確認
- 設計決策：無 spec 未覆蓋情況。
- 建議後續 task：W8 殘留「實機鑄幣頻率」屬經濟平衡——建議併入 **G1c 需求驅動生產**（需求迴路驅動蓋鑄幣廠 + 採金銀礦）後再量測，非獨立修。
