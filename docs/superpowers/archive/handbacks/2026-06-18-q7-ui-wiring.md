# Hand Back: Q7 UI Wiring（Q7-3 戰利品 / Q7-5 子隊任務 / Q7-6 faction gate）

Plan: `docs/superpowers/plans/2026-06-18-q7-ui-wiring-batch.md`
Branch: `worktree-agent-a217877a43642d97e`（隔離 worktree 既有 branch；非 `feat/q7-ui`，見「與 spec 差異」）

## 實作摘要

通用模型：暴露既有 registered command，零 sim 行為變更。

- `scripts/ui/encounter_view.gd`（Q7-3）
  - `_post_combat_hint`：`loot_pool` 非空 → 加 `[K]拿戰利品 [L]留下`。
  - post-combat 鍵處理 mirror `[J]收編`：`KEY_K`→command `take_loot`、`KEY_L`→`leave_loot`（留在畫面刷新提示，非離開）。loot 自 `last_encounter_result` 取，command 不傳參。
- `scripts/ui/text_ui_main.gd`（Q7-5 + Q7-6）
  - Q7-5：新增 `SUBTEAM_TASK_CHOICES` 常數 + static `_subteam_task_menu_str` / `_subteam_task_from_index`。`dispatch_subteam` 與 `order_subteam` UI 流程各插入「選任務」步驟，取代寫死 `TASK_IDLE`。合理子集：待機/覓食/安頓/巡邏/建設/偵查。command 介面不變（已支援任意 sub_task）。
  - Q7-6：`_build_faction_str` 非 leader 不列 `[A]設定目標 [B]調整徵收率`（僅顯 `[C]離開 [D]背叛`）；`_handle_faction_mode` `KEY_A`/`KEY_B` 非 leader no-op。`is_leader` 取自既有 `faction_panel` DTO（`leader_team_id == player_tid`），不直讀 WorldState（守 UI 邊界 invariant）。
- `scripts/debug/ui_logic_test.gd`：`_test_post_combat_hint` 加 loot hint 斷言（K/L、無 loot 不顯）。
- `scripts/debug/ui_flow_test.gd`：加三端到端斷言：
  - `_test_q7_3_take_loot_flow`：戰後 take_loot 經 bridge → 食物入庫 +30、敗隊 -30、`last_encounter_result` 清。
  - `_test_q7_5_dispatch_subteam_task`：派覓食子隊 → 經 `parent.subteam_ids` 查得 `current_task == TASK_FORAGE`（非 IDLE）。
  - `_test_q7_6_faction_gate_leader`：非 leader 面板無 `[A]/[B]`、有 `[C]`；leader 則顯 `[A]/[B]`。

Commits（逐 Task）：
1. `feat(ui): Q7-3 文字 UI 戰後 take_loot/leave_loot 接線`
2. `feat(ui): Q7-5 子隊派遣開放任務選擇（非寫死 IDLE）`（含 ui_flow_test 三斷言——`_initialize` 同時註冊三 func，需原子落地）
3. `fix(ui): Q7-6 faction 設定鈕只對 leader 顯示`

## 與 spec 差異

- **Branch 名**：plan 指定 `feat/q7-ui`，但本 session 啟動於既有隔離 worktree（branch `worktree-agent-a217877a43642d97e`），未原地 `git worktree add`（避免巢狀 worktree 撞 git）。請主 session merge 此 branch。
- **ui_flow_test 三斷言落同一 commit（Q7-5）**：因 `_initialize` 一次註冊三個 test func，拆到三 commit 會使中間 commit 編譯失敗（引用未定義 func）。故三斷言隨 Q7-5 原子落地，非各歸各 Task commit。Q7-3 commit 只含 encounter_view + ui_logic_test。
- **Q7-5 order_subteam 一併開放**：plan 說「order_subteam 若同樣寫死可一併（讀確認）」——確認其亦寫死 `TASK_IDLE`，已一併改為可選。
- **`dispatch_subteam` command 不回傳 `sub_id`**：plan 無提；測試改經 `parent.subteam_ids[-1]` 查剛派出的子隊（command 結果只含 ok/msg）。非行為變更，僅測試取值方式。

## 驗證

- `headless_test.gd`：`=== DONE ===`，無 SCRIPT ERROR。
- `ui_flow_test.gd`：`errors: 0`（含三新 Q7 斷言）。
- `ui_logic_test.gd`：loot hint 三斷言 PASS。（既有 2 個 vision dist FAIL 為 **baseline pre-existing**，已 stash 驗證與本 batch 無關。）
- `game_sim_multi.gd`：四情境（game_sim_test/tyrant/merchant/warzone）coin_eq delta=0、InvariantSummary 違反=0、無 SCRIPT ERROR。

## 連動風險

- `subteam_system` / `TaskArbiter`：Q7-5 現可派非 IDLE 高優先 task（PRIO_DISPATCH）給子隊。dispatch 對新 team 有建立豁免；`order_subteam` 走 `TaskArbiter.try_set`，已有「正忙」reject 路徑。invariant「每個高優先 task 必須有釋放條件」由既有 task 邏輯保障（覓食/巡邏等本就有釋放），本 batch 未新增 task 種類、未改釋放邏輯，風險低。建議主 session 確認所列子集（尤其 BUILD/SETTLE）在子隊 context 下釋放條件健全。
- `faction` 命令權限：Q7-6 只 gate UI display + UI handler；底層 `set_faction_goal`/`set_tribute_rate` command 仍自行驗 leader（reject）。display 現與 command 權限一致，無繞道風險。

## 待主 session 確認

- 子隊任務子集是否合適（目前：待機/覓食/安頓/巡邏/建設/偵查）。若要全 task 開放或調整子集，改 `SUBTEAM_TASK_CHOICES` 即可。
- Branch merge：本 session 未 merge main，依流程保留 branch 待主 session 處理。
