# B3 玩測 bug 批修 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修 3 個玩測 bug：attack-part 操作提示不清、自隊武裝數看不到、玩家遭遇戰全滅直接退出（應 game-over/繼承）。各配 UI-flow harness 自動測。

**Architecture:** attack-part = encounter_view 提示清晰化（純顯示）；U14b = controlled_team DTO 加武裝數欄位（仿 P1）+ status 顯示；U10b = encounter resolve 偵測玩家隊全滅 → 接既有 `_handle_player_leader_death`/game-over，非靜默退出。經既有 DTO/bridge，遵 UI 邊界。

**Tech Stack:** Godot 4.2.2 GDScript；headless + `ui_flow_test`/`ui_logic_test`；`.\tools\godot.ps1`。

依據：known_issues attack-part / U14b / U10b。命令缺口（調薪/武裝anon/成員equip）= B4 另批；交易介面 = 功能另 spec。

---

## 檔案結構

- `scripts/ui/encounter_view.gd`（改）：attack_select 模式 `_lbl_actions` 顯操作提示。
- `scripts/simulation/player_api_mapper.gd`（改）：`map_controlled_team` 加 `armed_count`/`anon_total`。
- `scripts/simulation/player_command_system.gd`（讀/借）：`calc_armed` 邏輯（或 npc_combat 既有）供 DTO 用。
- `scripts/ui/text_ui_main.gd`（改）：`_build_state_str` status 顯武裝數。
- `scripts/simulation/encounter_system.gd`（改）：`resolve_encounter_end` 玩家全滅 → game-over/heir。
- `scripts/debug/ui_flow_test.gd` / `ui_logic_test.gd`（改）：測試。

---

## Task 1: attack-part 操作提示（UX 清晰）

**Files:**
- Modify: `scripts/ui/encounter_view.gd`（`_refresh_ui` attack_select 分支）
- Test: `scripts/debug/ui_logic_test.gd`（static helper 測）

**根因**：R 進 attack_select 後，`_lbl_actions` 仍顯 idle 提示（QWEASD/R/Z…），未告知「↑↓選部位 / 移游標瞄敵 / Enter攻擊 / Esc取消」→ 玩家不知如何選部位。

- [ ] **Step 1: 寫失敗測試**

`ui_logic_test.gd` 加（提示組字抽 static helper）：
```gdscript
func _test_attack_select_hint() -> void:
	print("\n── attack_select 提示 ──")
	var EncView = load("res://scripts/ui/encounter_view.gd")
	var h: String = EncView._attack_select_hint("torso")
	_check("含 ↑↓選部位", h.contains("↑") and h.contains("部位"))
	_check("含 Enter 攻擊", h.contains("Enter") and h.contains("攻擊"))
	_check("含 Esc 取消", h.contains("Esc"))
	_check("顯當前部位", h.contains("torso"))
```

- [ ] **Step 2: 跑確認失敗** — helper 未定義。

- [ ] **Step 3: 實作**

`encounter_view.gd` 加 static helper：
```gdscript
static func _attack_select_hint(part: String) -> String:
	return "↑↓選部位:%s  移游標瞄敵  Enter攻擊  Esc取消" % part
```
`_refresh_ui` 的 attack_select 分支（現 line ~157）改：
```gdscript
	if _mode == "attack_select":
		_lbl_actions.text = _attack_select_hint(_selected_part)
		_lbl_cursor_info.text = "攻擊部位 ↑↓: %s" % _selected_part
```

- [ ] **Step 4: 跑確認通過** — `attack_select hint OK`
- [ ] **Step 5: Commit**

```bash
git add scripts/ui/encounter_view.gd scripts/debug/ui_logic_test.gd
git commit -m "fix(ui): attack_select 顯操作提示（↑↓選部位/瞄/Enter/Esc）"
```

---

## Task 2: U14b 自隊武裝數（DTO + status 顯示）

**Files:**
- Modify: `scripts/simulation/player_api_mapper.gd`（`map_controlled_team`）
- Modify: `scripts/ui/text_ui_main.gd`（`_build_state_str`）
- Test: `scripts/debug/headless_test.gd` + `ui_flow_test.gd`

**根因**：controlled_team DTO 無武裝數 → UI 無從顯。

- [ ] **Step 1: 寫失敗測試（DTO）**

`headless_test.gd`：
```gdscript
func _test_controlled_team_armed() -> void:
	print("--- controlled_team 武裝數 ---")
	var state := WorldState.new(); state.world = WorldData.new()
	var leader := PersonData.new(); leader.id = 0; leader.team_id = 0
	state.persons[0] = leader; state.player_id = 0
	var team := TeamData.new(); team.team_id = 0; team.leader_id = 0
	team.population = 10; team.armed_anon_ratio = 0.5
	team.anon_tiers = {"平民": 9}
	team.resources = {"weapon_melee_low": 5}
	state.teams[0] = team
	var ct: Dictionary = PlayerApiMapper.map_controlled_team(state)
	assert(ct.has("armed_count"), "DTO 應有 armed_count")
	assert(ct.has("anon_total"), "DTO 應有 anon_total")
	assert(int(ct["armed_count"]) >= 0, "armed_count 非負")
	print("controlled_team armed OK")
```

- [ ] **Step 2: 跑確認失敗**

- [ ] **Step 3: 實作**

`map_controlled_team` 回傳加：
```gdscript
		"anon_total": AnonTierSystem.total_pop(t),
		"armed_count": _armed_count(t),
```
helper（武裝數 = named 持武 + anon×armed_anon_ratio，仿 npc_combat.calc_armed 概念，但 mapper 靜態簡化）：
```gdscript
static func _armed_count(t: TeamData) -> int:
	var anon_total: int = AnonTierSystem.total_pop(t)
	var named_count: int = t.named_members.size() + (1 if t.leader_id != -1 else 0)
	var anon_pop: int = maxi(t.population - named_count, 0)
	return named_count + roundi(float(anon_pop) * float(t.armed_anon_ratio))
```
確認 `AnonTierSystem.total_pop` 存在（既有）；若 mapper 不便引 AnonTierSystem，改 `t.population` 推算。

- [ ] **Step 4: 跑確認通過** — `controlled_team armed OK`

- [ ] **Step 5: status 顯示 + flow 測試**

`text_ui_main._build_state_str` 人口行加武裝：
```gdscript
	lines.append("人口: %d  武裝: %d" % [ct.get("population", 0), ct.get("armed_count", 0)])
```
`ui_flow_test.gd` 加：
```gdscript
func _test_armed_count_shown() -> void:
	print("\n── 自隊武裝數顯示 ──")
	var node = await _make_ui()
	node._refresh()
	_check("status 含「武裝」", node._state_label.text.contains("武裝"))
	await _free_ui(node)
```

- [ ] **Step 6: Commit**

```bash
git add scripts/simulation/player_api_mapper.gd scripts/ui/text_ui_main.gd scripts/debug/headless_test.gd scripts/debug/ui_flow_test.gd
git commit -m "feat(ui): U14b 自隊武裝數（DTO armed_count + status 顯示）"
```

---

## Task 3: U10b 玩家遭遇戰全滅 → game-over/繼承

**Files:**
- Modify: `scripts/simulation/encounter_system.gd`（`resolve_encounter_end`）
- Test: `scripts/debug/headless_test.gd`

**根因**：玩家隊在遭遇戰全滅 → 無 game-over/heir 處理 → 靜默退出。

- [ ] **Step 1: 調查接點**

讀 `resolve_encounter_end` + `faction_ai._handle_player_leader_death`（既有玩家 leader 死處理）/ `_promote_successor`。確認：玩家隊全滅（population<=0 或 leader_id 對應 player 死）時應呼叫哪個（game_over flag / choose_heir forced event）。記接點。

- [ ] **Step 2: 寫失敗測試**

```gdscript
func _test_u10b_player_wiped() -> void:
	print("--- 玩家遭遇戰全滅 → game-over/heir ---")
	var state := WorldState.new(); state.world = WorldData.new()
	var leader := PersonData.new(); leader.id = 0; leader.team_id = 0
	state.persons[0] = leader; state.player_id = 0
	var pteam := TeamData.new(); pteam.team_id = 0; pteam.leader_id = 0; pteam.population = 0   # 全滅
	pteam.named_members = []
	state.teams[0] = pteam
	state.encounter_attacker_id = 9; state.encounter_defender_id = 0
	var enc := EncounterSystem.new()
	enc.resolve_encounter_end(state, "attacker_win")   # 玩家(defender)敗
	# 玩家隊全滅 + 無 named → game_over（非靜默）
	assert(state.game_over or state.player_forced_event.get("action","") == "choose_heir",
		"玩家全滅應 game_over 或 choose_heir，實際 game_over=%s fe=%s" % [state.game_over, str(state.player_forced_event)])
	print("U10b player wiped OK")
```

- [ ] **Step 3: 跑確認失敗**

- [ ] **Step 4: 實作**

`resolve_encounter_end` 結算後，加玩家全滅檢查：
```gdscript
	# U10b: 玩家隊全滅 → 接既有玩家死亡鏈（繼承/game-over），非靜默退出
	if state.player_id != -1:
		var pp = state.persons.get(state.player_id)
		if pp != null:
			var pt: TeamData = state.teams.get(pp.team_id)
			if pt != null and pt.population <= 0:
				FactionAISystem.new()._handle_player_leader_death(state, pt)
```
（依 Step 1 調查的真實接點對齊；若 `_handle_player_leader_death` 簽名/職責不符，用其揭示的正確 game-over/heir 入口。）

- [ ] **Step 5: 跑確認通過** — `U10b player wiped OK`
- [ ] **Step 6: Commit**

```bash
git add scripts/simulation/encounter_system.gd scripts/debug/headless_test.gd
git commit -m "fix: U10b 玩家遭遇戰全滅 → 接玩家死亡鏈（game-over/繼承）"
```

---

## Task 4: 註冊 + 驗證

- [ ] **Step 1: 註冊** 新測試於各 test 檔 `_initialize`。
- [ ] **Step 2: 全跑** — headless_test / ui_logic_test / ui_flow_test 無新增 SCRIPT ERROR（Bug8 baseline 除外）、新測試全綠。
- [ ] **Step 3: handback** — `docs/superpowers/handbacks/2026-06-15-b3-play-bugs.md`。GUI 真視覺（attack 提示是否清楚）標待人工 run-verify（flow/DTO 已自動測）。

---

## 注意事項（給實作者）

- **harness 用起來**：能 headless 測的（DTO/flow/提示組字）一律加 `ui_flow_test`/`ui_logic_test`，不要只靠人工。
- **U10b 先調查接點**（Step 1）：讀 `_handle_player_leader_death` 真實職責，對齊正確 game-over/heir 入口，勿臆造。
- **UI 邊界**：text_ui 經 bridge/DTO，勿直存 WorldState。
- **AnonTierSystem.total_pop** 確認存在（armed_count 用）；不便引則用 population 推算。
- 命令缺口（調薪/武裝anon set/成員 equip）= B4，不在本批。交易介面 = 功能 spec，不在本批。
- baseline Bug8（food 應進公庫）勿動。
