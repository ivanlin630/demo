# S5：背景律三條（HOW）—— ★三條【各自可回退】，而我要求【各自單獨落地】

**上游**：`2026-08-20-time-reanchor-tier-design.md` §3b（WHAT，用戶逐條拍 2026-08-20）
**LOCKED slice 表**：`| S5 | 背景律三條（季節曲線取代 6h 亂擲／池再生加季節／飢餓疲勞減半）| intended | 三條各自可回退 |`

## ★★★★★開工前擋下的兩件（★都是【結構】問題，不是實作問題）

### ①★季節曲線的效果，在標準床上【結構上量不到】
```
SEASON_LENGTH = TICKS_PER_SEASON = 90 天  ／  標準床窗 = 30 天
⇒ ★★30 / 90 = 0.33 個季節 ⇒【看不到任何一次季節轉換】
```
★**若把「季節曲線生效」寫成世界層驗收 ⇒ 那是【第六種不可達】（結構上不可能達成）。**
⇒ ★★**處置：拆成【函數層】與【世界層】兩種驗收** —— **季節曲線是純函數，直接測函數，不需要跑世界。**

### ②★★★刪掉 `randf_range` 不是小改動 —— **它會重擲整個世界**
```
harvest_system.gd:18  tile.harvest_factor = clampf(base + randf_range(-0.25, 0.25), 0.1, 2.0)
★每 tile、每次 tick_all 消耗一次 global RNG ⇒ ★★刪掉它 = 之後【每一顆骰子都換人擲】
⇒ ★★★fp 必變,而且是【世界大幅分岔】不是「某個數字動了」
```
★**⇒ 效果量測【不能】用「同 seed 前後比某隊發生什麼」** —— **那在分岔之後沒有意義**（今日血證：S2 隊數 112→125）。
★★**只能用【函數層】與【分佈層】。**

## ★scope：**三條各自單獨落地、各自量**（★LOCKED 說「各自可回退」，我把它推到「各自單獨交件」）
> ★★**理由**：**①會重擲世界、③會改飢餓/疲勞的絕對速度** —— **一起交 ＝ 兩個大變化夾在同一次 `fp` 變裡，歸因不可能。**
> ★★★**今天已經吃過這個虧**（S2 殘差義務：「夾著兩刀，分不開 S3 與 S4b」）。

| 條 | 內容 | 交件 |
|---|---|---|
| **S5a** | ★**季節曲線取代 6h 亂擲**：刪 `randf_range`；`SEASON_BASE` 四個離散值 → **相鄰季節間插值** | 單獨 |
| **S5b** | **tile 池再生 × 季節因子**（★野地幅度【比農田緩】） | 單獨 |
| **S5c** | ★**飢餓／疲勞時間減半** | 單獨 |

---

## ★★S5a：季節曲線
```
現況:season = (tick / SEASON_LENGTH) % 4 ；base = SEASON_BASE[season]（★階梯,四個離散值）
     tile.harvest_factor = clampf(base + randf_range(-0.25, 0.25), 0.1, 2.0)
要的:①刪 randf_range ②base 改成【平滑】—— 相鄰季節之間依季內進度插值
★★★而【不得引入新的魔術常數】:插值只用既有 SEASON_BASE 的四個值 + 季內進度 t∈[0,1)
```
★**驗收（★函數層為主，世界層只驗「亂擲真的沒了」）**：
```
①★函數層(不跑世界):給定 tick 序列 ⇒ ★★factor 必須【連續】(相鄰 tick 差 <= 一個可推導的上界)
   ★★★且【季界不得跳變】:t→1 與下一季 t→0 的值必須相等（★這是插值正確的字面量）
②★~~世界層:同一 tile、同一季內、不同 tick ⇒ factor 必須【相同】~~ ⛔**RETRACTED 2026-09-01**
   ★★**它與要求①【內部矛盾】**:插值就是要 factor 在季內變,而我從舊的階梯設計borrow了這條
   ★**替代（implementer 提，我裁採用，而它比我的好）**:**同一 tick、不同 tile ⇒ factor 完全相同**
      ⇒ ★★★舊寫法每 tile 獨立擲一次 randf ⇒ 同 tick 各 tile 必然不同
        ⇒ 它是「亂擲移除」的【字面量】,而且與插值【相容】
   ★**設計未動**:要求①③④⑤ 一字未改;動的只有這一條驗收
③★★fp 必變(RNG 流位移)⇒ ★★★而【不得】用世界層前後比較做效果量 —— 分岔了
④三跑 byte-identical（★刪 RNG 消費不影響決定性,但要驗）
```

## ★★S5b：池再生 × 季節
```
現況:REGEN_RATE(resource_system.gd:42) ／ regenerate_tiles(:129, cadence=TICKS_PER_DAY)
要的:野地池再生 × 季節因子,★而【幅度比農田緩】(野生態有緩衝)
★★★而「比農田緩」需要一個【新係數】—— WHAT 自己標「試跑+平衡警告」
   ⇒ ★它必須明標 provisional,且【單獨一個常數】(可回退)
```
★**驗收**：**①函數層：同一季、農田 vs 野地的季節乘數比 ＝ 明寫的那個係數 ②世界層效果需長窗 ⇒ ★明標【本輪不量】。**

## ★★S5c：飢餓／疲勞減半（★最單純的一條，而它是【純數值】）
```
現況:resource_system.gd:38 HUNGER_GAIN_PER_DAY   = 0.05   ⇒ 20 天滿
     resource_system.gd:39 HUNGER_RECOVER_PER_DAY = 0.1    ⇒ 10 天
     sim_runner.gd:9  FATIGUE_PER_DAY          = 0.048  ⇒ ~20.8 天
     sim_runner.gd:10 FATIGUE_RECOVERY_PER_DAY = 0.24   ⇒ ~4.2 天
要的(WHAT 原文):飢餓 20→10 天滿(恢復 10→5)、疲勞 21→10.5(紮營恢復 4.2→2.1)
⇒ ★四顆【各 ×2】,而★★它【保住】「恢復比積累快」的比例(1:2 不變)——WHAT 明寫要保
```
★**驗收**：**①算術：四顆的「到滿值天數」各自減半（可直接算，不必跑）②世界層：餓死／疲勞事件的發生時點分佈前移
★★③而【不得】宣稱「戲更好看」——那是新基線考的事，不是這一票的。**

## ★★★誠實限
- ★**S5a 的「平滑」只保證【函數連續】** —— ★★**它不保證「戲更好看」，那要新基線考憑質地判**（WHAT 自己說試跑）。
- ★**S5b 的係數是 provisional** —— ★★**而它是這一票唯一的新常數，回退＝改那一顆。**
- ★★★**三條都【不得】用「同 seed 前後比某隊」做效果證明** —— **S5a 一落地世界就分岔。**
