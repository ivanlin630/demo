---
slice: 市集會自我廣告（`feat/market-ads`）
owner: systems
status: draft — 待 R②
基於: blueprint 裁 2026-09-18（`…-blueprint-to-systems-markets-advertise-themselves.md`）
---

# §0 WHAT 的裁定（逐字，不改寫）

> ★裁：**(b)** —— 給貿易合法發現管道，形狀照你畫的：**傳聞可帶【那裡有市集】的交易專屬地點記錄，
> 不帶城的規模／歸屬／軍情細節**。
> ★★**原則入帳：知識的細節等級跟著【傳播動機】走** —— 市集**想被知道**（廣告是它存在的方式），軍情**想被藏**；
> **對打仗嚴、對做生意鬆不是雙標，是兩種知識的物理本來不同。**
> ★消費者清單 ＝ **交易目標選擇 ＋ 玩家市場板**（兩個，不破軸規）。
> ★★★**成對反事實 ＝ 軍事目標選擇【不得因此多看見任何東西】。**

# §1 現況（★管線已經有一半，這一票是接上另一半）

```
belief_system.gd:386-390  msg_market_pos(msg)：order_buy／order_sell ⇒ origin_pos；outpost_built ⇒ source_pos
belief_system.gd:329-333  harvest_tile_known 的 relay 路徑：把那些 pos 寫成【見過這塊地】
★★所以「聽說有那個地方」已經會進 team_tile_known —— ⇒ 缺的是【那塊地上有什麼】
★★★而我們自己立的規矩是：relay 來的 tile【不得】帶據點子記錄（軍情）
⇒ 本票加的是一個【交易專屬】的子記錄，語意與據點子記錄【不同軸】
```

# §2 形狀

```
known[tid] = {
   "outpost": {owner_id, level, last_tick},   ← ★親見才寫（既有，不動）
   "market":  {last_tick},                    ← ★★本票新增：relay 也可以寫
}
★market 子記錄【只說「這裡可以交易」】：不帶 owner、不帶 level、不帶任何規模
★★寫入端：relay（msg_market_pos 命中）⇒ 寫 market；★親見市集 ⇒ 也寫 market
★★★讀取端只有兩個（blueprint 的清單）：_find_trade_partner ／ 玩家市場板
```

# §3 驗收（★每格能紅；★★成對反事實是硬格）

| 格 | 內容 | 反向 |
|---|---|---|
| 2-a | ★**傳聞給得到交易目標**：只透過 relay 知道 `(x,y)` 的觀察者 ⇒ **交易候選含它** | 不寫 market ⇒ 不含 ⇒ 紅 |
| 2-b | ★★**傳聞【不】給軍情**：同一個觀察者對該地的 `outpost` 子記錄 **仍然不存在** | relay 寫了 outpost ⇒ 紅 |
| 2-c | ★★★**成對反事實（WHAT 明令）**：**軍事目標選擇的候選集，加本票前後【逐字相同】** | 多一筆 ⇒ 紅 |
| 2-d | ★**存在性回升多少**：世界級，商隊找得到交易對象的觀察者數（★對照 merge 前的 **12** 與拆 god-view 前的 **94**） | ★★見 §4 誠實限 |
| 2-e | ★**玩家市場板**只讀 market 子記錄，**不因此看到規模／歸屬** | 讀到了 ⇒ 紅 |
| 2-f | 到場點名 ＋ expect 釘 `N／N`（要件③） | 少一格 ⇒ 紅 |

# §4 ★★★這一票最大的風險（寫在前面，不是事後解釋）

```
它的效果【完全掛在訊息真的送得到】。
★而我手上的帳寫著：資訊網 arc「whole-world distribute 未通（49 隊床 deliver=0）」
⇒ ★★若那條路今天仍然不通，本票會【一格都不紅、而世界一點也沒變好】
```
⇒ **2-d 必須同時印出【母體】**：
```
有多少**會呼叫 `_find_trade_partner` 的隊**（★商隊族群，**不是全世界平均** —— reviewer 2026-09-18：dead-end 具體點名的是 **settled 隊**，而商隊會移動 ⇒ 用全世界平均會**因為別的族群塞車而錯殺一個對商隊其實通的訊號**）【收到過】任何一則 market 類訊息（★分母）
其中有多少因此找得到交易對象（★分子）
⇒ ★★★若分母 ≈ 0 ⇒ 本票【沒有失敗，是沒有機會】—— 而那是資訊網的帳，不是這一票的
```
★**這一格我要求【先跑分母再動 code】**：**若分母是 0，這一票應該暫停而不是硬做。**

# §4b ★★★前置（不是平行）：先修 `_find_occupy_target`

**reviewer 2026-09-18 揭**：`_find_occupy_target`（`faction_ai_system.gd`）**不走 `known_outposts()`**，
它直接問 `team_tile_known` **有沒有這塊地**（只問存在，不問裡面是不是據點子記錄），
**閘過了之後 live 讀 `tile.outpost_owner`／`outpost_level`**。
```
現況  ：那塊地從沒被任何 relay 提過 ⇒ 不在 team_tile_known ⇒ 那個閘【正確擋掉】
本票後：只因為 relay 過一則【市集】訊息 ⇒ 進了 team_tile_known ⇒ 閘放行 ⇒ 後面 live 讀
⇒ ★★一則【交易】訊息，會變成一條【軍事】資訊的通道
⇒ ★★★而那正是 WHAT 明令的成對反事實要防的事（2-c）
```
⇒ **所以它不是「順序比較好」，是【不先修它，2-c 那格就會真的紅】。**
★**修法很便宜**：`known_outposts()` 已在 main，換讀法即可（★而它會正確排除只有 `market` 子記錄的 entry ——
reviewer 核過 `belief_system.gd:364-379` 的 filter：`op = rec.get("outpost"); if not (op is Dictionary): continue`）。
★★追蹤：`defers.tsv` → `occupy-target-scan-reads-live-outpost-after-tile-gate`。

# §5 不在本票
- 資訊網的 whole-world distribute（另一條線）。
- 市集的**規模／價格**進 belief（★那是新的軸，且不在 blueprint 給的兩個消費者裡）。

# §6 ★★★2026-09-18 撤回：本票的第一個消費者【今天是死的】（implementer 揭，systems 自己 grep 核實）

```
_find_trade_partner 全庫呼叫者：headless_test.gd ＋ 量測床 ⇒ ★production 0 處
strategic_ai_system.gd:297-298 的註解逐字：「純查詢 scaffolding（無 TaskArbiter 呼叫，非違憲），仍供 headless_test 覆蓋」
```
⇒ ★**§4 那個「先跑分母」的判準，答案不是【訊息送不到】，是【就算送到也沒有人會去用它】** ——
★★**兩者要做的事完全不同**：前者去修資訊網，後者去問「這個消費者該不該活過來」。

## 對本票的後果（★等 blueprint 裁，spec 先停在這裡）
```
blueprint 的消費者清單 ＝ 交易目標選擇 ＋ 玩家市場板（兩個 ⇒ 不破「單一消費者禁開新軸」）
而交易目標選擇【今天是死的】⇒ ★活的消費者可能是 0 或 1
⇒ ★★照他自己那條軸規，這個軸【可能不該開】
```
★**所以本票狀態改為【等 WHAT 覆裁】** —— **不是被否決，是它的正當性依據要重算。**

## ★★★而這也是一條給我自己的規矩（寫在票上，不寫在別處）
**「這支函式的輸出變了」與「世界的行為變了」之間，隔著【有沒有人呼叫它】這一步。**
★**量一支函式之前，先 grep 它的呼叫點** —— ★★**而那段自述就在我引用的函式上方 4 行，我沒讀。**

# §7 ★★★CLOSED：世界早就有耳朵（blueprint 裁 2026-09-18）

```
真管線：_merchant_trade_target → _nearest_market_outpost → team_market_known（★三源：創世／親見／relay）
⇒ ①它從來沒讀 god-view ⇒ 拆除【零損失】
⇒ ②我要建的「傳聞帶市集記錄」⇒ ★它早就存在，只是在【另一個 store】上
```
★**blueprint 的定性（逐字）**：「**幻痛確診：整場警報量在死肢上。**」

## ★這一票的完整弧（他要求入帳）
```
拆違憲 → 量出驚人跌幅 → 裁新管道 → ★前提被殺 → ★★發現世界早就做對了
⇒ 他的定性：五步裡真正的產出是【對真管線的一次完整體檢】，不是白工
```
★★**而我要把帳記得更平**：**體檢是真的產出，而那段彎路是【我沒做前提檢查】造成的** ——
**兩句都成立，少了任何一句這筆帳就是歪的。**
★★★**不該被這句話安慰掉的那一半**：**下一次省下那段彎路的方法，是【量一支函式之前先 grep 它的呼叫點】**，
**不是「反正繞路也有收穫」。**

## 原則的處置（照他的裁定改註）
「知識的細節等級跟著傳播動機走」——★**留帳，而註記改成【描述既有現實，不是新設計】**：
`team_market_known` 吃 relay（市集想被知道）／`team_tile_known` 的據點子記錄只親見才寫（軍情想被藏）
⇒ **那兩個 store 的差別，本身就是這條原則已經在世界裡的樣子。**

