---
slice: ①一圈形態（窄形狀：只合併走訪、不共用計算結果）
owner: systems
status: CLOSED【依預註冊門檻】（2026-09-22）
基於: implementer 規模量測 2026-09-22（世代 6／HW-2，兩個 seed，★與 U₂ 同一個 log）
---

裁定：**不做。U₁ ＝ 9.44%（seed 1337）／9.24%（seed 77）＜ 預註冊門檻 10.0%。**

# §1 卷面（★我自己重算，不是轉貼）

```
  seed  T_seg(s)  T_calc   T_catch    L(窄)     S      U₁
  1337   53.934   15.178   15.630    42.88%   0.587   9.44%
    77   49.845   13.934   14.413    43.13%   0.573   9.24%
L  ＝ (T_seg − T_calc − T_catch) / T_seg   ｜ U₁ ＝ S × [ L×25% ＋ (1−L)×9.4% ]（預註冊式，未改）
★我算 9.44%，他卷面寫 9.45% —— 四捨五入，不影響判定。
★★母體衛生：圈數本輪【沒有塌】（(甲) 計時法、無 stub）｜gather 呼叫 9237／8707｜隊數 103／94
```

# §2 ★★★這是邊界案例（0.56 個百分點），所以我把它壓力測了

```
U₁ ＝ S × (0.094 ＋ 0.156 × L) ⇒ ★對 L 單調遞增（係數 0.156 > 0）⇒ 「L 是上界」⇒「U₁ 是上界」
★★要把 U₁ 推過 10.0%，需要：
     L ＝ 48.95%（比量到的 42.88% 高 **6.07 個百分點**）
  ＝ T_calc＋T_catch 被高估 **3.27 秒**（占 T_seg 的 6.07%）
★★★而【整套計時樁的成本】世代 5 量到是 **0.545 秒**（40.672 vs 40.127）
  ⇒ 需要的高估量是整套樁成本的 **6.0 倍** ⇒ **樁的偏差翻不動這個判定**
⇒ 這一格是 implementer 標成「誠實限」而沒有定量的，★**定量是判準持有者的活，我補上了**。
```

# §3 判準三件

```
①兩顆種子同側（9.44／9.24，都 < 10.0）⇒ 不必第三顆
②★U₁ 是【上界】且對 L 單調遞增 ⇒ 真省 ≤ 9.44% ⇒ 結案站在安全那一側
   （L 本身也是上界：迴圈體內沒有具名包住的計算被算進【走訪】側 ⇒ 真 L 更小 ⇒ U₁ 更小）
③★★第三格（≥10／<10／**不可判**）：母體沒塌、兩顆種子、樁偏差經 §2 定量後翻不動 ⇒ **可判**
```

# §4 ★成立謂詞（WHAT 規矩；★★他今天把它分成兩種，我照分）

```
【形狀謂詞｜讀得到 ⇒ 可 met_check】
  本結案只涵蓋**窄形狀**：只合併走訪、**不共用任何計算結果**。
  ⇒ ★**寬形狀（合併走訪＋共用計算結果）不在本結案範圍內** —— 它是【範圍外】不是【謂詞假】，
    而它與 `share-estimate-catch-up-across-all-seven-call-sites` 那一列重疊。
【規模謂詞｜量出來 ⇒ ★不可 grep，不編 met_check】
  L ＝ 42.88%／43.13%、S ＝ 0.587／0.573（世代 6／HW-2）。
  ⇒ 觸發器 ＝ **世代邊界時重量**。★★★本謂詞不可 grep，我沒有替它編檢查。
```

# §5 ★回絕的那一輪補量（記在這裡，不是「以後再說」）

```
implementer 提議把迴圈體內剩下的計算也具名包住再跑（約 12 分鐘）。
★回絕理由是**方向**：具名更多計算 ⇒ 那些時間從【走訪】側移到【計算】側 ⇒ L 變小 ⇒ U₁ 變小
  ⇒ **它只能讓結論更穩，不可能翻過門檻**。
★★而真正可能翻過門檻的是**反方向**（樁把 T_calc＋T_catch 灌大）—— 那一格已在 §2 定量否掉。
★★★兩個方向是兩件事，別讓「他提議了一個補量」看起來像「還有不確定性沒處理」。
```

# §6 落地

```
卷面：docs/measurements/2026-09-22-U1-one-pass-narrow-gen6-HW2.txt
raw ：docs/measurements/2026-09-22-U2-u2-s1337-gen6-HW2.log.gz（★與 U₂ 同源）
      docs/measurements/2026-09-22-U2-u2-s77-gen6-HW2.log.gz
★同源的好處：兩張票的 S 是【同一次量測的同一個數】⇒ 不會「各自量、分母差一點而那一點跨過門檻」
★★同源的壞處（他自己寫出來的）：**共用一切誤差** —— 樁若有系統性偏差，兩張票同方向錯。
   ⇒ 而 §2 的壓力測試正是針對這一點，且結論是翻不動。
```
