# 信使旅程切段（HOW）— 2026-09-11

> **開刀理由**：信使 **45 趟出發、1 次送達（2.2%）**，而同卷紮營 **79.2%** 證明尺已可信
> ⇒ ★**2.2% 是真的**。blueprint 裁：**sim 線第一序**（結盟 3.8%／協調失敗／belief 全面陳舊，嫌疑全指回這條腿）。

## ① 這一票**只量，不修**（★理由是我們這兩天踩過兩次）

```
★紮營那一格教的：**低／零可能是尺的病** ⇒ 先確認是世界的病。
★★而這次尺**已經**確認可信（同卷 fp 逐字相同、紮營由 0 → 79.2%）
  ⇒ 所以要問的不是「尺對不對」，是 **44 趟從哪幾個門出去的**。
★★★**禁提名嫌疑犯**：不要先去修送達判定、不要先調參數。
```

## ② 切段（★每一段一個計數，★★每一格貼 file:line）

```
①**出發**：`TASK_HERALD` commit 成立（母體；★起算時目標距離也記，但**不篩**）
②**途中任務被換**：★分三桶 —— (a)**同層 self-replace**（★求居案的兇手就是這個：「建設」@50）
   (b) 更高優先序搶走　(c) `release`
   ⇒ ★★**需求化優先序修完之後還會不會被換 ＝ 這一格要 before/after 兩側**
③**抵達目標**：走到 `order_target` 所在位置
④**★★★送達判定成立**（＝ blueprint 裁的「送達被讀取」）
```

## ③ ★★★「門鈴」格（★blueprint 點的，而我把它坐實到行號了）

```
送達的唯一入口是 **pairwise 相遇**（`interaction_system.gd`，★全庫只有 4 個呼叫點）：
  :390-393  `a.current_task == TASK_HERALD and a.order_target_id == id_b` → `_deliver_order`
            ★**而這四行在 `if same_faction:` 的分支【裡面】**（:385）
  :421-425  `TASK_HERALD and task_reason == "envoy_proposal" and order_target_id == id_b`
            → `_deliver_envoy_proposal`（★這條在 same_faction **外面**）
⇒ ★**抵達目標【格】不等於送達** —— 要**與那支目標隊本身相遇**（它可能已經走了）
⇒ ★★而由此生出**兩個必須單獨成格的問題（★標【待驗】，我只讀了 code）**：
  (i) **抵達了但沒相遇**幾次？（★這就是求居案「到了 4 次、領主見到 0 次」的同族）
  (ii) ★★★**跨派系、而 `task_reason != "envoy_proposal"` 的信使，有沒有任何送達分支？**
       —— 我 grep 了 `_deliver_order`／`_deliver_envoy_proposal` 的**全部呼叫點（共 4 個）**，
       沒有看到第三條路；★**但這是負斷言** ⇒ **要你用逐筆資料證實或推翻**，
       ★★不要拿我的 grep 當結論（★★★若成立，那一類信使 ＝ **結構上永遠送不到**）。
```

## ④ 驗收

```
①**44 趟的去向分桶加總 ＝ 44**（★沒有「其他」這一桶；分不出來的要具名列出）
②每一格**貼 file:line**：那個出口是在**哪一行**發生的
③★**fp 逐字相同** ⇒ 純觀測（★★同卷 fp 值寫進交件，這是「世界沒被我改動」的硬證據）
④★★★**個位數不判方向**：母體 45、而某些桶會是個位數
   ⇒ **只報數，不報趨勢**；要判方向就加窗或加 config（★這條我今天才踩過）
⑤窗末未完 **單獨一類、不進分母**（同前兩卷）
```

## ⑤ 不要做的

```
★不要修送達判定　★★不要調任何參數　★★★不要把「抵達」改成「送達」來讓數字好看
  —— ★**這一票的產出是【那 44 趟死在哪裡】，不是一個更好看的百分比。**
```
