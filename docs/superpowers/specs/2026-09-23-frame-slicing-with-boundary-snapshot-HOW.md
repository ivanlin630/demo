# 分片＋邊界快照（HOW spec）

**開票**：blueprint 裁 2026-09-23（門票＝世代 7 的 B3 玩家絕對尺不達標，兩顆種子同向）
**狀態**：★★★**HALT（R² premise_contradiction，2026-09-23）** —— 等 WHAT 裁。
> ★**這張票沒有驗收儀器**：`freeze_sample_bed.gd:96-99` 量的就是 `advance_tick` 那一個命令，
> 而 §4 自己寫著「`advance_tick` 契約不動、分片只作用在互動迴圈」⇒ **驗收②恒真**。
> ★★而更深的是：`frame_time_who_freezes_bed.gd:10/:18/:51` 明寫現有量法把【tick】當【frame】的代理，
> 理由是「ObserverBridge 的 12ms-budget 不可切割」—— **而這張票要做的就是把它切開**
> ⇒ ★★★**修法本身摧毀了驗收儀器的成立理由**。
> ★待裁：玩家的問題是【畫面凍住】（→ 要先開一張量測儀器票）還是【世界卡住】（→ 這張票治不到）。
**樣本**：★已經在手上 —— `docs/measurements/2026-09-23-B3-freeze-player-ruler-gen7.md`
的 >2s 事件逐筆原始清單（`B3-freeze-list-seed1337.txt` 107 筆／`B3-freeze-list-seed42.txt` 59 筆）
**不是**「再跑一次才知道」。

---

## §0 為什麼開這張票（判決，不是感覺）

```
[B3-FREEZE] gen=7 seed=1337 days=12/12 over2s_days=7/12 p99_ms=1161 verdict=FAIL
[B3-FREEZE] gen=7 seed=42   days=12/12 over2s_days=5/12 p99_ms=1052 verdict=FAIL
判準：>2s 的卡頓 ≤ 1／日 且 p99 < 1000ms ⇒ ★兩條都沒過，兩顆種子同向
```

★**兩顆同向＝訊號，方向相反才是洗牌。** 這是玩家會感覺到的東西，不是內部指標。

## §1 範圍（blueprint 定的序，我不改）

```
①【指令佇列化】—— ★獨立票，先做
   ・玩家指令改成進佇列，由 tick 邊界消費
   ・★★禁 flush 後門：不得提供「立刻套用」的旁路 —— 那會把這張票的前提挖掉
   ・已知牽動：14 處測試改寫
②【第二個分片 driver】＋【UI 讀 tick 邊界快照】
   ・分片 driver ★只給互動迴圈；`advance_tick` 的契約【不動】
   ・UI 改讀 tick 邊界的快照 ⇒ 兩個 bridge 換回傳型別
```

★①必須先單獨落地再做②：②依賴「指令已經是佇列化的」這個前提。

## §2 驗收（★這是本票最特別的地方）

```
①★世界指紋【逐字相同】—— 不改世界、仍在世代 7
  ⇒ world-fp／world-fp-ctrl 兩列必須維持 expect＝763e9ee91e7c91807b9a625d60362650
  ★★指紋一變，這張票就【失去它的驗收方法】（見 §3），不是「順便改了世界」而已。
②B3 在世代 7 重跑，同兩顆種子（1337／42）
  ⇒ 判準同上：>2s ≤ 1／日 且 p99 < 1000ms
```

## §3 ★★★為什麼這張票的歸因【成立】（blueprint 點出的，我同意並寫死）

```
一般的效能票沒辦法歸因：改動通常會改變世界 ⇒ 前後是【兩個不同的世界】
⇒ 差值裡混著【修法效果】與【世界分岔】，分不開。
★而這張票【不改世界】（驗收①要求指紋逐字相同）
⇒ ★★它是第一張能做【同機、同世界、before／after】的效能票 ⇒ 歸因成立。
⇒ ★★★所以驗收①不是形式：**它是讓驗收②有意義的前提**。
```

## §4 不變量

```
・`advance_tick` 契約不動 ⇒ 模擬端看不出差別（★分片只作用在互動迴圈）
・全量暫態可觀測性：新的 driver 與快照路徑若產生新的 decision／state，必須接 tap
・感知鐵律：本票不碰決策讀值，★但 UI 快照【不得】變成一條繞過 belief 的新讀值路徑
・時間量跨機不可比 ⇒ B3 重跑要帶 [HW] 戳，且與 2026-09-23 這一輪同機才可比
```

## §5 誠實限

```
★樣本是 12 天 × 2 顆種子 ⇒ 它答【方向】不答【分布】。
★★兩顆都是【後半窗】才開始出現 >2s（day6+／day8+）—— 這個形狀本身要被解釋，
  而本 spec 【沒有】解釋它：若修完 p99 降了但那個「後半窗才開始」的形狀還在，
  ⇒ 那代表還有第二個成因，本票沒碰到它。
★★★本票不含【卡頓成因的診斷】—— 它是一個結構修法（把互動迴圈與 tick 解耦），
  而不是「找出那 107 筆是誰造成的」。★那是另一件事，不要在驗收時把兩者混為一談。
```
