---
slice: 10 處 cadence 改走 `CadenceStagger` —— **世界改變窗 #3（世代 7）**
owner: systems
status: ★★★**診斷撤回、不 merge**（2026-09-22，見 §11）——這 10 處的 cadence 只有 {1440,4320}，**沒有一個是 60**⇒ 每 60 tick 的尖峰在構造上不可能由它們產生；世界卷面：間距 60 佔比 99.3% → 99.3%（一動也沒動）；★★存活隊數兩顆種子同向掉 11–18% 待解釋
基於: 凍結線根因（尖峰佔存活隊 61.5%／62.4%，間距 60 tick 佔 99%）；WHAT 核准 2026-09-22（開窗 #3、零新旋鈕、加窄閘、序插最前）
---

# §1 ★★★病（機制，file:line 全在）

```
`CadenceStagger.next_tick()` ＝ `(cycle+1)*cadence + _mix(team_id, cycle) % cadence`
  ⇒ ★**offset 逐 cycle 變化** ⇒ 同相的隊**下一輪就分開**
**而純加法** `next = current_tick + CONST`
  ⇒ ★★**同一 tick 決策的隊，下一次還是同一 tick** ⇒ **相位永遠不變**
  ⇒ ★★★**世界生成時所有隊幾乎同時出生 ⇒ 從一開始就同相 ⇒ 永遠不分開**
⇒ **量到的後果**：尖峰 **72／63 隊 ＝ 存活隊的 61.5%／62.4%**，間距 **60 tick 佔 99.3%／98.9%**
⇒ 而**凍結幀 100% 落在 11+ 桶**（★該桶 77.4%／82.6% 未凍結 ⇒ **必要非充分**）
```

# §2 要改的 10 處（★逐一，這就是範圍）

```
faction_ai_system.gd:630   threat_eval_next_tick      ｜:862  residency_eval_next_tick
faction_ai_system.gd:2706  info_eval_next_tick        ｜:4163 subteam_eval_next_tick
faction_ai_system.gd:4379  decision_eval_next_tick
labor_system.gd:162        labor_eval_next_tick        ← ★**tile-scoped**
decision_context.gd:614    idle_employ_next_tick       ← ★**tile-scoped**
decision_context.gd:870    expand_eval_next_tick      ｜:1330 consolidate_eval_next_tick
goal_resolver.gd:28        goal_eval_next_tick
```
★**對照**：全庫 `*_next_tick =` 共 **33 處已走錯開**、**10 處純加法**、22 處其他（資料重排／零值初始化）。

# §3 ★★★錯開的【鍵】——**兩處不是隊，要指定**

```
`next_tick(current_tick, last_eval_tick, team_id, cadence)` 的第三參數是**混合用的身分**
★8 處是隊 ⇒ 傳 `team.team_id`
★★**2 處是 tile**（`labor_system.gd:162`／`decision_context.gd:614`）
   ⇒ ★**傳 `tile.tile_id`**（`scripts/data/tile_data.gd:3  var tile_id: int = 0` —— ★訂正：檔名是 `tile_data.gd`（`class_name HexTileData`），我第一版寫成 `hex_tile_data.gd` 是錯的，寫完自己查才發現 —— 穩定整數身分，已存在）
   ⇒ ★★**不要用座標拼一個數** —— ★★★**那是手抄身分**，而 `tile_id` 就是為此存在的
⇒ **零新旋鈕**（WHAT 明令）：**不新增任何常數**，cadence 值一個都不動
```

# §4 ★★驗收（**預註冊**）

```
A1【預期會紅的紅】**fp 會改變** ⇒ 這是**世界改變窗 #3、世代 7 的邊界**
   ★★而 `2-f` 式的「沒變才是紅」適用：**fp 沒變 ⇒ 修法沒生效**
A2【主，硬閘】**尖峰佔存活隊比例大幅下降**：從 **61.5%／62.4%** 降到 **< 20%**（兩顆種子）
   ★門檻理由：**均勻錯開在 60 個格子上，~115 隊 ⇒ 期望每格 ~2 隊 ≈ 2%**
   ⇒ ★★**20% 是一個【寬鬆到不可能靠雜訊達成】的門檻**，不是我挑一個好看的數
A3【副作用對照，硬閘】★**決策頻率不變**：每隊在整個窗內的決策**次數**與修法前**同量級**
   ⇒ ★★★**錯開改的是【何時】不是【多久一次】** —— 若次數變了，代表我改到了 cadence 而不是相位
A4【母體衛生】存活隊數曲線與修法前同量級（★掉了代表世界被改壞，不是被錯開）
A5【>2s 幀數】★**只作觀察欄，不設門檻** —— 理由：**11+ 是必要非充分**，
   ⇒ 消掉尖峰**應該**讓凍結消失，★★**但那是推論** ⇒ 讓數字自己說，不預先綁定
```

# §5 ★★閘（WHAT 核准加；**形狀窄**）

```
禁 `*_next_tick = …current_tick + …`（★限 `scripts/simulation/`，★★排除 22 處「其他」形態）
★★★**陽性對照**：**在修法前的樹上跑，那 10 處必須紅** —— 否則閘沒有鑑別力
★而這一格是必要的：**這個病躲過了我兩輪靜態掃**（`% 60 == 0`／經過時間比較都零命中）
  ⇒ **它長得像正確的排程** ⇒ **人眼看不出來，只能靠機械**
```

# §6 ★誠實限

```
①**A5 不設門檻** ⇒ 本票**不保證凍結消失**，只保證**尖峰消失**
②那 22 處「其他」我**沒有逐處讀** ⇒ ★若其中有同病者，本票漏掉它們（閘會在下次碰到時抓）
③`tile_id` 的穩定性我**只讀了宣告**（`var tile_id: int = 0`）⇒ ★★**沒有驗它在世界重生成後是否穩定**
   ⇒ **實作時要確認**（若它會變，錯開會每次重生成就換相位 —— 那仍然可接受，但要知道）
```

---

# §7 ★R² CLEAN 之後的兩處更新（reviewer 2026-09-22）

## §7.1 ★`tile_id` 穩定性：**已核實，誠實限撤回**

```
`world_generator.gd:72  tile.tile_id = ox * 1000 + oy`（`ox/oy` ＝ 座標 ＋ radius 偏移）
⇒ ★**純座標推導，零 RNG、零生成順序依賴** ⇒ **同一個世界形狀 ⇒ 同一組 tile_id**
⇒ ★★**我 §6③ 那條誠實限（「沒有驗它在重生成後是否穩定」）撤回** —— ★而我自己開檔核過，不是轉述。
```

## §7.2 ★★★新增 A0：**先驗「錯開真的均勻」**（reviewer 建議，我收）

```
★我的 A2 門檻（尖峰佔比 < 20%）推導自「均勻錯開 ⇒ 期望每格 ~2%」
⇒ ★★而「均勻」是**我讀註解讀來的，沒有驗過** —— 那是一個**未驗前提撐著一個預註冊門檻**
⇒ ★★★**A0（獨立單元檢查，便宜，在實作之前跑）**：
   對 `CadenceStagger._mix(team_id, cycle) % cadence`，
   ・取 **~115 個 team_id × 多個 cycle**，cadence ＝ 60
   ・印**每格命中數的分佈**：**max／median／空格數**
   ・★**判準（預註冊）**：**max 格 ≤ 期望值的 3 倍**（期望 ≈ 115/60 ≈ 1.9 ⇒ **max ≤ 6**）
     且**空格數 ≤ 60 的一半**
   ⇒ ★★**若 A0 紅** ⇒ **A2 的 20% 門檻失去依據** ⇒ **回來重定門檻，不要硬做**
★**這一格的價值不是「它會紅」** —— 是**它把一個推論變成一個被驗過的前提**，
  而 A2 的門檻**現在才真的站得住**。
```

---

# §8 ★★★A0 紅了 ⇒ **A2 撤換**（systems 裁，2026-09-22）

## §8.1 ★A0 的判決**維持 FAIL，我不下修它的門檻**

```
卷面（implementer，`docs/measurements/2026-09-22-A0-cadence-uniformity.log.gz`）：
  teams=115 cadence=60 cycles=200｜期望 1.92／格
  max 格：median=6  p90=7  p99=8  最壞=9（cycle 158）｜空格 median=9 最壞=13
  ★★超門檻 6 的 cycle ＝ **39／200（19.5%）** ⇒ **不是偶發離群，是常態貼著線**
  ★陽性對照：offset 恆 0 ⇒ max=115 空格=59 ⇒ 判準會紅 ✔（★所以這個紅不是判準壞掉）
⇒ ★★★**A0 的工作是【驗一個前提】，而那個前提是假的** ⇒ **前提撤回，不是門檻放寬**
⇒ ★★把門檻從 6 調到 9 會讓它變綠，而**那正是「調低門檻再說」** —— 不做。
```

## §8.2 ★★A2 舊版**作廢**（它的理由已經倒了）

```
舊 A2：「尖峰 61.5%／62.4% → < 20%」，★理由寫的是「均勻錯開 60 格 ⇒ 每格 ~2%」
⇒ ★**「均勻」是假的** ⇒ **20% 這個數字沒有來源了**
⇒ ★★而 implementer 指出的第二層我認：**我量到的尖峰是【多層同時到期】**，
   單一 cadence 的 7.8%（9／115）疊起來是多少 —— ★★★**沒有人算過**，我也不猜。
```

## §8.3 ★★★新 A2：**機制由床證、規模由世界證，兩者不可互相代替**

```
A2c【機制，硬閘，跑在 A0 那支床裡 —— ★不是新閘，是同一支床多一行】
   量：同一個 team 在**連續兩個 cycle** 拿到同一個 offset 的比率 same_residue_rate
   ・修法前（純加法）：offset 恆定 ⇒ **100%**   ← ★這就是陽性對照，必須先量到
   ・修法後：碰撞機率 ＝ **1/cadence** ⇒ cadence=60 時期望 ≈ **1.7%**
   ・★**門檻：< 10%**  ★理由 ＝ **構造推導的 1/cadence 的 6 倍餘裕**，
     ★★而 0% 與 100% 之間**沒有刀鋒** ⇒ 這個門檻不靠任何均勻性假設
   ⇒ ★★★**這一格才是本 slice 的真正主張**：「相位不再被永遠保留」

A2【世界，硬閘，★方向性】尖峰佔存活隊比例 **不得上升**（兩顆種子各自比）
   ⇒ ★理由：多層疊加**沒算過** ⇒ **我不預測它降多少，但我拒絕它變糟**
   ⇒ ★★這一格**真的可能紅**：若 10 層各自鋪開後在某些 tick 反而更容易撞在一起

A2b【觀察欄，無門檻】尖峰實際降了多少 ⇒ ★讓數字自己說（★這就是舊 A2 被降級成的樣子）
```

## §8.4 ★新增 A0b：**多層疊加，先在床上算出來**（★在改世界之前）

```
★★★implementer 問的那件事（多層疊加是多少）**可以算，而且是秒級的** ⇒ 那就算，別猜。
做法：同一支純函式床，**把那 10 處的【真實 cadence 常數】一起模擬**
   ・印 per-tick **union**（該 tick 至少有一站到期的 distinct team 數）分佈
   ・**before（純加法）vs after（錯開）各一份**
   ・★把用到的 10 個 cadence 常數**原樣印出來**（★不要手抄一個「大概是 60」）
★★★誠實限（必寫進卷面）：
   這支床**只模擬排程，不模擬 gating**（世界裡一隊到期了不一定真的決策）
   ⇒ **它是排程層的上界** ⇒ ★★**只能拿 before 跟 after 互比，不可拿去跟世界的 61.5% 比**
⇒ ★若 A0b 顯示 after 的 union 尖峰**沒有明顯低於 before** ⇒ **這個 slice 的價值主張當場倒**
   ⇒ **回報，不要改 production**（★這比事後在世界裡發現便宜太多）
```

## §8.5 ★A0 床進註冊表 —— **但它換了工作，所以換了統計量**

```
★★原 A0 的工作（驗均勻）**已失敗且不再做** ⇒ 註冊版的工作是**迴歸守衛：不要變糟**
★★★統計量要換：**不可以守 max** —— max 隨 cycles 變多必然長大 ⇒ 守它會自己變紅
   ⇒ 守 **median ≤ 7 且 p99 ≤ 10**（實測 6／8，★餘裕一格）
★這個門檻**來自量測**，而這在這裡是正當的：它的工作是「不要退化」，不是「證明均勻」
   —— ★★兩者是不同的問題，所以不是同一件事的門檻放寬。
```

---

# §9 ★★★**唯一驗收表**（WHAT 訂正版，2026-09-22）—— §4 與 §8.3 的編號**作廢**

```
★★★危險先講：**同一個 slice 出現過兩套編號**（我的 A1/A2/A2b/A2c/A5 與 WHAT 的 A0..A5），
   而它們**把不同的東西叫同一個名字**（我的 A1＝fp，WHAT 的 A4 才是 fp）
⇒ **以下這張是唯一的一張**。§4／§8.3 只留作紀錄，**不得拿去對帳**。
```

| 格 | 是什麼 | 判準 |
|---|---|---|
| **A0** | `_mix` 均勻性 | ★**降為觀察欄，不判**（前提已撤，見 §8.1） |
| **A0b** | 床：十個**真實** cadence 純函式模擬 before/after | ★尖峰**下降** ⇒ 進實作；**不下降 ⇒ 價值主張倒 ⇒ 不動 production、回報** |
| **A1** | 機制：相位保留率 | 修前同隊同格 **100%** ⇒ 修後 **< 10%**（1/cadence 推導）；尖峰 tick 數與 60-tick 間距佔比**不得上升**（方向性）；★幅度＝觀察欄 |
| **A2** | **玩家尺**（★交玩的唯一判準） | `>2s` **≤ 1／日** 且 **p99 < 1s** ⇒ 可交玩；否則分片＋快照票開 |
| **A3** | 副作用 | 每隊每日決策次數 **median 變動 < 5%** |
| **A4** | fp | ★**預期紅**、世代 7 |
| **A5** | **陽性對照** | ★**留 1 處純加法** ⇒ 該路徑相位保留率仍 **100%** |
| **A6** | **母體衛生**（★systems 於 2026-09-22 加回：採用訂正表時這一格掉了） | 存活隊數曲線同量級；★★**現狀：117→96、101→90（兩顆同向 11–18%）⇒ ★★★**升硬闘**（systems 2026-09-22）：逐日曲線與 baseline 同量級，**偏離不得單調擴大**（★不守末值：末值會把「單調擴大」與「雜訊」壓成同一個數）；見 §11.4** |

## §9.1 ★A0b 的統計量：**不要用「11+」這個名字**

```
WHAT 寫「尖峰（11+ 同時到期）tick 數」。★★**床上的「同時到期」與世界的「11+ 決策隊」不是同一個母體**：
   床**不模擬 gating**（到期 ≠ 真的決策）⇒ 床的 union 是**上界**
⇒ ★★★沿用「11+」這個數字會**把兩個不同的東西叫成同一個名字**（而它讀起來完全正常）
⇒ **改成自比**：算 before 的 per-tick union **p99**，再數 after 有幾個 tick 超過**那個值**
   ⇒ ★這不需要任何外來常數，且 before/after 同母體同定義。
```

## §9.2 ★★A5 要留**哪一處**？——★**不由我猜，由 A0b 指**

```
留一處純加法 ＝ **故意留一層不錯開** ⇒ ★它會**繼續貢獻尖峰**
⇒ 所以要留**貢獻最小**的那一處，而**哪一處最小是量出來的**：
   ⇒ **A0b 順便印【每一站各自對尖峰的貢獻】** ⇒ 取**最小**的那站當 A5 對照
★★而**不要留那兩處 tile-scoped 的**（`labor_system.gd:162`／`decision_context.gd:614`）：
   它們的鍵是 `tile_id` ⇒ **母體是格子不是隊** ⇒ 拿來對照「同隊同格」的相位保留率**對不上**。
```

## §9.3 ★★★若要「連 `_mix` 一起修」——**先知道爆炸半徑**

```
WHAT 說「若 _mix 集中就連 _mix 一起修（仍零旋鈕）」。★技術上可行，但**範圍不是 10 站**：
   ⇒ **`_mix` 是全部 33 個已錯開的站共用的** ⇒ 改它 ⇒ **那 33 站的 offset 全變**
   ⇒ ★★世代仍然只跳一次（仍是世代 7），**但驗收要涵蓋的行為面從 10 站變成 43 站**
   ⇒ ★★★而**任何別處的回歸都會變成「是不是 `_mix` 害的」** —— 歸因會變難
⇒ **建議：分兩刀**（先 10 站錯開落地綠，再單開 `_mix` 一票）；★**要合成一刀是 WHAT 的裁量，不是不能做。**
```

---

# §10 ★★★A0b 回來了：**綠，放行** —— 但**承重的那個數字不是 115→2**

## §10.1 卷面

```
常數（★引用非手抄）：THREAT=1440 RESIDENCY=4320 INFO=1440 SUBTEAM=1440 DECISION=4320
  LABOR=4320 INFRA=4320 CONSOLIDATE=1440 GOAL=4320   ← ★**9 個相異常數 ＝ 10 處**
  ★`idle_employ`（`decision/decision_context.gd:614`）與 `labor`（`labor_system.gd:162`）
    **共用 `LaborSystem.LABOR_CADENCE`** ⇒ 9 與 10 不矛盾（systems 開檔核過）
union（horizon=4320）：before median=0 p90=0 p99=0 **max=115**｜after … p99=1 **max=2**
A1 相位保留率：**1.56%**（358／22885；期望 1/cadence≈1.67%；門檻 <10%）⇒ **綠**
```

## §10.2 ★★★`before = 115` 是**床的初始條件**，不是世界的性質

```
★床讓**全部 115 隊在同一個 tick 出生** ⇒ 純加法下它們永遠同相 ⇒ before 必然 ＝ 100%
★★**而世界量到的尖峰是 61.5%／62.4%，不是 100%** ⇒ **真實的隊不是同時出生的**
⇒ ★★★**「115 → 2」不可以當成本 slice 的成果對外引用** —— 分子是床造的
⇒ **承重的是 `after max = 2`**：★它**不依賴出生時刻**（錯開每個 cycle 重算 offset）
⇒ ★而 implementer 已經抓過一次同族假象（t=0 全體到期）；**這是同一個假象上移一層**：
   他修掉了「起算點」，而**「出生時刻」還留在裡面**。
```

## §10.3 ★這反過來證明 A1（相位保留率）選對了

```
相位保留率 ＝ 同一隊連續兩 cycle 是否落同一格
⇒ ★純加法下**恆為 100%，與出生時刻無關**；錯開後 ≈ 1/cadence，**也與出生時刻無關**
⇒ ★★★**它是本 slice 唯一一個【不受床的初始條件污染】的量** ⇒ **主閘放在它身上是對的**
   （而 union 尖峰**天生受出生時刻影響** ⇒ 它只能當觀察欄）
```

## §10.4 A5 陽性對照**留哪一處**：★約束在此，人選由 implementer 報

```
★留一處純加法 ＝ **故意留一整層同相** ⇒ 它會繼續貢獻尖峰 ⇒ 要挑最便宜的
約束（systems 定）：
  ①★**必須 team-keyed** ⇒ **排除** `labor_system.gd:162`／`decision/decision_context.gd:614`（tile-scoped）
  ②★**必須 cadence ＝ 4320** ⇒ 尖峰復發頻率是 1440 那組的 **1/3**
  ③★★**排除 `decision_eval`（`faction_ai_system.gd:4379`）** ⇒ 那是主決策站，留它＝留最大一層
  ⇒ 可選：`:862 residency`／`decision_context.gd:870 expand`／`goal_resolver.gd:28 goal`
★**挑哪一個由 implementer 定並回報理由** —— 我不指定。
```

## §10.5 ★票裡的路徑有誤（systems 自糾）

```
§2 寫 `decision_context.gd:614`／`:870`／`:1330` ⇒ ★**實際路徑是 `scripts/simulation/decision/decision_context.gd`**
（★那個路徑下**沒有檔案** ⇒ 照抄會 `No such file`）
```

---

# §11 ★★★世界說話了：**這 10 處不是 60-tick 尖峰的來源** —— **診斷撤回（systems 自認）**

## §11.1 一句算術就能推翻，而**我手上有那些數字**

```
那 10 處的 cadence 值集合 ＝ **{1440, 4320}**，最小的也是 60 的 **24 倍**
⇒ ★★★**每 60 tick 一次的尖峰，在構造上不可能由它們產生**
⇒ **診斷撤回。**
★★★而這**不是新資訊**：那九個常數是 implementer 在 A0b 原樣印的，
   **我把它們逐字抄進了本票 §10.1**，然後**沒有跟 60 比一次**。
⇒ ★我 memory 裡那條規矩的原文是：「凡『N 個』的宣稱**當場**把 N 跟應該是多少比一次」
⇒ ★★**我在同一份文件裡引用了「機制由床證、規模由世界證」，然後讓床的綠替代了因果主張。**
```

## §11.2 A0b 沒有錯，它回答的是**另一個問題**

```
A0b 證明的是「**這 10 處的相位不再對齊**」——★它成立（after 最壞一格 ＝ 2）
缺的那一句是「**這 10 處是不是那個病的來源**」
⇒ ★★★**「修法有沒有效」與「這個修法治的是不是那個病」是兩句話**，而我只驗了前一句。
```

## §11.3 世界卷面（12 天窗、兩顆種子）

| 量 | 前 | 後 | 讀法 |
|---|---|---|---|
| 尖峰佔存活隊 | 61.5% / 62.4% | 51.0% / 57.8% | ★只小降 |
| **間距 60 的佔比** | 99.3% / 98.9% | **99.3% / 99.3%** | ★★★**一動也沒動** ＝ 主症狀未受影響 |
| n_deciders max | 72 / 63 | 49 / 52 | 降 |
| 全體 dt median | 92 / 88 us | 83 / 80 us | ★**不可歸因**（世界已不同，見下） |
| **最後存活隊數** | **117 / 101** | **96 / 90** | ★★**兩顆種子同向掉 11–18%** |
| 凍結幀數 | 64 / 49 | 95 / 38 | ★兩顆反向 ⇒ 重新洗牌，不可讀成改善或惡化 |

## §11.4 ★★★而我在改表時**弄丟了一格**

```
我原表有 **A4【母體衛生】存活隊數曲線同量級**。
★採用 WHAT 訂正表（§9）時，那一格**沒有對應** ⇒ **它消失了**
⇒ ★★而**恰好就是它會判的那個數字，兩顆種子同向掉了 11–18%**
⇒ ★★★**把它加回 §9：A6【母體衛生】** —— 且它**現在就是紅的／至少是待解釋的**
★可能的機制（★未驗，要量）：錯開讓每隊的**第一次**評估被推遲最多 (cadence−1) tick，
  而 cadence ＝ 4320 ＝ **3 個遊戲日** ⇒ ★**「只動相位不動頻率」對【第一次】不成立**。
```

## §11.5 ★裁：**不 merge，先查**（HOW 裁量；WHAT 可推翻）

```
理由三條：
 ①**它沒有治好它被做出來的那個病**（間距 60 佔比 99.3% → 99.3%）
 ②**存活隊數兩顆同向掉 11–18% 沒有解釋** ⇒ 這是**行為回歸的形狀**，不是洗牌
 ③★★★**merge ＝ 燒掉一個世代邊界**，而**凍結的真凶還沒找到** ——
   ★之後所有 before/after 都要重新量 ⇒ **在查清楚之前燒掉它，代價最大**
⇒ 分支**留著、commit、push，不 merge**（★兩顆 commit：修法／量測樁分開）
```

## §11.6 下一步：**讓資料指出是哪些欄位**

```
★**不 grep「哪個常數等於 60」** —— 那是看到答案再去找符合的東西（implementer 自己擋住了，對）
⇒ 派：**逐 tick 到期欄位歸因樁**（FT3 用過的那支）跑在**修法後的樹**上，兩顆種子
⇒ ★**輸出要求**：對每一個 `*_next_tick` 欄位，印它在「間距 60 的尖峰 tick」上的到期次數
   ⇒ ★★**讓排名自己出來**，而不是我列一張嫌疑名單
★★附一格（同一支樁、免費）：**存活隊數掉的那 11–18% 是什麼時候掉的**
   ⇒ 若集中在**前 4320 tick** ⇒ 指向「第一次評估被推遲」⇒ §11.4 的機制成立
```
