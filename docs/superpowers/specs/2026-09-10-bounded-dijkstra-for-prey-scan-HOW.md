# HOW spec：獵物掃描的真正成本 —— ★**每隊一次【全圖 Dijkstra】**，而它算完就丟掉一大半

owner: systems ｜ 2026-09-10 ｜ **WHAT 授權**：blueprint 裁「(c) 先行」
（把每次喚醒變便宜 ⇒ 33-50 ms／隊 → 1 ms 級 ⇒ 17 隊風暴 ＝ 17 ms ⇒ **兩條憲法的相撞自動消失**）

---

## ⓪ ★★★上一版的診斷【錯了一層】—— 而正確的那一層更好修

```
我上一版寫：「每支隊對每個 discovered 隊跑一次尋路 ⇒ N=135 ⇒ 18,000 次尋路」。
★而我去讀 `catch_cost`（path_system.gd:54-66）之後發現：**它已經有快取**，
  而且是比 R² 提的【位置對】更好的形式：
     `_sssp_cache[world][from_key] = _dijkstra(state, from)`   ← ★**單源、全目的地**
  ⇒ 同一個 `from`，對【所有 to】只算一次。
⇒ ★★所以真正的成本【不是 18,000 次尋路】，是 **135 次【全圖單源 Dijkstra】**
   —— 每支正在決策的隊各一次，因為 `from ＝ 它自己的 tile_pos`。
```

★**而快取為什麼救不了**（★這一格是【推論】，要量）：

```
`from` ＝ 那支隊【當下的位置】，而隊【每 tick 都在動】
⇒ ★下一次它決策時 `from` 已經換了 ⇒ **新的 key ⇒ 新的一次全圖 Dijkstra**
⇒ ★★**快取的命中率可能接近 0**，而它看起來像一個有在運作的快取。
⇒ ★★★**這是【待驗】，不是結論** —— 驗收①就是量它。
```

---

## ① ★★★修法：**把 Dijkstra 收邊**（語意不變）

```
現況：`_dijkstra(state, from)` 算【整張圖】的最短成本。
★而 `estimate_catch_up`（:257-259）**把 `eta > AI_ETA_LIMIT` 的通通丟掉**：
     eta = cost × BASE_MOVE_TICKS ÷ relative_speed
     if eta > AI_ETA_LIMIT: return { reachable: false, reason: "too_far" }
⇒ ★★**遠的那一半【算完就丟】** —— 而 Dijkstra 是【由近而遠】展開的，
   ⇒ ★★★**只要在成本超過上界時停止展開，結果一模一樣。**
```

**上界怎麼取（★保守，語意不變是硬要求）**：

```
cost_bound ＝ AI_ETA_LIMIT × max_relative_speed ÷ BASE_MOVE_TICKS
★而 `max_relative_speed` 要取【這個世界可能的最大值】（不是這一隊的）——
  ⇒ ★★因為同一份 SSSP 結果會被不同速度的隊共用，
  ⇒ ★★★**上界取太緊 ＝ 改變語意**（某隊本來搆得到，變成搆不到）。
⇒ 實作要點：`_dijkstra` 多一個 `cost_limit` 參數，超過就不再 push；
   ★快取的 key 要**包含 cost_limit**（否則一個緊上界的結果會被寬上界的呼叫重用 ⇒ 靜默錯誤）。
```

---

## ② 驗收

| # | 格 | 判準 |
|---|---|---|
| ① | **★快取命中率（先量）** | 現況 `_sssp_cache` 的命中／未命中次數（★大世界 N ≥ 130）⇒ **若命中率其實很高，本票的前提就錯了** —— ★★先量再改 |
| ② | **★★語意不變** | `estimate_catch_up` 的回傳值（`reachable`／`eta`／`reason`）在改前改後**逐次相同**；★★★這是本票的地基 |
| ③ | **fp 不變** | 同 seed 同窗 ⇒ fp 完全相同（★依界限第十八條，附 ② 那格行為證據，不單腿） |
| ④ | **★★★成本掉下來** | `loop2.solo` 的**絕對 us／隊**：改前 33-50 ms ⇒ 目標 **1 ms 級**（blueprint 的目標）；★**在 N ≥ 130 的世界量**，並同時報 tick 數／遊戲天／規模三軸 |
| ⑤ | **喚醒風暴** | 一個 tick 內 17 隊同時被喚醒的情境 ⇒ 那一 tick 的 wall-time；★★這一格是**兩條憲法相撞是否消失**的直接證據 |
| ⑥ | **成對對照** | 把 `cost_limit` 拿掉（回到全圖）⇒ ④ 必須回到 33-50 ms；★沒有這格，④的綠證明不了是收邊造成的 |

---

## ③ 風險

```
①★**上界取太緊 ⇒ 語意變** —— 驗收② 是它的守衛，而它必須逐次比對不是抽樣。
②★★快取 key 沒帶 `cost_limit` ⇒ **靜默錯誤**（緊的結果被寬的呼叫重用）
  ⇒ ★★★而它的症狀是【某些隊突然搆不到本來搆得到的目標】—— 那會被誤讀成世界變了。
③★若①的命中率其實很高 ⇒ 本票的前提錯 ⇒ **停下來重新診斷**，不要硬改。
```

---

## ④ 這張票【不做】

```
①不改 `AI_ETA_LIMIT`（★改它＝改決策語意）
②不做距離預篩（★那是【誰進得了候選】的語意問題，要 blueprint 裁）
③★不動 `find_prosperity_prey` 的任何一個 `continue`
④★★不碰 belief-gate（`:238-243` 那一段是感知鐵律的東西）
```

---

## ⑤ ★而我要記下這張票的來歷（★它是今天第三次改寫）

```
①第一版：「前移濾網」⇒ R² 查實那兩個【不是 gate】⇒ 零效果，作廢。
②第二版：「位置對快取」⇒ ★我自己去讀 code，發現**它已經存在，而且是更好的單源形式**。
③★★本版：**真正的成本是【每隊一次全圖 Dijkstra】，而它算完丟掉一大半。**
⇒ ★★★三次改寫的共同教訓：**我前兩次都沒有去讀那支函式的內部。**
   —— 而 R² 第二次替我讀了一層，第三次是我自己讀的。
```

---

## ⑥ ★★★R² 回件（2026-09-10）：**CLEAN，直接 dispatch** —— 而我那兩格「未驗」他都給了確定答案

### (2) 上界【不用猜】：`estimate_catch_up` 用的速度函式是 `path_system` 自己那支簡化版

```
`path_system.gd:183-187`（★不是 movement_system 那支含坐騎/車輛/地形/超載的完整版）：
    static func _team_speed_mult(team: TeamData) -> float:
        var mult: float = 1.0
        mult *= clampf(1.0 - team.fatigue, 0.1, 1.0)
        # Hook 預留 speed_class（未實作）
        return mult
⇒ ★**只有疲勞懲罰** ⇒ 回傳值的數學上界 ＝ **1.0**（fatigue=0）。
⇒ ★★而 `:256 relative_speed` 要嘛 ＝ self_speed（≤1.0）要嘛 ＝ self−target（只會更小）
   ⇒ **全函式的 relative_speed 上界就是 1.0**，不是一個要另外查證的世界常數。
⇒ ★★★所以 `cost_bound` 是【可證明的】，不是估的：
     cost_bound = AI_ETA_LIMIT × MAX_SPEED_MULT ÷ BASE_MOVE_TICKS = 1200 × 1.0 ÷ 240 = 5.0
```

**R² 的要求（防未來 drift）＋★我補的第二個 drift 源**：

```
①★R²：**不要把 `1.0` 寫成裸數字** —— `_team_speed_mult` 檔頭自己標著
  「Hook 預留 speed_class（未實作）」⇒ 將來接上坐騎/載具時上界就不再是 1.0，
  ★★而 `cost_bound` 會【悄悄變太緊】——語意壞掉而**沒有任何報錯**。
  ⇒ 做法：`const MAX_SPEED_MULT := 1.0` 放在 `_team_speed_mult` **旁邊**（兩處一起看得到），
    `cost_bound` 讀這個常數。
②★★★而我補一個 R² 沒點名的【同型 drift 源】：**`BASE_MOVE_TICKS` 也不是 240**
  —— `movement_system.gd:5 const BASE_MOVE_TICKS := TimeScale.MOVE_TICKS_PER_HEX`
  ⇒ ★它是【時間尺度】的函式（時間統一 wave 已經改過它一次：×5→1）。
  ⇒ ★★所以 **`cost_bound` 絕不可以寫成字面 `5.0`**，必須在執行期由那三個具名量算出來。
  ⇒ ★★★**同一個形狀有兩個來源，R² 點名了一個** —— 而只修一個的結果，
    是下一次動 TimeScale 的人把語意改壞而閘全綠。
```

### (3) 「還有沒有別的維度」：★**code 自己的註解已經回答了**（我沒讀到那幾行）

```
`path_system.gd:26-33` 逐字寫著：
  「terrain 只在 world gen / game_setup 寫，runtime 永不變 → cost 圖靜態
   → 單源最短路算一次永續有效」／「若未來 runtime 改地形，須呼 clear_sssp()」
⇒ ★這是設計時就確認過的不變量，不是本票要驗的東西。
⇒ ★★R² 查實：`_dijkstra`/`catch_cost` 的唯一輸入是 `state.world.tiles` 的地形
  （經 `TERRAIN_COST` 換算）⇒ **沒有第二個會變的維度**。
⇒ ★★★而 `clear_sssp()`（:36）已經存在且已被接上（:50）
  ⇒ key 多一維【不影響它】（整層清）。
```

★**而我自己讀 caller 圖時補到的一格（R² 沒提，它讓本票更便宜）**：

```
`catch_cost` **全庫只有一個 caller**：`estimate_catch_up:246`。
⇒ ★所以實務上【只會存在一個 cost_limit 值】
  ⇒ ★★`cost_limit` 進 key **不會造成記憶體翻倍**（不是每個 from 存兩份全圖）——
    它是一張**便宜的保險**，防的是「將來有人用不同上界呼叫」那一天。
⇒ ★★★而反過來也成立：**上界可以無條件套用**，不必做成 opt-in 參數的兩條路徑。
```

### (1) 命中率：★R² 同意「先量再改」，並給一個**支持「可能不是 0」**的線索

```
★我的推論（隊每 tick 都在動 ⇒ from key 常換 ⇒ 命中率≒0）只對【正在移動的隊】成立。
★★而 `_sssp_cache` 的 key **只認位置、不認是哪支隊在問**（:58 `per_world.get(fk, {})`）
  ⇒ 命中不只來自「同一隊回到原位」，也來自**別的隊剛好從同一格查過**
    （例如多支隊在同一個 outpost 待命輪流決策 ⇒ 互相命中彼此的快取）。
⇒ ★★★驗收①的量法補一刀（判準不變，只是【怎麼切】）：
   **不要只報一個全域命中率** —— 拆成【移動中的隊】vs【駐守/待命的隊】兩組分別報，
   否則一個混合平均數會把「我的假設在哪一半成立」蓋掉。
```

### 其餘

```
驗收②~⑥、③風險段、④不做的事：R² 無異議。
blueprint 裁 (c) 先行、拒 (a) K-cap（排程器不得決定誰先活）：記錄在案，無異議。
```
