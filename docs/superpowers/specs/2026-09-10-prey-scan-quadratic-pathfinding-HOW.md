# HOW spec：獵物掃描的 O(N²)×尋路 —— ★26 秒凍結的【真正來源】

owner: systems ｜ 2026-09-10 ｜ 觸發：`loop2.solo` 絕對數字回來之後的兩行算術

---

## ① 數字（★不是推論，是量出來的除法）

```
量測員（warring_states 90 天窗，直接讀 raw log）：
   tick=129600  `loop2.solo` = 4,458,883 us  teams = 135 ⇒ ★**33.0 ms／隊**
   tick= 90780  `loop2.solo` = 6,670,512 us  teams = 132 ⇒ ★**50.5 ms／隊**
implementer（N ≈ 20-50 的世界，同一段 code）⇒ **26 us／次**
⇒ ★★差 **1270× ／ 1944×**，而**不是次數多，是【每次都貴 1000 倍】**。
```

★**兩個數都是真的** —— 差別在世界規模，而**那正是這一整天反覆出現的那條**
（兩個數字要能比，前提是同一個母體；而母體包含【規模】）。

---

## ② ★★★來源（file:line，讀出來的）

```
`_evaluate_solo` → `DecisionContext.gather` → `FactionAISystem.find_prosperity_prey`
   faction_ai_system.gd:244   for tid in state.team_discovered.get(team.team_id, []):
   faction_ai_system.gd:250   if not BeliefSystem.has_belief(...): continue
   faction_ai_system.gd:251   ★var catch_result := PathSystem.estimate_catch_up(state, team, tid, true)
   faction_ai_system.gd:252   if not catch_result.reachable: continue
⇒ ★**每一支正在決策的隊，對它【每一個 discovered 的隊】跑一次尋路。**
   N ≈ 20  ⇒ ~400 次尋路／pass
   N ≈ 135 ⇒ ★★**~18,000 次尋路／pass**（45×），再乘上每次尋路本身隨地圖變貴
   ⇒ ★★★**1000 倍完全在合理範圍 —— 這就是那 4.5 秒。**
```

★**而它與既有帳的關係**：`memory` 裡那條「LOD 真根 O(N²)／目標 50 隊」
—— ★★**今天它第一次有了【具體的呼叫點】**，而不是一個泛稱。

---

## ③ ★而錯開票【已經把凍結解掉了】—— 本票解的是【總量】

```
錯開後：135 隊 × 33 ms 分散到 60 個 tick ⇒ ★平均 2.25 隊／tick ≒ **74 ms／tick**
⇒ 4.5 秒 → 74 毫秒，★★**而總工作量一點都沒少（零 LOD，沒有偷工）。**
⇒ ★★★所以本票【不是急件】：**單幀已經不卡了**。
   它解的是【這個世界總共花在獵物掃描上的時間】—— 那影響的是**吞吐**（能跑多少天／多少隊），
   ★而不是用戶感受到的凍結。
⇒ **序：排在體驗窗之後、與攻擊門同批或之後（由 blueprint 排）。**
```

---

## ④ 修法（★三層，由淺到深；**本票只做第一層**）

```
第一層【語意不變】★本票範圍：
  ①**先過便宜的濾網，再尋路** —— 現況是 `has_belief` ⇒ 立刻尋路。
    ★而 `:262 weakness`（belief armed vs self armed）與 `:280 trip`（糧夠不夠）
      **都不需要尋路就能算** ⇒ ★★**把它們移到 `estimate_catch_up` 之前**，
      擋掉的候選就不必付尋路的錢。
    ⇒ ★★★**語意完全不變**（同一組 continue、同一組結果），只是順序換了。
  ②**尋路結果 per-tick 快取**：同一個 tick 內，`(team, tid)` 的 `estimate_catch_up`
    ★多支隊會重複問同一對 ⇒ 快取命中率要**量出來**再決定值不值得。
第二層【語意可能變】★不在本票：對 discovered 集合做距離預篩（例如只看最近 K 個）
  ⇒ ★★那會改變「誰進得了候選」⇒ **決策層語意** ⇒ 要 blueprint 裁。
第三層【架構】★不在本票：把「可達性」變成一個**世界層的共享結構**（而不是每隊各自問）
  ⇒ ★★★那是 LOD 真根那條 arc 的東西。
```

---

## ⑤ 驗收

| # | 格 | 判準 |
|---|---|---|
| ① | **★語意不變** | `find_prosperity_prey` 的**回傳值**在改前改後**逐隊逐 tick 相同**（★這一格是本票的地基：第一層修法**不准**改變任何一個決策） |
| ② | **★★fp 不變** | 同 seed 同窗 ⇒ fp 完全相同（★★依界限第十八條，同時附 ① 那格行為證據 —— 不單腿） |
| ③ | **尋路呼叫次數掉下來** | 逐 pass 統計 `estimate_catch_up` 的呼叫次數 ⇒ ★改前／改後對照；★★**這一格要在【大世界】量**（N ≥ 130），小世界看不到 |
| ④ | **★★★每隊成本掉下來** | `loop2.solo` 的**絕對 us／隊** ⇒ 改前 33-50 ms，改後多少；★**主詞是絕對值不是佔比**（今天的教訓） |
| ⑤ | **快取（若做）有命中** | 命中率 ＋ 記憶體；★若命中率低 ⇒ **不要留著它**（一個沒命中的快取只是多一份狀態） |

★**誠實限**：③④ 必須同窗同 config 同規模（★三軸：tick 數／遊戲天／規模）。

---

## ⑥ 這張票【不做】

```
①★不改任何一個 `continue` 的條件（只改順序）——語意不變是本票的地基
②不碰第二層（距離預篩）與第三層（世界層可達性結構）
③★★不順手改 `estimate_catch_up` 本身 —— ★★★它是別人的東西，
  而本票的論點是「少呼叫它」，不是「讓它變快」
```
---

## ⑦ ★★★R² 回件：**本票的兩個部分【都是空的】—— 全部作廢並改寫**

### (1) 第一層（前移濾網）＝ **零效果**，因為那兩個【根本不是 gate】

```
★R² 查實：`weakness`（:262）與 `trip`（:280）**是【計分項】不是【濾網】** ——
  整個函式從 `has_belief`（:250）之後到 `return` 之間**沒有第二個 `continue`**。
⇒ ★★**重排它們對尋路呼叫次數的影響是【零】**，不是我以為的「省一半」。
⇒ ★★★~~§④ 第一層~~ **作廢**。
```

### (2) 第二層（per-tick 快取）：★我的 0 命中直覺對，**而真正能省的地方在我自己劃的線外**

```
★照 spec 寫的 `(team, tid)` 當 key ⇒ 命中率確實 ≒0（每支隊各自 gather，組合天然不重複）
  ⇒ ~~這一項~~ **砍掉，沒有損失**。
★★而 R² 往 `PathSystem.estimate_catch_up`（path_system.gd:230-260）**內部多讀一層**：
   真正貴的是 `:246 cost = catch_cost(state, self_team.tile_pos, tgt_pos)`
   ⇒ ★★★**它的輸入是【兩個位置】，不是兩個隊的身分**
   ⇒ 同一 tick 裡不同隊問到【相近／相同的起訖位置】（例如聚在同一個市集附近的幾支隊
     評估同一批鄰近目標）⇒ `catch_cost(posA, posB)` **會重複算**
   ⇒ ★**快取的鍵應該是【位置對】不是【隊身分對】。**
⇒ ★★而它要動 `PathSystem` 內部 —— 而我 §⑥③ 明寫「不順手改 `estimate_catch_up`」
   ⇒ ★★★**我劃的那條線，正好把唯一可行的修法排除在外。**
```

★**這個形狀值得單獨記**：

```
**我在【知道省在哪】之前就劃了範圍** —— 而那條線是照【誰的東西】劃的（PathSystem 是別人的區），
★★而真實的成本分佈不照所有權走。
⇒ ★★★規矩：**範圍要在【知道成本在哪】之後才劃**；
   在那之前只能寫「範圍待定，先量」。
```

---

## ⑧ ★改寫後的本票（★取代 §④⑤⑥）

```
本票【作廢】，由兩件取代：
(A)★**位置對快取票**（語意不變，範圍在 `PathSystem` 內）：
    `catch_cost(state, posA, posB)` 的 per-tick 快取，★鍵＝【位置對】
    ⇒ 驗收：①回傳值逐次相同（語意不變）②★命中率要量出來 —— 命中率低就不要留
      ③尋路成本（絕對 us）改前後對照，★★在【大世界】量（N ≥ 130）
    ⇒ ★★★而它的範圍【本來就在 PathSystem】，不是「順手改別人的東西」——
      **是這張票的正確歸屬。**
(B)★★**語意預篩掛號**（不開票）：在尋路之前用【便宜的距離上界】篩掉遠得離譜的候選
    ⇒ ★它會改變「誰進得了候選」⇒ **決策層語意** ⇒ 要 blueprint 裁
    ⇒ ★★而在 (A) 的數字回來之前【不呈】：若 (A) 就夠了，這一件不必打擾世界。
```

## ⑨ ★而 R² 的 (3) 我照收：**錯開票的殘餘 spike 可能比我猜的嚴重**

```
我用平均值算「74 ms／tick」（135 ÷ 60 × 33 ms）——★而最壞的 tick 不是平均。
⇒ ★★而它已經在我派出去的那一格裡：**錯開票的效能證據要重跑在大世界，
   量【單 tick 最壞 `loop2.solo` 絕對 us】前後對照** ——
   ★★★那一格會直接給出殘餘 spike 的真值，而不是我的平均值。
⇒ 在那個數字回來之前，**本票（A）的序不定**。
```
