---
status: DRAFT(待 R²)
owner: systems
slice: observe-on-snapshot
authority: blueprint 裁 2026-09-01 —— ★scope 升級准、結構修【提前插隊】;優先序理由＝QA 判決工具可信度 > arc 進度
law: 用戶立法「觀測不得改變被觀測物」
---

# ★①為什麼不再逐寫壓制（★三連敗，路線死刑）
```
①`_begin_observe` 列【RNG + Probe】       ⇒ 漏掉 state 寫入
②`known_issues:653` 列【三項副作用】       ⇒ 其中一項已修（★過時）
③R² 查證列【idle_employ 一處】             ⇒ 實測一次 gather 改【3 個欄位】，而那顆沒觸發（★指錯）
★★三次失敗方式都不一樣（漏一類／過時／指錯）⇒ ★★★這不是「清單沒列好」，是【清單這個做法】不成立
```
★**而最嚴重的一項不是欄位**：`:273 ensure_fresh ⇒ WorldEvents.emit("labor_crisis")`
⇒ ★★**觀測可以發事件 ⇒ 事件會瞬醒隊伍 ⇒ 觀測【憑空製造一次決策】。**

# ★★②結構修：**觀測跑在唯讀快照／複本上**
```
★目標：★★【結構上無法寫真世界】—— 包含【無法 emit 事件】
⇒ 觀測入口取得的是【複本】,寫進複本的東西隨複本一起丟掉
```
## ★★★而這裡有一個會讓「隔離」變成假的坑，我先寫死
```
★GDScript 的 duplicate() 對【物件參考】是淺的
⇒ ★★若 WorldState 內含 PersonData/TeamData 之類的物件參考,淺拷貝之後
   複本與真世界【共用同一批物件】⇒ ★★★寫進去照樣穿透
⇒ 而那正好是【黑名單的第四次失敗】：以為隔離了,其實只隔離了一層
★★要求：拷貝完整性【由驗收證明】,不由讀 code 保證（見 §4①）
```

# ★★★③兩層，而第二層【今天就能上】並且會驗證第一層
```
第一層（結構）：觀測跑在複本上
第二層（守衛，★便宜）：★★觀測窗【前後】各取一次 state_fingerprint,不同 ⇒ 立刻爆
   ⇒ ★★★它不防止污染,但讓污染【立刻現形】而不是靜默
   ⇒ ★而它同時是第一層的驗證器：★★若複本沒隔乾淨,這顆守衛會叫
```
★**誠實限**：**fp 斷言的覆蓋 ＝ fp 的組成** —— ★★**fp 不含的欄位被改了它不會叫**
（★★★而我自己立過「改 fp 組成時該次比較作廢」那條 —— 同一個限制的另一面）。

# ★★④驗收
```
①★開 tracer vs 關 tracer，★★三跑 byte-identical
   ⇒ ★★★這條同時證明【拷貝完整】：若淺拷貝穿透，fp 會不同
②★★★【新，blueprint 加】觀測下 `labor_crisis` 型 emit ＝ 0
   ★失敗長相＝觀測仍能發事件 ⇒ 隔離沒蓋到 WorldEvents 這條路
③★陽性對照【走構造】不要靠隨機撞（★★R² 上一輪的要求，而它上一輪就抓到東西）
   ⇒ 構造一個【一定會走到 ensure_fresh】的情境
④★第二層守衛的陽性對照：★★手動在觀測窗內改一個 fp 涵蓋的欄位 ⇒ 守衛必須爆
   ⇒ ★★★否則那顆守衛是裝飾（今天已經有一顆掛在被動路徑上的前例）
```

# ★⑤不做
```
★不逐寫壓制（★★路線已死刑）／★不改 gather 本身（讀寫分離是排隊層另一票）
★★★不宣稱「所有觀測路徑都安全」—— 本票只蓋 specimen tracer 這一條；
   其餘觀測入口【列出來】,而「觀測路徑」沒有引擎窄口 ⇒ 那是【下界】
```
