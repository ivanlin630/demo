# specs/_archive — 已落地/被取代的舊 slice spec 封存

merged 或被後續設計取代的 per-slice spec 移此(封存日 2026-07-17,≤2026-07-15 全下沉)。

- durable 設計本體在 `game-design.md`/`invariants.md`/`progress.md` + auto-memory,非這些 per-slice scratch。
- 保留供溯源(git 亦有 rename 記錄)。
- 頂層 specs/ 只留 current arc(在飛/近期 merged 待 fast-follow)。
- 後續:slice merged + 無 in-flight 依賴 → mv 進來。
