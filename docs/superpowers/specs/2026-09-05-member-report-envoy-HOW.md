# HOW spec（草案，送 R²）— **成員位置回報：事件驅動信使**

**裁定（blueprint 2026-09-05）**：**不保證、隨往來 —— 但成員有腦，大事會派人送信。**
(a) 事件驅動**窄用** ＋ (c) 搭便車；**(b) 排程直投【否決】**（繞過相遇模型的特例走廊）。
**組織視野 ＝ 四源疊加 belief（親見／相遇／順風車／事件信使），零保證零特例。**

## §1 ★查證後，範圍比預期小（★(c) 已經存在）
```
★`message_system.gd:_exchange_intel` 【已經】在相遇時交換 belief entries:
   `BeliefSystem.best_estimate(giver, tgt)` ＋ 扭曲(mode) ＋ HOP_DECAY
⇒ ★★所以 (c)【搭便車】在結構上【已經存在】—— 不需要新做
★★★而 `known_member_states` 是從 `best_estimate` 導出的（faction_ai:863）
⇒ ★任何 belief 的改善【自動流過去】⇒ 【零改 consumer】
⇒ ★★所以本 slice 只剩 (a):【事件驅動的信使】
```

## §2 範圍（★只做 (a)）
```
①★在【位置級大事】發生時,由成員派 envoy 給領袖 —— 複用既有 `_dispatch_envoy`（faction_ai:1717）
②★★事件三種（blueprint 指定）:落腳建營／遷移完成／瀕危求援
③★★★零新語意:不新增訊息型別、不新增投遞路徑 —— 只是【在既有事件上呼既有函式】
```

## §3 ★★★【已驗，R² 直接查出來】前提成立，而**有 +1 tick 時差**
```
★dispatch 那一 tick:SYSTEMS 順序裡 vision 在 faction_ai 【之前】⇒ envoy 那 tick 才被創建 ⇒ 趕不上 vision
★★下一 tick:envoy 仍與母隊【同格】⇒ ★★★吃到【共位必見的 dist==0 保證】⇒ record_claim
   ⇒ envoy 帶著那筆 claim 上路 ⇒ 抵達時 `_exchange_intel`（雙向）轉交給 leader
⇒ ★【零 payload 要加】—— 信使自己就是載體,而它帶的是【它親眼看到母隊的那一筆】
```
★★**而這是兩刀的複合**：**共位必見讓信使「看得到自己的母隊」，而那正是它要送的東西。**
★★★**控制床的關鍵時序（R² 點名，否則會得偽陰性）**：
```
★【不要】在 dispatch 同一 tick 查 belief —— 那時 claim 還沒產生 ⇒ ★★會讀成「沒回報」
⇒ ★★★必須在 dispatch【+1 tick 之後】查
```

## §3b（原文，已被上方查證取代）★必須先驗的一個前提（★我不假設）
```
★假設:envoy【抵達】時,既有的 arrival intel exchange 會把【母隊的位置】帶給領袖
⇒ ★★而我【沒有驗證】這一條 —— 它決定本 slice 是「只要派信使」還是「還要帶 payload」
⇒ ★★★驗法:控制床 —— 母隊在遠處觸發事件 → envoy 抵達 → 檢查領袖的
   `known_member_states[母隊]` 有沒有更新成【母隊當時的位置】
⇒ ★若沒有 ⇒ 才需要在回報訊息裡明確帶 pos（而那仍是既有 params 欄位，不是新管道）
```

## §4 驗收
| # | 判準 |
|---|---|
| 1 | ★**控制床**：孤立成員觸發位置級大事 ⇒ envoy 派出 ⇒ 抵達後領袖 `known_member_states[該員]` **有位置** |
| 2 | ★★**徵收「無目標」下降** —— ★★★baseline **改用同窗同床的數字**：**90 日 × 3 seed ＝ 140／1062／108**（★★★而**不是**先前那個 12 日窗的 31：**不同窗的數字不是同一個量**）<br>★三張都是 **100% 由「belief 沒有位置」造成**（seed42 極端：1223 次贏、1062 次因不知道對方在哪而落跑）<br>★★**不要求歸零**（失聯仍該存在） |
| 3 | ★★★**失聯仍然可能**：存在【從未回報且從未相遇】的成員 ⇒ 領袖對它仍是 `(-1,-1)` |
| 4 | envoy 發送頻率：★每 seed 每 90 日的總數 ＋ 分事件類型（★★「不塞世界」要有數字，不是宣稱） |
| 5 | determinism 三跑一致（★`fp` 會變）＋ ★★零 RNG（派送不得耗 global RNG） |
| 6 | 憲法閘 PASS ＋ 17 支全綠 |
| 7 | ★**零新語意**：diff 顯示沒有新訊息型別、沒有新投遞路徑（★★只有事件點呼叫既有函式） |

## §5 不在範圍
```
★①人格化回報行為（誰勤於回報）—— blueprint 明說留給資訊 arc 思考層
★★②(b) 排程直投 —— 已否決
★★★③`known_member_states` 的導出方式 —— 不動（★四源疊加靠 belief 自己聚合）
```
