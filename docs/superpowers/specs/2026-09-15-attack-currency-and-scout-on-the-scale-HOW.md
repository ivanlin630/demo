---
owner: systems
status: 待 R²
date: 2026-09-15
supersedes: `2026-09-15-attack-loot-monotone-compression-HOW.md` §②（CAP 留白格已取消）、§⑦ 第二步
---

# ★★★這張票的一句話（藍圖裁，逐字進 spec）

> **「我不知道它有多肥」的正確反應是【去看】，不是【假設它一文不值】。**

# ① 病（已由 R² CONFIRMED 窮盡）

`faction_ai_system.gd:379-384 _belief_richness()` —— **三個 return、兩種單位**：

```gdscript
:380  if bel.has("coin_est") or ... : return (coin_est + food_est + material_est) / 100.0   # ★不同單位直接相加，除一個手填的 100
:382  if bel.has("resource_scale") : return float(bel.get("resource_scale", 0))              # ★★直接回 0..3 的【等級編號】
:384  return 0.0                                                                             # 中性 fallback（皆無資料）
```
⇒ ★**72% 撞頂的真因是「等級編號剛好等於除數」**（`clampf(3/3,0,1) = 1.000`；實測 tier1 p10/p50/max ＝ 2.0/3.0/3.0 逐字吻合）。

# ② 第一步：**單位對齊**（只動有分項情報那條路）

`:380` ⇒ **`Σ (各項估值 × TradeValuation.BASE_PRICE[該項])`**，**拿掉那個手填的 100**。
★**單位的來源不是發明的**：`derived_payoff` 用的就是 `BASE_PRICE` ⇒ **兩側同幣別**。

# ③ ★★★第二步：**型別可行性 —— 估不出價值的目標【不進攻擊排序】**

`:382` 那條路（只有等級編號）⇒ **不產生 richness 值**，而該目標 **不出現在攻擊 candidate 集合裡**。

## ★這一格的實作【必須是結構上的】，不是少加一項

```
R² 的血證：`score = (richness*greed + weakness*cruelty + border*ambition) / eta_days * logistics`
   ⇒ **A ＝ X ＋ Y（X 缺席）** 與 **A ＝ 0 ＋ Y（X＝0）** ⇒ **印出逐位元相同的分數**
⇒ ★★**所以「不參與」若只是把那一項拿掉，它與「算成 0」在行為上不可分辨**
⇒ ★★★**必須讓那個目標【不被產生成 candidate】** —— 而不是「產生了但少一項」。
```

**驗收格（必須能分辨這兩者）**：
- 同 `weakness`／同 `border` 的兩個目標，一個有分項情報、一個只有等級編號
  ⇒ ★**後者【不得出現在候選清單裡】**（不是「出現但排最後」）。
- ★★**反向對照**：把後者的情報補齊 ⇒ **它必須出現**。
  ⇒ **兩格成對，否則「永遠不出現」與「機制沒接上」不可分辨。**

## ★而「只修第一步、第二步維持現狀」是【不安全】的（★算術，非量測）

```
今天    有情報：600 / 100 ＝ **6**      只有等級：**0..3**   ⇒ 量級剛好同級（★巧合，不是設計）
修完①  有情報：600 × 2  ＝ **1200**     只有等級：**0..3**   ⇒ ★★量級差【幾百倍】
⇒ ★★★低情報目標永遠排最後 ⇒ **那不是保守，是靜默判死** ⇒ **①②必須同一刀。**
```

# ④ ★★★第三步：**偵查進秤，不是自動派**（藍圖硬格）

```
「不進攻擊排序」＝ **型別可行性**（估不出價值的東西不能被排序）—— 合法
「所以去偵查」   ＝ **一個 option**，★**要跟其他選項在同一把秤上比**
⇒ ★★★**禁走廊**：不得在「不進攻擊排序」之後直接 `try_set(TASK_SCOUT)`／直接派遣。
```

## 偵查 option 的價值：**解鎖資訊的期望價值 × 折現**

- **折現**走既有 `DiscountedFlow`；★**慎重人格 → δ**（`discounted_flow.gd:27-30 delta_of` 讀「慎重」，
  而**不是**貪婪 —— 用戶 2026-08-21 已裁「貪婪≠短視」）。
- ★★**單位必須是 coin 當量** —— 因為它要**進外層 argmax** 與攻擊／貿易等 option 同場比較。
  （★對照：票乙那條「先弄到錢」的 PV **只排內部序、不進外層**，所以那裡不受此限。**兩者不同，別抄錯。**）

## ★★★留白格（**要 R² 打**）：「解鎖資訊的期望價值」怎麼**導出**而不手抄

**禁手抄物理**（本專案立法）⇒ 不得填一個常數。候選形狀（**我列，並說明各自的毛病**）：

```
(i)  **機會成本形式**：偵查的價值 ＝「可能找到一個【比我現在最好的目標更好】的目標」
     ⇒ 用**本隊目前已知目標中最高的那個 richness** 當基準（★純自身狀態，零 god-view）
     ⇒ ★毛病：若本隊什麼都不知道，基準是 0 ⇒ **偵查價值也是 0** ⇒ **最需要偵查的隊最不想偵查**
(ii) **自估 base rate**（R² 找到的非 god-view 變體）：用本隊**已升到高情報**的其他目標的
     richness 取中位數當「這個世界一般長什麼樣」
     ⇒ ★★毛病：**是新機制不是接現成線**（`belief_system.gd` 沒有跨目標聚合），工程量大一截
     ⇒ ★★★且它與 (i) 有同一個病的變體：**一個都還沒升級過的隊，母體是空的**
(iii)**兩者取大**（有已知就用已知、沒有就用 base rate）
     ⇒ ★毛病：**它把「我不知道」拆成兩種來源而沒有回答【什麼都不知道時怎麼辦】**
```
★**要 R² 打的問題**：**「一個什麼都不知道的隊，它的偵查價值該是多少？」** ——
★★**三個候選全部在這一格塌成 0**，而**那正是這張票要解決的情境的極端版**。
⇒ ★★★**若這一格沒有非手抄的答案，那就要老實寫進 spec：這個機制在【全盲】狀態下不會啟動** ——
**而不是填一個讓它啟動的數字。**

## ★★★留白格【已裁】：採 (iv) —— 而正當性不是「有先例」

R² 找到先例：`belief_system.gd:37 ARMED_FRACTION_BASE`（戰力迷霧：不知道對方武裝多少
⇒ 一個具名、標 TEST VALUE、靠 measure 調的世界參數）。
★**而我不用「有先例」當理由** —— **先例是滑坡**。正當性在這一句：

> ★★★**問【世界答得出來嗎】** ——
> **答得出而你手填 ＝ 掩蓋（禁）；答不出而你手填並誠實標示 ＝ 設計選擇（合法）。**

```
「一支隊該持有多少錢」      ⇒ 世界答得出（＝它要買的東西的預算）⇒ 手填＝掩蓋 ⇒ ★禁
「對一個【毫無情報】的目標，預設它可能值多少」
   ⇒ ★★**世界答不出來，因為【沒有情報】就是前提** ⇒ 任何「導出」都需要資料，而前提是沒資料
   ⇒ ★★★**它在定義上不可導出** ⇒ **設計選擇，合法。**
```
（本專案自己的話，`discounted_flow.gd:22`：「★真參數 — 在真實量上劃線＝設計選擇。
世界答不出『應該幾天／該折多少』—— 而答不出就是它該留的證明」）

### ★★★而它必須帶三道守衛，否則就是下一個桁號

```
①**具名**（例：`SCOUT_VALUE_BLIND_PRIOR`），不得埋在式子裡
②**標 TEST VALUE ＋ 明寫「世界答不出，所以這是設計選擇」**（不得偽裝成推導來的）
③★★★**它是【先驗】不是【替代值】** —— **一旦真情報到手，它必須被取代、不得繼續參與**
   ⇒ ★**驗收要一格證明「情報到手後那個 prior 不再被使用」**
   ⇒ ★★**因為舊病就是這個**：`resource_scale` 的桁號本來只是一個粗略印象，
     卻被當成【價值】一直用下去。
```

## ★★★而 R² 查出一件改變本票規模的事實：**全盲是【預設狀態】不是邊角**

```
`game_setup.gd:705-734` 創世知識只種 **`team_discovered`**（知道對方【存在】）
grep `team_intel` game_setup.gd ⇒ **零命中**（從不種【它值多少】）
⇒ ★**每一對剛互相發現的鄰居，預設就落在 `:384 return 0.0`（皆無資料）**
  —— ★★**這比 tier0/1（好歹有一個桁號）情報更少，而它今天【已經】在攻擊候選池裡**
  （現有 code 沒排除它，只是巧合地跟 tier0/1 一樣被讀成 0）
⇒ ★★★**所以「全盲不啟動」的代價不是尾巴，是【世界剛生完那段時間的大半個母體】**
  ⇒ 而它會是「偵查恆不出現」—— **與舊病（tier0/1 恆輸）只是換了個名字。**
```
⇒ **這件事已呈藍圖**（他裁「情報不足退出攻擊排序」時，並不知道情報不足是預設狀態）。

# ⑤ 驗收（★三格，成對）

1. **不進排序**：低情報目標**不出現在候選清單**；補齊情報後**出現**。（★成對）
2. **偵查真的在秤上**（★★母體照 R² 改：**每一次 argmax 呼叫、且候選集裡確實含偵查 option 的那些次**
   —— ★**用【候選集裡有沒有】這個結構事實篩，比用隊的情報標籤篩精準**：
   **標籤是分類，候選集才是決策當下真的比較過的東西**；
   ★★★否則【生成失敗】會混進【生成後輸掉】，把勝率稀釋或扭曲）：
   ★**要有它【輸掉】的樣本** ——
   ★★**若偵查 100% 贏，那就是走廊換了個皮**，而那違反藍圖那一格。
3. **單位**：偵查 option 的 util 與攻擊 option 在**同一個量級**（coin 當量）
   ⇒ ★印出兩者的分布，**不是只印偵查有沒有 fire**。

## ★★★藍圖複核：裁決【仍成立】，而他把代價重新框成特性（2026-09-15）

> **開場大半母體進不了攻擊排序【不是代價，是特性】** ——
> **戰國的第一幕本來就該是互相摸底的【偵查時代】。**

★三道先驗守衛：**准**。

### 追加驗收兩格（藍圖）

```
④**早期窗偵查存在性**：在【世界剛生完】那段窗裡，偵查 option **真的出現並且有贏過**
   ⇒ ★**否則「偵查時代」只是一個說法** —— **而世界實際上是【什麼都沒做】**
   ⇒ ★★而它與②的【要有輸掉的樣本】**成對**：一個防「恆不出現」、一個防「恆贏」。
⑤**先驗被取代的逐筆證據**：同一個目標，情報到手前後各一筆
   ⇒ ★★★**證明那個 prior 【不再被使用】，而不是【被加進去】**
   ⇒ ★**逐筆，不是聚合** —— 聚合答不出「這一筆用的是哪一個」。
```

### ★★★量測紀律連動（藝圖，而它溯及既有讀數）

> **戰爭類讀數的窗必須【蓋過偵查時代】，否則會量出【假和平】。**

★**而這條與今日那條【每個數字帶窗】不同軸**：
那一條是**窗有多長**，這一條是**窗在哪裡** ——
★★**一個夠長但全落在偵查時代的窗，會誠實地量出一個假的和平。**
⇒ **不加閘**（用戶 2026-09-10）⇒ **行為規矩**：
  戰爭類讀數引用時，**除了天數＋config＋seed，還要講【它從第幾天開始】**。

## ★★★定稿裁定（2026-09-16，藍圖）：**零情報 ≠ 薄情報**，而**兩個【先驗】是兩回事**

### ★我先認一個混淆（藍圖抓的）

我把「先驗」當成一個東西，而它是**兩個**：
```
【攻擊側先驗】捿一個【目標身價】讓盲打算得出來  ⇒ ★**禁** —— 它假裝知道別人多肥
【偵查側先驗】承認不知道，所以【去看】有價值      ⇒ ★**准**（三守衛版仍有效）
⇒ ★★★**兩者答的是不同的問題**：一個對【目標】下斷言，一個對【行動】定價。
```

### 裁定：C —— **門檻管 admission，而且因人而異**

```
【零情報】`:384` 皆無資料   ⇒ ★**對任何人都不可行**（結構排除，連膣大者也不行）
【薄情報】`:382` 只有 `resource_scale`
   ⇒ ★★**admission 由人格門檻決定**：`BeliefSystem.confident_enough(state, team, prey, 慵重)`
     **膣大者門檻低 ⇒ 可盲打；慵重者門檻高 ⇒ 該目標對他不可行**
   ⇒ ★★★**情報不足從【世界的性質】變成【這個人跟這個目標之間】的性質。**
偵查照舊上秤（禁走廊不變）；`faction_ai_system.gd:440` 走廊票 **不撤**。
```
★**而門檻不是新機制**：`confident_enough` 第四個參數**本來就是慵重** ⇒ **把既有那顆接出來**。

### ★★★更正（2026-09-16，R² 量出尺度）：**不是傾斜，是【路開了但走不通】**

```
`faction_ai_system.gd:366`
   score = (richness × greed + weakness × cruelty + border × ambition) / eta_days × logistics
★各項的界：
   weakness  `:306-308` clampf(..., 0.0, 1.0)      ⇒ **[0,1]**
   border    `:319`     1.0 或 0.3                 ⇒ **[0.3,1]**
   greed/cruelty/ambition ＝人格值            ⇒ **約 [0,1]**
   richness  **單位對齊後** ＝ Σ(估 × BASE_PRICE) ⇒ ★**無上界，動輄破百**
⇒ ★★**差 2–3 個數量級** ⇒ **其餘兩項在數值上變得不相干**
⇒ ★★★**「膣大者可盲打」實務上約等於「只有沒有更好目標時才會贏」**。
```
⇒ **所以不得寫【已知傾斜】** —— **要寫【路開了但走不通】**：
★**兩句話對下一個人的意義完全不同** —— 前者讓他以為只是偏弱，後者告訴他這條路實際上沒有被走。

### ★★★而它比盲打大：**單位對齊會把一個三項的秤變成一項的秤**

```
今天：richness ＝ (coin+food+material)/100 ≈ 6，或桁號 0–3 ⇒ ★**與其餘兩項同數量級，三項可比**
對齊後：richness 破百 ⇒ ★★**殘忍與野心在「選誰打」這個決策上失效**
⇒ ★★★**我們修好一個病，而它會打壞一個目前成立的平衡。**
⇒ **已呈藍圖（WHAT）** —— **本 spec 在那一裁回來之前不 dispatch。**
★**而我要認一件事**：我取消原本那個 CAP 格時，理由是「coin 值沒有 0–1 天花板」
  —— ★★**我只想到【撞頂】，沒想到【同一個和式裡的三項必須可比】。**
```

### ★（保留）膣大者那一側的語意仍然成立

膣大者接受一個薄情報目標之後，**那個目標的 `richness` 項仍然沒有合法值**
⇒ ★**而這次它不是掩蓋**：**人格已經明示接受【在不知道身價的情況下打】**
  ⇒ **語意上就是「不把身價算進去」** ⇒ 它靠 `weakness`／`border` 與人競爭。
★★**而傾斜是真的**：同場比較時，**厚情報目標有 `richness` 加分、薄的沒有**
⇒ **膣大者實際上仍會很少盲打**。
⇒ ★★★**我【不】加補償** —— **補償就是捿值**；
  **而把它寫在這裡，是為了日後有人量到「盲打很少發生」時，知道那是【已知的設計傾斜】不是【斷鏈】。**

### 驗收追加兩格（★成對）

```
⑥**膣大者真的盲打過**：高膣量領袖對薄情報目標 **有進候選並且贏過**
⑦**慵重者沒有**：同一個薄情報目標，高慵重領袖 **它不在候選裡**
⇒ ★**兩格必須【同一個目標、不同人格】** ——
  ★★**否則「門檻有接上」與「目標本來就不同」分不開。**
```

# ⑥ 不做

- **不動壓縮**（原 spec §② 的 CAP 留白格**已取消** —— 單位對齊後很可能不需要）。
- 原 spec §⑧ 標著**【舊尺上的幾何】**的數字（0.900／1.232／72%／典型值 0.27）
  ⇒ ★**單位對齊後全部要重算，現在不得引用。**
- **不改 `score` 的公式形狀**（正規化／風險調整）⇒ 那是後續架構題。
- ★**`tier` 語意本來就該是 confidence 而不是 value**（我與 R² 獨立到達同一點）
  ⇒ **記錄為後續架構題，本票不動它。**
