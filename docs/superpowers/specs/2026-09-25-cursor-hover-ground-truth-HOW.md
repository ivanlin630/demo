# 游標懸停印整格真值（debug）——HOW spec（systems 2026-09-25）

用戶裁（逐字）：**「先顯示真值 我好 debug」**。blueprint 開票、意圖帳 #42 已補句。
現況 `text_ui_main.gd:939-962` **已經是真值**，只是①要按 Enter ②欄位少 ③沒有標示。

## ★★★§1 這張票最重要的一條：它是【明裁的 god-view 例外】，而例外要有牆

```
★感知鐵律（invariants.md）：決策／reaction 只能吃 belief，不能吃 god-view 真值。
★★而本票是【用戶明裁的例外】——它的正當性【只存在於「這是畫給人看的」這件事上】。
⇒ ★★★所以 HOW 的核心不是「怎麼印」，是【怎麼讓它結構上流不進決策】：
  ①真值只在 **render 路徑**上被讀，**不得寫進 `state` 的任何欄位**（含不得快取進 `player_state`）
  ②不得回傳給 `query_player`／不得進 `command_results`／不得進 `_events`
  ⇒ **沒有儲存，就沒有消費者** —— 這比「規定大家不要讀它」強，因為它不依賴任何人記得。
```

## §2 要做什麼

```
①游標移動時【即時】印（不用 Enter）
②欄位擴到整格：地形／收成係數／全部 resources／據點型別+等級+主人／
  格上所有隊（含人口、勢力）／（若有）控制方／tile id
③★區塊標題固定印：**「真值·debug（非附身者所知）」**（★機械可查，見 P2）
```

## ★★§3 而標題那一行不是裝飾，它防的是一個【認識論】的坑

```
★用戶拿真值 debug 時，會看到【附身者不知道的事】
⇒ ★★於是「AI 怎麼這麼笨」與「AI 根本不知道」在畫面上長得一樣
⇒ ★★★那一行字的作用，是讓每一次觀察都自帶【它的來源標籤】——
  否則用它得出的每一個關於「遊戲合不合理」的結論，都建在一個玩家拿不到的資訊上。
★同一件事的另一半：blueprint 已說【玩家版（讀 `team_tile_known`）另存小票不作廢】。
```

## §4 驗收（★每一格的負對照與判準寫在同一行）

```
P1 [即時] 移動游標【不按 Enter】⇒ 該格真值出現｜負對照：把即時觸發拿掉 ⇒ 必須紅
P2 [標題在] 卷面 grep 得到「真值·debug（非附身者所知）」｜負對照：改掉那行字 ⇒ 必須紅
P3 [★★不寫 state] 連續移動游標 N 次 ⇒ **world-fp 逐字不變**
   ｜負對照：故意把真值快取進 player_state ⇒ 必須紅
   ★★★母體地板：那 N 次要**真的移動到不同的格**（否則「fp 不變」在【游標沒動】時恆真）
P4 [票4 不得退化] 連呼 `node._refresh()` 兩次 ⇒ `_state_label.text` 逐字穩定（票4 的 P1b）
   ｜★這一格不是新加的，是**不准這張票把上一張剛還掉的債借回來**
P5 [★空格與滿格都要走] 游標移到【沒有任何隊的格】與【有多支隊的格】各一次 ⇒ 都不炸、都印得出
   ｜負對照：把多隊那一支的迴圈上限寫死成 1 ⇒ 必須紅
P6 [ui-flow] 綠｜P7 [電池] merge 前全電池｜★★交玩前照 merge 檢查表第 8／9 步
```

## ★★★§4b 併入的第二件：「推進一小時」鍵（blueprint 轉用戶裁 #6）

```
★鍵位【我定】：**`X`** —— 理由是量出來的，不是順手挑的：
  全檔 `KEY_X` 出現 **0 次**（已占用：, . W S A D Enter M Space G H R P I T F O U V K Esc Q Z，
  另有 0-9 在輸入模式、1-9 在另一個模式）★★而它就在 WASD 游標群旁邊，好按。
★★★**不得新開一條推進路徑**：照 `KEY_SPACE` 那一支的寫法呼叫 **既有的**
  `_bridge.request_advance(WorldState.TICKS_PER_HOUR)`
  ⇒ ★理由是今天剛踩過的那條：**兩個推進路徑其中一個沒跟上** —— 我們不再製造第三條。
★必印進頁腳 keymap（★玩家不需要記得它，**畫面要記得**）。
★Esc 可中斷 ＝ 沿用 SPACE 那一支已經有的行為，不另外做。
```

**驗收（負對照與判準同一行）**：

```
P8  [有這個鍵] 按 X ⇒ `current_tick` 增加 **TICKS_PER_HOUR**
    ｜負對照：把它改成 `TICKS_PER_DAY` ⇒ **必須紅**（★守的是「一小時」不是「會動」）
P9  [同一條路] ★grep 斷言：`request_advance` 的呼叫端**只有既有那些**，沒有新的推進實作
    ｜負對照：另寫一支繞過 `request_advance` 的推進 ⇒ 必須紅
P10 [頁腳有字] 卷面 grep 得到頁腳 keymap 裡的 `X`｜負對照：把那一項拿掉 ⇒ 必須紅
P11 [Esc 中斷] 推進中按 Esc ⇒ ★真的停下來（`current_tick` 小於 TICKS_PER_HOUR）
    ｜★★母體地板：那一輪要**真的推進中**（否則「停下來了」在【根本沒開始】時恆真）
P12 [fp] ★零 sim 改動 ⇒ **world-fp 逐字不變**｜負對照：順手改一行模擬 ⇒ 必須紅
```

## §5 不在本票

```
✘ 玩家版「已知資訊」（讀 `team_tile_known`）—— blueprint 明說另存小票
✘ 任何讓決策端讀得到這份真值的接線 —— ★那正是 §1 要擋的
```
