---
slice: 每小時一次的**全世界 pass** —— 凍結的真凶（`sim_runner.gd:358`）
owner: systems
status: ★**DIAG 先行**（2026-09-22）——★★★**先問那 1.2 秒花在哪，再談架構**；修法票尚未開
基於: implementer 兩輪量測（真凶定位 ＋ 尖峰⇒凍結那一環），四臂既有 log／CSV，未重跑世界
---

# §1 ★前提（file:line 坐實，systems 開檔核過）

```gdscript
# scripts/simulation/sim_runner.gd:358      （★main 上的行號）
if state.world.current_tick % NEAR_CADENCE == 0:     # sim_runner.gd:13  NEAR_CADENCE = WorldState.TICKS_PER_HOUR = 60
    var all_teams: Array = state.teams.keys()        # ★全世界的隊，一次進同一個 pass、零錯開
    …
    var pass_r: Dictionary = _run_systems(state, all_teams, NEAR_CADENCE, …)   # :401
```
★來源是第⑧票（2026-09-06）拆掉 near/far 分班：「全世界一個 pass、一個 cadence，取 near 的 60」。
★★**它是模閘不是排程欄** ⇒ 只掃 `*_next_tick` 的靜態掃**天生看不到它**（掃過兩輪各 0）。

# §2 ★★★數字（★全部來自既有 log，沒重跑）

```
pass tick 的 dt 中位數      1.10 ~ 1.44 秒
非 pass tick 的 dt 中位數   79 ~ 93 微秒
⇒ ★★★**約 15,000 倍**
凍結(>2s) ⇒ 尖峰 ＝ **100%**（桶 0 與桶 1-2 的凍結率 0.00%，四臂皆然）
尖峰 ⇒ 凍結 ＝ **13.4% ~ 33.6%**（★必要非充分，與「11+ 桶」同形）
```
★★★**而真正該看的不是那條 2 秒線**：**沒過線的 pass tick 也已經是 1.2 秒**
⇒ **2 秒是連續體上的一個懸崖，不是病的邊界** ⇒ ★**目標不是「少幾個 >2s」，是「這個 pass 不該是一整塊 1.2 秒」**。

# §3 ★★★第一步：**不選架構，先做相位拆解**

```
★理由：1.2 秒 ÷ ~100 隊 ＝ **每隊 12 毫秒** —— 那是一個【大得可疑】的數字
   ⇒ ★★**先問它花在哪**，再決定要不要動結構。**現成的量具就有**：
     `phase_timing` / `_pht("near.*")` / `[FaiPhase]` spike dump（`sim_runner.gd:140`）
⇒ **要的輸出**：pass tick 的 dt **按 phase 拆解**，兩顆種子，印母體與樣本數
⇒ ★**若某一個 phase 佔掉大半** ⇒ 修那一個，**架構不用動**（最便宜的出口）
⇒ ★★若**攤平在很多 phase 上** ⇒ 才是結構問題 ⇒ 進 §4
⇒ ★★★**三格，不是兩格**：還有一格是**幾個中等的頭** ⇒ 那要修 2~3 處，仍不必動架構
```

# §4 架構選項（★**等 §3 回來才選**，此處只記形狀與代價）

```
(甲)**分片**：每隊的 pass 落在 `tick % 60 == mix(team_id) % 60`
   ⇒ 每隊頻率不變、工作攤平到 60 個 tick
   ⇒ ★★**但違反不變量 #8 的警告**：隊的**生與滅 100% 發生在這個 pass 裡**
     ⇒ 同一小時內各隊看到的世界狀態不再一致 ⇒ **語意改變，且我們今天剛看過這種改變的代價**
(乙)**幀分片＋邊界快照**：語意不變（仍是同一個邏輯 tick），只把**牆鐘工作**攤到多幀
   ⇒ ★★★**它可以做到 fp 完全相同** ⇒ **驗收硬閘 ＝ fp 必須【不變】**
     （★與窗 #3 相反：那次 fp 變是預期；**純 perf 修法若改了 fp，它就失敗了**）
   ⇒ ★代價：`advance_tick` 有 **263 個呼叫點** ⇒ 只有「第二個 driver」路線可行
(丙)**降低 pass 內單隊成本** ⇒ ★由 §3 決定它存不存在
★**我的傾向**：**丙 → 乙 → 甲**。甲最便宜而**它改世界**，乙最貴而**它可以不改世界**。
```

# §5 ★不做的事

```
✘ 不因為「2 秒」去調那條線（★那是報表閾值，不是病）
✘ 不在 §3 回來之前選架構
✘ 不用 `alive = state.teams.size()` 當母體 —— ★**它含野獸 pseudo-team**（同日血證）
```

---

# §6 ★★★涵蓋率是**交件必印欄**，而**沒被蓋住的那一塊本身就是答案**（systems 2026-09-22 加）

```
implementer 的涵蓋率格**連續咬了三次**：**255.7% → 115.9% → 76.1%**
  ①255.7%：直接加總原始 tot ⇒ **父子都算了一遍**
  ②115.9%：扣掉巢狀仍超過 ⇒ 真身是 `*multi`（被多個外層呼叫的相位，時間已在父親裡）
  ③76.1%：兩者都處理後
★**>100% 是【母體不同源】的指紋** —— 而這一格正是為了它存在的
⇒ ★★★**交件必印涵蓋率**；★★而**未覆蓋的 23.9% 不是誤差，是一格【沒有主詞的時間】**
   ⇒ **它要被當成候選答案列進表裡**，不可以被四捨五入掉
   ⇒ ★若 top-1 只有 16%，而**無主詞的有 24%** ⇒ **最大的一塊其實還沒被命名。**
★巢狀表**不得自己發明**：用 production 的 `PHASE_PARENT`，
  並同時印 `FactionAISystem.phase_report()`（同一份資料、另一支既有的眼睛）
  —— ★**第二份表從出生就開始漂，而漂掉不會紅。**
```

# §7 ★R² 的界線（★免得之後被跳過）

```
・**本票（DIAG）不過 R²**：它不改 production、不改世界，只是量。
・★★★**但 §4 選定架構的那一刻，它就變成設計 slice ⇒ 必須過 R²，且要 WHAT 開窗。**
  ⇒ **不得以「這是同一張票的延續」為由跳站。**
```

---

# §8 ★★★那一格立刻兌現了 —— **「沒有主詞」的第一嫌疑是【我少讀了一本帳】**

```
無主詞 23.96% **在第一本帳裡排第一** ⇒ 追下去發現**有第二本**：
  `SimRunner._ph`（`sim_runner.gd:175`，per-tick，`_pht()` 累加）⇒ ★**涵蓋率 99.8%**
  而原本讀的 `FactionAISystem._fai_ph` 是【FactionAISystem 內部】的 ⇒ 涵蓋 76.0%
⇒ **主表換成第二本**，第一本降級成「鑽進 `near.faction_ai` 裡面的下一層」
★★★**通則**：涵蓋率的缺口，**第一嫌疑不是「世界沒有名字」，是「我少讀了一本」**
   ⇒ ★而它之所以被抓到，是因為那一格**被列進表裡跟別人排序** ——
     若它留在小數點後面當「誤差」，**這本帳今天不會被發現**。
```

## §8.1 ★★兩本帳**不可相加**，但**可以互相驗**（systems 加：這是免費的）

```
`_fai_ph` 是 `_ph` 的 `near.faction_ai` 【裡面】那一層，而 `solo_think` **兩邊都有**
⇒ ★**預註冊的一致性檢查**：
   **第一本（扣掉 `solo_think`）的總和  ≈  第二本的 `near.faction_ai`**
⇒ ★★**不一致 ⇒ 其中一本在對讀它的人說謊** ⇒ **那本身就是一個必須回報的發現**
⇒ ★★★這一格**不用多跑一次**：兩個數字同一輪都已經印出來了。
★而「≈」要寫成具體容許值並在**看數字之前**寫下來（★不是看完再決定幾 % 算合理）。
```

## §8.2 ★污染的數字會翻掉的正是**判準那一格**

```
implementer 的話我原樣收進票：
  **「兩個頭各佔 50%／28%」與「一個頭佔 78%」在修法上是【不同的答案】**
⇒ ★★而那正是三格判準（單一大頭／幾個中等的頭／攤平）在分的東西
⇒ ★★★**所以被 CPU 競爭污染的數字不是「不夠準」，是【會選錯架構】** —— 乾淨跑回來前不選。
```

