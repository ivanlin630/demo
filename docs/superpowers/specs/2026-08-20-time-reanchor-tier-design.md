# 時間重錨+頻率層級制(WHAT / vision)

status: LOCKED(2026-08-20 用戶逐段過目核可[根旋鈕小時制/層級制T0全突發+T2T3試跑+搬家八支/背景律三條/計時相對錨定+小時正典/工期表§3c]→R²;R① 免=前提全數 file:line 坐實於 inventory 檔[02 規則:已坐實則免])
owner: blueprint(WHAT)
溯源:用戶 2026-08-20 時間軸系列討論(遭遇戰動作換算合理性→兩圖同速援軍帳→重錨方案用戶自提→全頻率盤點→層級制用戶提框)。素材=`2026-08-20-time-frequency-inventory.md`(全掃描)。

## §0 命門(用戶定)
- **綁死案復活、零繃帶**:世界格時間=遭遇動作×格數,公式無係數。
- **遭遇戰時間尺**(已入意圖帳):同一時鐘 1:1、動作粒度=速度差解析度地板、戰術速≈行軍速(援軍帳)。
- **世界以時間思考**:所有頻率/時長常數以遊戲時間宣告,tick=推導值。
- **頻率=決策層級**:想多勤由「你在想多大的事」決定,不是每系統自己發明一顆 cadence。

## §1 時間重錨
- **根旋鈕=`TICKS_PER_HOUR`,唯一自由參數(用戶定:以小時為單位,日後可再改)**。初值 60(→tick=1 分=推導結果,非寫死身分);TICKS_PER_DAY 等全由它導;全 code 只講小時/天,換 tick 粒度=改這一顆。
- 遭遇動作=**10 分鐘**(時間宣告;tick 數=TICKS_PER_HOUR/6 推導)。**守衛:動作 tick 數≥10 才保速度差檔位**(time_const_check assert;=TICKS_PER_HOUR≥60 的下限由此而來,調小要先過這關)。
- 世界格=動作×MAP_SCALE=240 tick=**4 小時**(平原);`WORLD_SPEED_MULT` **刪除**;A2 四件套出隊列。
- 行為≈不變(移動 4.8h→4h=唯一小 intended;糧耗/格 -17%);UI 播放 `TICKS_PER_SECOND` ×6 同步。

## §2 常數規約(裸 tick 禁絕)
- 全時間常數以 `TimeScale.hours()` 宣告(**小時=正典單位,用戶定**;`days()`=語法糖);**禁裸 tick 字面量**。
- **★計時相對錨定(用戶定,治「節奏改→計時卡死」)**:凡守護「有預期時長活動」的 timeout/計時 → 一律 `k × 該活動預期時長`(工地=k×預期工期、貿易/JOIN=k×往返 ETA、失聯=k×訊息往返尺度;有 floor/ceiling)——物理/節奏變速自動成比例跟;**找不到自然錨的才准絕對小時**。審計逐顆標錨定對象;護欄型絕對值(如 L0 衰敗 3 天=物理)不在此限。
- 誠實命名:`PER_TICK` 只准真 per-tick;per-呼叫/per-窗改名。速度類附人話換算(格/天、動作/小時)。
- 審計工作清單=inventory 檔 B 派(~20 顆)+§9 魔法數;`debug/time_const_check.gd`=審計入口、擴充成規約守衛(新增裸 tick=FAIL)。

## §3 頻率層級制(T0-T4)
| 層 | 鐘 | 內容 |
|---|---|---|
| **T0 突發** | **事件驅動、無鐘** | **★所有突發事件全入(用戶定、原則性)**:被襲/跨餓線/勞力危機/關鍵情報/領袖死/目睹滅團/⋯——systems 全量盤點事件型別,**任何突發即喚醒相關決策層,不搞白名單挑食**;新增突發事件必掛 T0(與全量暫態可觀測性同族的不變量) |
| T1 操作 | 1 小時 | 物理心跳(採集/消耗/製造/移動/反應窗)——**不做選擇只執行**;不變 |
| T2 戰術 | 1 天(**試跑**) | task 重評/威脅/整併/子隊/徵收(人格調變保留)/俘虜/求援偵察/溢出 |
| T3 戰略 | 3 天(**試跑**) | 繁榮/goal 生成/居留/獨立戰略 ✓已是+**搬家八支**:個人目標(10h→)/野心階梯(10h→)/勢力戰略 AI(10h→)/結盟傾向(30h→)/背叛傾向(50h→)/基建方向(50h→)/派系更新(20h→)/意圖(1 天→,危機 T0 接管) |
| T4 世代 | 月/季 | 成年/野生再生/規劃視野/季節;現值 |
- 層級鐘=死常數**合憲**(=取樣格/基礎設施,同 TICKS_PER_DAY 類;憲法禁的是死常數替引擎做決策)。
- 人格調變=允許,三條件:劇情內理由+有界(floor/ceiling)+對稱(規則人人適用)。
- 公平三保證:同一取樣格/LOD 率等價不變/T0 人人瞬醒(鐘慢不害死人)。
- **試跑=T2/T3 值 provisional**:新基線考憑質地(故事反應性+效能)定案,考後可調。

## §3b 背景律(用戶逐條拍 2026-08-20)
- **W 分類確認**:連續律(率以天計、T1 每小時入帳)/日結律(餓死結算/溢出/prune/L0 衰敗)/週律(薪水 7 天)/月律(成年/野生再生)/季律(90 天)/晝夜律——**未點名者照現值確認**。
- **收成因子=季節曲線**:刪每 6h ±25% 亂擲;因子跟季節平滑走;未來天災/天候=T0 型事件擾動(噪音來自有名字的事件,不來自無名骰子)。
- **tile 池再生加季節**(試跑+平衡警告):野地池再生 ×季節因子,幅度比農田緩(野生態有緩衝);亂數同上=只由天災事件擾動;冬季稀缺 vs 現窮世界的平衡=新基線考同卷看。
- **飢餓/疲勞時間減半**(試跑):飢餓 20→10 天滿(恢復 10→5)、疲勞 21→10.5 天滿(紮營恢復 4.2→2.1)——**保「恢復比積累快」比例**;理由=舊尺度下兩線在正常行程/危機節奏中演不出戲。
- 互參:生育節奏=約一個月/名額(用戶拍 B,本體在 breed spec)——與月律共振。

## §3c 工期表(用戶核可 2026-08-20;宣告單位 person_hours;試跑)
**骨架=紮根(72 人時)當量尺,一顆錨推全表**:L0 紮營=24(⅓,不動)/L0→L1 紮根=72(不動,settlement 已拍)/農田=36(½,舊 7.2)/工坊/藥坊=72(×1,舊 16.8)/馬廄熔爐武坊甲坊=144(×2,舊 33.6)/鑄幣=288(×4,舊 72)/據點 L2=216(×3,舊 30)/L3=432(×6,舊 60);升級×等級倍數維持。方向=全面變慢,工期重新成為真代價(§4b 可行性過濾器咬得住、「不是基建狂魔」物理基礎)。工地取消=k×預期工期(相對錨定)自動跟。**審計必查:舊 BUILD_TICKS 與紮根工期雙軌對帳(誰還在用舊制)**。

## §4 七病同包清(盤點所得)
turn 定義統一(60t vs 24t)/EWMA cadence 相依修(need_hierarchy α 未除 day_fraction=近遠不等價)/移動信念 2 格/日→追物理 5(估算器形狀家族)/manufacturing 率 day_fraction 核對(疑近遠不等價)/魔法數收編(「2 天」×6 處/harvest 6h/player_trade 1000t/日夜段界/危機除數 4)/命名說謊 3 顆/debug 鏡像 drift 2 處。

## §5 驗收
- **統計等價床**:不變項=各系統每遊戲日事件率/速率(重錨純標籤部分必須統計持平;RNG 模式變故不驗 byte)。
- **intended-change 清單**:搬家八支+**徵收 30h→T2 24h(R² 補漏,人格調變 0.5~2.25 倍率形狀保留)**+T0 行為+移動 4.8→4h+糧/格 -17%+背景律三條+工期表——fp 標注、故事 QA 過。
- 新基線長考(與 LOD arc 共用一次)。
- 憲法閘/回歸照常。

## §6 排程與界外
- LOD arc 後同包落地;**T0=LOD arc 事件驅動思考同體**,執行順序合併由 systems 裁(HOW)。
- 界外:未點名頻率值照現值;NEAR/FAR cadence 不動;遭遇戰內部機制不動(只重標時間);頻率值再 tuning=考後憑證據。
