---
slice: ⑦一次 `gather` 內共用 `estimate_catch_up` 的結果（七個呼叫點）
owner: systems
status: **MERGED 2026-09-22**（`69c00dbdb` → main；★合併結果上全套 68 支綠、375s、rc=0）
基於: implementer 規模量測 2026-09-22（世代 6／HW-2，兩個 seed）；WHAT 預註冊門檻（U₇ ≥ 10.0% ⇒ 開修法票）
---

裁定：**開票。U₇ ＝ 19.13%（seed 1337）／19.09%（seed 77）≥ 預註冊門檻 10.0%。**

# §1 卷面（★我自己重算，不是轉貼）

```
        C₇（全部呼叫的 T 佔 gather）   D₇（重算佔比）      U₇ ＝ C₇×D₇
seed1337          0.2753                 0.6948            19.13%  ✔ 我算 0.19128
seed  77          0.2731                 0.6989            19.09%  ✔ 我算 0.19087
★D₇ 構造上界 1−1/7 ＝ 0.857 ⇒ 0.695／0.699 落在界內 ✔（★上界寫進派工信，第二次派上用場）
★★逐點分列加總 28.45% vs 總計時 27.53% ⇒ 差 0.92pp ＝ 3.3%（樁自身開銷）⇒ **C₇ 取較小者，保守** ✔
```

# §2 ★★★方向：**U₇ 是【下界】，而 U₁／U₂ 是【上界】** —— 這一格最容易被講反

```
U₂／U₁：上界 ⇒ 「低於門檻」⇒ 真值更小 ⇒ **結案**站在安全側
U₇    ：下界 ⇒ 「高於門檻」⇒ 真值更大 ⇒ **開票**站在安全側
★下界的兩個來源：①`trade_target` 本輪零呼叫 ⇒ C₇ 缺那一份
                 ②重算集中在【最貴】的三處（8.76%／8.47%／8.36%，呼叫數同量級 27~28 萬）
                   ⇒ 均勻假設低估了節省 —— ★★而這一格是**量出來的**，不是我推的
                   （這正是我在派工信裡要求「逐呼叫點分列」要回答的問題）
```

# §3 ★★★毛值 vs 淨值：**門檻說的是「開票」，不是「這個修法保證划算」**

```
19.13% 是【毛的】—— 它沒有扣掉快取自己的成本（每次查表的 hash＋比較）。
★我把翻盤所需的量算出來（判準持有者的活）：
   每次呼叫成本 ＝ 0.2753 × 91.864s ÷ 912861 ＝ **27.70 µs**
   要把淨值壓到 10% 以下，快取每次查表得吃掉 **9.19 µs** ＝ **一次完整 estimate_catch_up 的 33%**
⇒ ★★一個 Dictionary 查表要花掉一次 A*／SSSP＋belief 讀取的三分之一 —— **不合理，但我沒有量證**
⇒ ★★★所以它進**驗收**，不進推論：**A2 驗的是【淨值】，不是毛值。**
```

# §4 形狀

```
在 `PathSystem.estimate_catch_up` 內部加一層 **per-gather memo**：
  key   ＝ (self_team.team_id, target_id, trusted)
  ★trusted 進 key：今天 7 處全傳 true（我逐點核過），★★而「今天全是 true」不是不變量
  值    ＝ 該次回傳的 Dictionary（★唯讀共用；消費端不得就地改它）
  生命期＝ **一次 `gather` 之內**
  ★★★禁跨 gather、禁跨 tick —— 位置與 belief 會變，而那正是這個修法唯一的危險

## §4a ★★★生命期的**構造保證**（修訂 2026-09-22，implementer 的誠實限引出）

```
★他交了兩條獨立線證「這塊地目前乾淨」：
   【量】gather 進入 677 ＝ 離開 677、巢狀 0 次、且與床自己數的 677 三方對得上
   【結構】`gather()` 本體 502→1454 區間內真正的 `return` ＝ **0 處**（唯一命中是 :821 的【註解】）
   ★★而兩條都要：量測只證【這一輪沒發生】，結構只證【原始碼裡沒有那個形狀】
★★★**而他自己指出兩條線都看不到的那一格**：
   GDScript 的執行期錯誤會【中止那一支 func】⇒ gather 中途炙掉 ⇒ 旗標留在 true
   ⇒ **跨 gather 污染，而進入＝離開的計數與原始碼掃描【都不會亮】**
⇒ ★**所以本票不用「紀律」修它，用形狀修**：
```
**訂正後的形狀（兩層，而第二層才是真正的保證）**：
```
①**清在【進入】不是【離開】** ⇒ 上一次 gather 炙掉也不會把舊值留給下一次
②★★★**每筆 memo 帶一個 `gather_seq`（單調递增整數），讀的時候比對**
   ⇒ seq 不符 ⇒ 視同 miss（重算）
   ⇒ ★★**這讓正確性完全不依賴【清空路徑有沒有被跑到】** ——
      就算清空被跳過、旗標卡住、中途炙掉，舊值也**永遠回不來**
   ⇒ ★成本＝一個 int 比對（對比 27.70 µs 的一次呼叫，徽不足道）
```
★**判準句**：問的不是「清空會不會被跳過」，是「**被跳過了還會不會錯**」。

## §4b ★★★**禁把任何一行掛在 `Probe.enabled` 後面**（R² 找到的地雷，reviewer 2026-09-22）

```
★現場事實（我自己開檔核過）：
   decision_context.gd:503    if Probe.enabled: _in_gather = true
   decision_context.gd:1444   if Probe.enabled: _in_gather = false
   ★★而**每一個讀它的地方也都同樣閘控**（:635／:1081／:1162／:1503／:1510、
     belief_system.gd:214、faction_ai_system.gd:264／:7391）
   ⇒ 它是**純量測旗標**，這樣寫對於它自己的用途而言是對的。
★★★**危險在於它就站在 gather() 的 entry／exit 那兩行旁邊**：
   memo 的清空／seq 遞增若【照樣造句】寫成 `if Probe.enabled: ...`
   ⇒ **production（Probe 關著）永遠不清空**，而 key 不含 tick
   ⇒ 整場模擬累積，回傳任意舊 tick 的 eta／reachable
   ⇒ ★★**而它在 A1 底下也不會被抓到** —— 因為 A1 跑的時候 Probe 是【開】的，
      清空「剛好」照樣發生 ⇒ **地雷與驗收的盲點互相掩護**。
```
**硬規**：
```
★memo 的【清空】與【`gather_seq` 遞增】**必須無條件執行**，不得依附 `Probe.enabled`
  （tap 本身（hit／miss／size_max）可以閘控——★★**記帳可以閘，語意不可以**）
★★★而在 §4a 的形狀下，**整個正確性只掛在【一行無條件的 seq 遞增】上**
  （清空被跳過也不會錯，但 seq 不遞增 ⇒ 舊值全部變成命中）
  ⇒ **那一行就是要被守的那一格**，而 A1' 就是守它的。
```
```
★**為什麼不做得更聰明**（例如跨 tick 快取）：那是**另一張票**（`gather-cross-tick-cache-needs-a-data-structure` 已在 defers）。
★★本票只做「同一次 gather、同一把鑰匙、算一次」——**少做一件事就少一個要證的東西**。


## §4c ★★★**承重的那一行，要由【會紅的東西】守，不是由文件守**（R² 第二輪，reviewer 2026-09-22）

```
★reviewer 指出同一顆地雷【換了位置又出現】：舊版是【清空】被抄去掛 Probe，
  新版是【`_gather_seq` 遞增】被抄去掛 Probe —— ★★誘因一模一樣：
  gather() entry 那幾行現在【同時】站著
     :503  if Probe.enabled: _in_gather = true      ← 該閘控（純量測旗標）
     新增   _gather_seq += 1                         ← ★不該閘控（語意）
  兩行緊挨著，**複製貼上抄錯一個字就是同一種病**。
★★★而他指出的【更嚴重的那一半】，是我 §4b 沒看到的：
   Probe.enabled **預設 false**（我核過：scripts/debug/probe_stats.gd:14）
   而 A3／A5 第一層／A6 都要讀 `catchmemo.hit/miss`，tap 慣例是 Probe 門檻下才記
   ⇒ **這組驗收唯一能跑的配置就是 Probe 開**
   ⇒ 若遞增剛好也掛在同一個門檻下 ⇒ **遞增在測試時發生、在 production 不發生**
   ⇒ ★★**A1～A6 會【全部綠】，而 bug 只在真正的無 Probe 跑法下發作**（玩家／一般 headless）
   ⇒ ★★★**地雷與驗收互相掩護 —— 而這一次是【整張表】一起掩護。**
```
**修法：不是再寫一句「不得依附 Probe」（那還是紀律），是【讓驗收自己跑在 production 配置下】**
```
★關鍵事實（我核過）：**指紋床 `scripts/debug/world_fp_snapshot_bed.gd` 裡【零個 `Probe` 參照】**
  ⇒ **A1／A1' 不需要 Probe** ⇒ ★★**它們就跑在 `Probe.enabled = false`（＝production 配置）**
  ⇒ ★★★若有人把遞增掛到 `Probe.enabled` 後面，**A1 立刻紅**（因為 A1 那一輪 Probe 是關的）
  ⇒ **這樣「不得依附 Probe.enabled」從【文件上的規矩】變成【會紅的一格】。**
★而需要讀 tap 的那幾格（A3／A5 第一層／A6）只能跑 Probe 開 ⇒ **配置在 §6 逐格標明**。
```
★**溢位**：Godot 4 的 int 是 64-bit 有號；本票量級 912861 次／8 天窗
⇒ 要溢位需遠超任何合理遊戲時長 ⇒ **想過了，不擋**（寫下來是為了它不再是「沒想到」）。
★★**「seq 相同但世界已變」**：只要遞增點**只有一處、每次呼叫遞增一次、不條件式遞增**，
每次 `gather()` 都拿到嚴格遞增且互不相同的 seq ⇒ 該情境**在構造上不可能發生**
（★reviewer 的推論，我收：**它不是新風險，是「遞增單點」的推論結果** —— 守住遞增就自動守住它）。
# §5 ★不變量（★這條是我 owner 的，而它管我）

```
**全量暫態可觀測性**：新增 state 必接 tap ⇒ memo 是新 state ⇒ **必須有 tap**：
  `catchmemo.hit` / `catchmemo.miss` / `catchmemo.size_max`（每 gather）
★沒有 tap 的快取＝量測盲點，而下一次有人量 gather 成本時會看不見它。
```

# §6 ★★★驗收（**預註冊：在實作之前寫死**）

```
A1'【★★★A1 的陽性對照，R² 要求，**比 A1 先跑**】刻意破壞版：
   ★**破法就用 §4b 那顆地雷的形狀**——把 `gather_seq` 遞增掛到 `if Probe.enabled:` 後面
     （★★注意：在 §4a 的設計下，「只把【清空】掛 Probe」**不會壞事**（seq 擋住了）
       ⇒ **破壞點要指向真正承重的那一行** —— reviewer 提的破法針對的是修訂前的形狀）
   ⇒ 斷言：**破壞版的 fp 必須【不同於】正版**（同兩個 seed、同窗）
   ⇒ ★★★**若 fp 也一樣 ⇒ 這個窗對這種 bug 沒有鑑別力** ⇒ A1 降為【弱】、**不得當硬閘**，
      且要回頭找一個真的會踩到的窗（非把門檻放寬）
   ★【這格不做，A1「綠」的意義與「沒測」在事實上無法區分】—— reviewer 原句，收。
A1【等價，硬閘】兩個 seed 的**全世界指紋逐字相同**（vs 本票落地前的 main）
   ★注意極性與窗 #2 **相反**：那一票是「沒變才是紅」，★★**本票是「變了就是紅」**
   ⇒ 這一格同時補掉②結案書 §6 欠的那筆：「引數相同 ≠ 答案相同」——
     ★★★世代 6 的讀路徑零亂數只保證【可以共用】，**不保證共用了還一樣**；證它的是指紋不是推理
A2【淨值，硬閘】**帶著快取**量 gather 成本 ⇒ 相對 baseline 的淨省 **≥ 10.0%**（兩個 seed）
   ★這是預註冊門檻的同一個 10.0%，**但驗的是淨值**（§3）
   ★★＜10.0% ⇒ **回退、結案**（不准為了交差留一個省 3% 的快取）
A3【鑰匙對不對】`catchmemo.hit / (hit+miss)` 應 ≈ D₇ ＝ 0.69
   ★★★差很多 ⇒ **鑰匙錯了**（例如漏了 trusted、或 gather 邊界沒對齊）—— 這格是**構造交叉驗**
A4【母體衛生】`estimate_catch_up` 的**總呼叫次數不變**（912861／seed1337 同量級）、四條走訪圈數不變
   ★若呼叫數掉了 ⇒ 不是「省了」，是**控制流被改了** ⇒ A1 也會紅，但這格先講清楚
A5【陽性對照】加一個關掉 memo 的旗標 ⇒ **關掉後 A2 必須失敗**（回到 baseline 耗時）
   ★沒有這格，A2 綠了也不知道是不是 memo 的功勞
   ★★★**而 A5 自己要先證一層**（implementer 補的，收）：**先證那個旗標真的關得掉**
      （關掉之後 `catchmemo.hit` 必須歸零）——
      否則【A5 失敗了】與【旗標根本沒接上】**長得一模一樣**
A6【生命期，§4a】★注射一個【清空被跳過】的情境（強制不清）⇒ **A1 仍須綠**
   ★★因為 `gather_seq` 比對會把舊值當 miss ⇒ 這格在證【正確性不依賴清空路徑】
   ★★★若 A6 紅 ⇒ seq 機制沒接上（而不是「清空很可靠所以沒差」）
```


### §6' ★★★每一格【跑在哪個配置】（R² 第二輪要求，★這一欄本身就是防掩護的機制）

| 格 | 配置 | 為什麼 |
|---|---|---|
| **A1／A1'** | ★**`Probe.enabled = false`（production 配置）** | 指紋床零 Probe 參照 ⇒ 不需要 tap；★★而這正是抓「遞增被閘控」的唯一辦法 |
| A2（淨值） | 兩種都跑 | ★計時需要樁 ⇒ Probe 開量；★★**而淨值的基準跑法要同配置**，否則比的是兩個世界 |
| A3（hit 率） | Probe 開 | 要讀 `catchmemo.hit/miss` |
| A4（母體衛生） | Probe 開 | 要讀呼叫計數 |
| A5（旗標陽性對照） | Probe 開 | 第一層要看 hit 歸零 |
| A6（清空被跳過） | Probe 開 | 要讀 tap 確認 seq 機制在承重 |

★**誠實限（寫給下一個人）**：**A3／A4／A5／A6 這四格【沒有覆蓋 Probe 關的生產跑法】** ——
它們結構上只能在 Probe 開之下跑。★★**覆蓋那條路的只有 A1／A1'**，
⇒ ★★★所以 **A1 的配置不是細節，它是這張表唯一的 production 視角** —— 不准為了方便把它改成 Probe 開。
# §7 誠實限（★沿用 implementer 的，加我的）

```
①C₇ 計時 ⇒ 跨機不可引用（HW-2 專用）；D₇ 次數比 ⇒ 跨機有效、跨代不可引用
②`trade_target` 是否在別的 config／更長窗會跑到，**沒有驗** ⇒ C₇ 是下界
③★快取成本我是**估**的（§3），不是量的 ⇒ 它在 A2 才被真正回答
④窗 8 天、warring_states、隊數 103／94
```

# §8 落地（★我逐一開過檔）

```
docs/measurements/2026-09-22-U7-seven-sites-gen6-HW2.txt        3069 bytes ✔
docs/measurements/2026-09-22-U7-u7-s1337-gen6-HW2.log.gz       99569 bytes ✔
docs/measurements/2026-09-22-U7-u7-s77-gen6-HW2.log.gz         96179 bytes ✔
smoke2（未歸因 1233 那輪）／smoke3（錯假設那輪）／smoke4（修好那輪）      ✔
```

---

# §9 ★★★落地後補記：**A3 抓到的是【我這份 spec 的洞】，不是實作的錯**

```
我在 §4a 寫：「清在進入 ＋ 每筆帶 seq、讀時比對 ⇒ **正確性完全不依賴清空路徑**」，
並下了判準句：「問的不是【清空會不會被跳過】，是【被跳過了還會不會錯】。」
★★★而那句話**預設了「每一次呼叫都在某一次 gather 裡面」** ——
   `seq` 只在 gather【開始】時改變 ⇒ **gather 結束【之後】的呼叫，key 一樣 ⇒ 仍然命中那次 memo**
   ⇒ 而同一 tick 內移動已執行、位置已變 ⇒ **過期的 eta／reachable**
   ⇒ 實測不是零頭：1 天窗 gather 外呼叫 **6134 次**（總 18048 − gather 內 11914）
⇒ ★**這是我的老毛病：二分法少一格**（「這一次」／「下一次」，漏掉**【不在任何一次裡面】**）
```
**修法（implementer 做的，形狀正確）**：加**無條件**的 `_catchmemo_active`
（`catchmemo_begin_gather()` 開、`catchmemo_end_gather()` 關，兩者都不掛 `Probe.enabled`）
⇒ 把生命期拉回 §4 寫的「一次 gather 之內」。★**這是把實作拉回 spec，不是改設計。**

★★**而指出它的是 A3**（hit 率 0.5505 對不上 D₇ 0.69）——
而 **A3 是設計來抓【鑰匙錯】的，這次抓到的是【母體範圍錯】**。
⇒ ★★★**構造交叉驗的價值就在這裡：它不知道自己在抓什麼，它只知道兩個數該相等。**
★而 implementer **沒有照 spec 那句「差很多 ⇒ 鑰匙錯了」直接去改鑰匙**，
他先問「為什麼兩個數對不上」⇒ 才看到生命期超界。★★**照著我寫的結論去修，會修錯地方。**
