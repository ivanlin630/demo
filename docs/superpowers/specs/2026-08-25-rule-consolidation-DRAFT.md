# 改法包【草案】——★未生效、未動手（凍改令下的預備任務）

`from: systems` ｜ **狀態：草案，等用戶討論。★本檔【不改變任何現行規則】。**

## §1 ★★★先報一個我自己也嚇到的數字
**今天（2026-08-25）流程 doc 的新增行數**：
| 檔 | 今日新增 | 現有 | ★占比 |
|---|---|---|---|
| `01_architect.md` | ★**+480** | 673 | **71%** |
| `05_acceptance.md` | ★**+276** | 361 | ★★**76%** |
| `03b_measurer.md` | +241 | 596 | 40% |
| `00_roles.md` | +121 | 414 | 29% |
| `07_mailbox_trigger.md` | +106 | 294 | 36% |
| `invariants.md` | +61 | 824 | 7% |
| **合計** | ★**+1319 行** | | |

★★**這些法不是慢慢累積的，是【一天之內】長出來的，而且長在【同一個上下文】裡**（一連串量測翻船 ＋ 一次工作流停頓）。
⇒ ★★★**風險不是「規則錯」，是【過度擬合當日情境】＋【互相重疊】＋【下一個 session 讀不完】。**
★**同族**：用戶的 doc-strategy 裁定「**domain doc 瘦成 glance-aid**」—— ★**流程 doc 今天卻反向暴漲。**

## §2 ★★★核心提案：**不是再加規則，是【合併與降級】**
**我把今天新增的條目重讀一遍，發現它們其實是【三個母題】的化身。**

### ★母題 A：**「這個數字有資格回答這個問題嗎？」**
**化身**（今日新增）：母體三問→四問／恆真式一～五型（完美比例・前後皆 0・門檻早滿足・空真・沒接到失敗機制）／三種 0／儀器沒開要陽性對照／集合型優於計數型（**但空集合同樣可疑**）／稀有事件只能證存在不能比較／`tick-sample` 把 `n=3` 撐成 `n=328`／**分母本身也是結果**／判準必須可能失敗。
⇒ ★★**十餘條，全部可以掛在同一句下面。**

★**新化身（2026-08-25，blueprint 歸類，我採納他的分類而非我原本的）**：
> ★★**「觀測工具的視野邊界」被當成「世界邊界」。**

**實例**：**我想用 `git rev-list --count main..feat/<slice>` 判「在飛包排空」** ——
**實測 `feat/acquisition-paths-wire-in` 領先 `main` 【0 個 commit】，而那張票明明在飛**（工作在 worktree **未 commit**）。
⇒ ★**我原本把它歸到母題 B（有東西在動 ≠ 事情在前進），blueprint 歸到母題 A —— 他對**：
**問題不在「有沒有在動」，在【我的工具看不看得到】** ⇒ ★★**與母體塌陷（迴圈零迭代靜默通過）同族。**

★**已採納的判準（三件齊才算落地）**：
`git -C .worktrees/<slice> status --porcelain` 空 ＋ branch merged 進 `main` ＋ **判決信 `consumed`**。

### ★母題 B：**「有東西在動 ≠ 事情在前進」**
**化身**：`gate 沒擋 ≠ gate 沒執行`／`fp 不變 ≠ 新路徑有跑`／`落地 ≠ 遞送`／`RUNNING` 遮蔽 `COMMIT-NO-LETTER`／★`assert` **hang 偽裝成跑很久**／★★**殭屍心跳偽裝成 watchdog 活著**。
⇒ ★**blueprint 已看出要併成一題（活著／前進／送達三層）—— 我同意，而且它有 6 個實例。**

### ★母題 C：**「你依賴的那個東西，涵蓋範圍是什麼？」**
**化身**：真相源只蓋部分物理（`REGEN_RATE`）／列舉要挑收斂的那類／出處分類消滅碰撞但取代不了語意／**抄公式沒抄護欄**／`default` 三分類（零使用＝純負債・只有錯誤使用者・收益風險都真）／窮盡≠判準正確／母體滑動。
⇒ ★★**全部是「我以為它涵蓋 X，其實只涵蓋 X 的一部分」。**

## §3 ★提案的具體形狀（★三選一，用戶定）
| 方案 | 做法 | 代價 |
|---|---|---|
| ★**(a) 三母題 ＋ 例子表** | **每個母題一段正文 ＋ 一張【化身表】**（一行一個實例，附血證 commit） | ★**要重寫，但讀者從 1319 行降到 ~3 段** |
| (b) 只加索引 | 正文不動，前面加「三母題 → 條目」對照 | **省事，但 1319 行還在** |
| (c) 不動 | —— | ★**下個 session 讀不完 ⇒ 等於沒立** |

★★**我傾向 (a)**，理由：★**規則的價值在【被記得】，不在【被寫下】。**
★★★**今天有三條規則是【被違反之後才被想起來】的**（我自己違反的：抄公式沒抄護欄、心跳當證據、寫出不可達判準三次）——
⇒ ★**那不是「規則不夠」，是「規則太多以致於用不到」。**

## §4 ★★我【不】提案的事（明說，免得被當漏列）
- ★**不提「刪掉哪一條」** —— **每一條都有血證，刪是用戶的權。**
- ★**不提零產出偵測怎麼做** —— **那要先解 blueprint 說的「送達層」，否則 fire 了沒人聽。**
- ★**不動 `a3e0b4af`** —— 追認或退回等用戶。

## §5 ★附：今天真正改變【遊戲】的東西（供比例判斷）
| 已 merged 且改變行為 | `harvest-terrain-from-regen`（建設候選 `day000-only` → 貫穿 90 天）／`granary blind-view` 修 |
|---|---|
| ★**merged 但【不】改變行為** | ★**`means-end` 磚（dormant，零 caller）** ／ `build-eta` 等價重構（`fp` 不變） |
| **在飛** | `acquisition-paths-wire-in`（接線，讓上面那塊磚活過來） |
★★**這張表是我建議用戶拿來判「工作流 vs 專案」比例的依據。**

## §6 ★★★草案寫完當下就多了一個實例（**現場血證，2026-08-25**）
**measurer 回報：「我從未收到過那張票」。★他是對的 —— 我根本沒寄。**

**兩層都是我的**：
| # | 我做的 | 為什麼沒送達 |
|---|---|---|
| ① | ★**`append` 到一封【已 consumed】的信末尾** | ★★**`status` 不是 `open` ⇒ Monitor 不再喚醒** |
| ② | ★★**在給 reviewer 的信裡寫「已排進 measurer 的隊尾」** | ★★★**對第三方宣告 ≠ 對當事人投遞** |

⇒ ★**母題 B 的第 7 個化身**：**「我以為我派了」≠「他收到了」。**

### ★★而我做的機械防線【漏了它】—— 這點對改法包更重要
`_promise_check` 掃「**已請／已派／已寄／已轉**」四個動詞。
★**我寫的是「已【排進】」** ⇒ **不在列舉裡 ⇒ 漏網。**
★★★**這是母題 C（列舉了會發散的那一類）在【我自己的防線】上再犯一次** ——
**我今天立了那條法，然後被自己的列舉漏掉。**

> ★★**這件事支持 §3 的 (a) 方案**：**規則不是不夠，是【多到用不上】** ——
> **我在同一天內，立了兩條法，然後同時違反了那兩條。**

## §7 ★★★我原論點的【反例】——**規則也可能因為「住錯地方」而失效**（2026-08-25）

**§1-§3 的論點是「規則太多以致於用不上」（今天 +1319 行）。**
★**但同日查到一個相反的例子**：

**`discounted_flow.gd:15-19` 有兩條【用戶定、不可妥協】的「蟑螂地板」**，其中②：
> **`H_eff` 對「會改善被動收入的 option」不得由【執行前】存糧決定 —— 否則 catch-22 換一層。**

★★**而 `goal_resolver.gd:634` 的 `dev_coeff = food_days / DESPERATION_DAYS` 逐字落在這條裡**
（**沒料 → 沒糧 → `dev_coeff` 小 → 蓋糧倉不划算 → 沒糧倉 → 沒糧**）。

★★★**`grep` `docs/invariants.md` ＋ 所有 `docs/process/*.md`：「蟑螂地板」「catch-22」「執行前存糧」＝ 零命中。**
⇒ ★**那條法只活在一個檔的檔頭註解裡** —— **寫 `_candidate_util` 的人不會去讀 `discounted_flow.gd` 的檔頭。**

### ⇒ ★★草案的問題要多一個
| 原本只問 | ★**要加上** |
|---|---|
| **哪些條可以合併／降級？** | ★★★**這條法住在哪裡，才會被【需要它的人】讀到？** |

★**「太多」和「住錯地方」會產生【一模一樣的症狀】：規則存在，但沒有在該用的時候被用。**
★★**而兩者的修法相反**：**前者要刪，後者要【搬過去，甚至複製過去】。**

