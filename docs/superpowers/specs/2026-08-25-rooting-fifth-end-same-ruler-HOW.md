---
slice: rooting-fifth-end-same-ruler
tier: full
qa: required
from: systems
topic: 紮根納入折現同尺 —— 四端同秤漏掉的【第五端】(blueprint:分佈坐實則直接修,無需回報)
---

# 紮根：**同秤的第五端**

★**授權**：blueprint 2026-08-25 —— **紮根 ＝ 建點類，用戶折現磚裁定原文已授權「建點／建設類」**
⇒ ★**把 `rooting_drive` 換到折現真流同尺 ＝ 同一裁定的【補完】，不是新 WHAT。**
**分佈坐實即直接修，無需回報。**

## §1 現況（`file:line`）
`DiscountedFlow.flow_utility` 的 caller ＝ **4 個**（`terms.gd:117 / 198 / 211 / 232`）：
覓食・遷移找糧／佔村／併入／紮營。
★**`rooting_drive` 不在其中**，它回傳：
```gdscript
_feasible * clampf(ctx.settle_site_quality, 0.0, 1.0)   # ★[0,1] × [0,1]
```
⇒ **值域 [0,1]、典型值必然很小**（實測 `root_u 0.09~0.14`，對手 `winner_u 1.395`）。

★**真母體**：`terms.gd` 的 `*_drive` 類 term **共 21 個** —— **「四端」是我的工作範圍，不是母體**
（已入負斷言帳，見 `00_roles §覆蓋欄`）。

## §2 ★前置量測（**擋在動工前；可證偽點已寫**）
**要的**：四個 `flow_utility` 消費端的**實際 util 分佈** vs `rooting_drive` 的分佈（min／median／max）。
| 結果 | 判定 |
|---|---|
| `flow_utility` 端典型值**明顯高於** `rooting_drive` | ✅ **尺不同成立 ⇒ 動工** |
| ★兩者**同量級** | ★**推論垮 ⇒ 不做本票**，回頭查 `_feasible` 或 `settle_site_quality` 哪個把它拉死 |

## §3 修法
**`rooting_drive` 改走 `DiscountedFlow.flow_utility`**，與另四端**同一個入口**：
- **flow** ＝ 建成 L1 據點後的**真實被動所得**（同 `camp_target_est` 家族，**禁第二份產能常數**）
- **delay** ＝ ★**工期**（`OutpostSystem.build_eta_days` —— **單一真相源已在**）＋ 殘距
- **baseline** ＝ **真實被動所得**（同磚語意）
- ⛔ **不得為紮根造一次性公式**（折現磚模組頭條）

★**既有的兩項「真值」語意保留**：可行性（撐不撐得過）與選址品質 ——
**它們應該進 flow／delay，而不是外面再乘一次 [0,1]。**

## §4 acceptance（blueprint 指定：**照舊**）
| 面 | 判準 |
|---|---|
| ★**文明化閘** | 同床 **`outpost.l0_to_l1` > 0**，且**不低於 main** |
| ★**四端不退** | 既有四端的 `lost_to` 分佈**不得因此崩壞**（**兩面分開驗，防修回頭**） |

★**n 很小要當心**：`l0_to_l1` 在這個世界是**個位數**
⇒ ★**不得用 1 vs 0 宣告成敗**（我犯過一次）——**要看多 seed 或更長窗。**

## §5 閘
`headless` ／ `det×3`（**預期會變 ＝ intended-change；但 ★acceptance 用分佈不用 fp**）／
`constitution_gate` ／ `seam-gate`（HARD）

---

## §6 ★★分佈回來了：**混合結果** —— 我預寫的規則沒涵蓋這種

| 消費端 | median | vs 紮根 `0.307` |
|---|---|---|
| **紮營** | **1.585** | ★**5.2 倍** |
| **併入** | **1.125** | ★**3.7 倍** |
| 覓食／遷移找糧 | **0** | ★**不支持**（★measurer 自標：**受 truncation ＋ 異質零值疑慮，不宜單憑此否證**） |
| 佔村 | ★**母體 ＝ 0** | **本輪完全測不到** —— ★**閘沒執行，非閘擋**（他用了我們的分辨法） |

### ⚠️ 我要先認一件事：**我的可證偽點寫成了二選一**
我寫的是「**明顯高於 ⇒ 成立** ／ **同量級 ⇒ 推論垮**」。
★**實際是混合的，而我沒有為混合結果預留判讀。**
⇒ **教訓**：**預寫判讀規則時，要問「有沒有第三種結果」** ——
**二分法的判讀規則，遇到混合就等於沒寫。**

### ★★精確結論（**照實拆**）
**在【真正與紮根同池競爭、且有非零值】的對手上（紮營／併入），紮根低 3.7～5.2 倍**
⇒ ★**第五端假說【在這些對手上成立】。**
**覓食／遷移找糧 median 0** ⇒ **那多半是「多數時候不適用」，不是尺小**（applicable 問題，非尺問題）。

## §7 ★★★但這裡有【兩個不同的尺差】，不要混為一談
| # | 對照 | 倍數 | 修法 |
|---|---|---|---|
| ① | 紮根 vs **同池 static option**（紮營／併入） | **3.7～5.2×** | ★**本票（納入 `flow_utility` 同秤）** |
| ② | 紮根 vs **goal candidate**（`build_workshop` 等，`winner_u 1.395`） | **10～40×** | ★**本票【不解決】** —— 那是**兩條管線之間**的尺差 |

★★**修 ① 不會自動解決 ②。**
**②是「static option pipeline」與「goal candidate pipeline」的尺差** ——
★**與「四端同秤」是同一個病、但發生在更高一層**（**兩條管線沒同秤**）。

⇒ ★**acceptance 不得假設「修完紮根就會贏」** ——
**它會贏得過同池的紮營／併入，但仍可能輸給 `build_workshop` 那條管線。**
**若重跑後仍輸給 goal candidate ⇒ 那是 ②，不是本票失敗。**（★**先寫死，免得事後爭。**）

## §8 動工判定：**做**
blueprint 授權「**分佈坐實即直接修、無需回報**」。
★**①坐實（同池 3.7～5.2 倍）⇒ 動工有依據**；**②照實寫在票上，不混進本票的成敗。**

---

## §9 ★★前瞻警告：**本票 acceptance「文明化恢復」很可能也達不到**（2026-08-25 提前標）

**失敗磚的 28 筆前提型證據顯示**：★**紮根【有在嘗試】，但每次卡建材閘、【從未派出】。**

⇒ ★**修好本票（納入同秤）會讓紮根【贏更多】，但【贏了也派不出去】。**
⇒ ★★**「文明化恢復（`outpost.l0_to_l1 ≥ main`）」同樣依賴【建材閘】這件本票以外的事。**

**⇒ 依 `05_acceptance §跨票依賴` 提前標**：
> ★**`blocked-by: 建材閘`（`_dispatch_builder` 的「資源不足 1.5×」）**
> **本票的成敗判定 ＝【紮根的 util 是否與同池對手同尺】，不是【文明化有沒有發生】。**

★**這是提前標，不是事後補** —— **我剛因為「跨票依賴沒標」在失敗磚上被咬過一次。**

