# HOW spec（草案；★待 R²）— **發不發薪是【預算】問題，不是【身分】問題**（第⑥票）

WHAT 來源：blueprint 裁 2026-09-05（源於 team8 的坐實）。
★**意圖帳缺一行**：`docs/mechanism-intents.md` **目前沒有薪資/PRODUCE 這一行** ——
「村民自食其力，村長非家臣」只存在於 `salary_system.gd:30` 的**註解**裡，**不是裁過的意圖**。
⇒ **拔它不牴觸帳；但新規則要補一行進帳（blueprint 的欄位，我不代填）。**

## §1 病灶（★file:line）
```gdscript
salary_system.gd:29-32
func _pay_salary(state, team) -> void:
    # 居民 PRODUCE team 不走薪資系統（村民自食其力，村長非家臣）
    if team.tags.has(TeamData.TAG_PRODUCE):
        return
```
★**這是【身分閘】**：用一個 **tag** 決定經濟行為，**不看團庫、不看有沒有勞動** ⇒ **補丁閘**。
★★而它與 `interaction_system.gd:1509`（settle 追加 `TAG_PRODUCE`）合起來 ⇒ **所有定居下來的隊都停止發薪**
—— 包括 **config 唯一帶錢的商隊 team8**（day90 仍活著、1000 coin **三個時間點恆為 1000.00**）。

## §2 動作（★一刀，de-patch）
```
★刪 salary_system.gd:30-32 的 early-return
⇒ 發不發薪回歸【既有的預算約束】:payroll → coin_avail → budget_ratio → paid
⇒ ★★有記名勞動 ＋ 團庫有錢 ⇒ 發;沒錢 ⇒ 【發不出來】(誠實),★★★而不是【假裝不需要發】
★零新機制、零新常數 —— 這是【拔掉一個閘】不是【加一個機制】
```

## §3 ★★★預先登記的三個後果（★寫在動工前，不是事後解釋）
| # | 預測 | 為什麼 |
|---|---|---|
| 1 | ★**team8 開始從 1000 發薪 ⇒ 那筆錢第一次動** | blueprint 指定的驗收；★**它是【世界自己造出來的陽性對照】**（一個 90 天完全沒動過的量，動了就是這一刀造成的） |
| 2 | ★★**匿名池水位從 0.00 變成非 0** | `config/peaceful_economy.json` **12/12 隊都有 `anon_tiers`** ⇒ `salary_system.gd:75-77` 的 `anon_paid` 會 deposit 進 `anon_treasury`（★而它現在**一筆都沒有**） |
| 3 | ★★★**全世界每週出現 underpay 懲罰 ＋ `UnrestBank.add(team,1,"salary")` ＋ 一行 print** | 多數團庫是空的 ⇒ `budget_ratio≈0` ⇒ `ratio<1` ⇒ **忠誠扣分**。★**這是【新的、全域的】壓力，先前完全不存在** |
```
★★★而第 3 條【我不預先加緩衝】—— 加了就是把剛拔掉的補丁換一個位置貼回去
⇒ 但它【必須被印出來並被看見】:驗收要有 unrest／忠誠的前後對照,★而不是只報「錢動了」
```

## §4 ★與⑤（團內稅分軌）的交互 —— **⑥讓⑤從 0 變成非 0**
```
⑤的源扣繳掛在 salary_system.gd:64-67,而那段【現在從來跑不到】
⇒ ★⑥落地後,所得稅【第一次真的會抽到東西】
⇒ ★★所以⑤的驗收 #2c(「90 天床上稅收預期就是 0」)在⑥之後【會過期】
   ⇒ ★★★批後同 seed 重跑時,那一格要改寫成【非 0】,而不是留著舊結論
★序:⑤先(它改的是同一段 code 的內部);⑥後(它決定那段 code 跑不跑)
```

## §5 驗收
| # | 判準 |
|---|---|
| 1 | ★**team8 的 coin 不再恆為 1000.00**（★★逐日印它，**不是只印期末** —— 期末相同可能是「發了又收回來」） |
| 2 | ★★`salary_named` / `salary_anon` 的**次數與總額從 0 變成非 0**，★**per-team**（★★★而**哪些隊仍然是 0** 要可讀 —— 那是「真的沒錢」不是「沒跑到」） |
| 3 | ★★★**匿名池水位**：期末 per-team `anon_treasury` ＋ 全程最高/中位（現況全 0.00） |
| 4 | ★**unrest ／ 忠誠的前後對照**（★★預測 3 的代價，要被看見不能被解釋掉）。★★★**逐發薪日印**（day7／14／21…），**不要只印窗期聚合** —— R² 查證：`SALARY_INTERVAL` 是**全域同步、無 stagger** ⇒ 這個代價是**逐 7 日的尖峰**不是平滑分布，**聚合會把尖峰平均掉讀成噪音** |
| 5 | ★**鑑別力**：把這一刀撤掉，判準 1〜3 **必須全部變回 0** —— ★★若某一條在撤掉後仍然綠，那條在驗別的東西 |
| 6 | determinism 三跑一致（`fp` 會變）＋ 憲法閘 ＋ 全部 merge-gate（★含 `headless` 的**清單**比對） |

## §6 不在範圍（★具名）
```
★①「發不出薪水」的緩衝/寬限機制 —— ★★不做(那是把補丁換位置貼回去);先量代價,再由 blueprint 裁要不要有戲
★★②settle 追加 TAG_PRODUCE 這件事本身(interaction_system.gd:1509)—— ★本票不動它:
   ⑥之後 tag 不再決定發不發薪,而【tag 語意該不該是「追加」】是另一個 WHAT
★★★③居民隊的收入來源(賣產出換不到錢)—— 那是 B 議程,不是本票
★④意圖帳補行:blueprint 的欄位,我不代填
★★⑤`SALARY_INTERVAL` 沒有 stagger 這件事本身 —— ★具名:本專案已有 `CadenceStagger`
   (零 RNG、由 (tick, team_id, cadence) 純函數決定),而薪資【沒有用它】
   ⇒ ★★全世界同一天發薪＝同一天集體不滿;★★★而這是【下一張票】,不在本票
   (先量到尖峰的形狀,再決定要不要把它打散)
```
