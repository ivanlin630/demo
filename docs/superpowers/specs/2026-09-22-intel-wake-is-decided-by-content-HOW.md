---
slice: 情報瞬醒改由**內容**決定（威脅才瞬醒，其餘折進下一次排定思考）
owner: systems
status: ★★★**WHAT 裁【值得】、實作已開工**（2026-09-22，世代 7）——★理由是**設計正確性**不是效能副作用；★★B3 玩家尺＝**待量、不作推銷**；f＝0.31（兩顆全距 0.08，不宣稱精度）記觀察；R² 全通過；★★★驗收輪次照機器記憶體排隊、**用戶玩遊戲時讓路**
基於: DIAG 票 `2026-09-22-the-hourly-whole-world-pass-DIAG.md` §11；WHAT 裁定 2026-09-22（喚醒語意）
---

# §1 ★前提（file:line 坐實，systems 開檔核過）

```gdscript
# scripts/simulation/world_events.gd:64
static func emit(state: WorldState, kind: String, subjects: Array) -> void:
    for tid in subjects:
        state.pending_rethink[id] = true      # ★★★只存「有事」，**不存 kind、不存內容**
# scripts/simulation/world_events.gd:100
static func pending_source(...) -> String:
    if state.pending_rethink.has(team_id): return "cur"    # ★恆回 "cur" ⇒ 消費端問不出來源
# scripts/simulation/belief_system.gd:293
WorldEvents.emit(state, "intel_arrived", [obs_id])          # ★情報的唯一 emit 點
```
★量到的（DIAG §11）：`woke_only` **441 次全部在 pass tick**、非 pass **0**；
emit 端 12 種事件 **100% 落在 pass tick**，`intel_arrived` **佔 94%**。

# §2 ★★★這條裁定**不能在消費端實作** —— 而那指出一個更好的形狀

```
WHAT 裁：「**緊迫由內容決定，不由管道決定**」
★但消費端（`faction_ai_system.gd:8081` 的 `_woke`）**手上只有一個布林** ——
  `kind` 沒存、`pending_source` 恆回 `"cur"` ⇒ **它不可能依內容分流**
⇒ 兩條路：
  (a) 讓 `pending_rethink` 帶內容 ⇒ **新增狀態 ＋ 每次喚醒多一次查詢**
  (b) ★★★**把判斷搬到 emit 端**：**非威脅的情報【根本不 emit 瞬醒】**
⇒ **選 (b)**。理由三條：
   ①**資訊在那裡**：`belief_system.gd:293` 當下手上就有 `tgt_id`／`fields`／觀察者的 `team_intel`
   ②★★**零新狀態、零新查詢** ⇒ 不會把省下來的時間又花掉
   ③★★★**它讓 B1 的「非威脅 intel 引發的瞬醒 ＝ 0」變成【構造保證】**，
     而不是一張要有人維護的清單 —— **清單保證會因為漏列一個點而變綠。**
```

# §3 ★★★感知鐵律（**我 owner 的憲法，這裡先套回自己**）

```
威脅判定**只能讀【觀察者自己的 belief】**，★**不得讀世界真值**。
⇒ 可用：`state.team_intel[obs_id]` 底下那一筆 claim 的內容（**這隊知道的東西**）
⇒ ★**不得**：直接去 `state.teams[tgt_id]` 讀對方此刻的兵力／位置／意圖
⇒ ★★**判準句**：**「如果這隊其實被騙了，這個判斷會不會跟著錯？」** ——
   **答案必須是「會」**。若答案是「不會」，代表我讀了它不該知道的東西。
★★★而這一條**不是提醒，是驗收格**：spec 送審時 R² 要能在 diff 裡指出讀的是哪個容器。
```

# §4 ★第 0 步（**在實作之前，而且它可能直接殺掉這張票**）

```
★**先量：那 5386 次 `intel_arrived` 裡，有多少會被判為【威脅相關】？**
   ・純 tap，不改行為：在 `belief_system.gd:293` 前計數兩類（威脅／非威脅）
   ・兩顆種子、與 DIAG 同窗（★窗長要與 B1 的母體一致）
⇒ ★★**它把 B1 從【方向性】變成【一個推導出來的數字】**：
   威脅佔比 ＝ f ⇒ **預期 `woke_only` ≈ 441 × f ＋ 非情報喚醒（~6%）**
⇒ ★★★**若 f 很高（例如 > 0.6）⇒ 這張票買不到 35%** ⇒ **回報，不要硬做**
   （★而那不是失敗：它會把答案推向 (乙) 幀分片，而那正是 WHAT 留的 defer 門票。）
★**「假設為假時這格長什麼樣」**：若情報其實不是喚醒波主因 ⇒ f 無論多少，`woke_only` 都不會降。
```

# §5 驗收（★WHAT 預註冊版原文照收 ＋ §4 推導出的門檻）

| 格 | 判準 |
|---|---|
| **B1** 機制 | pass tick 上 `woke_only` 次數相對修前**下降**；★**門檻由 §4 的 f 推導**（≈ 441×f＋非情報）；**非威脅 intel 引發的 T0 瞬醒 ＝ 0（構造保證）**；威脅 intel 引發的瞬醒次數與修前**同量級（±20%）** |
| **B2** 行為 | 威脅反應延遲（威脅情報 → 第一個反應動作的 tick 數）median 修前後**同（±10%）**；非威脅情報 → 動作延遲 median **≤ 60 tick** |
| **B3** 玩家尺 | `>2s` 幀數／p99／median（同尺）；可交玩門檻不變（**≤1/日 且 p99<1s**） |
| **B4** 副作用 | 每隊每日決策次數 median **±5%**；**A6 守 `extinct`／`starve`／`combat`**（★帶主詞，不用 `teams.size()`） |
| **B5** fp | 指紋床改變 ＝ **預期紅** ⇒ 世代 7；★**樁開／關 fp 相同**（不變量 #7） |
| **B6** 陽性對照 | 把「**威脅也折進排定**」跑一次 ⇒ **B2 的威脅延遲必須惡化** ⇒ 證明 B2 會紅 |

★**WHAT 的「假設為假」句原文保留**：若情報不是喚醒波主因 ⇒ B1 的 `woke_only` 不會降、B3 不動
⇒ ★★**兩格一起綠才算**。

# §6 閘（★形狀窄 ＋ 陽性對照）

```
禁：在 `belief_system.gd` 的威脅判定路徑上出現 `state.teams[` 直讀對方欄位
  ⇒ ★**陽性對照**：把判定改成讀世界真值 ⇒ 該閘**必須紅**（否則它沒有鑑別力）
★★而閘只掃**威脅判定那一段的函式範圍**，不掃整個檔（★錨在函式不在行號 —— 行號會被推走）
```

# §7 ★不做的事

```
✘ 不讓 `pending_rethink` 帶 payload（那是 (a)，會把省下的時間花回去）
✘ 不列「哪些事件型別可以等」的白名單 —— ★**WHAT 明令用內容切，不用管道切**
✘ 不碰十處純加法那條分支（HELD，不搭這班）
✘ ★**在 §4 的 f 回來之前不寫實作** —— f 高就不做，那不是失敗，是省下一次世代邊界
```

---

# §8 ★★★R² ①：**`pending_rethink` 是共用通道，還有三個無關的消費者**

```
R² 窮盡了全部消費點（`scripts/simulation/`，排除 debug）——★**這一格我自己說過我沒窮盡**：
  `faction_ai_system.gd:1352`  INDEP_INFRA（獨立隊基礎建設排程）
  `faction_ai_system.gd:1436`  LADDER（野心階梯評估）
  `reaction_system.gd:62`      GOAL（人物個人目標重評）
  （★第四個才是本票要改的那個：主決策 T0）
⇒ ★★**這三個跟「威脅判定」完全無關** ——建設排程／野心階梯／個人目標重評，
   沒有一個在問「這個情報是不是威脅」
⇒ ★★★**它們會被連帶延遲，不是因為 WHAT 裁定它們該被威脅內容管，
   是因為它們剛好共用同一根水管。**
```

## §8.1 ★用 R² 給的邏輯句型問，**而我的答案是【不確定】**

```
問：「如果 INDEP_INFRA／LADDER／GOAL 其實也需要立刻反應，這個延遲會不會讓它們變糟？」
★**已知**：三者**都有自己的 cadence 保底**（瞬醒是【加速路徑】，不是唯一路徑）
   ⇒ 延遲**有上界** ＝ 各自的 cadence
★**不知道**：非威脅情報（價格／市集／遠方八卦）**對它們**是不是有意義的加速訊號
   —— ★★市價變動對「要不要蓋東西」**可能**是有意義的，而**我沒有量過**
⇒ ★★★**所以我不寫「不會變糟」**。★**寫下「不確定」，然後照 R² 給的出路做**：
   **B2／B4 明確擴大涵蓋這三格，不靠 B4 的聚合次數順便接住。**
```

## §8.2 ★驗收擴增（★★三格各自具名，不併進聚合）

| 新格 | 判準 |
|---|---|
| **B4a** INDEP_INFRA | `faction_ai_system.gd:1352` 的**評估次數／日** median **±10%**；★**並印它的【間隔 p99】**（聚合次數會把「變慢」藏起來） |
| **B4b** LADDER | `faction_ai_system.gd:1436` 同上 |
| **B4c** GOAL | `reaction_system.gd:62` 同上 |
★**三格任一惡化 ⇒ 回報，不要用「反正它們也在製造尖峰、一起降是好事」帶過** ——
★★★**那句話可能是對的，但它是 WHAT 的裁量，不是我的。**（已呈報，見 §8.3）

## §8.3 ★★已呈 WHAT 的那一句（**在回覆之前不實作**）

```
**「非威脅情報不再瞬醒」這件事，要不要連帶套用在 INDEP_INFRA／LADDER／GOAL 上？」**
  (甲) **要** ⇒ 本票照現況做，B4a~c 只是觀察欄
  (乙) **不要** ⇒ 抑制只能**限定在思考路徑**上 ⇒ 需要一個**範圍化的通道**
       ★代價：多一個小容器 ＋ 三處各多一次查詢；★★好處：**本票的行為改變範圍 ＝ WHAT 裁定的範圍**
★**我的傾向 (乙)**：★★★**「剛好共用同一根水管」不是改變它們行為的理由。**
```

# §9 ★★R² ②：**陷阱的 file:line 點名**（不留抽象提醒）

```
`belief_system.gd:264-271`（`record_claim` 內，★**就在插入點 :293 的正上方、同一個函式裡**）
  已存在一段 **gate-ok 標記過的 god-view 讀**（`_o` / `_t`，純統計、不進 claim 內容、不進決策）
⇒ ★★★**硬規（寫進票，不是提醒）：新的威脅判定邏輯【不得】使用或延伸 `_o` / `_t`。**
   那段的作用域**僅限統計**；★**威脅判定只能讀 `state.team_intel[obs_id]`（觀察者自己知道的東西）**
⇒ ★**這個「後門」連 file:line 都已經現成擺在那裡** —— 而我今天犯過一次同形的錯
   （拿鐵律擋前門、後門讀 god-view）⇒ **所以它以禁令寫死，不以判準句提醒。**
```

## §9.1 ★閘的共存情境要單獨驗

```
`record_claim` **本身就含有一段已核准的 gate-ok god-view 讀** ⇒ 閘掃這個函式時必須：
  ・**放過**既有 gate-ok 標記那幾行
  ・**抓到**新增在旁邊、沒有標記的違規
⇒ ★★★**實作完必須單獨跑這一格的陽性對照**：
   **把新邏輯故意寫成讀 `_t` 且不標 gate-ok ⇒ 閘必須紅**
   ⇒ ★若它不紅，代表 fingerprint 跟舊標記**混雜命中或漏抓**（★該機制有 collision 前科）。
```

---

# §10 ★★★WHAT 裁 (乙)：**抑制限定在主決策 T0 思考路徑** —— 形狀改變 ⇒ **本票回頭再過一次 R²**

```
裁定：INDEP_INFRA／LADDER／GOAL **行為不動**；
     「共用同一根水管」不是改它們的理由；它們該不該對非威脅情報變遲鈍 ＝ **另一張票**
⇒ ★★這改變了修法形狀（從「不 emit」變成「範圍化」）
⇒ ★★★照本票 §7 與我自己對 R² 的承諾：**形狀改變 ⇒ 回頭再過一次 R²**。
```

## §10.1 ★設計（HOW 自治）：**emit 端一個旗標，預設維持現狀**

```gdscript
# world_events.gd:64
static func emit(state, kind, subjects, wake_thinking: bool = true) -> void:
    for tid in subjects:
        state.pending_rethink[id] = true                  # ★三個既有消費者：一行都不動
        if wake_thinking: state.pending_think[id] = true  # ★新：只有思考路徑讀它
# belief_system.gd:293 —— ★唯一傳 false 的呼叫點
```
★★★**為什麼預設 `true`**：**任何未來新增的 emit 都會【自動】維持瞬醒**
   ⇒ **構造保證** —— 不會因為有人漏改一處而靜默失去瞬醒（★清單保證會）。
★**思考路徑改讀 `pending_think`**：`faction_ai_system.gd:8081` 的 `_woke`
   ⇒ ★★**它仍然吃得到其餘 11 種事件**（被襲等）—— **護欄①不受影響**

## §10.2 ★★★兩條硬約束（★讀 code 才看得到，不讀會踩）

```
①**新集合必須在同一點清空**：`world_events.gd:171 state.pending_rethink.clear()`
   ⇒ ★`world_events.gd:6` 明寫：**pending_rethink 不入 `state_fingerprint` 的正當性基礎
     ＝【單 tick 內清空】** ⇒ ★★**新集合不同步清 ⇒ 它就得入 fp** ⇒ **fp 語意當場破掉**
②**`world_state.gd:156` 旁邊加欄位**，且**同樣不入 `state_fingerprint`**（理由同①）
```

## §10.3 ★我發現 R² 的四個消費者**漏了第五條路徑**

```
`world_events.gd:106-128`：**faction 層的查詢**（★直接讀 `pending_rethink`，換一個索引）
  註解自己寫著：「**這不是第二套機制 —— 它讀的是同一份 `pending_rethink`**」，服務**五支 T3 節點**
⇒ ★**它算不算「主決策 T0 思考路徑」？** —— **裁定的字面沒有涵蓋它**
⇒ ★★★**我用 WHAT 自己的原則預設處置：不動它**（保守側 ＝ 行為不變）
⇒ **它繼續讀 `pending_rethink`** ⇒ 行為零改變；★**並列進同一列 defer**。
```

## §10.4 defer（★**觸發條件寫成機械的**）

```
本票量 f 那一步**順手印每個消費者的 woke 份額**（含 §10.3 的 faction 路徑）
⇒ ★**若這四條非思考路徑合計 ≥ 20% 的尖峰** ⇒ **開新票各自裁**
⇒ ★★**不是「有人記得」** —— 已登進 `docs/process/defers.tsv`（`t0-wake-nonthinking-consumers`）
```

---

# §11 ★★★威脅定義：**不自己發明 —— 用已經存在的那把秤**（systems 裁 2026-09-22）

```
implementer 量出：**f 不是一個數字，是【定義的函數】**
   f(敵意) = 0.093｜f(距離≤3) = 0.753｜f(敵意且距離≤3) = 0.084
⇒ ★我的 `f > 0.6 ⇒ 不做` 門檻，**第二個定義過不了、另兩個輕鬆過**
⇒ ★★**所以「要不要做」等價於「威脅怎麼定義」** —— 而那是我的欄。
```

## §11.1 ★★★而這把秤**已經存在**：`scripts/simulation/threat_assessment.gd`

```gdscript
ThreatAssessment.score(state, self_team, other) -> float
   raw = approach*1.0 + hostility*1.0 + (power_ratio-1.0)*0.5   # ★「敵軍逼近」正是它在算的
const THREAT_BASE_THRESHOLD := 0.3 / THREAT_INFLATION_MEASURED   # = 0.3 / 4.33
```
★**四個理由，每一個都是今天已經踩過的坦**：
```
①**零新常數**：門檻是 `0.3 / 實測膨脅係數`，**血統寫在註解裡**
   ⇒ ★我若自己挺一個「距離≤3」，**那就是下一代的手抄物理**（用戶立法禁止）
②**感知鐵律是構造保證不是承諾**：該檔註解自寫「**∴ 威脅評估全 belief**」
   （Slice D 已經把 `dist_factor` 那條 god-view 折掉）⇒ ★★**它已經被審過一次了**
③**它是秤不是闘** ⇒ ★★★正是 WHAT 說的「**急症走秤**」的喚醒版
④**不造第二支** ⇒ ★**第二支從出生就開始 drift，而 drift 不會紅**
```

## §11.2 ★★介面形狀的硬規（★R² 要的那一句，用這個形狀重寫）

```
`score()` 的簽章吃 `other: TeamData` ⇒ **取 handle 需要 `state.teams.get(tgt_id)`**
⇒ ★★★**允許：取 handle 並【整個】交給 `ThreatAssessment.score`**
   **禁止：在威脅判定路徑上【讀它任何一個欄位】**（兵力／位置／意圖）
⇒ ★仍然禁止使用或延伸 `belief_system.gd:264-271` 的 `_o`／`_t`（§9 不變）
★★**判準句不變**：「如果這隊其實被騙了，這個判斷會不會跟著錯？】**答案必須是「會」**
   ⇒ ★★★`score()` 內部走 `BeliefSystem.best_estimate` ⇒ **答案是「會」** ✅
```

## §11.3 ★重量 f（★★因為定義已經換了）

```
★先前三個 f 都是**候選定義**；現在定義確定為：
   **`ThreatAssessment.score(...) >= THREAT_BASE_THRESHOLD`**
⇒ ★★**請用這個謂詞重算 f**（★舊卷面能補算就不要重跑）
⇒ ★**預期（寫在看數字之前）**：`score` 合了 hostility＋approach＋power
   ⇒ 形狀最接近**合取**（f ≈ 0.084）⇒ **預期 f 落在 0.05 ~ 0.20**
   ⇒ ★★★**若實測落在 0.6 以上 ⇒ 我的形狀理解錯了** ⇒ **回報，不要改定義去遷就門檻**
```

## §11.4 ★WHAT 點的三種情況，**只有一種在這個母體裡**

```
WHAT：「敵軍逼近／盟友被襲／剐掠目標是自己」
★而 implementer 量到：那 5386 次 **全部是位置情報**（emit 條件 ＝ `fields.has("tile_pos")`）
⇒ ★★**「盟友被襲」與「剐掠目標是自己」根本不在這個母體裡** ——
   它們走其餘 11 種事件，而那些 **一律維持瞬醒**（`wake_thinking` 預設 true）
⇒ ★★★**所以本票的抑制面比裁定的字面還窄** —— 這是好事，但要說出來。
```

---

# §12 ★★★`f > 0.6 ⇒ 不做` 這條門檻**壞了** —— 改寫成【收益函數】（systems 2026-09-22）

```
2 天 smoke：**f = 0.6021**（3243／5386）⇒ 照字面該觸發「不做」，而它**只超過 0.002**
★implementer 拒絕拿它觸發規則，理由引的是我今天寫的「**勉強點火 ＝ 沒有鑑別力**」
⇒ ★★**他是對的，而錯的是我的門檻形狀** ——
   **我把一個【連續的收益問題】壓成一條線，然後讓線去決定燒不燒一個世代邊界。**
```

## §12.1 ★改寫：**收益是 f 的函數，把對應表寫在數字之前**

```
喚醒波被削掉的比例 ＝ **(1 − f)**（非威脅那一半不再瞬醒）
而 `solo_think` 占 pass 時間 **34.7%** 且它在 pass tick 上幾乎全由喚醒驅動
⇒ **預期削掉的 pass 時間 ≈ (1 − f) × 34.7%**

   f = 0.10 ⇒ ≈ 31%｜f = 0.30 ⇒ ≈ 24%｜f = 0.50 ⇒ ≈ 17%
   **f = 0.60 ⇒ ≈ 14%**｜f = 0.75 ⇒ ≈ 9%｜f = 0.90 ⇒ ≈ 3.5%
```
★★**而真正要問的不是「f 多少」，是「這些 % 能不能跨過玩家尺」**：
```
pass dt median **1.15 秒**；削 14% ⇒ **≈ 0.99 秒**；削 31% ⇒ **≈ 0.79 秒**
★★★而 B3 的可交玩門檻是 **`>2s` ≤ 1／日 且 p99 < 1s**
   ⇒ **兩個數字都還在一秒上下** ⇒ ★**本票單獨幾乎肯定跨不過 B3**
⇒ ★★★**所以這張票的意義不是「修好凍結」，是「拿掉一個不該存在的人工同步」** ——
   而那是否值得燒一個世代邊界，**是 WHAT 的欄**。
```

## §12.2 ★★判法（**寫在 12 天數字回來之前**）

```
①**不拿 2 天 smoke 觸發任何一條規則**（implementer 已經這樣做，對）
②**12 天 × 兩顆種子** 回來之後，把 **f 與對應的預期 % 一起呈 WHAT**，
   ★★**並同句註明「這個 % 跨不過 B3」** ⇒ **讓他裁的是【值不值得】不是【數字好不好看】**
③★★★**我不再自己訂一條 f 的線** —— 因為這條線的真身是「值不值得燒世代」，
   **而那一句我没有權裁**。
★兩顆種子的 f 差距也要印 ⇒ **差距比「離 0.6 多近」更能說明這個數字穩不穩**。
```

## §12.3 ★而感知鐵律那一格：**implementer 讀內文後自己撤回了反向斷言**

```
他先前寫「`ThreatAssessment` 名叫 belief-threat，讀的是世界真值」—— **撤回**
真相（他開檔讀內文）：`:44-48` 位置只在**此刻真的看得見**才用 `other.tile_pos`，
  否則 `belief_pos`；`:74-75` `_approach_score` 先過 `observe_velocity` 可見性閘；
  `:88` `_power_ratio` 走 `best_estimate`（註解寫著「禁讀 `other.population`」）
⇒ ★★**感知鐵律是構造保證** ⇒ §11 的前提**成立**（★仍待 R² 獨立核）
⇒ ★★★而他自己指出錯的形狀：**他看的是【簽章】與【呼叫端】，沒讀函式內文** ——
   而「簽章不是證據」正是我 memory 裡的原句。
```

---

# §13 ★★★R² §11 兩句精確化（全收）

## §13.1 ★我把【慣例】寫成了【構造保證】

```
我寫：「`score()` 內部走 belief ⇒ **感知鐵律是構造保證**」
★R² 精確化：**不是「從不碰 `other.tile_pos`」**，是
   **「只在 visible-this-tick 時碰」**（`belief.last_tick == current_tick`）——
   而那是 **belief-等價**，與 `estimate_catch_up` **同一慣例**
⇒ ★★**結論不變**（不違反感知鐵律），★★★**而措辭必須改** ——
   因為「構造保證」會讓下一個人以為它**永遠不碰真值**，
   而實情是它**碰，只是只在【碰了也等於沒碰】的時候碰**。
★**而這個差別有實際後果**：若以後有人改了「visible-this-tick」的定義，
   **這條保證會跟著壞，而「構造保證」這四個字不會提醒任何人**。
```

## §13.2 ★★介面硬規補一句：**不只禁讀欄位，也禁呼叫非 belief-gated 方法**

```
舊：允許取 handle 並【整個】交給 `score()`；**禁止讀它任何欄位**
★新增：**也禁止在威脅判定路徑上呼叫 `other` 的【非 belief-gated 方法】**
⇒ ★★理由：**「禁讀欄位」有一個現成的繞道 —— `other.某個方法()`**
   而它在 diff 裡**長得不像讀欄位**
⇒ ★★★**閘要掃兩種形狀**；陽性對照也要各跑一次（讀欄位／呼叫方法）。
```

---

# §14 ★★★省下來的是【同時性】不是【工作】 —— 而這送了一個**守恆格**

```
`SimRunner.NEAR_CADENCE = 60` ★**剛好就是 solo 自己的 cadence**
⇒ 被抑制的那一批**不會少想**，只會**改在自己的相位想**
⇒ ★★**pass 時間降 ≈ (1−f)×34.7%，而非 pass 時間【等量上升】，總量守恆**
⇒ ★★★**所以 B3 幾乎一定過不了 —— 不是因為削得不夠多，是因為總工作量沒有變少。**
```

## §14.1 ★B4 從【希望】升成【預測】

```
舊：B4「每隊每日決策次數 median ±5%」—— ★它本來只是一個**副作用防線**
新：★★**它現在是一個【從機制推導出來的預測】** —— 次數**應該≈不變**
⇒ ★★★**而預測與防線的差別在於：它紅的時候意思不同** ——
   防線紅 ＝「不小心改到了別的東西」；
   **預測紅 ＝「我對這個修法的理解是錯的」** ⇒ **停下來，不是調一調。**
```

## §14.2 ★★★新增 **B7（守恆格，免費）**：兩邊的時間要加得起來

```
**Δ（pass 總時間） ＋ Δ（非 pass 總時間） ≈ 0**
★**若 pass 降了而非 pass 沒有等量上升** ⇒ ★★**工作消失了**
   ⇒ ★★★**那不是「省」，是「漏」** —— 有一批隊**真的少想了** ⇒ **回報，不要當成好消息**
★而這一格**不用多跑一次**：兩個數字同一輪都已經在卷面上。
```

## §14.3 ★預註冊：非 pass tick 會升到哪裡（**寫在看數字之前**）

```
重分配的量 ≈ (1−f) × 448 ms ⇒ f=0.60 時 ≈ **179 ms**
攤到每個週期的 **59 個非 pass tick** ⇒ **≈ 3.0 ms／tick**
⇒ 非 pass tick 從 **≈311 µs** 升到 **≈ 3.3 ms**（★約 10 倍，但仍遠低於一幀預算）
⇒ ★★**若實測遠高於此** ⇒ 重分配**不均勻** ⇒ ★★★**回報** ——
   那代表被抑制的那批**並沒有回到自己的相位**，而是堆到了別的地方。
```

## §14.4 ★★而這把本票要呈 WHAT 的那句變得**更精確**

```
舊：「削 14%，跨不過 B3，值不值得？」
新：★★★**「它一點工作也沒省 —— 它只是把工作從【全世界同一 tick】攤回【各自的相位】。
     而那正是一開始就該有的樣子。值不值得為這個燒一個世代邊界？」**
⇒ ★**這個問法比舊的誠實** —— 舊的還帶著「削了多少」的暗示，像是效能票；
   **它不是效能票，它是【拿掉人工同步】的票。**
```

---

# §15 ★★★訂正我自己：**不是「第五條」，是 11 條**（systems 2026-09-22）

```
我寫（§10.3）：「R² 的四個消費者**漏了第五條**」
★implementer 裸掃的結果：**生產端共 11 個消費點**
   `pending_source_faction` ×6（faction_ai 1247／1271／1293／1841、strategic_ai 37／65）
   `pending_source` ×3（faction_ai 1352／1436、reaction_system 62）
   `is_pending` ×2（faction_ai 4351 `_should_reeval`、8081 solo）
⇒ ★★**我的「第五條」是【我只找到一條】，不是【它只漏了一條】**
⇒ ★★★而我當時還對 R² 說「這大概就是它從你的窮盡裡消失的原因」
   —— **那句話把一個【我沒窮盡】講成了【我補完了】。**
```

## §15.1 ★而漏掉的**機制**找到了（★★這才是要留下來的）

```
`scripts/debug/s4b_wake_coverage.gd` 的 `SUPPORTS` 是**手抄的 9 條**，
**剛好等於有 `tap_wake` 的那 9 個**
⇒ ★**那份清單看起來權威，而它是【照名單數】** ⇒ 沒 tap 的就從清單消失
⇒ ★★**而消失的兩個正好是最大的兩個**：
   `SOLO`（占 pass tick 時間 **34.7%**）**完全沒有 tap_wake**；
   `_should_reeval` 只有【無後綴】的 `reeval.event`
⇒ ★★★**這就是「第五條被漏掉」的那個機制本身** ——
   **清單保證會正好漏掉【還沒裝儀器】的那一個，而那通常是最大的。**
★implementer 已補 `SOLO`／`REEVAL` 兩支（舊鍵不動，`s4b_wake_coverage.gd:119` 在讀）。
```

## §15.2 ★defer 跟著改（原本寫「四條非思考路徑」）

```
`t0-wake-nonthinking-consumers` 的母體從 **4** 改成 **10**（11 減去 solo 那一條）
⇒ ★**觸發條件不變**（合計 ≥ 20% 尖峰 ⇒ 開新票），★★**但母體寫錯會讓分母小一半。**
```

---

# §16 ★★★B7 要重寫 —— **它與我自己的 B5 打架**（systems 自糾 2026-09-22）

```
B5 寫：**fp 改變 ＝ 預期紅 ⇒ 世代 7**（★兩臂是**不同的世界**）
B7 我寫：**Δ(pass) ＋ Δ(非 pass) ≈ 0**（★★而那需要**同一個世界**才成立）
⇒ ★★★**兩條在同一份 spec 裡互相矛盾，而是我寫的** ——
   世界都不同了，**總工作量本來就會不一樣**，沒有什麼可以「守恆」。
★而我今天已經犯過同型：**引用一條規則的那一刻，注意力在「它擋住了誰」不在「它也管我」**。
```

## §16.1 ★重寫後的 B7：**不要求守恆，要求【不要消失】**

| 格 | 判準（重寫） |
|---|---|
| **B7** | ★**絕對量**：`pass 總 dt` ＋ `非 pass 合計`（★**兩個數字床已經同時在印**，`pass_tick_phase_breakdown_bed`）⇒ **修法後的總和，不得低於修法前總和減去【跨種子變異】** ⇒ ★★**低於 ⇒ 工作真的消失了 ⇒ 回報，不要當好消息** |

```
★★**門檻的【單位】是跨種子變異**，不是我挺一個 % ——
   **修法前兩顆種子的總和差距就是那把尺**（★兩臂四個數字同一輪都在卷面上）
★★★而它**不再叫「守恆格」** —— 叫守恆會讓下一個人拿它當等式用，
   而它實際上只是一個**單向的【沒有東西不見】檢查**。
```

## §16.2 ★★implementer 點的量級陷阱（**寫死，否則正常會被讀成警報**）

```
`非 pass 合計` 現在只有 **5.3M us**，而 `pass 總 dt` 是 **372.6M us** ⇒ ★**差 70 倍**
⇒ ★★**「非 pass 等量上升」在【百分比】上會是一個巨大的數字**（+300% 之類）
   ⇒ ★★★**而那是正常的，不是異常**
⇒ ★**所以 B7 只能用絕對量，不得寫成「兩邊各自變動 ±x%」**
★★通則：**兩個量級差很大的數字，用百分比表達會把正常變成警報** ——
   而反方向也成立：**用絕對量表達小的那邊，會把警報變成正常**。
```

## §16.3 ★而這一格不需要新儀器

```
`scripts/debug/pass_tick_phase_breakdown_bed.gd` **已經同時印兩個數**
⇒ **B7 ＝ 兩臂相減，零新 tap、零新床**（implementer 核的）
⇒ ★★而那正是今天反覆的那一條：**造東西之前先 grep【誰已經在做這件事】**。
```

---

# §17 ★★★B7 的尺：**n＝2 給的是【全距】不是【變異】**（implementer 點出，systems 收）

```
實測（乾淨 12 天、兩顆種子、零重跑）：
   `pass 總 dt` 差 **7.14%**｜`非 pass 合計` 差 **3.37%**｜**總和 差 6.98%**
★★★而他不建議把 6.98% 直接寫進 B7 —— 理由是：
   **兩個點給的是全距，不是變異** ⇒ **那條噂音底線本身沒有母體**
⇒ ★**收** —— 這與本場反覆的「先問母體再問比率」是同一條，
   只是這次**母體少的是【我用來定門檻的那把尺】本身**。
```

## §17.1 ★做法（便宜，排在 f 那一輪之後）

```
**多跑兩顆種子**（≈ 7 分鐘／顆）⇒ n＝4
★★**而 n＝4 仍然不是「變異」** ⇒ 所以門檻用法要跟著謝虛：
   **它是一條【底線】，不是一個【精確門檻】**
⇒ **B7 帶寬用 max(觀測全距) × 2**（★寬侧錯 ⇒ 宁可漏報不要誤報，
   因為這一格的工作是「抓【整批隊不想了】」，那種事的幅度遠大於噪音）
```

## §17.2 ★★★而我要誠實說出 B7 的鑑別力限

```
真的「工作消失」會有多大？若一批隊**完全不想了**，那是 solo（34.7%）的一大塊
而跨種子噪音 ≈ 7% ⇒ ★★**餘裕不厚**
⇒ ★**若實測的下降落在【噪音與漏掉】之間** ⇒ **判【不可判】，加種子，不二選一**
⇒ ★★★**而這句話要寫在卷面上，不是等到數字回來才說** ——
   否則它會變成一個【看完數字才發明的藉口】。
```

---

# §18 ★★★B1 回來了，而它比我的模型小一半 —— **因為我的模型漏了【重疊】**

```
實測：`woke_only` **−29.5%（s1337）／−35.3%（s77）** ⇒ ★**方向相同 ＝ 訊號**
而我的 §12 模型預期：情報佔喚醒 **94%** × 非威脅 **(1−f)=0.686** ⇒ **≈ 64%**
⇒ ★★**實測只有模型的一半。**
★★★**而差別有名字：重疊（union）** ——
   `woke_only` 數的是【被喚醒的隊】，不是【喚醒事件】；
   一支隊若**同時**被非威脅情報與別的事件點到，**抑制情報並不會讓它不醒**
⇒ ★**所以 §12 那個 (1−f)×34.7% 是【上界】不是【預期值】** —— 而我當時沒說。
```

## §18.1 ★而我不再重算一個新的估值

```
用實測反推：喚醒波削 **≈ 30%** ⇒ pass 時間削 ≈ **0.30 × 34.7% ≈ 10%**
★★**但我不把這個 10% 拿去到處講** —— 因為：
   **B3 本來就會直接量到 pass 時間與幀時**
   ⇒ ★★★**再推一個估值，只是在量到之前多造一個可以被引用的數字。**
⇒ **停估，等量。**
```

## §18.2 ★★對外要撤的那一句

```
我給 WHAT：「削 **22.4% ~ 25.2%**」 ⇒ ★**那是上界**，而我寫成了預期值
⇒ **撤回那個區間**；換成：**喚醒波實測削 30%，而 pass 時間的真數由 B3 量**
★★**而這不改變裁定**：WHAT 裁的理由是**設計正確性**，不是那個百分比
   ⇒ ★★★**而我仍然要撤** —— **裁定不靠它，不代表它可以錯著放。**
```

## §18.3 ★對照欄都指同一個方向（★★這才是讓人安心的那一半）

```
`due_only` 的非 pass 端 **−2.3%／−2.2%** ⇒ ★**相位那條路沒被碰到**（預期如此）
`skip` **+20.3%／+20.0%** ⇒ ★喚醒少了 ⇒ 檢查後略過的變多（機制上必然）
⇒ ★★**三欄方向一致** ⇒ **不是「數字有動」，是【機制在動】**。
★**而 B1 的後半（威脅 intel 引發的瞬醒 ±20%）這一輪切不出來** ⇒ **待下一輪，不假裝它過了**。
```

---

# §19 ★★★B4a 不是紅、**紅的是我的 B4a** —— 而我已經修過同型的 B7

```
實測：B4a（INDEP_INFRA）**−19.7%／−23.2%**，兩顆同向且超標（門檻 ±10%）
★而我開檔核過（`git show origin/feat/intel-wake-by-content:world_events.gd`）：
   `state.pending_rethink[id] = true`  ← **仍然無條件**
   `if wake_thinking: state.pending_think[id] = true` ← 被闘住的只有新集合
⇒ ★★**那三個消費者拿到的訊號【逐位元不變】** ⇒ **機制不可能碰到它們**
⇒ ★★★**那 −20% 是【世界分歧】，不是我們改壞了什麼** ——
   **B5 明寫 fp 必須改變（世代 7）⇒ 兩臂本來就是兩個世界。**
```

## §19.1 ★★而這是**同一個錯誤的第二次**

```
我在 §16 已經抳過一次：**B7 要求跨臂守恆，而 B5 明寫兩臂是不同的世界**
★★**而我修了 B7，沒把同一個修法套回 B4a／B4b／B4c** ——
   **它們一樣是【跨世界比絕對次數】。**
⇒ ★★★**「修一處沒修同型另一處」** —— 我 memory 裡原文就是這句。
```

## §19.2 ★B4a／b／c 重寫：**構造保證取代數量門檻**

| 格 | 重寫後 |
|---|---|
| **B4a/b/c** | ★**硬閘改成【構造】**：`emit()` 裡 `state.pending_rethink[id] = true` **必須無條件**（★不得被 `wake_thinking` 或任何條件包住）⇒ **三個消費者的輸入不變 ＝ 可被 diff 證明**；★★**次數與間隔 p99 降為【觀察欄】**——★★★**跨世界比絕對次數，在 B5 之下沒有意義** |

```
★**而這不是把門檻放寬** —— **它更強**：
   數量門檻只能在兩個世界之間猜；**構造保證是一行 code 的事實**。
★★**met_check（機械）**：`emit()` 內 `pending_rethink[id] = true` 不得出現在任何 `if` 之內
   ⇒ ★★★**陽性對照：把它包進 `if wake_thinking:` ⇒ 這個檢查必須紅**。
```

## §19.3 ★B1 後半與 B4b／B4c 的【不可判】：**理由比 implementer 說的更深**

```
他說：方向相反（B4b −13.6%／+1.7%；B1 後半 +7.8%／−11.4%）→ 不可判 —— ★**對**
★★**而更深的理由是：它們本來就不可判** —— 跨世界比絕對次數
   ⇒ ★**方向一致也不會讓它變得可判**（B4a 就是方向一致而我差點把它判成真紅）
⇒ ★★★**兩顆同向只能排除【雜訊】，不能排除【共同的混淆】** ——
   而世界分歧對兩顆種子是**共同的**。
```

---

# §20 ★★★票對註冊表對帳：**四格沒儀器 —— 而大半的正確處置是【降成構造保證】，不是【去建儀器】**

```
implementer 對帳（★grep 查的，不是回憶）：
   **四格完全沒儀器**：B1 後半／B2／B4／B7
   **兩格半套**：B4a/b/c 缺【間隔 p99】｜B3 有 `frames_over_budget` 但**沒量過**
   **真的可用**：B1 前半｜§6 闘（已登註冊表）｜B5 床存在但**未登註冊表**
★★★**而我不去補四支儀器** —— 因為其中三格**本來就不該是數量門檻**：
   **它們與 B4a 同病（跨世界比絕對數），而我上一次又只修了 B4a。**
```

## §20.1 ★逐格處置（★★四格降級、兩格建、一格登記）

| 格 | 新處置 | 由什麼判 |
|---|---|---|
| **B1 後半**（威脅瞬醒 ±20%） | ★**降成構造保證**：威脅分支仍呼叫 `emit(..., true)` ⇒ **它的喚醒路徑與修法前逐位元相同** | **diff／met_check**（不需跑） |
| **B2**（威脅反應延遲） | ★★**同上降級** —— ★★★**既然威脅那條路的 emit 參數是 `true`，威脅的反應路徑就【沒有被修法碰過】** ⇒ **延遲不可能因機制而變** | **diff／met_check** |
| **B4**（每隊每日決策次數） | ★**降成觀察欄**（跨世界比絕對數） | 卷面印，不判 |
| **B4a/b/c** | ★已是構造保證（§19.2）⇒ ★★**【間隔 p99】那個要求【刪掉】** —— 它是量化版的配套，而量化版已撤 | **met_check**（`pending_rethink` 無條件） |
| **B3**（玩家尺） | ★**保留為硬閘，且它是唯一真正會說話的那一格** | `frames_over_budget`（**★已存在、production 常開**）⇒ **要量** |
| **B7**（守恆） | ★**保留**，★★**比較器要建**（兩個數字已在卷面上）⇒ **70 倍那件事寫進判準，不是註解** | **要建的比較器** |
| **B5**（fp） | ★**床存在 ⇒ 登註冊表** | 註冊表 id（待登） |
| **§6 閘** | 已登 ✔ | 註冊表 |

## §20.2 ★★★對帳的真正價值

```
★**不是「發現四支儀器要建」** —— 是**發現三格不該存在**。
★★而它們不該存在的理由，**我上一次已經寫過了（§19）** ——
   **跨世界比絕對數在 B5 之下沒有意義** ⇒ ★**而我只修了 B4a**。
⇒ ★★★**同一個修法、第三次沒套完** （B7 → B4a → 這一批）
   —— **而這一次是對帳把它逗出來的，不是我想起來的。**
```

---

# §21 ★★★B3【不可判】、B7【沒有鑑別力】—— **兩個都是我的驗收設計問題**

```
實測（兩臂 × 兩顆種子）：**表上每一個量方向都不一致**
   `pass 總 dt` **+0.1%／−12.8%**｜`總和` **+0.2%／−12.5%**
   **B3 的 `>2s` 幀數 94→104／60→20**
⇒ ★依不變量 #8（方向相反 ＝ 洗牌）⇒ **全部【不可判】**
⇒ ★★**而 B3 正是這張票唯一指望會說話的那一格。**
```

## §21.1 ★★★B3 用錯了統計量：**罕見事件的【計數】在 n＝2 下永遠判不了**

```
`>2s` 幀數 ＝ **罕見事件的計數**（94、104、60、20）
   ⇒ ★**它的種子間變異本來就巨大** ⇒ **n＝2 永遠判不了**
★★**而同一輪裡有一個母體大得多的量**：**`pass tick` 的 dt 分佈（每臂 288 個樣本）**
⇒ ★★★**B3 的判準應該從【數幾個幀超標】改成【分佈有沒有移動】**：
   **pass tick dt 的 median／p90（288 樣本）**，而不是數那幾個尾事件。
★**而玩家尺（>2s ≤ 1／日）仍然是交玩的門檻** —— 只是**它不能用來判這一刀有沒有用**：
   ★★**門檻與【判別工具】是兩件事，而我把它們寫成了同一格。**
```

## §21.2 ★★B7 的帶寬比現象還寬 ＝ **沒有鑑別力**

```
帶寬允許 **126.2M us**，而觀測到的最大變化只有 **48.5M us**
⇒ ★**它會放行「工作真的消失了一大塊」的情況**
⇒ ★★**這是「陽性對照只贏 0.03」的同一個病** —— **過了，而過不代表什麼。**
⇒ ★★★**處置：B7 降為【觀察欄】，並在票上明記「它目前沒有鑑別力」** ——
   **不留一個沒有鑑別力的綠。**
★要讓它有力需要什麼：**更多種子**（真正的變異，而不是 n＝2 的全距）
   ⇒ ★★**而機器正吃緊 ⇒ 本輪不追**；**寫進 defer，不寫進註解**。
```

## §21.3 ★所以這張票現在的真實狀態

```
**世界層的數量門檻：一個都沒有過** —— **全部不可判或無鑑別力**
**而過的那些是**：構造保證（B4a/b/c、B1 後半、B2）＋ B1 前半 ＋ §6 閘
⇒ ★★**這不推翻裁定** —— WHAT 裁的理由是**設計正確性**，不是數字
⇒ ★★★**但我要把這一句說出口**：
   **這一刀 merge 之後，我們【不會】知道它對幀時做了什麼** ——
   **而那是一個要被接受的代價，不是一個可以裝作沒看見的空格。**
```
