---
slice: failure-memory-structural-identity
tier: full
qa: required
from: systems
topic: 失敗記憶改用【結構身分 key,零人工表】—— 覆蓋從 2 個 option 變成構造性 100%
---

# 失敗記憶：**結構身分 key**（blueprint 定磚 2026-08-25）

> ★**折價是「這條路我試過、失敗了」的【真實資訊】，不是排序旋鈕。**
> （blueprint 指定寫在開頭；同「排序＝折現值比較的自然輸出、禁新排序常數」的精神。）

## §1 現況（診斷已坐實，`file:line`）
| | 現況 |
|---|---|
| 靜態 option 路 | `decision_engine.gd:76` `u *= FailureMemory.mult_for_option(...)` |
| ★goal candidate 路 | `decision_engine.gd:104` **用生 util 進池，完全不過折價** |
| ★**接線面積** | `OPTION_FAIL_KEY` **只有 2 筆**（買糧／買料）⇒ **其餘 option 恆 1.0** |
| caller | `mult_for_option` production 全樹**只有 1 個** |

⇒ ★**光把 `:104` 接上 `mult_for_option` 沒有用** —— candidate 的 label 不在那兩筆裡，一樣回 1.0。
⇒ ★**手工對照表在這裡註定漏**：candidate label 是**組合出來的**
（`goal_resolver.gd:447` `gt + ":" + frontier_kind`、`deliver_<res>`、`<label>:delegate`）⇒ **label 空間近乎開放。**

## §0 ★★前置量測（**R² 裁：從「事後死水欄」升格為 dispatch 前的前提**）

**要答的一個數字**：★**exact-pair 的命中率會不會趨近 0？**

**具體**：`build_workshop` 那 45 次（team 11），
★**是「同一個 tile 反覆蓋不成」還是「45 個不同 tile 各試一次」？** 併報一般化的 **target 重複率分佈**。

| 結果 | 意義 | 處置 |
|---|---|---|
| **target 高度重複**（同一 tile 反覆） | exact-pair **咬得到** | ✅ 照 §3 做 |
| ★**target 幾乎每次不同** | ★**exact-pair 命中率趨近 0 ⇒ 折價形同不存在** | ★**回頭找 blueprint 重定語意**（類級泛化的門檻），**不得自行擴大** |

★**reviewer 的理由我照收**：這是本 session **一路「先量後改」紀律的同型情境** ——
**我原本把它放在 §5 當事後死水欄，那等於「做完才發現做了個恆 1.0 的機制」。**

### ✅ 答案（measurer 2026-08-25，**母體 101 筆完整、非樣本**）
| | 值 |
|---|---|
| team 11 那 45 次 `build_workshop` | ★**2 個 distinct target**：`(10,8)` **33 次**、`(13,6)` **12 次** |
| 一般化（所有 location-bound 候選：`build_*` / `deliver_material` / `maintain_*`） | ★**distinct target 數 1～2、各自重度重複**；**沒有一筆是「每次都不同 target」** |

⇒ ★★**exact-pair 咬得到。磚放行。**
（**既不是「同一 tile 反覆」也不是「45 個都不同」——是「少數 target 各自重度重複」，
 那正是 exact-pair 最有效的形狀。**）

## §0b ★★真實隱藏成本（**R² 親驗，非我多慮**）

**`_mk_candidate` / `_mk_delegate_candidate` 的 candidate dict【只存融合後的 label 字串】** ——
★**`gt` 與 `frontier_kind` 沒有各自成為獨立欄位**；**只有 `target`（藏在 `to_task` 內）是乾淨的。**

⇒ ★**「結構欄位本來就在」這個前提【垮一半】。**
**要做結構 id，必須先給這兩個函式各加 2 個欄位** ——
★**這是本票的真實成本，寫在這裡，不准在交件時才冒出來。**

**性質**：這是**補齊結構資訊**（把已經隱含在組合字串裡的東西還原成欄位），
**不是新增設計** ⇒ 但**它會動到 candidate 的產出格式**，**所有讀 candidate 的地方要一起看**。

## §2 ★磚：**結構身分 key，零人工表**

**key ＝ `(option 結構 id, target id)`**，★**直接由 dispatch 自帶的結構欄位機械導出** ——
**goal type ／ frontier kind ／ target 本來就在**，**不經 label 字串對照。**

★**覆蓋 ＝ 構造性 100%**：**每個動作天生有身分，無表可漏。**
⇒ **`OPTION_FAIL_KEY` 這張人工表消失**（列管物種見 `00_roles §覆蓋欄`）。

## §3 語意（**blueprint 指定，不得自行擴大**）
- ★**先做 exact-pair**：「**蓋工坊@tileX 失敗** ⇒ 折 **蓋工坊@tileX**」
- ⛔ ★**類級泛化（折所有蓋工坊）【不預做】** ——
  **診斷證明需要才加**。**理由：過度泛化 ＝ 懲罰擴散、反傷探索。**
- **與失敗律既有四項相容**：連續折價／TTL／失效升 T0 **照舊，不動。**

## §3a ★★裁定：**橋放在【記錄側】——(B)**（systems 2026-08-25）

implementer 指出 `OPTION_FAIL_KEY` **不是懶人表，是兩種語彙之間的【橋】**：
| 側 | 語彙 | 例 |
|---|---|---|
| **記錄側**（`order_system.gd:127`） | ★**依賴 key**「我依賴的那件事失敗了」 | `("買單", "material")` |
| **決策側**（§2） | ★**動作 key**「這個動作＠這個目標」 | `("build_workshop:resource", tile(10,8))` |

**⇒ 裁 (B)：下令時把「是誰下的令」（結構 id ＋ target）夾帶進 order，失敗時用那個 key 記。**

### ★三條理由（前兩條是硬的）
1. ★**(A) 違反本 spec §2 的原話**：「**由 dispatch 自帶的結構欄位機械導出**」——
   **夾帶者是 dispatch，不是 scorer。** (A) 要在 scoring 時對所有靜態 option 取 `to_task`，
   **那是把導出責任搬到 scorer 身上**，與 §2 相反。
2. ★★**(A) 做不到本票的目的**：`(A)` 只咬得到 **`買單`**，
   ★**咬不到「那個一直重下單的 goal」** —— 而 `build_workshop` 連贏 45 次**正是本票存在的理由**。
   **一個達不成目的的做法，不是選項。**
3. **(A) 撞既有血證**：`decision_engine:210` 的 `to_task` **正是 A1 抓到的分母污染源**
   （12 vs 真實 9），且 **23 option × 每 cadence** 撞 perf pin（`faction_ai` 93.7%）。
   ★**明知是污染源還要再走一次，等於重犯。**

### ★成本照實記
**order（與其他會失敗的動作載體）要多帶一個欄位。** 這是 (B) 的真實代價，**寫在票面。**

## §3b ★★訂正：`∅` 是 **fallback**，不是靜態 option 的通則

**我原本寫**「`target` 為空 ⇒ key ＝ `(option 結構 id, ∅)`，**行為等同現行按 option 折價**」。
★**implementer 指出這句不精確**：**現行「按 option 折價」實際查的是【依賴 key】**
（`買糧` 查的是 `("買單","food")`，不是 `買糧` 本身）。

**在 (B) 之下的正確語意**：
- `買糧` 下的單**夾帶** `("買糧", food)` ⇒ ★**靜態 option 也有 target（就是那個資源）**
  ⇒ ★**`∅` 的情況其實很少** —— 只用於「**沒有載體、也沒有目標**」的動作。
- ⇒ **`(option, ∅)` 保留為 fallback**，**不是靜態 option 的預設形狀。**

### ★連帶接受一項行為變化（**明確裁定，標 intended-change**）
**(B) 之下 `買糧` 的 key 從 `("買單","food")` 變成 `("買糧", food)`**
⇒ ★**舊 key 的既有記憶對不上新 key。**
**接受。** 理由：
① **失敗記憶是 TTL 短命的**，實務上**一輪就換完**；
② ★**不接受的話就得留一張新舊 key 的轉譯表 —— 那正是這張磚要消滅的東西**（又一份人工真相）。
⇒ **交件時標 `intended-change`，並在 handback 明列「切換當下既有記憶清空」。**

### ★★過渡窗觀測 tap（reviewer 建議，我採納 —— **理由是我自己的規則**）
我接受斷代的**理由**是「**TTL 短命，一輪就換完**」。
★**那是一個假設。** 依 `01_architect §假設不靜默`：**假設要能【自己喊出來】，不是靠我相信。**

**加一顆常設 tap**：**新 key 空間的失敗記憶【條目數】**（以及**首次命中的 tick**）。
| 觀測 | 意義 |
|---|---|
| 過渡窗內條目數**回升到穩定水位** | ✅ **「一輪換完」被證實**，斷代自癒 |
| ★**長期停在 0 或極低** | ★**新 key 根本沒被寫入** ⇒ **我們做出了第三個「恆 1.0」的機制** |

★**這顆的價值**：**它把「我判斷可以接受」變成「世界會告訴你可不可以接受」。**
（reviewer 明說**這是建議不是擋**，但我認為它比擋更有用 —— **擋只會延後，tap 會回答。**）

## §3c ★靜態 option 的 key 退化語意（原 §3b 內容，語意已由 §3b 訂正）

**measurer 指出**：★**靜態 options（備戰／貿易／survival…）本來就不帶 target** ⇒ **不在 exact-pair 適用範圍。**

⇒ **寫死語意**：**`target` 為空時，key ＝ `(option 結構 id, ∅)`**
—— 行為**等同現行的「按 option 折價」**，**不是特例、不是第二套**。
★**這一條必須明寫**，否則 implementer 會在「靜態 option 沒有 target 怎麼辦」上**自行決定**，
**那就是兩套語意的起點。**

## §4 folding（★**單一失敗記憶，不得有兩套**）
- **candidate 路（`:104`）接上同一把尺**
- ★**靜態 option 路收斂到同一個 key 空間** ⇒ **一套記憶、一個查詢入口**
- ⛔ **不准出現「candidate 用新的、option 用舊的」** —— 那就是又一份 drift 的真相

## §5 驗收
1. ★**覆蓋率**：接線面積從 **2 個 option** ⇒ **報實際涵蓋的 (結構 id, target) 對數**
   （★依 `00_roles §覆蓋欄`：**記 done 必同記覆蓋率**）
2. ★**行為**：`build_workshop` 對同一目標**連續失敗後 util 要真的下降**
   —— **測規律不測結果**（同 join 法條測的形狀）：
   *同一 (動作, 目標) 連續失敗 N 次 ⇒ 第 N+1 次的 util 嚴格小於第 1 次*
3. **反面**：**不同 target 的同類動作【不受影響】**（證明沒有偷做類級泛化）
4. ⚠️~~`det fp` **預期會變 ＝ intended-change**~~ ⇒ ★**我這條預測錯了，且錯在把 fp 當生效訊號**
   **實測 `fp` 沒變**（`c1e3f7c5…` ×3，與 base 相同）。
   ★**兩種可能：床沒覆蓋 vs 沒生效** —— ★**分辨它們的是【覆蓋率】，不是 fp。**
   （覆蓋率給了答案：**19 個結構 id、760 次折價、`build_workshop:resource` 140 次** ⇒ **生效了，是 a4 床沒跑到。**）
   ⇒ ★**acceptance 一律用【覆蓋率 ＋ suppressed 分佈】，不得用 `fp`**（implementer 建議，採納）。
   `headless` 分流照實列。
5. ★**死水兩欄**：新 key 的**呼叫頻率**與**輸入變異性**（**別做出第二個恆 1.0 的機制**）
   —— ★**但主要防線已前移到 §0**（R² 裁）：**命中率要在動工前就知道，不是做完才發現。**

## §6 閘
`headless` ／ `det×3`（intended-change） ／ `constitution_gate` ／ `seam-gate`（HARD，`tier: full`）
／ tap 全 Probe-gated、**禁耗 global RNG**

---

## §7 ★★★驗收：**磚自身健康，但世界層明確 regression ⇒ 不 merge**

**measurer（2026-08-25）**：

### 磚自身：**健康**
| 項 | 結果 |
|---|---|
| 覆蓋率 | main **1 個 option** → branch ★**16 個 structural id** |
| `build_workshop:resource` | **140** —— **逐字符合 implementer 自報** |
| 規律驗證 | **算術證明 `count 1→2→3` 嚴格遞減、至 `COUNT_CAP` 後 plateau** ⇒ **符合設計非 bug** |
| 反面驗證 | code 結構 ＋ exact-pair 既有數據**雙重佐證** |

### ★★★世界層：**明確 regression**
| 指標 | main | branch |
|---|---|---|
| outpost day90 | **12**（> day0 11） | ★**8**（**< day0 11**） |
| 中途新增 | **1** | ★**0** |
| `outpost.l0_to_l1` | **1** | ★**0**（**counter 整條缺席**） |
| 棄置率 | 83.3% | ★**90.0%** |
| 零採集 `collect.no_outpost_no_camp_zero_food` | 1042 | **886（改善）** |

★**measurer 沒做因果診斷、只呈現對照** —— **正確**（那是我的活）。

## §8 ★★我的判讀（**高信度假說，標待驗，不得當結論**）

★**折價反傷探索**：
**紮根／建設是「本來就低成功率」的長期投資**（實測：`applicable 102 → 贏 1`）。
**它一失敗就被折價 ⇒ 更難贏 ⇒ 再也不會贏** ⇒ ★**文明化動作歸零。**

### ★支持這個讀法的，正好是那個「改善」
**零採集改善（1042 → 886）與據點歸零【同時發生】** ——
⇒ ★**「不再做徒勞的事」與「不再做困難但必要的事」是【同一個機制】的兩面。**
**這不是兩個獨立結果，是一個結果的兩個出口。**

### ★★★而我認為真根在 key 的粒度
**失敗記憶 key ＝ `(結構 id, target)` —— 它帶了「誰失敗」，★但【沒有帶「為什麼失敗」】。**
⇒ **「這次缺料，下次補上就能蓋」** 與 **「這地方根本蓋不成」** ★**被同等折價。**
⇒ **前者是【暫時的】，折價卻是【累積的】** ⇒ **暫時性障礙被永久化。**

★**注意這不能靠「長期投資類豁免」解決** —— 那又是一張**手工分類表**
（我剛把這個物種列管，不能自己犯第二次）。

## §9 處置
- ⛔ **不 merge。**
- **要的診斷**（**判讀規則先寫**）：
  ① **紮根／建設類被折價的次數與 util 變化** —— 證實或推翻 §8
  ② ★`outpost.l0_to_l1 = 0` 是「**沒贏**」還是「**贏了沒開工**」
     （★**counter 整條缺席**表示可能連 argmax 都沒贏過 ⇒ 與 §8 一致）
- ★**設計問題呈 blueprint**：**失敗折價要不要帶「失敗原因」**（暫時 vs 結構性）？
  ★**我不自裁** —— 這動到磚的語意，而磚是 blueprint 定的。

---

## §10 ★★★裁定：**帶原因 —— 但形式是【反饋分流】，不是 key 增維**（blueprint 2026-08-25）

★**我提的是「key 加一維失敗類別」；blueprint 裁的形式更好：`key` 不動，分流發生在【反饋寫入端】。**

### 失敗類別三分（★**機械可判**：fail **擲出點**的一次性結構標注，**不是對照表**）
| 類別 | 例 | 處置 |
|---|---|---|
| ★**前提型** | **缺料／缺人／付不起** ＝ **計畫好、世界沒備妥** | ★**不折價** ＋ **記 `blocked-by`** |
| **執行型** | **真試真敗** | **折價 exact-pair，照磚** |
| **失效型** | 目標消失／不可行 | **T0**（既有機制） |

★**`blocked-by` 不是廢資訊**：blueprint 明指它是**未來脊椎 means-end 磚的鉤子**
（**缺料 → 去弄料**）—— **資訊留存，不是丟掉。**

### ★★法理（blueprint 用我自己的話回敬我，而且對）
> **我寫在 spec 開頭的是：「折價 ＝ 這條路我試過、失敗了」的【真實資訊】。**
> ★**那麼缺料的真實教訓是「先解決料」，不是「這裡不好」。**
> ⇒ ★**把前提型折價 ＝ 把【假教訓】寫進記憶 ＝ 另一種估算器說謊。**

⇒ **這條與〈估算器禁手抄物理〉同一條法**：**記憶要追事實的真形狀。**

### ★型別判定不屬於「手工白名單」物種
**fail 點型別 ＝ 擲出點的結構標注**（**code 結構，非平行維護的表**）
⇒ ★**白名單物種的列管不適用**（我前一天才列管那個物種，這裡先確認不衝突）。

### ★原設計互證（blueprint 指出）
**失敗律 HOW 本來就有「失效升 T0／劣勢只折價」的二分** ——
★**這次只是把那個二分【下沉到折價端】，語意一致，不是新發明。**

## §11 ★★acceptance：**兩面分開驗，防修回頭**
| 面 | 判準 |
|---|---|
| ★**文明化恢復** | 重跑後 **`outpost.l0_to_l1` ≥ main** |
| ★**徒勞折價仍咬** | **買糧 339 型**（真試真敗）**仍然被折價** |

★**兩面必須分開報** —— **只看其中一面，「修回頭」（把折價整個關掉）也會過關。**
（同族：`§N`／`05_acceptance §margin 稀釋` —— **一個數字下降有兩種原因**。）

## §12 診斷（**含 QA 提的那顆，能一刀分開**）
QA 故事稽核：team8 這輪 **5 次紮營 committed、一次紮根都沒接上**；
**建設每次都在候選裡但持續低分（0.2336–0.2616）**，被乞食／遷移找糧／紮營／求和全面壓過。

★**QA 提的 tap 是能定案的那顆**：**`mult()` penalty【之前】的原始 util。**
⇒ **原始 util 對比折價後 util ⇒ 直接看出折價貢獻了多少** ——
**這一刀分開「建設本來就該輸」與「失敗磚額外壓低」。採納，列為診斷①的必要欄位。**

### ⚠️ 但我要訂正 QA 的一個措辭
QA 寫「這輪 team8 比之前成功那輪弱很多（pop 6→2、ef 0–3）⇒ **WHAT 層面資源真的更緊**」，
並據此說**無法排除「建設本來就該輸」**。
★**在【同床同 seed、唯一自變數是磚】的對照裡，「這輪世界更慘」不是 confound，是【結果】。**
⇒ **它不能用來解釋掉 regression**，但**它確實留下一個未定問題**：
**因果路徑是「折價直接壓低建設」還是「折價造成級聯、世界整體變窮」？**
★**QA 提的那顆 tap 剛好也能分開這兩條** —— 所以結論不變：**加那個 tap。**

---

## §13 ★★★診斷回來：**我的假說【推翻】**（照 §12 預寫的規則辦）

| 診斷 | 結果 |
|---|---|
| ①建設類被折價 | ★**紮根本身【0 次】被 failure-memory 折價過**（18 個 suppressed structural id 裡沒有「紮根」） |
| ①懸殊差距的來源 | ★**`root_u 0.09~0.14` vs `winner_u 1.395`（10~40 倍）＝ 紮根自己 baseline util 結構性偏低**，**不是被壓下去** |
| ①近戰時 suppression 的方向 | ★**若有影響也是【幫】紮根，不是害它** |
| ②漏斗 | ★**第三種結果**：`won=2 → start=1 → complete=0 → l0_to_l1=0` ⇒ **斷點在【施工本身】從未完工** |

⇒ ★**「折價反傷探索」在字面意義上不成立，撤回。**
（**我在 §12 就寫好了「建設類幾乎沒被折價 ⇒ 假說推翻」這一格，照辦。**）

### ⚠️ 而且我要收回另一句：**「明確 regression」是過度解讀**
`outpost.l0_to_l1` **main 1 / branch 0** —— ★**那是 1 vs 0，不是 100 vs 0。**
在一個「**102 次 applicable 只贏 1~2 次**」的世界裡，★**1 vs 0 沒有統計意義。**
⇒ **我稱它「明確 regression」是我讀過頭了。**
（`outpost day90 8 vs 12` 差距較大、仍待解釋，**但那條也需要 main 同款 tap 才對得起來** —— measurer 已指出本輪 main 沒有這兩個新 tap。）

## §14 ★★★但診斷撈出一個**比原假說大得多**的結構事實

**`DiscountedFlow.flow_utility` 的 caller ＝ 四個**（`terms.gd:117 / 198 / 211 / 232`）：
**覓食・遷移找糧 ／ 佔村 ／ 併入 ／ 紮營。**

★★**`rooting_drive`（紮根）不在裡面。** 它回傳：
```gdscript
_feasible * clampf(ctx.settle_site_quality, 0.0, 1.0)   # ★[0,1] × [0,1]
```
⇒ ★**兩個 [0,1] 因子相乘 ⇒ 值域 [0,1]，且典型值必然很小**
—— **measurer 量到的 `0.09~0.14` 完全符合這個形狀**（例：`0.3 × 0.4 = 0.12`）。

⇒ ★★★**紮根是「四端同秤」漏掉的【第五端】。**
**它跟已經改用折現真流的鄰居比，尺根本不同** ——
**這不是「輸了」，是【結構上不可能贏】。**

### ★這與我們一路追的所有線收斂
「紮根為什麼不發生」的答案，**不是** drop、**不是** latch、**不是** 折價 ——
★**是它的估值從來沒有跟對手放在同一把尺上。**
（**四端同秤那一刀我漏了它** —— 當時的表列了覓食／遷移找糧／併入／佔村／紮營，**沒列紮根**。）

### 待驗（**不得當結論**）
- **推論**：不同尺 ⇒ 紮根結構性打不過 ——**強，但仍是推論**
- ★**可證偽點**：若 `flow_utility` 的實際輸出也落在 `[0,1]` 且典型值同量級，**則尺並無不同，此推論垮**
- ⇒ **要 measurer 量：四個 `flow_utility` 消費端的【實際 util 分佈】 vs `rooting_drive` 的分佈**

---

## §15 ★★★`blocked_total = 0` 的第二層意義：**記錄側只有【一個事件源】**

implementer 報 `blocked_total = 0` ⇒ 他判「**前提型在這張床一次都沒觸發 ⇒ 對 A 面的影響是構造性的零**」。**那一層對。**
★**但我追了第二層：為什麼沒觸發？**

**窮盡**：`FailureMemory.record` 的 **production 呼叫點 ＝ 1 個**
（`order_system.gd:127`「買單」；另一處 `world_events.gd:33` 是 `record_invalidation` ＝ 失效型／T0 路徑）。

⇒ ★★★**失敗記憶的【記錄側】只接了一個事件源：買單失敗。**

### 這一刀切開了三件事
| 現象 | 真因 |
|---|---|
| `blocked_total = 0` | ★**前提型（缺料）確實大量發生**（T1：28 次 dispatch_fail **全是資源不足**）——**但那些不經買單 ⇒ 沒進記憶** |
| ★**紮根 0 次被折價** | ★**紮根不下買單**（站在原地蓋）⇒ **它的失敗【永遠】不會進 FailureMemory** |
| 「覆蓋率 1 → 16／19」 | ★**量的是「查詢側能認得多少 key」，不是「有多少失敗真的被記下來」** ——<br>19 個 structural id ＝ **19 種「下過買單且失敗」的下令者** |

★★**「覆蓋率」這個詞在這張票上有兩個意思，我沒有分開講**：
**①查詢側覆蓋**（`mult_for_option` 認得幾種 key）★**②記錄側覆蓋**（幾種失敗真的被記下來）。
**我們把 ① 從 2 提到 19，② 一直是 1。**

### ★★所以紮根有【兩個獨立的病】，都在同一個地方
| # | 病 | 票 |
|---|---|---|
| ① | ★**util 尺不同**（`[0,1]×[0,1]` vs 折現真流）⇒ **結構上贏不了** | `rooting-fifth-end-same-ruler` |
| ② | ★**失敗不進記憶**（不下買單）⇒ **就算輸了也學不到** | ★**本節，新** |

⇒ ★**這兩個是獨立的**：**修好 ① 讓它贏得了；修好 ② 讓它輸了會學。缺一個都不完整。**

## §16 processing
- **implementer 兩條紀律都對**：`blocked_total=0 ⇒ 構造性零影響`；
  ★**「沒有同床 main baseline ⇒ 不宣稱回歸」** —— **跨 branch 比正是我們一直拒絕的事，他守住了。**
  （★**同床 baseline 是我開票時就該要求的** —— 同 `04_qa`「比較型主張需兩邊都有」。**我漏的。**）
- **記錄側覆蓋 ⇒ 呈 blueprint**（它改的是磚的**進料口**，不只是接線）。

---

## §17 ★★★裁定：**記錄側【擴】—— 而且這不是新定義**（blueprint 2026-08-25）

★**blueprint 指出我漏看的東西**：**失敗律原文本來就要求**
> **「仲裁拒單／組隊失敗／資源不足／路不通 → 必反饋」**

⇒ ★**律的枚舉【早就含非買單類】。**
⇒ ★★**記錄側 ＝ 1 源 ＝ 律的覆蓋率 `1/N`** ——
**那正是我自己立的「機制已立 ≠ 機制已覆蓋」的【正主】。**
（**我立了那條法，卻沒有把它套到我正在做的這張票上。**）

## §18 ★進料口從哪來：**A1 合規盤點的產出物**（**不由磚發明**）

★**「什麼算一次失敗」不是這張磚要定義的** ——
**A1 合規盤點的要求本來就是**：**每個 drop 點「消滅 **or** 變成【有反饋的 typed 失敗事件】」。**
⇒ ★**那些 typed 事件【就是】記錄側的事件源。**
⇒ 磚的 `record()` **接上它們** ＋ **三分流（前提／執行／失效）在寫入端分路。**

### ★★這讓 A1 那張「原 scope 證偽」的票**有了第二個用途**
A1 我結成「**原 scope 證偽、無病可修，但儀器與窮盡列舉留下當永久資產**」。
★**現在那份「窮盡列舉的 drop 點」變成【記錄側覆蓋率的母體】。**
⇒ ★**這就是為什麼證偽的票不能丟掉產出物** —— **當時它是診斷，現在它是母體。**

## §19 ★覆蓋帳**雙側化**（`00_roles §覆蓋欄` 同步）
| 側 | 定義 | 可機械數？ |
|---|---|---|
| ★**記錄側覆蓋** | **已接線 typed fail 站數 ／ A1 窮盡母體站數** | ✅ **機械可數，無手工表** |
| **查詢側覆蓋** | `mult_for_*` 認得幾種 key | ✅ 照舊 |

★**兩側都要報。只報一側 ＝ 我這次犯的錯。**

## §20 acceptance（**加一條**）
| 面 | 判準 |
|---|---|
| 文明化恢復 | `outpost.l0_to_l1 ≥ main`（★**同床**） |
| 徒勞折價仍咬 | 買糧 339 型仍被折價 |
| ★**新增** | ★**紮根失敗（執行型）重跑後【真的出現在記憶裡】** |

★**第三條是這一輪的直接產物**：**現在我們知道紮根的失敗根本沒進去，那就要驗它進得去。**

## §21 落地
**與三分流【同磚、同 branch】。**

---

## §22 ★★★裁定：**仲裁拒單 ＝ 第四類「未獲允許嘗試」⇒ 不入記憶**

implementer 把記錄側接到一個 **A1 已證明從不觸發**的站點，**自己抓到並照實報**，
並指出**真正會發生的紮根失敗是【仲裁拒單】**（A1 實測 **3 次** ＝ persist_hold 1 ＋ 同層搶班 2），
★**但那格型別沒裁，他不自己歸** —— **正確，那確實只有我能裁。**

### 用 blueprint 的法理去問：**「被仲裁擋下」的真實教訓是什麼？**
| 型 | 教訓 |
|---|---|
| 前提型（缺料） | 「**先解決料**」 |
| 執行型（真試真敗） | 「**這條路走不通**」 |
| 失效型（目標消失） | 「**這件事沒意義了**」 |
| ★**仲裁拒單** | ★**「我現在在做別的更重要的事」** |

⇒ ★★★**那根本不是關於【這個目標】的資訊，是關於【我自己當下狀態】的資訊。**
⇒ **折價它 ＝ 把「我剛才在忙」記成「那個地方不好」＝ 又一次把假教訓寫進記憶。**
★**與 blueprint 對前提型的法理完全同構。**

### ⇒ **裁定：仲裁拒單【不入失敗記憶】**
★**但不是丟掉** —— 它是「**排不上隊**」的證據，
而**「排不上隊」正是 blueprint 已裁「過缺件表、歸脊椎長程計畫層」的那條線**。
⇒ **記在該去的地方**（`persist.hold` ／ `lost_to` 這些既有計數），**不要記進失敗記憶。**

## §23 ★★紮根的【執行型】失敗事件 —— **它不存在，要造**

**窮盡**：`grep` 全 production **沒有任何「施工放棄」的 typed 事件**。
（`camp.abandoned`＝`harvest_system.gd:44` 的 **L0 camp 無人衰減**，★**不是紮根施工放棄**。）

★**紮根真正的執行型失敗 ＝「開工了、但停滯到放棄」**（A1 實測：`start 4 → complete 1` ⇒ **3 次**）
⇒ ★**那三次目前【什麼都沒發生】** —— 沒有事件、沒有記憶、沒有教訓。

⇒ ★★**這正是 A1 合規盤點要求的形狀**：
> **每個 drop 點「消滅 **or** 變成【有反饋的 typed 失敗事件】」**

⇒ **要造這個 typed 事件**（`construction_abandoned` 之類），**它才是紮根的執行型進料口。**

## §24 ⚠️ 我寫的 acceptance **有錯**，訂正
我寫「**紮根失敗（執行型）重跑後真的出現在記憶裡**」——
★**那條假設了「紮根的執行型失敗事件存在」。它不存在。**
⇒ **在造出該事件之前，這條 acceptance【不可達】** —— implementer 說得對。

**訂正為兩段**：
1. ★**先造** `construction_abandoned` typed 事件（**A1 合規盤點的產出物之一**）
2. **再驗**：**該事件發生時，紮根的執行型失敗出現在記憶裡**

★**教訓**：**我寫 acceptance 時假設了一個進料口存在，沒有先確認。**
—— **同「假設不靜默」家族：假設要先驗，不能寫進驗收條件裡當前提。**

---

## §25 ★三面第一輪：**②綠、③未適用、①【不該掛在本票】**

| 面 | 結果 | 判讀 |
|---|---|---|
| ★**②徒勞折價仍咬** | **買糧 27 次仍被折價 ＝ 綠** | ✅ ★**核心沒被三分流破壞 —— 防修回頭那一面守住了** |
| ★**③紮根執行型失敗進記憶** | **＝ 0**，且 ★**③b 前提型 `blocked_total` 也 ＝ 0** | ⚠️ ★**不是 fail，是【未適用】** —— 見下 |
| ①文明化恢復（`l0_to_l1 ≥ main`） | **＝ 0**，且這輪**根本沒紮根成功** | ⚠️ ★**這一面【我當初就不該掛在本票】** —— 見 §26 |

### ★③ 為什麼是「未適用」而不是「失敗」
**根因我早就查到了**（`§15`）：★**記錄側只有【買單】一個事件源。**
⇒ **缺料失敗（T1：28 次 `dispatch_fail` 全是資源不足）不經買單 ⇒ 不進記憶**
⇒ ★**前提型永遠沒料可分 ⇒ `blocked_total` 必然 0。**

⇒ ★★**這不是新問題，是【記錄側擴】那件事還沒做完的【必然結果】。**
**implementer 已加 `③c 入口 tap` 分辨「接線斷」vs「根本沒走到」** ——
★**預期會落在「根本沒走到」**，而那正好會**證實**上面的推論。
（★**他自己加那顆 tap ＝ 死水兩欄／「閘沒擋 vs 閘沒執行」已經內化，不用我再要求。**）

## §26 ⚠️★★①「文明化恢復」**是我把跨票依賴寫進了本票的 acceptance**

**① 要成立，前提是【紮根贏得了】。**
★**而紮根贏不了的原因是【第五端：尺不同】** —— **那是另一張票（`rooting-fifth-end-same-ruler`），還沒做。**

⇒ ★**① 在第五端落地之前，【本來就不該指望它動】** ——
**它現在讀 0，不是「磚失敗了」，是「它依賴的那張票還沒做」。**

★**這與我在第五端票寫的「acceptance 不得假設修完就會贏」是【同一個疏漏的兩面】**：
**我在兩張票之間搬運期待，卻沒有在任何一張票上標明【誰依賴誰】。**

### ⇒ 處置
- **① 從本票 acceptance 【移出】**，改標為 **`blocked-by: rooting-fifth-end-same-ruler`**
- **本票的 acceptance 剩兩面**：★**②徒勞折價仍咬（已綠）** ＋ **③紮根執行型失敗進記憶（等記錄側擴）**

---

## §27 ★★★③c 打掉我的預期：**不是「未適用」，是【真的斷了】**

**我預測**：`blocked_total = 0` 的原因是「**根本沒走到**」（記錄側只有買單、缺料失敗不經買單）。
★**③c 入口 tap 的實測**：
```
builder_entry   = 28
資源不足        = 28    ← ★正是呼叫 record_blocked 的那一支
blocked_total   =  0
```
⇒ ★★**走到了，而且走了 28 次。是【接線斷】，不是沒走到。**
⇒ ★**我的預測【不成立】，明確作廢，別再往「沒走到」找。**

### ★斷點：`record_blocked` 開頭那行 —— **身分空就【靜默 return】**

## §28 ★★這一行錯在【兩層】

### ①**靜默**
★**失敗路徑不得靜默吞掉。** 28 次進來、0 次記下、**沒有任何訊號** ——
★**若不是 implementer 自己加了 ③c 入口 tap，我會把「未適用」寫進帳，而真相是「斷了」。**
（**同族**：「假設不靜默」「守衛要輸出已處置完的結果」——★**這次是【失敗路徑】靜默。**）

### ②★★**它去讀【當下值】拿身分** —— 而在這個時序上，當下值必然是空的
★**`_dispatch_builder` 的失敗發生在【派遣之前】** ⇒ **`current_dispatch_id` 根本還沒被蓋上。**
⇒ ★**那不是 bug，是【時序】：「派遣失敗」比「身分被寫入」更早發生。**

★★**而正解早就寫在 (B) 裁定裡**：
> **「夾帶者是 dispatch，不是 scorer。」**
⇒ ★**同一句話在這裡的版本：「夾帶者是【呼叫端】，不是 `record_blocked` 自己。」**
**呼叫端手上本來就有 `leader_team` / `target_pos` / `outpost_type`。**

## §29 修法（**兩件，缺一不可**）
1. ★**身分由呼叫端傳入**（不要在被呼叫端讀當下值反解）
2. ★**身分若仍為空 ⇒ 記 tap 並明確處置，⛔不得靜默 return**
   —— **「我沒辦法記」也是一個必須被看見的結果。**

## §30 ⇒ ③ 的狀態改判
| 先前 | 現在 |
|---|---|
| 「**未適用**」（記錄側還沒擴） | ★**「真的斷了」** —— **28 次進來、0 次記下** |
⇒ ★**③ 從「等別的票」變成「本票要修」。**

---

## §31 ★★★③ 結案：**「有證據的未進料」≠「沒發生」** ⇒ **判準改寫**

**修法驗證形狀【完全如預測】**：
| 量 | 修前 → 修後 |
|---|---|
| `blocked_total` | **0 → 28** |
| `blocked_no_identity` | **28 → 0** |
| ★買糧折價 | **27 → 27**（★**無連帶損傷 —— 兩面互證再次奏效**） |

★**這是今天少見的「預測完全對」** —— 對照前面多次被實測打掉，**值得標出來**。

### ★★③ 仍是 0，但**性質變了**
| 先前 | 現在 |
|---|---|
| 「**假設**沒發生」 | ★**「有證據的【未進料】」** |

**證據**：★**28 筆前提型 ＝ 紮根【有在嘗試】，但每次卡建材閘、【從未派出】。**
⇒ ★★**沒有執行，就不可能有執行型失敗。**
⇒ ★★★**第三面在這張床【結構上不可能綠】，不是「還沒發生」。**

### ⇒ 判準改寫（**不能留一個結構不可達的條目**）
| | 舊 | ★新 |
|---|---|---|
| ③ | 「**紮根執行型失敗出現在記憶裡**」 | ★**「記錄側整條鏈通了 —— 任一型別有料進來且身分正確」** |
⇒ ★**28 筆前提型【已經滿足新判準】**（`no_identity` 歸零 ＝ 身分正確）。
★**並明標**：**執行型在這張床【零樣本】，因為紮根從未派出（有證據，非假設）。**

★**理由**：**原判準把「鏈通了」與「這張床上恰好有那種樣本」綁在一起** ——
**前者是本票的責任，後者是世界的狀態。**（**同 `05_acceptance §跨票依賴` 的家族。**）

## §32 ★★★三條獨立證據鏈都指向「紮根不發生」，**但它們在不同層**
| # | 層 | 證據 |
|---|---|---|
| ① | **argmax（秤輸）** | 第五端：`root_u 0.307` vs 紮營 `1.585`（**3.7～5.2×**） |
| ② | **dispatch（派不出）** | T1：**28 次 `dispatch_fail` 全是「資源不足 1.5×」** |
| ③ | ★**失敗記憶（獨立第三條）** | ★**28 筆前提型 ＝ 有嘗試、卡建材、從未派出** |

★**①與②③不矛盾，是漏斗的兩段**：**102 applicable → 少數贏 → 贏了也卡建材。**

### ★★⇒ 前瞻警告（**寫給第五端票，免得重蹈覆轍**）
★**修好第五端會讓紮根【贏更多】，但那 28 筆前提型說它【贏了也派不出去】。**
⇒ ★**第五端票的 acceptance「文明化恢復」很可能【也達不到】** ——
**因為它同樣依賴「建材閘」這件本票以外的事。**
⇒ ★**依 `05_acceptance §跨票依賴`：第五端票要先標 `blocked-by: 建材閘`，不要等它讀 0 才發現。**
（★**我剛因為「跨票依賴沒標」被咬過一次，這次提前標。**）

### 📌 待收的一條線
**blueprint 開給 measurer 的「那 28 次缺哪種建材 ＋ genuine-depletion vs 盲派」平行票，我還沒收到結果。**
★**那條線現在是【三條證據鏈的共同下游】** —— 它的優先序應該上升。

