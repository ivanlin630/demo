---
slice: ②`estimate_catch_up` 在一次 `gather` 內只算一次
owner: systems
status: CLOSED【依預註冊門檻】（2026-09-22）
基於: implementer 規模量測 2026-09-22（世代 6／HW-2，兩個 seed）
---

裁定：**不做。U₂ ＝ 8.39%（seed 1337）／8.15%（seed 77）＜ 預註冊門檻 10.0%。**

# §1 卷面（★我自己重算過，不是轉貼）

```
             S       E      D2      U₂ ＝ S×E×D2
seed 1337  0.587   0.290   0.493        8.39%
seed   77  0.573   0.289   0.492        8.15%
S ＝ 那幾段佔 gather.*｜E ＝ estimate_catch_up 佔那幾段｜D2 ＝ 那兩條走訪裡【同鍵重算】的呼叫佔比
★我的獨立重算：0.587×0.290×0.493 ＝ 0.0839 ✔｜270320/548405 ＝ 0.4929 ✔｜227484/462680 ＝ 0.4917 ✔
★★卷面與 raw 我逐一開過檔（四個檔都在，見 §5）
```

# §2 ★★★成立謂詞（WHAT 2026-09-22 新規矩：以事實為理由的結案，結案書必寫）

```
P1【形狀】生產碼裡 `PathSystem.estimate_catch_up(` 的呼叫點 ＝ **7**（全在 faction_ai_system.gd），
   而本票的合併形狀**只涵蓋其中 2 處**（attack_scan／`_find_weakest_prey` 那兩條走訪）。
   ⇒ ★**可機械檢查**（met_check 釘住 7 這個外部常數）。變成 8 ⇒ 算術要重推 ⇒ 謂詞假 ⇒ 自動重開。
P2【規模】在世代 6／HW-2 下 S×E ＝ 0.170／0.166，而 D2 的**構造上界是 0.5**（同鍵第 2 次以後）
   ⇒ ★★**這一格不可 grep** —— 它是量出來的，不是讀出來的。
   ⇒ ★★★所以我**不假裝**它有 met_check：它的觸發器是**世代邊界時重量**，
      而它躺在 defers 的 `one-cycle-shape-…` 那一列旁邊，由下一次量 gather 成本的人順手核。
      **我把這個誠實限寫出來，而不是編一條會恆綠的檢查。**
```

# §3 ★判準怎麼下的（三件，缺一不可）

```
①兩個 seed 同側（8.39／8.15，都 < 10.0）⇒ 不是邊界案例，**不需要第三顆種子**
②★U₂ 是【上界】不是實際值 ⇒ 真省 ≤ 8.39% ⇒ **結案站在安全那一側**
③★★★第三格檢查（「可比較的量必然是三格」）：≥10／<10／**不可判**。
   不可判要看：母體塌陷？D2 超出構造上界？只有一顆種子？——★三項皆否 ⇒ **可判**。
```

# §4 ★★★不在本票、而且**必須另外記帳**的那一塊（★別讓它被吸收掉）

```
implementer 第一版用【全部 7 個呼叫點】算出 D_all ＝ 0.695／0.699 ——
★它超過構造上界 0.5，而他**自己把它當成母體錯配的信號**（不是「賺更多」）⇒ 正確。
★★但那個數回答的是**另一個問題**：「把 7 處全部共用」的重複率比「只合併那兩條」高很多。
★★★而它**沒有被量過**：E 的分母只涵蓋那 2 處的計時 ⇒ **另外 5 處的 T 是空白**
   ⇒ 那張票的 U【算不出來】，不是「比較小」。
⇒ 已登 defers：`share-estimate-catch-up-across-all-seven-call-sites`（要量的就是那 5 處的 T）
```

# §5 落地（★我逐一開過檔，不是信他說在手上）

```
docs/measurements/2026-09-22-U2-estimate-catch-up-gen6-HW2.txt          2549 bytes ✔
docs/measurements/2026-09-22-U2-u2-s1337-gen6-HW2.log.gz              98950 bytes ✔
docs/measurements/2026-09-22-U2-u2-s77-gen6-HW2.log.gz                95080 bytes ✔
docs/measurements/2026-09-22-U2-u2-smoke2-gen6-HW2.log.gz             17064 bytes ✔
★樁只活在拋棄樹 A:/GDS/_gt4，不進 main（implementer 自報）
```

# §6 ★未證的那一格（結案不依賴它，但寫下來）

```
implementer §三① 自報：他證了【兩次呼叫的引數相同】，**沒有證【答案相同】**。
★那需要全世界指紋，而它是**修法的驗收條件**——而我們不做這個修法 ⇒ 結案不依賴它。
★★他提議加樁再跑一輪（約 12 分鐘）⇒ **我回絕了**：它不會改變 U₂，也不會改變門檻判定。
★★★若 P1 或 P2 將來被推翻而本票重開，**那一格要在開票時補回驗收**。
```
