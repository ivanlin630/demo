---
owner: systems
status: 待 R²
date: 2026-09-16
for: measurer
---

# 這一輪要回答什麼

**世代 5（sha `2fb10d7c1`）落地之後，那把新的秤【真的在秤】嗎？**
★**而所有世代 4 以前的攻擊／偵查讀數不可與它比**（`_generation-boundary.md` 已記：**尺換了**）。

# ★★★四格，全部【成對反事實】

```
\u2460**處境攻擊有時贏**：餓且有牙的隊（team80 型）⇒ **攻擊會贏 argmax**
   ★對照：**飽且目標硬的隊 ⇒ 仍然不打**
   ⇒ ★★**兩格成對** —— **只驗前者，「它總是打」也會綠。**
\u2461**偵查真發生**：`recon.dispatch.engine.ok > 0`
   ★對照：**舊走廊 `g3.scout_dispatch` 全窗 ＝ 0**（★新制下它不該再產生偵查）
\u2462**先驗被取代的【逐筆】證據**：同一目標，情報到手前後各一筆
   ⇒ ★**逐筆，不是聚合** —— **聚合答不出「這一筆用的是哪一個」。**
\u2463**粒度有生效**：桶 1／桶 2／桶 3 的 `richness` **嚴格單調、最小 > 0、最大 < CAP**
```

# ★★偵查要用【三個數】夾住，不是一個

```
**候選數**（它有沒有被生出來）／**勝數**（它有沒有贏）／**設上數**（它有沒有變成行動）
⇒ ★**對帳式：**設上 ＋ no-op ＝ 【走到仲裁】**
   ★★★**更正（2026-09-16）**：我原寫「設上 ＋ no-op ＝ **勝數**」—— **不成立**。
   **派工迭代逐名次走 `ranked` 並 `continue` 試次佳**
   ⇒ **走到仲裁的次數 ≥ 勝數**（贏家一定走到，而排後面的也可能走到）
   ⇒ ★**【勝數】與【走到仲裁】之間沒有恆等式**
   ⇒ ★★**而我前一輪才因為這件事被更正過（「61 次不是勝數」）** ——
     **然後我把同一個已被推翻的前提寫進了一支守衛。**
   ⇒ ★★★**implementer 拒絕裝它（「我不裝一支注定恆紅的守衛」）是對的** ——
     **一支永遠 FAIL 的閘，三天後就是背景雜訊。**** —— ★★**對不上 ⇒ 先查儀器，不要先講世界。**
⇒ ★★★**只看「總量」會漏「贏了卻沒被設上」** —— **機制看起來動了，而世界沒變。**
現況基準（樹 `42e1f0915`）：**候選 5312／贏 578**
   ⇒ `docs/measurements/2026-09-16-attack-currency-final-10day-v2-raw.txt:6818`
```

# ★多 seed（★本輪的主要理由）

```
★**存在性**（「這條路會不會 fire」）⇒ 單 seed 夠
★★**比率**（勝率／佔比／「幾支隊」）⇒ **單輪單窗不可引用** ⇒ **本輪就是為它跑的**
⇒ ★★★**而 (4a)/(4b) 的 priority tap 掛在這一輪**（一直掛著，沒有被塞進別輪）：
   **那 621 次「優先序不足」，擋它的 task priority ＞ 它自己 ⇒ (4a) 正確；否則 ⇒ (4b) 手不聽腦。**
```

# ★★★跑法（照【為隨時被殺而設計】三條）

```
\u2460**每一欄標「它最早在第幾天可判」** ⇒ 能用短窗答的不要綁長窗
\u2461**逐日 flush**；「跑完才印」只留給真的需要全窗的欄位（★本 spec 只有\u2462需要）
\u2462**窗戳寫「實際第幾天／目標第幾天／為什麼停」** ——
   `27/30 外部截斷` ≠ `30/30 完成` ≠ `27/30 自持門檻停`
★**開跑前看 `FreeMB`、跑完再印一次**；★★**樹的 commit 要記**；
★★★**戰爭類讀數的窗要標【含不含偵查時代】** —— **窗夠長但全落在開場，會誠實地量出假和平。**
```

# 不做

- 不調任何參數（★**這是量測，不是修法**）。
- 不與世代 4 以前的數字直接比（★**尺換了**）。
