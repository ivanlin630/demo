# HOW spec：給秤一個胃口 —— 攻擊的【機會＋需要】項

- **WHAT 授權**：blueprint `2026-09-12-blueprint-to-systems-give-the-scale-an-appetite.md`
- **病因（現場證據）**：攻擊 util 零 final **1238 次、全部來自 `terms_sum`**（係數／失敗乘數正常）
  ⇒ 四個 term 的 drive 同時 0，而 weight 都非 0
  ⇒ ★**四項全是【授權形狀】**：`faction_duty` 要派系令／`attack_drive` **也**要派系令／
    `intent_fit` 要征服身分／`feud_pull` 要有仇
  ⇒ ★★**門降級了，秤沒有** —— 同一個病的下一層。

---

## ① ★★★前置：**一個必須先答的前提**（★便宜，而且它可能改變裁定）

```
blueprint 的組成是：**掠奪值 ＝ belief 估的對方資產 × 贏率 × 自身需求 − 風險**
⇒ ★而「**belief 估的對方資產**」**我查到的是矛盾的證據**：
  ・`distortion_engine.gd:85` 寫 `snap["food_est"] *= …`（**像是有**）
  ・`decision_context.gd:957` 逐字寫 **「A2 感知鐵律：…belief schema **無 food_est** → 降級」**（**像是沒有**）
⇒ ★★**我不用讀 code 判它** —— **要逐筆資料**：
  ①`best_estimate()` 回來的字典**實際含哪些鍵**（跑一趟，印鍵的分布）
  ②`food_est`／`coin_est`／`armed_est` **各有幾筆真的存在**（母體 ＝ 所有 belief 條目）
⇒ ★★★**若「對方資產」的 belief **不存在或幾乎全空** ⇒ 那 blueprint 的「零新狀態」前提不成立**
  ⇒ **停下來回報**，不要自己補一個新欄位（★那會是**新狀態**，而它需要重新裁）。
★而**已確認存在**的兩塊（不必再查）：
  ・需求：`need_oracle.gd:82 need_keep()` ／ `:223 demand()`
  ・贏率／戰力接地：`decision_context.gd:460` 有效武裝比 → `terms.gd:218` `capability_factor`
    （★**而 `loot_drive` 這個 term 已經存在並疊了 capability** —— 先讀它，可能有現成形狀可沿用）
```

## ② 設計（★前置答完才動）

```
新增一個 **機會＋需要** 的 drive（掛在攻擊 option 的 terms 上）：
  **值 ＝ f(【belief 的對方資產估計】, 【贏率】, 【自身需求】, 【風險】)**
★**三條硬規則**：
  ①**資產只能讀 belief**（★感知鐵律：**禁 god-view 直讀對方 `resources`／`population`**）
  ②**需求走 need oracle**（★**禁手抄物理**、禁死常數）
  ③**贏率走既有的 capability 接地**（★不要新造一個戰力公式）
★★**授權四項【保留】**，角色從**唯一來源**改成**加成**：
  派系令／血仇**讓分數更高**，而**沒有它們時分數不再是 0**。
★★★**人格調製而非常數**：好戰／貪婪**放大**，慎重**壓低** ——
  ★**MODULATE 真值，不是 boost 一個常數**（禁 crank）。
```

## ③ 驗收（★★★blueprint 的「分布本身就是證明」，我做成可機械檢查）

```
①**面對面不動手的殘量**：754 那個母體重量，**攻擊 util 零值佔比必須下降**
②★★**分布活著**：`攻擊=0.000` 的**反覆出現必須消失**，代之以**連續分布**
   ⇒ 機械判準：**非零 util 的【變異】不得為 0**（★不是「有非零值」就算過 ——
     全部同一個數也叫非零，而那是**另一個常數**）
③★★★**成對對照（方向性）**：**窮而弱的目標 → 低分**／**肥而弱的目標 → 高分**
   ⇒ **兩格都要跑**：只驗一邊證明不了秤在讀資產
④★**授權群 vs 無授權群**：**無派系令、無血仇、非征服身分**的那一群，
   **util 不得仍然全為 0** ⇒ ★★若仍全 0 ⇒ **機會項沒有接上**（「加成」偷偷變回「唯一來源」）
⑤**fp 會變**（裁定接受）＋ 同 seed 兩跑相同
⑥★**禁死旗／禁 crank**：不得為了讓攻擊贏而調高權重 ——
  ★★若加了機會項之後攻擊**仍然很少贏**，那是**平衡題**，回 blueprint，**不回頭調數字**。
```

## ④ 不做的

```
★不動門（攻擊門已 merge）｜★★不動相遇機制｜★★★不新增 belief 欄位（見 §①）
```

---

## ⑤ ★★★R² 回件：**那個矛盾不是矛盾，是【兩層】**（2026-09-12 CLEAN）

```
・**tier2**：有 detailed `food_est`（`interaction_system.gd:1343-1358`）
・**tier0／1**：只有粗的 `resource_scale` 桶（`vision_system.gd:150-179`，**零 `food_est`**）
⇒ ★**我引的兩則都對，各自講對【一半母體】** ——
  ★★而我把它讀成「矛盾」，是因為**我把 belief 當成單一母體**。
⇒ ★★★這是「假窮盡」的鏡像：**不是有人讀錯，是母體有分層而我們沒有分層地問。**
★而現成解法**已經在跑**：`_belief_richness` ⇒ **不算新狀態** ⇒ **blueprint 的「零新狀態」前提成立**。
⇒ §① 的前置**降級為「照抄事實」**：不必再跑一趟去問，**但 tier 這件事要進設計**（見下）。
```

### ★★驗收補一格（★因為 tier 分層是【設計事實】，不是雜訊）

```
⑦★**依 belief tier 分桶看攻擊 util 的分布**（tier0／1／2 各一列）
  ⇒ ★★**預期**：**tier2（知道得多）的隊，分數應該更會【跟著目標的肥瘦走】**；
    tier0／1 只有粗桶 ⇒ 分數應該**更平**。
  ⇒ ★★★**而這一格的意義超出本票**：若它成立，
    **「情報優勢」第一次變成【行為上的優勢】** —— 知道得多的人，挑得更準。
  ⇒ ★**若不成立**（三層分布一樣）⇒ **回報**：那代表 `_belief_richness` 沒有真的進到這個秤裡。
★注意：**這一格是【觀察】不是【目標】** —— 不准為了讓它好看而調 tier 權重（禁 crank）。
```
