---
role: reviewer
code: "02"
status: idle
current_ticket: "-"
updated: 2026-08-05
---

# 02 reviewer 現況

**狀態**：閒置（★領主照護loop build(a24d4c71) R②merge-gate判決CLEAN(兩必查項皆獨立逐行核完成)→measurer量cohesion①natural真考；HOW R②已綠；失聯帳本defensive/rescue真consumer fix(baf2a670,修QA REFUTE手不聽腦) R②merge-gate判決CLEAN→measurer re-measure→QA新verdict→merge(與cohesion合併里程碑)；g3 bond counter+uprising gate build已merge(含★revert scare獨立HEAD驗證)；g3延伸HOW已綠；勢力凝聚力build(90631616)已merge(measurer量分化揭③下游解鎖FAILED因第四出口g3.betrayal漏修,催生這輪延伸)；HOW R②已綠；失聯帳本build(a3c11288) R②merge-gate判決CLEAN已merge(補完批兩slice皆綠)；L3循環貿易build已綠；game_setup faction-key robust已綠；distribute免費直注relief/side-dispatch/de-scan已綠(資訊網arc症1機制面收官)；herald非team輕carrier已綠；Part2 side-action/dispatch fix/bootstrap fix皆已綠；資訊網whole HOW已綠4片已commit worktree；L1 distribute de-patch/製造樞紐HOW已綠；size-matter/CASE-B弧線四塊全merged待measurer §8最終數字；三腿+means-end+持守統一arc 全綠）

**★★領主照護loop build(a24d4c71) R②merge-gate判決CLEAN（`2026-08-05-reviewer-to-systems-R2-care-loop-build-mergegate-CLEAN.md`）**：★★必查項②獨立逐行核完成——讀`feat/lord-care-loop`branch HEAD live內容(非commit歷史)確認firsthand觀察write整段(讀`target.population`/`effective_food`合成distress、去重、寫`team_known`)確實inline寫在既有`if scout.tile_pos==target.tile_pos`co-location分支內部，comment裡把上輪我的顧慮逐字記下來。★test⑤是這輪最重要的adversarial驗證——故意把scout搬離村莊後assert『不會』發生firsthand write，正面測試防線本身非只測有co-location時會寫。必查項①holding refresh-and-keep親讀`_step_contact_ledger`確認新增`kind=="holding"`專屬分支，逾時觸發反應後重置`dispatched_tick`/`expected_return_tick`並append回`kept`(非落入resolved丟棄路徑)，搭配`_ensure_holding_ledger`(once-guard)同cadence迴圈裡先呼叫，兩機制疊加比我上輪列的兩選項都做了更完整。test①直接呼真實`_step_contact_ledger`確認entry觸發反應後仍留在ledger。care/ignore(`_pick_care_reaction`)雖2候選但義氣/統領vs野心兩個獨立加權util比大小，非直接比人格特質，結構genuine。test④走完整`_tick_info_scout`→真實`GoalResolver._distribute_candidates`端到端確認observation真的餵到賑濟候選生成。determinism/constitution/零死常數皆親驗坐實。CLEAN→measurer量cohesion①natural真考(moderate-distress ex-ante床)→QA→merge→blueprint推用戶。

**（舊）領主照護loop HOW R②判決CLEAN+2必查項（`2026-08-05-reviewer-to-systems-R2-care-loop-HOW-CLEAN.md`）**：blueprint選P4=(a)firsthand觀察write。★必查項①holding「refresh-not-resolve」語意跟`_step_contact_ledger`既有迴圈結構有真衝突——親讀該函式(faction_ai_system.gd:4697-4719)確認任何entry一旦逾時觸發反應就`resolved=true`永久丟出`dispatch_ledger`(對herald/scout/convoy一次性派出正確，但holding要求持久監看查完不該消失)，spec的「or lazy補建」機制沒講清楚是否真能等價於refresh，要求implementer/systems動工前選一條路寫清楚(lazy全補建 vs`_step_contact_ledger`對holding kind特殊處理refresh塞回kept)非留給implementer自己猜——猜錯會是「領主查過一次自家村後就再也不會被提醒去查」這種難發現的靜默失效。★必查項②(這輪風險最高項)：新firsthand觀察write函式讀的資料類型(村食物/人口)跟本arc更早幾輪distribute-descan那次修掉的`_resident_food_runway`god-view違規幾乎一樣，唯一合法性差異=co-location gate——要求這個新函式**必須**寫在`_tick_info_scout`既有`if scout.tile_pos==target.tile_pos`分支裡面，不能變成外部可獨立呼叫函式，否則等於復刻剛修好的違規；merge-gate那輪會逐行核對呼叫點位置。care/ignore competing argmax結構跟已驗證過的react_util四類同款、distribute reuse/零死常數程度界線皆親驗合理。CLEAN+2必查項→dispatch implementer(`feat/lord-care-loop`)→量cohesion①natural真考→QA→merge。

**（舊）領主照護loop(一件解三題:cohesion①natural+ledger lord-village lane+relief proactive) R①判決CLEAN+1須HOW決斷項（`2026-08-05-reviewer-to-blueprint-R1-care-loop-verdict.md`）**：★這輪驗證方式特別注意——這幾週並行開的feature branch(`feat/faction-cohesion`/`feat/missing-contact-ledger`/`feat/L3-circuit-trade`/`feat/info-network-whole`)彼此還沒互相merge，親grep確認`_ledger_record`在physically checked-out的worktree(`feat/info-network-whole`)裡grep不到，改用`git show feat/missing-contact-ledger:<path>`切到正確branch尖端才抓到——避免worktree stale誤判。P1親驗坐實：確認`_ledger_record`只有3處呼叫點(herald/scout/convoy)，無第4種holding/村莊監控kind，「未wired」屬實。P2親驗坐實：`_tick_info_scout`確認co-location抵達時`scout.info_returned`真fire。P3沿用本session稍早distribute三輪已深度驗證的belief-based免費gift機制。★P4(重點驗)親驗坐實=真缺口存在非過度謹慎：`_deposit_help_need`親讀確認只relay村子`active_orders`裡已存在的買單，不會從scout firsthand co-location這件事本身產生任何獨立「這村子看起來多需要」的觀察值——如果村子自己還沒post買單，scout到了也不會產生新distress訊號，「觀察write路」確實不存在，只有「relay既有訂單」這條窄路。要求HOW階段明確決斷：(a)補一條真正的co-location firsthand觀察write(同格物理在場=既有感知鐵律carve-out合法)或(b)明確論證relay既有買單已經夠用(理由：真正需要的村子遲早會透過既有need-driven order-posting管線自己貼單)——不論選哪條都要交代，非留白默認夠用。CLEAN→鎖→systems HOW。

**（舊）失聯帳本defensive/rescue真consumer fix(baf2a670) R②merge-gate判決CLEAN（`2026-08-05-reviewer-to-systems-R2-ledger-fix-mergegate-CLEAN.md`）**：QA REFUTE抓到「手不聽腦」——上輪`_apply_contact_reaction`的defensive/rescue兩類argmax結構雖真但選中後是write-only flag零consumer，慎重領主選defensive跟野心領主選writeoff世界效果毫無差異=假分化，撞[[feedback_verify_execution_end]]。這輪修法：①defensive→親讀`decision_context.gd`確認`team.contact_vigilant_until>current_tick`時既有`threat_threshold`真的被`maxf(...-CONTACT_VIGILANCE_THREAT_DROP,0)`下修，`threat_threshold`是既有備戰/防衛/求和決策已在讀的既有機制，非新旋鈕。②rescue→親grep確認`SubteamSystem.dispatch_anon_messenger`(subteam_system.gd:70-85)是這整個arc從herald/scout輪就在用的既有anon信使共用原語，reuse非發明；`_lost_unit_pos`親驗belief best_estimate優先、缺則退回ledger自己的last_known_pos，零live god-view。test⑦真呼`DecisionContext.gather()`比較threat_threshold改動前後真實輸出、test⑧真掃`state.teams`找有沒有真spawn出對的子隊——皆integration-level驗證非查flag。determinism byte-identical但雜湊≠前一輪inert baseline=誠實佐證真的改到行為。CLEAN→measurer re-measure(4類全真+分化+98breakdown)→QA新verdict→merge(與cohesion合併里程碑)。

**（舊）g3 bond counter+uprising faction_id gate build(03f03ce4+00a40775) R②merge-gate判決CLEAN（`2026-08-05-reviewer-to-systems-R2-g3-uprising-build-CLEAN.md`）**：★systems這輪標註diplomatic_ai counter曾被linter/concurrent revert一次已re-apply，特別要求我獨立再驗。我沒有只跑`git show <commit>`看歷史diff(那證不了現狀)，改用`git show feat/faction-cohesion:<path>`直接讀branch尖端live內容——確認`diplomatic_ai_system.gd`HEAD真的有`driver -= FactionAISystem.new()._faction_stay_benefit(...)`(完整comment都在)、`faction_ai_system.gd`HEAD的`_evaluate_uprising`第一行真的是`if team.faction_id==-1:return`——兩處都是這輪獨立重新從branch尖端撈出來確認，非採信systems自我陳述，確認真landed非又被revert。bond counter diff(03f03ce4)只加6行+新測試檔`g3_betrayal_bond_test.gd`；test①這次改用真實`betrayal_assessment`端到端呼叫驗證分化(比上輪uprising test⑤的重算公式寫法更紮實)。uprising gate(00a40775)親讀確認精準鏡射defect gate結構，修R①那輪發現的第四個缺口(independent隊空觸發uprising副作用)，純加一行零改動既有決策邏輯。§1雙向/零god-view/genuine opportunism/determinism皆親驗坐實。CLEAN→measurer re-measure(改用established-factions床)→QA→merge。

**（舊）g3.betrayal延伸(勢力凝聚力第四出口bond counter-term) HOW R②判決CLEAN（`2026-08-05-reviewer-to-systems-R2-g3-extension-HOW-CLEAN.md`）**：measurer量③(rep床不再秒崩)FAILED，真根是`diplomatic_ai`的g3.betrayal機制完全沒被前一批cohesion arc碰過、仍是單邊秤(driver=機會+不忠,零bond/relief counter)=同病灶第四個出口。親讀`diplomatic_ai_system.gd:296-300`確認driver公式(personality+advantage×BETRAY_ADVANTAGE_GAIN、power_gap>0.5抑制)確實零counter項，`ally_pop_est`來源(faction snapshot/belief)確認非live god-view。★這輪最重要查核：共享`_faction_stay_benefit`會不會變第五套精度——親確認這函式是`FactionAISystem`上無instance-state純函式，`diplomatic_ai_system.gd`要呼叫走`FactionAISystem.new()._faction_stay_benefit(...)`這個本session已見過幾十次的既有跨class呼叫慣例，不需要先重構成static，最小改動就真的呼叫同一份code非另抄一份，上輪抓到的「兩套精度」風險零成本避免。§1防crank五點逐一對上實際code：`_execute_betrayal`(:324-333)的`clear_team_faction`完全未被觸及=零刪；counter是減去已驗證genuine的值非boost常數；零god-view繼承既有性質；genuine opportunism保留(stay_benefit≈0時driver幾乎不變仍可能過門檻)；`consider_betrayal`的randf soft-band(僅在窄margin內生效)完全沒被觸碰，counter只是driver計算上游插入的純算術減項，determinism不受影響。0.65 semi-cliff正確defer避免scope creep。CLEAN→回systems→dispatch implementer續build（`feat/faction-cohesion`）→re-measure(★③下游解鎖真驗+4出口佔比map)→QA→merge。

**★★勢力凝聚力 build(90631616) R②merge-gate判決CLEAN（`2026-08-05-reviewer-to-systems-R2-cohesion-build-mergegate-CLEAN.md`）**：★上輪必查項親讀diff確認真落地——`_trigger_defection_evaluation`的`a_score`從粗糙`has_benefactor_memory`flat+0.3真的換成`honor+_faction_stay_benefit(state,team)`，三決策點統一同一helper非留兩套精細度。§1防crank五條逐一親驗：①defect`execute()`/uprising Path B流亡完全沒出現在diff裡=走的一側保留未焊死②`stay_benefit`公式讀真機制(relief_memory飽和+heard_reputation)非常數加成③test④親驗adversarial(竄改領主live population+另一人memory後stay_benefit不變)④`unrest>=20`precondition確認在diff hunk外未被觸碰⑤三決策點整併如上。★test①(命門)親讀確認是嚴格controlled-variable實驗(相同distress+相同低honor只變relief history，被救過不defect/沒被救defect)——這個session看過設計最紮實的分化測試之一。test③直接呼叫真實`_market_visitor_sell`走完整settle路徑驗benefactor write非mock。輕量觀察(非阻塞)：test⑤方法論較弱(手動重算公式比對非直接呼`_evaluate_uprising`，親自逐項核對這輪常數跟真實code無漂移但對未來公式改動偵測力弱)；`COHESION_RELIEF_SAT=3.0`非DERIVED既有常數但有文字理由。P3立國查根結果(『立國』goal從未emit,orphan)印證上輪追的卡點，正確歸類正統arc記檔非本arc順修。CLEAN→measurer量分化(好vs爛領主壽命+該散照散、無配額)→QA故事稽核→用戶。

**（舊）勢力凝聚力(P4真好處接留走秤) HOW R②判決CLEAN+1必查項（`2026-08-05-reviewer-to-systems-R2-cohesion-HOW-CLEAN.md`）**：grounding=exit-attribution量測證defect為genuine(unrest-gated distress非honor cliff)→主刀改成P4真好處接留走秤非de-patch(拆genuine=反向crank，blueprint親自confirm P4為primary)。Seam親驗坐實：`known_reputations`(team_data.gd:227)/benefactor memory既有write點(interaction_system.gd:1204/player_command_system.gd:1013)皆逐字對上spec claim；distribute relief settle成功點(interaction_system.gd:877-886，這arc前幾輪已審過)是乾淨掛點。★意外發現：spec Seam段漏列了第三個既有『留vs走』決策入口——`_trigger_defection_evaluation`(faction_ai_system.gd:4620-4639，由失聯帳本arc的`_evaluate_owner_contact`觸發)本來就已經在用benefactor memory(粗糙flat+0.3 bonus)當a_score一部分，跟這次要蓋的精細版`_faction_stay_benefit`是同一底層事實兩套不同精細度讀法——要求systems/implementer交代這個關係(整併或說明保持獨立理由)，非阻塞但必須有交代，避免同一事實多套讀法散落各處(同失聯帳本那輪`_evaluate_owner_contact`重複偵測邏輯同款risk)。§1防crank雙向(禁boost逼留+禁刪真走)/uprising 3道門+後果秤(上輪R①意外發現被正確吸收)/立國查根範圍規劃皆親驗合理。CLEAN→回systems→dispatch implementer build(`feat/faction-cohesion`)→量分化→QA故事稽核→用戶。

**（舊）R①判決CLEAN+1補充發現（`2026-08-05-reviewer-to-blueprint-R1-cohesion-verdict.md`）**：資訊網arc收官後新弧線——faction-fragility三擋驗收(relief窗被起義斷/rep床崩單勢力/L3無cross-faction domain)催生的政治入口arc。P1親驗坐實：`event_faction_defect.gd:4/16`確認`DEFECT_HONOR_THRESHOLD=0.35`是純布林門檻函式(honor<0.35 or trust<0.35)零util weighing。P2親驗坐實：`faction_ai_system.gd:4571/4577`兩條uprising結局路徑(守城/流亡)皆unconditional呼`clear_team_faction`，零讀任何後果變數。P3親驗坐實且多追一層卡點：`_declare_established`只在`:1820 if「立國」in f.goals`才會被呼，真正該查的根是「什麼條件下f.goals會被塞進立國」非函式本身邏輯，比spec原引用位置更精確一步。★P4(重點驗)親驗坐實非誇大：完整讀過`_evaluate_uprising`(:4535-4553)全函式跟`event_faction_defect.check()`，確認兩個「留vs走」決策入口從頭到尾零relief/labor/distribute訊號讀取，只讀人格值+硬門檻。★意外發現：`_evaluate_uprising`自己藏了三道spec P1沒提到的額外死常數門檻(avg_loy>=0.2/unrest_turns<60/stress_sources<2)，要求HOW階段「拆死常數出口」刀口一併納入考量非只拆義氣/信義那組。CLEAN→等exit-attribution量測一起鎖spec→systems R②。

**★★失聯帳本 build(a3c11288) R②merge-gate判決CLEAN（`2026-08-05-reviewer-to-systems-R2-ledger-build-mergegate-CLEAN.md`）**：★★上輪硬追蹤(react_util 4類必competing argmax非if/elif)這輪親自逐行驗證坐實——`_pick_contact_reaction`確認四類各自算util存Dictionary、迴圈找max，真argmax非決策樹；`missing_contact_ledger_test.gd`測試④(spec自標「命門」)用4組單一trait=1.0組合逐一驗證四類都真能被argmax選中(統領→redispatch/野心→writeoff/慎重→defensive/義氣→rescue全過)，非只測預設分支。輕量觀察(非阻塞)：`overdue_ratio`是四類共用乘數，argmax結果數學上約分後等同純比較四trait大小，逾時嚴重度本身不影響選哪一類只影響會不會fire——記給measurer留意per-team util dump多樣性，非結構違規。整併義務親讀diff確認`_evaluate_owner_contact`真的改呼共享`_contact_elapsed_days`、`CONTACT_TIMEOUT_DAYS`門檻原地留著本批不動、owner-leader-changed分支完全沒被誤觸。letter deliver hook只在成功送達resolve、timeout/intercept刻意不清帳(沉默即資訊,母隊逾時ledger自然偵測)符合WHAT設計；subteam merge-back inline resolve hook親讀確認乾淨。零god-view test⑤親驗adversarial(竄改subject.anon_cohorts人口assert elapsed不變)非空殼。CLEAN→merge。資訊網補完批(L3+失聯帳本)兩slice皆綠。

**★★L3循環貿易 build(06c8b452) R②merge-gate判決CLEAN（`2026-08-05-reviewer-to-systems-R2-L3-build-mergegate-CLEAN.md`）**：親讀完整git diff(非只信commit msg摘要)確認三塊實作對HOW spec逐一對應。塊①`_scan_best_market`等新函式(faction_ai_system.gd+87行)公式跟上輪審過的HOW一致，唯一差異=archetype從「乘整體」改「只乘gain項」，commit message誠實交代原因(乘整體會讓TRADE在負util時探索更少=sign-flip bug，implementer自己headless RED抓到並修正，非偷渡scope)。塊②`team_market_last_read`(world_state.gd+4行)+`read_market_board`stamp點(order_system.gd+4行)精準對應到場firsthand讀板才stamp。塊③options.gd一行擴充applicable。★calibration錨真值親驗非空話：`MARKET_ARB_NORM=100.0`親grep`goal_resolver.gd:237`確認`DELIVER_PAYOFF_NORM=100.0`逐字相同非巧合聲稱。測試`l3_circuit_trade_test.gd`完整讀過6案，尤其⑤是真adversarial測試(竄改市集`tile.public_storage`到99999後assert`visit_util`完全不變)，直接把感知鐵律釘成可重跑回歸測試非空殼斷言。新增欄位皆minimal/additive/comment清楚，determinism/constitution structural evidence跟自報數字一致。CLEAN→merge。ledger build待implementer續。

**★★L3循環貿易+失聯帳本 HOW R②×2判決各CLEAN+1輕追蹤（`2026-08-05-reviewer-to-systems-R2-L3-ledger-HOW-CLEAN.md`）**：上輪R①指出的兩個問題這輪皆被HOW設計原文吸收。**(1)L3**：親讀`decision_context.gd:11-12/165-166`確認`has_goods`/`has_arb`既有ctx欄可直接擴充；`has_arb`背後的`best_arbitrage_order`(order_system.gd:302-331)本session已審過只讀`team_known`belief零market live stock，reuse它當`arb_expectation`讓感知鐵律近乎自動繼承。spec的Seam段逐字對應我上輪指出的「naive/settled擋門」兩個精確缺口，方向正確(升`_nearest_market_outpost`+放寬applicable)非新建平行option。輕追蹤=calibration常數(trip weight/staleness_norm)延續同款錨真值紀律。**(2)失聯帳本**：★整併義務這輪親逐行核對到具體行號——`_evaluate_owner_contact`(faction_ai_system.gd:4658-4663)精準對應spec要抽出的`_contact_elapsed_days`共享原語、單一呼叫點(:843)代表重構風險低、`CONTACT_TIMEOUT_DAYS`門檻留原地的範圍切分乾淨(owner-leader-changed分支`:4666-4674`正確排除在外)——非只信文字宣稱。`TeamData.gd`親驗`named_members`/`active_orders`/`subteam_ids`皆Array欄位確認`dispatch_ledger`新增是同款慣例。輕追蹤=要求implementer把四類反應(再派/防禦/救援flag/註銷)做成彼此competing的util候選集(同herald/scout mini-util模式)、非依人格特質高低直接if/elif分支，否則會退化成偽裝util的死常數門檻。CLEAN→回systems→dispatch implementer分片build（`feat/L3-circuit-trade`/`feat/missing-contact-ledger`）→量測人格分化→QA。

**★★L3循環貿易+失聯帳本 R①×2判決（`2026-08-05-reviewer-to-blueprint-R1-L3-and-ledger-verdict.md`）**：資訊網補完批兩份新spec前提factcheck。**(1)L3循環貿易CLEAN+必訂正P2/P3**：親讀`_merchant_trade_target`(faction_ai_system.gd:2563-2573)+`_nearest_market_outpost`(2578-2593)+`貿易`option applicable(options.gd:18`has_goods or has_arb`)發現P2「商人只對已聽聞單反應、無主動去讀板決策」字面上不成立——**既有一條naive的「無arb時巡最近已知市集」fallback路**，抵達會firsthand讀板。真缺口比claim描述的窄：這條既有路(a)永遠揀最近零staleness/util秤(b)零人格分化(c)受`has_goods or has_arb`擋門零貨零arb的settled隊進不去(d)只能去`team_market_known`(vision半徑/relay聽過)範圍內市集永遠發現不了全新未知遠方市集。要求P2/P3改寫成精確版本，HOW方向應是「升級`_nearest_market_outpost`的naive揀法成genuine util秤+人格modulate、reuse既有`貿易`option seam」非「從零蓋新option」，否則走偏建平行機制撞`feedback_no_patch_on_settled_architecture`。**(2)失聯帳本CLEAN+1個R②必查提醒**：P1親驗坐實且證據比claim更豐富——grep到10+個獨立timeout常數(FOUNDING/TRADE/STATION/SCOUT/FLEE/CONTACT/CONSTRUCT_TRANSIT/letter/envoy/scout)全是子單位側自我到期非母隊側帳本，散落程度比spec自己講的「3旋鈕」還誇張。★意外挖到`_evaluate_owner_contact`+`_trigger_defection_evaluation`(faction_ai_system.gd:4651-4664)——方向相反(村發現領主久無音訊,非領主發現遠村久無音訊)但用同一套底層原語(`BeliefSystem.best_estimate().last_tick`+`_DAYS`門檻)的既有「失聯偵測→反應」機制，P1沒提到。要求R②/HOW階段必須明確處理跟這個既有機制的整合關係(折進統一帳本 vs 刻意保留獨立並交代原因)，否則新帳本系統自己會變成第4個散落知識點，正撞上這spec自己想解的3旋鈕病。CLEAN→blueprint鎖spec→systems做HOW→R②。

**★★game_setup faction-key robust(test-infra correctness) HOW R②判決CLEAN+2澄清項（`2026-08-05-reviewer-to-systems-R2-gamesetup-faction-key-CLEAN.md`）**：T3診斷揭game_setup層bug——`world_state.gd:121 create_faction`用`_next_faction_id`(sequential計數器)分配actual faction id，跟config自訂的faction_id完全無關；親讀`game_setup.gd:578`確認現況比claim描述更直白：`state.create_faction(...)`**連回傳值都沒接**(非只是用錯值，是根本沒存)，`:585-586`直接拿原始config faction_id當actual id去查`state.factions.has(fid2)`+`set_team_faction`，精準對應T1誤入T2的faction/T3 factionless症狀。修法=新增`cfg_to_actual`map(leader建立時登記actual回傳值、非leader查表用actual id)，親驗邏輯正確、不動`create_faction`本身(engine)。★澄清①：`:598/607`那段god-view創世seed的同faction discovered邏輯親讀確認是純config值互比(從未碰`state.factions`/`team.faction_id`)，跟`:578/585-586`性質不同，不需要跟著改成actual id版本，要求implementer區分清楚別為了掃描完整度多此一舉甚至手滑引入新bug(非阻塞)。★澄清②：「conforming bed=config faction_id剛好0..N-1」判準不夠精確，真正條件是leader在teams_cfg陣列出現順序跟faction_id遞增順序一致(因_next_faction_id按陣列出現順序非數值排序分配)，這個更精細條件已被spec自己要求的「跑warring/economy/lord_distribution bed驗不破」步驟自然涵蓋，非新增工作只是提醒measurer解讀時留意(非阻塞)。determinism/scope=test-infra皆親驗成立，不涉感知鐵律/決策genuine審查線(faction setup非決策讀值)。CLEAN→回systems→build（續`feat/info-network-whole`）→re-measure症1端到端（★T3也救活對稱、T1仍救活）+warring 2seed→QA故事稽核→arc-done判。

**★★distribute免費直注relief(機制最後一bug) HOW R②判決CLEAN（`2026-08-04-reviewer-to-systems-R2-distribute-free-relief-CLEAN.md`）**：side-dispatch解決dispatch=0後diagnostic#6揭最後一站——convoy 6/6 arrive非黑洞、但settle站5/6 bail(`sell_owner_no_coin`×4/`sell_ownerless`×1)。★這輪逐行親算確認dead-code claim非誇大：`interaction_system.gd:767 oask=maxf(TradeValuation.local_value(owner,res,state)*pf,0.0)`，`pf`來自`price_factor=clampf((0.5+greed)/(0.5+honor),0,CAP)`，分子`(0.5+greed)`在greed>=0時最小0.5永不到0，代表`oask`結構上永不為0→`:855 free_dist=(override_ask==0.0)`這個「免費仁君路」分支確實UNREACHABLE，SLICE B本來設計了免費路但從沒被踩到過的dead code，非設計錯。`:857`(owner無coin bail)/`:865-866`(無價bail)/`:869-872`(affordability cap擠壓qty)三處親讀確認全部用`free_dist`當閘，改`oask=0.0`後三處全繞過。`:881-882`親算確認`bid=0`時coin雙向`±0`no-op守恆非信comment。`:884-886`確認`distribute.deliver`/`food_delivered`既有tap早就佈好、只是從沒被bump過。`:854 owner==null`bail確認在free_dist判斷之前，1/6 edge定位精準。mini-util(發不發賑濟的人格秤)前兩輪已驗證這輪完全不動，這次修法純粹是「送的時候要不要收錢」的settle層修正，非決策層動手腳。CLEAN→回systems→build（續`feat/info-network-whole`）→re-measure症1端到端（`distribute.deliver`5/6→6/6+`food_delivered`顯著>1+糧真到resident runway回升）→QA故事稽核。

**★★distribute side-dispatch(症1雙端side-action對稱完成) HOW R②判決CLEAN（`2026-08-04-reviewer-to-systems-R2-distribute-side-dispatch-CLEAN.md`）**：de-scan解候選生成(candidate_eval 0→680)但dispatch仍0——distribute留主argmax跟T0恆覓食競爭輸，跟herald/scout移出前signature相同。blueprint RATIFY：distribute=side-action第三型(領主下令派賑濟convoy=directive、身體照覓食、同求援對稱)。①主argmax零改動親驗坐實：`goal_resolver.gd:117`跟`:115 _deliver_candidates`同款standalone append，移除不牽動其他行。★親驗上輪(distribute de-scan)要求的`:122-124`舊god-view自辯comment訂正確實被聽進去、真的落地成「舊自辯已被用戶資訊網arc否定」的文字，非提了就散。②mini-util genuine：親比對`_distribute_candidates`util body跟上輪讀過的belief-sourced版本逐字一致，這輪只換觸發路徑(主argmax→side-dispatch)非重新發明。③side-action邊界scope硬限：spec`:7-10`親讀確認「三型明列(herald/scout/distribute)+每新增型需blueprint sign-off」已正式寫入spec、非我方要求後補，blueprint自己意識到creep風險並主動設sign-off關卡。④reuse：`_try_herald_side`(faction_ai_system.gd:1652)確認既有`_try_X_side`模板(throttle/need-gate/mini-util/Probe tap四段結構)給distribute抄非即興；`_dispatch_convoy`(3311)確認存在。CLEAN→回systems→build（續`feat/info-network-whole`）→re-measure症1端到端（distribute.dispatch/food_delivered>0+糧真到resident runway回升＝症1首次閉環）→QA故事稽核。

**★★distribute de-scan(資訊網arc最後一哩) HOW R②判決CLEAN+★1嚴重非阻塞追蹤（`2026-08-04-reviewer-to-systems-R2-distribute-descan-CLEAN.md`）**：herald-carrier修好交付後RE-measure#4揭最後一個root——letter真送達(delivered 8/8)但distribute.dispatch仍0，親讀`goal_resolver.gd:204-211 _resident_food_runway`確認**真god-view live-read**（直讀`resident.population`/`minor_population`/`ResourceSystem.effective_food(state,resident)`三個live動態欄，非belief）+`:169 DISTRIB_DEFICIT_DAYS=4.0`真死常數硬continue閘。★同檔`:122-123`有段舊comment「intra-faction自有居民deficit=合法非god-view」跟這次「正是用戶否定的領主直掃」定性互斥——要求implementer訂正這段殘留錯誤自我辯護。★★這輪最重要發現：親讀`constitution_gate.gd:40 GV_TEAMSTATE_RE`正則只認字面`state.teams[id].field`緊鄰pattern，而這段違規是`:164 var resident=state.teams[rid]`先存local var、隔幾行才讀`resident.population`——完全不會命中正則；親查`constitution_baseline_v2.txt`全檔零相關entry，證實這不是「先前審過標legit現在翻案」，是**gate正則本身有盲點、這段違規活在codebase裡從沒被抓到過**（含本session我自己前幾輪多次直讀這區域也沒抓到）。非阻塞這次fix（fix本身正確拿掉違規），但要求systems記一筆tracking：找時間掃`decision/`+`faction_ai_system.gd`裡「先`=state.teams[...]`存local var、後續讀該var動態欄位」這個常見寫法還有沒有其他殘留。`is_resident_static`(faction_ai_system.gd:503-513)親驗只讀結構性欄位(tags/outpost_owner/outpost_level)零live動態態，applicability換源後仍乾淨。`DISTRIB_RELIEF_NORM`新常數延續同款calibration錨真值紀律(非阻塞)。CLEAN→回systems→build（續`feat/info-network-whole`）→re-measure症1端到端（distribute憑belief+人格fire→糧真到resident runway回升）→QA故事稽核。

**★★herald非team輕carrier(in-transit letter物件)+A③名冊full HOW R②判決CLEAN（`2026-08-04-reviewer-to-systems-R2-herald-carrier-CLEAN.md`）**：這輪是資訊網arc目前最大的結構性改動——herald從「假裝是team」整個改成`state.in_transit_letters`非team物件。★root B親驗到比spec文字更精確嚴重的證據：親讀`faction_ai_system.gd:773-787`確認這是唯一succession偵測點，對任何`team.leader_id==-1`無條件呼`on_leader_death`；親讀`event_system.gd:31-63`確認無named時真的從匿名晉升新named leader——代表兩輪前建的anon-herald(leader_id=-1 phantom-leader設計)只要進full-sim每tick都會被這條路徑升級成真named，完全吃掉當初「信使不需named軍官」的設計意圖，比spec敘述還嚴重。★主動比較過更便宜的替代方案：親讀`faction_ai_system.gd:777-779`發現`beast_kind`單點exemption precedent確實存在(「野獸不進決策迴圈」)，但herald需要免的是succession+cull+subteam-routing+combat-target四處系統非一處，比照beast做法要疊四個豁免點=真補丁閘味，一次性de-team比scatter四處exemption更站得住——這個比較是我自己做的，非單純接受blueprint裁定文字。reuse親驗坐實：`_deposit_help_need`(faction_ai_system.gd:1486)/`market_orders`(Part1既有board)/`_try_herald_side`(1534，行號精準對上)皆真existing。A③名冊full=`_find_own_outpost`同款position-only欄位讀取邏輯只是從「查一個team」廣化成「查全faction找最近」，同款風險profile。anon detach的1pop疑慮排除(這codebase anon是cohort聚合計數非individual PersonData，detach=真sink cost非孤兒record)。CLEAN→回systems→build（續`feat/info-network-whole`）→★re-measure on FACTION bed（症1端到端：letter抵seat deposit→領主聞→distribute fire→糧真到resident）→QA故事稽核。

**★★Part2 (a)side-action(求援/偵察脫離主argmax→平行side-dispatch) HOW R②判決CLEAN+2追蹤項（`2026-08-04-reviewer-to-systems-R2-part2-side-action-CLEAN.md`）**：dispatch fix修好後diagnostic揭新root——求援applicable但每次都輸主argmax(rank3/4，winner=返家補給util0.04-0.21)，distribute下游於herald確認(distress進team_known後distribute util0.659會贏)。blueprint裁(a)：求援≠主任務(村莊邊覓食邊派人求救=用戶核心例，逼進單task argmax互斥=category error，同「信使≠subteam」家族)，此WHAT判斷已由blueprint裁定非這輪重審範圍。HOW=求援/偵察移出REGISTRY主池+新平行`_step6b2_info_dispatch`(faction_ai後、mini-util人格加權cost-benefit>0才spawn anon)。①主argmax零改動親驗坐實：`options.gd:344-370`求援/偵察兩entry自成一體dict，移除不牽動其他entry計算，determinism-neutral結構上站得住。②mini-util genuine：`help_need_severity`沿用既有真值component，人格_pmult modulate非boost；`RELIEF_EXPECT`/`ANON_COST`兩新常數延續idle-labor/mfg-hub/bootstrap系列同款calibration錨真值紀律追蹤(非阻塞)。③scope硬限親驗支持：spec文字硬寫herald/scout兩具名分支非泛化plugin機制，要求build時implementer真寫死兩支非做成「任何option可掛進來跑」的通用side-task框架(非阻塞、build階段紅線)。throttle機制`task_reason=="help_call"/"info_scout"`親grep對上既有code(faction_ai_system.gd:1497/1847/1851)+既有`:531`「避免重派spam」in-flight guard前例支持可build。⑤感知鐵律/determinism沿用前兩輪(bootstrap/dispatch fix)已驗證的anon carrier零特權機制，零新RNG。CLEAN→build（續`feat/info-network-whole`）→re-measure whole（herald_dispatched>0+distribute連動+人格分化+主argmax determinism+Part1/3不退）→QA故事稽核。

**★★Part2 dispatch fix(①applicable gate於spawn-ability+②求援herald reframe=anon 1人messenger) HOW R②判決CLEAN+1輕追蹤（`2026-08-04-reviewer-to-systems-R2-part2-dispatch-anon-herald-CLEAN.md`）**：bootstrap fix修好applicable後re-measure揭露新死結——dispatch=0（`faction_ai_system.gd:1445-1448 _dispatch_help_herald`要`_pick_subteam_leader`挑到spare named才派得出，小餓resident稀有named→永遠送不出）+seed1337 regression（unexecutable-but-applicable option進rank擾動軌跡）。blueprint裁GO：①gate於spawn-ability(look-before-leap同`has_buyable_food`前例，親讀`decision_context.gd:331-335`/`options.gd:246-253`確認前例真存在非發明類比)②求援herald reframe=anon 1人messenger(信使≠subteam，村莊派個人求救=用戶核心例)。★這輪重點驗證②非平行求解器：親讀`subteam_system.gd:3-65 dispatch()`確認第10-13行結構性要求named sub_leader(找不到回-1)——精準對應root，代表anon路線不可能沿用這函式必須新寫，spec自己誠實寫「新spawn路或adapt」非隱藏。但新程式碼裡沒有發明新資源機制：`leader_id=-1`是既有phantom-leader pattern(`subteam_system.gd:153/200`comment講清population getter已容)、pop真扣走既有`AnonTierSystem.transfer_proportional`(dispatch()自己內部line59也用同函式)、`_evaluate_subteam`的`help_call`專屬branch已存在(1852,非新開)——「新程式碼但零新記帳機制」結論站得住。輕追蹤(非阻塞)：若`_spawn_anon_herald`照抄dispatch()的proportional資源split，1人信使會順便搬走母隊一小份全資源非只pop，spec未明講要不要跳過，建議implementer量測時順手看有無異常資源流失。CLEAN→回systems→build（續`feat/info-network-whole`）→re-measure whole（`help.herald_dispatched>0`+`distribute.dispatch/food_delivered>0`+seed1337 regression消+scout spare named假設驗）→QA故事稽核。

**★★bootstrap fix(名冊fallback治herald/scout 0 fire) HOW R②判決CLEAN+必訂正④（`2026-08-04-reviewer-to-systems-R2-bootstrap-fix-CLEAN-correction.md`）**：資訊網whole已build4片(worktree`info-network-whole`commit ac7d3975)後measurer驗出Part2(求援/偵察)0 fire=新bootstrap死結(`decision_context.gd:348/363`help/scout_target_pos卡`BeliefSystem.best_estimate`live-belief，faction成員從不meet→無belief位→永不applicable)。修法=名冊fallback(`_faction_roster_pos`：fresh belief優先、無→reuse既有`_find_own_outpost`查自家勢力固定據點位)。①②③⑤親驗坐實：util公式(`decision_context.gd:341/354`severity/staleness×人格)一字未改、`_faction_roster_pos`只回`tile_pos`零live-state讀、reuse`_find_own_outpost`(已在`constitution_baseline_v2.txt:72`標legit零新gv_mapscan fingerprint)非平行求解器。★④不成立需訂正：親讀用戶ratified原文(`2026-08-04-blueprint-to-systems-bootstrap-ratified-GO.md:19`)「分裂=名冊凍成belief快照帶走(對方後續新建/棄置不知、會過時)」=帶走一份會過時的快照；MVP的faction-gate讀當下`faction_id`=分裂瞬間直接零資訊(連快照都沒建立過)非「有快照但過時」，兩者機制不同，spec自稱「達④outcome」是自我認證過頭。★但non-blocking：Part2消費者從頭到尾只鎖同勢力，ex-faction位置這條路徑本來就不會被讀，訂正只是措辭誠實度非重新設計，不需要多等一輪R②。CLEAN+要求spec§4④改如實描述(「達成不live-track半個outcome，frozen-snapshot半未做、標tracking」)→回systems→build（續`feat/info-network-whole`）→re-measure whole（canonical harness）→QA故事稽核。

**★★資訊網whole HOW R②判決CLEAN+2追蹤項（`2026-08-04-reviewer-to-systems-R2-infonet-whole-CLEAN.md`）**：承R①CLEAN鎖spec後的whole-build HOW審，針對systems自己標最需戳的①god-view②reuse兩點親驗。★①親讀`read_market_board`(order_system.gd:194-219)確認運作對象是單一tile的`market_orders`陣列非跨市集索引——Part1擴充(訪客deposit team_known副本+累積+輻射)只改變同一結構「寫入」的內容，讀取範圍依然是單tile，機制層面god-view乾淨，`constitution_gate`detector不會跳。但親自指出一個design text驗證不到的風險並要求追蹤：熱門市集若被高頻造訪，relay機制可能讓資訊擴散速度/廣度功能上逼近near-global-awareness即使結構未違憲(hub效應)——要求§5驗收額外量「資訊實際擴散速度/廣度分布」非只看detector有沒跳，此要求已寫入spec §5(`2026-08-04-information-network-whole-HOW.md:75`)。★④親讀`_dispatch_envoy`(faction_ai_system.gd:1323-1348)確認`(mother,target_id,ptype)`簽名本身是通用「派信使做什麼」參數化設計、目標讀`BeliefSystem.best_estimate`(belief-based非god-view)、走既有SubteamSystem+冗餘機制——這次求援用途是自然參數化延伸非新平行系統；親grep`TASK_SCOUT`既有task_reason區分機制確認同理可reuse。reuse claim確認為真。genuine非crank要求比照idle-labor/mfg-hub同款紀律(herald_cost/scout_cost/info_value scale須錨真值非反推能fire的常數)。Part3/4範圍拿捏誠實(reuse既有reserve非預先over-build、famine-flee邏輯鏈跟R①親追過的機制鏈吻合)。CLEAN→回systems dispatch implementer分片build(S-prop/S-herald/S-scout/S-trade)→整合whole一次量。

**★★資訊網核心(通例全面網+有意收集決策+交易面fold) WHAT R①判決CLEAN（`2026-08-04-reviewer-to-blueprint-R1-infonet-core-verdict.md`）**：承§5執行塌陷三層root的用戶已confirm whole-build一次量scope，最高風險的地方是「一root三症」詮釋(distribute敗/relocate敗/commerce-info敗)是否成立——若不成立整個whole-build的地基塌。★親驗到機制層非表面類比：`faction_ai_system.gd:3908-3933 _find_food_seek_target`源②明確呼叫`received_sell_orders`，跟distribute-zero(D2)問題裡`received_buy_orders`是同一家族函式、讀同一個`state.team_known`底層結構、被同一個`propagate_on_arrival`(message_system.gd:79 `if tile_pos!=tile_pos:continue` co-location閘)餓死——這是我自己往下追一層挖到的file:line證據，非停在診斷JSON citation表面。且D2(distribute-zero)這個症狀我在SLICE B分配政策那兩輪R②已經徹底審過util/argmax/candidate生成全部正常(rank0贏util1.33)，唯一binding是received_buy_orders不達——這個「非決策層問題純資訊層問題」的結論是我自己更早、獨立於這次audit已經確認過的，兩條線收斂互相佐證非新猜測。P1(co-location閘)/P3(read_market_board物理抵達限定)/P5(interaction_system窄交易面only owner public_storage)皆親讀坐實；P2(「只缺拓撲非缺decay」)親算`:103`衰減公式`strength×(1-HOP_DECAY)×time_factor`本身就是multi-hop-ready設計，佐證只是傳播拓撲太窄(唯co-location觸發)非decay機制本身有問題。P4(求援/派信使決策不存在)跟本session反覆讀過的options.gd全貌一致。CLEAN→鎖spec→dispatch systems whole build。

**★L1 intra-faction distribute de-patch HOW R②CLEAN（`2026-08-03-reviewer-to-systems-R2-L1-distribute-depatch-CLEAN.md`）**：§5執行塌陷root三層之L1，measure坐實(領主food=3940但居民餓死distribute.dispatch=0，因`_distribute_candidates:154`讀`received_buy_orders`=team_known co-location閘，領主未跟居民共位就永遠不知道自家人在餓)。★親讀`goal_resolver.gd`現況確認②cross-faction隔離是結構性保證非承諾：`_distribute_candidates:154`跟`_deliver_candidates:232`都呼`received_buy_orders`但完全是獨立function body(:126起/:220起)，新helper`_intra_faction_food_buy_orders`只給前者呼叫，後者一行都沒被碰——架構上不可能漏，非小心避開。①util formula(relief+coin，我在SLICE B兩輪已驗證過)完全在改動範圍外，只換取得buy_orders的感知來源，跟genuine/crank無關。`is_resident_static`(faction_ai_system.gd:503)親驗為既有真function非新造，「同勢力成員身份=行政記帳非需belief」跟我在SLICE B分配政策最初那輪核可的推理一致，非新先例。CLEAN→回systems dispatch implementer build。

**★★製造樞紐湧現 HOW R②CLEAN+2追蹤項（`2026-08-03-reviewer-to-systems-R2-manufacturing-hub-CLEAN.md`）**：systems自己點名①(goods消耗當出口需求源)「最需你戳」，花最多力氣想清楚跟乙crank的性質差異：乙是直接調一個已存在util乘數(除了讓分數變大沒有獨立經濟意義)；這次是在世界模型裡補一個真實消耗事實(人口用掉製造品)，且親讀`need_oracle.gd:109-110`確認goods現況`_self_use=0`的comment原文「無自用消費sink」讀起來像早期簡化非設計終局——結構上是填補模型缺口非為樞紐特別發明消費者，跟乙性質不同。★但要求①`GOODS_UPKEEP_RATE`校準要錨定跟「樞紐要不要emerge」無關的真實參照(類比既有FOOD_PER_PERSON_PER_DAY量級)，非反推「調到剛好長出來」——結構genuine不代表數字自動誠實，同idle-labor那輪PER_HAND_OUTPUT的要求邏輯。②要求emergence量測看「跨隊異質性」(有的隊選hub有的不選)非「加了需求就有反應」這種supply-demand恆等式當證據——後者哪怕拿掉決策引擎硬寫if-else都會發生，不能當湧現證明，要求明確寫進§4驗收敘述。B/C(import convoy鏡射_dispatch_convoy+de-local靠convoy物理橋接)復用已驗證機制確認乾淨，unify非patch/感知鐵律/need-gated皆核到位。CLEAN→dispatch隔離branch(可分A→B→C)。

**★製造樞紐湧現(單大隊第二種生產強權)WHAT R①判決CLEAN（`2026-08-03-reviewer-to-blueprint-R1-manufacturing-hub-verdict.md`）**：承§8三判定(單大隊採掘非強權)的後續大框——補齊決策輸入讓製造/貿易樞紐emergent非script。P1親讀`manufacturing_system.gd:146`確認`target=need_keep+demand`、`need_oracle.gd:109-110`確認goods的`_self_use=0`+非PURE_INTERMEDIATE+非CONSTRUCTION_COST_RES三者相加need_keep=0=100%需求驅動，詮釋成立非誇大。P3親讀`need_oracle.gd:96,153`確認`demand()`真的迭代`team_known`過濾`order_buy`類型belief訊息聚合，感知鐵律合規。P5親讀`interaction_system.gd:860 _credit_owner_coin`確認利潤真入team coin守恆。P2/P4沿用本session已深度驗證過的`_deliver_candidates`/`_dispatch_convoy`/`_tick_convoy`/`_resolve_resource_prereq`/`_supply_chain`既有認識。三缺口皆親驗真缺非漏看：(A)`order_system.gd:108-115`確認買賣單量級綁死在單隊自己的surplus/reserve落差，無大宗聚落級需求源機制(B)`_dispatch_convoy`的cargo來源固定是home vault撥出，只有「送出去」沒有「去外面取回來」的反向變體(3)`_market_visitor_buy`撮合對象是訪客當下站的那個tile的TileBank，天生物理同格限定，進料出貨撞同一道牆。P1-P5+三缺口全CLEAN，「機器已存在樞紐該emergent」地基站得住。

**★manufacturing per-labor-allocation de-patch HOW R②CLEAN（`2026-08-03-reviewer-to-systems-R2-mfg-depatch-CLEAN.md`）**：measurer實測揪出領導軸真根——親讀`manufacturing_system.gd:67`確認`current_task!=TASK_MANUFACTURE`這個補丁閘擋在已整合勞力池的完整生產邏輯(:82-96)之前，任何隊current_task剛好不是"製造"就整段跳過，精準對應「飽和度6.7%+材料消耗0.000全程」的測量結果——facility蓋出來後除非隊剛好被決策engine選中執行製造task否則永不運轉。教科書式補丁閘優先查案例([[feedback-patch-gate-first]])，de-patch(移除這一行)是對的手術非疊補丁。gather對稱claim憑本session稍早直接讀過`collect_resources`零task-gate的記憶確認為真。need-gated(fill=0→worker_rate=0)/materials/position/軍隊排除等既有gate皆不受影響繼續生效。grep其餘9處TASK_MANUFACTURE引用皆是「這task出現在清單裡時如何處理」的優雅降級用法，非硬依賴，vestigial follow-up正確拆分成獨立項非混進這次minimal de-patch。CLEAN→dispatch隔離branch。

**★idle-labor→建設 融合驗(merge前)CLEAN（`2026-08-03-reviewer-to-systems-R2-idle-labor-build-merge-CLEAN.md`）**：親讀`_idle_employ_value`完整實作確認我上輪追蹤項(PER_HAND_OUTPUT反推)徹底解決——比要求的更乾淨，根本沒引入這個常數，`full_output`直接用`level×LABOR_SCALE×(0.5+avg_skill×0.5)×rate`複用manufacturing真worker_rate公式形狀，`K_MFG`/`LABOR_SCALE`/`RATES`皆既有真常數、`need_weight`真need_oracle呼叫，零發明數字。guardrail雙層確認(`if opt!="建設"`+weight中性1.0無人格crank)。★stale-base疑慮親驗排除非信note：`terms.gd`diff起點index hash`11a6c76c`跟我上輪親驗乙-revert後的exact乾淨狀態一致，親自對過非信任帶過。附帶肯定implementer主動抓到+自修perf regression(遞迴NeedOracle呼叫掛cache)的measure-first紀律。CLEAN→merge。measurer §8 re-measure領導軸+§7.1b小隊下滑追蹤項皆待最終數字。

**★idle-labor→建設 genuine激勵(治§8領導軸) HOW R②CLEAN（`2026-08-03-reviewer-to-systems-R2-idle-labor-build-CLEAN.md`）**：親讀`options.gd:40-46`確認建設`applicable`恆true(零ctx依賴)+`terms.gd:193`(`settle_fit`對建設回傳固定常數0.4)+`terms.gd:116`(`ambition_drive`只吃ambition_gap)——確認建設util目前完全labor-blind，premise精準。`idle_employ_value=min(idle_labor,D_NEW_WORKSTATION)×PER_HAND_OUTPUT×need_weight`結構上非crank：三因子皆有真grounding(idle_labor複用已驗證的labor_system.gd/D_NEW_WORKSTATION沿用勞力池常數/need_weight走真need_oracle零需求則term=0)，跟乙那次「純粹讓util變大的任意倍率」性質不同，self-limit(閒勞力隨設施吸收遞減)避免無限誘因。★追蹤項：`PER_HAND_OUTPUT`這個新常數若獨立發明(非從manufacturing真實worker_rate公式反推)，結構genuine的外殼下仍可能藏一個縮小版crank——要求implementer訂值時附上校準推導非憑空取數。§4 spread/militarize gap-deferral範圍切分誠實合理，MVP建設-only非偷懶迴避。CLEAN→dispatch隔離branch，§8 re-measure領導軸追平才宣稱。

**★統一勞力池 融合驗(merge前)CLEAN（`2026-08-03-reviewer-to-systems-R2-labor-pool-merge-CLEAN.md`）**：親讀`labor_system.gd`全文111行新檔+多個commit實際diff確認8審點在真code成立(非只信commit msg彙總)。①統一非平行：`rebalance()`同一函式同一份字典處理採集+製造workstation。②deterministic：cascade迭代邏輯親讀確認正確(8輪water-filling+sorted key+零RNG)。③承載獨立/④baseline保真/⑤多隊防雙算：與上輪HOW審核一致，code落地無走樣。★⑧need-gate契約——這輪投入最多心力驗證的風險點：親讀`git show 61b2a354`確認headless 10測更新是真修非偷改斷言掩蓋regression——`_fief_make_tax_state`真的加TAG_PRODUCE+建構一個genuine belief buy-order(`origin_team=9`非自己、`expire_tick`未過期)塞進`team_known`滿足新need-gate，完全遵守感知鐵律；供給鏈多級測試fixture(`weaponsmith_level`+`smelter_level`串聯、holding=0缺口全開)真的會抓到`ore_steel`/`ore_iron`(皆PURE_INTERMEDIATE)傳導斷鏈，非表面測試。CLEAN→merge。measurer §8階段記得帶上輪追蹤項(小隊多活動下滑survivable)一起量。

**★統一勞力池HOW R②CLEAN（`2026-08-03-reviewer-to-systems-R2-labor-pool-CLEAN.md`）**：不升R①(premise是我上輪R①已驗過的同組file:line含P5訂正，這輪是執行層演算法設計非新大框斷言)。★P5訂正正確：§6「tile生態承載獨立不碰」+「人均遞減=demand-cap+current庫存雙機制」，採集sqrt併入新池、真承載繼續由current/COLLECT_RATE/regen把關，符合我上輪要求方向。§2b多隊防雙算親算驗證：2隊共址share=labor_mult×(team_pop/pool)各自比例相加=labor_mult×1.0(單一fill值一次非重複雙算)，單隊時比例=1退化自然銜接。收斂邏輯(water-filling比例分配+8迭代上限+sorted key)站得住，工位數量規模下綽綽有餘收斂。★追蹤項(非blocking)：baseline-preserving±5%只覆蓋pop=5單隊單工位情境，但小隊同時採集+經營1-2設施是常見非edge case情況，這種情況下勞力會被真實分割、各活動產出必然低於舊制(這是redesign應有效果非bug)——要求measurer量測階段額外測「典型小隊總產出相對舊baseline的下滑幅度」是否落在遊戲設計可接受範圍，非意外造成早期經濟全面崩潰式難度暴增。CLEAN→dispatch隔離branch。

**★★統一勞力池(size在生產matter)WHAT R①判決（`2026-08-03-reviewer-to-blueprint-R1-unified-labor-pool-verdict.md`）**：blueprint直接dispatch的R①(承2026-08-02 CASE-B規模經濟未模型化定案)。P1(`manufacturing_system:82`pop_mult在RECIPE_GROUPS迴圈外算一次、迴圈內每設施共用同一值零競爭)/P2(`resource_system:63`獨立函式各自算、零交叉引用)/P3(`outpost_system:166 _has_resident_on_tile`純布林閘、勞力量級=team.population非據點stat)三項親讀file:line坐實。P4(兩套pop_mult互不搶)邏輯上站得住。★P5(sqrt=tile覓食承載上限、神聖不准動)不成立——親驗三理由：①manufacturing用逐字相同公式但明顯不涉tile生態，兩處都無comment佐證「承載」定性，這是spec自己詮釋非code既有語意②親讀`_collect_from_tile:254-284`確認tile真正生態承載是`current`庫存×`COLLECT_RATE`×regen這套獨立機制，`pop_mult`只是疊乘在結果上的額外係數，跟tile能提供多少完全是兩個維度③spec自己§守憲提的`min(勞力率,tile承載)`是min-cap結構，但現有sqrt是乘法係數，數學結構對不上，就算要保留承載概念也需要重新設計非「維持現狀」。且spec§設計本身已寫「所有生產活動吃同一池：採集+製造」，跟§守憲把採集sqrt捧成例外自相矛盾。判決：P1-P4CLEAN、P5觸發blueprint自己預先設下的「守憲條款要改」，非halt整個大框，要求二選一訂正(採集sqrt一起併入新池 vs 明講這是待設計的新min-cap非既有機制)。

**★★乙整併util boost完整REVERT CLEAN（`2026-08-02-reviewer-to-systems-R2-yi-revert-CLEAN.md`）**：親讀`git show b65a9692:terms.gd`實際檔案內容(非只看diff/信commit msg)確認absorb_drive/join_drive逐字回到我自己boost開工前最早直接讀取的原始公式，4個crank常數(`ABSORB_DRIVE_BASE_V2`/`AMB_GAIN`/`JOIN_PROTECT_GAIN`/`JOIN_DRIVE_CAP`)真刪除非隱藏，genuine核心(`resource_slack`/`yield_pos`)行級比對三個時間點(boost前/boost後/revert後)文字一致未受牽連。★重要反思記錄：這次revert是**用戶直接戳破**(commit comment「用戶戳破+blueprint令」)才發現的——原本「決策層util-starvation」框架本身有問題：0.104低util不是公式壞掉，是engine在**沒有規模經濟機制**下如實算出「吸收小隊沒實質收益」，是誠實的低分非錯誤的低分；真root=規模經濟未模型化(genuine finding)非tuning問題。我前兩輪審查驗證了「公式跟citation對得上」「改動是鬆綁常數非疊補丁」，但沒有往上一層問「這個低util是不是其實算對了」——這是我自己審查深度的盲點，非systems流程漏了什麼，這次revert我認、無異議。CLEAN→merge回誠實genuine baseline。日後規模若真該matter，走genuine建模路徑(WHAT層定案)非調係數硬讓option fire。

**★乙整併util boost 融合驗(merge前)CLEAN（`2026-08-01-reviewer-to-systems-R2-yi-merge-CLEAN.md`）**：親讀`git show 21119a35`完整`terms.gd`diff確認我上輪追蹤項(join_drive新protection項連續性)親驗解決非信自報——`protection=JOIN_PROTECT_GAIN×best_protector_rep×survival×low_amb`純連續乘，除既有option-type過濾外零新if判斷。absorb_drive改動只動係數(`BASE_V2`/`AMB_GAIN`)無新分支，四約束真code確認成立。★flag(dispatch10→merge1)可接受(決策層starvation已治好0→10，完成層是既有JOIN 33→1同款belief-stale問題非新引入)，但要求§5額外切分：蒸發率是純繼承既有movement問題、還是新野心導向targeting(選中更遠目標)讓蒸發率惡化，兩種情況處理優先序不同，非籠統歸咎帶過。CLEAN→merge+跑merge-result consol_boost_test驗。

**★乙整併util boost(de-patch util-starvation) HOW R②CLEAN（`2026-08-01-reviewer-to-systems-R2-yi-consolidation-util-boost-CLEAN.md`）**：親讀`terms.gd:59`(`ABSORB_DRIVE_BASE=1.0`,comment「T3正規化1.2→1.0」)+`:224-230`(absorb_drive完整公式)+`:129-134`(join_drive quality-only term)確認§0b餓死公式坐實精準非猜測——三個≤1因子連乘(野心`ambition_gap`先被×0.3閹割再塞`0.5+0.5×`band)量級跟觀測到的ownutil0.104吻合非巧合。A absorb的de-patch(拿掉×0.3閹割、base保守抬)是放寬既有過度正規化死常數非疊新補丁，方向對。★B join新增protection urgency方向合理但exact公式未定(保守起步靠§5合量tune，這codebase一貫做法)——這輪沒辦法grep驗證這條的連續性(約束②)，要求implementer落地後專門複驗這條公式是連續乘除非階梯if-gate；dev-verify自己的「掃ambition連續性」硬斷也會攔到，非漏測。CLEAN→dispatch隔離branch。

**★SLICE B領主分配 融合驗(merge前)CLEAN（`2026-08-01-reviewer-to-systems-R2-sliceB-merge-CLEAN.md`）**：親讀`git show de33885f`完整diff(非只信commit msg)確認4約束在真code成立——①候選無`if kind==distribute`特判繞argmax(併入同一`out`陣列，kind檢查只在DELIVER執行時routing非決策層)②util/price_factor全連續乘除無`if greed>X`階梯③`override_ask=local_value×price_factor`modulation現成函式零新定價class④DELIVER復用`_market_visitor_sell`+`TradeValuation`零新market/order class。guard放寬邏輯(`free_dist=(override_ask==0.0)`精準scoping、付費端affordability cap原樣保留、`override_ask=-1`既有路徑零改動)跟上輪訂正承諾逐行對上非空頭支票。★flag(distribute.dispatch=0 in warring因領主普遍窮到過不了前濾)措辭誠實——明講mechanism綠(bed裡真fire util 0.61/0.75)但organic未觀測+講清原因+point到§5合量查，非「拿別處證據頂替這裡觀測」的舊模式，認可不需加碼。CLEAN→merge+跑merge-result lord_distribution_bed驗。

**★SLICE B override_ask訂正確認CLEAN（`2026-08-01-reviewer-to-systems-R2-sliceB-correction-confirmed.md`）**：systems訂正精準命中我上輪抓的洞——`override_ask:float=-1`sentinel(normal trade零變動)+guard放寬僅scoped在`override_ask==0`(跳819 owner_coin<=0/828 bid<=0/829除零，qty改min(order_rem,sellable))+付費端(`>0`)affordability cap維持不動(苛捐雜稅情境不受影響，範圍拿捏節制)。★親自推一層why：這情境owner=居民(買單掛在自己tile)、visitor=porter，放寬owner_coin<=0語意也對非只是技術補丁——越窮的居民越該是免費賑濟對象，若被這guard擋掉等於免費機制照顧不到真正缺錢的人，違背仁君救濟設計初衷。premise§0b#4親讀`order_system.gd:128-136`確認PROVEN(`effective_food`低於`food_security_target`自動`post_order(buy,food,need)`)，distribute全騎現成buy-order pipeline的收斂claim為真。無異議，implementer隔離branch繼續動工不用停。

**★★SLICE B統一光譜(終稿) R②CLEAN+1必要訂正（`2026-08-01-reviewer-to-systems-R2-sliceB-spectrum-issues.md`）**：blueprint裁B-only擴成統一光譜(給免費/賣居民人格定價/賣外)。§0b三premise親grep全PROVEN(interaction:806-807/838-839 coin雙向轉移、faction_ai:2521 member_tax真實、intra-faction只擋self-trade:736)。①②④約束文字設計一致。★③抓到具體漏洞：親讀`interaction_system.gd:815-828`確認`_market_visitor_sell`的`bid=TradeValuation.local_value(...)`是函式內部算，沒有外部price注入參數(跟有人格markdown的`ask_price`是不同函式，只用在買方路徑)——828行既有guard`if bid<=0.0:bail`現在語意是「沒行情=不成交」的合理安全閥，但spec的「仁君免費」headline情境(price_factor=0→ask=0)若照字面塞進現有bid，會被這個guard當成「沒行情」靜默bail掉(bail只bump probe key不報錯，容易被誤判成別的bug)。spec斷言「現成路能處理ask=0」親驗不成立。要求二選一講清楚：①比照deliver_cargo模式加`price_override`參數且明確排除該路徑的bid<=0 bail②distribute分支price_factor==0時完全繞過`_market_visitor_sell`改inline免費轉移。CLEAN+1必要訂正，單點技術細節補完不需整輪R②重來。

**★SLICE B分配政策(領主分配) HOW R②CLEAN（`2026-08-01-reviewer-to-systems-R2-sliceB-distribution-CLEAN.md`）**：複用已深度驗證機制(convoy FETCH/RETURN、deliver_cargo bypass-reserve、GOAL_UTIL_CAP家族、unrest sink)，直接自驗。★親算`HONOR_WEIGHT=(0.3+honor)/(0.3+greed)`範圍0.23~4.33(honor/greed∈[0,1]獨立值)——確認這次util公式不重蹈economy-headroom那輪「payoff結構性到不了cap，cap形同虛設」的坑，這個乘數真的能推pre-clamp值遠超1.5，義氣領主的distribute候選真能贏過同樣受cap管的deliver-candidate，非又一次無效lever斷言。DELIVER新終點分支(`_resolve_market_at_outpost`加`kind=="distribute"`)乾淨獨立於既有market_visitor_sell路徑，守恆同構已驗證的market-delivery模式。WEIGH非GATE(§1連續util競爭無硬類別閘)/感知鐵律(自有faction member記帳非god-view)/約束1(同convoy脊椎只DELIVER新增終點分支非平行搬運路，`_load_convoy_cargo`現址faction_ai:3002跟spec citation:2964小幅行號漂移非實質問題)/tap覆蓋皆核到位。剝削A/B fork(§6)是WHAT層定價子部分岔，不影響這輪seam本體CLEAN判決。CLEAN→待blueprint A/B confirm後dispatch implementer。

**★SLICE A flow-fix：convoy協調散未填單 R②CLEAN+1必要訂正（`2026-07-31-reviewer-to-systems-R2-sliceA-coordinate-spread-issues.md`）**：spec把「live-scan active convoy子隊」跟「state級registry」當同等選項並列，親查`_on_team_extinct`(faction_ai:2524-2547)確認這是任何team死亡(含porter戰鬥死/餓死)的泛用路徑，完全沒有convoy專屬清理hook——若選state-registry，porter半路死亡不會經過DELIVER/RETURN/dissolve任何清理點，registry認領永久變幽靈、未來convoy誤判單已填滿而跳過，等於復發這輪要解決的「未填單沒人去」同型問題。live-scan結構性免疫(死隊自動從state.teams消失，下次掃描自動不算，無需額外清理code、不可能漏清)。要求鎖定live-scan，state-registry選項刪除或強制要求接進`_on_team_extinct`覆蓋所有死亡路徑非只三點。effective_rem計算/散選util-weighted非round-robin/measure-verify紀律皆核對到位不用重設計。CLEAN+1必要訂正→回systems，訂正後可直接進implementer不需整輪R②重來。

**★SLICE A refine：DELIVER賣full cargo繞porter reserve R②CLEAN（`2026-07-31-reviewer-to-systems-R2-sliceA-refine-full-cargo-CLEAN.md`）**：窄範圍機械修正，親讀現況code不需召異質。★上輪追蹤項(convoy.return和平床telemetry=0)已由measurer獨立確認解決(commit 74c97cb7:「convoy.return修0→5/0→33物理round-trip正常非zombie」)，我要求的「別讓別處證明頂替這裡觀測」這次真的被落實補證，非靜默滑過。這輪根因親讀`interaction_system.gd:812-814`確認精準(porter把待交付cargo當自己reserve扣，扣完sell_no_surplus bail)。fix安全性三點驗證：①`grep`確認`_market_visitor_sell`全專案唯一call site(:758)，新增optional參數`deliver_cargo=-1.0`零遺漏風險②normal sell零影響是結構性保證(非convoy訪客天生無`convoy_phase`key自動落-1路)非靠小心③conservation airtight——親讀:817`qty=min(order_rem,surplus,ocoin/bid)`確認即使繞reserve放寬sellable上限，真正成交量仍被買方order/coin兩個獨立上限夾住，:822/:825deposit跟spend都用capped後的qty非原始deliver_cargo，賣不完隨RETURN帶回母隊。CLEAN→implementer，送達率measured真升要求已在spec TDD內建不用加碼。

**★後勤SLICE A convoy實作R②CLEAN（`2026-07-31-reviewer-to-systems-R2-logistics-sliceA-convoy-impl-CLEAN.md`）**：親讀`git show 4754214e`確認lifecycle interop完全落地——`TASK_CONVOY`分支插在`TASK_SETTLE`分支之前+離generic fallback更遠，結構保證攔截順序對，上輪ISSUES②③④精準驗收。RETURN機制/DELIVER/cargo守恆(exact-load conserving)皆親驗坐實。★1追蹤項(非blocking)：`convoy.return`和平床telemetry=0，implementer說「功能已證(merge/無zombie/warring59)」是間接證據非這輪run真觀測到——親算DELIVER完成同tick即轉RETURN(對稱回程)，若4次dispatch集中在6個月視窗後段，回程真的可能還沒走完run就結束，這是合理解釋但非驗證過的事實，跟本session「別讓別處證明頂替這裡觀測」的一貫要求一致，要求measurer拉dispatch-tick時間戳或延長run天數二選一補證，非照單全收「已證」。CLEAN→merge，GATE-B撮合第一次真的活起來。

**★後勤SLICE A供給-delivery convoy(GATE-B撮合物理送貨) R②CLEAN（`2026-07-31-reviewer-to-systems-R2-logistics-sliceA-supply-delivery-CLEAN.md`）**：measure-first定案(賣方dump釘死GATE-B真gap=option不存在非argmax輸)，窄範圍新增(deliver option)+既有修正(convoy②③④)，risk surface已被前兩輪深掃過，直接自驗不需再召異質。②③④convoy生命週期精準對應我上輪ISSUES——`faction_ai:1719-1760`/`:1737`/`:1753-1755`引用跟我上輪親讀行號完全一致，非敷衍改字；④撤persist-hold改純靠③專屬分支，採納我上輪建議方案。DELIVER→買方連結親驗坐實：`interaction_system.gd:781``_market_visitor_buy`讀`TileBank.get_stored`，跟DELIVER的`TileBank.deposit`是同一底層storage，連結是真的非空談。(A)deliver option紀律做對：spec自己把「dump per-option util驗真fire」列硬性TDD，非上兩輪失敗模式(斷言util夠高沒驗)重演。1追蹤項(非blocking)：payoff正規化公式沒給實數，implementer交付時須附真dump驗證量級合理，spec自己TDD已涵蓋只是點名別漏。CLEAN→implementer。

**★★economy-decision headroom meta-fix R②異質ISSUES，make-or-break從嚴（`2026-07-31-reviewer-to-systems-R2-economy-headroom-metafix-issues.md`）**：本session最高風險審查(觸must-fix①護欄)。★★好消息：護欄不破——親算food_days=1.9(boost幾乎退底最危險點)`dev_coeff=1.9/3.0≈0.633`真正守住這條線非cap，`SAFE_FOOD_DAYS`取1.0或2.0算出來goal util同一個數字(0.95)——cap改法/SAFE_FOOD_DAYS怎麼定都不影響護欄安全，我原本最擔心的中度飢餓區間cap搶跑疑慮親算後不成立。★★壞消息：①核心lever「食物scaled cap headroom」親驗是no-op——`goal_registry.gd:40-53`payoff全部1.0或1.5，util=payoff×3個≤1乘數，結構上永遠≤1.5，既有1.5cap本來就沒真的擋到任何候選，拉到3.0不改變任何結果。spec自標「核心lever」實際不起作用，必須重設計(拉高payoff+重驗新payoff下護欄，或改找真正卡economy的因素=distance discount)。②「一根解全家」過譽：`goal_resolver.gd:181-182`owner同格construction直接defer給infra path不產生goal candidate，完全繞過這條util路徑，這輪fix碰不到同格建設。③1b/1c沒有own-supply候選可掛(convoy還沒落地，我上輪ISSUES擋下)，應延後到convoy HOW訂正後才做，現在寫會是空談或誤套市場買候選。ISSUES→回systems，護欄安全免修，①②③需處理才能dispatch implementer。

**★★後勤SLICE A convoy transport HOW R②異質ISSUES（`2026-07-31-reviewer-to-systems-R2-logistics-slice-A-convoy-issues.md`）**：本session最嚴重一次「決策生成≠真執行」風險，異質+我雙線親驗坐實非猜測。★★①第一驗收核心假設不成立：spec斷言pull-convoy util該高過覓食，親讀`goal_resolver.gd`逐行核對——`_candidate_util`(:354-360)=`payoff×dev_coeff×discount`，`GOAL_UTIL_CAP=1.5`明文<`SURVIVAL_BOOST_MAX(2.5)`(:16)結構性輸survival、`discount`含`hex_dist`(:333-337)距離越遠util越低(pull-convoy賣點=遠方surplus，恰好拉低自己util)、`dev_coeff`絕境趨0(:356)。公式裡沒有任何「自家所有權/可靠性」項，跟trade-trip/founding-preempt同款斷言模式，這次親驗證實formula本身不成立，非猜。②task-type plumbing真洞：`team_data.gd`無TASK_CONVOY，spec沒指定porter用哪個task，persist-hold保護(靠`PROGRESSIVE_HOLD_TASKS`陣列成員)若implementer沒新增常數會靜默失效。③lifecycle interop真會被劫：親讀`_evaluate_subteam`(faction_ai:1719-1760)確認TASK_SETTLE誤用會在DELIVER地點直接`_convert_to_resident`轉居民(1737-1746)，就算換新task沒專屬分支也會被generic merge_queue fallback(1753-1756)在卸貨前攔截召回。④persist-hold是錯工具：短程convoy sunk-cost累積不夠(同founding floor那輪lean≤0.5陷阱同款數學)，且`_decide_subteam`只在TASK_IDLE才重評(1758)，非IDLE的porter本就已sticky不需要借persist-hold。ISSUES→回systems，4項處理完①有真正util方案或替代保證機制才能再議implementer。

**★★後勤統一大arc WHAT R①異質scale-correction-required（`2026-07-31-reviewer-to-systems-R1-logistics-scale-correction.md`）**：blueprint授權大框升異質。6條premise全CLEAN(P1/P2/P5信systems驗、P3/P4/P6我親驗：`faction_ai:629`限自家outpost/`_collect_member_tax:2434-2448`確認收coin非material/trade-trip數據是我本session稍早親測的和平經濟床Q3第一手數據)，無premise_contradiction。★核心=means-end「當現成其實新建」血證重演risk：異質+我雙線驗「現成零件」claim——§3.1腳夫子隊複用子隊dispatch：**載重model真複用，但convoy生命週期(取貨/遠端非母隊交付/原隊返航)不存在**(親驗`try_merge_back`subteam_system:67-83要求回母隊格才整隊併入消失，`_TRANSIT_TASKS`期間禁merge-back，無「送達非母隊目的地+原隊完整返航」路)；§4/§7 runway糧橋=現成零件：**最虛的一條**，親讀`food_flow.gd`全篇確認算的是該隊自己存活runway(單資源食物/單向/耗用非交付)，跟「運貨」語意不同，真複用只有ETA算式+`_fund_subteam_from_vault`撥款樣式兩個小零件；§7 unrest管線=SLICE B接口：**誠實**，sink端現成、source端(分配剝削訊號)雖新建但§6/§9已列HOW項未藏起來。§7 vault現成vs§1無路矛盾：確認「端點現成、邊(跨距離運送)100%新建」。判決scale correction required(非halt)——4點訂正(§3.1/§4/§7措辭撤回過譽複用claim+§6 SLICE A規模改寫反映convoy主體是新核心)後可進HOW SLICE A。三層設計/切slice邏輯本身不用打回blueprint重寫。

**★founding persist floor R②確認CLEAN（`2026-07-30-reviewer-to-systems-R2confirm-founding-persist-floor-CLEAN.md`）**：上輪4項必補+1連結項逐句核對全落地非敷衍(§1 lean分層算式一字不差/§4 WEIGH-not-GATE理由收錄/§5 persist.hold真fire斷言明文/§5b team14 timing對比+revisit後備條款/§5c b5496643連結講清不矛盾)。不凍紅線/crisis bypass/floor值/TASK_CONSTRUCT範圍維持上輪判定不重驗。CLEAN→dispatch implementer→measurer和平床re-run→QA。本session最高風險改動，兩輪異質+雙線獨立驗證收斂乾淨。

**★★founding-completion persist floor R②異質CLEAN+4必補（`2026-07-30-reviewer-to-systems-R2-founding-persist-floor-issues.md`）**：這輪session最高風險改動(唯一有真實regression前科的code區:2ab45856merge→5292faec revert latch凍seed1337世界)，召異質+自己雙線獨立驗證。★★不凍紅線清白：異質讀真正被revert的latch diff(5b166eb1)確認舊latch危險在skip-reeval(世界真停演化)+繞arbiter強制召回，這輪floor兩者都不做，只改persist_strength值餵既有try_set:64-70單點return false、_should_reeval全未動、施工完成自動釋放floor(自限終止)——結構性異於latch。crisis bypass(task_arbiter:66)跟persist值結構獨立確認。★4項必補(不擋方向)：①spec措辭誤導——親算base_persist=CAP×progress×lean，lean=0.2(務實)在progress=1時僅0.06永遠不到原threshold(0.1)，floor對這種人格是全程唯一保護非只cold-start bootstrap，要求訂正措辭②TDD要斷言persist.hold這個Probe key真的對founding子隊bump過，非只信complete_build>0倒推(本session反覆教訓:execution-verified≠只信最終結果)③team14 timing——hard floor蓋過safe_factor既有侵蝕，剛開工正在餓的隊撐到crisis才放手比原生行為晚，要求measurer額外確認沒多出本來不會發生的餓死案例④★我自己另抓：同日更早已有`b5496643`診斷文件明文「persist.hold假設REFUTED」(refute對象是`TaskArbiter.transition`,不同函式,不矛盾，且那份文件footnote早預言「若persist真有問題是保護不夠」正是這輪根因)，要求spec補一句連結避免未來誤讀成自相矛盾。WEIGH-not-GATE判準：同意「合理例外」，①的發現讓這判斷更站得住(floor×lean會讓某人格永遠0%完工，不是人格分化是引擎死角)。floor值0.15/TASK_CONSTRUCT-vs-TASK_BUILD範圍核對乾淨。CLEAN→回systems訂正4項→implementer。

**★和平經濟床實作R②CLEAN+2必補（`2026-07-30-reviewer-to-systems-R2-peaceful-economy-bed-impl-CLEAN.md`）**：親驗雙run determinism幾乎全同(逐行比對`warring_harness.gd:114-127`跟bed`_print_team_stories`)，但抓到bed第二輪漏清`FactionAISystem._a2b_remote_tribute_payers`(WarringHarness.run:119明文clear防跨run污染，親查`interaction_system.gd:607-609`確認這dict真被讀非純觀測)——全隊faction_id=-1正常不會fire但非機制上保證乾淨，要求補1行。★確認你自己focus③疑對：`indep.gate_*`只在`_evaluate_independent_strategy`(建國ally/subjugate外交gate)bump，跟①情境實際測的goal_resolver材料驅動founding是兩條不同機制——①隊野心≈0.3本就低於AMBITION_FOUND_MIN(0.55)，gate_ambitious=0是意料中事非「無障礙」；真正對應①機制的訊號是`construct.start=14`(動機層fire了)vs`complete_build=0`(執行層卡住)，要求report/measurer交接時明講別讓下游誤讀gate*=0。apothecary→material→forest founding機制核實乾淨(outpost_system:61-66真cost=50確認)。CLEAN→merge，2項必補在measurer產權威4問數前完成，避免污染QA故事稽核跟blueprint裁分支信任基礎。

**★和平經濟觀測床HOW R²v2 CLEAN（`2026-07-30-reviewer-to-systems-R2v2-peaceful-economy-bed-CLEAN.md`）**：①/③修法(established outpost+facility deficit≥`CONSTRUCTION_DESIRE_MIN`(need_oracle:29,真實既有常數0.3)→need_keep(material)>0)親驗坐實真活(接進`_construction_facility_need`:59+`goal_resolver`:59/74，非換包裝參數，因果斷點真接上)。★fixture-liveness斷言(t0斷言need_keep(material)>0+forest tile in range否則FAIL拒開工)比我上輪要求的更進一步——把「fixture活不活」焊進機制本身非單次修正。honest措辭區分code-provable已知(fresh無outpost bootstrap gap記known_issues) vs live經驗案(established secondary-founding)到位。②④維持。CLEAN→implementer(config+薄bed+liveness斷言)→measurer→blueprint裁分支。

**★★和平經濟觀測床(measure-first Step0)HOW R②異質ISSUES（`2026-07-30-reviewer-to-systems-R2-peaceful-economy-bed-issues.md`）**：召異質+自己逐行複驗。★先訂正自己原懷疑(疑`faction_id:-1`會讓情境碰不到`_evaluate_infrastructure`真·蓋新outpost分支)——方向錯，means-end通路(`goal_resolver._resolve_resource_prereq`:212-219)對independent隊是通的，撤回。★但親驗抓到更深問題：①founding情境(material≈0+forest旁)對**無outpost的隊**是causally死路——親查`need_oracle.gd`：`need_keep(material)=_self_use(material∈PURE_INTERMEDIATE恆0)+_supply_chain(無設施恆0)+_construction_facility_need(無own outpost:39 early-return 0)`三項全恆0；`goal_resolver.gd:197`：`holding(0)>=need_keep(0)`→`0>=0`為真→買/採兩分支都碰不到就先吐空。①的自變量(material持有量)對無outpost隊的決策狀態沒有因果連結(holding=0跟9999完全一樣)——不fire是讀code就能預判的必然，非待測經驗問題，比confound更嚴重，違反Step0「別訊號理論式pivot、用資料裁分支」的初衷(資料還沒跑就已知答案)。要求①二選一：(a)改情境給①隊一個已有非森林outpost+設施缺料需求(讓need_keep(material)真非0，變活決策)(b)不改情境但spec誠實化措辭，講清楚①測的其實是「material需求bootstrap既知缺口」非開放經驗問題。②(已有outpost lv1)④(food軸非outpost-gated)設計乾淨當對照組佐證①是特例。★③交易情境料窮側如果也沒outpost會踩同一顆雷，要求一併查。ISSUES→回systems，②④不用動。

**★observer-no-global-RNG靜態閘實作R②CLEAN（`2026-07-29-reviewer-to-systems-R2-observer-rng-gate-impl-CLEAN.md`）**：負向lookbehind逃生口逐案手推(bare/dotted/行首/property賦值/alternation順序)全無洞+Godot RegEx底層PCRE2對固定長度lookbehind(`[\w.]`單字元類)向來支援；pick_random/shuffle確認走獨立`RNG_METHOD_RE`(讀code證非共用逃生口誤放行)；constitution_gate baseline親grep`scripts/simulation/`驗證(唯一命中`encounter_system.gd`非決策檔本就不掃，"74 removed=0"非空話)；3檔marker親讀全文確認真零RNG(含marker行`f79bd8ac`分支上確認加在檔頭)；TDD14/14(8 hit+6 miss)核對到位。上輪要求的randfn/seed向量+constitution_gate同步擴充皆確認落地。CLEAN→merge-gate接入，純靜態零runtime影響。

**★observer-no-global-RNG靜態閘 HOW R②CLEAN+1必補（`2026-07-29-reviewer-to-systems-R2-observer-rng-gate-CLEAN.md`）**：窄範圍regex機制自己直接可驗不召異質。逃生口(識別字+`.`前綴=本地放行)設計對，親grep `world_generator.gd:51`確認`RandomNumberGenerator.new()`+`rng.`前綴是真實既有模式非空想；pick_random/shuffle無本地overload的斷言核實為真(Godot 4.x Array/Dict這兩個方法沒有可傳RNG實例的版本)。★抓到向量集不完整：5類漏`randfn`(GlobalScope真實高斯隨機函式)+裸`seed(...)`(全域重播種——比單次randf更危險,呼一次就打亂之後所有隨機呼叫,不用自己再耗RNG就能靜默污染，比目前任何一個抓的向量都更貼血證要害)。要求擴成7類+順帶同步補進`constitution_gate.gd`的RNG_RE(§2c本就打算補pick_random/shuffle，同邏輯延伸)。marker慣例vs中央清單判斷合理(自文件化耐腐)，殘留風險(忘加marker=另一種記性依賴，但嚴重度較低)記一筆非必修。CLEAN+1必補→implementer，純靜態零風險非其他arc blocker。

**★糧流B1(糧橋+top-up)實作R②CLEAN（`2026-07-29-reviewer-to-systems-R2-food-flow-B1-CLEAN.md`）**：systems判「B1機制對、A1 target沒fire因warring上游founding從不dispatch非B1缺陷」——親查`git show 91fe5741 -- faction_ai_system.gd`全diff確認改動全在`_dispatch_builder`函式體內(既有pop-check後插go/no-go、既有subteam建立後插top-up)，沒碰任何上游呼叫路徑(means-end argmax/infra評估/busy gate/task_arbiter)——結構上下游檢查不可能是「呼叫點從不被呼叫」的原因，systems判斷邏輯坐實。★但自己另抓一項：「收編取代礦山bootstrap」這句話沒測且疑似縮水——舊`BOOTSTRAP_DAYS=50`comment明寫涵蓋「施工+設施全周期」(礦山不能種田需撐到設施完工能上市場)，新top-up`_need_food`公式只算建outpost本身ETA(親算civilian lv1 eta_build≈16.7天)，沒算「完工後還要蓋設施才能自足」那段——概念上就是沒對齊同一個需求；`food_bridge_test.gd`4條斷言(確認"4/4"=4斷言非4函式，跟Slice A同款標法一致非計數不符)全用plains fixture，礦山案例完全沒測到，跟A1 target一樣是「該驗場景這輪沒真跑過」。不擋merge(對非礦山站是純改善)，但要求列Slice4(b)和平economy measure首驗項，非本輪blocker。CLEAN→merge。

**★糧流感知Slice B HOW R②v2 CLEAN（`2026-07-29-reviewer-to-systems-R2v2-food-flow-slice-B-CLEAN.md`）**：上輪3項ISSUES核對訂正逐條落地——①配糧改測`sub.resources.food`(frac-split實際持有)非carry_capacity，第5真新建(通用food top-up)明列+收編取代礦山bootstrap(2651-2674+2721 upgrade)非疊加②引用改標`_dispatch_builder:2603-2698`③hunt_ev措辭訂正(隊技能非tile)+`hunt_preview`重用點明非新建+折扣公式2候選(clampf(wild_game/expected_take,0,1)或e^遞減)留B2 R②定案(合理分工非空話)。三項皆實質訂正非文字敷衍，無新洞。CLEAN→dispatch implementer B1→measurer specimen-off→QA稽核★A1子隊真不餓死(execution-verified，別讓aggregate派遣數頂替cross-slice tripwire)→B2先定折扣公式R②→B3/B4增量。

**★★糧流感知Slice B HOW R②異質ISSUES（`2026-07-29-reviewer-to-systems-R2-food-flow-slice-B-HOW-issues.md`）**：召異質+自己逐條核，sub-slice切法/B1先行/RNG紀律方向皆對，但抓到3項：①★★配糧公式測錯量——spec「配糧=carry_capacity」是重量上限非實際持有食物；親查子隊糧食真正來源是`SubteamSystem.dispatch`(subteam_system.gd:36-40)人口比例frac-split，跟`_fund_subteam_from_vault`(faction_ai:2856-2885)只補`OUTPOST_COST`(outpost_system.gd:10-18，只material/tools無food key)完全無關——carry_capacity幾乎總是遠大於實際持有量，用它當gate等於空放行，B1主打的A1 victim不會被真正擋下；等於被「出發配糧」一句話蓋住的第5個真新建項(通用食物撥付/top-up機制)。②診斷引用錯function——spec引`faction_ai:1250-1252`當「查母隊非子隊」證據，親查該行在`_evaluate_independent_strategy`(1185行`parent_team_id!=-1`即return，只有母隊自己走)，走到底是信使/自我攻擊，根本沒子隊被create；真正派實體建造子隊的是`_dispatch_builder`(2603-2698)，唯一子隊存活檢查是礦山限定ad-hoc bootstrap(2651-2674)，非礦山site完全沒有。③打獵EV核心公式(chance/yield)其實已存在於`hunt_preview`(hunt_system.gd:40-46，零randf零改世界)非新建，但沿路存量遞減折扣全文找不到公式純斷言。ISSUES→回systems訂正①(硬性)②③(補強)後重新R②。

**★糧流感知Slice A 實作 R②CLEAN（`2026-07-29-reviewer-to-systems-R2-food-flow-slice-A-impl-CLEAN.md`）**：persist×safe_ratio 三項講死逐條核到落地(`_safe_factor` eta/safe_ratio/ratio_floor 公式跟人格方向對，5 task 排除確認落 return 分支)。team14 根治數字(固執0.05 vs 務實0.002)親算=lean(Slice1既有)+ratio_floor(本輪新增)兩人格因子同方向疊加，非算式錯誤。★抓到 HOW spec 內部矛盾：§1 已明文延後「假設tile投影器」到 Slice B，§2 措辭卻把「當前tile可持續採」列進 Slice A inflow——實作只認已完工自家outpost(`outpost_level==0 or outpost_owner!=team.team_id→0`)，等於採 §1 的窄範圍(施工中未完工outpost=inflow恆零)，跟 §2 字面有落差；核對後認定這是同一項(§1已排除的投影器)，非漏做，是我 R②時該抓的 spec 文字矛盾非本輪新洞。不擋 merge(team14 核心動機=runway/ETA比對，施工期inflow=0不影響判斷方向，且比 Slice A 前更保守非退步)。要求：spec §2 文字待 Slice B 定案一併修正+這條併入 Slice B「投影器」待辦非另開新項。TDD 6函式9斷言核對到位、世界不凍(attrition2.03%/teams49→63)延續健康信號。CLEAN→merge→下一步measurer specimen-off→QA team14根治稽核→Slice B(dispatch前重走R①/R②，含這條spec文字澄清)。

**★糧流感知Slice A HOW R②CLEAN（`2026-07-29-reviewer-to-systems-R2-food-flow-slice-A-CLEAN.md`）**：R①收窄正確——inflow收斂harvest-only，打獵EV/任意tile投影器/派遣閘/人口hook全排到SliceB/C。★persist×safe_ratio三項要求逐條核到：①乘法縮放非硬塌(明文引用PROGRESSIVE_HOLD attrition→0血證)②5種無ETA task排除(非硬造假ETA)③hysteresis抖動抑制。ETA_days公式親查`outpost_system.gd:307 construction_ticks_left-=maxi(pop,1)`維度一致非杜撰。harvest-only inflow用真`resource_system:63-76`collection公式(outpost_mult×pop_mult×skill)非簡化布林proxy，比GATE-A當年精確。`ratio_floor(人格)`跟既有`lean`是WHAT §2明講的兩個不同人格軸，架構上獨立相乘非重複計算。本slice零RNG接觸(打獵EV排除到B，我R①的RNG陷阱這輪不出現)。CLEAN→dispatch implementer，Slice B(打獵EV+投影器+派遣閘)dispatch前記得重新走R①/R②。

**★★糧流感知(runway-aware)WHAT R①CONTRADICTION（`2026-07-29-reviewer-to-systems-R1-food-flow-scale-CONTRADICTION.md`）**：means-end同款異質驗規模，這次又不成立。我自己親讀`hunt_system.gd`全文確認：`hunt_small_game`是單格單次RNG擲骰(`randf()>=chance`)、`hunt_preview`連wild_game存量都不讀——沒有任何多格路線多天聚合模型，§5把「打獵」列進「皆現成」不準。★我自己多抓一個agent沒明講的：若「橋」可行性判斷需要預期打獵收穫，絕不能直接呼這個真擲骰函式估算(會在規劃步驟耗global RNG=observer污染，同`feedback_observer_no_global_rng`鐵律)，要求HOW明文用期望值公式非真擲骰。agent另找4項：①sustainable inflow現有pattern(`decision_context.gd:283-294`，我GATE-A那輪親讀過)只認home outpost+輸出布林非連續量+沒乘真collection公式加成，比新設計要的「任意tile連續值」窄很多②`persist_strength.gd`(我Slice2親讀過)的ETA_days只TASK_BUILD有真值，其餘5種gated task退回沉沒成本proxy無法反推剩餘時間③派遣閘要接4-5個既有dispatch call site非1處④人口變動無單一event hook分散多處。★`safe_ratio×persist_strength`交互要求HOW現在講死(同一組code剛因範圍太寬出過真regression，不能重蹈)。halt回blueprint調規模認知。

**★持守統一Slice3 R②CLEAN（`2026-07-28-reviewer-to-systems-R2-persistence-slice3-CLEAN.md`）**：我上輪要求的`new_util`矛盾解法正確——純門檻式(`persist_strength>THRESHOLD`)不需要new_util也不需要call site改簽名。★親自對照確認這個guard位置正好攔在`outpost_system.gd:431`(`start_build`用PRIO_DISPATCH同外交同層)的same-tier self-replace路徑之前——直接對準真A1根因(觀測補洞輪親驗過的同層搶班)非泛用防護剛好覆蓋。★ratify implementer的「progressive-only→completable-only」白名單收窄(`PROGRESSIVE_HOLD_TASKS`只BUILD族)——execution-verified抓到初版硬擋全部committed導致PRODUCE隊鎖死attrition→0(逼近凍世界同病)，收窄後恢復；這是我R①「FLEE無終點不套」邏輯的自然延伸，PRODUCE/TRADE/GOVERN有進度但無自然終點若硬擋會退化成新latch，要求寫回HOW spec正式文字非只commit message。危機/玩家/同task三例外逐條核對，single-point return false非skip-reeval結構確認跟舊latch本質不同。TDD 7/7乾淨無計數問題。放行merge→建議下一步A1 focused re-measure驗證真閉環(execution-verified非只R②綠燈)。

**★持守統一Slice2 R②CLEAN（`2026-07-28-reviewer-to-systems-R2-persistence-slice2-CLEAN.md`）**：`construction_ticks_total`重建公式逐action核對(civilian lv1=100親查`BUILD_TICKS`常數確認、`upgrade_facility`用`current_level_key`只完工才變確認中途重算不漂移)；新鮮度事件點(`_tick_construction`+`_on_arrival`)覆蓋到位。TDD⑥設計巧妙(near-complete vs just-started用**相同**task_start_tick,若退化回時間proxy兩者會相等,`pn>pe`直接證明真用construction-tick)⑦驗新鮮度即時反映+欄位寫回。世界不凍/perf/憲法對齊皆延續。放行merge→dispatch Slice3(★提醒：我上輪R②要求的§6/§7 new_util資料來源矛盾還沒解，Slice3開工前systems需先補上，非本輪新要求)。

**★持守統一Slice1決策層 R②CLEAN（`2026-07-28-reviewer-to-systems-R2-persistence-slice1-CLEAN.md`）**：`PersistStrength`公式核實(sunk×lean×clamp，`task_start_tick`確認是既有arbiter維護欄位非新造proxy，`0.3<SURVIVAL_BOOST_MAX 2.5`親驗)；世界不凍回歸親算(teams 49→64成長+attrition兩者皆非0皆活，跟latch那次全歸零churn完全不同型態)；progressive-only gate(FLEE排除)吻合我R①發現。★scope裁定兩項：SOLO_COMMITMENT_BONUS親grep確認零消費者=死碼,skip准；survival_committed_stall的`stall_patience_factor`(我更早審查輪已親讀過)本來就人格加權非flat,排除准且不該勉強塞新公式(冗餘求解器同款毛病,留獨立機制)。TDD 5函式/10斷言核對上。放行merge→dispatch Slice2(進度事件新鮮度)。

**★持守統一執行層HOW R②（`2026-07-28-reviewer-to-systems-R2-persistence-execution-HOW-issues.md`）**：CLEAN with 1 required clarification。★自己先誤判又被異質糾正記一筆：我原懷疑「rank層升級COMMITMENT_BONUS就夠,83 call site try_set改寫是不必要複雜化」——親查`outpost_system.gd:431`(`start_build`直呼`transition()`完全在argmax外,COMMITMENT_BONUS的opt==current_option機制根本碰不到TASK_BUILD)否證我的懷疑，rank-only修法對這整類非-argmax動作結構性無效。★git log親驗latch真實merge(2ab45856)→revert(5292faec)歷史(fresh clean repro坐實世界凍結regression+"16/16 completion全upgrade_facility零build"沒真閉環)——支持這次try_set軟util偏置路線是排除另兩條路後剩下的那條，非過度工程。「83 call site」措辭核實非誤導(spec§7已寫清楚是普查非改點數,persist_strength讀team欄位非傳參數)。★唯一要求：§6 pseudocode「new_util > current_util+persist」的new_util沒有明確資料來源(`try_set`簽名`task_arbiter.gd:38-39`只有priority int從未有util值)，§6與§7字面矛盾(要嘛新增參數要嘛換成不需要new_util的門檻式設計)，implementer開工前需講死。方向對不需退回blueprint。

**★★持守統一(27機制收成一套) R①CONTRADICTION（`2026-07-28-reviewer-to-systems-R1-persistence-unification-CONTRADICTION.md`）**：重演means-end R①同款異質框外驗規模斷言，這次三規模斷言皆不成立。我自己複驗3項：①`COMMITMENT_BONUS`(decision_engine.gd:6,88,173)零跨層通路(算完即丟,task_arbiter零引用)②FLEE無progress概念(結束判定=外部威脅消失+flat timeout,無「逃多遠」量)③27張表本身不完整(親抓faction_ai:3451-3463礦山builder求生豁免未列表)。agent另揪：PRIO階層/combat_lock/emergency_guard答的是「哪層級贏」跟「這動作投入多少」是兩條獨立軸不該揉一起(WHAT spec自己§3c已半承認該留外,跟§4「全收」自相矛盾)；~29處production call site硬編PRIO常數呼try_set，換util-weighted是重寫仲裁演算法非接線；TRADE/FOUNDING「距離縮放timeout」是dispatch時算好的一次性預算非即時進度量，「absorbed」措辭掩蓋要重做。建議收窄範圍：決策層bonus家族(5-6項)真能收，階層/combat-lock/危機地板/跨層共讀/FLEE明確排除或列獨立子案。halt回blueprint調WHAT範圍。

**★specimen 撞車解 R②b CLEAN（`2026-07-28-reviewer-to-systems-R2b-specimen-collision-resolved-CLEAN.md`）**：implementer選項1(commit既有全API+丟窄化版)，逐項比對最終內容確認：全API復原byte-identical/窄化版丟棄/regression真的重新對齊(改呼setup_from_env非殘留已刪select())/adhoc_demo 2-arg不受影響/`known_issues.md:20-24`親自grep確認真記了(非只信commit message)。放行merge。specimen量測可信度框架解封。

**★specimen selection RNG-neutral 修 R②異質 ISSUES（`2026-07-28-reviewer-to-systems-R2-specimen-rng-fix-issues.md`）**：implementer bisect翻案(leak非tracer wrap,是選取pick_random,已刪ad-hoc script)systems ratify——診斷+修法邏輯親驗CLEAN(select()零RNG/regression刻意跑real far-LOD非force_full_hd/tracer零改動)。★must-fix：親自git status確認`scripts/debug/specimen_dump_helper.gd`**現存在main working dir**(untracked,2026-07-19建,`03b_measurer.md:146`文件化`SPECIMEN_TEAM_ID`死隊追蹤標準流程依賴+`adhoc_specimen_demo.gd:27`呼2-arg`dump()`)——分支新檔同路徑同class_name但API窄化(砍SPECIMEN_TEAM_ID+dump()改0-arg)，merge會炸demo腳本+丟失文件化工作流依賴。★反轉：既有檔SPECIMEN_SAMPLE_N路徑本來就已經是零RNG strided(同分支演算法)，真正bug源頭是另一支已刪ad-hoc腳本非這個既有檔——這次修等於在不知情下重新發明+意外砍窄既有能力。要求merge前解決untracked檔衝突(commit既有檔或保留全API合併,不能砍SPECIMEN_TEAM_ID)。gate followup同意可拆但要求明列known_issues(第4次同族,別靠人工記性接)。

**★resume 治本 R②v3 CLEAN（`2026-07-25-reviewer-to-systems-R2v3-resume-loadbearing-CLEAN.md`）**：implementer execution-verified(1mo tap)誠實坐實 latch fires 94.6%但 complete 仍未改善→不 ship，先記一功(execution-verified抓真洞非只走流程)。新根:latch早退不更新last_decision_tick→directive_fresh(latch上方)仍會leak→builder棄工地但仍在格→resume owner/resident gate排除builder自己→永久stall。修=`_try_resume_construction`優先召回`construction_team_id`原隊(繞owner/resident,因它是原施工隊本人非找代管)。逐項核對：release先歸零task_priority讓transition guard不擋(親算)/`_tick_construction`只認同格TASK_BUILD非認誰→續建非重開/邊角(stale死亡+離格改建+戰鬥+糧)四guard覆蓋完整/插入點:2746位置精確。(B)directive-immunity留followup視thrash程度，spec已誠實標非樂觀假設。CLEAN→dispatch implementer→execution-verified(outpost_built>0)才收。

**★construction commitment latch R②v2 CLEAN（`2026-07-25-reviewer-to-systems-R2v2-construction-latch-CLEAN.md`）**：systems 接受威脅悶死洞，訂正=`force_reeval`參數穿透`_decide_unified`/`_should_reeval`(最前面`if force_reeval:return true`同既有crisis-edge/directive模式)+威脅段(:423)改傳true繞latch。★我自己額外排查「還有沒有第二個同款漏洞」：grep全檔`decision_eval_next_tick=`逐條核對，只有原本那個(:422 威脅段=current_tick立即生效)是「reset timer混過cadence分支」模式，另兩處(:1525/:1926)是should_reeval已過後排程非繞過、:3486-3487讀完上下文確認是獨立survival分支不經should_reeval且不碰TASK_BUILD——確認沒兄弟漏洞。TDD威脅/深餓分開測落實。CLEAN→dispatch implementer→execution-verified(outpost_built>0)才收。

**★★construction commitment latch 審憲 ISSUES（`2026-07-25-reviewer-to-systems-R2-construction-latch-issues.md`）**：measurer 6mo tap 定案 A1 stall 真根(外交/build 同 PRIO_DISPATCH50,transition guard 只擋>=THREAT70同級不擋,施工隊每 cadence 被搶外交)+修①`_should_reeval` TASK_BUILD latch+②timeout release 對稱。★親讀抓到 spec 憲法論證本身事實錯誤：聲稱「威脅已在 crisis/stuck gate 打斷」但親驗 `_decision_crisis`(faction_ai:1868-1878)只管人口/糧崩跟威脅零關係、`_is_stuck`管路徑卡住——真正威脅回應是另一條獨立路徑(:401-423 busy-preemptible threat check，手動 reset cadence timer 繞節流)，新 latch 若放在 cadence 分支之前會**靜默悶住這條逃命路**——隊蓋房子時被真敵人逼近到 threshold 仍不會逃，血案量級比原 A1 bug(蓋不成房子)更重(可能被殲滅)。要求 latch 只擋純例行 cadence reeval、不擋 threat-force 路徑+補威脅測項(非用深餓測項頂替)。核心根因診斷+修②對稱皆 CLEAN，非推翻方向只要求位置訂正。

**★construction pipeline 可觀測性補洞 R②CLEAN（`2026-07-25-reviewer-to-systems-R2-construction-obs-tap-CLEAN.md`）**：A1 merge 後測仍 FAIL(dispatch巨量但completion=0)，systems code-trace 到極限仍需 runtime data。4條因果鏈候選親驗：#2 transition guard(`task_arbiter.gd:118`)間歇性候選(`PRIO_DISPATCH50<PRIO_THREAT70`確認正常不擋,需子隊被threat re-rank才卡,信心標「需驗」準確)/#4 `_try_resume_construction`(faction_ai:2742)對founding荒地structurally必然失效(`is_owner`結構恆假,信心標「確定不需runtime」準確)。tap設計對症(5點恰好區分#2/#3/#4三假說非泛用log)、純觀測禁RNG無副作用確認。CLEAN→dispatch implementer加tap→measurer定位一階→systems一次修全部卡點(不whack-a-mole)。

**★means-end A1 修 implementer 交付 merge-gate CLEAN（264a1844，`2026-07-25-reviewer-to-systems-R2-means-end-A1-fix-done-CLEAN.md`）**：機械層核實 3 分支路由+TDD 真移動驅動(非 teleport,首版遮 same-tile-no-arrival bug 已修)。★裁①偏離(same-tile facility defer infra)召第三方模型獨立複核我自己前裁——結論**更貼憲法**:means-end 8 個 build_F goal payoff 全平 1.5(`goal_registry.gd:46-53`),原 in-place 選法其實是 dict 插入序贏非真 utility,defer 給真連續 desire-score 的 `_pick_facility` 反而更守「utility 餵 utility 非 scripted」。★親自順藤揪出新項(非本刀新增,S4 原設計就有,這輪複查才浮現)：`_evaluate_infrastructure`/`_evaluate_independent_infrastructure`(faction_ai:3057+/3077+)本就是既有、對每個 outpost(含remote)固定 cadence 跑`_pick_facility`+派`_dispatch_facility_builder`的機制——means-end 保留的 remote facility 分支跟它完全同目標同 consumer，重疊度未測，記入 focused re-measure 範圍(非擋 merge)。★TDD 數字第三次同款不符(自報6/6,實際7函式/19斷言)，這次明講需要流程改(报数字前實跑讀真 `=== DONE ===` 輸出)非只提醒。CLEAN→merge放行→measurer focused re-measure(A1閉環+新增remote-facility重疊度)→QA。

**means-end S1-S7 WHOLE-DONE**（骨架→資源型→定位+閉環→設施→委派→折現→cadence 收尾，7 輪 merge-gate 全 CLEAN，含 2 輪 HOW-spec 異質框外審 R①CONTRADICTION→R②issues→R②複核CLEAN）。backlog watch 4 項（S3 unowned 優選/S4 facility-type 改建/S5 residency 8-12 浪費帶/S7 maintain-goal stale-satisfied 3天窗口）留 whole-measure 觀察，非 blocking。詳細逐輪記錄見 handback 檔（`2026-07-24`~`2026-07-25` 開頭 `*means-end*` 系列）。

**貧窮陷阱三腿全綠（食安 GATE-A + coin extraction + material-hold-protection）**：三線 merge-gate + spec R② 皆 CLEAN，詳見對應 handback。

---
> 慣例（此檔 owner=reviewer 自更）：開工 → `status: working` + `current_ticket: <handback檔名或topic>`；完工 → `status: idle` + `current_ticket: "-"`。01(系統/architect) grep frontmatter 監控。舊細節記錄見 git history / handback 原檔，此檔定期 prune 保持可讀。
