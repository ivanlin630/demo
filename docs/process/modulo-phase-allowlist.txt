# docs/process/modulo-phase-allowlist.txt —— ★相位守衛的【具名放行清單】（第⑦票 2026-09-05）
# ★2026-09-06 一併移除【⑧ 之後 code 已無】的 1 筆（閘自己指出來的）：
#   - scripts/simulation/sim_runner.gd | if state.world.current_tick % FAR_ZONE_INTERVAL == 0:
#   ⇒ ★★閘的「allowlist 有、code 已無」不是 FAIL，而【放著不動就會累積成假清單】。

# ★★★2026-09-06（`modulo-same-shape-4`）：以下 3 筆【已遷 CadenceStagger】⇒ 從清單移除：
#   - scripts/simulation/harvest_system.gd | if state.world.current_tick % WorldState.TICKS_PER_DAY == 0:
#   - scripts/simulation/harvest_system.gd | if state.world.current_tick % WorldState.TICKS_PER_MONTH != 
#   - scripts/simulation/population_system.gd | if state.world.current_tick % WorldState.TICKS_PER_MONTH == 
#   ⇒ ★這份清單【變小了】—— 而變小要在卷面上看得到，否則「遷完了」跟「忘了遷」長得一樣。

#
# ★★★這【不是豁免清單，是「已經有人判過」的清單】——
#   閘擋的是【新出現而沒人判過的形狀】，不是「有裸 modulo」本身。
#
# ★格式（TAB 分隔）：出現次數 <TAB> 檔案 <TAB> 正規化後的整行 <TAB> 判過的理由
#   ★★次數也要對：有人多加一行一模一樣的，key 不變而次數變 ⇒ 一樣紅（那格是鑑別力）。
#
# ★★判準（R² 指出【不用發明】）：`sim_runner.gd` 的 SYSTEMS registry 自己的 `shape` 欄位
#   `state`＝每 tick 都跑到的 whole-state step ⇒ 精確 modulo 安全
#   `teams`／`teams_cadence`＝【被 pass 相位過濾過】⇒ 精確 modulo 會整個掉進縫裡（不是失真，是不發生）
#
# ★★★【2026-09-06 CLOSE】原本這裡寫「清單裡標【同型待裁】的那四筆，等 systems 裁」。
#   ★而現在 12 列裡【沒有任何一列標著待裁】—— 門牌指向一個不存在的東西。
#   ★★兩種讀法長得一樣：(a) 已經裁完了 (b) 還有四筆沒裁而標記掉了。
#   ⇒ 對照 code 定案是 (a)：那四筆＝本票已遷的四處
#      （harvest 的 game／predator／herb ＋ population 的 minor_mature），
#      它們的安全理由確實是「43200%1440==0 算術整除」不是機制，而現在全部走 `_due` 到期比較。
#   ★★★裁定：CLOSE，不留待辦。★而這段【不刪掉改成留註記】，是因為
#      「本來就沒有待裁」跟「裁完了」在卷面上必須分得出來 —— 前者代表當初登記錯。
1	scripts/simulation/day_night_system.gd	return float(state.world.current_tick % WorldState.TICKS_PER_DAY) / \	值不是閘：算一天之內的相位比例，沒有 == 0 的分支 ⇒ 不會被相位吃掉
1	scripts/simulation/faction_ai_system.gd	"mod_infra": state.world.current_tick % INFRA_INTERVAL, "n_factions": state.factions.size()}, 5)	觀測不是閘：Probe.note 的欄位（唯讀記錄）⇒ 不決定任何事發不發生
1	scripts/simulation/harvest_system.gd	var t: float = float(state.world.current_tick % SEASON_LENGTH) / float(SEASON_LENGTH)	值不是閘：季節相位比例
1	scripts/simulation/manpower_system.gd	if state.world.current_tick % CAPTIVE_CADENCE != 0:	安全：_step_captives 在 advance_tick 頂層【每 tick 都跑到】⇒ 沒有外層相位
1	scripts/simulation/sim_runner.gd	if state.world.current_tick % (WorldState.TICKS_PER_DAY / 4) == 0:	安全：advance_tick 頂層 —— ★這一層【就是】排程本身，它每 tick 都被求值
1	scripts/simulation/sim_runner.gd	if state.world.current_tick % (WorldState.TICKS_PER_DAY / 4) == 0: # 每 6 小時	安全：advance_tick 頂層（同上，另一處）
1	scripts/simulation/sim_runner.gd	if state.world.current_tick % NEAR_CADENCE == 0:	安全：★near pass 的定義（同上）
1	scripts/simulation/sim_runner.gd	if state.world.current_tick % PopulationSystem.OVERFLOW_CHECK_INTERVAL == 0:	安全：advance_tick 頂層
1	scripts/simulation/sim_runner.gd	if state.world.current_tick % WorldState.TICKS_PER_DAY == 0 and _perf_count > 0:	安全：advance_tick 頂層（perf 輸出）
2	scripts/simulation/sim_runner.gd	if state.world.current_tick % WorldState.TICKS_PER_DAY == 0:	安全：advance_tick 頂層（日界＋rootdiff 對照各一）
