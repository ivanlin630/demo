**主 session 職責**（brainstorm → spec → plan 設計，★**不實作**）：
設計 spec｜審核 plan｜跨系統一致性｜**merge 管理**｜更新 docs。★開場必讀 `docs/invariants.md`。

## ★★★觸發式必讀（開場【不】讀，★做到那件事的當下【必】讀）

| 你要做這件事 | ★**動手前必讀** |
|---|---|
| ★**開驗收考／診斷考** | ★**`docs/process/09_exam_gate.md`**（★半成品禁跑驗收考） |
| ★**動信箱／Monitor／watchdog** | ★**`docs/process/07_mailbox_trigger.md`** |
| **動機器軌（orchestrator）** | `docs/process/08_machine_workflow_v2.md` |
| ★**改既有機制** | ★★**`docs/mechanism-intents.md`**（WHAT 權威帳：★code 服從表、表服從用戶） |
| ★**要 `git pull --rebase`** | ★★**`detail/01_architect-cases.md`「`git pull --rebase` 在含 merge commit 的歷史上是破壞性的」** |
| ★**要處理長壽 slice branch 的新鮮度** | **`detail/01_architect-cases.md`「用【落後量】觸發，不用【時間】」** |
| ★**要拿一張【綠】的床當結案／驗收證據** | ★★**先對它做一次陽性對照**（弄壞被守的東西 → 必須變紅）——★★★**沒對照過的綠床不配當證據**（「綠」與「過期到不再咬得住」長得一樣；blueprint 裁 2026-09-03「用時付費」） |
| ★**有人在跑【序列批次】（多 seed／多床一輪跑完）** | ★★**那批跑完之前，樹是被它鎖住的**——中途改 production／換工具，**還沒開始的那幾顆會吃到新版**，於是修前修後**混在同一批裡而且看不出來**（血證 2026-09-03 兩次：wrapper 換版、退 production 檔量修前 churn）。★★★**改樹前先問「現在有沒有批次在跑」，不是問「現在有沒有人在忙」。** ★**第三次它收了錢（2026-09-04）**：批次被砍、中途改過床 ⇒ **丟掉整批重跑**（接續會讓三顆不同版）—— **有成本時仍被遵守＝它有牙齒。** |
| ★**要派一個「量 X」的工單** | ★★**先問「X 是不是已經在某個 tap 裡了，只是沒印出來」**——血證 2026-09-03：`zhagen.lost_table` 早就記了 opt/util 全表，缺的只是一段 print，★★★而我差點為了一個**已經存在的數字**多跑三顆 seed。（與「儀器沒開」相反：**資料在檔案裡，只是沒有人讀出來** —— 報告上長得一樣，成本差三顆 seed。）★★★**第二問**：**「我要的【對照組】，是不是已經有一批在跑了？」**——血證同日：無-tap 基準本要特地跑一趟，而在飛的 `#2 dump` 跑的正是還沒加 tap 的樹 ⇒ **它就是基準**。 |
| ★**要下任何「查過了」的結論** | ★★**`detail/01_architect-cases.md`「用錯鑰匙」——寫下你的問句 vs 指令實際答的問句，不一樣就是用錯鑰匙**（一天內七次） |
| ★**要用【新工具】測【舊樹】（或要跑 branch code）** | ★★**從 main dir 跑，`--path` 指那棵 worktree** ——**wrapper 用 main 的、code 用 branch 的** ⇒ ★**不動任何 branch、不 checkout、不必合 main**。★★★血證 2026-09-07：我建議「合 main 拿新 wrapper」，而正解是【工具與被測物可以分離】—— **那條招式寫在 `CLAUDE.md` 的【量測員】那行，於是它被讀成「只有量測員需要」。** |
| **要引用某條被搬走的規則** | `docs/process/detail/*-cases.md`（同標題節） |
> ★★★**規則綁誰就住誰家**（blueprint 立 2026-09-02）：**一條規則要放進【會被它約束的那個角色】的必讀，不是【執行它的那個角色】的必讀。**
> ★血證：「dump 票必指定母體」約束的是**開票的 systems**，若放進 `03b_measurer.md` ＝ 放進執行者的必讀，★★**而違反它的人看不到**。
> ★★**這張表本身要短** —— **它的用途是「知道有這個入口」，不是取代那些檔。**

> ★★**本節【不得被切】** —— 它的用途是「在動手的那一刻看見入口」，移到 `detail` 等於廢掉它。

## ★★流程六條（★2026-08-27 收攏：六個殘骸壓成一張表 —— ★★N 個殘骸＝N 個入口）

| 一句話（操作性的部分留在這裡） | detail 節標題（逐字） |
|---|---|
| **3 層流程依規模選**，主 session 第一句需求即判層級 | 3 層流程（依規模選，主 session 第一句需求即判層級） |
| ★**無斷點自動鏈 ≠ 跳站**：reviewer 是鏈上的站。**R² 每 slice 必過；R① 只新概念大框才啟用** | ★兩道對抗閘（reviewer，spec 前後各一——不可省，2026-07-10 釘死） |
| ★**spec 鎖後 dispatch ＝ 直接寫 `to:implementer status:open` 到 main mailbox**（不需 live 終端、不需人肉轉述），★★別問用戶 | ★spec/plan 鎖後直接 dispatch，別問用戶（2026-07-09 定死） |
| ★★**hook 提醒 ≠ gate**：**gate 要裝在【執行點】（鎖／merge），advisory 放上游必漏**（compact 會洗掉記憶） | ★★spec 鎖在長跑因果 = QA-verdict 機械閘（2026-08-04 用戶定，治 QA-hook 連漏） |
| **`plans/` 停用 —— HOW spec 就是唯一產物** | ★裁定：`plans/` 停用，HOW spec 就是唯一產物（systems 裁 2026-08-21） |
| ★**派工單必帶 `slice:` 與 `tier:`** ＋★★**「本 slice 會碰哪些檔」**（新鮮度②要用） | ★P9 交接縫：派工單必帶 `slice:` 與 `tier:`（2026-08-21 用戶核） |

> ★**全部血證／原文 → `detail/01_architect-cases.md`（同標題節，字串逐字保留）**

## 設計 checklist（spec 前必過）

- **judge 盤點（藍圖裁定 2026-07-02，R2 desync 教訓）**：統一/新增一個概念的判斷器時，**必須盤點並退役/收編所有既存 judge，不並存**。新系統上線前問：「這概念已有 judge 嗎？」（首燒統一 intent 菜單只加新 judge 沒退役 `derive_archetype` → 兩判斷器讀同 values 48% 分類矛盾。矩陣抓結構 fork、抓不到語意重複——兩公式判同概念要 runtime measure 才現形。）

- ★★★**檢索義務（雙向引用紀律，2026-08-26 立、blueprint 核）**——**立案／寫 spec 前，兩個方向都要查**：
  | 方向 | 做法 | ★沒做會怎樣 |
  |---|---|---|
  | ①**別引到死的** | 引用 `known_issues`／`progress` 的數字前，查它有沒有被推翻：★**`grep -n RETRACTED docs/known_issues.md`**（★★**錨用 ASCII，不要把 `⛔` 打進 pattern —— emoji 在 shell pattern 裡不一定活得下來，會回 `0` 而那是【假警報】；血證 2026-08-27**）＋**查到那行順便往下掃幾行**（找「訂正／推翻／削弱／誠實 NULL」與日期） | ★**做錯事**（有症狀：做完發現不對） |
  | ★②**別漏掉活的** | `grep` 主題關鍵字掃 `progress.md`＋`known_issues.md`，把命中的**【日期＋結論＋當時排的修法類別】**列進 spec 背景段 | ★★**重做已經做過的事**——★★★**沒有症狀**，除非有人去查 |
  ★★**而同一條 pin 可以【同時】犯兩種錯** —— **查到之後要分開看**：
  **【它指的位置】**（常常還活著）vs **【它附帶的宣稱】**（可能已死）。
  ★**血證**：`progress.md:1112` 的 `rank_scored 93.7%` —— **位置保住，而它掛的成長律（N²）被四顆探針否證。**
  ⇒ ★★**整條照抄 ＝ 連死的那半一起繼承；完全不查 ＝ 重走三個月前走過的路。**
  ★**目的不是照做，是知道自己在重複什麼。**

> ★詳 → `detail/01_architect-cases.md`

## ★★★`LOCKED` 的界線：**鎖的是【裁決】，不是【事實】**（systems 立 ／ blueprint WHAT 側背書 2026-08-27）
> ★★**「LOCKED 鎖的是裁決，事實歸量測管。訂正事實不碰設計 ＝ 不算解鎖；碰設計才回 blueprint ＋ 用戶。」**（blueprint 原話）

★**為什麼需要這條**：**LOCKED spec 正好是最常被當成權威來引用的那種文件** ——
★★**若「LOCKED」被讀成「連引用的事實也不能動」，它就會變成【死事實的永久居所】。**
★**血證 2026-08-27**：時間重錨 LOCKED spec §2 引用 `k≈2.004（O(N²)）`，**而四組數字沒有一組支持 N²**。

### ⇒ 訂正 LOCKED spec 裡的【事實】：准，但四件必須齊備
```
(a) 保留原文刪節線            (b) 指出推翻它的證據（哪一天、哪張床、什麼數字）
(c) 指出【該引哪一段】         ★(d) 明標【設計未動、哪幾節一字未改】   ←★★LOCKED 專屬
```
★★★**(d) 的理由**：**沒有它，下一個人分不出「有人偷改了設計」與「有人訂正了事實」。**
★**同型第二例（同日）**：**S0 的【取得方法】跑不了（要比的東西不存在）⇒ 改方法、判準一字未動 ⇒ 一樣走這四件。**
⇒ ★★**分界線**：**判準／裁決／slice 分解 ＝ 設計（碰它要回上游）；引用的數字、取得方法 ＝ 事實與 HOW（自己訂正並報備）。**



## ★★★三母題（2026-08-26 收攏：24 節壓成 3 節）

★**收攏理由**：原本這裡是 24 個各自一行的標題＋24 個「詳見 detail」——
★★**24 個入口等於沒有入口**：開場讀不完，就等於規則存在但不會被用到。
★★★**血證都在 `detail/01_architect-cases.md`（同標題節），標題字串逐字保留，查得到。**

---

### 母題A ── **母體與判準**：你到底在數什麼、拿什麼跟什麼比

| 一句話 | detail 節標題 |
|---|---|
| ★★★**驗「搬家有沒有掉東西」時，母體是【搬家前的全文】，不是【搬家後的標題清單】**（標題在 ≠ 內容在；★★而 `detail` 檔有 4× 重複副本 ⇒ `grep -c 標題` 在那裡是【恆真式】，還要問「在哪一份」） | ★驗【標題命中】≠ 驗【內容命中】（2026-08-27） |
| ★**「窮盡」保證覆蓋率，不保證判準正確**（掃了 29 個 config、判準本身是錯的） | 「窮盡」保證覆蓋率，**不保證判準正確** |
| ★**問真相源要問「它涵蓋哪個【物理】」，不是「它是不是【權威】」** | 問真相源要問「它涵蓋哪個【物理】」 |
| ★**出處分類消滅【碰撞】，但取代不了【語意判斷】**（兩層，互不替代） | 訂正我自己：**出處分類消滅【碰撞】** |
| ★**列舉要挑收斂的那一軸，而「收斂」＝這集合會不會因我們多做事而長大** | 列舉要挑【收斂】的那一軸 |
| ★**手工分類表無法避免時：允許表，但必須配【機械 falsifier】** | 手工分類表無法避免時 |
| **證據的【維度】要對上主張的【維度】**（不含時間的等式證不了時間性主張） | 證據的**維度**要對上主張的**維度** |
| **引用站點用語意錨，不用行號**（行號跨輪不可靠） | 引用站點用**語意錨** |

---

### 母題B ── **閘與儀器**：它會不會 fire、它是不是在說謊

| 一句話 | detail 節標題 |
|---|---|
| ★★**造一個新機制先問「它會 fire 嗎」——死水兩欄要【前移】** | 造一個新機制時，**先問「它會 fire 嗎」** |
| ★★**恆假式：永遠紅的閘 ＝ 沒有閘**（跟恆真式一樣零資訊） | 恆假式：**永遠紅的閘 ＝ 沒有閘** |
| ★**有資格否決一張票的量測，必須擋在動工前**（不能寫成事後驗收欄） | 前置量測 vs 事後驗收欄 |
| ★**`seam-gate` 綠 ≠ acceptance 過** | `seam-gate` 綠 **≠** acceptance 過 |
| ★**「假設不靜默」的完整形狀 ＝ 偵測 ＋ 主動告知**（只偵測不告知＝沒做） | 「假設不靜默」的完整形狀 |
| ★**`tier` 的判準是「會不會改變世界行為」，不是「工程大小」** | `tier` 的判準 |
| **觀測路徑與決策路徑的失敗處置相反**（觀測失敗要吵，決策失敗要退讓） | 觀測路徑與決策路徑的**失敗處置相反** |
| **移除一個過嚴的閘不是零風險操作**（它可能正擋著別的東西） | 「移除一個過嚴的閘」**不是零風險操作** |
| **修 bug 後擺動太大（652→0）要當心修過頭** | 修 bug 後 `652 → 0` |
| **一條被完整交叉驗證的儀器鏈長什麼樣**（罕見，當正面樣板） | 另記：一條**被完整交叉驗證**的儀器鏈 |
| ★★★**一個判準可以【因為錯的理由】而綠** —— 數字對 ≠ 涵蓋範圍對（判準寫死檔名，剛好也＝0，但下一個長在別處的抓不到） | ★2026-08-26 `state`-default 票（本節） |

---

### ★★★驗收與儀器的【界限】四條（2026-08-26 同日立，★血證全文 → `detail/01_architect-cases.md`）

★**它們的共同形狀**：**判準看起來對、工具看起來綠，而它量的東西不是你以為的那個。**

| 條 | 一句話 | 最危險的地方 |
|---|---|---|
| ★**`fp` 該不該變** | 由**「這次改動會不會改到任何一次呼叫的【實際引數】」**決定，**不由 `tier` 決定** | ★同一天同一條鏈上我把它**搞反過一次**（一張要求變＝不可達、一張必須不變），而兩張 `tier` 都是 behavior |
| ★★**因果下游 ≠ 可達** | 判準指對方向，**不保證那個母體現在存在**；★**處置是【分兩層】不是延後**（fixture 給即時驗證、organic 掛到母體出現那天） | ★★同一條 arc 上絆兩次，形狀相同：**判準對、母體空** |
| ★★★**對帳式的界限** | 它證明**母體內部無漏**，**不證明母體本身完整** ⇒ **立完要再問「有沒有東西【在進入這個母體之前】就消失了」** | ★★★**母體被削之後，對帳式不但不會紅，它還會【更乾淨】** —— 一個「完美對帳」的儀器，可能正因為沒看到那些東西才完美 |
| ★**工具狀態偽裝成回歸** | merge 帶進新 `class_name` 檔 ⇒ **驗閘前必跑 `--import`**（否則 `not declared` ＋ 由它衍生的 `isn't a constant expression`，整份 compile 失敗） | ★**`test-ran-floor` 現在會自己講這個成因** —— ★★**觀察到症狀的工具，應該把最常見的成因講出來** |

★**而這四條全部靠一個東西才被看見**：**`Q1「有沒有跑完」**。
★★**沒有它，一個沒跑的測試套長得跟一個全綠的測試套一模一樣**（baseline 全部 stale ⇒ 讀起來像世界變好了）。

### 母題C ── **前提與框架**：在檢查結論之前，先檢查問題本身

| 一句話 | detail 節標題 |
|---|---|
| ★★**不只結論會建立在錯誤前提上，「問題怎麼問」也會** | 不只結論會建立在錯誤前提上 |
| ★★**互斥證據 ＝ 框架訊號**：兩邊都有證據且互斥時，先懷疑問題問錯了 | 互斥證據 ＝ 框架訊號 |
| ★**被推翻的猜測要明確作廢**，否則會變成下一輪的隱性前提 | 被推翻的猜測要**明確作廢** |
| ★**事件的身分要在【事件開始時】凍結，不能在【偵測到時】反查**（偵測總是晚於發生） | 事件的身分要在**事件開始時**凍結 |
| ★**決策層與仲裁層是兩個互不相通的閘**（別假設它們相通） | 決策層與仲裁層是**兩個互不相通的閘** |
| **驗證的價值不在推翻，在於【知道】**（驗完沒推翻，也是收穫） | 而這次驗證的價值 |

---

## ★★★spec 紀律三條（2026-08-25 從 `invariants.md` 搬入並壓縮）
**原本 176 行躲在一個叫「寫 spec 掛上決策層前…」的標題底下，★而那個標題只講了其中第一條。**
★**搬家理由**：**它們是【我們怎麼做事】，不是【世界怎麼運作】** —— ★★**放在 `invariants.md` 等於沒人在對的時機讀到。**

> ★詳 → `detail/01_architect-cases.md`

## ★★★派工/銷案四條（一條一行；★血證與完整判準 → `detail/01_architect-cases.md` 同標題節）

> ★**為什麼擠成一張表**：四條各自是對的，而**四個標題＝四個入口**；systems 開場合計曾到 647 行（上限 600）
> ⇒ ★★**規則存在但讀不完 ＝ 不會被用到。搬走的只有【血證】，規則本文一行沒少。**

| # | 規則（★這一行就是本體） |
|---|---|
| **1** | ★**銷案也要對帳** ——【自述不算證據】：任何「已修/已關」都要附**可獨立重跑的那一格**（file:line 或床的輸出），★★不是「我做完了」。 |
| **2** | ★**派量測/盤點票之前，自己先 `grep known_issues`** ——★★票裡要附「**已 grep：<結果>**」；★★★**搜索義務是雙目標**（`known_issues` ＋ `archive/resolved_issues.md`），否則會重派一個 7 週前就修好的東西。 |
| **3** | ★**派盤點票時附一顆【已知命中】當校準物** ——★★**沒有校準物的盤點，回來的 0 分不出「真的沒有」與「找法不對」。** |
| **8** | ★★★**「報下一站」≠「報 implementer」**（blueprint 立 2026-09-03）：**merge 類出貨的【下一站】包含 blueprint** ——★**裁過 scope 的票尤其**（他手上有一份「在飛」的帳，而那份帳只有你能結）。★★**操作判準**：問「**這件事改變了誰手上的帳？**」——★★★**改變了 scope／凍結條件／優先序的，就必須回報裁它的人**，而不是只回報執行它的人。★**血證 2026-09-03**：兩張他裁過 scope 的票都已 merge，而我整條線只報 implementer ⇒ 他的 watchdog 報「main 靜 1h14m ＋ 信箱空」，**而那是真的：我在做事，只是沒往那一站送。** |
| **7** | ★★★**任何新清單【born-with 過期紅】**（blueprint 立 2026-09-02）：新開一份要人維護的清單（白名單／manifest／豁免表／baseline）時，**每個條目必須帶【owner ＋ 到期或觸發條件】，而【過期的條目本身要紅】** ——★**不是「有條目就綠」**。★★**理由**：沒有這條，清單會變成【加一行就綠】的橡皮圖章，★★★**而那比沒有清單更糟：它【看起來像被管住了】**。★**血證（同一天三種腐爛）**：`known_issues` 條目 stale 到指向兩個月前的現場／憲法 baseline 一行兩義（「判過合法」與「已知漏、暫緩【★非裁定：此處是【規則說明中的舉例】】」長得一樣）／`faction_ai:494` 註解說 3 站而實際 4 站、設的只有 2 站。 |
| **6** | ★★★**一路用 cherry-pick 把東西搬進 main ＝ 放棄所有【以 commit 為單位】的對帳工具**——★**唯一可信的是【樹比對】**（`git diff main <branch> -- scripts/…`）。★★**而 commit 層工具【兩個方向都會騙】**：`HEAD..branch`／`git cherry` 給假的「沒落地」（114／72，實際 20 檔）、`git branch --contains` 給假的「沒落地」而內容其實全在。★★★**對帳的 scope 要涵蓋【所有會被引用的路徑】** —— 血證：我只掃 `scripts/simulation`＋`scripts/data`，而 `scripts/debug` 差 **266 行（比 production 還大）**，裡面躺著一個我幾小時前要求、對方做完、而 main 沒有的修改。★★血證 2026-09-02：同一個問題三個工具三個答案——`HEAD..branch` ＝ 114（cherry-pick 不留 ancestry）／`git cherry` ＝ 72（手動解過衝突就變 patch-id）／**樹比對 ＝ 20 檔**。★★★而我搬了十幾顆，**從沒做過一次樹比對** ⇒ 期間有一顆純註解 commit（`MUTATES` 標記）**從沒進 main** 而沒有人發現。⇒ **merge-gate 已加 `tree-div`（★只印不判：branch 永遠有合法 WIP，硬閘會永久紅）。** |
| **5** | ★★★**dump／量測票必須指定【母體】**（team ＋ tick 窗），不得只寫「對那 N 次 dump」。★**血證 2026-09-02**：我開的 dump 票沒寫母體 ⇒ 回來的三筆是**健康隊、開局兩天內**（`pop 8-9`／`runway 35-40 天`／`famine 0`），而要問的是 `pop=2` 瀕死隊在 `tick≈52798`；★★**執行方一筆不差，是票錯**。★★★而「**分子分母同一時刻同一母體**」是我自己立的判準——**拿去要求別人，開票時自己沒寫**。 |
| **4** | ★★★**A 級條目開票前一律先 code 複驗**（12 條已驗 6 條、★**5 條 stale**）：①符號還在不在 ②**錨對不對**（行號會漂，用 `檔::符號`）③斷言還成不成立（★特別是負斷言）④★★**分清「靜態可驗」與「要跑才知道」**——行為病靜態查不出來 ⇒ 標 `狀態：未確認`＋回訪：量測窗，★★★**不要寫成【已知未修】送去開票**。★**兩種 stale 都要查**：內容 stale（病被別的刀修掉了）／**錨 stale**（病還在但門牌指錯 ⇒ 照錨去看會看到「什麼都沒有」，而那看起來像已經修好）。 |


## ★★★三條機械規矩（★2026-09-06 收攏：三節 70 行壓成一張表 —— ★★N 個殘骸＝N 個入口）

| 一句話（★操作性的部分留在這裡，照著做不用翻） | detail 節標題（逐字） |
|---|---|
| ★**merge 一律在【暫時短路徑 worktree】做**；★★**閘在 `push` 前跑**（`commit` 那一步可能不是你按的）；★★★**主 dir 的 `commit` 一律帶 pathspec**（`git commit -- <明列檔名>`）——危險的是【裸 `git commit` 吃整個 index】，不是 `git add`。新檔要先 `git add` 再 pathspec commit。**被收走了不重寫歷史，補一顆說明 commit。** | ★★★merge 不在共用 main dir 做（systems 立 2026-09-05，血證同日） |
| ★**每寫一條驗收判準就問一次**：「把這個機制關掉（rate=0／旗標關掉／函式回傳空），**這一條會不會還是綠的**？」★★會 ⇒ 它不是在驗這個機制。**至少要有一條「機制關掉就會紅」**（量本身非 0 **且**與其他數對得上）。★★★**守恆式天生對「有沒有做事」不敏感——零流量也守恆。** | ★★★驗收判準要有【鑑別力】：問「這條在機制沒生效時會不會也成立」（systems 立 2026-09-05，implementer 揭） |
| ★**真 detach 的代價是【沒有完成通知】** ⇒ **launch 之後【同一個回合】就 arm 一個盯結果的 Monitor**（「我會記得」在同型第三次後已被證偽）。★★**過濾器要同時涵蓋 PASS 與 FAIL** —— 只盯 PASS 的話，紅了會【安靜地沒有通知】。 | ★★★真 detach 之後，立刻 arm 一個盯結果的 Monitor（blueprint 揭 2026-09-06，同型第三次） |

> ★**全部血證／原文 → `detail/01_architect-cases.md`（同標題節，字串逐字保留）**
