# 儀器騙人：血證目錄（按需讀，**不要一次讀完**）

> ★這份是從 auto-memory `feedback_instrument_lies_three_forms` **原樣搬出來**的血證（2026-09-24，systems）。
> ★★搬的理由：那份 memory 長到 1164 行／42k token ⇒ **它已經無法被讀進上下文** ——
>   而一條讀不到的規則，跟一條不存在的規則，在行為上一樣。
> ★★★所以判準與形狀留在 memory（那是每次都該進腦的），**血證搬到這裡**（那是要查的時候才讀的）。
>
> ★用法：**不要背這份清單**。memory 那邊的三層檢查與統一判準才是入口；
>   這裡是「我懷疑自己撞到某一型」的時候，拿關鍵字 grep 進來找那一次是怎麼被抓到的。
> ★★而每一節都保留了【日期 ＋ 是誰抓到的】—— 那兩個欄位是它可查證的部分，別刪。

---


## ★★★同一條紀律有【兩個方向】（implementer 指出 2026-08-26，我沒看到第二個）
「**結論指向【剛才那個改動】時，先查工具**」——
★**我一直把它當【用來懷疑自己】的**（我的結論可能建在壞掉的儀器上）。
★★**它還有第二個方向：【用來不誤傷別人】。**

**血證**：`material-gate-persona` merge 後 headless 全紅
（`Identifier "BuildAfford" not declared` ＋ 由它衍生的 `isn't a constant expression` ⇒ 整份 compile 失敗）。
★**那個紅精確地指著 implementer 剛交的東西** —— **而它是【我這邊 class 快取沒重建】。**
**跑完 `--import`：`7 vs 7 PASS`，code 一行沒改。**
⇒ ★★★**若我照報，他會回頭去改一個沒有壞的 `const`，而一個正確的裁定會被撤掉。**

★**兩個角色拿到不同結果的原因值得記**：**他的 worktree 快取有那個 class、main 的沒有**
⇒ **同一份 code、兩個工具狀態、兩個結論。**

## ★★處置：**讓工具自己講成因**（比記住規則可靠）
`test-ran-floor.sh`：Q1=NO 且輸出含 `not declared`／`Compilation failed`
⇒ **直接印「最可能是 class 快取沒重建，跑 `--import` 再來，別把這個紅報成回歸」。**
> ★★★**觀察到症狀的工具，應該把最常見的成因講出來，而不是讓每個人各自重新診斷一次。**

★**而這次真正救場的是 `Q1`（有沒有跑完）**：**沒有它，我看到的會是「baseline 全部 stale」——那讀起來像世界變好了。**
**一個沒跑的測試套，長得跟一個全綠的測試套一模一樣。**

## ★★★「空輸出」有【三種來源】，而它們長得一模一樣（2026-08-26 一天內三種都出現）
| 來源 | 那天的實例 |
|---|---|
| ① **真的沒事** | 反例的位置有預留、而它是空的（戲服假說那顆） |
| ② ★**儀器沒開／讀錯 key** | `Probe.samples.get(舊名, [])` 回空陣列、改名只改生產者 |
| ③ ★★**自己炸了** | `String(Array)` parse error ⇒ **印完標題就結束、exit code 0、報告全空** |
| ④ ★★★**產生了但【傳輸途中被截斷】** | **console 抓到的輸出斷在表頭中間，而落地的量測檔是完整的** ⇒ **只看 console 會以為「表沒印出來」** |
| ⑤ ★★**要跑的東西【不在這條 branch 上】** | probe 檔還留在另一條未 merge 的 branch ⇒ **exit 0、無錯誤、無輸出** |

★★★**三者的畫面完全相同：一份空報告。**
⇒ ★**處置**：**看到「空」的第一件事不是解讀它，是問【它有沒有機會不是空的】** ——
**①要有預留的位置（反例槽）②要有非零對照（讀者端拿到過非零）③要有結尾標記（跑完了嗎）
④★以【落地檔】為準，console 只當進度指示 ⑤★★跑之前先確認【那個檔在這條 branch 上】。**
★★**這五個守衛各擋一種，缺一種就有一種空會被讀成「沒發生」。**
★★★**2026-08-26 一天之內，①③④⑤ 各出現一次，而它們的畫面完全相同：什麼都沒有。**

★**而 ③ 是「工具狀態偽裝成災難」的鏡像**：**那型是【沒壞的東西看起來壞了】，這型是【壞掉的東西看起來沒事】。**
★★**後者更貴，因為沒有人會去查一個看起來沒事的東西。**

## ★★★存量【差分】在 cap-bound 的池子上恆為 0 —— 而它看起來像「沒人碰過」（2026-08-26）
**血證**：8 座老熟林，30 天後 `material` **Δ +0**，據此報「**沒有任何一隊碰得到**」。
★**而池子是 cap-bound、每日 regen 12**（`resource_system.gd:130-134`）
⇒ ★★**只要日採 ≤ 12，隔天就補回滿 ⇒ Δ 恆為 0，而材料【一直在流進隊裡】。**
**同一批格子改用「逐 tick 累加下降量」：累計汲取 217／183／182／153。**

⇒ ★★★**Δ 分不出【沒人採】與【採得比再生慢】，而這兩件的下一步完全相反**
（前者要接線／要人去，後者要加產能或減消耗）。

★**通則**：**要量「有沒有在用」，量【流量】不要量【差分】** ——
**任何有上限或會回補的量（池子／庫存／cap-bound 資源），差分都會把持續的使用讀成零。**
★★**而它的危險在於方向**：**差分為 0 ⇒ 讀起來像「機制沒生效」⇒ 下一步會去加大機制，而真正的問題可能根本不在那裡。**

## ★★★閘的誤報有【複利成本】：它訓練出「先標豁免再說」的反射（2026-08-26，blueprint 命名為警報可信度經濟學）
**事故**：憲法閘攔住全線 push，兩條新命中**都是 tap 裡的比較**
（`Probe.bump("a" if x >= 3 else "b")`）——**替已發生的事命名，不決定任何事。**
★**而閘的 `THRESHOLD_RE` 分不出**：
| | |
|---|---|
| ★**決定的比較** | `if x < THRESHOLD: return false` ⇒ 該抓 |
| ★★**命名已發生之事的比較** | 在 `Probe.` 呼叫裡 ⇒ 不該抓 |

★★★**而我們正在【系統性地】加後者** —— **每一顆診斷 tap 的核心工作就是「把一個出口分類」，而分類就是比較。**
⇒ ★**這不是一次性誤判，是一個【會隨我們做得越好而越常誤報】的閘。**

## ⇒ 真正的成本不是麻煩，是【可信度】
> ★★**誤報會訓練人習慣「先標 `gate-ok` 再說」** —— ★★★**而那正好會讓【真的違憲】那一次也被順手標掉。**
★**同族**：氣囊誤報 ⇒ 人拆掉氣囊。**一個常常喊錯的警報，比沒有警報更危險，因為它同時消耗了【注意力】與【對警報的信任】。**

★**處置兩層**：
①**當下**：★**逐行驗 ＋ git 坐實那幾行是誰加的**（本次坐實兩條都是當天的 tap）——**不要在解鎖壓力下往下亂標。**
②★★**結構**：**修閘本身**（跳過含 `Probe.` 的行），**並留逃生口**：
★★★**若發現有 code 把決策寫在 `Probe.` 的引數裡，那是更嚴重的問題 —— 回報，不要順手跳過。**

## ★★跨行結轉的狀態，必須在所有 early-`continue` 之前【無條件】更新（2026-08-26，閘自身的修法）
閘要「把 `Probe.xxx(...)` 從該行剝掉再測剩下的」，而 `Probe.bump("...%s" % [site,` **換行後續行一個 `Probe.` 字樣都沒有**
⇒ 需要**括號深度結轉**。★**而掃描迴圈有多個 `continue`（非決策檔／`gate-ok` 行／非 dfunc）**：
> ★★★**任何一個跳過都會讓結轉失步，而失步之後【每一行】的剝離結果都是錯的 —— 且不會有症狀。**
⇒ ★**通則：狀態機的更新不能寫在會被 `continue` 略過的位置。**
★**這條是 implementer 用【陽性對照組】抓出自己 v1 的缺陷，不是讀出來的。**

## ★★另：我建議的形狀被下游改對了，而理由是【無症狀漏抓】
我建議「整行含 `Probe.` 就跳過」 ⇒ ★**`if x > SOME_THRESHOLD and Probe.enabled:` 會被整行放行 ⇒ 真門檻漏抓且無症狀。**
改成**剝離後再測**：**剝完沒東西剩 ⇒ 比較本來就在 `Probe.` 引數裡；剝完還命中 ⇒ 決策在觀測之外，照抓。**
⇒ ★**我留的逃生口（決策寫進 `Probe.` 引數＝更嚴重）在這個形狀下自動保留，不用另外寫。**

## ★★★第四形態：**「改動沒效果」與「改動根本不在被跑的那棵樹」長得一模一樣**（2026-08-26 血證）
implementer 為了寄信 `cd` 到 main dir，**後續 `sed`／`python` 的 cwd 就留在 main** ⇒
整批改動寫進 `A:\GDS\demo\scripts\...`，而 godot 跑的是 `--path .worktrees/<slice>` 的**沒改過的** code。
★**症狀＝數字全 0，看起來像「我的接線錯了」** ⇒ ★★**他當下第一個念頭是去改【已經是對的】那份 code。**
⇒ ★★★**與 class 快取那次同族**：**結論指向「剛才那個改動沒用／害的」時，先確認【跑的是不是那份改動】。**
★**便宜的機械防線**：**改完先 `git status`，看那個檔有沒有出現在【該出現的那棵樹】裡，再跑。**
★**處置示範（沒重寫 history、沒碰別人的檔）**：`git diff` 取 patch → `git checkout --` 只還原自己那兩個檔 → `git apply` 進 worktree → 逐項 grep 驗過才重跑。連 [[feedback_concurrent_session_wip_sweep]]。

## ★★★陽性對照要餵【同一支被測邏輯】不同引數 —— 不是另寫一份斷言（2026-08-26，R² 給的形狀）
要證明一個 assert 讀的是**關係**而不是**碰巧寫死了那個數字**：
```gdscript
static func storage_fits(cap: float, cost: float, margin: float) -> bool:
    return cap >= cost * margin          # ★唯一真值
```
**真閘**餵真常數；**對照 A/B/C** 分別餵 `cap*0.5`／`cost*3`／`margin*3`，**三組都必須翻紅**。
★**關鍵**：**三組對照與真閘呼叫同一支函式，只是引數被扭曲** ——
★★**不是「自己另外寫三段紅色斷言去說服自己」，是【同一份判斷邏輯】在三種扭曲輸入下必須翻臉。**
★★★**而它可證偽**：**若函式內部退化成只比 `cap >= cost`（根本沒用上 `margin`），對照 C 就不會紅** ⇒ **對照抓得到退化實作。**
★**與「拿掉註解仍 PASS」那型的分工**：那型驗**文件與機制有沒有脫鉤**；這型驗**斷言讀的是關係還是巧合**。**兩個問題不同，別互相代用。**
★**同族紀律**：fixture 必須呼叫**真正的 production 函式**，不得自己重寫一份邏輯再斷言。

## ★★★「這個改動會讓 `fp` 變」是一個【關於某一張床】的斷言，不是關於改動的斷言（2026-08-26）
我 spec 寫死：「倉容改 ＝ 世界不同 ⇒ `fp` 兩床都會變」，並據此把回歸防線從逐位元降級成守恆帳。
★**實測 `fp` 逐位元【不變】** —— 因為 `a4_determinism_check` 跑的是 `warring_states`，
★★**而那張床上這個 cap 根本不 binding（沒有任何公庫撞到 200）⇒ 抬到 250 對世界零影響。**
⇒ ★★★**「預期會變而沒變」看起來像【回歸防線失效】，實際上是防線【根本沒被觸及】。**
★**寫 `fp` 預期時要連床一起寫**：「在 `<哪張床>` 上會不會變」，**因為 `fp` 只測一張床。**

## ⇒ ★★而 implementer 的處置是對的：**沒有去找一個能讓 `fp` 變的跑法**
> ★★★**「我沒有因為它該變卻沒變，就去找一個能讓它變的跑法 —— 那會是把儀器調到符合預期。」**
★**收下當紀律**：**預測與實測不符時，先解釋差異，不要改跑法去追預測。**（同族：低 util 先問是否 genuine，不要 crank。）

### ⇒ ★★★★再前面一層：**還要連「機制有沒有 fire」一起寫**（同日，下一票）
階梯溶解那票改動不小（刪掉一整條迴圈、新增第三個出口），★**而 `fp` 逐位元不變** ——
**因為 `ok_upgrade` 從未 fire ⇒ 世界一模一樣。**
⇒ ★★**一個從未 fire 的機制，不管改得多大，`fp` 都不會動。**
★**所以 `fp` 預期的完整寫法是**：**「在 `<哪張床>` 上、若 `<哪個機制>` 真的 fire，`fp` 會變」** —— **兩個條件缺一都會誤判防線。**

## ★★★搬走一個分母時，要在原地留一張【改道通知】（2026-08-26）
階梯溶解後 `upg.eval_entry / own_tile_seen / upg.call` **全部變 0**。
★**那不是「升級路徑又斷了」，是分母搬進了 `_pick_facility`** —— **而兩者印出來長得一模一樣。**
⇒ implementer 在該段直接印：**「本段全 0 是預期的，升級已變成 `_pick_facility` 的第三個出口 `ok_upgrade`，見下表」。**
★★**這正是我們一路在防的「0 分不出沒發生與沒接上」，只是這次它出現在【自己剛剛搬走的分母】上。**
★★★**通則：改掉一個 counter 的語意／位置時，舊位置要留下能自我解釋的痕跡，不能只留一個 0。**

## ★fixture 不得手抄 schema（欄位名／常數名）
第一版手抄 `workshop_level`（**那個欄位根本不存在，當場崩**）⇒ 改讀 `FACILITY_DEF[f].current_level_key`。
★★**手抄 schema 的 fixture，日後改名／新增時會 stale 成【假綠】** —— 同 [[feedback_no_handcopied_physics]] 家族。

## ★權限判斷放【呼叫端】還是【函式內】，會因為呼叫端傳的是誰而產生不同語意
`_pick_facility` 若自己判 `outpost_owner == team.team_id`：★**`lord_scan` 傳的是 `owner_team`** ⇒ **憑空多出「屬下升級自己的據點」。**
⇒ 改成 `allow_upgrade` **由呼叫端決定**（`lord_scan` 傳 `owner_team.team_id == leader_team.team_id`）。
★★**通則：共用函式裡的「我有沒有資格」判斷，要看每個呼叫端把誰當成「我」。**

## ★★★長跑被砍＝輸出歸零 ⇒ 改成【事件發生當下就落地＋flush】（2026-08-26，measurer 的方法突破）
前兩次長窗（30 分鐘／1 小時）被外部 timeout 砍掉，**輸出全部歸零**。
★**第三次改成「每次 spike 發生就當場寫檔 ＋ flush」** ⇒ **跑到 53.3% 被砍，而 387 筆【全部保住】。**
⇒ ★★**通則：長跑的產出不要等到結尾才寫** —— **被砍是常態，而「跑完才落地」讓每一次被砍都等於零。**
★★★**而它同時解決「空輸出五種來源」裡最難分的那一種**：**「還沒跑完」與「跑完了但沒東西」** ——
**事件即時落地之後，磁碟上的筆數本身就是進度條。**
★**代價**：**未完成聲明要寫死**（本次：`tick 3839/7200 = 53.3%`，後半段完全沒量到）。

## ★★★★而第③形態（工具狀態偽裝成災難）今天以【假零】現身，而且【我提供了那個錯誤解釋】（2026-08-27）
merge 完錯峰後，**我沒跑 `--import`**（`cadence_stagger.gd` 是新的 `class_name`，CLAUDE.md 明寫必跑）
⇒ ★**`CadenceStagger` 未宣告 ⇒ 整個 `faction_ai_system.gd` 解析失敗 ⇒ 跑出「市場活動 ＝ 0」。**
★★**而我在【前一封信裡】剛好寫過**：「**若撮合路徑在那床走不到，那『頻率≈0』不是答案，是【床選錯了】**」
⇒ ★★★**我等於給了下游一個現成的、聽起來很合理的錯誤解釋 —— 一個【停止追查的藉口】。**
★**measurer 沒有照著走，他去查了工具狀態並跑 `--import`。**

⇒ ★★★★**兩條**：
①**merge 含新 `class_name` 檔 ⇒ 【我】必須當場跑 `--import`** —— **這是 merge 站的責任，不是下游的。**
②★★**給下游「可能的解釋」時，要同時給「先排除工具狀態」** ——
**否則那個解釋會變成停止追查的地方，而它剛好是最常見的假訊號來源。**
★★★**「0」＋「我剛剛提示過的合理原因」＝ 最危險的組合。**

## ★★★★機械檢查的【錨】要用 ASCII —— emoji 打進 grep pattern 會回假 `0`（2026-08-27 血證）
我把快速通道寫成 `grep -n '⛔RETRACTED' docs/known_issues.md` 並落進 `01_architect.md`。
★**今天自己跑它 ⇒ 回 `0`** ⇒ **看起來像「我今天加的兩條 RETRACTED 標記全不見了」**（共用 main dir，那是可信的災難故事）。
★★**真相：檔案裡兩條都在，是【我 pattern 裡的 emoji 在 shell 沒活下來】。**
⇒ ★★★**儀器騙人的第 N 種：檢查【自己】壞掉，而症狀長得像【被檢查的東西】壞掉。**
★**修法**：**emoji 留給人眼，grep 錨一律用 ASCII 關鍵字**（`RETRACTED`）。**已改進 `01_architect.md`。**
★★**通則**：**任何要被機械執行的標記，錨必須是 ASCII；裝飾可以是 emoji，但不能【只有】emoji。**
★★★**而發現它的方式是「結果太乾淨/太災難時再驗一次」** —— **`0` 對一個我剛親手加過東西的檔案，本身就不合理。**

## ★★★★★規則型分類器：**寬規則會【安靜地】吃掉專用規則，而唯一的症狀是「專用規則命中 0」**（2026-08-27）
S1b 的裸 tick 分類器：**規則順序即優先序，寬規則排在專用規則前 ⇒ 寬的先命中 `break` ⇒ 專用的永遠輪不到。**
★**表現出來是「已死規則」（命中 0）**，★★**而【清單本身長得完全正常】**：
**143 筆、對帳一致、`NEEDS_HUMAN=0`、每筆都有理由。**
★★★**肉眼審那份清單一輩子看不到這個病 —— 因為被吃掉的那筆有一個【看起來很合理的理由】**
（`sentinel_default：get(k,0) 的預設值是哨兵` —— 對那一行是真的，只是不是全部）。
⇒ ★★★★**「已死規則（命中 0）」必須當成【訊號】不是雜訊 —— 它是唯一能看見遮蔽的東西。**
★**而它抓到的不是小事**：**`player_trade_system.gd:103` 的 `current_tick - 1000` 是真裸 tick，
被判成「不是時間量」⇒ 頭條結論「(a) ＝ 0」是錯的（真值 1）。**
★★**防線（我加的第三條）**：**每條規則印出【它命中幾筆】** —— **0 ⇒ 被遮蔽；異常大 ⇒ 太寬。**

## ★★★★而「一句真話可以證明錯的事」（同票，implementer 自己揭）
那顆改動的 caller **全是 player-facing**（`grep` 窮盡：3 處），★**headless 世界迴圈根本不驅動它。**
> ★★**「我本來可以直接寫『`fp` 逐位元不變 ⇒ 等價』—— 那句話會是真的，但它證的不是這件事。」**
⇒ ★★★**`fp` 不變是真的，但它證的是「沒有別的東西被改壞」，不是「這顆等價」。**
★**通則：證據要對得上【命題】—— 「它是真的」不等於「它證得了」。**
★★**他改用【算術】證等價**（`hours(100) = 100*240/24 = 1000`，整數除法無餘 ⇒ 逐值相同）—— **那才是對得上的證據。**

## ★★「不需要改」與「不是時間量」是兩個【不同的類】，而混掉的方向要看
`50 * TICKS_PER_HOUR` **是時間量**，只是**已由具名常數導出、會隨根自動縮放** ⇒ **(c) 不需要改。**
★**被誤判成 (d) 不是時間量** ⇒ ★★**而 (d) 的性質是「被判進去的就永遠不再被人看到」。**
⇒ ★★★**混類別時要問：【混掉的方向往哪倒】—— 往「不再被看到」倒的那個方向，錯誤會永久沉底。**

## ★★★★稽核指令要排除【註解行】—— 否則「修好了」與「在註解裡提到」長得一樣（2026-08-27，我自己造的陷阱）
修完兩支 hook 後跑窮盡稽核 `grep -q 'show-toplevel'` ⇒ ★**它們【仍然】命中。**
★★**原因：我在修法上面寫了解釋註解，而註解裡提到了 `--show-toplevel` 這個字串。**
⇒ ★★★**稽核對「真的在用」與「註解裡提到」不分 —— 而我剛好是那個把字串寫進註解的人。**
★**修法**：**先剝註解再測**（`grep -vE '^\s*#' "$f" | grep -q …`），同憲法閘「剝掉觀測呼叫再測那一行」的形狀。

## ★★★而修完之後的第二個發現：**不是所有 hook 都該指 main**
```
★信箱類（handbacks）⇒ 必須指 main —— ★★信箱只有一個，worktree 裡是空的
★★code/doc 掃描類     ⇒ 應該指 worktree —— ★★★你要掃的是【你正在改的那份】
```
⇒ ★**修法不是「全部換成 `--git-common-dir`」，是【按資源歸屬選】。**
★★**沒講清楚，下一個人會「順手修好」那 7 支掃描器，然後它們開始掃錯的樹。**
★**要修的集合 ＝「真用法 ∩ 碰 handbacks」**（本次修完為空）。

## ★★附一：`--git-common-dir` 是那把鑰匙
`--show-toplevel` 在 worktree 回 worktree 根；`--git-dir` 回 worktree 私有的 `.git`。
★**只有 `--git-common-dir` 回 main 的 `.git`，其父目錄＝main 工作樹根，★★而它在 main 裡跑也同樣正確。**
（陽性對照：worktree 與 main 各跑一次，都解到 `/a/GDS/demo`。）

## ★★★★附二：**手動跑 hook 要餵 stdin，否則 `cat` 阻塞會【看起來像我的修法把它弄掛了】**
`IN=$(cat 2>/dev/null || echo '{}')` —— ★**沒有管線時它會等 stdin ⇒ 我的手動呼叫掛了 2 分鐘。**
★★**而我當下的第一個念頭是「我剛改的東西把 Stop hook 弄掛了」** —— **那是同一天第三次「工具狀態偽裝成災難」。**
⇒ ★★★**驗 hook 一律 `echo '{}' | SESSION_ROLE=<role> timeout 20 bash <hook>`。**

## ★★★★第四次（implementer 自揭，同日）：**驗證指令的 cwd 被【前一條指令的 `cd`】帶走**
他要驗「在 worktree 裡跑」，★**而 Bash 工具的 working directory 在呼叫之間【持續】** ⇒
★★**他的驗證其實在 main 跑，差點報成「worktree 驗過了」。**
⇒ ★★★**通則：任何依賴 cwd 的驗證，`cd` 必須寫在【同一條指令裡】，不能依賴前一條。**
★**而他順手把驗證擴大到 4 個 cwd 位置（根／子目錄／深子目錄／worktree）＋
`--git-common-dir` 的 4 種回傳形態（相對 `.git`／`../.git`／`../../.git`／絕對路徑）** ——
★★**`dirname` + `cd` 四種都吃得下。我只驗了 2 個位置。**
★★★**同日四次「工具狀態偽裝成災難／偽裝成成功」，四次分屬四個人／四種形態 —— 這不是巧合，是這個環境的常態。**

## ★★★★★百分比會騙人：**報比率必附【絕對數】**（2026-08-27，它當場殺掉我自己的故事）
`noop_no_outpost −19.4%` **聽起來像主戲** ⇒ ★**實際是 `36 → 29`，只有 7 次。**
★★**而真正的主戲是 `no_facility 318→382 (+64)` 與 `no_material 265→211 (−54)` 的【對調】。**
⇒ ★★★**我拿三個百分比就編出了一條因果鏈** —— **改要絕對數之後，那條鏈當場斷。**
★**通則**：**要求交件時「只報絕對數，不要百分比」是一個【便宜且有效】的反制** ——
★★**百分比抹掉量級，而量級正是「這桶撐不撐得起那個變化」的唯一依據。**（同族：`報總數必附集中度`。）

## ★★★★「自洽」不是健康的證據（2026-08-27，implementer 自述）
> **「這次沒有矛盾可抓 —— 數字自洽、床照常結束、只是量錯了東西。」**
★**前兩次工具騙人都是靠【矛盾】抓到的**（量 0 而次數 107／母體數沒變）——★★**這次沒有矛盾。**
⇒ ★★★**一份自洽的報告可以整份都在量錯的東西**；**自洽只證明「內部一致」，不證明「量的是那個東西」。**

## ★★★同一個零可以有【兩個成因疊著】，各解釋一半（2026-08-27）
`GOAL_CHECK` 零資料 ＝ ①**far pass 不整除**（`4320 mod 600 = 120` ⇒ 永不命中）＋②**near 隊列恆空**（觀察者床 `player_pos=(-1,-1)` ⇒ 全隊皆 far）。
★**只找到其中一個就收手 ⇒ 得到一個【自洽但不完整】的解釋。**
★★**而更深一層**：**LOD 的 `trials` 是【次數補償】，補不了【相位】** ⇒ ★★★**任何 `% tick` 的相位型 gate 都逃過既有補償，且無症狀。**

## ★★★第④形態：**編輯工具靜默腐蝕【它正在寫的那支守衛】**（2026-09-02，同日三次）

前三種是「量測工具騙人」。第四種是**寫工具的工具騙人** —— ★**而症狀是【守衛安靜地失去鑑別力】，不是報錯。**

| 事故 | 我寫的 | 落地變成 | 後果 |
|---|---|---|---|
| ① | `sed` 用 `\1` backreference | **0x01 控制字元** | 抽不出 trailer ⇒ 迴圈跑 0 次 ⇒ self-test 誤判 |
| ② | `${line%$'\r'}` | **真 CR 字元** | CRLF 沒去掉 ⇒ **138 條舊條目全被當成新條目** |
| ③ | `python .replace()` **沒 assert** | **靜默 no-op** | 白名單暫存檔沒生成 ⇒ awk 讀不到 ⇒ **5 處全部誤報** |

★**共同機制**：**python/sed 的跳脫層**與**shell 的跳脫層**疊在一起，
★★**而錯誤的結果仍然是一支「跑得起來、exit 0、印得出結論」的腳本。**

★★★**規則**：
1. **寫 shell 檔用 quoted heredoc（`<<'EOF'`）整檔重寫**，★**不要用 python 字串取代去修含跳脫序列的行**。
2. **每一次程式化編輯都要有 assert（python）或落地後 `grep -c` 驗**——★★**沒 assert 的 replace 是靜默 no-op**。
3. **閘改完必跑陽性對照**，★★★**而且陽性對照要能在【預設參數】下跑到**
   （血證：白名單那顆事故 commit 不在預設 40 顆視窗內 ⇒ 平常那次跑其實沒有對照 ⇒ 加 self-test）。
4. ★**`grep -Fx` 比對含 emoji（4-byte UTF-8）的行會失敗** ⇒ 閘裡一律 `export LC_ALL=C` 做 byte-exact。

相關：[[feedback_enumerate_engine_owned_axis]]（同族：閘改完必跑陽性對照）、[[feedback_nobody_owns_shrinking]]（同族：失效是靜默的）

### ★★★第④形態的一個具體慣犯：**用 `index("### ")` 找區段結束點**（2026-09-02 同日犯兩次）

```python
i = s.index(要刪的區段標題)
j = s.index("### ", i+10)      # ★★★錯：只找【同層級】
s = s[:i] + new + s[j:]        # ⇒ 夾在中間的 `## ` 大標題連同內文一起被吃掉
```
★**血證**：`known_issues.md` 的「means-end binding root」9 行區段，**同一天被我用同一個手法刪掉兩次** ——
★★**第一次我復原了、還寫進 commit message**，★★★**而我只改了【結果】沒改【方法】，兩小時後又犯。**

⇒ **改法（方法層，不是這一次的修補）**：
1. 結束點用 **`re.search(r'\n#{1,6} ', s[i+5:])`** ——★**任何層級**的下一個標題。
2. ★★**每次區段級編輯後，比對【標題全集】**：`diff <(git show <前一顆>:f | grep -E '^#{1,6} ' | sort) <(grep -E '^#{1,6} ' f | sort)`
   —— ★★★**只看「我剛好注意到的那一處」抓不到這個錯**（第一次就是別人的 grep 讓我發現的）。
3. **能用 `replace(anchor, new+anchor)` 就不要用切片** —— 插入不會刪東西，切片會。

## ★★★★★第五形態：**跨 run 不重置的 static ⇒ 同 process 第二輪【靜默少計】，而第一輪是對的**（2026-09-03）
```
scripts/simulation/decision/goal_resolver.gd:492   static var _fall_seen: Dictionary = {}
:532-534  if not _fall_seen.has(_dk): _fall_seen[_dk] = true; Probe.bump("goal.res_fall_distinct.%s" % res)
⇒ ★`Probe.reset()` 清 counter，★★而 static 跨 run 不清
⇒ 第二次跑同 seed ⇒ 同樣的三元組已在 dict 裡 ⇒ ★★★一次都不 bump
```
★**毒性在【局部正確】**：★★**一份輸出裡有正確的部分，於是沒有任何一格看起來壞掉** ——
★★★**它不會紅，只會少**，除非有人剛好比對兩次（`observability_path_test` 就是那個人，而它紅了很久沒人讀）。
⇒ **偵測法（今天奏效的那一手）**：**把兩次跑的順序【對調】** ——
★**差異跟著【誰先跑】換邊 ⇒ 是 state 洩漏；跟著【開關】走 ⇒ 才是儀器污染。**
★★**同一手也洗清了 tracer**（world signature 雙向 byte-identical ⇒ 憲法級「觀測不得改變被觀測物」成立）。
★★★**族群大小已量**：production 可變 `static var` **15 個**（常數表／快取／累積狀態混在一起），逐個判「跨 run 會不會被清」的列舉票已派。
相關：[[feedback_observer_no_global_rng]]（同族但那條是儀器【真的】改了世界；本條是儀器沒改世界、只是自己記錯）。

## ★★★第五形態：**有些壞掉【沒有事後補救】，修法只能約束呼叫端**（2026-09-04，CP950 三臂探針）
```
★(a)直接呼叫 wrapper = UTF-8  (b)先印一行 stdout 再呼叫 = CP950  (c)先寫 stderr 再呼叫 = ★UTF-8
⇒ ★★(c) 才是關鍵臂:它把變因從【有沒有多一層】收窄到【有沒有寫過 stdout】
⇒ ★★★成因 = PowerShell host writer 在【第一次 stdout 輸出】就定死編碼
```
★**而 `[Console]::OutputEncoding` 與 `[Console]::SetOut` 兩個都試過、都無效**
（PowerShell 不走 `[Console]::Out`，它有自己的 host pipeline）。
⇒ ★★**修法形狀＝寫成 caller contract（呼叫端約束），不是在 wrapper 裡加一段補救**
⇒ ★★★**一段「看起來在保護、實際無效」的 code 比沒有更糟** —— **它會讓下一個人停止懷疑。**
★**通則**：**排除法要排到【分水嶺】為止**，兩臂只能說「多一層就壞」，第三臂才說得出「是寫過 stdout」。

## ★★★第六形態：**儀器裝好了但【沒有接電】＋ 判準【沒有鑑別力】**（2026-09-05，同日兩件）

### (a) 測試存在，但沒有任何 runner 會跑它
```
★三次同型:headless_test.gd(2026-09-03,註冊表註解自陳「兩顆已 merge 的 slice 弄紅 fixture 時十支全綠」)
   ／belief_freshness_invariant_test.gd(2026-09-05)／unified_commerce_test.gd(2026-09-05,★從來就不在表上)
⇒ ★★「未來有人做錯會自動變紅」在【被註冊之前】是空頭支票:它不會紅,它連跑都不會跑
⇒ ★★★而失效是【靜默】的 —— 一支不會跑的測試,跟一支跑了而且過了的測試,在任何輸出上都長得一樣
```
★**真正的洞不是「忘了註冊」，是【沒有標記能分辨常設不變量與用完即棄的診斷床】**
（`scripts/debug/*_test.gd` 135 支 vs 註冊表點名 6 支 —— 而 135:6 本身不是醜聞，**多數本來就該用完即棄**）。
⇒ **寫反向斷言＝兩個動作**：寫測試 ＋ 往 `docs/process/merge-gates.tsv` 加一行（★expect 要是**斷言**不是**完成標記**）。

### (b) 判準在「機制關掉」時也會綠 —— ★守恆式天生如此
```
★血證:⑤團內稅驗收我列四條,前三條(扣繳後餘額/團庫流出=net/CoinAudit=0)【與稅率無關】
   ⇒ 源扣繳下「團庫流出 ＝ 成員收到」在 rate=0 時恆成立
   ⇒ ★★稅完全沒生效,整套測試照樣全綠;四條裡只有【人格梯度】有鑑別力
⇒ ★★★機械問法:「把機制關掉(rate=0/旗標關/函式回傳空),這條會不會【還是綠的】?」
   會 ⇒ 它在驗別的東西(守恆/型別/沒崩),不是在驗這個機制
```
★**守恆式特別會騙人：它們天生對「有沒有做事」不敏感 —— 零流量也守恆。**
★★至少要有一條「機制關掉就會紅」的：**那個量本身非 0**　**且**　**它與其他數對得上**
（例：扣繳額非 0 ★**而且** `gross − 扣繳 ＝ 成員收到` —— 單看「非 0」還不夠）。

## ★★★第⑤型（2026-09-06，同日三次）：**同一份位元組，兩個工具兩種語意——而【比對】剛好跨在那條縫上**

| 縫 | 兩邊 | 症狀 |
|---|---|---|
| **CRLF** | `pathlib.write_text` 寫 CRLF／`.gitattributes` 把 blob 正規化成 LF | ★**只有工作副本壞，而 diff 看不見** ⇒ 23/23 閘 `no-verdict` 而每支閘都好好的 |
| **`ensure_ascii=True`** | hook 輸出 `零產出`／我 grep 中文 | ★**把一支【正常運作】的 hook 判成「沒響」** —— 陽性對照自己壞了 |
| **MSYS vs Windows python** | `cp`/`grep` 吃 `/a/GDS/demo/...`／`python` 打不開 | ★★**同一支腳本前後兩行，「路徑是對的」同時為真與為假** ⇒ patch 沒套上而實驗照跑 |

> ★★**共同形狀：不是「工具壞了」，是【兩個工具對同一份位元組的語意不同】，
> 而我做的【比對】剛好跨在那條縫上** ⇒ 比對結果反映的是縫，不是被比的東西。
> ★★★**處置：比對前先確認兩邊同編碼／同路徑語意** —— 這條在 2026-08-27 已經記過一次，**今天付了三次息**。

相關：[[feedback_prewritten_verdict_convinces]]（假紅燈被預寫解讀加持）、[[feedback_guard_input_population_never_verified]]

## ★★★規則綁在【載體】上，載體一換就沒套上（implementer 自報 2026-09-07）
```
我早上教的:「被砍斷的 gate log 跟【跑完但有紅】長得一樣,唯一憑據是尾巴那行總結」
★而他當天在【test log】上重新踩了一次:2687 行、SCRIPT ERROR=0、沒有 Parse Error,
   ★★而檔案【結束在模擬 log 的中間】⇒ 「assertion = 0」量的是【還沒跑到】不是【都過了】
⇒ ★★★他的自述:【規則我記得,載體換了就沒套上】
```
★**這與「規矩沒有跟著搬進腳本」是同一個形狀**：**規則被記成【某個場景的規則】，而不是【某個性質的規則】。**
★★**處置：把檢查放在【單一入口】，而不是每個載體各記一次。**
```
本例:godot.ps1 的收尾列【只有正常結束才寫】(被 kill 就沒有)
⇒ ★`.claude/hooks/.godot-runs.log` 有沒有結束列 = 載體無關的完跑證據
⇒ ★★而【不】在 stdout 加標記:多個閘拿輸出逐位元比 fp,多一行會把所有 baseline 一次打掉
```
> ★★★**通則：當一條規則被同一個人在【換一種載體】之後重新踩到，
> 那不是「他忘了」，是【那條規則被綁在載體上】——修法是把它移到單一入口。**

---

## ★★★2026-09-07 ⑦：**我造的陽性對照，是照著【我的偵測器的形狀】造的**

```
分診分類器判紅：grep 'Assertion failed|\[FAIL\]|SCRIPT ERROR'
自檢樣本：      printf 'SCRIPT ERROR: Assertion failed: boom'  ⇒ 期待 red ⇒ 通過
⇒ ★樣本是【偵測器認得的形狀】⇒ 自檢只證明「偵測器認得自己的假設」
⇒ ★★它【永遠】抓不到「偵測器不認得的紅」
```
**實際代價**：131 支床的紅訊號普查 ⇒ `[FAIL]` 方括號式 99 支、★**裸 `FAIL ` 等其他式 31 支（24%）**
⇒ 那 31 支**失敗時會被判 green**（安靜地失敗），而自檢**三向全綠**。

## ★修法：陽性對照要用【真實樣本】，而且要**成對**
```
會紅：從真實床採來的 5 種形狀（`  FAIL  ...`／`HAS FAILURE`／`FAILS=3`／`[FAIL]`／`Assertion failed`）
不會亂紅：4 種通過樣本（`  PASS  ok`／`ALL PASS（fail=0）`…）★缺這半就會做出恆紅
```
★★★**而那個「不得過度匹配」的新自檢，第一件事就是擋下我自己剛寫壞的 regex**
（awk 把 `\[FAIL\]` 吃成字元類 `[FAIL]` ⇒ `PASS` 裡的 `A` 也算紅）——**它有牙。**

## ★★同批的【錯誤結論】要記，因為它差點蓋掉真發現
我先報「29/131（22%）零判準通道＝永遠不會紅」⇒ ★**錯的，真數是 1**。
那 29 支幾乎全是帶編號閘的 TDD 床，判準印成 `print("  FAIL ", m)`（裸 FAIL）
⇒ 我的 grep 要 `\[FAIL\]`/`FAILED` ⇒ 全部誤判 ⇒ **假窮盡形態③（搜「我以為答案長的樣子」）**。
★**我做了「裸符號掃、印總數」，卻在【符號的形狀】上先射箭再畫靶。**
⇒ ★★★**兩個結論的差別**：「22% 的床是裝飾」＝說**世界壞了**；
   「24% 的床能在我的分診裡安靜失敗」＝說**我的儀器壞了**。★後者才是真的。

★真的零判準通道只有 `coin_b_verify_bed.gd`（80 行，首尾各算一次 `CoinAudit.total()` **卻從不比較**）
—— 而那支是**直接讀完全檔**才下的判斷，不是 grep 出來的。★**負斷言要靠讀，不能只靠搜。**

清單：`docs/measurements/2026-09-07-beds-verdict-channel-shapes.txt`

## ★★★⑧（同日）：**正確答案寫在【給人看的字串】裡，不在【能讓閘變紅的地方】**

```gdscript
coin_b_verify_bed.gd:37
  print("[CoinAudit] end total=%.4f delta=%.4f（無鑄幣機制應=0）" % [...])
```
★**期望值就寫在那裡**——「無鑄幣機制應=0」——**而它從不比較**。
同檔 `InvariantAudit.check()` 也一樣：算出 violations，**只 print**。
⇒ 全檔 80 行、`[FAIL]` 0 次、`assert` 0 次 ⇒ **永遠 exit 0**。

★★**這不是「忘了寫判準」，是【判準寫成了給人讀的散文】。**
  ⇒ 它讀起來**比真正的判準還讓人放心**（因為它把正確答案講出來了）。
★★★**辨識法**：看到 print 字串裡有「應該」「必須」「預期」「should」「expected」——
  ★**問下一句：這個「應該」有沒有一個 `if` 在管它？** 沒有 ⇒ 它是散文，不是判準。


---

## ★★★⑧【判決沒有主詞】——它不說自己是關於哪一棵樹的（2026-09-08）

```
merge-gates.sh:10  REG="docs/process/merge-gates.tsv"   ← 讀【工作區】不是 HEAD
全檔對「樹乾不乾淨」零報告；log 開頭零 provenance
⇒ 血證：.merge-gates-last.log 記著 `✓ computed-prop`，
   而 HEAD 上那行 expect 是未跳脫的 `[COMPUTED-PROP] PASS`（grep -qE 當字元類）
   ⇒ ★在 HEAD 上永遠不可能綠；那次的綠是靠一份【未 commit 的修改】跑出來的
⇒ ★★「35/35 全綠」【不可歸因於任何一顆 commit】
⇒ ★★★而連【閘的定義本身】（註冊表/runner）都可能是髒的，而判決長得一模一樣
```
★**修法不是拒絕執行**（改自己的 slice 時本來就該髒），是【標明適用範圍】：
`[TREE] HEAD=<sha> registry=<clean|DIRTY> runner=<clean|DIRTY>` ➕ 一行「本次判決只適用於…」。
★★**通則**：**一個判決必須說出它是【關於什麼】的判決**——
跟【一個數字沒有窗口長度就不是一個數字】是同一條。
★★★而我已經給 `tools/godot.ps1` 加過 `[TREE]`，却沒給【閃的那支】加
—— **修一處、同型另一處沒跟著改**（[[feedback_nobody_owns_shrinking]] 同族）。

## ★★⑧b ④的新載體：**0x0D**（同日兩人各栽一次）
```
檔案裡有【字面 CR】（`${id%$'<CR>'}` —— 修 Windows 換行毒判準那段）
Python 文字模式讀寫 ⇒ universal-newline 把【單獨的 CR】也當行尾轉成 LF
⇒ $'<CR>' 被切成兩行 ⇒ 註解斷掉 ⇒ 未閉合反引號 ⇒ unexpected EOF
★同日 implementer 的版本：註解裡放字面 "0
0"，寫檔時變真換行，同樣切壞檔
⇒ ★★兩次都是靠 `bash -n` 拓到，不是靠 code review
⇒ ★★★修法：改 shell/有控制字元的檔一律【二進位讀寫】+驗 CR 數前後相同，且 `bash -n` 進收尾步驟
```


## ★★★④ 同日三種載體（2026-09-08）—— 而第三種是【不是語法錯】的那種

```
0x01  編輯工具把字面控制字元寫進守衛
0x0D  Python 文字模式讀寫把【單獨的 CR】當行尾 ⇒ $``r'' 被切成兩行 ⇒ unexpected EOF
` (反引號)  python -c "..." 用【雙引號】包，而內容含反引號 ⇒ bash 做了命令替換
              ⇒ ★把兩個【識別字】静默刪掉，而剩下的句子仍然通順：
                 「真正卡住的地方是　的　【讀端】」
              ⇒ ★★不是語法錯，是【主詞被刪掉却讀起來像規則的規則】。
                 bash -n 過、人眼掃過去也像句話 ⇒ ★★★只有【回頭找那個識別字】的人會發現。
```
★**機械防線**（規則早就寫在護欄④，而我今天又犯）：
**寫內容一律用 quoted heredoc（`<<'PY' ╱ <<'LETTER'），
不用 python -c "..."、不用雙引號包含反引號的字串。**
★★而驗收不能只看 `bash -n`：它對【被刪掉的識別字】天生盲。
寫完要回去 `grep` 一次【你以為你寫進去的那幾個字】。

**★★★這一族的【邊界】（implementer 指出 2026-09-09）：不是所有壞結論都來自壞儀器。**
有一種形狀是**儀器沒壞、數字是真的**——它只是**回答了另一個問題**。
血證：成對反事實回報 `eff 0 隊／gate 0 隊`，讀成「這個改動不咬人」；
實際那個 0 是真的，但它答的是「**在 1 天窗、糧倉仍為初始 800 的條件下有沒有翻轉**」。
**How to apply:** 看到一個乾淨的 0 先問兩句——①**這個 0 是假的嗎**（儀器沒接電／母體是空的）
②**這個 0 是真的但答了別的問題嗎**（窗長／母體／時刻與我要問的不同）。
★危險在②：去查儀器會發現它**好端端的**，然後你會更相信那個結論。
相關：[[feedback_claim_scope_exceeds_evidence]]、[[feedback_guard_input_population_never_verified]]。

---

**★⑨ 查表失敗【靜默落到一個貌似合理的預設值】**（implementer 自報 2026-09-11）
血證：床裡把 `覓食` 打成 `覚食`（U+8993 vs U+899A）⇒ `priority_for()` 查無
⇒ 落到預設 `PRIO_DISPATCH` **50** ⇒ 印出「覓食＝50」。
★**而 50 完全合理**（它就是預設值）⇒ **只看數字的話這一格會被當成真的**
（真值是 80，而 50/80 正是那張票的全部結論）。
★★抓到它的是**標籤自己看起來不對**（少一個字），**不是數字不對**。
**How to apply:** 任何「名字 → 值」的查表輸出，**要印原始 key**，
讓【查無】在畫面上長得跟【查到】不一樣；預設值路徑要能被看見（印 `<default>`）。

**★★⑩ 陽性對照把【現況】當成「乾淨」基準 ⇒ 世界一旦真的紅，對照必然失準 ⇒ 閘自廢**
血證（systems 2026-09-11）：`.claude/hooks/failure-feedback-coverage.sh:110-113`
`cp "$OPT_DEFAULT" "$d/opt.gd"` ⇒ 「乾淨⇒綠」那一格**用的就是活的檔**。
新 option 真的沒登記 ⇒ 乾淨格變紅 ⇒ `★對照失準` ⇒ `exit 3` **本輪作廢**。
★畫面上的順序是：**先印出真發現，再宣布這一輪不算數。**
★★★而它作廢的樣子長得像【**儀器壞了**】，不像【**世界壞了**】——
而一支把現況當基準的對照**永遠分不出這兩個**（同本檔 ⑧ 的另一面）。
**How to apply:** 對照的「乾淨」那一格必須是**凍結的最小合成 fixture**，
不是 `cp` 活的檔；★而「會紅」那幾格（假樣本⇒具名紅／矛盾⇒紅）可以吃現況，它們不受污染。

**★★★⑪ episode 的【鍵】含會刷新的欄位 ⇒ 分母灌大、成功率稀釋**（2026-09-11/12，一天內兩張表倒在這上面）
床用 `move_target` 當「一趟旅程」的鍵，而**追人型任務的 `move_target` 每 tick 依 intel 刷新**
⇒ **一趟被切成很多段**。血證：信使 **2.2% vs 50%**（同一個世界、兩支床）；
迎戰母體 **921 → 193（灌大 4.8 倍）**，而「沒有目標 36%」的真值是 **6.2%**
—— ★**切段碎片有可預測的特徵：它們長得像「沒有目標」**（切點落在目標更換那一刻）。
**How to apply:** ★任何比率，**同句必須寫出【分母怎麼切的】**；
★★床的檔頭要寫死「一趟」的定義（鍵是什麼、為什麼那樣切）；
★★★而**救命的是 `fp` 逐字相同** —— 沒有它，「數字變 5 倍」會被讀成**世界變了**，
而不是**我們之前數錯了**。

**★同日的正面教訓（★★它跟上面是一對）**：迎戰那張表**被同一種檢驗打過而沒有倒** ——
母體改 4.8 倍、最大格作廢，**排序沒翻還拉開** ⇒ ★★★**穩健性檢驗不是為了推翻，
是為了知道結論有多硬**；三張被尺打倒的表裡，**被真的測試過的那一張站著出來**。

**★★★⑫ `| tail -N` 把【被中斷的長跑】變成【完全沒有輸出】**（systems 2026-09-12，同日第三次被自己的管線咬）
背景長跑寫成 `cmd 2>&1 | tail -10` ⇒ `tail` **要等串流結束才吐** ⇒ 中途被殺（OOM／逾時）
⇒ **輸出檔是空的** ⇒ ★**「跑了多遠」這個唯一有用的資訊沒了**，
而畫面上「0 支跑完」看起來像**它一開始就死**，實際上可能跑了 40 支。
**How to apply:** ★長跑**不要在來源端截斷** —— 讓完整輸出落檔，**要看尾巴自己去 `tail` 那個檔**。
★★同族：任何「只保留最後 N 行」的包裝，在**異常結束**時保留的是 **0 行**，不是最後 N 行。
★★★**而它的真正形狀（implementer 磨利 2026-09-12）**：
**那不是「沒有結果」，是【工具把結果吃掉了】—— 而兩者在畫面上一樣。**
（對照組：他用 `| Out-File` 的同種長跑被 OOM 殺掉兩次，**檔裡都有半份輸出**
 ⇒ ★**可以讀出「跑到第幾天」** ⇒ 同一種中斷，一個有證據一個沒有，差別只在管線。）

**★★★⑬ 判準寫在 docstring，而 code 吐不出那一格 ⇒ 那個結論等於不存在**（implementer 自捕 2026-09-12）

spec 寫三種結論（真趨勢／蝴蝶／**趨勢成立而幅度不可引用**），
而彙整器**只吐得出兩種** ⇒ ★**第三種永遠不會亮**，**而它不會報錯**：表照印、結論照出。
⇒ ★★**「從來沒亮過」與「這次不該亮」在畫面上長得一模一樣。**
⇒ ★★★**病的位置最深**：**它在判決器裡，而判決器正是我們用來判「有沒有病」的東西。**

**How to apply:** ★**每一種結論都要有一組能讓它亮一次的輸入**（**逐結論的陽性對照**），
★★**三組都要留在檔案裡** —— 因為**下一次有人改那支彙整器時，這三組是唯一會攔住他的東西**。

**★★★⑩的第二筆（同一天，2026-09-12）**：彌總器的 `--selftest` **掃到了真的資料夾而不是它自己的 fixture**。
★早上那一筆是 `failure-feedback-coverage` 的「乾淨對照」直接 `cp` 活的檔；
★★**通則**：**自檢的輸入必須是【它自己造的、凍結的】** ——
**只要它會去讀「現在的真實資料」，它就會在世界變化時給出無法解釋的紅或綠。**
★★★而它發生兩次的事實本身就是證據：**這不是粗心，是【寫自檢時最自然的動作】**。

**★★★⑭ 【實跑 0 格】必須是紅 —— 否則「什麼都沒做」與「全部通過」長得一樣**（systems 立 2026-09-12）

**通則**：**任何自檢／閘，輸出必須包含【實跑 N】，而 N ＝ 0 ⇒ 回傳碼非 0。**
★理由：兩者都是**沒有紅字 ＋ 回傳碼 0**。
★★同一天出現三次（**不是同一個 bug，是同一個【形狀】**）：
①`merge-gates` 分批跑照印「註冊表 55 支」⇒ **看起來像跑了全部**（已改印「實跑 N 支」）
②`failure-feedback-coverage` 的對照 ABORT ⇒ **先印出真發現、再宣布本輪不算數**
③彙整器的 `--selftest` ⇒ **印了標題、跑 0 格、回傳碼 0**
⇒ ★★★**工具在「什麼都沒做」的時候，看起來跟「做完了」一樣。**

**★配套（同日，implementer）**：修這類病要**結構解不是紀律解** ——
「**這個檔裡不再有 import 期成形的設定常數 ⇒ 沒有東西可以被 capture**」
★★**關鍵不是「我改對了那三個」，是「沒有東西可以被 capture」：前者要靠記得，後者不用。**
★★★而**會紅的證明**是必須的 —— 沒有它，「我改成呼叫時解析」只是一句宣稱。

**★★★⑪ git 的「差異」視角預設不含未追蹤 —— 而我們的東西有一大半是未追蹤的**（同一天三筆，2026-09-12）

①**296 封信失蹤**：`git commit -F msg -- <paths>` **只收已追蹤檔的修改與刪除**
  ⇒ 一個「搬移」被切成兩半，只有刪除那一半進了 git。
②**樹的身分雜湊**：systems 本來要用 `git diff HEAD | sha256`，★**而我們正在跑的那支床是未追蹤的** ⇒ **雜湊會剛好漏掉被量的東西**
  （implementer 擋下；已改用 `git status --porcelain`，它**本來就列 `??`**）。
③**mailbox-integrity** 把自己的盲點印在每一輪輸出裡，而沒人拿它對帳。
⇒ ★★**前兩個是同一句話** ⇒ ★★★**在這個專案裡，「diff」幾乎永遠是錯的母體**
（床、量測檔、剛寫好的信——全是未追蹤的中間產物）。
**How to apply:** 要「這棵樹現在長什麼樣」一律用 **`git status --porcelain`**（含 `??`），★**不要用 `git diff`**；要「內容一樣嗎」就**逐檔雜湊**。

**★★★⑫人眼看不見的識別字（同一天兩次，2026-09-12）**

①`覚` vs `覓` ⇒ `priority_for` 查無 ⇒ 落到預設 **50** ★**而 50 完全合理**
②U+8DA1 vs U+8D9F ⇒ ★★**在 CP950 主控台是亂碼，所以【看起來都一樣】**（同一個字錯五次）
⇒ ★★★**識別字的差異在【人的檢查管道】上不可見** ⇒ 「以後小心」完全無效
（同族：[[feedback_defect_invisible_to_reading_channel]]）。
**How to apply:** ★**把它變成表上會紅的一格**（機械比對）；
★★而本專案**大量用中文當 key** ⇒ ★★★**中文 key 一律要有一個機械比對它的地方**
（白名單／對帳／具名紅）。

**★配套（同日，implementer）：【自述】vs【可查證】**
「這支工具只讀 log、不參與任何一趡」寫成**自述** ⇒ 下一個人只能選擇相不相信；
寫成**可查證**（工具自報 `sha/clean`）⇒ ★**從【請相信我】變成【你可以查我】**。

**★★★⑬設計文件裡的詞不是 key**（systems 裁 2026-09-12）

血證：`specimen_bed.gd:57` 讀 `w.get("賣貨", 0)`，而 option 註冊表裡
**只有 `貿易`／`囤貨`，沒有 `賣貨`** ⇒ **那一項永遠回 0**，
而那行印的標籤是「winner=貿易/賣貨 次數」。
★**而 `賣貨` 在【文件】裡到處都是**（`game-design.md:259`、`mechanism-intents.md:51`）
⇒ ★★**它不是手滑，是【把設計語言的詞當成實作的 key】**
⇒ ★★★**本專案的設計語言與實作 key 用同一種文字，所以它「看起來像存在」**
（英文專案裡 `sellGoods` 不存在會拼字紅，而我們這裡它**只是安靜地回 0**）。
**How to apply:** ★讀任何註冊表之前，**先對照註冊表本身**；
★★抓到一個「永遠回 0」的統計 ⇒ **下一件事是查它有沒有被引用過**
（★★★因為它壞掉的期間，**任何讀過它的人都拿到了一個假的 0**）。

**★★★為什麼教訓要寫在 code 旁而不是只寫在信裡**（implementer 一句話講完，2026-09-12）

「**下一個看到那行的人不會讀到這封信。**」
⇒ ★**信是給【現在這個對話】的；註解是給【未來那個讀到這一行的人】的。**
★★同日兩個反例（systems 與 implementer 各一）：
・systems 裁「折進帳①那張票」——★**那張票只存在於信裡**（blueprint 的 watchdog 才抓到）
・implementer 把三種結論寫在 **docstring**，而 code 只吐得出兩種 ——★**判準只存在於文字裡**
⇒ ★★★**同一個形狀：東西寫在【不會被執行、也不會被下一個人看到】的地方。**

**★★★⑮「所有值都一樣」是【算術】的簽名，不是【世界】的簽名**（implementer 拆解 2026-09-15）

血證：`loot` 撞頂 155 列 ⇒ 拆開 ＝ **tier1 的 92 列** ＋ tier2 的 63 列。
★**那 92 列的 `est` 逐字全部是 `3.0`** —— 因為 `resource_scale` 是 **0..3 的桶號**，
而除數剛好也是 **3.0** ⇒ **桶號 ÷ 除數 ＝ 1.0** ⇒ ★★**撞頂是【算術必然】，不是「目標很肥」**。
⇒ ★★★**一個真實的世界量不會讓 92 個不同的目標拿到逐字相同的數。**
**How to apply:** 看到**一群值完全相等** ⇒ **先找那個讓它們相等的機制**，
★**不要先解釋世界**。

**★同日的鏡像（systems 犯）**：看到 `est` **中位 0.022** 就推「值都很小」——
真相是**一半的列是結構性 0**（沒有 belief）。
⇒ ★★**兩次都是同一個缺口：沒有問「這個數字為什麼長這樣」。**

**★★★而它還有一個操作後果**（implementer）：**一個症狀底下可以有兩個病**，
而**先修其中一個會把另一個蓋住** ——
「**先壓縮會把單位病蓋在一條漂亮的曲線下**」：壓縮後那 92 列不再都是 1.0，
**而它們仍然是【桶號】**。

## ⑨ **一個【寫不出來】的守衛**——注入式對照撞上編譯期常數（2026-09-15）
提案：「在測試裡往 `BASE_PRICE` 塞一個假鍵，斷言新集合不變」⇒ 形狀完全正確（它把守衛放進
**修法真正改變行為的區間**）。★**而 Godot 4 的 `const Dictionary` 是【編譯期】唯讀**：
```
SCRIPT ERROR: Parse Error: Cannot assign a new value to a constant.
ERROR: Failed to load script ... with error "Parse error".
```
⇒ ★★**整支測試檔載不起來** ⇒ **那不是「那一格紅」，那是【整支閘永久死掉】**
⇒ ★★★**而「閘死掉」與「閘判紅」在 merge-gate 畫面上都是 FAIL** —— 一支永遠 FAIL 的閘，
  幾天後就變成背景噪音，**連帶讓同一張表上真正的紅也開始被略過**（連 [[feedback_no_gate_inflation]]）。
**通則：提出一個守衛時，先問「它【寫得出來】嗎」，再問「它會不會紅」。**
★**替代形狀**：約束若是關於**宣告的形狀**（不准由某表導出），誠實的儀器就是**讀原始碼**——
並且**在同一支測試裡對一段故意違規的字串跑同一個判準、斷言它會紅**（陽性對照，不碰 production）。
★★**同案另一半**：原本那格「斷言集合不含 coin」**今天恆真** ⇒ 違規寫法會**通過**它 ⇒ 零鑑別力
  ⇒ **只驗一個今天恆真的命題＝沒驗**（[[feedback_defect_becomes_control]] 同族）。

## ⑩ **正向失效：漏抓的不是壞東西，是好東西**（2026-09-15，implementer 命名）
defer token 的解除條件我寫成 `grep -A6 'func _belief_richness' … | grep -q 'BASE_PRICE'`。
★**implementer 把定價搬進新函式、原函式變成兩行薄包裝** ⇒ **那六行的視窗裡沒有 BASE_PRICE**
⇒ ★★**修法真的落地了，而偵測器說它沒有** ⇒ **那個鬧鐘永遠不會響。**
★★★**而這一族比假紅危險**：**假紅會吵，正向失效是安靜的** —— **它看起來就像「時候還沒到」。**

**★而最難堪的是我在同一封 commit message 裡寫下了這個病的名字**
（「別用 grep 常數名，實作換個命名會恆假」），**然後換一種方式得了同一個病**
⇒ **知道病名不會讓你免疫；只有把判準錨在【世界的狀態】才會。**

**修法形狀**：★**解除條件不要問「有沒有人做了這件事」，要問「那個病還在不在」** ——
前者會被【談論】滿足、也會被【重構】躲掉；後者只跟世界狀態綁。
★★**而它必須排除註解**（`grep -v '^[[:space:]]*#'`）——
否則「在註解裡解釋自己修了什麼」這個**好習慣**會把鬧鐘關掉。
★★★**成對對照要三格**：病還在（未達成）／病拿掉（達成）／**只剩註解提到它（仍達成）**。

## ⑪ **一支守衛偵測不了【它自己沒有被執行】**（2026-09-16）
implementer 在 PowerShell 裡開 merge-gate ⇒ **找不到 `bash`** ⇒ **runner 從未啟動**，
**而背景任務回報 `exit code 0`** ⇒ ★**「沒跑」長得跟「跑完了、綠」一模一樣。**
★★**而我早就在 runner 裡裝了「實跑 N 支；N==0 ⇒ 紅」** —— **那一顆在這裡沒有用**：
**它住在 runner 裡面，而失敗發生在 runner【被啟動之前】。**
⇒ ★★★**通則：守衛無法覆蓋「它自己沒被叫到」那一格** ——
  **這一族只能靠【外部的、內容性的證據】，不能靠回傳碼。**
**⇒ 判讀規矩**：**一輪閘有沒有跑，看【結尾橫幅】（`註冊表 N 支｜總時 …`），不看 exit code。**
★**同族**：[[reference_godot_detach_requires_path]] ⑦「回傳碼兩方向都騙人」、
  本檔 ⑤「裝好了但沒接電」—— ★★**而這一顆更前面一層：連【裝】都沒發生。**

## ⑫ ★★★**寫在世界聽不懂的名字上**（2026-09-16；blueprint 裁為「裝好沒通電」的新亞型）

```
`FEUD_SEVERITY` 裡躺著 `"extorted": 0.30` —— **有人設計過它。**
而 `write_memory(…, "extorted", …)` 在全庫 **零呼叫點**。
真正的勒索寫的是 `"special_taxed"`（`interaction_system.gd:696`），
**而它不在 `_write_relation_edge`／`_update_relations`／`_trigger_goals` 三個 match 的任何一支**
⇒ 落進 `_` ⇒ **零邊、零標量、零 goal。**
第二例同病：`"rejected_aid"`（求救不應）有三個寫入者，**也不在任何 match 裡**。
```
⇒ ★**症狀＝那個機制【存在、看得到、被設計過】，而它一次也沒有發生過。**
⇒ ★★**而它比「沒接電」更會騙人**：`grep "extorted"` **有東西**（常數表、match 分支）
  ⇒ **看起來已經接好了**；要看見它是死的，必須 grep **那個名字的【呼叫點】**。

**⇒ 判準（不靠記得的那一半）**：
**接線一個 type／reason／key 之前，先 grep 它的【呼叫點】而不是它的【名字】** ——
★**`grep <name>` 回答「有沒有人寫過它」；`grep <emit_fn>(…, "<name>"` 才回答「世界會不會產生它」。**
★★**而「零呼叫點」這個事實，會被【同名的常數表與 match 分支】完整地遮住。**

★★★**衍生**：修法不是「把新機制接到那個名字上」，是**改成世界真的在寫的那個名字**
（本例：把 match 的鍵 `"extorted"` 改成 `"special_taxed"`，**改名不加鍵** ——
留著死名字就是留一個**永遠不會被觸發的分支**，而它下一次又會讓人以為接好了）。

同族：本檔 ⑤「裝好了但沒接電」／[[feedback_spec_premise_verify_decision_layer]]（掛機制前先 grep 目標動作的呼叫點）／
[[feedback_guard_input_population_never_verified]]（守衛的輸入母體從沒被驗證）。

## ★★★⑥的孫形態：**只查一支就下【全稱】**（2026-09-16，同日連三次）
```
我講【機制怎麼算】：「`person = maxf(好戰, 殘忍)`」⇒ 錯（那是攻擊）
更正成「`maxf(好戰, 貪婪)`」⇒ ★**也錯** —— **那仍是攻擊，而我們在談掖奪**
實際：**攻擊 `maxf(好戰, 貪婪)`／掖奪 `maxf(好戰, 殘忍)`** —— **兩支本來就不同軸**
```
★★**共同形狀**：**這個世界有兩個很像的東西，而我每次只查了其中一個。**
★★★**而它是【grep 呼叫點不是 grep 名字】那條規矩的變體**（blueprint 指出）：
**核【全稱】之前，先列舉全部【消費者】** ——
**「這個機制怎麼算」這種句子，它的主詞常常不只一個。**

## ★★★⑨ grep 命中【名字】不命中【意思】——同一天被騙兩次（2026-09-17）

我寫 `defers.tsv` 的機械解除條件（met_check），**同一輪有兩顆判錯**：

| 我的 pattern | 命中了什麼 | 後果 |
|---|---|---|
| `outpost_known` | ★**一行註解**（`# …免建 team_outpost_known 大 store…`） | 判成「病已消失」＝**恆真** |
| `per_tick` | ★**一個無關的 probe 名**（`rootdiff.__CTRL_B_per_tick`） | 同上 |

★**兩次都不是 pattern 寫錯，是【符號文本】與【我要問的那件事】不是同一個東西**
——★★這是 ⑥「寫在世界聽不懂的名字上」的**鏡像**：⑥ 是 grep 到名字**而它沒有呼叫點**；
⑨ 是 grep 到文本**而它根本不是那個東西**。**兩者都讓「有沒有」這個問題得到假答案。**

**How to apply**：
1. ★**met_check 寫完【當場跑一次】並確認極性**（現在應該 NOT-MET ⇒ 若是 MET 就是恆真）。
2. ★★**再跑一次陽性對照**：同形 pattern 對**確實存在**的近親要命中（證明它不是恆假的正則）。
3. ★★★**剝掉註解再判**（`| grep -vE "^[[:space:]]*#"`），並**挑不會撞名的專一 token**
   ——★寧可 pattern 窄到「只有真的做了那件事才會出現」，也不要寬到會被一句話騙。

## ★★★⑩ 陽性對照【自己沒打中】——而它讀起來就是「這裡沒有問題」（2026-09-18）

要證明「某張床的一格靜默死掉時，它會照樣印通過」，implementer 寫了一支注射器故意殺掉一格。
```
第一版：死亡寫在【被呼叫的 helper】裡 ⇒ 執行期錯誤中止的是 helper，★那一格照樣跑完
        ⇒ 床印 sites=72、PASS ⇒ ★★讀起來是「這支床沒有洞」
改對後：死亡發生在【那一格自己的 frame】 ⇒ 同一支床 sites 從 72 變 0 ⇒ 病現形
```
★**「我的工具沒作用」與「這裡沒有問題」長得一模一樣** —— 而**前者的結論是空的、後者是一個實質斷言**。
★★**所以陽性對照有兩層**：①先證明【注射會致死】②才用它的陰性結果下結論。
**跳過①，②的每一個「乾淨」都是無效的。**

## ★⑨的變體：Monitor 完成偵測撞到【逐 tick debug 行】提前誤報「跑完」（measurer 2026-09-22）
```
until grep -qE "母體|GODOT TIMEOUT" log.txt; do sleep 10; done
⇒ 提前在 tick=194/17280(1.1%) 就觸發 —— 因為每個 tick 的除錯行本身印著「母體=tick總時>100000us」
   （字面上有「母體」兩字，但那是完全不同語意的一行，不是床結尾的 [母體] 總結行）
⇒ 我當場報「跑完」，其實行程還在背景繼續跑（行數持續在長）
```
★同 ⑨ 的病：pattern 命中了【文本】不是【我要問的那件事】。★★**Monitor/等待型的版本更危險**：
靜態 grep 誤判只是「判斷錯」，**這裡誤判會導致【提前收工去讀一份還沒寫完的檔案】**，
下游可能直接把未完成的部分數字當成正式結論。
**修法**：完成偵測一律錨【wrapper 層的終止標記】（`exited with code`／`GODOT TIMEOUT`），
不要用床自己印的、可能在別的語境下重複出現的中文詞當 anchor；有結尾總結行時額外用 `^\[` 開頭錨定整行。

★★★**同批的第二半（同樣值得記）**：「暴露面大」不是「有病」的證據 ——
實測結果**跟排序相反**：呼叫數 9 的那支**免疫**（判決讀各格產出、預設是毒值 ⇒ 格死掉自然紅），
呼叫數 4 的憲法閘**有洞**。⇒ **先注射、再決定要不要修；代理值不能當結論。**
★**而免疫要記下【它靠什麼免疫】**：那支的免疫只是一個 `.get(k, -1)` 的毒值預設，
**改成 `0` 免疫當天消失、而且不會紅。**

### ★⑨的鏡像：**你寫的【引用】會被你的閘讀成【使用】**（implementer 2026-09-18，同族第四次）

他把**舊的判決橫幅逐字抄進註解**當說明 ⇒ ★**閘照樣紅**（它 grep 整個檔案）。
```
⑨：grep 命中【名字／文字】，而那不是我要問的【意思】
本則：★我自己寫的【引用】，對 grep 而言與【真的發生了那件事】完全一樣
⇒ ★★同族第四次（前三次：閘自己解釋「什麼情況會 FAIL」的文字被當成真 FAIL／
   註解裡的 outpost_known 讓 met_check 判成已治癒／無關的 probe 名 per_tick）
```
★★★**通則**：**只要判準是【文字比對】，「使用」與「引用」就分不開** ——
⇒ **要嘛把判準移到結構化位置**（runner 2026-09-02 就是這樣改的），
⇒ **要嘛在文件／註解裡【不要逐字抄那個字串】**（改述、或拆開寫）。
★**而這條對我特別適用**：我整天在信與 spec 裡**逐字引用**閘的輸出。


## ★★★⑪【環境失敗被判成測試失敗】—— 兩者都是 rc≠0，而它們要人做的事完全相反（2026-09-22）
```
血證：新機上 Bash 端 spawn 的 powershell 沒有 Process-scope Bypass ⇒ godot.ps1 一次都沒被載入
     ⇒ 整批 65 支裡 40 支「紅」—— ★而畫面上它長得就像 40 個真問題
★★而我第一版的診斷還把 rc 說成 0（那是我自己接了 `| head` 吃掉回傳碼量出來的）
   ⇒ 真值 rc＝1 ⇒ **runner 的第一道 RC 判準沒有瞎**，我對它的指控要撤回
```
★**修法＝給環境紅一個【第三種顏色】**（改既有 runner，不新增閘）：
```
離開碼 0 ＝ 全過｜1 ＝ 判準紅｜★2 ＝ 本輪【不可判】：引擎一次都沒被啟動 ⇒ 綠與紅都不算、不進任何統計
★★判斷順序本身就是防誤判：**先判「真的通過」**（rc＝0 且 expect 命中）
   ⇒ 這樣一支只是【在說明文字裡提到那些字】的閘不會被誤判成環境紅
★★★而「剩下那幾支的綠」也不能充數：**部分樣本的綠不是全部通過**
```
★**兩個方向要分開驗，而且可能在不同輪次**：
陽性＝環境真壞著的那幾支＋合成成對（「提到那些字但自己通過」⇒ 仍是 ✓）；
陰性＝健康環境整批跑完 **ENV＝0**（★我早上明寫「今天驗不了」，而它是被後來兩輪驗掉的）。
⇒ ★★★**「這一輪沒有母體」與「那個分類沒被驗過」是兩句話。**

★★★2026-09-22 ⑧的**另一個方向**：grep 命中名字不命中意思，除了製造**假的綠燈**，也會製造**假的 backlog**：我的【欠點名】判準只認 `到場點名|roll_call|_attendance` 三個名字 ⇒ 數出 6 支；而 5 支本來就有，只是叫 `EXPECT_SECTIONS`（且註冊表 expect 又釘一次 `N/N`＝雙重外部釘死）⇒ 真數 **1**，而那 1 支是我自己當天補註冊的。⇒ ★**兩個方向都要記**：假綠燈讓你不修，假 backlog 讓你去修不存在的東西。⇒ ★★★同批另一條：**「判決印得出來」與「到場的人是全的」是兩件事** ——我做了成對驗證（反轉判準 ⇒ 16 FAIL／原檔 ⇒ ALL PASS）就把床註冊了，而那只證了前者；GDScript 中止粒度＝當前這支 func ⇒ 某支 test 炙掉仍印 ALL PASS。

★★★2026-09-22 ⑩**從【標籤】推內容**：我把檔名 `memo0`／`memo1` 讀成「0＝關、1＝開」⇒ 算出「快取讓 >2s 幀數變多」（結論剛好相反）；真相是檔名是 `MEMO_OFF=` 的值。★沒送出去是因為那份 log 裡的 `gather.* 合計 89.013` 與**卷面上「memo 關＝89.013」對不上**。⇒ ★★**抓到它的是【另一份獨立記錄的同一個數】，不是我小心**。⇒ ★★★通則：**不要從標籤推內容**；標籤與內容是兩條獨立通道，而它們**對不上的時候才會說話** —— 所以讀任何一份量測檔，先拿它裡的一個數去對卷面。

---

★★★**2026-09-22 ⑫【逐行 regex 對跨行呼叫靜默零命中】—— 而它給出的是【相反的結論】**
全庫週期普查第一版：逐行掃 `CadenceStagger.next_tick(`，而庫裡**約一半是換行寫的**
⇒ 那些呼叫點**靜默不命中** ⇒ 普查印出「**生產端沒有 60 的 cadence**」。
修成跨行之後，生產欄 2 筆 → 5 筆，**而 60 那一排就是這樣冒出來的**。
- ★**危險不在漏，在【漏出一個完整、自洽、相反的答案】** —— 它讀起來完全正常。
- ★★同批同族：**普查沒有排除自己** ⇒ 把床裡的合成對照行當成庫裡真的週期印出來
  ⇒ **宣告一個不存在的東西**。⇒ 普查類一律先問：**我的樣本裡有沒有我自己？**
- ★★★**我自己的版本**：我掃 `% TICKS_PER_HOUR == 0` 得到 0，而 code 寫的是 `% NEAR_CADENCE`
  （`NEAR_CADENCE = WorldState.TICKS_PER_HOUR`）⇒ **同一顆常數在那裡叫別的名字**
  ⇒ 我的「掃出 0」被我讀成「不存在」，而它其實是「**我 grep 了名字、沒 grep 意思**」（＝⑥⑨同族第三次）。
  ⇒ **修法：掃【值】與【呼叫點】，別掃你以為它會叫的名字；別名一層就足以讓窮盡宣稱破功。**

★**同族第四次（2026-09-22，進程版）**：判「機器上還有沒有 Godot 在跑」，
我給的謂詞是 `CommandLine -like '*godot*'` ⇒ 同一時刻抓到 **8**（bash.exe ×5、powershell.exe ×1、引擎 ×2），
而正確謂詞 `Name -like 'Godot_v*'` ＝ **2**。
- ★★**呼叫它的那個 shell 命令列裡就有 `godot`** ⇒ **它永遠不會回報 0**
  ⇒ 錯的謂詞在這裡**不是漏抓，是恆真** ⇒ **守衛恆紅 ⇒ 被繞過**（同日第二次踩恆紅守衛）。
- ★通則：**謂詞要落在「那個東西是什麼」，不是「哪裡出現過那個字」。**
- ★★★附帶兩條：**一次跑 ＝ 2 個行程**（console + 本體）⇒ 2 不是兩輪；
  **經 `powershell.exe → bash` 的輸出中文會變 CP950 亂碼** ⇒ 交出去的工具標籤一律 ASCII。
- ★★而「結論對、工具錯」要分開講：**留下來被下一個人用的是工具，不是我的結論。**

★★★**形態⑧（2026-09-22）：陰性對照【用錯的理由過了】**
批次同碼守衛的第一版只比 `codehash`，而字元類把 `clean` 截成 `c`
⇒ 兩份都是 `c` ⇒ **「相同」成立，而理由是假的**。
- ★**抳到它的唯一原因：那支守衛把【比對到的值】印了出來**。只印 YES／NO 就永遠看不到。
- ★★通則：**判決可以因為錯的理由而正確** ⇒ **比對類守衛必須印出兩個操作元**。
- ★★★而它與「守衛要輸出【已處置的結果】」是**對偶，不是矛盾**：
  **行動類守衛輸出結果；比對類守衛輸出【據以判斷的原料】。**
- ★同批另一件：**`codehash=clean` 對任何乾淨樹都一樣** ⇒ 它不是身分；
  身分 ＝ **HEAD sha ＋ dirty 狀態**（sha 分哪一個 commit，dirty 分有沒有未提交改動）。

---

★★★**從索引搬回來的四條**（2026-09-22 systems 清理：它們曾經**只存在於 `MEMORY.md`**）

- **⑦我造的陽性對照是照著我偵測器的形狀造的** ⇒ 自檢只證明偵測器認得自己的假設，
  永遠抓不到它不認得的紅。血證：**131 床紅訊號普查 ＝ 99 支 `[FAIL]` 式／
  ★31 支（24%）【裸 FAIL】式** ⇒ 失敗被判 green 而自檢三向全綠。
  修法 ＝ **真實樣本 ＋ 成對（會紅 ＋ 不會亂紅）**。
- **⑩陽性對照【自己沒打中】，而它讀起來就是「這裡沒有問題」**：
  **注射寫在被呼叫的 helper 裡** ⇒ 中止的是 helper、那一格照樣跑完
  ⇒ **陽性對照有兩層：先證明注射會致死，才用陰性結果下結論**。
- **假窮盡形態③：在【符號形狀】上射箭畫靶** ——
  我先報「**29 支零判準通道**」而真數是 **1**；
  ★而「世界壞了」與「我的儀器壞了」是兩個結論。
- **暴露面大 ≠ 有病**（呼叫數 9 的免疫、4 的有洞）⇒ **先注射再決定要不要修**；
  ★而**免疫要記【靠什麼免疫】** —— 毒值預設改一個字就沒了，而它不會紅。

---

★★★**2026-09-22：陽性對照用【合成語料】 ⇒ 它與真實語料【脫鉤】**（形態⑦的兄弟）

`periodic_schedule_census_bed`：形狀命中 **0** 時 ⇒ 印「相異值 0 個」、點名 6／6、**rc=0**
⇒ **空普查是綠的**。★★而**它的陽性對照照樣過** ——
因為對照測的是**合成行**：
- ★**對照證明的是「偵測器會動」；它【不】證明「偵測器在真實語料上找到了東西」。**
  **兩者是不同的宣稱，而合成對照對第二個宣稱永遠是癃的。**
- ★★**修法：對照盡量用【真實語料】**；若必須用合成的，
  **就必須另外有一個【真實語料母體 > 0】的檢查**（否則 ⇒ 不可判，不是綠）。
- ★★★而它與形態⑦（我造的對照是照著我偵測器的形狀造的）**是同一個病的兩個面**：
  ⑦說「對照只認得我的假設」；這一面說「對照連語料都不是真的」。


- ★★★⑬**靜默重試把【真實阻斷】壓成【一切正常】**（2026-09-22，implementer 自書）：
  他 commit 撞 `index.lock: File exists`，處置是 `for i in 1 2 3` 重試 ⇒ **第三次成功**
  ⇒ **卷面上沒有任何一個字提到「這裡曾經失敗過」**。
  ★★而同一段時間我在另一頭把那 7 分鐘的安靜讀成「他停工了」，差點越界接手他的活。
  ⇒ **製造那段安靜的有兩個來源：鎖，和他的重試。** 兩個都不會留下訊息。
  ★修法（行為，不是新工具）：**重試要留痕** —— 成功了也要印「第 N 次才成功」；
  ★★而在讀的那一端：**「沒有新 commit」的真身可能是【commit 被擋住】，不是【沒有人在做事】**
  ⇒ 懷疑對方停工時，先看他的**工作區**與**鎖**，不要只看 `git log`。
  ★★★這是 memory 裡「`| tail -N` 把失敗壓成看起來像成功」的**人類版**：
  同一個病，一次出現在管線上，一次出現在重試迴圈裡。

- ★★★⑭**`repr()` 分不出【真控制字元】與【跳脫序列】**（2026-09-22，我被自己的驗證騙了一次）：
  我改完 `tr -d '…'` 後用 `repr(b[i:i+14])` 檢查，印出 `b"tr -d '\r\n' | g"` ⇒ 我讀成「已經是跳脫形」。
  ★**而 `repr` 對【真的 0x0D 位元組】印出來的也正是 `\r`** ⇒ 兩種我需要分辨的東西，在它下面長得一模一樣。
  實況（用 `list(bytes)` 才看得到）：`39, 13, 10, 39` ＝ `'` 真CR 真LF `'`。
  ⇒ ★★修法兩條：
    (a) 位元組層手術**用明確的 byte list**（`bytes([116,114,32,45,100,32,39,92,114,92,110,39])`），
        **不要用字串字面量** —— 我的跳脫序列今天在傳遞過程中被吃掉至少兩次
        （shell → heredoc → python 之間，每一層都可能再吃一次）。
    (b) 驗收**用位元組計數**（`b.count(bytes([13])) == 0`）＋**長度差**（每處 +2），**不要用 `repr`**。
  ★★★通則：**驗證工具必須能分辨我要分辨的那兩件事** —— 否則它只會確認我已經相信的那一件。

- ★★★⑮**還原注射時，把修法一起還原掉了**（2026-09-22，implementer 自書）：
  陽性對照的流程是「注射 ⇒ 看它紅 ⇒ **還原** ⇒ 看它綠」。
  而注射與修法**在同一個檔**時，`git checkout -- <file>` 把**兩者一起**還原
  ⇒ ★你接著驗的那棵樹**不含你的修法**，而它**看起來一切正常**（綠、乾淨、可 commit）。
  ⇒ ★★它**不會紅**，只會讓你以為驗過了 —— 而「驗過了」正是你接下來要對別人宣稱的那句話。
  ★修法：還原**只還原注射的那幾行**（`git diff` 當場看一次差異，或注射前先 commit 修法），
  ★★★並在還原後**重新確認修法仍在**（grep 修法的特徵字串），再跑陰性那一臂。
  ★同族：這是「陽性對照自己沒打中」的鏡像 —— 那個是注射沒到達，這個是**修法被撤掉**。

- ★★★⑯**斷言的【名字】比它的【範圍】大**（2026-09-22，implementer 挖出）：
  `assert(... , "fallback 失效")` —— 名字讀起來管**整個** fallback，
  而它只餵了「未知 type ＋ description **非空**」那一支
  ⇒ ★「未知 type ＋ description **為空**」那一格**從來沒被跑過**，
    而**看測試清單的人會以為 fallback 驗過了**。
  ⇒ ★★所以正確的說法**不是**「那一格沒驗過」，**是「驗了相鄰的那一格」**——
    這兩句差在後者**說出了它為什麼看起來像驗過了**。
  ★機械檢查：讀測試清單時，把每個斷言名稱旁邊的**實際餵料**列出來比一次；
    **名字是作者的意圖，餵料才是母體。**
  ★★★同族：這是「grep 命中名字不命中意思」在**測試清單**上的版本 ——
    清單上的那一行是名字，而覆蓋率是餵料決定的。

- ★★★【統一判準】⑩與⑮⑰的共同句（implementer 2026-09-22 改寫，我收）：
  **「紅的那一條，是不是我要驗的那一條？」**
  三種已知形狀，同一句就抓得住：
  ・注射寫在**被呼叫的 helper** 裡 ⇒ 中止的是 helper，被判的那一格照樣跑完（綠）。
  ・注射**打死了前面一格** ⇒ ★在 `assert` 會**中止執行**的語言裡（GDScript），
    後面所有格**根本沒有開始**，而卷面看起來只是「有一格紅」
    ⇒ ★★你會拿這一臂宣稱「後面那格接得住回歸」，**而它從頭到尾沒執行過**。
  ・還原注射時把修法一起還原 ⇒ 陰性那一臂驗的是一棵不含修法的樹。
  ⇒ **看到紅就要問紅的是誰**，而不是「有沒有紅」。★紅的【數量】不是證據，紅的【主詞】才是。

## ★索引回收（2026-09-23）—— 下列子句原本【只存在於索引】

★索引曾經變成儲存層（49.2% 的詞句檔案裡找不到）⇒ 縮索引前先搬進來，避免修剪掉知識。

- ★★★grep 【名字】不命中【意思】（同一顆常數可能叫別的名字）

- ★【索引孤兒續行回收 2026-09-23】原本卡在 MEMORY.md 裡、不屬於任何條目行的殘句：
  就讀成已跳脫,而 repr 對真 0x0D 印的也是 )⇒位元組手術用明確 byte list 不用字串字面量(跳脫在 shell→heredoc→python 每一層都可能再被吃一次),驗收用位元組計數+長度差不用 repr;★通則=驗證工具必須能分辨我要分辨的那兩件事,否則它只會確認我已經相信的那一件；★★★⑮還原注射時把修法一起還原掉(注射與修法同檔 ⇒ git checkout -- 兩者一起還原 ⇒ 接著驗的那棵樹不含修法,而它看起來一切正常:綠、乾淨、可 commit)⇒它不會紅只會讓你以為驗過了;修法=只還原注射那幾行+還原後 grep 修法特徵字串確認它仍在,再跑陰性那臂;★這是「陽性對照自己沒打中」的鏡像(那個是注射沒到達,這個是修法被撤掉)；★★★⑯斷言的【名字】比它的【範圍】大(assert 名叫「fallback 失效」而只餵了 description 非空那一支 ⇒ 空 description 那格從沒跑過,而看清單的人以為 fallback 驗過了)⇒正確說法不是「那格沒驗過」是「驗了相鄰的那一格」(後者說出它為什麼看起來像驗過);機械檢查=把每個斷言名稱旁邊的【實際餵料】列出來比一次,名字是作者意圖、餵料才是母體;★這是「grep 命中名字不命中意思」在測試清單上的版本；★★★【統一判準】「紅的那一條,是不是我要驗的那一條?」——注射打在 helper(被判那格照樣跑完)／注射打死前面一格(assert 中止執行 ⇒ 後面所有格根本沒開始,而卷面只顯示「有一格紅」)／還原注射時把修法一起還原,三種形狀同一句抓得住;★紅的【數量】不是證據,紅的【主詞】才是
  ★成因：我往索引追加子句時，文字裡帶了真換行 ⇒ 按行切之後尾巴被留下來。
  ★★而它看起來像一行正常的文字，只是沒有主詞 ⇒ 下次有人剪索引時會直接把它刪掉。

## ⑱ `>` 在命令【執行之前】就截斷 —— 命令失敗 ≠ 檔案沒事（2026-09-23 血證，我造成的）

```
我下：git show feat/<branch>:<檔> > <檔>       想把同事的信還原成他分支上那一份
真相：那個路徑【不在他分支上】(他的信根本沒 commit，是共用 main dir 的未追蹤檔)
     ⇒ git 回 fatal、什麼都沒輸出
     ⇒ ★而重導向【早就】把目標檔截成 0 位元組了
結果：同事那封信被我毀掉，我是事後 wc -c 才看到 0
```

★機制：shell 先建立重導向（`O_TRUNC`），**才** exec 那個命令。
⇒ 「命令的回傳碼」與「檔案的狀態」**是兩件獨立的事**，而我們習慣只看前者。

★★這是 `| head` 吃掉 `$?` 的**鏡像**：那個是**成功的假象蓋掉失敗**，
這個是**失敗的命令仍然造成破壞**。兩者的共同點 —— **傷害在沒有訊息的那一側**。

★★★機械修法：要用命令的輸出覆蓋一個**已存在**的檔，先寫到暫存再 `mv`：

```
git show REF:path > "$TMP" && mv "$TMP" path     # ★失敗時原檔完好
```

★補：還原之後要**說出來源**。我是從自己讀過的內容貼回去的，不是從 blob 還原的 ——
那兩件事在卷面上長得一樣，而可信度差很多 ⇒ 還原時同句寫「這份的出處是什麼」。

## ⑲ 同一個 bracket 運算式，位元組模式與字元模式差 65 倍（2026-09-23，implementer 撞到）

```
同一棵樹、同一串 56 個簡體字、同一個 pathspec：
  git grep -l  "[$SIMP_CHARS]"  ⇒ 593 檔     ★預設引擎＝位元組集合
  git grep -lP "[$SIMP_CHARS]"  ⇒   9 檔
  grep -rl     "[$SIMP_CHARS]"  ⇒   9 檔     （GNU grep，LANG=*.UTF-8）
```

★機制：UTF-8 的 CJK 字是三位元組，bracket 在位元組模式下被拆成**位元組的集合**，
而那些前導位元組（0xE4–0xE9）幾乎每個中文字都有 ⇒ **任何中文檔案都命中**。
593 不是「有簡體字的檔數」，是「有中文的檔數」。

★★**危害方向是假陽性**（不是假陰性）⇒ 它不會漏掉真的那幾檔，
它會用幾百檔的雜訊**把真的那幾檔淹掉** —— 而雜訊會讓整支守衛開始被忽略。

★★★這種守衛是 **locale-dependent** 的：`merge-gates.sh` 那支 runner `export LC_ALL=C`
⇒ 把這類 lint 掛進註冊表，它會**靜悄悄地**換一個語意。

修法（已落地 `71627619d`）＝**掃之前先驗儀器**：拿一串**只含正體字**的對照串打同一個 pattern，
UTF-8 下不該命中；命中 ⇒ 本輪在位元組模式 ⇒ 印**第三種顏色（不可判）**，不列清單也**不印綠**。
★三格都要實測（UTF-8 rc=1／`LC_ALL=C` rc=0／一個真簡體字 rc=0），否則那一格是恆真項。

---

## ★★★2026-09-23 血證（第五形態的清楚案例：**狀態機有一格從來沒 fire 過，而註解替它編好了理由**）

```
peers.sh 的 lock 有四欄（wpid sid cpid proto=2），而它只 `read -r wpid sid cpid`
⇒ 第四欄黏在 cpid 後面 ⇒ tasklist 查 "24004	proto=2" 永遠查無
⇒ ★`NO-WATCH`（終端開著但 watcher 掉了）這個狀態**從來沒有 fire 過一次**
★★而檔頭註解寫著：「舊格式 lock 沒有 claude_pid ⇒ NO-WATCH 永遠不會 fire，這是保守正確的」
⇒ ★★★那個解釋**是錯的**（格式早就有 cpid 了），而它讓一個真 bug 看起來像【已知限制】
```

★**通則**：當一個守衛的某條分支從沒亮過，「為什麼它不亮」的解釋**必須是量出來的，不是寫出來的** ——
★★一段講得通的理由會讓人停止追問，而**停止追問正是 bug 需要的全部條件**。
★★★抓法：把那一格的**輸入**印出來（這裡就是 `cpid` 的實際字串），不要只讀判準。

---

## ★★★2026-09-23｜第三格（裝好但沒接電）**第一次被量出來**，而它不是一支是一批

```
起因：implementer 發現 value-key 閘的自檢【從 2026-09-09 寫好至今一次都沒在電池裡跑過】
  —— 註冊表跑的是 `bash value-key-gate.sh`（不帶旗標），而自檢只在 `--selfcheck` 分支裡
★而這是一個【可列舉】的問題 ⇒ 不該用「以後注意」⇒ 全庫普查有 --selfcheck 的 8 支：
  接電 3（role-commit-scope／watchdog-beacon／value-key）
  ★接了一半 1（live-team-ratchet 在註冊表但跑不帶旗標 ⇒ 自檢也從沒跑過）
  ★★完全沒有呼叫者 3（fai-new-ratchet／registry-axis-ratchet／worktree-sweep，全庫 0 次被提及）
  人工工具 1（stale-lock-check）
```

**★★★通則：「寫好」與「接上」是兩個各自獨立的動作，而中間沒有任何東西連著它們。**
⇒ 一支沒接電的閘，**它的沉默跟一支【一直綠】的閘在卷面上完全一樣**。

★處置形狀：**一支一支接**，每支接之前先單跑看它現在紅不紅
（★★休眠久了 baseline 會走味 ⇒ **接上當天就紅是資訊不是意外**），每接一支付一次電池時間 ⇒ 不一次全接。
★誠實限：呼叫者是用 grep 查的（`.claude/**`＋`docs/process/**` 的 json/sh/tsv）⇒ 有別處呼叫就是誤報。
追蹤：`defers.tsv: dormant-gates-never-wired`。

---

## ★★★2026-09-23｜兩條關於【工具本身】的，同一天各出現第二次

### ①過寬的稽核工具**不是沉默的，它是喧鬧的**

```
implementer 的第一版遞移閉包稽核用【名字】解析呼叫 ⇒ `reset()`／`emit()` 全庫撞名
⇒ 閉包爆成 **886 支、274 個「寫點」**，裡面全是完全不相干的東西
⇒ 改成「同檔裸呼叫＋具名類別」才收斂到 **11 支、0 個寫點**
```
★★**過寬的過濾器會產出一份【看起來很認真】的報告，而它的結論是噪音。**
★★★而它跟「filter 太寬讓穩定是切掉的」是同一族的兩面：
  **切太多 ⇒ 假綠；收太多 ⇒ 假忙。兩邊都長得像做了很多事。**

### ②**編輯管線會吃掉反斜線** ⇒ 處置是【不要寫需要反斜線的形狀】

```
血證：同一個編輯管線一天吃掉五次反斜線，其中三次產生過真缺陷
  （最後一次僥倖無害：單行三元式＋反斜線續行，續行符沒進檔案）
★處置不是「下次小心」——★★那是【要人記得】的處置，而它今天已經失敗五次
⇒ ★★★改成【形狀層】的規矩：**不要寫需要反斜線的形狀**（續行、正則裡的轉義）
   ⇒ 明式 if/else 取代單行三元式＋續行；正則改用字元類或先組字串
★同一天我自己也踩過同一個管線的另一面：多行 replace 靜默失敗（CRLF 不匹配），
  而【靜默】才是共通點——不是報錯，是**看起來什麼都沒發生**。
```
★★★訂正（2026-09-24 搬檔時發現）：本節的程式塊【原本沒關起來】⇒ 後面每一節都被渲染進它裡面。★而它就是本檔在講的那一種病：**不報錯、讀起來也像正常**。

## ★★★④的新載體（2026-09-24，systems 自犯）：**整塊守衛被插進了 printf 的格式字串裡**

```
我給 merge-gates.sh 加一個 --selfcheck（這支新 carry 的陽性對照），
用 python 按行序插在「_mg_carry() 定義行」的下一行。
★而那支函式的 printf 格式字串裡【嵌了一個真換行】（`printf '%s<CR/LF>' ...`）
⇒ 它在檔案上是【兩行】⇒ 我插到了【字串中間】
⇒ ★★整塊 --selfcheck 成為字串內容：`bash -n` 綠、語法合法、**它是死的**
⇒ ★★★而症狀是【跑了整輪 82 支電池】—— 因為那個提前 exit 沒發生。
```
★**抓到它的不是 `bash -n`（它綠），是【行為不對】**：--selfcheck 應該幾秒就回，而它跑了 120 秒。
★★**通則**：**插行式編輯的【錨】必須是一個【完整的語法單位】**——
而 shell 裡最常見的不完整單位就是**嵌真換行的引號字串**（本 repo 到處都是這種 printf）。
★★★**修法兩層**：①格式字串改用 \t 與 \n 跳脫，不嵌真換行；
②**新加的守衛要有一個【不靠真跑】的驗證動作**：跑完問「它花了多久、印了什麼」，
而不是問「有沒有報錯」—— ★**一個被吃進字串的守衛永遠不會報錯。**

★而它同時是本檔【裝好但沒接電】那一型的極端例：**連【代碼存在】都是假的** ——
grep 得到它、讀起來也對，而它在語法樹上只是一堆字元。
