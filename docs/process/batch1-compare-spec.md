# 批次一 對比輪 量測規格（systems 定 2026-09-06）

★**觸發**：第一批全部落地（④③⑤⑥⑦ merged ＋ **⑧ merged**）後，**同 seed 重跑一輪**對比 90 日基準。
★★**而它是【兩張經濟票的觸發器】**：`money-genesis-start`／`market-thickness-v0` 的 `met_check`
都是「**對比輪的量測檔已產出**」 ⇒ **這一輪不跑，那兩票永遠不會開始。**

## §1 對比對象（★母體紀律）
```
★同 seed／同 config／同窗(90 日)/同 world 路徑 —— ★★而【三個不同的世界不可互比】:
   ①批前基準  ②批後(⑧ 之前)  ③批後(⑧ 之後)
   ⇒ ★⑧ 改的是【每隊每 tick 的執行頻率】⇒ ②③ 之間【所有的率都會變】
   ⇒ ★★★所以【主對比是 ①vs③】,而 ② 只在需要歸因「是哪一刀造成的」時才拿出來
★而 perf 三者都要印(它是⑧ 的已知代價,不是失敗)
```

## §2 批次自身的成效格（★每一格都要印分母）
| # | 格 | 來源票 |
|---|---|---|
| 1 | `try_set` 擋因分布（徵收吃掉幾成引擎路被擋） | ④ |
| 2 | JOIN 的 `true<belief`／徵收「無目標」率 | ③（★**已否證**，本輪只是看它有沒有變） |
| 3 | 所得稅：`salary_named`／`salary_anon` 總額與次數、per-team | ⑤＋⑥ |
| 4 | 薪資執行：`_pay_salary` entry 次數（★**每隊**，眾數應 ∈{3,4}，**且沒有一隊是 0**） | ⑦ |
| 5 | 因發薪的 unrest（`reason="salary"` 的 driver 流量）逐發薪日 | ⑥#4 |
| 6 | 匿名池水位（期末 per-team ＋ 全程最高/中位） | ⑤ |
| 7 | ★per-team 執行次數 vs 距錨點距離（**至少三個系統**，★★含**沒有被本批修過**的） | ⑧ |

## §3 ★★★新增三格（C/D，用戶核可 2026-09-06；★只加量測，不動機制）
```
★C-1【設施升級真的發生幾次】(90 日)
   ★★而【分母】要印:有據點的隊 × 曾經走到「升級評估」那一步的次數
   ⇒ ★★★因為【0 次】有兩種:沒人想升級 vs 那條 path 根本沒被走到
     —— 而這正是今天已經坐實過一次的形狀(升級從來沒被提出,因為它掛在 faction 迴圈上)
★C-2【大團 vs 小隊的人均產出比】
   ★管道已驗:resource_system.gd:131 `fyield = farming_level × FARM_UNIT_YIELD × farm_labor × harvest × day_fraction`
     ⇒ 【level 真的乘進去】⇒ 規模紅利【在物理層存在】
   ⇒ ★★所以這一格問的是【知道→做到通不通】,不是「有沒有這個機制」
   ⇒ ★★★分母:各級距的隊數(★而【某一級距沒有隊】⇒ 那格【不可判】不是 0)
★D【物價 clamp 的命中率】
   ★clamp 在 trade_valuation.gd:162:`sr = clampf(shortage, -0.5, 4.0(活命糧)|1.0(其餘))`
     ⇒ 價格區間 = BASE_PRICE × [0.5, 2.0]，活命糧 × [0.5, 5.0]
   ⇒ ★★量:①撞下界次數 ②撞上界次數 ③未撞次數(★三者互斥且窮盡,加總 = local_value 呼叫次數)
   ⇒ ★★★讀法:命中率【高】= 這個閥在扛事(拆掉會出事)；【低】= 它是裝飾(可拆)
     —— ★而【兩種結論都要能下】,所以三格都要印,不能只印命中率
★★★★可行性(measurer 2026-09-06):C-1／C-2 【零新 tap】(C-1 既有三段分母 faction_ai:5375-5427＋
   `upg.eval_entry`／`infra.stop.1_upgrade`;C-2 直讀 `team.food_produce_avg` per-team EWMA)
   ⇒ ★D 【需要一顆新 tap】,已派 implementer
   ⇒ ★★而【判準形式我改過】:比 clamp【之前】的 shortage,不是比 clamp 之後的 sr
      —— 後者會把【被夾住】與【剛好等於邊界】混在一起,而且要靠 epsilon
★★★★【2026-09-06 實作時抓到的結構事實 —— D 格的解讀文字必須含這一段】
```
★上界【永遠不會被夾到】,而那是【由構造決定】不是【世界觀察】:
   `_stock` = ResourceSystem.effective_holding = team.resources[res] + 自家糧倉 public_storage[res]
   ⇒ ★兩項皆 >= 0 ⇒ shortage = (target − stock)/maxf(target,1.0) 【恆 <= 1.0】
   ⇒ 生存品放大後 = 1.0 + (1.0−0.5)×6.0 = 4.0 ★★【恰好等於上界,永不超過】
   ⇒ 非生存品 shortage <= 1.0 ★★【恰好等於上界,永不超過】
⇒ ★★★所以【上界命中率恆為 0%】
⇒ ★而若不寫這一段,卷面那個 0% 會被讀成「物價從來沒撞到上限」——
   ★★那句話【是對的,但它的意思不是讀者以為的那個意思】
⇒ 判準因此寫成【反向斷言 hi == 0】:它非 0 ＝【前提破了】(stock 變成可負／放大係數改了)
   —— ★★★而那才是真正該紅的時刻
⇒ ★另加一條:【可達的兩桶必須非零】—— 沒有它,「加總對得上」在桶永遠不 fire 時照樣全綠(0+0+N==N)
   ⇒ ★★那個對帳式【對 tap 死掉完全不敏感】
```
```

## §3b ★★★★【0】與【機制不存在】必須分開標（measurer 2026-09-06 抓到）
```
★血證:`collect_member_tax` 整支在 ⑤ 已【移除】(spec §5b 硬禁令,不是副作用)
⇒ ★★所以【③世界】任何 member_tax 相關讀數是【機制不存在】,不是【0】
⇒ ★★★而【前置量測那時函式還在】(b0c4c990 跑的時候 d7ff9835 還沒 merge)
   ⇒ 兩者【性質不同,不能混著讀】
★通則:卷面每一格若是 0,必須標出它是哪一種:
   ①機制在、真的沒發生   ②★機制【已不存在】   ③★★儀器沒開／母體為空(⇒ 判不可判)
⇒ ★★★而【三者在數字上長得一模一樣】—— 這是今天整天的主題,而它在對比輪會【一次出現十格】
```

## §4 通則（★這一輪的全部格子都適用）
```
①★【分母全印】—— 不能只印比率
②★★【母體為空 ⇒ 判「不可判」,不是判紅、也不是判綠】
③★★★【三個世界不可互比】的地方要在卷面上寫出來,不是留給讀者推
④perf 三者都印,而【它是已知代價不是失敗】
```
