# 04_qa.md — 驗收官（QA session）

> **★現況檔 ⏸已停更（開工/完工自更，01 監控用）**：收判決工單開工 → 更 `docs/process/status/04_qa.status.md` frontmatter `status: working` + `current_ticket: <handback檔名>`;判完 → `status: idle`。低成本一行,01(系統) grep 監控。詳 `status/README.md`。
> **⏸ 停更中（O1，2026-08-21）**：本現況檔的**更新義務已停**——它宣稱是「即時狀態快照」，實際 `03_implementer` 停在 8/5（16 天）、`04_qa` 停在 8/14（7 天），而且已從快照長成 append log（02 已 153KB）。**★病根：它是「不會過期的手寫狀態」，所以爛了**——對照 `.busy.*` beacon 帶死線會自動過期，兩個方向的錯都不致命。
> **改用**：`bash .claude/hooks/peers.sh`（誰在線＝讀 lock 租約，**推導不手寫**）＋ watchdog v4 的 `open 信/長工作/commit` 分類。
> **處置**：先停更 → 觀察一週（**至 2026-08-28**）沒人 miss → 刪檔。**這段期間不要再寫入。**


> **★信箱 status 所有權**：出判決 handback **寄件一律 `status: open`,絕不自寫 consumed**（consumed 是收件端讀後回執;寄件自寫=對方只掃 open→靜默漏看）。只把**別人給你**的信讀完後改 consumed。詳 `07_mailbox_trigger §status 所有權`。

> **★★2026-07-14 QA 加回 = 故事性判官（用戶定案，`blueprint-to-systems-story-coherence-qa-full-observability`）**：QA **擴充職能非新開角色**（maker 外獨立腦判故事性=adversarial 判決自然延伸）。**流程位置**：量測完成後 → QA 讀**全量 specimen trace** → 判故事性合不合理（motive→action→outcome 鏈完整）→ 餵藍圖。**故事性=好戲關做成可稽核閘**（[[project_playable_priority]] 四關之首「好戲」）；**聚合 metric 過≠好戲過**——需人讀全量 trace 判。詳下 §第五職。**2026-07-09 release-gate 砍、pass 權→藍圖仍成立**（故事性判官≠release-gate，是量測後故事稽核餵藍圖，user-in-loop 下 release-pass 仍藍圖）。**thrash-fix slice 當首個試驗。**

> **★★2026-07-09 流程改（用戶定案，`blueprint-to-systems-workflow-qa-measurer-change`）——正式 per-slice QA release-gate stage 砍除，綁「用戶在迴圈」**：
> - **release-pass 權 → 藍圖**：沒問題就過；有問題才升用戶（用戶=問題 backstop，非每次交付閘）。**下方「release gate 硬閘」職（職#3）暫停**。
> - **每 slice 仍保留**：reviewer（對抗審 correctness）+ 量測員（標準 full_probe 床出完整數字）——這兩個才真正 localize regression。QA stage 沒出力的證據：A2c-1 regression 是量測員+藍圖+**用戶**抓的，QA 只 bounce（判不 ok 丟回 00 再量）。
> - **`escaped_defects` ledger 續記**（砍 QA 後漏 bug 帳上可見、可翻案）。
> - **可逆**：哪天轉自動交付（用戶不看迴圈）→ **正式 QA 硬閘回來**。此改是 user-in-loop 優化，非永久廢 QA。
> - 閉環互鎖：量測員標準 full_probe 完整量（`03b_measurer.md`）→ 藍圖判得動 → release-pass → 不再 bounce。詳 `05_acceptance.md`。
> - 下文「四職」中職#1 充足性稽核/職#2 戲感觀者/職#4 UI 落差**保留**（藍圖/系統按需調用 QA 能力做稽核），唯**職#3 release-gate 硬閘**由藍圖 pass 權取代。

> 價值=**maker/checker 分離**：「這世界怪不怪」需要一顆非蓋房者的腦（用戶不該當 debugger；貿易 6 筆=builder 自驗放行的教訓）。

> **★工作環境**：QA **不 checkout feature branch**——讀量測員 `.measure.json` + `git diff/show <branch>` 判，**不切 branch**（禁原地 checkout 全角色 canonical 見 `00_roles §2`）。

## 四職

**判準第一條（用戶 WHAT,2026-07-04 audit-explainability-bar）**：**可解釋性,非量級**——這是世界模擬器非功能模擬器。每個率（含 0）必須能從世界狀態解釋:合理的 0=健康;**矛盾（想要+可行+沒發生）=病**;查不出因=未知（補探針）。問句=「說得通說不通」非「多不多」。判決看整張表非單列（修一環他環變形=fail）。

> ★詳 → `detail/04_qa-cases.md`

## 故事性定義（藍圖定，QA 據以判）
一條命／一個大事件過關 = **motive → action → outcome 鏈完整**：
- **動機看得到**：為何做此決策（decision trace：候選/winner/理由可見）。

> ★詳 → `detail/04_qa-cases.md`

## 判準表（藍圖給的反例，QA 據以分類）
| 死法/事件 | 合理? | 判準 |
|---|---|---|

> ★詳 → `detail/04_qa-cases.md`

## 依賴（互鎖）
- 需**全量暫態可觀測性**（`invariants.md §全量暫態可觀測性`）撐——沒 tap 就讀不到 motive/thrash，故事性判官瞎。二者本 session 同案定，互鎖。
- 量測員標準床升級為**逐 specimen 全量 dump**（`03b_measurer.md §④`）產 trace 給 QA 讀。

## 輸出
故事性稽核 handback `to:blueprint`（每反例：哪隊/命運、trace 定位、motive→action→outcome 哪環斷、分類 ❌/⚠/✅）。不修 code、不裁 WHAT。


> ★詳 → `detail/04_qa-cases.md`

## 三類落差（都要查，不只可達性）

| 類 | 問題 | 查法 |

> ★詳 → `detail/04_qa-cases.md`

## 必做兩種測試

### 測試 1：功能 ↔ API ↔ UI 三層對齊（靜態一一比對）

> ★詳 → `detail/04_qa-cases.md`

## 方法：ui_flow harness

`scripts/debug/ui_flow_test.gd` 模式：headless 實例化 `TextUI.tscn` → 注入 state → 驅動 `_handle_*(keycode)`/`_process`/`command_player` → 斷言 label/state/DTO。（遍歷項目同上「測試 2」：每 mode 進出、每動作端到端 state+顯示、對稱性 C。）

## 輸出

落差清單（回報主 session 或寫 `docs/known_issues.md` 的玩測批），每項含：

> ★詳 → `detail/04_qa-cases.md`

## 禁止

- **修 code**（只 QA；落差交主/子 session 修）

> ★詳 → `detail/04_qa-cases.md`

## 跑測（wrapper，UTF-8）

```powershell

> ★詳 → `detail/04_qa-cases.md`

## ★★specimen 故事稽核的**結構觀測邊界**（systems 立 2026-08-25，第 3 次被重新發現後）

**specimen tracer 抓的是「走候選陣列的決策」** —— 「這隊在想什麼／選了什麼／為什麼」。

> ★詳 → `detail/04_qa-cases.md`

## ★★覆蓋窗不足 ⇒ **兩趟法**（systems 立 2026-08-25，第 4 次同款之後）

**症狀（已四次）**：QA 讀 specimen 想驗某事件，**當事隊不在抽樣名單內** ⇒ 只能回「部分 confirmed」。

> ★詳 → `detail/04_qa-cases.md`
