# headless_test 已知失敗 baseline——★從【實跑輸出】生成，不手寫。
# 產生方式：grep "Assertion failed: " <輸出> | sed 's/.*Assertion failed: //' | sort -u
# ★登記於此 ≠ 可以不修：這張表只是讓【新增】失敗看得見，
#   每一條都要被判 stale test（改測試）或真 regression（開票修）。
# ★這張表不涵蓋另外兩類（見 handback）：
#   ①非-assert 的 SCRIPT ERROR 8 行（其中 7 行 own_granary_tile nil）
#   ②`[FAIL]` print 3 行（不經 assert，掃不到）
[p2a] join weight 太低 0.41
FORCE(任rung)→ambient_train_drive 0.5
rung 擴張+武力 未選擴張 intent, got={ "type": "防衛", "target_id": -1, "why": "慎重/威脅驅動，備戰守土" }
戰鬥中(combat_target≠-1) → 197 擋 → 不 resolve
紮營=1.0
